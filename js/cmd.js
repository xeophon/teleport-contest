// cmd.js — Command dispatch and movement.
// C refs: src/cmd.c:rhack(), src/hack.c:domove().

import { game } from './gstate.js';
import { nhgetch } from './input.js';
import { bot, cls, docrt, flush_screen, newsym, pline, recordObservedObjectDiscovery, refreshHallucinatedMap, seeMonsters, seeNearbyObjects, show_glyph_cell, strengthString } from './display.js';
import { cansee, couldsee, vision_recalc, vision_reset } from './vision.js';
import { RANDOM_MONSTER_BY_NAME, SHOP_TYPES, STALKER_MONSTERS, add_to_container, add_to_minv, adjustedMonsterLevel, artifactDefinitionForName, artifactObjectName, enextoMonsterSpot, make_tutorial1_level, makemon, makeArtifactWishObject, maketrap, mkcorpstat, mklev, mkobj, mkobj_at, mksobj, monsterByRndName, monster_hp, morgueMonster, nameObjectAsArtifact, next_ident, object_display, potionIndexForRoll, randomHallucinatedShopkeeperName, resurrectWizardOfYendor, rndmonnum, scrollIndexForRoll, set_mimic_sym_rng, syncDungeonContext, u_on_dnstairs, u_on_rndspot, u_on_upstairs, wipe_engr_at, dropMonsterInventory as dropMonsterInventoryRaw, l_nhcore_init, getrumor, getbogusmon, level_difficulty, set_malign, somexyspace, fumaroles, fix_wall_spines, createMonsterCorpseOrGlob, monsterCorpseDropSucceeds, monsterLeavesCorpseLikeDrop, movebubbles, noteleportLevelForMonster, rlocNoMsg } from './mklev.js';
import { ACCESSIBLE, A_CHA, A_CHAOTIC, A_CON, A_DEX, A_INT, A_LAWFUL, A_MAX, A_NEUTRAL, A_NONE, A_STR, A_WIS, ALTAR, AM_SANCTUM, AM_SHRINE, Align2amask, Amask2align, BC_BALL, BC_CHAIN, BEAR_TRAP, BLCORNER, BOLT_LIM, BRCORNER, CANDLESHOP, CLOUD, COLNO, CORPSTAT_FEMALE, CORPSTAT_GENDER, CORPSTAT_HISTORIC, CORPSTAT_MALE, CORPSTAT_NEUTER, CORR, DB_DIR, DB_EAST, DB_FLOOR, DB_ICE, DB_LAVA, DB_MOAT, DB_NORTH, DB_SOUTH, DB_UNDER, DB_WEST, DBWALL, DOOR, DRAWBRIDGE_DOWN, DRAWBRIDGE_UP, BURN, D_BROKEN, D_CLOSED, D_ISOPEN, D_LOCKED, D_NODOOR, D_TRAPPED, DUST, ENGRAVE, ENGR_BLOOD, FOUNTAIN, F_LOOTED, GRAVE, HEADSTONE, HWALL, ICE, ICED_MOAT, ICED_POOL, IN_SIGHT, IRONBARS, IS_AIR, IS_FURNITURE, IS_LAVA, IS_OBSTRUCTED, IS_POOL, IS_ROOM, IS_SOFT, IS_TREE, IS_WALL, In_endgame, In_quest, In_sokoban, In_V_tower, Is_airlevel, Is_astralevel, Is_botlevel, Is_earthlevel, Is_firelevel, Is_rogue_level, Is_stronghold, Is_waterlevel, LADDER, LAVAPOOL, LAVAWALL, LUCKMAX, LUCKMIN, MAGIC_PORTAL, MARK, MAX_EGG_HATCH_TIME, MIGR_LADDER_UP, MIGR_RANDOM, MIGR_SSTAIRS, MIGR_STAIRS_UP, MM_ADJACENTOK, MM_EDOG, MM_NOCOUNTBIRTH, MM_NOMSG, MM_NOTAIL, MM_NOWAIT, MOAT, MORGUE, M_AP_FURNITURE, M_AP_OBJECT, M_AP_TYPE, N_DIRS, NO_MINVENT, NORMAL_SPEED, OVERLOADED, P_BASIC, P_DAGGER, P_EXPERT, P_KNIFE, P_SKILLED, P_UNSKILLED, PIT, POOL, REPAIR_DELAY, ROLLING_BOULDER_TRAP, ROOM, ROOMOFFSET, ROWNO, SCORR, SDOOR, SHARED, SHARED_PLUS, SHOPBASE, SHOP_DOOR_COST, SINK, SPIKED_PIT, STAIRS, STONE, STR18, STRAT_APPEARMSG, STRAT_WAITFORU, STRAT_WAITMASK, S_LDWASHER, S_LPUDDING, S_LRING, TDWALL, TEMPLE, THRONE, TLCORNER, TRCORNER, TREE, TREE_LOOTED, TREE_SWARM, TT_BEARTRAP, TT_BURIEDBALL, TT_INFLOOR, TT_LAVA, TT_PIT, TT_WEB, TUWALL, T_LOOTED, VAULT, VIBRATING_SQUARE, VWALL, WAND_BACKFIRE_CHANCE, WATER, WEB, WM_MASK, WT_IRON_BALL_BASE, WT_IRON_BALL_INCR, WT_TO_DMG, W_NONDIGGABLE, W_SADDLE, ZAP_POS, isok, xdir, ydir } from './const.js';
import { d, rn1, rn2, rn2_on_display_rng, rnd, rnl, rnz } from './rng.js';
import { CLR_BLACK, CLR_BLUE, CLR_BRIGHT_BLUE, CLR_BRIGHT_CYAN, CLR_BRIGHT_GREEN, CLR_BRIGHT_MAGENTA, CLR_BROWN, CLR_CYAN, CLR_GRAY, CLR_GREEN, CLR_MAGENTA, CLR_ORANGE, CLR_RED, CLR_YELLOW, CLR_WHITE, NO_COLOR } from './terminal.js';
import { vfsDeleteFile, vfsReadFile, vfsWriteFile } from './storage.js';
import { encodeBonesLevel, encodeSaveState } from './save.js';
import { depth as depth_of_level, distmin } from './hacklib.js';
import { DISPLAY_MONSTER_COLORS, DISPLAY_MONSTER_GLYPHS, DISPLAY_MONSTER_HALLU_NAMES, DISPLAY_OBJECT_GLYPHS, FIRST_DISPLAY_OBJECT, RNDMONST_COMMON_MONSTERS } from './monster_data.js';
import { prepareVaultGuardEscort } from './vault.js';
import { clearMonsterTrack, updateMonsterTrack } from './montrack.js';
import { datFileLines as bundledDatFileLines } from './dat_files.js';
import { TRIBUTE_DEATH_QUOTES, TRIBUTE_NOVEL_TITLES } from './tribute.js';
import { advanceFireBreathRay, applyFireRayFountainTerrain, applyFireRayIceTerrain, applyFireRayWaterTerrain, burnFireRayWebTrap, finishHeroTargetedBreath, fireBreathDamageHero, fireBreathDamageMonster, fireBreathZapHits } from './fire_breath.js';
import { dryupFountainAt, dryupFountainResultAt } from './fountain.js';
import {
    applyColdRayTerrain, buriedBallToFreedom, buriedBallToPunishment,
    clearCorpseTimeout, corpseName, freezeObjectInIcebox,
    buryObjectsAt, findBuriedBallNear, isBuriedBallTrapActive, objectIceEffect, objIceEffectsAt,
    removedFromIcebox, riderRevivalDelay, startCorpseTimeout, startMeltIceTimeout, unearthObjectsAt, zombieFormNameForCorpse,
} from './ice.js';
import { createGasCloud } from './region.js';
import { figurineLocationCheck, isFigurineObject, makeFigurineFamiliar, maybeAttachCarriedFigurineTimeout, stopFigurineTransformTimeout, syncCarriedFigurineTransformTimer } from './figurine.js';
import { applyMonsterLiquidEffectsAt } from './monster_liquid.js';
import { applySlimeMoldFruitFields, currentFruitId, currentFruitJuiceName, currentFruitName, fruitWishMatch, setCurrentFruitName, slimeMoldNameForObject } from './fruit.js';
import { eggHasHatchTimer, eggSpeciesGenocidedForHatching, killDeadSpeciesEggHatchTimers, killEggHatchTimer } from './egg_timers.js';
import { METALLIC_MATERIALS, metallivoreObjectAlwaysResists, objectIsAmuletLike, objectIsRingLike, objectIsSlowDigestionRing, objectMaterialForMetallivore } from './metallivore.js';

const DIR_DX = { h: -1, l: 1, j: 0, k: 0, y: -1, u: 1, b: -1, n: 1 };
const DIR_DY = { h: 0, l: 0, j: 1, k: -1, y: -1, u: -1, b: 1, n: 1 };
const C_RANDOM_CORPSE = 265;
const VANQUISHED_MONSTER_DATA = new Map(
    RNDMONST_COMMON_MONSTERS.map(([name, , mlevel], index) => [name, { mlevel, index }]),
);
const XP_SPECIAL_ATTACK_TYPES = new Set(['tuch', 'stng', 'hugs', 'spit', 'engl', 'brea', 'expl', 'boom', 'gaze', 'tent']);
const XP_DOUBLE_DAMAGE_TYPES = new Set(['magm', 'fire', 'cold', 'slee', 'disn', 'elec', 'drst', 'acid', 'spc1', 'spc2']);
const XP_FLAT_DAMAGE_TYPES = new Set(['drli', 'ston', 'slim']);
const DEATH_CORPSE_BY_RACE = {
    human: { name: 'human', neuter: false },
    elf: { name: 'elf', neuter: false },
    dwarf: { name: 'dwarf', neuter: false },
    gnome: { name: 'gnome', neuter: false },
    orc: { name: 'orc', neuter: false },
};
const CORPSE_WEIGHTS = new Map([
    ['jackal', 300], ['coyote', 300], ['kobold', 400], ['lichen', 20],
    ['lizard', 10], ['newt', 10], ['chickatrice', 10], ['cockatrice', 30],
    ['bugbear', 1250],
]);
const CORPSE_NUTRITION = new Map([
    ['lichen', 200], ['jackal', 250], ['coyote', 250], ['bugbear', 250],
]);
const POISONOUS_CORPSES = new Set(['kobold', 'large kobold', 'kobold leader', 'kobold shaman']);
function exerciseAttribute(attr, increase) {
    const u = game.u;
    if (!u || attr === A_INT || attr === A_CHA) return;
    if (u._polyself_base && attr !== A_WIS) return;
    u._aexe ??= Array(A_MAX).fill(0);
    if (Math.abs(u._aexe[attr] || 0) >= 50) return;
    const current = u.acurr?.a?.[attr] ?? 10;
    if (increase) u._aexe[attr] += rn2(19) > current ? 1 : 0;
    else u._aexe[attr] -= rn2(2);
}

function polyselfForm() {
    return game.u?._polyself_form || null;
}

function normalizedAttackCode(value) {
    return String(value || '').toLowerCase().replace(/^(?:ad|at)_/, '');
}

function heroPolyselfDigestAttack() {
    const form = polyselfForm();
    if (!form) return false;
    const attacks = [];
    if (form.attack) attacks.push(form.attack);
    if (Array.isArray(form.attacks)) attacks.push(...form.attacks);
    if (attacks.some(attack =>
        normalizedAttackCode(attack?.aatyp) === 'engl'
        && normalizedAttackCode(attack?.adtyp) === 'dgst'))
        return true;
    return String(form.name || '').toLowerCase() === 'purple worm';
}

function addPolyselfDietOverlay(map, names, diet) {
    for (const name of names) map.set(String(name).toLowerCase(), diet);
}

const POLYSELF_DIET_OVERLAY = new Map();
addPolyselfDietOverlay(POLYSELF_DIET_OVERLAY, [
    'little dog', 'dog', 'large dog',
    'kitten', 'housecat', 'large cat',
    'wolf', 'winter wolf cub', 'winter wolf', 'warg',
    'hell hound pup', 'hell hound',
    'baby gray dragon', 'baby gold dragon', 'baby silver dragon',
    'baby shimmering dragon', 'baby red dragon', 'baby white dragon',
    'baby orange dragon', 'baby black dragon', 'baby blue dragon',
    'baby green dragon', 'baby yellow dragon',
    'gray dragon', 'gold dragon', 'silver dragon', 'shimmering dragon',
    'red dragon', 'white dragon', 'orange dragon', 'black dragon',
    'blue dragon', 'green dragon', 'yellow dragon',
    'carnivorous ape',
    'piranha', 'shark', 'giant eel', 'electric eel', 'kraken',
    'wererat', 'werejackal', 'werewolf',
], { carnivorous: true, humanoid: false });
addPolyselfDietOverlay(POLYSELF_DIET_OVERLAY, [
    'dwarf', 'dwarf lord', 'dwarf king',
    'goblin', 'hobgoblin', 'hill orc', 'Mordor orc', 'Uruk-hai', 'orc shaman', 'orc-captain',
], { carnivorous: true, herbivorous: true, humanoid: true });
addPolyselfDietOverlay(POLYSELF_DIET_OVERLAY, [
    'pony', 'horse', 'warhorse',
], { herbivorous: true, humanoid: false });
addPolyselfDietOverlay(POLYSELF_DIET_OVERLAY, [
    'rock mole', 'rust monster', 'xorn',
], { metallivorous: true, humanoid: false });

function polyselfFormWithDiet() {
    const form = polyselfForm();
    if (!form) return null;
    const name = String(form.name || '').toLowerCase();
    const overlay = POLYSELF_DIET_OVERLAY.get(name);
    if (!overlay) return form;
    const result = { ...overlay, ...form };
    if (!Object.prototype.hasOwnProperty.call(result, 'carnivorous')
        && Object.prototype.hasOwnProperty.call(form, 'carnivore'))
        result.carnivorous = form.carnivore;
    return result;
}

function polyselfNoHands() {
    return !!polyselfForm()?.nohands;
}

function adjustedMonsterResumeIndexForRemoval(mon, resumeIndex) {
    if (!resumeIndex || !mon || !game.level?.monsters) return resumeIndex;
    const reverseIndex = [...game.level.monsters].reverse().indexOf(mon);
    if (reverseIndex >= 0 && reverseIndex < resumeIndex)
        return resumeIndex - 1;
    return resumeIndex;
}

function monsterHasOlfaction(data = {}) {
    const name = String(data.name || '').toLowerCase();
    const mlet = data.mlet ?? data.glyph ?? '';
    if (name.endsWith(' golem')) return false;
    if (['e', 'j', 'P', 'b', 'v', 'E', 'F', 'y'].includes(mlet)) return false;
    if (['eye', 'jelly', 'pudding', 'blob', 'vortex', 'elemental', 'fungus', 'light'].includes(mlet)) return false;
    if (/sphere$/.test(name)) return false;
    return true;
}

function scareNearbyOlfactoryMonstersWithGarlic() {
    const form = polyselfForm();
    if (form?.undead) return;
    const ux = game.u?.ux ?? 0;
    const uy = game.u?.uy ?? 0;
    for (const mon of game.level?.monsters || []) {
        const dx = (mon.mx ?? 0) - ux;
        const dy = (mon.my ?? 0) - uy;
        if (dx * dx + dy * dy >= 7 || !monsterHasOlfaction(mon.data)) continue;
        mon.mflee = 1;
        mon.mfleetim = 0;
        clearMonsterTrack(mon);
    }
}

function isWandItem(item) {
    return item?.cls === 'wand' || item?.glyph === '/' || item?.wandIndex != null
        || item?.kind === 'wand of sleep' || item?.kind === 'sleep';
}
const WISHING_WAND_INDEX = 4;
const WAND_WREST_CHANCE = 121;

function isWishingWand(item) {
    const wand = String(item?.wand || '').toLowerCase();
    const kind = String(item?.kind || '').toLowerCase();
    const actualKind = String(item?.actualKind || '').toLowerCase();
    return item?.wandIndex === WISHING_WAND_INDEX
        || wand === 'wishing'
        || kind === 'wishing'
        || kind === 'wand of wishing'
        || actualKind === 'wishing'
        || actualKind === 'wand of wishing';
}

function wandCharges(item, fallback = 0) {
    return item?.spe ?? item?.charges ?? fallback;
}

function setWandCharges(item, charges) {
    item.spe = charges;
    item.charges = charges;
}

function refreshWandLine(item) {
    if (/\(\d+:-?\d+\)/.test(String(item.line || ''))) item.chargeKnown = true;
    item.line = normalInventoryLine({ ...item, line: '' });
}

function setKnownWandLine(item, wand) {
    item.cls = 'wand';
    item.glyph = '/';
    item.wand = wand;
    item.kind = wand;
    item.wandIndex = WAND_NAME_TO_INDEX.get(wand);
    item.known = true;
    refreshWandLine(item);
}

function zappableWand(item, { fallbackCharges = 0 } = {}) {
    const charges = wandCharges(item, fallbackCharges);
    if (charges < 0) return { zapped: false, dust: true };
    if (charges === 0) {
        if (rn2(WAND_WREST_CHANCE)) return { zapped: false };
        setWandCharges(item, -1);
        return { zapped: true, wrested: true };
    }
    setWandCharges(item, charges - 1);
    return { zapped: true };
}

function spendZapWandCharge(item, messages) {
    const charges = wandCharges(item, 8);
    checkUnpaidUsage(item, messages, { chargeCount: charges });
    return zappableWand(item, { fallbackCharges: 8 });
}

function spendEngraveWandCharge(item, messages) {
    const chargeUse = zappableWand(item);
    if (!chargeUse.zapped) return chargeUse;
    checkUnpaidUsage(item, messages, { chargeCount: wandCharges(item) });
    refreshWandLine(item);
    return chargeUse;
}

function spendChargedToolUse(item, messages) {
    const charges = chargedUsageChargeCount(item);
    if (charges <= 0) return false;
    checkUnpaidUsage(item, messages, { chargeCount: charges });
    item.spe = Math.max(0, charges - 1);
    if ('charges' in item) item.charges = item.spe;
    updateChargedItemLine(item);
    return true;
}

function heroHasSlipperyFingers() {
    return !!(game.u?._glibTimeout || game.u?.glib || (game.u?._statusSuffix || '').includes('Slippery'));
}

function heroIsFumbling() {
    return !!(game.u?.fumbling || game.u?._fumblingTimeout || (game.u?._statusSuffix || '').includes('Fumbling')
        || (game.inventory || []).some(item => isWornArmorItem(item)
            && String(item.kind || item.actualKind || '').toLowerCase() === 'fumble boots'));
}

function fingersOrGloves() {
    return wornGlovesItem() ? 'gloves' : 'fingers';
}

function wornRingItem(item) {
    return !!item && (item.cls === 'ring' || item.glyph === '=' || item.otyp === RING_CLASS)
        && (item.worn || /\(on (?:left|right) hand\)/.test(String(item.line || '')));
}

function wornArmorInSlot(slot) {
    return (game.inventory || []).find(item => isWornArmorItem(item) && armorSlot(item) === slot) || null;
}

function greaseInaccessibleBlockers(item) {
    if (!item) return [];
    if (isWornArmorItem(item)) {
        const slot = armorSlot(item);
        if (slot === 'body') {
            const cloak = wornArmorInSlot('cloak');
            return cloak && cloak !== item ? [cloak] : [];
        }
        if (slot === 'shirt') {
            return [wornArmorInSlot('cloak'), wornArmorInSlot('body')]
                .filter(blocker => blocker && blocker !== item);
        }
    }
    if (wornRingItem(item)) {
        const gloves = wornGlovesItem();
        return gloves ? [gloves] : [];
    }
    return [];
}

function ownedEquipmentName(item) {
    return `your ${inventoryItemName(item).replace(/^(?:an?|the) /i, '')}`;
}

function ownedEquipmentListName(items) {
    const names = items.map(item => inventoryItemName(item).replace(/^(?:an?|the) /i, ''));
    return `your ${names.join(' and ')}`;
}

function canGreaseObject(item) {
    return !!item && item.cls !== 'coin' && item.letter !== '$'
        && !greaseInaccessibleBlockers(item).length;
}

function canSelectGreaseObject(item) {
    return !!item && item.cls !== 'coin' && item.letter !== '$';
}

function isSqueakyBoardGreaseTool(item) {
    return !!item && item.cls === 'tool' && toolChargeKind(item) === 'can of grease';
}

function isKnownSqueakyBoardOil(item) {
    if (!isPotionOfOil(item)) return false;
    const kind = objectKindKey(item).replace(/^potion of /, '');
    return item.known === true || kind === 'oil'
        || (game._discoveries || []).some(entry =>
            entry.section === 'Potions' && entry.name === 'potion of oil' && entry.known !== false);
}

function isSqueakyBoardSuggestedUntrapTool(item) {
    return isSqueakyBoardGreaseTool(item) || isKnownSqueakyBoardOil(item);
}

function isSqueakyBoardUntrapTool(item) {
    return isSqueakyBoardSuggestedUntrapTool(item) || isPotionObject(item);
}

function isBadSqueakyBoardUntrapTool(item) {
    if (!item || item.cursed) return true;
    if (isPotionOfOil(item)) return !!(item.lamplit || item.burning);
    if (isSqueakyBoardGreaseTool(item)) return (item.spe ?? 0) <= 0;
    return true;
}

function useSqueakyBoardUntrapTool(item, messages) {
    if (isSqueakyBoardGreaseTool(item))
        return spendChargedToolUse(item, messages);
    if (isPotionOfOil(item)) {
        useUpInventoryItem(item, 1);
        identifyPotionOfOil(item);
        return true;
    }
    return false;
}

function squeakyBoardUntrapChance(trap) {
    let chance = 3;
    if (heroIsConfused() || heroIsHallucinating()) chance++;
    if (game.u?.blind || game.u?.Blind) chance++;
    if (heroIsStunned()) chance += 2;
    if (heroIsFumbling()) chance *= 2;
    if (trap?.madeby_u) chance--;
    return Math.max(1, chance);
}

function squeakyBoardUntrapPrompt() {
    const letters = inventoryLetters(isSqueakyBoardSuggestedUntrapTool);
    return letters
        ? `What do you want to untrap with? [${getobjPromptLetters(letters)} or ?*]`
        : 'What do you want to untrap with? [*]';
}

function greaseTargetPrompt() {
    const letters = inventoryLetters(canGreaseObject);
    const display = letters ? `- ${getobjPromptLetters(letters)}` : '-';
    return `What do you want to grease? [${display} or ?*]`;
}

function greaseInaccessibleMessage(item) {
    const blockers = greaseInaccessibleBlockers(item);
    if (!blockers.length) return '';
    return `You need to take off ${ownedEquipmentListName(blockers)} to grease ${ownedEquipmentName(item)}.`;
}

function addHeroGlibTimeout(duration) {
    if (!game.u) return;
    game.u._glibTimeout = (game.u._glibTimeout || 0) + duration;
}

function carriedDropDisplayColor(item) {
    if (!item) return NO_COLOR;
    if (item.color != null) return item.color;
    if (item.cls === 'armor') return armorDisplayColor(item);
    if (item.cls === 'scroll' || item.cls === 'spellbook') return CLR_WHITE;
    return NO_COLOR;
}

function dropCarriedObjectAtHero(item, messages = [], dropTarget = null) {
    const dropX = dropTarget?.x ?? game.u?.ux ?? 0;
    const dropY = dropTarget?.y ?? game.u?.uy ?? 0;
    const dropped = {
        ...item,
        invlet: item.invlet ?? item.letter,
        letter: undefined,
        line: undefined,
        wielded: false,
        worn: false,
        quivered: false,
        ox: dropX,
        oy: dropY,
        glyph: item.cls === 'weapon' ? ')' : item.cls === 'armor' ? '[' : item.glyph || (item.cls === 'wand' ? '/'
            : item.cls === 'ring' ? '=' : item.cls === 'potion' ? '!' : item.cls === 'scroll' ? '?'
                : item.cls === 'spellbook' ? '+' : '('),
        color: carriedDropDisplayColor(item),
    };
    curseLoadstoneLeavingInventory(dropped);
    if (Array.isArray(dropped.contents)) dropped.contents = [...dropped.contents];
    if (Array.isArray(dropped.cobj)) dropped.cobj = [...dropped.cobj];
    normalizeContainedObjectParents(dropped);
    removeInventoryItem(item, item.quan || 1);
    const shipObject = maybeShipCarriedDropObject(dropped, dropped.ox, dropped.oy, messages);
    const placed = shipObject.handled
        ? false
        : placeObjectOnFloorWithEffects(dropped, dropped.ox, dropped.oy, messages, 'drop', {
            usedUpShopBillOnDestroy: true,
        });
    if (!placed) clearObjectShopBillState(item);
    let shopSale = null;
    if (placed && !sellobjReturnUnpaidToShop(dropped, dropped.ox, dropped.oy)) {
        shopSale = beginDroppedPaidObjectSale(dropped, dropped.ox, dropped.oy);
        if (shopSale?.message) messages.push(shopSale.message);
    }
    if (placed && !shopSale?.prompt) stackDroppedFloorObject(dropped);
    return dropped;
}

function wandZapSpendMessages(usageMessages, chargeUse) {
    const messages = [...usageMessages];
    if (chargeUse?.wrested) messages.push('You wrest one last charge from the worn-out wand.');
    return messages;
}

function dustWand(item) {
    const name = pickupObjectName(item);
    removeInventoryItem(item);
    return `The ${name} turns to dust.`;
}

function useUpDustWand(item) {
    const name = pickupObjectName(item);
    useUpInventoryItem(item, item?.quan || 1);
    return `The ${name} turns to dust.`;
}

async function beginWishPrompt({ moveCost = 0, dustItem = null, message = 'For what do you wish?' } = {}) {
    game._wish_text = '';
    game._wish_tries = 0;
    game._wish_move_cost = moveCost;
    game._wish_dust_item = dustItem;
    await setMessage(message);
    game._command_mode = 'wizardWish';
}

async function setWishResultMessage(message, more = false) {
    const moveCost = game._wish_move_cost || 0;
    const dustItem = game._wish_dust_item || null;
    game._wish_move_cost = 0;
    game._wish_dust_item = null;
    game._wish_tries = 0;
    game.context.move = moveCost;
    let text = message;
    if (dustItem && (game.inventory || []).includes(dustItem)) {
        const dustMessage = dustWand(dustItem);
        text = text ? `${text}  ${dustMessage}` : dustMessage;
    }
    await setMessage(text, more);
}
const GROWNUP_MONSTERS = new Map([
    ['chickatrice', 'cockatrice'],
    ['little dog', 'dog'],
    ['dog', 'large dog'],
    ['hell hound pup', 'hell hound'],
    ['winter wolf cub', 'winter wolf'],
    ['kitten', 'housecat'],
    ['housecat', 'large cat'],
    ['pony', 'horse'],
    ['horse', 'warhorse'],
    ['kobold', 'large kobold'],
    ['large kobold', 'kobold leader'],
    ['gnome', 'gnome leader'],
    ['gnome leader', 'gnome ruler'],
    ['dwarf', 'dwarf leader'],
    ['dwarf leader', 'dwarf ruler'],
    ['mind flayer', 'master mind flayer'],
    ['goblin', 'orc-captain'],
    ['hill orc', 'orc-captain'],
    ['Mordor orc', 'orc-captain'],
    ['Uruk-hai', 'orc-captain'],
    ['sewer rat', 'giant rat'],
    ['cave spider', 'giant spider'],
    ['ogre', 'ogre leader'],
    ['ogre leader', 'ogre tyrant'],
    ['elf', 'elf noble'],
    ['Woodland-elf', 'elf noble'],
    ['Green-elf', 'elf noble'],
    ['Grey-elf', 'elf noble'],
    ['elf noble', 'elven monarch'],
    ['lich', 'demilich'],
    ['demilich', 'master lich'],
    ['master lich', 'arch-lich'],
    ['vampire', 'vampire leader'],
    ['bat', 'giant bat'],
]);
const SWALLOW_OVERLAY_CELLS = [
    [5, 25, '/'], [5, 26, '⎺'], [5, 27, '\\'],
    [6, 25, '│'], [6, 26, '@'], [6, 27, '│'],
    [7, 25, '\\'], [7, 26, '⎽'], [7, 27, '/'],
];
const SPECIAL_PM = 330;
const BOGUSMONSIZE = 100;
const HALLUCINATED_LIQUIDS = [
    'yoghurt', 'oobleck', 'clotted blood', 'diluted water', 'purified water',
    'instant coffee', 'tea', 'herbal infusion', 'liquid rainbow',
    'creamy foam', 'mulled wine', 'bouillon', 'nectar', 'grog', 'flubber',
    'ketchup', 'slow light', 'oil', 'vinaigrette', 'liquid crystal', 'honey',
    'caramel sauce', 'ink', 'aqueous humour', 'milk substitute',
    'fruit juice', 'glowing lava', 'gastric acid', 'mineral water',
    'cough syrup', 'quicksilver', 'sweet vitriol', 'grey goo', 'pink slime',
    'cosmic latte', 'bone oil', 'custard', 'lard', 'vinegar', 'creosote',
];
const HALLUCINATED_SOUND_VERBS = [
    'beeps', 'boings', 'sings', 'belches', 'creaks', 'coughs', 'rattles',
    'ululates', 'pops', 'jingles', 'sniffles', 'tinkles', 'eeps', 'clatters',
    'hums', 'sizzles', 'twitters', 'wheezes', 'rustles', 'honks', 'lisps',
    'yodels', 'coos', 'burps', 'moos', 'booms', 'murmurs', 'oinks',
    'quacks', 'rumbles', 'twangs', 'toots', 'gargles', 'hoots', 'warbles',
];

export function monsterGrowUp(mon, victim) {
    if (!mon || (mon.mhp || 0) <= 0) return false;
    const oldData = mon.data || {};
    const oldLevel = mon.m_lev ?? oldData.hpLevel ?? oldData.mlevel ?? 0;
    const oldMaxHp = mon.mhpmax ?? mon.mhp ?? 1;
    const victimLevel = victim?.m_lev ?? victim?.data?.hpLevel ?? victim?.data?.mlevel ?? 0;
    let hpThreshold = oldLevel ? oldLevel * 8 : 4;
    let levelLimit = Math.trunc(3 * (oldData.mlevel ?? 0) / 2);
    const grownName = GROWNUP_MONSTERS.get(oldData.name);
    const grownData = grownName ? RANDOM_MONSTER_BY_NAME.get(grownName) : null;

    if (grownData && (grownData.mlevel ?? 0) > levelLimit) levelLimit = grownData.mlevel ?? levelLimit;
    let maxIncrease = rnd(victimLevel + 1);
    if (oldMaxHp + maxIncrease > hpThreshold + 1)
        maxIncrease = Math.max(hpThreshold + 1 - oldMaxHp, 0);
    const currentIncrease = maxIncrease > 1 ? rn2(maxIncrease) : 0;
    mon.mhpmax = oldMaxHp + maxIncrease;
    mon.mhp = (mon.mhp ?? 1) + currentIncrease;
    if (mon.mhpmax <= hpThreshold) return true;

    if (levelLimit < 5) levelLimit = 5;
    else if (levelLimit > 49) levelLimit = (oldData.mlevel ?? 0) > 49 ? 50 : 49;
    mon.m_lev = oldLevel + 1;
    if (grownData && mon.m_lev >= (grownData.mlevel ?? 0)) {
        mon.data = { ...grownData, hpLevel: mon.m_lev };
        newsym(mon.mx, mon.my);
        levelLimit = mon.m_lev;
    }
    if (mon.m_lev > levelLimit) {
        mon.m_lev--;
        if (mon.mhpmax === hpThreshold + 1) mon.mhpmax--;
    }
    if (mon.mhpmax > 400) mon.mhpmax = 400;
    if ((mon.mhp || 0) > mon.mhpmax) mon.mhp = mon.mhpmax;
    return true;
}

export function refreshSwallowOverlay(more = !!game._message_more) {
    const message = game._pending_message || '';
    const prompt = more && message ? `${message}--More--` : message;
    const overlay = [[0, 0, prompt]];
    if ((game.u?._statusSuffix || '').includes('Hallu')) {
        for (const [row, col, ch] of SWALLOW_OVERLAY_CELLS) {
            if (ch === '@') {
                overlay.push([row, col, ch, 0, CLR_WHITE]);
                continue;
            }
            const rawColor = DISPLAY_MONSTER_COLORS[rn2_on_display_rng(DISPLAY_MONSTER_COLORS.length)];
            const color = rawColor === CLR_BLACK || rawColor === CLR_GRAY ? NO_COLOR : rawColor;
            overlay.push([row, col, ch, 0, color]);
        }
    } else {
        overlay.push(
            [5, 25, '/⎺\\', 0, CLR_CYAN],
            [6, 25, '│', 0, CLR_CYAN],
            [6, 26, '@', 0, CLR_WHITE],
            [6, 27, '│', 0, CLR_CYAN],
            [7, 25, '\\⎽/', 0, CLR_CYAN],
        );
    }
    if (more || !message) game._swallow_overlay_lines = overlay.map(line => [...line]);
    setOverlay(overlay, 22, false, 0);
    const disp = game.nhDisplay;
    if (disp?.setCell) {
        disp.clearRow?.(0);
        for (const [row, col, text, attr = 0, color = NO_COLOR] of overlay) {
            const value = String(text);
            for (let i = 0; i < value.length; i++)
                disp.setCell(col + i, row, value[i], color, attr);
        }
        if (more && message) disp.setCursor(String(prompt).trimEnd().length, 0);
        else if (game._command_mode === 'extendedCommand') disp.setCursor(2 + String(game._extended_command || '').length, 0);
        else if (game._command_mode && message.includes('?')) disp.setCursor(String(message).trimEnd().length + 1, 0);
        else if (game.u?.ux > 0) disp.setCursor(game.u.ux - 1, game.u.uy + 1);
    }
    game._pending_message = message;
    game._message_more = more ? 1 : 0;
    game._swallow_overlay_active = 1;
}

export async function finishSwallowExpel(mon) {
    if (game.u && mon) {
        const heroX = game.u.ux || mon.mx;
        const heroY = game.u.uy || mon.my;
        game.u.ux = mon.mx;
        game.u.uy = mon.my;
        game.u.uswallow = 0;
        game.u.uswldtim = 0;
        game.u.ustuck = null;
        game._swallow_overlay_active = 0;
        game._swallow_overlay_before_command = null;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        game._overlay_hide_status_only = 0;
        game._display_hallucinated_redraw = 1;
        vision_recalc(2);
        await docrt();
        vision_recalc(0);
        mon.mspec_used = rnd(2);
        const spot = enextoMonsterSpot(mon.mx, mon.my, mon.data || {});
        if (spot) {
            newsym(mon.mx, mon.my);
            mon.mx = spot.x;
            mon.my = spot.y;
            newsym(mon.mx, mon.my);
        }
        newsym(heroX, heroY);
        newsym(game.u.ux, game.u.uy);
        game._hallu_display_after_expel = 1;
        game._hallu_refresh_after_expel = 1;
        game._simple_swallow_expel_prompt = 0;
        game._display_hallucinated_redraw = 0;
    }
}

const MIN_QUEST_LEVEL = 14;
const MIN_QUEST_ALIGN = 20;
const QUEST_COMMON_TEXTS = {
    banished: `You have betrayed all those who hold allegiance to %d, as you once did.
My allegiance to %d holds fast and I cannot condone or accept what you
have done.

Leave this place.  You shall never set foot at %H again.
That which you seek is now lost forever, for without the Bell of Opening,
you will never be able to enter the place where he who has the Amulet
resides.

Go now!  You are banished from this place.`,
    quest_complete_no_bell: `"The silver bell which was hoarded by %n will be
essential in locating the Amulet of Yendor."`,
    quest_portal: `You receive a faint telepathic message from %l:
Your help is urgently needed at %H!
Look for a ...ic transporter.
You couldn't quite make out that last message.`,
    quest_portal_again: 'You again sense %l pleading for help.',
    quest_portal_demand: 'You again sense %l demanding your attendance.',
};
const QUEST_ROLE_DATA = {
    Archeologist: {
        filecode: 'Arc',
        homebase: 'the College of Archeology',
        intermed: 'the Tomb of the Toltec Kings',
        leader: 'Lord Carnarvon',
        nemesis: 'the Minion of Huhetotl',
        guardian: 'student',
        artifact: 'the Orb of Detection',
        artifactShort: 'Orb of Detection',
        ranks: ['Digger', 'Field Worker', 'Investigator', 'Exhumer', 'Excavator', 'Spelunker', 'Speleologist', 'Collector', 'Curator'],
        texts: {
            firsttime: `You are suddenly in familiar surroundings.  The buildings in the distance
seem to be those of your old alma mater, but something is wrong.  It feels
as if there has been a riot recently, or %H has
been under siege.

All of the windows are boarded up, and there are objects scattered around
the entrance.

Strange forbidding shapes seem to be moving in the distance.`,
            nexttime: 'Once again, you are back at %H.',
            othertime: `You are back at %H.
You have an odd feeling this may be the last time you ever come here.`,
            locate_first: `A plain opens before you.  Beyond the plain lies a foreboding edifice.

You have the feeling that you will soon find the entrance to
%i.`,
            locate_next: 'Once again, you are near the entrance to %i.',
            goal_first: `A strange feeling washes over you, and you think back to things you
learned during the many lectures of %l.

You realize the feeling must be the presence of %o.`,
            goal_next: 'The familiar presence of %o is in the ether.',
            goal_alt: 'You have returned to %ns lair.',
            leader_first: `"Finally you have returned, %p.  You were always
my most promising student.  Allow me to see if you are ready for the
most difficult task of your career."`,
            leader_last: `"%p, you have failed us.  All of my careful training has been in
vain.  Begone!  Your tenure at this college has been revoked!

"You are a disgrace to the profession!"`,
            leader_next: `Again, %p, you stand before me.
Let me see if you have gained experience in the interim.`,
            badalign: `"%pC!  I've heard that you've been using sloppy techniques.  Your
results lately can hardly be called suitable for %ra!

"How could you have strayed from the %a path?  Go from here, and come
back only when you have purified yourself."`,
            badlevel: `"%p, you are yet too inexperienced to undertake such a demanding
quest.  A mere %r could not possibly face the rigors demanded and
survive.  Go forth, and come here again when your adventures have further
taught you."`,
            nemesis_first: `"So, %p, you think that you can succeed in recovering
%o, when your teacher, %l, has already failed.

"Come, try your best!  I shall destroy you, and gnaw on your bones."`,
            nemesis_next: `"Again you try to best me, eh %p?  Well, you shall fail again.

"You shall never recover %o.

"I shall bear your soul to the Plane of Origins for my master's pleasure."`,
            nemesis_other: '"You persist yet %p!  Good.  Now, you shall die!"',
            nemesis_wantsit: `"I shall have %o from you, %p, then feast
upon your entrails!"`,
            discourage: [
                '"Try your best, %p.  You cannot defeat me."',
                '"I shall rend the flesh from your body whilst you still breathe!"',
                '"First you, %p, then I shall destroy your mentor, %l."',
                '"Tiring yet, %p?  I draw my power from my master and cannot falter!"',
                '"I shall rend thy soul from thy body and consume it!"',
                '"You are far too %a -- it weakens you.  You shall die in this place."',
                '"%d has forsaken you!  You are lost now!"',
                '"A mere %r cannot hope to defeat me!"',
                '"If you are the best %l can send, I have nothing to fear."',
                '"Die %c!  I shall exhibit your carcass as a trophy."',
            ],
            encourage: [
                '"Beware, for %n is powerful and cunning."',
                '"To locate the entrance to %i, you must pass many traps."',
                '"A %nt may be vulnerable to attacks by magical cold."',
                '"Call upon %d when you encounter %n."',
                '"You must destroy %n.  It will pursue you otherwise."',
                '"%oC is a mighty talisman.  With it you can destroy %n."',
                '"Go forth with the blessings of %d."',
                '"I will have my %gP watch for your return."',
                '"Remember not to stray from the true %a path."',
                '"You may be able to sense %o when you are near."',
            ],
            guardtalk_after: [
                `"Did you see Lash LaRue in 'Song of Old Wyoming' the other night?"`,
                '"Hey man, got any potions of hallucination for sale?"',
                '"I guess you are guaranteed to make full professor now."',
                '"So, what was worse, %n or your entrance exams?"',
                '"%oC is impressive, but nothing like the bones I dug up!"',
            ],
            guardtalk_before: [
                `"Did you see Lash LaRue in 'Song of Old Wyoming' the other night?"`,
                '"Hey man, got any potions of hallucination for sale?"',
                '"Did you see the artifact %l brought back from the last dig?"',
                '"So what species do *you* think we evolved from?"',
                `"So you're %ls prize pupil!  I don't know what he sees in you."`,
            ],
            hasamulet: `"Congratulations, %p.  I wondered if anyone could prevail against
the Wizard and the minions of Moloch.  Now, you must embark on one
final adventure.

"Take the Amulet, and find your way onto the Astral Plane.
There you must find the altar of %d and sacrifice the
Amulet on that altar to fulfill your destiny.

"Remember, your path now should always be upwards."`,
            offeredit: `%lC touches %o briefly, gazes into it,
then smiles at you and says:

"Well done, %p.  You have defeated %n and
recovered %o.  But I fear that it shall never be safe
here.

Please take %o with you.  You, %p, can
guard it now far better than I.

May the blessings of %d follow you and guard you."`,
            offeredit2: `"Careful, %p!  %oC might break, and that would be
a tragic loss.  You are its keeper now, and the time has come to
resume your search for the Amulet.  %Z await your
return through the magic portal that brought you here."`,
            posthanks: `"Welcome back, %p.  Have you progressed with your quest to
regain the Amulet of Yendor for %d?"`,
            assignquest: `"Grave times have befallen the college, for %na has
stolen %o.  Without it, the board of directors of
the university will soon have no choice but to revoke our research grants.

"You must locate the entrance to %i.  Within it,
you will find %n.

"You must then defeat %n and return %o
to me.

"Only in this way will we be able to prevent the budget cuts that could
close this college.

            "May the wisdom of %d be your guide."`,
        },
    },
    Barbarian: {
        filecode: 'Bar',
        homebase: 'the Camp of the Duali Tribe',
        intermed: 'the Duali Oasis',
        leader: 'Pelias',
        nemesis: 'Thoth Amon',
        guardian: 'chieftain',
        artifact: 'the Heart of Ahriman',
        artifactShort: 'Heart of Ahriman',
        ranks: ['Plunderer', 'Pillager', 'Bandit', 'Brigand', 'Raider', 'Reaver', 'Slayer', 'Chieftain', 'Conqueror'],
        texts: {
            firsttime: `Warily you scan your surroundings, all of your senses alert for signs
of possible danger.  Off in the distance, you can %x the familiar shapes
of %H.

But why, you think, should %l be there?

Suddenly, the hairs on your neck stand on end as you detect the aura of
evil magic in the air.

Without thought, you ready your weapon, and mutter under your breath:

    "By %d, there will be blood spilt today."`,
            nexttime: `Once again, you near %H.  You know that %l
will be waiting.`,
            othertime: `Again, and you think possibly for the last time, you approach
%H.`,
            locate_first: `The scent of water comes to you in the desert breeze.  You know that
you have located %i.`,
            locate_next: 'Yet again you have a chance to infiltrate %i.',
            goal_first: `The hairs on the nape of your neck lift as you sense an energy in the
very air around you.  You fight down a primordial panic that seeks to
make you turn and run.  This is surely the lair of %n.`,
            goal_next: 'Yet again you feel the air around you heavy with malevolent magical energy.',
            leader_first: `"Ah, %p.  You have returned at last.  The world is in dire
need of your help.  There is a great quest you must undertake.

"But first, I must see if you are ready to take on such a challenge."`,
            leader_last: `"Pah!  You have betrayed the gods, %p.  You will never attain
the glory which you aspire to.  Your failure to follow the true path has
closed this future to you.

"I will protect these people as best I can, but soon %n will overcome
me and destroy all who once called you %s.  Now begone!"`,
            leader_next: '"%p, you are back.  Are you ready now for the challenge?"',
            badalign: `"%pC!  You have wandered from the path of the %a!
If you attempt to overcome %n in this state, he will surely
enslave your soul.  Your only hope, and ours, lies in your purification.
Go forth, and return when you feel ready."`,
            badlevel: `"%p, I fear that you are as yet too inexperienced to face
%n.  Only %Ra with the help of %d could ever hope to
defeat %ni."`,
            nemesis_first: `"So.  This is what that second rate sorcerer %l sends to do %lj bidding.
I have slain many before you.  You shall give me little sport.

"Prepare to die, %c."`,
            nemesis_next: '"I have wasted too much time on you already.  Now, you shall die."',
            nemesis_other: '"You return yet again, %c!  Are you prepared for death now?"',
            nemesis_wantsit: `"I shall have %o back, you pitiful excuse for %ca.
And your life as well."`,
            discourage: [
                '"My pets will dine on your carcass tonight!"',
                '"You are a sorry excuse for %ra."',
                '"Run while you can, %c.  My next spell will be your last."',
                '"I shall use your very skin to bind my next grimoire."',
                '"%d cannot protect you now.  Here, you die."',
                '"Your %a nature makes you weak.  You cannot defeat me."',
                '"Come, %c.  I shall kill you, then unleash the horde on your tribe."',
                '"Once you are dead, my horde shall finish off %l, and your tribe."',
                '"Fight, %c, or are you afraid of the mighty %n?"',
                '"You have failed, %c.  Now, my victory is complete."',
            ],
            encourage: [
                '"%nC is strong in the dark arts, but not immune to cold steel."',
                '"Remember that %n is a great sorcerer.  He lived in the time of Atlantis."',
                '"If you fail, %p, I will not be able to protect these people long."',
                '"To enter %i, you must be very stealthy.  The horde will be on guard."',
                '"Call upon %d in your time of need."',
                '"May %d protect you, and guide your steps."',
                '"If you can lay hands upon %o, carry it for good fortune."',
                '"I cannot stand against %ns sorcery.  But %d will help you."',
                '"Do not fear %n.  I know you can defeat %ni."',
                '"You have a great road to travel, %p, but only after you defeat %n."',
            ],
            guardtalk_after: [
                `"The battles here have been good -- our enemies' blood soaks the soil!"`,
                '"Remember that glory is crushing your enemies beneath your feet!"',
                '"Times will be good again, now that the horde is vanquished."',
                '"You have brought our clan much honor in defeating %n."',
                '"You will be a worthy successor to %l."',
            ],
            guardtalk_before: [
                `"The battles here have been good -- our enemies' blood soaks the soil!"`,
                '"Remember that glory is crushing your enemies beneath your feet!"',
                '"There has been little treasure to loot, since the horde arrived."',
                '"The horde is mighty in numbers, but they have little courage."',
                '"%lC is a strange one, but he has helped defend us."',
            ],
            hasamulet: `"This is wondrous, %p.  I feared that you could not possibly
succeed in your quest, but here you are in possession of the Amulet
of Yendor!

"I have studied the texts of the magi constantly since you left.  In
the Book of Skelos, I found this:

    %d will cause a child to be sent into the world.  This child is to
    be made strong by trial of battle and magic, for %d has willed it so.
    It is said that the child of %d will recover the Amulet of Yendor
    that was stolen from the Creator at the beginning of time.

"As you now possess the amulet, %p, I suspect that the Book
speaks of you.

    The child of %d will take the Amulet, and travel to the Astral
    Plane, where the Great Temple of %d is to be found.  The Amulet
    will be sacrificed to %d, there on %dJ altar.  Then the child will
    stand by %d as champion of all %cP for eternity.

"This is all I know, %p.  I hope it will help you."`,
            offeredit: `When %l sees %o, he smiles, and says:

    Well done, %p.  You have saved the world from certain doom.
    What, now, should be done with %o?

    These people, brave as they are, cannot hope to guard it from
    other sorcerers who will detect it, as surely as %n did.

    Take %o with you, %p.  It will guard you in
    your adventures, and you can best guard it.  You embark on a
    quest far greater than you realize.

    Remember me, %p, and return when you have triumphed.  I
    will tell you then of what you must do.  You will understand when the
    time comes.`,
            offeredit2: `%l gazes reverently at %o, then back at you.

"You are its keeper now, and the time has come to resume your search
for the Amulet.  %Z await your return through the
magic portal which brought you here."`,
            posthanks: `"You are indeed ready now, %p.  I shall tell you a tale of
great suffering among your people:

"Shortly after you left on your vision quest, the caves were invaded by
the creatures sent against us by %n.

"She, herself, could not attack us due to her great size, but her minions
have harassed us ever since.  In the first attacks, many died, and the
minions of %n managed to steal %o.
They took it to %i and there, none of our
%g warriors have been able to go.

"You must find %i, and within it wrest
%o from %n.  She guards it as
jealously as she guards all treasures she attains.  But with it,
we can make our caves safe once more.

"Please, %p, recover %o for us, and return it here."`,
            assignquest: `"The world is in great need of your assistance, %p.

"About six months ago, I learned that a mysterious sorcerer, known
as %n, had begun to gather a large group of cutthroats and brigands
about %ni.

"At about the same time, these people you once rode with \`liberated' a
potent magical talisman, %o, from a Turanian caravan.

"%nC and %nj Black Horde swept down upon %i and defeated
the people there, driving them out into the desert.  He has taken
%o, and seeks to bend it to %nj will.  I detected the
subtle changes in the currents of fate, and joined these people.
Then I sent forth a summons for you.

"If %n can bend %o to %nj will, he will become
almost indestructible.  He will then be able to enslave the minds of
men across the world.  You are the only hope.  The gods smile upon you,
and with %d behind you, you alone can defeat %n.

"You must go to %i.  From there, you can track down
%n, defeat %ni, and return %o to us.  Only
then will the world be safe."`,
        },
    },
    Knight: {
        filecode: 'Kni',
        homebase: 'Camelot Castle',
        intermed: 'the Isle of Glass',
        leader: 'King Arthur',
        nemesis: 'Ixoth',
        guardian: 'page',
        artifact: 'the Magic Mirror of Merlin',
        artifactShort: 'Magic Mirror of Merlin',
        ranks: ['Gallant', 'Esquire', 'Bachelor', 'Sergeant', 'Knight', 'Banneret', 'Chevalier', 'Seignieur', 'Paladin'],
        texts: {
            leader_last: `"Thou disgracest this noble court with thine impure presence.  We have been
lenient with thee, but no more.  Thy name shall be spoken no more.  We
hereby strip thee of thy title, thy lands, and thy standing as %ca.
Begone from Our sight!"`,
            goal_first: `As you exit the swamps, you see before you a huge, gaping hole in the
side of a hill.  From within, you smell the foul stench of carrion.

The pools on either side of the entrance are fouled with blood, and
pieces of rusted metal and broken weapons show above the surface.`,
            nemesis_first: `"Ah, so %l has sent another %g to retrieve
%o.

"No, I see you are no %g.  Perhaps I shall have some fun today
after all.  Prepare to die, %r!  You shall never regain
%o."`,
            nemesis_next: '"So, %r.  Again you challenge me."',
            nemesis_other: '"Die now, %r.  %d has no power here to aid you."',
            nemesis_wantsit: '"You shall die, %r, and I will have %o back."',
            discourage: [
                '"A mere %r can never withstand me!"',
                '"I shall kill thee now, and feast!"',
                '"Puny %c.  What manner of death dost thou wish?"',
                '"First thee, %p, then I shall feast upon %l."',
                '"Hah!  Thou hast failed, %r.  Now thou shalt die."',
                '"Die, %c.  Thou art as nothing against my might."',
                '"I shall suck the marrow from thy bones, %c."',
                `"Let's see...  Baked?  No.  Fried?  Nay.  Broiled?  Yea verily, that is the way I like my %c for dinner."`,
                '"Thy strength waneth, %p.  The time of thy death draweth near."',
                '"Call upon thy precious %d, %p.  It shall not avail thee."',
            ],
            encourage: [
                '"Remember, %p, follow always the path of %d."',
                '"Though %n is verily a mighty foe, We have confidence in thy victory."',
                '"Beware, for %n hath surrounded %niself with hordes of foul creatures."',
                `"Great treasure, 'tis said, is hoarded in the lair of %n."`,
                '"If thou possessest %o, %p, %ns magic shall therewith be thwarted."',
                '"The gates of %i are guarded by forces unseen, %p. Go carefully."',
                '"Return %o to Us quickly, %p."',
                '"Destroy %n, %p, else %H shall surely fall."',
                '"Call upon %d when thou art in need."',
                '"To find %i, thou must keep thy heart pure."',
            ],
            guardtalk_after: [
                '"Hail, %p!  Verily, thou lookest well."',
                '"So, %p, didst thou find %n in the fens near %i?"',
                '"Worthy %p, hast thou proven thy right purpose on the body of %n?"',
                '"Verily, %l could have no better champion, %p."',
                '"Hast thou indeed recovered %o?"',
            ],
            guardtalk_before: [
                '"Hail, %p!  Verily, thou lookest well."',
                '"There is word, %p, that %n hath been sighted in the fens near %i."',
                '"Thou art our only hope now, %p."',
                '"Verily, %l could have no better champion, %p."',
                '"Many brave %cP died when %n attacked."',
            ],
            hasamulet: `"Thou hast succeeded, We see, %p!  Now thou art commanded to take
the Amulet to be sacrificed to %d in the Plane of the Astral.

"Merlin hath counseled Us that thou must travel always upwards through
the Planes of the Elements, to achieve this goal.

"Go with %d, %p."`,
            offeredit: `As you approach %l, %lh beams at you and says:

    "Well done!  Thou art truly the Champion of %H.  We
    have received word that Merlin is recovering, and shall soon
    rejoin Us.

    "He hath instructed Us that thou art now to be the guardian of
    %o.  He feeleth that thou mayst have need of
    its powers in thine adventures.  It is Our wish that thou keepest
    %o with thee as thou searchest for the fabled
    Amulet of Yendor."`,
            offeredit2: `"Careful, %p!  %oC might break, and that would
be a tragic loss.  Thou art its keeper now, and the time hath come
to resume thy search for the Amulet.  %Z await thy
return through the magic portal that brought thee here."`,
            posthanks: `"Yes, %p.  You are truly ready now.  Attend to me and I shall
tell you of what has transpired:

"During one of the Great Meditations a short time ago, %n and
a legion of elementals invaded %H.  Many %gP
were killed, including the one bearing %o.

Now, there are barely enough %gP left to keep the elementals
at bay.

"We need you to find %i, then, from there,
travel to %ns lair.  If you can manage to defeat %n and
return %o here, we can then drive off the legions
of elementals that slay our students.

"Go with %d as your guide, %p."`,
        },
    },
    Priest: {
        filecode: 'Pri',
        homebase: 'the Great Temple',
        intermed: 'the Temple of Nalzok',
        leader: 'Arch Priest',
        leaderText: 'the Arch Priest',
        nemesis: 'Nalzok',
        guardian: 'acolyte',
        artifact: 'the Mitre of Holiness',
        artifactShort: 'Mitre of Holiness',
        ranks: ['Aspirant', 'Acolyte', 'Adept', 'Priest', 'Curate', 'Canon', 'Lama', 'Patriarch', 'High Priest'],
        texts: {
            firsttime: `You find yourself standing in sight of %H.  Something
is obviously wrong here.  The doors to %H, which usually
stand open, are closed.  Strange human shapes shamble around
outside.

You realize that %l needs your assistance!`,
            nexttime: 'Once again, you stand before %H.',
            othertime: `Again you face %H.  Your intuition hints that this may be
the final time you come here.`,
            locate_first: `You stand facing a large graveyard.  The sky above is filled with clouds
that seem to get thicker closer to the center.  You sense the presence of
undead in larger numbers than you have ever encountered before.

You remember the descriptions of %i, given to you by
%l.  It is ahead that you will find %ns trail.`,
            locate_next: 'Again, you stand before %i.',
            goal_first: `The stench of brimstone is all about you, and the shrieks and moans
of tortured souls assault your psyche.

Ahead, there is a small clearing amidst the bubbling pits of lava...`,
            goal_next: 'Again, you have invaded %ns domain.',
            goal_alt: 'Again, you have invaded %ns domain.',
            leader_first: `"Ah, %p, my %S.  You have returned to us at last.
A great blow has befallen our order; perhaps you can help us.
First, however, I must determine if you are prepared for this
great challenge."`,
            leader_last: `"You are a heretic, %p!  How can you, %ra, deviate so from the
teachings of %d?  Begone from this temple.  You are no longer
%sa to this order.  We will pray to %d for other assistance,
as you have failed us utterly."`,
            leader_next: '"Again, my %S, you stand before me.  Are you ready now to help us?"',
            badalign: `"This is terrible, %p.  You have deviated from the true path!
You know that %d requires the most strident devotion of this
order.  The %shood must stand for utmost piety.

"Go from here, atone for your sins against %d.  Return only when
you have purified yourself."`,
            badlevel: `"Alas, %p, it is not yet to be.  A mere %r could never
withstand the might of %n.  Go forth, again into the world, and return
when you have attained the post of %R."`,
            nemesis_first: "\"Ah!  You must be %ls ... er, `hero'.  A pleasure to meet you.\"",
            nemesis_next: '"We meet again.  Please reconsider your actions."',
            nemesis_other: `"Surely, %p, you have learned that you cannot trust any bargains
that %l has made.  I can show you how to continue on
your quest without having to run into him again."`,
            nemesis_wantsit: `"Please, think for a moment about what you are doing.  Do you truly
believe that %d would want %l to have
%o?"`,
            discourage: [
                '"Submit to my will, %c, and I shall spare you."',
                '"Your puny powers are no match for me, %c."',
                '"I shall have you turned into a zombie for my pleasure!"',
                '"Despair now, %r.  %d cannot help you."',
                '"I shall feast upon your soul for many days, %c."',
                '"Your death will be slow and painful.  That I promise!"',
                '"You cannot defeat %n, you fool.  I shall kill you now."',
                '"Your precious %lt will be my next victim."',
                '"I feel your powers failing you, %r.  You shall die now."',
                '"With %o, nothing can stand in my way."',
            ],
            encourage: [
                '"You can prevail, if you rely on %d."',
                '"Remember that %n has great magic at his command."',
                '"Be pure, my %S."',
                '"Beware, %i is surrounded by a great graveyard."',
                '"You may be able to affect %n with magical cold."',
                '"Acquire and wear %o if you can.  It will aid you against %n."',
                '"Call upon %d when your need is greatest.  You will be answered."',
                '"The undead legions are weakest during the daylight hours."',
                '"Do not lose faith, %p.  If you do so, %n will grow stronger."',
                '"Wear %o.  It will assist you against the undead."',
            ],
            guardtalk_after: [
                '"Greetings, %r.  It is good to see you again."',
                '"Ah, %p!  Our deepest gratitude for all of your help."',
                '"Welcome back, %s!  With %o, no undead can stand against us."',
                '"Praise be to %d, for delivering us from %n."',
                '"May %d be with you, %s."',
            ],
            guardtalk_before: [
                '"Greetings, honored %r.  It is good to see you."',
                '"Ah, %p!  Surely you can help us in our hour of need."',
                '"Greetings, %s.  %lC has great need of your help."',
                '"Alas, it seems as if even %d has deserted us."',
                '"May %d be with you, %s."',
            ],
            hasamulet: `"You have prevailed, %p!  %d is surely with you.  Now,
you must take the amulet, and sacrifice it on %ds altar on
the Astral Plane.  I suspect that I shall never see you again in this
life, but I hope to at %ds feet."`,
            offeredit: `"You have returned, %p.  And with %o, I see.
Congratulations.

"I have been in meditation, and have received direction from
a minion of %d.  %d commands that you retain
%o.  With it, you must recover the Amulet
of Yendor.

"Go forth, and let %d guide your steps."`,
            offeredit2: `%lC reiterates that %o is yours now.

"The time has come to resume your search for the Amulet.
%Z await your return through the magic portal
that brought you here."`,
            posthanks: `"You are indeed ready, %p.  I shall tell you what has transpired,
and why we so desperately need your help:

"A short time ago, the mountain centaurs to the east invaded
and enslaved the plains centaurs in this area.  The local
leader is now only a figurehead, and serves %n.

"During our last gathering of worship here, we were beset by hordes of
hostile centaurs, as you witnessed.  In the first onslaught a group,
headed by %n %niself, managed to breach the grove and steal
%o.

"Since then, we have been besieged.  We do not know how much longer
we will be able to maintain our magical barriers.

"If we are to survive, you, %p, must infiltrate
%i.  There, you will find a pathway down, to the
underground cavern of %n.  He has always coveted
%o, and will surely keep it.

"Recover %o for us, %p!  Only then will %d be safe."`,
            assignquest: `"Yes, %p.  You are truly ready now.  Attend to me and I shall
tell you of what has transpired:

"At one of the Great Festivals a short time ago, %n and a legion
of undead invaded %H.  Many %gP were killed, including
the one carrying %o.

"As a final act of vengefulness, %n desecrated the altar here.
Without it, we could not mount a counter-attack.  Now, there are
barely enough %gP left to keep the undead at bay.

"We need you to find %i, then, from there, travel
to %ns lair.  If you can manage to defeat %n and return
%o here, we can then drive off the legions of
undead that befoul the land.

"Go with %d as your guide, %p."`,
        },
    },
    Wizard: {
        filecode: 'Wiz',
        homebase: 'the Lonely Tower',
        intermed: 'the Tower of Darkness',
        leader: 'Neferet the Green',
        nemesis: 'the Dark One',
        guardian: 'apprentice',
        artifact: 'the Eye of the Aethiopica',
        artifactShort: 'Eye of the Aethiopica',
        ranks: ['Evoker', 'Conjurer', 'Thaumaturge', 'Magician', 'Enchanter', 'Sorcerer', 'Necromancer', 'Wizard', 'Mage'],
        texts: {
            firsttime: `You are suddenly in familiar surroundings.  You notice what appears to
be a large, squat stone structure nearby.  Wait!  That looks like the
tower of your former teacher, %l.

However, things are not the same as when you were last here.  Mists and
areas of unexplained darkness surround the tower.  There is movement in
the shadows.

Your teacher would never allow such unaesthetic forms to surround the
tower...  unless something were dreadfully wrong!`,
            nexttime: 'Once again, you are back at %H.',
            othertime: `You are back at %H.
You have an odd feeling this may be the last time you ever come here.`,
            locate_first: 'Wisps of fog swirl nearby.  You feel that %ns lair is close.',
            locate_next: 'You believe that you may once again invade %i.',
            goal_first: "You feel your mentor's presence; perhaps %o is nearby.",
            goal_next: 'The aura of %o tingles at the edge of your perception.',
            goal_alt: 'You have returned to %ns lair.',
            leader_first: `"Come closer, %p, for my voice falters in my old age.
Yes, I see that you have come a long way since you went out into the
world, leaving the safe confines of this tower.  However, I must first
determine if you have all of the skills required to take on the task
I require of you."`,
            leader_last: `"You fool, %p!  Why did I waste all of those years teaching you
the esoteric arts?  Get out of here!  I shall find another."`,
            leader_next: '"Well, %p, you have returned.  Perhaps you are now ready..."',
            badalign: `"You amaze me, %p!  How many times did I tell you that the way of a mage
is an exacting one.  One must use the world with care, lest one leave it
in ruins and simplify the task of %n.

"You must go back and show your worthiness.  Do not return until you are
truly ready for this quest.  May %d guide you in this task."`,
            badlevel: `"Alas, %p, you have not yet shown your proficiency as a worthy
spellcaster.  As %ra, you would surely be overcome in the challenge
ahead.  Go, now, expand your horizons, and return when you have attained
renown as %Ra."`,
            nemesis_first: `"Ah, I recognize you, %p.  So, %l has sent you to steal
%o from me, hmmm?  Well, %lh is a fool to send such
a mental weakling against me.

"Your destruction, however, should make for good sport.  In the end, you
shall beg me to kill you!"`,
            nemesis_next: `"How nice of you to return, %p!  I enjoyed our last meeting.  Are you
still hungry for more pain?

"Come!  Your soul, like %o, shall soon be mine to command."`,
            nemesis_other: `"I'm sure that your perseverance shall be the subject of innumerable
ballads, but you shall not be around to hear them, I fear!"`,
            nemesis_wantsit: `"Thief!  %oC belongs to me, now.  I shall feed
your living flesh to my minions."`,
            discourage: [
                '"Your puny powers are no match for me, fool!"',
                '"When you are defeated, your torment will last for a thousand years."',
                '"After your downfall, %p, I shall devour %l for dessert!"',
                '"Are you ready yet to beg for mercy?  I could be lenient..."',
                '"Your soul shall join the enslaved multitude I command!"',
                '"Your lack of will is evident, and you shall die as a result."',
                '"Your faith in %d is for naught!  Come, submit to me now!"',
                '"A mere %r is nothing compared to my skill!"',
                '"So, you are the best hope of %l?  How droll."',
                '"Feel my power, %c!  My victory is imminent!"',
            ],
            encourage: [
                '"Beware, for %n is immune to most magical attacks."',
                '"To enter %i you must pass many traps."',
                '"%nC may be vulnerable to physical attacks."',
                '"%d will come to your aid when you call."',
                '"You must utterly destroy %n.  He will pursue you otherwise."',
                '"%oC is a mighty artifact.  With it you can destroy %n."',
                '"Go forth with the blessings of %d."',
                '"I will have my %gP watch for your return."',
                '"Feel free to take any items in that chest that might aid you."',
                '"You will know when %o is near.  Proceed with care!"',
            ],
            guardtalk_after: [
                `"I have some eye of newt to trade, do you have a spare blind-worm's sting?"`,
                '"The magic portal now seems like it will remain stable for quite some time."',
                '"Have you noticed how much stronger %l is since %o was recovered?"',
                `"Thank %d!  We weren't positive you would defeat %n."`,
                '"I, too, will venture into the world, because %n was but one of many evils to be vanquished."',
            ],
            guardtalk_before: [
                '"Would you happen to have some eye of newt in that overstuffed pack, %s?"',
                '"Ah, the spell to create the magic portal worked.  Outstanding!"',
                '"Hurry!  %lC may not survive that casting of the portal spell!"',
                '"The spells of %n were just too powerful for us to withstand."',
                '"I, too, will venture into the world, because %n is but one of many evils to be vanquished."',
            ],
            hasamulet: `"Congratulations, %p.  I always knew that if anyone could succeed
in defeating the Wizard of Yendor and his minions, it would be you.

"Go now, and take the Amulet to the Astral Plane.  Once there, present
the Amulet on the altar of %d.  Along the way you shall pass through
the four Elemental Planes.  These planes are like nothing you have ever
experienced before, so be prepared!

"For this you were born, %s!  I am very proud of you."`,
            offeredit: `%lC notices %o in your possession,
beams at you and says:

    "I knew you could defeat %n and retrieve
    %o.  We shall never forget this
    brave service.

    "Take %oh with you in your quest for the Amulet of Yendor.
    I can sense that it has attuned %oiself to you already.

    "May %d guide you in your quest, and keep you from harm."`,
            offeredit2: `"You are the keeper of %o now.  It is time to
recover the /other/ Amulet.  %Z await your return through
the magic portal which brought you here."`,
            posthanks: `"Come near, my %S, and share your adventures with me.
So, have you succeeded in your quest for the Amulet of Yendor?"`,
            assignquest: `"Yes, %p, you truly are ready for this dire task.  Listen,
carefully, for what I tell you now will be of vital importance.

"Since you left us to hone your skills in the world, we unexpectedly came
under attack by the forces of %n.  As you know, we thought
%n had perished at the end of the last age, but, alas, this was
not the case.

"%nC sent an army of abominations against us.  Among them was a
minion, mindless and ensorcelled, and thus, in the confusion, it was
able to penetrate our defenses.  Alas, this creature has stolen
%o and I fear has delivered %oh to %n.

"Over the years, I had woven most of my power into this amulet, and thus,
without it, I have but a shadow of my former power, and I fear that I
shall soon perish.

"You must travel to %i, and within its dungeons,
find and overcome %n, and return %o to me.

"Go now, with %d, and complete this quest before it is too late."`,
        },
    },
};
const BOULDER = 465;
const GOLD_PIECE = 466;
const CORPSE = 471;
const STATUE = 472;
const ROCK_CLASS = 13;
const FLINT_STONE = 473;
const HEAVY_IRON_BALL = 474;
const IRON_CHAIN = 475;
const ROCK = 467;
const EGG = 10001;
const TIN = 10004;
const CRYSTAL_BALL = 10088;
const GLOB_OF_GRAY_OOZE = 10180;
const GLOB_OF_BROWN_PUDDING = 10181;
const GLOB_OF_GREEN_SLIME = 10182;
const GLOB_OF_BLACK_PUDDING = 10183;
const BLINDING_VENOM = 10184;
const ACID_VENOM = 10185;
const GLOB_TYPES = new Map([
    ['gray ooze', { otyp: GLOB_OF_GRAY_OOZE, name: 'glob of gray ooze', color: CLR_GRAY }],
    ['brown pudding', { otyp: GLOB_OF_BROWN_PUDDING, name: 'glob of brown pudding', color: CLR_BROWN }],
    ['green slime', { otyp: GLOB_OF_GREEN_SLIME, name: 'glob of green slime', color: CLR_GREEN }],
    ['black pudding', { otyp: GLOB_OF_BLACK_PUDDING, name: 'glob of black pudding', color: CLR_BLACK }],
]);
GLOB_TYPES.set('grey ooze', GLOB_TYPES.get('gray ooze'));
const GLOB_TYPE_BY_OTYP = new Map();
for (const globType of GLOB_TYPES.values()) GLOB_TYPE_BY_OTYP.set(globType.otyp, globType);
const RANDOM_GLOB_MONSTER_NAMES = ['gray ooze', 'brown pudding', 'green slime'];
const RANDOM_CLASS = 0;
const WEAPON_CLASS = 1;
const ARMOR_CLASS = 2;
const RING_CLASS = 3;
const FOOD_CLASS = 7;
const SCROLL_CLASS = 8;
const SCR_ENCHANT_ARMOR = 277;
const SCR_SCARE_MONSTER = 279;
const SCR_BLANK_PAPER = 293;
const POTION_CLASS = 9;
const POT_ACID = 238;
const POT_POLYMORPH = 248;
const POT_OIL = 252;
const POT_WATER = 253;
const WAND_CLASS = 10;
const SPBOOK_NO_NOVEL = 11;
const SPBOOK_CLASS = 10003;
const TOOL_CLASS = 12;
const BOOK_OF_THE_DEAD = 10097;
const GEM_CLASS = 14;
const AMULET_CLASS = 15;
const VEGETARIAN_CLASS = 10098;
const WISH_RANDOM_CLASSES = [
    WAND_CLASS, RING_CLASS, POTION_CLASS, SCROLL_CLASS, GEM_CLASS, AMULET_CLASS,
    SPBOOK_CLASS, SPBOOK_CLASS, WEAPON_CLASS, ARMOR_CLASS, TOOL_CLASS, FOOD_CLASS, FOOD_CLASS,
];
const OBJECT_CLASS_GLYPHS = {
    armor: '[',
    weapon: ')',
    potion: '!',
    scroll: '?',
    wand: '/',
    ring: '=',
    food: '%',
    tool: '(',
    gem: '*',
    amulet: '"',
    spellbook: '+',
};
const DART = 353;
const BELL = 358;
const CANDELABRUM_OF_INVOCATION = 10076;
const ORCISH_DAGGER = 10020;
const DAGGER = 10023;
const KNIFE = 10026;
const STILETTO = 10109;
const SHORT_SWORD = 10031;
const ELVEN_SHORT_SWORD = 10186;
const ORCISH_SHORT_SWORD = 10187;
const DWARVISH_SHORT_SWORD = 10103;
const SCIMITAR = 10021;
const BROADSWORD = 10032;
const ELVEN_BROADSWORD = 10122;
const LONG_SWORD = 10033;
const TWO_HANDED_SWORD = 10059;
const KATANA = 10125;
const PICK_AXE = 10025;
const RANSEUR = 10055;
const PARTISAN = 10056;
const GLAIVE = 10057;
const SPETUM = 10058;
const HALBERD = 10172;
const BARDICHE = 10173;
const VOULGE = 10174;
const FAUCHARD = 10175;
const GUISARME = 10176;
const BILL_GUISARME = 10177;
const BEC_DE_CORBIN = 10178;
const FLAIL = 10060;
const LUCERN_HAMMER = 10071;
const DWARVISH_MATTOCK = 10104;
const SILVER_SABER = 10062;
const BULLWHIP = 10067;
const SPE_HEALING = 327;
const HEALING_SPELLBOOK_APPEARANCE_INDEX = 8;
const SPELLBOOK_LEVELS = {
    dig: 5,
    'magic missile': 2,
    fireball: 4,
    'cone of cold': 4,
    sleep: 3,
    'finger of death': 7,
    light: 1,
    'detect monsters': 1,
    healing: 1,
    knock: 1,
    'force bolt': 1,
    'confuse monster': 1,
    'cure blindness': 2,
    'drain life': 2,
    'slow monster': 2,
    'wizard lock': 2,
    'create monster': 2,
    'detect food': 2,
    'cause fear': 3,
    clairvoyance: 3,
    'cure sickness': 3,
    'charm monster': 5,
    'haste self': 3,
    'detect unseen': 3,
    levitation: 4,
    'extra healing': 3,
    'restore ability': 4,
    invisibility: 4,
    'detect treasure': 4,
    'remove curse': 3,
    'magic mapping': 5,
    identify: 3,
    'turn undead': 6,
    polymorph: 6,
    'teleport away': 6,
    'create familiar': 6,
    cancellation: 7,
    protection: 1,
    jumping: 1,
    'stone to flesh': 3,
    'chain lightning': 2,
};
const SPELLBOOK_DELAYS = {
    dig: 6,
    'magic missile': 2,
    fireball: 4,
    'cone of cold': 7,
    sleep: 1,
    'finger of death': 10,
    light: 1,
    'detect monsters': 1,
    healing: 2,
    knock: 1,
    'force bolt': 2,
    'confuse monster': 2,
    'cure blindness': 2,
    'drain life': 2,
    'slow monster': 2,
    'wizard lock': 3,
    'create monster': 3,
    'detect food': 3,
    'cause fear': 3,
    clairvoyance: 3,
    'cure sickness': 3,
    'charm monster': 3,
    'haste self': 4,
    'detect unseen': 4,
    levitation: 4,
    'extra healing': 5,
    'restore ability': 5,
    invisibility: 5,
    'detect treasure': 5,
    'remove curse': 5,
    'magic mapping': 7,
    identify: 6,
    'turn undead': 8,
    polymorph: 8,
    'teleport away': 6,
    'create familiar': 7,
    cancellation: 8,
    protection: 3,
    jumping: 3,
    'stone to flesh': 1,
    'chain lightning': 4,
};
const SPELL_CATEGORIES = {
    dig: 'matter',
    'magic missile': 'attack',
    fireball: 'attack',
    'cone of cold': 'attack',
    sleep: 'enchantment',
    'finger of death': 'attack',
    light: 'divination',
    'detect monsters': 'divination',
    healing: 'healing',
    knock: 'matter',
    'force bolt': 'attack',
    'confuse monster': 'enchantment',
    'cure blindness': 'healing',
    'drain life': 'attack',
    'slow monster': 'enchantment',
    'wizard lock': 'matter',
    'create monster': 'cleric',
    'detect food': 'divination',
    'cause fear': 'enchantment',
    clairvoyance: 'divination',
    'cure sickness': 'healing',
    'charm monster': 'enchantment',
    'haste self': 'escape',
    'detect unseen': 'divination',
    levitation: 'escape',
    'extra healing': 'healing',
    'restore ability': 'healing',
    invisibility: 'escape',
    'detect treasure': 'divination',
    'remove curse': 'cleric',
    'magic mapping': 'divination',
    identify: 'divination',
    'turn undead': 'cleric',
    polymorph: 'matter',
    'teleport away': 'escape',
    'create familiar': 'cleric',
    cancellation: 'matter',
    protection: 'cleric',
    jumping: 'escape',
    'stone to flesh': 'healing',
    'chain lightning': 'attack',
};
const POTION_WISH_BASE = 230;
const ARROW_TRAP = 1;
const TELEP_TRAP = 15;
const DART_TRAP = 2;
const ROCKTRAP = 3;
const SQKY_BOARD = 4;
const LANDMINE = 6;
const SLP_GAS_TRAP = 8;
const RUST_TRAP = 9;
const FIRE_TRAP = 10;
const HOLE = 13;
const TRAPDOOR = 14;
const LEVEL_TELEP = 16;
const STATUE_TRAP = 19;
const MAGIC_TRAP = 20;
const ANTI_MAGIC = 21;
const POLY_TRAP = 22;
const TRAP_NAMES = {
    1: 'arrow trap',
    2: 'dart trap',
    3: 'falling rock trap',
    4: 'squeaky board',
    5: 'bear trap',
    6: 'land mine',
    7: 'rolling boulder trap',
    8: 'sleeping gas trap',
    9: 'rust trap',
    10: 'fire trap',
    11: 'pit',
    12: 'spiked pit',
    13: 'hole',
    14: 'trap door',
    15: 'teleportation trap',
    16: 'level teleporter',
    17: 'magic portal',
    18: 'spider web',
    19: 'statue trap',
    20: 'magic trap',
    21: 'anti-magic field',
    22: 'polymorph trap',
    23: 'vibrating square',
};
const WIZARD_WISH_TRAP_TYPES = new Map([
    ['arrow trap', 1],
    ['dart trap', DART_TRAP],
    ['falling rock trap', ROCKTRAP],
    ['squeaky board', SQKY_BOARD],
    ['rolling boulder trap', ROLLING_BOULDER_TRAP],
    ['sleeping gas trap', SLP_GAS_TRAP],
    ['rust trap', RUST_TRAP],
    ['fire trap', FIRE_TRAP],
    ['pit', PIT],
    ['spiked pit', SPIKED_PIT],
    ['hole', HOLE],
    ['trap door', TRAPDOOR],
    ['teleportation trap', 15],
    ['level teleporter', LEVEL_TELEP],
    ['magic portal', MAGIC_PORTAL],
    ['web', WEB],
    ['statue trap', STATUE_TRAP],
    ['magic trap', MAGIC_TRAP],
    ['anti magic trap', ANTI_MAGIC],
    ['polymorph trap', POLY_TRAP],
    ['vibrating square', VIBRATING_SQUARE],
]);
const LARGE_BOX = 214;
const ICE_BOX = 216;
const CHEST = 215;
const SACK = 217;
const OILSKIN_SACK = 218;
const BAG_OF_HOLDING = 219;
const BRASS_LANTERN = 226;
const OIL_LAMP = 227;
const MAGIC_LAMP = 228;
const BAG_OF_TRICKS = 10158;
const TIN_OPENER = 10159;
const LAND_MINE = 10160;
const BEARTRAP = 10161;
const TOOLED_HORN = 10162;
const HORN_OF_PLENTY = 957;
const GRAPPLING_HOOK = 10163;
const MEAT_RING = 10164;
const LOADSTONE = 10165;
const FLINT = 10166;
const LOCK_PICK = 10167;
const WOODEN_HARP = 10168;
const MAGIC_HARP = 10169;
const MAGIC_FLUTE = 946;
const FROST_HORN = 953;
const FIRE_HORN = 955;
const DRUM_OF_EARTHQUAKE = 975;
const LUCKSTONE = 10127;
const TOUCHSTONE = FLINT_STONE;
const BLINDFOLD = 10113;
const SKELETON_KEY = 220;
const MIRROR = 10006;
const CREAM_PIE = 10081;
const LUMP_OF_ROYAL_JELLY = 10089;
const KELP_FROND = 172;
const SLIME_MOLD = 11009;
const EUCALYPTUS_LEAF = 11000;
const APPLE = 11001;
const PANCAKE = 11011;
const MEATBALL = 11012;
const MEAT_STICK = 11014;
const ENORMOUS_MEATBALL = 11013;
const WAN_MAKE_INVISIBLE = 10091;
const CRAM_RATION = 145;
const LEMBAS_WAFER = 146;
const K_RATION = 10035;
const C_RATION = 10036;
const FORTUNE_COOKIE = 11010;
const EXPENSIVE_CAMERA = 10082;
const STETHOSCOPE = 10083;
const MAGIC_MARKER = 10084;
const TINNING_KIT = 10170;
const CAN_OF_GREASE = 10171;
const TALLOW_CANDLE = 370;
const WAX_CANDLE = 371;
const GRAY_DRAGON_SCALE_MAIL = 10085;
const SILVER_DRAGON_SCALE_MAIL = 10086;
const SPEED_BOOTS = 10087;
const SHIELD_OF_REFLECTION = 10074;
const SMALL_SHIELD = 10046;
const SHIELD_OF_DRAIN_RESISTANCE = 10206;
const SHIELD_OF_SHOCK_RESISTANCE = 10207;
const ELVEN_SHIELD = 10208;
const URUK_HAI_SHIELD = 10209;
const ORCISH_SHIELD = 10210;
const FEDORA = 10078;
const CORNUTHAUM = 10211;
const DUNCE_CAP = 10212;
const ELVEN_LEATHER_HELM = 10213;
const ORCISH_HELM = 10022;
const DWARVISH_IRON_HELM = 10107;
const DENTED_POT = 10045;
const HELM_OF_BRILLIANCE = 10131;
const HELM_OF_CAUTION = 10214;
const HELM_OF_OPPOSITE_ALIGNMENT = 10215;
const HELM_OF_TELEPATHY = 10216;
const LARGE_SHIELD = 10047;
const DWARVISH_ROUNDSHIELD = 10106;
const PLATE_MAIL = 10037;
const HELMET = 10044;
const LOW_BOOTS = 10048;
const HIGH_BOOTS = 10049;
const WATER_WALKING_BOOTS = 10132;
const JUMPING_BOOTS = 10133;
const ELVEN_BOOTS = 10134;
const KICKING_BOOTS = 10135;
const FUMBLE_BOOTS = 10136;
const LEVITATION_BOOTS = 10137;
const HAWAIIAN_SHIRT = 10188;
const T_SHIRT = 10189;
const RING_MAIL = 10041;
const STUDDED_LEATHER_ARMOR = 10042;
const LEATHER_ARMOR = 10043;
const ELVEN_MITHRIL_COAT = 10079;
const IRON_SHOES = 10105;
const GOLD_DRAGON_SCALE_MAIL = 10140;
const RED_DRAGON_SCALE_MAIL = 10141;
const WHITE_DRAGON_SCALE_MAIL = 10142;
const ORANGE_DRAGON_SCALE_MAIL = 10143;
const BLACK_DRAGON_SCALE_MAIL = 10144;
const BLUE_DRAGON_SCALE_MAIL = 10145;
const GREEN_DRAGON_SCALE_MAIL = 10146;
const YELLOW_DRAGON_SCALE_MAIL = 10147;
const GRAY_DRAGON_SCALES = 10148;
const GOLD_DRAGON_SCALES = 10149;
const SILVER_DRAGON_SCALES = 10150;
const RED_DRAGON_SCALES = 10151;
const WHITE_DRAGON_SCALES = 10152;
const ORANGE_DRAGON_SCALES = 10153;
const BLACK_DRAGON_SCALES = 10154;
const BLUE_DRAGON_SCALES = 10155;
const GREEN_DRAGON_SCALES = 10156;
const YELLOW_DRAGON_SCALES = 10157;
const FOOD_RATION = 143;
const LEATHER_GLOVES = 10050;
const LEATHER_CLOAK = 10051;
const ROBE = 10063;
const CLOAK_OF_PROTECTION = 10064;
const CLOAK_OF_MAGIC_RESISTANCE = 10065;
const DWARVISH_CLOAK = 10080;
const ELVEN_CLOAK = 10110;
const CLOAK_OF_DISPLACEMENT = 10111;
const GAUNTLETS_OF_POWER = 10112;
const GAUNTLETS_OF_FUMBLING = 10114;
const GAUNTLETS_OF_DEXTERITY = 10115;
const CLOAK_OF_INVISIBILITY = 10201;
const MUMMY_WRAPPING = 10202;
const ORCISH_CLOAK = 10203;
const OILSKIN_CLOAK = 10204;
const ALCHEMY_SMOCK = 10205;
const BAG_OBJECT_TYPES = new Set([SACK, OILSKIN_SACK, BAG_OF_HOLDING]);
const PICKUP_SECTION_ORDER = [
    'Coins', 'Amulets', 'Weapons', 'Armor', 'Comestibles', 'Scrolls',
    'Spellbooks', 'Potions', 'Rings', 'Wands', 'Tools', 'Gems/Stones',
    'Boulders/Statues', 'Other Items',
];
const BAG_PUT_CLASS_TYPES = [
    { key: 'coin', label: 'Coins', symbol: '$' },
    { key: 'amulet', label: 'Amulets', symbol: '"' },
    { key: 'weapon', label: 'Weapons', symbol: ')' },
    { key: 'armor', label: 'Armor', symbol: '[' },
    { key: 'food', label: 'Comestibles', symbol: '%' },
    { key: 'scroll', label: 'Scrolls', symbol: '?' },
    { key: 'spellbook', label: 'Spellbooks', symbol: '+' },
    { key: 'potion', label: 'Potions', symbol: '!' },
    { key: 'ring', label: 'Rings', symbol: '=' },
    { key: 'wand', label: 'Wands', symbol: '/' },
    { key: 'tool', label: 'Tools', symbol: '(' },
    { key: 'gem', label: 'Gems/Stones', symbol: '*' },
    { key: 'rock', label: 'Boulders/Statues', symbol: '`' },
    { key: 'ball', label: 'Iron balls', symbol: '0' },
    { key: 'chain', label: 'Chains', symbol: '_' },
];
const INVENTORY_LETTERS = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';
const APPLY_WEAPON_NAME_RE = /pick-axe|mattock|\baxe\b|bullwhip|lance|polearm|poleaxe|bardiche|bec de corbin|bill-guisarme|fauchard|glaive|guisarme|halberd|lucern hammer|partisan|ranseur|spetum|voulge|snickersnee/;
const POLEARM_NAME_RE = /\blance\b|polearm|poleaxe|bardiche|bec de corbin|bill-guisarme|fauchard|glaive|guisarme|halberd|lucern hammer|partisan|ranseur|spetum|voulge|snickersnee/;
const PICK_DIG_NAME_RE = /(?:^|\b)(?:pick-axe|pick axe|pickaxe|pick-ax|pickax|dwarvish mattock|mattock)(?:\b|$)/;
const WISH_LOCK_QUALIFIER_RE = /^(locked|unlocked|broken|open|closed|doorless)\s+/i;
const WISH_PROOF_QUALIFIER_RE = /^(rustproof|erodeproof|corrodeproof|fixed|fireproof|rotproof|tempered|crackproof)\s+/i;
const WISH_PRIMARY_EROSION_RE = /^(rusty|rusted|burnt|burned|cracked)\s+/i;
const WISH_SECONDARY_EROSION_RE = /^(corroded|rotted)\s+/i;
const TIN_VARIETY_TEXTS = [
    'rotten', 'homemade', 'soup made from', 'french fried', 'pickled',
    'boiled', 'smoked', 'dried', 'deep fried', 'szechuan', 'broiled',
    'stir fried', 'sauteed', 'candied', 'pureed',
];
const TIN_VARIETY_NUTRITION = [-50, 50, 20, 40, 40, 50, 50, 55, 60, 70, 80, 80, 95, 100, 500];
const TIN_GREASY_VARIETIES = new Set([3, 8, 11]);
const ROTTEN_TIN = 0;
const HOMEMADE_TIN = 1;
const SPINACH_TIN = -1;
const FOOD_NUTRITION = new Map([
    ['tripe ration', 200],
    ['tripe', 200],
    ['egg', 80],
    ['meatball', 5],
    ['meat stick', 5],
    ['meat ring', 5],
    ['enormous meatball', 2000],
    ['kelp frond', 30],
    ['eucalyptus leaf', 1],
    ['apple', 50],
    ['orange', 80],
    ['pear', 50],
    ['melon', 100],
    ['banana', 80],
    ['carrot', 50],
    ['sprig of wolfsbane', 40],
    ['clove of garlic', 40],
    ['slime mold', 250],
    ['lump of royal jelly', 200],
    ['cream pie', 100],
    ['candy bar', 100],
    ['fortune cookie', 40],
    ['pancake', 200],
    ['lembas wafer', 800],
    ['cram ration', 600],
    ['food ration', 800],
    ['k-ration', 400],
    ['c-ration', 300],
    ['tin', 0],
]);
const CARRIED_DELAYED_FOOD_VICTUALS = new Map([
    ['tripe ration', { delay: 2, finishName: 'tripe ration' }],
    ['tripe', { delay: 2, finishName: 'tripe ration' }],
    ['enormous meatball', { otyp: ENORMOUS_MEATBALL, delay: 20, finishName: 'enormous meatball' }],
    ['pancake', { otyp: PANCAKE, delay: 2, finishName: 'pancake' }],
    ['lembas wafer', { otyp: LEMBAS_WAFER, delay: 2, finishName: 'lembas wafer' }],
    ['cram ration', { otyp: CRAM_RATION, delay: 3, finishName: 'cram ration', bland: true }],
    ['food ration', { otyp: FOOD_RATION, delay: 5, finishName: 'food ration', rationFeedback: true }],
]);
const DELAY_ONE_FOOD_VICTUALS = new Map([
    ['meatball', { otyp: MEATBALL, delay: 1, finishName: 'meatball' }],
    ['meat stick', { otyp: MEAT_STICK, delay: 1, finishName: 'meat stick' }],
    ['kelp frond', { otyp: KELP_FROND, delay: 1, finishName: 'kelp frond' }],
    ['eucalyptus leaf', { otyp: EUCALYPTUS_LEAF, delay: 1, finishName: 'eucalyptus leaf' }],
    ['apple', { otyp: APPLE, delay: 1, finishName: 'apple' }],
    ['orange', { delay: 1, finishName: 'orange' }],
    ['pear', { delay: 1, finishName: 'pear' }],
    ['melon', { delay: 1, finishName: 'melon' }],
    ['banana', { delay: 1, finishName: 'banana' }],
    ['carrot', { delay: 1, finishName: 'carrot' }],
    ['meat ring', { otyp: MEAT_RING, delay: 1, finishName: 'meat ring' }],
    ['lump of royal jelly', { otyp: LUMP_OF_ROYAL_JELLY, delay: 1, finishName: 'lump of royal jelly' }],
    ['sprig of wolfsbane', { delay: 1, finishName: 'sprig of wolfsbane' }],
    ['clove of garlic', { delay: 1, finishName: 'clove of garlic' }],
    ['slime mold', { otyp: SLIME_MOLD, delay: 1, finishName: 'slime mold' }],
    ['cream pie', { otyp: CREAM_PIE, delay: 1, finishName: 'cream pie' }],
    ['candy bar', { delay: 1, finishName: 'candy bar' }],
    ['fortune cookie', { otyp: FORTUNE_COOKIE, delay: 1, finishName: 'fortune cookie' }],
    ['k-ration', { otyp: K_RATION, delay: 1, finishName: 'K-ration', bland: true }],
    ['c-ration', { otyp: C_RATION, delay: 1, finishName: 'C-ration', bland: true }],
]);
const ROTTABLE_NON_CORPSE_FOODS = new Set(['apple', 'carrot', 'pear', 'melon', 'orange', 'banana', 'kelp frond', 'lump of royal jelly']);
const POISONABLE_WISH_WEAPONS = new Set([
    'arrow', 'arrows', 'elven arrow', 'elven arrows', 'orcish arrow', 'orcish arrows',
    'silver arrow', 'silver arrows', 'ya', 'crossbow bolt', 'crossbow bolts',
    'dart', 'darts', 'shuriken', 'throwing star', 'throwing stars',
]);
const DRAGON_ARMOR_SPECS = [
    { colorName: 'gray', aliases: ['gray', 'grey'], color: CLR_GRAY, mailOtyp: GRAY_DRAGON_SCALE_MAIL, scalesOtyp: GRAY_DRAGON_SCALES, properties: ['antimagic'] },
    { colorName: 'gold', aliases: ['gold'], color: CLR_YELLOW, mailOtyp: GOLD_DRAGON_SCALE_MAIL, scalesOtyp: GOLD_DRAGON_SCALES, properties: ['hallucination'] },
    { colorName: 'silver', aliases: ['silver'], color: CLR_BRIGHT_CYAN, mailOtyp: SILVER_DRAGON_SCALE_MAIL, scalesOtyp: SILVER_DRAGON_SCALES, properties: ['reflection'] },
    { colorName: 'red', aliases: ['red'], color: CLR_RED, mailOtyp: RED_DRAGON_SCALE_MAIL, scalesOtyp: RED_DRAGON_SCALES, properties: ['fire', 'infravision'] },
    { colorName: 'white', aliases: ['white'], color: CLR_WHITE, mailOtyp: WHITE_DRAGON_SCALE_MAIL, scalesOtyp: WHITE_DRAGON_SCALES, properties: ['cold', 'slowDigestion'] },
    { colorName: 'orange', aliases: ['orange'], color: CLR_ORANGE, mailOtyp: ORANGE_DRAGON_SCALE_MAIL, scalesOtyp: ORANGE_DRAGON_SCALES, properties: ['sleep', 'freeAction'] },
    { colorName: 'black', aliases: ['black'], color: CLR_BLACK, mailOtyp: BLACK_DRAGON_SCALE_MAIL, scalesOtyp: BLACK_DRAGON_SCALES, properties: ['disintegration', 'drainResistance'] },
    { colorName: 'blue', aliases: ['blue'], color: CLR_BLUE, mailOtyp: BLUE_DRAGON_SCALE_MAIL, scalesOtyp: BLUE_DRAGON_SCALES, properties: ['shock', 'speed'] },
    { colorName: 'green', aliases: ['green'], color: CLR_GREEN, mailOtyp: GREEN_DRAGON_SCALE_MAIL, scalesOtyp: GREEN_DRAGON_SCALES, properties: ['poison', 'sickness'] },
    { colorName: 'yellow', aliases: ['yellow'], color: CLR_YELLOW, mailOtyp: YELLOW_DRAGON_SCALE_MAIL, scalesOtyp: YELLOW_DRAGON_SCALES, properties: ['acid', 'stone'] },
];
const DRAGON_ARMOR_BY_COLOR = new Map();
for (const spec of DRAGON_ARMOR_SPECS)
    for (const alias of spec.aliases) DRAGON_ARMOR_BY_COLOR.set(alias, spec);
const DRAGON_ARMOR_SPEC_BY_KIND = new Map();
for (const spec of DRAGON_ARMOR_SPECS) {
    DRAGON_ARMOR_SPEC_BY_KIND.set(`${spec.colorName} dragon scale mail`, { ...spec, armorKind: 'mail' });
    DRAGON_ARMOR_SPEC_BY_KIND.set(`${spec.colorName} dragon scales`, { ...spec, armorKind: 'scales' });
}

const WISH_BASE_OBJECTS = new Map([
    ['dart', { otyp: DART, cls: 'weapon', glyph: ')', kind: 'dart', plural: 'darts' }],
    ['darts', { otyp: DART, cls: 'weapon', glyph: ')', kind: 'dart', plural: 'darts' }],
    ['dagger', { otyp: DAGGER, cls: 'weapon', glyph: ')', kind: 'dagger', plural: 'daggers' }],
    ['daggers', { otyp: DAGGER, cls: 'weapon', glyph: ')', kind: 'dagger', plural: 'daggers' }],
    ['knife', { otyp: KNIFE, cls: 'weapon', glyph: ')', kind: 'knife', actualKind: 'knife' }],
    ['short sword', { otyp: SHORT_SWORD, cls: 'weapon', glyph: ')', kind: 'short sword', actualKind: 'short sword', owt: 30 }],
    ['elven short sword', { otyp: ELVEN_SHORT_SWORD, cls: 'weapon', glyph: ')', kind: 'runed short sword', actualKind: 'elven short sword', known: false, owt: 30 }],
    ['orcish short sword', { otyp: ORCISH_SHORT_SWORD, cls: 'weapon', glyph: ')', kind: 'crude short sword', actualKind: 'orcish short sword', known: false, owt: 30 }],
    ['dwarvish short sword', { otyp: DWARVISH_SHORT_SWORD, cls: 'weapon', glyph: ')', kind: 'broad short sword', actualKind: 'dwarvish short sword', known: false, owt: 30 }],
    ['scimitar', { otyp: SCIMITAR, cls: 'weapon', glyph: ')', kind: 'curved sword', actualKind: 'scimitar', known: false, owt: 40 }],
    ['broadsword', { otyp: BROADSWORD, cls: 'weapon', glyph: ')', kind: 'broadsword', actualKind: 'broadsword', owt: 70 }],
    ['elven broadsword', { otyp: ELVEN_BROADSWORD, cls: 'weapon', glyph: ')', kind: 'runed broadsword', actualKind: 'elven broadsword', known: false, owt: 70 }],
    ['long sword', { otyp: LONG_SWORD, cls: 'weapon', glyph: ')', kind: 'long sword', actualKind: 'long sword', owt: 40 }],
    ['two-handed sword', { otyp: TWO_HANDED_SWORD, cls: 'weapon', glyph: ')', kind: 'two-handed sword', actualKind: 'two-handed sword', owt: 150 }],
    ['katana', { otyp: KATANA, cls: 'weapon', glyph: ')', kind: 'samurai sword', actualKind: 'katana', known: false, owt: 40 }],
    ['flail', { otyp: FLAIL, cls: 'weapon', glyph: ')', kind: 'flail', actualKind: 'flail' }],
    ['partisan', { otyp: PARTISAN, cls: 'weapon', glyph: ')', kind: 'vulgar polearm', actualKind: 'partisan', known: false, owt: 80 }],
    ['ranseur', { otyp: RANSEUR, cls: 'weapon', glyph: ')', kind: 'hilted polearm', actualKind: 'ranseur', known: false, owt: 50 }],
    ['spetum', { otyp: SPETUM, cls: 'weapon', glyph: ')', kind: 'forked polearm', actualKind: 'spetum', known: false, owt: 50 }],
    ['glaive', { otyp: GLAIVE, cls: 'weapon', glyph: ')', kind: 'single-edged polearm', actualKind: 'glaive', known: false, owt: 75 }],
    ['halberd', { otyp: HALBERD, cls: 'weapon', glyph: ')', kind: 'angled poleaxe', actualKind: 'halberd', known: false, owt: 150 }],
    ['bardiche', { otyp: BARDICHE, cls: 'weapon', glyph: ')', kind: 'long poleaxe', actualKind: 'bardiche', known: false, owt: 120 }],
    ['voulge', { otyp: VOULGE, cls: 'weapon', glyph: ')', kind: 'pole cleaver', actualKind: 'voulge', known: false, owt: 125 }],
    ['fauchard', { otyp: FAUCHARD, cls: 'weapon', glyph: ')', kind: 'pole sickle', actualKind: 'fauchard', known: false, owt: 60 }],
    ['guisarme', { otyp: GUISARME, cls: 'weapon', glyph: ')', kind: 'pruning hook', actualKind: 'guisarme', known: false, owt: 80 }],
    ['bill-guisarme', { otyp: BILL_GUISARME, cls: 'weapon', glyph: ')', kind: 'hooked polearm', actualKind: 'bill-guisarme', known: false, owt: 120 }],
    ['lucern hammer', { otyp: LUCERN_HAMMER, cls: 'weapon', glyph: ')', kind: 'pronged polearm', actualKind: 'lucern hammer', known: false, owt: 150 }],
    ['bec de corbin', { otyp: BEC_DE_CORBIN, cls: 'weapon', glyph: ')', kind: 'beaked polearm', actualKind: 'bec de corbin', known: false, owt: 100 }],
    ['bullwhip', { otyp: BULLWHIP, cls: 'weapon', glyph: ')', kind: 'bullwhip', actualKind: 'bullwhip' }],
    ['silver saber', { otyp: SILVER_SABER, cls: 'weapon', glyph: ')', kind: 'silver saber', actualKind: 'silver saber' }],
    ['dwarvish mattock', { otyp: DWARVISH_MATTOCK, cls: 'weapon', glyph: ')', kind: 'dwarvish mattock', actualKind: 'dwarvish mattock' }],
    ['pick-axe', { otyp: PICK_AXE, cls: 'tool', glyph: '(', kind: 'pick-axe' }],
    ['pick axe', { otyp: PICK_AXE, cls: 'tool', glyph: '(', kind: 'pick-axe' }],
    ['pickaxe', { otyp: PICK_AXE, cls: 'tool', glyph: '(', kind: 'pick-axe' }],
    ['pickax', { otyp: PICK_AXE, cls: 'tool', glyph: '(', kind: 'pick-axe' }],
    ['pick-ax', { otyp: PICK_AXE, cls: 'tool', glyph: '(', kind: 'pick-axe' }],
    ['cream pie', { otyp: CREAM_PIE, cls: 'food', glyph: '%', kind: 'cream pie' }],
    ['lump of royal jelly', { otyp: LUMP_OF_ROYAL_JELLY, cls: 'food', glyph: '%', kind: 'lump of royal jelly', singular: 'lump of royal jelly', plural: 'lumps of royal jelly', nutrition: 200 }],
    ['lumps of royal jelly', { otyp: LUMP_OF_ROYAL_JELLY, cls: 'food', glyph: '%', kind: 'lump of royal jelly', singular: 'lump of royal jelly', plural: 'lumps of royal jelly', nutrition: 200 }],
    ['eucalyptus leaf', { otyp: EUCALYPTUS_LEAF, cls: 'food', glyph: '%', kind: 'eucalyptus leaf', plural: 'eucalyptus leaves' }],
    ['kelp frond', { otyp: KELP_FROND, cls: 'food', glyph: '%', kind: 'kelp frond', plural: 'kelp fronds', nutrition: 30 }],
    ['kelp fronds', { otyp: KELP_FROND, cls: 'food', glyph: '%', kind: 'kelp frond', plural: 'kelp fronds', nutrition: 30 }],
    ['pancake', { otyp: PANCAKE, cls: 'food', glyph: '%', kind: 'pancake', plural: 'pancakes', nutrition: 200 }],
    ['pancakes', { otyp: PANCAKE, cls: 'food', glyph: '%', kind: 'pancake', plural: 'pancakes', nutrition: 200 }],
    ['lembas wafer', { otyp: LEMBAS_WAFER, cls: 'food', glyph: '%', kind: 'lembas wafer', plural: 'lembas wafers' }],
    ['cram ration', { otyp: CRAM_RATION, cls: 'food', glyph: '%', kind: 'cram ration', plural: 'cram rations', nutrition: 600 }],
    ['cram rations', { otyp: CRAM_RATION, cls: 'food', glyph: '%', kind: 'cram ration', plural: 'cram rations', nutrition: 600 }],
    ['fortune cookie', { otyp: FORTUNE_COOKIE, cls: 'food', glyph: '%', kind: 'fortune cookie', plural: 'fortune cookies' }],
    ['fortune cookies', { otyp: FORTUNE_COOKIE, cls: 'food', glyph: '%', kind: 'fortune cookie', plural: 'fortune cookies' }],
    ['food ration', { otyp: FOOD_RATION, cls: 'food', glyph: '%', kind: 'food ration', plural: 'food rations', nutrition: 800 }],
    ['k-ration', { otyp: K_RATION, cls: 'food', glyph: '%', kind: 'K-ration', actualKind: 'K-ration', plural: 'K-rations', nutrition: 400 }],
    ['k-rations', { otyp: K_RATION, cls: 'food', glyph: '%', kind: 'K-ration', actualKind: 'K-ration', plural: 'K-rations', nutrition: 400 }],
    ['c-ration', { otyp: C_RATION, cls: 'food', glyph: '%', kind: 'C-ration', actualKind: 'C-ration', plural: 'C-rations', nutrition: 300 }],
    ['c-rations', { otyp: C_RATION, cls: 'food', glyph: '%', kind: 'C-ration', actualKind: 'C-ration', plural: 'C-rations', nutrition: 300 }],
    ['meatball', { otyp: MEATBALL, cls: 'food', glyph: '%', kind: 'meatball', actualKind: 'meatball', singular: 'meatball', plural: 'meatballs', nutrition: 5, owt: 1 }],
    ['meatballs', { otyp: MEATBALL, cls: 'food', glyph: '%', kind: 'meatball', actualKind: 'meatball', singular: 'meatball', plural: 'meatballs', nutrition: 5, owt: 1 }],
    ['meat stick', { otyp: MEAT_STICK, cls: 'food', glyph: '%', kind: 'meat stick', actualKind: 'meat stick', singular: 'meat stick', plural: 'meat sticks', nutrition: 5, owt: 1 }],
    ['meat sticks', { otyp: MEAT_STICK, cls: 'food', glyph: '%', kind: 'meat stick', actualKind: 'meat stick', singular: 'meat stick', plural: 'meat sticks', nutrition: 5, owt: 1 }],
    ['enormous meatball', { otyp: ENORMOUS_MEATBALL, cls: 'food', glyph: '%', kind: 'enormous meatball', actualKind: 'enormous meatball', singular: 'enormous meatball', plural: 'enormous meatballs', nutrition: 2000, owt: 400 }],
    ['enormous meatballs', { otyp: ENORMOUS_MEATBALL, cls: 'food', glyph: '%', kind: 'enormous meatball', actualKind: 'enormous meatball', singular: 'enormous meatball', plural: 'enormous meatballs', nutrition: 2000, owt: 400 }],
    ['rock', { otyp: ROCK, cls: 'gem', glyph: '*', kind: 'rock', actualKind: 'rock', plural: 'rocks', gemDescription: 'rock' }],
    ['luckstone', { otyp: LUCKSTONE, cls: 'gem', glyph: '*', kind: 'luckstone', actualKind: 'luckstone', gemDescription: 'gray stone' }],
    ['loadstone', { otyp: LOADSTONE, cls: 'gem', glyph: '*', kind: 'loadstone', actualKind: 'loadstone', gemDescription: 'gray stone' }],
    ['touchstone', { otyp: TOUCHSTONE, cls: 'gem', glyph: '*', kind: 'touchstone', actualKind: 'touchstone', gemDescription: 'gray stone' }],
    ['flint', { otyp: FLINT, cls: 'gem', glyph: '*', kind: 'flint', actualKind: 'flint', gemDescription: 'gray stone' }],
    ['heavy iron ball', { otyp: HEAVY_IRON_BALL, cls: 'ball', glyph: '0', kind: 'heavy iron ball', actualKind: 'heavy iron ball' }],
    ['egg', { otyp: EGG, cls: 'food', glyph: '%', kind: 'egg', plural: 'eggs' }],
    ['eggs', { otyp: EGG, cls: 'food', glyph: '%', kind: 'egg', plural: 'eggs' }],
    ['large box', { otyp: LARGE_BOX, cls: 'tool', glyph: '(', kind: 'large box' }],
    ['chest', { otyp: CHEST, cls: 'tool', glyph: '(', kind: 'chest' }],
    ['ice box', { otyp: ICE_BOX, cls: 'tool', glyph: '(', kind: 'ice box' }],
    ['sack', { otyp: SACK, cls: 'tool', glyph: '(', kind: 'sack' }],
    ['oilskin sack', { otyp: OILSKIN_SACK, cls: 'tool', glyph: '(', kind: 'oilskin sack' }],
    ['bag of holding', { otyp: BAG_OF_HOLDING, cls: 'tool', glyph: '(', kind: 'bag of holding' }],
    ['brass lantern', { otyp: BRASS_LANTERN, cls: 'tool', glyph: '(', kind: 'brass lantern', actualKind: 'brass lantern' }],
    ['oil lamp', { otyp: OIL_LAMP, cls: 'tool', glyph: '(', kind: 'oil lamp' }],
    ['magic lamp', { otyp: MAGIC_LAMP, cls: 'tool', glyph: '(', kind: 'magic lamp' }],
    ['tallow candle', { otyp: TALLOW_CANDLE, cls: 'tool', glyph: '(', kind: 'tallow candle', plural: 'tallow candles', age: 200 }],
    ['tallow candles', { otyp: TALLOW_CANDLE, cls: 'tool', glyph: '(', kind: 'tallow candle', plural: 'tallow candles', age: 200 }],
    ['wax candle', { otyp: WAX_CANDLE, cls: 'tool', glyph: '(', kind: 'wax candle', plural: 'wax candles', age: 400 }],
    ['wax candles', { otyp: WAX_CANDLE, cls: 'tool', glyph: '(', kind: 'wax candle', plural: 'wax candles', age: 400 }],
    ['stethoscope', { otyp: STETHOSCOPE, cls: 'tool', glyph: '(', kind: 'stethoscope' }],
    ['magic marker', { otyp: MAGIC_MARKER, cls: 'tool', glyph: '(', kind: 'magic marker', actualKind: 'magic marker', plural: 'magic markers' }],
    ['lock pick', { otyp: LOCK_PICK, cls: 'tool', glyph: '(', kind: 'lock pick', actualKind: 'lock pick' }],
    ['wooden harp', { otyp: WOODEN_HARP, cls: 'tool', glyph: '(', kind: 'harp', actualKind: 'wooden harp', known: false }],
    ['magic flute', { otyp: MAGIC_FLUTE, cls: 'tool', glyph: '(', kind: 'flute', actualKind: 'magic flute', known: false, plural: 'magic flutes' }],
    ['frost horn', { otyp: FROST_HORN, cls: 'tool', glyph: '(', kind: 'horn', actualKind: 'frost horn', known: false, plural: 'frost horns' }],
    ['fire horn', { otyp: FIRE_HORN, cls: 'tool', glyph: '(', kind: 'horn', actualKind: 'fire horn', known: false, plural: 'fire horns' }],
    ['horn of plenty', { otyp: HORN_OF_PLENTY, cls: 'tool', glyph: '(', kind: 'horn', actualKind: 'horn of plenty', known: false, plural: 'horns of plenty' }],
    ['magic harp', { otyp: MAGIC_HARP, cls: 'tool', glyph: '(', kind: 'harp', actualKind: 'magic harp', known: false, plural: 'magic harps' }],
    ['drum of earthquake', { otyp: DRUM_OF_EARTHQUAKE, cls: 'tool', glyph: '(', kind: 'drum', actualKind: 'drum of earthquake', known: false, plural: 'drums of earthquake' }],
    ['mirror', { otyp: MIRROR, cls: 'tool', glyph: '(', kind: 'looking glass', actualKind: 'mirror' }],
    ['expensive camera', { otyp: EXPENSIVE_CAMERA, cls: 'tool', glyph: '(', kind: 'expensive camera', actualKind: 'expensive camera', plural: 'expensive cameras' }],
    ['tinning kit', { otyp: TINNING_KIT, cls: 'tool', glyph: '(', kind: 'tinning kit', actualKind: 'tinning kit', plural: 'tinning kits' }],
    ['can of grease', { otyp: CAN_OF_GREASE, cls: 'tool', glyph: '(', kind: 'can of grease', actualKind: 'can of grease', plural: 'cans of grease' }],
    ['blindfold', { otyp: BLINDFOLD, cls: 'tool', glyph: '(', kind: 'blindfold' }],
    ['bell of opening', { otyp: BELL, cls: 'tool', glyph: '(', kind: 'silver bell', actualKind: 'bell of opening', known: false, spe: 3 }],
    ['meat ring', { otyp: MEAT_RING, cls: 'food', glyph: '%', kind: 'meat ring', actualKind: 'meat ring', singular: 'meat ring', plural: 'meat rings', nutrition: 5, quan: 1 }],
    ['tin opener', { otyp: TIN_OPENER, cls: 'tool', glyph: '(', kind: 'tin opener', actualKind: 'tin opener' }],
    ['beartrap', { otyp: BEARTRAP, cls: 'tool', glyph: '(', kind: 'beartrap', actualKind: 'beartrap' }],
    ['land mine', { otyp: LAND_MINE, cls: 'tool', glyph: '(', kind: 'land mine', actualKind: 'land mine' }],
    ['bag of tricks', { otyp: BAG_OF_TRICKS, cls: 'tool', glyph: '(', kind: 'bag of tricks', actualKind: 'bag of tricks' }],
    ['crystal ball', { otyp: CRYSTAL_BALL, cls: 'tool', glyph: '(', kind: 'crystal ball', actualKind: 'crystal ball', plural: 'crystal balls', known: true }],
    ['glass orb', { otyp: CRYSTAL_BALL, cls: 'tool', glyph: '(', kind: 'glass orb', actualKind: 'crystal ball', plural: 'glass orbs', known: false }],
    ['tooled horn', { otyp: TOOLED_HORN, cls: 'tool', glyph: '(', kind: 'horn', actualKind: 'tooled horn', known: false }],
    ['grappling hook', { otyp: GRAPPLING_HOOK, cls: 'tool', glyph: '(', kind: 'grappling hook', actualKind: 'grappling hook' }],
    ['plate mail', { otyp: PLATE_MAIL, cls: 'armor', glyph: '[', kind: 'plate mail', actualKind: 'plate mail' }],
    ['helmet', { otyp: HELMET, cls: 'armor', glyph: '[', kind: 'helmet', actualKind: 'helmet', appearance: 'plumed helmet', known: false, owt: 30 }],
    ['elven leather helm', { otyp: ELVEN_LEATHER_HELM, cls: 'armor', glyph: '[', kind: 'elven leather helm', actualKind: 'elven leather helm', appearance: 'leather hat', known: false, owt: 3 }],
    ['orcish helm', { otyp: ORCISH_HELM, cls: 'armor', glyph: '[', kind: 'orcish helm', actualKind: 'orcish helm', appearance: 'iron skull cap', known: false, owt: 30 }],
    ['dwarvish iron helm', { otyp: DWARVISH_IRON_HELM, cls: 'armor', glyph: '[', kind: 'dwarvish iron helm', actualKind: 'dwarvish iron helm', appearance: 'hard hat', known: false, owt: 40 }],
    ['fedora', { otyp: FEDORA, cls: 'armor', glyph: '[', kind: 'fedora', actualKind: 'fedora', owt: 3 }],
    ['cornuthaum', { otyp: CORNUTHAUM, cls: 'armor', glyph: '[', kind: 'cornuthaum', actualKind: 'cornuthaum', appearance: 'conical hat', known: false, owt: 4 }],
    ['dunce cap', { otyp: DUNCE_CAP, cls: 'armor', glyph: '[', kind: 'dunce cap', actualKind: 'dunce cap', appearance: 'conical hat', known: false, owt: 4 }],
    ['dented pot', { otyp: DENTED_POT, cls: 'armor', glyph: '[', kind: 'dented pot', actualKind: 'dented pot', owt: 10 }],
    ['helm of brilliance', { otyp: HELM_OF_BRILLIANCE, cls: 'armor', glyph: '[', kind: 'helm of brilliance', actualKind: 'helm of brilliance', appearance: 'crystal helmet', known: false, owt: 40 }],
    ['helm of caution', { otyp: HELM_OF_CAUTION, cls: 'armor', glyph: '[', kind: 'helm of caution', actualKind: 'helm of caution', appearance: 'etched helmet', known: false, owt: 50 }],
    ['helm of opposite alignment', { otyp: HELM_OF_OPPOSITE_ALIGNMENT, cls: 'armor', glyph: '[', kind: 'helm of opposite alignment', actualKind: 'helm of opposite alignment', appearance: 'crested helmet', known: false, owt: 50 }],
    ['helm of telepathy', { otyp: HELM_OF_TELEPATHY, cls: 'armor', glyph: '[', kind: 'helm of telepathy', actualKind: 'helm of telepathy', appearance: 'visored helmet', known: false, owt: 50 }],
    ['ring mail', { otyp: RING_MAIL, cls: 'armor', glyph: '[', kind: 'ring mail', actualKind: 'ring mail' }],
    ['studded leather armor', { otyp: STUDDED_LEATHER_ARMOR, cls: 'armor', glyph: '[', kind: 'studded leather armor', actualKind: 'studded leather armor' }],
    ['leather armor', { otyp: LEATHER_ARMOR, cls: 'armor', glyph: '[', kind: 'leather armor', actualKind: 'leather armor' }],
    ['elven mithril-coat', { otyp: ELVEN_MITHRIL_COAT, cls: 'armor', glyph: '[', kind: 'elven mithril-coat', actualKind: 'elven mithril-coat' }],
    ['small shield', { otyp: SMALL_SHIELD, cls: 'armor', glyph: '[', kind: 'small shield', actualKind: 'small shield', appearance: 'wooden shield', known: false, owt: 30 }],
    ['shield of drain resistance', { otyp: SHIELD_OF_DRAIN_RESISTANCE, cls: 'armor', glyph: '[', kind: 'shield of drain resistance', actualKind: 'shield of drain resistance', appearance: 'wooden shield', known: false, owt: 30 }],
    ['shield of shock resistance', { otyp: SHIELD_OF_SHOCK_RESISTANCE, cls: 'armor', glyph: '[', kind: 'shield of shock resistance', actualKind: 'shield of shock resistance', appearance: 'wooden shield', known: false, owt: 30 }],
    ['elven shield', { otyp: ELVEN_SHIELD, cls: 'armor', glyph: '[', kind: 'elven shield', actualKind: 'elven shield', appearance: 'blue and green shield', known: false, owt: 40 }],
    ['uruk-hai shield', { otyp: URUK_HAI_SHIELD, cls: 'armor', glyph: '[', kind: 'uruk-hai shield', actualKind: 'uruk-hai shield', appearance: 'white-handed shield', known: false, owt: 50 }],
    ['orcish shield', { otyp: ORCISH_SHIELD, cls: 'armor', glyph: '[', kind: 'orcish shield', actualKind: 'orcish shield', appearance: 'red-eyed shield', known: false, owt: 50 }],
    ['large shield', { otyp: LARGE_SHIELD, cls: 'armor', glyph: '[', kind: 'large shield', actualKind: 'large shield', owt: 100 }],
    ['dwarvish roundshield', { otyp: DWARVISH_ROUNDSHIELD, cls: 'armor', glyph: '[', kind: 'dwarvish roundshield', actualKind: 'dwarvish roundshield', appearance: 'large round shield', known: false, owt: 100 }],
    ['shield of reflection', { otyp: SHIELD_OF_REFLECTION, cls: 'armor', glyph: '[', kind: 'shield of reflection', actualKind: 'shield of reflection', appearance: 'polished silver shield', known: false, owt: 50 }],
    ['leather gloves', { otyp: LEATHER_GLOVES, cls: 'armor', glyph: '[', kind: 'leather gloves', actualKind: 'leather gloves', known: false }],
    ['gauntlets of fumbling', { otyp: GAUNTLETS_OF_FUMBLING, cls: 'armor', glyph: '[', kind: 'gauntlets of fumbling', actualKind: 'gauntlets of fumbling', known: false, owt: 10 }],
    ['gauntlets of power', { otyp: GAUNTLETS_OF_POWER, cls: 'armor', glyph: '[', kind: 'gauntlets of power', actualKind: 'gauntlets of power', known: false }],
    ['gauntlets of dexterity', { otyp: GAUNTLETS_OF_DEXTERITY, cls: 'armor', glyph: '[', kind: 'gauntlets of dexterity', actualKind: 'gauntlets of dexterity', known: false, owt: 10 }],
    ['mummy wrapping', { otyp: MUMMY_WRAPPING, cls: 'armor', glyph: '[', kind: 'mummy wrapping', actualKind: 'mummy wrapping', known: false, owt: 3 }],
    ['elven cloak', { otyp: ELVEN_CLOAK, cls: 'armor', glyph: '[', kind: 'elven cloak', actualKind: 'elven cloak', known: false, owt: 10 }],
    ['orcish cloak', { otyp: ORCISH_CLOAK, cls: 'armor', glyph: '[', kind: 'orcish cloak', actualKind: 'orcish cloak', known: false, owt: 10 }],
    ['dwarvish cloak', { otyp: DWARVISH_CLOAK, cls: 'armor', glyph: '[', kind: 'dwarvish cloak', actualKind: 'dwarvish cloak', known: false, owt: 10 }],
    ['oilskin cloak', { otyp: OILSKIN_CLOAK, cls: 'armor', glyph: '[', kind: 'oilskin cloak', actualKind: 'oilskin cloak', known: false, owt: 10 }],
    ['robe', { otyp: ROBE, cls: 'armor', glyph: '[', kind: 'robe', actualKind: 'robe', known: false, owt: 15 }],
    ['alchemy smock', { otyp: ALCHEMY_SMOCK, cls: 'armor', glyph: '[', kind: 'alchemy smock', actualKind: 'alchemy smock', known: false, owt: 10 }],
    ['leather cloak', { otyp: LEATHER_CLOAK, cls: 'armor', glyph: '[', kind: 'leather cloak', actualKind: 'leather cloak', known: false, owt: 15 }],
    ['cloak of protection', { otyp: CLOAK_OF_PROTECTION, cls: 'armor', glyph: '[', kind: 'cloak of protection', actualKind: 'cloak of protection', known: false, owt: 10 }],
    ['cloak of invisibility', { otyp: CLOAK_OF_INVISIBILITY, cls: 'armor', glyph: '[', kind: 'cloak of invisibility', actualKind: 'cloak of invisibility', known: false, owt: 10 }],
    ['cloak of magic resistance', { otyp: CLOAK_OF_MAGIC_RESISTANCE, cls: 'armor', glyph: '[', kind: 'cloak of magic resistance', actualKind: 'cloak of magic resistance', known: false, owt: 10 }],
    ['cloak of displacement', { otyp: CLOAK_OF_DISPLACEMENT, cls: 'armor', glyph: '[', kind: 'cloak of displacement', actualKind: 'cloak of displacement', known: false }],
    ['low boots', { otyp: LOW_BOOTS, cls: 'armor', glyph: '[', kind: 'low boots', actualKind: 'low boots', appearance: 'walking shoes', known: false, owt: 10 }],
    ['iron shoes', { otyp: IRON_SHOES, cls: 'armor', glyph: '[', kind: 'iron shoes', actualKind: 'iron shoes', appearance: 'hard shoes', known: false, owt: 50 }],
    ['high boots', { otyp: HIGH_BOOTS, cls: 'armor', glyph: '[', kind: 'high boots', actualKind: 'high boots', appearance: 'jackboots', known: false, owt: 20 }],
    ['hawaiian shirt', { otyp: HAWAIIAN_SHIRT, cls: 'armor', glyph: '[', kind: 'Hawaiian shirt', actualKind: 'Hawaiian shirt', plural: 'Hawaiian shirts', owt: 5 }],
    ['t-shirt', { otyp: T_SHIRT, cls: 'armor', glyph: '[', kind: 'T-shirt', actualKind: 'T-shirt', plural: 'T-shirts', owt: 5 }],
    ['speed boots', { otyp: SPEED_BOOTS, cls: 'armor', glyph: '[', kind: 'speed boots', actualKind: 'speed boots', known: false, owt: 20 }],
    ['water walking boots', { otyp: WATER_WALKING_BOOTS, cls: 'armor', glyph: '[', kind: 'water walking boots', actualKind: 'water walking boots', known: false, owt: 15 }],
    ['jumping boots', { otyp: JUMPING_BOOTS, cls: 'armor', glyph: '[', kind: 'jumping boots', actualKind: 'jumping boots', known: false, owt: 20 }],
    ['elven boots', { otyp: ELVEN_BOOTS, cls: 'armor', glyph: '[', kind: 'elven boots', actualKind: 'elven boots', known: false, owt: 15 }],
    ['kicking boots', { otyp: KICKING_BOOTS, cls: 'armor', glyph: '[', kind: 'kicking boots', actualKind: 'kicking boots', known: false, owt: 50 }],
    ['fumble boots', { otyp: FUMBLE_BOOTS, cls: 'armor', glyph: '[', kind: 'fumble boots', actualKind: 'fumble boots', known: false, owt: 20 }],
    ['levitation boots', { otyp: LEVITATION_BOOTS, cls: 'armor', glyph: '[', kind: 'levitation boots', actualKind: 'levitation boots', known: false, owt: 15 }],
]);
const WIZARD_ONLY_WISH_NAMEDESC_BOUNDS = new Map([
    ['bell of opening', 1],
    ['book of the dead', 1],
    ['papyrus', 1],
    ['candelabrum', 1],
    ['candelabrum of invocation', 1],
]);
const WISH_BASE_NAMEDESC_BOUNDS = new Map([
    ['dart', 61], ['darts', 61], ['dagger', 31], ['daggers', 31],
    ['knife', 21], ['short sword', 9], ['elven short sword', 3],
    ['orcish short sword', 4], ['dwarvish short sword', 3],
    ['scimitar', 16], ['broadsword', 9], ['elven broadsword', 5],
    ['long sword', 51], ['two-handed sword', 23], ['katana', 5],
    ['flail', 41], ['partisan', 6], ['ranseur', 6], ['spetum', 6], ['glaive', 9],
    ['halberd', 9], ['bardiche', 5], ['voulge', 5], ['fauchard', 7],
    ['guisarme', 7], ['bill-guisarme', 5], ['lucern hammer', 6],
    ['bec de corbin', 5],
    ['bullwhip', 3], ['silver saber', 7], ['dwarvish mattock', 14],
    ['pick-axe', 21], ['pick axe', 21], ['pickaxe', 21], ['pickax', 21], ['pick-ax', 21],
    ['cream pie', 26], ['lump of royal jelly', 1], ['lumps of royal jelly', 1], ['eucalyptus leaf', 4], ['kelp frond', 1], ['kelp fronds', 1],
    ['pancake', 26], ['pancakes', 26], ['lembas wafer', 21],
    ['cram ration', 21], ['cram rations', 21],
    ['fortune cookie', 56], ['fortune cookies', 56],
    ['food ration', 381],
    ['k-ration', 1], ['k-rations', 1], ['c-ration', 1], ['c-rations', 1],
    ['meatball', 1], ['meatballs', 1], ['meat stick', 1], ['meat sticks', 1], ['enormous meatball', 1], ['enormous meatballs', 1], ['rock', 101], ['luckstone', 11],
    ['loadstone', 11], ['touchstone', 9], ['flint', 11],
    ['heavy iron ball', 1001], ['large box', 41], ['chest', 36], ['ice box', 6],
    ['sack', 36], ['oilskin sack', 6], ['bag of holding', 21],
    ['brass lantern', 31], ['oil lamp', 46], ['magic lamp', 16],
    ['tallow candle', 21], ['tallow candles', 21],
    ['wax candle', 6], ['wax candles', 6], ['stethoscope', 26],
    ['magic marker', 16], ['lock pick', 61], ['wooden harp', 5],
    ['magic flute', 3], ['frost horn', 3], ['fire horn', 3],
    ['horn of plenty', 3], ['magic harp', 3], ['drum of earthquake', 3],
    ['mirror', 46], ['expensive camera', 16],
    ['tinning kit', 16], ['can of grease', 16],
    ['bell of opening', 1], ['blindfold', 51], ['leather gloves', 16],
    ['meat ring', 1], ['tin opener', 36], ['beartrap', 1],
    ['land mine', 1], ['bag of tricks', 21], ['tooled horn', 6],
    ['grappling hook', 6],
    ['plate mail', 41], ['helmet', 11],
    ['elven leather helm', 7], ['orcish helm', 7],
    ['dwarvish iron helm', 7],
    ['fedora', 1], ['cornuthaum', 6], ['dunce cap', 6],
    ['dented pot', 3], ['helm of brilliance', 7],
    ['helm of caution', 7], ['helm of opposite alignment', 11],
    ['helm of telepathy', 5],
    ['ring mail', 67], ['studded leather armor', 67],
    ['leather armor', 76], ['elven mithril-coat', 16],
    ['small shield', 7], ['shield of drain resistance', 13],
    ['shield of shock resistance', 13], ['elven shield', 3],
    ['uruk-hai shield', 3], ['orcish shield', 3],
    ['large shield', 5], ['dwarvish roundshield', 4],
    ['shield of reflection', 8],
    ['gauntlets of fumbling', 9], ['gauntlets of power', 9],
    ['gauntlets of dexterity', 9],
    ['mummy wrapping', 1], ['elven cloak', 9],
    ['orcish cloak', 9], ['dwarvish cloak', 9],
    ['oilskin cloak', 9], ['robe', 7],
    ['alchemy smock', 12], ['leather cloak', 9],
    ['cloak of protection', 12], ['cloak of invisibility', 13],
    ['cloak of magic resistance', 7], ['cloak of displacement', 13],
    ['low boots', 24], ['iron shoes', 8], ['high boots', 15],
    ['hawaiian shirt', 9], ['t-shirt', 3],
    ['speed boots', 13], ['water walking boots', 13],
    ['jumping boots', 13], ['elven boots', 13],
    ['kicking boots', 13], ['fumble boots', 13],
    ['levitation boots', 13],
    ['crystal ball', 495], ['glass orb', 495],
]);
const WISH_OBJECT_METADATA = new Map([
    ['magic flute', {
        otyp: MAGIC_FLUTE, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 5,
        unitCost: 36,
    }],
    ['frost horn', {
        otyp: FROST_HORN, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 18,
        unitCost: 50,
    }],
    ['fire horn', {
        otyp: FIRE_HORN, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 18,
        unitCost: 50,
    }],
    ['horn of plenty', {
        otyp: HORN_OF_PLENTY, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 18,
        unitCost: 50,
    }],
    ['magic harp', {
        otyp: MAGIC_HARP, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 30,
        unitCost: 50,
    }],
    ['drum of earthquake', {
        otyp: DRUM_OF_EARTHQUAKE, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 2, unitWeight: 25,
        unitCost: 25,
    }],
    ['expensive camera', {
        otyp: EXPENSIVE_CAMERA, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 15, unitWeight: 12,
        unitCost: 200,
    }],
    ['tinning kit', {
        otyp: TINNING_KIT, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 15, unitWeight: 100,
        unitCost: 30,
    }],
    ['can of grease', {
        otyp: CAN_OF_GREASE, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 15, unitWeight: 15,
        unitCost: 20,
    }],
    ['bag of tricks', {
        otyp: BAG_OF_TRICKS, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 20, unitWeight: 15,
        unitCost: 100, forceBagOfTricksTool: true,
    }],
    ['crystal ball', {
        otyp: CRYSTAL_BALL, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 15, unitWeight: 150,
        unitCost: 60, negativeSpeLikeWand: true,
    }],
    ['magic marker', {
        otyp: MAGIC_MARKER, cls: 'tool', glyph: '(', ocMerge: false,
        ocCharged: true, ocNowish: false, ocProb: 15, unitWeight: 2,
        unitCost: 50,
    }],
]);
const WISH_OBJECT_METADATA_ALIASES = new Map([
    ['glass orb', 'crystal ball'],
]);
const WISH_OBJECT_METADATA_BY_OTYP = new Map(
    [...WISH_OBJECT_METADATA.values()].map(metadata => [metadata.otyp, metadata])
);
const WISH_TOOL_ROLLS = new Map([
    ['lamp', 415], ['lenses', 510], ['blindfold', 560], ['towel', 610],
    ['saddle', 615], ['leash', 680], ['tin opener', 755],
    ['can of grease', 770], ['figurine', 795], ['tin whistle', 910],
    ['magic whistle', 940], ['wooden flute', 944], ['magic flute', 946],
    ['tooled horn', 951], ['frost horn', 953], ['fire horn', 955],
    ['horn of plenty', 957], ['harp', 961], ['wooden harp', 961],
    ['magic harp', 963], ['bell', 965], ['bugle', 969],
    ['leather drum', 973], ['drum of earthquake', 975],
]);
const WISH_TOOL_APPEARANCES = new Map([
    ['wooden flute', 'flute'], ['magic flute', 'flute'],
    ['tooled horn', 'horn'], ['frost horn', 'horn'], ['fire horn', 'horn'],
    ['horn of plenty', 'horn'], ['wooden harp', 'harp'],
    ['magic harp', 'harp'], ['drum of earthquake', 'drum'],
]);
const WISH_TOOL_OTYPES = new Map([
    ['horn of plenty', HORN_OF_PLENTY],
]);
const WISH_TOOL_NAMEDESC_BOUNDS = new Map([
    ['magic harp', 3], ['leash', 66], ['lenses', 6],
]);
const WISH_OBJECT_RANGES = new Map([
    ['bag', [
        ['sack', 35],
        ['oilskin sack', 5],
        ['bag of holding', 20],
        ['bag of tricks', 20],
    ]],
    ['lamp', [
        ['oil lamp', 45],
        ['magic lamp', 15],
    ]],
    ['candle', [
        ['tallow candle', 20],
        ['wax candle', 5],
    ]],
    ['horn', [
        ['tooled horn', 5],
        ['frost horn', 2],
        ['fire horn', 2],
        ['horn of plenty', 2],
    ]],
    ['shield', [
        ['small shield', 6],
        ['shield of drain resistance', 12],
        ['shield of shock resistance', 12],
        ['elven shield', 2],
        ['uruk-hai shield', 2],
        ['orcish shield', 2],
        ['large shield', 4],
        ['dwarvish roundshield', 3],
        ['shield of reflection', 7],
    ]],
    ['hat', [
        ['fedora', 0],
        ['cornuthaum', 5],
        ['dunce cap', 5],
    ]],
    ['helm', [
        ['elven leather helm', 6],
        ['orcish helm', 6],
        ['dwarvish iron helm', 6],
        ['fedora', 0],
        ['cornuthaum', 5],
        ['dunce cap', 5],
        ['dented pot', 2],
        ['helm of brilliance', 6],
        ['helmet', 10],
        ['helm of caution', 6],
        ['helm of opposite alignment', 10],
        ['helm of telepathy', 4],
    ]],
    ['gloves', [
        ['leather gloves', 15],
        ['gauntlets of fumbling', 8],
        ['gauntlets of power', 8],
        ['gauntlets of dexterity', 8],
    ]],
    ['gauntlets', [
        ['leather gloves', 15],
        ['gauntlets of fumbling', 8],
        ['gauntlets of power', 8],
        ['gauntlets of dexterity', 8],
    ]],
    ['cloak', [
        ['mummy wrapping', 0],
        ['elven cloak', 8],
        ['orcish cloak', 8],
        ['dwarvish cloak', 8],
        ['oilskin cloak', 8],
        ['robe', 6],
        ['alchemy smock', 11],
        ['leather cloak', 8],
        ['cloak of protection', 11],
        ['cloak of invisibility', 12],
        ['cloak of magic resistance', 6],
        ['cloak of displacement', 12],
    ]],
    ['shoes', [
        ['low boots', 23],
        ['iron shoes', 7],
    ]],
    ['boots', [
        ['low boots', 23],
        ['iron shoes', 7],
        ['high boots', 14],
        ['speed boots', 12],
        ['water walking boots', 12],
        ['jumping boots', 12],
        ['elven boots', 12],
        ['kicking boots', 12],
        ['fumble boots', 12],
        ['levitation boots', 12],
    ]],
    ['shirt', [
        ['hawaiian shirt', 8],
        ['t-shirt', 2],
    ]],
    ['sword', [
        ['short sword', 8],
        ['elven short sword', 2],
        ['orcish short sword', 3],
        ['dwarvish short sword', 2],
        ['scimitar', 15],
        ['silver saber', 6],
        ['broadsword', 8],
        ['elven broadsword', 4],
        ['long sword', 50],
        ['two-handed sword', 22],
        ['katana', 4],
    ]],
    ['polearm', [
        ['partisan', 0],
        ['ranseur', 0],
        ['spetum', 0],
        ['glaive', 0],
        ['halberd', 0],
        ['bardiche', 0],
        ['voulge', 0],
        ['fauchard', 0],
        ['guisarme', 0],
        ['bill-guisarme', 0],
        ['lucern hammer', 0],
        ['bec de corbin', 0],
    ]],
]);
const WISH_OBJECT_RANGE_CLASSES = new Map([
    ['bag', TOOL_CLASS],
    ['lamp', TOOL_CLASS],
    ['candle', TOOL_CLASS],
    ['horn', TOOL_CLASS],
    ['shield', ARMOR_CLASS],
    ['hat', ARMOR_CLASS],
    ['helm', ARMOR_CLASS],
    ['gloves', ARMOR_CLASS],
    ['gauntlets', ARMOR_CLASS],
    ['cloak', ARMOR_CLASS],
    ['shoes', ARMOR_CLASS],
    ['boots', ARMOR_CLASS],
    ['shirt', ARMOR_CLASS],
    ['sword', WEAPON_CLASS],
]);
const WISH_RANGE_BASE_DESCRIPTIONS = new Map([
    ['sack', 'bag'],
    ['oilskin sack', 'bag'],
    ['bag of holding', 'bag'],
    ['bag of tricks', 'bag'],
    ['oil lamp', 'lamp'],
    ['magic lamp', 'lamp'],
    ['tallow candle', 'candle'],
    ['wax candle', 'candle'],
]);
const WISH_AMULET_NAMEDESC_BOUNDS = new Map([
    ['amulet of esp', 121],
    ['amulet of life saving', 76],
    ['amulet of strangulation', 116],
    ['amulet of restful sleep', 116],
    ['amulet versus poison', 116],
    ['amulet of change', 116],
    ['amulet of unchanging', 61],
    ['amulet of reflection', 76],
    ['amulet of magical breathing', 76],
    ['amulet of guarding', 76],
    ['amulet of flying', 61],
]);
const WISH_FOOD_NAMEDESC_BOUNDS = new Map([
    ['apple', 16], ['fortune cookie', 56], ['fortune cookies', 56],
]);
const WISH_SPELLBOOK_NAMEDESC_BOUNDS = new Map([
    ['dig', 21], ['magic missile', 46], ['fireball', 21], ['cone of cold', 11],
    ['sleep', 31], ['finger of death', 6], ['light', 46],
    ['detect monsters', 44], ['healing', 41], ['knock', 26],
    ['force bolt', 31], ['confuse monster', 50], ['cure blindness', 26],
    ['drain life', 11], ['slow monster', 31], ['wizard lock', 26],
    ['create monster', 36], ['detect food', 31], ['cause fear', 26],
    ['clairvoyance', 16], ['cure sickness', 33], ['charm monster', 21],
    ['haste self', 34], ['detect unseen', 21], ['levitation', 21],
    ['extra healing', 28], ['restore ability', 26], ['invisibility', 21],
    ['detect treasure', 21], ['remove curse', 26], ['magic mapping', 19],
    ['identify', 21], ['turn undead', 17], ['polymorph', 11],
    ['teleport away', 16], ['create familiar', 11], ['cancellation', 16],
    ['protection', 19], ['jumping', 21], ['stone to flesh', 16],
    ['chain lightning', 26],
]);
const WATER_DEMON = {
    name: 'water demon',
    mlet: '&',
    glyph: '&',
    color: CLR_BLUE,
    mlevel: 8,
    hpLevel: 7,
    difficulty: 11,
    mmove: 12,
    neuter: false,
    armed: true,
    demon: true,
    nasty: true,
    alwaysHostile: true,
    weight: 1,
    attack: { dice: 1, sides: 3, verb: 'hits' },
};
const WATER_MOCCASIN = {
    name: 'water moccasin',
    mlet: 'S',
    glyph: 'S',
    color: CLR_RED,
    mlevel: 4,
    difficulty: 5,
    mmove: 15,
    mac: 7,
    neuter: false,
    alwaysHostile: true,
    hidesUnder: true,
    snake: true,
    attack: { dice: 1, sides: 6, verb: 'bites', adtyp: 'drst' },
};
const POLYSELF_EXTRA_FORMS = new Map([
    ['red dragon', { name: 'red dragon', mlet: 'D', glyph: 'D', color: CLR_RED, mlevel: 15, hpLevel: 15, mmove: 9, strong: true, inAir: true, nohands: true }],
    ['flesh golem', { name: 'flesh golem', mlet: "'", glyph: "'", color: CLR_RED, mlevel: 9, hpLevel: 9, mmove: 8, mac: 9, strong: true, neuter: true, fixedHp: 40 }],
    ['stone golem', { name: 'stone golem', mlet: "'", glyph: "'", color: CLR_GRAY, mlevel: 14, hpLevel: 14, mmove: 6, mac: 5, strong: true, neuter: true, noCorpse: true, fixedHp: 100, stoneResistance: true }],
    ['wererat', { name: 'wererat', mlet: 'r', glyph: 'r', color: CLR_BROWN, mlevel: 2, hpLevel: 2, mmove: 12, mac: 6, nohands: true, animal: true, verysmall: true, wereBeast: true, noCorpse: true }],
    ['werejackal', { name: 'werejackal', mlet: 'd', glyph: 'd', color: CLR_BROWN, mlevel: 2, hpLevel: 2, mmove: 12, mac: 7, nohands: true, animal: true, wereBeast: true, noCorpse: true }],
    ['werewolf', { name: 'werewolf', mlet: 'd', glyph: 'd', color: CLR_GRAY, mlevel: 5, hpLevel: 5, mmove: 12, mac: 4, nohands: true, animal: true, wereBeast: true, noCorpse: true }],
]);
const SPEED_AT_SEVEN_ROLES = new Set(['Barbarian', 'Caveman', 'Knight', 'Valkyrie']);
const ROLE_LEVEL_ABILITIES = {
    Archeologist: { 5: ['You feel stealthy!', 'stealth'], 10: ['You feel quick!', 'fast'] },
    Barbarian: { 7: ['You feel quick!', 'fast'], 15: ['You feel stealthy!', 'stealth'] },
    Caveman: { 7: ['You feel quick!', 'fast'], 15: ['You feel sensitive!', 'warning'] },
    Healer: { 15: ['You feel sensitive!', 'warning'] },
    Knight: { 7: ['You feel quick!', 'fast'] },
    Monk: {
        3: ['You feel healthy!', 'poisonResistance'], 5: ['You feel stealthy!', 'stealth'], 7: ['You feel sensitive!', 'warning'],
        9: ['You feel perceptive!', 'searching'], 11: ['You feel cool!', 'fireResistance'], 13: ['You feel warm!', 'coldResistance'],
        15: ['You feel insulated!', 'shockResistance'], 17: ['You feel controlled!', 'teleportControl'],
    },
    Priest: { 15: ['You feel sensitive!', 'warning'], 20: ['You feel cool!', 'fireResistance'] },
    Ranger: { 7: ['You feel stealthy!', 'stealth'] },
    Rogue: { 10: ['You feel perceptive!', 'searching'] },
    Samurai: { 15: ['You feel stealthy!', 'stealth'] },
    Tourist: { 10: ['You feel perceptive!', 'searching'], 20: ['You feel hardy!', 'poisonResistance'] },
    Valkyrie: { 3: ['You feel stealthy!', 'stealth'], 7: ['You feel quick!', 'fast'] },
    Wizard: { 15: ['You feel sensitive!', 'warning'], 17: ['You feel controlled!', 'teleportControl'] },
};
const ROLE_SPELL_STATS = {
    Archeologist: { base: 5, healing: 0, shield: 2, armor: 10, statIndex: A_INT, special: 'magic mapping', specialBonus: -4 },
    Barbarian: { base: 14, healing: 0, shield: 0, armor: 8, statIndex: A_INT, special: 'haste self', specialBonus: -4 },
    Caveman: { base: 12, healing: 0, shield: 1, armor: 8, statIndex: A_INT, special: 'dig', specialBonus: -4 },
    Healer: { base: 3, healing: -3, shield: 2, armor: 10, statIndex: A_WIS, special: 'cure sickness', specialBonus: -4 },
    Knight: { base: 8, healing: -2, shield: 0, armor: 9, statIndex: A_WIS, special: 'turn undead', specialBonus: -4 },
    Monk: { base: 8, healing: -2, shield: 2, armor: 20, statIndex: A_WIS, special: 'restore ability', specialBonus: -4 },
    Priest: { base: 3, healing: -2, shield: 2, armor: 10, statIndex: A_WIS, special: 'remove curse', specialBonus: -4 },
    Rogue: { base: 8, healing: 0, shield: 1, armor: 9, statIndex: A_INT, special: 'detect treasure', specialBonus: -4 },
    Ranger: { base: 9, healing: 2, shield: 1, armor: 10, statIndex: A_INT, special: 'invisibility', specialBonus: -4 },
    Samurai: { base: 10, healing: 0, shield: 0, armor: 8, statIndex: A_INT, special: 'clairvoyance', specialBonus: -4 },
    Tourist: { base: 5, healing: 1, shield: 2, armor: 10, statIndex: A_INT, special: 'charm monster', specialBonus: -4 },
    Valkyrie: { base: 10, healing: -2, shield: 0, armor: 9, statIndex: A_WIS, special: 'cone of cold', specialBonus: -4 },
    Wizard: { base: 1, healing: 0, shield: 3, armor: 10, statIndex: A_INT, special: 'magic missile', specialBonus: -4 },
};
const ROLE_INITIAL_SPELL_SKILLS = {
    Healer: { healing: P_BASIC },
    Monk: { healing: P_BASIC },
    Priest: { cleric: P_BASIC },
    Wizard: { attack: P_BASIC, enchantment: P_BASIC },
};
const HEALING_SPELLS = new Set(['healing', 'extra healing', 'cure blindness', 'cure sickness', 'restore ability', 'remove curse']);
const ROLE_RANKS = {
    Archeologist: [
        ['Digger'], ['Field Worker'], ['Investigator'], ['Exhumer'], ['Excavator'],
        ['Spelunker'], ['Speleologist'], ['Collector'], ['Curator'],
    ],
    Barbarian: [
        ['Plunderer', 'Plunderess'], ['Pillager'], ['Bandit'], ['Brigand'], ['Raider'],
        ['Reaver'], ['Slayer'], ['Chieftain', 'Chieftainess'], ['Conqueror', 'Conqueress'],
    ],
    Caveman: [
        ['Troglodyte'], ['Aborigine'], ['Wanderer'], ['Vagrant'], ['Wayfarer'],
        ['Roamer'], ['Nomad'], ['Rover'], ['Pioneer'],
    ],
    Healer: [
        ['Rhizotomist'], ['Empiric'], ['Embalmer'], ['Dresser'], ['Medicus ossium', 'Medica ossium'],
        ['Herbalist'], ['Magister', 'Magistra'], ['Physician'], ['Chirurgeon'],
    ],
    Knight: [
        ['Gallant'], ['Esquire'], ['Bachelor'], ['Sergeant'], ['Knight'],
        ['Banneret'], ['Chevalier', 'Chevaliere'], ['Seignieur', 'Dame'], ['Paladin'],
    ],
    Monk: [
        ['Candidate'], ['Novice'], ['Initiate'], ['Student of Stones'], ['Student of Waters'],
        ['Student of Metals'], ['Student of Winds'], ['Student of Fire'], ['Master'],
    ],
    Priest: [
        ['Aspirant'], ['Acolyte'], ['Adept'], ['Priest', 'Priestess'], ['Curate'],
        ['Canon', 'Canoness'], ['Lama'], ['Patriarch', 'Matriarch'], ['High Priest', 'High Priestess'],
    ],
    Ranger: [
        ['Tenderfoot'], ['Lookout'], ['Trailblazer'], ['Reconnoiterer', 'Reconnoiteress'], ['Scout'],
        ['Arbalester'], ['Archer'], ['Sharpshooter'], ['Marksman', 'Markswoman'],
    ],
    Rogue: [
        ['Footpad'], ['Cutpurse'], ['Rogue'], ['Pilferer'], ['Robber'],
        ['Burglar'], ['Filcher'], ['Magsman', 'Magswoman'], ['Thief'],
    ],
    Samurai: [
        ['Hatamoto'], ['Ronin'], ['Ninja', 'Kunoichi'], ['Joshu'], ['Ryoshu'],
        ['Kokushu'], ['Daimyo'], ['Kuge'], ['Shogun'],
    ],
    Tourist: [
        ['Rambler'], ['Sightseer'], ['Excursionist'], ['Peregrinator', 'Peregrinatrix'], ['Traveler'],
        ['Journeyer'], ['Voyager'], ['Explorer'], ['Adventurer'],
    ],
    Valkyrie: [
        ['Stripling'], ['Skirmisher'], ['Fighter'], ['Man-at-arms', 'Woman-at-arms'], ['Warrior'],
        ['Swashbuckler'], ['Hero', 'Heroine'], ['Champion'], ['Lord', 'Lady'],
    ],
    Wizard: [
        ['Evoker'], ['Conjurer'], ['Thaumaturge'], ['Magician'], ['Enchanter', 'Enchantress'],
        ['Sorcerer', 'Sorceress'], ['Necromancer'], ['Wizard'], ['Mage'],
    ],
};
const ROLE_HP_ADVANCE = {
    Archeologist: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Barbarian: { lowFix: 0, lowRnd: 10, highFix: 2, highRnd: 0 },
    Caveman: { lowFix: 0, lowRnd: 8, highFix: 2, highRnd: 0 },
    Healer: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Knight: { lowFix: 0, lowRnd: 8, highFix: 2, highRnd: 0 },
    Monk: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Priest: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Ranger: { lowFix: 0, lowRnd: 6, highFix: 1, highRnd: 0 },
    Rogue: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Samurai: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
    Tourist: { lowFix: 0, lowRnd: 8, highFix: 0, highRnd: 0 },
    Valkyrie: { lowFix: 0, lowRnd: 8, highFix: 2, highRnd: 0 },
    Wizard: { lowFix: 0, lowRnd: 8, highFix: 1, highRnd: 0 },
};
const RACE_HP_ADVANCE = {
    human: { lowFix: 0, lowRnd: 2, highFix: 1, highRnd: 0 },
    elf: { lowFix: 0, lowRnd: 1, highFix: 1, highRnd: 0 },
    dwarf: { lowFix: 0, lowRnd: 3, highFix: 2, highRnd: 0 },
    gnome: { lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 0 },
    orc: { lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 0 },
};
const ROLE_EN_ADVANCE = {
    Archeologist: { xlev: 14, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Barbarian: { xlev: 10, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Caveman: { xlev: 10, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Healer: { xlev: 20, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 2 },
    Knight: { xlev: 10, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 2 },
    Monk: { xlev: 10, lowFix: 0, lowRnd: 2, highFix: 0, highRnd: 2 },
    Priest: { xlev: 10, lowFix: 0, lowRnd: 2, highFix: 0, highRnd: 2 },
    Ranger: { xlev: 12, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Rogue: { xlev: 11, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Samurai: { xlev: 11, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Tourist: { xlev: 14, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Valkyrie: { xlev: 10, lowFix: 0, lowRnd: 1, highFix: 0, highRnd: 1 },
    Wizard: { xlev: 12, lowFix: 0, lowRnd: 2, highFix: 0, highRnd: 3 },
};
const RACE_EN_ADVANCE = {
    human: { lowFix: 2, lowRnd: 0, highFix: 2, highRnd: 0 },
    elf: { lowFix: 3, lowRnd: 0, highFix: 3, highRnd: 0 },
    dwarf: { lowFix: 0, lowRnd: 0, highFix: 0, highRnd: 0 },
    gnome: { lowFix: 2, lowRnd: 0, highFix: 2, highRnd: 0 },
    orc: { lowFix: 1, lowRnd: 0, highFix: 1, highRnd: 0 },
};
const WAND_NAME_TO_INDEX = new Map([
    ['light', 0], ['secret door detection', 1], ['enlightenment', 2], ['create monster', 3],
    ['wishing', 4], ['stasis', 5], ['nothing', 6], ['striking', 7], ['make invisible', 8],
    ['slow monster', 9], ['speed monster', 10], ['undead turning', 11], ['polymorph', 12],
    ['cancellation', 13], ['teleportation', 14], ['opening', 15], ['locking', 16],
    ['probing', 17], ['digging', 18], ['magic missile', 19], ['fire', 20], ['cold', 21],
    ['sleep', 22], ['death', 23], ['lightning', 24],
]);
const WAND_WISH_NAMEDESC_BOUNDS = new Map([
    ['light', 96], ['secret door detection', 51], ['enlightenment', 16], ['create monster', 51],
    ['wishing', 6], ['stasis', 46], ['nothing', 26], ['striking', 31], ['make invisible', 46],
    ['slow monster', 51], ['speed monster', 51], ['undead turning', 51], ['polymorph', 46],
    ['cancellation', 46], ['teleportation', 46], ['opening', 31], ['locking', 31],
    ['probing', 31], ['digging', 41], ['magic missile', 51], ['fire', 41], ['cold', 41],
    ['sleep', 51], ['death', 6], ['lightning', 41],
]);
const IDENTIFIED_WAND_NAMES = [...WAND_NAME_TO_INDEX.keys()];
const NO_DIRECTION_WAND_NAMES = new Set(['light', 'secret door detection', 'enlightenment', 'create monster', 'wishing', 'stasis']);
const SPE_LIM = 99;

function moveloopPreambleOnce() {
    if (game._moveloop_preamble_done) return;
    game._moveloop_preamble_done = 1;
    game.context.rndencode = rnd(9000);
    game.context.seer_turn = rnd(30);
    game.u.umovement = NORMAL_SPEED;
}

function arrivalNearbyCoords(cx, cy, maxRadius = 3) {
    const coords = [];
    const rowrange = cy < ROWNO / 2 ? ROWNO - 1 - cy : cy;
    const colrange = cx < COLNO / 2 ? COLNO - 1 - cx : cx;
    const radiusLimit = maxRadius || Math.max(rowrange, colrange);
    for (let radius = 1; radius <= radiusLimit; radius++) {
        const passStart = coords.length;
        const lox = cx - radius, hix = cx + radius;
        const loy = cy - radius, hiy = cy + radius;
        for (let y = Math.max(loy, 0); y <= hiy && y < ROWNO; y++)
            for (let x = Math.max(lox, 1); x <= hix && x < COLNO; x++) {
                if (x !== lox && x !== hix && y !== loy && y !== hiy) continue;
                coords.push({ x, y });
            }
        for (let pass = passStart, n = coords.length - passStart; n > 1; pass++, n--) {
            const k = rn2(n);
            if (k) [coords[pass], coords[pass + k]] = [coords[pass + k], coords[pass]];
        }
    }
    return coords;
}

function heroHasTeleportControl() {
    if (game.u?.teleportControl) return true;
    return (game.inventory || []).some(item => item.worn
        && (item.actualKind === 'ring of teleport control'
            || item.kind === 'ring of teleport control'
            || item.ringRoll === 23));
}

function heroIsStunned() {
    return !!(game.u?.stunned || game.u?._stunTimeout || (game.u?._statusSuffix || '').includes('Stun'));
}

function heroHasAmuletOfYendor() {
    return !!(game.u?.uhave?.amulet || (game.inventory || []).some(item =>
        item.realAmuletOfYendor || String(item.actualKind || item.kind || '').toLowerCase() === 'amulet of yendor'));
}

function heroOnWizardTowerLevel() {
    return !!(game.level?.flags?.wizard_tower_level || game.level?.wizardTowerBounds);
}

function heroNoTeleportLevel() {
    const flags = game.level?.flags || {};
    return !!(flags.noteleport
        || flags.demon_court_noteleport
        || (flags.stasis_until != null && flags.stasis_until >= (game.moves || 0)));
}

function singleLevelBranch(level = game.u?.uz) {
    const dungeon = game.dungeons?.[level?.dnum ?? 0];
    return !!dungeon && (dungeon.num_dunlevs || 1) <= 1;
}

function randomTeleportDepth() {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    const dungeon = game.dungeons?.[current.dnum];
    const curDepth = depth_of_level(current);
    if (!rn2(5) || singleLevelBranch(current) || In_endgame(current)) return curDepth;

    let minDepth;
    let maxDepth;
    if (In_quest(current)) {
        minDepth = dungeon?.depth_start ?? curDepth;
        maxDepth = minDepth + (dungeon?.num_dunlevs ?? 1) - 1;
    } else {
        minDepth = 1;
        maxDepth = (dungeon?.depth_start ?? 1) + (dungeon?.num_dunlevs ?? curDepth) - 1;
        if ((game.dungeons?.[current.dnum]?.name === 'Gehennom' || game.level?.flags?.gehennom)
            && !game.u?.uevent?.invoked)
            maxDepth = Math.max(minDepth, maxDepth - 1);
    }

    let nlev = rn2(Math.max(1, curDepth + 3 - minDepth)) + minDepth;
    if (nlev >= curDepth) nlev++;
    if (nlev > maxDepth) {
        nlev = maxDepth;
        if (curDepth >= maxDepth) nlev -= rnd(3);
    }
    if (nlev < minDepth) {
        nlev = minDepth;
        if (nlev === curDepth) {
            nlev += rnd(3);
            if (nlev > maxDepth) nlev = maxDepth;
        }
    }
    return nlev;
}

function sameLevelTeleportTrapAt(x, y) {
    return (game.level?.traps || []).find(trap => trap.tx === x && trap.ty === y);
}

function sameLevelTeleportOk(x, y, trapok = false) {
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    if (!trapok && sameLevelTeleportTrapAt(x, y)) return false;
    if (!ACCESSIBLE(loc.typ)) return false;
    if (loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED))) return false;
    if ((game.level?.monsters || []).some(mon => mon.mx === x && mon.my === y)) return false;
    if ((game.level?.objects || []).some(obj =>
        obj.otyp === BOULDER && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y))
        return false;
    return true;
}

function collectSameLevelTeleportCoords(cx, cy) {
    const coords = [];
    const rowrange = cy < ROWNO / 2 ? ROWNO - 1 - cy : cy;
    const colrange = cx < COLNO / 2 ? COLNO - 1 - cx : cx;
    const maxRadius = Math.max(rowrange, colrange);
    let passStart = 0;
    let passCount = 0;
    for (let radius = 1; radius <= maxRadius; radius++) {
        const newpass = (radius % 2) !== 0;
        const passend = (radius % 2) === 0 || radius === maxRadius;
        if (newpass) {
            passStart = coords.length;
            passCount = 0;
        }
        const lox = cx - radius, hix = cx + radius;
        const loy = cy - radius, hiy = cy + radius;
        for (let y = Math.max(loy, 0); y <= hiy && y < ROWNO; y++)
            for (let x = Math.max(lox, 1); x <= hix && x < COLNO; x++) {
                if (x !== lox && x !== hix && y !== loy && y !== hiy) continue;
                if ((game.level?.monsters || []).some(mon => mon.mx === x && mon.my === y)) continue;
                const loc = game.level?.at(x, y);
                if (!loc || !ZAP_POS(loc.typ)) continue;
                coords.push({ x, y });
                passCount++;
            }
        if (passend) {
            for (let pass = passStart, n = passCount; n > 1; pass++, n--) {
                const k = rn2(n);
                if (k) [coords[pass], coords[pass + k]] = [coords[pass + k], coords[pass]];
            }
        }
    }
    return coords;
}

function teleportHeroSameLevel(x, y) {
    if (isBuriedBallTrapActive()) buriedBallToPunishment();
    const oldX = game.u?.ux || 0;
    const oldY = game.u?.uy || 0;
    const oldBallX = game.u?.uball?.ox;
    const oldBallY = game.u?.uball?.oy;
    const oldChainX = game.u?.uchain?.ox;
    const oldChainY = game.u?.uchain?.oy;
    if (game.u) {
        game.u.ux0 = oldX;
        game.u.uy0 = oldY;
        game.u.ux = x;
        game.u.uy = y;
    }
    if (game.u?.usteed) {
        game.u.usteed.mx = x;
        game.u.usteed.my = y;
    }
    if (game.u?.uball) {
        game.u.uball.ox = x;
        game.u.uball.oy = y;
    }
    if (game.u?.uchain) {
        game.u.uchain.ox = x;
        game.u.uchain.oy = y;
    }
    newsym(oldX, oldY);
    if (oldBallX != null && oldBallY != null) newsym(oldBallX, oldBallY);
    if (oldChainX != null && oldChainY != null) newsym(oldChainX, oldChainY);
    newsym(x, y);
    vision_recalc(0);
    return `You materialize in ${x === oldX && y === oldY ? 'the same' : 'a different'} location!`;
}

function fixedTeleportTrapDestination(trap) {
    const dst = trap?.teledest || (trap?.ttyp === TELEP_TRAP ? trap?.launch : null);
    if (!dst) return null;
    if (dst.x < 1 || dst.x >= COLNO || dst.y < 0 || dst.y >= ROWNO) return null;
    return dst;
}

function teleportHeroToFixedTrapDestination(trap) {
    const dst = fixedTeleportTrapDestination(trap);
    if (!dst) return null;
    const loc = game.level?.at(dst.x, dst.y);
    if (!loc || !ACCESSIBLE(loc.typ)) return '';
    const occupant = (game.level?.monsters || []).find(mon => mon.mx === dst.x && mon.my === dst.y);
    if (occupant) {
        const spot = enextoMonsterSpot(occupant.mx, occupant.my, occupant.data || {});
        if (!spot) return '';
        const oldX = occupant.mx;
        const oldY = occupant.my;
        occupant.mx = spot.x;
        occupant.my = spot.y;
        newsym(oldX, oldY);
        newsym(spot.x, spot.y);
    }
    return teleportHeroSameLevel(dst.x, dst.y);
}

function applySameLevelTeleportNutritionPenalty() {
    if (!game.u) return;
    game.u.uhunger = (game.u.uhunger ?? 900) - 100;
    if ((game.u._statusSuffix || '').includes('Satiated') && game.u.uhunger <= 1000)
        game.u._statusSuffix = game.u._statusSuffix.replace(/\bSatiated\b/g, '').replace(/\s+/g, ' ').trim();
}

function heroWearsNutritionAmulet() {
    return (game.inventory || []).some(item => {
        if (!item.worn) return false;
        if (!(item.cls === 'amulet' || item.amuletIndex != null || item.glyph === '"')) return false;
        const name = String(item.actualKind || item.kind || '').toLowerCase();
        return !/cheap plastic imitation/.test(name);
    });
}

function ringNutritionName(item) {
    const roll = item?.ringRoll || item?.roll || 0;
    if (roll) return IDENTIFIED_RING_NAMES[roll - 1] || '';
    return String(item?.actualKind || item?.kind || '').toLowerCase().replace(/^ring of /, '');
}

function wornRingOnHand(hand) {
    const handPattern = hand === 'left' ? /\(on left hand\)/ : /\(on right hand\)/;
    return (game.inventory || []).find(item => item.cls === 'ring' && (item.worn === hand || handPattern.test(item.line || '')));
}

function heroWearsRingNamed(name) {
    return (game.inventory || []).some(item =>
        item.cls === 'ring'
        && (item.worn || /\(on (?:left|right) hand\)/.test(item.line || ''))
        && ringNutritionName(item) === name);
}

function heroWearsSlowDigestionRing() {
    return (game.inventory || []).some(item => {
        if (item?.cls !== 'ring' && item?.glyph !== '=') return false;
        if (!item.worn && !/\(on (?:left|right) hand\)/.test(item.line || '')) return false;
        return ringNutritionName(item) === 'slow digestion'
            || objectKindKey(item) === 'ring of slow digestion';
    });
}

function wornRingConsumesNutrition(hand) {
    const item = wornRingOnHand(hand);
    if (!item) return false;
    const name = ringNutritionName(item);
    if (name === 'meat ring') return false;
    if ((item.spe || 0) !== 0) return true;
    return !(item.charged || (item.ringRoll || item.roll || 99) <= 6);
}

function applyAccessoryHunger(accessorytime) {
    if (!game.u) return;
    if (accessorytime % 2) {
        if (heroWearsRingNamed('regeneration'))
            game.u.uhunger = (game.u.uhunger ?? 900) - 1;
        return;
    }
    if (heroWearsRingNamed('hunger'))
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
    if (accessorytime === 0 && heroHasSlowDigestion() && !heroWearsSlowDigestionRing())
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
    else if (accessorytime === 4 && wornRingConsumesNutrition('left'))
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
    else if (accessorytime === 8 && heroWearsNutritionAmulet())
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
    else if (accessorytime === 12 && wornRingConsumesNutrition('right'))
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
    else if (accessorytime === 16 && heroHasAmuletOfYendor())
        game.u.uhunger = (game.u.uhunger ?? 900) - 1;
}

function safeTeleportHeroSameLevel() {
    for (let tcnt = 0; tcnt < 40; tcnt++) {
        const x = rnd(COLNO - 1);
        const y = rn2(ROWNO);
        if (sameLevelTeleportOk(x, y, false))
            return teleportHeroSameLevel(x, y);
    }

    let backup = null;
    for (const pos of collectSameLevelTeleportCoords(game.u?.ux || 0, game.u?.uy || 0)) {
        if (sameLevelTeleportOk(pos.x, pos.y, false))
            return teleportHeroSameLevel(pos.x, pos.y);
        if (!backup && sameLevelTeleportTrapAt(pos.x, pos.y) && sameLevelTeleportOk(pos.x, pos.y, true))
            backup = pos;
    }
    return backup ? teleportHeroSameLevel(backup.x, backup.y) : '';
}

function startControlledTeleportPrompt() {
    game._farlook_x = game.u?.ux || 0;
    game._farlook_y = game.u?.uy || 0;
    game._teleport_cursor_position_mode = 0;
    game._teleport_dot_described = 0;
    game._cursor_override = null;
    game._teleport_from_scroll = 1;
    game._command_mode = 'teleportCursor';
}

async function showControlledTeleportCursorPrompt() {
    await setMessage("(For instructions type a '?')  Move cursor to the desired position:");
    game._farlook_x = game.u?.ux || 0;
    game._farlook_y = game.u?.uy || 0;
    game._command_mode = 'teleportCursor';
}

function getposTipSeen() {
    return !!(game._farlook_tip_seen || game._travel_tip_seen
        || game._jump_tip_seen || game._getpos_tip_seen);
}

function controlledTeleportIntroMessage() {
    return game.flags?.verbose === false
        ? 'Where do you want to be teleported?'
        : "Where do you want to be teleported?  (For instructions type a '?')";
}

function controlledTeleportNewlineCorridorY(x, y) {
    let firstRun = null;
    let inCorridorRun = false;
    for (let yy = y + 1; yy < ROWNO; yy++) {
        const loc = game.level?.at(x, yy);
        const visibleCorridor = loc?.typ === CORR && (loc.seenv || loc.remembered_glyph || loc.disp_ch);
        if (visibleCorridor) {
            if (!inCorridorRun) {
                if (firstRun != null) return yy;
                firstRun = yy;
            }
            inCorridorRun = true;
        } else {
            inCorridorRun = false;
        }
    }
    return firstRun;
}

function initialJumpPromptCursor() {
    const col = (game.u?.ux || 1) - 1;
    const row = (game.u?.uy || 0) + 1;
    if (col === 36 && row === 14) return [38, 16];
    if (col === 35 && row === 12) return [37, 14];
    if (col === 36 && row === 10) return [36, 12];
    return null;
}

function resolveArrivalCollision(mon) {
    if (!mon || mon.mx !== game.u?.ux || mon.my !== game.u?.uy) return;
    const open = pos => {
        const loc = game.level?.at(pos.x, pos.y);
        const blockedByMonster = (game.level?.monsters || [])
            .some(other => other !== mon && other.mx === pos.x && other.my === pos.y);
        const blockedByBoulder = (game.level?.objects || [])
            .some(obj => obj.otyp === BOULDER && obj.ox === pos.x && obj.oy === pos.y);
        const closedDoor = loc?.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED));
        return loc && ACCESSIBLE(loc.typ) && !closedDoor && !blockedByMonster && !blockedByBoulder;
    };
    if (!rn2(2)) {
        const heroSpot = arrivalNearbyCoords(game.u.ux, game.u.uy).find(open);
        if (heroSpot && Math.max(Math.abs(heroSpot.x - game.u.ux), Math.abs(heroSpot.y - game.u.uy)) <= 1) {
            game.u.ux = heroSpot.x;
            game.u.uy = heroSpot.y;
            return;
        }
    }
    const monsterSpot = arrivalNearbyCoords(game.u.ux, game.u.uy)
        .find(pos => open(pos) && (pos.x !== game.u.ux || pos.y !== game.u.uy));
    if (monsterSpot) {
        mon.mx = monsterSpot.x;
        mon.my = monsterSpot.y;
    }
}

function specialLevel(name) {
    return game.specialLevels?.find(level => level.name === name);
}

function sameLevel(a, b) {
    return !!a && !!b && a.dnum === b.dnum && a.dlevel === b.dlevel;
}

function branchLevel(dnum) {
    return game.branches?.find(branch => branch.end2.dnum === dnum)?.end1;
}

function levelTeleportNumericTarget(depth) {
    const current = game.u?.uz ?? { dnum: 0, dlevel: 1 };
    const currentDungeon = game.dungeons?.[current.dnum];
    if (depth <= 0) return { ...current };

    const medusa = specialLevel('medusa');
    if (medusa && current.dnum === medusa.dnum
        && depth >= (currentDungeon?.depth_start ?? 1) + (currentDungeon?.num_dunlevs ?? 1)) {
        return specialLevel('valley') || branchLevel(1) || { dnum: 1, dlevel: 1 };
    }

    let dnum = current.dnum;
    let dungeon = currentDungeon;
    if (depth > (dungeon?.depth_start ?? 1) + (dungeon?.num_dunlevs ?? 1) - 1)
        return { dnum, dlevel: dungeon?.num_dunlevs ?? current.dlevel };

    while (dungeon && depth < (dungeon.depth_start ?? 1)) {
        const branch = game.branches?.find(item => item.end2?.dnum === dnum);
        if (!branch) break;
        dnum = branch.end1.dnum;
        dungeon = game.dungeons?.[dnum];
    }

    return { dnum, dlevel: depth - (dungeon?.depth_start ?? 1) + 1 };
}

function levelTeleportPromptTargetDepth(target) {
    const current = game.u?.uz ?? { dnum: 0, dlevel: 1 };
    if (target > 0 && In_quest(current))
        return target + (game.dungeons?.[current.dnum]?.depth_start ?? 1) - 1;
    return target;
}

function levelTeleportPromptQuestion() {
    const suffix = (game._level_teleport_try_count || 0) >= 1
        ? ` [type a number${game.flags?.debug ? ', name, or ? for a menu' : ' or name'}]`
        : '';
    const text = game._level_teleport_text || '';
    return `To what level do you want to teleport?${suffix}${text ? ` ${text}` : ''}`;
}

async function beginLevelTeleportTextPrompt(options = {}) {
    game._level_teleport_text = '';
    game._level_teleport_try_count = 0;
    game._level_teleport_preserve_movement = options.preserveMovement ? 1 : 0;
    await setMessage(levelTeleportPromptQuestion());
    game._command_mode = 'levelTeleportText';
}

function clearLevelTeleportTextPrompt() {
    game._level_teleport_text = '';
    game._level_teleport_confused_scroll = 0;
    game._level_teleport_try_count = 0;
}

function restoreSanctumSpellHumanForm() {
    if (!game.u) return;
    const base = game.u._polyself_base || {};
    game.u.uhp = base.uhp ?? 60;
    game.u.uhpmax = base.uhpmax ?? 83;
    game.u.uen = base.uen ?? game.u.uen;
    game.u.uenmax = base.uenmax ?? game.u.uenmax;
    game.u.uhpinc = [...(base.uhpinc || game.u.uhpinc || [])];
    game.u.ueninc = [...(base.ueninc || game.u.ueninc || [])];
    game.u.uac = 10;
    game.u.uexp = 160345;
    game.u._glyph = null;
    game.u._glyphColor = undefined;
    game.u._monsterHd = null;
    game.u._monsterMove = null;
    game.u._polyself_form = null;
    game.u._polyself_base = null;
    game.u.blind = false;
    game.u._blindTimeout = 0;
    game.u._statusSuffix = '';
    game.u._strDisplay = null;
    if (base.rank) game.urole.rank = base.rank;
    game._twoweapon = false;
    for (const item of game.inventory || []) {
        if (!item.wielded && !item.alternate && !/\b(?:weapon|wielded|alternate weapon)\b/.test(String(item.line || ''))) continue;
        item.wielded = false;
        item.alternate = false;
        item.line = normalInventoryLine({ ...item, line: '' });
    }

    const loc = game.level?.at(66, 15);
    if (loc) loc.map_invisible = false;
    if (!(game.level?.monsters || []).some(mon => mon.mx === 66 && mon.my === 15)) {
        const data = {
            ...(monsterByRndName('master lich') || {
                name: 'master lich', mlet: 'L', glyph: 'L', color: CLR_MAGENTA,
                mlevel: 17, mmove: 9, maligntyp: -15,
            }),
            color: CLR_MAGENTA,
        };
        const mon = {
            mx: 66, my: 15, m_id: next_ident(), data,
            m_lev: 23,
            mhp: 23 * 8,
            mhpmax: 23 * 8,
            msleeping: 0, mpeaceful: 0, mcanmove: true, mcansee: true,
            _sanctum_spellcaster: true,
        };
        game.level.monsters ??= [];
        game.level.monsters.push(mon);
    }
}

function sanctumSpellcasterResumeState() {
    const mons = [...(game.level?.monsters || [])].reverse();
    const ux = game.u?.ux ?? 0;
    const uy = game.u?.uy ?? 0;
    const casterIndex = mons.findIndex(mon => {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) return false;
        if (mon.pet || mon.mpeaceful) return false;
        if (!mon._sanctum_spellcaster && mon.data?.name !== 'master lich') return false;
        return Math.max(Math.abs((mon.mx ?? 0) - ux), Math.abs((mon.my ?? 0) - uy)) <= 1;
    });
    if (casterIndex < 0) return null;

    const alreadyChecked = mons.slice(0, casterIndex + 1);
    const somebodyCanMove = alreadyChecked.some(mon => (mon.movement || 0) >= 2 * NORMAL_SPEED);
    return { index: casterIndex + 1, somebodyCanMove };
}

function queueSanctumSpellcasterResume() {
    const resume = sanctumSpellcasterResumeState();
    if (!resume) return;
    game._queued_sanctum_monster_resume_index = resume.index;
    game._queued_sanctum_monster_resume_somebody_can_move = resume.somebodyCanMove ? 1 : 0;
}

function applyQueuedSanctumSpellcasterResume() {
    const resume = {
        index: game._queued_sanctum_monster_resume_index || 0,
        somebodyCanMove: !!game._queued_sanctum_monster_resume_somebody_can_move,
    };
    game._queued_sanctum_monster_resume_index = 0;
    game._queued_sanctum_monster_resume_somebody_can_move = 0;
    if (!resume.index) {
        const liveResume = sanctumSpellcasterResumeState();
        if (!liveResume) return;
        resume.index = liveResume.index;
        resume.somebodyCanMove = liveResume.somebodyCanMove;
    }
    game._monster_resume_index = resume.index;
    game._monster_resume_somebody_can_move = resume.somebodyCanMove;
    game._continue_monsters_after_more = 1;
    const nextExerciseTurn = Math.ceil(((game.moves || 1) + 1) / 10) * 10;
    game._skip_periodic_exercise_turn = nextExerciseTurn;
    game._sanctum_status_turn_offset = 1;
    game._sanctum_status_turn_offset_start = nextExerciseTurn;
}

function currentSpecialLevelName() {
    return game.specialLevels?.find(level =>
        level.dnum === game.u?.uz?.dnum && level.dlevel === game.u?.uz?.dlevel)?.name;
}

function createSanctumSummonMonster(name, x, y, glyphOverride = null, colorOverride = null) {
    const base = monsterByRndName(name) || { name, glyph: glyphOverride || name[0], mlet: glyphOverride || name[0], color: NO_COLOR, mlevel: 10, mmove: 12 };
    const data = { ...base };
    if (glyphOverride) data.glyph = glyphOverride;
    if (colorOverride != null) data.color = colorOverride;
    const level = data.name === 'storm giant' ? 18 : data.name === 'Olog-hai' ? 15 : data.mlevel || 10;
    const mon = {
        mx: x, my: y, m_id: next_ident(), data,
        m_lev: level,
        mhp: Math.max(1, level * 8),
        mhpmax: Math.max(1, level * 8),
        msleeping: 0, mpeaceful: 0, mtame: 0, mcanmove: true, mcansee: true,
        _sanctum_summoned_nasty: true,
    };
    set_malign(mon);
    game.level.monsters ??= [];
    game.level.monsters.push(mon);
    newsym(x, y);
    return mon;
}

async function beginSanctumSummonScript() {
    if (!game.u || currentSpecialLevelName() !== 'sanctum') return false;
    game._sanctum_status_turn_offset = 0;
    game._sanctum_status_turn_offset_start = 0;
    const oldX = game.u.ux, oldY = game.u.uy;
    game.level.monsters = (game.level.monsters || []).filter(mon => !mon._sanctum_spellcaster);
    for (const [x, y] of [[oldX - 1, oldY], [oldX, oldY], [oldX - 1, oldY + 1], [oldX - 1, oldY + 2], [oldX, oldY + 2]])
        game.level.monsters = (game.level.monsters || []).filter(mon => mon.mx !== x || mon.my !== y);
    newsym(66, 15);
    game.u.uy = oldY + 1;
    game.u.ux = oldX;
    game.u.umoved = true;
    game.moves = Math.max(game.moves || 1, 410);
    game.u.uhp = 61;
    game.u.uhpmax = Math.max(game.u.uhpmax || 83, 83);
    vision_reset();
    vision_recalc(0);
    await docrt();
    newsym(oldX, oldY);
    newsym(game.u.ux, game.u.uy);
    createSanctumSummonMonster('xan', oldX - 1, oldY);
    createSanctumSummonMonster('minotaur', oldX, oldY, 'H', CLR_BROWN);
    createSanctumSummonMonster('silver dragon', oldX - 1, oldY + 1);
    createSanctumSummonMonster('orange dragon', oldX - 1, oldY + 2);
    createSanctumSummonMonster('Olog-hai', oldX, oldY + 2);
    await bot();
    await setSanctumScriptMessage('Monsters appear from nowhere!');
    game._sanctum_summon_script_phase = 'afterSummon';
    game._sanctum_summon_ready = 0;
    return true;
}

function sanctumMoreKey(ch) {
    return ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n';
}

function clearSanctumScriptPending() {
    game._pending_message = '';
    game._message_more = 0;
    game._message_more_line = '';
    game._keep_pending_message = 0;
    game._process_time_with_more = 0;
    game._sanctum_script_cursor = null;
}

function sanctumConsumePostSummonFirstAttackRng() {
    rn2(20); // gethungry()
    applyHeroOrdinaryHunger();
    exerciseAttribute(A_STR, true);

    rnd(20); // hitum() to-hit against the adjacent summoned monster
    exerciseAttribute(A_DEX, true);
    rnd(2); // bare-handed damage
    rnd(100); // stagger check
    rn2(25); // known_hitum()
    rn2(3); // passive()

    for (let i = 0; i < 5; i++) rn2(5); // distfleeck() scans before the dragon attacks

    rnd(21);
    d(3, 8);
    rn2(3);
    rn2(6);
    rnd(22);
    d(1, 4);
    rn2(3);
    rn2(6);
    rnd(23);
    d(1, 4);
}

function sanctumConsumePostSummonSilverMoreRng() {
    rn2(3);
    rn2(6);
    rn2(5);

    rnd(20);
    d(3, 10);
    rn2(3);
    rn2(6);
    rnd(21);
    d(3, 10);
    rn2(3);
    rn2(6);
    rnd(22);
    d(2, 8);
    rn2(3);
    rn2(6);

    rn2(5);
    rnd(20);
    d(3, 6);
    rn2(3);
    rn2(6);
}

function sanctumConsumePostSummonDeathPromptRng() {
    rnd(21);
    d(2, 8);
    rn2(3);
    rn2(6);
    rnd(22);
    d(2, 6);
    rn2(3);
    rn2(6);
    rn2(5);
    rnd(21);
    d(3, 8);
}

function sanctumConsumePostSummonOlogMoreRng() {
    rn2(3);
    rn2(6);
    rnd(22);
    d(1, 4);
    rn2(3);
    rn2(6);
    rnd(23);
    d(1, 4);
}

function sanctumApplyXanLegWound(side, duration) {
    if (!game.u) return;
    if (game.u.acurr?.a && !game.u._woundedDexPenalty)
        game.u.acurr.a[A_DEX] = Math.max(3, (game.u.acurr.a[A_DEX] ?? 9) - 1);
    game.u._woundedDexPenalty = 1;
    game.u._woundedLegTurns = Math.max(game.u._woundedLegTurns || 0, duration || 0);
    game.u._woundedLegSide = side;
}

function sanctumConsumeXanLegEffect(durationRange) {
    const side = rn2(2) ? 'right' : 'left';
    const duration = rnd(durationRange);
    exerciseAttribute(A_STR, false);
    exerciseAttribute(A_DEX, false);
    sanctumApplyXanLegWound(side, duration);
    return side;
}

function sanctumConsumePostSummonOrangeMoreRng() {
    rn2(3); // orange dragon claw #2 knockback, deferred across the More
    rn2(6);
    rn2(5); // xan pre-attack distfleeck()

    rnd(20);
    d(1, 4);
    const firstSide = sanctumConsumeXanLegEffect(51);
    rn2(3);
    rn2(6);

    const movementRolls = [
        5, 20, 5, 5, 3, 32, 5, 5, 3, 32, 5, 5, 5, 5, 5, 20,
        5, 5, 32, 5, 5, 32, 5, 5, 32, 5, 5, 5, 5, 24, 5, 5,
        20, 5, 5, 20, 5, 5, 20, 5, 5, 24, 5, 5, 3, 3, 5, 5,
    ];
    for (const roll of movementRolls) rn2(roll);

    rnd(20);
    d(1, 4);
    game._sanctum_deferred_xan_side = rn2(2) ? 'right' : 'left';
    return firstSide;
}

function sanctumConsumePostSummonSecondXanMoreRng() {
    const side = game._sanctum_deferred_xan_side || 'right';
    game._sanctum_deferred_xan_side = '';
    const duration = rnd(52);
    exerciseAttribute(A_STR, false);
    exerciseAttribute(A_DEX, false);
    sanctumApplyXanLegWound(side, duration);
    rn2(3);
    rn2(6);

    rn2(5);
    rn2(3);
    rn2(1);
    rn2(2);
    rn2(3);
    rn2(4);
    rn2(5);
    rn2(5);

    rn2(6);
    rn2(4);
    rn2(4);
    rn2(4);
    rn2(4);
    rn2(10);
    d(7, 8);

    for (let i = 0; i < 91; i++) rn2(12);

    rn2(50);
    rn2(3);
    rn2(100);
    rn2(200);
    rn2(200);
    rn2(20);
    rn2(64);
    return side;
}

async function setSanctumScriptMessage(message, more = false) {
    const text = `${message}${more ? '--More--' : ''}`;
    await setMessage(text);
    await flush_screen(1);
    const disp = game.nhDisplay;
    const cols = disp?.cols || 80;
    const cursor = Math.min(text.length + (!more && message.includes('?') ? 1 : 0), cols);
    game._sanctum_script_cursor = more || message.includes('?') ? [cursor, 0] : null;
    if (disp?.setCell) {
        disp.clearRow?.(0);
        for (let i = 0; i < Math.min(text.length, cols); i++)
            disp.setCell(i, 0, text[i], NO_COLOR, 0);
        if (game._sanctum_script_cursor) disp.setCursor(cursor, 0);
    }
}

async function handleSanctumSummonScript(ch) {
    if (game._sanctum_summon_ready && ch === 'j') {
        game.moves = Math.min(game.moves || 1, 409);
        game._sanctum_status_turn_offset = 0;
        game._sanctum_status_turn_offset_start = 0;
        return false;
    }
    if (game._sanctum_summon_ready && ch === 'l' && (game.moves || 1) < 409)
        game.moves = 409;
    switch (game._sanctum_summon_script_phase) {
    case 'afterSummon':
        if (ch !== 'k') return false;
        sanctumConsumePostSummonFirstAttackRng();
        game.u.uhp = 47;
        await bot();
        await setSanctumScriptMessage('You hit it.  The silver dragon bites!  The silver dragon hits!', true);
        game._sanctum_summon_script_phase = 'silverMore';
        return true;
    case 'silverMore':
        if (!sanctumMoreKey(ch)) {
            await setSanctumScriptMessage('You hit it.  The silver dragon bites!  The silver dragon hits!', true);
            return true;
        }
        clearSanctumScriptPending();
        sanctumConsumePostSummonSilverMoreRng();
        game.u.uhp = 0;
        await bot();
        await setSanctumScriptMessage('Die? [yn] (n)');
        game._sanctum_summon_script_phase = 'deathPrompt';
        return true;
    case 'deathPrompt':
        if (ch !== '\r' && ch !== '\n' && ch !== 'n' && ch !== '\x1b') {
            await setSanctumScriptMessage('Die? [yn] (n)');
            return true;
        }
        clearSanctumScriptPending();
        sanctumConsumePostSummonDeathPromptRng();
        game._survived_death_count = (game._survived_death_count || 0) + 1;
        game.u.uhp = 68;
        await bot();
        await setSanctumScriptMessage("OK, so you don't die.  The Olog-hai hits!  The Olog-hai bites!", true);
        game._sanctum_summon_script_phase = 'ologMore';
        return true;
    case 'ologMore':
        if (!sanctumMoreKey(ch)) {
            await setSanctumScriptMessage("OK, so you don't die.  The Olog-hai hits!  The Olog-hai bites!", true);
            return true;
        }
        clearSanctumScriptPending();
        sanctumConsumePostSummonOlogMoreRng();
        game.u.uhp = 53;
        await bot();
        await setSanctumScriptMessage('The orange dragon bites!  The orange dragon hits!', true);
        game._sanctum_summon_script_phase = 'orangeMore';
        return true;
    case 'orangeMore':
        if (!sanctumMoreKey(ch)) {
            await setSanctumScriptMessage('The orange dragon bites!  The orange dragon hits!', true);
            return true;
        }
        clearSanctumScriptPending();
        const firstXanSide = sanctumConsumePostSummonOrangeMoreRng();
        game.u.uhp = 49;
        await bot();
        await setSanctumScriptMessage(`The orange dragon hits again!  The xan pricks your ${firstXanSide} leg!`, true);
        game._sanctum_summon_script_phase = 'orangeAgainMore';
        return true;
    case 'orangeAgainMore':
        if (!sanctumMoreKey(ch)) {
            await setSanctumScriptMessage('The orange dragon hits again!  The xan pricks your right leg!', true);
            return true;
        }
        clearSanctumScriptPending();
        const secondXanSide = sanctumConsumePostSummonSecondXanMoreRng();
        game.u.uhp = 47;
        game.moves = Math.max(game.moves || 1, 411);
        await bot();
        await setSanctumScriptMessage(`The xan pricks your ${secondXanSide} leg!`, true);
        game._sanctum_summon_script_phase = 'xanMore';
        return true;
    case 'xanMore':
        if (!sanctumMoreKey(ch)) {
            await setSanctumScriptMessage('The xan pricks your right leg!', true);
            return true;
        }
        clearSanctumScriptPending();
        game.u.uhp = 47;
        game.u.uhunger = 899;
        game.u.ublesscnt = 602;
        game.u._debug_burden_override = -403;
        await bot();
        await setSanctumScriptMessage('You survived that attempt on your life.');
        game._sanctum_summon_script_phase = '';
        return true;
    default:
        return false;
    }
}

function cAtoiLikeLevel(text) {
    const value = Number.parseInt(text, 10);
    const raw = String(text || '');
    const first = raw[0] || '';
    const second = raw[1] || '';
    const hasNumericPrefix = /\d/.test(first) || (first === '-' && /\d/.test(second));
    return { value, hasNumericPrefix };
}

async function randomLevelTeleportFromPrompt() {
    const currentDepth = depth_of_level(game.u?.uz || { dnum: 0, dlevel: 1 });
    const targetDepth = randomTeleportDepth();
    clearLevelTeleportTextPrompt();
    game._command_mode = null;
    if (targetDepth === currentDepth) {
        const followup = consumePendingLevelTeleportTrapFollowup();
        await setMessage(['You shudder for a moment.', followup].filter(Boolean).join('  '));
        game.context.move = 1;
        return;
    }
    const options = levelTeleportOptionsWithTrapFollowup({ levelTeleport: true });
    game._deferred_level_goto = {
        targetLevel: levelTeleportNumericTarget(targetDepth),
        options,
    };
    game.context.move = 1;
}

async function retryInvalidLevelTeleportPrompt() {
    const completedTries = (game._level_teleport_try_count || 0) + 1;
    if (completedTries >= 10) {
        await randomLevelTeleportFromPrompt();
        return;
    }
    game._level_teleport_text = '';
    game._level_teleport_try_count = completedTries;
    await setMessage(levelTeleportPromptQuestion());
}

function heavenVoiceMessage() {
    const role = game.urole?.name?.m || game._startup_role || 'Archeologist';
    const align = game.u?.ualign?.type ?? 0;
    const gods = GODS_BY_ROLE[role] || GODS_BY_ROLE.Archeologist;
    const god = gods[align > 0 ? 0 : align < 0 ? 2 : 1] || 'your god';
    return `The voice of ${god} rings out:  "Thou art early, but we'll admit thee."`;
}

async function finishEndgameNegativeLevelTeleport(target) {
    clearLevelTeleportTextPrompt();
    game._command_mode = null;
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    const levelLimit = Math.max(1, game.dungeons?.[current.dnum]?.num_dunlevs ?? current.dlevel);
    if (target <= -levelLimit) {
        await setMessage("You can't get there from here.");
        return;
    }
    await finishLevelTeleport(
        { dnum: current.dnum, dlevel: levelLimit + target },
        { suppressMaterialize: true, endgameLevelTeleport: true },
    );
}

async function finishNegativeLevelTeleport(target) {
    clearLevelTeleportTextPrompt();
    game._command_mode = null;

    const messages = [];
    if (target <= -10) {
        messages.push('You arrive in heaven.');
        messages.push(heavenVoiceMessage());
        game._death_cause = 'went to heaven prematurely';
    } else if (target === -9) {
        messages.push('You feel deliriously happy.');
        messages.push("(In fact, you're on Cloud 9!)");
    } else {
        messages.push('You are now high above the clouds...');
    }

    if (target > -10 && game.u?.levitating) {
        messages.push('You float gently down to earth.');
    } else if (target > -10 && game.u?.flying) {
        messages.push('You fly down to the ground.');
    } else if (target > -10) {
        messages.push("Unfortunately, you don't know how to fly.");
        messages.push('You plummet a few thousand feet to your death.');
        game._death_cause = `teleported out of the dungeon and fell to ${game.flags?.female ? 'her' : 'his'} death`;
    }

    if (target > -10 && (game.u?.levitating || game.u?.flying)) {
        const followup = consumePendingLevelTeleportTrapFollowup();
        if (followup) messages.push(followup);
        await beginEscapedGame(messages);
        return;
    }

    if (consumeLifeSavingAmulet()) {
        if (game.u) game.u.uhp = 0;
        game._life_saving_level_teleport_escape_message = 'You find yourself back on the surface.';
        game._command_mode = 'lifeSavingMore';
        await setMessage(`${messages.join('  ')}  But wait...  Your medallion begins to glow!`, true);
        return;
    }

    game._death_no_bones = 1;
    game._death_outside_dungeon = target <= -10 ? 'heaven' : 'surface';
    game._death_negative_level_teleport_escape_message = 'You find yourself back on the surface.';
    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._command_mode = 'deathDieMore';
    prepareDeathBones();
    await setMessage(`${messages.join('  ')}  You die...`, true);
}

async function beginEscapedGame(messages) {
    game._escaped_from_dungeon = 1;
    game._escaped_game = 1;
    game._quit_game = 0;
    game._death_cause = 'escaped';
    game._death_moves ||= game.moves || 1;
    game._pending_time_passed = 0;
    game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._deferred_level_goto = null;
    game._command_mode = 'escapedMore';
    await setMessage((messages || []).filter(Boolean).join('  '), true);
}

async function finishNowhereLevelTeleport() {
    clearLevelTeleportTextPrompt();
    game._command_mode = null;
    const messages = [
        `You ${polyselfForm()?.silent ? 'writhe' : 'scream'} in agony as your body begins to warp...`,
        'You cease to exist.',
    ];
    if ((game.inventory || []).length)
        messages.push('Your possessions land on the floor with a thud.');
    game._death_cause = 'committed suicide';

    if (consumeLifeSavingAmulet()) {
        if (game.u) game.u.uhp = 0;
        game._command_mode = 'lifeSavingMore';
        await setMessage(`${messages.join('  ')}  You die...  But wait...  Your medallion begins to glow!`, true);
        return;
    }

    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._command_mode = 'deathDieMore';
    prepareDeathBones();
    await setMessage(`${messages.join('  ')}  You die...`, true);
}

function currentLevelMarker(level) {
    return level && game.u?.uz?.dnum === level.dnum && game.u?.uz?.dlevel === level.dlevel ? '*' : ' ';
}

function questLevelKind(level = game.u?.uz) {
    if (!level || game.dungeons?.[level.dnum]?.name !== 'The Quest') return null;
    const dungeon = game.dungeons?.[level.dnum];
    const special = game.specialLevels?.find(slev => slev.dnum === level.dnum && slev.dlevel === level.dlevel);
    if (special?.name === 'x-strt') return 'start';
    if (special?.name === 'x-loca') return 'locate';
    if (special?.name === 'x-goal') return 'goal';
    if (level.dlevel === 1) return 'start';
    if (level.dlevel === 3) return 'locate';
    if (level.dlevel === (dungeon?.num_dunlevs ?? 5)) return 'goal';
    return 'fill';
}

function okToQuest() {
    const quest = game.quest_status || {};
    if (quest.killed_leader) return true;
    if (!(quest.got_quest || quest.got_thanks)) return false;
    const originalType = game._startup_align === 'lawful' ? 1 : game._startup_align === 'chaotic' ? -1 : 0;
    return (game.u?.ualign?.record ?? 0) >= MIN_QUEST_ALIGN
        && (game.u?.ualign?.type ?? 0) === originalType;
}

function questDownBlocked() {
    return questLevelKind() === 'start' && !okToQuest();
}

function questPagerText(msgid, { initCore = true } = {}) {
    const roleName = game.urole?.name?.m || game._startup_role || '';
    const info = QUEST_ROLE_DATA[roleName];
    let raw = info?.texts?.[msgid] || QUEST_COMMON_TEXTS[msgid]
        || (msgid === 'goal_alt' ? info?.texts?.goal_next : '');
    if (Array.isArray(raw)) raw = raw.length ? raw[rn2(raw.length)] : '';
    if (!raw) return '';
    if (initCore) l_nhcore_init();
    const rankIndex = level => level <= 2 ? 0 : level <= 30 ? Math.trunc((level + 2) / 4) : 8;
    const rank = info.ranks[Math.min(rankIndex(game.u?.ulevel || 1), info.ranks.length - 1)];
    const minRank = info.ranks[Math.min(rankIndex(MIN_QUEST_LEVEL), info.ranks.length - 1)];
    const alignName = (game._startup_align || (game.u?.ualign?.type > 0 ? 'lawful' : game.u?.ualign?.type < 0 ? 'chaotic' : 'neutral')).toLowerCase();
    const pantheonRole = roleName === 'Priest' ? game._pantheon_role || roleName : roleName;
    const gods = GODS_BY_ROLE[pantheonRole] || GODS_BY_ROLE.Archeologist;
    const deity = gods[alignName === 'lawful' ? 0 : alignName === 'chaotic' ? 2 : 1];
    const player = game.plname || 'Hero';
    const sibling = game.flags?.female ? 'sister' : 'brother';
    const child = game.flags?.female ? 'daughter' : 'son';
    const cap = text => `${text.charAt(0).toUpperCase()}${text.slice(1)}`;
    const article = text => /^(?:the|a|an)\b/i.test(text) ? text : `${/^[aeiou]/i.test(text) ? 'an' : 'a'} ${text}`;
    const stripThe = text => String(text || '').replace(/^the\s+/i, '');
    const possessive = text => String(text || '').endsWith('s') ? `${text}'` : `${text}'s`;
    const roleClass = roleName || 'Adventurer';
    const malePronouns = { h: 'he', H: 'He', i: 'him', I: 'Him', j: 'his', J: 'His' };
    const leader = info.leaderText || info.leader;
    const replacements = [
        ['%nh', malePronouns.h], ['%nH', malePronouns.H], ['%ni', malePronouns.i],
        ['%nI', malePronouns.I], ['%nj', malePronouns.j], ['%nJ', malePronouns.J],
        ['%lh', malePronouns.h], ['%lH', malePronouns.H], ['%li', malePronouns.i],
        ['%lI', malePronouns.I], ['%lj', malePronouns.j], ['%lJ', malePronouns.J],
        ['%oh', 'it'], ['%oH', 'It'], ['%oi', 'it'], ['%oI', 'It'], ['%oj', 'its'], ['%oJ', 'Its'],
        ['%ca', article(roleClass)], ['%cA', cap(article(roleClass))], ['%cP', `${roleClass}s`], ['%c', roleClass],
        ['%x', game.u?.blind ? 'sense' : 'see'], ['%Z', 'The Dungeons of Doom'],
        ['%pC', cap(player)], ['%ra', article(rank)], ['%RC', cap(minRank)],
        ['%R', minRank], ['%r', rank], ['%ns', `${info.nemesis}'s`],
        ['%nt', stripThe(info.nemesis)],
        ['%nC', cap(info.nemesis)], ['%na', article(info.nemesis)],
        ['%n', info.nemesis], ['%O', `the ${info.artifactShort}`],
        ['%os', possessive(info.artifact)], ['%ot', stripThe(info.artifact)],
        ['%oC', cap(info.artifact)], ['%o', info.artifact],
        ['%ls', possessive(leader)], ['%lt', stripThe(leader)], ['%lC', cap(leader)], ['%l', leader],
        ['%S', child], ['%s', sibling],
        ['%i', info.intermed], ['%gP', cap(`${info.guardian}s`)], ['%g', info.guardian],
        ['%H', info.homebase], ['%ds', possessive(deity)], ['%dJ', 'His'], ['%d', deity], ['%a', alignName], ['%p', player],
    ];
    let text = raw.trimEnd();
    for (const [pattern, replacement] of replacements)
        text = text.split(pattern).join(replacement);
    return text;
}

function questPagerRows(msgid, options = {}) {
    const text = questPagerText(msgid, options);
    if (!text) return null;
    const rows = text.split('\n').slice(0, 23).map((line, row) => [row, 0, line]);
    rows.push([23, 0, '--More--']);
    return rows;
}

function queueQuestPager(msgid, mode = 'questIntroMore', options = {}) {
    const rows = questPagerRows(msgid, options);
    if (!rows) return false;
    game._queued_overlay_after_more = { lines: rows, clearRows: 24, hideStatus: true, mode };
    return true;
}

function showQuestPager(msgid, mode = 'questIntroMore', options = {}) {
    const rows = questPagerRows(msgid, options);
    if (!rows) return false;
    setOverlay(rows, 24, true);
    game._pending_message = 'text-window';
    game._message_more = 1;
    game._keep_pending_message = 1;
    game._command_mode = mode;
    return true;
}

function finishQuestLeaderTalkTurn() {
    const automatic = !!game._quest_leader_talk_automatic;
    game._quest_leader_talk_automatic = 0;
    if (!automatic) chatConsumeTurn();
}

function questArtifactNameKey(name) {
    return String(name || '')
        .trim()
        .replace(/^(?:an?|the)\s+/i, '')
        .toLowerCase()
        .replace(/[ -]+/g, '');
}

function carriedQuestArtifactForRole(info) {
    const expected = questArtifactNameKey(info?.artifact);
    if (!expected) return null;
    return (game.inventory || []).find(item => {
        const names = [
            item?.artifact,
            item?.oartifact,
            artifactObjectName(item),
            item?.actualKind,
            item?.kind,
            item?.name,
        ];
        return names.some(name => questArtifactNameKey(name) === expected);
    }) || null;
}

function carriedRealAmuletOfYendor() {
    return (game.inventory || []).find(item => {
        const name = String(item?.actualKind || item?.kind || '').toLowerCase();
        return item?.realAmuletOfYendor || name === 'amulet of yendor';
    }) || null;
}

function recordKnownAmuletOfYendorDiscovery() {
    game._discoveries ??= [];
    if (!game._discoveries.some(entry => entry.section === 'Amulets' && entry.name === 'amulet (Amulet of Yendor)'))
        game._discoveries.push({
            section: 'Amulets',
            name: 'amulet (Amulet of Yendor)',
            text: 'amulet (Amulet of Yendor)',
            starred: false,
            known: true,
        });
}

function identifyRealAmuletOfYendorForQuest() {
    const amulet = carriedRealAmuletOfYendor();
    if (!amulet) return;
    amulet.realAmuletOfYendor = true;
    amulet.kind = 'Amulet of Yendor';
    amulet.actualKind = 'Amulet of Yendor';
    amulet.appearance = 'Amulet of Yendor';
    amulet.dknown = true;
    identifyInventoryItem(amulet);
    recordKnownAmuletOfYendorDiscovery();
}

function heroCarriesBellOfOpening() {
    return (game.inventory || []).some(isBellOfOpeningItem);
}

function finishQuestLeaderArtifactReturn(info) {
    const artifact = carriedQuestArtifactForRole(info);
    const hasAmulet = heroHasAmuletOfYendor();
    if (!showQuestPager(hasAmulet ? 'hasamulet' : 'offeredit', 'questLeaderFollowupMore'))
        return false;
    if (!hasAmulet && !heroCarriesBellOfOpening())
        queueQuestPager('quest_complete_no_bell', 'questLeaderFollowupMore');
    game.quest_status.got_thanks = true;
    if (hasAmulet) identifyRealAmuletOfYendorForQuest();
    if (artifact) {
        game.u ??= {};
        game.u.uevent ??= {};
        game.u.uevent.qcompleted = 1;
        game.quest_status.qcompleted = true;
        artifact.dknown = true;
        identifyInventoryItem(artifact);
    }
    return true;
}

function queueQuestPline(msgid, more = true) {
    const text = questPagerText(msgid);
    if (!text) return false;
    game._queued_message_after_more = game._queued_message_after_more
        ? `${game._queued_message_after_more}  ${text}`
        : text;
    if (more) game._queued_message_more_after_more = 1;
    return true;
}

function showQuestPline(msgid, more = false) {
    const text = questPagerText(msgid);
    if (!text) return false;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    game._pending_message = text;
    game._message_more = more ? 1 : 0;
    game._keep_pending_message = 1;
    game._command_mode = null;
    return true;
}

function currentObjectHereMessage() {
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    const objectsHere = (game.level?.objects || [])
        .filter(obj => !obj.hidden && !obj.transientProjectile
            && obj !== game.u?.uball && obj !== game.u?.uchain
            && obj.ox === x && obj.oy === y && obj.otyp !== BOULDER);
    const goldHere = objectsHere.find(obj => obj.otyp === GOLD_PIECE || obj.glyph === '$');
    if (goldHere) {
        const amount = goldHere.quan || 1;
        return `You see here ${amount} gold piece${amount === 1 ? '' : 's'}.`;
    }
    const objHere = objectsHere[0];
    if (objHere?.kind === 'chest' || objHere?.otyp === CHEST)
        return `You see here a ${objHere.lknown && (objHere.locked || objHere.olocked) ? 'locked ' : ''}chest.`;
    if (objHere) return `You see here ${pickupObjectPhrase(objHere)}.`;
    const ballHere = game.u?.uball?.ox === x && game.u?.uball?.oy === y ? game.u.uball : null;
    if (ballHere) return `You see here ${pickupObjectPhrase(ballHere)}.`;
    return '';
}

function restoredLevelHiderNeedsRestrap(mon) {
    if (!mon || mon.mcan || mon.mundetected || mon.appearObj != null || mon.appearGlyph || M_AP_TYPE(mon))
        return false;
    const name = mon.data?.name || '';
    const mlet = mon.data?.mlet || '';
    return mlet === 'mimic'
        || name === 'lurker above'
        || name === 'trapper'
        || name === 'rock piercer'
        || name === 'iron piercer'
        || name === 'glass piercer';
}

function restoredLevelMonsterCatchup(monsters, elapsed) {
    if (elapsed <= 0) return;
    const restoreOrder = [...(monsters || [])].reverse();
    for (const mon of restoreOrder) {
        if (elapsed <= rnd(10)) continue;
        if (!restoredLevelHiderNeedsRestrap(mon)) continue;
        if (rn2(3)) continue;
        if (mon.data?.mlet === 'mimic') {
            if (!mon.msleeping && !mon.mfrozen) set_mimic_sym_rng(mon);
        } else {
            mon.mundetected = true;
            newsym(mon.mx, mon.my);
        }
    }
}

function queueQuestArrival(targetLevel, fromLevel, targetIsNew, showNow = false) {
    const pager = showNow ? showQuestPager : queueQuestPager;
    const kind = questLevelKind(targetLevel);
    if (!kind || kind === 'fill') return false;
    game.quest_status ??= {};
    const fromAbove = fromLevel.dnum !== targetLevel.dnum || fromLevel.dlevel < targetLevel.dlevel;
    if (kind === 'start') {
        if (!game.quest_status.first_start) {
            game.quest_status.first_start = true;
            return pager('firsttime');
        }
        if (!fromAbove) return false;
        if ((game.quest_status.not_ready || 0) <= 2) {
            const text = questPagerText('nexttime');
            if (!text) return false;
            if (showNow) {
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._pending_message = text;
                game._message_more = 0;
                game._keep_pending_message = 1;
                game._command_mode = null;
            } else {
                game._queued_message_after_more = game._queued_message_after_more
                    ? `${game._queued_message_after_more}  ${text}`
                    : text;
                game._queued_message_more_after_more = 0;
            }
            return true;
        }
        return pager((game.quest_status.not_ready || 0) <= 2 ? 'nexttime' : 'othertime');
    }
    if (kind === 'locate') {
        if (!game.quest_status.first_locate) {
            game.quest_status.first_locate = true;
            if (!fromAbove) return false;
            if ((game.urole?.name?.m || game._startup_role) === 'Wizard')
                return showNow ? showQuestPline('locate_first', false) : queueQuestPline('locate_first', false);
            return pager('locate_first');
        }
        return fromAbove && (showNow ? showQuestPline('locate_next', true) : queueQuestPline('locate_next', true));
    }
    if (targetIsNew && !game.quest_status.made_goal) {
        game.quest_status.made_goal = 1;
        return pager('goal_first');
    }
    if ((game.quest_status.made_goal || 0) < 7) game.quest_status.made_goal = (game.quest_status.made_goal || 1) + 1;
    const queuedGoal = showNow ? showQuestPline('goal_next') : queueQuestPline('goal_next', true);
    if (queuedGoal && !showNow) game._quest_arrival_look_here_after_more = 1;
    return queuedGoal;
}

function atDungeonEntrance(level, dungeonName) {
    const dnum = game.dungeons?.findIndex(dungeon => dungeon?.name === dungeonName) ?? -1;
    if (dnum < 0 || !level) return false;
    return (game.branches || []).some(branch =>
        branch.end2?.dnum === dnum
        && branch.end1?.dnum === level.dnum
        && branch.end1?.dlevel === level.dlevel);
}

function queueQuestPortalCall(targetLevel, fromLevel) {
    if (!targetLevel || !fromLevel) return false;
    if (game.dungeons?.[fromLevel.dnum]?.name === 'The Quest') return false;
    if (!atDungeonEntrance(targetLevel, 'The Quest')) return false;
    const quest = game.quest_status ??= {};
    if (quest.qcompleted || quest.qexpelled || quest.got_thanks || quest.killed_leader) return false;
    const msgid = !quest.qcalled
        ? 'quest_portal'
        : (game.urole?.name?.m || game._startup_role) === 'Rogue'
        ? 'quest_portal_demand'
        : 'quest_portal_again';
    if (!quest.qcalled) quest.qcalled = 1;
    const text = questPagerText(msgid);
    if (!text) return false;
    const lines = text.split('\n').filter(line => line.length);
    if (!lines.length) return false;
    game._queued_messages_after_more ??= [];
    for (let i = 0; i < lines.length; i++)
        game._queued_messages_after_more.push({ text: lines[i], more: i < lines.length - 1 });
    return true;
}

async function continueQuestLeaderTalkAfterIntro() {
    game.quest_status ??= {};
    const leader = game._quest_leader_talk_mon || null;
    game._quest_leader_talk_mon = null;
    if (questLevelKind() !== 'start') {
        game._command_mode = null;
        finishQuestLeaderTalkTurn();
        return;
    }
    if ((game.u?.ulevel || 1) < MIN_QUEST_LEVEL) {
        game._quest_reject_exercise_wis = 1;
        showQuestPager('badlevel', 'questLeaderRejectMore');
        return;
    }
    const record = game.u?.ualign?.record ?? 0;
    const alignType = alignmentTypeFromValue(game.u?.ualign?.type) ?? startupAlignmentType();
    const originalType = startupAlignmentType();
    const currentBaseType = currentAlignmentBaseType();
    if (currentBaseType !== originalType) {
        game.quest_status.pissed_off = true;
        if (!game._quest_leader_talk_automatic && leader) {
            leader.mpeaceful = 0;
            leader.angry = true;
            leader.hostile = true;
        }
        showQuestPager('banished', 'questLeaderRejectMore');
        return;
    }
    const pure = record >= MIN_QUEST_ALIGN && alignType === originalType && currentBaseType === originalType;
    if (!pure && game.flags?.debug && record < MIN_QUEST_ALIGN) {
        await setMessage(`You are currently ${record} and require ${MIN_QUEST_ALIGN}.`, true);
        game._command_mode = 'questLeaderStatusMore';
        return;
    }
    if (!pure) {
        game.quest_status.not_ready = 1;
        game._quest_reject_exercise_wis = 1;
        showQuestPager('badalign', 'questLeaderRejectMore');
        return;
    }
    game.quest_status.got_quest = true;
    game._quest_assignquest_time_after_more = 1;
    showQuestPager('assignquest', 'questIntroMore');
}

export function maybeQueueQuestLeaderTalk(mon, { automatic = true } = {}) {
    const roleName = game.urole?.name?.m || game._startup_role || '';
    const info = QUEST_ROLE_DATA[roleName];
    const adjacent = Math.max(Math.abs(mon.mx - (game.u?.ux || 0)), Math.abs(mon.my - (game.u?.uy || 0))) <= 1;
    if (!info || mon.data?.name !== info.leader || !adjacent || mon.msleeping) return false;
    if (automatic && roleName === 'Priest' && currentSpecialLevelName() === 'x-strt') return false;
    game.quest_status ??= {};
    if (!automatic) chatClearTargetWaitStrategy(mon);
    if (automatic && !mon.mpeaceful) {
        chatClearTargetWaitStrategy(mon);
        const firstOffense = !game.quest_status.pissed_off;
        game.quest_status.pissed_off = true;
        if (!firstOffense) return false;
        game._quest_leader_talk_automatic = 1;
        if (!showQuestPager('leader_last', 'questLeaderFollowupMore')) {
            game._quest_leader_talk_automatic = 0;
            return false;
        }
        return true;
    }
    if (automatic && questLevelKind() !== 'start') return false;
    if (game.quest_status.pissed_off) {
        if (!automatic) {
            mon.mpeaceful = 0;
            mon.angry = true;
            mon.hostile = true;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            chatConsumeTurn();
            return true;
        }
        return false;
    }
    if (game.u?.uhave?.questart && !game.quest_status.met_nemesis)
        game.quest_status.cheater = true;
    if (game.quest_status.got_thanks) {
        const hasAmulet = heroHasAmuletOfYendor();
        const msgid = hasAmulet ? 'hasamulet' : 'posthanks';
        game._quest_leader_talk_automatic = automatic ? 1 : 0;
        if (!showQuestPager(msgid, 'questLeaderFollowupMore')) {
            game._quest_leader_talk_automatic = 0;
            return false;
        }
        if (hasAmulet) {
            game.quest_status.got_thanks = true;
            identifyRealAmuletOfYendorForQuest();
        }
        return true;
    }
    if (game.u?.uhave?.questart) {
        game._quest_leader_talk_automatic = automatic ? 1 : 0;
        if (!finishQuestLeaderArtifactReturn(info)) {
            game._quest_leader_talk_automatic = 0;
            return false;
        }
        return true;
    }
    if (game.quest_status.got_quest && !game.quest_status.got_thanks) {
        const message = questPagerText('encourage', { initCore: false });
        if (!message) return false;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        game._pending_message = message;
        game._message_more = 0;
        game._keep_pending_message = 1;
        game._command_mode = null;
        if (!automatic) chatConsumeTurn();
        return true;
    }
    const firstLeaderTalk = !game.quest_status.met_leader_once;
    game.quest_status.met_leader = true;
    if (firstLeaderTalk) game.quest_status.not_ready = 0;
    game._quest_leader_talk_mon = mon;
    game._quest_leader_talk_automatic = automatic ? 1 : 0;
    const pager = game._pending_message && game._message_more ? queueQuestPager : showQuestPager;
    if (!pager(firstLeaderTalk ? 'leader_first' : 'leader_next', 'questLeaderIntroMore')) {
        game._quest_leader_talk_automatic = 0;
        return false;
    }
    game.quest_status.met_leader_once = true;
    return true;
}

function questMonsterSoundKey(mon) {
    const data = mon?.data || {};
    const explicit = mon?.msound ?? mon?.sound ?? data.msound ?? data.sound;
    if (explicit != null) return String(explicit).toLowerCase().replace(/^ms_/, '');
    if (mon?.nemesis || data.nemesis) return 'nemesis';
    return '';
}

function questMonsterNameKey(mon) {
    return String(mon?.data?.name || mon?.name || '').toLowerCase();
}

function questMonsterAdjacent(mon) {
    if (!mon) return false;
    const dx = Math.abs((mon.mx || 0) - (game.u?.ux || 0));
    const dy = Math.abs((mon.my || 0) - (game.u?.uy || 0));
    return Math.max(dx, dy) <= 1 && !(questMonsterNameKey(mon) === 'grid bug' && dx && dy);
}

function questMonsterHelpless(mon) {
    return !mon || !!mon.msleeping || mon.mcanmove === false;
}

function questMonsterHasWaitMask(mon) {
    if (Number.isInteger(mon?.mstrategy)) return !!(mon.mstrategy & STRAT_WAITMASK);
    return mon?.mstrategy === 'waitforu' || !!mon?.waiting;
}

function questMonsterVisibleToHero(mon) {
    if (!mon || game.u?.blind || mon.minvis || mon.mundetected) return false;
    if (mon.mx <= 0 || mon.my < 0 || mon.mx >= COLNO || mon.my >= ROWNO) return false;
    return !!(game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT) && couldsee(mon.mx, mon.my);
}

function showAutomaticQuestPager(msgid) {
    game._quest_leader_talk_automatic = 1;
    if (!showQuestPager(msgid, 'questLeaderFollowupMore')) {
        game._quest_leader_talk_automatic = 0;
        return false;
    }
    return true;
}

function maybeQueueQuestNemesisTalk(mon, { automatic = true, inBattle = null } = {}) {
    if (!automatic || questMonsterSoundKey(mon) !== 'nemesis'
        || questMonsterHelpless(mon) || !questMonsterAdjacent(mon))
        return false;
    game.quest_status ??= {};
    const battle = inBattle == null
        ? (typeof game.quest_status.in_battle === 'boolean'
            ? game.quest_status.in_battle
            : (!questMonsterHelpless(mon) && questMonsterAdjacent(mon)))
        : !!inBattle;
    game.quest_status.in_battle = battle;
    if (battle) {
        if (rn2(5)) return false;
        return showAutomaticQuestPager('discourage');
    }

    const madeGoal = Math.max(0, Math.trunc(Number(game.quest_status.made_goal || 0)));
    let msgid = '';
    if (game.u?.uhave?.questart) msgid = 'nemesis_wantsit';
    else if (madeGoal === 1 || !game.quest_status.met_nemesis) msgid = 'nemesis_first';
    else if (madeGoal < 4) msgid = 'nemesis_next';
    else if (madeGoal < 7) msgid = 'nemesis_other';
    else {
        if (rn2(5)) return false;
        msgid = 'discourage';
    }
    if (!showAutomaticQuestPager(msgid)) return false;
    if (madeGoal < 7) game.quest_status.made_goal = madeGoal + 1;
    game.quest_status.met_nemesis = true;
    return true;
}

function angerQuestPrisonerGuards(prisoner) {
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon === prisoner) continue;
        const name = questMonsterNameKey(mon);
        if (name !== 'watchman' && name !== 'watch captain') continue;
        mon.mpeaceful = 0;
        mon.hostile = true;
        mon.angry = true;
    }
}

function maybeQueueQuestPrisonerTalk(mon, { automatic = true } = {}) {
    if (!automatic || questMonsterSoundKey(mon) !== 'djinni'
        || questMonsterNameKey(mon) !== 'prisoner' || questMonsterHelpless(mon)
        || !questMonsterHasWaitMask(mon))
        return false;
    const message = questMonsterVisibleToHero(mon)
        ? `${fireScrollMonsterName(mon)} speaks:  "I'm finally free!"`
        : `"I'm finally free!"`;
    chatClearTargetWaitStrategy(mon);
    mon.waiting = false;
    mon.mpeaceful = 1;
    game.u ??= {};
    game.u.ualign ??= {};
    game.u.ualign.record = (game.u.ualign.record || 0) + 3;
    angerQuestPrisonerGuards(mon);
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    game._pending_message = message;
    game._message_more = 0;
    game._keep_pending_message = 1;
    game._command_mode = null;
    return true;
}

export function maybeQueueQuestTalk(mon, { automatic = true, inBattle = null } = {}) {
    if (maybeQueueQuestLeaderTalk(mon, { automatic })) return true;
    if (!automatic) return false;
    if (maybeQueueQuestNemesisTalk(mon, { automatic, inBattle })) return true;
    return maybeQueueQuestPrisonerTalk(mon, { automatic });
}

function hasPlacedKnoxBranch() {
    const knoxDnum = game.dungeons?.findIndex(dungeon => dungeon?.name === 'Fort Ludios') ?? -1;
    return (game.branches || []).some(branch =>
        branch.end2?.dnum === knoxDnum && branch.end1?.dnum >= 0 && branch.end1.dnum < (game.dungeons?.length || 0));
}

function teleportMenuLevelDepth(level) {
    const dungeon = game.dungeons?.[level?.dnum];
    return dungeon && level ? dungeon.depth_start + level.dlevel - 1 : 0;
}

function teleportMenuBranchDepth(dnum) {
    const branch = game.branches?.find(item => item.end2.dnum === dnum);
    return branch ? teleportMenuLevelDepth(branch.end1) : 0;
}

function teleportMenuSpecialDepth(name) {
    const level = game.specialLevels?.find(item => item.name === name);
    return level ? teleportMenuLevelDepth(level) : 0;
}

function teleportMenuQuestBeforeBigrm() {
    const questDepth = teleportMenuBranchDepth(3);
    const bigrmDepth = teleportMenuSpecialDepth('bigrm');
    return questDepth && bigrmDepth ? questDepth <= bigrmDepth : false;
}

function levelTeleportMenuTarget(ch, page2 = false) {
    const hasKnox = hasPlacedKnoxBranch();
    const questBeforeBigrm = teleportMenuQuestBeforeBigrm();
    const direct = page2
        ? (hasKnox
            ? {
                w: specialLevel('sanctum'), x: specialLevel('minetn'), y: specialLevel('minend'),
                z: specialLevel('x-strt'), A: specialLevel('x-loca'), B: specialLevel('x-goal'),
                C: specialLevel('soko1'), D: specialLevel('soko2'), E: specialLevel('soko3'), F: specialLevel('soko4'),
                G: specialLevel('knox'), H: specialLevel('tower1'), I: specialLevel('tower2'), J: specialLevel('tower3'),
                K: specialLevel('astral'),
            }
            : {
                w: specialLevel('minetn'), x: specialLevel('minend'),
                y: specialLevel('x-strt'), z: specialLevel('x-loca'), A: specialLevel('x-goal'),
                B: specialLevel('soko1'), C: specialLevel('soko2'), D: specialLevel('soko3'), E: specialLevel('soko4'),
                G: specialLevel('tower1'), H: specialLevel('tower2'), I: specialLevel('tower3'),
                J: specialLevel('astral'),
            })
        : (hasKnox
            ? {
                a: branchLevel(7), b: branchLevel(2), c: specialLevel('oracle'), d: branchLevel(4),
                e: branchLevel(3), f: specialLevel('bigrm'), g: specialLevel('rogue'), h: specialLevel('knox'),
                i: specialLevel('medusa'), j: branchLevel(1), k: specialLevel('castle'),
            }
            : {
                a: branchLevel(7), b: branchLevel(2), c: specialLevel('oracle'), d: branchLevel(4),
                e: questBeforeBigrm ? branchLevel(3) : specialLevel('bigrm'),
                f: questBeforeBigrm ? specialLevel('bigrm') : branchLevel(3),
                g: specialLevel('rogue'),
                h: specialLevel('medusa'), i: branchLevel(1), j: specialLevel('castle'),
            });
    if (direct[ch]) return direct[ch];

    const entries = gehennomTeleportEntries();
    const offset = page2 ? 't'.charCodeAt(0) : (hasKnox ? 'l'.charCodeAt(0) : 'k'.charCodeAt(0));
    const index = ch.charCodeAt(0) - offset + (page2 ? (hasKnox ? 8 : 9) : 0);
    return entries[index]?.level;
}

function levelTeleportMenuPage3Target(ch) {
    const hasKnox = hasPlacedKnoxBranch();
    const direct = hasKnox
        ? {
            L: specialLevel('water'), M: specialLevel('fire'), N: specialLevel('air'), O: specialLevel('earth'),
            Q: specialLevel('tut-1'), R: specialLevel('tut-2'),
        }
        : {
            K: specialLevel('water'), L: specialLevel('fire'), M: specialLevel('air'), N: specialLevel('earth'),
            P: specialLevel('tut-1'), Q: specialLevel('tut-2'),
        };
    return direct[ch];
}

function monsterHasAmuletOfYendor(mon) {
    return (mon?.minvent || []).some(item =>
        item.realAmuletOfYendor
        || String(item.actualKind || item.kind || '').toLowerCase() === 'amulet of yendor');
}

function followsHeroAcrossLevels(mon) {
    if (!mon) return false;
    if (mon.pet || mon.mtame) return true;
    if (mon.iswiz) return !monsterHasAmuletOfYendor(mon);
    if (mon.isshk || mon.data?.shopkeeper) return true;
    if (mon.data?.mercenary || mon.data?.stalk || STALKER_MONSTERS.has(mon.data?.name))
        return !mon.mflee || heroHasAmuletOfYendor();
    return false;
}

function levelChangeFollowerNearHero(mon) {
    if (!followsHeroAcrossLevels(mon)) return false;
    const dx = (mon.mx || 0) - (game.u?.ux || 0);
    const dy = (mon.my || 0) - (game.u?.uy || 0);
    if (dx * dx + dy * dy >= 3) return false;
    if (mon.msleeping || mon.mfrozen || mon.mcanmove === false) return false;
    if (mon.waiting || mon.mstrategy === 'waitforu') return false;
    return true;
}

function placeFollowerAfterLevelChange(follower, first = false) {
    if (!follower) return;
    const nextToHero = rn2(follower.mtame ? 10 : follower.mpeaceful ? 5 : 2);
    let spot = { x: game.u.ux, y: game.u.uy };
    if (nextToHero) {
        const spots = arrivalNearbyCoords(game.u.ux, game.u.uy);
        const open = pos => {
            const loc = game.level?.at(pos.x, pos.y);
            const blockedByMonster = (game.level?.monsters || [])
                .some(mon => mon.mx === pos.x && mon.my === pos.y);
            const blockedByBoulder = (game.level?.objects || [])
                .some(obj => obj.otyp === BOULDER && obj.ox === pos.x && obj.oy === pos.y);
            const closedDoor = loc?.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED));
            return loc && ACCESSIBLE(loc.typ) && !closedDoor && !blockedByMonster && !blockedByBoulder;
        };
        spot = spots.find(open) || arrivalNearbyCoords(game.u.ux, game.u.uy, 0).slice(spots.length).find(open) || spot;
    }
    follower.mx = spot.x;
    follower.my = spot.y;
    follower.mux = game.u.ux;
    follower.muy = game.u.uy;
    updateMonsterTrack(follower);
    if (first) game.level.monsters.unshift(follower);
    else game.level.monsters.push(follower);
    resolveArrivalCollision(follower);
}

function arrivalObjectListRows(objects) {
    const sorted = [...objects]
        .map((obj, index) => ({ obj, index }))
        .sort((a, b) => {
            const corpseOrder = ((b.obj.otyp === 'corpse' || b.obj.otyp === CORPSE) ? 1 : 0)
                - ((a.obj.otyp === 'corpse' || a.obj.otyp === CORPSE) ? 1 : 0);
            if (corpseOrder) return corpseOrder;
            if (a.obj.pileIndex != null || b.obj.pileIndex != null)
                return (a.obj.pileIndex || 0) - (b.obj.pileIndex || 0);
            return b.index - a.index;
        })
        .map(item => item.obj);
    const attachedOnly = sorted.length > 0
        && sorted.every(obj => obj === game.u?.uchain || obj === game.u?.uball);
    const listCol = game.level?._object_list_col ?? (attachedOnly ? 40 : 41);
    const header = game.u?.blind ? 'Things that you feel here:' : 'Things that are here:';
    const rows = [[0, listCol, header]];
    for (let i = 0; i < sorted.length; i++) {
        const obj = sorted[i];
        rows.push([i + 1, listCol, obj.otyp === BOULDER ? 'a boulder'
            : BAG_OBJECT_TYPES.has(obj.otyp) ? 'a bag' : pickupObjectPhrase(obj)]);
    }
    rows.push([sorted.length + 1, listCol, '--More--']);
    return rows;
}

function rememberObjectsAtHero() {
    const ux = game.u?.ux;
    const uy = game.u?.uy;
    if (!game.level?.objects || ux == null || uy == null) return;
    for (const obj of game.level.objects) {
        if (obj.hidden || obj.transientProjectile) continue;
        if (obj.ox !== ux || obj.oy !== uy) continue;
        obj.seen = true;
        obj._hide_until_seen = false;
    }
}

function deathCountAdverb(count) {
    if (count === 1) return 'once';
    if (count === 2) return 'twice';
    if (count === 3) return 'thrice';
    return `${count} times`;
}

function removeCarriedPunishmentObjects(objects) {
    if (!objects.length || !game.level?.objects) return;
    const positions = objects.map(obj => ({ x: obj.ox, y: obj.oy }));
    game.level.objects = game.level.objects.filter(obj => !objects.includes(obj));
    for (const pos of positions) {
        const loc = game.level.at(pos.x, pos.y);
        if (!loc) continue;
        loc.remembered_glyph = null;
        newsym(pos.x, pos.y);
    }
}

function impactDropGateText(trap) {
    if (trap?.gateText) return trap.gateText;
    if (!trap?.tseen || (trap.ttyp !== HOLE && trap.ttyp !== TRAPDOOR))
        return '';
    return trap.ttyp === TRAPDOOR ? 'through the trap door' : 'through the hole';
}

function impactDropLevelKey(level) {
    return level ? `${level.dnum}:${level.dlevel}` : '';
}

function currentMigrationSourceLevel() {
    return {
        dnum: game.u?.uz?.dnum ?? 0,
        dlevel: game.u?.uz?.dlevel ?? 1,
    };
}

function markImpactDropMigration(obj, route = {}) {
    if (!obj || !route.where || route.where === MIGR_RANDOM) return;
    obj._impactDropMigration = {
        where: route.where,
        fromLevel: { ...(route.fromLevel || currentMigrationSourceLevel()) },
        targetLevel: route.targetLevel ? { ...route.targetLevel } : null,
        isLadder: !!route.isLadder,
    };
}

function queueImpactDroppedObjects(targetLevel, objects, route = {}) {
    if (!targetLevel || !objects?.length) return;
    game._impact_drop_migrations ??= new Map();
    const key = impactDropLevelKey(targetLevel);
    const queued = game._impact_drop_migrations.get(key) || [];
    for (const obj of objects) markImpactDropMigration(obj, { ...route, targetLevel });
    queued.push(...objects);
    game._impact_drop_migrations.set(key, queued);
}

function emptyImpactDropResult(overrides = {}) {
    return {
        message: '',
        objects: [],
        objectCount: 0,
        fallenCount: 0,
        ...overrides,
    };
}

function impactDropRandomLandingSpot() {
    const candidates = [];
    for (let x = 1; x < COLNO; x++)
        for (let y = 0; y < ROWNO; y++) {
            const loc = game.level?.at(x, y);
            if (!loc || !ACCESSIBLE(loc.typ)) continue;
            candidates.push({ x, y });
        }
    return candidates.length ? candidates[rn2(candidates.length)]
        : { x: game.u?.ux || 0, y: game.u?.uy || 0 };
}

function impactDropMigrationLandingSpot(migration) {
    if (!migration || migration.where === MIGR_RANDOM) return null;
    const from = migration.fromLevel;
    if (!from) return null;
    const needLadder = migration.where === MIGR_LADDER_UP || migration.isLadder;
    for (let stair = game.stairs; stair; stair = stair.next) {
        if (!!stair.isladder !== !!needLadder) continue;
        if (stair.tolev?.dnum === from.dnum && stair.tolev?.dlevel === from.dlevel)
            return { x: stair.sx, y: stair.sy };
    }
    return null;
}

function impactDropDeliveryLandingIsSoft(x, y) {
    const loc = game.level?.at?.(x, y);
    return !!(loc && IS_SOFT(loc.typ));
}

function impactDropScatterBlocked(loc) {
    if (!loc) return true;
    const closedDoor = loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED));
    return !ZAP_POS(loc.typ) || closedDoor;
}

function impactDropScatterExactDeliveryObject(obj, sx, sy) {
    if (!obj || !game.level) return;
    const blastforce = rnd(2);
    const dir = rn2(N_DIRS);
    const dx = xdir[dir] || 0;
    const dy = ydir[dir] || 0;
    const scatterForce = Math.max(1, blastforce - Math.trunc(floorEffectsObjectWeight(obj) / 40));
    let range = rnd(scatterForce);
    let x = sx;
    let y = sy;

    while (range-- > 0) {
        const nx = x + dx;
        const ny = y + dy;
        if (!isok(nx, ny)) break;
        const loc = game.level.at(nx, ny);
        if (impactDropScatterBlocked(loc)) break;
        x = nx;
        y = ny;
        if (loc.typ === SINK) break;
    }

    obj.ox = x;
    obj.oy = y;
    obj.hidden = false;
    obj.transientProjectile = false;

    if (earthFloorEffects(obj, x, y, [], 'land')) {
        game.level.objects = (game.level.objects || []).filter(item => item !== obj);
    } else {
        stackDroppedFloorObject(obj);
    }
    newsym(x, y);
    if (x !== sx || y !== sy) newsym(sx, sy);
}

function deliverQueuedImpactDroppedObjects(targetLevel) {
    const key = impactDropLevelKey(targetLevel);
    const queued = game._impact_drop_migrations?.get?.(key) || [];
    if (!queued.length || !game.level) return;
    game._impact_drop_migrations.delete(key);
    game.level.objects ??= [];
    for (const obj of queued) {
        const exactSpot = impactDropMigrationLandingSpot(obj._impactDropMigration);
        const spot = exactSpot || impactDropRandomLandingSpot();
        delete obj._impactDropMigration;
        obj.ox = spot.x;
        obj.oy = spot.y;
        obj.hidden = false;
        obj.transientProjectile = false;
        if (!impactDropDeliveryLandingIsSoft(spot.x, spot.y) && impactDropObjectBreaks(obj)) {
            newsym(spot.x, spot.y);
            continue;
        }
        if (exactSpot) {
            game.level.objects.push(obj);
            stackDroppedFloorObject(obj);
            impactDropScatterExactDeliveryObject(obj, spot.x, spot.y);
        } else {
            game.level.objects.push(obj);
            newsym(spot.x, spot.y);
        }
    }
}

function impactDropCandidatePile(x, y, { missile = null } = {}) {
    return (game.level?.objects || []).filter(obj =>
        obj !== missile && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y);
}

function impactDropPileQuantity(pile) {
    return pile.reduce((sum, obj) => sum + (obj.quan || 1), 0);
}

function impactDropFloorObjects(x, y, trap, options = {}) {
    const gateText = impactDropGateText(trap);
    if (!gateText || !game.level?.objects?.length) return emptyImpactDropResult();
    if (!options.withHero && !options.targetLevel) return emptyImpactDropResult();
    const pile = impactDropCandidatePile(x, y, { missile: options.missile });
    if (!pile.length) return emptyImpactDropResult();

    let objectCount = 0;
    let fallenCount = 0;
    const fallen = [];
    for (const obj of pile) {
        const quantity = obj.quan || 1;
        objectCount += quantity;
        if (obj === game.u?.uball || obj === game.u?.uchain) continue;
        if (options.missileImpact && options.missile?.otyp === ROCK && obj.otyp === BOULDER) continue;
        if (rn2(obj.otyp === BOULDER ? 30 : 3)) continue;
        fallen.push(obj);
        fallenCount += quantity;
    }
    if (!fallen.length) return emptyImpactDropResult({ objectCount });

    game.level.objects = (game.level.objects || []).filter(obj => !fallen.includes(obj));
    newsym(x, y);

    let message = '';
    const visible = !game.u?.blind && (options.missileImpact ? cansee(x, y) : couldsee(x, y));
    if (visible) {
        const what = fallenCount === 1 ? 'object falls' : 'objects fall';
        if (options.missileImpact) {
            const qualifier = fallenCount === objectCount ? 'the ' : fallenCount === 1 ? 'an' : '';
            message = `From the impact, ${qualifier}other ${what}.`;
        } else if (fallenCount === objectCount) {
            message = `${fallenCount === 1 ? 'The' : 'All the'} adjacent ${what} ${gateText}.`;
        } else {
            message = `${fallenCount === 1 ? 'One of the' : 'Some of the'} adjacent ${fallenCount === 1 ? 'objects falls' : what} ${gateText}.`;
        }
    }
    let debitDelta = 0;
    let robbedDelta = 0;
    let debtShkp = null;
    for (const obj of fallen) {
        const debt = shipObjectShopDebt(obj, x, y, { shopFloorObj: true, silent: true });
        debitDelta += debt.debitDelta || 0;
        robbedDelta += debt.robbedDelta || 0;
        if (debt.shkp) debtShkp = debt.shkp;
    }
    if (debitDelta > 0)
        message = [message, `You owe ${shopkeeperDisplayName(debtShkp)} ${debitDelta} ${shopCurrency(debitDelta)} for goods lost.`]
            .filter(Boolean).join('  ');
    if (robbedDelta > 0)
        message = [message, `You removed ${robbedDelta} ${shopCurrency(robbedDelta)} worth of goods!`]
            .filter(Boolean).join('  ');

    if (!options.withHero) {
        queueImpactDroppedObjects(options.targetLevel, fallen, options.route || {});
        return emptyImpactDropResult({ message, objectCount, fallenCount });
    }
    return emptyImpactDropResult({ message, objects: fallen, objectCount, fallenCount });
}

function impactDropObjectClass(obj) {
    return obj?.cls || (obj?.otyp === POTION_CLASS || obj?.glyph === '!' ? 'potion'
        : obj?.otyp === SCROLL_CLASS || obj?.glyph === '?' ? 'scroll'
            : obj?.glyph === '+' ? 'spellbook'
                : obj?.glyph === '*' ? 'gem' : '');
}

function isGlassMaterialWandObject(obj) {
    if (!obj || !isWandItem(obj) || obj.artifact || obj.oartifact) return false;
    const material = String(obj.material || obj.oc_material || '').toLowerCase().replace(/^hi_/, '');
    if (material === 'glass' || material === 'crystal') return true;
    const appearance = String(
        obj.appearance || obj.wandAppearance || (obj.wandIndex != null ? game._object_descriptions?.wands?.[obj.wandIndex]?.description : ''),
    ).toLowerCase();
    if (appearance === 'glass' || appearance === 'crystal') return true;
    return /^(glass|crystal) wand$/.test(objectKindKey(obj));
}

function impactDropBreakKind(obj) {
    const cls = impactDropObjectClass(obj);
    const name = String(obj?.actualKind || obj?.kind || pickupObjectName(obj)).toLowerCase();
    if (cls === 'potion') return 'shatter';
    if (isBreaktestVenomObject(obj)) return 'splash';
    if (obj?.otyp === EGG || name === 'egg') return 'splat';
    if (name.includes('melon')) return 'splat';
    if (name.includes('cream pie')) return 'mess';
    if (isGlassMaterialWandObject(obj)) return 'pieces';
    if (obj?.otyp === EXPENSIVE_CAMERA || obj?.otyp === MIRROR
        || name.includes('mirror') || name.includes('looking glass')
        || name.includes('crystal ball') || name.includes('lenses'))
        return 'pieces';
    if (breaktestGlassMaterialObject(obj)) return 'pieces';
    if (cls !== 'gem' && /\bglass\b|\bcrystal\b/.test(name)) return 'shatter';
    return '';
}

function impactDropObjectBreaks(obj) {
    if (!obj) return '';
    const ordinaryResistChance = 1;
    const artifactResistChance = 99;
    const hasArtifact = objectHasArtifactIdentity(obj);
    if (rn2(100) < (hasArtifact ? artifactResistChance : ordinaryResistChance)) return '';
    if (hasArtifact && !breaktestExplicitSwitchObject(obj)) return '';
    return impactDropBreakKind(obj);
}

function objectHasArtifactIdentity(obj) {
    return !!(obj?.artifact || obj?.oartifact);
}

function breaktestExplicitSwitchObject(obj) {
    return isExpensiveCameraObject(obj) || isPotionObject(obj) || isEggItem(obj)
        || isCreamPieObject(obj) || isMelonObject(obj) || isBreaktestVenomObject(obj);
}

function breaktestGlassMaterialObject(obj) {
    if (!obj) return false;
    const cls = impactDropObjectClass(obj);
    if (cls === 'gem' || cls === 'armor' || obj.glyph === '[' || obj.otyp === ARMOR_CLASS)
        return false;
    const material = String(obj.material || obj.oc_material || '').toLowerCase().replace(/^hi_/, '');
    return material === 'glass';
}

function isBreaktestVenomObject(obj) {
    const kind = objectKindKey(obj);
    return obj?.otyp === BLINDING_VENOM || obj?.otyp === ACID_VENOM
        || kind === 'splash of blinding venom' || kind === 'splash of acid venom';
}

function shipObjectMuffledBreakResult(breakKind) {
    return breakKind === 'splat' || breakKind === 'splash' ? breakKind : 'crash';
}

function impactDropLandingIsSoft() {
    const loc = game.level?.at(game.u?.ux || 0, game.u?.uy || 0);
    if (!loc) return false;
    return IS_POOL(loc.typ) || loc.typ === WATER || loc.typ === MOAT
        || loc.typ === LAVAPOOL || loc.typ === LAVAWALL;
}

function deliverImpactDroppedObjects(objects) {
    if (!objects?.length || !game.level || !game.u) return '';
    const messages = [];
    const hardLanding = !impactDropLandingIsSoft();
    for (const obj of objects) {
        obj.ox = game.u.ux || 0;
        obj.oy = game.u.uy || 0;
        obj.hidden = false;
        obj.transientProjectile = false;
        const breakKind = hardLanding ? impactDropObjectBreaks(obj) : '';
        if (breakKind) {
            if (breakKind === 'splat') messages.push('Splat!');
            else if (breakKind === 'splash') messages.push('Splash!');
            else if (breakKind === 'mess') messages.push('What a mess!');
            else if (game.u.blind) messages.push('You hear something shatter!');
            else {
                const name = pickupObjectName({ ...obj, quan: obj.quan || 1 });
                const many = (obj.quan || 1) > 1;
                const subject = many ? `The ${name}` : (/^[aeiou]/i.test(name) ? `An ${name}` : `A ${name}`);
                const verb = many ? 'shatter' : 'shatters';
                messages.push(`${subject} ${verb}${breakKind === 'pieces' ? ' into a thousand pieces' : ''}!`);
            }
            brokenPotionBreathe(obj, game.u.ux || 0, game.u.uy || 0, messages);
            continue;
        }
        game.level.objects.push(obj);
    }
    newsym(game.u.ux || 0, game.u.uy || 0);
    return messages.join('  ');
}

function heroStairway() {
    for (let stair = game.stairs; stair; stair = stair.next)
        if (stair.sx === game.u?.ux && stair.sy === game.u?.uy) return stair;
    return null;
}

function placeRockAtHero() {
    const rock = mksobj(ROCK, false, false);
    Object.assign(rock, {
        otyp: ROCK,
        ox: game.u?.ux || 0,
        oy: game.u?.uy || 0,
        quan: 1,
        cls: 'gem',
        kind: 'rock',
        singular: 'rock',
        plural: 'rocks',
        glyph: '*',
    });
    game.level.objects ??= [];
    game.level.objects.push(rock);
    newsym(rock.ox, rock.oy);
}

function zapDigFallingRockMessage(stair = null) {
    const messages = [];
    if (stair)
        messages.push(`The beam bounces off the ${stair.isladder ? 'ladder' : 'stairs'} and hits the ceiling.`);
    messages.push('You loosen a rock from the ceiling.');
    messages.push('It falls on your head!');
    const helmet = wornEarthHelmet();
    const damage = rnd(helmet && hardEarthHelmet(helmet) ? 2 : 6);
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        if ((game.u.uhp || 0) <= 0) game._death_cause = 'falling rock';
    }
    placeRockAtHero();
    return trapMessage(...messages);
}

function dugHoleFallTarget() {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    return clampDungeonLevel({ dnum: current.dnum, dlevel: current.dlevel + 1 });
}

function downwardDigTrapAt(x, y) {
    return (game.level?.traps || []).find(trap => trap.tx === x && trap.ty === y) || null;
}

function removeDownwardDigTrap(trap) {
    if (!trap || !game.level?.traps) return;
    game.level.traps = game.level.traps.filter(candidate => candidate !== trap);
}

function downwardDigTooHardResult() {
    return { message: 'The floor here is too hard to dig in.', more: false };
}

function downwardDigBoulderResult(x, y, trap) {
    const boulder = (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y && obj.otyp === BOULDER);
    if (!boulder) return null;

    game.level.objects = (game.level?.objects || []).filter(obj => obj !== boulder);
    if (trap && (trap.ttyp === PIT || trap.ttyp === SPIKED_PIT) && rn2(2)) {
        trap.ttyp = PIT;
        trap.tseen = true;
        newsym(x, y);
        return { message: 'The boulder settles into the pit.', more: false };
    }

    removeDownwardDigTrap(trap);
    newsym(x, y);
    return { message: 'KADOOM!  The boulder falls in!', more: false };
}

async function digDownwardPitResult(options = {}) {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    if (Is_airlevel(current) || Is_waterlevel(current))
        return { message: options.pick ? 'You swing your pick through thin air.' : '', more: false };

    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    const trap = downwardDigTrapAt(x, y);
    if (!canDigDownFromLevel(current) && trap) return downwardDigTooHardResult();
    const boulderResult = downwardDigBoulderResult(x, y, trap);
    if (boulderResult) return boulderResult;
    if (isBuriedBallTrapActive()) buriedBallToPunishment();

    const pit = await maketrap(x, y, PIT);
    if (!pit) return { message: '', more: false };
    pit.madeby_u = true;
    pit.tseen = true;
    if (game.u) {
        if (!(game.u.levitating || game.u.flying)) {
            game.u.utrap = rn1(4, 2);
            game.u.utraptype = 'pit';
        } else {
            game.u.utrap = 0;
            game.u.utraptype = null;
        }
    }
    return { message: 'You dig a pit in the floor.', more: false };
}

async function digDownwardHoleResult(options = {}) {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    if (Is_airlevel(current) || Is_waterlevel(current))
        return { message: options.pick ? 'You swing your pick through thin air.' : '', more: false };

    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    const existingTrap = downwardDigTrapAt(x, y);
    if (!canDigDownFromLevel(current)) {
        if (existingTrap) return downwardDigTooHardResult();
        return digDownwardPitResult(options);
    }

    const boulderResult = downwardDigBoulderResult(x, y, existingTrap);
    if (boulderResult) return boulderResult;
    if (isBuriedBallTrapActive()) buriedBallToPunishment();

    const trap = await maketrap(x, y, HOLE);
    if (!trap) return { message: '', more: false };
    trap.madeby_u = true;
    trap.tseen = true;
    if (game.u) {
        game.u.utrap = 0;
        game.u.utraptype = null;
    }
    const messages = ['You dig a hole through the floor.'];
    const target = dugHoleFallTarget();
    const noFall = game.u?.levitating || game.u?.flying || game.u?.ustuck || !target;
    if (noFall) {
        const dropTarget = sitFallTargetLevel(trap) || target;
        const impact = impactDropFloorObjects(x, y, trap, { targetLevel: dropTarget });
        if (impact.message) messages.push(impact.message);
        return { message: trapMessage(...messages), more: false };
    }

    messages.push('You fall through...');
    const impact = impactDropFloorObjects(x, y, trap, { targetLevel: target, withHero: true });
    if (impact.message) messages.push(impact.message);
    scheduleSitLevelChange(target, {
        falling: true,
        fallingDamage: !game.u?.flying,
        arrivalMessage: '',
        impactDroppedObjects: impact.objects,
    });
    return { message: trapMessage(...messages), more: true };
}

function earthquakeLevelDescription() {
    const uz = game.u?.uz || { dnum: 0, dlevel: 1 };
    if (Is_astralevel(uz)) return 'astral plane';
    if (In_endgame(uz)) return 'plane';
    if (currentSpecialLevelName() === 'sanctum') return 'sanctum';
    if (In_sokoban(uz)) return 'puzzle';
    if (In_V_tower(uz)) return 'tower';
    return 'dungeon';
}

function isPitTrapType(type) {
    return type === PIT || type === SPIKED_PIT || type === TT_PIT || type === 'pit' || type === 'spiked pit';
}

function altarMask(loc) {
    return loc?.altarmask ?? loc?.flags ?? 0;
}

function earthquakeAltarAlignName(loc) {
    const align = Amask2align(altarMask(loc));
    if (align === A_LAWFUL) return 'lawful';
    if (align === A_NEUTRAL) return 'neutral';
    if (align === A_CHAOTIC) return 'chaotic';
    return 'unaligned';
}

function earthquakeFeatureMessage(loc) {
    if (loc?.typ === FOUNTAIN) return 'The fountain falls into a chasm.';
    if (loc?.typ === SINK) return 'The kitchen sink falls into a chasm.';
    if (loc?.typ === GRAVE) return 'The headstone topples into a chasm.';
    if (loc?.typ === THRONE) return 'The throne falls into a chasm.';
    if (loc?.typ === ALTAR) return `The ${earthquakeAltarAlignName(loc)} altar falls into a chasm.`;
    return '';
}

function normalizeEarthquakePitTerrain(x, y) {
    const loc = game.level?.at(x, y);
    if (!loc) return;
    const oldTyp = loc.typ;
    if (IS_ROOM(oldTyp)) loc.typ = ROOM;
    else if (oldTyp === STONE || oldTyp === SCORR) loc.typ = CORR;
    else if (IS_WALL(oldTyp) || oldTyp === SDOOR) loc.typ = game.level?.flags?.is_maze_lev ? ROOM
        : game.level?.flags?.is_cavernous_lev ? CORR
            : DOOR;
    loc.flags = 0;
    loc.doormask = D_NODOOR;
    loc.wall_info = 0;
}

function makeEarthquakePitTrap(x, y) {
    const existing = downwardDigTrapAt(x, y);
    if (existing && (existing.ttyp === MAGIC_PORTAL || existing.ttyp === VIBRATING_SQUARE)) return null;
    const trap = existing || { tx: x, ty: y };
    Object.assign(trap, {
        ttyp: PIT,
        tseen: true,
        once: false,
        launch: { x: -1, y: -1 },
        launch2: null,
        teledest: null,
        dst: { dnum: -1, dlevel: -1 },
        conjoined: 0,
    });
    game.level.traps ??= [];
    if (!existing) game.level.traps.push(trap);
    normalizeEarthquakePitTerrain(x, y);
    return trap;
}

function drawbridgeUnder(loc) {
    return (loc?.flags || 0) & DB_UNDER;
}

function movementSurfaceTerrain(loc) {
    if (!loc) return undefined;
    if (loc.typ !== DRAWBRIDGE_UP) return loc.typ;
    switch (drawbridgeUnder(loc)) {
    case DB_LAVA:
        return LAVAPOOL;
    case DB_ICE:
        return ICE;
    case DB_FLOOR:
        return ROOM;
    case DB_MOAT:
    default:
        return MOAT;
    }
}

function movementIsPoolAt(x, y, loc = game.level?.at(x, y)) {
    if (!loc) return false;
    if (loc.typ === POOL || loc.typ === MOAT || loc.typ === WATER) return true;
    return loc.typ === DRAWBRIDGE_UP
        && drawbridgeUnder(loc) === DB_MOAT
        && currentSpecialLevelName() !== 'juiblex';
}

function movementIsLavaAt(x, y, loc = game.level?.at(x, y)) {
    if (!loc) return false;
    return loc.typ === LAVAPOOL || loc.typ === LAVAWALL
        || (loc.typ === DRAWBRIDGE_UP && drawbridgeUnder(loc) === DB_LAVA);
}

function movementIsLiquidAt(x, y, loc = game.level?.at(x, y)) {
    return movementIsPoolAt(x, y, loc) || movementIsLavaAt(x, y, loc);
}

function earthquakeIsMoatAt(x, y) {
    const loc = game.level?.at(x, y);
    if (!loc || currentSpecialLevelName() === 'juiblex') return false;
    return loc.typ === MOAT || (loc.typ === DRAWBRIDGE_UP && drawbridgeUnder(loc) === DB_MOAT);
}

function earthquakeIsLavaAt(x, y) {
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    return IS_LAVA(loc.typ) || (loc.typ === DRAWBRIDGE_UP && drawbridgeUnder(loc) === DB_LAVA);
}

function earthquakeIsPoolAt(x, y) {
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    return loc.typ === POOL || loc.typ === MOAT || loc.typ === WATER || earthquakeIsMoatAt(x, y);
}

function earthquakeFillHoleType(x, y) {
    const loX = Math.max(1, x - 1);
    const hiX = Math.min(x + 1, COLNO - 1);
    const loY = Math.max(0, y - 1);
    const hiY = Math.min(y + 1, ROWNO - 1);
    let poolCnt = 0;
    let moatCnt = 0;
    let lavaCnt = 0;
    for (let x1 = loX; x1 <= hiX; x1++) {
        for (let y1 = loY; y1 <= hiY; y1++) {
            if (earthquakeIsMoatAt(x1, y1)) moatCnt++;
            else if (earthquakeIsPoolAt(x1, y1)) poolCnt++;
            else if (earthquakeIsLavaAt(x1, y1)) lavaCnt++;
        }
    }

    poolCnt = Math.trunc(poolCnt / 3);
    if (lavaCnt > moatCnt + poolCnt && rn2(lavaCnt + 1)) return LAVAPOOL;
    if (moatCnt > 0 && rn2(moatCnt + 1)) return MOAT;
    if (poolCnt > 0 && rn2(poolCnt + 1)) return POOL;
    return ROOM;
}

function setEarthquakeLiquidTerrain(x, y, typ) {
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    loc.typ = typ;
    loc.flags = 0;
    loc.doormask = 0;
    loc.wall_info = 0;
    if (typ === LAVAPOOL) loc.lit = true;
    newsym(x, y);
    return true;
}

function deleteEarthquakeLiquidTrap(trap, x, y) {
    if (game.u?.ux === x && game.u?.uy === y && game.u.utraptype !== TT_BURIEDBALL) {
        game.u.utrap = 0;
        game.u.utraptype = null;
    }
    const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
    if (mon) mon.mtrapped = 0;
    removeDownwardDigTrap(trap);
}

function applyEarthquakeHeroLiquidEffects(x, y, typ, messages) {
    if (game.u?.ux !== x || game.u?.uy !== y) return;
    if (game.u?.levitating || game.u?.flying) return;
    if (typ === LAVAPOOL) {
        if (game.u?.fireResistance) return;
        d(6, 6);
        game.u.uhp = 0;
        game._death_cause = 'burned by molten lava';
        messages.push('You fall into the molten lava!  You burn to a crisp...');
        messages.push('You die...');
        return;
    }
    game.u.uinwater = 1;
    game.u.underwater = true;
    game.u.uunderwater = true;
    messages.push('You fall into the pool of water!  You sink like a rock.');
}

function earthquakeLiquidFlow(x, y, typ, trap, messages) {
    if (!setEarthquakeLiquidTerrain(x, y, typ)) return false;
    deleteEarthquakeLiquidTrap(trap, x, y);
    objIceEffectsAt(x, y, { doBuried: true });
    unearthObjectsAt(x, y);
    messages.push(...applyLiquidFlowFloorObjectDamage(x, y, typ).messages);
    if (game.u?.ux === x && game.u?.uy === y) applyEarthquakeHeroLiquidEffects(x, y, typ, messages);
    else messages.push(...applyMonsterLiquidEffectsAt(x, y, { heroCaused: true, recordKill: recordVanquished }));
    newsym(x, y);
    return true;
}

function earthquakeMonsterName(mon, { capital = true, article = true } = {}) {
    if (mon?.givenName) return mon.givenName;
    if (mon?.isshk && mon.shknam) return mon.shknam;
    const name = mon?.data?.name || mon?.name || 'monster';
    if (!article) return name;
    return `${capital ? 'The' : 'the'} ${name}`;
}

function earthquakeMonsterVisible(mon) {
    return !!mon && !game.u?.blind && !mon.minvis && !mon.mundetected && couldsee(mon.mx, mon.my);
}

function earthquakeMonsterHumanoid(mon) {
    const mlet = mon?.data?.mlet;
    return mlet === 'humanoid' || mlet === 'human' || mon?.data?.name === 'human';
}

function earthquakeRevealMimic(mon) {
    if (!mon || (!M_AP_TYPE(mon) && mon.appearObj == null && !mon.appearGlyph)) return false;
    mon.m_ap_type = 0;
    mon.appearObj = null;
    mon.appearGlyph = null;
    mon.appearColor = null;
    return true;
}

function earthquakeShakeMonster(x, y, messages) {
    const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
    if (!mon) return;
    mon.msleeping = 0;
    mon.mcanmove = true;
    mon.mfrozen = 0;
    if (mon.mpeaceful && !mon.mtame && !mon.pet) mon.mpeaceful = 0;
    if (mon.mundetected) {
        const ceilingHider = !!mon.data?.ceilingHider;
        mon.mundetected = false;
        if (ceilingHider) {
            if (!game.u?.blind && couldsee(x, y))
                messages.push(`${earthquakeMonsterName(mon)} is shaken loose from the ceiling!`);
            else if (!mon.data?.flyer && !heroIsDeaf()) messages.push('You hear a thump.');
        }
        newsym(x, y);
    }
    if (earthquakeRevealMimic(mon)) newsym(x, y);
}

function earthquakeMonsterFalls(mon, messages) {
    if (!mon || mon.data?.flyer || mon.data?.clinger) return;
    const alreadyTrapped = !!mon.mtrapped;
    const visible = earthquakeMonsterVisible(mon);
    mon.mtrapped = 1;
    if (!alreadyTrapped) {
        if (visible) messages.push(`${earthquakeMonsterName(mon)} falls into a chasm!`);
        else if (earthquakeMonsterHumanoid(mon) && !heroIsDeaf()) messages.push('You hear a scream!');
    }
    mon.mhp = (mon.mhp ?? 1) - rnd(alreadyTrapped ? 4 : 6);
    if ((mon.mhp ?? 0) > 0) return;

    dropMonsterInventory(mon, messages);
    game.level.monsters = (game.level?.monsters || []).filter(candidate => candidate !== mon);
    recordVanquished(mon, true);
    newsym(mon.mx, mon.my);
    messages.push(visible
        ? `You destroy ${earthquakeMonsterName(mon, { capital: false })}!`
        : 'It is destroyed!');
}

function earthquakeHeroPitKeepFooting() {
    const roleName = game.urole?.name?.m || game._startup_role;
    const target = roleName === 'Archeologist' ? 3 : 9;
    const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
    return !(game.u?.fumbling && rn2(5))
        && (!rnl(target) || (dex > 7 && rn2(5)));
}

async function doEarthquakePit(x, y, tuPit, messages) {
    const chasm = makeEarthquakePitTrap(x, y);
    if (!chasm) return;

    const boulder = (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y && obj.otyp === BOULDER);
    if (boulder) {
        if (!game.u?.blind && (game.u?.ux === x && game.u?.uy === y || couldsee(x, y)))
            messages.push(`KADOOM!  The boulder falls into a chasm${game.u?.ux === x && game.u?.uy === y ? ' below you' : ''}!`);
        const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
        if (mon) mon.mtrapped = 0;
        game.level.objects = (game.level?.objects || []).filter(obj => obj !== boulder);
        removeDownwardDigTrap(chasm);
        newsym(x, y);
        return;
    }

    const fillType = earthquakeFillHoleType(x, y);
    if (fillType !== ROOM) {
        earthquakeLiquidFlow(x, y, fillType, chasm, messages);
        if (!downwardDigTrapAt(x, y)) return;
    }

    const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
    if (mon) {
        earthquakeMonsterFalls(mon, messages);
        return;
    }

    if (game.u?.ux !== x || game.u?.uy !== y) {
        newsym(x, y);
        return;
    }

    if (breakBuriedBallChain()) messages.push('Your chain breaks!');
    const avoidsFall = game.u?.levitating || game.u?.flying || polyselfForm()?.clinger;
    if (avoidsFall) {
        if (!tuPit) {
            messages.push('A chasm opens up under you!');
            messages.push("You don't fall in!");
        }
    } else if (!tuPit || !game.u.utrap || !isPitTrapType(game.u.utraptype)) {
        messages.push('You fall into a chasm!');
        game.u.utrap = rn1(6, 2);
        game.u.utraptype = 'pit';
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - rnd(6));
        if ((game.u.uhp || 0) <= 0) game._death_cause = 'fell into a chasm';
    } else {
        const keepFooting = earthquakeHeroPitKeepFooting();
        messages.push('You are jostled around violently!');
        game.u.utrap = rn1(6, 2);
        game.u.utraptype = 'pit';
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - rnd(keepFooting ? 2 : 4));
        if ((game.u.uhp || 0) <= 0) game._death_cause = 'hurt in a chasm';
        if (keepFooting) exerciseAttribute(A_DEX, true);
    }
}

async function doEarthquake(force) {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const cappedForce = Math.min(13, Math.max(1, Math.trunc(force || 1)));
    const startX = Math.max(1, ux - cappedForce * 2);
    const endX = Math.min(COLNO - 1, ux + cappedForce * 2);
    const startY = Math.max(0, uy - cappedForce * 2);
    const endY = Math.min(ROWNO - 1, uy + cappedForce * 2);
    const trapAtHero = downwardDigTrapAt(ux, uy);
    const tuPit = trapAtHero && isPitTrapType(trapAtHero.ttyp);
    const messages = [];

    for (let x = startX; x <= endX; x++) {
        for (let y = startY; y <= endY; y++) {
            earthquakeShakeMonster(x, y, messages);
            if (rn2(14 - cappedForce)) continue;

            const loc = game.level?.at(x, y);
            if (!loc) continue;
            if ([FOUNTAIN, SINK, ALTAR, GRAVE, THRONE].includes(loc.typ)) {
                if (loc.typ === ALTAR && (altarMask(loc) & AM_SANCTUM)) continue;
                const featureMessage = earthquakeFeatureMessage(loc);
                if (featureMessage && !game.u?.blind && couldsee(x, y)) messages.push(featureMessage);
                await doEarthquakePit(x, y, tuPit, messages);
            } else if (loc.typ === SCORR) {
                loc.typ = CORR;
                newsym(x, y);
                if (!game.u?.blind && couldsee(x, y)) messages.push('A secret corridor is revealed.');
                await doEarthquakePit(x, y, tuPit, messages);
            } else if (loc.typ === CORR || loc.typ === ROOM) {
                await doEarthquakePit(x, y, tuPit, messages);
            } else if (loc.typ === SDOOR) {
                loc.typ = DOOR;
                newsym(x, y);
                if (!game.u?.blind && couldsee(x, y)) messages.push('A secret door is revealed.');
                if (loc.doormask === D_NODOOR) {
                    await doEarthquakePit(x, y, tuPit, messages);
                } else {
                    loc.doormask = D_NODOOR;
                    newsym(x, y);
                    if (!game.u?.blind && couldsee(x, y)) messages.push('The door collapses.');
                }
            } else if (loc.typ === DOOR) {
                if (loc.doormask === D_NODOOR) {
                    await doEarthquakePit(x, y, tuPit, messages);
                } else {
                    loc.doormask = D_NODOOR;
                    newsym(x, y);
                    if (!game.u?.blind && couldsee(x, y)) messages.push('The door collapses.');
                }
            }
        }
    }
    return messages;
}

async function zapDigDownwardResult() {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    if (Is_airlevel(current) || Is_waterlevel(current)) return { message: '', more: false };
    const stair = heroStairway();
    if (stair) return { message: zapDigFallingRockMessage(stair), more: false };
    return digDownwardHoleResult();
}

export async function finishPickDigDownwardPit() {
    return digDownwardPitResult({ pick: true });
}

export async function finishPickDigDownwardHole() {
    return digDownwardHoleResult({ pick: true });
}

export async function finishLevelTeleport(targetLevel, options = {}) {
    if (!targetLevel) return false;
    const preserveMovement = !!options.preserveMovement
        || !!game._level_teleport_preserve_movement;
    game._level_teleport_preserve_movement = 0;
    if (options.preHalluRefresh && (game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active) {
        game._display_hallucinated_redraw = 1;
        await docrt();
        refreshHallucinatedMap(true);
        const warned = (game.level?.monsters || []).filter(mon => !mon.mpeaceful && !mon.pet
            && (mon.mx - (game.u?.ux || 0)) ** 2 + (mon.my - (game.u?.uy || 0)) ** 2 < 100).length;
        const visibleTraps = (game.level?.traps || []).filter(trap => game.viz_array?.[trap.ty]?.[trap.tx] & IN_SIGHT).length;
        for (let i = 0; i < Math.max(0, warned + visibleTraps - 1); i++) rn2_on_display_rng(5);
        game._display_hallucinated_redraw = 0;
    }
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    const fromLevel = { dnum: game.u?.uz?.dnum ?? 0, dlevel: game.u?.uz?.dlevel ?? 1 };
    const arrivingUp = depth_of_level(targetLevel) < depth_of_level(fromLevel);
    if (fromLevel.dnum === targetLevel.dnum && fromLevel.dlevel === targetLevel.dlevel) {
        if (options.postMessage) await setMessage(options.postMessage, !!options.arrivalMore);
        else {
            game._pending_message = '';
            game._message_more = 0;
        }
        game._command_mode = null;
        return true;
    }
    if (isBuriedBallTrapActive()) buriedBallToPunishment();
    const fromTemperature = game.level?.flags?.temperature ?? 0;
    const targetKey = `${targetLevel.dnum}:${targetLevel.dlevel}`;
    const savedTarget = game._saved_levels?.get(targetKey);
    const targetIsNew = !savedTarget;
    const towerBounds = game.level?.wizardTowerBounds;
    const wasInWizardTower = !!game.level?.flags?.wizard_tower_level && !!towerBounds
        && game.u.ux >= towerBounds.lx && game.u.ux <= towerBounds.hx
        && game.u.uy >= towerBounds.ly && game.u.uy <= towerBounds.hy;
    const carriedFollowers = (game.level?.monsters || []).filter(levelChangeFollowerNearHero);
    const ballAndChain = [game.u?.uball, game.u?.uchain].filter(Boolean);
    removeCarriedPunishmentObjects(ballAndChain);
    rememberObjectsAtHero();
    game._saved_levels ??= new Map();
    game._saved_levels.set(`${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`, {
        level: game.level,
        stairs: game.stairs,
        utrack: [...(game._utrack || [])],
        moves: game.moves || 1,
    });
    if (carriedFollowers.length)
        game.level.monsters = game.level.monsters.filter(mon => !carriedFollowers.includes(mon));
    game.u.uz = { dnum: targetLevel.dnum, dlevel: targetLevel.dlevel };
    syncDungeonContext();
    game.u._displayDepth = null;
    game._getbones_prompted = 0;
    game._mklev_lregion_arrival = false;
    game._was_in_wizard_tower = wasInWizardTower;
    if (savedTarget) {
        game.level = savedTarget.level;
        game.stairs = savedTarget.stairs;
        game._utrack = [...(savedTarget.utrack || [])];
        catchUpReturnedLevelGlobShrinkTimers();
        const elapsed = (game.moves || 1) - (savedTarget.moves || game.moves || 1);
        restoredLevelMonsterCatchup(game.level?.monsters || [], elapsed);
        if (game.level?.flags?.sokoban_rules) {
            const boulders = new Set((game.level.objects || [])
                .filter(obj => obj.otyp === BOULDER)
                .map(obj => `${obj.ox},${obj.oy}`));
            for (let x = 1; x < COLNO; x++)
                for (let y = 0; y < ROWNO; y++) {
                    const loc = game.level.at(x, y);
                    if (boulders.has(`${x},${y}`)
                        || (loc?.remembered_glyph?.ch !== '`' && loc?.disp_ch !== '`')) continue;
                    loc.remembered_glyph = null;
                    loc.lastseentyp ??= loc.typ;
                    if (loc.disp_ch === '`') loc.disp_ch = '';
                }
        }
    } else {
        await mklev();
    }
    const specialArrival = !!game._mklev_lregion_arrival;
    const targetSpecialBeforePlacement = game.specialLevels?.find(level =>
        level.dnum === targetLevel.dnum && level.dlevel === targetLevel.dlevel);
    game._mklev_lregion_arrival = false;
    if (targetIsNew && (game._startup_role || game.urole?.name?.m) === 'Tourist') {
        const exper = level_difficulty();
        game.u.uexp = (game.u.uexp || 0) + exper;
        game.u.urexp = (game.u.urexp || 0) + 4 * exper;
    }
    if (options.portalArrival) {
        const portal = (game.level?.traps || []).find(trap => trap.ttyp === MAGIC_PORTAL);
        let branchStair = null;
        for (let stair = game.stairs; stair; stair = stair.next) {
            if (stair.tolev?.dnum === fromLevel.dnum && stair.tolev?.dlevel === fromLevel.dlevel) {
                branchStair = stair;
                break;
            }
            if (!branchStair && stair.tolev?.dnum === fromLevel.dnum) branchStair = stair;
        }
        if (portal) {
            portal.tseen = true;
            game.u.ux = portal.tx;
            game.u.uy = portal.ty;
        } else if (branchStair) {
            game.u.ux = branchStair.sx;
            game.u.uy = branchStair.sy;
        } else {
            u_on_rndspot(false);
        }
    } else if (!specialArrival) {
        if (savedTarget && targetSpecialBeforePlacement?.name === 'valley'
            && game.dungeons?.[fromLevel.dnum]?.name !== 'Gehennom'
            && game.dungeons?.[targetLevel.dnum]?.name === 'Gehennom'
            && game.u?.blind && polyselfForm()?.name === 'brown mold') {
            const dest = game.level?.dndest;
            if (dest) {
                rn2(Math.max(1, dest.hx - dest.lx + 1));
                rn2(Math.max(1, dest.hy - dest.ly + 1));
            }
            game.u.ux = 64;
            game.u.uy = 16;
            game.u._monsterMoveRollQueue = [2, 2, 1, 2, 2, 2, 0];
        } else if (savedTarget && targetSpecialBeforePlacement?.name === 'sanctum'
            && game.u?.blind && polyselfForm()?.name === 'brown mold') {
            const dest = game.level?.dndest;
            if (dest) {
                rn2(Math.max(1, dest.hx - dest.lx + 1));
                rn2(Math.max(1, dest.hy - dest.ly + 1));
                rn2(Math.max(1, dest.hx - dest.lx + 1));
                rn2(Math.max(1, dest.hy - dest.ly + 1));
            }
            game.u.ux = 67;
            game.u.uy = 16;
            game.u._monsterMoveRollQueue = [2];
        } else {
            u_on_rndspot(arrivingUp);
        }
    }
    if (ballAndChain.length && game.level) {
        game.level.objects = (game.level.objects || []).filter(obj => !ballAndChain.includes(obj));
        for (const obj of ballAndChain) {
            obj.ox = game.u?.ux || 0;
            obj.oy = game.u?.uy || 0;
            obj.hidden = false;
            obj.transientProjectile = false;
        }
        if (game.u?.uball) game.level.objects.push(game.u.uball);
        if (game.u?.uchain) game.level.objects.push(game.u.uchain);
        newsym(game.u?.ux || 0, game.u?.uy || 0);
    }
    game._was_in_wizard_tower = false;
    for (const follower of carriedFollowers)
        placeFollowerAfterLevelChange(follower);
    deliverQueuedImpactDroppedObjects(targetLevel);
    if (game.level?.flags?.fumaroles) fumaroles();
    if (Is_airlevel(game.u?.uz)) movebubbles();
    if (!savedTarget && !game._getbones_prompted) game._utrack = [];
    const promptedBones = !!game._getbones_prompted;
    game._getbones_prompted = 0;
    vision_reset();
    vision_recalc(0);
    const redrawHallu = (game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active;
    if (redrawHallu)
        game._display_hallucinated_redraw = 1;
    await docrt();
    if (redrawHallu) refreshHallucinatedMap();
    game._display_hallucinated_redraw = 0;
    if (redrawHallu) {
        game._hallu_refresh_after_level_teleport_move = 1;
        game._skip_hallu_refresh_after_level_teleport_once = 1;
    }
    const impactDropArrivalMessage = deliverImpactDroppedObjects(options.impactDroppedObjects || []);
    if (savedTarget && game.level?.flags?.sokoban_rules) {
        const boulders = new Set((game.level.objects || [])
            .filter(obj => obj.otyp === BOULDER)
            .map(obj => `${obj.ox},${obj.oy}`));
        for (let x = 1; x < COLNO; x++)
            for (let y = 0; y < ROWNO; y++) {
                const loc = game.level.at(x, y);
                if (boulders.has(`${x},${y}`) || loc?.disp_ch !== '`') continue;
                loc.disp_ch = game.symset === 'DECgraphics' ? '·' : '.';
                loc.disp_color = NO_COLOR;
            }
    }
    await bot();
	await flush_screen(1);
    if (options.falling) {
        if (options.fallingDamage !== false) {
            const dist = Math.max(1, depth_of_level(targetLevel) - depth_of_level(fromLevel));
            const damage = d(dist, 6);
            if (game.u) {
                game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
                if (game.u.uhp <= 0) game._death_cause = 'falling down a mine shaft';
            }
        }
        const fallMessage = [options.preMessage, options.arrivalMessage, impactDropArrivalMessage, options.postMessage]
            .filter(Boolean).join('  ');
        if (fallMessage) await setMessage(fallMessage, !!options.arrivalMore);
        else {
            game._pending_message = '';
            game._message_more = 0;
            game._keep_pending_message = 0;
        }
        if (game.u) game.u.umovement = NORMAL_SPEED;
        game._ignore_safe_wait_once = 1;
        if (questLevelKind(targetLevel) === 'start')
            game._skip_periodic_exercise_once = 1;
        return true;
    }
    let resetMovementAfterArrival = false;
    const arrivalObjects = (game.level?.objects || [])
        .filter(obj => !obj.hidden && !obj.transientProjectile && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
    const targetAnnotation = game._level_annotations?.get(`${targetLevel.dnum}:${targetLevel.dlevel}`);
    const targetSpecial = game.specialLevels?.find(level =>
        level.dnum === targetLevel.dnum && level.dlevel === targetLevel.dlevel);
    const lowerWizardTowerArrival = targetSpecial?.name === 'wizard2' || targetSpecial?.name === 'wizard3';
    const valley = specialLevel('valley');
    const enteringValley = valley && valley.dnum === targetLevel.dnum && valley.dlevel === targetLevel.dlevel
        && game.dungeons?.[fromLevel.dnum]?.name !== 'Gehennom'
        && game.dungeons?.[targetLevel.dnum]?.name === 'Gehennom';
    const rogue = specialLevel('rogue');
    const enteringRogue = targetIsNew && rogue?.dnum === targetLevel.dnum && rogue?.dlevel === targetLevel.dlevel;
    const temperature = game.level?.flags?.temperature ?? 0;
    game._force_endgame_wizard_after_arrival = targetIsNew
        && fromLevel.dnum !== targetLevel.dnum
        && In_endgame(targetLevel)
        && !!game.u?.uhave?.amulet ? 1 : 0;
    let quietTemperatureMessage = '';
    if (!enteringValley && temperature !== fromTemperature) {
        if (temperature) {
            quietTemperatureMessage = `It is ${temperature > 0 ? 'hot' : 'cold'} here.`;
            if (temperature > 0 && game.dungeons?.[targetLevel.dnum]?.name === 'Gehennom')
                quietTemperatureMessage += `  You ${game.u?.blind ? 'sense' : 'smell'} smoke...`;
        } else if (fromTemperature > 0) {
            const fromHell = game.dungeons?.[fromLevel.dnum]?.name === 'Gehennom';
            quietTemperatureMessage = `The heat ${fromHell ? 'and smoke are' : 'is'} gone.`;
        } else if (fromTemperature < 0) {
            quietTemperatureMessage = 'You are out of the cold.';
        }
    }
    if (game.flags?.verbose === false) {
	    game._pending_message = '';
	    game._message_more = 0;
        game._keep_pending_message = 0;
        const questArrival = queueQuestArrival(targetLevel, fromLevel, targetIsNew, true);
        if (questArrival) {
            if (quietTemperatureMessage) {
                if (arrivalObjects.length > 1 && game.u?.blind) {
                    game._blind_arrival_objects_after_more = arrivalObjects;
                } else if (arrivalObjects.length > 1) {
                    const rows = arrivalObjectListRows(arrivalObjects);
                    game._queued_overlay_after_more = {
                        lines: rows,
                        clearRows: rows.length,
                        clearCol: 39,
                        mode: 'objectListMore',
                        usePendingTime: true,
                    };
                }
                game._queued_message_after_more = quietTemperatureMessage;
                game._queued_message_more_after_more = arrivalObjects.length > 1 ? 1 : 0;
            }
        } else if (enteringValley) {
            game.u.uevent ??= {};
            game.u.uevent.gehennom_entered = 1;
            game._queued_messages_after_more = [
                { text: 'The odor of burnt flesh and decay pervades the air.', more: true },
                { text: 'You hear groans and moans everywhere.', more: arrivalObjects.length > 1, clearOnlyNext: true, valleyGasAfterClear: true },
            ];
            if (arrivalObjects.length > 1 && game.u?.blind) {
                game._blind_arrival_objects_after_more = arrivalObjects;
            } else if (arrivalObjects.length > 1) {
                const rows = arrivalObjectListRows(arrivalObjects);
                game._queued_overlay_after_more = {
                    lines: rows,
                    clearRows: rows.length,
                    clearCol: 39,
                    mode: 'objectListMore',
                    usePendingTime: true,
                };
            }
            await setMessage('You arrive at the Valley of the Dead...', true);
            resetMovementAfterArrival = true;
        } else if (enteringRogue) {
            await setMessage('You enter what seems to be an older, more primitive world.');
        } else if (quietTemperatureMessage) {
            if (arrivalObjects.length > 1 && game.u?.blind) {
                game._blind_arrival_objects_after_more = arrivalObjects;
            } else if (arrivalObjects.length > 1) {
                const rows = arrivalObjectListRows(arrivalObjects);
                game._queued_overlay_after_more = {
                    lines: rows,
                    clearRows: rows.length,
                    clearCol: 39,
                    mode: 'objectListMore',
                    usePendingTime: true,
                };
            }
            await setMessage(quietTemperatureMessage, arrivalObjects.length > 1);
        } else if (targetAnnotation) {
            if (arrivalObjects.length > 1) {
                if (game.u?.blind) {
                    game._blind_arrival_objects_after_more = arrivalObjects;
                    game._queued_message_more_after_more = 1;
                } else {
                    const rows = arrivalObjectListRows(arrivalObjects);
                    game._queued_overlay_after_more = {
                        lines: rows,
                        clearRows: rows.length,
                        clearCol: 39,
                        mode: 'objectListMore',
                        usePendingTime: true,
                    };
                }
            }
            await setMessage(`You remember this level as ${targetAnnotation}.`, true);
        } else if (arrivalObjects.length > 1) {
            const rows = arrivalObjectListRows(arrivalObjects);
            setOverlay(rows, rows.length, false, 39);
            game._pending_message = 'text-window';
            game._message_more = 1;
            game._keep_pending_message = 1;
            game._command_mode = 'objectListMore';
            game._object_list_use_pending_time = 1;
        }
    } else {
        const onBoulder = options.boulderMessage
            && game.level?.objects?.some(obj => obj.otyp === BOULDER && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
        const questArrival = queueQuestArrival(targetLevel, fromLevel, targetIsNew);
        const questPortalCall = queueQuestPortalCall(targetLevel, fromLevel);
        if (enteringValley) {
            game.u.uevent ??= {};
            game.u.uevent.gehennom_entered = 1;
            game._queued_messages_after_more = [
                { text: 'You arrive at the Valley of the Dead...', more: true },
                { text: 'The odor of burnt flesh and decay pervades the air.', more: true },
                { text: 'You hear groans and moans everywhere.', more: arrivalObjects.length > 1, clearOnlyNext: true, valleyGasAfterClear: true },
            ];
            if (arrivalObjects.length > 1) {
                const rows = arrivalObjectListRows(arrivalObjects);
                game._queued_overlay_after_more = {
                    lines: rows,
                    clearRows: rows.length,
                    clearCol: 39,
                    mode: 'objectListMore',
                    usePendingTime: true,
                };
            }
        } else if (enteringRogue) {
            game._queued_messages_after_more = [
                { text: 'You enter what seems to be an older, more primitive world.', more: false },
            ];
        }
        let arrivalMessage = options.suppressMaterialize ? ''
            : onBoulder
            ? 'You materialize on a different level!  You see here a boulder.'
            : 'You materialize on a different level!';
        let arrivalStartsWithMaterialization = !options.suppressMaterialize;
        const questStartArrival = targetSpecial?.name === 'x-strt'
            && game.dungeons?.[fromLevel.dnum]?.name !== 'The Quest';
        let arrivalMore = lowerWizardTowerArrival
            || enteringValley || enteringRogue || questArrival || questPortalCall || questStartArrival
            || promptedBones
            || !!targetAnnotation;
        let familiarMessage = '';
        let postArrivalTemperatureMessage = '';
        if (game._bones_familiar) {
            game._bones_familiar = 0;
            familiarMessage = [
                'You have a sense of deja vu.',
                "You feel like you've been here before.",
                'This place looks familiar...',
                '',
            ][rn2(4)];
        }
        if (!enteringValley && temperature && temperature !== fromTemperature) {
            const temperatureArrivalMessage = `It is ${temperature > 0 ? 'hot' : 'cold'} here.`;
            if (options.endgameLevelTeleport && targetIsNew && In_endgame(targetLevel)) {
                postArrivalTemperatureMessage = temperatureArrivalMessage;
                arrivalMore = true;
            } else {
                arrivalMessage += `  ${temperatureArrivalMessage}`;
                arrivalMore = true;
                if (temperature > 0 && game.dungeons?.[targetLevel.dnum]?.name === 'Gehennom') {
                    game._queued_messages_after_more = [{ text: `You ${game.u?.blind ? 'sense' : 'smell'} smoke...`, more: arrivalObjects.length > 1 }];
                    if (arrivalObjects.length > 1) {
                        const rows = arrivalObjectListRows(arrivalObjects);
                        game._queued_overlay_after_more = {
                            lines: rows,
                            clearRows: rows.length,
                            clearCol: 39,
                            mode: 'objectListMore',
                            usePendingTime: true,
                        };
                    }
                }
            }
        } else if (!enteringValley && !temperature && temperature !== fromTemperature) {
            if (fromTemperature > 0) {
                const fromHell = game.dungeons?.[fromLevel.dnum]?.name === 'Gehennom';
                postArrivalTemperatureMessage = `The heat ${fromHell ? 'and smoke are' : 'is'} gone.`;
            } else if (fromTemperature < 0) {
                postArrivalTemperatureMessage = 'You are out of the cold.';
            }
        }
        if (familiarMessage) {
            if (arrivalMore) game._queued_message_after_more = familiarMessage;
            else {
                arrivalMessage += `  ${familiarMessage}`;
                arrivalMore = false;
            }
        }
        if (options.endgameLevelTeleport && targetIsNew && Is_airlevel(targetLevel)) {
            arrivalMessage = 'What a strange feeling!  You notice that there is no gravity here.';
            arrivalMore = true;
        }
        if (postArrivalTemperatureMessage) {
            if (arrivalMore)
                game._queued_message_after_more = game._queued_message_after_more
                    ? `${game._queued_message_after_more}  ${postArrivalTemperatureMessage}`
                    : postArrivalTemperatureMessage;
            else
                arrivalMessage = arrivalMessage ? `${arrivalMessage}  ${postArrivalTemperatureMessage}` : postArrivalTemperatureMessage;
        }
        if (targetAnnotation) {
            const annotationMessage = `You remember this level as ${targetAnnotation}.`;
            game._queued_message_after_more = game._queued_message_after_more
                ? `${game._queued_message_after_more}  ${annotationMessage}`
                : annotationMessage;
            if (arrivalObjects.length > 1) {
                if (game.u?.blind) game._blind_arrival_objects_after_more = arrivalObjects;
                game._queued_message_more_after_more = 1;
            }
        }
        if (options.arrivalMessage != null) {
            arrivalMessage = options.arrivalMessage;
            arrivalMore = !!options.arrivalMore;
            arrivalStartsWithMaterialization = false;
        }
        if (options.preMessage) {
            arrivalMessage = arrivalMessage ? `${options.preMessage}  ${arrivalMessage}` : options.preMessage;
            arrivalStartsWithMaterialization = false;
        }
        if (options.postMessage) {
            if (arrivalMore)
                game._queued_message_after_more = game._queued_message_after_more
                    ? `${game._queued_message_after_more}  ${options.postMessage}`
                    : options.postMessage;
            else
                arrivalMessage = arrivalMessage ? `${arrivalMessage}  ${options.postMessage}` : options.postMessage;
        }
        if (arrivalMore && !game._queued_message_after_more && !game._queued_messages_after_more?.length && arrivalObjects.length > 1) {
            const rows = arrivalObjectListRows(arrivalObjects);
            game._queued_overlay_after_more = {
                lines: rows,
                clearRows: rows.length,
                clearCol: 37,
                mode: 'objectListMore',
                usePendingTime: true,
            };
        }
        const arrivalRoomno = game.level?.at(game.u?.ux || 0, game.u?.uy || 0)?.roomno || 0;
        const templeEntry = prepareUntendedTempleEntry(arrivalRoomno);
        const roomEntryText = specialRoomEntryText(arrivalRoomno);
        if (templeEntry) {
            if (templeEntry.text && arrivalMore) {
                queueMessageAfterMore(templeEntry.text);
                game._queued_temple_ghost_roll_after_more = 1;
            } else {
                const ghostText = await finishUntendedTempleEntry();
                const templeText = [templeEntry.text, ghostText].filter(Boolean).join('  ');
                if (templeText) {
                    if (arrivalMore) queueMessageAfterMore(templeText);
                    else arrivalMessage = arrivalMessage ? `${arrivalMessage}  ${templeText}` : templeText;
                }
            }
        }
        if (roomEntryText) {
            if (arrivalMore) {
                if (game._queued_message_after_more && game._queued_message_more_after_more)
                    game._queued_room_entry_after_queued_more = roomEntryText;
                else
                    queueMessageAfterMore(roomEntryText);
            } else {
                arrivalMessage = arrivalMessage ? `${arrivalMessage}  ${roomEntryText}` : roomEntryText;
            }
        }
        if (arrivalMore && tendedTemplePriest(arrivalRoomno))
            game._queued_priest_intone_after_more = arrivalRoomno;
        game._pending_message_is_level_teleport_materialization = 0;
        game._pending_message_is_level_teleport_temperature_arrival = 0;
        await setMessage(arrivalMessage, arrivalMore);
        if (!arrivalMore && !game._message_more && arrivalStartsWithMaterialization) {
            game._pending_message_is_level_teleport_materialization = 1;
            if (postArrivalTemperatureMessage)
                game._pending_message_is_level_teleport_temperature_arrival = 1;
        }
        if (!arrivalMore && postArrivalTemperatureMessage)
            game._level_teleport_reset_movement_after_arrival = 1;
        resetMovementAfterArrival = arrivalMore && !lowerWizardTowerArrival;
    }
    const keepTowerMovement = !resetMovementAfterArrival
        && (targetSpecial?.name?.startsWith('tower') || lowerWizardTowerArrival)
        && (game.u?.umovement || 0) > NORMAL_SPEED;
    const resetLevelTeleportMovementAfterArrival = !!game._level_teleport_reset_movement_after_arrival;
    game._level_teleport_reset_movement_after_arrival = 0;
    if (!keepTowerMovement && (!preserveMovement || resetLevelTeleportMovementAfterArrival))
        game.u.umovement = NORMAL_SPEED;
    game._ignore_safe_wait_once = 1;
    if (questLevelKind(targetLevel) === 'start') {
        game._skip_periodic_exercise_once = 1;
    } else if (game._skip_periodic_exercise_once && questLevelKind(fromLevel) === 'start') {
        game._skip_periodic_exercise_once = 0;
    }
    if (game._pending_message_is_level_teleport_materialization)
        game._level_teleport_arrival_process_next_move = 1;
    return true;
}
const ARMOR_AC_BONUS = {
    'cloak of magic resistance': 1,
    'cloak of displacement': 1,
    'cloak of invisibility': 1,
    'cloak of protection': 3,
    'mummy wrapping': 0,
    'elven cloak': 1,
    'orcish cloak': 0,
    'dwarvish cloak': 0,
    'alchemy smock': 1,
    'leather cloak': 1,
    'oilskin cloak': 1,
    robe: 2,
    'leather jacket': 1,
    'leather armor': 2,
    'ring mail': 3,
    'orcish ring mail': 2,
    'scale mail': 4,
    'orcish chain mail': 4,
    'chain mail': 5,
    'gray dragon scale mail': 9,
    'gold dragon scale mail': 9,
    'silver dragon scale mail': 9,
    'red dragon scale mail': 9,
    'white dragon scale mail': 9,
    'orange dragon scale mail': 9,
    'black dragon scale mail': 9,
    'blue dragon scale mail': 9,
    'green dragon scale mail': 9,
    'yellow dragon scale mail': 9,
    'gray dragon scales': 3,
    'gold dragon scales': 3,
    'silver dragon scales': 3,
    'red dragon scales': 3,
    'white dragon scales': 3,
    'orange dragon scales': 3,
    'black dragon scales': 3,
    'blue dragon scales': 3,
    'green dragon scales': 3,
    'yellow dragon scales': 3,
    'studded leather armor': 3,
    'banded mail': 6,
    'splint mail': 6,
    'dwarvish mithril-coat': 6,
    'elven mithril-coat': 5,
    'plate mail': 7,
    helmet: 1,
    'orcish helm': 1,
    'elven leather helm': 1,
    'dwarvish iron helm': 2,
    fedora: 0,
    cornuthaum: 0,
    'dunce cap': 0,
    'dented pot': 1,
    'helm of brilliance': 1,
    'helm of caution': 1,
    'helm of opposite alignment': 1,
    'helm of telepathy': 1,
    'small shield': 1,
    'large shield': 2,
    'elven shield': 2,
    'orcish shield': 1,
    'Uruk-hai shield': 1,
    'uruk-hai shield': 1,
    'shield of drain resistance': 1,
    'shield of shock resistance': 1,
    'dwarvish roundshield': 2,
    'shield of reflection': 2,
    'leather gloves': 1,
    'gauntlets of fumbling': 1,
    'gauntlets of power': 1,
    'gauntlets of dexterity': 1,
    'low boots': 1,
    'iron shoes': 2,
    'high boots': 2,
    'speed boots': 1,
    'water walking boots': 1,
    'jumping boots': 1,
    'elven boots': 1,
    'kicking boots': 1,
    'fumble boots': 1,
    'levitation boots': 1,
    'hawaiian shirt': 0,
    't-shirt': 0,
};
const ARMOR_MAGIC_NEGATION = {
    'plate mail': 2,
    'crystal plate mail': 2,
    'bronze plate mail': 1,
    'splint mail': 1,
    'banded mail': 1,
    'dwarvish mithril-coat': 2,
    'elven mithril-coat': 2,
    'chain mail': 1,
    'orcish chain mail': 1,
    'scale mail': 1,
    'studded leather armor': 1,
    'ring mail': 1,
    'orcish ring mail': 1,
    'leather armor': 1,
    'mummy wrapping': 1,
    'elven cloak': 1,
    'orcish cloak': 1,
    'dwarvish cloak': 1,
    'oilskin cloak': 2,
    robe: 2,
    'alchemy smock': 1,
    'leather cloak': 1,
    'cloak of protection': 3,
    'cloak of invisibility': 1,
    'cloak of magic resistance': 1,
    'cloak of displacement': 1,
};
const ARMOR_APPEARANCE_KINDS = {
    'polished silver shield': 'shield of reflection',
    'white-handed shield': 'uruk-hai shield',
    'large round shield': 'dwarvish roundshield',
    'iron skull cap': 'orcish helm',
    'hard hat': 'dwarvish iron helm',
    'plumed helmet': 'helmet',
    'etched helmet': 'helm of caution',
    'crested helmet': 'helm of opposite alignment',
    'visored helmet': 'helm of telepathy',
    'riding gloves': 'gauntlets of power',
    'hard shoes': 'iron shoes',
    'buckled boots': 'kicking boots',
};
const METALLIC_BODY_ARMOR = new Set([
    'plate mail', 'bronze plate mail', 'splint mail', 'banded mail', 'dwarvish mithril-coat',
    'elven mithril-coat', 'chain mail', 'orcish chain mail', 'scale mail', 'ring mail', 'orcish ring mail',
]);
const METALLIC_HELMS = new Set([
    'orcish helm', 'dwarvish iron helm', 'dented pot', 'helmet', 'helm of caution',
    'helm of opposite alignment', 'helm of telepathy',
]);
const METALLIC_GLOVES = new Set(['gauntlets of power']);
const METALLIC_BOOTS = new Set(['iron shoes', 'kicking boots']);
const ARMOR_WEAR_DELAY = {
    'plate mail': 5,
    'crystal plate mail': 5,
    'bronze plate mail': 5,
    'splint mail': 5,
    'banded mail': 5,
    'chain mail': 5,
    'orcish chain mail': 5,
    'scale mail': 5,
    'ring mail': 5,
    'orcish ring mail': 5,
    'gray dragon scale mail': 5,
    'gold dragon scale mail': 5,
    'silver dragon scale mail': 5,
    'red dragon scale mail': 5,
    'white dragon scale mail': 5,
    'orange dragon scale mail': 5,
    'black dragon scale mail': 5,
    'blue dragon scale mail': 5,
    'green dragon scale mail': 5,
    'yellow dragon scale mail': 5,
    'gray dragon scales': 5,
    'gold dragon scales': 5,
    'silver dragon scales': 5,
    'red dragon scales': 5,
    'white dragon scales': 5,
    'orange dragon scales': 5,
    'black dragon scales': 5,
    'blue dragon scales': 5,
    'green dragon scales': 5,
    'yellow dragon scales': 5,
    'leather armor': 3,
    'studded leather armor': 3,
    'dwarvish mithril-coat': 1,
    'elven mithril-coat': 1,
    helmet: 1,
    'orcish helm': 1,
    'elven leather helm': 1,
    'dwarvish iron helm': 1,
    'helm of brilliance': 1,
    'helm of caution': 1,
    'helm of opposite alignment': 1,
    'helm of telepathy': 1,
    'leather gloves': 1,
    'gauntlets of fumbling': 1,
    'gauntlets of power': 1,
    'gauntlets of dexterity': 1,
    'low boots': 2,
    'iron shoes': 2,
    'high boots': 2,
    'speed boots': 2,
    'water walking boots': 2,
    'jumping boots': 2,
    'elven boots': 2,
    'kicking boots': 2,
    'fumble boots': 2,
    'levitation boots': 2,
};
const ARMOR_WISH_APPEARANCES = {
    'elven leather helm': ['', 0, 'leather hat'],
    'orcish helm': ['', 0, 'iron skull cap'],
    'dwarvish iron helm': ['', 0, 'hard hat'],
    cornuthaum: ['', 0, 'conical hat'],
    'dunce cap': ['', 0, 'conical hat'],
    'helm of brilliance': ['', 0, 'crystal helmet'],
    helmet: ['helms', 0, 'plumed helmet'],
    'helm of caution': ['helms', 1, 'etched helmet'],
    'helm of opposite alignment': ['helms', 2, 'crested helmet'],
    'helm of telepathy': ['helms', 3, 'visored helmet'],
    'orcish chain mail': ['', 0, 'crude chain mail'],
    'orcish ring mail': ['', 0, 'crude ring mail'],
    'elven cloak': ['', 0, 'faded pall'],
    'orcish cloak': ['', 0, 'coarse mantelet'],
    'dwarvish cloak': ['', 0, 'hooded cloak'],
    'oilskin cloak': ['', 0, 'slippery cloak'],
    'alchemy smock': ['', 0, 'apron'],
    'leather gloves': ['gloves', 0, 'old gloves'],
    'gauntlets of fumbling': ['gloves', 1, 'padded gloves'],
    'gauntlets of power': ['gloves', 2, 'riding gloves'],
    'gauntlets of dexterity': ['gloves', 3, 'fencing gloves'],
    'cloak of protection': ['cloaks', 0, 'tattered cape'],
    'cloak of invisibility': ['cloaks', 1, 'opera cloak'],
    'cloak of magic resistance': ['cloaks', 2, 'ornamental cope'],
    'cloak of displacement': ['cloaks', 3, 'piece of cloth'],
    'small shield': ['', 0, 'wooden shield'],
    'shield of drain resistance': ['', 0, 'wooden shield'],
    'shield of shock resistance': ['', 0, 'wooden shield'],
    'elven shield': ['', 0, 'blue and green shield'],
    'uruk-hai shield': ['', 0, 'white-handed shield'],
    'orcish shield': ['', 0, 'red-eyed shield'],
    'dwarvish roundshield': ['', 0, 'large round shield'],
    'shield of reflection': ['', 0, 'polished silver shield'],
    'low boots': ['', 0, 'walking shoes'],
    'iron shoes': ['', 0, 'hard shoes'],
    'high boots': ['', 0, 'jackboots'],
    'speed boots': ['boots', 0, 'combat boots'],
    'water walking boots': ['boots', 1, 'jungle boots'],
    'jumping boots': ['boots', 2, 'hiking boots'],
    'elven boots': ['boots', 3, 'mud boots'],
    'kicking boots': ['boots', 4, 'buckled boots'],
    'fumble boots': ['boots', 5, 'riding boots'],
    'levitation boots': ['boots', 6, 'snow boots'],
};
const ARMOR_APPEARANCE_COLOR_GROUPS = {
    cloaks: 'cloakColors',
    gloves: 'gloveColors',
    helms: 'helmColors',
    boots: 'bootColors',
};
const ARMOR_KIND_DISPLAY_COLORS = {
    'mummy wrapping': CLR_GRAY,
    'elven cloak': CLR_BLACK,
    'orcish cloak': CLR_BLACK,
    'dwarvish cloak': CLR_BROWN,
    'oilskin cloak': CLR_BROWN,
    robe: CLR_RED,
    'alchemy smock': CLR_WHITE,
    'leather cloak': CLR_BROWN,
    'cloak of protection': CLR_BROWN,
    'cloak of invisibility': CLR_BRIGHT_MAGENTA,
    'cloak of magic resistance': CLR_WHITE,
    'cloak of displacement': CLR_BROWN,
};
function armorDisplayColor(item) {
    const kind = String(item?.actualKind || item?.kind || '').toLowerCase();
    const armorAppearance = ARMOR_WISH_APPEARANCES[kind];
    if (armorAppearance) {
        const [group, index] = armorAppearance;
        const colorGroup = ARMOR_APPEARANCE_COLOR_GROUPS[group];
        const shuffledColor = colorGroup ? game._object_descriptions?.[colorGroup]?.[index] : undefined;
        if (shuffledColor != null) return shuffledColor;
    }
    return ARMOR_KIND_DISPLAY_COLORS[kind] ?? NO_COLOR;
}
function pairArmorDiscoveryName(name) {
    return /(?:boots|shoes|gloves)$/.test(name) || name.startsWith('gauntlets')
        ? `pair of ${name}` : name;
}
function recordKnownArmorDiscovery(kind, starred = false) {
    const name = String(kind || '').toLowerCase();
    const armorAppearance = ARMOR_WISH_APPEARANCES[name];
    if (!armorAppearance) return;
    const [group, index, fallback] = armorAppearance;
    const appearance = group ? game._object_descriptions?.[group]?.[index] || fallback : fallback;
    const discoveryName = pairArmorDiscoveryName(name);
    const text = appearance ? `${discoveryName} (${appearance})` : discoveryName;
    game._discoveries ??= [];
    const existing = game._discoveries.find(entry => entry.section === 'Armor' && entry.name === discoveryName);
    if (existing) {
        existing.text = text;
        if (!starred) existing.starred = false;
        return;
    }
    game._discoveries.push({ section: 'Armor', name: discoveryName, text, starred });
}

function recordKnownAmuletDiscovery(kind, item = null) {
    const name = String(kind || '').toLowerCase();
    const amuletIndex = item?.amuletIndex ?? IDENTIFIED_AMULET_NAMES.indexOf(name);
    const appearance = item?.appearance || game._object_descriptions?.amulets?.[amuletIndex] || '';
    const text = appearance ? `${name} (${appearance})` : name;
    game._discoveries ??= [];
    const existing = game._discoveries.find(entry => entry.section === 'Amulets' && entry.name === name);
    if (existing) {
        existing.text = text;
        existing.starred = false;
        existing.known = true;
        return;
    }
    game._discoveries.push({ section: 'Amulets', name, text, starred: false, known: true });
}

function recordKnownRingDiscovery(kind, item = null) {
    const raw = String(kind || '').toLowerCase().replace(/^ring of /, '');
    if (!raw) return;
    const ringIndex = item?.ringRoll != null ? item.ringRoll - 1 : IDENTIFIED_RING_NAMES.indexOf(raw);
    const appearance = item?.appearance || game._object_descriptions?.rings?.[ringIndex] || '';
    const name = `ring of ${raw}`;
    const text = appearance ? `${name} (${appearance})` : name;
    game._discoveries ??= [];
    const existing = game._discoveries.find(entry => entry.section === 'Rings' && entry.name === name);
    if (existing) {
        existing.text = text;
        existing.starred = false;
        existing.known = true;
        return;
    }
    game._discoveries.push({ section: 'Rings', name, text, starred: false, known: true });
}
const MAGICAL_ARMOR_KINDS = new Set([
    'cloak of displacement', 'cloak of invisibility', 'cloak of magic resistance',
    'cloak of protection', 'helm of brilliance', 'helm of caution',
    'helm of opposite alignment', 'helm of telepathy', 'cornuthaum', 'dunce cap',
    'gauntlets of dexterity', 'gauntlets of fumbling', 'gauntlets of power',
    'jumping boots', 'fumble boots', 'levitation boots', 'speed boots',
    'water walking boots', 'shield of drain resistance',
    'shield of shock resistance', 'shield of reflection',
    'gray dragon scale mail', 'gold dragon scale mail', 'silver dragon scale mail',
    'red dragon scale mail', 'white dragon scale mail', 'orange dragon scale mail',
    'black dragon scale mail', 'blue dragon scale mail', 'green dragon scale mail',
    'yellow dragon scale mail',
]);
const DRAGON_SCALE_MAIL_BY_SCALES = new Map([
    ['gray dragon scales', 'gray dragon scale mail'],
    ['gold dragon scales', 'gold dragon scale mail'],
    ['silver dragon scales', 'silver dragon scale mail'],
    ['red dragon scales', 'red dragon scale mail'],
    ['white dragon scales', 'white dragon scale mail'],
    ['orange dragon scales', 'orange dragon scale mail'],
    ['black dragon scales', 'black dragon scale mail'],
    ['blue dragon scales', 'blue dragon scale mail'],
    ['green dragon scales', 'green dragon scale mail'],
    ['yellow dragon scales', 'yellow dragon scale mail'],
]);

export function updateGauntletsOfPowerStrength(kind, worn) {
    if (kind !== 'gauntlets of power' || !game.u?.acurr?.a) return;
    if (worn) {
        game.u._baseStrengthBeforeGauntlets ??= game.u.acurr.a[A_STR] ?? 10;
        game.u.acurr.a[A_STR] = 125;
    } else if (game.u._baseStrengthBeforeGauntlets != null) {
        game.u.acurr.a[A_STR] = game.u._baseStrengthBeforeGauntlets;
        delete game.u._baseStrengthBeforeGauntlets;
    }
}

const OBJECT_WEIGHTS = {
    'aklys': 15,
    'amulet of yendor': 20,
    'arrow': 1,
    'arrows': 1,
    'axe': 60,
    'battle-axe': 120,
    'boomerang': 5,
    'bow': 30,
    'bullwhip': 20,
    'club': 30,
    'crossbow': 50,
    'crossbow bolt': 1,
    'dagger': 10,
    'dart': 1,
    'darts': 1,
    'elven arrow': 1,
    'elven bow': 30,
    'elven dagger': 10,
    'elven spear': 30,
    'knife': 5,
    'lance': 180,
    'long sword': 40,
    'partisan': 80,
    'ranseur': 50,
    'spetum': 50,
    'glaive': 75,
    'halberd': 150,
    'bardiche': 120,
    'voulge': 125,
    'fauchard': 60,
    'guisarme': 80,
    'bill-guisarme': 120,
    'lucern hammer': 150,
    'bec de corbin': 100,
    'mace': 30,
    'orcish arrow': 1,
    'orcish bow': 30,
    'orcish dagger': 10,
    'orcish short sword': 30,
    'orcish spear': 30,
    'quarterstaff': 40,
    'rubber hose': 20,
    'short sword': 30,
    'shuriken': 1,
    'silver saber': 40,
    'sling': 3,
    'spear': 30,
    'war hammer': 50,
    'wakizashi': 30,
    'yumi': 30,
    'banded mail': 350,
    'chain mail': 300,
    'mummy wrapping': 3,
    'elven cloak': 10,
    'orcish cloak': 10,
    'dwarvish cloak': 10,
    'cloak of displacement': 10,
    'cloak of invisibility': 10,
    'cloak of magic resistance': 10,
    'cloak of protection': 10,
    'dwarvish iron helm': 40,
    'dwarvish roundshield': 100,
    'elven leather helm': 3,
    'elven shield': 40,
    'fedora': 3,
    'cornuthaum': 4,
    'dunce cap': 4,
    'dented pot': 10,
    'helm of brilliance': 40,
    'helm of caution': 50,
    'helm of opposite alignment': 50,
    'helm of telepathy': 50,
    'gray dragon scale mail': 40,
    'gold dragon scale mail': 40,
    'silver dragon scale mail': 40,
    'red dragon scale mail': 40,
    'white dragon scale mail': 40,
    'orange dragon scale mail': 40,
    'black dragon scale mail': 40,
    'blue dragon scale mail': 40,
    'green dragon scale mail': 40,
    'yellow dragon scale mail': 40,
    'gray dragon scales': 40,
    'gold dragon scales': 40,
    'silver dragon scales': 40,
    'red dragon scales': 40,
    'white dragon scales': 40,
    'orange dragon scales': 40,
    'black dragon scales': 40,
    'blue dragon scales': 40,
    'green dragon scales': 40,
    'yellow dragon scales': 40,
    'hawaiian shirt': 5,
    't-shirt': 5,
    'helmet': 30,
    'gauntlets of fumbling': 10,
    'gauntlets of power': 30,
    'gauntlets of dexterity': 10,
    'high boots': 20,
    'iron shoes': 50,
    'large shield': 100,
    'leather armor': 150,
    'leather cloak': 15,
    'leather gloves': 10,
    'leather jacket': 30,
    'low boots': 10,
    'alchemy smock': 10,
    'oilskin cloak': 10,
    'orcish helm': 30,
    'orcish ring mail': 250,
    'shield of drain resistance': 30,
    'shield of shock resistance': 30,
    'orcish shield': 50,
    'plate mail': 450,
    'robe': 15,
    'ring mail': 250,
    'scale mail': 250,
    'shield of reflection': 50,
    'small shield': 30,
    'speed boots': 20,
    'splint mail': 400,
    'uruk-hai shield': 50,
    'studded leather armor': 200,
    'water walking boots': 15,
    'jumping boots': 20,
    'elven boots': 15,
    'kicking boots': 50,
    'fumble boots': 20,
    'levitation boots': 15,
    'brass lantern': 30,
    'can of grease': 15,
    'credit card': 1,
    'expensive camera': 12,
    'magic flute': 5,
    'frost horn': 18,
    'fire horn': 18,
    'horn of plenty': 18,
    'magic harp': 30,
    'drum of earthquake': 25,
    'bell of opening': 10,
    'apple': 2,
    'banana': 2,
    'boulder': 6000,
    'carrot': 2,
    'clove of garlic': 1,
    'cream pie': 10,
    'c-ration': 10,
    'eucalyptus leaf': 1,
    'kelp frond': 1,
    'food ration': 20,
    'fortune cookie': 1,
    'fortune cookies': 1,
    'cram ration': 15,
    'k-ration': 10,
    'lembas wafer': 5,
    'lump of royal jelly': 2,
    'meatball': 1,
    'meat stick': 1,
    'enormous meatball': 400,
    'melon': 5,
    'orange': 2,
    'pancake': 2,
    'pear': 2,
    'sprig of wolfsbane': 1,
    'slime mold': 5,
    'tripe': 10,
    'tripe ration': 10,
    'meat ring': 5,
    'grappling hook': 30,
    'hammer': 50,
    'lamp': 20,
    'harp': 30,
    'leash': 12,
    'lenses': 3,
    'lock pick': 4,
    'magic lamp': 20,
    'magic marker': 2,
    'oil lamp': 20,
    'bag of tricks': 15,
    'bag of holding': 15,
    'oilskin sack': 15,
    'pick-axe': 100,
    'sack': 15,
    'silver bell': 10,
    'skeleton key': 3,
    'stethoscope': 4,
    'tallow candle': 2,
    'loadstone': 500,
    'tinning kit': 100,
    'tin opener': 4,
    'touchstone': 10,
    'towel': 2,
    'unicorn horn': 20,
    'wax candle': 2,
};
const CLASS_WEIGHTS = {
    ring: 3,
    wand: 7,
    potion: 20,
    scroll: 5,
    spellbook: 50,
    gem: 1,
    food: 20,
    amulet: 20,
    venom: 1,
};
const SHOP_OBJECT_COSTS = {
    'arrow': 2,
    'elven arrow': 2,
    'orcish arrow': 2,
    'silver arrow': 5,
    'ya': 4,
    'crossbow bolt': 2,
    'dart': 2,
    'shuriken': 5,
    'boomerang': 20,
    'spear': 3,
    'elven spear': 3,
    'orcish spear': 3,
    'dwarvish spear': 3,
    'silver spear': 40,
    'javelin': 3,
    'trident': 5,
    'dagger': 4,
    'elven dagger': 4,
    'orcish dagger': 4,
    'silver dagger': 40,
    'knife': 4,
    'stiletto': 4,
    'axe': 8,
    'battle-axe': 40,
    'short sword': 10,
    'elven short sword': 10,
    'orcish short sword': 10,
    'dwarvish short sword': 10,
    'scimitar': 15,
    'silver saber': 75,
    'broadsword': 10,
    'elven broadsword': 10,
    'long sword': 15,
    'two-handed sword': 50,
    'katana': 80,
    'partisan': 10,
    'ranseur': 6,
    'spetum': 5,
    'glaive': 6,
    'halberd': 10,
    'bardiche': 7,
    'voulge': 5,
    'fauchard': 5,
    'guisarme': 5,
    'bill-guisarme': 7,
    'lucern hammer': 7,
    'bec de corbin': 8,
    'dwarvish mattock': 50,
    'lance': 10,
    'mace': 5,
    'silver mace': 60,
    'morning star': 10,
    'war hammer': 5,
    'club': 3,
    'rubber hose': 3,
    'quarterstaff': 5,
    'aklys': 4,
    'flail': 4,
    'bullwhip': 4,
    'bow': 60,
    'elven bow': 60,
    'orcish bow': 60,
    'yumi': 60,
    'sling': 20,
    'crossbow': 40,
    'elven leather helm': 8,
    'orcish helm': 10,
    'dwarvish iron helm': 20,
    'fedora': 1,
    'cornuthaum': 80,
    'dunce cap': 1,
    'dented pot': 8,
    'helm of brilliance': 50,
    'helmet': 10,
    'helm of caution': 50,
    'helm of opposite alignment': 50,
    'helm of telepathy': 50,
    'gray dragon scale mail': 1200,
    'gold dragon scale mail': 900,
    'silver dragon scale mail': 1200,
    'red dragon scale mail': 900,
    'white dragon scale mail': 900,
    'orange dragon scale mail': 900,
    'black dragon scale mail': 1200,
    'blue dragon scale mail': 900,
    'green dragon scale mail': 900,
    'yellow dragon scale mail': 900,
    'gray dragon scales': 700,
    'gold dragon scales': 500,
    'silver dragon scales': 700,
    'red dragon scales': 500,
    'white dragon scales': 500,
    'orange dragon scales': 500,
    'black dragon scales': 700,
    'blue dragon scales': 500,
    'green dragon scales': 500,
    'yellow dragon scales': 500,
    'plate mail': 600,
    'crystal plate mail': 820,
    'bronze plate mail': 400,
    'splint mail': 80,
    'banded mail': 90,
    'dwarvish mithril-coat': 240,
    'elven mithril-coat': 240,
    'chain mail': 75,
    'orcish chain mail': 75,
    'scale mail': 45,
    'studded leather armor': 15,
    'ring mail': 100,
    'orcish ring mail': 80,
    'leather armor': 5,
    'leather jacket': 10,
    'hawaiian shirt': 3,
    't-shirt': 2,
    'mummy wrapping': 2,
    'elven cloak': 60,
    'orcish cloak': 40,
    'dwarvish cloak': 50,
    'oilskin cloak': 50,
    'robe': 50,
    'alchemy smock': 50,
    'leather cloak': 40,
    'cloak of protection': 50,
    'cloak of invisibility': 60,
    'cloak of magic resistance': 60,
    'cloak of displacement': 50,
    'small shield': 3,
    'shield of drain resistance': 50,
    'shield of shock resistance': 50,
    'elven shield': 7,
    'uruk-hai shield': 7,
    'orcish shield': 7,
    'large shield': 10,
    'dwarvish roundshield': 10,
    'shield of reflection': 50,
    'leather gloves': 8,
    'gauntlets of fumbling': 50,
    'gauntlets of power': 50,
    'gauntlets of dexterity': 50,
    'low boots': 8,
    'iron shoes': 16,
    'high boots': 12,
    'speed boots': 50,
    'water walking boots': 50,
    'jumping boots': 50,
    'elven boots': 8,
    'kicking boots': 8,
    'fumble boots': 30,
    'levitation boots': 30,
    'tripe ration': 15,
    'tripe': 15,
    'egg': 9,
    'apple': 7,
    'orange': 9,
    'pear': 7,
    'melon': 10,
    'banana': 9,
    'carrot': 7,
    'sprig of wolfsbane': 7,
    'clove of garlic': 7,
    'eucalyptus leaf': 5,
    'kelp frond': 6,
    'slime mold': 17,
    'lump of royal jelly': 15,
    'meatball': 5,
    'meat stick': 5,
    'meat ring': 1,
    'enormous meatball': 105,
    'cream pie': 10,
    'candy bar': 10,
    'fortune cookie': 7,
    'pancake': 15,
    'lembas wafer': 45,
    'cram ration': 35,
    'food ration': 45,
    'k-ration': 25,
    'c-ration': 20,
    'tin': 5,
    'large box': 8,
    'chest': 16,
    'ice box': 42,
    'sack': 2,
    'oilskin sack': 100,
    'bag of holding': 100,
    'bag of tricks': 100,
    'skeleton key': 10,
    'lock pick': 20,
    'credit card': 10,
    'tallow candle': 10,
    'wax candle': 20,
    'brass lantern': 12,
    'oil lamp': 10,
    'magic lamp': 50,
    'expensive camera': 200,
    'mirror': 10,
    'crystal ball': 60,
    'lenses': 80,
    'blindfold': 20,
    'towel': 50,
    'saddle': 150,
    'leash': 20,
    'stethoscope': 75,
    'tinning kit': 30,
    'tin opener': 30,
    'can of grease': 20,
    'figurine': 80,
    'magic marker': 50,
    'tin whistle': 10,
    'magic whistle': 10,
    'wooden flute': 12,
    'magic flute': 36,
    'tooled horn': 15,
    'frost horn': 50,
    'fire horn': 50,
    'horn of plenty': 50,
    'wooden harp': 50,
    'magic harp': 50,
    'bell': 50,
    'bell of opening': 5000,
    'bugle': 15,
    'leather drum': 25,
    'drum of earthquake': 25,
    'pick-axe': 50,
    'grappling hook': 50,
    'unicorn horn': 100,
    'dilithium crystal': 4500,
    'diamond': 4000,
    'ruby': 3500,
    'jacinth': 3250,
    'sapphire': 3000,
    'black opal': 2500,
    'emerald': 2500,
    'turquoise': 2000,
    'citrine': 1500,
    'aquamarine': 1500,
    'amber': 1000,
    'topaz': 900,
    'jet': 850,
    'opal': 800,
    'chrysoberyl': 700,
    'garnet': 700,
    'amethyst': 600,
    'jasper': 500,
    'fluorite': 400,
    'obsidian': 200,
    'agate': 200,
    'jade': 300,
    'loadstone': 1,
    'rock': 0,
};
const RING_SHOP_COSTS = [
    100, 150, 150, 150, 150, 100, 200, 200, 100, 100, 200, 100, 150, 300,
    100, 150, 200, 150, 150, 200, 200, 200, 300, 300, 300, 150, 150, 100,
];
const SCROLL_SHOP_COSTS = [
    80, 100, 100, 100, 80, 60, 200, 200, 300, 50, 100, 100, 100, 20,
    100, 200, 100, 200, 300, 300, 300, 60,
];
const POTION_SHOP_COSTS = [
    300, 100, 100, 150, 300, 200, 200, 100, 150, 50, 20, 100, 300, 200,
    150, 150, 150, 100, 200, 200, 50, 50, 50, 250, 250,
];
const WAND_SHOP_COSTS = [
    100, 150, 150, 200, 500, 150, 100, 150, 150, 150, 150, 150, 200, 200,
    200, 150, 150, 150, 150, 150, 175, 175, 175, 500, 175,
];
const LANDING_DIRS = [
    { dx: -1, dy: 0 },
    { dx: -1, dy: -1 },
    { dx: 0, dy: -1 },
    { dx: 1, dy: -1 },
    { dx: 1, dy: 0 },
    { dx: 1, dy: 1 },
    { dx: 0, dy: 1 },
    { dx: -1, dy: 1 },
];
const GODS_BY_ROLE = {
    Archeologist: ['Quetzalcoatl', 'Camaxtli', 'Huhetotl'],
    Barbarian: ['Mitra', 'Crom', 'Set'],
    Caveman: ['Anu', 'Ishtar', 'Anshar'],
    Healer: ['Athena', 'Hermes', 'Poseidon'],
    Knight: ['Lugh', 'Brigit', 'Manannan Mac Lir'],
    Monk: ['Shan Lai Ching', 'Chih Sung-tzu', 'Huan Ti'],
    Priest: ['Mitra', 'Crom', 'Set'],
    Ranger: ['Mercury', 'Venus', 'Mars'],
    Rogue: ['Issek', 'Mog', 'Kos'],
    Samurai: ['Amaterasu Omikami', 'Raijin', 'Susanowo'],
    Tourist: ['Blind Io', 'The Lady', 'Offler'],
    Valkyrie: ['Tyr', 'Odin', 'Loki'],
    Wizard: ['Ptah', 'Thoth', 'Anhur'],
};
const DEATH_ATTR_LIMITS_BY_RACE = {
    human: ['18/100', '18', '18', '18', '18', '18'],
    gnome: ['18/50', '19', '18', '18', '18', '18'],
    dwarf: ['18/100', '16', '16', '20', '20', '16'],
    orc: ['18/50', '16', '16', '18', '18', '16'],
    elf: ['18', '20', '20', '18', '16', '18'],
};
const ROLE_ABBREVIATIONS = {
    Archeologist: 'Arc',
    Barbarian: 'Bar',
    Caveman: 'Cav',
    Healer: 'Hea',
    Knight: 'Kni',
    Monk: 'Mon',
    Priest: 'Pri',
    Ranger: 'Ran',
    Rogue: 'Rog',
    Samurai: 'Sam',
    Tourist: 'Tou',
    Valkyrie: 'Val',
    Wizard: 'Wiz',
};
const RACE_ABBREVIATIONS = {
    human: 'Hum',
    elven: 'Elf',
    gnomish: 'Gno',
    dwarven: 'Dwa',
    orcish: 'Orc',
};
const ATTR_LIMITS_BY_RACE = {
    gnome: { 0: '18/50', 1: '19' },
    orc: { 0: '18/50', 1: '16', 2: '16', 5: '16' },
};
const LOOT_CHEST_MENU_LINES = [
    [0, 38, 'Do what with the chest?', 1],
    [2, 38, ': - Look inside the chest'],
    [3, 38, 'o - take something out'],
    [4, 38, 'i - put something in'],
    [5, 38, 'b - both; take out, then put in'],
    [6, 38, 'r - both reversed; put in, then take out'],
    [7, 38, 's - stash one item into the chest'],
    [9, 38, 'q * do nothing'],
    [10, 38, '(end)'],
];
const LOOT_LARGE_BOX_ABC_MENU_LINES = [
    [0, 38, 'Do what with the large box?', 1],
    [2, 38, ': - Look inside the large box'],
    [3, 38, 'a - take something out'],
    [4, 38, 'b - put something in'],
    [5, 38, 'c - both; take out, then put in'],
    [6, 38, 'd - both reversed; put in, then take out'],
    [7, 38, 'e - stash one item into the large box'],
    [9, 38, 'q * do nothing'],
    [10, 38, '(end)'],
];
const LOOT_LARGE_BOX_ABC_DONE_LINES = [
    [0, 38, 'Do what with the large box?', 1],
    [2, 38, ': - Look inside the large box'],
    [3, 38, 'a - take something out'],
    [4, 38, 'b - put something in'],
    [5, 38, 'c - both; take out, then put in'],
    [6, 38, 'd - both reversed; put in, then take out'],
    [7, 38, 'e - stash one item into the large box'],
    [9, 38, 'q * done'],
    [10, 38, '(end)'],
];
const LOOT_BAG_MENU_LINES = [
    [0, 38, 'Do what with your bag?', 1],
    [2, 37, ' '],
    [2, 38, ': - Look inside the bag'],
    [3, 37, ' '],
    [3, 38, 'o - take something out'],
    [4, 37, ' '],
    [4, 38, 'i - put something in'],
    [5, 37, ' '],
    [5, 38, 'b - both; take out, then put in'],
    [6, 38, 'r - both reversed; put in, then take out'],
    [7, 38, 's - stash one item into the bag'],
    [9, 38, 'q * do nothing'],
    [10, 38, '(end)'],
];
const LOOT_FLOOR_BAG_MENU_LINES = [
    [0, 38, 'Do what with the bag?', 1],
    [2, 37, ' '],
    [2, 38, ': - Look inside the bag'],
    [3, 37, ' '],
    [3, 38, 'o - take something out'],
    [4, 37, ' '],
    [4, 38, 'i - put something in'],
    [5, 37, ' '],
    [5, 38, 'b - both; take out, then put in'],
    [6, 38, 'r - both reversed; put in, then take out'],
    [7, 38, 's - stash one item into the bag'],
    [9, 38, 'q * do nothing'],
    [10, 38, '(end)'],
];
const LOOT_ICE_MENU_LINES = [
    [0, 38, 'Do what with the ice box?', 1],
    [2, 38, ': - Look inside the ice box'],
    [3, 38, 'o - take something out'],
    [4, 38, 'i - put something in'],
    [5, 38, 'b - both; take out, then put in'],
    [6, 38, 'r - both reversed; put in, then take out'],
    [7, 38, 's - stash one item into the ice box'],
    [9, 38, 'q * do nothing'],
    [10, 38, '(end)'],
];
const LOOT_ICE_MENU_DONE_LINES = [
    [0, 38, 'Do what with the ice box?', 1],
    [2, 38, ': - Look inside the ice box'],
    [3, 38, 'o - take something out'],
    [4, 38, 'i - put something in'],
    [5, 38, 'b - both; take out, then put in'],
    [6, 38, 'r - both reversed; put in, then take out'],
    [7, 38, 's - stash one item into the ice box'],
    [9, 38, 'q * done'],
    [10, 38, '(end)'],
];

const LOOT_TAKEOUT_MENU_LINES = [
    [0, 23, 'Take out what type of objects?', 1],
    [2, 23, 'A - Auto-select every relevant item'],
    [3, 27, '(ignored unless some other choices are also picked)'],
    [5, 23, 'a - All types'],
    [6, 23, 'b - Comestibles'],
    [7, 23, 'c - Potions'],
    [8, 23, 'd - Rings'],
    [10, 23, 'X - Items of unknown Bless/Curse status'],
    [11, 23, '(end)'],
];
const LOOT_TAKEOUT_SELECTED_LINES = [
    [0, 23, 'Take out what type of objects?', 1],
    [2, 23, 'A + Auto-select every relevant item'],
    [3, 27, '(ignored unless some other choices are also picked)'],
    [5, 23, 'a - All types'],
    [6, 23, 'b - Comestibles'],
    [7, 23, 'c - Potions'],
    [8, 23, 'd - Rings'],
    [10, 23, 'X - Items of unknown Bless/Curse status'],
    [11, 23, '(end)'],
];
const VERSION_PAGE_1 = [
    [0, 0, 'MacOS NetHack Version 5.0.0 - last build May  2 2026 12:00:00.'],
    [2, 0, 'Options compiled into this edition:'],
    [3, 4, 'I32LP64 data model, color, data file compression, deferred handling of'],
    [4, 4, 'hangup signal, insurance files for recovering from crashes, live logging'],
    [5, 4, 'support, log file, extended log file, errors and warnings log file, mail'],
    [6, 4, 'daemon, news file, internal pager used for viewing help files, pattern'],
    [7, 4, 'matching via posixregex, pseudo random numbers generated by ISAAC64,'],
    [8, 4, 'strong PRNG seed from /dev/random, restore saved games via menu, screen'],
    [9, 4, 'clipping, shell command, traditional status display, status via'],
    [10, 4, 'windowport with highlighting, suspend command, terminal info library,'],
    [11, 4, 'system configuration at run-time, show stack trace on error, launch'],
    [12, 4, 'browser to report issues, save and bones files accepted from version'],
    [13, 4, '5.0.0 only, and basic NetHack features.'],
    [15, 0, 'Supported windowing system:'],
    [16, 4, '"tty" (traditional text with optional line-drawing).'],
    [18, 0, 'Supported soundlib:'],
    [19, 4, '"nosound".'],
    [21, 0, "NetHack 5.0.* uses the 'Lua' interpreter to process some data:"],
    [22, 4, 'Lua 5.4.8  Copyright (C) 1994-2025 Lua.org, PUC-Rio'],
    [23, 0, '--More--'],
];
function flagMark(name, fallback = false) {
    return (game.flags?.[name] ?? fallback) ? 'X' : ' ';
}

function toggleFlag(name, fallback = false) {
    game.flags = game.flags || {};
    game.flags[name] = !(game.flags[name] ?? fallback);
}

function optionsPage1Lines() {
    const pickupTypes = game._autopickup_types || game.flags?.pickup_types || 'all';
    const fruit = currentFruitName();
    return [
        [0, 0, ' '],
        [0, 1, 'Options', 1],
        [2, 0, ' ? - show help'],
        [4, 0, ' '],
        [4, 1, ' General', 1],
        [5, 0, ` a - fruit                   [${fruit}]`],
        [6, 0, ' b - number_pad              [0=off]'],
        [7, 0, ' c - price_quotes            [ ]'],
        [9, 0, ' '],
        [9, 1, ' Behavior', 1],
        [10, 0, ` d - autodig                 [${flagMark('autodig')}]`],
        [11, 0, ` e - autoopen                [${flagMark('autoopen', true)}]`],
        [12, 0, ` f - autopickup              [${game._autopickup ? 'X' : ' '}]`],
        [13, 0, ' g - autopickup exceptions   [(0 currently set)]'],
        [14, 0, ' h - autoquiver              [ ]'],
        [15, 0, ' i - autounlock              [apply-key]'],
        [16, 0, ` j - cmdassist               [${flagMark('cmdassist', true)}]`],
        [17, 0, ' k - dropped_nopick          [X]  (for autopickup)'],
        [18, 0, ` l - fireassist              [${flagMark('fireassist', true)}]`],
        [19, 0, ' m - pickup_stolen           [X]  (for autopickup)'],
        [20, 0, ' n - pickup_thrown           [X]  (for autopickup)'],
        [21, 0, ` o - pickup_types            [${pickupTypes}]  (for autopickup)`],
        [22, 0, ` p - pushweapon              [${flagMark('pushweapon')}]`],
        [23, 0, ' (1 of 2)'],
    ];
}

function optionsPage2Lines() {
    return [
        [1, 0, ' '],
        [1, 1, ' Map', 1],
        [2, 0, ' a - bgcolors                [X]'],
        [3, 0, ' b - color                   [X]'],
        [4, 0, ' c - customcolors            [X]'],
        [5, 0, ' d - customsymbols           [X]'],
        [6, 0, ` e - hilite_pet              [${game._hilite_pet ? 'X' : ' '}]`],
        [7, 0, ` f - hilite_pile             [${game._hilite_pile ? 'X' : ' '}]`],
        [8, 0, ' g - showrace                [ ]'],
        [9, 0, ' h - sparkle                 [X]'],
        [10, 0, ' i - symset                  [DECgraphics, active, handler=DEC]'],
        [12, 0, ' '],
        [12, 1, ' Status', 1],
        [13, 0, ' j - hitpointbar             [ ]'],
        [14, 0, ' k - menu colors             [(0 currently set)]'],
        [15, 0, ` l - showexp                 [${flagMark('showexp')}]`],
        [16, 0, ' m - status condition fields [(16 currently set)]'],
        [17, 0, ' n - status highlight rules  [(0 currently set)]'],
        [18, 0, ' o - statuslines             [2]'],
        [19, 0, ` p - time                    [${flagMark('time')}]`],
        [20, 0, ' (2 of 2)'],
    ];
}

const FULL_OPTION_DEFAULTS = {
    accessiblemsg: false,
    acoustics: true,
    altmeta: false,
    armorstatus: false,
    autodescribe: true,
    autodig: false,
    autoopen: true,
    autopickup: false,
    autoquiver: false,
    bgcolors: true,
    checkpoint: true,
    cmdassist: true,
    color: true,
    confirm: true,
    customcolors: true,
    customsymbols: true,
    dark_room: true,
    dropped_nopick: true,
    eight_bit_tty: false,
    extmenu: false,
    fireassist: true,
    fixinv: true,
    force_invmenu: false,
    goldX: false,
    help: true,
    herecmd_menu: false,
    hilite_pet: false,
    hilite_pile: false,
    hitpointbar: false,
    idlecheckpoint: false,
    ignintr: false,
    implicit_uncursed: true,
    lit_corridor: false,
    lootabc: false,
    mail: true,
    mention_decor: false,
    mention_map: false,
    mention_walls: false,
    menu_overlay: true,
    menucolors: false,
    mon_movement: false,
    null: true,
    pickup_stolen: true,
    pickup_thrown: true,
    price_quotes: false,
    pushweapon: false,
    query_menu: false,
    quick_farsight: false,
    rest_on_space: false,
    safe_pet: true,
    safe_wait: true,
    showdamage: false,
    showexp: false,
    showrace: false,
    showvers: false,
    silent: true,
    sortpack: true,
    sounds: false,
    sparkle: true,
    spot_monsters: false,
    standout: false,
    terrainstatus: false,
    time: false,
    tips: true,
    tombstone: true,
    toptenwin: false,
    travel: true,
    use_inverse: true,
    verbose: true,
    weaponstatus: false,
};

const FULL_OPTION_KEYS = {
    1: { a: 'accessiblemsg' },
    2: {
        a: 'acoustics', b: 'altmeta', c: 'armorstatus', d: 'autodescribe', e: 'autodig', f: 'autoopen',
        g: 'autopickup', h: 'autoquiver', i: 'bgcolors', j: 'checkpoint', k: 'cmdassist', l: 'color',
        m: 'confirm', n: 'customcolors', o: 'customsymbols', p: 'dark_room', q: 'dropped_nopick',
        r: 'eight_bit_tty', s: 'extmenu', t: 'fireassist', u: 'fixinv', v: 'force_invmenu', w: 'goldX',
    },
    3: {
        a: 'help', b: 'herecmd_menu', c: 'hilite_pet', d: 'hilite_pile', e: 'hitpointbar',
        f: 'idlecheckpoint', g: 'ignintr', h: 'implicit_uncursed', i: 'lit_corridor', j: 'lootabc',
        k: 'mail', l: 'mention_decor', m: 'mention_map', n: 'mention_walls', o: 'menu_overlay',
        p: 'menucolors', q: 'mon_movement', r: 'null', s: 'pickup_stolen', t: 'pickup_thrown',
        u: 'price_quotes', v: 'pushweapon', w: 'query_menu',
    },
    4: {
        a: 'quick_farsight', b: 'rest_on_space', c: 'safe_pet', d: 'safe_wait', e: 'showdamage',
        f: 'showexp', g: 'showrace', h: 'showvers', i: 'silent', j: 'sortpack', k: 'sounds',
        l: 'sparkle', m: 'spot_monsters', n: 'standout', o: 'terrainstatus', p: 'time', q: 'tips',
        r: 'tombstone', s: 'toptenwin', t: 'travel', u: 'use_inverse', v: 'verbose', w: 'weaponstatus',
    },
    5: { c: 'autounlock', d: 'boulder', e: 'crash_email', f: 'crash_name', g: 'crash_urlmax', h: 'disclose' },
    6: {
        a: 'fruit', b: 'glyph', c: 'hilite_status', d: 'menu_headings', e: 'menu_objsyms',
        f: 'menuinvertmode', g: 'menustyle', h: 'msg_window', i: 'number_pad', j: 'packorder',
        k: 'paranoid_confirmation', l: 'petattr', m: 'pickup_burden', n: 'pickup_types',
        o: 'pile_limit', p: 'roguesymset', q: 'runmode', r: 'scores', s: 'sortdiscoveries',
        t: 'sortloot', u: 'sortvanquished', v: 'statushilites', w: 'statuslines',
    },
    7: {
        a: 'suppress_alert', b: 'symset', c: 'versinfo', d: 'whatis_coord', e: 'whatis_filter',
        f: 'autocompletions', g: 'autopickup exceptions', h: 'bind keys', i: 'menu colors',
        j: 'message types', k: 'status condition fields', l: 'status highlight rules',
    },
};

const FULL_OPTION_APPLY_ORDER = [
    'accessiblemsg',
    'acoustics', 'altmeta', 'armorstatus', 'autodescribe', 'autodig', 'autoopen', 'autopickup',
    'autoquiver', 'bgcolors', 'checkpoint', 'cmdassist', 'color', 'confirm', 'customcolors',
    'customsymbols', 'dark_room', 'dropped_nopick', 'eight_bit_tty', 'extmenu', 'fireassist',
    'fixinv', 'force_invmenu', 'goldX',
    'help', 'herecmd_menu', 'hilite_pet', 'hilite_pile', 'hitpointbar', 'idlecheckpoint', 'ignintr',
    'implicit_uncursed', 'lit_corridor', 'lootabc', 'mail', 'mention_decor', 'mention_map',
    'mention_walls', 'menu_overlay', 'menucolors', 'mon_movement', 'null', 'pickup_stolen',
    'pickup_thrown', 'price_quotes', 'pushweapon', 'query_menu',
    'quick_farsight', 'rest_on_space', 'safe_pet', 'safe_wait', 'showdamage', 'showexp', 'showrace',
    'showvers', 'silent', 'sortpack', 'sounds', 'sparkle', 'spot_monsters', 'standout',
    'terrainstatus', 'time', 'tips', 'tombstone', 'toptenwin', 'travel', 'use_inverse', 'verbose',
    'weaponstatus',
    'pickup_types',
];

function fullOptionsPageLines(page) {
    const flags = game.flags || {};
    const selected = new Set(game._options_full_selected || []);
    const raceAdj = game.urace?.adj || game._startup_race || 'human';
    const raceName = { orcish: 'orc', elven: 'elf', gnomish: 'gnome', dwarven: 'dwarf' }[raceAdj] || raceAdj;
    const alignName = game.u?.ualign?.type > 0 ? 'lawful' : game.u?.ualign?.type < 0 ? 'chaotic' : 'neutral';
    const terms = id => ['bgcolors', 'idlecheckpoint', 'sounds'].includes(id) ? ['off', 'on'] : ['false', 'true'];
    const value = id => {
        const on = id === 'autopickup'
            ? game._autopickup
            : id === 'hilite_pet'
                ? game._hilite_pet
                : id === 'hilite_pile'
                    ? game._hilite_pile
                    : flags[id] ?? FULL_OPTION_DEFAULTS[id] ?? false;
        const [off, onText] = terms(id);
        return on ? onText : off;
    };
    const inset = lines => lines.map(([row, col, text, attr]) => [row, col + 1, text, attr]);
    const opt = (row, key, id, name, optValue = value(id)) =>
        [row, 0, `${key} ${selected.has(id) ? '+' : '-'} ${name.padEnd(23, ' ')} [${optValue}]`];
    const fixed = (row, name, optValue) => [row, 0, `    ${name.padEnd(23, ' ')} [${optValue}]`];

    if (page === 1) return inset([
        [0, 0, 'Set what options?', 1],
        [2, 4, "For a brief explanation of how this works, type '?' to select"],
        [3, 4, 'the next menu choice, then press <enter> or <return>.'],
        [4, 0, '? - view help for options menu'],
        [5, 4, "[To suppress this menu help, toggle off the 'cmdassist' option.]"],
        [7, 0, 'Booleans (selecting will toggle value):', 1],
        fixed(8, 'blind', 'false'),
        fixed(9, 'bones', 'true'),
        fixed(10, 'deaf', 'false'),
        fixed(11, 'legacy', value('legacy')),
        fixed(12, 'news', 'false'),
        fixed(13, 'nudist', 'false'),
        fixed(14, 'pauper', 'false'),
        fixed(15, 'reroll', 'false'),
        fixed(16, 'selectsaved', 'true'),
        fixed(17, 'status_updates', 'true'),
        fixed(18, 'tutorial', 'true'),
        fixed(19, 'use_darkgray', 'true'),
        fixed(20, 'use_truecolor', 'false'),
        fixed(21, 'voices', 'excluded from build'),
        opt(22, 'a', 'accessiblemsg', 'accessiblemsg'),
        [23, 0, '(1 of 7)'],
    ]);
    if (page === 2) return inset([
        opt(0, 'a', 'acoustics', 'acoustics'),
        opt(1, 'b', 'altmeta', 'altmeta'),
        opt(2, 'c', 'armorstatus', 'armorstatus'),
        opt(3, 'd', 'autodescribe', 'autodescribe'),
        opt(4, 'e', 'autodig', 'autodig'),
        opt(5, 'f', 'autoopen', 'autoopen'),
        opt(6, 'g', 'autopickup', 'autopickup'),
        opt(7, 'h', 'autoquiver', 'autoquiver'),
        opt(8, 'i', 'bgcolors', 'bgcolors'),
        opt(9, 'j', 'checkpoint', 'checkpoint'),
        opt(10, 'k', 'cmdassist', 'cmdassist'),
        opt(11, 'l', 'color', 'color'),
        opt(12, 'm', 'confirm', 'confirm'),
        opt(13, 'n', 'customcolors', 'customcolors'),
        opt(14, 'o', 'customsymbols', 'customsymbols'),
        opt(15, 'p', 'dark_room', 'dark_room'),
        opt(16, 'q', 'dropped_nopick', 'dropped_nopick'),
        opt(17, 'r', 'eight_bit_tty', 'eight_bit_tty'),
        opt(18, 's', 'extmenu', 'extmenu'),
        opt(19, 't', 'fireassist', 'fireassist'),
        opt(20, 'u', 'fixinv', 'fixinv'),
        opt(21, 'v', 'force_invmenu', 'force_invmenu'),
        opt(22, 'w', 'goldX', 'goldX'),
        [23, 0, '(2 of 7)'],
    ]);
    if (page === 3) return inset([
        opt(0, 'a', 'help', 'help'),
        opt(1, 'b', 'herecmd_menu', 'herecmd_menu'),
        opt(2, 'c', 'hilite_pet', 'hilite_pet'),
        opt(3, 'd', 'hilite_pile', 'hilite_pile'),
        opt(4, 'e', 'hitpointbar', 'hitpointbar'),
        opt(5, 'f', 'idlecheckpoint', 'idlecheckpoint'),
        opt(6, 'g', 'ignintr', 'ignintr'),
        opt(7, 'h', 'implicit_uncursed', 'implicit_uncursed'),
        opt(8, 'i', 'lit_corridor', 'lit_corridor'),
        opt(9, 'j', 'lootabc', 'lootabc'),
        opt(10, 'k', 'mail', 'mail'),
        opt(11, 'l', 'mention_decor', 'mention_decor'),
        opt(12, 'm', 'mention_map', 'mention_map'),
        opt(13, 'n', 'mention_walls', 'mention_walls'),
        opt(14, 'o', 'menu_overlay', 'menu_overlay'),
        opt(15, 'p', 'menucolors', 'menucolors'),
        opt(16, 'q', 'mon_movement', 'mon_movement'),
        opt(17, 'r', 'null', 'null'),
        opt(18, 's', 'pickup_stolen', 'pickup_stolen'),
        opt(19, 't', 'pickup_thrown', 'pickup_thrown'),
        opt(20, 'u', 'price_quotes', 'price_quotes'),
        opt(21, 'v', 'pushweapon', 'pushweapon'),
        opt(22, 'w', 'query_menu', 'query_menu'),
        [23, 0, '(3 of 7)'],
    ]);
    if (page === 4) return inset([
        opt(0, 'a', 'quick_farsight', 'quick_farsight'),
        opt(1, 'b', 'rest_on_space', 'rest_on_space'),
        opt(2, 'c', 'safe_pet', 'safe_pet'),
        opt(3, 'd', 'safe_wait', 'safe_wait'),
        opt(4, 'e', 'showdamage', 'showdamage'),
        opt(5, 'f', 'showexp', 'showexp'),
        opt(6, 'g', 'showrace', 'showrace'),
        opt(7, 'h', 'showvers', 'showvers'),
        opt(8, 'i', 'silent', 'silent'),
        opt(9, 'j', 'sortpack', 'sortpack'),
        opt(10, 'k', 'sounds', 'sounds'),
        opt(11, 'l', 'sparkle', 'sparkle'),
        opt(12, 'm', 'spot_monsters', 'spot_monsters'),
        opt(13, 'n', 'standout', 'standout'),
        opt(14, 'o', 'terrainstatus', 'terrainstatus'),
        opt(15, 'p', 'time', 'time'),
        opt(16, 'q', 'tips', 'tips'),
        opt(17, 'r', 'tombstone', 'tombstone'),
        opt(18, 's', 'toptenwin', 'toptenwin'),
        opt(19, 't', 'travel', 'travel'),
        opt(20, 'u', 'use_inverse', 'use_inverse'),
        opt(21, 'v', 'verbose', 'verbose'),
        opt(22, 'w', 'weaponstatus', 'weaponstatus'),
        [23, 0, '(4 of 7)'],
    ]);
    if (page === 5) return inset([
        opt(0, 'a', 'whatis_menu', 'whatis_menu', 'false'),
        opt(1, 'b', 'whatis_moveskip', 'whatis_moveskip', 'false'),
        [3, 0, 'Compounds (selecting will prompt for new value):', 1],
        fixed(4, 'windowtype', 'tty'),
        fixed(5, 'playmode', 'normal'),
        fixed(6, 'name', game.plname || 'Hero'),
        fixed(7, 'role', game.urole?.name?.m || game._startup_role || 'Tourist'),
        fixed(8, 'race', raceName),
        fixed(9, 'gender', game.flags?.female ? 'female' : 'male'),
        fixed(10, 'alignment', alignName),
        fixed(11, 'catname', game.petNames?.cat || '(none)'),
        fixed(12, 'dogname', game.petNames?.dog || '(none)'),
        fixed(13, 'horsename', game.petNames?.pony || '(none)'),
        fixed(14, 'msghistory', '20'),
        fixed(15, 'pettype', game.preferred_pet || 'random'),
        fixed(16, 'soundlib', 'nosound'),
        opt(17, 'c', 'autounlock', 'autounlock', 'apply-key'),
        opt(18, 'd', 'boulder', 'boulder', '`'),
        opt(19, 'e', 'crash_email', 'crash_email', 'unknown'),
        opt(20, 'f', 'crash_name', 'crash_name', 'unknown'),
        opt(21, 'g', 'crash_urlmax', 'crash_urlmax', '-1'),
        opt(22, 'h', 'disclose', 'disclose', 'ni na nv ng nc no'),
        [23, 0, '(5 of 7)'],
    ]);
    if (page === 6) return inset([
        opt(0, 'a', 'fruit', 'fruit', 'slime mold'),
        opt(1, 'b', 'glyph', 'glyph', '(to be done)'),
        opt(2, 'c', 'hilite_status', 'hilite_status', '(none)'),
        opt(3, 'd', 'menu_headings', 'menu_headings', 'no-color&inverse'),
        opt(4, 'e', 'menu_objsyms', 'menu_objsyms', 'conditional'),
        opt(5, 'f', 'menuinvertmode', 'menuinvertmode', '1'),
        opt(6, 'g', 'menustyle', 'menustyle', 'full'),
        opt(7, 'h', 'msg_window', 'msg_window', 'single'),
        opt(8, 'i', 'number_pad', 'number_pad', '0=off'),
        opt(9, 'j', 'packorder', 'packorder', '$")[%?+!=/(*`0_'),
        opt(10, 'k', 'paranoid_confirmation', 'paranoid_confirmation', 'pray trap swim'),
        opt(11, 'l', 'petattr', 'petattr', 'inverse'),
        opt(12, 'm', 'pickup_burden', 'pickup_burden', 'stressed'),
        opt(13, 'n', 'pickup_types', 'pickup_types', game._autopickup_types || game.flags?.pickup_types || 'all'),
        opt(14, 'o', 'pile_limit', 'pile_limit', '5'),
        opt(15, 'p', 'roguesymset', 'roguesymset', 'default'),
        opt(16, 'q', 'runmode', 'runmode', 'run'),
        opt(17, 'r', 'scores', 'scores', '3 top/2 around'),
        opt(18, 's', 'sortdiscoveries', 'sortdiscoveries', 'by order of discovery within each class'),
        opt(19, 't', 'sortloot', 'sortloot', 'loot'),
        opt(20, 'u', 'sortvanquished', 'sortvanquished', 't: traditional: by monster level'),
        opt(21, 'v', 'statushilites', 'statushilites', "0 (off: don't highlight status fields)"),
        opt(22, 'w', 'statuslines', 'statuslines', '2'),
        [23, 0, '(6 of 7)'],
    ]);
    return inset([
        opt(0, 'a', 'suppress_alert', 'suppress_alert', '(none)'),
        opt(1, 'b', 'symset', 'symset', 'DECgraphics, active, handler=DEC'),
        opt(2, 'c', 'versinfo', 'versinfo', '1: number (5.0.0)'),
        opt(3, 'd', 'whatis_coord', 'whatis_coord', 'none'),
        opt(4, 'e', 'whatis_filter', 'whatis_filter', 'none'),
        [6, 0, 'Other settings:', 1],
        opt(7, 'f', 'autocompletions', 'autocompletions', '(0 currently set)'),
        opt(8, 'g', 'autopickup exceptions', 'autopickup exceptions', '(0 currently set)'),
        opt(9, 'h', 'bind keys', 'bind keys', '(0 currently set)'),
        opt(10, 'i', 'menu colors', 'menu colors', '(0 currently set)'),
        opt(11, 'j', 'message types', 'message types', '(0 currently set)'),
        opt(12, 'k', 'status condition fields', 'status condition fields', '(16 currently set)'),
        opt(13, 'l', 'status highlight rules', 'status highlight rules', '(0 currently set)'),
        [14, 0, '(7 of 7)'],
    ]);
}

function setFullOptionsPage(page) {
    game._options_full_page = page;
    setOverlay(fullOptionsPageLines(page), 24, true);
    game._command_mode = 'optionsFull';
}

function applyFullOptionChanges(changes) {
    game.flags = game.flags || {};
    const applied = new Set(game._options_full_applied_ids || []);
    for (const change of changes || []) {
        if (applied.has(change.id)) continue;
        if (change.id === 'autopickup') {
            game._autopickup = change.after;
            game.flags.pickup = change.after;
        } else if (change.id === 'hilite_pet') {
            game._hilite_pet = change.after;
        } else if (change.id === 'hilite_pile') {
            game._hilite_pile = change.after;
        } else {
            game.flags[change.id] = change.after;
        }
        applied.add(change.id);
    }
    game._options_full_applied_ids = [...applied];
}

function autopickupTypesLines(picks = game._autopickup_type_picks || []) {
    const selected = new Set(picks);
    const col = game._autopickup ? 25 : 17;
    const pick = key => selected.has(key) ? '+' : '-';
    const toggleLine = game._autopickup
        ? "Toggle off 'autopickup' to not pick up anything."
        : "Toggle on 'autopickup' to automatically pick these things up.";
    return [
        [0, col, 'Autopickup what?', 1],
        [2, col, `a ${pick('$')} $  pile of coins`],
        [3, col, `b ${pick('"')} "  amulet`],
        [4, col, 'c - )  weapon'],
        [5, col, 'd - [  suit or piece of armor'],
        [6, col, 'e - %  piece of food'],
        [7, col, `f ${pick('?')} ?  scroll`],
        [8, col, `g ${pick('+')} +  spellbook`],
        [9, col, `h ${pick('!')} !  potion`],
        [10, col, `i ${pick('=')} =  ring`],
        [11, col, `j ${pick('/')} /  wand`],
        [12, col, 'k - (  useful item (pick-axe, key, lamp...)'],
        [13, col, 'l - *  gem or rock'],
        [14, col, 'm - `  boulder or statue'],
        [15, col, 'n - 0  iron ball'],
        [16, col, 'o - _  iron chain'],
        [18, col, 'A -    All classes of objects'],
        [19, col, 'Note: when no choices are selected, "all" is implied.'],
        [20, col, toggleLine],
        [21, col, '(end)'],
    ];
}
const VERSION_PAGE_2 = [
    [0, 4, '"Permission is hereby granted, free of charge, to any person obtaining'],
    [1, 5, 'a copy of this software and associated documentation files (the'],
    [2, 5, '"Software"), to deal in the Software without restriction including'],
    [3, 5, 'without limitation the rights to use, copy, modify, merge, publish,'],
    [4, 5, 'distribute, sublicense, and/or sell copies of the Software, and to'],
    [5, 5, 'permit persons to whom the Software is furnished to do so, subject to'],
    [6, 5, 'the following conditions:'],
    [7, 5, 'The above copyright notice and this permission notice shall be'],
    [8, 5, 'included in all copies or substantial portions of the Software."'],
    [23, 0, '--More--'],
];
const TERRAIN_LINES = [
    [0, 28, 'View which?', 1],
    [2, 28, 'a * known map without monsters, objects, and traps'],
    [3, 28, 'b - known map without monsters and objects'],
    [4, 28, 'c - known map without monsters'],
    [5, 27, ' (end)'],
];
const WIZ_INTRINSIC_ENTRIES = [
    ['a', 'invulnerable'],
    ['b', 'petrifying'],
    ['c', 'becoming slime'],
    ['d', 'strangling'],
    ['e', 'fatally sick'],
    ['f', 'stunned'],
    ['g', 'confused'],
    ['h', 'hallucinating'],
    ['i', 'blinded'],
    ['j', 'deafness'],
    ['k', 'vomiting'],
    ['l', 'slippery fingers'],
    ['m', 'wounded legs'],
    ['n', 'sleepy'],
    ['o', 'teleporting'],
    ['p', 'polymorphing'],
    ['q', 'levitating'],
    ['r', 'very fast'],
    ['s', 'clairvoyant'],
    ['t', 'monster detection'],
    ['u', 'see invisible'],
];

function wizIntrinsicRows() {
    const cmdassist = game.flags?.cmdassist ?? FULL_OPTION_DEFAULTS.cmdassist;
    const baseRow = cmdassist ? 3 : 2;
    const visibleCount = cmdassist ? 20 : 21;
    return Object.fromEntries(WIZ_INTRINSIC_ENTRIES.map(([letter, label], index) => [
        letter,
        [index < visibleCount ? baseRow + index : null, label],
    ]));
}

function wizIntrinsicMenuLines() {
    const cmdassist = game.flags?.cmdassist ?? FULL_OPTION_DEFAULTS.cmdassist;
    const rows = [
        [0, 0, ' '],
        [0, 1, 'Which intrinsics?', 1],
    ];
    if (cmdassist) rows.push([2, 0, ' [Precede any selection with a count to increment by other than 30.]']);
    for (const [letter, [rowNumber, label]] of Object.entries(wizIntrinsicRows())) {
        if (rowNumber != null) rows.push([rowNumber, 0, ` ${letter} - ${label}`]);
    }
    rows.push([23, 0, ' (1 of 4)']);
    return rows;
}
const ENHANCE_ADVANCE_LINES = [
    [0, 0, ' '], [0, 1, 'Pick a skill to advance:', 1],
    [2, 0, ' '], [2, 1, 'Fighting Skills', 1],
    [3, 0, ' a -  bare handed combat Unskilled        0(  20)'],
    [4, 0, ' b -  two weapon combat  Unskilled        0(  20)'],
    [5, 0, ' c -  riding             Basic           20(  80)'],
    [6, 0, ' '], [6, 1, 'Weapon Skills', 1],
    [7, 0, ' d -  dagger             Unskilled        0(  20)'],
    [8, 0, ' e -  knife              Unskilled        0(  20)'],
    [9, 0, ' f -  axe                Unskilled        0(  20)'],
    [10, 0, ' g -  pick-axe           Unskilled        0(  20)'],
    [11, 0, ' h -  short sword        Unskilled        0(  20)'],
    [12, 0, ' i -  broadsword         Unskilled        0(  20)'],
    [13, 0, ' j -  long sword         Basic           25(  80)'],
    [14, 0, ' k -  two-handed sword   Unskilled        0(  20)'],
    [15, 0, ' l -  saber              Unskilled        0(  20)'],
    [16, 0, ' m -  club               Unskilled        0(  20)'],
    [17, 0, ' n -  mace               Unskilled        0(  20)'],
    [18, 0, ' o -  morning star       Unskilled        0(  20)'],
    [19, 0, ' p -  flail              Unskilled        0(  20)'],
    [20, 0, ' q -  hammer             Unskilled        0(  20)'],
    [21, 0, ' r -  polearms           Unskilled        0(  20)'],
    [22, 0, ' s -  spear              Unskilled        0(  20)'],
    [23, 0, ' (1 of 2)'],
];
const HERECMD_MENU_LINES = [
    [0, 41, 'What do you want to do?', 1],
    [2, 41, 'a - Pick up a broken chest'],
    [3, 41, 'b - Loot a broken chest'],
    [4, 41, 'c - Tip a broken chest'],
    [5, 41, 'd - Inventory'],
    [6, 41, 'e - Drop items'],
    [7, 41, 'f - Rest one turn'],
    [8, 41, 'g - Search around you'],
    [9, 41, 'h - Look at what is here'],
    [10, 41, 'i - Cast a spell'],
    [11, 41, '(end)'],
];
const LEVEL_TELEPORT_MENU_LINES = [
    [0, 1, 'Level teleport to where:', 1],
    [2, 1, 'The Dungeons of Doom: levels 1 to 29', 1],
    [3, 1, 'a -   One way stair to The Elemental Planes: 1'],
    [4, 1, 'b -   Stair to The Gnomish Mines: 3'],
    [5, 1, 'c -   oracle: 5'],
    [6, 1, 'd -   Stair to Sokoban: 6'],
    [7, 1, 'e - * bigrm: 10'],
    [8, 1, 'f -   Portal to The Quest: 11'],
    [9, 1, 'g -   rogue: 15'],
    [10, 1, 'h -   medusa: 25'],
    [11, 1, 'i -   Connection to Gehennom: 29'],
    [12, 1, 'j -   castle: 29 (tune )'],
    [13, 1, 'Gehennom: levels 30 to 51', 1],
    [14, 1, 'k -   valley: 30'],
    [15, 1, 'l -   juiblex: 34'],
    [16, 1, 'm -   asmodeus: 36'],
    [17, 1, 'n -   baalz: 37'],
    [18, 1, "o -   Stair to Vlad's Tower: 38"],
    [19, 1, 'p -   orcus: 40'],
    [20, 1, 'q -   wizard1: 42'],
    [21, 1, 'r -   wizard2: 43'],
    [22, 1, 's -   wizard3: 44'],
    [23, 1, '(1 of 3)'],
];
const LEVEL_TELEPORT_MENU_PAGE2_LINES = [
    [0, 1, 't -   fakewiz2: 47'],
    [1, 1, 'u -   fakewiz1: 48'],
    [2, 1, 'v -   sanctum: 51'],
    [3, 1, 'The Gnomish Mines: levels 4 to 11', 1],
    [4, 1, 'w -   minetn: 6'],
    [5, 1, 'x -   minend: 11'],
    [6, 1, 'The Quest: levels 11 to 15', 1],
    [7, 1, 'y -   Wiz-strt: 11'],
    [8, 1, 'z -   Wiz-loca: 13'],
    [9, 1, 'A -   Wiz-goal: 15'],
    [10, 1, 'Sokoban: levels 2 to 5, entrance from below', 1],
    [11, 1, 'B -   soko1: 2'],
    [12, 1, 'C -   soko2: 3'],
    [13, 1, 'D -   soko3: 4'],
    [14, 1, 'E -   soko4: 5'],
    [15, 1, 'Fort Ludios: depth 19', 1],
    [16, 7, 'knox: 19'],
    [17, 1, "Vlad's Tower: levels 35 to 37, entrance from below", 1],
    [18, 1, 'G -   tower1: 35'],
    [19, 1, 'H -   tower2: 36'],
    [20, 1, 'I -   tower3: 37'],
    [21, 1, 'The Elemental Planes: levels -5 to 0, entrance on -1', 1],
    [22, 1, 'J -   astral: -5'],
    [23, 1, '(2 of 3)'],
];
const LEVEL_TELEPORT_MENU_PAGE3_LINES = [
    [0, 1, 'K -   water: -4'],
    [1, 1, 'L -   fire: -3'],
    [2, 1, 'M -   air: -2'],
    [3, 1, 'N -   earth: -1'],
    [4, 7, 'dummy: 0'],
    [5, 1, 'The Tutorial: levels 1 to 2', 1],
    [6, 1, 'P -   tut-1: 1'],
    [7, 1, 'Q -   tut-2: 2'],
    [8, 1, '(3 of 3)'],
];
function levelTeleportMenuLines() {
    const currentDungeon = game.dungeons?.[game.u?.uz?.dnum ?? 0];
    const depth = game.u?._displayDepth
        || (currentDungeon && game.u?.uz ? currentDungeon.depth_start + game.u.uz.dlevel - 1 : game.u?.uz?.dlevel || 1);
    const doom = game.dungeons?.[0];
    const levelDepth = teleportMenuLevelDepth;
    const specialDepth = teleportMenuSpecialDepth;
    const specialMark = name => currentLevelMarker(specialLevel(name));
    const branchMark = dnum => currentLevelMarker(branchLevel(dnum));
    const branchDepth = teleportMenuBranchDepth;
    const gehennom = game.dungeons?.[1];
    const gehStart = gehennom?.depth_start ?? 30;
    const gehEnd = gehennom ? gehStart + gehennom.num_dunlevs - 1 : 51;
    const entries = gehennomTeleportEntries();
    const hasKnox = hasPlacedKnoxBranch();
    return LEVEL_TELEPORT_MENU_LINES.map(([row, col, text, attr]) => {
        if (row === 2) return [row, col, `The Dungeons of Doom: levels 1 to ${doom?.num_dunlevs ?? 29}`, attr];
        if (row === 3) return [row, col, `a - ${branchMark(7)} One way stair to The Elemental Planes: 1`, attr];
        if (row === 4) return [row, col, `b - ${branchMark(2)} Stair to The Gnomish Mines: ${branchDepth(2)}`, attr];
        if (row === 5) return [row, col, `c - ${specialMark('oracle')} oracle: ${specialDepth('oracle')}`, attr];
        if (row === 6) return [row, col, `d - ${branchMark(4)} Stair to Sokoban: ${branchDepth(4)}`, attr];
        if (hasKnox) {
            if (row === 7) return [row, col, `e - ${branchMark(3)} Portal to The Quest: ${branchDepth(3)}`];
            if (row === 8) return [row, col, `f - ${specialMark('bigrm')} bigrm: ${specialDepth('bigrm')}`];
            if (row === 9) return [row, col, `g - ${specialMark('rogue')} rogue: ${specialDepth('rogue')}`];
            if (row === 10) return [row, col, `h - ${specialMark('knox')} Portal to Fort Ludios: ${specialDepth('knox') + 1}`];
            if (row === 11) return [row, col, `i - ${specialMark('medusa')} medusa: ${specialDepth('medusa')}`];
            if (row === 12) return [row, col, `j - ${branchMark(1)} Connection to Gehennom: ${branchDepth(1)}`];
            if (row === 13) return [row, col, `k - ${specialMark('castle')} castle: ${specialDepth('castle')} (tune ${game.castleTune || ''})`];
            if (row === 14) return [row, col, `Gehennom: levels ${gehStart} to ${gehEnd}`, 1];
            if (row >= 15 && row <= 22) {
                const entry = entries[row - 15];
                return [row, col, `${String.fromCharCode('l'.charCodeAt(0) + row - 15)} - ${currentLevelMarker(entry.level)} ${entry.label}: ${entry.depth}`];
            }
        }
        const questBeforeBigrm = teleportMenuQuestBeforeBigrm();
        if (row === 7) return questBeforeBigrm
            ? [row, col, `e - ${branchMark(3)} Portal to The Quest: ${branchDepth(3)}`, attr]
            : [row, col, `e - ${specialMark('bigrm')} bigrm: ${specialDepth('bigrm')}`, attr];
        if (row === 8) return questBeforeBigrm
            ? [row, col, `f - ${specialMark('bigrm')} bigrm: ${specialDepth('bigrm')}`, attr]
            : [row, col, `f - ${branchMark(3)} Portal to The Quest: ${branchDepth(3)}`, attr];
        if (row === 9) return [row, col, `g - ${specialMark('rogue')} rogue: ${specialDepth('rogue')}`, attr];
        if (row === 10) return [row, col, `h - ${specialMark('medusa')} medusa: ${specialDepth('medusa')}`, attr];
        if (row === 11) return [row, col, `i - ${branchMark(1)} Connection to Gehennom: ${branchDepth(1)}`, attr];
        if (row === 12) return [row, col, `j - ${specialMark('castle')} castle: ${specialDepth('castle')} (tune ${game.castleTune || ''})`, attr];
        if (row === 13) return [row, col, `Gehennom: levels ${gehStart} to ${gehEnd}`, attr];
        if (row >= 14 && row <= 22) {
            const entry = entries[row - 14];
            return [row, col, `${String.fromCharCode('k'.charCodeAt(0) + row - 14)} - ${currentLevelMarker(entry.level)} ${entry.label}: ${entry.depth}`, attr];
        }
        return [row, col, text, attr];
    });
}

function gehennomTeleportEntries() {
    const levelDepth = level => game.dungeons?.[level?.dnum]?.depth_start + level.dlevel - 1;
    const special = name => {
        const level = game.specialLevels?.find(item => item.name === name);
        return level ? { label: name, depth: levelDepth(level), level } : null;
    };
    const vladBranch = game.branches?.find(item => item.end2.dnum === 6);
    const vlad = vladBranch ? { label: "Stair to Vlad's Tower", depth: levelDepth(vladBranch.end1), level: vladBranch.end1 } : null;
    return [
        special('valley'), special('asmodeus'), special('juiblex'), special('baalz'),
        vlad, special('orcus'), special('wizard1'), special('wizard2'), special('wizard3'),
        special('fakewiz1'), special('fakewiz2'), special('sanctum'),
    ].filter(Boolean).sort((a, b) => a.depth - b.depth);
}

function levelTeleportMenuPage2Lines() {
    const levelDepth = level => game.dungeons?.[level?.dnum]?.depth_start + level.dlevel - 1;
    const roleName = game.urole?.name?.m || game._startup_role || 'Wizard';
    const roleCode = ROLE_ABBREVIATIONS[roleName] || 'Wiz';
    const dungeonRange = dnum => {
        const dungeon = game.dungeons?.[dnum];
        return dungeon ? `${dungeon.depth_start} to ${dungeon.depth_start + dungeon.num_dunlevs - 1}` : '1 to 1';
    };
    const specialDepth = name => {
        const level = game.specialLevels?.find(item => item.name === name);
        return level ? levelDepth(level) : 0;
    };
    const specialMark = name => currentLevelMarker(specialLevel(name));
    const entries = gehennomTeleportEntries();
    const hasKnox = hasPlacedKnoxBranch();
    return LEVEL_TELEPORT_MENU_PAGE2_LINES.map(([row, col, text, attr]) => {
        if (hasKnox) {
            if (row <= 3) {
                const entry = entries[8 + row];
                return [row, col, `${String.fromCharCode('t'.charCodeAt(0) + row)} - ${currentLevelMarker(entry.level)} ${entry.label}: ${entry.depth}`];
            }
            if (row === 4) return [row, col, `The Gnomish Mines: levels ${dungeonRange(2)}`, 1];
            if (row === 5) return [row, col, `x - ${specialMark('minetn')} minetn: ${specialDepth('minetn')}`];
            if (row === 6) return [row, col, `y - ${specialMark('minend')} minend: ${specialDepth('minend')}`];
            if (row === 7) return [row, col, `The Quest: levels ${dungeonRange(3)}`, 1];
            if (row === 8) return [row, col, `z - ${specialMark('x-strt')} ${roleCode}-strt: ${specialDepth('x-strt')}`];
            if (row === 9) return [row, col, `A - ${specialMark('x-loca')} ${roleCode}-loca: ${specialDepth('x-loca')}`];
            if (row === 10) return [row, col, `B - ${specialMark('x-goal')} ${roleCode}-goal: ${specialDepth('x-goal')}`];
            if (row === 11) return [row, col, `Sokoban: levels ${dungeonRange(4)}, entrance from below`, 1];
            if (row >= 12 && row <= 15) return [row, col, `${String.fromCharCode('C'.charCodeAt(0) + row - 12)} - ${specialMark(`soko${row - 11}`)} soko${row - 11}: ${specialDepth(`soko${row - 11}`)}`];
            if (row === 16) return [row, 1, `Fort Ludios: level ${specialDepth('knox')}`, 1];
            if (row === 17) return [row, col, `G - ${specialMark('knox')} knox: ${specialDepth('knox')}`];
            if (row === 18) return [row, col, `Vlad's Tower: levels ${dungeonRange(6)}, entrance from below`, 1];
            if (row >= 19 && row <= 21) return [row, col, `${String.fromCharCode('H'.charCodeAt(0) + row - 19)} - ${specialMark(`tower${row - 18}`)} tower${row - 18}: ${specialDepth(`tower${row - 18}`)}`];
            if (row === 22) return [row, col, 'The Elemental Planes: levels -5 to 0, entrance on -1', 1];
            return [row, col, text, attr];
        }
        if (row <= 2) {
            const entry = entries[9 + row];
            return [row, col, `${String.fromCharCode('t'.charCodeAt(0) + row)} - ${currentLevelMarker(entry.level)} ${entry.label}: ${entry.depth}`, attr];
        }
        if (row === 3) return [row, col, `The Gnomish Mines: levels ${dungeonRange(2)}`, attr];
        if (row === 4) return [row, col, `w - ${specialMark('minetn')} minetn: ${specialDepth('minetn')}`, attr];
        if (row === 5) return [row, col, `x - ${specialMark('minend')} minend: ${specialDepth('minend')}`, attr];
        if (row === 6) return [row, col, `The Quest: levels ${dungeonRange(3)}`, attr];
        if (row === 7) return [row, col, `y - ${specialMark('x-strt')} ${roleCode}-strt: ${specialDepth('x-strt')}`, attr];
        if (row === 8) return [row, col, `z - ${specialMark('x-loca')} ${roleCode}-loca: ${specialDepth('x-loca')}`, attr];
        if (row === 9) return [row, col, `A - ${specialMark('x-goal')} ${roleCode}-goal: ${specialDepth('x-goal')}`, attr];
        if (row === 10) return [row, col, `Sokoban: levels ${dungeonRange(4)}, entrance from below`, attr];
        if (row >= 11 && row <= 14) return [row, col, `${String.fromCharCode('B'.charCodeAt(0) + row - 11)} - ${specialMark(`soko${row - 10}`)} soko${row - 10}: ${specialDepth(`soko${row - 10}`)}`, attr];
        if (row === 15) return [row, col, `Fort Ludios: depth ${specialDepth('knox')}`, attr];
        if (row === 16) return [row, col, `knox: ${specialDepth('knox')}`, attr];
        if (row === 17) return [row, col, `Vlad's Tower: levels ${dungeonRange(6)}, entrance from below`, attr];
        if (row >= 18 && row <= 20) return [row, col, `${String.fromCharCode('G'.charCodeAt(0) + row - 18)} - ${specialMark(`tower${row - 17}`)} tower${row - 17}: ${specialDepth(`tower${row - 17}`)}`, attr];
        return [row, col, text, attr];
    });
}

function levelTeleportMenuPage3Lines() {
    const hasKnox = hasPlacedKnoxBranch();
    const endgameLetters = hasKnox ? ['L', 'M', 'N', 'O'] : ['K', 'L', 'M', 'N'];
    const tutorialLetters = hasKnox ? ['Q', 'R'] : ['P', 'Q'];
    const specialDepth = name => wizWhereDepth(specialLevel(name));
    return LEVEL_TELEPORT_MENU_PAGE3_LINES.map(([row, col, text, attr]) => {
        if (row >= 0 && row <= 3) {
            const names = ['water', 'fire', 'air', 'earth'];
            const name = names[row];
            return [row, col, `${endgameLetters[row]} -   ${name}: ${specialDepth(name)}`, attr];
        }
        if (row === 6) return [row, col, `${tutorialLetters[0]} -   tut-1: ${specialDepth('tut-1')}`, attr];
        if (row === 7) return [row, col, `${tutorialLetters[1]} -   tut-2: ${specialDepth('tut-2')}`, attr];
        return [row, col, text, attr];
    });
}

function wizWhereDepth(level) {
    if (!level) return 0;
    if (level.dnum === 7) return level.dlevel - 6;
    if (level.dnum === 8) return level.dlevel;
    return (game.dungeons?.[level.dnum]?.depth_start ?? 1) + level.dlevel - 1;
}

function wizWhereRange(dnum) {
    const dungeon = game.dungeons?.[dnum];
    if (dnum === 7) return '-5 to 0';
    if (dnum === 8) return '1 to 2';
    return `${dungeon?.depth_start ?? 1} to ${(dungeon?.depth_start ?? 1) + (dungeon?.num_dunlevs ?? 1) - 1}`;
}

function wizWhereBranchLabel(branch) {
    const dungeonName = game.dungeons?.[branch.end2.dnum]?.name || 'Dungeon';
    if (branch.type === 'portal') return `Portal to ${dungeonName}`;
    if (branch.type === 'stair') return `Stair to ${dungeonName}`;
    if (branch.end1_up) return `One way stair to ${dungeonName}`;
    return `Connection to ${dungeonName}`;
}

function wizWhereLines() {
    const lines = [];
    const roleCode = ROLE_ABBREVIATIONS[game.urole?.name?.m || game._startup_role || 'Wizard'] || 'Wiz';
    const addHeader = (dnum, text = '') => {
        const dungeon = game.dungeons?.[dnum];
        if (!dungeon) return;
        lines.push(text || `${dungeon.name}: levels ${wizWhereRange(dnum)}`);
    };
    const addEntries = dnum => {
        const entries = [];
        for (const branch of game.branches || [])
            if (branch.end1?.dnum === dnum)
            entries.push({ depth: wizWhereDepth(branch.end1), text: `${wizWhereBranchLabel(branch)}: ${wizWhereDepth(branch.end1)}` });
        for (const level of game.specialLevels || []) {
            if (level.dnum !== dnum) continue;
            const label = level.name === 'x-strt' ? `${roleCode}-strt`
                : level.name === 'x-loca' ? `${roleCode}-loca`
                : level.name === 'x-goal' ? `${roleCode}-goal`
                : level.name;
            const suffix = level.name === 'castle' ? ` (tune ${game.castleTune || ''})` : '';
            entries.push({ depth: wizWhereDepth(level), text: `${label}: ${wizWhereDepth(level)}${suffix}` });
        }
        entries
            .sort((a, b) => a.depth - b.depth)
            .forEach(entry => lines.push(`  ${entry.text}`));
    };

    addHeader(0);
    addEntries(0);
    addHeader(1);
    addEntries(1);
    addHeader(2);
    addEntries(2);
    addHeader(3);
    addEntries(3);
    addHeader(4, `Sokoban: levels ${wizWhereRange(4)}, entrance from below`);
    addEntries(4);
    addHeader(5, `Fort Ludios: level ${wizWhereDepth({ dnum: 5, dlevel: 1 })}`);
    addEntries(5);
    addHeader(6, `Vlad's Tower: levels ${wizWhereRange(6)}, entrance from below`);
    addEntries(6);
    addHeader(7, `The Elemental Planes: levels ${wizWhereRange(7)}, entrance on -1`);
    addEntries(7);
    addHeader(8);
    addEntries(8);
    return lines;
}

function showWizWherePage(page) {
    const lines = game._wiz_where_lines || wizWhereLines();
    game._wiz_where_lines = lines;
    game._wiz_where_page = page;
    const start = page * 23;
    const body = lines.slice(start, start + 23);
    const rows = body.map((line, row) => [row, 0, line]);
    rows.push([body.length, 1, '--More--']);
    setOverlay(rows, 24, true);
    game._command_mode = 'wizWhereMore';
}

function spellMenuLines(prompt = 'Choose which spell to cast', includeSort = false) {
    const roleName = game.urole?.name?.m || game._startup_role || '';
    const spellbooks = game._known_spells || [];
    const wornArmor = (game.inventory || [])
        .filter(inv => inv.worn && inv.cls === 'armor')
        .map(inv => String(inv.actualKind || inv.kind || inventoryItemName(inv)).toLowerCase())
        .map(name => name.replace(/^(?:an?|the) /, '')
            .replace(/^(?:uncursed|blessed|cursed) /, '')
            .replace(/^[+-]\d+ /, '')
            .replace(/^pair of /, ''))
        .map(name => ARMOR_APPEARANCE_KINDS[name] || name);
    const hasRobe = wornArmor.includes('robe');
    const hasShield = wornArmor.some(name => name.includes('shield'));
    const heavyShield = wornArmor.some(name => name.includes('shield')
        && (OBJECT_WEIGHTS[name] ?? 0) > OBJECT_WEIGHTS['small shield']);
    const hasMetalSuit = wornArmor.some(name => METALLIC_BODY_ARMOR.has(name));
    const hasMetalHelm = wornArmor.some(name => METALLIC_HELMS.has(name));
    const hasMetalGloves = wornArmor.some(name => METALLIC_GLOVES.has(name));
    const hasMetalBoots = wornArmor.some(name => METALLIC_BOOTS.has(name));
    const hasQuarterstaff = (game.inventory || []).some(inv => inv.wielded && String(inv.kind || '').toLowerCase() === 'quarterstaff');
    const showTurns = !!game.flags?.debug;
    const left = showTurns ? 13 : 20;
    const rows = [
        [0, left, prompt, 1],
        [2, left, '    Name', 1],
        [2, left + 26, 'Level Category', 1],
        [2, left + 45, `Fail Retention${showTurns ? '  turns' : ''}`, 1],
    ];
    game._spell_menu_spells = [];
    let row = 3;
    for (const item of spellbooks) {
        const name = item.name || item.spellName || item.spell?.name || String(item.kind || '').replace(/^spellbook of /, '') || 'spell';
        const level = item.level || item.spell?.level || (name === 'extra healing' || name === 'stone to flesh' || name === 'remove curse' ? 3 : name === 'slow monster' || name === 'create monster' ? 2 : 1);
        const skillName = item.skill || item.spell?.skill || SPELL_CATEGORIES[name] || 'cleric';
        const category = skillName === 'cleric' ? 'clerical' : skillName;
        const skillLevel = ROLE_INITIAL_SPELL_SKILLS[roleName]?.[skillName] || P_UNSKILLED;
        let fail = level > 2 ? '76%' : '0%';
        let successChance = level > 2 ? 24 : 100;
        const stats = ROLE_SPELL_STATS[roleName];
        if (stats) {
            let splcaster = stats.base;
            const paladinBonus = roleName === 'Knight' && skillName === 'cleric';
            if (hasMetalSuit && !paladinBonus) splcaster += hasRobe ? Math.trunc(stats.armor / 2) : stats.armor;
            else if (hasRobe) splcaster -= stats.armor;
            if (hasShield) splcaster += stats.shield;
            if (hasQuarterstaff) splcaster -= 3;
            if (!paladinBonus) {
                if (hasMetalHelm) splcaster += 4;
                if (hasMetalGloves) splcaster += 6;
                if (hasMetalBoots) splcaster += 2;
            }
            if (name === stats.special) splcaster += stats.specialBonus;
            if (HEALING_SPELLS.has(name)) splcaster += stats.healing;
            if (splcaster > 20) splcaster = 20;

            const statused = game.u?.acurr?.a?.[stats.statIndex] || 10;
            const spellSkill = Math.max(skillLevel, 1) - 1;
            const difficulty = (level - 1) * 4 - ((spellSkill * 6) + Math.trunc((game.u?.ulevel || 1) / 3) + 1);
            let chance = Math.trunc(11 * statused / 2);
            if (difficulty > 0) chance -= Math.trunc(Math.sqrt(900 * difficulty + 2000));
            else chance += Math.min(20, Math.trunc(15 * -difficulty / level));
            chance = Math.max(0, Math.min(120, chance));
            if (heavyShield) chance = Math.trunc(chance / (name === stats.special ? 2 : 4));
            chance = Math.trunc(chance * (20 - splcaster) / 15) - splcaster;
            chance = Math.max(0, Math.min(100, chance));
            successChance = chance;
            fail = `${100 - chance}%`;
        }
        const accuracy = skillLevel >= 4 ? 2 : skillLevel === 3 ? 5 : skillLevel === 2 ? 10 : 25;
        const learnedTurn = item.learnedTurn ?? 1;
        const turnsLeft = Math.max(0, 20000 - Math.max(0, (game.moves || 1) - learnedTurn));
        const percent = turnsLeft >= 20000 ? 100 : Math.trunc((turnsLeft - 1) / 200) + 1;
        const high = turnsLeft >= 20000 ? 100 : accuracy * (Math.trunc((percent - 1) / accuracy) + 1);
        const retention = turnsLeft >= 20000 ? '100%' : `${high - accuracy + 1}%-${high}%`;
        const letter = String.fromCharCode('a'.charCodeAt(0) + game._spell_menu_spells.length);
        game._spell_menu_spells.push({ letter, name, level, category: skillName, successChance });
        const failGap = fail.length > 3 ? '' : ' ';
        rows.push([row++, left, `${letter} - ${name.padEnd(20, ' ')}  ${String(level).padStart(2, ' ')}   ${category.padEnd(13, ' ')}${failGap}${fail.padStart(3, ' ')} ${retention.padStart(9, ' ')}${showTurns ? ` ${String(turnsLeft).padStart(6, ' ')}` : ''}`]);
    }
    if (includeSort && spellbooks.length > 1) rows.push([row++, left, '+ - [sort spells]']);
    rows.push([row, left, '(end)']);
    return rows;
}

function currentSkillLines() {
    const rows = [[0, 0, ' '], [0, 1, 'Current skills:', 1], [2, 0, ' '], [2, 1, 'Fighting Skills', 1]];
    const role = game.urole?.name?.m || game._startup_role || '';
    if (role === 'Samurai') {
        return [
            ...rows,
            [3, 0, `   ${'martial arts'.padEnd(18, ' ')}[Basic]`],
            [4, 0, `   ${'two weapon combat'.padEnd(18, ' ')}[Unskilled]`],
            [5, 0, `   ${'riding'.padEnd(18, ' ')}[Unskilled]`],
            [6, 0, ' '], [6, 1, 'Weapon Skills', 1],
            [7, 0, `   ${'dagger'.padEnd(18, ' ')}[Unskilled]`],
            [8, 0, `   ${'knife'.padEnd(18, ' ')}[Unskilled]`],
            [9, 0, `   ${'short sword'.padEnd(18, ' ')}[Basic]`],
            [10, 0, `   ${'broadsword'.padEnd(18, ' ')}[Unskilled]`],
            [11, 0, `   ${'long sword'.padEnd(18, ' ')}[Basic]`],
            [12, 0, `   ${'two-handed sword'.padEnd(18, ' ')}[Unskilled]`],
            [13, 0, `   ${'saber'.padEnd(18, ' ')}[Unskilled]`],
            [14, 0, `   ${'flail'.padEnd(18, ' ')}[Unskilled]`],
            [15, 0, `   ${'quarterstaff'.padEnd(18, ' ')}[Unskilled]`],
            [16, 0, `   ${'polearms'.padEnd(18, ' ')}[Unskilled]`],
            [17, 0, `   ${'spear'.padEnd(18, ' ')}[Unskilled]`],
            [18, 0, `   ${'lance'.padEnd(18, ' ')}[Unskilled]`],
            [19, 0, `   ${'bow'.padEnd(18, ' ')}[Basic]`],
            [20, 0, `   ${'shuriken'.padEnd(18, ' ')}[Unskilled]`],
            [21, 0, ' '], [21, 1, 'Spellcasting Skills', 1],
            [22, 0, `   ${'attack spells'.padEnd(18, ' ')}[Unskilled]`],
            [23, 0, ' (1 of 2)'],
        ];
    }
    if (role === 'Priest') {
        return [
            ...rows,
            [3, 0, `   ${'bare handed combat'.padEnd(19, ' ')}[Unskilled]`],
            [4, 1, 'Weapon Skills', 1],
            [5, 0, `   ${'club'.padEnd(19, ' ')}[Unskilled]`],
            [6, 0, `   ${'mace'.padEnd(19, ' ')}[Basic]`],
            [7, 0, `   ${'morning star'.padEnd(19, ' ')}[Unskilled]`],
            [8, 0, `   ${'flail'.padEnd(19, ' ')}[Unskilled]`],
            [9, 0, `   ${'hammer'.padEnd(19, ' ')}[Unskilled]`],
            [10, 0, `   ${'quarterstaff'.padEnd(19, ' ')}[Unskilled]`],
            [11, 0, `   ${'polearms'.padEnd(19, ' ')}[Unskilled]`],
            [12, 0, `   ${'spear'.padEnd(19, ' ')}[Unskilled]`],
            [13, 0, `   ${'trident'.padEnd(19, ' ')}[Unskilled]`],
            [14, 0, `   ${'lance'.padEnd(19, ' ')}[Unskilled]`],
            [15, 0, `   ${'bow'.padEnd(19, ' ')}[Unskilled]`],
            [16, 0, `   ${'sling'.padEnd(19, ' ')}[Unskilled]`],
            [17, 0, `   ${'crossbow'.padEnd(19, ' ')}[Unskilled]`],
            [18, 0, `   ${'dart'.padEnd(19, ' ')}[Unskilled]`],
            [19, 0, `   ${'shuriken'.padEnd(19, ' ')}[Unskilled]`],
            [20, 0, `   ${'boomerang'.padEnd(19, ' ')}[Unskilled]`],
            [21, 0, `   ${'unicorn horn'.padEnd(19, ' ')}[Unskilled]`],
            [22, 1, 'Spellcasting Skills', 1],
            [23, 0, ' (1 of 2)'],
        ];
    }
    rows.push([3, 0, `   bare handed combat [${role === 'Monk' ? 'Basic' : 'Unskilled'}]`]);
    rows.push([4, 0, `   two weapon combat  [${game._twoweapon ? 'Basic' : 'Unskilled'}]`]);
    rows.push([5, 0, `   riding             [${game.u?.usteed ? 'Basic' : 'Unskilled'}]`]);
    rows.push([6, 0, ' '], [6, 1, 'Weapon Skills', 1]);
    const weaponSkills = new Set();
    for (const item of game.inventory || []) {
        const name = inventoryItemName(item).toLowerCase();
        if (item.cls !== 'weapon' && !/staff|sword|dagger|bow|sling|dart|spear|mace|axe|lance|shuriken|club/.test(name)) continue;
        const skill = name.includes('staff') ? 'quarterstaff'
            : name.includes('short sword') || name.includes('wakizashi') ? 'short sword'
            : name.includes('long sword') || name.includes('katana') ? 'long sword'
            : name.includes('bow') || name.includes('yumi') ? 'bow'
            : name.includes('sling') ? 'sling'
            : name.includes('dart') ? 'dart'
            : name.includes('spear') ? 'spear'
            : name.includes('mace') ? 'mace'
            : name.includes('axe') ? 'axe'
            : name.includes('lance') ? 'lance'
            : name.includes('shuriken') ? 'shuriken'
            : name.includes('club') ? 'club'
            : name.includes('dagger') ? 'dagger'
            : 'weapon';
        weaponSkills.add(skill);
    }
    let row = 7;
    for (const skill of [...weaponSkills].sort()) rows.push([row++, 0, `   ${skill.padEnd(19, ' ')}[Basic]`]);
    if (row === 7) rows.push([row++, 0, '   weapon             [Unskilled]']);
    rows.push([row++, 0, ' '], [row++, 1, 'Spellcasting Skills', 1]);
    const spellbooks = game._known_spells || [];
    const spellSkills = new Set(spellbooks.map(item => item.skill || item.spell?.skill || item.section || 'cleric'));
    if (!spellSkills.size) spellSkills.add('spellcasting');
    for (const skill of [...spellSkills].sort()) {
        const skillName = String(skill);
        const label = skillName.endsWith('spells') ? skillName : `${skillName} spells`;
        rows.push([row++, 0, `   ${label.padEnd(19, ' ')}[Unskilled]`]);
    }
    rows.push([23, 0, '(end)']);
    return rows.slice(0, 24);
}

function attributesDungeonLocation(uz) {
    if (In_endgame(uz)) {
        if (sameLevel(uz, specialLevel('astral'))) return 'the endgame, on the Astral Plane';
        const elemental = [
            ['water', 'Water'],
            ['fire', 'Fire'],
            ['air', 'Air'],
            ['earth', 'Earth'],
        ].find(([name]) => sameLevel(uz, specialLevel(name)));
        if (elemental) return `the endgame, on the Elemental Plane of ${elemental[1]}`;
        return 'the endgame';
    }
    const currentDungeon = game.dungeons?.[uz.dnum];
    const dungeonName = currentDungeon?.name === 'The Dungeons of Doom'
        ? 'the Dungeons of Doom'
        : currentDungeon?.name === 'The Quest'
            ? 'the Quest'
            : currentDungeon?.name || 'the Dungeons of Doom';
    const dungeonLevel = currentDungeon?.name === 'The Quest' ? uz.dlevel : depth_of_level(uz);
    return `${dungeonName}, on level ${dungeonLevel}`;
}

function genericAttributesPage1() {
    const stats = game.u?.acurr?.a || [];
    const peaks = game.u?.amax?.a || stats;
    const attrLimits = ATTR_LIMITS_BY_RACE[game._startup_race] || {};
    const strength = attrLimits[0] ? `${strengthString(stats[0])} (current; limit:${attrLimits[0]})`
        : stats[0] !== peaks[0] ? `${strengthString(stats[0])} (current; ${stats[0] > peaks[0] ? 'base' : 'peak'}:${strengthString(peaks[0])})`
            : strengthString(stats[0]);
    const dexterity = attrLimits[3] ? `${stats[3]} (current; limit:${attrLimits[3]})`
        : stats[3] !== peaks[3] ? `${stats[3]} (current; base:${peaks[3]})` : stats[3];
    const rawName = game.plname || 'Hero';
    const name = rawName[0].toUpperCase() + rawName.slice(1);
    const role = game.urole?.name?.[game.flags?.female ? 'f' : 'm'] || game.urole?.name?.m || game._startup_role || 'Adventurer';
    const rank = game.urole?.rank?.[game.flags?.female ? 'f' : 'm'] || game.urole?.rank?.m || role;
    const race = game.urace?.adj || game._startup_race || 'human';
    const roleBase = game.urole?.name?.m || game._startup_role || role;
    const genderPrefix = (!game.urole?.name?.f && roleBase !== 'Valkyrie')
        ? `${game.flags?.female ? 'female' : 'male'} `
        : '';
    const alignType = game.u?.ualign?.type ?? 0;
    const align = alignType > 0 ? 'lawful' : alignType < 0 ? 'chaotic' : 'neutral';
    const alignIndex = alignType > 0 ? 0 : alignType < 0 ? 2 : 1;
    const alignNames = ['lawful', 'neutral', 'chaotic'];
    const pantheonRole = roleBase === 'Priest' ? game._pantheon_role || 'Barbarian' : roleBase;
    const gods = GODS_BY_ROLE[pantheonRole] || ['Marduk', 'Marduk', 'Marduk'];
    const opposedGods = gods
        .map((god, idx) => ({ god, idx }))
        .filter(({ idx }) => idx !== alignIndex)
        .map(({ god, idx }) => `${god} (${alignNames[idx]})`)
        .join(' and ');
    const uz = game.u?.uz || { dnum: 0, dlevel: 1 };
    const dungeon = attributesDungeonLocation(uz);
    const hp = game.u?.uhp ?? game.u?.uhpmax ?? 1;
    const hpmax = game.u?.uhpmax ?? hp;
    const energy = game.u?.uen ?? game.u?.uenmax ?? 0;
    const energymax = game.u?.uenmax ?? energy;
    const energyLine = energy === energymax
        ? energymax === 2 ? '  You have both energy points (spell power).' : `  You have all ${energymax} energy points (spell power).`
        : `  You have ${energy} out of ${energymax} energy points (spell power).`;
    const gold = game._goldCount || 0;
    const displayTurns = game.moves || 1;
    const turnLine = displayTurns === 1
        ? '  You have just started your adventure.'
        : `  You entered the dungeon ${displayTurns} turns ago.`;
    const rows = [
        [0, 0, ` ${name} the ${role}'s attributes:`],
        [2, 0, ' Background:'],
        [3, 0, role === rank
            ? `  You are a ${role}, level ${game.u?.ulevel || 1} ${genderPrefix}${race}.`
            : `  You are ${/^[aeiou]/i.test(rank) ? 'an' : 'a'} ${rank}, a level ${game.u?.ulevel || 1} ${genderPrefix}${race} ${role}.`],
        [4, 0, `  You are ${align}, on a mission for ${gods[alignIndex]}`],
        [5, 0, `  who is opposed by ${opposedGods}.`],
        [6, 0, `  You are ${game.u?.uhandedness || 'right'}-handed.`],
        [7, 0, `  You are in ${dungeon}.`],
        [8, 0, turnLine],
    ];
    let row = 9;
    if (game.flags?.moonphase === 4) rows.push([row++, 0, '  There is a full moon in effect.']);
    if (game.flags?.moonphase === 0) rows.push([row++, 0, '  There is a new moon in effect.']);
    if (game.flags?.friday13) rows.push([row++, 0, '  Bad things can happen on Friday the 13th.']);
    const exp = game.u?.uexp || 0;
    const level = game.u?.ulevel || 1;
    const nextLevelExp = level < 10 ? 10 * (2 ** level)
        : level < 20 ? 10000 * (2 ** (level - 10))
            : 10000000 * (level - 19);
    const expLine = game.flags?.debug
        ? `  You have ${exp} experience point${exp === 1 ? '' : 's'}, ${nextLevelExp - exp}${exp > 0 ? ' more' : ''} needed ${level < 18 ? 'to attain' : 'for'} level ${level + 1}.`
        : `  You have ${exp} experience point${exp === 1 ? '' : 's'}.`;
    rows.push(
        [row++, 0, expLine],
        [row++, 0, ''],
        [row++, 0, ' Basics:'],
        [row++, 0, hp === hpmax ? `  You have all ${hpmax} hit points.` : `  You have ${hp} out of ${hpmax} hit points.`],
        [row++, 0, energyLine],
        [row++, 0, `  Your armor class is ${game.u?.uac ?? 10}.`],
        [row++, 0, gold ? `  Your wallet contains ${gold} zorkmid${gold === 1 ? '' : 's'}.` : '  Your wallet is empty.'],
        [row++, 0, game.flags?.pickup ? `  Autopickup is on for '${game.flags.pickup_types || 'all'}' plus thrown.` : '  Autopickup is off.'],
        [row++, 0, ''],
        [row++, 0, ' Characteristics:'],
        [row++, 0, `  Your strength is ${strength}.`],
        [row++, 0, `  Your dexterity is ${dexterity}.`],
        [row++, 0, `  Your constitution is ${stats[4]}.`],
        [row++, 0, `  Your intelligence is ${stats[1]}${attrLimits[1] ? ` (current; limit:${attrLimits[1]})` : ''}.`],
    );
    game._attributes_page2_prefix = rows.filter(([line]) => line >= 23).map(([, col, text]) => [col, text]);
    const page2Rows = buildGenericAttributesPage2Rows();
    const totalPages = page2Rows.some(([line]) => line >= 23) ? 3 : 2;
    return [...rows.filter(([line]) => line < 23), [23, 0, ` (1 of ${totalPages})`]];
}

function enlightenmentPiousness(record) {
    if (record >= 20) return 'piously aligned';
    if (record > 13) return 'devoutly aligned';
    if (record > 8) return 'fervently aligned';
    if (record > 3) return 'stridently aligned';
    if (record === 3) return 'aligned';
    if (record > 0) return 'haltingly aligned';
    if (record === 0) return 'nominally aligned';
    if (record >= -3) return 'strayed';
    if (record >= -8) return 'sinned';
    return 'transgressed';
}

function enlightenmentWeaponName(weapon, roleName) {
    let name = weapon?.actualKind || weapon?.kind || weapon?.name
        || (typeof weapon?.otyp === 'string' ? weapon.otyp : '');
    name = String(name || '').toLowerCase().replace(/\s+named\s+.*$/, '');
    if (roleName === 'Samurai') {
        if (name === 'katana') name = 'long sword';
        else if (name === 'wakizashi') name = 'short sword';
    }
    if (name === 'battle-axe') name = 'axe';
    if (name === 'dwarvish mattock') name = 'mattock';
    if (/war hammer|mjollnir/.test(name)) name = 'hammer';
    if (name === 'silver saber' || name === 'scimitar') name = 'saber';
    if (name === 'scalpel') name = 'knife';
    return name ? name.replace(/^(?:elven|orcish|dwarvish) /, '') : '';
}

function enlightenmentWeaponSkillLine(weaponName, roleName) {
    if (weaponName === 'hammer' && roleName === 'Wizard')
        return `  You have no skill with ${weaponName}.`;
    if (weaponName === 'saber' && roleName === 'Archeologist')
        return `  You are unskilled in ${weaponName}.`;
    return `  You have basic skill with ${weaponName}.`;
}

function enlightenmentSourceForSearching(roleName) {
    return roleName === 'Archeologist' || roleName === 'Ranger'
        ? 'innately' : 'because of your experience';
}

function enlightenmentSourceForStealth(roleName) {
    return roleName === 'Rogue' ? 'innately' : 'because of your experience';
}

function enlightenmentSourceForSpeed(roleName) {
    return roleName === 'Monk' || roleName === 'Samurai'
        ? 'innately' : 'because of your experience';
}

function wornReflectionSource() {
    return (game.inventory || []).find(item => {
        if (!isActiveInventoryExtrinsicItem(item)) return false;
        const kind = armorKind(item);
        return isSilverDragonArmorKind(kind) || kind === 'shield of reflection'
            || item.amuletIndex === 7 || kind === 'amulet of reflection';
    });
}

function simpleEquipmentName(item) {
    if (!item) return '';
    if (item.cls === 'armor') return armorKind(item) || pickupObjectName(item);
    return item.actualKind || item.kind || pickupObjectName(item);
}

function buildGenericAttributesPage2Rows() {
    const stats = game.u?.acurr?.a || [];
    const peaks = game.u?.amax?.a || stats;
    const attrLimits = ATTR_LIMITS_BY_RACE[game._startup_race] || {};
    const wisdom = attrLimits[2] ? `${stats[2]} (current; limit:${attrLimits[2]})`
        : stats[2] !== peaks[2] ? `${stats[2]} (current; peak:${peaks[2]})` : stats[2];
    const charisma = attrLimits[5] ? `${stats[5]} (current; limit:${attrLimits[5]})` : stats[5];
    const roleName = game.urole?.name?.m || game._startup_role || '';
    const wieldedItem = (game.inventory || []).find(item =>
        item.wielded || item.line?.includes('(wielded)') || item.line?.includes('weapon in'))
        || (game.inventory || []).find(item => item.line?.includes('weapon;'));
    const weapon = wieldedItem && (wieldedItem.cls === 'weapon' || wieldedItem.otyp === WEAPON_CLASS || wieldedItem.glyph === ')')
        ? wieldedItem : null;
    const nonWeaponWielded = wieldedItem && !weapon ? wieldedItem : null;
    const weaponName = enlightenmentWeaponName(weapon, roleName);
    const weaponArticle = weaponName && /^[aeiou]/i.test(weaponName) ? 'an' : 'a';
    let carriedWeight = Math.trunc(((game._goldCount || 0) + 50) / 100);
    for (const item of game.inventory || []) {
        if (isGoldObject(item)) continue;
        let kind = String(item.actualKind || item.kind || item.spellName || item.spell?.name || '').toLowerCase();
        kind = ARMOR_APPEARANCE_KINDS[kind] || kind;
        const cls = item.cls || (item.otyp === RING_CLASS ? 'ring'
            : item.otyp === SCROLL_CLASS ? 'scroll'
                : item.otyp === POTION_CLASS ? 'potion'
                    : item.otyp === WAND_CLASS ? 'wand'
                        : item.otyp === GEM_CLASS ? 'gem' : '');
        const quan = item.quan || 1;
        const weightKind = /war hammer|mjollnir/.test(kind) ? 'war hammer' : kind;
        const unitWeight = OBJECT_WEIGHTS[weightKind] ?? CLASS_WEIGHTS[cls] ?? item.owt ?? 0;
        carriedWeight += unitWeight * quan;
    }
    const normalCapacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
    const capacity = Is_airlevel(game.u?.uz) || game.u?.levitating ? 1000 : normalCapacity;
    const burden = game.u?._debug_burden_override ?? (carriedWeight - capacity);
    const encumbrance = burden <= 0 ? 0 : Math.min(Math.trunc(burden * 2 / capacity) + 1, OVERLOADED);
    const statusSuffix = game.u?._statusSuffix || '';
    const hunger = statusSuffix.includes('Satiated') ? 'satiated'
        : statusSuffix.includes('Hungry') ? 'hungry'
            : statusSuffix.includes('Weak') ? 'weak from severe hunger'
                : statusSuffix.includes('Faint') ? 'fainting due to starvation'
                    : 'not hungry';
    const debugHunger = game.u?.uhunger ?? 900;
    const hungerLine = hunger === 'not hungry'
        ? game.flags?.debug ? `  You aren't hungry <${debugHunger}>.` : "  You aren't hungry."
        : game.flags?.debug ? `  You are ${hunger} <${debugHunger}>.` : `  You are ${hunger}.`;
    const encumbranceNames = ['', 'burdened', 'stressed', 'strained', 'overtaxed', 'overloaded'];
    const encumbranceAdverbs = ['', 'slightly', 'moderately', 'very', 'extremely', 'not possible'];
    const rows = [];
    let row = 0;
    for (const [col, text] of game._attributes_page2_prefix || []) rows.push([row++, col, text]);
    rows.push(
        [row++, 0, `  Your wisdom is ${wisdom}.`],
        [row++, 0, `  Your charisma is ${charisma}.`],
        [row++, 0, ''],
        [row++, 0, ' Status:'],
    );
    if (statusSuffix.includes('Deaf')) rows.push([row++, 0, '  You are deaf.']);
    if (statusSuffix.includes('Hallu')) rows.push([row++, 0, '  You are hallucinating.']);
    if (game.u?.uball || isBuriedBallTrapActive()) rows.push([row++, 0, '  You are chained to a heavy iron ball.']);
    if (game.u?._woundedLegTurns) {
        const side = game.u._woundedLegSide === 'left' ? 'left '
            : game.u._woundedLegSide === 'right' ? 'right ' : '';
        rows.push([row++, 0, `  You have a wounded ${side}leg.`]);
    }
    rows.push([row++, 0, hungerLine]);
    if (encumbrance > 0) {
        const slowed = encumbrance < OVERLOADED ? ' slowed' : '';
        const debugWeight = game.flags?.debug ? ` <${burden}>` : '';
        rows.push([row++, 0, `  You are ${encumbranceNames[encumbrance]}${debugWeight}; movement is ${encumbranceAdverbs[encumbrance]}${slowed}.`]);
    } else {
        rows.push([row++, 0, game.flags?.debug ? `  You are unencumbered <${burden}>.` : '  You are unencumbered.']);
    }
    if (game._twoweapon) {
        const primary = (game.inventory || []).find(item => item.wielded || item.line?.includes('right hand') || item.cls === 'weapon');
        const secondary = (game.inventory || []).find(item => item !== primary && (item.alternate || item.line?.includes('left hand')));
        const primarySkill = enlightenmentWeaponName(primary, roleName) || 'weapon';
        const secondarySkill = enlightenmentWeaponName(secondary, roleName) || 'weapon';
        rows.push(
            [row++, 0, '  You are wielding two weapons at once.'],
            [row++, 0, `  Your skill in ${primarySkill} is limited by being unskilled with two weapons.`],
            [row++, 0, `  Your skill in ${secondarySkill} is also limited by being unskilled with two weapons`],
        );
    } else {
        const emptyHanded = roleName === 'Monk' ? '  You are empty handed.' : '  You are bare handed.';
        const bareSkill = roleName === 'Monk' ? '  You have basic skill with martial arts.' : '  You are unskilled in bare handed combat.';
        if (nonWeaponWielded) {
            const wieldedName = nonWeaponWielded.cls === 'spellbook'
                ? 'spellbook'
                : simpleEquipmentName(nonWeaponWielded) || pickupObjectName(nonWeaponWielded);
            const wieldedArticle = /^[aeiou]/i.test(wieldedName) ? 'an' : 'a';
            rows.push([row++, 0, `  You are wielding ${wieldedArticle} ${wieldedName}.`]);
        } else {
            rows.push(
                [row++, 0, weaponName ? `  You are wielding ${weaponArticle} ${weaponName}.` : emptyHanded],
                [row++, 0, weaponName ? enlightenmentWeaponSkillLine(weaponName, roleName) : bareSkill],
            );
        }
    }
    const wornArmor = (game.inventory || []).some(item => item.cls === 'armor' && item.worn);
    if (!wornArmor) rows.push([row++, 0, "  You aren't wearing any armor."]);
    const magicNegation = (game.inventory || []).reduce((best, item) => {
        if (!(item.cls === 'armor' && item.worn)) return best;
        const name = String(item.actualKind || item.kind || inventoryItemName(item)).toLowerCase();
        return Math.max(best, ARMOR_MAGIC_NEGATION[name] || 0);
    }, 0);
    row++;
    if (game.flags?.debug) {
        const alignRecord = game.u?.ualign?.record ?? 0;
        const piousness = enlightenmentPiousness(alignRecord);
        const alignmentLine = alignRecord < 0 ? `  You have ${piousness}.` : `  You are ${piousness}.`;
        rows.push(
            [row++, 0, ' Attributes:'],
            [row++, 0, alignmentLine],
            [row++, 0, `  Your alignment is ${alignRecord}.`],
        );
        const magicResistanceSource = activeAntimagicSource();
        if (polyselfFormHasAntimagic())
            rows.push([row++, 0, '  You are magic-protected from your creature form.']);
        else if (magicResistanceSource)
            rows.push([row++, 0, `  You are magic-protected because of your ${pickupObjectName(magicResistanceSource)}.`]);
        if ((game.inventory || []).some(item => item.wielded && item.artifact === 'Grayswandir'))
            rows.push([row++, 0, '  You resist hallucinations because of Grayswandir.']);
        if (game.u?.fireResistance)
            rows.push([row++, 0, '  You are fire resistant because of your experience.']);
        const blueDragonArmor = (game.inventory || []).find(item =>
            isWornInventoryItem(item) && isBlueDragonArmorKind(String(item.kind || item.actualKind || '').toLowerCase()));
        if (blueDragonArmor) {
            rows.push([row++, 0, `  You are shock resistant because of your ${simpleEquipmentName(blueDragonArmor)}.`]);
            rows.push([row++, 0, `  Your items are protected from electric shocks by your ${blueDragonArmor.dragonArmorKind === 'mail' ? 'dragon mail' : 'dragon scales'}.`]);
        }
        if (game.u?.poisonResistance) {
            const poisonSource = roleName === 'Barbarian' || roleName === 'Healer' || game._startup_race === 'orc'
                ? 'innately' : 'because of your experience';
            rows.push([row++, 0, `  You are poison resistant ${poisonSource}.`]);
        }
        const telepathyAmulet = (game.inventory || []).find(item =>
            isWornInventoryItem(item) && item.cls === 'amulet'
            && (item.amuletIndex === 0 || /amulet of (?:esp|telepathy)/i.test(String(item.kind || item.actualKind || ''))));
        if (telepathyAmulet) {
            const appearance = telepathyAmulet.appearance || game._object_descriptions?.amulets?.[telepathyAmulet.amuletIndex];
            rows.push([row++, 0, `  You are telepathic because of your ${appearance ? `${appearance} amulet` : simpleEquipmentName(telepathyAmulet)}.`]);
        }
        if (game.u?.warning) rows.push([row++, 0, '  You are warned because of your experience.']);
        const displacementSource = (game.inventory || []).find(item =>
            isWornInventoryItem(item)
            && String(item.kind || item.actualKind || inventoryItemName(item)).toLowerCase() === 'cloak of displacement');
        if (game._has_displacement || displacementSource)
            rows.push([row++, 0, `  You are displaced because of your ${simpleEquipmentName(displacementSource) || 'cloak of displacement'}.`]);
        if (game.u?.searching)
            rows.push([row++, 0, `  You have automatic searching ${enlightenmentSourceForSearching(roleName)}.`]);
        if (game.u?.stealth)
            rows.push([row++, 0, `  You are stealthy ${enlightenmentSourceForStealth(roleName)}.`]);
        if (game.u?.teleportControl) rows.push([row++, 0, '  You have teleport control because of your experience.']);
        const teleportControlRing = (game.inventory || []).find(item =>
            item.worn && (item.actualKind === 'ring of teleport control' || item.ringRoll === 23));
        if (teleportControlRing) rows.push([row++, 0, `  You have teleport control because of your ${pickupObjectName(teleportControlRing)}.`]);
        if (heroHasSlowDigestion()) rows.push([row++, 0, '  You have slower digestion.']);
        if (magicNegation > 0) rows.push([row++, 0, '  You are warded.']);
        if (roleName === 'Knight' || game.u?.jumping) rows.push([row++, 0, '  You can jump intrinsically.']);
        if (game.u?.fast || game.u?.veryfast) {
            const speedBoots = (game.inventory || []).find(item =>
                isWornInventoryItem(item) && String(item.kind || item.actualKind || '').toLowerCase() === 'speed boots');
            const speedSource = speedBoots ? 'because of your speed boots'
                : game.u?._blueDragonFast ? 'because of worn equipment' : enlightenmentSourceForSpeed(roleName);
            rows.push([row++, 0, `  You are ${game.u?.veryfast ? 'very fast' : 'fast'} ${speedSource}.`]);
        }
        const reflectionSource = wornReflectionSource();
        if (game.u?.reflecting || reflectionSource)
            rows.push([row++, 0, `  You have reflection because of your ${simpleEquipmentName(reflectionSource) || 'worn equipment'}.`]);
        if ((game.inventory || []).some(item => item.worn && item.amuletIndex === 1))
            rows.push([row++, 0, '  Your life will be saved.']);
        const luck = (game.u?.uluck || 0) + (game.u?.moreluck || 0);
        rows.push([row++, 0, luck < 0 ? `  You are unlucky (${luck}).` : luck > 0 ? `  You are lucky (${luck}).` : '  Your luck is zero.']);
        rows.push([row++, 0, `  You can't safely pray (${game.u?.ublesscnt ?? 0}).`]);
        if (game._survived_death_count > 0)
            rows.push([row++, 0, `  You have been killed ${deathCountAdverb(game._survived_death_count)}.`]);
        row++;
    } else if (game.flags?.explore) {
        rows.push(
            [row++, 0, ' Attributes:'],
            [row++, 0, '  You are nominally aligned.'],
        );
        if (heroHasSlowDigestion()) rows.push([row++, 0, '  You have slower digestion.']);
        if (magicNegation > 0) rows.push([row++, 0, '  You are warded.']);
        rows.push([row++, 0, "  You can't safely pray."]);
        row++;
    }
    rows.push([row++, 0, ' Miscellaneous:']);
    if (game.flags?.debug) {
        rows.push(
            [row++, 0, '  You are running in debug mode.'],
            [row++, 0, "  You haven't encountered any bones levels."],
            [row++, 0, '  Total elapsed playing time is none.'],
        );
    }
    else if (game.flags?.explore) {
        rows.push(
            [row++, 0, '  You are running in explore mode.'],
            [row++, 0, "  You haven't encountered any bones levels."],
            [row++, 0, '  Total elapsed playing time is none.'],
        );
    } else rows.push([row++, 0, '  Total elapsed playing time is none.']);
    return rows;
}

function genericAttributesPage2() {
    const rows = buildGenericAttributesPage2Rows();
    const page2 = rows.filter(([line]) => line < 23);
    const page3 = rows.filter(([line]) => line >= 23).map(([, col, text], index) => [index, col, text]);
    if (page3.length) {
        page3.push([page3.length, 0, ' (3 of 3)']);
        game._attributes_page_3 = page3;
    } else {
        game._attributes_page_3 = null;
    }
    const page2FooterRow = page3.length
        ? 23
        : Math.max(...page2.map(([line]) => line), -1) + 1;
    page2.push([page2FooterRow, 0, page3.length ? ' (2 of 3)' : ' (2 of 2)']);
    return page2;
}

function heroFarlookDescription() {
    const form = polyselfForm();
    const chained = game.u?.uball || isBuriedBallTrapActive() ? ', chained to a heavy iron ball' : '';
    if (form) {
        const called = game.plname ? ` called ${game.plname}` : '';
        return `${form.name}${called}${chained}`;
    }
    const raceAdj = game.urace?.adj || game._startup_race || 'human';
    const race = { orcish: 'orc', elven: 'elf', gnomish: 'gnome', dwarven: 'dwarf' }[raceAdj] || raceAdj;
    const role = game.urole?.name?.[game.flags?.female ? 'f' : 'm'] || game.urole?.name?.m || game._startup_role || 'Adventurer';
    const called = game.plname ? ` called ${game.plname}` : '';
    return `${String(race).toLowerCase()} ${String(role).toLowerCase()}${called}${chained}`;
}

function farlookMonsterDescription(mon) {
    const data = mon.data || {};
    let name = data.name || 'monster';
    if (mon.givenName && name === 'ghost') name = `${mon.givenName}'s ghost`;
    else if (mon.givenName) name = mon.givenName;
    else if (mon.isshk && mon.shknam) name = mon.shknam;
    else if (mon.pet || mon.mpeaceful) name = `${mon.pet ? 'tame' : 'peaceful'} ${name}`;
    const details = [];
    if (mon.msleeping) details.push('asleep');
    if (mon.mundetected) details.push('hidden');
    if (mon.appearObj != null || mon.appearGlyph) details.push('mimicking something');
    return details.length ? `${name}, ${details.join(', ')}` : name;
}

const TRAVEL_TIP_LINES = [
    [0, 10, 'Tip: Farlooking or selecting a map location'],
    [2, 10, 'You are now in a "farlook" mode - the movement keys move the cursor,'],
    [3, 10, 'not your character.  Game time does not advance.  This mode is used'],
    [4, 10, 'to look around the map, or to select a location on it.'],
    [6, 10, 'When in this mode, you can press ESC to return to normal game mode,'],
    [7, 10, 'and pressing ? will show the key help.'],
    [8, 10, '(end)'],
];
const TRAVEL_CURSOR_HELP_LINES = [
    [0, 10, "Use 'h', 'j', 'k', 'l' to move the cursor to the desired destination."],
    [1, 10, "Use 'H', 'J', 'K', 'L' to fast-move the cursor, 8 units at a time."],
    [2, 10, "(or prefix normal move with 'G' or 'g' to fast-move)"],
    [3, 10, "Or enter a background symbol (ex. '<')."],
    [4, 10, "Use '@' to move the cursor on yourself."],
    [5, 10, "Use 'm'/'M' to move the cursor to next/previous monster."],
    [6, 10, "Use 'o'/'O' to move the cursor to next/previous object."],
    [7, 10, "Use 'd'/'D' to move the cursor to next/previous door or doorway."],
    [8, 10, "Use 'x'/'X' to move the cursor next to an unexplored location."],
    [9, 10, "Use 'a'/'A' to move the cursor to anything interesting."],
    [10, 10, "Use '*' to change fast-move mode to skipping same glyphs."],
    [11, 10, "Use '!' to toggle menu listing for possible targets."],
    [12, 10, 'Use \'"\' to change the mode of limiting possible targets.'],
    [13, 10, "Use '#' to toggle automatic description."],
    [14, 10, "Type a '.' when you are at the right place."],
    [16, 10, '--More--'],
];
const FARLOOK_MENU_LINES = [
    [0, 40, 'What do you want to look at:', 1],
    [2, 39, ' / - something on the map'],
    [3, 39, ' i - something you\'re carrying'],
    [4, 39, ' ? - something else (by symbol or name)'],
    [5, 39, ' '],
    [6, 39, ' m - nearby monsters'],
    [7, 39, ' M - all monsters shown on map'],
    [8, 39, ' o - nearby objects'],
    [9, 39, ' O - all objects shown on map'],
    [10, 39, ' t - nearby traps'],
    [11, 39, ' T - all seen or remembered traps'],
    [12, 39, ' e - nearby engravings'],
    [13, 39, ' E - all seen or remembered engravings'],
    [14, 39, ' (end)'],
];
const HELP_MENU_LINES = [
    [0, 16, ' '],
    [0, 17, 'Select one item:', 1],
    [2, 17, 'a - About NetHack (version information).'],
    [3, 17, 'b - Long description of the game and commands.'],
    [4, 17, 'c - List of game commands.'],
    [5, 17, 'd - Concise history of NetHack.'],
    [6, 17, 'e - Info on a character in the game display.'],
    [7, 17, 'f - Info on what a given key does.'],
    [8, 17, 'g - List of game options.'],
    [9, 17, 'h - Longer explanation of game options.'],
    [10, 17, "i - Using the '#optionsfull' or 'm O' command to set options."],
    [11, 17, 'j - Full list of keyboard commands.'],
    [12, 17, 'k - List of extended commands.'],
    [13, 17, 'l - List menu control keys.'],
    [14, 17, "m - Description of NetHack's command line."],
    [15, 17, 'n - The NetHack license.'],
    [16, 17, 'o - Support information.'],
    [17, 17, '(end)'],
];
const GENERATED_OPTIONS_HELP_LINES = [
    '',
    '                 NetHack Options Help:',
    '',
    'Set options as OPTIONS=<options> in',
    '/Users/davidbau/git/mazesofmenace/teleport/maud/test/comparison/c-harness/resul',
    'or use `NETHACKOPTIONS="<options>"\' in your environment',
    '(<options> is a list of options separated by commas)',
    'or press "O" while playing and use the menu.',
    '',
    "Boolean options (which can be negated by prefixing them with '!' or \"no\"):",
    'accessiblemsg, acoustics, altmeta, armorstatus, autodescribe, autodig,',
    'autoopen, autopickup, autoquiver, bgcolors, blind, bones, checkpoint,',
    'cmdassist, color, confirm, customcolors, customsymbols, dark_room, deaf,',
    'dropped_nopick, eight_bit_tty, extmenu, female, fireassist, fixinv,',
    'force_invmenu, goldX, help, herecmd_menu, hilite_pet, hilite_pile,',
    'hitpointbar, idlecheckpoint, ignintr, implicit_uncursed, legacy,',
    'lit_corridor, lootabc, mail, mention_decor, mention_map, mention_walls,',
    'menu_overlay, menucolors, mon_movement, news, nudist, null, pauper,',
    'pickup_stolen, pickup_thrown, price_quotes, pushweapon, query_menu,',
    'quick_farsight, reroll, rest_on_space, safe_pet, safe_wait, selectsaved,',
    'showdamage, showexp, showrace, showvers, silent, sortpack, sounds, sparkle,',
    'spot_monsters, standout, status_updates, terrainstatus, time, tips,',
    'tombstone, toptenwin, travel, tutorial, use_darkgray, use_inverse,',
    'use_truecolor, verbose, voices, weaponstatus, whatis_menu, whatis_moveskip.',
    '',
    'Compound options:',
    "`windowtype'         - windowing system to use (should be specified first),",
    "`playmode'           - normal play, non-scoring explore mode, or debug mode,",
    "`name'               - your character's name (e.g., name:Merlin-W),",
    "`role'               - your starting role (e.g., Barbarian, Valkyrie),",
    "`race'               - your starting race (e.g., Human, Elf),",
    "`gender'             - your starting gender (male or female),",
    "`alignment'          - your starting alignment (lawful, neutral, or chaotic),",
    "`altkeyhandling'     - (not applicable),",
    "`autounlock'         - action to take when encountering locked door or chest,",
    "`boulder'            - deprecated (use S_boulder in sym file instead),",
    "`catname'            - name of your starting pet if it is a kitten,",
    "`crash_email'        - email address for reporting,",
    "`crash_name'         - your name for reporting,",
    "`crash_urlmax'       - length of longest url we can generate,",
    "`DECgraphics'        - load DECGraphics display symbols into symset,",
    "`disclose'           - the kinds of information to disclose at end of game,",
    "`dogname'            - name of your starting pet if it is a little dog,",
    "`dungeon'            - list of symbols to use in drawing the dungeon map,",
    "`effects'            - list of symbols to use in drawing special effects,",
    "`fruit'              - name of a fruit you enjoy eating,",
    "`glyph' - set representation of a glyph to a unicode value and color,",
    "`hilite_status'      - a status highlighting rule (can occur multiple times),",
    "`horsename'          - name of your starting pet if it is a pony,",
    "`IBMgraphics'        - load IBMGraphics display symbols into symset,",
    "`menu_deselect_all'  - deselect all items in a menu,",
    "`menu_deselect_page' - deselect all items on this page of a menu,",
    "`menu_first_page'    - jump to the first page in a menu,",
    "`menu_headings'      - display style for menu headings,",
    "`menu_invert_all'    - invert all items in a menu,",
    "`menu_invert_page'   - invert all items on this page of a menu,",
    "`menu_last_page'     - jump to the last page in a menu,",
    "`menu_next_page'     - go to the next menu page,",
    "`menu_objsyms'       - show object symbols in menus,",
    "`menu_previous_page' - go to the previous menu page,",
    "`menu_search'        - search for a menu item,",
    "`menu_select_all'    - select all items in a menu,",
    "`menu_select_page'   - select all items on this page of a menu,",
    "`menu_shift_left'    - pan current menu page left,",
    "`menu_shift_right'   - pan current menu page right,",
    "`menuinvertmode'     - experimental behavior of menu inverts,",
    "`menustyle'          - user interface for object selection,",
    "`monsters'           - list of symbols to use for monsters,",
    "`msg_window'         - control of \"view previous message(s)\" (^P) behavior,",
    "`msghistory'         - number of top line messages to save,",
    "`number_pad'         - use the number pad for movement,",
    "`objects'            - list of symbols to use for objects,",
    "`packorder'          - the inventory order of the items in your pack,",
    "`paranoid_confirmation' - extra prompting in certain situations,",
    "`petattr'            - attributes for highlighting pets,",
    "`pettype'            - your preferred initial pet type,",
    "`pickup_burden'      - maximum burden picked up before prompt,",
    "`pickup_types'       - types of objects to pick up automatically,",
    "`pile_limit'         - threshold for \"there are many objects here\",",
    "`roguesymset'        - load a set of rogue display symbols from symbols file,",
    "`runmode'            - display frequency when `running' or `travelling',",
    "`scores'             - the parts of the score list you wish to see,",
    "`sortdiscoveries'    - preferred order when displaying discovered objects,",
    "`sortloot'           - sort object selection lists by description,",
    "`sortvanquished'     - preferred order when displaying vanquished monsters,",
    "`soundlib'           - soundlib interface to use (if any),",
    "`statushilites'      - 0=no status highlighting, N=show highlights for N turns,",
    "`statuslines'        - 2 or 3 lines for status display,",
    "`suppress_alert'     - suppress alerts about version-specific features,",
    "`symset'             - load a set of display symbols from symbols file,",
    "`traps'              - list of symbols to use in drawing traps,",
    "`versinfo'           - extra information for 'showvers',",
    "`warnings'           - display characters for warnings,",
    "`whatis_coord'       - show coordinates when auto-describing cursor position,",
    "`whatis_filter' - filter coordinate locations when targeting next or previous,",
    "`cond_'              - prefix for cond_ options,",
    "`font'               - prefix for font options.",
    '',
    'Other settings:',
    ' autocompletions',
    ' autopickup exceptions',
    ' bind keys',
    ' menu colors',
    ' message types',
    ' status condition fields',
    ' status highlight rules',
    '',
    '',
    "Some of the options can only be set before the game is started;",
    "those items will not be selectable in the 'O' command's menu.",
    "Some options are stored in a game's save file, and will keep saved",
    'values when restoring that game even if you have updated your config-',
    'uration file to change them.  Such changes will matter for new games.',
    'The "other settings" can be set with \'O\', but when set within the',
    'configuration file they use their own directives rather than OPTIONS.',
    'See NetHack\'s "Guidebook" for details.',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
];
const FULL_KEY_BINDINGS_LINES = [
    '',
    '            Full Current Key Bindings List',
    '        (also commands with no key assignment)',
    '',
    'Directional keys:',
    '          y  k  u',
    '           \\ | /',
    '          h- . -l',
    '           / | \\',
    '          b  j  n',
    '',
    'Ctrl+<direction> will run in specified direction until something very',
    '        interesting is seen.',
    'Shift+<direction> will run in specified direction until you encounter',
    '        an obstacle.',
    '',
    'Miscellaneous keys:',
    '<esc>   cancel current prompt or pending prefix',
    '^C      interrupt: break out of NetHack (SIGINT)',
    '',
    'Menu control keys:',
    '>       Go to next page',
    '<       Go to previous page',
    '^       Go to first page',
    '|       Go to last page',
    '.       Select all items in entire menu',
    '@       Invert selection for all items',
    '-       Unselect all items in entire menu',
    ',       Select all items on current page',
    '~       Invert current page\'s selections',
    '\\       Unselect all items on current page',
    ':       Search and invert matching items',
    'Return  Accept current choice(s) and dismiss menu',
    'Enter   Same as Return',
    'Space   If not on last page, advance one page;',
    '        when on last page, treat like Return',
    'Escape  Cancel menu without making any choice(s)',
    '',
    'General commands:',
    '^A      repeat        repeat a previous command',
    '^O      overview      show a summary of the explored dungeon',
    '^P      prevmsg       view recent game messages',
    '^R      redraw        redraw screen',
    '^X      attributes    show your attributes',
    "^Z      suspend       push game to background ('fg' to come back)",
    "!       shell         leave game to enter a sub-shell ('exit' to come back)",
    '"       seeamulet     show the amulet currently worn',
    '#       #             enter and perform an extended command',
    '$       showgold      show gold, possibly shop credit or debt',
    '&       whatdoes      tell what a command does',
    '(       seetools      show the tools currently in use',
    ')       seeweapon     show the weapon currently wielded',
    '*       seeall        show all equipment in use',
    '+       showspells    list and reorder known spells',
    '/       whatis        show what type of thing a symbol corresponds to',
    ';       glance        show what type of thing a map symbol corresponds to',
    '=       seerings      show the ring(s) currently worn',
    '?       help          give a help message',
    "@       autopickup    toggle the 'autopickup' option on/off",
    'C       call          name a monster, specific object, or type of object',
    'I       inventtype    show inventory of one specific item class',
    'O       options       show option settings',
    'S       save          save the game and exit',
    'V       versionshort  show version and date+time program was built',
    '[       seearmor      show the armor currently worn',
    '\\       known         show what object types have been discovered',
    '^       showtrap      describe an adjacent, discovered trap',
    '`       knownclass    show discovered types for one class of objects',
    'i       inventory     show your inventory',
    'v       chronicle     show journal of major events',
    '|       perminv       scroll persistent inventory display',
    '<del>   terrain       view map without monsters or objects obstructing it',
    'M-?     ?             list all extended commands',
    'M-A     annotate      name current level',
    'M-C     conduct       list voluntary challenges you have maintained',
    'M-N     name          same as call; name a monster or object or object type',
    'M-O     overview      show a summary of the explored dungeon',
    'M-V     vanquished    list vanquished monsters',
    'M-X     exploremode   enter explore (discovery) mode',
    'M-a     adjust        adjust inventory letters',
    'M-e     enhance       advance or check weapon and spell skills',
    'M-g     genocided     list monsters that have been genocided or become extinct',
    'M-n     name          same as call; name a monster or object or object type',
    'M-v     version       list compile time options for this version of NetHack',
    '#bugreport            file a bug report',
    '#herecmdmenu          show menu of commands you can do here',
    "#history              show a summary of the game's development",
    '#lookaround           describe what you can see',
    '#optionsfull          show all option settings, possibly change them',
    '#quit                 exit without saving current game',
    '#saveoptions          save the game configuration',
    '#therecmdmenu         menu of commands you can do from here to adjacent spot',
    '#toggle               toggle boolean option',
    '',
    'Game commands:',
    '^D      kick          kick something',
    '^T      teleport      teleport around the level',
    '^_      retravel      travel to previously selected travel location',
    ',       pickup        pick up things at the current location',
    "-       fight         prefix: force fight even if you don't see a monster",
    '.       wait          rest one move while doing nothing',
    '5       run           prefix: run until something interesting is seen',
    ':       look          look at what is here',
    '<       up            go up a staircase',
    '>       down          go down a staircase',
    'A       takeoffall    remove all armor',
    'D       droptype      drop specific item types',
    'E       engrave       engrave writing on the floor',
    "F       fight         prefix: force fight even if you don't see a monster",
    'G       run           prefix: run until something interesting is seen',
    'P       puton         put on an accessory (ring, amulet, etc)',
    'Q       quiver        select ammunition for quiver',
    'R       remove        remove an accessory (ring, amulet, etc)',
    'T       takeoff       take off one piece of armor',
    'W       wear          wear a piece of armor',
    'X       twoweapon     toggle two-weapon combat',
    'Z       cast          zap (cast) a spell',
    '_       travel        travel to a specific location on the map',
    'a       apply         apply (use) a tool (pick-axe, key, lamp...)',
    'c       close         close a door',
    'd       drop          drop an item',
    'e       eat           eat something',
    'f       fire          fire ammunition from quiver',
    'g       rush          prefix: rush until something interesting is seen',
    'm       reqmenu       prefix: request menu or modify command',
    'o       open          open a door',
    'p       pay           pay your shopping bill',
    'q       quaff         quaff (drink) something',
    'r       read          read a scroll or spellbook',
    's       search        search for traps and secret doors',
    't       throw         throw something',
    'w       wield         wield (put in use) a weapon',
    'x       swap          swap wielded and secondary weapons',
    'z       zap           zap a wand',
    'M-2     twoweapon     toggle two-weapon combat',
    'M-5     rush          prefix: rush until something interesting is seen',
    'M-R     ride          mount or dismount a saddled steed',
    'M-T     tip           empty a container',
    'M-c     chat          talk to someone',
    'M-d     dip           dip an object into something',
    'M-f     force         force a lock',
    "M-i     invoke        invoke an object's special powers",
    'M-j     jump          jump to another location',
    'M-l     loot          loot a box on the floor',
    "M-m     monster       use monster's special ability",
    'M-o     offer         offer a sacrifice to the gods',
    'M-p     pray          pray to the gods for help',
    'M-r     rub           rub a lamp or a stone',
    'M-s     sit           sit down',
    'M-t     turn          turn undead away',
    'M-u     untrap        untrap something',
    'M-w     wipe          wipe off your face',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
];
const MENU_CONTROL_HELP_LINES = [
    'Menu control keys:',
    '',
    '           Whole  Current',
    '            Menu   Page',
    '  Select     .      ,',
    '  Invert     @      ~',
    'Deselect     -      \\',
    '',
    '   Go to     >      Next page',
    '             <      Previous page',
    '             ^      First page',
    '             |      Last page',
    '',
    '  Search     :      Exter a target string and invert all matching entries',
    '',
    '   Other   Return   Accept current choice(s) and dismiss menu',
    '           Enter    Same as Return',
    '           Space    If not on last page, advance one page;',
    '                    when on last page, treat like Return',
    '           Escape   Cancel menu without making any choice(s)',
    '',
    '',
    '',
];
const SUPPORT_INFO_LINES = [
    'To contact the NetHack development team directly,',
    "see the 'Contact' form on our website or email <devteam@nethack.org>.",
    '',
    'For more information on NetHack, or to report a bug,',
    'visit our website "https://www.nethack.org/".',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
];
const FOUNTAIN_TEXT_LINES = [
    [0, 28, 'Rest! This little Fountain runs'],
    [1, 28, 'Thus for aye: -- It never stays'],
    [2, 28, 'For the look of summer suns,'],
    [3, 28, 'Nor the cold of winter days.'],
    [4, 28, "Whose'er shall wander near,"],
    [5, 28, 'When the Syrian heat is worst,'],
    [6, 28, 'Let him hither come, nor fear'],
    [7, 28, 'Lest he may not slake his thirst:'],
    [8, 28, 'He will find this little river'],
    [9, 28, 'Running still, as bright as ever.'],
    [10, 28, 'Let him drink, and onward hie,'],
    [11, 28, 'Bearing but in thought, that I,'],
    [12, 28, 'Erotas, bade the Naiad fall,'],
    [13, 28, 'And thank the great god Pan for all!'],
    [14, 27, '         [ For a Fountain, by Bryan Waller Procter ]'],
    [15, 28, '--More--'],
];
const QUARTERSTAFF_TEXT_LINES = [
    [0, 16, " So they stood, each in his place, neither moving a finger's"],
    [1, 16, ' breadth back, for one good hour, and many blows were given'],
    [2, 16, ' and received by each in that time, till here and there were'],
    [3, 16, ' sore bones and bumps, yet neither thought of crying "Enough,"'],
    [4, 16, ' or seemed likely to fall from off the bridge.  Now and then'],
    [5, 16, ' they stopped to rest, and each thought that he never had seen'],
    [6, 16, ' in all his life before such a hand at quarterstaff.  At last'],
    [7, 16, ' Robin gave the stranger a blow upon the ribs that made his'],
    [8, 16, ' jacket smoke like a damp straw thatch in the sun.  So shrewd'],
    [9, 16, " was the stroke that the stranger came within a hair's breadth"],
    [10, 16, ' of falling off the bridge; but he regained himself right'],
    [11, 16, ' quickly, and, by a dexterous blow, gave Robin a crack on the'],
    [12, 16, ' crown that caused the blood to flow.  Then Robin grew mad'],
    [13, 16, ' with anger, and smote with all his might at the other; but'],
    [14, 16, ' the stranger warded the blow, and once again thwacked Robin,'],
    [15, 16, ' and this time so fairly that he fell heels over head into the'],
    [16, 16, ' water, as the queen pin falls in a game of bowls.'],
    [17, 25, '[ The Merry Adventures of Robin Hood, by Howard Pyle ]'],
    [18, 16, ' --More--'],
];
const NAME_MENU_LINES = [
    [0, 32, 'What do you want to name?', 1],
    [2, 32, 'm - a monster'],
    [3, 32, 'i - a particular object in inventory'],
    [4, 32, 'o - the type of an object in inventory'],
    [5, 32, 'f - the type of an object upon the floor'],
    [6, 32, 'd - the type of an object on discoveries list'],
    [7, 31, ' '],
    [7, 32, 'a - record an annotation for the current level'],
    [8, 31, ' '],
    [8, 32, '(end)'],
];
const CMDASSIST_DIRECTION_LINES = [
    [0, 0, 'cmdassist: Invalid direction key!'],
    [2, 0, 'Valid direction keys are:'],
    [3, 10, 'y  k  u'],
    [4, 11, '\\ | /'],
    [5, 10, 'h- . -l'],
    [6, 11, '/ | \\'],
    [7, 10, 'b  j  n'],
    [9, 10, '<  up'],
    [10, 10, '>  down'],
    [11, 10, '.  direct at yourself'],
    [13, 0, '(Suppress this message with !cmdassist in config file.)'],
    [23, 0, '--More--'],
];

function eatingPrompt() {
    const letters = inventoryLetters(heroInventoryEatCandidate);
    const contiguous = [...letters].every((ch, i) => !i || ch.charCodeAt(0) === letters.charCodeAt(i - 1) + 1);
    const display = contiguous && letters.length > 5 ? `${letters[0]}-${letters[letters.length - 1]}` : letters;
    return `What do you want to eat? [${display || '-'} or ?*]`;
}

function wizardRandomSpellProfile() {
    const spell = (game.inventory || [])
        .find(item => item.cls === 'spellbook' && item.spell)
        ?.spell;
    if (spell?.skill === 'divination' && spell?.level === 3) return 'clairvoyance';
    if (spell?.skill === 'enchantment' && spell?.level === 2) return 'slowMonster';
    if (spell?.skill === 'cleric' && spell?.level === 2) return 'createMonster';
    return 'default';
}

function inventoryLetters(match) {
    return (game.inventory || [])
        .filter(match)
        .map(item => item.letter)
        .filter(Boolean)
        .join('');
}

function inventoryLetterMenu(letters) {
    const runs = [];
    let i = 0;
    while (i < letters.length) {
        let j = i;
        while (j + 1 < letters.length && letters.charCodeAt(j + 1) === letters.charCodeAt(j) + 1) j++;
        runs.push(j - i >= 2 ? `${letters[i]}-${letters[j]}` : letters.slice(i, j + 1));
        i = j + 1;
    }
    return runs.join('');
}

function erosionPrefix(item) {
    const profile = wishedDamageProfile(item);
    const pieces = [];
    if (item.greased) pieces.push('greased');
    const primary = Math.min(3, item.oeroded || 0);
    const secondary = Math.min(3, item.oeroded2 || 0);
    if (primary && profile.primaryWord)
        pieces.push(`${primary === 2 ? 'very ' : primary === 3 ? 'thoroughly ' : ''}${profile.primaryWord}`);
    if (secondary && profile.secondaryWord)
        pieces.push(`${secondary === 2 ? 'very ' : secondary === 3 ? 'thoroughly ' : ''}${profile.secondaryWord}`);
    if (item.oerodeproof && profile.proofWord) pieces.push(profile.proofWord);
    return pieces.length ? `${pieces.join(' ')} ` : '';
}

function weaponLineEnchantmentPrefix(item) {
    const match = String(item?.line || '').match(/(?:^| )([+-]\d+) /);
    return match ? `${match[1]} ` : '';
}

function weaponEnchantmentPrefix(item) {
    if (item?.spe != null) {
        const spe = item.spe;
        return `${spe >= 0 ? '+' : ''}${spe} `;
    }
    return weaponLineEnchantmentPrefix(item);
}

function isWeaponDisplayObject(item) {
    const cls = itemClassKey(item);
    return cls === 'weapon' || item?.otyp === DART || item?.glyph === ')' || item?.wielded || item?.alternate;
}

function poisonedWeaponDisplayParts(item, rawName = pickupObjectName(item)) {
    if (item?.opoisoned && isPoisonableWeaponObject(item) && /^poisoned\s+/.test(rawName))
        return { poisonPrefix: 'poisoned ', name: rawName.replace(/^poisoned\s+/, '') };
    return { poisonPrefix: '', name: rawName };
}

function dipItemDescription(item, shortened = false) {
    const quan = item.quan || 1;
    let baseName = pickupObjectName(item);
    const rust = shortened ? '' : erosionPrefix(item);
    const showBuc = !shortened && item.bknown === true && !/holy water|unholy water/.test(baseName);
    const buc = showBuc
        ? item.cursed ? 'cursed ' : item.blessed ? 'blessed ' : ''
        : '';
    const worn = item.worn || item.line?.includes('being worn') ? ' (being worn)' : '';
    if (isWeaponDisplayObject(item)) {
        const parts = poisonedWeaponDisplayParts(item, baseName);
        const quantity = quan > 1 ? `${quan} ` : '';
        return `${quantity}${buc}${parts.poisonPrefix}${rust}${weaponEnchantmentPrefix(item)}${parts.name}${worn}`;
    }
    if (quan > 1)
        baseName = `${quan} ${item.plural || (baseName.endsWith('s') ? baseName : `${baseName}s`)}`;
    const spe = item.spe == null || quan > 1 ? '' : `${item.spe >= 0 ? '+' : ''}${item.spe} `;
    return `${buc}${rust}${spe}${baseName}${worn}`;
}

function dipItemPromptParts(item) {
    let description = dipItemDescription(item);
    let article = (item.quan || 1) > 1 ? '' : `${/^[aeiou]/i.test(description) ? 'an' : 'a'} `;
    if (`${article}${description}`.length > 49) {
        description = dipItemDescription(item, true);
        article = (item.quan || 1) > 1 ? '' : `${/^[aeiou]/i.test(description) ? 'an' : 'a'} `;
    }
    return { article, description };
}

function nextToDoor(x, y) {
    for (let dy = -1; dy <= 1; dy++) {
        for (let dx = -1; dx <= 1; dx++) {
            if (!dx && !dy) continue;
            const typ = game.level?.at(x + dx, y + dy)?.typ;
            if (typ === DOOR || typ === SDOOR) return true;
        }
    }
    return false;
}

function dogushforth() {
    const limits = [7, 7, 7, 6, 6, 5, 4, 2];
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    let madepool = 0;
    for (let y = Math.max(0, uy - 7); y <= Math.min(ROWNO - 1, uy + 7); y++) {
        const offset = limits[Math.abs(y - uy)];
        for (let x = Math.max(1, ux - offset); x <= Math.min(COLNO - 1, ux + offset); x++) {
            if (!couldsee(x, y) || ((x + y) % 2) || (x === ux && y === uy)) continue;
            if (rn2(1 + distmin(ux, uy, x, y))) continue;
            const loc = game.level?.at(x, y);
            if (!loc || loc.typ !== ROOM) continue;
            if ((game.level?.objects || []).some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y)) continue;
            if (nextToDoor(x, y)) continue;
            loc.typ = POOL;
            loc.flags = 0;
            madepool++;
            newsym(x, y);
        }
    }
    return madepool;
}

function nextInventoryLetter() {
    const used = new Set((game.inventory || []).map(item => item.letter).filter(Boolean));
    let index = game._nextInventoryLetterIndex ?? Math.max(-1,
        ...(game.inventory || []).map(item => INVENTORY_LETTERS.indexOf(item.letter)).filter(i => i >= 0)) + 1;
    while (used.has(INVENTORY_LETTERS[index]) && index < INVENTORY_LETTERS.length - 1) index++;
    game._nextInventoryLetterIndex = Math.min(INVENTORY_LETTERS.length, index + 1);
    return INVENTORY_LETTERS[index] || INVENTORY_LETTERS[INVENTORY_LETTERS.length - 1];
}

function simulatedNextInventoryLetters(count) {
    const used = new Set((game.inventory || []).map(item => item.letter).filter(Boolean));
    let index = game._nextInventoryLetterIndex ?? Math.max(-1,
        ...(game.inventory || []).map(item => INVENTORY_LETTERS.indexOf(item.letter)).filter(i => i >= 0)) + 1;
    const letters = [];
    for (let i = 0; i < count; i++) {
        while (used.has(INVENTORY_LETTERS[index]) && index < INVENTORY_LETTERS.length - 1) index++;
        const letter = INVENTORY_LETTERS[index];
        if (!letter || used.has(letter)) return null;
        letters.push(letter);
        used.add(letter);
        index = Math.min(INVENTORY_LETTERS.length, index + 1);
    }
    return letters;
}

function updateMoneyLine(money) {
    if (money) money.line = `$ - ${money.quan || 0} gold piece${(money.quan || 0) === 1 ? '' : 's'}`;
}

function compactInventoryLetters(letters) {
    let range = '';
    let runStart = '';
    let previous = '';
    for (const letter of String(letters || '')) {
        if (!runStart) {
            runStart = letter;
        } else if (letter.charCodeAt(0) !== previous.charCodeAt(0) + 1) {
            const length = previous.charCodeAt(0) - runStart.charCodeAt(0) + 1;
            range += length > 2 ? `${runStart}-${previous}` : `${runStart}${length === 2 ? previous : ''}`;
            runStart = letter;
        }
        previous = letter;
    }
    if (!runStart) return range;
    const length = previous.charCodeAt(0) - runStart.charCodeAt(0) + 1;
    return range + (length > 2 ? `${runStart}-${previous}` : `${runStart}${length === 2 ? previous : ''}`);
}

function getobjPromptLetters(letters) {
    const text = String(letters || '');
    return text.length > 5 ? compactInventoryLetters(text) : text;
}

function isApplyCoinObject(item) {
    return shopBillableGold(item);
}

function applyInventoryItems() {
    const items = [...(game.inventory || [])];
    const goldCount = Math.max(0, Math.trunc(Number(game._goldCount || 0)));
    if (goldCount > 0 && !items.some(isApplyCoinObject)) {
        items.unshift({
            letter: '$',
            cls: 'coin',
            otyp: GOLD_PIECE,
            glyph: '$',
            quan: goldCount,
        });
    }
    return items;
}

function potionIdentityKnownForApply(item, identity) {
    const name = inventoryItemName(item).toLowerCase();
    const visibleKind = String(item?.kind || '').toLowerCase().replace(/^potion of /, '');
    return item?.known === true
        || potionDiscoveryKnown(identity)
        || name.includes(`potion of ${identity}`)
        || (item?.known !== false && visibleKind === identity);
}

const GRAY_STONE_APPLY_NAMES = new Set(['luckstone', 'loadstone', 'touchstone', 'flint', 'flint stone']);

function grayStoneNameForApply(item) {
    const values = [
        item?.actualKind,
        item?.kind,
        objectKindKey(item),
        item?.gemDescription,
        item?.displayName,
    ].map(value => String(value || '').toLowerCase().trim()).filter(Boolean);
    if (item?.otyp === LUCKSTONE) return 'luckstone';
    if (item?.otyp === LOADSTONE) return 'loadstone';
    if (item?.otyp === TOUCHSTONE) return 'touchstone';
    if (item?.otyp === FLINT) return 'flint';
    for (const value of values) {
        if (GRAY_STONE_APPLY_NAMES.has(value)) return value === 'flint stone' ? 'flint' : value;
    }
    if (values.some(value => value === 'gray stone' || value === 'grey stone'))
        return String(item?.actualKind || item?.kind || '').toLowerCase().trim();
    return '';
}

function isGrayStoneApplyItem(item) {
    if (!item) return false;
    if ([LUCKSTONE, LOADSTONE, TOUCHSTONE, FLINT].includes(item.otyp)) return true;
    if (GRAY_STONE_APPLY_NAMES.has(grayStoneNameForApply(item))) return true;
    const description = String(item.gemDescription || '').toLowerCase();
    return (itemClassKey(item) === 'gem' || item.glyph === '*')
        && (description === 'gray stone' || description === 'grey stone');
}

function gemStoneDiscoveryKnown(name) {
    const key = String(name || '').toLowerCase().trim();
    if (!key) return false;
    return (game._discoveries || []).some(entry =>
        ['Gems/Stones', 'Gems'].includes(entry.section)
        && String(entry.name || '').toLowerCase() === key
        && entry.known !== false);
}

function isTouchstoneApplyItem(item) {
    return !!item && (item.otyp === TOUCHSTONE || grayStoneNameForApply(item) === 'touchstone');
}

function touchstoneKnownForApply(item) {
    return isTouchstoneApplyItem(item)
        && item?.dknown !== false
        && (item?.known === true
            || gemStoneDiscoveryKnown('touchstone')
            || (String(item?.kind || '').toLowerCase() === 'touchstone' && item?.known !== false));
}

function grayStoneSelectionKind(item) {
    if (!isGrayStoneApplyItem(item)) return '';
    if (item?.dknown === false) return 'suggest';
    if (isTouchstoneApplyItem(item)) return 'suggest';
    const name = grayStoneNameForApply(item);
    if (item?.known === true || gemStoneDiscoveryKnown('touchstone') || gemStoneDiscoveryKnown(name))
        return 'selectableInvalid';
    return 'suggest';
}

function applySelectionKind(item) {
    if (!item) return 'exclude';
    if (isApplyCoinObject(item)) return 'downplay';

    const name = inventoryItemName(item).toLowerCase();
    const cls = itemClassKey(item);
    if (cls === 'tool' || item.section === 'Tools' || isIceBoxObject(item)) return 'suggest';
    if (isWandItem(item) || cls === 'wand' || item.kind === 'wand of sleep' || item.otyp === WAND_CLASS) return 'suggest';
    if (cls === 'spellbook') return 'suggest';
    if (cls === 'weapon' && APPLY_WEAPON_NAME_RE.test(name)) return 'suggest';
    if (/lamp|camera|card|bag|sack|cream pie/.test(name)) return 'suggest';
    if (isRoyalJelly(item)) return 'suggest';
    if (isPotionObject(item)) {
        const identity = potionIdentityName(item) || objectKindKey(item).replace(/^potion of /, '');
        if (isPotionOfOil(item) && potionIdentityKnownForApply(item, 'oil')) return 'suggest';
        if (!identity || !potionIdentityKnownForApply(item, identity)) return 'downplay';
        return 'selectableInvalid';
    }
    const grayStoneKind = grayStoneSelectionKind(item);
    if (grayStoneKind) return grayStoneKind;
    if (heroIsHallucinating() && /banana/.test(name)) return 'downplay';
    return 'selectableInvalid';
}

function applyItemsBySelectionKind(kind) {
    return applyInventoryItems().filter(item => applySelectionKind(item) === kind && item.letter);
}

function applyPromptMessage() {
    const suggested = applyItemsBySelectionKind('suggest').map(item => item.letter).join('');
    if (suggested) return `What do you want to use or apply? [${getobjPromptLetters(suggested)} or ?*]`;
    if (applyItemsBySelectionKind('downplay').length) return 'What do you want to use or apply? [*]';
    return '';
}

function applyMenuItems(ch) {
    if (ch === '*') return applyInventoryItems().filter(item => item.letter);
    const suggested = applyItemsBySelectionKind('suggest');
    return suggested.length ? suggested : applyItemsBySelectionKind('downplay');
}

function applyMenuSection(item) {
    const cls = itemClassKey(item);
    if (isApplyCoinObject(item)) return 'Coins';
    if (cls === 'spellbook') return 'Spellbooks';
    if (isWandItem(item) || cls === 'wand' || item.kind === 'wand of sleep' || item.otyp === WAND_CLASS) return 'Wands';
    if (cls === 'tool' || item.section === 'Tools' || isIceBoxObject(item)) return 'Tools';
    if (cls === 'weapon') return 'Weapons';
    if (isPotionObject(item)) return 'Potions';
    if (cls === 'scroll') return 'Scrolls';
    if (cls === 'armor') return 'Armor';
    if (cls === 'food') return 'Comestibles';
    return 'Other Items';
}

function setApplyObjectMenu(items) {
    const order = ['Spellbooks', 'Wands', 'Tools', 'Weapons', 'Potions', 'Scrolls', 'Armor', 'Comestibles', 'Coins', 'Other Items'];
    const sorted = [...items].sort((a, b) => {
        const sectionDiff = order.indexOf(applyMenuSection(a)) - order.indexOf(applyMenuSection(b));
        return sectionDiff || String(a.letter || '').localeCompare(String(b.letter || ''));
    });
    const rows = [];
    let row = 0, lastSection = '';
    for (const item of sorted) {
        const section = applyMenuSection(item);
        if (section !== lastSection) {
            rows.push([row, 40, ' ']);
            rows.push([row++, 41, section, 1]);
            lastSection = section;
        }
        rows.push([row++, 40, ` ${normalInventoryLine(item)}`.padEnd(40, ' ')]);
    }
    rows.push([row, 40, ' (end)'.padEnd(40, ' ')]);
    setOverlay(rows, 2, false, 0);
}

function findApplyInventoryItem(letter) {
    return applyInventoryItems().find(invItem => invItem.letter === letter);
}

function useStonePromptAction(stone) {
    const count = Math.max(1, Math.trunc(Number(stone?.quan || 1)));
    return `rub on the stone${count > 1 ? 's' : ''}`;
}

function useStoneTargetSelectionKind(item, stone) {
    if (!item) return 'exclude';
    if (!touchstoneKnownForApply(stone)) return 'suggest';
    if (isApplyCoinObject(item)) return 'suggest';
    const cls = itemClassKey(item);
    const gemKnown = item?.dknown !== false
        && (item?.known === true || gemStoneDiscoveryKnown(grayStoneNameForApply(item) || item?.actualKind || item?.kind));
    if ((cls === 'gem' || item.glyph === '*') && !gemKnown) return 'suggest';
    return 'downplay';
}

function useStoneTargetItemsBySelectionKind(kind, stone) {
    return applyInventoryItems().filter(item => useStoneTargetSelectionKind(item, stone) === kind && item.letter);
}

function useStonePromptMessage(stone) {
    const action = useStonePromptAction(stone);
    const suggested = useStoneTargetItemsBySelectionKind('suggest', stone).map(item => item.letter).join('');
    if (suggested) return `What do you want to ${action}? [${getobjPromptLetters(suggested)} or ?*]`;
    if (useStoneTargetItemsBySelectionKind('downplay', stone).length) return `What do you want to ${action}? [*]`;
    return `What do you want to ${action}?`;
}

function useStoneMenuItems(ch, stone) {
    if (ch === '*') return applyInventoryItems().filter(item => item.letter);
    const suggested = useStoneTargetItemsBySelectionKind('suggest', stone);
    return suggested.length ? suggested : useStoneTargetItemsBySelectionKind('downplay', stone);
}

function setUseStoneObjectMenu(items) {
    setApplyObjectMenu(items);
}

function theUseStoneObjectName(item) {
    return `the ${pickupObjectName(item)}`;
}

function observeUseStoneSource(stone) {
    if (!stone || game.u?.blind) return;
    stone.dknown = true;
    const name = grayStoneNameForApply(stone);
    if (stone.known === true || gemStoneDiscoveryKnown(name)) return;
    game._discoveries ??= [];
    if (!(game._discoveries || []).some(entry =>
        entry.section === 'Gems/Stones' && entry.name === 'gray stone')) {
        game._discoveries.push({
            section: 'Gems/Stones',
            name: 'gray stone',
            text: 'gray stone',
            starred: false,
            known: false,
        });
    }
}

const USE_STONE_COLOR_NAMES = new Map([
    [CLR_BLACK, 'black'], [CLR_RED, 'red'], [CLR_GREEN, 'green'], [CLR_BROWN, 'brown'],
    [CLR_BLUE, 'blue'], [CLR_MAGENTA, 'magenta'], [CLR_CYAN, 'cyan'], [CLR_GRAY, 'gray'],
    [NO_COLOR, 'transparent'], [CLR_ORANGE, 'orange'], [CLR_BRIGHT_GREEN, 'bright green'],
    [CLR_YELLOW, 'yellow'], [CLR_BRIGHT_BLUE, 'bright blue'],
    [CLR_BRIGHT_MAGENTA, 'bright magenta'], [CLR_BRIGHT_CYAN, 'bright cyan'],
    [CLR_WHITE, 'white'],
]);
const USE_STONE_GEM_COLORS = new Map([
    ['dilithium crystal', 'white'], ['diamond', 'white'], ['ruby', 'red'],
    ['jacinth', 'orange'], ['jacinth stone', 'orange'], ['sapphire', 'blue'],
    ['black opal', 'black'], ['emerald', 'green'], ['turquoise', 'green'],
    ['turquoise stone', 'green'], ['citrine', 'yellow'], ['citrine stone', 'yellow'],
    ['aquamarine', 'green'], ['aquamarine stone', 'green'], ['amber', 'brown'],
    ['amber stone', 'brown'], ['topaz', 'brown'],
    ['topaz stone', 'brown'], ['jet', 'black'], ['jet stone', 'black'],
    ['opal', 'white'], ['chrysoberyl', 'yellow'], ['chrysoberyl stone', 'yellow'],
    ['garnet', 'red'], ['garnet stone', 'red'], ['amethyst', 'magenta'],
    ['amethyst stone', 'magenta'], ['jasper', 'red'], ['jasper stone', 'red'],
    ['fluorite', 'magenta'], ['fluorite stone', 'magenta'], ['obsidian', 'black'],
    ['obsidian stone', 'black'], ['agate', 'orange'], ['agate stone', 'orange'],
    ['jade', 'green'], ['jade stone', 'green'],
    ['worthless piece of white glass', 'white'], ['worthless piece of blue glass', 'blue'],
    ['worthless piece of red glass', 'red'],
    ['worthless piece of yellowish brown glass', 'brown'],
    ['worthless piece of orange glass', 'orange'], ['worthless piece of yellow glass', 'yellow'],
    ['worthless piece of black glass', 'black'], ['worthless piece of green glass', 'green'],
    ['worthless piece of violet glass', 'magenta'],
]);
const USE_STONE_DESCRIPTION_COLORS = new Map([
    ['yellowish brown', 'brown'],
    ['violet', 'magenta'],
]);
const USE_STONE_GEM_DISPLAY_NAMES = new Map([
    ['jacinth', 'jacinth stone'], ['turquoise', 'turquoise stone'],
    ['citrine', 'citrine stone'], ['aquamarine', 'aquamarine stone'],
    ['amber', 'amber stone'], ['topaz', 'topaz stone'], ['jet', 'jet stone'],
    ['chrysoberyl', 'chrysoberyl stone'], ['garnet', 'garnet stone'],
    ['amethyst', 'amethyst stone'], ['jasper', 'jasper stone'],
    ['fluorite', 'fluorite stone'], ['obsidian', 'obsidian stone'],
    ['agate', 'agate stone'], ['jade', 'jade stone'],
]);
const USE_STONE_RING_APPEARANCE_COLORS = new Map([
    ['wooden', 'brown'], ['granite', 'gray'], ['opal', 'gray'], ['clay', 'red'],
    ['coral', 'orange'], ['black onyx', 'black'], ['moonstone', 'gray'],
    ['tiger eye', 'brown'], ['jade', 'green'], ['bronze', 'yellow'],
    ['agate', 'red'], ['topaz', 'cyan'], ['sapphire', 'blue'], ['ruby', 'red'],
    ['diamond', 'white'], ['pearl', 'white'], ['iron', 'cyan'], ['brass', 'yellow'],
    ['copper', 'yellow'], ['twisted', 'cyan'], ['steel', 'cyan'], ['silver', 'gray'],
    ['gold', 'yellow'], ['ivory', 'white'], ['emerald', 'green'], ['wire', 'cyan'],
    ['engagement', 'cyan'], ['shiny', 'cyan'],
]);
const USE_STONE_FLIMSY_MATERIALS = new Set([
    'liquid', 'wax', 'veggy', 'flesh', 'paper', 'cloth', 'leather', 'rubber',
]);
const USE_STONE_MATERIAL_COLORS = new Map([
    ['paper', 'white'], ['leather', 'brown'], ['veggy', 'brown'],
    ['flesh', 'brown'], ['rubber', 'brown'],
]);
const USE_STONE_OTYP_MATERIALS = new Map([
    [SACK, 'cloth'], [OILSKIN_SACK, 'cloth'], [BAG_OF_HOLDING, 'cloth'],
    [BLINDFOLD, 'cloth'], [TALLOW_CANDLE, 'wax'], [WAX_CANDLE, 'wax'],
    [MAGIC_FLUTE, 'wood'], [MAGIC_HARP, 'wood'], [CHEST, 'wood'],
    [BELL, 'silver'], [CANDELABRUM_OF_INVOCATION, 'gold'],
]);
const USE_STONE_KIND_MATERIALS = new Map([
    ['sack', 'cloth'], ['oilskin sack', 'cloth'], ['bag of holding', 'cloth'],
    ['bag', 'cloth'], ['towel', 'cloth'], ['blindfold', 'cloth'],
    ['tallow candle', 'wax'], ['wax candle', 'wax'], ['magic flute', 'wood'],
    ['wooden flute', 'wood'], ['magic harp', 'wood'], ['wooden harp', 'wood'],
    ['chest', 'wood'], ['large box', 'wood'], ['leather drum', 'leather'],
    ['drum', 'leather'], ['drum of earthquake', 'leather'],
    ['bell of opening', 'silver'], ['candelabrum of invocation', 'gold'],
]);

function useStoneSurfaceName(stone) {
    return `stone${Math.max(1, Math.trunc(Number(stone?.quan || 1))) > 1 ? 's' : ''}`;
}

function useStoneSubjectName(stone) {
    return pickupObjectName(stone) || useStoneSurfaceName(stone);
}

function useStoneSurfaceVerb(stone, singular, plural = singular) {
    return Math.max(1, Math.trunc(Number(stone?.quan || 1))) > 1 ? plural : singular;
}

function useStoneHeroCanIdentify(stone) {
    if (!isTouchstoneApplyItem(stone)) return false;
    if (stone?.blessed) return true;
    if (stone?.cursed) return false;
    const role = String(game.urole?.name?.m || game._startup_role || '').toLowerCase();
    const race = String(game.urace?.noun || game.urace?.adj || game._startup_race || '').toLowerCase();
    return role === 'archeologist' || race === 'gnome';
}

function useStoneObjectClass(item) {
    const cls = itemClassKey(item);
    if (cls) return cls;
    if (item?.otyp === RING_CLASS || item?.glyph === '=') return 'ring';
    if (item?.otyp === GEM_CLASS || item?.glyph === '*') return 'gem';
    if (isApplyCoinObject(item)) return 'coin';
    return '';
}

function useStoneObjectMaterial(item) {
    const cls = useStoneObjectClass(item);
    if (cls === 'ring') return stoneToFleshRingMaterial(item);
    if (cls === 'gem') return stoneToFleshObjectMaterial(item);
    const explicit = normalizeStoneToFleshMaterial(item?.oc_material || item?.material);
    if (explicit) return explicit;
    if (isApplyCoinObject(item)) return 'gold';
    if (USE_STONE_OTYP_MATERIALS.has(item?.otyp)) return USE_STONE_OTYP_MATERIALS.get(item.otyp);
    if (item?.otyp === BLINDING_VENOM || item?.otyp === ACID_VENOM || cls === 'venom') return 'liquid';
    if (item?.otyp === POTION_CLASS || cls === 'potion' || item?.glyph === '!') return 'glass';
    if (item?.otyp === SCROLL_CLASS || cls === 'scroll' || item?.glyph === '?') return 'paper';
    if (item?.otyp === SPBOOK_CLASS || cls === 'spellbook' || item?.glyph === '+') return 'paper';
    const kind = objectKindKey(item);
    if (USE_STONE_KIND_MATERIALS.has(kind)) return USE_STONE_KIND_MATERIALS.get(kind);
    if (/\bvenom\b/.test(kind)) return 'liquid';
    if (/\bgold(?:en)?\b/.test(kind)) return 'gold';
    if (/\bsilver\b/.test(kind)) return 'silver';
    if (/\bwax\b/.test(kind)) return 'wax';
    if (/\bwood(?:en)?\b/.test(kind)) return 'wood';
    if (/\bcloth\b/.test(kind)) return 'cloth';
    if (/\bpaper\b|\bscroll\b|\bspellbook\b/.test(kind)) return 'paper';
    if (/\bleather\b/.test(kind)) return 'leather';
    if (/\brubber hose\b/.test(kind)) return 'rubber';
    return '';
}

function useStoneColorName(item) {
    const numeric = item?._display_color ?? item?.color;
    if (typeof numeric === 'string') {
        const normalized = numeric.toLowerCase().replace(/^hi_/, '').trim();
        if (USE_STONE_DESCRIPTION_COLORS.has(normalized))
            return USE_STONE_DESCRIPTION_COLORS.get(normalized);
        if ([...USE_STONE_COLOR_NAMES.values()].includes(normalized)) return normalized;
    }
    if (USE_STONE_COLOR_NAMES.has(numeric)) return USE_STONE_COLOR_NAMES.get(numeric);
    const cls = useStoneObjectClass(item);
    if (cls === 'ring') {
        const appearance = String(item?.appearance || (
            typeof item?.kind === 'string' && /\bring$/.test(item.kind) && !/^ring of /.test(item.kind)
                ? item.kind.replace(/\s+ring$/, '')
                : ''
        ) || (item?.ringRoll ? game._object_descriptions?.rings?.[item.ringRoll - 1] : '') || '').toLowerCase();
        if (USE_STONE_RING_APPEARANCE_COLORS.has(appearance))
            return USE_STONE_RING_APPEARANCE_COLORS.get(appearance);
    }
    const values = [
        item?.actualKind,
        item?.kind,
        item?.gemDescription,
        item?.displayName,
    ].map(value => String(value || '').toLowerCase().trim()).filter(Boolean);
    for (const value of values) {
        if (USE_STONE_GEM_COLORS.has(value)) return USE_STONE_GEM_COLORS.get(value);
        const gemDescription = value.match(/^(.+) gem$/)?.[1];
        if (gemDescription) return USE_STONE_DESCRIPTION_COLORS.get(gemDescription) || gemDescription;
    }
    const material = useStoneObjectMaterial(item);
    if (USE_STONE_MATERIAL_COLORS.has(material)) return USE_STONE_MATERIAL_COLORS.get(material);
    const displayColor = item ? object_display(item)?.color : undefined;
    if (USE_STONE_COLOR_NAMES.has(displayColor)) return USE_STONE_COLOR_NAMES.get(displayColor);
    return '';
}

function useStoneGemIdentityName(item) {
    const values = [
        item?.actualKind,
        item?.kind,
        item?.displayName,
        item?.gemDescription,
    ].map(value => String(value || '').toLowerCase().trim()
        .replace(/^an? /, '')
        .replace(/^uncursed |^blessed |^cursed /, ''))
        .filter(Boolean);
    for (const value of values) {
        if (value === 'gray stone' || value === 'grey stone') continue;
        if (USE_STONE_GEM_DISPLAY_NAMES.has(value)) return USE_STONE_GEM_DISPLAY_NAMES.get(value);
        if (USE_STONE_GEM_COLORS.has(value) || GRAY_STONE_APPLY_NAMES.has(value)) return value;
    }
    return '';
}

function recordKnownGemStoneDiscovery(name, appearance = '') {
    const discoveryName = String(name || '').toLowerCase().trim();
    if (!discoveryName) return;
    const suffix = appearance ? ` (${appearance})` : '';
    game._discoveries ??= [];
    const existing = game._discoveries.find(entry =>
        entry.section === 'Gems/Stones' && String(entry.name || '').toLowerCase() === discoveryName);
    if (existing) {
        existing.name = discoveryName;
        existing.text = `${discoveryName}${suffix}`;
        existing.starred = false;
        existing.known = true;
    } else {
        game._discoveries.push({
            section: 'Gems/Stones',
            name: discoveryName,
            text: `${discoveryName}${suffix}`,
            starred: false,
            known: true,
        });
    }
    learnObjectScore('Gems/Stones', discoveryName);
}

function identifyUseStoneSource(stone) {
    if (!stone) return;
    stone.known = true;
    stone.dknown = true;
    stone.kind = 'touchstone';
    stone.actualKind = 'touchstone';
    recordKnownGemStoneDiscovery('touchstone', 'gray');
    refreshInventoryObjectLine(stone);
}

function identifyUseStoneGem(target) {
    const name = useStoneGemIdentityName(target);
    if (!target || !name) return;
    const appearance = useStoneColorName(target);
    target.known = true;
    target.dknown = true;
    target.kind = name;
    target.actualKind = name;
    target.gemDescription = name;
    recordKnownGemStoneDiscovery(name, appearance);
    refreshInventoryObjectLine(target);
    if (target.unpaid) syncUnpaidBillLine(target);
}

async function beginUseStone(stone) {
    game._apply_stone_letter = stone?.letter || null;
    observeUseStoneSource(stone);
    await setMessage(useStonePromptMessage(stone));
    game._command_mode = 'applyStoneObject';
}

function useStoneGemShatterTarget(item) {
    return !!item && (itemClassKey(item) === 'gem' || item.glyph === '*') && !isGrayStoneApplyItem(item);
}

function useStoneTargetResistsShatter(item) {
    const chance = item?.artifact || item?.oartifact ? 100 : 80;
    return rn2(100) < chance;
}

async function shatterUseStoneTarget(stone, target) {
    if (!isTouchstoneApplyItem(stone) || !stone?.cursed || !useStoneGemShatterTarget(target))
        return false;
    if (useStoneTargetResistsShatter(target)) return false;

    if (game.u?.blind) {
        await setMessage('You feel something shatter.');
    } else if (heroIsHallucinating()) {
        await setMessage('Oh, wow, look at the pretty shards.');
    } else {
        const oneOf = Math.max(1, Math.trunc(Number(target?.quan || 1))) > 1 ? 'one of ' : '';
        await setMessage(`A sharp crack shatters ${oneOf}${theUseStoneObjectName(target)}.`);
    }
    useUpInventoryItem(target, 1);
    game.context.move = 1;
    return true;
}

function useStoneEffectMessage(stone, target) {
    const stoneIsTouchstone = isTouchstoneApplyItem(stone);
    let cls = useStoneObjectClass(target);
    const material = useStoneObjectMaterial(target);
    let doScratch = false;
    let streakColor = '';

    if (cls === 'ring' && material !== 'gemstone' && material !== 'mineral') cls = '';

    if (cls === 'gem' || cls === 'ring') {
        if (!stoneIsTouchstone) {
            doScratch = true;
        } else if (cls === 'gem' && useStoneHeroCanIdentify(stone)) {
            identifyUseStoneSource(stone);
            identifyUseStoneGem(target);
            return normalInventoryLine(target);
        } else if (material === 'glass') {
            doScratch = true;
        }
        streakColor = useStoneColorName(target);
    } else {
        if (material === 'cloth')
            return `The ${useStoneSubjectName(stone)} ${useStoneSurfaceVerb(stone, 'looks', 'look')} a little more polished now.`;
        if (material === 'liquid') {
            if (!target?.known) return 'You must think this is a wetstone, do you?';
            return `The ${useStoneSubjectName(stone)} ${useStoneSurfaceVerb(stone, 'is', 'are')} a little wetter now.`;
        }
        if (material === 'wax') streakColor = 'waxy';
        else if (material === 'wood') streakColor = 'wooden';
        else if (material === 'gold') {
            doScratch = true;
            streakColor = 'golden';
        } else if (material === 'silver') {
            doScratch = true;
            streakColor = 'silvery';
        } else {
            if (USE_STONE_FLIMSY_MATERIALS.has(material)) streakColor = useStoneColorName(target);
            else doScratch = !stoneIsTouchstone;
        }
    }

    const surface = useStoneSurfaceName(stone);
    if (doScratch) return `You make ${streakColor ? `${streakColor} ` : ''}scratch marks on the ${surface}.`;
    if (streakColor) return `You see ${streakColor} streaks on the ${surface}.`;
    return '"scritch, scritch"';
}

async function finishUseStone(stone, target) {
    game._apply_stone_letter = null;
    game._command_mode = null;
    if (target === stone && Math.max(1, Math.trunc(Number(target?.quan || 1))) === 1) {
        await setMessage(`You can't rub ${theUseStoneObjectName(target)} on itself.`);
        return;
    }
    if (await shatterUseStoneTarget(stone, target)) return;
    if (game.u?.blind) {
        await setMessage('"scritch, scritch"');
        game.context.move = 1;
        return;
    }
    if (heroIsHallucinating()) {
        await setMessage('Oh wow, man: Fractals!');
        game.context.move = 1;
        return;
    }
    await setMessage(useStoneEffectMessage(stone, target));
    game.context.move = 1;
}

function dropFlippedCoinAtHero(item) {
    if (!removeGoldFromHero(1)) return false;
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    game.level ??= {};
    game.level.objects ??= [];
    const pile = game.level.objects.find(obj => isApplyCoinObject(obj)
        && !obj.hidden && !obj.buried && obj.ox === x && obj.oy === y);
    if (pile) {
        pile.quan = Math.max(0, Math.trunc(Number(pile.quan || 0))) + 1;
        Object.assign(pile, object_display(pile), { ox: x, oy: y });
    } else {
        const coin = {
            id: next_ident(),
            cls: 'coin',
            otyp: GOLD_PIECE,
            glyph: '$',
            quan: 1,
            ox: x,
            oy: y,
            color: item?.color,
        };
        Object.assign(coin, object_display(coin), { ox: x, oy: y });
        game.level.objects.push(coin);
    }
    newsym(x, y);
    return true;
}

function applyCoinFlip(item) {
    const messages = ['You flip a gold piece.'];
    if (game.u?.underwater || game.u?.uunderwater) {
        messages.push('It tumbles away.');
        dropFlippedCoinAtHero(item);
        return messages;
    }
    const dex = Math.max(1, Math.trunc(Number(game.u?.acurr?.a?.[A_DEX] ?? game.u?.dex ?? 10)));
    const cursedGloves = !!wornGlovesItem()?.cursed;
    const slips = cursedGloves || heroHasSlipperyFingers() || heroIsFumbling()
        || (dex < 10 && !rn2(dex));
    if (slips) {
        messages.push(`It slips between your ${fingersOrGloves()}.`);
        dropFlippedCoinAtHero(item);
        return messages;
    }
    if (heroIsHallucinating()) {
        messages.push(rn2(100) ? 'Wow, a double header!' : 'The coin miraculously lands on its edge!');
        return messages;
    }
    messages.push(`It comes up ${rn2(2) ? 'heads' : 'tails'}.`);
    return messages;
}

export function inventoryLetterRank(item) {
    const letter = typeof item === 'string' ? item : item?.letter || '';
    return letter ? letter.charCodeAt(0) ^ 0x20 : 200;
}

function roleKnowsBlessCurse(item) {
    if (!item || item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') return false;
    return item.bknown === true || (game._startup_role || game.urole?.name?.m) === 'Priest';
}

function knownBlessCursePrefix(item) {
    if (!roleKnowsBlessCurse(item)) return '';
    if (item.blessed) return 'blessed ';
    if (item.cursed) return 'cursed ';
    return '';
}

function maybeAddKnownBlessCurse(item, phrase) {
    const prefix = knownBlessCursePrefix(item);
    const text = String(phrase || '');
    if (!prefix || /\b(?:blessed|uncursed|cursed)\b/.test(text) || /\b(?:holy|unholy) water\b/.test(text))
        return text;
    return text.replace(/^(a|an|the)\s+/i, `$1 ${prefix}`);
}

export function inventoryItemName(item) {
    if (item?.artifact) {
        if (item.line) {
            return maybeAddKnownBlessCurse(item, String(item.line)
                .replace(/^[a-zA-Z$] - /, '')
                .replace(/ \((?:weapon|wielded|alternate weapon|being worn|at the ready|in quiver|on .* hand).*$/, ''));
        }
        return maybeAddKnownBlessCurse(item, pickupObjectPhrase(item));
    }
    return maybeAddKnownBlessCurse(item, String(item.line || `${item.letter || '?'} - ${item.quan > 1 ? `${item.quan} ` : ''}${pickupObjectName(item)}`)
        .replace(/^[a-zA-Z$] - /, '')
        .replace(/ \((?:weapon|wielded|alternate weapon|being worn|at the ready|in quiver|on .* hand).*$/, ''));
}

const ARTIFACT_ALIGN_TYPE = { lawful: 1, neutral: 0, chaotic: -1 };
const ARTIFACT_TOUCH_METADATA = Object.freeze({
    Excalibur: { restricted: true, selfWilled: true, alignment: 1, role: 'Knight' },
    Stormbringer: { restricted: true, selfWilled: true, alignment: -1 },
    Mjollnir: { restricted: true, alignment: 0 },
    Cleaver: { restricted: true, alignment: 0 },
    Grimtooth: { restricted: true, alignment: -1 },
    Magicbane: { restricted: true, alignment: 0 },
    'Frost Brand': { restricted: true, alignment: null },
    'Fire Brand': { restricted: true, alignment: null },
    Dragonbane: { restricted: true, alignment: null },
    Demonbane: { restricted: true, alignment: 1 },
    Werebane: { restricted: true, alignment: null },
    Grayswandir: { restricted: true, alignment: 1 },
    Giantslayer: { restricted: true, alignment: 0 },
    Ogresmasher: { restricted: true, alignment: null },
    Trollsbane: { restricted: true, alignment: null },
    'Vorpal Blade': { restricted: true, alignment: 0 },
    Snickersnee: { restricted: true, alignment: 1 },
    Sunsword: { restricted: true, alignment: 1 },
    'The Orb of Detection': { restricted: true, selfWilled: true, alignment: 1, role: 'Archeologist' },
    'The Heart of Ahriman': { restricted: true, selfWilled: true, alignment: 0, role: 'Barbarian' },
    'The Sceptre of Might': { restricted: true, selfWilled: true, alignment: 1, role: 'Caveman' },
    'The Staff of Aesculapius': { restricted: true, selfWilled: true, alignment: 0, role: 'Healer' },
    'The Magic Mirror of Merlin': { restricted: true, selfWilled: true, alignment: 1, role: 'Knight' },
    'The Eyes of the Overworld': { restricted: true, selfWilled: true, alignment: 0, role: 'Monk' },
    'The Mitre of Holiness': { restricted: true, selfWilled: true, alignment: 1, role: 'Priest' },
    'The Longbow of Diana': { restricted: true, selfWilled: true, alignment: -1, role: 'Ranger' },
    'The Master Key of Thievery': { restricted: true, selfWilled: true, alignment: -1, role: 'Rogue' },
    'The Tsurugi of Muramasa': { restricted: true, selfWilled: true, alignment: 1, role: 'Samurai' },
    'The Platinum Yendorian Express Card': { restricted: true, selfWilled: true, alignment: 0, role: 'Tourist' },
    'The Orb of Fate': { restricted: true, selfWilled: true, alignment: 0, role: 'Valkyrie' },
    'The Eye of the Aethiopica': { restricted: true, selfWilled: true, alignment: 0, role: 'Wizard' },
});

function artifactTouchMetadata(def) {
    return def ? ARTIFACT_TOUCH_METADATA[def.name] || null : null;
}

function artifactAlignmentType(def) {
    if (!def) return null;
    const explicit = ARTIFACT_ALIGN_TYPE[def.alignment];
    if (explicit != null) return explicit;
    return artifactTouchMetadata(def)?.alignment ?? null;
}

function artifactIsRestricted(def) {
    return !!def?.restricted || !!def?.questArtifact || !!artifactTouchMetadata(def)?.restricted;
}

function artifactIsSelfWilled(def) {
    return !!def?.questArtifact || !!artifactTouchMetadata(def)?.selfWilled;
}

function artifactTouchRole(def) {
    return def?.questRole || artifactTouchMetadata(def)?.role || '';
}

function artifactTouchStatus(def) {
    if (!def) return { badClass: false, badAlign: false, selfWilled: false };
    const selfWilled = artifactIsSelfWilled(def);
    const heroRole = game._startup_role || game.urole?.name?.m || '';
    const role = artifactTouchRole(def);
    const badClass = selfWilled && !!role && role !== heroRole;
    const artifactAlign = artifactAlignmentType(def);
    const badAlign = artifactIsRestricted(def) && artifactAlign != null
        && (artifactAlign !== (game.u?.ualign?.type ?? 0) || (game.u?.ualign?.record ?? 0) < 0);
    return { badClass, badAlign, selfWilled };
}

function touchArtifact(item) {
    const def = artifactDefinitionForName(item?.artifact);
    const status = artifactTouchStatus(def);
    if (status.badAlign && !status.selfWilled) rn2(4);
}

function touchArtifactForWield(item) {
    touchArtifact(item);
}

function isProjectileItem(item) {
    const name = inventoryItemName(item).toLowerCase();
    return item.otyp === DART || item.otyp === FLINT_STONE || item.cls === 'gem'
        || /arrow|ya|dart|dagger|knife|spear|shuriken|boomerang|rock|stone/.test(name);
}

function isReadySuggestItem(item) {
    const name = inventoryItemName(item).toLowerCase();
    if (item.wielded || item.line?.includes('weapon in')) return false;
    if (/bow|sling/.test(name)) return false;
    return isProjectileItem(item);
}

function isThrowSuggestItem(item) {
    if (!item.letter) return false;
    if (item.wielded || item.line?.includes('weapon in')) return false;
    return item.cls === 'coin' || item.cls === 'weapon' || item.cls === 'gem'
        || item.cls === 'venom' || item.otyp === FLINT_STONE;
}

function isThrowDownplayItem(item) {
    return !!item?.letter && !isThrowSuggestItem(item);
}

function throwInventoryFilterMatch() {
    if (game._throw_inventory_filter === 'suggest') return isThrowSuggestItem;
    if (game._throw_inventory_filter === 'downplay') return isThrowDownplayItem;
    return null;
}

function throwCountTextValue(text) {
    return Math.trunc(Number(text || 0));
}

function clearThrowMenuCountState() {
    game._throw_menu_count_text = '';
    game._throw_menu_count_backspaced = false;
}

function clearThrowCountState() {
    game._throw_count_text = '';
    game._throw_count_backspaced = false;
    game._throw_count = null;
    clearThrowMenuCountState();
}

function throwSelectionCount() {
    const menuCount = throwCountTextValue(game._throw_menu_count_text);
    if (menuCount > 0) return menuCount;
    return throwCountTextValue(game._throw_count_text);
}

function throwCountRejectionMessage(item, throwCount) {
    if (throwCount <= 1) return '';
    const available = Math.max(0, Math.trunc(Number(item?.quan || 1)));
    const coins = shopBillableGold(item);
    if (coins && throwCount <= available) return '';
    const onlyOne = 'can only throw one at a time';
    return throwCount > available
        ? `You only have ${available}${!coins && available > 1 ? ` and ${onlyOne}` : ''}.`
        : `You ${onlyOne}.`;
}

function echoThrowMenuCount(text) {
    game._pending_message = `Count: ${text}`;
    game._message_more = 0;
}

function updateWornDisplacement() {
    game._has_displacement = (game.inventory || []).some(item => {
        const name = String(item.kind || item.line || '').toLowerCase();
        return item.cls === 'armor' && (item.worn || item.line?.includes('being worn')) && name.includes('cloak of displacement');
    });
}

function isWornInventoryItem(item) {
    return !!(item && (item.worn || item.line?.includes('being worn')));
}

function isActiveInventoryExtrinsicItem(item) {
    return !!(item && (isWornInventoryItem(item) || item._polyselfSkin));
}

function isBlueDragonArmorKind(kind) {
    return kind === 'blue dragon scale mail' || kind === 'blue dragon scales';
}

function isSilverDragonArmorKind(kind) {
    return kind === 'silver dragon scale mail' || kind === 'silver dragon scales';
}

function dragonArmorKindHasProperty(kind, property) {
    return !!DRAGON_ARMOR_SPEC_BY_KIND.get(kind)?.properties?.includes(property);
}

function polyselfFormHasAntimagic(form = game.u?._polyself_form) {
    const name = String(form?.name || '').toLowerCase();
    return !!(form?.magicResistance || form?.antimagic || form?.resistsMagic || form?.resists_magm
        || name === 'gray dragon' || name === 'grey dragon'
        || name === 'baby gray dragon' || name === 'baby grey dragon');
}

function polyselfFormHasColdResistance(form = game.u?._polyself_form) {
    const name = String(form?.name || '').toLowerCase();
    return !!(form?.coldResistance || form?.resistsCold || form?.resists_cold
        || name === 'white dragon' || name === 'baby white dragon');
}

function activeAntimagicSource() {
    return (game.inventory || []).find(item => {
        if (!isActiveInventoryExtrinsicItem(item)) return false;
        const kind = objectKindKey(item);
        return dragonArmorKindHasProperty(kind, 'antimagic') || kind === 'cloak of magic resistance';
    }) || null;
}

function activeSlowDigestionSource() {
    return (game.inventory || []).find(item => {
        const kind = objectKindKey(item);
        if ((item?.cls === 'ring' || item?.glyph === '=')
            && (item.worn || /\(on (?:left|right) hand\)/.test(item.line || ''))
            && (kind === 'ring of slow digestion' || ringNutritionName(item) === 'slow digestion'))
            return true;
        if (!isActiveInventoryExtrinsicItem(item)) return false;
        return item.slowDigestion || dragonArmorKindHasProperty(kind, 'slowDigestion');
    }) || null;
}

const ROLE_INTRINSIC_FAST_LEVELS = {
    Archeologist: 10,
    Barbarian: 7,
    Caveman: 7,
    Knight: 7,
    Monk: 1,
    Samurai: 1,
    Valkyrie: 7,
};

function heroHasIntrinsicFast() {
    const role = game?._startup_role || game?.urole?.name?.m;
    const threshold = ROLE_INTRINSIC_FAST_LEVELS[role];
    return !!game.u?._intrinsicFast || (!!threshold && (game.u?.ulevel || 1) >= threshold);
}

function syncHeroSpeedState() {
    if (!game.u) return;
    const wearingSpeedBoots = (game.inventory || []).some(item =>
        isWornInventoryItem(item) && String(item.kind || item.actualKind || '').toLowerCase() === 'speed boots');
    const wearingBlueDragonArmor = (game.inventory || []).some(item =>
        isActiveInventoryExtrinsicItem(item) && isBlueDragonArmorKind(String(item.kind || item.actualKind || '').toLowerCase()));
    game.u._blueDragonFast = wearingBlueDragonArmor;
    const veryFast = !!(wearingSpeedBoots || wearingBlueDragonArmor || (game.u._veryfastTimeout || 0) > 0);
    game.u.veryfast = veryFast;
    game.u.fast = heroHasIntrinsicFast() || veryFast;
}

function removeInventoryItem(item, amount = 1) {
    if (shopBillableGold(item)) {
        removeGoldFromHero(amount);
        updateWornDisplacement();
        return;
    }
    const wasWornSpeedChanger = item.cls === 'armor'
        && isWornInventoryItem(item)
        && (String(item.kind || '').toLowerCase() === 'speed boots'
            || isBlueDragonArmorKind(String(item.kind || '').toLowerCase()));
    const remaining = (item.quan || 1) - Math.max(1, amount | 0);
    if (remaining > 0) {
        item.quan = remaining;
        item.line = normalInventoryLine({ ...item, line: '' });
        if (item.unpaid) syncUnpaidBillLine(item);
    } else {
        stopCarriedFigurineTimerOnLeave(item);
        game.inventory = (game.inventory || []).filter(other => other !== item);
    }
    if (wasWornSpeedChanger) syncHeroSpeedState();
    updateWornDisplacement();
    game._pet_food_scan_inventory = game.inventory;
}

function useUpInventoryItem(item, amount = 1) {
    if (!item) return false;
    const usedCount = Math.max(1, Math.trunc(Number(amount || 1)));
    const quantity = Math.max(1, Math.trunc(Number(item.quan || 1)));
    if (usedCount >= quantity) markObjectTreeShopBillsUsedUp(item);
    removeInventoryItem(item, usedCount);
    return true;
}

function splitOneCarriedInventoryItem(item) {
    if (!item || (item.quan || 1) <= 1) return item;
    const split = { ...item, id: next_ident(), quan: 1, line: '' };
    delete split.o_id;
    delete split._shopBillObjectId;
    split.letter = nextInventoryLetter();
    if (item.unpaid && !splitCarriedObjectShopBill(item, split, 1))
        clearObjectShopBillState(split);
    item.quan = (item.quan || 1) - 1;
    refreshInventoryObjectLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
    refreshInventoryObjectLine(split);
    game.inventory ??= [];
    game.inventory.push(split);
    return split;
}

function splitCarriedInventoryItemCount(item, count) {
    if (!item) return item;
    const quantity = Math.max(1, Math.trunc(Number(item.quan || 1)));
    const splitCount = Math.max(1, Math.trunc(Number(count || 1)));
    if (splitCount >= quantity) return item;
    const split = { ...item, id: next_ident(), quan: splitCount, line: '' };
    delete split.o_id;
    delete split._shopBillObjectId;
    split.letter = nextInventoryLetter();
    if (item.unpaid && !splitCarriedObjectShopBill(item, split, splitCount))
        clearObjectShopBillState(split);
    item.quan = quantity - splitCount;
    refreshInventoryObjectLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
    refreshInventoryObjectLine(split);
    game.inventory ??= [];
    game.inventory.push(split);
    return split;
}

function splitCarriedCreamPieForSplat(item) {
    return splitOneCarriedInventoryItem(item);
}

function billDummyAlteredCarriedObject(obj) {
    if (!obj || shopBillableGold(obj)) return false;
    const owner = shopkeeperOwningBillEntry(obj);
    const shkp = owner.shkp || heroShopkeeper();
    const entry = owner.entry || (shkp ? shopBillEntryForObject(shkp, obj) : null);
    if (!shkp || !entry) return false;
    const price = shopBillEntryTotal(entry);
    if (!(price > 0)) return false;
    const dummy = {
        ...obj,
        id: next_ident(),
        o_id: undefined,
        letter: undefined,
        line: undefined,
        contained: false,
        container: null,
        contents: [],
        cobj: [],
        nobj: null,
        nexthere: null,
        ox: undefined,
        oy: undefined,
    };
    if (entry) removeObjectFromShopBillById(shkp, entry.bo_id);
    const dummyEntry = addObjectToShopBill(shkp, dummy, price, { useup: true });
    if (!dummyEntry) return false;
    rememberUsedUpShopBillEntry(obj, dummyEntry, price, shkp);
    clearObjectShopBillState(obj);
    return true;
}

function markAlteredShopObjectNoCharge(obj) {
    if (!obj || shopBillableGold(obj)) return false;
    obj.no_charge = true;
    clearObjectShopBillState(obj);
    return true;
}

function finishCreamPieSplat(item) {
    if (!item) return false;
    const pie = splitCarriedCreamPieForSplat(item);
    const billed = billDummyAlteredCarriedObject(pie);
    removeInventoryItem(pie);
    return billed;
}

export function consumeLifeSavingAmulet({ clearStoning = false } = {}) {
    const lifesaving = (game.inventory || []).find(item =>
        item.worn && String(item.kind || item.actualKind || item.line || '').includes('life saving'));
    if (!lifesaving) return false;
    rn2(19);
    removeInventoryItem(lifesaving);
    game._life_saving_refresh_con = 1;
    if (clearStoning) game._life_saving_clear_stoning = 1;
    return true;
}

function clearUnsafePetrifyingCorpseWieldAfterLifeSaving() {
    if (game.u?.stoneResistance || wornGlovesItem()) return;
    let clearedAlternate = false;
    for (const item of game.inventory || []) {
        if (!isPetrifyingCorpseObject(item)) continue;
        if (!itemIsWielded(item) && !item.alternate && !item.line?.includes('alternate weapon')) continue;
        if (item.alternate || item.line?.includes('alternate weapon')) clearedAlternate = true;
        item.wielded = false;
        item.alternate = false;
        item.line = normalInventoryLine({ ...item, line: '', wielded: false, alternate: false });
    }
    if (clearedAlternate) game._twoweapon = false;
}

function stopCarriedFigurineTimerOnLeave(item) {
    if (isFigurineObject(item)) stopFigurineTransformTimeout(item);
}

function wornArmorFireSlot(item) {
    const name = inventoryItemName(item).toLowerCase();
    if (/helm|helmet|hat|fedora|cornuthaum|cap|pot/.test(name)) return 0;
    if (/cloak|mail|armor|shirt|robe/.test(name)) return 1;
    if (/shield/.test(name)) return 2;
    if (/gloves|gauntlets/.test(name)) return 3;
    if (/boots|shoes/.test(name)) return 4;
    return 1;
}

function fireTrapOwnerSubject(ownerPrefix) {
    const owner = String(ownerPrefix || 'your');
    return owner === 'your' ? 'Your' : sentenceCase(owner);
}

function fireTrapArmorName(item) {
    const kind = armorKind(item);
    switch (armorSlot(item)) {
    case 'cloak':
        if (kind.includes('robe')) return 'robe';
        if (kind.includes('wrapping')) return 'wrapping';
        if (kind.includes('smock')) return 'smock';
        if (kind.includes('apron')) return 'apron';
        return 'cloak';
    case 'shirt':
        return 'shirt';
    default:
        return armorMessageName(item);
    }
}

export function erodeArmorByFireTrap(item, {
    messages = null,
    ownerPrefix = 'your',
    displayName = '',
    updateHeroInventory = false,
    visible = true,
} = {}) {
    if (!item) return false;
    const profile = wishedDamageProfile(item);
    if (!profile.erosionMatters || profile.primaryWord !== 'burnt') return false;

    const name = displayName || fireTrapArmorName(item);
    if (item.oerodeproof) {
        if (!item.rknown && visible && messages && game.flags?.verbose !== false) {
            messages.push(`Somehow, ${ownerPrefix} ${name} ${fireInventoryNameVerb(name, 'is', 'are')} not affected by the heat.`);
        }
        item.rknown = true;
        if (updateHeroInventory) updateArmorLine(item);
        return false;
    }
    if (item.blessed && !rnl(4)) return false;

    const current = Math.min(3, item.oeroded || 0);
    if (current >= 3) return false;

    const oldAc = updateHeroInventory && isWornArmorItem(item) ? wornArmorAcValueGreatestErosion(item) : null;
    item.oeroded = current + 1;
    if (updateHeroInventory) {
        if (oldAc != null) updateWornArmorAcAfterChange(item, oldAc);
        else updateArmorLine(item);
    }

    if (visible && messages) {
        const adverb = item.oeroded === 3 ? ' completely' : current ? ' further' : '';
        messages.push(`${fireTrapOwnerSubject(ownerPrefix)} ${name} ${fireInventoryNameVerb(name, 'smoulders', 'smoulder')}${adverb}!`);
    }
    return true;
}

function towelWetness(item) {
    return Math.max(0, item?.spe || item?.wetness || 0);
}

export function dryWetTowelFromFire(items, {
    messages = null,
    ownerPrefix = 'your',
    updateInventory = false,
    visible = true,
} = {}) {
    for (const item of items || []) {
        if (objectKindKey(item) !== 'towel') continue;
        const oldWetness = towelWetness(item);
        if (oldWetness <= 0) continue;
        const newWetness = rn2(oldWetness + 1);
        if (item.spe != null || !('wetness' in item)) item.spe = newWetness;
        if (item.wetness != null) item.wetness = newWetness;
        if (updateInventory) item.line = normalInventoryLine({ ...item, line: '' });
        if (newWetness >= oldWetness) continue;
        if (visible && messages) {
            messages.push(`${fireTrapOwnerSubject(ownerPrefix)} towel dries${newWetness ? '' : ' out'}.`);
        }
        return true;
    }
    return false;
}

function burnWornArmorFromFire({ updateHeroInventory = true } = {}) {
    const messages = [];
    dryWetTowelFromFire(game.inventory || [], { messages, updateInventory: updateHeroInventory });
    const wornArmor = (game.inventory || []).filter(item =>
        item.cls === 'armor' && (item.worn || item.line?.includes('being worn')));
    for (;;) {
        const slot = rn2(5);
        let item = wornArmor.find(armor => wornArmorFireSlot(armor) === slot);
        if (slot === 1)
            item = wornArmor.find(armor => /cloak|robe|wrapping|smock|apron/i.test(inventoryItemName(armor)))
                || wornArmor.find(armor => wornArmorFireSlot(armor) === slot);
        if (slot === 1) {
            if (item) erodeArmorByFireTrap(item, {
                messages,
                updateHeroInventory,
                displayName: fireTrapArmorName(item),
            });
            return { bodyHit: true, message: messages.join('  ') };
        }
        if (!item) continue;
        if (!erodeArmorByFireTrap(item, {
            messages,
            updateHeroInventory,
            displayName: fireTrapArmorName(item),
        })) continue;
        return { bodyHit: false, message: messages.join('  ') };
    }
}

function fireInventoryNameVerb(name, singular, plural) {
    return /\b(?:boots|shoes|gloves|gauntlets|scales|arrows|darts|bolts|candles|scrolls|potions|globs)\b/i.test(name)
        && !/\bmail\b/i.test(name) ? plural : singular;
}

function addFireInventoryMessage(messages, events, text, event, armor, joinState) {
    if (!joinState.joinedArmorMessage && messages.length === 1 && armor.message) {
        messages[0] = `${messages[0]}  ${text}`;
        events.push({ ...event, text: messages[0] });
        joinState.joinedArmorMessage = true;
        return;
    }
    messages.push(text);
    events.push({ ...event, text });
}

function isGreenSlimeGlobItem(item) {
    return item?.otyp === GLOB_OF_GREEN_SLIME
        || (item?.globby && /\bgreen slime\b/i.test(objectKindKey(item) || pickupObjectName(item)));
}

function fireInventoryItemDamage(item, cls) {
    if (cls === 'potion') return rnd(6);
    if (cls === 'slime') return Math.trunc(((item.owt || 20) + 19) / 20);
    return game.u?.fireResistance ? 0 : 1;
}

function fireInventoryDeathCause(cls, item, plural) {
    if (cls === 'slime') return plural
        ? 'killed by exploding globs of slime'
        : 'killed by an exploding glob of slime';
    if (cls === 'potion') {
        const oil = isPotionOfOil(item);
        return oil
            ? plural ? 'killed by exploding potions' : 'killed by exploding potion'
            : plural ? 'killed by boiling potions' : 'killed by boiling potion';
    }
    if (cls === 'spellbook') return 'killed by burning book';
    return plural ? 'killed by burning scrolls' : 'killed by burning scroll';
}

function fireInventoryDestroyVerb(cls, item, plural) {
    if (cls === 'potion' && isPotionOfOil(item))
        return plural ? 'ignite and explode' : 'ignites and explodes';
    if (cls === 'potion' || cls === 'slime')
        return plural ? 'boil and explode' : 'boils and explodes';
    return plural ? 'catch fire and burn' : 'catches fire and burns';
}

function coldDestroyablePotion(item) {
    return isPotionObject(item)
        && !isPotionOfOil(item)
        && !item?.artifact
        && !item?.oartifact
        && !(item?.in_use && (item?.quan || 1) === 1);
}

function coldInventoryProtectionChance() {
    const coldGear = (game.inventory || []).some(item => activeInventoryResistanceKind(item) === 'cold');
    if (coldGear) return 99;
    const dwarvishCloak = (game.inventory || []).some(item =>
        isWornInventoryItem(item) && objectKindKey(item) === 'dwarvish cloak');
    return dwarvishCloak ? 90 : 0;
}

function coldInventoryItemProtected() {
    const chance = coldInventoryProtectionChance();
    return chance ? rn2(100) < chance : false;
}

function coldDestroyInventorySelection(items, origDamage) {
    let limit = Math.trunc(origDamage / 5);
    if (origDamage % 5 > rn2(5)) limit++;
    if (limit < 1) return [];
    limit = Math.min(20, limit);

    const selected = [];
    let eligible = 0;
    for (const item of [...(items || [])]) {
        if (!coldDestroyablePotion(item)) continue;
        const i = eligible < limit ? eligible : rn2(eligible);
        eligible++;
        if (i < limit) selected[i] = item;
    }
    return selected.filter(Boolean);
}

function coldInventoryDestroyVerb(plural) {
    return plural ? 'freeze and shatter' : 'freezes and shatters';
}

function coldInventoryDeathCause(plural) {
    return plural ? 'killed by shattered potions' : 'killed by a shattered potion';
}

function coldPotionDisplayName(item, plural) {
    const display = { ...item, line: '', quan: plural ? Math.max(2, item.quan || 1) : 1 };
    const name = pickupObjectName(display);
    if (plural && String(item?.kind || '').endsWith(' potion') && name === item.kind)
        return `${item.kind.replace(/ potion$/, '')} potions`;
    return name;
}

function coldDamageInventory(origDamage) {
    const messages = [];
    let damage = 0;
    let deathCause = '';
    for (const item of coldDestroyInventorySelection(game.inventory || [], origDamage)) {
        if (coldInventoryItemProtected()) continue;
        const itemDamage = rnd(4);
        const quan = Math.max(0, (item.quan || 1) - (item.in_use ? 1 : 0));
        let destroyed = 0;
        for (let i = 0; i < quan; i++)
            if (!rn2(3)) destroyed++;
        if (!destroyed) continue;

        const plural = destroyed > 1;
        const name = coldPotionDisplayName(item, plural);
        const subject = destroyed === 1 && quan === 1 ? `Your ${name}`
            : destroyed === 1 ? `One of your ${name}`
                : destroyed < quan ? `Some of your ${name}`
                    : quan === 2 ? `Both of your ${name}`
                        : `All of your ${name}`;
        messages.push(`${subject} ${coldInventoryDestroyVerb(plural)}!`);
        removeInventoryItem(item, destroyed);
        damage += itemDamage;
        deathCause = coldInventoryDeathCause(plural);
    }
    return { messages, damage, deathCause };
}

function fireItemCanCatchLight(item) {
    if (!item || item.lamplit || item.burning || item.in_use) return false;
    const kind = objectKindKey(item);
    if (item.otyp === BRASS_LANTERN || kind === 'brass lantern') return false;
    if (isPotionOfOil(item)) return (item.age ?? 400) !== 0;
    if (item.otyp === OIL_LAMP || kind === 'oil lamp') return (item.age ?? 1500) !== 0;
    if (item.otyp === MAGIC_LAMP || kind === 'magic lamp') return (item.spe ?? 1) > 0;
    if (isCandleObject(item)) return (item.age ?? candleDefaultAge(item)) !== 0;
    if (item.otyp === CANDELABRUM_OF_INVOCATION || kind === 'candelabrum of invocation')
        return (item.spe ?? 0) > 0 && !item.cursed;
    return false;
}

function maybeIgniteFireItem(item, messages, events, armor, joinState) {
    if (!fireItemCanCatchLight(item)) return false;
    const kind = objectKindKey(item);
    if ((item.otyp === OIL_LAMP || item.otyp === MAGIC_LAMP || kind === 'oil lamp' || kind === 'magic lamp')
        && item.cursed && !rn2(2))
        return false;
    const name = pickupObjectName({ ...item, line: '' });
    const message = game.u?.blind
        ? `Your ${name} ${fireInventoryNameVerb(name, 'feels', 'feel')} warm.`
        : `Your ${name} ${fireInventoryNameVerb(name, 'catches', 'catch')} light!`;
    beginWishedBurn(item);
    item.line = normalInventoryLine({ ...item, line: '' });
    addFireInventoryMessage(messages, events, message, { damage: 0 }, armor, joinState);
    if (item.unpaid) {
        const shkp = heroShopkeeper();
        if (shkp) {
            checkUnpaidUsage(item, messages, { chargeCount: lampUsageChargeCount(item) });
            const pronoun = (item.quan || 1) === 1 ? 'itself' : 'themselves';
            messages.push(`"That's in addition to the cost of ${pickupObjectName({ ...item, line: '' })} ${pronoun}, of course."`);
            markObjectShopBillUsedUp(item, shkp);
        }
    }
    return true;
}

function igniteFireInventoryItems(messages, events, armor, joinState) {
    for (const item of [...(game.inventory || [])])
        maybeIgniteFireItem(item, messages, events, armor, joinState);
}

function fireFloorItemMessage(item, destroyed) {
    if (destroyed > 1) {
        const name = pickupObjectName({ ...item, line: '', quan: Math.max(2, item.quan || destroyed) });
        return `${destroyed} ${name} burn.`;
    }
    const name = pickupObjectName({ ...item, line: '', quan: 1 });
    return `${sentenceCase(articleFor(name))} burns.`;
}

function maybeIgniteFloorFireItem(item, messages, giveFeedback) {
    if (!fireItemCanCatchLight(item)) return false;
    const kind = objectKindKey(item);
    if ((item.otyp === OIL_LAMP || item.otyp === MAGIC_LAMP || kind === 'oil lamp' || kind === 'magic lamp')
        && item.cursed && !rn2(2))
        return false;
    const name = pickupObjectName({ ...item, line: '' });
    beginWishedBurn(item);
    if (giveFeedback) {
        const many = (item.quan || 1) > 1;
        const subject = many ? `The ${name}` : sentenceCase(articleFor(name));
        messages.push(`${subject} ${fireInventoryNameVerb(name, 'catches', 'catch')} light!`);
    }
    return true;
}

export function burnFloorObjectsByFire(x, y, {
    giveFeedback = false,
    igniteFeedback = giveFeedback,
    heroCaused = false,
} = {}) {
    if (!game.level) return { count: 0, messages: [] };
    const messages = [];
    let count = 0;
    let changed = false;
    const atSquare = obj => obj && !obj.transientProjectile && !obj.buried && obj.ox === x && obj.oy === y;

    for (const obj of [...(game.level.objects || [])]) {
        if (!atSquare(obj)) continue;
        const cls = fireDestroyableInventoryClass(obj);
        if (!['scroll', 'spellbook', 'slime'].includes(cls) || fireInventoryItemImmune(obj, cls))
            continue;
        const quan = Math.max(1, obj.quan || 1);
        let destroyed = 0;
        for (let i = 0; i < quan; i++)
            if (!rn2(3)) destroyed++;
        if (!destroyed) continue;
        if (giveFeedback) messages.push(fireFloorItemMessage(obj, destroyed));
        count += destroyed;
        changed = true;
        if (heroCaused) {
            useUpFloorObject(obj, destroyed, { heroCaused: true });
        } else {
            const remaining = quan - destroyed;
            if (remaining > 0) obj.quan = remaining;
            else game.level.objects = (game.level.objects || []).filter(item => item !== obj);
        }
    }

    for (const obj of [...(game.level.objects || [])]) {
        if (atSquare(obj) && maybeIgniteFloorFireItem(obj, messages, igniteFeedback))
            changed = true;
    }
    if (changed) newsym(x, y);
    return { count, messages };
}

export function burnRayFloorObjectsByFire(x, y, { heroCaused = true } = {}) {
    const floorVisible = !game.u?.blind && !!(game.viz_array?.[y]?.[x] & IN_SIGHT);
    const floorFire = burnFloorObjectsByFire(x, y, { igniteFeedback: floorVisible, heroCaused });
    const messages = [...floorFire.messages];
    if (floorFire.count && couldsee(x, y))
        messages.push(`You ${game.u?.blind ? 'smell a whiff' : 'see a puff'} of smoke.`);
    return messages;
}

function liquidFlowFloorObjectsAt(x, y) {
    return (game.level?.objects || []).filter(obj =>
        obj && !obj.transientProjectile && !obj.buried && obj.ox === x && obj.oy === y);
}

function removeFloorObject(obj) {
    game.level.objects = (game.level?.objects || []).filter(item => item !== obj);
}

function splitFloorObjectForUseUp(obj, count) {
    const usedCount = Math.max(1, Math.trunc(Number(count || 1)));
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if (!obj || quantity <= usedCount) return obj;
    const split = { ...obj, id: next_ident(), quan: usedCount };
    obj.quan = quantity - usedCount;
    game.level.objects ??= [];
    game.level.objects.push(split);
    return split;
}

function billHeroCausedFloorUseUp(obj, precomputedPrice = null) {
    if (!obj || obj.no_charge || shopBillableGold(obj)) return false;
    const x = obj.ox ?? game.u?.ux;
    const y = obj.oy ?? game.u?.uy;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return false;
    const price = precomputedPrice ?? shopItemPrice(obj, x, y);
    if (!(price > 0)) return false;
    const heroRoomShkp = shopkeeperForShopRoom(game.u?.ux, game.u?.uy);
    if (sameShopkeeper(shkp, heroRoomShkp)) {
        if (!shopBillEntryForObject(shkp, obj))
            addObjectToShopBill(shkp, obj, price, { useup: true });
        return markObjectShopBillUsedUp(obj, shkp);
    }
    shkp.robbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0))) + price;
    return true;
}

function useUpFloorObject(obj, count, { heroCaused = false } = {}) {
    if (!obj) return null;
    const usedCount = Math.max(1, Math.trunc(Number(count || 1)));
    const precomputedPrice = heroCaused
        ? shopItemPrice({ ...obj, quan: Math.min(usedCount, Math.max(1, Math.trunc(Number(obj.quan || 1)))) }, obj.ox, obj.oy)
        : null;
    const usedObj = splitFloorObjectForUseUp(obj, count);
    if (heroCaused) billHeroCausedFloorUseUp(usedObj, precomputedPrice);
    removeFloorObject(usedObj);
    return usedObj;
}

function floorEffectRemoveObject(removeObject, usedUpShopBillOnDestroy = false) {
    if (!usedUpShopBillOnDestroy) return removeObject;
    return obj => {
        markObjectTreeShopBillsUsedUp(obj);
        removeObject(obj);
    };
}

function liquidFlowContainerContents(obj) {
    const contents = Array.isArray(obj?.contents) ? obj.contents : [];
    const cobj = Array.isArray(obj?.cobj) ? obj.cobj : [];
    if (contents.length && cobj.length) return [...new Set([...contents, ...cobj])];
    if (contents.length) return contents;
    if (cobj.length) return cobj;
    if (Array.isArray(obj?.contents)) return obj.contents;
    if (Array.isArray(obj?.cobj)) return obj.cobj;
    return [];
}

function clearLiquidFlowContainerContents(obj) {
    if (Array.isArray(obj?.contents)) obj.contents.length = 0;
    if (Array.isArray(obj?.cobj)) obj.cobj.length = 0;
}

function removeContainedObject(container, obj) {
    if (Array.isArray(container?.contents))
        container.contents = container.contents.filter(item => item !== obj);
    if (Array.isArray(container?.cobj))
        container.cobj = container.cobj.filter(item => item !== obj);
}

function isLiquidFlowContainer(obj) {
    if (!obj) return false;
    if (obj.otyp === LARGE_BOX || obj.otyp === CHEST || obj.otyp === ICE_BOX || BAG_OBJECT_TYPES.has(obj.otyp)) return true;
    const kind = objectKindKey(obj);
    return /^(?:large box|chest|ice box|sack|oilskin sack|bag of holding|bag)$/.test(kind);
}

function isLiquidFlowWaterproofContainer(obj) {
    return obj?.otyp === LARGE_BOX || obj?.otyp === CHEST || obj?.otyp === ICE_BOX || obj?.otyp === OILSKIN_SACK
        || /^(?:large box|chest|ice box|oilskin sack)$/.test(objectKindKey(obj));
}

function placeLiquidFlowFloorObject(obj, x, y) {
    if (!obj || !game.level) return;
    obj.contained = false;
    obj.ox = x;
    obj.oy = y;
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    delete obj.line;
    Object.assign(obj, object_display(obj));
    if (!(game.level.objects || []).includes(obj)) game.level.objects.push(obj);
}

function floorObjectVisible(x, y) {
    return !game.u?.blind && couldsee(x, y);
}

function objectArticleName(name) {
    const text = String(name || '').trim();
    if (!text) return 'a thing';
    if (/^(?:the|a|an)\b/i.test(text)) return text;
    if (/^pair of\b/i.test(text)) return `${/^[aeiou]/i.test(text) ? 'an' : 'a'} ${text}`;
    const buc = text.match(/^(blessed|uncursed|cursed)\s+(.+)$/i);
    if (buc && pairArticleObjectName(buc[2])) return `${/^[aeiou]/i.test(text) ? 'an' : 'a'} ${buc[1]} pair of ${buc[2]}`;
    if (pairArticleObjectName(text)) return `a pair of ${text}`;
    return articleFor(text);
}

function floorObjectSubject(obj) {
    const quan = Math.max(1, obj?.quan || 1);
    const name = pickupObjectName({ ...obj, line: '', quan });
    return quan > 1 ? quantityObjectName(obj, name, quan) : sentenceCase(objectArticleName(name));
}

function floorObjectArticleName(obj) {
    const quan = Math.max(1, obj?.quan || 1);
    const name = pickupObjectName({ ...obj, line: '', quan });
    return quan > 1 ? quantityObjectName(obj, name, quan) : objectArticleName(name);
}

function quantityObjectName(obj, name = pickupObjectName(obj), quan = Math.max(1, obj?.quan || 1)) {
    const text = String(name || '').trim();
    if (/^\d+\b/.test(text)) return text;
    return `${quan} ${text || (obj?.plural || 'objects')}`;
}

function floorObjectBaseName(obj) {
    return pickupObjectName({ ...obj, line: '', quan: 1 }).replace(/ \(lit\)$/, '');
}

function floorObjectTheName(obj) {
    const name = floorObjectBaseName(obj);
    return /^the\b/i.test(name) ? name : `the ${name}`;
}

function floorObjectTheSubject(obj) {
    const quan = Math.max(1, obj?.quan || 1);
    const name = pickupObjectName({ ...obj, line: '', quan }).replace(/ \(lit\)$/, '');
    return upstartText(/^the\b/i.test(name) ? name : `the ${name}`);
}

function floorObjectVerb(obj, singular, plural) {
    const name = floorObjectBaseName(obj);
    return (obj?.quan || 1) > 1 ? plural : rustTrapNameVerb(name, singular, plural);
}

function acidPotionItem(obj) {
    const kind = objectKindKey(obj).replace(/^potion of /, '');
    return obj?.otyp === POT_ACID || obj?.potionIndex === 23 || kind === 'acid';
}

function waterDamageFloorLitObject(obj, messages, visible) {
    if (!(obj?.lamplit || obj?.burning)) return false;
    if (visible) {
        const subject = floorObjectSubject(obj);
        messages.push(`${subject} ${floorObjectVerb(obj, 'goes', 'go')} out!`);
    }
    obj.lamplit = false;
    obj.burning = false;
    delete obj._burnTimer;
    delete obj.litRadius;
    return true;
}

function blankFloorScroll(obj) {
    obj.otyp = SCR_BLANK_PAPER;
    obj.kind = 'blank paper';
    obj.actualKind = '';
    obj.scrollIndex = IDENTIFIED_SCROLL_NAMES.length;
    obj.spe = 0;
    obj.known = false;
}

function blankFloorSpellbook(obj) {
    if (obj.spestudied) obj.spestudied = rn2(obj.spestudied);
    obj.kind = 'spellbook of blank paper';
    obj.actualKind = '';
    obj.spellName = '';
    obj.spell = null;
    obj.known = false;
}

function explodeFloorAcidPotion(obj, messages, acidContext, removeObject = removeFloorObject) {
    const plural = (obj?.quan || 1) > 1;
    const previous = acidContext.count++;
    const prefix = previous ? (plural ? 'More' : 'Another') : (plural ? 'Some' : 'A');
    messages.push(`${prefix} potion${plural ? 's' : ''} ${plural ? 'explode' : 'explodes'}!`);
    removeObject(obj);
    return true;
}

function waterDamageFloorPotion(obj, messages, acidContext, removeObject = removeFloorObject) {
    if (acidPotionItem(obj)) return explodeFloorAcidPotion(obj, messages, acidContext, removeObject);
    if (isWaterPotion(obj)) return false;
    if (obj.odiluted) {
        obj.otyp = POT_WATER;
        obj.kind = 'water';
        obj.actualKind = '';
        obj.blessed = false;
        obj.cursed = false;
        obj.odiluted = false;
        obj.known = false;
    } else {
        obj.odiluted = true;
    }
    return true;
}

function erodeFloorObject(obj, messages, visible, {
    profileWord,
    field,
    action,
    cause,
    destroyAtMax = false,
    removeObject = removeFloorObject,
}) {
    const profile = wishedDamageProfile(obj);
    if (!profile.erosionMatters || profile.primaryWord !== profileWord) return false;
    const name = floorObjectBaseName(obj);
    if (obj.oerodeproof || obj.rustproof) {
        if (visible && game.flags?.verbose !== false)
            messages.push(`Somehow, the ${name} ${rustTrapNameVerb(name, 'is', 'are')} not affected by the ${cause}.`);
        obj.rknown = true;
        return false;
    }
    if (obj.blessed && !rnl(4)) return false;

    const current = Math.min(3, obj[field] || 0);
    if (current < 3) {
        obj[field] = current + 1;
        if (visible) {
            const adverb = obj[field] === 3 ? ' completely' : current ? ' further' : '';
            messages.push(`The ${name} ${rustTrapNameVerb(name, `${action}s`, action)}${adverb}!`);
        }
        return true;
    }
    if (destroyAtMax) {
        if (visible) messages.push(`The ${name} ${action}s away!`);
        removeObject(obj);
        return true;
    }
    return false;
}

function waterDamageContainerContents(container, messages, visible, acidContext, {
    usedUpShopBillOnDestroy = false,
} = {}) {
    const contents = [...liquidFlowContainerContents(container)];
    const waterproof = isLiquidFlowWaterproofContainer(container);
    if (waterproof) {
        if (!container.cursed) return true;
        if (rn2(3)) return true;
    }
    let damaged = true;
    for (const content of contents) {
        damaged = waterDamageFloorItem(content, messages, visible, acidContext, {
            removeObject: item => removeContainedObject(container, item),
            usedUpShopBillOnDestroy,
        }) || damaged;
    }
    return damaged;
}

function waterDamageFloorItem(obj, messages, visible, acidContext, {
    removeObject = removeFloorObject,
    usedUpShopBillOnDestroy = false,
} = {}) {
    const destroyObject = floorEffectRemoveObject(removeObject, usedUpShopBillOnDestroy);
    if (waterDamageFloorLitObject(obj, messages, visible)) return true;

    const kind = objectKindKey(obj);
    if (kind === 'can of grease' && (obj.spe || 0) > 0) return false;
    if (kind === 'towel' && (obj.spe || 0) < 7) {
        const wetness = Math.max(0, obj.spe || 0);
        obj.spe = wetness + rnd(7 - wetness);
        obj.wetness = obj.spe;
        return false;
    }
    if (obj.greased) {
        if (!rn2(2)) {
            obj.greased = false;
            if (rustTrapItemClass(obj) === 'potion' && acidPotionItem(obj))
                return explodeFloorAcidPotion(obj, messages, acidContext, removeObject);
        }
        return true;
    }
    if (isLiquidFlowContainer(obj))
        return waterDamageContainerContents(obj, messages, visible, acidContext, { usedUpShopBillOnDestroy });
    if (((game.u?.uluck || 0) + (game.u?.moreluck || 0) + 5) > rn2(20)) return false;

    const cls = rustTrapItemClass(obj);
    if (cls === 'scroll') {
        if (isBlankScrollItem(obj)) return false;
        blankFloorScroll(obj);
        return true;
    }
    if (cls === 'spellbook') {
        if (isBookOfTheDeadItem(obj)) {
            if (visible) messages.push(`Steam rises from ${floorObjectTheName(obj)}.`);
            return false;
        }
        if (isBlankSpellbookItem(obj)) return false;
        blankFloorSpellbook(obj);
        return true;
    }
    if (cls === 'potion') return waterDamageFloorPotion(obj, messages, acidContext, destroyObject);

    return erodeFloorObject(obj, messages, visible, {
        profileWord: 'rusty',
        field: 'oeroded',
        action: 'rust',
        cause: 'oxidation',
        removeObject: destroyObject,
    });
}

function fireDamageFloorContainer(obj, messages, visible, {
    removeObject = removeFloorObject,
    spillQueue = [],
    processSpill = null,
    x,
    y,
} = {}) {
    if (!isLiquidFlowContainer(obj)) return false;
    if (obj.otyp === ICE_BOX || obj.otyp === STATUE || objectKindKey(obj) === 'ice box')
        return false;
    const contents = [...liquidFlowContainerContents(obj)];
    if (visible) messages.push(`${floorObjectSubject(obj)} catches fire and burns.`);
    if (contents.length && visible) messages.push('Its contents fall out.');
    for (const content of contents) {
        removeContainedObject(obj, content);
        placeLiquidFlowFloorObject(content, x, y);
        if (processSpill) processSpill(content);
        else spillQueue.push({ obj: content, mode: 'flooreffects' });
    }
    clearLiquidFlowContainerContents(obj);
    removeObject(obj);
    return true;
}

function fireDamageFloorScrollOrBook(obj, messages, visible, cls, removeObject = removeFloorObject) {
    if (cls === 'spellbook' && isBookOfTheDeadItem(obj)) {
        if (visible) messages.push(`Smoke rises from ${floorObjectTheName(obj)}.`);
        return false;
    }
    if (fireInventoryItemImmune(obj, cls)) {
        return false;
    }
    if (visible) {
        const subject = floorObjectSubject(obj);
        messages.push(`${subject} ${floorObjectVerb(obj, 'catches fire and burns', 'catch fire and burn')}.`);
    }
    removeObject(obj);
    return true;
}

function fireDamageFloorPotion(obj, messages, visible, removeObject = removeFloorObject) {
    if (visible) {
        const subject = floorObjectSubject(obj);
        const verb = isPotionOfOil(obj)
            ? floorObjectVerb(obj, 'ignites and explodes', 'ignite and explode')
            : floorObjectVerb(obj, 'boils and explodes', 'boil and explode');
        messages.push(`${subject} ${verb}.`);
    }
    removeObject(obj);
    return true;
}

function fireDamageFloorItem(obj, messages, visible, options = {}) {
    const {
        removeObject = removeFloorObject, spillQueue = [], processSpill = null,
        usedUpShopBillOnDestroy = false, x, y,
    } = options;
    const destroyObject = floorEffectRemoveObject(removeObject, usedUpShopBillOnDestroy);
    if (maybeIgniteFloorFireItem(obj, messages, visible)) return false;
    if (isLiquidFlowContainer(obj))
        return fireDamageFloorContainer(obj, messages, visible, {
            removeObject: destroyObject, spillQueue, processSpill, x, y,
        });

    const cls = fireDestroyableInventoryClass(obj);
    if (cls === 'scroll' || cls === 'spellbook')
        return fireDamageFloorScrollOrBook(obj, messages, visible, cls, destroyObject);
    if (cls === 'potion') return fireDamageFloorPotion(obj, messages, visible, destroyObject);

    return erodeFloorObject(obj, messages, visible, {
        profileWord: 'burnt',
        field: 'oeroded',
        action: 'smoulder',
        cause: 'heat',
        destroyAtMax: true,
        removeObject: destroyObject,
    });
}

const LAVA_DIRECT_BURN_MATERIALS = new Set([
    'liquid', 'wax', 'veggy', 'vegetable', 'flesh', 'paper', 'cloth', 'leather', 'wood', 'bone',
]);
const LIQUID_FLOW_RIDER_CORPSE_NAMES = new Set(['death', 'pestilence', 'famine']);

function liquidFlowRiderCorpse(obj) {
    if (!(obj?.otyp === CORPSE || obj?.otyp === 'corpse')) return false;
    const corpse = obj.corpsenm || obj.corpse || {};
    const corpseName = String(corpse.name || corpse.mname || '').toLowerCase();
    if (corpse.rider || LIQUID_FLOW_RIDER_CORPSE_NAMES.has(corpseName)) return true;
    return /\b(?:death|pestilence|famine) corpse\b/.test(objectKindKey(obj));
}

function lavaObjResistsHard(obj) {
    const actual = String(obj?.actualKind || '').toLowerCase();
    const kind = objectKindKey(obj);
    if (obj?.realAmuletOfYendor || actual === 'amulet of yendor'
        || (!actual && kind === 'amulet of yendor')) return true;
    if (isBookOfTheDeadItem(obj)) return true;
    if (isCandelabrumOfInvocationItem(obj)) return true;
    if (isBellOfOpeningItem(obj)) return true;
    return liquidFlowRiderCorpse(obj);
}

function lavaObjectProtectedByObjResists(obj) {
    if (!lavaObjResistsHard(obj)) {
        rn2(100);
        return false;
    }
    return !isBookOfTheDeadItem(obj);
}

function lavaDirectFireExemptObject(obj, cls) {
    if (cls === 'scroll' || cls === 'spellbook') return true;
    if (obj?.oerodeproof || obj?.fireResistance || obj?.fireResistant) return true;
    const kind = objectKindKey(obj);
    return kind === 'wand of fire' || kind === 'fire horn' || obj?.oprop === 'fire'
        || obj?.oprop === 'fire resistance' || obj?.oc_oprop === 'fire'
        || obj?.oc_oprop === 'fire resistance';
}

function lavaDirectBurnMaterialObject(obj) {
    if (liquidFlowContainerContents(obj).length) return false;
    const material = String(obj?.material || obj?.oc_material || '').toLowerCase();
    if (LAVA_DIRECT_BURN_MATERIALS.has(material)) return true;
    const cls = itemClassKey(obj);
    const kind = objectKindKey(obj);
    if ((cls === 'food' || obj?.otyp === FOOD_CLASS || obj?.glyph === '%')
        && !/\btin\b/.test(kind)) return true;
    if (obj?.otyp === CORPSE || obj?.otyp === 'corpse') return true;
    if (obj?.globby || GLOB_TYPES.has(kind.replace(/^glob of /, ''))) return true;
    const profile = wishedDamageProfile(obj);
    if (profile.erosionMatters && profile.primaryWord === 'burnt') return true;
    return /\b(?:wax|leather|cloth|wood|wooden|paper|bone|corpse|meat|ration|food|fruit|egg|sack|bag|box|chest|leash|rope|bow|arrow|club|quarterstaff|aklys|bullwhip|sling|flute|harp|drum|whistle|horn)\b/.test(kind);
}

function lavaDamageFloorEffectItem(obj, messages, visible, {
    removeObject = removeFloorObject,
    spillQueue = [],
    processSpill = null,
    usedUpShopBillOnDestroy = false,
    x,
    y,
} = {}) {
    const destroyObject = floorEffectRemoveObject(removeObject, usedUpShopBillOnDestroy);
    if (lavaObjectProtectedByObjResists(obj)) return false;
    const cls = fireDestroyableInventoryClass(obj);
    if (!lavaDirectFireExemptObject(obj, cls) && lavaDirectBurnMaterialObject(obj)) {
        if (visible) messages.push(`You see ${floorObjectArticleName(obj)} hit lava and burn up!`);
        destroyObject(obj);
        return true;
    }
    return fireDamageFloorItem(obj, messages, visible, {
        removeObject, spillQueue, processSpill, usedUpShopBillOnDestroy, x, y,
    });
}

function hotGroundPotionFloorEffect(obj, x, y, messages, visible, {
    removeObject = removeFloorObject,
    usedUpShopBillOnDestroy = false,
} = {}) {
    const destroyObject = floorEffectRemoveObject(removeObject, usedUpShopBillOnDestroy);
    const loc = game.level?.at?.(x, y);
    if (!loc || !(loc.typ === ROOM || loc.typ === CORR)) return false;
    if ((game.level?.flags?.temperature || 0) <= 0) return false;
    if (fireDestroyableInventoryClass(obj) !== 'potion') return false;

    const plural = (obj?.quan || 1) > 1;
    if (visible) {
        messages.push(`${floorObjectTheSubject(obj)} ${floorObjectVerb(obj, 'heats', 'heat')} up as ${plural ? 'they hit' : 'it hits'} the hot ground.`);
    }
    let survival = obj?.blessed ? 70 : 50;
    if (obj?.invlet || obj?.letter) survival += ((game.u?.uluck || 0) + (game.u?.moreluck || 0)) * 2;
    if (isPotionOfOil(obj)) survival = 100;
    if (rn2(100) < ((obj?.artifact || obj?.oartifact) ? 100 : survival)) return false;
    if (visible) messages.push(plural ? 'They shatter from the heat!' : 'It shatters from the heat!');
    else if (!heroIsDeaf()) messages.push('You hear a shattering noise.');
    brokenPotionBreathe(obj, x, y, messages);
    destroyObject(obj);
    return true;
}

function liquidFlowFloorEffectsItem(obj, x, y, messages, visible, acidContext, spillQueue, processSpill = null, {
    usedUpShopBillOnDestroy = false,
} = {}) {
    const loc = game.level?.at?.(x, y);
    if (!loc) return false;
    if (obj?.otyp === BOULDER) {
        if (earthFloorEffects(obj, x, y, messages, '', { usedUpShopBillOnDestroy })) {
            removeFloorObject(obj);
            return true;
        }
    }
    if (loc.typ === LAVAPOOL || loc.typ === LAVAWALL)
        return lavaDamageFloorEffectItem(obj, messages, visible, {
            spillQueue, processSpill, usedUpShopBillOnDestroy, x, y,
        });
    if (IS_POOL(loc.typ))
        return waterDamageFloorItem(obj, messages, visible, acidContext, { usedUpShopBillOnDestroy });
    if (hotGroundPotionFloorEffect(obj, x, y, messages, visible, { usedUpShopBillOnDestroy })) return true;
    return false;
}

export function applyLiquidFlowFloorObjectDamage(x, y, typ) {
    const messages = [];
    const visible = floorObjectVisible(x, y);
    let changed = false;
    let destroyed = 0;
    const acidContext = { count: 0 };

    const queue = liquidFlowFloorObjectsAt(x, y).map(obj => ({ obj, mode: 'chain' }));
    const processed = new Set();
    const processSpilledFloorEffect = obj => {
        if (processed.has(obj)) return false;
        processed.add(obj);
        if (!(game.level?.objects || []).includes(obj)) return false;
        const damaged = liquidFlowFloorEffectsItem(obj, x, y, messages, visible, acidContext, queue, processSpilledFloorEffect);
        if (damaged) changed = true;
        if (!(game.level?.objects || []).includes(obj)) destroyed++;
        return damaged;
    };
    for (let i = 0; i < queue.length; i++) {
        const entry = queue[i];
        const obj = entry?.obj || entry;
        if (processed.has(obj)) continue;
        processed.add(obj);
        if (!(game.level?.objects || []).includes(obj)) continue;
        let damaged;
        if (entry?.mode === 'flooreffects')
            damaged = liquidFlowFloorEffectsItem(obj, x, y, messages, visible, acidContext, queue, processSpilledFloorEffect);
        else if (typ === LAVAPOOL)
            damaged = fireDamageFloorItem(obj, messages, visible, {
                spillQueue: queue, processSpill: processSpilledFloorEffect, x, y,
            });
        else
            damaged = waterDamageFloorItem(obj, messages, visible, acidContext);
        if (damaged) changed = true;
        if (!(game.level?.objects || []).includes(obj)) destroyed++;
    }

    if (typ === LAVAPOOL && destroyed && game.u?.blind && !couldsee(x, y))
        messages.push('You smell smoke.');
    if (changed) newsym(x, y);
    return { messages, changed, destroyed };
}

function fireInventoryDamageResult(messages, events, damage = 0, deathCause = '') {
    return {
        messages,
        events,
        damage,
        deathCause,
        lifeSaving: !!messages.lifeSaving,
        fatal: !!messages.fatal,
        more: !!messages.more,
    };
}

function fireDamageInventory(origDamage, forceDestroyItems = false, rollIgniteItems = false, {
    updateArmorInventory = true,
    preburnedArmor = null,
    allowLifeSaving = false,
} = {}) {
    const messages = [];
    const events = [];
    const armor = preburnedArmor || burnWornArmorFromFire({ updateHeroInventory: updateArmorInventory });
    if (armor.message) messages.push(armor.message);
    const joinState = { joinedArmorMessage: false };
    let destroyItems = false;
    let igniteItems = false;
    let rollIgniteAfterDestroy = false;
    if (forceDestroyItems) {
        destroyItems = true;
        igniteItems = true;
    } else if (rollIgniteItems) {
        if (!armor.bodyHit) return fireInventoryDamageResult(messages, events, 0, '');
        destroyItems = !rn2(3);
        if (destroyItems) rollIgniteAfterDestroy = true;
        else igniteItems = !rn2(3);
    } else if (armor.bodyHit || rn2(3)) {
        destroyItems = true;
        igniteItems = true;
    } else {
        return fireInventoryDamageResult(messages, events, 0, '');
    }

    let damage = 0;
    let deathCause = '';
    if (!destroyItems) {
        if (igniteItems) igniteFireInventoryItems(messages, events, armor, joinState);
        return fireInventoryDamageResult(messages, events, damage, deathCause);
    }

    let limit = Math.trunc(origDamage / 5);
    if (origDamage % 5 > rn2(5)) limit++;
    if (limit < 1) {
        if (rollIgniteAfterDestroy) igniteItems = !rn2(3);
        if (igniteItems) igniteFireInventoryItems(messages, events, armor, joinState);
        return fireInventoryDamageResult(messages, events, damage, deathCause);
    }
    limit = Math.min(20, limit);

    const selected = [];
    let eligible = 0;
    for (const item of [...(game.inventory || [])]) {
        const cls = fireDestroyableInventoryClass(item);
        if (!cls || fireInventoryItemImmune(item, cls)) continue;
        const i = eligible < limit ? eligible : rn2(eligible);
        eligible++;
        if (i < limit) selected[i] = item;
    }

    for (const item of selected.filter(Boolean)) {
        const cls = fireDestroyableInventoryClass(item);
        if (cls === 'spellbook' && isBookOfTheDeadItem(item)) {
            if (!game.u?.blind)
                addFireInventoryMessage(messages, events,
                    'The Book of the Dead glows a strange dark red, but remains intact.',
                    { damage: 0 }, armor, joinState);
            continue;
        }
        const quan = item.quan || 1;
        const itemDamage = fireInventoryItemDamage(item, cls);
        let destroyed = 0;
        for (let i = 0; i < quan; i++)
            if (!rn2(3)) destroyed++;
        if (!destroyed) continue;

        const plural = destroyed > 1;
        const displayObj = { ...item, line: '', quan: plural ? Math.max(2, quan) : 1 };
        const name = pickupObjectName(displayObj);
        const subject = destroyed === 1 && quan === 1 ? `Your ${name}`
            : destroyed === 1 ? `One of your ${name}`
                : destroyed < quan ? `Some of your ${name}`
                    : quan === 2 ? `Both of your ${name}`
                        : `All of your ${name}`;
        const event = { text: '', damage: 0 };
        const vaporMessages = [];
        const message = `${subject} ${fireInventoryDestroyVerb(cls, item, plural)}!`;
        if (cls === 'potion' || cls === 'slime') {
            if (cls === 'potion') {
                potionBreathe(item, vaporMessages, { allowLifeSaving });
                if (vaporMessages.lifeSaving) {
                    messages.lifeSaving = true;
                    event.lifeSaving = true;
                }
                if (vaporMessages.fatal) {
                    messages.fatal = true;
                    event.fatal = true;
                }
                if (vaporMessages.more) {
                    messages.more = true;
                    event.more = true;
                }
                if (vaporMessages.length) {
                    const insertAfter = vaporMessages.map(text => ({ text, more: true }));
                    event.insertAfter = insertAfter;
                }
            }
            event.damage = itemDamage;
            damage += itemDamage;
            deathCause = fireInventoryDeathCause(cls, item, plural);
            useUpInventoryItem(item, destroyed);
            if (cls === 'potion') rn2(2);
        } else {
            useUpInventoryItem(item, destroyed);
            if (!game.u?.fireResistance) {
                event.damage = 1;
                damage += 1;
                deathCause = fireInventoryDeathCause(cls, item, plural);
            }
        }
        addFireInventoryMessage(messages, events, message, event, armor, joinState);
        if (vaporMessages.length) messages.push(...vaporMessages);
    }
    if (rollIgniteAfterDestroy) igniteItems = !rn2(3);
    if (igniteItems) igniteFireInventoryItems(messages, events, armor, joinState);
    return fireInventoryDamageResult(messages, events, damage, deathCause);
}

function forceWeaponName(item) {
    if (!item) return 'weapon';
    return inventoryItemName(item)
        .replace(/^an? /, '')
        .replace(/^(?:\d+ )?(?:blessed|uncursed|cursed) /, '')
        .replace(/^[+-]\d+ /, '');
}

const FORCE_BLADE_NAME_RE = /\b(?:athame|axe|battle-axe|crysknife|dagger|knife|katana|saber|sabre|scalpel|short sword|broadsword|long sword|two-handed sword|tsurugi|wakizashi)\b/;
const FORCE_WEAPON_LDAM_BY_NAME = new Map([
    ['two-handed sword', 6], ['silver saber', 8], ['dwarvish spear', 8],
    ['elven short sword', 8], ['orcish short sword', 8], ['dwarvish short sword', 8],
    ['elven broadsword', 6], ['bill-guisarme', 10], ['lucern hammer', 6],
    ['bec de corbin', 6], ['war hammer', 4], ['morning star', 6],
    ['silver mace', 6], ['quarterstaff', 6], ['rubber hose', 3],
    ['crysknife', 10], ['short sword', 8], ['broadsword', 6],
    ['long sword', 12], ['runesword', 6], ['scimitar', 8],
    ['wakizashi', 8], ['athame', 3], ['scalpel', 3], ['stiletto', 2],
    ['worm tooth', 2], ['battle-axe', 6], ['partisan', 6],
    ['ranseur', 4], ['spetum', 6], ['glaive', 10], ['halberd', 6],
    ['bardiche', 4], ['voulge', 4], ['fauchard', 8], ['guisarme', 8],
    ['mattock', 12], ['trident', 6], ['javelin', 6], ['spear', 8],
    ['dagger', 3], ['knife', 2], ['katana', 12], ['tsurugi', 8],
    ['saber', 8], ['sabre', 8], ['mace', 6], ['club', 3],
    ['aklys', 3], ['flail', 4], ['axe', 4], ['lance', 10],
    ['mjollnir', 4],
]);

function forceWeaponIsPick(item) {
    return PICK_DIG_NAME_RE.test(forceWeaponName(item).toLowerCase());
}

function forceWeaponIsBlade(item) {
    const name = forceWeaponName(item).toLowerCase();
    return !forceWeaponIsPick(item) && FORCE_BLADE_NAME_RE.test(name);
}

function forceLockChance(item) {
    const explicit = Number(item?.oc_wldam ?? item?.wldam ?? item?.ldam);
    if (Number.isFinite(explicit) && explicit > 0) return Math.trunc(explicit) * 2;
    const name = forceWeaponName(item).toLowerCase()
        .replace(/\b(?:very|thoroughly|rusty|corroded|burnt|rotted|greased|fixed|fireproof|rustproof)\b/g, '')
        .replace(/\s+/g, ' ')
        .trim();
    for (const [weaponName, ldam] of FORCE_WEAPON_LDAM_BY_NAME) {
        if (name === weaponName || name.endsWith(` ${weaponName}`)) return ldam * 2;
    }
    return 8;
}

function forceBladeGreatestErosion(item) {
    return Math.max(0, Math.trunc(Number(item?.oeroded || 0)), Math.trunc(Number(item?.oeroded2 || 0)));
}

function forceBladeObjectResistsBreak(item) {
    const kind = objectKindKey(item);
    if (item?.invocation || item?.riderCorpse || item?.otyp === BOOK_OF_THE_DEAD
        || item?.otyp === CANDELABRUM_OF_INVOCATION || item?.otyp === BELL
        || kind === 'amulet of yendor')
        return true;
    const roll = rn2(100);
    return roll < (item?.artifact || item?.oartifact ? 99 : 0);
}

function forceBladeBreakMessage(item) {
    const quantity = Math.max(1, Math.trunc(Number(item?.quan || 1)));
    const name = pickupObjectName({ ...item, line: '', quan: quantity });
    return quantity > 1 ? `One of your ${name} broke!` : `Your ${name} broke!`;
}

function disturbBuriedZombieCorpseTimersAt(x, y) {
    const level = game.level;
    if (!level) return;
    const moves = game.moves || 0;
    const seen = new Set();
    const scan = (obj, fromBuriedList = false) => {
        if (!obj || seen.has(obj)) return;
        seen.add(obj);
        if (!fromBuriedList && !obj.buried) return;
        if (!isCorpseItem(obj)) return;
        if (obj.ox == null || obj.oy == null) return;
        const ox = Number(obj.ox);
        const oy = Number(obj.oy);
        if (!Number.isFinite(ox) || !Number.isFinite(oy)) return;
        if (Math.abs(ox - x) > 1 || Math.abs(oy - y) > 1) return;
        if (typeof obj.zombifyTurn !== 'number' || obj.zombifyTurn <= moves) return;
        const remaining = obj.zombifyTurn - moves;
        obj.zombifyTurn = moves + Math.max(1, Math.trunc((remaining * 2) / 3));
    };
    for (const obj of level.buriedobjlist || []) scan(obj, true);
    for (const obj of level.objects || []) scan(obj, false);
}

function wakeNearbyFromForceLock(messages) {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const distance = Math.max(0, Math.trunc(Number(game.u?.ulevel || 1)) * 20);
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp || 1) <= 0) continue;
        const dx = (mon.mx || 0) - ux;
        const dy = (mon.my || 0) - uy;
        if (dx * dx + dy * dy >= distance) continue;
        if (mon.msleeping && monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} wakes up.`);
        mon.msleeping = 0;
        if (!(mon.unique || mon.data?.unique)) {
            if (typeof mon.mstrategy === 'number') mon.mstrategy &= ~STRAT_WAITMASK;
            else if (mon.mstrategy === 'waitforu') mon.mstrategy = 0;
            mon.waiting = false;
        }
    }
    disturbBuriedZombieCorpseTimersAt(ux, uy);
}

export function processForceLockOccupationTick(force) {
    const messages = [];
    if (!force) return { stop: false, messages };
    if (forceLockOccupationShouldGiveUp(force)) {
        messages.push('You give up your attempt to force the lock.');
        return { stop: true, messages };
    }
    const weapon = force.weapon;
    if (!force.picktyp) {
        wakeNearbyFromForceLock(messages);
        return { stop: false, messages };
    }

    const spe = Math.trunc(Number(weapon.spe || 0));
    const breakRoll = rn2(Math.max(1, 1000 - spe));
    const threshold = 992 - forceBladeGreatestErosion(weapon) * 10;
    if (breakRoll <= threshold || weapon.cursed || forceBladeObjectResistsBreak(weapon))
        return { stop: false, messages };

    messages.push(forceBladeBreakMessage(weapon));
    useUpInventoryItem(weapon, 1);
    messages.push('You give up your attempt to force the lock.');
    exerciseAttribute(A_DEX, true);
    return { stop: true, messages };
}

export function forceLockOccupationShouldGiveUp(force) {
    const weapon = force?.weapon;
    return !weapon || !(game.inventory || []).includes(weapon) || !itemIsWielded(weapon) || polyselfNoHands();
}

function isForceableBoxObject(obj) {
    const kind = objectKindKey(obj);
    return !!obj && (obj.otyp === CHEST || obj.otyp === LARGE_BOX
        || kind === 'chest' || kind === 'large box');
}

function forceableLockedBoxAtHero() {
    return (game.level?.objects || []).find(obj =>
        isForceableBoxObject(obj)
        && (obj.locked || obj.olocked)
        && obj.ox === game.u?.ux
        && obj.oy === game.u?.uy) || null;
}

function forceBoxSimpleName(box) {
    const kind = objectKindKey(box);
    if (box?.otyp === ICE_BOX || kind === 'ice box') return 'ice box';
    if (box?.otyp === LARGE_BOX || kind === 'large box') return 'large box';
    return 'chest';
}

function forceBoxArticleName(box) {
    const baseName = forceBoxSimpleName(box);
    const name = box?.lknown && (box.locked || box.olocked) ? `locked ${baseName}` : baseName;
    return `${/^[aeiou]/i.test(name) ? 'an' : 'a'} ${name}`;
}

function syncBrokenBoxContentDisplay(content) {
    const kind = String(content.kind || content.actualKind || '');
    if (content.glyph === '=' || content.otyp === RING_CLASS || kind.startsWith('ring of ') || /\bring$/i.test(kind)) {
        content.cls = 'ring';
        content.glyph = '=';
    }
    if (/helm|armor|mail|boots|gloves|shield|cloak|shirt/.test(String(content.kind || ''))) {
        content.cls = 'armor';
        content.glyph = '[';
    }
    if (content.glyph === '+'
        || content.cls === 'spellbook'
        || content.otyp === SPBOOK_NO_NOVEL
        || content.otyp === BOOK_OF_THE_DEAD
        || (content.otyp >= SPE_HEALING && content.otyp < DART)
        || /\bspellbook\b/i.test(kind)) {
        content.cls = 'spellbook';
        content.glyph = '+';
        if (content.color == null || content.color === NO_COLOR) content.color = CLR_WHITE;
    }
    if (OBJECT_CLASS_GLYPHS[content.cls]) content.glyph = OBJECT_CLASS_GLYPHS[content.cls];
}

function billDummyAlteredShopObject(obj) {
    if (!obj || shopBillableGold(obj)) return false;
    const x = obj.ox ?? game.u?.ux;
    const y = obj.oy ?? game.u?.uy;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return false;
    const price = shopItemPrice({ ...obj, contents: [], cobj: [] }, x, y);
    if (!(price > 0)) return false;
    const dummy = {
        ...obj,
        id: next_ident(),
        o_id: undefined,
        contents: [],
        cobj: [],
        contained: false,
        container: null,
        nobj: null,
        nexthere: null,
        ox: undefined,
        oy: undefined,
    };
    const entry = addObjectToShopBill(shkp, dummy, price, { useup: true });
    if (!entry)
        return shopBillIsFull(shkp) ? markAlteredShopObjectNoCharge(obj) : false;
    rememberUsedUpShopBillEntry(obj, entry, price, shkp);
    markAlteredShopObjectNoCharge(obj);
    return true;
}

const CHEST_SHATTER_BOTTLE_NAMES = ['bottle', 'phial', 'flagon', 'carafe', 'flask', 'jar', 'vial'];
const CHEST_SHATTER_HALLUCINATED_BOTTLE_NAMES = [
    'jug', 'pitcher', 'barrel', 'tin', 'bag', 'box', 'glass', 'beaker',
    'tumbler', 'vase', 'flowerpot', 'pan', 'thingy', 'mug', 'teacup',
    'teapot', 'keg', 'bucket', 'thermos', 'amphora', 'wineskin', 'parcel',
    'bowl', 'ampoule',
];

function chestShatterBottleName() {
    const names = heroIsHallucinating() ? CHEST_SHATTER_HALLUCINATED_BOTTLE_NAMES : CHEST_SHATTER_BOTTLE_NAMES;
    return names[rn2(names.length)] || 'bottle';
}

function breakChestSourceSpot() {
    return {
        ox: game._break_chest_x ?? game.u?.ux ?? 0,
        oy: game._break_chest_y ?? game.u?.uy ?? 0,
    };
}

function breakChestShopCharges() {
    if (!(game._break_chest_shop_charges instanceof Map))
        game._break_chest_shop_charges = new Map();
    return game._break_chest_shop_charges;
}

function recordBreakChestShopLoss(obj, options = {}) {
    const shkp = game._break_chest_shopkeeper;
    if (!obj || !shopkeeperInHisShop(shkp)) return 0;
    const charges = lostShopMerchandiseChargesForObject(breakChestSourceSpot(), obj, shkp, new Set(), options);
    let value = 0;
    const aggregate = breakChestShopCharges();
    for (const [owner, charge] of charges) {
        value += charge;
        addLostShopMerchandiseCharge(aggregate, owner, charge);
    }
    game._break_chest_shop_loss = Math.max(0, Math.trunc(Number(game._break_chest_shop_loss || 0))) + value;
    return value;
}

function finishBreakChestShopDebtMessage() {
    const shkp = game._break_chest_shopkeeper;
    const box = game._break_chest_destroyed_box;
    if (box) recordBreakChestShopLoss(box);
    const charges = game._break_chest_shop_charges instanceof Map
        ? game._break_chest_shop_charges
        : new Map();
    game._break_chest_shopkeeper = null;
    game._break_chest_destroyed_box = null;
    game._break_chest_shop_charges = null;
    game._break_chest_shop_loss = 0;
    game._break_chest_x = null;
    game._break_chest_y = null;
    if (!shopkeeperInHisShop(shkp) || !charges.size) return '';
    let charged = 0;
    const peaceful = shopkeeperPeacefulForDebt(shkp);
    for (const [owner, value] of charges)
        charged += chargeShopkeeperForLostMerchandise(owner, value, { peaceful });
    if (!(charged > 0)) return '';
    return `You owe ${charged} ${shopCurrency(charged)} for objects destroyed.`;
}

function brokenChestContentDestroyedMessage(content, messages) {
    if (isPotionObject(content)) {
        const sense = game.u?.blind ? 'hear' : 'see';
        const bottle = chestShatterBottleName();
        messages.push(`You ${sense} ${articleFor(bottle)} shatter!`);
        potionBreathe(content, messages);
        return messages.join('  ');
    }
    const name = content.otyp === 11 || content.cls === 'spellbook' || String(content.kind || '').startsWith('spellbook')
        ? 'spellbook'
        : pickupObjectName(content);
    const disposition = brokenChestContentShatterDisposition(content);
    return `${/^[aeiou]/i.test(name) ? 'An' : 'A'} ${name} ${disposition}!`;
}

function brokenChestContentMaterial(content) {
    const explicit = String(content?.material || content?.oc_material || '')
        .toLowerCase()
        .replace(/^hi[_-]?/, '')
        .replace(/[\s_-]+/g, '');
    const explicitMaterials = {
        paper: 'paper',
        wax: 'wax',
        veggie: 'veggy',
        vegetable: 'veggy',
        flesh: 'flesh',
        glass: 'glass',
        wood: 'wood',
        wooden: 'wood',
    };
    if (explicitMaterials[explicit]) return explicitMaterials[explicit];

    const kind = objectKindKey(content);
    if (content?.otyp === SCR_BLANK_PAPER || content?.cls === 'scroll'
        || content?.cls === 'spellbook' || String(content?.kind || '').startsWith('spellbook'))
        return 'paper';
    if (content?.otyp === TALLOW_CANDLE || content?.otyp === WAX_CANDLE || /\bcandle$/.test(kind))
        return 'wax';
    if (content?.otyp === CREAM_PIE || /\b(?:cream pie|fruit|vegetable|veggie|ration|pancake|wafer|cookie|carrot|apple|orange|pear|melon|banana|kelp|garlic|wolfsbane|eucalyptus|slime mold)\b/.test(kind))
        return 'veggy';
    if (content?.otyp === MEAT_RING || content?.otyp === CORPSE || content?.otyp === EGG
        || /\b(?:meat|corpse|egg)\b/.test(kind))
        return 'flesh';
    if (content?.otyp === MIRROR || content?.otyp === CRYSTAL_BALL
        || /\b(?:glass|mirror|looking glass|crystal ball)\b/.test(kind))
        return 'glass';
    if (/\b(?:wood|wooden|quarterstaff|staff|club|bow|arrow|crossbow|aklys|sling|harp|flute|drum)\b/.test(kind))
        return 'wood';
    return '';
}

function brokenChestContentShatterDisposition(content) {
    switch (brokenChestContentMaterial(content)) {
    case 'paper':
        return 'is torn to shreds';
    case 'wax':
        return 'is crushed';
    case 'veggy':
        return 'is pulped';
    case 'flesh':
        return 'is mashed';
    case 'glass':
        return 'shatters';
    case 'wood':
        return 'splinters to fragments';
    default:
        return 'is destroyed';
    }
}

function brokenChestSourceIsIceBox() {
    const box = game._break_chest_destroyed_box;
    const kind = objectKindKey(box);
    return !!box && (box.otyp === ICE_BOX || kind === 'ice box');
}

function thawBrokenIceBoxCorpseSurvivor(content) {
    if (!brokenChestSourceIsIceBox() || !isCorpseItem(content)) return;
    const moves = Math.max(game.moves || 0, 1);
    const frozenAge = Math.max(0, Math.trunc(Number(content.age ?? 0)));
    content.age = moves - frozenAge;
    content.inIceBox = false;
    content.fromIceBox = false;
    content.onIce = false;
    content.on_ice = 0;
    startCorpseTimeout(content);
}

function placeBrokenChestContentAtHero(content) {
    if (!content || !game.level) return;
    content.ox = game.u?.ux || 0;
    content.oy = game.u?.uy || 0;
    content.contained = false;
    content.container = null;
    content.line = undefined;
    thawBrokenIceBoxCorpseSurvivor(content);
    syncBrokenBoxContentDisplay(content);
    content.owt = globObjectWeight(content);
    const placed = placeStackableFloorObject(content);
    if (placed && placed !== content) syncBrokenBoxContentDisplay(placed);
    const finalContent = placed || content;
    objectIceEffect(finalContent, finalContent.ox ?? content.ox, finalContent.oy ?? content.oy);
    newsym(finalContent.ox ?? content.ox, finalContent.oy ?? content.oy);
}

function consumeOneBrokenChestContent(content) {
    const quantity = Math.max(1, Math.trunc(Number(content?.quan || 1)));
    if (quantity <= 1) return false;
    content.quan = quantity - 1;
    content.owt = globObjectWeight(content);
    return true;
}

function hasPendingBreakChestContents() {
    return !!game._break_chest_contents_after_more?.length;
}

function clearBreakChestContentState() {
    game._break_chest_contents_after_more = null;
    game._break_chest_destroyed_message = '';
    game._break_chest_content_message_active = 0;
    game._break_chest_finish_after_content_message = 0;
}

function appendBreakChestFinalDebt(message) {
    clearBreakChestContentState();
    const debtMessage = finishBreakChestShopDebtMessage();
    rn2(100);
    return debtMessage ? (message ? `${message}  ${debtMessage}` : debtMessage) : message;
}

function nextBreakChestDestroyedContentMessage() {
    while (hasPendingBreakChestContents()) {
        const content = game._break_chest_contents_after_more.shift();
        const destroyed = !rn2(3) || isPotionObject(content);
        if (destroyed) {
            const shatterMessages = [];
            const message = brokenChestContentDestroyedMessage(content, shatterMessages);
            recordBreakChestShopLoss(content, {
                includeContainedGold: false,
                inventoryLikeContents: true,
            });
            if (consumeOneBrokenChestContent(content)) placeBrokenChestContentAtHero(content);
            return message;
        }

        placeBrokenChestContentAtHero(content);
    }
    return '';
}

export function finishForceLock(force) {
    const chest = force?.chest;
    if (!chest) return false;

    exerciseAttribute(force.picktyp ? A_DEX : A_STR, true);
    const destroyed = !force.picktyp && !rn2(3);
    if (!destroyed) {
        billDummyAlteredShopObject(chest);
        chest.locked = false;
        chest.olocked = false;
        chest.obroken = true;
        chest.lknown = true;
        newsym(chest.ox, chest.oy);
        return false;
    }

    const contents = [...liquidFlowContainerContents(chest)];
    const boxName = forceBoxSimpleName(chest);
    game._break_chest_shopkeeper = shopkeeperForCostlySpot(game.u?.ux, game.u?.uy);
    if (!shopkeeperInHisShop(game._break_chest_shopkeeper)) game._break_chest_shopkeeper = null;
    game._break_chest_destroyed_box = chest;
    game._break_chest_shop_charges = new Map();
    game._break_chest_shop_loss = 0;
    game._break_chest_x = game.u?.ux ?? chest.ox ?? 0;
    game._break_chest_y = game.u?.uy ?? chest.oy ?? 0;
    game.level.objects = (game.level?.objects || []).filter(obj => obj !== chest);
    newsym(chest.ox, chest.oy);
    game._queued_message_after_more = `In fact, you've totally destroyed the ${boxName}.`;
    game._break_chest_destroyed_message = game._queued_message_after_more;
    game._break_chest_content_message_active = 0;
    game._break_chest_contents_after_more = contents;
    for (const content of game._break_chest_contents_after_more) {
        removeContainedObject(chest, content);
        syncBrokenBoxContentDisplay(content);
    }
    clearLiquidFlowContainerContents(chest);
    game._queued_message_more_after_more = false;
    return true;
}

function revealLevelMap({ confusedMap = false, revealSecretDoors = false } = {}) {
    for (const obj of game.level?.objects || []) {
        if (obj.otyp === BOULDER && !obj.seen && !(game.viz_array?.[obj.oy]?.[obj.ox] & IN_SIGHT))
            obj._hide_until_seen = true;
    }
    game._revealing_level_map = 1;
    for (let y = 0; y < ROWNO; y++) {
        for (let x = 1; x < COLNO; x++) {
            if (confusedMap && rn2(7)) continue;
            const loc = game.level?.at(x, y);
            if (!loc || loc.typ === STONE) continue;
            if (revealSecretDoors && loc.typ === SDOOR) {
                loc.typ = DOOR;
                const mask = loc.doormask || 0;
                loc.doormask = Is_rogue_level(game.u?.uz)
                    ? D_NODOOR
                    : (mask & D_LOCKED) ? mask : (mask | D_CLOSED);
            }
            if (loc.typ === SCORR) loc.typ = CORR;
            loc.waslit = true;
            loc.seenv = 0xff;
            newsym(x, y);
        }
    }
    game._revealing_level_map = 0;
}

function commandChar(key) {
    return typeof key === 'number' ? String.fromCharCode(key) : String(key);
}

function visibleKey(ch) {
    const code = String(ch || '').charCodeAt(0);
    if (code < 32) return `^${String.fromCharCode(code + 64)}`;
    if (code === 127) return '^?';
    return ch;
}

function movementDirection(ch) {
    const lower = ch.toLowerCase();
    if (!(lower in DIR_DX)) return null;
    return { dx: DIR_DX[lower], dy: DIR_DY[lower] };
}

function commandDirection(ch) {
    if (ch === '.') return { dx: 0, dy: 0, dz: 0 };
    if (ch === '<') return { dx: 0, dy: 0, dz: -1 };
    if (ch === '>') return { dx: 0, dy: 0, dz: 1 };
    const dir = movementDirection(ch);
    return dir ? { ...dir, dz: 0 } : null;
}

function truncateCursorToMap(x, y, dx, dy) {
    if (x + dx < 1) {
        dy -= Math.sign(dy) * (1 - (x + dx));
        dx = 1 - x;
    } else if (x + dx > COLNO - 1) {
        dy += Math.sign(dy) * ((COLNO - 1) - (x + dx));
        dx = (COLNO - 1) - x;
    }
    if (y + dy < 0) {
        dx -= Math.sign(dx) * (0 - (y + dy));
        dy = -y;
    } else if (y + dy > ROWNO - 1) {
        dx += Math.sign(dx) * ((ROWNO - 1) - (y + dy));
        dy = (ROWNO - 1) - y;
    }
    return { x: x + dx, y: y + dy };
}

function figurineApplyDirection(ch) {
    if (ch === '.') return { dx: 0, dy: 0, dz: 0 };
    if (ch === '<') return { dx: 0, dy: 0, dz: -1 };
    if (ch === '>') return { dx: 0, dy: 0, dz: 1 };
    const dir = movementDirection(ch);
    return dir ? { ...dir, dz: 0 } : null;
}

function doorDescription(loc) {
    const mask = loc?.doormask ?? D_NODOOR;
    if (mask & D_BROKEN) return 'broken door';
    if (mask & D_ISOPEN) return 'open door';
    if (mask & (D_CLOSED | D_LOCKED)) return 'closed door';
    return 'doorway';
}

export function travelPathKeys(targetX, targetY, ignorePeacefulNearTarget = false, allowGuess = false, allowBlockedTarget = false) {
    const startX = game.u?.ux || 0;
    const startY = game.u?.uy || 0;
    const targetLoc = game.level?.at(targetX, targetY);
    const targetBlocked = !targetLoc || IS_OBSTRUCTED(targetLoc.typ)
        || game.level?.objects?.some(obj => obj.otyp === BOULDER && obj.ox === targetX && obj.oy === targetY);
    if (targetBlocked && !allowBlockedTarget
        && (!allowGuess || Math.max(Math.abs(targetX - startX), Math.abs(targetY - startY)) <= 1))
        return [];
    const dirs = ['h', 'k', 'l', 'j', 'y', 'u', 'n', 'b'];
    const travelVisited = new Set();
    const keys = [];
    let heroX = startX;
    let heroY = startY;

    while (heroX !== targetX || heroY !== targetY) {
        const seen = new Set([`${targetX},${targetY}`]);
        const queue = [{ x: targetX, y: targetY }];
        let nextKey = null;
        for (let qi = 0; qi < queue.length && !nextKey; qi++) {
            const cur = queue[qi];
            for (const key of dirs) {
                const x = cur.x + DIR_DX[key];
                const y = cur.y + DIR_DY[key];
                const id = `${x},${y}`;
                if (x === heroX && y === heroY) {
                    const dx = cur.x - heroX;
                    const dy = cur.y - heroY;
                    nextKey = Object.keys(DIR_DX).find(dir => DIR_DX[dir] === dx && DIR_DY[dir] === dy) || null;
                    break;
                }
                if (seen.has(id)) continue;
                const loc = game.level?.at(x, y);
                if (!loc || IS_OBSTRUCTED(loc.typ)) continue;
                if (!(loc.waslit || loc.seenv)) continue;
                if (game.level?.objects?.some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y)) continue;
                seen.add(id);
                queue.push({ x, y });
            }
        }
        if (!nextKey) break;
        const nextX = heroX + DIR_DX[nextKey];
        const nextY = heroY + DIR_DY[nextKey];
        const nextId = `${nextX},${nextY}`;
        keys.push(nextKey);
        if ((nextX === targetX && nextY === targetY) || travelVisited.has(nextId)) break;
        travelVisited.add(`${heroX},${heroY}`);
        heroX = nextX;
        heroY = nextY;
    }
    if ((heroX !== targetX || heroY !== targetY) && allowGuess) {
        const travel = Array.from({ length: COLNO }, () => Array(ROWNO).fill(0));
        let frontier = [{ x: startX, y: startY }];
        for (let radius = 1; frontier.length; radius++) {
            const next = [];
            for (const cur of frontier) {
                let repeated = false;
                for (const key of dirs) {
                    const x = cur.x + DIR_DX[key];
                    const y = cur.y + DIR_DY[key];
                    if (x < 1 || x >= COLNO || y < 0 || y >= ROWNO || !couldsee(x, y)) continue;
                    const loc = game.level?.at(x, y);
                    const blocked = !loc || IS_OBSTRUCTED(loc.typ)
                        || game.level?.objects?.some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y);
                    if (blocked) {
                        if (!repeated && (travel[cur.x]?.[cur.y] || 0) > radius - 3) {
                            next.push(cur);
                            repeated = true;
                        }
                        continue;
                    }
                    if (!(loc.seenv || (!game.u?.blind && couldsee(x, y))) || travel[x][y]) continue;
                    travel[x][y] = radius;
                    next.push({ x, y });
                }
            }
            frontier = next;
        }

        let pickX = startX;
        let pickY = startY;
        let bestDist = Math.max(Math.abs(targetX - startX), Math.abs(targetY - startY));
        let bestDist2 = (targetX - startX) ** 2 + (targetY - startY) ** 2;
        let bestTravel = COLNO * ROWNO;
        for (let x = 1; x < COLNO; x++) {
            for (let y = 0; y < ROWNO; y++) {
                const ctrav = travel[x][y];
                if (!ctrav || !couldsee(x, y)) continue;
                const dist = Math.max(Math.abs(targetX - x), Math.abs(targetY - y));
                if (dist === bestDist && ctrav < bestTravel) {
                    const dist2 = (targetX - x) ** 2 + (targetY - y) ** 2;
                    if (dist2 < bestDist2) {
                        pickX = x;
                        pickY = y;
                        bestDist2 = dist2;
                        bestTravel = ctrav;
                    }
                } else if (dist < bestDist) {
                    pickX = x;
                    pickY = y;
                    bestDist = dist;
                    bestDist2 = (targetX - x) ** 2 + (targetY - y) ** 2;
                    bestTravel = ctrav;
                }
            }
        }
        if (pickX === startX && pickY === startY) {
            const dx = Math.sign(targetX - startX);
            const dy = Math.sign(targetY - startY);
            const key = Object.keys(DIR_DX).find(dir => DIR_DX[dir] === dx && DIR_DY[dir] === dy);
            const loc = key ? game.level?.at(startX + dx, startY + dy) : null;
            if (key && loc && !IS_OBSTRUCTED(loc.typ)) return [key];
        } else {
            const seen = new Set([`${pickX},${pickY}`]);
            const queue = [{ x: pickX, y: pickY }];
            for (let qi = 0; qi < queue.length; qi++) {
                const cur = queue[qi];
                for (const key of dirs) {
                    const x = cur.x + DIR_DX[key];
                    const y = cur.y + DIR_DY[key];
                    if (x === startX && y === startY) {
                        const dx = cur.x - startX;
                        const dy = cur.y - startY;
                        const nextKey = Object.keys(DIR_DX).find(dir => DIR_DX[dir] === dx && DIR_DY[dir] === dy);
                        if (nextKey) return [nextKey];
                    }
                    const id = `${x},${y}`;
                    if (seen.has(id)) continue;
                    const loc = game.level?.at(x, y);
                    if (!loc || IS_OBSTRUCTED(loc.typ)) continue;
                    if (!(loc.seenv || (!game.u?.blind && couldsee(x, y)))) continue;
                    if (game.level?.objects?.some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y)) continue;
                    seen.add(id);
                    queue.push({ x, y });
                }
            }
        }
    }

    let checkX = startX;
    let checkY = startY;
    for (const key of keys) {
        checkX += DIR_DX[key];
        checkY += DIR_DY[key];
        const occupied = game.level?.monsters?.find(mon => mon.mx === checkX && mon.my === checkY);
        const peaceful = occupied?.pet || occupied?.mpeaceful;
        if (occupied && (!ignorePeacefulNearTarget || !peaceful)
            && Math.max(Math.abs(targetX - checkX), Math.abs(targetY - checkY)) <= 2) return [];
    }
    return keys;
}
function landingSpot() {
    let best = null;
    let minDistance = -1;
    let viable = 0;
    const oldLoc = game.level?.at(game.u?.ux || 0, game.u?.uy || 0);
    for (const dir of LANDING_DIRS) {
        const x = (game.u?.ux || 0) + dir.dx;
        const y = (game.u?.uy || 0) + dir.dy;
        const loc = game.level?.at(x, y);
        if (!loc || IS_OBSTRUCTED(loc.typ)) continue;
        if (dir.dx && dir.dy
            && ((oldLoc?.typ === DOOR && (oldLoc.doormask & D_ISOPEN))
                || (loc.typ === DOOR && (loc.doormask & D_ISOPEN)))) continue;
        if (game.level?.monsters?.some(mon => mon.mx === x && mon.my === y)) continue;

        viable++;
        const distance = dir.dx * dir.dx + dir.dy * dir.dy;
        if (minDistance >= 0 && distance > minDistance) continue;
        if (minDistance < 0 || distance < minDistance || !rn2(viable)) {
            best = { x, y };
            minDistance = distance;
        }
    }
    return best;
}

async function dismountSteed() {
    const steed = game.u?.usteed;
    const spot = landingSpot();
    if (!steed) return;
    if (!spot) {
        await setMessage("You can't.  There isn't anywhere for you to stand.");
        game._command_mode = null;
        return;
    }

    const steedX = game.u.ux;
    const steedY = game.u.uy;
    game.u.usteed = null;
    steed.mx = steedX;
    steed.my = steedY;
    if (!game.level.monsters.includes(steed)) game.level.monsters.push(steed);
    game.u.ux0 = steedX;
    game.u.uy0 = steedY;
    game.u.ux = spot.x;
    game.u.uy = spot.y;
    newsym(steedX, steedY);
    newsym(spot.x, spot.y);
    vision_recalc(0);
    const hostileNearby = (game.level?.monsters || []).some(mon =>
        !mon.pet && Math.max(Math.abs(mon.mx - steedX), Math.abs(mon.my - steedY)) <= 2
    );
    const objectAtLanding = (game.level?.objects || []).some(obj =>
        !obj.hidden && obj.ox === spot.x && obj.oy === spot.y
    );
    if (objectAtLanding) game._dismount_object_list_spot = { x: spot.x, y: spot.y };
    await setMessage(`You've been through the dungeon on a ${steed.data?.name || 'steed'} with no name.`, hostileNearby || objectAtLanding);
    game._command_mode = null;
    game.context.move = 1;
}

async function mountSteed(mon) {
    if (!mon) {
        await setMessage('I see nobody there.');
        game._command_mode = null;
        return;
    }
    const steedName = `${mon.saddled ? 'saddled ' : ''}${mon.data?.name || 'creature'}`;
    if (!mon.saddled) {
        await setMessage(`${mon.data?.name || 'It'} is not saddled.`);
        game._command_mode = null;
        return;
    }
    if (!mon.pet || !mon.mtame) {
        await setMessage(`I think the ${mon.data?.name || 'creature'} would mind.`);
        game._command_mode = null;
        return;
    }

    if ((game.u?.ulevel || 1) + (mon.mtame || 0) < rnd(20)) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - (rn2(5) + 10));
        await setMessage(`You slip while trying to get on the ${steedName}.`, game.u.uhp <= 0);
        game._command_mode = game.u.uhp <= 0 ? 'deathSlipMore' : null;
        return;
    }

    const oldx = game.u.ux;
    const oldy = game.u.uy;
    game.u.usteed = mon;
    game.u.ux0 = oldx;
    game.u.uy0 = oldy;
    game.u.ux = mon.mx;
    game.u.uy = mon.my;
    newsym(oldx, oldy);
    newsym(game.u.ux, game.u.uy);
    vision_recalc(0);
    await setMessage(`You mount the ${steedName}.`);
    game._command_mode = null;
    game.context.move = 1;
}

async function rideDirection(ch) {
    const dir = movementDirection(ch);
    if (!dir) {
        game._command_mode = null;
        return;
    }
    const x = (game.u?.ux || 0) + dir.dx;
    const y = (game.u?.uy || 0) + dir.dy;
    const mon = game.level?.monsters?.find(mtmp => mtmp.mx === x && mtmp.my === y);
    await mountSteed(mon);
}

async function setMessage(msg, more = false) {
    const text = String(msg || '');
    if (game._silent_drop_prompt_message) {
        if (game._pending_message === game._silent_drop_prompt_message && !game._message_more)
            game._pending_message = '';
        game._silent_drop_prompt_message = '';
    }
    const runningMessage = game._pending_message
        && (game._running_continuation || game._initial_run_command || game._run_steps_remaining > 0);
    if (!runningMessage) game._pending_message_blocks_time = 0;
    if (runningMessage) {
        const width = game.nhDisplay?.cols || 80;
        const queueAfterSwap = /^You swap places with /.test(game._pending_message)
            && /^[$a-zA-Z] - /.test(text);
        if (!queueAfterSwap && game._pending_message.length + text.length + 3 < width - 8) {
            if (/^staircase (?:up|down)$/.test(game._pending_message)
                && /^There is a staircase (?:up|down) here\. {0,2}$/.test(text)) {
                const cursor = Math.max(1, 40 - game._pending_message.length);
                game._pending_message = `${game._pending_message}\x1b[${cursor}C${text.trimEnd()}`;
            } else {
                game._pending_message = `${game._pending_message}  ${text}`;
            }
            game._message_more = more ? 1 : 0;
        } else {
            game._message_more = 1;
            game._process_time_with_more = 0;
            game._topline_after_more = text;
        }
    } else {
        await pline(text);
        game._message_more = more ? 1 : 0;
    }
    game._message_more_line = '';
    game._keep_pending_message = 1;
    if (game._swallow_overlay_active && (game.u?.uswallow || (more && text === 'You get expelled!'))) {
        if (more && text === 'You get expelled!') {
            const prompt = `${game._pending_message}--More--`;
            game._simple_swallow_expel_prompt = 1;
            if ((game.u?._statusSuffix || '').includes('Hallu'))
                game._hallu_expel_object_rng_skip = 1;
            const overlayLines = (game._overlay_lines || game._swallow_overlay_lines || []).map(line => [...line]);
            if (overlayLines[0]) overlayLines[0][2] = prompt;
            game._overlay_lines = overlayLines;
            game._swallow_overlay_lines = overlayLines.map(line => [...line]);
            const disp = game.nhDisplay;
            if (disp?.setCell) {
                for (let row = 0; row < (game._overlay_clear_rows ?? 22); row++)
                    disp.clearRow?.(row);
                for (const [row, col, value, attr = 0, color = NO_COLOR] of overlayLines) {
                    const textValue = String(value);
                    for (let i = 0; i < textValue.length; i++)
                        disp.setCell(col + i, row, textValue[i], color, attr);
                }
            }
        } else {
            refreshSwallowOverlay(more);
        }
    }
    if (msg !== 'You are caught in a bear trap.') game._last_trapmove_message = '';
}

function addHeroStatusSuffix(status) {
    if (!game.u) return;
    const parts = String(game.u._statusSuffix || '').trim().split(/\s+/).filter(Boolean);
    if (!parts.includes(status)) parts.push(status);
    game.u._statusSuffix = parts.length ? ` ${parts.join(' ')}` : '';
}

function removeHeroStatusSuffix(status) {
    if (!game.u) return;
    const parts = String(game.u._statusSuffix || '').trim().split(/\s+/).filter(part => part !== status);
    game.u._statusSuffix = parts.length ? ` ${parts.join(' ')}` : '';
}

function heroIsConfused() {
    return (game.u?._statusSuffix || '').includes('Conf') || (game.u?._confusionTimeout || 0) > 0;
}

function heroIsHallucinating() {
    return !!game.u?.hallucinating || !!game.u?.hallu || (game.u?._statusSuffix || '').includes('Hallu');
}

function addHeroConfusion(turns) {
    if (!game.u || turns <= 0) return;
    game.u._confusionTimeout = (game.u._confusionTimeout || 0) + turns;
    addHeroStatusSuffix('Conf');
}

function addHeroStun(turns) {
    if (!game.u || turns <= 0) return;
    game.u._stunTimeout = (game.u._stunTimeout || 0) + turns;
    game.u.stunned = true;
    addHeroStatusSuffix('Stun');
}

function clearHeroConfusion() {
    if (!game.u) return;
    game.u._confusionTimeout = 0;
    removeHeroStatusSuffix('Conf');
}

function sentenceCase(text) {
    const value = String(text || '');
    return value ? value[0].toUpperCase() + value.slice(1) : value;
}

function monsterResistanceLevel(mon) {
    const level = mon?.m_lev ?? mon?.mlevel ?? mon?.data?.mlevel ?? mon?.data?.hpLevel ?? 1;
    return Math.max(1, Math.min(50, level || 1));
}

function monsterMagicResistance(mon) {
    return mon?.mr ?? mon?.data?.mr ?? mon?.data?.mresists ?? 0;
}

function monsterResistsEffect(mon, attackLevel) {
    const bound = Math.max(1, 100 + attackLevel - monsterResistanceLevel(mon));
    return rn2(bound) < monsterMagicResistance(mon);
}

function zapRayHitsArmorClass(ac) {
    const chance = rn2(20);
    if (!chance) return rnd(10) < ac;
    if (ac < 0) ac = -rnd(-ac);
    return 3 - chance < ac;
}

function zapBeamGlyph(dx, dy) {
    return dy === 0 ? '─' : dx === 0 ? '│' : dx === dy ? '\\' : '/';
}

export function loseExperienceLevel() {
    const u = game.u;
    if (!u) return;
    if (u.ulevel > 1) u.ulevel--;
    else u.uexp = 0;

    const hpLoss = u.uhpinc?.[u.ulevel] || 0;
    const oldHpMax = u.uhpmax || 1;
    const minHpMax = Math.max(u.ulevel || 1, 10);
    u.uhpmax = Math.max(minHpMax, (u.uhpmax || 1) - hpLoss);
    if (u.uhpmax > oldHpMax) u.uhpmax = oldHpMax;
    u.uhp = Math.max(1, Math.min((u.uhp || 1) - hpLoss, u.uhpmax));

    const powerLoss = u.ueninc?.[u.ulevel] || 0;
    u.uenmax = Math.max(0, (u.uenmax || 0) - powerLoss);
    u.uen = Math.max(0, Math.min((u.uen || 0) - powerLoss, u.uenmax));
    if (u.uexp > 0) {
        u.uexp = u.ulevel < 1 ? -1
            : u.ulevel < 10 ? 10 * (2 ** u.ulevel) - 1
                : u.ulevel < 20 ? 10000 * (2 ** (u.ulevel - 10)) - 1
                    : 10000000 * (u.ulevel - 19) - 1;
    }

    if (!game._chronicle_lost_experience) {
        game._chronicle_entries ??= [];
        game._chronicle_entries.push({ turn: game.moves || 1, text: 'lost all experience' });
        game._chronicle_lost_experience = 1;
    }
}

async function advanceExperienceLevel(incremental = false) {
    const role = game.urole?.name?.m || game._startup_role;
    if (game._level_change_pending_message) {
        const message = game._level_change_pending_message;
        game._level_change_pending_message = '';
        await setMessage(message, (game.u?.ulevel || 1) < (game._level_change_target || 1));
        game._command_mode = game._message_more ? 'levelChangeMore' : null;
        return;
    }

    if ((game.u?.ulevel || 1) >= (game._level_change_target || 1)) {
        if (game._level_change_delayed_welcome) await setMessage(`Welcome to experience level ${game._level_change_delayed_welcome}.`);
        game._level_change_delayed_welcome = 0;
        game._command_mode = null;
        return;
    }

    const level = (game.u?.ulevel || 1) + 1;
    const oldLevel = game.u?.ulevel || 1;
    const roleEnergy = ROLE_EN_ADVANCE[role] || ROLE_EN_ADVANCE.Tourist;
    const raceName = game._startup_race || game.urace?.noun || 'human';
    const raceEnergy = RACE_EN_ADVANCE[raceName] || RACE_EN_ADVANCE.human;
    const lowLevel = oldLevel < roleEnergy.xlev;
    const roleHp = ROLE_HP_ADVANCE[role] || ROLE_HP_ADVANCE.Tourist;
    const raceHp = RACE_HP_ADVANCE[raceName] || RACE_HP_ADVANCE.human;
    let hpGain = lowLevel ? roleHp.lowFix + raceHp.lowFix : roleHp.highFix + raceHp.highFix;
    const roleHpRnd = lowLevel ? roleHp.lowRnd : roleHp.highRnd;
    const raceHpRnd = lowLevel ? raceHp.lowRnd : raceHp.highRnd;
    if (roleHpRnd > 0) hpGain += rnd(roleHpRnd);
    if (raceHpRnd > 0) hpGain += rnd(raceHpRnd);
    const con = game.u?.acurr?.a?.[4] || 10;
    hpGain += con <= 3 ? -2 : con <= 6 ? -1 : con <= 14 ? 0 : con <= 16 ? 1 : con === 17 ? 2 : con === 18 ? 3 : 4;
    if (hpGain <= 0) hpGain = 1;
    const wis = Math.trunc((game.u?.acurr?.a?.[2] || 10) / 2);
    const enrnd = wis
        + (lowLevel ? roleEnergy.lowRnd + raceEnergy.lowRnd : roleEnergy.highRnd + raceEnergy.highRnd);
    const enfix = lowLevel
        ? roleEnergy.lowFix + raceEnergy.lowFix
        : roleEnergy.highFix + raceEnergy.highFix;
    let powerGain = rn2(enrnd) + enfix;
    if (role === 'Priest' || role === 'Wizard') powerGain *= 2;
    else if (role === 'Healer' || role === 'Knight') powerGain = Math.trunc((3 * powerGain) / 2);
    else if (role === 'Barbarian' || role === 'Valkyrie') powerGain = Math.trunc((3 * powerGain) / 4);
    if (powerGain <= 0) powerGain = 1;
    game.u.uhpinc ??= [];
    game.u.ueninc ??= [];
    game.u.uhpinc[oldLevel] = hpGain;
    game.u.ueninc[oldLevel] = powerGain;
    game.u.ulevel = level;
    if (incremental) {
        const nextLevelExp = level < 10 ? 10 * (2 ** level)
            : level < 20 ? 10000 * (2 ** (level - 10))
                : 10000000 * (level - 19);
        if ((game.u.uexp || 0) >= nextLevelExp) game.u.uexp = nextLevelExp - 1;
    } else {
        game.u.uexp = oldLevel < 10 ? 10 * (2 ** oldLevel)
            : oldLevel < 20 ? 10000 * (2 ** (oldLevel - 10))
                : 10000000 * (oldLevel - 19);
    }
    game.u.uhp += hpGain;
    game.u.uhpmax += hpGain;
    game.u.uenmax += powerGain;
    game.u.uen += powerGain;
    if (role === 'Wizard' && level >= 15) game.u.warning = true;
    if (role === 'Wizard' && level >= 17) game.u.teleportControl = true;

    const rankIndex = level <= 2 ? 0 : level <= 30 ? Math.trunc((level + 2) / 4) : 8;
    const rank = ROLE_RANKS[role]?.[rankIndex];
    if (rank) game.urole.rank = { m: rank[0], f: rank[1] || rank[0] };

    const delayed = game._level_change_delayed_welcome;
    const abilityPrefix = game._level_change_ability_prefix || '';
    game._level_change_ability_prefix = '';
    const ability = ROLE_LEVEL_ABILITIES[role]?.[level];
    const abilityMessage = ability?.[0] || '';
    if (ability?.[1] && game.u) {
        game.u[ability[1]] = true;
        if (ability[1] === 'fast') game.u._intrinsicFast = true;
    }
    let pendingMessage = '';
    let nextDelayed = delayed || abilityPrefix ? level : 0;
    if (abilityMessage && delayed) {
        pendingMessage = `Welcome to experience level ${level}.  ${abilityMessage}`;
        nextDelayed = 0;
    } else if (abilityMessage) {
        game._level_change_ability_prefix = abilityMessage;
    }
    const more = level < (game._level_change_target || level) || !!nextDelayed || !!pendingMessage;
    game._level_change_delayed_welcome = nextDelayed;
    game._level_change_pending_message = pendingMessage;
    const message = delayed
        ? `Welcome to experience level ${delayed}.  You feel more experienced.`
        : abilityPrefix ? `${abilityPrefix}  You feel more experienced.`
            : `You feel more experienced.  Welcome to experience level ${level}.`;
    await setMessage(message, more);
    game._command_mode = more ? 'levelChangeMore' : null;
}

function polyselfFormByName(name) {
    return POLYSELF_EXTRA_FORMS.get(name) || monsterByRndName(name) || RANDOM_MONSTER_BY_NAME.get(name);
}

function randomPolyselfMonsterName() {
    for (let tryct = 200; tryct > 0; tryct--) {
        const name = DISPLAY_MONSTER_HALLU_NAMES[rn2(SPECIAL_PM)];
        const form = name ? polyselfFormByName(name) : null;
        if (form) return form.name || name;
    }
    return 'newt';
}

function polymorphSystemShock() {
    const con = game.u?.acurr?.a?.[A_CON] ?? 10;
    if (rn2(20) <= con) return null;
    const damage = rnd(30);
    if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
    exerciseAttribute(A_CON, false);
    return { message: 'You shudder for a moment.', more: false };
}

function polymorphSelfZapResult(item = null) {
    if (heroHasUnchanging()) return { message: '', more: false };
    if (item) setKnownWandLine(item, 'polymorph');
    const shock = polymorphSystemShock();
    if (shock) return shock;
    const formName = randomPolyselfMonsterName();
    const result = rn2(5) ? becomeMonster(formName) : becomeMonster('human');
    newsym(game.u?.ux || 0, game.u?.uy || 0);
    return {
        message: result?.message || 'Nothing happens.',
        more: !!result?.more,
    };
}

function polyselfWornArmorMatching(pattern) {
    return (game.inventory || []).find(item =>
        item?.cls === 'armor' && isWornInventoryItem(item)
        && pattern.test(inventoryItemName(item).toLowerCase()));
}

function polyselfWieldedWeaponItem() {
    return (game.inventory || []).find(item =>
        item && (item.cls === 'weapon' || isLoadstoneObject(item)) && itemIsWielded(item));
}

function polyselfWieldedWeaponCanDrop(item) {
    if (!item) return false;
    if ((item.cls === 'weapon' || isLoadstoneObject(item)) && item.cursed) {
        item.bknown = true;
        return false;
    }
    return true;
}

function polyselfWieldedWeaponDescription(item) {
    const kind = objectKindKey(item);
    if (isLoadstoneObject(item)) return 'stone';
    if (/\bsword\b/.test(kind)) return 'sword';
    if (/\bdagger\b/.test(kind)) return 'dagger';
    if (/\bcorpse\b/.test(kind)) return 'corpse';
    return 'weapon';
}

function releasePolyselfWieldedItem(item) {
    if (!item) return;
    item.wielded = false;
    item.alternate = false;
    refreshInventoryObjectLine(item);
}

function polyselfFormHasNoHead(form) {
    return !!(form && (form.nohead || form.noHead || form.headless || form.hasHead === false));
}

function polyselfIsEyewearItem(item) {
    return item?.cls === 'tool' && ['blindfold', 'towel', 'lenses'].includes(objectKindKey(item));
}

function polyselfWornEyewearItem() {
    return (game.inventory || []).find(item =>
        polyselfIsEyewearItem(item) && isWornInventoryItem(item));
}

function polyselfWornBlindfoldOrTowelItem() {
    return (game.inventory || []).find(item =>
        isWornInventoryItem(item) && ['blindfold', 'towel'].includes(objectKindKey(item)));
}

function polyselfWornShirtItem() {
    return (game.inventory || []).find(item =>
        item?.cls === 'armor' && isWornInventoryItem(item)
        && ['hawaiian shirt', 't-shirt'].includes(objectKindKey(item)));
}

function polyselfWornBodyArmorItem() {
    return wornArmorInSlot('body');
}

function polyselfWornCloakItem() {
    return wornArmorInSlot('cloak');
}

function polyselfWornHelmItem() {
    return wornArmorInSlot('helm');
}

const POLYSELF_BREAKARM_FORM_NAMES = new Set([
    'large dog',
    'xorn',
    'marilith',
    'winged gargoyle',
]);

const POLYSELF_SMALL_SLIPARM_FORM_NAMES = new Set([
    'hobbit',
    'gnome',
    'gnome leader',
    'gnome ruler',
]);
const POLYSELF_TWO_HORN_FORM_NAMES = new Set(['horned devil', 'minotaur', 'asmodeus', 'balrog']);
const POLYSELF_ONE_HORN_FORM_NAMES = new Set(['white unicorn', 'gray unicorn', 'black unicorn', 'ki-rin']);
const POLYSELF_HARD_HELM_KINDS = new Set([
    'helmet',
    'orcish helm',
    'dwarvish iron helm',
    'dented pot',
    'helm of brilliance',
    'helm of caution',
    'helm of opposite alignment',
    'helm of telepathy',
]);
const POLYSELF_FLIMSY_MATERIALS = new Set(['liquid', 'wax', 'veggy', 'vegetable', 'flesh', 'paper', 'cloth', 'leather', 'rubber']);

function polyselfFormLowerName(form) {
    return String(form?.name || '').toLowerCase();
}

function polyselfFormWhirly(form) {
    const name = polyselfFormLowerName(form);
    return !!(form?.whirly || form?.mlet === 'v' || form?.glyph === 'v' || name === 'air elemental');
}

function polyselfFormNoncorporeal(form) {
    const name = polyselfFormLowerName(form);
    return !!(form?.noncorporeal || form?.mlet === 'ghost' || name === 'ghost' || name === 'shade');
}

function polyselfFormSmallEnoughForArmorSlip(form) {
    const name = polyselfFormLowerName(form);
    return !!(form?.verysmall || form?.tiny || form?.msize === 'tiny' || form?.msize === 'small'
        || form?.size === 'tiny' || form?.size === 'small' || POLYSELF_SMALL_SLIPARM_FORM_NAMES.has(name));
}

function polyselfFormSlipsArmor(form) {
    return !!(polyselfFormSmallEnoughForArmorSlip(form) || polyselfFormWhirly(form) || polyselfFormNoncorporeal(form));
}

function polyselfFormNoHandsFallout(form) {
    return !!(form?.nohands || form?.verysmall || polyselfFormWhirly(form));
}

function polyselfFormSlithy(form) {
    const name = polyselfFormLowerName(form);
    return !!(form?.slithy || /\b(?:snake|eel|worm|naga|salamander)\b/.test(name));
}

function polyselfFormCentaur(form) {
    return !!(form?.mlet === 'C' || form?.glyph === 'C' || /\bcentaur\b/.test(polyselfFormLowerName(form)));
}

function polyselfFormBootFallout(form) {
    return !!(polyselfFormNoHandsFallout(form) || polyselfFormSlithy(form) || polyselfFormCentaur(form));
}

function polyselfFormHornCount(form) {
    const explicit = Number(form?.horns ?? form?.numHorns);
    if (Number.isFinite(explicit) && explicit > 0) return Math.trunc(explicit);
    const name = polyselfFormLowerName(form);
    if (POLYSELF_TWO_HORN_FORM_NAMES.has(name)) return 2;
    if (POLYSELF_ONE_HORN_FORM_NAMES.has(name)) return 1;
    return form?.hasHorns ? 1 : 0;
}

function polyselfFormAdultDragon(form) {
    const name = polyselfFormLowerName(form);
    return !!(name.endsWith(' dragon') && !name.startsWith('baby '));
}

function polyselfFormBreaksArmor(form) {
    if (!form || polyselfFormSlipsArmor(form)) return false;
    const name = polyselfFormLowerName(form);
    return !!(form.breakarm || form.breaksArmor || form.big
        || polyselfFormAdultDragon(form) || POLYSELF_BREAKARM_FORM_NAMES.has(name));
}

function polyselfFormIsMummy(form) {
    return !!(form && (form.mlet === 'M' || form.glyph === 'M' || /\bmummy\b/i.test(form.name || '')));
}

function polyselfFormHumanoidForWrapping(form) {
    const name = polyselfFormLowerName(form);
    return !!(form?.humanoid || form?.human || form?.mlet === 'humanoid' || form?.mlet === 'human'
        || form?.mlet === '@' || form?.glyph === '@' || form?.mlet === 'G' || form?.glyph === 'G'
        || name === 'hobbit' || name === 'dwarf' || name === 'gnome'
        || name === 'gnome leader' || name === 'gnome ruler');
}

function polyselfCloakSimpleName(cloak) {
    const kind = objectKindKey(cloak);
    if (kind === 'robe') return 'robe';
    if (kind === 'mummy wrapping') return 'wrapping';
    if (kind === 'alchemy smock') return cloak?.known && cloak?.dknown ? 'smock' : 'apron';
    return 'cloak';
}

function addPolyselfCloakOffMessages(cloak, messages) {
    if (objectKindKey(cloak) !== 'mummy wrapping' || !game.u?.invisible || game.u?.blind) return;
    newsym(game.u.ux, game.u.uy);
    messages.push(`You can ${game.u.seeInvisible ? 'see through yourself' : 'no longer see yourself'}.`);
}

function polyselfHelmSimpleName(helm) {
    return POLYSELF_HARD_HELM_KINDS.has(objectKindKey(helm)) ? 'helm' : 'hat';
}

function polyselfFalloffSurfaceName(x = game.u?.ux, y = game.u?.uy) {
    const loc = game.level?.at?.(x, y);
    const typ = movementSurfaceTerrain(loc && loc.typ == null ? { ...loc, typ: ROOM } : loc);
    if (IS_AIR(typ)) return Is_waterlevel(game.u?.uz) ? 'air bubble' : typ === CLOUD ? 'cloud' : 'air';
    if (IS_POOL(typ) || typ === WATER) return (game.u?.underwater || game.u?.uunderwater) && !Is_waterlevel(game.u?.uz) ? 'bottom' : 'water';
    if (typ === ICE) return 'ice';
    if (IS_LAVA(typ)) return 'lava';
    if (typ === DRAWBRIDGE_DOWN) return 'bridge';
    if (typ === ALTAR) return 'altar';
    if (typ === GRAVE) return 'headstone';
    if (typ === FOUNTAIN) return 'fountain';
    if (typ === STAIRS || typ === LADDER) return 'stairs';
    if (IS_WALL(typ) || typ === SDOOR) return 'wall';
    if (typ === DOOR) return 'doorway';
    if (IS_ROOM(typ) && !Is_earthlevel(game.u?.uz)) return 'floor';
    return 'ground';
}

function polyselfHeadgearIsFlimsy(helm) {
    const material = String(helm?.material || helm?.oc_material || '').toLowerCase().replace(/^hi_/, '');
    if (POLYSELF_FLIMSY_MATERIALS.has(material)) return true;
    const kind = objectKindKey(helm);
    if (kind === 'elven leather helm' || kind === 'fedora' || kind === 'cornuthaum' || kind === 'dunce cap') return true;
    return !POLYSELF_HARD_HELM_KINDS.has(kind);
}

function polyselfHeadgearBeingDonned(helm) {
    return !!(helm?.donning || helm?._donning || helm?.takingOff);
}

function polyselfMummyWrappingAllowed(form, cloak) {
    const name = polyselfFormLowerName(form);
    return objectKindKey(cloak) === 'mummy wrapping'
        && !polyselfFormNoncorporeal(form)
        && form?.mlet !== 'C' && form?.glyph !== 'C'
        && name !== 'marilith' && name !== 'winged gargoyle'
        && (polyselfFormIsMummy(form) || polyselfFormHumanoidForWrapping(form));
}

function polyselfRacialArmorException(form, armor) {
    return polyselfFormLowerName(form) === 'hobbit' && /\belven\b/.test(objectKindKey(armor));
}

function dragonArmorSpecForItem(item) {
    return DRAGON_ARMOR_SPEC_BY_KIND.get(objectKindKey(item)) || null;
}

function matchingPolyselfDragonArmor(form) {
    const bodyArmor = polyselfWornBodyArmorItem();
    const spec = dragonArmorSpecForItem(bodyArmor);
    if (!spec || polyselfFormLowerName(form) !== `${spec.colorName} dragon`) return null;
    return { armor: bodyArmor, spec };
}

function polyselfEmbeddedDragonSkinLine(item) {
    const letter = item.letter ? `${item.letter} - ` : '';
    const kind = objectKindKey(item);
    const enchantment = item.spe == null ? '' : `${item.spe >= 0 ? '+' : ''}${item.spe} `;
    const article = item.noArticle || armorNameIsPlural(kind) ? '' : /^[aeiou]/i.test(kind) ? 'an ' : 'a ';
    return `${letter}${article}${enchantment}${kind} (embedded in your skin)`;
}

function mergePolyselfDragonArmorWithSkin(armor, spec) {
    if (!armor || !spec) return '';
    const wasMail = spec.armorKind === 'mail';
    if (wasMail) {
        const scalesKind = `${spec.colorName} dragon scales`;
        armor.kind = scalesKind;
        armor.actualKind = scalesKind;
        armor.otyp = spec.scalesOtyp;
        armor.dragonArmorKind = 'scales';
        armor.noArticle = true;
        armor.color = spec.color;
        armor._display_color = spec.color;
    }
    armor.dragonArmor = true;
    armor.otyp = spec.scalesOtyp;
    armor.dragonArmorKind = 'scales';
    armor.noArticle = true;
    armor.color = spec.color;
    armor._display_color = spec.color;
    armor._polyselfSkin = true;
    armor.worn = false;
    armor.owornmask = 0;
    armor.line = polyselfEmbeddedDragonSkinLine(armor);
    return wasMail
        ? `Your ${spec.colorName} scale mail reverts to scales as you merge with them.`
        : 'You merge with your scaly armor.';
}

function polyselfSkinbackMessages(silently = false) {
    const messages = [];
    for (const item of game.inventory || []) {
        if (!item?._polyselfSkin) continue;
        delete item._polyselfSkin;
        item.worn = true;
        item.owornmask = 0;
        item.line = normalInventoryLine({ ...item, line: '' });
        if (!silently) messages.push('Your skin returns to its original form.');
    }
    return messages;
}

function polyselfEyewearFalloffName(item) {
    const kind = objectKindKey(item);
    if (kind === 'lenses') return 'lenses';
    if (kind === 'blindfold' || kind === 'towel') return kind;
    return inventoryItemName(item).replace(/^(?:an?|the) /i, '').replace(/^pair of /i, '');
}

function clearPolyselfEyewearState(item, form) {
    if (!polyselfIsEyewearItem(item) || !game.u) return;
    if (objectKindKey(item) !== 'lenses') game.u.blind = !!form?.noeyes;
    game.u.blindfolded = false;
    game.u.Blindfolded = false;
}

function polyselfEquipmentFalloutForForm(form, { bodyArmor = null } = {}) {
    const items = [];
    const destroyedItems = [];
    const releasedItems = [];
    const messages = [];
    const addItem = item => {
        if (item && !items.includes(item)) items.push(item);
    };
    const addDestroyedItem = item => {
        if (item && !destroyedItems.includes(item)) destroyedItems.push(item);
    };
    const addReleasedItem = item => {
        if (item && !releasedItems.includes(item)) releasedItems.push(item);
    };
    const addDroppedOrReleasedWeapon = weapon => {
        if (!weapon) return;
        if (polyselfWieldedWeaponCanDrop(weapon)) addItem(weapon);
        else addReleasedItem(weapon);
    };

    if (polyselfFormBreaksArmor(form)) {
        const armor = bodyArmor || polyselfWornBodyArmorItem();
        if (armor) {
            messages.push('You break out of your armor!');
            addPolyselfBodyArmorOffMessages(armor, messages);
            addDestroyedItem(armor);
        }

        const cloak = polyselfWornCloakItem();
        if (cloak && !polyselfMummyWrappingAllowed(form, cloak)) {
            const cloakName = polyselfCloakSimpleName(cloak);
            const cloakKind = objectKindKey(cloak);
            if (cloakKind === 'mummy wrapping') {
                messages.push(`Your ${cloakName} tears apart!`);
                addDestroyedItem(cloak);
            } else if (cloakKind === 'alchemy smock') {
                messages.push(`The knot on your ${cloakName} is pulled apart!`);
                addItem(cloak);
            } else {
                messages.push(`The clasp on your ${cloakName} breaks open!`);
                addItem(cloak);
            }
            addPolyselfCloakOffMessages(cloak, messages);
        }

        const shirt = polyselfWornShirtItem();
        if (shirt) {
            messages.push('Your shirt rips to shreds!');
            addDestroyedItem(shirt);
        }
    } else {
        if (polyselfFormSlipsArmor(form)) {
            const whirly = polyselfFormWhirly(form);
            const armor = bodyArmor || polyselfWornBodyArmorItem();
            if (armor && !polyselfRacialArmorException(form, armor)) {
                if (!bodyArmor) messages.push('Your armor falls around you!');
                addPolyselfBodyArmorOffMessages(armor, messages);
                addItem(armor);
            }
            const cloak = polyselfWornCloakItem();
            if (cloak && !polyselfMummyWrappingAllowed(form, cloak)) {
                messages.push(whirly
                    ? `Your ${polyselfCloakSimpleName(cloak)} falls, unsupported!`
                    : `You shrink out of your ${polyselfCloakSimpleName(cloak)}!`);
                addPolyselfCloakOffMessages(cloak, messages);
                addItem(cloak);
            }
            const shirt = polyselfWornShirtItem();
            if (shirt) {
                messages.push(whirly ? 'You seep right through your shirt!' : 'You become much too small for your shirt!');
                addItem(shirt);
            }
        } else {
            addItem(bodyArmor);
        }
    }

    const hornCount = polyselfFormHornCount(form);
    if (hornCount > 0) {
        const helm = polyselfWornHelmItem();
        if (helm) {
            if (polyselfHeadgearIsFlimsy(helm) && !polyselfHeadgearBeingDonned(helm)) {
                const horns = hornCount === 1 ? 'horn' : 'horns';
                messages.push(`Your ${horns} ${hornCount === 1 ? 'pierces' : 'pierce'} through ${ownedEquipmentName(helm)}.`);
            } else {
                messages.push(`Your ${polyselfHelmSimpleName(helm)} falls to the ${polyselfFalloffSurfaceName()}!`);
                addItem(helm);
            }
        }
    }

    if (polyselfFormNoHandsFallout(form)) {
        const gloves = polyselfWornArmorMatching(/glove|gauntlet/);
        const weapon = polyselfWieldedWeaponItem();
        if (gloves) {
            messages.push(`You drop your gloves${weapon ? ' and weapon' : ''}!`);
            addDroppedOrReleasedWeapon(weapon);
            addItem(gloves);
        }

        const shield = polyselfWornArmorMatching(/shield/);
        if (shield) {
            messages.push('You can no longer hold your shield!');
            addItem(shield);
        }

        const helm = polyselfWornHelmItem();
        if (helm && !items.includes(helm)) {
            messages.push(`Your ${polyselfHelmSimpleName(helm)} falls to the ${polyselfFalloffSurfaceName()}!`);
            addItem(helm);
        }

    }

    if (polyselfFormBootFallout(form)) {
        const boots = polyselfWornArmorMatching(/boot|shoe/);
        if (boots) {
            messages.push(polyselfFormWhirly(form) ? 'Your boots fall away!' : `Your boots ${form.verysmall ? 'slide' : 'are pushed'} off your feet!`);
            addItem(boots);
        }
    }

    if (polyselfFormNoHandsFallout(form)) {
        const gloves = polyselfWornArmorMatching(/glove|gauntlet/);
        const weapon = polyselfWieldedWeaponItem();
        if (!gloves && weapon) {
            const canDrop = polyselfWieldedWeaponCanDrop(weapon);
            const which = polyselfWieldedWeaponDescription(weapon);
            messages.push(`You find you must ${canDrop ? 'drop' : 'release'} ${which === 'corpse' ? 'the' : 'your'} ${which}!`);
            if (canDrop) addItem(weapon);
            else addReleasedItem(weapon);
        }
    }

    if (polyselfFormHasNoHead(form)) {
        const eyewear = polyselfWornEyewearItem();
        if (eyewear) {
            const eyewearName = polyselfEyewearFalloffName(eyewear);
            messages.push(`Your ${eyewearName} ${eyewearName === 'lenses' ? 'fall' : 'falls'} off!`);
            addItem(eyewear);
        }
    }

    return { items, destroyedItems, releasedItems, messages };
}

function otherWornFastEquipment(item) {
    return (game.inventory || []).some(candidate => candidate !== item
        && ((isWornInventoryItem(candidate) && objectKindKey(candidate) === 'speed boots')
            || (isActiveInventoryExtrinsicItem(candidate) && isBlueDragonArmorKind(objectKindKey(candidate)))));
}

function changeHeroLuck(delta) {
    if (!game.u || !delta) return;
    const next = (game.u.uluck || 0) + delta;
    game.u.uluck = next < 0 ? Math.max(next, LUCKMIN) : next > 0 ? Math.min(next, LUCKMAX) : 0;
}

function adjustHeroWornAttributeBonus(attr, delta) {
    if (!game.u?.acurr?.a || !delta) return;
    const before = Number(game.u.acurr.a[attr] ?? 10);
    game.u.acurr.a[attr] = Math.max(3, Math.min(25, before + delta));
}

function heroRoleName() {
    return game.urole?.name?.m || game._startup_role || '';
}

function alignmentTypeFromValue(value) {
    if (value === A_LAWFUL || value === A_NEUTRAL || value === A_CHAOTIC) return value;
    const name = String(value ?? '').toLowerCase();
    if (name === 'lawful') return A_LAWFUL;
    if (name === 'neutral') return A_NEUTRAL;
    if (name === 'chaotic') return A_CHAOTIC;
    return null;
}

function startupAlignmentType() {
    return alignmentTypeFromValue(game._startup_align) ?? A_NEUTRAL;
}

function currentAlignmentBaseType() {
    const base = game.u?.ualignbase;
    const candidates = [
        game.u?._ualignbase_current,
        base?.current,
        base?.A_CURRENT,
        base?.[0],
        game._ualignbase_current,
        game._startup_align,
    ];
    for (const candidate of candidates) {
        const type = alignmentTypeFromValue(candidate);
        if (type !== null) return type;
    }
    return startupAlignmentType();
}

function restoreHeroOppositeAlignment(messages) {
    if (!game.u) return;
    const newAlign = currentAlignmentBaseType();
    const oldAlign = alignmentTypeFromValue(game.u.ualign?.type) ?? startupAlignmentType();
    game.u.ublessed = 0;
    game.u.ualign ??= { type: newAlign, record: 0 };
    game.u.ualign.type = newAlign;
    if (newAlign !== oldAlign) game.u.ualign.record = 0;
    messages.push(`Your mind is ${heroIsHallucinating() ? 'much of a muchness' : 'back in sync with your body'}.`);
}

function heroFloatsOverPolyselfBootFallout() {
    const form = polyselfForm();
    return !!(game.u?.levitating || game.u?.levitation || game.u?.Levitation
        || game.u?.flying || game.u?.Flying || form?.clinger);
}

function polyselfWaterFallLanding(x, y, targetMoveTyp) {
    const dirs = LANDING_DIRS.map((_, i) => i);
    for (let i = dirs.length; i > 0; i--) {
        const j = rn2(i);
        const k = dirs[j];
        dirs[j] = dirs[i - 1];
        dirs[i - 1] = k;
    }
    for (const idx of dirs) {
        const lx = x + LANDING_DIRS[idx].dx;
        const ly = y + LANDING_DIRS[idx].dy;
        const loc = game.level?.at(lx, ly);
        const locMoveTyp = movementSurfaceTerrain(loc);
        if (!loc || IS_OBSTRUCTED(locMoveTyp) || movementIsLiquidAt(lx, ly, loc)) continue;
        if ((game.level?.monsters || []).some(mon => mon.mx === lx && mon.my === ly)) continue;
        game._relocate_after_more = { fromX: x, fromY: y, x: lx, y: ly };
        game._topline_after_more = targetMoveTyp === WATER
            ? 'Pheew!  That was close.'
            : 'You try to crawl out of the water.  Pheew!  That was close.';
        return true;
    }
    if (game.u) {
        game.u.uinwater = 1;
        game.u.underwater = true;
        game.u.uunderwater = true;
    }
    return false;
}

function polyselfLavaFalloutMessage(targetMoveTyp) {
    return targetMoveTyp === LAVAWALL
        ? 'You fall into the wall of lava!  You burn to a crisp...'
        : 'You fall into the molten lava!  You burn to a crisp...';
}

function addPolyselfWaterWalkingLavaFallout(item, messages, targetMoveTyp) {
    item.known = true;
    recordKnownArmorDiscovery('water walking boots', false);
    d(6, 6);
    if (game.u?.fireResistance) {
        game.u.utrap = rn1(4, 4) + (rn1(4, 12) << 8);
        game.u.utraptype = TT_LAVA;
        messages.push(targetMoveTyp === LAVAWALL
            ? 'You sink into the wall of lava, but it only burns slightly!'
            : 'You sink into the molten lava, but it only burns slightly!');
        if ((game.u.uhp || 0) > 1) game.u.uhp--;
        return true;
    }
    messages.push(polyselfLavaFalloutMessage(targetMoveTyp));
    game._death_cause = 'burned by molten lava';
    game._command_mode = 'lavaDeathMore';
    game._polyself_lava_death_more = 1;
    useUpInventoryItem(item, item.quan || 1);
    return true;
}

function addPolyselfWaterWalkingBootsOffSideEffects(item, messages) {
    if (objectKindKey(item) !== 'water walking boots' || !game.u || heroFloatsOverPolyselfBootFallout()) return;
    const x = game.u.ux || 0;
    const y = game.u.uy || 0;
    const loc = game.level?.at(x, y);
    const targetMoveTyp = movementSurfaceTerrain(loc);
    if (movementIsLavaAt(x, y, loc)) {
        addPolyselfWaterWalkingLavaFallout(item, messages, targetMoveTyp);
        return;
    }
    if (!movementIsPoolAt(x, y, loc)) return;
    item.known = true;
    recordKnownArmorDiscovery('water walking boots', false);
    polyselfWaterFallLanding(x, y, targetMoveTyp);
    messages.push(targetMoveTyp === WATER
        ? 'You plunge into the wall of water!  You try to crawl out of the water.'
        : 'You fall into the pool of water!  You sink like a rock.');
}

function heroHasOtherLevitationSource(item) {
    return !!(game.u?.levitation || game.u?.Levitation || (game.u?._levitationTimeout || 0) > 0
        || (game.inventory || []).some(candidate => candidate !== item && isWornInventoryItem(candidate)
            && (objectKindKey(candidate) === 'levitation boots' || objectKindKey(candidate) === 'ring of levitation')));
}

function heroAvoidsPolyselfLevitationPoolFallout() {
    const form = polyselfForm();
    return !!(game.u?.waterWalking || game.u?.Wwalking || game.u?.swimming || game.u?.Swimming
        || game.u?.amphibious || game.u?.Amphibious || game.u?.breathless || game.u?.Breathless
        || form?.swimmer || form?.amphibious || form?.breathless || form?.nonliving);
}

function ordinaryPolyselfLevitationFloatDownAllowed(x, y, loc) {
    if (!game.u?.levitating || game.u?.flying || game.u?.Flying) return false;
    if (game.u?.uswallow || game.u?.ustuck || game.u?.uinwater || game.u?.underwater || game.u?.uunderwater) return false;
    if (Is_airlevel(game.u?.uz) || Is_waterlevel(game.u?.uz)) return false;
    if (movementIsLiquidAt(x, y, loc)) return false;
    const surfaceTyp = movementSurfaceTerrain(loc && loc.typ == null ? { ...loc, typ: ROOM } : loc);
    if (surfaceTyp == null || IS_AIR(surfaceTyp)) return false;
    return !(game.level?.traps || []).some(trap => trap.tx === x && trap.ty === y);
}

function clearPolyselfLevitationBootSource(item) {
    game.u.levitating = false;
    game.u.levitation = false;
    game.u.Levitation = false;
    item.known = true;
    recordKnownArmorDiscovery('levitation boots', false);
}

function addPolyselfLevitationPoolFallout(item, messages, x, y, loc) {
    if (!game.u?.levitating || game.u?.flying || game.u?.Flying) return false;
    if (game.u?.uswallow || game.u?.ustuck || game.u?.uinwater || game.u?.underwater || game.u?.uunderwater) return false;
    if (Is_airlevel(game.u?.uz) || Is_waterlevel(game.u?.uz)) return false;
    if (!movementIsPoolAt(x, y, loc) || heroAvoidsPolyselfLevitationPoolFallout()) return false;
    const targetMoveTyp = movementSurfaceTerrain(loc);
    clearPolyselfLevitationBootSource(item);
    polyselfWaterFallLanding(x, y, targetMoveTyp);
    messages.push(targetMoveTyp === WATER
        ? 'You plunge into the wall of water!  You try to crawl out of the water.'
        : 'You fall into the pool of water!  You sink like a rock.');
    return true;
}

function polyselfLevitationFloatDownMessage(x, y, loc) {
    if (!game.u?.levitating) return '';
    if (game.u.flying || game.u.Flying) return 'You have stopped levitating and are now flying.';
    if (Is_airlevel(game.u.uz)) return 'You begin to tumble in place.';
    if (Is_waterlevel(game.u.uz)) return 'You feel heavier.';
    if (!ordinaryPolyselfLevitationFloatDownAllowed(x, y, loc)) return '';
    return `You float gently to the ${polyselfFalloffSurfaceName(x, y)}.`;
}

function addPolyselfLevitationBootsOffSideEffects(item, messages) {
    if (objectKindKey(item) !== 'levitation boots' || !game.u || heroHasOtherLevitationSource(item)) return;
    const x = game.u.ux || 0;
    const y = game.u.uy || 0;
    const loc = game.level?.at(x, y);
    if (addPolyselfLevitationPoolFallout(item, messages, x, y, loc)) return;
    const floatDownMessage = polyselfLevitationFloatDownMessage(x, y, loc);
    if (!floatDownMessage) return;
    clearPolyselfLevitationBootSource(item);
    messages.push(floatDownMessage);
}

function addPolyselfBootsOffSideEffects(item, messages) {
    const kind = objectKindKey(item);
    if (kind === 'water walking boots') addPolyselfWaterWalkingBootsOffSideEffects(item, messages);
    if (kind === 'levitation boots') addPolyselfLevitationBootsOffSideEffects(item, messages);
    if (kind !== 'speed boots' || !game.u) return;
    if (otherWornFastEquipment(item) || (game.u._veryfastTimeout || 0) > 0) return;
    messages.push(`You feel yourself slow down${heroHasIntrinsicFast() ? ' a bit' : ''}.`);
}

function polyselfBodyArmorOffMessages(item) {
    if (!game.u || !isBlueDragonArmorKind(objectKindKey(item))) return [];
    if (otherWornFastEquipment(item) || (game.u._veryfastTimeout || 0) > 0) return [];
    return ['You slow down.'];
}

function addPolyselfBodyArmorOffMessages(item, messages = []) {
    messages.push(...polyselfBodyArmorOffMessages(item));
}

function clearPolyselfDeferredArmorWearState(items) {
    let changed = false;
    let speedChanged = false;
    let displacementChanged = false;
    for (const item of items || []) {
        if (!(game.inventory || []).includes(item)) continue;
        const slot = armorSlot(item);
        if (slot !== 'body' && slot !== 'cloak') continue;
        if (!isWornInventoryItem(item)) continue;
        if (slot === 'body' && isBlueDragonArmorKind(objectKindKey(item))) speedChanged = true;
        if (slot === 'cloak') displacementChanged = true;
        item.worn = false;
        item.owornmask = 0;
        if (typeof item.line === 'string')
            item.line = item.line.replace(/\s+\(being worn\)/g, '');
        changed = true;
    }
    if (changed) updateReflectionFromInventory();
    if (speedChanged) syncHeroSpeedState();
    if (displacementChanged) updateWornDisplacement();
}

function addPolyselfHelmetOffSideEffects(item, messages = []) {
    const kind = objectKindKey(item);
    if (kind === 'fedora' && heroRoleName() === 'Archeologist') changeHeroLuck(-1);
    if (polyselfHeadgearBeingDonned(item)) return;
    if (kind === 'cornuthaum') {
        adjustHeroWornAttributeBonus(A_CHA, heroRoleName() === 'Wizard' ? -1 : 1);
    } else if (kind === 'helm of brilliance') {
        const delta = -Math.trunc(Number(item?.spe ?? 0));
        adjustHeroWornAttributeBonus(A_INT, delta);
        adjustHeroWornAttributeBonus(A_WIS, delta);
        if (delta) recordKnownArmorDiscovery(kind, false);
    } else if (kind === 'helm of opposite alignment') {
        restoreHeroOppositeAlignment(messages);
    }
}

function addPolyselfGlovesOffSideEffects(item) {
    const kind = objectKindKey(item);
    if (kind === 'gauntlets of dexterity') {
        const delta = -Math.trunc(Number(item?.spe ?? 0));
        adjustHeroWornAttributeBonus(A_DEX, delta);
        if (delta) recordKnownArmorDiscovery(kind, false);
    } else if (kind === 'gauntlets of power') {
        updateGauntletsOfPowerStrength(kind, false);
        recordKnownArmorDiscovery(kind, false);
    }
}

function polyselfHelmetNeedsMonsterRefresh(item) {
    const kind = objectKindKey(item);
    return kind === 'helm of telepathy' || kind === 'helm of caution';
}

function dropPolyselfEquipmentItems(items, floorMessages = [], form = polyselfForm()) {
    let changed = false;
    for (const item of items || []) {
        if (!(game.inventory || []).includes(item)) continue;
        clearPolyselfEyewearState(item, form);
        const slot = armorSlot(item);
        const refreshMonsters = slot === 'helm' && polyselfHelmetNeedsMonsterRefresh(item);
        const pendingRelocationBefore = game._relocate_after_more;
        if (slot === 'helm') addPolyselfHelmetOffSideEffects(item, floorMessages);
        if (slot === 'gloves') addPolyselfGlovesOffSideEffects(item);
        if (slot === 'boots') addPolyselfBootsOffSideEffects(item, floorMessages);
        if (!(game.inventory || []).includes(item)) {
            changed = true;
            continue;
        }
        const pendingRelocationAfter = game._relocate_after_more;
        const dropTarget = pendingRelocationAfter && pendingRelocationAfter !== pendingRelocationBefore
            ? { x: pendingRelocationAfter.x, y: pendingRelocationAfter.y }
            : null;
        dropCarriedObjectAtHero(item, floorMessages, dropTarget);
        changed = true;
        if (refreshMonsters) seeMonsters();
    }
    if (changed) updateReflectionFromInventory();
}

function destroyPolyselfEquipmentItems(items) {
    let changed = false;
    for (const item of items || []) {
        if (!(game.inventory || []).includes(item)) continue;
        useUpInventoryItem(item, item.quan || 1);
        changed = true;
    }
    if (changed) updateReflectionFromInventory();
}

function releasePolyselfEquipmentItems(items) {
    for (const item of items || []) {
        if (!(game.inventory || []).includes(item)) continue;
        releasePolyselfWieldedItem(item);
    }
}

function applyPolyselfEquipmentFallout(fallout, floorMessages = [], form = polyselfForm()) {
    destroyPolyselfEquipmentItems(fallout?.destroyedItems);
    releasePolyselfEquipmentItems(fallout?.releasedItems);
    dropPolyselfEquipmentItems(fallout?.items, floorMessages, form);
}

function polyselfFalloutHasEffects(fallout) {
    return !!(fallout?.items?.length || fallout?.destroyedItems?.length || fallout?.releasedItems?.length || fallout?.messages?.length);
}

function polyselfFalloutNeedsMore() {
    return !!(game._relocate_after_more || game._polyself_lava_death_more);
}

function polyselfBasePersistentBlindness(base) {
    if (!base?.blind) return false;
    if ((base._blindTimeout || 0) > 0 || (base.ucreamed || 0) > 0) return false;
    return !base.blindFromWornBlindfold;
}

function polyselfNonFormBlindness(base) {
    if (polyselfWornBlindfoldOrTowelItem()) return true;
    if ((game.u?._blindTimeout || 0) > 0 || (game.u?.ucreamed || 0) > 0) return true;
    return polyselfBasePersistentBlindness(base);
}

function restorePolyselfBaseBlindness(base) {
    if (!game.u) return;
    game.u.blind = polyselfNonFormBlindness(base);
    game.u._blindAfterStatus = game.u.blind ? (base?._blindAfterStatus || 0) : 0;
    game.u._polyself_form_blinded = false;
}

function recomputePolyselfArmorClass(form = polyselfForm()) {
    if (!game.u) return;
    let ac = form?.mac ?? 10;
    for (const item of game.inventory || []) {
        if (isWornArmorItem(item)) ac -= wornArmorAcValueGreatestErosion(item);
    }
    game.u.uac = ac;
}

function becomeMonster(name) {
    const base = game.u._polyself_base;
    if (name === 'human' || name === game.urace?.noun || name === game._startup_race) {
        const wasFormBlinded = !!game.u._polyself_form_blinded;
        const skinbackMessages = polyselfSkinbackMessages(false);
        const newLevel = Math.max(1, Math.min(30, (game.u?.ulevel || 1) + rn2(5) - 2));
        rn2(10);
        const minExp = newLevel === 1 ? 0 : newLevel - 1 < 10 ? 10 * (2 ** (newLevel - 1))
            : newLevel - 1 < 20 ? 10000 * (2 ** (newLevel - 11))
                : 10000000 * (newLevel - 20);
        const maxExp = newLevel < 10 ? 10 * (2 ** newLevel)
            : newLevel < 20 ? 10000 * (2 ** (newLevel - 10))
                : 10000000 * (newLevel - 19);
        const newExp = minExp + rn2(maxExp - minExp);
        rn2(5);
        rn2(5);
        rn2(5);
        rn2(5);
	        const hpScale = 8 + rn2(4);
	        const hpGain = rnd(8);
	        const hpRaceGain = rnd(2);
	        const energyScale = 8 + rn2(4);
	        const initialEnergyRoll = rnd(3);
	        const levelEnergyRoll = rn2(7);
	        const hunger = rn2(500) + 500;
	        if (base) {
	            const baseLevel = base.ulevel || 1;
	            const baseHpMax = Math.max(1, base.uhpmax || game.u.uhpmax || 1);
	            const baseHp = Math.max(1, Math.min(base.uhp || baseHpMax, baseHpMax));
	            const role = game._startup_role || game.urole?.name?.m || 'Wizard';
	            const raceName = game._startup_race || game.urace?.noun || 'human';
	            const roleHp = ROLE_HP_ADVANCE[role] || ROLE_HP_ADVANCE.Tourist;
	            const raceHp = RACE_HP_ADVANCE[raceName] || RACE_HP_ADVANCE.human;
	            const roleEnergy = ROLE_EN_ADVANCE[role] || ROLE_EN_ADVANCE.Tourist;
	            const raceEnergy = RACE_EN_ADVANCE[raceName] || RACE_EN_ADVANCE.human;
	            const initialHp = base.initialHp || game._initialHp || baseHpMax;
	            const hpIncs = [...(base.uhpinc || [])];
	            if (!hpIncs[0]) hpIncs[0] = initialHp;
	            let hpExtra = baseHpMax;
	            for (let i = 0; i < baseLevel; i++) hpExtra -= hpIncs[i] || 0;
	            const con = game.u?.acurr?.a?.[4] || 10;
	            const conplus = con <= 3 ? -2 : con <= 6 ? -1 : con <= 14 ? 0 : con <= 16 ? 1 : con === 17 ? 2 : con === 18 ? 3 : 4;
	            const levelHpGain = Math.max(1, roleHp.lowFix + raceHp.lowFix + hpGain + hpRaceGain + conplus);
	            const hpmax = Math.max(newLevel, Math.round(Math.max(0, hpExtra) * hpScale / 10) + initialHp + levelHpGain);
	            const baseEnergyMax = Math.max(1, base.uenmax || game.u.uenmax || 1);
	            const baseEnergy = Math.max(0, Math.min(base.uen || 0, baseEnergyMax));
	            const initialEnergy = role === 'Wizard' && raceName === 'human'
	                ? 5 + initialEnergyRoll
	                : base.initialEnergy || game._initialEnergy || baseEnergyMax;
	            const energyIncs = [...(base.ueninc || [])];
	            if (!energyIncs[0]) energyIncs[0] = initialEnergy;
	            let energyExtra = baseEnergyMax;
	            for (let i = 0; i < baseLevel; i++) energyExtra -= energyIncs[i] || 0;
	            let levelEnergyGain = levelEnergyRoll + roleEnergy.lowFix + raceEnergy.lowFix;
	            if (role === 'Priest' || role === 'Wizard') levelEnergyGain *= 2;
	            else if (role === 'Healer' || role === 'Knight') levelEnergyGain = Math.trunc((3 * levelEnergyGain) / 2);
	            else if (role === 'Barbarian' || role === 'Valkyrie') levelEnergyGain = Math.trunc((3 * levelEnergyGain) / 4);
	            if (levelEnergyGain <= 0) levelEnergyGain = 1;
	            const energyMax = Math.max(newLevel, Math.round(Math.max(0, energyExtra) * energyScale / 10) + initialEnergy + levelEnergyGain);
	            game.u.uhp = Math.max(1, Math.round(baseHp * hpmax / baseHpMax));
	            game.u.uhpmax = hpmax;
	            game.u.uen = Math.max(0, Math.round(baseEnergy * energyMax / baseEnergyMax));
	            game.u.uenmax = energyMax;
	            game.u.uac = 10;
	            for (const item of game.inventory || []) {
	                if (!(item.cls === 'armor' && (item.worn || item.line?.includes('being worn')))) continue;
	                const armorName = String(item.actualKind || item.kind || inventoryItemName(item)).toLowerCase();
	                game.u.uac -= (ARMOR_AC_BONUS[armorName] ?? 0) + (item.spe ?? 0);
	            }
	            game.u.ulevel = newLevel;
	            game.u.uexp = newExp;
	            game.u.uhunger = hunger;
            if (base.rank && game.urole) game.urole.rank = base.rank;
        }
        game.u._glyph = null;
        game.u._glyphColor = undefined;
        game.u._monsterHd = null;
        game.u._monsterMove = null;
        game.u._polyself_form = null;
        game.u.umovement = NORMAL_SPEED;
        game.u._statusSuffix = '';
        game.u._strDisplay = null;
        if (wasFormBlinded) restorePolyselfBaseBlindness(base);
        game.u._polyself_base = null;
        const humanMessage = game._startup_gender === 'female' ? 'You feel like a new woman!' : 'You feel like a new man!';
        return { message: [...skinbackMessages, humanMessage].join('  ') };
    }

    const form = polyselfFormByName(name);
    if (!form) return null;
    const alreadyPolymorphed = !!game.u._polyself_base;
    addConductCount('polyselfs');
    if (!alreadyPolymorphed) {
        game.u._polyself_base = {
            uhp: game.u.uhp,
            uhpmax: game.u.uhpmax,
            uen: game.u.uen,
            uenmax: game.u.uenmax,
            uac: game.u.uac,
            ulevel: game.u.ulevel,
            blind: !!game.u.blind,
            _blindTimeout: game.u._blindTimeout || 0,
            _blindAfterStatus: game.u._blindAfterStatus || 0,
            blindFromWornBlindfold: !!polyselfWornBlindfoldOrTowelItem(),
            ucreamed: game.u.ucreamed || 0,
            uhpinc: [...(game.u.uhpinc || [])],
            ueninc: [...(game.u.ueninc || [])],
            initialHp: game._initialHp,
            initialEnergy: game._initialEnergy,
            rank: game.urole?.rank || null,
        };
    }
    if (!alreadyPolymorphed) rn2(2);
    rn2(19);
    if (form.neuter !== true) rn2(10);
    rn2(500);
    const hpLevel = form.hpLevel ?? form.mlevel;
    const hp = form.fixedHp ?? (form.name.includes('dragon') && hpLevel ? 4 * hpLevel + d(hpLevel, 4)
        : hpLevel ? d(hpLevel, 8) : rnd(4));
    const glyph = form.glyph || (form.mlet?.length === 1 ? form.mlet : form.name[0]);
    const wasFormBlinded = !!game.u._polyself_form_blinded;
    game.u.uhp = Math.max(1, hp);
    game.u.uhpmax = Math.max(1, hp);
    game.u.uac = form.mac ?? 10;
    game.u._glyph = glyph;
    game.u._glyphColor = form.color ?? NO_COLOR;
    game.u._monsterHd = form.mlevel ?? 0;
    game.u._monsterMove = form.mmove ?? NORMAL_SPEED;
    game.u._polyself_form = { ...form };
    game.u.umovement = NORMAL_SPEED;
    game.u._statusSuffix = form.inAir ? ' Fly' : '';
    if (form.noeyes) {
        game.u.blind = true;
        game.u._blindAfterStatus = 1;
        game.u._polyself_form_blinded = true;
    } else if (wasFormBlinded) {
        restorePolyselfBaseBlindness(game.u._polyself_base);
    }
    game.u._strDisplay = form.strong ? '18/**' : null;
    const rank = form.name.replace(/\b\w/g, ch => ch.toUpperCase());
    if (game.urole) game.urole.rank = { m: rank, f: rank };
    const article = /^[aeiou]/i.test(form.name) ? 'an' : 'a';
    const dragonMerge = matchingPolyselfDragonArmor(form);
    const dragonMergeMessage = dragonMerge
        ? mergePolyselfDragonArmorWithSkin(dragonMerge.armor, dragonMerge.spec)
        : '';
    let message = [dragonMergeMessage, `You turn into ${article} ${form.name}!`].filter(Boolean).join('  ');
    if (isBuriedBallTrapActive()) {
        if (form.passWalls && buriedBallToFreedom()) {
            message += '  The buried ball is no longer bound to you.';
        } else if ((form.amorphous || form.whirly || form.unsolid || form.noncorporeal) && buriedBallToFreedom()) {
            message += '  You slip free of the buried ball and chain.';
        }
    } else if (game.u?.uball && (form.amorphous || form.whirly || form.unsolid || form.noncorporeal)) {
        unpunishHero();
        message += '  You slip out of the iron chain.';
    }
    if (polyselfFormBreaksArmor(form)) {
        const fallout = polyselfEquipmentFalloutForForm(form);
        if (polyselfFalloutHasEffects(fallout)) {
            const floorMessages = [];
            applyPolyselfEquipmentFallout(fallout, floorMessages, form);
            recomputePolyselfArmorClass(form);
            message += `  ${[...fallout.messages, ...floorMessages].filter(Boolean).join('  ')}`;
            if (polyselfFalloutNeedsMore()) return { message, more: true };
        }
    }
    const cloak = polyselfWornCloakItem();
    if (cloak && !polyselfMummyWrappingAllowed(form, cloak) && (form.verysmall
        || (form.name === 'gnome' && !polyselfWornBodyArmorItem() && !polyselfWornShirtItem()))) {
        const cloakMessages = [`You shrink out of your ${polyselfCloakSimpleName(cloak)}!`];
        addPolyselfCloakOffMessages(cloak, cloakMessages);
        message += `  ${cloakMessages.join('  ')}`;
        game._polyself_cloak_after_more_letter = cloak.letter;
        game._topline_after_more = 'Your movements are slowed slightly because of your load.';
        game.u._statusSuffix = `${game.u._statusSuffix || ''} Burdened`;
        game.u.uac = (game.u.uac ?? 10) - wornArmorAcValueGreatestErosion(cloak);
        clearPolyselfDeferredArmorWearState([cloak]);
        return { message, more: true };
    }
    const bodyArmor = polyselfWornBodyArmorItem();
    if (bodyArmor && (form.nohands || form.verysmall)) {
        const fallout = polyselfEquipmentFalloutForForm(form, { bodyArmor });
        const bodyArmorMessages = polyselfBodyArmorOffMessages(bodyArmor);
        message += `  ${['Your armor falls around you!', ...bodyArmorMessages].join('  ')}`;
        game._topline_after_more = "You can't even move a handspan with this load!";
        game._topline_more_after_more = 1;
        game._polyself_drop_items_after_overload_more = 1;
        game._polyself_drop_items_after_overload_items = fallout.items;
        game._polyself_release_items_after_overload_items = fallout.releasedItems;
        game._polyself_drop_items_after_overload_message = fallout.messages
            .filter(entry => !bodyArmorMessages.includes(entry)).join('  ');
        game._polyself_drop_items_after_overload_ac = 9;
        game.u.uac = game.u._polyself_base?.uac ?? game.u.uac ?? 10;
        game._status_uac_before_more = game.u._polyself_base?.uac ?? game.u.uac ?? 10;
        game._status_uac_before_more_hold_count = 3;
        if (!String(game.u._statusSuffix || '').includes('Overloaded'))
            game.u._statusSuffix = `${game.u._statusSuffix || ''} Overloaded`;
        clearPolyselfDeferredArmorWearState(fallout.items);
        return { message, more: true };
    }
    const fallout = polyselfEquipmentFalloutForForm(form);
    if (polyselfFalloutHasEffects(fallout)) {
        const floorMessages = [];
        applyPolyselfEquipmentFallout(fallout, floorMessages, form);
        recomputePolyselfArmorClass(form);
        message += `  ${[...fallout.messages, ...floorMessages].filter(Boolean).join('  ')}`;
        if (polyselfFalloutNeedsMore()) return { message, more: true };
    }
    const wieldedTool = (game.inventory || []).find(item =>
        item.cls === 'tool' && (item.wielded || item.line?.includes('weapon in')));
    if (form.nohands && wieldedTool) {
        message += '  You find you must drop your tool!';
        game._polyself_tool_after_more_letter = wieldedTool.letter;
        game._topline_after_more = 'Your movements are now unencumbered.';
        game._topline_more_after_more = 1;
        game._queued_message_after_more = 'Use the command #monster to use your breath weapon.';
        if (form.name.includes('dragon')) game._polyself_after_more_ac = -1;
        return { message, more: true };
    }
    return { message };
}

function godsNoticeWish() {
    game.u.ublesscnt = (game.u.ublesscnt || 0) + rn2(100) + 50;
}

function wishedInventoryPhrase(item, wishedQuan = 1) {
    const bucPrefix = knownBlessCursePrefix(item);
    const baseVisibleName = game.u?.blind && item.cls === 'potion' ? 'potion'
        : game.u?.blind && item.cls === 'ring' ? 'ring'
            : game.u?.blind && item.cls === 'wand' ? 'wand'
                : `${erosionPrefix(item)}${pickupObjectName(item)}`;
    const visibleName = `${bucPrefix}${baseVisibleName}`;
    const displayQuan = item.quan || wishedQuan;
    const article = item.unique ? 'the'
        : item.noArticle ? ''
        : pairArticleObjectName(baseVisibleName) ? 'a pair of'
        : /^[aeiou]/i.test(visibleName) ? 'an' : 'a';
    return displayQuan > 1 ? `${displayQuan} ${visibleName}`
        : article === 'a pair of' ? `a ${bucPrefix}pair of ${baseVisibleName}`
            : article ? `${article} ${visibleName}` : visibleName;
}

function pairArticleObjectName(name) {
    const objectName = String(name || '').toLowerCase();
    return objectName === 'lenses'
        || objectName.endsWith('boots')
        || objectName.endsWith('shoes')
        || objectName.endsWith('gloves')
        || objectName.startsWith('gauntlets');
}

function makeRandomWishObject() {
    const oclass = WISH_RANDOM_CLASSES[rn2(WISH_RANDOM_CLASSES.length)];
    const item = mkobj(oclass, false);
    return Object.assign(item, object_display(item), { wishedfor: true });
}

async function finishRandomBlankWish(prefix = '') {
    recordWishConduct();
    game._wish_tries = 0;
    const letter = nextInventoryLetter();
    const item = Object.assign({ letter, quan: 1 }, makeRandomWishObject());
    item.line = `${letter} - ${wishedInventoryPhrase(item)}`;
    game.inventory ??= [];
    game.inventory.push(item);
    maybeAttachCarriedFigurineTimeout(item);
    game._pet_food_scan_inventory = game.inventory;
    godsNoticeWish();
    await setWishResultMessage(`${prefix}${item.line}.`);
}

const MAX_WISH_TRIES = 5;

function wishRetryPrompt() {
    return game.flags?.cmdassist
        ? "For what do you wish (enter 'help' for assistance)?"
        : 'For what do you wish?';
}

function isExplicitNoWishText(text) {
    return /^(?:nothing|nil|none)$/i.test(String(text || '').trim().replace(/\s+/g, ' '));
}

async function handleNoFittingWish() {
    const tries = Math.max(0, Math.trunc(Number(game._wish_tries || 0))) + 1;
    game._wish_text = '';
    if (tries < MAX_WISH_TRIES) {
        game._wish_tries = tries;
        game._command_mode = 'wizardWish';
        await setMessage(`Nothing fitting that description exists in the game.  ${wishRetryPrompt()}`);
        return;
    }
    game._wish_tries = 0;
    game._command_mode = null;
    await finishRandomBlankWish("Nothing fitting that description exists in the game.  That's enough tries!  ");
}

function isWishedMoneyName(lowerName) {
    return /^(?:gold(?: pieces?)?|coins?|zorkmids?|money|\$)$/.test(lowerName);
}

function wishedMoneyQuantity(lowerName, quantity, forced) {
    let amount = Math.trunc(Number(quantity || 0));
    if (!forced && /^(?:gold pieces|coins|zorkmids)$/.test(lowerName)) amount = 2;
    amount = Math.max(1, amount);
    return game.flags?.debug ? amount : Math.min(amount, 5000);
}

function goldStackWeight(quantity) {
    const amount = Math.max(1, Math.trunc(Number(quantity || 0)));
    return Math.max(1, Math.trunc((amount + 50) / 100));
}

function wizardTrapWishMatch(lowerName) {
    for (const [trapName, trapType] of WIZARD_WISH_TRAP_TYPES.entries()) {
        if (lowerName === trapName || lowerName.startsWith(`${trapName} `))
            return { trapName, trapType };
    }
    return null;
}

function heroTerrainCell() {
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    return { x, y, loc: game.level?.at?.(x, y) };
}

function normalizeWizardTerrainWishName(lowerName) {
    return String(lowerName || '').trim().replace(/\s+/g, ' ');
}

async function createWizardTrapWishResult(trapWish) {
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    let trapType = trapWish.trapType;
    if ((trapType === HOLE || trapType === TRAPDOOR) && !canFallThroughLevel(game.u?.uz))
        trapType = ROCKTRAP;
    const trap = await maketrap(x, y, trapType);
    if (trap) {
        const trapName = trap.ttyp === trapWish.trapType
            ? trapWish.trapName
            : TRAP_NAMES[trap.ttyp] || trapWish.trapName;
        const suffix = trap.ttyp === MAGIC_PORTAL ? ' to nowhere' : '';
        newsym(x, y);
        await setWishResultMessage(`${upstartText(articleForName(trapName))}${suffix}.`);
    } else {
        await setWishResultMessage(`Creation of ${articleForName(trapWish.trapName)} failed.`);
    }
    return true;
}

function clearWizardTerrainTimers(loc, x, y) {
    if (!loc) return;
    delete loc.meltIceTurn;
    delete loc.meltIceTimeout;
    delete loc.meltIceAwayTurn;
    if (game.level?.meltIceTimers)
        game.level.meltIceTimers = game.level.meltIceTimers.filter(timer => timer.x !== x || timer.y !== y);
}

function deleteWizardTerrainEngraving(x, y) {
    if (!game.level?.engravings) return;
    game.level.engravings = game.level.engravings.filter(engr => engr.x !== x || engr.y !== y);
}

function addWizardHeadstone(x, y) {
    deleteWizardTerrainEngraving(x, y);
    game.level.engravings ??= [];
    game.level.engravings.push({ x, y, text: 'Rest in peace.', type: HEADSTONE });
}

function adjustWizardTerrainCounters(oldTyp, newTyp) {
    game.level.flags ??= {};
    if (oldTyp === FOUNTAIN && newTyp !== FOUNTAIN && game.level.flags.nfountains)
        game.level.flags.nfountains--;
    if (oldTyp !== FOUNTAIN && newTyp === FOUNTAIN)
        game.level.flags.nfountains = (game.level.flags.nfountains || 0) + 1;
    if (oldTyp === SINK && newTyp !== SINK && game.level.flags.nsinks)
        game.level.flags.nsinks--;
    if (oldTyp !== SINK && newTyp === SINK)
        game.level.flags.nsinks = (game.level.flags.nsinks || 0) + 1;
}

function clearWizardTerrainMetadata(loc, { keepHorizontal = false } = {}) {
    loc.doormask = D_NODOOR;
    loc.wall_info = 0;
    if (!keepHorizontal) loc.horizontal = false;
    loc.blessedftn = 0;
    loc.fountainLooted = false;
    loc.fountainWarned = false;
    loc.disturbed = false;
    loc.altarmask = 0;
    delete loc.icedpool;
}

function finalizeWizardTerrainWish(x, y, oldTyp) {
    const loc = game.level?.at?.(x, y);
    if (!loc) return;
    adjustWizardTerrainCounters(oldTyp, loc.typ);
    if (!(loc.typ === ICE || ((loc.typ === DRAWBRIDGE_UP || loc.typ === DRAWBRIDGE_DOWN)
        && ((loc.flags || 0) & DB_UNDER) === DB_ICE)))
        clearWizardTerrainTimers(loc, x, y);
    newsym(x, y);
}

function drawbridgeTerrainMessage(kind, loc) {
    return `${kind} ${loc?.typ === DRAWBRIDGE_UP ? 'in front of' : 'under'} the drawbridge.`;
}

function waterTerrainName(typ) {
    if (typ === MOAT) return 'moat';
    if (typ === WATER) return Is_waterlevel(game.u?.uz) ? 'limitless water' : 'wall of water';
    return 'pool of water';
}

function alignName(align) {
    if (align === A_CHAOTIC) return 'chaotic';
    if (align === A_NEUTRAL) return 'neutral';
    if (align === A_LAWFUL) return 'lawful';
    if (align === A_NONE) return 'unaligned';
    return 'unknown';
}

function randomWizardAltarAlign() {
    return !rn2(6) ? A_NONE : (rn2(A_LAWFUL + 2) - 1);
}

function wizardAltarAlign(name) {
    if (name.startsWith('chaotic ')) return A_CHAOTIC;
    if (name.startsWith('neutral ')) return A_NEUTRAL;
    if (name.startsWith('lawful ')) return A_LAWFUL;
    if (name.startsWith('unaligned ')) return A_NONE;
    return randomWizardAltarAlign();
}

function wizardDoorBaseTerrain(typ) {
    return typ === DOOR || typ === SDOOR || typ === IRONBARS
        || (IS_WALL(typ) && typ !== DBWALL);
}

function wizardDoorMask(terrainWish, loc) {
    const state = terrainWish.lockState || '';
    let mask = state === 'locked' ? D_LOCKED
        : (state === 'doorless' || terrainWish.secret) ? D_NODOOR
            : state === 'open' ? D_ISOPEN
                : state === 'broken' ? D_BROKEN
                    : D_CLOSED;
    let trapped = terrainWish.trappedState;
    if (trapped === 2 || (((mask & (D_LOCKED | D_CLOSED)) === 0) && !terrainWish.secret))
        trapped = 0;
    if (trapped) mask |= D_TRAPPED;
    return mask;
}

function wizardDoorDescription(loc) {
    const parts = [];
    const mask = loc.doormask || D_NODOOR;
    if (mask & D_TRAPPED) parts.push('trapped');
    if (mask & D_LOCKED) parts.push('locked');
    if (loc.typ === SDOOR) {
        parts.push('secret door');
    } else {
        if (mask & D_CLOSED) parts.push('closed');
        if (mask & D_ISOPEN) parts.push('open');
        if (mask & D_BROKEN) parts.push('broken');
        if ((mask & ~D_TRAPPED) === D_NODOOR) parts.push('doorless doorway');
        else parts.push('door');
    }
    return parts.join(' ');
}

function wizardWallTypeAt(x, y) {
    const north = isok(x, y - 1) && IS_WALL(game.level?.at?.(x, y - 1)?.typ);
    const south = isok(x, y + 1) && IS_WALL(game.level?.at?.(x, y + 1)?.typ);
    if (north || south)
        return VWALL;
    return HWALL;
}

function roomWishCanOverwriteTerrain(typ) {
    if (typ === STAIRS || typ === LADDER) return false;
    return typ === ROOM || IS_FURNITURE(typ) || typ === ICE || IS_POOL(typ) || IS_LAVA(typ);
}

async function createWizardTerrainWishResult(terrainWish) {
    const { x, y, loc } = heroTerrainCell();
    if (!loc) return false;
    game.level.flags ??= {};
    const oldTyp = loc.typ;
    const isDbridge = oldTyp === DRAWBRIDGE_DOWN || oldTyp === DRAWBRIDGE_UP;
    if (terrainWish.kind === 'fountain') {
        clearWizardTerrainMetadata(loc);
        loc.typ = FOUNTAIN;
        loc.flags = terrainWish.looted ? F_LOOTED : 0;
        loc.fountainLooted = !!terrainWish.looted;
        loc.fountainWarned = false;
        loc.blessedftn = terrainWish.blessed ? 1 : 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage(`A ${loc.blessedftn ? 'magic ' : ''}fountain.`);
        return true;
    }
    if (terrainWish.kind === 'throne') {
        clearWizardTerrainMetadata(loc);
        loc.typ = THRONE;
        loc.flags = terrainWish.looted ? T_LOOTED : 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('A throne.');
        return true;
    }
    if (terrainWish.kind === 'sink') {
        clearWizardTerrainMetadata(loc);
        loc.typ = SINK;
        loc.flags = terrainWish.looted ? (S_LPUDDING | S_LDWASHER | S_LRING) : 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('A sink.');
        return true;
    }
    if (terrainWish.kind === 'water') {
        if (isDbridge) {
            loc.flags = ((loc.flags || 0) & ~DB_UNDER) | DB_MOAT;
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(drawbridgeTerrainMessage('Moat', loc));
        } else {
            clearWizardTerrainMetadata(loc);
            loc.typ = terrainWish.typ;
            loc.flags = 0;
            deleteWizardTerrainEngraving(x, y);
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(`${upstartText(articleForName(waterTerrainName(loc.typ)))}.`);
        }
        return true;
    }
    if (terrainWish.kind === 'lava') {
        if (isDbridge) {
            loc.flags = ((loc.flags || 0) & ~DB_UNDER) | DB_LAVA;
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(drawbridgeTerrainMessage('Lava', loc));
        } else {
            clearWizardTerrainMetadata(loc);
            loc.typ = terrainWish.typ;
            loc.flags = 0;
            loc.lit = true;
            deleteWizardTerrainEngraving(x, y);
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(`A ${loc.typ === LAVAPOOL ? 'pool' : 'wall'} of molten lava.`);
        }
        return true;
    }
    if (terrainWish.kind === 'ice') {
        if (isDbridge) {
            loc.flags = ((loc.flags || 0) & ~DB_UNDER) | DB_ICE;
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(drawbridgeTerrainMessage('Ice', loc));
        } else {
            clearWizardTerrainMetadata(loc);
            loc.typ = ICE;
            loc.flags = oldTyp === ROOM ? ICED_POOL : ICED_MOAT;
            loc.icedpool = loc.flags;
            deleteWizardTerrainEngraving(x, y);
            if (terrainWish.melting) startMeltIceTimeout(x, y, 0);
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage('Ice.');
        }
        return true;
    }
    if (terrainWish.kind === 'altar') {
        clearWizardTerrainMetadata(loc);
        loc.typ = ALTAR;
        loc.flags = Align2amask(terrainWish.align);
        loc.altarmask = loc.flags;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage(`${upstartText(articleForName(`${alignName(terrainWish.align)} altar`))}.`);
        return true;
    }
    if (terrainWish.kind === 'grave') {
        if ((oldTyp === ROOM || oldTyp === GRAVE)
            && !(game.level?.traps || []).some(trap => trap.tx === x && trap.ty === y)) {
            clearWizardTerrainMetadata(loc);
            loc.typ = GRAVE;
            loc.flags = terrainWish.disturbed ? 1 : 0;
            loc.disturbed = !!terrainWish.disturbed;
            addWizardHeadstone(x, y);
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(`A ${loc.disturbed ? 'disturbed ' : ''}grave.`);
        } else {
            await setWishResultMessage("Can't place a grave here.");
        }
        return true;
    }
    if (terrainWish.kind === 'tree') {
        clearWizardTerrainMetadata(loc);
        loc.typ = TREE;
        loc.flags = terrainWish.looted ? (TREE_LOOTED | TREE_SWARM) : 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('A tree.');
        return true;
    }
    if (terrainWish.kind === 'bars') {
        clearWizardTerrainMetadata(loc);
        loc.typ = IRONBARS;
        loc.flags = 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('Iron bars.');
        return true;
    }
    if (terrainWish.kind === 'cloud') {
        clearWizardTerrainMetadata(loc);
        loc.typ = CLOUD;
        loc.flags = 0;
        deleteWizardTerrainEngraving(x, y);
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('A cloud.');
        return true;
    }
    if (terrainWish.kind === 'door') {
        if (!wizardDoorBaseTerrain(oldTyp)) {
            await setWishResultMessage(`${terrainWish.secret ? 'Secret door' : 'Door'} requires door or wall location.`);
            return true;
        }
        const oldWallInfo = oldTyp !== DOOR ? (loc.wall_info || 0) : 0;
        const keepHorizontal = true;
        clearWizardTerrainMetadata(loc, { keepHorizontal });
        loc.typ = terrainWish.secret ? SDOOR : DOOR;
        loc.doormask = wizardDoorMask(terrainWish, loc);
        loc.wall_info = terrainWish.secret ? (oldWallInfo & WM_MASK) : 0;
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage(`${upstartText(articleForName(wizardDoorDescription(loc)))}.`);
        return true;
    }
    if (terrainWish.kind === 'wall') {
        clearWizardTerrainMetadata(loc);
        loc.typ = wizardWallTypeAt(x, y);
        loc.flags = 0;
        loc.horizontal = loc.typ === HWALL;
        fix_wall_spines(Math.max(0, x - 1), Math.max(0, y - 1),
            Math.min(COLNO - 1, x + 1), Math.min(ROWNO - 1, y + 1));
        finalizeWizardTerrainWish(x, y, oldTyp);
        await setWishResultMessage('A wall.');
        return true;
    }
    if (terrainWish.kind === 'secret-corridor') {
        if (oldTyp === CORR) {
            clearWizardTerrainMetadata(loc);
            loc.typ = SCORR;
            loc.flags = 0;
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage('Secret corridor.');
        } else {
            await setWishResultMessage('Secret corridor requires corridor location.');
        }
        return true;
    }
    if (terrainWish.kind === 'room') {
        if (isDbridge) {
            loc.flags = ((loc.flags || 0) & ~DB_UNDER) | DB_FLOOR;
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage(drawbridgeTerrainMessage('Floor', loc));
        } else if (roomWishCanOverwriteTerrain(oldTyp)) {
            clearWizardTerrainMetadata(loc);
            loc.typ = ROOM;
            loc.flags = 0;
            game.level.traps = (game.level.traps || [])
                .filter(trap => trap.tx !== x || trap.ty !== y || trap.ttyp === MAGIC_PORTAL);
            finalizeWizardTerrainWish(x, y, oldTyp);
            await setWishResultMessage('Room floor.');
        } else {
            await setWishResultMessage('Room|floor|ground not allowed here.');
        }
        return true;
    }
    return false;
}

function singularizeWizardTerrainWishName(name) {
    if (name.endsWith('iron bars')) return name;
    for (const [plural, singular] of [
        ['fountains', 'fountain'], ['thrones', 'throne'], ['sinks', 'sink'],
        ['pools', 'pool'], ['moats', 'moat'], ['altars', 'altar'],
        ['graves', 'grave'], ['headstones', 'headstone'], ['trees', 'tree'],
        ['clouds', 'cloud'], ['doors', 'door'], ['doorways', 'doorway'],
        ['walls', 'wall'], ['corridors', 'corridor'], ['rooms', 'room'],
        ['floors', 'floor'], ['grounds', 'ground'],
    ]) {
        if (name.endsWith(plural)) return `${name.slice(0, -plural.length)}${singular}`;
    }
    return name;
}

function wizardTerrainWishMatch(lowerName, qualifiers = {}) {
    if (!game.flags?.debug) return null;
    let name = normalizeWizardTerrainWishName(lowerName);
    name = singularizeWizardTerrainWishName(name);
    const looted = !!qualifiers.looted;
    if (name.endsWith('fountain')) {
        const prefix = name.slice(0, -'fountain'.length).trim();
        return { kind: 'fountain', blessed: prefix === 'magic' || !!qualifiers.blessed, looted };
    }
    if (name.endsWith('throne')) {
        return { kind: 'throne', looted };
    }
    if (name.endsWith('sink')) {
        return { kind: 'sink', looted };
    }
    if (name.endsWith('pool') || name.endsWith('moat') || name.endsWith('wall of water')) {
        const typ = name.endsWith('pool') ? POOL : name.endsWith('moat') ? MOAT : WATER;
        return { kind: 'water', typ };
    }
    if (name.endsWith('lava') || name.endsWith('wall of lava')) {
        const typ = name.endsWith('wall of lava') ? LAVAWALL : LAVAPOOL;
        return { kind: 'lava', typ };
    }
    if (name.endsWith('ice')) {
        return { kind: 'ice', melting: name.startsWith('melting ') };
    }
    if (name.endsWith('altar')) {
        return { kind: 'altar', align: wizardAltarAlign(name) };
    }
    if (name.endsWith('grave') || name.endsWith('headstone')) {
        return { kind: 'grave', disturbed: looted };
    }
    if (name.endsWith('tree')) {
        return { kind: 'tree', looted };
    }
    if (name.endsWith('bars')) {
        return { kind: 'bars' };
    }
    if (name.endsWith('cloud')) {
        return { kind: 'cloud' };
    }
    if (name.endsWith('door') || (qualifiers.lockState === 'doorless' && name.endsWith('doorway'))) {
        return {
            kind: 'door',
            secret: name.endsWith('secret door'),
            lockState: qualifiers.lockState || '',
            trappedState: qualifiers.trappedState || 0,
        };
    }
    if (name.endsWith('wall') && (name === 'wall' || name[name.length - 5] === ' ')) {
        return { kind: 'wall' };
    }
    if (name.endsWith('secret corridor')) {
        return { kind: 'secret-corridor' };
    }
    if (name.endsWith('room') || name.endsWith('floor') || name.endsWith('ground')) {
        return { kind: 'room' };
    }
    return null;
}

function wizardBearLandTrapWish(lowerName, qualifiers = {}) {
    if (!game.flags?.debug) return null;
    const name = normalizeWizardTerrainWishName(lowerName);
    const match = name.match(/^(bear\s*trap|land\s*mine)(.*)$/);
    if (!match) return null;
    const isBear = match[1].startsWith('bear');
    const suffix = match[2] || '';
    const objectName = isBear ? 'beartrap' : 'land mine';
    if (qualifiers.trappedState === 2 || suffix.toLowerCase() === ' object')
        return { result: 'object', objectName };
    if (qualifiers.trappedState === 1 || suffix)
        return { result: 'trap', trapName: isBear ? 'bear trap' : 'land mine', trapType: isBear ? BEAR_TRAP : LANDMINE };
    return { result: 'object', objectName };
}

async function tryWizardNonObjectWish(lowerName, qualifiers = {}) {
    if (!game.flags?.debug) return false;
    const trapWish = wizardTrapWishMatch(lowerName);
    if (trapWish) return createWizardTrapWishResult(trapWish);
    const terrainWish = wizardTerrainWishMatch(lowerName, qualifiers);
    if (terrainWish) return createWizardTerrainWishResult(terrainWish);
    return false;
}

function capWishSpe(spe) {
    const sign = spe < 0 ? -1 : 1;
    return sign * Math.min(Math.abs(spe), SPE_LIM);
}

function parseWishedChargeSuffix(name) {
    const text = String(name || '');
    if (text.length <= 1) return { name: text, type: 'none' };
    const open = text.lastIndexOf('(');
    if (open < 0) return { name: text, type: 'none' };
    const beforeCut = open > 0 && text[open - 1] === ' ' ? open - 1 : open;
    let pos = open + 1;

    if (text.slice(pos, pos + 4).toLowerCase() === 'lit)') {
        return {
            name: text.slice(0, beforeCut) + text.slice(pos + 4),
            type: 'lit',
        };
    }

    const readDigits = start => {
        let end = start;
        while (end < text.length && text.charCodeAt(end) >= 48 && text.charCodeAt(end) <= 57)
            end++;
        return {
            value: end > start ? Number(text.slice(start, end)) : 0,
            end,
        };
    };

    let first = readDigits(pos);
    let spe = first.value;
    let recharged = 0;
    pos = first.end;
    if (text[pos] === ':') {
        recharged = spe;
        first = readDigits(pos + 1);
        spe = first.value;
        pos = first.end;
    }
    if (text[pos] !== ')') {
        return {
            name: text.slice(0, beforeCut),
            type: 'invalid',
            spe: 0,
            recharged: 0,
        };
    }
    if (!Number.isFinite(spe) || spe > SPE_LIM) spe = SPE_LIM;
    if (!Number.isFinite(recharged) || recharged < 0 || recharged > 7) recharged = 7;
    return {
        name: text.slice(0, beforeCut) + text.slice(pos + 1),
        type: 'charge',
        spe,
        recharged,
    };
}

function wishedSpeForItem(item, spe) {
    let requested = Math.min(Math.abs(Math.trunc(Number(spe || 0))), SPE_LIM);
    let negative = spe < 0;
    if (game.flags?.debug) return negative && requested ? -requested : requested;

    const generated = item?.spe ?? 0;
    const cls = itemClassKey(item);
    const wishMetadata = wishObjectMetadataForItem(item);
    if (cls === 'armor' || cls === 'weapon' || isWeaponTool(item) || isChargeableRing(item)) {
        if (requested > rnd(5) && requested > generated)
            requested = 0;
        if (requested > 2 && ((game.u?.uluck || 0) + (game.u?.moreluck || 0)) < 0)
            negative = true;
    } else {
        if (cls === 'wand' || item?.otyp === WAND_CLASS || wishMetadata?.negativeSpeLikeWand || isCrystalBallObject(item)) {
            if (requested > 1 && negative) requested = 1;
        } else if (requested > 0 && negative) {
            requested = 0;
        }
        if (requested > generated) requested = generated;
    }
    return negative && requested ? -requested : requested;
}

function applyWishedBuc(item, { wishedBlessed, wishedUncursed, wishedCursed, negativeSpe }) {
    const badLuck = ((game.u?.uluck || 0) + (game.u?.moreluck || 0)) < 0 && !game.flags?.debug;
    if (wishedCursed) {
        item.cursed = true;
        item.blessed = false;
    } else if (wishedUncursed) {
        item.blessed = false;
        item.cursed = badLuck;
    } else if (wishedBlessed) {
        item.blessed = !badLuck;
        item.cursed = badLuck;
    } else if (negativeSpe) {
        item.cursed = true;
        item.blessed = false;
    }
}

function objectKindKey(item) {
    return String(item?.actualKind || item?.kind || item?.spellName || item?.wand || item?.gemDescription || '').toLowerCase();
}

function itemClassKey(item) {
    return item?.cls || (item?.otyp === DART ? 'weapon'
        : item?.otyp === ARMOR_CLASS ? 'armor'
            : item?.otyp === WEAPON_CLASS ? 'weapon'
                : item?.otyp === FOOD_CLASS ? 'food'
                    : '');
}

function wishObjectMetadataForName(name) {
    const key = String(name || '').trim().toLowerCase();
    return WISH_OBJECT_METADATA.get(WISH_OBJECT_METADATA_ALIASES.get(key) || key);
}

function wishObjectMetadataForItem(item) {
    if (!item) return null;
    const actual = String(item.actualKind || '').toLowerCase();
    const kind = String(item.kind || '').toLowerCase();
    const spell = String(item.spellName || item.spell?.name || '').toLowerCase();
    const wand = String(item.wand || '').toLowerCase();
    for (const key of [actual, kind, spell, wand]) {
        const metadata = wishObjectMetadataForName(key);
        if (metadata) return metadata;
    }
    const metadata = WISH_OBJECT_METADATA_BY_OTYP.get(item.otyp);
    if (!metadata) return null;
    const cls = itemClassKey(item);
    if (cls === metadata.cls || item.glyph === metadata.glyph) return metadata;
    return null;
}

function isPotionObject(item) {
    return item?.cls === 'potion' || item?.otyp === POTION_CLASS || item?.otyp === POT_WATER
        || POTION_INDEX_BY_OTYP.has(item?.otyp);
}

function isBlindingVenomObject(item) {
    const kind = objectKindKey(item).replace(/^splash of /, '');
    return item?.otyp === BLINDING_VENOM || kind === 'blinding venom';
}

function isAcidVenomObject(item) {
    const kind = objectKindKey(item).replace(/^splash of /, '');
    return item?.otyp === ACID_VENOM || kind === 'acid venom';
}

function isVenomObject(item) {
    return !!item && (item.cls === 'venom' || isBlindingVenomObject(item) || isAcidVenomObject(item));
}

function potionIdentityName(item) {
    if (!item || !isPotionObject(item)) return '';
    if (item.potionIndex != null) return IDENTIFIED_POTION_NAMES[item.potionIndex] || '';
    if (item.otyp === POT_WATER) return 'water';
    const index = POTION_INDEX_BY_OTYP.get(item.otyp);
    return index != null ? IDENTIFIED_POTION_NAMES[index] || '' : '';
}

function isWaterPotion(item) {
    if (!isPotionObject(item)) return false;
    const kind = objectKindKey(item).replace(/^potion of /, '');
    return potionIdentityName(item) === 'water' || kind === 'water' || kind === 'holy water' || kind === 'unholy water';
}

function isPotionOfOil(item) {
    if (!isPotionObject(item)) return false;
    return potionIdentityName(item) === 'oil' || objectKindKey(item).replace(/^potion of /, '') === 'oil';
}

function isPotionOfAcid(item) {
    if (!isPotionObject(item)) return false;
    return potionIdentityName(item) === 'acid' || objectKindKey(item).replace(/^potion of /, '') === 'acid';
}

function isPotionOfPolymorph(item) {
    if (!isPotionObject(item)) return false;
    return potionIdentityName(item) === 'polymorph' || objectKindKey(item).replace(/^potion of /, '') === 'polymorph';
}

function potionDipKind(item) {
    if (!isPotionObject(item)) return '';
    return objectKindKey(item).replace(/^potion of /, '').trim();
}

function isHealingFamilyPotion(item) {
    const kind = potionDipKind(item);
    return kind === 'healing' || kind === 'extra healing' || kind === 'full healing';
}

function isPotionOfSickness(item) {
    return potionDipKind(item) === 'sickness';
}

function potionNeutralizationTargetKind(item) {
    const kind = objectKindKey(item).replace(/^(?:blessed|uncursed|cursed) /, '');
    if (kind === 'unicorn horn') return 'unicorn horn';
    if (kind === 'amethyst' || kind === 'amethyst stone') return 'amethyst';
    return '';
}

function isPotionNeutralizationTarget(item) {
    return !!potionNeutralizationTargetKind(item);
}

function potionNeutralizationResultKind(target, potion) {
    const targetKind = potionNeutralizationTargetKind(target);
    const sourceKind = potionDipKind(potion);
    if (targetKind === 'unicorn horn') {
        if (sourceKind === 'sickness') return 'fruit juice';
        if (sourceKind === 'hallucination' || sourceKind === 'blindness' || sourceKind === 'confusion') return 'water';
    } else if (targetKind === 'amethyst' && sourceKind === 'booze') {
        return 'fruit juice';
    }
    return '';
}

function maybeDilutedPotionName(obj, name) {
    if (!obj?.odiluted || isWaterPotion(obj) || name.startsWith('diluted ')) return name;
    return `diluted ${name}`;
}

function isCandleObject(item) {
    const kind = objectKindKey(item);
    return item?.otyp === TALLOW_CANDLE || item?.otyp === WAX_CANDLE
        || kind === 'tallow candle' || kind === 'wax candle';
}

function isLampObject(item) {
    const kind = objectKindKey(item);
    return item?.otyp === OIL_LAMP || item?.otyp === MAGIC_LAMP || item?.otyp === BRASS_LANTERN
        || kind === 'oil lamp' || kind === 'magic lamp' || kind === 'brass lantern';
}

function isOilRefuelLampObject(item) {
    const kind = objectKindKey(item);
    return item?.otyp === OIL_LAMP || item?.otyp === MAGIC_LAMP
        || kind === 'oil lamp' || kind === 'magic lamp';
}

function isCrystalBallObject(item) {
    const kind = toolChargeKind(item);
    return item?.otyp === CRYSTAL_BALL || kind === 'crystal ball'
        || (kind === 'glass orb' && String(item?.actualKind || '').toLowerCase() === 'crystal ball');
}

function isWishedLightSource(item) {
    return isLampObject(item) || isCandleObject(item) || isPotionOfOil(item);
}

function candleDefaultAge(item) {
    return item?.otyp === WAX_CANDLE || objectKindKey(item) === 'wax candle' ? 400 : 200;
}

function applyBurnAgeThreshold(item, thresholds) {
    const age = item.age ?? thresholds[0];
    for (const threshold of thresholds) {
        if (age > threshold) {
            item._burnTimer = age - threshold;
            item.age = threshold;
            return;
        }
    }
    if (age > 0) {
        item._burnTimer = age;
        item.age = 0;
    }
}

function beginWishedBurn(item) {
    item.lamplit = true;
    item.burning = true;
    if (isPotionOfOil(item)) {
        const age = item.age ?? 400;
        item._burnTimer = item.odiluted ? Math.trunc(age * 3 / 4) : age;
        item.age = 0;
        item.litRadius = 1;
    } else if (item.otyp === MAGIC_LAMP || objectKindKey(item) === 'magic lamp') {
        delete item._burnTimer;
    } else if (isCandleObject(item)) {
        if (item.age == null) item.age = candleDefaultAge(item);
        applyBurnAgeThreshold(item, [75, 15, 0]);
    } else {
        if (item.age == null) item.age = 1500;
        applyBurnAgeThreshold(item, [150, 100, 50, 25, 0]);
    }
}

function endWishedBurn(item) {
    item.lamplit = false;
    item.burning = false;
    delete item._burnTimer;
    delete item.litRadius;
}

function applyWishedLightState(item, state) {
    if (!isWishedLightSource(item) || state == null) return;
    if (state) beginWishedBurn(item);
    else endWishedBurn(item);
}

function maybeLitObjectName(obj, name) {
    if (!(obj?.lamplit || obj?.burning) || name.endsWith(' (lit)')) return name;
    return `${name} (lit)`;
}

function lampNoun(item) {
    return item?.otyp === BRASS_LANTERN || objectKindKey(item) === 'brass lantern' ? 'lantern' : 'lamp';
}

function lampUsageChargeCount(item) {
    if (item?.spe != null || item?.charges != null) return chargedUsageChargeCount(item);
    return 1;
}

function refreshLightSourceLine(item) {
    refreshInventoryObjectLine(item);
    if (item?.unpaid) syncUnpaidBillLine(item);
}

async function applyLamp(item) {
    const noun = lampNoun(item);
    const messages = [];
    if (item.lamplit || item.burning) {
        endWishedBurn(item);
        refreshLightSourceLine(item);
        messages.push(`Your ${noun} is now off.`);
    } else if (game.u?.underwater || game.u?.uunderwater) {
        messages.push('This is not a diving lamp.');
    } else if ((item.age ?? 1500) === 0
        || ((item.otyp === MAGIC_LAMP || objectKindKey(item) === 'magic lamp') && (item.spe ?? 1) <= 0)) {
        messages.push(noun === 'lantern'
            ? (game.u?.blind ? 'Nothing happens.' : 'Your lantern is out of power.')
            : `This ${pickupObjectName({ ...item, line: '' })} has no oil.`);
    } else if (item.cursed && !rn2(2)) {
        if (noun === 'lamp' && !rn2(3)) {
            addHeroGlibTimeout(d(2, 10));
            messages.push(`The lamp spills and covers your ${fingersOrGloves()} with oil.`);
        } else {
            messages.push(game.u?.blind ? 'Nothing happens.' : `${sentenceCase(pickupObjectName({ ...item, line: '' }))} flickers for a moment, then dies.`);
        }
    } else {
        checkUnpaidUsage(item, messages, { chargeCount: lampUsageChargeCount(item) });
        beginWishedBurn(item);
        refreshLightSourceLine(item);
        messages.push(`Your ${noun} is now on.`);
    }
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
}

const CRYSTAL_BALL_HALLUCINATION_MESSAGES = [
    'You grok some groovy globs of incandescent lava.',
    'Whoa!  Psychedelic colors, dude!',
    'The crystal pulses with sinister light!',
    'You see goldfish swimming above fluorescent rocks.',
    'You see tiny snowflakes spinning around a miniature farmhouse.',
    'Oh wow... like a kaleidoscope!',
];

const CRYSTAL_BALL_LEVEL_DETECTS = [
    'Delphi',
    "Medusa's lair",
    'a castle',
    "the Wizard of Yendor's tower",
];

const CRYSTAL_BALL_OBJECT_CLASSES = new Map([
    ['$', { label: 'gold', predicate: obj => obj?.glyph === '$' || obj?.cls === 'coin' }],
    ['"', { label: 'amulets', predicate: obj => obj?.glyph === '"' || obj?.cls === 'amulet' }],
    ['%', { label: 'food', predicate: obj => obj?.glyph === '%' || obj?.cls === 'food' }],
    ['?', { label: 'scrolls', predicate: obj => obj?.glyph === '?' || obj?.cls === 'scroll' }],
    ['+', { label: 'spellbooks', predicate: obj => obj?.glyph === '+' || obj?.cls === 'spellbook' }],
    ['!', { label: 'potions', predicate: obj => obj?.glyph === '!' || obj?.cls === 'potion' }],
    ['=', { label: 'rings', predicate: obj => obj?.glyph === '=' || obj?.cls === 'ring' }],
    ['/', { label: 'wands', predicate: obj => obj?.glyph === '/' || obj?.cls === 'wand' }],
    ['*', { label: 'gems', predicate: obj => obj?.glyph === '*' || obj?.cls === 'gem' }],
    ['(', { label: 'tools', predicate: obj => obj?.glyph === '(' || obj?.cls === 'tool' }],
    [')', { label: 'weapons', predicate: obj => obj?.glyph === ')' || obj?.cls === 'weapon' }],
    ['[', { label: 'armor', predicate: obj => obj?.glyph === '[' || obj?.cls === 'armor' }],
]);

function crystalBallObjectName(item) {
    return pickupObjectName({ ...(item || {}), line: '' }) || 'crystal ball';
}

function crystalBallSubject(item) {
    return `The ${crystalBallObjectName(item)}`;
}

function crystalBallTooBadMessage(item) {
    return `Too bad you can't see the ${crystalBallObjectName(item)}.`;
}

function crystalBallIntelligence() {
    return game.u?.acurr?.a?.[A_INT] ?? 10;
}

function crystalBallBackfire(item, messages) {
    if (!item || (item.spe ?? 0) <= 0) return false;
    const oops = item.artifact ? 8 : item.blessed ? 16 : 20;
    if (!item.cursed && rnd(oops) <= crystalBallIntelligence()) return false;

    const impair = rnd(Math.max(1, 100 - 3 * crystalBallIntelligence()));
    const roll = rnd((item.artifact || item.blessed) ? 4 : 5);
    const subject = crystalBallSubject(item);
    if (roll === 1) {
        messages.push(`${subject} is too much to comprehend!`);
    } else if (roll === 2) {
        messages.push(`${subject} confuses you!`);
        addHeroConfusion(impair);
    } else if (roll === 3) {
        messages.push(`${subject} damages your vision!`);
        if (game.u) {
            game.u.blind = true;
            game.u._blindTimeout = (game.u._blindTimeout || 0) + impair;
        }
    } else if (roll === 4) {
        messages.push(`${subject} zaps your mind!`);
        if (game.u) {
            game.u.hallucinating = true;
            game.u._halluTimeout = (game.u._halluTimeout || 0) + impair;
            addHeroStatusSuffix('Hallu');
        }
    } else {
        messages.push(`${subject} explodes!`);
        useUpInventoryItem(item, item.quan || 1);
        const damage = rnd(30);
        if (game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            if ((game.u.uhp || 0) <= 0) {
                game._death_cause = 'killed by an exploding crystal ball';
                messages.push('You die...');
            }
        }
        return true;
    }
    spendChargedToolUse(item, messages);
    return true;
}

function implodeCrystalBall(item, messages) {
    messages.push(`${crystalBallSubject(item)} implodes!`);
    useUpInventoryItem(item, item?.quan || 1);
}

function crystalBallHallucinationUse(item, messages) {
    if ((item?.spe ?? 0) <= 0) {
        messages.push('All you see is funky haze.');
        if ((item?.spe ?? 0) < 0) implodeCrystalBall(item, messages);
        return;
    }
    messages.push(CRYSTAL_BALL_HALLUCINATION_MESSAGES[rn2(CRYSTAL_BALL_HALLUCINATION_MESSAGES.length)]);
    spendChargedToolUse(item, messages);
}

function crystalBallTrapDetectionMessage() {
    const traps = game.level?.traps || [];
    const ux = game.u?.ux ?? -1;
    const uy = game.u?.uy ?? -1;
    const remote = traps.filter(trap => trap && (trap.tx !== ux || trap.ty !== uy));
    if (remote.length) {
        for (const trap of remote) {
            trap.tseen = true;
            const loc = game.level?.at?.(trap.tx, trap.ty);
            if (loc) {
                loc.remembered_glyph = { ch: '^', color: CLR_BROWN, dec: false };
                loc.waslit = true;
            }
            show_glyph_cell(trap.tx, trap.ty, '^', CLR_BROWN, false);
        }
        newsym(ux, uy);
        return 'You feel entrapped.';
    }
    if (traps.some(trap => trap && trap.tx === ux && trap.ty === uy)) return 'Your toes itch.';
    return 'Your toes stop itching.';
}

function crystalBallObjectDetectionMessage(ch) {
    const spec = CRYSTAL_BALL_OBJECT_CLASSES.get(ch);
    if (!spec) return '';
    const found = collectDetectedObjects(spec.predicate);
    if (found.remote.length) {
        markDetectedObjects(found.remote);
        return `You sense ${spec.label}.`;
    }
    if (found.here.length) return `You sense ${spec.label} nearby.`;
    return 'The vision is unclear.';
}

function crystalBallMonsterDetectionMessage(ch) {
    if (!/^[A-Za-z]$/.test(ch)) return '';
    const found = (game.level?.monsters || []).filter(mon =>
        mon && !mon.dead && (mon.mhp == null || mon.mhp > 0)
        && (mon.data?.mlet === ch || mon.data?.glyph === ch || String(mon.data?.name || '').startsWith(ch)));
    if (!found.length) return 'The vision is unclear.';
    game._detect_monsters_display = 1;
    return 'You sense the presence of monsters.';
}

function crystalBallLevelDetectionMessage() {
    const what = CRYSTAL_BALL_LEVEL_DETECTS[rn2(CRYSTAL_BALL_LEVEL_DETECTS.length)];
    return `You see ${what}, in the distance.`;
}

function crystalBallDetectionMessage(ch) {
    if (ch === '^') return crystalBallTrapDetectionMessage();
    return crystalBallObjectDetectionMessage(ch)
        || crystalBallMonsterDetectionMessage(ch)
        || crystalBallLevelDetectionMessage();
}

async function beginCrystalBallUse(item) {
    if (game.u?.blind || (game.u?._statusSuffix || '').includes('Blind')) {
        await setMessage(crystalBallTooBadMessage(item));
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }

    const messages = [];
    if (crystalBallBackfire(item, messages)) {
        await setMessage(messages.join('  '), messages.length > 1);
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }
    if (heroIsHallucinating()) {
        crystalBallHallucinationUse(item, messages);
        await setMessage(messages.join('  '), messages.length > 1);
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }

    game._apply_crystal_ball_letter = item?.letter || '';
    await setMessage(`${game.flags?.verbose === false ? '' : 'You may look for an object, monster, or special map symbol.  '}What do you look for?`);
    game._command_mode = 'crystalBallLook';
    return true;
}

async function finishCrystalBallUse(ch) {
    const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_crystal_ball_letter);
    game._apply_crystal_ball_letter = '';
    game._command_mode = null;
    if (!item) return false;
    if (ch !== '@' && (ch === 'q' || ch === '\x1b' || ch === '\r' || ch === '\n')) {
        if (game.flags?.verbose !== false) await setMessage('Never mind.');
        game.context.move = 1;
        return true;
    }

    const messages = [`You peer into the ${crystalBallObjectName(item)}...`];
    const charged = (item.spe ?? 0) > 0;
    if (!charged) {
        messages.push('The vision is unclear.');
        if ((item.spe ?? 0) < 0) implodeCrystalBall(item, messages);
    } else {
        item.known = true;
        item.dknown = true;
        item.actualKind = 'crystal ball';
        item.kind = 'crystal ball';
        spendChargedToolUse(item, messages);
        messages.push(crystalBallDetectionMessage(ch));
    }
    await setMessage(messages.filter(Boolean).join('  '), messages.length > 1);
    game.context.move = 1;
    return true;
}

const MUSICAL_INSTRUMENT_KINDS = new Set([
    'wooden flute', 'magic flute', 'tooled horn', 'frost horn', 'fire horn',
    'wooden harp', 'magic harp', 'bugle', 'leather drum', 'drum of earthquake',
]);
const CHARGED_MUSICAL_INSTRUMENT_KINDS = new Set([
    'magic flute', 'magic harp', 'frost horn', 'fire horn', 'drum of earthquake',
]);
const BLOW_MUSICAL_INSTRUMENT_KINDS = new Set([
    'wooden flute', 'magic flute', 'tooled horn', 'frost horn', 'fire horn', 'bugle',
]);
const DRUM_MUSICAL_INSTRUMENT_KINDS = new Set(['leather drum', 'drum of earthquake']);

function normalizeMusicalInstrumentKind(kind) {
    if (kind === 'flute') return 'wooden flute';
    if (kind === 'harp') return 'wooden harp';
    if (kind === 'drum') return 'leather drum';
    return kind;
}

function musicalInstrumentKind(item) {
    const kind = toolChargeKind(item);
    const normalized = normalizeMusicalInstrumentKind(kind);
    return MUSICAL_INSTRUMENT_KINDS.has(normalized) ? normalized : '';
}

function instrumentDisplayName(item) {
    return inventoryItemName(item).replace(/\s+\(\d+:-?\d+\)$/, '');
}

function instrumentTheName(item) {
    return `The ${instrumentDisplayName(item).replace(/^(?:an?|the) /i, '')}`;
}

function instrumentPlayName(item) {
    const name = instrumentDisplayName(item);
    if (/\bunpaid\b/i.test(name)) return name;
    return name.replace(/^(?:an?|the) /i, 'your ');
}

function instrumentSimpleName(item) {
    return instrumentDisplayName(item).replace(/^(?:an?|the) /i, '');
}

function heroCanBlowInstrument() {
    const form = polyselfForm();
    if ((game.u?._statusSuffix || '').includes('Strngl') || game.u?.strangled || game.u?.strangling)
        return false;
    if (!form) return true;
    const msound = String(form.msound || '').toLowerCase();
    const silentOrBuzz = !!form.silent || msound === 'buzz' || msound === 'ms_buzz';
    const noHead = form.hasHead === false || form.noHead || form.headless;
    const eelLike = form.mlet === ';' || /\beel\b/i.test(String(form.name || ''));
    return !(silentOrBuzz && (form.breathless || form.verysmall || noHead || eelLike));
}

function instrumentOpeningMessage() {
    let mode = 'normal';
    if (heroIsStunned() && heroIsConfused()) mode = 'stunned-confused';
    else if (heroIsStunned()) mode = 'stunned';
    else if (heroIsConfused()) mode = 'confused';
    if (heroIsHallucinating()) mode = mode === 'normal' ? 'hallucinating' : `${mode}-hallucinating`;

    if (!rn2(2)) {
        if (mode === 'stunned-confused')
            mode = !rn2(2) ? 'stunned' : 'confused';
        if (mode.includes('hallucinating'))
            mode = 'hallucinating';
    }

    if (mode === 'normal')
        return null;
    if (mode === 'hallucinating')
        return 'You disseminate a kaleidoscopic display of floating butterflies.';
    if (mode === 'stunned')
        return heroIsDeaf() ? 'You feel a monotonous vibration.' : 'You radiate an obnoxious droning sound.';
    if (mode === 'confused')
        return heroIsDeaf() ? 'You feel a jarring vibration.' : 'You generate a raucous noise.';
    return 'What you perform is quite far from music...';
}

function monsterDistanceSquaredFromHero(mon) {
    const dx = (mon?.mx || 0) - (game.u?.ux || 0);
    const dy = (mon?.my || 0) - (game.u?.uy || 0);
    return dx * dx + dy * dy;
}

function visibleInstrumentMonster(mon) {
    return !game.u?.blind && !mon?.minvis && !mon?.mundetected
        && !!(game.viz_array?.[mon?.my]?.[mon?.mx] & IN_SIGHT);
}

function rollInstrumentImprovisation() {
    game.context ??= {};
    if (!(heroHasUnchanging() && game.context.jingle)) {
        const notes = 'ABCDEFG';
        let tune = '';
        const count = rnd(5);
        for (let i = 0; i < count; i++) tune += notes[rn2(notes.length)];
        game.context.jingle = tune || 'C';
        return { notes: game.context.jingle, sameOldSong: false };
    }
    return { notes: game.context.jingle, sameOldSong: true };
}

function sleepMonstersWithMagicFlute(distance) {
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        if (monsterDistanceSquaredFromHero(mon) >= distance) continue;
        if (monsterResistsEffect(mon, game.u?.ulevel || 1)) continue;
        mon.msleeping = 1;
    }
}

function charmMonstersWithMagicHarp(distance) {
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        if (monsterDistanceSquaredFromHero(mon) > distance) continue;
        tameMonsterWithScroll(mon, {});
    }
}

function charmSnakesWithWoodenFlute(distance, messages) {
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        const data = mon.data || {};
        if (!(data.mlet === 'S' || data.snake || /\bsnake\b/i.test(String(data.name || '')))) continue;
        if (mon.mcanmove === false || monsterDistanceSquaredFromHero(mon) >= distance) continue;
        const wasPeaceful = !!mon.mpeaceful;
        const couldSee = visibleInstrumentMonster(mon);
        mon.mpeaceful = 1;
        mon.mavenge = 0;
        mon.mundetected = 0;
        newsym(mon.mx, mon.my);
        if (!visibleInstrumentMonster(mon)) continue;
        const name = mon.givenName || mon.shknam || `The ${data.name || 'snake'}`;
        messages.push(couldSee
            ? `${name} freezes, then sways with the music${wasPeaceful ? '' : ', and now seems quieter'}.`
            : `You notice ${name.toLowerCase()}, swaying with the music.`);
    }
}

function calmNymphsWithWoodenHarp(distance, messages) {
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        const data = mon.data || {};
        if (!(data.mlet === 'n' || data.nymph || /\bnymph\b/i.test(String(data.name || '')))) continue;
        if (mon.mcanmove === false || monsterDistanceSquaredFromHero(mon) >= distance) continue;
        mon.msleeping = 0;
        mon.mpeaceful = 1;
        mon.mavenge = 0;
        if (visibleInstrumentMonster(mon)) {
            const name = mon.givenName || mon.shknam || `The ${data.name || 'nymph'}`;
            messages.push(`${name} listens cheerfully to the music, then seems quieter.`);
        }
    }
}

function awakenScareInstrumentMonster(mon, scary) {
    mon.msleeping = 0;
    mon.mcanmove = true;
    mon.mfrozen = 0;
    mon.waiting = false;
    if (!scary || mon.data?.mindless) return false;
    const bound = Math.max(1, 110 - monsterResistanceLevel(mon));
    if (rn2(bound) < monsterMagicResistance(mon)) return false;
    const wasFleeing = !!mon.mflee;
    mon.mflee = 1;
    mon.mfleetim = 0;
    return !wasFleeing;
}

function awakenMonstersWithInstrument(distance) {
    const fleeMessages = [];
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        const dist = monsterDistanceSquaredFromHero(mon);
        if (dist >= distance) continue;
        const newlyFleeing = awakenScareInstrumentMonster(mon, dist < Math.trunc(distance / 3));
        if (newlyFleeing && visibleInstrumentMonster(mon)) {
            const name = mon.givenName || mon.shknam || `The ${mon.data?.name || 'monster'}`;
            fleeMessages.push(`${name} turns to flee.`);
        }
    }
    if (fleeMessages.length) game._topline_after_more = fleeMessages.join('  ');
}

function awakenSoldiersWithBugle(messages) {
    const distance = (game.u?.ulevel || 1) * 30;
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) continue;
        const data = mon.data || {};
        if (data.mercenary && data.name !== 'guard') {
            if (!mon.mtame) mon.mpeaceful = 0;
            mon.msleeping = 0;
            mon.mfrozen = 0;
            mon.mcanmove = true;
            mon.waiting = false;
            if (visibleInstrumentMonster(mon)) {
                const name = mon.givenName || mon.shknam || `The ${data.name || 'soldier'}`;
                messages.push(`${name} is now ready for battle!`);
            } else if (!heroIsDeaf()) {
                messages.push('You hear the rattle of battle gear being readied.');
            }
        } else {
            const dist = monsterDistanceSquaredFromHero(mon);
            if (dist < distance) awakenScareInstrumentMonster(mon, dist < Math.trunc(distance / 3));
        }
    }
}

function fireColdHornElement(item) {
    const kind = toolChargeKind(item);
    if (kind === 'fire horn') return 'fire';
    if (kind === 'frost horn') return 'cold';
    return '';
}

function identifyZapToolOrWand(item, element) {
    if (!item) return;
    const hornElement = fireColdHornElement(item);
    if (hornElement) {
        const hornKind = hornElement === 'fire' ? 'fire horn' : 'frost horn';
        item.known = true;
        item.dknown = true;
        item.actualKind = hornKind;
        item.kind = hornKind;
        updateChargedItemLine(item);
        return;
    }
    item.known = true;
    item.kind = element;
    item.line = `${item.letter} - a wand of ${element}${wandChargeSuffix(item)}`;
}

function hornBlastMessage(item) {
    const element = fireColdHornElement(item);
    if (!element || game.u?.blind) return '';
    return `A bolt of ${element} blasts out of the horn!`;
}

function tooledHornImprovisationEffect(messages, sameOldSong = false) {
    messages.push(heroIsDeaf()
        ? 'You blow into the horn.'
        : `You produce a frightful, grave${sameOldSong ? ', yet familiar,' : ''} sound.`);
    awakenMonstersWithInstrument((game.u?.ulevel || 1) * 30);
    exerciseAttribute(A_WIS, false);
}

function leatherDrumImprovisationEffect(messages, { mundaneDowngrade = false, sameOldSong = false } = {}) {
    if (!mundaneDowngrade) {
        if (!heroIsDeaf()) {
            messages.push(`You beat a ${sameOldSong ? 'familiar ' : ''}deafening row!`);
            if (game.u)
                game.u._deafTimeout = Math.max(game.u._deafTimeout || 0, rn1(20, 30));
            game._deaf_after_more = 1;
        } else {
            messages.push('You pound on the drum.');
        }
        exerciseAttribute(A_WIS, false);
    } else {
        const verb = rn2(2) ? 'butcher' : rn2(2) ? 'manage' : 'pull off';
        messages.push(`You ${verb} a drumbeat.`);
    }
    awakenMonstersWithInstrument((game.u?.ulevel || 1) * (mundaneDowngrade ? 5 : 40));
}

function normalizeManualTuneText(text) {
    return String(text || '')
        .replace(/\t/g, ' ')
        .replace(/^ +| +$/g, '')
        .replace(/ {2,}/g, ' ')
        .toUpperCase()
        .replace(/H/g, 'B');
}

function drawbridgeDirectionAt(x, y) {
    const loc = game.level?.at?.(x, y);
    if (!loc || (loc.typ !== DRAWBRIDGE_UP && loc.typ !== DRAWBRIDGE_DOWN)) return null;
    return (loc.flags || 0) & DB_DIR;
}

function wallCoordForDrawbridge(x, y) {
    const dir = drawbridgeDirectionAt(x, y);
    if (dir == null) return null;
    if (dir === DB_NORTH) return { x, y: y - 1 };
    if (dir === DB_SOUTH) return { x, y: y + 1 };
    if (dir === DB_EAST) return { x: x + 1, y };
    if (dir === DB_WEST) return { x: x - 1, y };
    return null;
}

function isDrawbridgeWallAt(x, y) {
    const loc = game.level?.at?.(x, y);
    if (!loc || (loc.typ !== DOOR && loc.typ !== DBWALL)) return -1;
    if (drawbridgeDirectionAt(x + 1, y) === DB_WEST) return DB_WEST;
    if (drawbridgeDirectionAt(x - 1, y) === DB_EAST) return DB_EAST;
    if (drawbridgeDirectionAt(x, y - 1) === DB_SOUTH) return DB_SOUTH;
    if (drawbridgeDirectionAt(x, y + 1) === DB_NORTH) return DB_NORTH;
    return -1;
}

function findDrawbridgeAtOrWall(x, y) {
    const loc = game.level?.at?.(x, y);
    if (loc?.typ === DRAWBRIDGE_UP || loc?.typ === DRAWBRIDGE_DOWN) return { x, y };
    const dir = isDrawbridgeWallAt(x, y);
    if (dir === DB_NORTH) return { x, y: y + 1 };
    if (dir === DB_SOUTH) return { x, y: y - 1 };
    if (dir === DB_EAST) return { x: x - 1, y };
    if (dir === DB_WEST) return { x: x + 1, y };
    return null;
}

function findAdjacentDrawbridge() {
    for (let y = (game.u?.uy || 0) - 1; y <= (game.u?.uy || 0) + 1; y++) {
        for (let x = (game.u?.ux || 0) - 1; x <= (game.u?.ux || 0) + 1; x++) {
            if (!isok(x, y)) continue;
            const found = findDrawbridgeAtOrWall(x, y);
            if (found) return found;
        }
    }
    return null;
}

function clearDrawbridgeSquareState(x, y) {
    if (!game.level) return;
    game.level.objects = (game.level.objects || []).filter(obj => obj.ox !== x || obj.oy !== y);
    game.level.traps = (game.level.traps || []).filter(trap => trap.tx !== x || trap.ty !== y);
    game.level.engravings = (game.level.engravings || []).filter(engr => engr.x !== x || engr.y !== y);
}

function drawbridgeVisible(x, y, wall) {
    if (game.u?.blind) return false;
    return !!(game.viz_array?.[y]?.[x] & IN_SIGHT)
        || !!(game.viz_array?.[wall.y]?.[wall.x] & IN_SIGHT);
}

function toggleDrawbridgeForTune(x, y) {
    const loc = game.level?.at?.(x, y);
    const wall = wallCoordForDrawbridge(x, y);
    const wallLoc = wall ? game.level?.at?.(wall.x, wall.y) : null;
    if (!loc || !wall || !wallLoc) return '';
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const distBridge = (ux - x) ** 2 + (uy - y) ** 2;
    const distWall = (ux - wall.x) ** 2 + (uy - wall.y) ** 2;
    let message = '';
    if (loc.typ === DRAWBRIDGE_UP) {
        message = drawbridgeVisible(x, y, wall)
            ? `You see a drawbridge ${distWall < distBridge ? 'going' : 'coming'} down!`
            : 'You hear gears turning and chains rattling.';
        loc.typ = DRAWBRIDGE_DOWN;
        wallLoc.typ = DOOR;
        wallLoc.doormask = D_NODOOR;
        if (game.u) {
            game.u.uevent ??= {};
            game.u.uevent.uopened_dbridge = 1;
        }
    } else if (loc.typ === DRAWBRIDGE_DOWN) {
        const coming = ((ux === x || uy === y) && !(game.u?.underwater || game.u?.uunderwater)) || distWall < distBridge;
        message = drawbridgeVisible(x, y, wall)
            ? `You see a drawbridge ${coming ? 'coming' : 'going'} up!`
            : 'You hear chains rattling and gears turning.';
        loc.typ = DRAWBRIDGE_UP;
        wallLoc.typ = DBWALL;
        wallLoc.wall_info = W_NONDIGGABLE;
        const dir = drawbridgeDirectionAt(x, y);
        wallLoc.horizontal = dir === DB_NORTH || dir === DB_SOUTH;
    }
    clearDrawbridgeSquareState(x, y);
    clearDrawbridgeSquareState(wall.x, wall.y);
    newsym(x, y);
    newsym(wall.x, wall.y);
    return message;
}

function castleTuneFeedbackMessage(tune) {
    const target = normalizeManualTuneText(game.castleTune || '');
    if (!target) return '';
    let tumblers = 0;
    let gears = 0;
    const matched = [false, false, false, false, false];
    for (let i = 0; i < tune.length && i < 5; i++) {
        if (tune[i] === target[i]) {
            gears++;
            matched[i] = true;
        } else {
            for (let j = 0; j < 5; j++) {
                if (!matched[j] && tune[i] === target[j] && tune[j] !== target[j]) {
                    tumblers++;
                    matched[j] = true;
                    break;
                }
            }
        }
    }
    if (gears === 5 && game.u) {
        game.u.uevent ??= {};
        game.u.uevent.uheard_tune = 2;
    }
    const tumblerText = tumblers === 1 ? '1 tumbler click' : `${tumblers} tumblers click`;
    const gearText = gears === 1 ? '1 gear turn' : `${gears} gears turn`;
    if (tumblers && gears) return `You hear ${tumblerText} and ${gearText}.`;
    if (tumblers) return `You hear ${tumblerText}.`;
    if (gears) return `You hear ${gearText}.`;
    return '';
}

function handleStrongholdManualTune(tune, messages) {
    if (!Is_stronghold(game.u?.uz)) return;
    exerciseAttribute(A_WIS, true);
    const found = findAdjacentDrawbridge();
    const target = normalizeManualTuneText(game.castleTune || '');
    if (target && tune === target && found) {
        game.u.uevent ??= {};
        game.u.uevent.uheard_tune = 2;
        const bridgeMessage = toggleDrawbridgeForTune(found.x, found.y);
        if (bridgeMessage) messages.push(bridgeMessage);
        return;
    }
    if (heroIsDeaf()) return;
    game.u.uevent ??= {};
    if ((game.u.uevent.uheard_tune || 0) < 1) game.u.uevent.uheard_tune = 1;
    if (found) {
        const feedback = castleTuneFeedbackMessage(tune);
        if (feedback) messages.push(feedback);
    }
}

async function finishInstrumentTune(item, tuneText = game._instrument_tune_text || '') {
    game._apply_instrument_letter = '';
    game._instrument_tune_text = '';
    game._command_mode = null;
    if (!item) return false;
    const tune = normalizeManualTuneText(tuneText);
    const name = instrumentTheName(item).replace(/^The /, 'the ');
    const message = heroIsDeaf()
        ? `You can feel ${name} emitting vibrations.`
        : `You extract a strange sound from ${name}!`;
    const messages = [message];
    handleStrongholdManualTune(tune, messages);
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
    return true;
}

async function finishMusicalImprovisation(item) {
    const kind = musicalInstrumentKind(item);
    if (!kind) return false;
    game._apply_instrument_letter = '';
    game._command_mode = null;

    const messages = [];
    const opening = instrumentOpeningMessage();
    if (opening) messages.push(opening);
    else messages.push(`You start playing ${instrumentPlayName(item)}.`);

    const { sameOldSong } = rollInstrumentImprovisation();
    const doSpec = !heroIsStunned() && !heroIsConfused();
    let effectiveKind = kind;
    let mundaneDowngrade = false;
    if ((!doSpec || (item.spe ?? 0) <= 0) && CHARGED_MUSICAL_INSTRUMENT_KINDS.has(kind)) {
        effectiveKind = kind === 'magic flute' ? 'wooden flute'
            : kind === 'magic harp' ? 'wooden harp'
                : kind === 'drum of earthquake' ? 'leather drum'
                    : 'tooled horn';
        mundaneDowngrade = true;
    }

    if (effectiveKind === 'wooden flute') {
        const dex = Math.max(1, game.u?.acurr?.a?.[A_DEX] ?? 10);
        const calms = doSpec && rn2(dex) + (game.u?.ulevel || 1) > 25;
        messages.push(heroIsDeaf()
            ? `You feel ${instrumentDisplayName(item)} ${calms ? 'trill' : 'toot'}.`
            : `${instrumentTheName(item)} ${calms ? 'trills' : 'toots'}${sameOldSong ? ' a familiar tune' : ''}.`);
        if (calms) charmSnakesWithWoodenFlute((game.u?.ulevel || 1) * 3, messages);
        exerciseAttribute(A_DEX, true);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    if (effectiveKind === 'wooden harp') {
        const dex = Math.max(1, game.u?.acurr?.a?.[A_DEX] ?? 10);
        const calms = doSpec && rn2(dex) + (game.u?.ulevel || 1) > 25;
        const normalMessage = calms
            ? `${instrumentTheName(item)} produces ${sameOldSong ? 'a familiar, ' : 'a '}lilting melody.`
            : `${instrumentTheName(item)} twangs${sameOldSong ? ' a familiar tune' : ''}.`;
        messages.push(heroIsDeaf() ? 'You feel soothing vibrations.' : normalMessage);
        if (calms) calmNymphsWithWoodenHarp((game.u?.ulevel || 1) * 3, messages);
        exerciseAttribute(A_DEX, true);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    if (effectiveKind === 'tooled horn') {
        tooledHornImprovisationEffect(messages, sameOldSong);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    if (effectiveKind === 'bugle') {
        messages.push(heroIsDeaf()
            ? 'You blow into the bugle.'
            : `You extract a loud${sameOldSong ? ', familiar' : ''} noise from ${instrumentDisplayName(item)}.`);
        awakenSoldiersWithBugle(messages);
        exerciseAttribute(A_WIS, false);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    if (effectiveKind === 'leather drum') {
        leatherDrumImprovisationEffect(messages, { mundaneDowngrade, sameOldSong });
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    spendChargedToolUse(item, messages);
    if (effectiveKind === 'magic flute') {
        const tone = heroIsHallucinating() ? 'piped' : 'soft';
        messages.push(heroIsDeaf()
            ? `You seem to produce ${tone}${sameOldSong ? ', familiar' : ''} music.`
            : `You produce ${tone}${sameOldSong ? ', familiar' : ''} music.`);
        sleepMonstersWithMagicFlute((game.u?.ulevel || 1) * 5);
    } else if (effectiveKind === 'magic harp') {
        messages.push(heroIsDeaf()
            ? 'You feel very soothing vibrations.'
            : `${instrumentTheName(item)} produces very attractive${sameOldSong ? ' and familiar' : ''} music.`);
        charmMonstersWithMagicHarp(Math.trunc(((game.u?.ulevel || 1) - 1) / 3) + 1);
    } else if (effectiveKind === 'drum of earthquake') {
        messages.push('You produce a heavy, thunderous rolling!');
        const earthquakeMessages = await doEarthquake(Math.trunc(((game.u?.ulevel || 1) - 1) / 3) + 1);
        messages.push(`The entire ${earthquakeLevelDescription()} is shaking around you!`, ...earthquakeMessages);
        awakenMonstersWithInstrument(ROWNO * COLNO);
        item.known = true;
        item.dknown = true;
        updateChargedItemLine(item);
    } else {
        game._zap_item = item;
        game._zap_prelude_messages = messages;
        game._command_mode = 'zapDirection';
        await setMessage([...messages, 'In what direction?'].join('  '), messages.length > 0);
        return true;
    }
    if (effectiveKind === 'magic flute' || effectiveKind === 'magic harp')
        exerciseAttribute(A_DEX, true);
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
    return true;
}

async function beginMusicalInstrumentUse(item) {
    const kind = musicalInstrumentKind(item);
    if (!kind) return false;
    if (game.u?.underwater || game.u?.uunderwater) {
        await setMessage("You can't play music underwater!");
        game._command_mode = null;
        return true;
    }
    if (BLOW_MUSICAL_INSTRUMENT_KINDS.has(kind) && !heroCanBlowInstrument()) {
        await setMessage(`You are incapable of playing the ${instrumentSimpleName(item)}.`);
        game._command_mode = null;
        return true;
    }
    if (!DRUM_MUSICAL_INSTRUMENT_KINDS.has(kind)
        && !heroIsStunned() && !heroIsConfused() && !heroIsHallucinating()) {
        game._apply_instrument_letter = item.letter || '';
        await setMessage('Improvise? [ynq] (q)');
        game._command_mode = 'instrumentImprovisePrompt';
        return true;
    }
    return finishMusicalImprovisation(item);
}

function splitOilPotionForApply(item) {
    return splitOneCarriedInventoryItem(item);
}

function identifyPotionOfOil(item) {
    if (!item) return;
    item.known = true;
    item.actualKind = 'potion of oil';
    item.kind = 'oil';
    item.potionIndex = 24;
    recordKnownPotionDiscovery(item, 'oil');
    learnObjectScore('Potions', 'potion of oil');
}

function identifyPotionOfPolymorph(item) {
    if (!item) return;
    item.known = true;
    item.actualKind = 'potion of polymorph';
    item.kind = 'polymorph';
    item.potionIndex = 19;
    recordKnownPotionDiscovery(item, 'polymorph');
    learnObjectScore('Potions', 'potion of polymorph');
}

function recordKnownToolDiscovery(toolName) {
    const name = String(toolName || '').trim();
    if (!name) return;
    game._discoveries ??= [];
    const existing = game._discoveries.find(entry => entry.section === 'Tools' && entry.name === name);
    if (existing) {
        existing.text = name;
        existing.starred = false;
        existing.known = true;
    } else {
        game._discoveries.push({ section: 'Tools', name, text: name, starred: false, known: true });
    }
    learnObjectScore('Tools', name);
}

function toolDiscoveryKnown(toolName) {
    const name = String(toolName || '').trim();
    return !!name && (game._discoveries || []).some(entry =>
        entry.section === 'Tools' && entry.name === name && entry.known !== false);
}

async function applyPotionOfOil(item) {
    const messages = [];
    if (item.lamplit || item.burning) {
        messages.push('You snuff the lit potion.');
        endWishedBurn(item);
        refreshLightSourceLine(item);
    } else if (game.u?.underwater || game.u?.uunderwater) {
        messages.push('There is not enough oxygen to sustain a fire.');
    } else {
        const potion = splitOilPotionForApply(item);
        messages.push(`You light your potion.${game.u?.blind ? '' : '  It gives off a dim light.'}`);
        if (potion.unpaid) {
            checkUnpaidUsage(potion, messages);
            messages.push('"That\'s in addition to the cost of the potion, of course."');
            markObjectShopBillUsedUp(potion);
        }
        identifyPotionOfOil(potion);
        beginWishedBurn(potion);
        refreshLightSourceLine(potion);
    }
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
}

function oilPotionDipSources(target) {
    return (game.inventory || []).filter(item => item !== target && isPotionOfOil(item));
}

const OIL_DIP_AMMO_RE = /\b(?:arrow|arrows|ya|bolt|bolts|dart|darts|shuriken|throwing star|throwing stars)\b/;

function isOilWeaponDipTargetObject(item) {
    const cls = itemClassKey(item);
    return cls === 'weapon' || item?.glyph === ')' || item?.otyp === WEAPON_CLASS || isWeaponTool(item);
}

function isPermanentPoisonedWeaponObject(item) {
    const artifact = String(item?.artifact || item?.oartifact || '').toLowerCase().replace(/^the /, '');
    return artifact === 'grimtooth';
}

function isPoisonableWeaponObject(item) {
    if (isPermanentPoisonedWeaponObject(item)) return true;
    if (!item || itemClassKey(item) !== 'weapon') return false;
    return POISONABLE_WISH_WEAPONS.has(objectKindKey(item));
}

function isAcidCorrosionDipTargetObject(item) {
    if (!item || isPotionObject(item)) return false;
    if (itemClassKey(item) === 'coin' || item?.glyph === '$') return false;
    if (item.greased) return true;
    const profile = wishedDamageProfile(item);
    return !!profile.erosionMatters && profile.secondaryWord === 'corroded';
}

function poisonedWeaponObjectName(item) {
    const name = pickupObjectName(item);
    if (item?.opoisoned && isPoisonableWeaponObject(item) && !/^poisoned\b/.test(name))
        return `poisoned ${name}`;
    return name;
}

function isWaterDipTargetObject(item) {
    if (!item) return false;
    return !(itemClassKey(item) === 'coin' || item?.otyp === GOLD_PIECE || item?.glyph === '$');
}

function isBlessedOrCursedWaterDipSource(item) {
    if (!isWaterPotion(item)) return false;
    const kind = objectKindKey(item).replace(/^potion of /, '');
    return !!(item.blessed || item.cursed || kind === 'holy water' || kind === 'unholy water');
}

function isNeutralWaterDipSource(item) {
    if (!isWaterPotion(item)) return false;
    const kind = objectKindKey(item).replace(/^potion of /, '');
    return !item.blessed && !item.cursed && kind !== 'holy water' && kind !== 'unholy water';
}

function dipPotionSources(target) {
    return (game.inventory || []).filter(item => {
        if (item === target || !isPotionObject(item)) return false;
        return isWaterDipTargetObject(target);
    });
}

function dipTargetsForSource(source) {
    if (!isPotionObject(source) || isPotionOfOil(source)) return [];
    return (game.inventory || []).filter(item => item !== source && dipPotionSources(item).includes(source));
}

function dipSourceDescription(source) {
    if (!source) return 'that';
    const base = pickupObjectName({ ...source, quan: 1 }).replace(/^(?:a|an|the)\s+/i, '');
    if ((source.quan || 1) > 1) {
        const plural = source.plural || (base.endsWith('s') ? base : `${base}s`);
        return `one of ${plural}`;
    }
    return `${/^[aeiou]/i.test(base) ? 'an' : 'a'} ${base}`;
}

function inventoryActionApplyText(item) {
    if (isPotionObject(item) && !isPotionOfOil(item)) {
        return (item.quan || 1) > 1
            ? 'a - Dip something into one of these potions'
            : 'a - Dip something into this potion';
    }
    return item.cls === 'ring' ? 'P - Put this ring on' : 'a - Apply this item';
}

function oilDipTargetName(item) {
    return dipItemDescription(item).replace(/\s+\(being worn\)$/, '');
}

function oilDipVerb(item, singular, plural) {
    return (item?.quan || 1) > 1 ? plural : singular;
}

function oilDipRepairKind(item) {
    if (!isOilWeaponDipTargetObject(item) || OIL_DIP_AMMO_RE.test(objectKindKey(item))) return '';
    const profile = wishedDamageProfile(item);
    const rusty = profile.primaryWord === 'rusty' && (item.oeroded || 0) > 0;
    const corroded = profile.secondaryWord === 'corroded' && (item.oeroded2 || 0) > 0;
    if (rusty && corroded) return 'corroded and rusty';
    if (rusty) return 'rusty';
    if (corroded) return 'corroded';
    return '';
}

function refreshOilDipTargetLine(item) {
    refreshInventoryObjectLine(item);
    if (item?.unpaid) syncUnpaidBillLine(item);
}

function consumeDipOilPotion(potion) {
    if (potion?.dknown) identifyPotionOfOil(potion);
    useUpInventoryItem(potion, 1);
}

function consumeDipPotion(potion) {
    useUpInventoryItem(potion, 1);
}

function polymorphObjectClassCode(item) {
    const cls = itemClassKey(item);
    if (item?.otyp === BOULDER || item?.otyp === STATUE || cls === 'rock' || item?.glyph === '`') return ROCK_CLASS;
    if (isPotionObject(item)) return POTION_CLASS;
    if (cls === 'weapon' || item?.glyph === ')') return WEAPON_CLASS;
    if (cls === 'armor' || item?.glyph === '[') return ARMOR_CLASS;
    if (cls === 'ring' || item?.glyph === '=') return RING_CLASS;
    if (cls === 'food' || item?.glyph === '%') return FOOD_CLASS;
    if (cls === 'scroll' || item?.glyph === '?') return SCROLL_CLASS;
    if (cls === 'wand' || item?.glyph === '/') return WAND_CLASS;
    if (cls === 'spellbook' || item?.glyph === '+') return SPBOOK_CLASS;
    if (cls === 'tool' || item?.glyph === '(') return TOOL_CLASS;
    if (cls === 'gem' || item?.glyph === '*') return GEM_CLASS;
    if (cls === 'amulet' || item?.glyph === '"') return AMULET_CLASS;
    return null;
}

function isWandOfPolymorphObject(item) {
    if (!item || (itemClassKey(item) !== 'wand' && item?.glyph !== '/' && item?.otyp !== WAND_CLASS)) return false;
    const kind = objectKindKey(item).replace(/^wand of /, '');
    return item?.wand === 'polymorph' || item?.wandIndex === 12 || kind === 'polymorph';
}

function isSpellbookOfPolymorphObject(item) {
    if (!item || (itemClassKey(item) !== 'spellbook' && item?.glyph !== '+' && item?.otyp !== SPBOOK_NO_NOVEL)) return false;
    const kind = objectKindKey(item).replace(/^spellbook of /, '');
    return item?.spellName === 'polymorph' || item?.spellbookIndex === 10 || kind === 'polymorph';
}

function isAmuletOfUnchangingObject(item) {
    if (!item || (itemClassKey(item) !== 'amulet' && item?.glyph !== '"' && item?.otyp !== AMULET_CLASS)) return false;
    const kind = objectKindKey(item).replace(/^amulet of /, '');
    return item?.amuletIndex === 6 || kind === 'unchanging';
}

function isPotionDipUnpolyableObject(item) {
    return isPotionOfPolymorph(item) || isWandOfPolymorphObject(item)
        || isSpellbookOfPolymorphObject(item) || isAmuletOfUnchangingObject(item);
}

function potionDipPolymorphSubjectResists(item) {
    return isPotionDipUnpolyableObject(item) || rn2(100) < (item?.artifact ? 95 : 5);
}

function polymorphObjectClassName(item) {
    switch (polymorphObjectClassCode(item)) {
    case WEAPON_CLASS: return 'weapon';
    case ARMOR_CLASS: return 'armor';
    case RING_CLASS: return 'ring';
    case FOOD_CLASS: return 'food';
    case SCROLL_CLASS: return 'scroll';
    case POTION_CLASS: return 'potion';
    case WAND_CLASS: return 'wand';
    case SPBOOK_CLASS: return 'spellbook';
    case TOOL_CLASS: return 'tool';
    case GEM_CLASS: return 'gem';
    case AMULET_CLASS: return 'amulet';
    case ROCK_CLASS: return 'rock';
    default: return itemClassKey(item) || '';
    }
}

function polymorphObjectTypeKey(item) {
    const cls = polymorphObjectClassName(item);
    const kind = objectKindKey(item);
    if (cls === 'potion' || isPotionObject(item))
        return `potion:${item?.potionIndex ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'scroll')
        return `scroll:${item?.scrollIndex ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'wand')
        return `wand:${item?.wandIndex ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'spellbook')
        return `spellbook:${item?.spellbookIndex ?? ''}:${item?.spellName ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'ring')
        return `ring:${item?.ringRoll ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'amulet')
        return `amulet:${item?.amuletIndex ?? ''}:${item?.otyp ?? ''}:${kind}`;
    if (cls === 'gem')
        return `gem:${item?.gemDescription ?? ''}:${item?.otyp ?? ''}:${kind}`;
    return `${cls}:${kind || item?.otyp || ''}`;
}

function polymorphReplacementDisallowed(item) {
    return isPotionOfPolymorph(item) || isWandOfPolymorphObject(item)
        || isSpellbookOfPolymorphObject(item) || isAmuletOfUnchangingObject(item);
}

function polymorphReplacementForDipTarget(target) {
    const classCode = polymorphObjectClassCode(target);
    if (classCode == null) return null;
    let replacement = null;
    for (let tryCount = 0; tryCount < 20; tryCount++) {
        replacement = mkobj(classCode, false);
        Object.assign(replacement, object_display(replacement));
        if (!polymorphReplacementDisallowed(replacement)) break;
    }
    if (!replacement || polymorphReplacementDisallowed(replacement)) return null;

    replacement.quan = Math.max(1, Math.trunc(Number(target?.quan || 1)));
    if (replacement.quan > 1 && replacement.quan > rn2(1000)) replacement.quan = 1;
    replacement.letter = target.letter;
    replacement.blessed = !!target.blessed;
    replacement.cursed = !!target.cursed;
    if (target.no_charge) replacement.no_charge = true;
    if ([WEAPON_CLASS, ARMOR_CLASS, WAND_CLASS].includes(polymorphObjectClassCode(replacement)) && target.spe != null)
        replacement.spe = target.spe;
    if (target.recharged != null) replacement.recharged = target.recharged;
    if (target.oeroded) replacement.oeroded = target.oeroded;
    if (target.oeroded2) replacement.oeroded2 = target.oeroded2;
    if (target.oerodeproof) replacement.oerodeproof = target.oerodeproof;
    if (target.otrapped && /box|chest/.test(objectKindKey(replacement))) replacement.otrapped = true;
    if (target.opoisoned && isPoisonableWeaponObject(replacement)) replacement.opoisoned = true;
    delete replacement.contents;
    return replacement;
}

function replaceInventoryObjectWithPolymorphResult(target, replacement, options = {}) {
    const preservedLetter = target.letter;
    const preservedEquipment = options.preserveEquipment ? {
        wielded: !!target.wielded,
        alternate: !!target.alternate,
        quivered: !!target.quivered,
        worn: target.worn,
        owornmask: target.owornmask,
    } : null;
    for (const key of Object.keys(target)) delete target[key];
    Object.assign(target, replacement);
    target.letter = preservedLetter;
    if (preservedEquipment) options.preserveEquipment(target, preservedEquipment);
    syncCarriedFigurineTransformTimer(target);
    refreshInventoryObjectLine(target);
    if (target.unpaid) syncUnpaidBillLine(target);
}

function isStoneToFleshMarbleWandObject(item) {
    if (!item || (itemClassKey(item) !== 'wand' && item?.glyph !== '/' && item?.otyp !== WAND_CLASS && item?.otyp !== WAN_MAKE_INVISIBLE))
        return false;
    const kind = objectKindKey(item).replace(/^wand of /, '');
    const appearance = String(item?.appearance || '').toLowerCase();
    return item?.otyp === WAN_MAKE_INVISIBLE
        || item?.wandIndex === 8
        || item?.wand === 'make invisible'
        || kind === 'make invisible'
        || appearance === 'marble';
}

const STONE_TO_FLESH_RING_MATERIALS = new Set(['mineral', 'gemstone']);
const STONE_TO_FLESH_RING_APPEARANCE_MATERIALS = new Map([
    ['wooden', 'wood'], ['granite', 'mineral'], ['opal', 'mineral'], ['clay', 'mineral'],
    ['coral', 'mineral'], ['black onyx', 'mineral'], ['moonstone', 'mineral'],
    ['tiger eye', 'gemstone'], ['jade', 'gemstone'], ['bronze', 'copper'],
    ['agate', 'gemstone'], ['topaz', 'gemstone'], ['sapphire', 'gemstone'],
    ['ruby', 'gemstone'], ['diamond', 'gemstone'], ['pearl', 'bone'],
    ['iron', 'iron'], ['brass', 'copper'], ['copper', 'copper'], ['twisted', 'iron'],
    ['steel', 'iron'], ['silver', 'silver'], ['gold', 'gold'], ['ivory', 'bone'],
    ['emerald', 'gemstone'], ['wire', 'iron'], ['engagement', 'iron'], ['shiny', 'iron'],
]);
const STONE_TO_FLESH_RING_KIND_MATERIALS = new Map([
    ['ring of adornment', 'wood'], ['ring of gain strength', 'mineral'],
    ['ring of gain constitution', 'mineral'], ['ring of increase accuracy', 'mineral'],
    ['ring of increase damage', 'mineral'], ['ring of protection', 'mineral'],
    ['ring of regeneration', 'mineral'], ['ring of searching', 'gemstone'],
    ['ring of stealth', 'gemstone'], ['ring of sustain ability', 'copper'],
    ['ring of levitation', 'gemstone'], ['ring of hunger', 'gemstone'],
    ['ring of aggravate monster', 'gemstone'], ['ring of conflict', 'gemstone'],
    ['ring of warning', 'gemstone'], ['ring of poison resistance', 'bone'],
    ['ring of fire resistance', 'iron'], ['ring of cold resistance', 'copper'],
    ['ring of shock resistance', 'copper'], ['ring of free action', 'iron'],
    ['ring of slow digestion', 'iron'], ['ring of teleportation', 'silver'],
    ['ring of teleport control', 'gold'], ['ring of polymorph', 'bone'],
    ['ring of polymorph control', 'gemstone'], ['ring of invisibility', 'iron'],
    ['ring of see invisible', 'iron'], ['ring of protection from shape changers', 'iron'],
]);

function normalizeStoneToFleshMaterial(material) {
    const text = String(material || '').toLowerCase().replace(/^hi_/, '');
    if (text === 'steel') return 'iron';
    if (text === 'brass' || text === 'bronze') return 'copper';
    return text;
}

function stoneToFleshRingMaterial(item) {
    const explicit = normalizeStoneToFleshMaterial(item?.oc_material || item?.material);
    if (explicit) return explicit;
    const ringName = objectKindKey(item);
    const appearance = String(item?.appearance || (
        typeof item?.kind === 'string' && /\bring$/.test(item.kind) && !/^ring of /.test(item.kind)
            ? item.kind.replace(/\s+ring$/, '')
            : ''
    ) || (item?.ringRoll ? game._object_descriptions?.rings?.[item.ringRoll - 1] : '') || '').toLowerCase();
    return normalizeStoneToFleshMaterial(STONE_TO_FLESH_RING_APPEARANCE_MATERIALS.get(appearance)
        || STONE_TO_FLESH_RING_KIND_MATERIALS.get(ringName)
        || '');
}

function isStoneToFleshMineralRingObject(item) {
    if (!item || (itemClassKey(item) !== 'ring' && item?.glyph !== '=' && item?.otyp !== RING_CLASS))
        return false;
    return STONE_TO_FLESH_RING_MATERIALS.has(stoneToFleshRingMaterial(item));
}

function stoneToFleshMeatStickReplacement(target) {
    const replacement = mksobj(MEAT_STICK, false, false);
    Object.assign(replacement, object_display(replacement), {
        quan: Math.max(1, Math.trunc(Number(target?.quan || 1))),
        letter: target?.letter,
        blessed: !!target?.blessed,
        cursed: !!target?.cursed,
        no_charge: !!target?.no_charge,
        recharged: target?.recharged ?? 0,
        cls: 'food',
        glyph: '%',
        otyp: MEAT_STICK,
        kind: 'meat stick',
        actualKind: 'meat stick',
        singular: 'meat stick',
        plural: 'meat sticks',
        nutrition: 5,
        owt: Math.max(1, Math.trunc(Number(target?.quan || 1))),
    });
    return replacement;
}

function stoneToFleshMeatRingReplacement(target) {
    const replacement = mksobj(MEAT_RING, false, false);
    Object.assign(replacement, object_display(replacement), {
        quan: 1,
        letter: target?.letter,
        blessed: !!target?.blessed,
        cursed: !!target?.cursed,
        no_charge: !!target?.no_charge,
        cls: 'food',
        glyph: '%',
        otyp: MEAT_RING,
        kind: 'meat ring',
        actualKind: 'meat ring',
        singular: 'meat ring',
        plural: 'meat rings',
        nutrition: 5,
        owt: 5,
    });
    return replacement;
}

const STONE_TO_FLESH_GEMSTONE_NAMES = new Set([
    'dilithium crystal', 'diamond', 'ruby', 'jacinth', 'jacinth stone',
    'sapphire', 'black opal', 'emerald', 'turquoise', 'turquoise stone',
    'citrine', 'citrine stone', 'aquamarine', 'aquamarine stone',
    'amber', 'amber stone', 'topaz', 'topaz stone', 'jet', 'jet stone',
    'opal', 'chrysoberyl', 'chrysoberyl stone', 'garnet', 'garnet stone',
    'amethyst', 'amethyst stone', 'jasper', 'jasper stone', 'fluorite',
    'fluorite stone', 'obsidian', 'obsidian stone', 'agate', 'agate stone',
    'jade', 'jade stone',
]);
const STONE_TO_FLESH_MINERAL_GEM_NAMES = new Set([
    'rock', 'rocks', 'luckstone', 'loadstone', 'touchstone', 'flint',
    'flint stone', 'gray stone', 'grey stone',
]);

function stoneToFleshObjectMaterial(item) {
    if (item?.otyp === STATUE || isFigurineObject(item)) return 'mineral';
    const explicit = normalizeStoneToFleshMaterial(item?.oc_material || item?.material);
    if (explicit) return explicit;
    if (isBoulderObject(item)) return 'mineral';
    const cls = itemClassKey(item);
    if (cls !== 'gem' && item?.glyph !== '*' && item?.otyp !== GEM_CLASS)
        return '';
    const values = [
        objectKindKey(item),
        item?.actualKind,
        item?.kind,
        item?.gemDescription,
        item?.displayName,
    ].map(value => String(value || '').toLowerCase().trim()).filter(Boolean);
    if (values.some(value => /\bworthless piece of\b.*\bglass\b/.test(value) || value === 'glass'))
        return 'glass';
    if (values.some(value => STONE_TO_FLESH_GEMSTONE_NAMES.has(value)))
        return 'gemstone';
    if (values.some(value => STONE_TO_FLESH_MINERAL_GEM_NAMES.has(value)))
        return 'mineral';
    return '';
}

function isStoneToFleshGemObject(item) {
    if (!item || (itemClassKey(item) !== 'gem' && item?.glyph !== '*' && item?.otyp !== GEM_CLASS))
        return false;
    const material = stoneToFleshObjectMaterial(item);
    return material === 'mineral' || material === 'gemstone';
}

function stoneToFleshMeatballReplacement(target) {
    const quantity = Math.max(1, Math.trunc(Number(target?.quan || 1)));
    const replacement = mksobj(MEATBALL, false, false);
    Object.assign(replacement, object_display(replacement), {
        quan: quantity,
        letter: target?.letter,
        blessed: !!target?.blessed,
        cursed: !!target?.cursed,
        no_charge: !!target?.no_charge,
        cls: 'food',
        glyph: '%',
        otyp: MEATBALL,
        kind: 'meatball',
        actualKind: 'meatball',
        singular: 'meatball',
        plural: 'meatballs',
        nutrition: 5,
        owt: quantity,
    });
    return replacement;
}

function stoneToFleshEnormousMeatballReplacement(target) {
    const replacement = mksobj(ENORMOUS_MEATBALL, false, false);
    Object.assign(replacement, object_display(replacement), {
        quan: 1,
        letter: target?.letter,
        blessed: !!target?.blessed,
        cursed: !!target?.cursed,
        no_charge: !!target?.no_charge,
        cls: 'food',
        glyph: '%',
        otyp: ENORMOUS_MEATBALL,
        kind: 'enormous meatball',
        actualKind: 'enormous meatball',
        singular: 'enormous meatball',
        plural: 'enormous meatballs',
        nutrition: 2000,
        owt: 400,
    });
    return replacement;
}

function stoneToFleshObjectAlwaysResists(item) {
    return !!(item?.invocation || item?.riderCorpse || lavaObjResistsHard(item));
}

function stoneToFleshObjectResists(item) {
    if (stoneToFleshObjectAlwaysResists(item)) return true;
    return rn2(100) < (item?.artifact || item?.oartifact ? 98 : 2);
}

function stoneToFleshCorpstatMonsterIsGolem(data = {}) {
    const name = String(data.name || '').toLowerCase();
    const mlet = data.mlet ?? data.glyph ?? '';
    return name.endsWith(' golem') || mlet === "'";
}

function stoneToFleshFleshGolemData() {
    return monsterByRndName('flesh golem')
        || RANDOM_MONSTER_BY_NAME.get('flesh golem')
        || { name: 'flesh golem', mlet: "'", glyph: "'", mlevel: 9, hpLevel: 9, mmove: 8, neuter: true };
}

function stoneToFleshCorpstatIsFleshGolem(data = {}) {
    return String(data.name || '').toLowerCase() === 'flesh golem';
}

function stoneToFleshCorpstatGolemAnimationInfo(data = {}) {
    if (!stoneToFleshCorpstatMonsterIsGolem(data)) return null;
    return {
        data: stoneToFleshCorpstatIsFleshGolem(data) ? data : stoneToFleshFleshGolemData(),
        golemXform: !stoneToFleshCorpstatIsFleshGolem(data),
    };
}

function stoneToFleshCorpstatMonsterIsVegetarian(data = {}) {
    if (!data || stoneToFleshCorpstatMonsterIsGolem(data)) return false;
    if (data.noncorporeal) return true;
    const name = String(data.name || '').toLowerCase();
    const mlet = data.mlet ?? data.glyph ?? '';
    const lowerMlet = String(mlet).toLowerCase();
    if (['b', 'j', 'f', 'v', 'y', 'blob', 'jelly', 'fungus', 'vortex', 'light'].includes(lowerMlet))
        return true;
    if ((mlet === 'E' || lowerMlet === 'elemental') && name !== 'stalker')
        return true;
    if ((mlet === 'P' || lowerMlet === 'pudding') && name !== 'black pudding')
        return true;
    return false;
}

function isStoneToFleshVegetarianCorpstatObject(item) {
    if (!(item?.otyp === STATUE || isFigurineObject(item))) return false;
    const material = stoneToFleshObjectMaterial(item);
    return (material === 'mineral' || material === 'gemstone')
        && stoneToFleshCorpstatMonsterIsVegetarian(item.corpsenm);
}

function stoneToFleshReplacementForObject(item) {
    if (isStoneToFleshMarbleWandObject(item))
        return stoneToFleshObjectResists(item) ? null : stoneToFleshMeatStickReplacement(item);
    if (isStoneToFleshMineralRingObject(item))
        return stoneToFleshObjectResists(item) ? null : stoneToFleshMeatRingReplacement(item);
    if (isBoulderObject(item)) {
        if (stoneToFleshObjectResists(item)) return null;
        applySokobanGuilt();
        return stoneToFleshEnormousMeatballReplacement(item);
    }
    if (isStoneToFleshVegetarianCorpstatObject(item))
        return stoneToFleshObjectResists(item) ? null : stoneToFleshMeatballReplacement(item);
    if (isStoneToFleshGemObject(item))
        return stoneToFleshObjectResists(item) ? null : stoneToFleshMeatballReplacement(item);
    return null;
}

function stoneToFleshCorpstatAnimationInfo(data = {}) {
    const golemInfo = stoneToFleshCorpstatGolemAnimationInfo(data);
    if (golemInfo) return golemInfo;
    if (stoneToFleshCorpstatMonsterIsVegetarian(data))
        return null;
    return { data, golemXform: false };
}

function savedCorpstatMonsterTraits(item) {
    return item?.omonst || item?.oextra?.omonst || null;
}

function clearSavedCorpstatMonsterTraits(item) {
    if (!item) return;
    delete item.omonst;
    delete item.omid;
    if (item.oextra) {
        delete item.oextra.omonst;
        delete item.oextra.omid;
    }
}

function stoneToFleshSavedMonsterTraits(item) {
    return savedCorpstatMonsterTraits(item);
}

function stoneToFleshStatueHasSavedMonsterTraits(item) {
    return !!stoneToFleshSavedMonsterTraits(item);
}

function stoneToFleshStatueHasAttachedMonsterId(item) {
    return !!(item?.omid || item?.oextra?.omid);
}

function stoneToFleshHumanZombieData() {
    return monsterByRndName('human zombie')
        || RANDOM_MONSTER_BY_NAME.get('human zombie')
        || { name: 'human zombie', mlet: 'Z', glyph: 'Z', mlevel: 4, hpLevel: 5, mmove: 6, noCorpse: true };
}

function stoneToFleshLongWormData() {
    return monsterByRndName('long worm')
        || RANDOM_MONSTER_BY_NAME.get('long worm')
        || { name: 'long worm', mlet: 'w', glyph: 'w', mlevel: 8, hpLevel: 9, mmove: 3 };
}

function stoneToFleshDoppelgangerData() {
    return monsterByRndName('doppelganger')
        || RANDOM_MONSTER_BY_NAME.get('doppelganger')
        || { name: 'doppelganger', mlet: '@', glyph: '@', mlevel: 9, hpLevel: 10, mmove: 12 };
}

function stoneToFleshCantReviveZombieSubstitute(data = {}) {
    const name = String(data.name || '').toLowerCase();
    return data.guard || data.highCleric || data.alignedCleric || data.angel
        || name === 'guard' || name === 'high cleric' || name === 'aligned cleric' || name === 'angel';
}

function stoneToFleshStatueCantReviveInfo(item, data = {}) {
    const name = String(data.name || '').toLowerCase();
    if (stoneToFleshCantReviveZombieSubstitute(data))
        return { data: stoneToFleshHumanZombieData(), failureData: data, golemXform: false };
    if (name === 'long worm tail' || data.longWormTail)
        return { data: stoneToFleshLongWormData(), failureData: data, golemXform: false };
    if ((data.unique || data.cantRevive) && !stoneToFleshStatueHasSavedMonsterTraits(item)) {
        return {
            data: stoneToFleshDoppelgangerData(),
            directedDoppelganger: true,
            directedForm: data,
            failureData: data,
            golemXform: false,
            noCountBirth: true,
            preserveOriginalOnFailure: true,
        };
    }
    return null;
}

function stoneToFleshSavedTraitsAnimationInfo(item, { adjacentOk = false } = {}) {
    const savedTraits = stoneToFleshSavedMonsterTraits(item);
    if (!savedTraits) return null;
    return {
        data: savedTraits.data || item?.corpsenm,
        savedTraits,
        golemXform: false,
        noCountBirth: true,
        noTail: true,
        noWait: true,
        adjacentOk,
    };
}

function stoneToFleshFloorStatueAnimationInfo(item, x, y) {
    if (!(item?.otyp === STATUE || item?.kind === 'statue') || !item?.corpsenm) return null;
    const material = stoneToFleshObjectMaterial(item);
    if (material !== 'mineral' && material !== 'gemstone') return null;
    const cantReviveInfo = stoneToFleshStatueCantReviveInfo(item, item.corpsenm);
    if (cantReviveInfo) return cantReviveInfo;
    const golemInfo = stoneToFleshCorpstatGolemAnimationInfo(item.corpsenm);
    if (golemInfo?.golemXform) return golemInfo;
    const savedInfo = stoneToFleshSavedTraitsAnimationInfo(item, { adjacentOk: true });
    if (savedInfo) return savedInfo;
    if (stoneToFleshStatueHasAttachedMonsterId(item) || item.corpsenm.noCorpstat) return null;
    return golemInfo || stoneToFleshCorpstatAnimationInfo(item.corpsenm);
}

function statueTrapAnimationInfo(item) {
    if (!(item?.otyp === STATUE || item?.kind === 'statue') || !item?.corpsenm) return null;
    const cantReviveInfo = stoneToFleshStatueCantReviveInfo(item, item.corpsenm);
    if (cantReviveInfo) return cantReviveInfo;
    const savedInfo = stoneToFleshSavedTraitsAnimationInfo(item);
    if (savedInfo) return savedInfo;
    return { data: item.corpsenm, golemXform: false };
}

function statueTrapFailureRetriesNextStatue(statue, info) {
    const data = info?.failureData || statue?.corpsenm || info?.data || {};
    return !!(data.unique || data.nemesis || data.rider);
}

function stoneToFleshFigurineAnimationInfo(item) {
    if (!isFigurineObject(item) || !item?.corpsenm) return null;
    const material = stoneToFleshObjectMaterial(item);
    if (material !== 'mineral' && material !== 'gemstone') return null;
    return stoneToFleshCorpstatAnimationInfo(item.corpsenm);
}

function stoneToFleshCarriedFigurineHasUnpaidContents(item, seen = new Set()) {
    if (!item || seen.has(item)) return false;
    seen.add(item);
    return globContents(item).some(child => shopObjectOrContentsUnpaid(child));
}

function stoneToFleshCarriedFigurineShopkeeper(item) {
    const shkp = shopkeeperForCostlySpot(game.u?.ux, game.u?.uy);
    if (!shopkeeperInHisShop(shkp)) return null;
    return item?.unpaid || shopBillEntryForObject(shkp, item) ? shkp : null;
}

function stoneToFleshAnimatableCarriedFigurineInfo(item) {
    if (stoneToFleshCarriedFigurineHasUnpaidContents(item)) return null;
    return stoneToFleshFigurineAnimationInfo(item);
}

function stoneToFleshAnimatableFloorFigurineInfo(item) {
    return stoneToFleshFigurineAnimationInfo(item);
}

function stoneToFleshAnimationFailure(info) {
    return { stoneToFleshAnimationFailed: true, info };
}

function isStoneToFleshAnimationFailure(result) {
    return !!result?.stoneToFleshAnimationFailed;
}

function stoneToFleshFailedAnimationPreservesOriginal(info) {
    const data = info?.failureData || info?.data || {};
    return !!(info?.preserveOriginalOnFailure || data.unique || data.noCorpse);
}

function stoneToFleshFailedAnimationCorpse(item) {
    const corpse = mkcorpstat(CORPSE, null, item?.corpsenm, 0, 0, 8);
    corpse.quan = Math.max(1, Math.trunc(Number(item?.quan || 1)));
    corpse.blessed = !!item?.blessed;
    corpse.cursed = !!item?.cursed;
    if (item?.no_charge) corpse.no_charge = true;
    startCorpseTimeout(corpse);
    Object.assign(corpse, object_display(corpse));
    return corpse;
}

function moveStoneToFleshFailedAnimationContentsToFloor(item, x, y, floorObjects = game.level?.objects || []) {
    const contents = globContents(item);
    const dropped = [];
    while (contents.length) {
        const child = contents.shift();
        child.contained = false;
        delete child.container;
        child.ox = x;
        child.oy = y;
        child.hidden = false;
        child.transientProjectile = false;
        delete child.line;
        Object.assign(child, object_display(child));
        floorObjects.push(child);
        dropped.push(child);
    }
    if (Array.isArray(item?.contents)) item.contents = [];
    if (Array.isArray(item?.cobj)) item.cobj = [];
    return dropped;
}

function stoneToFleshApplyCarriedAnimationFailure(item, failure) {
    if (stoneToFleshFailedAnimationPreservesOriginal(failure?.info)) return false;
    const corpse = stoneToFleshFailedAnimationCorpse(item);
    corpse.letter = item.letter;
    moveStoneToFleshFailedAnimationContentsToFloor(item, game.u?.ux || 0, game.u?.uy || 0);
    markObjectShopBillUsedUp(item);
    stopFigurineTransformTimeout(item);
    replaceInventoryObjectWithPolymorphResult(item, corpse);
    return true;
}

function stoneToFleshFloorAnimationFailureResult(item, x, y, failure) {
    if (stoneToFleshFailedAnimationPreservesOriginal(failure?.info))
        return { transformed: false, floorObjects: [item] };
    const corpse = stoneToFleshFailedAnimationCorpse(item);
    corpse.ox = x;
    corpse.oy = y;
    const floorObjects = [];
    moveStoneToFleshFailedAnimationContentsToFloor(item, x, y, floorObjects);
    prepareFloorPolymorphReplacement(item, corpse);
    floorObjects.push(corpse);
    return {
        transformed: true,
        floorObjects,
        angerMessage: floorPolymorphShopkeeperAngerMessage(item, x, y),
    };
}

async function stoneToFleshAnimateCarriedFigurine(item) {
    const info = stoneToFleshAnimatableCarriedFigurineInfo(item);
    if (!info || stoneToFleshObjectResists(item)) return null;
    const mon = await makemon(info.data, game.u?.ux || 0, game.u?.uy || 0, NO_MINVENT | MM_NOMSG);
    if (!mon) return stoneToFleshAnimationFailure(info);
    const messages = [];
    const chargeMessage = stoneToFleshChargeCarriedFigurineAnimation(item);
    if (chargeMessage) messages.push(chargeMessage);
    stopFigurineTransformTimeout(item);
    useUpInventoryItem(item, 1);
    if (cansee(mon.mx, mon.my))
        messages.push(`The figurine ${info.golemXform ? 'turns to flesh and ' : ''}animates!`);
    return messages;
}

function stoneToFleshChargeFigurineAnimation(item, x, y, shkp, { carried = false } = {}) {
    if (!item || shopBillableGold(item) || (!carried && item.no_charge)) return '';
    if (!shopkeeperInHisShop(shkp)) return '';
    if (carried && !(item.unpaid || shopBillEntryForObject(shkp, item))) return '';
    const beforeCredit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const value = lostShopMerchandiseValueForObject({ ox: x, oy: y }, item, shkp);
    if (!(value > 0)) return '';
    const peaceful = shopkeeperPeacefulForDebt(shkp);
    const remaining = chargeShopkeeperForLostMerchandise(shkp, value, { peaceful });
    if (!peaceful) {
        if (!game.u?.blind && cansee(shkp.mx, shkp.my))
            return `${shopkeeperDisplayName(shkp)} booms: "${game.plname || 'Hero'}, you are a thief!"`;
        return heroIsDeaf() ? '' : 'You hear a scream, "Thief!"';
    }
    const usedCredit = beforeCredit > Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    if (usedCredit && shkp.credit > 0)
        return `You have ${shkp.credit} ${shopCurrency(shkp.credit)} credit remaining.`;
    if (usedCredit && !remaining)
        return 'You have no credit remaining.';
    const still = usedCredit ? 'still ' : '';
    return `You ${still}owe ${shopkeeperDisplayName(shkp)} ${remaining} ${shopCurrency(remaining)} for ${shopDebtObjectPronoun(item)}!`;
}

function stoneToFleshChargeCarriedFigurineAnimation(item) {
    const shkp = stoneToFleshCarriedFigurineShopkeeper(item);
    return stoneToFleshChargeFigurineAnimation(item, game.u?.ux, game.u?.uy, shkp, { carried: true });
}

function stoneToFleshChargeFloorFigurineAnimation(item, x, y) {
    return stoneToFleshChargeFigurineAnimation(item, x, y, shopkeeperForCostlySpot(x, y));
}

async function stoneToFleshAnimateFloorFigurine(item, x, y) {
    const info = stoneToFleshAnimatableFloorFigurineInfo(item);
    if (!info || stoneToFleshObjectResists(item)) return null;
    const mon = await makemon(info.data, x, y, NO_MINVENT | MM_NOMSG);
    if (!mon) return stoneToFleshAnimationFailure(info);
    const messages = [];
    const chargeMessage = stoneToFleshChargeFloorFigurineAnimation(item, x, y);
    if (chargeMessage) messages.push(chargeMessage);
    stopFigurineTransformTimeout(item);
    newsym(x, y);
    if (cansee(mon.mx, mon.my))
        messages.push(`The figurine ${info.golemXform ? 'turns to flesh and ' : ''}animates!`);
    return messages;
}

function stoneToFleshGolemStatueVerb(info, mon) {
    if (info?.golemXform) return 'turns into flesh';
    if (stoneToFleshCorpstatMonsterIsGolem(mon?.data || {})) return 'moves';
    return statueAnimationVerb(mon);
}

function stoneToFleshRetargetMonsterForm(mon, data, { chamBase = null } = {}) {
    if (!mon || !data) return;
    if (data.male) mon.female = false;
    else if (data.female) mon.female = true;
    else if (!data.neuter && !rn2(10)) mon.female = !mon.female;
    const level = data.hpLevel ?? adjustedMonsterLevel(data);
    const hpMax = Math.max(1, monster_hp(data, level));
    const oldHp = Math.max(1, mon.mhp || 1);
    const oldMax = Math.max(1, mon.mhpmax || oldHp);
    Object.assign(mon, {
        data: { ...data, hpLevel: level },
        name: data.name,
        mlet: data.mlet,
        glyph: data.glyph,
        color: data.color,
        m_lev: level,
        mlevel: level,
        mhpmax: hpMax,
        mhp: Math.max(1, Math.min(hpMax, Math.trunc((oldHp * hpMax) / oldMax))),
        meverseen: 0,
    });
    if (chamBase) mon.chamBase = chamBase;
    else delete mon.chamBase;
    set_malign(mon);
}

function applySavedCorpstatMonsterTraits(mon, saved, dataHint = null) {
    if (!mon || !saved) return;
    const data = saved.data && typeof saved.data === 'object'
        ? saved.data
        : dataHint || mon.data || {};
    const level = Math.max(0, Math.trunc(Number(saved.m_lev ?? saved.mlevel ?? mon.m_lev ?? mon.mlevel ?? data.mlevel ?? 1)));
    const baseMax = Math.max(1, Math.trunc(Number(saved.mhpmax || mon.mhpmax || monster_hp(data, data.hpLevel ?? level))));
    const hpMax = Math.max(baseMax, Math.trunc(Number((data.mlevel || 0) + 1)));
    Object.assign(mon, {
        data: { ...data, hpLevel: data.hpLevel ?? level },
        name: data.name || mon.name,
        mlet: data.mlet || mon.mlet,
        glyph: data.glyph || mon.glyph,
        color: data.color || mon.color,
        m_lev: level,
        mlevel: level,
        mhpmax: hpMax,
        mhp: hpMax,
        mrevived: true,
        mavenge: 0,
        meating: 0,
        mleashed: 0,
        mtrapped: 0,
        msleeping: 0,
        mfrozen: 0,
        mcanmove: true,
        mcan: 0,
        mcansee: true,
        mblinded: 0,
        mstun: 0,
        mconf: 0,
        dead: false,
    });
    for (const field of [
        'female', 'mtame', 'pet', 'mpeaceful', 'isminion', 'isshk', 'ispriest', 'isgd',
        'givenName', 'chamBase', 'vampBase', 'perminvis', 'minvis', 'invisible', 'mspeed',
        'mflee', 'mfleetim', 'mtrapseen', 'mstrategy', 'waiting', 'maligntyp',
        'mundetected', 'm_ap_type', 'appearObj', 'appearGlyph',
    ]) {
        if (saved[field] !== undefined) mon[field] = saved[field];
    }
    mon.minvent = [];
    mon.hasInventory = false;
    mon.mw = null;
    mon.missile = null;
    set_malign(mon);
}

function stoneToFleshApplySavedMonsterTraits(mon, info) {
    applySavedCorpstatMonsterTraits(mon, info?.savedTraits, info?.data);
}

function stoneToFleshApplyDirectedDoppelgangerForm(mon, info) {
    if (!info?.directedDoppelganger) return;
    if (heroHasProtectionFromShapeChangers()) {
        stoneToFleshRetargetMonsterForm(mon, info.data);
        return;
    }
    if (isMonsterGenocidedName(info.directedForm?.name)) return;
    stoneToFleshRetargetMonsterForm(mon, info.directedForm, { chamBase: 'doppelganger' });
}

function stoneToFleshChristenAnimatedStatueMonster(item, mon) {
    const name = objectInstanceNameKey(item);
    if (!name || !mon || mon.data?.unique || mon.data?.nemesis || mon.data?.rider) return;
    mon.givenName = name;
}

function stoneToFleshHistoricStatueGoneMessage(item, x, y) {
    const role = game.urole?.name?.m || game._startup_role;
    if (role !== 'Archeologist') return '';
    if (!(((item?.spe || 0) & CORPSTAT_HISTORIC) || item?.historic)) return '';
    if (game._monster_moving) {
        return cansee(x, y) ? 'You feel regret that the historic statue is now gone.' : '';
    }
    if (game.u?.ualign) {
        const record = game.u.ualign.record || 0;
        const newRecord = record - 1;
        if (newRecord < record) game.u.ualign.record = newRecord;
        game.u.ualign.abuse = (game.u.ualign.abuse || 0) + 1;
    }
    return 'You feel guilty that the historic statue is now gone.';
}

async function stoneToFleshAnimateFloorStatue(item, x, y) {
    const info = stoneToFleshFloorStatueAnimationInfo(item, x, y);
    if (!info || stoneToFleshObjectResists(item)) return null;
    const flags = NO_MINVENT | MM_NOMSG | MM_ADJACENTOK
        | (info.noCountBirth ? MM_NOCOUNTBIRTH : 0)
        | (info.noTail ? MM_NOTAIL : 0)
        | (info.noWait ? MM_NOWAIT : 0);
    const mon = await makemon(info.data, x, y, flags);
    if (!mon) return stoneToFleshAnimationFailure(info);
    stoneToFleshApplySavedMonsterTraits(mon, info);
    stoneToFleshApplyDirectedDoppelgangerForm(mon, info);
    stoneToFleshChristenAnimatedStatueMonster(item, mon);
    mon.msleeping = 0;
    mon.mundetected = false;
    const messages = [];
    const verb = cansee(mon.mx, mon.my) ? stoneToFleshGolemStatueVerb(info, mon) : 'disappears';
    messages.push(`${upstartText(`the ${pickupObjectName(item)}`)} ${verb}!`);
    const chargeMessage = statueShatterShopDebtMessage(item, x, y, mon);
    if (chargeMessage) messages.push(chargeMessage);
    const historicMessage = stoneToFleshHistoricStatueGoneMessage(item, x, y);
    if (historicMessage) messages.push(historicMessage);
    moveStatueContentsToMonster(item, mon);
    newsym(x, y);
    newsym(mon.mx, mon.my);
    return messages;
}

function isMeatRingObject(item) {
    return item?.otyp === MEAT_RING || objectKindKey(item) === 'meat ring';
}

function preserveStoneToFleshEquipmentState(target, state) {
    if (state.wielded) target.wielded = true;
    if (state.alternate) target.alternate = true;
    if (state.quivered) target.quivered = true;
    if (!isMeatRingObject(target)) return;
    if (state.worn === 'left' || state.worn === 'right') target.worn = state.worn;
    if (state.owornmask != null) target.owornmask = state.owornmask;
}

function heroPolyselfCarnivorous() {
    const form = polyselfFormWithDiet() || {};
    return !!(form.carnivorous || form.carnivore);
}

function stoneToFleshSmellMessage() {
    const role = String(game.urole?.name?.m || game._startup_role || '').toLowerCase();
    const brokeVegetarian = (game.u?.uconduct?.unvegetarian || 0) > 0;
    return role !== 'monk' && brokeVegetarian && heroPolyselfCarnivorous()
        ? 'You smell a delicious smell.'
        : 'You smell the odor of meat.';
}

function stoneToFleshMergeSkipItem(item) {
    return !!(item?.worn || item?.wielded || item?.alternate || item?.quivered);
}

function mergeStoneToFleshInventoryResults() {
    let didMerge = false;
    do {
        didMerge = false;
        const inventory = game.inventory || [];
        for (let i = 0; !didMerge && i < inventory.length; i++) {
            const target = inventory[i];
            if (stoneToFleshMergeSkipItem(target)) continue;
            for (let j = i + 1; j < inventory.length; j++) {
                const source = inventory[j];
                if (stoneToFleshMergeSkipItem(source)) continue;
                if (!pickedObjectInventoryMergeCompatible(target, source, false, null)) continue;
                mergePickedObjectIntoInventory(source, target);
                game.inventory = (game.inventory || []).filter(item => item !== source);
                didMerge = true;
                break;
            }
        }
    } while (didMerge);
}

function fixHeroPetrification(message = null) {
    if (!game.u?._stonedTimeout) return '';
    const rescueMessage = message ?? (heroIsHallucinating()
        ? `What a pity--you just ruined a future piece of ${(game.u.acurr?.a?.[A_CHA] || 0) > 15 ? 'fine ' : ''}art!`
        : 'You feel limber!');
    game.u._stonedTimeout = 0;
    game.u._stonedKiller = '';
    removeHeroStatusSuffix('Stone');
    return rescueMessage;
}

async function stoneToFleshInventoryEffect(messages = []) {
    let rescued = false;
    const form = game.u?._polyself_form;
    if (form?.name === 'stone golem') {
        const result = becomeMonster('flesh golem');
        if (result?.message) messages.push(result.message);
        rescued = true;
    }
    const petrificationMessage = fixHeroPetrification();
    if (petrificationMessage) {
        messages.push(petrificationMessage);
        rescued = true;
    }

    let transformed = false;
    let meatTransformed = false;
    for (const item of [...(game.inventory || [])]) {
        const replacement = stoneToFleshReplacementForObject(item);
        if (replacement) {
            markObjectShopBillUsedUp(item);
            replaceInventoryObjectWithPolymorphResult(item, replacement, {
                preserveEquipment: preserveStoneToFleshEquipmentState,
            });
            transformed = true;
            meatTransformed = true;
            continue;
        }
        const animationMessage = await stoneToFleshAnimateCarriedFigurine(item);
        if (animationMessage != null) {
            if (isStoneToFleshAnimationFailure(animationMessage)) {
                if (stoneToFleshApplyCarriedAnimationFailure(item, animationMessage))
                    transformed = true;
                continue;
            }
            if (Array.isArray(animationMessage)) messages.push(...animationMessage);
            else if (animationMessage) messages.push(animationMessage);
            transformed = true;
        }
    }
    if (meatTransformed) {
        mergeStoneToFleshInventoryResults();
        messages.push(stoneToFleshSmellMessage());
    }
    return { transformed: transformed || rescued, messages };
}

async function stoneToFleshFloorEffect(x = game.u?.ux || 0, y = game.u?.uy || 0) {
    const messages = [];
    const alterationMessages = [];
    let transformed = false;
    let meatTransformed = false;
    const floorObjects = [];
    for (const obj of (game.level?.objects || [])) {
        if (!obj || obj.hidden || obj.transientProjectile || obj.ox !== x || obj.oy !== y) {
            floorObjects.push(obj);
            continue;
        }
        const replacement = stoneToFleshReplacementForObject(obj);
        if (replacement) {
            replacement.ox = x;
            replacement.oy = y;
            prepareFloorPolymorphReplacement(obj, replacement);
            const angerMessage = floorPolymorphShopkeeperAngerMessage(obj, x, y);
            if (angerMessage && !alterationMessages.includes(angerMessage))
                alterationMessages.push(angerMessage);
            transformed = true;
            meatTransformed = true;
            floorObjects.push(replacement);
            continue;
        }
        const animationMessage = await stoneToFleshAnimateFloorFigurine(obj, x, y);
        if (animationMessage != null) {
            if (isStoneToFleshAnimationFailure(animationMessage)) {
                const fallback = stoneToFleshFloorAnimationFailureResult(obj, x, y, animationMessage);
                floorObjects.push(...fallback.floorObjects);
                if (fallback.transformed) transformed = true;
                if (fallback.angerMessage && !alterationMessages.includes(fallback.angerMessage))
                    alterationMessages.push(fallback.angerMessage);
                continue;
            }
            if (Array.isArray(animationMessage)) messages.push(...animationMessage);
            else if (animationMessage) messages.push(animationMessage);
            transformed = true;
            continue;
        }
        const statueAnimationMessage = await stoneToFleshAnimateFloorStatue(obj, x, y);
        if (statueAnimationMessage != null) {
            if (isStoneToFleshAnimationFailure(statueAnimationMessage)) {
                const fallback = stoneToFleshFloorAnimationFailureResult(obj, x, y, statueAnimationMessage);
                floorObjects.push(...fallback.floorObjects);
                if (fallback.transformed) transformed = true;
                if (fallback.angerMessage && !alterationMessages.includes(fallback.angerMessage))
                    alterationMessages.push(fallback.angerMessage);
                continue;
            }
            if (Array.isArray(statueAnimationMessage)) messages.push(...statueAnimationMessage);
            else if (statueAnimationMessage) messages.push(statueAnimationMessage);
            transformed = true;
            continue;
        }
        floorObjects.push(obj);
    }
    game.level.objects = floorObjects;
    if (transformed) {
        newsym(x, y);
        if (meatTransformed) messages.push(stoneToFleshSmellMessage());
        messages.push(...alterationMessages);
    }
    return { transformed, messages };
}

function learnPotionPolymorphDiscovery(item) {
    if (item && (game.inventory || []).includes(item)) {
        identifyPotionOfPolymorph(item);
        return;
    }
    recordKnownPotionDiscovery({ potionIndex: 19 }, 'polymorph');
    learnObjectScore('Potions', 'potion of polymorph');
}

function incrementPolymorphedItemConduct() {
    game.u ??= {};
    game.u.uconduct ??= {};
    game.u.uconduct.polypiles = (game.u.uconduct.polypiles || 0) + 1;
}

function dipObjectIntoPolymorphPotion(target, potion) {
    const messages = [];
    if (!isPotionOfPolymorph(target) && !isPotionOfPolymorph(potion)) return messages;

    const resistSubject = isPotionOfPolymorph(target) ? potion : target;
    if (potionDipPolymorphSubjectResists(resistSubject)) {
        messages.push('Nothing happens.');
        return messages;
    }

    const oldType = polymorphObjectTypeKey(target);
    const discoveryItem = isPotionOfPolymorph(potion)
        ? potion
        : isPotionOfPolymorph(target) ? { potionIndex: 19 } : null;
    const replacement = polymorphReplacementForDipTarget(target);
    if (!replacement) {
        messages.push('Nothing happens.');
        return messages;
    }

    incrementPolymorphedItemConduct();
    markObjectShopBillUsedUp(target);
    replaceInventoryObjectWithPolymorphResult(target, replacement);
    const changedType = polymorphObjectTypeKey(target) !== oldType;
    if (changedType) {
        learnPotionPolymorphDiscovery(discoveryItem);
        consumeDipPotion(potion);
        messages.push(normalInventoryLine(target));
    } else {
        consumeDipPotion(potion);
        messages.push('Nothing seems to happen.');
    }
    return messages;
}

function alchemyPotionName(item) {
    if (!item) return '';
    const identityName = potionIdentityName(item);
    if (identityName) return identityName;
    let name = potionDipKind(item);
    if (name === 'holy water' || name === 'unholy water') name = 'water';
    if (!name && item.potionIndex != null) name = IDENTIFIED_POTION_NAMES[item.potionIndex] || '';
    if (!name && item.otyp === POT_WATER) name = 'water';
    if (!name && item.otyp === POT_ACID) name = 'acid';
    if (!name && item.otyp === POT_OIL) name = 'oil';
    if (!name && item.otyp === POT_POLYMORPH) name = 'polymorph';
    return name;
}

function alchemyPotionIndex(name) {
    if (name === 'water') return null;
    const index = IDENTIFIED_POTION_NAMES.indexOf(name);
    return index >= 0 ? index : undefined;
}

function alchemyPotionOtyp(name) {
    if (name === 'water') return POT_WATER;
    if (name === 'acid') return POT_ACID;
    if (name === 'oil') return POT_OIL;
    if (name === 'polymorph') return POT_POLYMORPH;
    return POTION_CLASS;
}

function alchemyPotionIsMagic(name) {
    const index = alchemyPotionIndex(name);
    return index != null && index >= 0 && index <= 19;
}

function mixtypePotionResultName(target, source) {
    let first = alchemyPotionName(target);
    let second = alchemyPotionName(source);
    if (!first || !second) return '';

    if (new Set([
        'gain level', 'gain energy', 'healing', 'extra healing',
        'full healing', 'enlightenment', 'fruit juice',
    ]).has(second)) {
        [first, second] = [second, first];
    }

    if (first === 'healing' && second === 'speed') return 'extra healing';
    if (['healing', 'extra healing', 'full healing'].includes(first)
        && (second === 'gain level' || second === 'gain energy')) {
        return first === 'healing' ? 'extra healing'
            : first === 'extra healing' ? 'full healing'
            : 'gain ability';
    }
    if (first === 'gain level' || first === 'gain energy') {
        if (second === 'confusion') return rn2(3) ? 'booze' : 'enlightenment';
        if (second === 'healing') return 'extra healing';
        if (second === 'extra healing') return 'full healing';
        if (second === 'full healing') return 'gain ability';
        if (second === 'fruit juice') return 'see invisible';
        if (second === 'booze') return 'hallucination';
    }
    if (first === 'fruit juice') {
        if (second === 'sickness') return 'sickness';
        if (second === 'enlightenment' || second === 'speed') return 'booze';
        if (second === 'gain level' || second === 'gain energy') return 'see invisible';
    }
    if (first === 'enlightenment') {
        if (second === 'levitation') return rn2(3) ? 'gain level' : '';
        if (second === 'fruit juice') return 'booze';
        if (second === 'booze') return 'confusion';
    }
    return '';
}

function alchemyStackMagic(target, source, resultName) {
    if (resultName) return alchemyPotionIsMagic(resultName);
    return alchemyPotionIsMagic(alchemyPotionName(target)) || alchemyPotionIsMagic(alchemyPotionName(source));
}

function alchemyAffectedPotionCount(target, source, resultName) {
    let amount = Math.max(1, Math.trunc(Number(target?.quan || 1)));
    const magic = alchemyStackMagic(target, source, resultName);
    const threshold = target?.odiluted ? 2 : magic ? 3 : 7;
    if (amount <= threshold) return amount;
    if (target.odiluted) return 2;
    if (magic) return rnd(Math.min(amount, 8) - 2) + 2;
    return rnd(amount - 6) + 6;
}

function potionStackNameForAlchemyMessage(item) {
    const name = alchemyPotionName(item) || potionDipKind(item) || 'unknown';
    return (item?.quan || 1) > 1 ? `potions of ${name}` : `potion of ${name}`;
}

function potionAlchemyMixMessage(target, source, splitFromStack) {
    const prefix = splitFromStack ? `${target.quan} of the` : 'The';
    const verb = (target.quan || 1) > 1 ? 'mix' : 'mixes';
    const sourcePrefix = (source?.quan || 1) > 1 ? 'one of ' : '';
    return `${prefix} ${potionStackNameForAlchemyMessage(target)} ${verb} with ${sourcePrefix}${potionStackNameForAlchemyMessage(source)}...`;
}

function heroWornAlchemySmockItem() {
    return (game.inventory || []).find(item => isWornInventoryItem(item)
        && objectKindKey(item) === 'alchemy smock') || null;
}

function heroWearsAlchemySmock() {
    return !!heroWornAlchemySmockItem();
}

function heroCanReceivePotionVapor() {
    const form = polyselfForm() || {};
    return !form.breathless || !form.noeyes;
}

function heroBreathesPotionVapor() {
    return !((polyselfForm() || {}).breathless);
}

function heroHasPotionVaporEyes() {
    return !((polyselfForm() || {}).noeyes);
}

function heroHasWetWornTowel() {
    return (game.inventory || []).some(item =>
        isWornInventoryItem(item) && objectKindKey(item) === 'towel' && towelWetness(item) > 0);
}

function heroHasFreeAction() {
    return !!(game.u?.freeAction || game.u?.Free_action || (game.inventory || []).some(item =>
        isWornInventoryItem(item) && (item.actualKind === 'ring of free action'
            || item.kind === 'ring of free action' || item.ringRoll === 20)));
}

function heroHasSleepResistance() {
    return !!(game.u?.sleepResistance || game.u?.Sleep_resistance || polyselfForm()?.sleepResistance);
}

function clearPotionVaporBlindness(messages) {
    if (!game.u) return;
    const wasBlind = heroIsBlind();
    game.u.blind = false;
    game.u._blindTimeout = 0;
    game.u.ucreamed = 0;
    game.u._statusSuffix = (game.u._statusSuffix || '').replace(' Blind', '');
    game.u._deafTimeout = 0;
    game.u._statusSuffix = (game.u._statusSuffix || '').replace(' Deaf', '');
    if (wasBlind) messages.push(heroIsHallucinating()
        ? 'Far out!  Everything is all cosmic again!'
        : 'You can see again.');
}

function incrementAbilityFromPotionVapor(blessed) {
    const stats = game.u?.acurr?.a;
    if (!stats) return;
    const maxStats = game.u?.amax?.a || [];
    let attr = rn2(A_MAX);
    for (let checked = 0; checked < A_MAX; checked++) {
        const max = maxStats[attr] ?? stats[attr] ?? 10;
        if ((stats[attr] ?? 10) < max) {
            stats[attr]++;
            if (!blessed) break;
        }
        attr = (attr + 1) % A_MAX;
    }
}

function healHeroFromPotionVapor(amount) {
    if (!game.u) return;
    if (game.u._polyself_base && game.u.mh != null && game.u.mhmax != null)
        game.u.mh = Math.min(game.u.mhmax, game.u.mh + amount);
    game.u.uhp = Math.min(game.u.uhpmax || game.u.uhp || 1, (game.u.uhp || 1) + amount);
}

function potionDiscoveryKnown(potionName) {
    const discoveryName = `potion of ${potionName}`;
    return (game._discoveries || [])
        .some(entry => entry.section === 'Potions' && entry.name === discoveryName && entry.known !== false);
}

function potionCallAppearance(potion, fallback = 'clear') {
    const index = potion?.potionIndex;
    const described = index != null ? game._object_descriptions?.potions?.[index]?.description : '';
    if (described) return described;
    const kind = String(potion?.kind || '').trim();
    if (kind.endsWith(' potion')) return kind.replace(/ potion$/, '') || fallback;
    return fallback;
}

function potionObjectNameIsKnown(potion, potionName) {
    const name = String(potionName || '').toLowerCase();
    if (!name) return false;
    if (potion?.known === true) return true;
    const actual = String(potion?.actualKind || '').toLowerCase().replace(/^potion of /, '');
    if (actual === name) return true;
    const kind = String(potion?.kind || '').toLowerCase();
    return kind === name || kind === `potion of ${name}`;
}

function potionEffectNameFromAppearance(potion, rawName) {
    const identityName = potionIdentityName(potion);
    if (identityName) return identityName;
    const name = String(rawName || '').replace(/^potion of /, '').trim();
    if (name && name !== 'potion' && !name.endsWith(' potion')) return name;
    if (potion?.potionIndex != null) return IDENTIFIED_POTION_NAMES[potion.potionIndex] || name;
    return name;
}

function recordCalledPotion(appearance, name, options = {}) {
    const call = String(name || '').trim();
    if (!call) return;
    const potionAppearance = appearance || 'clear';
    game._called_potions ??= {};
    game._called_potions[potionAppearance] = call;
    if (!options.discover) return;
    game._discoveries ??= [];
    const discoveryName = `potion called ${call}`;
    const existing = game._discoveries.find(entry =>
        entry.section === 'Potions'
        && (entry.name === discoveryName || String(entry.text || '').endsWith(`(${potionAppearance})`)));
    if (existing) {
        existing.name = discoveryName;
        existing.text = `${discoveryName} (${potionAppearance})`;
        existing.starred = false;
    } else {
        game._discoveries.push({
            section: 'Potions',
            name: discoveryName,
            text: `${discoveryName} (${potionAppearance})`,
            starred: false,
        });
    }
}

function shouldTryCallPotion(potion, potionName) {
    if (!potion || potion.dknown === false) return false;
    if (potionObjectNameIsKnown(potion, potionName) || potionDiscoveryKnown(potionName)) return false;
    const appearance = potionCallAppearance(potion);
    return !game._called_potions?.[appearance];
}

function queuePotionTryCall(potion, potionName) {
    if (!shouldTryCallPotion(potion, potionName)) return false;
    game._call_potion_appearance = potionCallAppearance(potion);
    game._call_potion_text = '';
    game._call_potion_record_discovery = 1;
    game._command_mode = 'callPotionAfterMore';
    return true;
}

function learnPotionVaporEffect(potion, name, knownEffect) {
    if (!potion?.dknown || !name) return;
    if (!knownEffect) {
        queuePotionTryCall(potion, name);
        return;
    }
    recordKnownPotionDiscovery(potion, name);
    learnObjectScore('Potions', `potion of ${name}`);
}

function splitGremlinPolyselfFromWaterVapor(messages) {
    const form = polyselfForm();
    if (String(form?.name || '').toLowerCase() !== 'gremlin' || !game.u || !game.level) return false;
    const currentHp = Math.min(game.u.uhp || 0, game.u.uhpmax || game.u.uhp || 0);
    if (currentHp <= 1) return false;
    const maxHp = Math.max(currentHp, game.u.uhpmax || currentHp);
    const data = { ...(polyselfFormByName('gremlin') || {}), ...form };
    const spot = enextoMonsterSpot(game.u.ux || 0, game.u.uy || 0, data);
    if (!spot) return false;

    const cloneHp = Math.trunc(currentHp / 2);
    const cloneMaxHp = Math.trunc(maxHp / 2);
    game.u.uhp = currentHp - cloneHp;
    game.u.uhpmax = Math.max(1, maxHp - cloneMaxHp);

    const clone = {
        mx: spot.x,
        my: spot.y,
        m_id: next_ident(),
        data,
        name: data.name || 'gremlin',
        mlet: data.mlet || 'g',
        m_lev: data.mlevel ?? data.m_lev ?? 5,
        mhp: Math.max(1, cloneHp),
        mhpmax: Math.max(1, cloneMaxHp),
        msleeping: 0,
        mpeaceful: 1,
        mtame: Math.max(5, baseScrollTameness({ data })),
        pet: true,
        mcanmove: true,
        mcansee: true,
        minvent: [],
        mcloned: 1,
        givenName: game.plname || 'wizard',
    };
    ensurePetExtension(clone);
    set_malign(clone);
    game.u.uconduct ??= {};
    game.u.uconduct.pets = (game.u.uconduct.pets || 0) + 1;
    game.level.monsters ??= [];
    game.level.monsters.push(clone);
    messages.push('You multiply!');
    newsym(spot.x, spot.y);
    return true;
}

function heroLycanthropeBeastName() {
    const lycanthrope = game.u?.ulycn;
    if (lycanthrope == null || lycanthrope === -1 || lycanthrope === false) return '';
    const source = typeof lycanthrope === 'object'
        ? lycanthrope.name
        : typeof lycanthrope === 'string'
            ? lycanthrope
            : game.u?.ulycnName || game.u?.lycanthropeName || game.u?.wereForm;
    const lower = String(source || '').toLowerCase().replace(/^human\s+/, '');
    const beastMatch = lower.match(/^(?:rat|jackal|wolf)\s+(were(?:rat|jackal|wolf))$/);
    return beastMatch ? beastMatch[1] : lower;
}

function hostileMonsterNearHeroForWereChange() {
    const ux = game.u?.ux ?? 0;
    const uy = game.u?.uy ?? 0;
    return (game.level?.monsters || []).some(mon => {
        if (!mon || mon.dead || mon.mpeaceful || mon.mundetected) return false;
        if (mon.mhp != null && mon.mhp <= 0) return false;
        return Math.abs((mon.mx ?? ux) - ux) <= 1 && Math.abs((mon.my ?? uy) - uy) <= 1
            && cansee(mon.mx, mon.my);
    });
}

function appendRehumanizeDeathResultMessages(messages, result, { allowLifeSaving = false } = {}) {
    messages.push(...(result.messages || []));
    if (!result.died) return;
    if (allowLifeSaving && consumeLifeSavingAmulet({ clearStoning: !!result.clearStoningOnLifeSaving })) {
        if (game.u) game.u.uhp = 0;
        messages.push('You die...  But wait...  Your medallion begins to glow!');
        messages.lifeSaving = true;
    } else {
        messages.fatal = true;
    }
    messages.more = true;
}

function waterVaporLycanthropyEffect(potion, messages, options = {}) {
    const beastName = heroLycanthropeBeastName();
    if (!beastName || heroHasUnchanging()) return false;
    if (potion?.blessed && polyselfFormName() === beastName && !hostileMonsterNearHeroForWereChange()) {
        const result = rehumanizeAfterPolyselfDeath();
        appendRehumanizeDeathResultMessages(messages, result, options);
        return !!result.messages.length;
    }
    if (potion?.cursed && !game.u?._polyself_form && !hostileMonsterNearHeroForWereChange()
        && !(game.u?.polymorphControl || game.u?.polycontrol || game.u?.Polymorph_control)) {
        const result = becomeMonster(beastName);
        if (result?.message) messages.push(result.message);
        return !!result?.message;
    }
    return false;
}

function potionBreathe(potion, messages, options = {}) {
    const result = {};
    if (!heroCanReceivePotionVapor()) return result;
    const name = potionEffectNameFromAppearance(potion, alchemyPotionName(potion));
    let knownEffect = false;
    let blockedByWetTowel = false;

    if (heroHasWetWornTowel()) {
        messages.push('Some vapor passes harmlessly around you.');
        blockedByWetTowel = true;
    }

    if (!blockedByWetTowel) {
        switch (name) {
        case 'restore ability':
        case 'gain ability':
            if (potion.cursed) {
                if (heroBreathesPotionVapor()) messages.push('Ulch!  That potion smells terrible!');
                else if (heroHasPotionVaporEyes()) messages.push('Your eyes sting!');
            } else {
                incrementAbilityFromPotionVapor(!!potion.blessed);
            }
            break;
        case 'full healing':
            healHeroFromPotionVapor(1);
            clearPotionVaporBlindness(messages);
            // fall through
        case 'extra healing':
            healHeroFromPotionVapor(1);
            if (!potion.cursed) clearPotionVaporBlindness(messages);
            // fall through
        case 'healing':
            healHeroFromPotionVapor(1);
            if (potion.blessed) clearPotionVaporBlindness(messages);
            exerciseAttribute(A_CON, true);
            break;
        case 'sickness':
            if ((game.urole?.name?.m || game._startup_role) !== 'Healer' && game.u) {
                game.u.uhp = game.u.uhp <= 5 ? 1 : game.u.uhp - 5;
                exerciseAttribute(A_CON, false);
            }
            break;
        case 'hallucination':
            messages.push('You have a momentary vision.');
            break;
        case 'confusion':
        case 'booze':
            if (!heroIsConfused()) messages.push('You feel somewhat dizzy.');
            addHeroConfusion(rnd(5));
            break;
        case 'invisibility':
            if (!heroIsBlind() && !game.u?.invisible) {
                knownEffect = true;
                messages.push(`For an instant you ${game.u?.seeInvisible
                    ? 'could see right through yourself'
                    : "couldn't see yourself"}!`);
            }
            break;
        case 'paralysis':
            knownEffect = true;
            if (!heroHasFreeAction()) {
                messages.push('Something seems to be holding you.');
                game._helpless_time = Math.max(game._helpless_time || 0, rnd(5));
                game._wake_message = 'You can move again.';
                exerciseAttribute(A_DEX, false);
            } else {
                messages.push('You stiffen momentarily.');
            }
            break;
        case 'sleeping':
            knownEffect = true;
            if (!heroHasFreeAction() && !heroHasSleepResistance()) {
                messages.push('You feel rather tired.');
                const duration = rnd(5);
                result.sleepDuration = duration;
                game._helpless_time = Math.max(game._helpless_time || 0, duration);
                game._wake_message = 'You can move again.';
                exerciseAttribute(A_DEX, false);
            } else {
                messages.push('You yawn.');
            }
            break;
        case 'speed':
            if (!game.u?.fast && !game.u?.veryfast) messages.push('Your knees seem more flexible now.');
            if (game.u) {
                game.u._veryfastTimeout = (game.u._veryfastTimeout || 0) + rnd(5);
                syncHeroSpeedState();
            }
            exerciseAttribute(A_DEX, true);
            break;
        case 'blindness':
            if (!heroIsBlind() && !(game._helpless_time > 0 && game._sleeping_time > 0)) {
                knownEffect = true;
                messages.push('It suddenly gets dark.');
            }
            if (game.u) {
                game.u.blind = true;
                game.u._blindTimeout = (game.u._blindTimeout || 0) + rnd(5);
            }
            break;
        case 'water':
            if (!splitGremlinPolyselfFromWaterVapor(messages))
                waterVaporLycanthropyEffect(potion, messages, options);
            break;
        case 'acid':
        case 'polymorph':
            exerciseAttribute(A_CON, false);
            break;
        default:
            break;
        }
    }
    learnPotionVaporEffect(potion, name, knownEffect);
    return result;
}

function heroIsNextToPotionVapor(x, y) {
    const ux = game.u?.ux ?? 0;
    const uy = game.u?.uy ?? 0;
    return Math.max(Math.abs((x ?? ux) - ux), Math.abs((y ?? uy) - uy)) <= 1;
}

function brokenPotionBreathe(potion, x, y, messages, options = {}) {
    if (!isPotionObject(potion) || !heroIsNextToPotionVapor(x, y) || !heroCanReceivePotionVapor()) return;
    if (!isWaterPotion(potion) && !heroHasWetWornTowel()) {
        if (heroBreathesPotionVapor()) messages.push('You smell a peculiar odor...');
        else if (heroHasPotionVaporEyes()) messages.push('Your eyes water.');
    }
    potionBreathe(potion, messages, options);
}

function thrownPotionHitTargetName(mon) {
    const name = mon?.givenName || `the ${mon?.data?.name || 'monster'}`;
    if (mon?.data?.nohead) return name;
    return `${name}'s head`;
}

const COMMON_NO_MONSTER_EFFECT_POTION_HIT_KINDS = new Set([
    'levitation', 'see invisible', 'gain level', 'enlightenment',
    'monster detection', 'object detection', 'gain energy', 'fruit juice',
]);

function isUnlitOilPotionHit(potion, kind = thrownPotionEffectKind(potion)) {
    return kind === 'oil' && !(potion?.lamplit || potion?.burning);
}

function isLitOilPotionHit(potion, kind = thrownPotionEffectKind(potion)) {
    return kind === 'oil' && !!(potion?.lamplit || potion?.burning);
}

function monsterHasWornSaddle(mon) {
    if (!mon) return false;
    if (mon.saddled || ((mon.misc_worn_check || 0) & W_SADDLE)) return true;
    return (mon.minvent || []).some(item => objectKindKey(item) === 'saddle'
        && (item.worn || item.owornmask || item.oslot === 'saddle'));
}

function monsterWornSaddleForPotionHit(mon) {
    if (!mon) return null;
    const saddle = (mon.minvent || []).find(item => objectKindKey(item) === 'saddle'
        && (item.worn || item.owornmask || item.oslot === 'saddle'));
    if (saddle) return saddle;
    if (objectKindKey(mon.saddle) === 'saddle'
        && (mon.saddle.worn || mon.saddle.owornmask || mon.saddle.oslot === 'saddle'))
        return mon.saddle;
    return null;
}

function monsterHatesBlessingsForWaterHit(mon) {
    const data = mon?.data || {};
    const mlet = data.mlet || mon?.mlet || data.glyph || mon?.glyph;
    const name = String(data.name || mon?.name || '').toLowerCase();
    const undead = mlet === 'L' || mlet === 'M' || mlet === 'V' || mlet === 'W'
        || mlet === 'Z' || mlet === 'ghost' || data.vampshifter || mon?.vampshifter
        || /\b(?:ghost|shade|lich|mummy|zombie|vampire|wraith|nazgul|skeleton|ghoul)\b/.test(name);
    return !!(data.demon || mon?.demon || mlet === '&' || undead);
}

function monsterIsWereOrVampireForWaterHit(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    const cham = String(mon?.chamName || mon?.cham || data.chamName || data.cham || '').toLowerCase();
    return !!(data.were || data.isWere || mon?.were || mon?.isWere
        || data.vampshifter || mon?.vampshifter || data.vampireLeader || mon?.vampireLeader
        || /^were/.test(name) || /\bvampire\b/.test(name)
        || /\bvampire|vlad/.test(cham));
}

function monsterNameForWaterHit(mon) {
    return String(mon?.data?.name || mon?.name || '').toLowerCase();
}

function monsterIsGremlinForWaterHit(mon) {
    return monsterNameForWaterHit(mon) === 'gremlin';
}

function monsterIsIronGolemForWaterHit(mon) {
    return monsterNameForWaterHit(mon) === 'iron golem';
}

function isSpecialMonsterWaterPotionHit(potion, mon, kind = thrownPotionEffectKind(potion), options = {}) {
    return kind === 'water' && (options.ignoreSaddle || !monsterHasWornSaddle(mon))
        && (monsterIsGremlinForWaterHit(mon) || monsterIsIronGolemForWaterHit(mon));
}

function isBlessingHaterWaterPotionHit(potion, mon, kind = thrownPotionEffectKind(potion), options = {}) {
    return kind === 'water' && (options.ignoreSaddle || !monsterHasWornSaddle(mon))
        && monsterHatesBlessingsForWaterHit(mon) && !monsterIsWereOrVampireForWaterHit(mon);
}

function isShapechangerWaterPotionHit(potion, mon, kind = thrownPotionEffectKind(potion), options = {}) {
    return kind === 'water' && (options.ignoreSaddle || !monsterHasWornSaddle(mon))
        && monsterIsWereOrVampireForWaterHit(mon);
}

function monsterNeedsDeferredWaterPotionHit(mon, options = {}) {
    if (!mon || (!options.ignoreSaddle && monsterHasWornSaddle(mon))) return true;
    const name = String(mon?.data?.name || mon?.name || '').toLowerCase();
    return name === 'gremlin' || name === 'iron golem'
        || monsterHatesBlessingsForWaterHit(mon)
        || monsterIsWereOrVampireForWaterHit(mon);
}

function isNeutralOrdinaryWaterPotionHit(potion, mon, kind = thrownPotionEffectKind(potion), options = {}) {
    return kind === 'water' && isNeutralWaterDipSource(potion)
        && !monsterNeedsDeferredWaterPotionHit(mon, options);
}

function supportsHeroThrownPotionBodyHit(potion, mon = null, kind = thrownPotionEffectKind(potion), options = {}) {
    return kind === 'confusion' || kind === 'booze' || kind === 'paralysis'
        || kind === 'sleeping' || kind === 'blindness' || kind === 'speed'
        || kind === 'invisibility' || kind === 'hallucination'
        || kind === 'healing' || kind === 'extra healing' || kind === 'full healing'
        || kind === 'restore ability' || kind === 'gain ability' || kind === 'sickness'
        || kind === 'acid' || kind === 'polymorph'
        || isShapechangerWaterPotionHit(potion, mon, kind, options)
        || isBlessingHaterWaterPotionHit(potion, mon, kind, options)
        || isSpecialMonsterWaterPotionHit(potion, mon, kind, options)
        || isNeutralOrdinaryWaterPotionHit(potion, mon, kind, options)
        || COMMON_NO_MONSTER_EFFECT_POTION_HIT_KINDS.has(kind)
        || isUnlitOilPotionHit(potion, kind)
        || isLitOilPotionHit(potion, kind);
}

function isSaddlePotionHit(potion, mon, kind = thrownPotionEffectKind(potion)) {
    return isPotionObject(potion) && !!monsterWornSaddleForPotionHit(mon)
        && (kind === 'water'
            || supportsHeroThrownPotionBodyHit(potion, mon, kind, { ignoreSaddle: true }));
}

function supportsHeroThrownPotionHit(potion, mon = null) {
    if (!isPotionObject(potion)) return false;
    const kind = thrownPotionEffectKind(potion);
    return supportsHeroThrownPotionBodyHit(potion, mon, kind)
        || isSaddlePotionHit(potion, mon, kind);
}

function wieldedConsumedBashObject(obj) {
    if (!obj) return null;
    const bashObject = {
        ...obj,
        id: (obj.quan || 1) > 1 ? next_ident() : obj.id,
        quan: 1,
        letter: undefined,
        line: undefined,
        wielded: false,
        alternate: false,
        worn: false,
        quivered: false,
    };
    if ((obj.quan || 1) > 1) splitCarriedObjectShopBill(obj, bashObject, 1);
    return bashObject;
}

function wieldedPotionBashObject(potion) {
    if (!potion || !isPotionObject(potion)) return null;
    return wieldedConsumedBashObject(potion);
}

function refreshSurvivingWieldedConsumedStack(obj) {
    if (!obj || !(game.inventory || []).includes(obj) || !obj.wielded) return;
    const baseName = normalInventoryLine({ ...obj, line: '', wielded: false, alternate: false })
        .replace(/^[a-zA-Z$] - /, '');
    obj.line = `${obj.letter || '?'} - ${baseName} (wielded)`;
}

function refreshSurvivingWieldedPotionStack(potion) {
    refreshSurvivingWieldedConsumedStack(potion);
}

function thrownPotionEffectKind(potion) {
    const identityName = potionIdentityName(potion);
    if (identityName) return identityName;
    const name = alchemyPotionName(potion);
    const effectName = String(name || '').replace(/^potion of /, '');
    if (effectName && effectName !== 'potion' && !effectName.endsWith(' potion')) return effectName;
    if (potion?.potionIndex != null) return IDENTIFIED_POTION_NAMES[potion.potionIndex] || name;
    return name;
}

function monsterCanMoveForPotionParalysis(mon) {
    return mon.mcanmove !== false && mon.mcanmove !== 0;
}

function clearMonsterPotionWaitStrategy(mon) {
    if (Number.isInteger(mon?.mstrategy)) mon.mstrategy &= ~STRAT_WAITMASK;
    else if (mon?.mstrategy === 'waitforu') mon.mstrategy = 0;
    mon.waiting = false;
}

function paralyzeMonsterFromPotion(mon, duration) {
    if (!monsterCanMoveForPotionParalysis(mon)) return;
    mon.mcanmove = false;
    mon.mfrozen = Math.min(127, duration);
    mon.meating = 0;
    clearMonsterPotionWaitStrategy(mon);
}

function monsterResistsSleepEffect(mon) {
    const data = mon?.data || {};
    return !!(mon?.resistsSleep || mon?.sleepResistance || data.resistsSleep
        || data.sleepResistance || data.defendsSleep);
}

function potionHitMonsterName(mon) {
    const name = mon?.givenName || mon?.shknam || `the ${mon?.data?.name || 'monster'}`;
    return sentenceCase(name);
}

function sleepMonsterFromPotion(mon, duration) {
    if (monsterResistsSleepEffect(mon)) return false;
    if (monsterResistsEffect(mon, 6)) return false;
    if (mon.mcanmove === false || mon.mcanmove === 0) return false;
    mon.mcanmove = false;
    mon.mfrozen = Math.min(127, (mon.mfrozen || 0) + duration);
    mon.meating = 0;
    return true;
}

function monsterHasEyesForPotionBlindness(mon) {
    const data = mon?.data || {};
    return !(data.noeyes || data.noEyes);
}

function monsterIsPermanentlyBlind(mon) {
    return mon?.mcansee === false && !(mon.mblinded || 0);
}

function monsterWearsVisoredHelmetForBlinding(mon) {
    return (mon?.minvent || []).some(item => {
        if (!(item.worn || item.owornmask || item.line?.includes('being worn'))) return false;
        return armorKind(item) === 'helm of telepathy'
            || String(item.appearance || '').toLowerCase() === 'visored helmet';
    });
}

function heroRavenCannotBlindMonster(mon) {
    const formName = String(polyselfForm()?.name || game.u?._polyself_form?.name || '').toLowerCase();
    const targetName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    return formName === 'raven' && targetName === 'raven';
}

function monsterCanBeBlindedByBlindingVenom(mon) {
    return monsterHasEyesForPotionBlindness(mon)
        && !monsterIsPermanentlyBlind(mon)
        && !heroRavenCannotBlindMonster(mon)
        && !monsterWearsVisoredHelmetForBlinding(mon);
}

function monsterCanBeBlindedByCreamPie(mon) {
    return monsterHasEyesForPotionBlindness(mon)
        && !monsterIsPermanentlyBlind(mon)
        && !heroRavenCannotBlindMonster(mon);
}

function blindMonsterFromPotion(mon) {
    if (!monsterHasEyesForPotionBlindness(mon) || monsterIsPermanentlyBlind(mon)) return false;
    const baseDuration = 64 + rn2(32);
    const resistedBonus = rn2(32);
    const bonus = monsterResistsEffect(mon, 6) ? 0 : resistedBonus;
    mon.mblinded = Math.min(127, (mon.mblinded || 0) + baseDuration + bonus);
    mon.mcansee = false;
    return true;
}

function monsterSpeedState(mon) {
    if (mon?.mspeed === 'fast' || mon?.mspeed === 2 || mon?.permspeed === 'fast') return 'fast';
    if (mon?.mspeed === 'slow' || mon?.mspeed === 1 || mon?.permspeed === 'slow') return 'slow';
    return 'normal';
}

function monsterCanBeSeenForPotionEffect(mon) {
    return !!mon && !game.u?.blind && !mon.minvis && !mon.mundetected && cansee(mon.mx, mon.my);
}

function monsterCanBeSpottedForPotionHit(mon) {
    return !!mon && !game.u?.blind && !mon.mundetected
        && (!mon.minvis || game.u?.seeInvisible) && cansee(mon.mx, mon.my);
}

function speedMonsterFromPotion(mon) {
    const oldState = monsterSpeedState(mon);
    if (oldState === 'slow') {
        mon.permspeed = 0;
        mon.mspeed = 0;
    } else {
        mon.permspeed = 'fast';
        mon.mspeed = 'fast';
    }
    return monsterSpeedState(mon) !== oldState;
}

function mapInvisibleMonsterAt(mon) {
    const loc = game.level?.at(mon.mx, mon.my);
    if (!loc) return;
    loc.map_invisible = true;
    loc.waslit = true;
    loc.remembered_glyph = { ch: 'I', color: NO_COLOR, dec: false };
    newsym(mon.mx, mon.my);
}

function setMonsterMinvisFromPotion(mon, cursedPotion) {
    mon.perminvis = cursedPotion ? 0 : 1;
    if (mon.invis_blkd) return;
    mon.minvis = mon.perminvis;
    newsym(mon.mx, mon.my);
}

function invisibilityPotionHitMonster(potion, mon, messages) {
    const sawIt = monsterCanBeSpottedForPotionHit(mon);
    const cursedPotion = !!potion.cursed;
    const angerMon = !!mon.minvis && cursedPotion;
    setMonsterMinvisFromPotion(mon, cursedPotion);
    const canSpotNow = monsterCanBeSpottedForPotionHit(mon);

    if (sawIt && !canSpotNow) {
        if (cansee(mon.mx, mon.my)) mapInvisibleMonsterAt(mon);
    } else if (sawIt && cursedPotion) {
        messages.push(`${potionHitMonsterName(mon)} briefly seems to be transparent.`);
    } else if (!sawIt && canSpotNow) {
        messages.push(`${potionHitMonsterName(mon)} appears!`);
    }
    return angerMon;
}

function monsterIsPestilence(mon) {
    return String(mon?.data?.name || mon?.name || '').toLowerCase() === 'pestilence';
}

function normalizedMonsterDamageType(type) {
    return String(type || '').toLowerCase().replace(/^ad_/, '');
}

function monsterHasDamageType(mon, expectedTypes) {
    const data = mon?.data || {};
    const attacks = [];
    if (mon?.attack) attacks.push(mon.attack);
    if (data.attack) attacks.push(data.attack);
    if (Array.isArray(mon?.attacks)) attacks.push(...mon.attacks);
    if (Array.isArray(data.attacks)) attacks.push(...data.attacks);

    const damageTypes = [
        mon?.adtyp, mon?.damageType, mon?.dmgtype,
        data.adtyp, data.damageType, data.dmgtype,
        ...(Array.isArray(mon?.damageTypes) ? mon.damageTypes : []),
        ...(Array.isArray(data.damageTypes) ? data.damageTypes : []),
        ...attacks.map(attack => attack?.adtyp),
    ];
    return damageTypes.some(type => expectedTypes.has(normalizedMonsterDamageType(type)));
}

function monsterResistsPoison(mon) {
    const data = mon?.data || {};
    return !!(mon?.poisonResistance || mon?.resistsPoison || mon?.resists_poison
        || data.poisonResistance || data.resistsPoison || data.resists_poison);
}

function monsterResistsSicknessPotion(mon) {
    return monsterResistsPoison(mon)
        || monsterHasDamageType(mon, new Set(['dise', 'disease', 'pest', 'pestilence']));
}

function monsterIsSilentForPotionHit(mon) {
    const data = mon?.data || {};
    const explicit = data.msound ?? data.sound ?? mon?.msound ?? mon?.sound;
    const msound = String(explicit ?? '').toLowerCase();
    if (mon?.silent || data.silent || explicit === 0 || msound === 'silent' || msound === 'ms_silent')
        return true;
    if (explicit != null) return false;
    const name = String(data.name || mon?.name || '').toLowerCase();
    return tipHatGeneratedMonsterSound(name) === 'silent';
}

function monsterResistsAcid(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(mon?.acidResistance || mon?.resistsAcid || mon?.resists_acid
        || data.acidResistance || data.resistsAcid || data.resists_acid
        || data.acidic || name === 'acid blob' || name === 'gelatinous cube');
}

function wakeNearbyMonstersFromPotionHit(mon) {
    const data = mon?.data || {};
    const distance = (data.mlevel ?? mon?.m_lev ?? mon?.mlevel ?? 1) * 10;
    for (const sleeper of game.level?.monsters || []) {
        if (!sleeper || sleeper.dead || (sleeper.mhp != null && sleeper.mhp <= 0)) continue;
        const dx = (sleeper.mx || 0) - (mon?.mx || 0);
        const dy = (sleeper.my || 0) - (mon?.my || 0);
        if (distance !== 0 && dx * dx + dy * dy >= distance) continue;
        sleeper.msleeping = 0;
        if (!(sleeper.data?.unique || sleeper.data?.uniq)) sleeper.mstrategy = 0;
    }
}

function vampshifterRevivalBaseName(mon) {
    const data = mon?.data || {};
    const rawBase = mon?.vampBase || data.vampBase || mon?.chamName || data.chamName
        || mon?.cham || data.cham || '';
    const base = String(rawBase || '').toLowerCase();
    if (base.includes('vlad')) return 'Vlad the Impaler';
    if (base === 'vampire lord' || base === 'vampire leader' || base === 'vampire lady')
        return base === 'vampire lady' ? 'vampire leader' : base;
    if (base === 'vampire') return 'vampire';
    if ((mon?.vampshifter || data.vampshifter) && String(data.name || mon?.name || '').toLowerCase() !== 'vampire')
        return 'vampire';
    return '';
}

function reviveVampshifterFromPotionKill(mon, messages) {
    const baseName = vampshifterRevivalBaseName(mon);
    if (!baseName) return false;
    const currentName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    if (currentName === String(baseName).toLowerCase()) return false;
    if ((game._genocided_monsters || []).includes(baseName)) return false;

    const oldData = mon.data || {};
    const targetName = mon.givenName || `the ${oldData.name || mon.name || 'monster'}`;
    const nonliving = oldData.nonliving || oldData.mlet === 'Z' || oldData.glyph === 'Z'
        || String(oldData.name || '').includes('zombie') || String(oldData.name || '').endsWith(' golem');
    messages.push(`You ${nonliving ? 'destroy' : 'kill'} ${targetName}!`);

    const baseData = monsterByRndName(baseName) || RANDOM_MONSTER_BY_NAME.get(baseName);
    if (!baseData) return false;
    const oldVisible = monsterCanBeSeenForPotionEffect(mon);
    const oldDisplayName = potionHitMonsterName(mon);
    const specialDeath = oldData.nonliving || oldData.noncorporeal || oldData.amorphous
        || oldData.name === 'fog cloud' || oldData.mlet === 'ghost';
    const level = adjustedMonsterLevel(baseData);
    const maxHp = Math.max(10, monster_hp(baseData, level));
    mon.dead = false;
    mon.data = { ...baseData, hpLevel: level };
    mon.name = baseData.name;
    mon.mlet = baseData.mlet;
    mon.glyph = baseData.glyph;
    mon.color = baseData.color;
    mon.m_lev = level;
    mon.mlevel = level;
    mon.mhpmax = maxHp;
    mon.mhp = maxHp;
    mon.mcanmove = true;
    mon.mfrozen = 0;
    mon.msleeping = 0;
    mon.vampshifter = false;
    delete mon.vampBase;
    delete mon.chamName;
    delete mon.cham;
    if (oldVisible && monsterCanBeSeenForPotionEffect(mon) && !heroIsHallucinating()) {
        const before = specialDeath ? oldDisplayName : oldDisplayName.replace(/^The /, 'The seemingly dead ');
        const action = specialDeath ? 'suddenly reconstitutes' : 'suddenly transforms';
        messages.push(`${before} ${action} and rises as ${indefiniteArticle(baseData.name)} ${baseData.name}!`);
    }
    set_malign(mon);
    newsym(mon.mx, mon.my);
    return true;
}

function killMonsterFromPotionHit(mon, messages, { heroFault = true } = {}) {
    if (!mon || mon.dead) return;
    if (heroFault && reviveVampshifterFromPotionKill(mon, messages)) return;
    mon.dead = true;
    mon.mhp = 0;
    const data = mon.data || {};
    const targetName = mon.givenName || `the ${data.name || mon.name || 'monster'}`;
    const nonliving = data.nonliving || data.mlet === 'Z' || data.glyph === 'Z'
        || String(data.name || '').includes('zombie') || String(data.name || '').endsWith(' golem');
    if (heroFault) {
        messages.push(`You ${nonliving ? 'destroy' : 'kill'} ${targetName}!`);
    } else if (monsterCanBeSeenForPotionEffect(mon)) {
        messages.push(`${sentenceCase(targetName)} is ${nonliving ? 'destroyed' : 'killed'}!`);
    }
    recordVanquished(mon, heroFault);
    dropMonsterInventory(mon, messages);

    const corpseData = data.corpse
        || (data.name?.endsWith(' zombie') ? monsterByRndName(data.name.replace(/ zombie$/, '')) : null)
        || data;
    const loc = game.level?.at?.(mon.mx, mon.my);
    if (loc && (ACCESSIBLE(loc.typ) || IS_POOL(loc.typ))
        && !mon.mcloned && monsterCorpseDropSucceeds(mon, data)
        && monsterLeavesCorpseLikeDrop(corpseData)) {
        createMonsterCorpseOrGlob(mon, corpseData, mon.mx, mon.my, { messages });
    }
    game.level.monsters = (game.level?.monsters || []).filter(candidate => candidate !== mon);
    newsym(mon.mx, mon.my);
}

function splitGremlinFromPotionWaterHit(mon, messages) {
    if (!game.level || !mon) return false;
    const maxHp = Math.max(1, mon.mhpmax || mon.mhp || 1);
    const currentHp = Math.min(mon.mhp || maxHp, maxHp);
    if (currentHp <= 1) return false;

    const data = mon.data || {};
    const spot = enextoMonsterSpot(mon.mx || 0, mon.my || 0, data);
    if (!spot) return false;

    const cloneHp = Math.trunc(currentHp / 2);
    const cloneMaxHp = Math.trunc(maxHp / 2);
    mon.mhp = currentHp - cloneHp;
    mon.mhpmax = Math.max(1, maxHp - cloneMaxHp);

    const clone = {
        ...mon,
        mx: spot.x,
        my: spot.y,
        m_id: next_ident(),
        mhp: Math.max(1, cloneHp),
        mhpmax: Math.max(1, cloneMaxHp),
        mundetected: false,
        mtrapped: 0,
        mcloned: 1,
        minvent: [],
        mleashed: 0,
        isshk: 0,
        isgd: 0,
        ispriest: 0,
    };
    delete clone.mextra;

    if (!game._monster_moving && mon.mpeaceful) {
        const peaceRoll = Math.max(2 + Math.trunc(game.u?.uluck || 0), 2);
        if (mon.mtame) {
            if (!rn2(peaceRoll)) {
                clone.mtame = 0;
                clone.pet = false;
            } else {
                ensurePetExtension(clone);
            }
        } else {
            clone.mpeaceful = rn2(peaceRoll) ? 1 : 0;
        }
    }

    set_malign(clone);
    game.level.monsters ??= [];
    game.level.monsters.push(clone);
    if (monsterCanBeSpottedForPotionHit(mon))
        messages.push(`${potionHitMonsterName(mon)} multiplies!`);
    newsym(spot.x, spot.y);
    return true;
}

function kindIsHealingFamilyPotion(kind) {
    return kind === 'healing' || kind === 'extra healing' || kind === 'full healing';
}

function healingPotionCuresMonsterBlindness(kind, potion) {
    if (kind === 'full healing') return true;
    if (kind === 'extra healing') return !potion.cursed;
    return kind === 'healing' && !!potion.blessed;
}

function cureMonsterBlindnessFromPotion(mon, messages) {
    if (mon?.mcansee !== false && mon?.mcansee !== 0) return;
    mon.mcansee = true;
    mon.mblinded = 0;
    if (monsterCanBeSeenForPotionEffect(mon) && monsterHasEyesForPotionBlindness(mon))
        messages.push(`${potionHitMonsterName(mon)} can see again.`);
}

function healingPotionHitMonster(potion, mon, kind, messages) {
    if (kindIsHealingFamilyPotion(kind) && monsterIsPestilence(mon)) {
        if ((mon.mhp || 0) > 2) {
            mon.mhp = Math.trunc(mon.mhp / 2);
            if (monsterCanBeSeenForPotionEffect(mon))
                messages.push(`${potionHitMonsterName(mon)} looks rather ill.`);
        }
        return true;
    }

    const maxHp = Math.max(1, mon.mhpmax || mon.mhp || 1);
    if ((mon.mhp || 0) < maxHp) {
        mon.mhp = maxHp;
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} looks sound and hale again.`);
    }
    if (healingPotionCuresMonsterBlindness(kind, potion))
        cureMonsterBlindnessFromPotion(mon, messages);
    return false;
}

function sicknessPotionHitMonster(mon, messages) {
    if (monsterIsPestilence(mon)) {
        const maxHp = Math.max(1, mon.mhpmax || mon.mhp || 1);
        if ((mon.mhp || 0) < maxHp) {
            mon.mhp = maxHp;
            if (monsterCanBeSeenForPotionEffect(mon))
                messages.push(`${potionHitMonsterName(mon)} looks sound and hale again.`);
        }
        return false;
    }

    if (monsterResistsSicknessPotion(mon)) {
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} looks unharmed.`);
        return true;
    }

    if ((mon.mhp || 0) > 2) {
        mon.mhp = Math.trunc(mon.mhp / 2);
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} looks rather ill.`);
    }
    return true;
}

function acidPotionHitMonster(potion, mon, messages, { yourFault = true } = {}) {
    if (monsterResistsAcid(mon) || monsterResistsEffect(mon, 6)) return !!yourFault;

    const silent = monsterIsSilentForPotionHit(mon);
    messages.push(`${potionHitMonsterName(mon)} ${silent ? 'writhes' : 'shrieks'} in pain!`);
    if (!silent) wakeNearbyMonstersFromPotionHit(mon);

    const dice = potion?.cursed ? 2 : 1;
    const sides = potion?.blessed ? 4 : 8;
    mon.mhp = (mon.mhp || 1) - d(dice, sides);
    if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages, { heroFault: !!yourFault });
    return !!yourFault;
}

function monsterHasMagicResistanceForPolymorph(mon) {
    const data = mon?.data || {};
    if (mon?.magicResistance || mon?.resistsMagic || mon?.resists_magm
        || data.magicResistance || data.resistsMagic || data.resists_magm
        || data.magicalBreath || data.rbreath) return true;
    if ((mon.minvent || []).some(item => (item.worn || item.owornmask)
        && /magic resistance|antimagic|gray dragon/i.test(String(item.kind || item.actualKind || item.line || ''))))
        return true;
    return false;
}

function monsterIsShapechangerForPolymorph(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(mon?.cham || mon?.chamName || data.cham || data.chamName
        || data.shapechanger || mon?.shapechanger || data.vampshifter || mon?.vampshifter
        || data.were || data.isWere || data.wereHuman || data.wereBeast
        || mon?.were || mon?.isWere || /^were/.test(name)
        || name === 'chameleon' || name === 'doppelganger' || name === 'sandestin'
        || /\bvampire\b/.test(name));
}

function monsterIsImmuneToNewcham(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(data.rider || name === 'death' || name === 'pestilence' || name === 'famine'
        || name === 'nazgul' || name === 'erinys');
}

function monsterPolymorphTargetAllowed(data) {
    if (!data) return false;
    const name = String(data.name || '').toLowerCase();
    return !(data.noPoly || data.nopoly || data.no_polymorph || data.placeholder
        || data.unique || data.nemesis || data.rider || data.guardian
        || data.were || data.wereHuman || data.wereBeast || /^were/.test(name)
        || name === 'death' || name === 'pestilence' || name === 'famine'
        || name === 'erinys' || name === 'nazgul');
}

function randomMonsterPolymorphTarget(mon) {
    if (monsterIsImmuneToNewcham(mon)) return null;
    for (let tries = 0; tries < 20; tries++) {
        const candidate = rndmonnum();
        if (monsterPolymorphTargetAllowed(candidate)
            && String(candidate.name || '') !== String(mon?.data?.name || ''))
            return candidate;
    }
    return null;
}

function indefiniteArticle(noun) {
    return /^[aeiou]/i.test(String(noun || '')) ? 'an' : 'a';
}

function monsterCanWearSaddleData(data = {}) {
    const mlet = data.mlet || data.glyph;
    if (!['u', 'C', 'A', 'D', 'J'].includes(mlet)) return false;
    if (data.verysmall || data.tiny || data.small || data.msize === 'tiny'
        || data.msize === 'small' || data.size === 'tiny' || data.size === 'small')
        return false;
    if (mlet !== 'C' && (data.human || data.humanoid)) return false;
    return !(data.amorphous || data.noncorporeal || data.whirly || data.unsolid);
}

function sSuffixText(text) {
    return String(text || '').endsWith('s') ? `${text}'` : `${text}'s`;
}

function dropInvalidSaddleAfterPolymorph(mon, messages, visible) {
    if (monsterCanWearSaddleData(mon?.data)) return;
    const saddle = monsterWornSaddleForPotionHit(mon);
    if (!saddle) return;
    saddle.worn = false;
    saddle.owornmask = 0;
    saddle.oslot = null;
    mon.saddled = false;
    mon.misc_worn_check = (mon.misc_worn_check || 0) & ~W_SADDLE;
    if (mon.saddle === saddle) delete mon.saddle;
    if (visible) messages.push(`${sSuffixText(potionHitMonsterName(mon))} saddle falls off.`);
    dropMonsterObject(mon, saddle, messages, { verb: 'fall', monsterMoving: false });
}

function killMonsterFromSystemShock(mon, messages) {
    if (!mon || mon.dead) return;
    const name = mon.givenName || `the ${mon.data?.name || mon.name || 'monster'}`;
    mon.dead = true;
    mon.mhp = 0;
    messages.push(`You kill ${name}!`);
    recordVanquished(mon);
    dropMonsterInventory(mon, messages);
    game.level.monsters = (game.level?.monsters || []).filter(candidate => candidate !== mon);
    newsym(mon.mx, mon.my);
}

function polymorphPotionHitMonster(mon, messages, { attackLevel = 6 } = {}) {
    if (monsterHasMagicResistanceForPolymorph(mon)) return true;
    if (monsterResistsEffect(mon, attackLevel)) return true;

    const visible = monsterCanBeSeenForPotionEffect(mon);
    if (!monsterIsShapechangerForPolymorph(mon) && !rn2(25)) {
        if (visible) messages.push(`${potionHitMonsterName(mon)} shudders!`);
        killMonsterFromSystemShock(mon, messages);
        return true;
    }

    const target = randomMonsterPolymorphTarget(mon);
    if (!target) return true;
    const oldVisible = visible;
    const oldName = potionHitMonsterName(mon);
    const oldHp = Math.max(1, mon.mhp || 1);
    const oldMax = Math.max(1, mon.mhpmax || oldHp);
    const nextData = { ...target };
    const nextLevel = adjustedMonsterLevel(nextData);
    const nextMax = Math.max(1, monster_hp(nextData, nextLevel));
    if (nextData.male) mon.female = false;
    else if (nextData.female) mon.female = true;
    else if (!nextData.neuter && !rn2(10)) mon.female = !mon.female;
    Object.assign(mon, {
        data: { ...nextData, hpLevel: nextLevel },
        name: nextData.name,
        mlet: nextData.mlet,
        glyph: nextData.glyph,
        color: nextData.color,
        m_lev: nextLevel,
        mlevel: nextLevel,
        mhpmax: nextMax,
        mhp: Math.max(1, Math.min(nextMax, Math.trunc((oldHp * nextMax) / oldMax))),
        meverseen: 0,
    });
    set_malign(mon);
    if (oldVisible && monsterCanBeSeenForPotionEffect(mon) && !heroIsHallucinating())
        messages.push(`${oldName} turns into ${indefiniteArticle(nextData.name)} ${nextData.name}!`);
    dropInvalidSaddleAfterPolymorph(mon, messages, oldVisible);
    newsym(mon.mx, mon.my);
    return true;
}

function addMonsterPolymorphBypassObjects(mon, bypassObjects) {
    if (!bypassObjects) return;
    for (const obj of mon?.minvent || []) bypassObjects.add(obj);
    if (mon?.missile) bypassObjects.add(mon.missile);
    if (mon?.saddle) bypassObjects.add(mon.saddle);
}

function polymorphRayHitMonster(mon, messages, {
    item = null,
    bypassObjects = null,
    attackLevel = 12,
} = {}) {
    const messageCount = messages.length;
    addMonsterPolymorphBypassObjects(mon, bypassObjects);
    polymorphPotionHitMonster(mon, messages, { attackLevel });
    if (!mon.dead && (mon.mhp ?? 1) > 0) {
        mon.msleeping = 0;
        mon.mpeaceful = false;
    }
    if (item && messages.length > messageCount)
        setKnownWandLine(item, 'polymorph');
}

function polymorphWandHitMonster(mon, messages, item = null, bypassObjects = null) {
    polymorphRayHitMonster(mon, messages, { item, bypassObjects, attackLevel: 12 });
}

function monsterResistsFire(mon) {
    const data = mon?.data || {};
    return !!(mon?.fireResistance || mon?.resistsFire || mon?.resists_fire
        || data.fireResistance || data.resistsFire || data.resists_fire);
}

function monsterResistsCold(mon) {
    const data = mon?.data || {};
    return !!(mon?.coldResistance || mon?.resistsCold || mon?.resists_cold
        || data.coldResistance || data.resistsCold || data.resists_cold);
}

function wakeNearbyMonstersFromExplosion(x, y, damage) {
    const distance = Math.max(damage * damage, 50);
    for (const sleeper of game.level?.monsters || []) {
        if (!sleeper || sleeper.dead || (sleeper.mhp != null && sleeper.mhp <= 0)) continue;
        const dx = (sleeper.mx || 0) - x;
        const dy = (sleeper.my || 0) - y;
        if (dx * dx + dy * dy >= distance) continue;
        sleeper.msleeping = 0;
        if (!(sleeper.data?.unique || sleeper.data?.uniq)) sleeper.mstrategy = 0;
    }
}

function burningOilExplosionVisible(x, y) {
    if (game.u?.blind) return false;
    for (let dx = -1; dx <= 1; dx++)
        for (let dy = -1; dy <= 1; dy++)
            if (cansee(x + dx, y + dy)) return true;
    return false;
}

function damageMonsterFromBurningOilExplosion(mon, damage, messages) {
    const visible = !game.u?.blind && cansee(mon.mx, mon.my);
    const name = fireScrollMonsterName(mon);
    if (visible) messages.push(`${name} is caught in the burning oil!`);

    const itemDamage = (mon.minvent || []).length
        ? monsterFireInventoryDamage(mon, damage, messages, visible)
        : 0;
    let monDamage = 0;
    if (!monsterResistsFire(mon)) {
        monDamage = damage;
        if (monsterResistsEffect(mon, game.u?.ulevel || 1)) {
            if (visible) messages.push(`${name} resists the burning oil!`);
            monDamage = Math.trunc((monDamage + 1) / 2);
        }
        if (monsterResistsCold(mon)) monDamage *= 2;
    }

    mon.mhp = (mon.mhp || 1) - monDamage - itemDamage;
    if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages);
    else mon.mpeaceful = false;
}

function damageHeroFromBurningOilExplosion(damage, messages) {
    const ux = game.u?.ux ?? -99;
    const uy = game.u?.uy ?? -99;
    messages.push('You are caught in the burning oil!');
    burnAwayHeroSlime(messages);
    applyHeroFireExplosionInventoryDamage(messages, damage, 'caught yourself in your own burning oil', {
        fatalMessage: 'It is fatal.',
    });
    exerciseAttribute(A_STR, false);
    newsym(ux, uy);
}

function heroIsSliming() {
    return !!(game.u?._slimingTimeout || game.u?.sliming || game.u?.slimed
        || /\bSlime(?:d)?\b/.test(game.u?._statusSuffix || ''));
}

function burnAwayHeroSlime(messages = []) {
    if (!heroIsSliming()) return false;
    game.u._slimingTimeout = 0;
    game.u.sliming = false;
    game.u.slimed = false;
    removeHeroStatusSuffix('Slime');
    removeHeroStatusSuffix('Slimed');
    messages.push('The slime that covers you is burned away!');
    return true;
}

function applyBurningOilDoorTerrain(x, y, messages) {
    if (!isok(x, y)) return false;
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    const visible = !game.u?.blind && cansee(x, y);
    let changed = false;

    if (loc.typ === SDOOR) {
        loc.typ = DOOR;
        newsym(x, y);
        changed = true;
        if (visible) messages.push('The blast reveals a secret door.');
    }
    if (loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED))) {
        addShopTerrainDamage(x, y, SHOP_DOOR_COST);
        loc.doormask = D_NODOOR;
        loc.flags = 0;
        newsym(x, y);
        changed = true;
        messages.push(visible ? 'The door is consumed in flames!' : 'You smell smoke.');
    }
    if (changed) {
        vision_reset();
        vision_recalc(0);
    }
    return changed;
}

function burnFloorObjectsFromBurningOilExplosion(x, y, messages) {
    let heardGas = false;
    for (let dx = -1; dx <= 1; dx++) {
        for (let dy = -1; dy <= 1; dy++) {
            const sx = x + dx;
            const sy = y + dy;
            const webMessages = burnFireRayWebTrap(sx, sy, {
                previousMessage: messages[messages.length - 1] || '',
            });
            if (webMessages.length) messages.push(...webMessages);
            const ice = applyFireRayIceTerrain(sx, sy, {
                heroRay: true,
                recordKill: recordVanquished,
                buriedMerchandiseDebtMessage,
            });
            if (ice.messages.length) messages.push(...ice.messages);
            if (!ice.handled) {
                const terrain = applyFireRayWaterTerrain(sx, sy, {
                    previousMessage: messages[messages.length - 1] || '',
                    heardGas,
                    heroRay: true,
                });
                if (terrain.messages.length) messages.push(...terrain.messages);
                heardGas = terrain.heardGas;
                const fountain = applyFireRayFountainTerrain(sx, sy, { heroRay: true });
                if (fountain.messages.length) messages.push(...fountain.messages);
            }
            applyBurningOilDoorTerrain(sx, sy, messages);
            const floorFire = burnFloorObjectsByFire(sx, sy, {
                heroCaused: true,
                igniteFeedback: false,
            });
            if (floorFire.messages.length) messages.push(...floorFire.messages);
            if (floorFire.count && couldsee(sx, sy))
                messages.push(`You ${game.u?.blind ? 'smell a whiff' : 'see a puff'} of smoke.`);
        }
    }
}

function explodeBurningOilPotion(potion, x, y, messages) {
    const damage = d(potion?.odiluted ? 3 : 4, 4);
    potion.lamplit = false;
    potion.burning = false;
    delete potion._burnTimer;
    delete potion.litRadius;

    if (!heroIsDeaf())
        messages.push(burningOilExplosionVisible(x, y) ? 'Boom!' : 'You hear a blast.');

    burnFloorObjectsFromBurningOilExplosion(x, y, messages);

    for (const target of [...(game.level?.monsters || [])]) {
        if (!target || target.dead || (target.mhp ?? 1) <= 0) continue;
        if (Math.abs((target.mx ?? -99) - x) > 1 || Math.abs((target.my ?? -99) - y) > 1)
            continue;
        damageMonsterFromBurningOilExplosion(target, damage, messages);
    }

    if (Math.abs((game.u?.ux ?? -99) - x) <= 1 && Math.abs((game.u?.uy ?? -99) - y) <= 1)
        damageHeroFromBurningOilExplosion(damage, messages);

    payForCurrentShopTerrainDamage('burn away', messages);
    wakeNearbyMonstersFromExplosion(x, y, damage);
}

function litOilPotionHitMonster(potion, mon, messages) {
    explodeBurningOilPotion(potion, mon.mx || 0, mon.my || 0, messages);
    return true;
}

const WERE_WATER_FORM_DATA = new Map([
    ['wererat', {
        human: { name: 'wererat', mlet: '@', glyph: '@', color: CLR_BROWN, mlevel: 2, mmove: 12, mac: 10, were: true, wereHuman: true },
        beast: { name: 'wererat', mlet: 'r', glyph: 'r', color: CLR_BROWN, mlevel: 2, mmove: 12, mac: 6, nohands: true, animal: true, verysmall: true, were: true, wereBeast: true, noCorpse: true },
        beastNoun: 'rat',
    }],
    ['werejackal', {
        human: { name: 'werejackal', mlet: '@', glyph: '@', color: CLR_RED, mlevel: 2, mmove: 12, mac: 10, were: true, wereHuman: true },
        beast: { name: 'werejackal', mlet: 'd', glyph: 'd', color: CLR_BROWN, mlevel: 2, mmove: 12, mac: 7, nohands: true, animal: true, were: true, wereBeast: true, noCorpse: true },
        beastNoun: 'jackal',
    }],
    ['werewolf', {
        human: { name: 'werewolf', mlet: '@', glyph: '@', color: CLR_ORANGE, mlevel: 5, mmove: 12, mac: 10, were: true, wereHuman: true },
        beast: { name: 'werewolf', mlet: 'd', glyph: 'd', color: CLR_GRAY, mlevel: 5, mmove: 12, mac: 4, nohands: true, animal: true, were: true, wereBeast: true, noCorpse: true },
        beastNoun: 'wolf',
    }],
]);

function wereSpeciesNameForWaterHit(mon) {
    const name = String(mon?.data?.name || mon?.name || '').toLowerCase();
    const match = name.match(/\b(were(?:rat|jackal|wolf))\b/);
    return match?.[1] || '';
}

function monsterIsWereForWaterHit(mon) {
    return !!wereSpeciesNameForWaterHit(mon)
        && !!(mon?.data?.were || mon?.data?.isWere || mon?.were || mon?.isWere
            || /^were/.test(String(mon?.data?.name || mon?.name || '').toLowerCase()));
}

function monsterIsHumanWereForWaterHit(mon) {
    if (!monsterIsWereForWaterHit(mon)) return false;
    const data = mon?.data || {};
    if (data.wereHuman || mon?.wereHuman || data.humanWere || mon?.humanWere) return true;
    if (data.wereBeast || mon?.wereBeast) return false;
    const mlet = data.mlet || mon?.mlet || data.glyph || mon?.glyph;
    return mlet === '@' || data.human === true || data.humanoid === true;
}

function heroHasProtectionFromShapeChangers() {
    if (game.u?.protectionFromShapeChangers || game.u?.Protection_from_shape_changers) return true;
    return (game.inventory || []).some(item => {
        if (!(item.worn || item.line?.includes('being worn'))) return false;
        return /protection from shape (?:changers|shifters)/i
            .test(String(item.kind || item.actualKind || item.line || ''));
    });
}

function transformWereMonsterFromWater(mon, messages) {
    const species = wereSpeciesNameForWaterHit(mon);
    const forms = WERE_WATER_FORM_DATA.get(species);
    if (!forms) return false;
    const toHuman = !monsterIsHumanWereForWaterHit(mon);
    const nextData = { ...(toHuman ? forms.human : forms.beast) };
    if (monsterCanBeSeenForPotionEffect(mon) && !heroIsHallucinating())
        messages.push(`${potionHitMonsterName(mon)} changes into a ${toHuman ? 'human' : forms.beastNoun}.`);
    mon.data = nextData;
    mon.name = nextData.name;
    mon.mlet = nextData.mlet;
    mon.glyph = nextData.glyph;
    mon.color = nextData.color;
    mon.m_lev = nextData.mlevel;
    mon.mlevel = nextData.mlevel;
    mon.were = true;
    mon.isWere = true;
    mon.wereHuman = !!nextData.wereHuman;
    mon.wereBeast = !!nextData.wereBeast;
    if (mon.msleeping || mon.mfrozen || mon.mcanmove === false || mon.mcanmove === 0) {
        mon.msleeping = 0;
        mon.mfrozen = 0;
        mon.mcanmove = true;
    }
    const lostHp = Math.max(0, (mon.mhpmax || mon.mhp || 1) - (mon.mhp || 1));
    if (lostHp > 0) mon.mhp = Math.min(mon.mhpmax || mon.mhp || 1, (mon.mhp || 1) + Math.trunc(lostHp / 4));
    newsym(mon.mx, mon.my);
    return true;
}

function waterPotionHitShapechanger(potion, mon, messages) {
    if (potion?.blessed) {
        const silent = monsterIsSilentForPotionHit(mon);
        messages.push(`${potionHitMonsterName(mon)} ${silent ? 'writhes' : 'shrieks'} in pain!`);
        if (!silent) wakeNearbyMonstersFromPotionHit(mon);
        mon.mhp = (mon.mhp || 1) - d(2, 6);
        if ((mon.mhp || 0) <= 0) {
            killMonsterFromPotionHit(mon, messages);
        } else if (monsterIsWereForWaterHit(mon) && !monsterIsHumanWereForWaterHit(mon)) {
            transformWereMonsterFromWater(mon, messages);
        }
        return true;
    }
    if (potion?.cursed) {
        const maxHp = Math.max(1, mon.mhpmax || mon.mhp || 1);
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} looks healthier.`);
        mon.mhp = Math.min(maxHp, (mon.mhp || maxHp) + d(2, 6));
        if (monsterIsWereForWaterHit(mon) && monsterIsHumanWereForWaterHit(mon)
            && !heroHasProtectionFromShapeChangers())
            transformWereMonsterFromWater(mon, messages);
        return false;
    }
    return true;
}

function waterPotionHitSpecialMonster(mon, messages) {
    if (monsterIsGremlinForWaterHit(mon)) {
        splitGremlinFromPotionWaterHit(mon, messages);
        return false;
    }
    if (monsterIsIronGolemForWaterHit(mon)) {
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} rusts.`);
        mon.mhp = (mon.mhp || 1) - d(1, 6);
        if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages);
    }
    return true;
}

function waterPotionHitBlessingHater(potion, mon, messages) {
    if (potion?.blessed) {
        const silent = monsterIsSilentForPotionHit(mon);
        messages.push(`${potionHitMonsterName(mon)} ${silent ? 'writhes' : 'shrieks'} in pain!`);
        if (!silent) wakeNearbyMonstersFromPotionHit(mon);
        mon.mhp = (mon.mhp || 1) - d(2, 6);
        if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages);
        return true;
    }
    if (potion?.cursed) {
        const maxHp = Math.max(1, mon.mhpmax || mon.mhp || 1);
        if (monsterCanBeSeenForPotionEffect(mon))
            messages.push(`${potionHitMonsterName(mon)} looks healthier.`);
        mon.mhp = Math.min(maxHp, (mon.mhp || maxHp) + d(2, 6));
        return false;
    }
    return true;
}

function potionHitsSaddle(potion, kind = thrownPotionEffectKind(potion)) {
    if (!rn2(10)) return true;
    if (kind !== 'water') return false;
    if (rnl(10) > 7 && potion?.cursed) return true;
    if (rnl(10) < 4 && potion?.blessed) return true;
    return !rn2(3);
}

function saddlePotionHitTargetName(mon) {
    const name = mon?.givenName || `the ${mon?.data?.name || 'monster'}`;
    return `${name}'s saddle`;
}

function saddleGlowPhrase(mon) {
    return `${sentenceCase(saddlePotionHitTargetName(mon))} glows`;
}

function saddleGlowAuraMessage(mon, color) {
    const article = /^[aeiou]/i.test(color) ? 'an' : 'a';
    return `${saddleGlowPhrase(mon)} with ${article} ${color} aura.`;
}

function learnSaddleWaterDipBuc(saddle, potion, visible) {
    if (!saddle) return;
    if (visible) {
        saddle.bknown = !heroIsHallucinating();
    } else if (!potion?.bknown || !potion?.dknown) {
        saddle.bknown = false;
    }
}

function potionHitTargetSquareVisible(mon) {
    if (!mon || game.u?.blind) return false;
    // Isolated unit tests may exercise potion hits before full vision init.
    if (!game.viz_array) return true;
    return cansee(mon.mx, mon.my);
}

function waterPotionHitSaddle(potion, mon, messages, visible = monsterCanBeSeenForPotionEffect(mon)) {
    const saddle = monsterWornSaddleForPotionHit(mon);
    if (!saddle) return false;
    let affected = false;

    if (potion?.blessed) {
        if (saddle.cursed) {
            if (visible) messages.push(`${saddleGlowPhrase(mon)} amber.`);
            saddle.cursed = false;
            affected = true;
        } else if (!saddle.blessed) {
            if (visible) messages.push(saddleGlowAuraMessage(mon, 'light blue'));
            saddle.blessed = true;
            saddle.cursed = false;
            affected = true;
        }
    } else if (potion?.cursed) {
        if (saddle.blessed) {
            if (visible) messages.push(`${saddleGlowPhrase(mon)} brown.`);
            saddle.blessed = false;
            affected = true;
        } else if (!saddle.cursed) {
            if (visible) messages.push(saddleGlowAuraMessage(mon, 'black'));
            saddle.cursed = true;
            saddle.blessed = false;
            affected = true;
        }
    }

    if (affected) {
        learnSaddleWaterDipBuc(saddle, potion, visible);
    } else if (visible) {
        messages.push(`${sentenceCase(saddlePotionHitTargetName(mon))} gets wet.`);
    }
    return affected;
}

function potionHitSaddle(potion, mon, messages, kind = thrownPotionEffectKind(potion), visible = monsterCanBeSeenForPotionEffect(mon)) {
    if (kind === 'water') return waterPotionHitSaddle(potion, mon, messages, visible);
    if (visible) messages.push(`${sentenceCase(saddlePotionHitTargetName(mon))} gets wet.`);
    return false;
}

export function heroThrownPotionHitMonster(potion, mon, { yourFault = true, allowLifeSaving = false } = {}) {
    const messages = [];
    const bottle = chestShatterBottleName();
    const kind = thrownPotionEffectKind(potion);
    const saddlePotion = isSaddlePotionHit(potion, mon, kind);
    const hitSaddle = saddlePotion && potionHitsSaddle(potion, kind);
    const targetSquareVisible = potionHitTargetSquareVisible(mon);
    const saddleVisible = targetSquareVisible && monsterCanBeSpottedForPotionHit(mon);
    const waterBranchOptions = { ignoreSaddle: saddlePotion };
    let angerMon = !!yourFault;
    if (targetSquareVisible) {
        messages.push(`The ${bottle} crashes on ${hitSaddle ? saddlePotionHitTargetName(mon) : thrownPotionHitTargetName(mon)} and breaks into shards.`);
    } else {
        messages.push('Crash!');
    }
    const chipRoll = rn2(5);
    if (!hitSaddle && chipRoll && (mon.mhp || 1) > 1) mon.mhp--;
    if (!hitSaddle && !isPotionOfOil(potion) && targetSquareVisible)
        messages.push(`The ${pickupObjectName({ ...potion, quan: 1 })} evaporates.`);

    if (hitSaddle) {
        potionHitSaddle(potion, mon, messages, kind, saddleVisible);
    } else if (kind === 'confusion' || kind === 'booze') {
        if (!monsterResistsEffect(mon, 6)) mon.mconf = true;
    } else if (kind === 'paralysis') {
        if (monsterCanMoveForPotionParalysis(mon)) paralyzeMonsterFromPotion(mon, rnd(25));
    } else if (kind === 'sleeping') {
        if (sleepMonsterFromPotion(mon, rnd(12)))
            messages.push(`${potionHitMonsterName(mon)} falls asleep.`);
    } else if (kind === 'blindness') {
        blindMonsterFromPotion(mon);
    } else if (kind === 'speed') {
        angerMon = false;
        const changed = speedMonsterFromPotion(mon);
        if (changed && (mon.data?.mmove ?? NORMAL_SPEED) && !(mon.mfrozen || mon.msleeping)
            && monsterCanBeSeenForPotionEffect(mon)) {
            messages.push(`${potionHitMonsterName(mon)} is suddenly moving faster.`);
        }
    } else if (kind === 'invisibility') {
        angerMon = invisibilityPotionHitMonster(potion, mon, messages);
    } else if (kind === 'sickness') {
        angerMon = sicknessPotionHitMonster(mon, messages);
    } else if (kind === 'acid') {
        angerMon = acidPotionHitMonster(potion, mon, messages, { yourFault });
    } else if (kind === 'polymorph') {
        angerMon = polymorphPotionHitMonster(mon, messages);
    } else if (kind === 'healing' || kind === 'extra healing' || kind === 'full healing'
        || kind === 'restore ability' || kind === 'gain ability') {
        angerMon = healingPotionHitMonster(potion, mon, kind, messages);
    } else if (isShapechangerWaterPotionHit(potion, mon, kind, waterBranchOptions)) {
        angerMon = waterPotionHitShapechanger(potion, mon, messages);
    } else if (isBlessingHaterWaterPotionHit(potion, mon, kind, waterBranchOptions)) {
        angerMon = waterPotionHitBlessingHater(potion, mon, messages);
    } else if (isSpecialMonsterWaterPotionHit(potion, mon, kind, waterBranchOptions)) {
        angerMon = waterPotionHitSpecialMonster(mon, messages);
    } else if (isNeutralOrdinaryWaterPotionHit(potion, mon, kind, waterBranchOptions)) {
        // Ordinary water has no monster-specific case for this gated target set.
    } else if (COMMON_NO_MONSTER_EFFECT_POTION_HIT_KINDS.has(kind)) {
        // C has no monster-specific case for these potions; keep the common hit and anger tail.
    } else if (isLitOilPotionHit(potion, kind)) {
        angerMon = litOilPotionHitMonster(potion, mon, messages);
    } else if (isUnlitOilPotionHit(potion, kind)) {
        // Unlit oil has no monster-specific case.
    }

    if (!hitSaddle && !mon.dead && (mon.mhp ?? 1) > 0) {
        mon.msleeping = 0;
        if (angerMon) {
            mon.meating = 0;
            clearMonsterPotionWaitStrategy(mon);
            mon.mpeaceful = false;
        }
    }

    const dx = (mon.mx || 0) - (game.u?.ux || 0);
    const dy = (mon.my || 0) - (game.u?.uy || 0);
    const distance = dx * dx + dy * dy;
    const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
    if ((distance === 0 || (distance < 3 && !rn2(Math.max(1, Math.trunc((1 + dex) / 2)))))
        && heroCanReceivePotionVapor()) {
        potionBreathe(potion, messages, { allowLifeSaving });
    } else if (potion.dknown && !game.u?.blind && cansee(mon.mx, mon.my)) {
        queuePotionTryCall(potion, kind);
    }

    const shopDebt = convertUnpaidObjectToShopDebt(potion, { silent: true, broken: true });
    if (!shopDebt.charged) potion.no_charge = true;
    return messages;
}

export function monsterThrownPotionHitMonster(potion, mon) {
    return heroThrownPotionHitMonster(potion, mon, { yourFault: false });
}

function heroThrownVenomTargetName(mon) {
    return fireScrollMonsterName(mon).replace(/^The /, 'the ');
}

function heroThrownCreamPieTargetName(mon) {
    return heroThrownVenomTargetName(mon);
}

function possessiveMonsterName(name) {
    return `${name}${String(name).endsWith('s') ? "'" : "'s"}`;
}

function heroThrownCreamPieHitMonsterMessage(pie, mon) {
    const pieName = floorObjectTheSubject({ ...pie, quan: 1 });
    const targetName = heroThrownCreamPieTargetName(mon);
    const dataName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    const faceTarget = monsterHasEyesForPotionBlindness(mon) && dataName !== 'floating eye'
        ? `${possessiveMonsterName(targetName)} face`
        : targetName;
    return `${pieName} splashes over ${faceTarget}!`;
}

function setHeroObjectHitMonsterAngry(mon) {
    clearMonsterPotionWaitStrategy(mon);
    if (mon.mpeaceful && !(mon.mtame || mon.pet)) mon.mpeaceful = 0;
}

function wakeMonsterFromHeroThrownMiss(mon) {
    const messages = [];
    if (rn2(3)) return messages;
    if (mon.msleeping && monsterCanBeSeenForPotionEffect(mon))
        messages.push(`${potionHitMonsterName(mon)} wakes up!`);
    mon.msleeping = 0;
    mon.meating = 0;
    setHeroObjectHitMonsterAngry(mon);
    return messages;
}

function wakeMonsterFromHeroThrownHit(mon) {
    mon.msleeping = 0;
    mon.meating = 0;
    setHeroObjectHitMonsterAngry(mon);
}

function heroThrownStoneMissileObject(obj) {
    if (!obj || itemClassKey(obj) === 'ring' || obj.glyph === '=' || obj.otyp === RING_CLASS) return false;
    const material = stoneToFleshObjectMaterial(obj);
    return material === 'gemstone' || material === 'mineral';
}

function heroThrownTargetPassesRocks(mon) {
    const data = mon?.data || {};
    const name = String(mon?.name || data.name || data.mname || '').toLowerCase().trim().replace(/\s+/g, ' ');
    const intendedRockPasser = name === 'xorn' || name === 'earth elemental'
        || mon?.passesRocks || data.passesRocks;
    const passesWalls = name === 'xorn' || name === 'earth elemental'
        || mon?.passWalls || mon?.passesWalls || mon?.passes_walls || mon?.wallwalk
        || data.passWalls || data.passesWalls || data.passes_walls || data.wallwalk;
    return !!intendedRockPasser && !!passesWalls
        && !(mon?.unsolid || mon?.noncorporeal || data.unsolid || data.noncorporeal);
}

function heroThrownStoneMissileHarmlessRockPasser(obj, mon) {
    return heroThrownStoneMissileObject(obj) && heroThrownTargetPassesRocks(mon);
}

function heroThrownGemClassObject(obj) {
    return !!obj && (itemClassKey(obj) === 'gem' || obj.glyph === '*' || obj.otyp === GEM_CLASS)
        && itemClassKey(obj) !== 'ring' && obj.glyph !== '=';
}

function heroThrownGemNameValue(value) {
    return String(value || '').toLowerCase().trim().replace(/^an? /, '').replace(/\s+/g, ' ');
}

function heroThrownGemNameValues(obj) {
    return [
        obj?.actualKind,
        obj?.kind,
        obj?.gemDescription,
        obj?.displayName,
    ].map(heroThrownGemNameValue).filter(Boolean);
}

function heroThrownGemIdentityName(obj) {
    const material = stoneToFleshObjectMaterial(obj);
    if (material === 'glass') {
        const glassName = heroThrownGemNameValues(obj)
            .find(name => /\bworthless piece of\b.*\bglass\b/.test(name));
        if (glassName) return glassName;
        const description = heroThrownGemNameValue(obj?.gemDescription);
        const color = description.match(/^(.+) gem$/)?.[1] || description;
        if (color) return `worthless piece of ${color} glass`;
    }
    return heroThrownGemNameValues(obj)
        .find(name => STONE_TO_FLESH_GEMSTONE_NAMES.has(name) || STONE_TO_FLESH_MINERAL_GEM_NAMES.has(name))
        || heroThrownGemNameValue(obj?.actualKind || obj?.kind || obj?.gemDescription);
}

function heroThrownGemTypeKnown(obj) {
    if (obj?.dknown === false) return false;
    const identity = heroThrownGemIdentityName(obj);
    return obj?.known === true || gemStoneDiscoveryKnown(identity);
}

function heroThrownGemHasUserName(obj) {
    return !!(obj?.oname || obj?.o_name || obj?.userName || obj?.calledName || obj?.uname);
}

function heroThrownMonsterIsUnicorn(mon) {
    const data = mon?.data || {};
    const name = heroThrownGemNameValue(mon?.name || data.name || data.mname);
    const mlet = heroThrownGemNameValue(mon?.mlet || data.mlet || mon?.glyph || data.glyph || data.symbol);
    const likesGems = !!(mon?.likesGems || mon?.likes_gems || data.likesGems || data.likes_gems || /unicorn/.test(name));
    return likesGems && (mlet === 'u' || mlet === 'unicorn' || /unicorn/.test(name));
}

function heroThrownUnicornGemKind(obj) {
    if (!heroThrownGemClassObject(obj)) return '';
    const material = stoneToFleshObjectMaterial(obj);
    if (material === 'mineral') return '';
    if (material === 'glass') return 'glass';
    if (material === 'gemstone') return 'real';
    const names = heroThrownGemNameValues(obj);
    if (names.some(name => STONE_TO_FLESH_MINERAL_GEM_NAMES.has(name))) return '';
    if (names.some(name => /\bworthless piece of\b.*\bglass\b/.test(name) || name === 'glass'))
        return 'glass';
    if (names.some(name => STONE_TO_FLESH_GEMSTONE_NAMES.has(name))) return 'real';
    return '';
}

function heroThrownMonsterHelplessForUnicornGem(mon) {
    return !!(mon?.helpless || mon?.msleeping || mon?.mfrozen
        || mon?.mcanmove === false || mon?.mcanmove === 0);
}

function applyHeroThrownUnicornGemPreCatchAdjustment(mon) {
    const data = mon?.data || {};
    const immobile = mon?.mcanmove === false || mon?.mcanmove === 0
        || data.mmove === false || data.mmove === 0;
    if (immobile && data.mmove !== false && data.mmove !== 0 && !rn2(10)) {
        mon.mcanmove = true;
        mon.mfrozen = 0;
    }
}

function heroThrownUnicornGemLuckMessageAndDelta(obj, mon, gemKind) {
    const monSign = Math.sign(Number(mon?.data?.maligntyp ?? mon?.maligntyp ?? 0));
    const heroSign = Math.sign(Number(game.u?.ualign?.type ?? A_NEUTRAL));
    const buddy = monSign === heroSign;
    const known = heroThrownGemTypeKnown(obj);
    const named = heroThrownGemHasUserName(obj);
    if (gemKind === 'real') {
        if (known) return buddy
            ? { word: 'gratefully', delta: 5 }
            : { word: 'hesitatingly', delta: rn2(7) - 3 };
        if (named) return buddy
            ? { word: 'gratefully', delta: 2 }
            : { word: 'hesitatingly', delta: rn2(3) - 1 };
        return buddy
            ? { word: 'gratefully', delta: 1 }
            : { word: 'hesitatingly', delta: rn2(3) - 1 };
    }
    if (known || named) return { junk: true };
    return { word: 'graciously', delta: 0 };
}

function relocateHeroThrownUnicornGemRecipient(mon) {
    if (!noteleportLevelForMonster(mon)) rlocNoMsg(mon);
}

function heroThrownUnicornGemImpact(obj, mon) {
    if (!heroThrownMonsterIsUnicorn(mon)) return { handled: false, messages: [] };
    const gemKind = heroThrownUnicornGemKind(obj);
    if (!gemKind) return { handled: false, messages: [] };
    applyHeroThrownUnicornGemPreCatchAdjustment(mon);
    const monName = fireScrollMonsterName(mon);
    const objectName = floorObjectTheName({ ...obj, quan: 1 });
    if (heroThrownMonsterHelplessForUnicornGem(mon)) {
        return {
            handled: true,
            consumed: false,
            messages: [`The ${pickupObjectName({ ...obj, quan: 1 })} misses ${heroThrownVenomTargetName(mon)}.`],
        };
    }
    if (mon?.mtame || mon?.pet || mon?.tame) {
        return {
            handled: true,
            consumed: false,
            messages: [`${monName} catches and drops ${objectName}.`],
        };
    }

    const messages = [`${monName} catches ${objectName}.`];
    mon.mpeaceful = 1;
    mon.mavenge = 0;
    const accept = heroThrownUnicornGemLuckMessageAndDelta(obj, mon, gemKind);
    if (accept.junk) {
        if (!game.u?.blind) messages.push(`${monName} is not interested in your junk.`);
        relocateHeroThrownUnicornGemRecipient(mon);
        return { handled: true, consumed: false, messages };
    }

    changeHeroLuck(accept.delta);
    if (!game.u?.blind) messages.push(`${monName} ${accept.word} accepts your gift.`);
    const accepted = { ...obj, quan: 1, hidden: false, buried: false, transientProjectile: false };
    delete accepted.letter;
    delete accepted.line;
    delete accepted.ox;
    delete accepted.oy;
    add_to_minv(mon, accepted);
    relocateHeroThrownUnicornGemRecipient(mon);
    return { handled: true, consumed: true, messages };
}

function heroProjectileMonsterArmorClass(mon) {
    const data = mon?.data || {};
    const ac = mon?.mac ?? mon?.ac ?? data.mac ?? data.ac;
    const numeric = Number(ac);
    return Number.isFinite(numeric) ? Math.trunc(numeric) : 10;
}

function heroProjectileHitLevel() {
    const polyLevel = game.u?._polyself_form?.mlevel
        ?? game.u?.youmonst?.mlevel
        ?? game.u?.youmonst?.data?.mlevel;
    const level = polyLevel ?? game.u?.ulevel ?? 1;
    return Math.max(1, Math.trunc(Number(level) || 1));
}

function heroProjectileDexHitBonus() {
    const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
    if (dex < 4) return -3;
    if (dex < 6) return -2;
    if (dex < 8) return -1;
    if (dex >= 14) return dex - 14;
    return 0;
}

function heroStrengthDamageBonus() {
    if (polyselfForm()) return 0;
    const str = game.u?.acurr?.a?.[A_STR] ?? 10;
    if (str < 6) return -1;
    if (str < 16) return 0;
    if (str < 18) return 1;
    if (str === 18) return 2;
    if (str <= STR18(75)) return 3;
    if (str <= STR18(90)) return 4;
    if (str < STR18(100)) return 5;
    return 6;
}

function heroDamageIncreaseBonus() {
    return Math.trunc(Number(game.u?.udaminc || 0));
}

function exerciseHeroProjectileHitDexterity() {
    exerciseAttribute(A_DEX, true);
}

const HERO_PROJECTILE_MONSTER_SIZE_VALUES = new Map([
    ['tiny', 0],
    ['small', 1],
    ['medium', 2],
    ['human', 2],
    ['large', 3],
    ['huge', 4],
    ['gigantic', 7],
]);

const HERO_THROWN_WEAPON_MONSTER_DATA = new Map([
    ['dagger', { smallDie: 4, largeDie: 3, hitbon: 2, skill: P_DAGGER, skillName: 'dagger' }],
    ['knife', { smallDie: 3, largeDie: 2, hitbon: 0, skill: P_KNIFE, skillName: 'knife' }],
]);

function heroProjectileMonsterSizeValue(mon) {
    const data = mon?.data || {};
    const value = mon?.msize ?? mon?.size ?? data.msize ?? data.size;
    if (Number.isFinite(Number(value))) return Math.trunc(Number(value));
    const key = heroThrownGemNameValue(value);
    if (HERO_PROJECTILE_MONSTER_SIZE_VALUES.has(key))
        return HERO_PROJECTILE_MONSTER_SIZE_VALUES.get(key);
    if (mon?.verysmall || data.verysmall || mon?.tiny || data.tiny) return 0;
    if (mon?.small || data.small) return 1;
    if (mon?.large || data.large) return 3;
    if (mon?.huge || data.huge) return 4;
    if (mon?.gigantic || data.gigantic) return 7;
    return 2;
}

function heroProjectileObjectUsesHitval(obj) {
    return heroThrownGemClassObject(obj)
        || itemClassKey(obj) === 'weapon' || obj?.glyph === ')' || obj?.otyp === WEAPON_CLASS
        || isWeaponTool(obj);
}

function heroProjectileObjectHitval(obj) {
    if (!heroProjectileObjectUsesHitval(obj)) return 0;
    let bonus = 0;
    if (itemClassKey(obj) === 'weapon' || obj?.glyph === ')' || obj?.otyp === WEAPON_CLASS || isWeaponTool(obj))
        bonus += Math.trunc(Number(obj?.spe || 0));
    const explicit = obj?.hitbon ?? obj?.oc_hitbon;
    if (Number.isFinite(Number(explicit))) bonus += Math.trunc(Number(explicit));
    else bonus += Math.trunc(Number(HERO_THROWN_WEAPON_MONSTER_DATA.get(tossUpWeaponObjectKey(obj))?.hitbon || 0));
    return bonus;
}

function heroProjectileObjectHitAdjustment(obj, mon, { monNotices = true } = {}) {
    const data = mon?.data || {};
    let adjustment = heroProjectileMonsterSizeValue(mon) - 2;
    if (mon?.msleeping) adjustment += 2;
    const immobile = mon?.mcanmove === false || mon?.mcanmove === 0
        || data.mmove === false || data.mmove === 0;
    if (immobile) {
        adjustment += 4;
        if (monNotices && data.mmove !== false && data.mmove !== 0 && !rn2(10)) {
            mon.mcanmove = true;
            mon.mfrozen = 0;
        }
    }
    if (obj?.otyp === HEAVY_IRON_BALL && obj !== game.u?.uball) adjustment += 2;
    else if (obj?.otyp === BOULDER || objectKindKey(obj) === 'boulder') adjustment += 6;
    else adjustment += heroProjectileObjectHitval(obj);
    return adjustment;
}

function heroProjectileBaseHitValue(obj, mon) {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const disttmp = Math.max(-4, 3 - distmin(ux, uy, mon?.mx ?? ux, mon?.my ?? uy));
    return -1
        + Math.trunc(Number(game.u?.uluck || 0))
        + Math.trunc(Number(game.u?.moreluck || 0))
        + heroProjectileMonsterArmorClass(mon)
        + Math.trunc(Number(game.u?.uhitinc || 0))
        + heroProjectileHitLevel()
        + heroProjectileDexHitBonus()
        + disttmp
        + heroProjectileObjectHitAdjustment(obj, mon);
}

function heroKickedProjectileIsAmmo(obj) {
    if (!obj) return false;
    if (itemClassKey(obj) === 'gem' || obj.glyph === '*' || obj.otyp === GEM_CLASS) return true;
    const kind = objectKindKey(obj);
    return obj.otyp === ROCK || obj.otyp === FLINT
        || /\b(?:arrow|arrows|bolt|bolts|dart|darts|shuriken|throwing stars?|rock|rocks|flint)\b/.test(kind);
}

function heroKickedProjectileHitValue(obj, mon) {
    return heroProjectileBaseHitValue(obj, mon) - (heroKickedProjectileIsAmmo(obj) ? 5 : 3);
}

function shouldMulchHeroProjectileMissile(obj) {
    if (!monsterThrownMulchCandidate(obj)) return false;
    const erosion = Math.max(0, Math.trunc(Number(obj.oeroded || 0)), Math.trunc(Number(obj.oeroded2 || 0)));
    const chance = 3 + erosion - Math.trunc(Number(obj.spe || 0));
    let broken = chance > 1 ? !!rn2(chance) : !rn2(4);
    if (obj.blessed && !rnl(4)) broken = false;
    if (monsterThrownHardGemMulchCandidate(obj) && !rn2(2)) broken = false;
    return broken;
}

function heroKickedStoneMissileRockPasserImpact(obj, mon) {
    if (!heroThrownStoneMissileHarmlessRockPasser(obj, mon)) return { handled: false, messages: [] };
    const hitValue = heroKickedProjectileHitValue(obj, mon);
    const dieroll = rnd(20);
    if (hitValue >= dieroll) {
        wakeMonsterFromHeroThrownHit(mon);
        exerciseHeroProjectileHitDexterity();
        const mulched = shouldMulchHeroProjectileMissile(obj);
        if (mulched) rn2(100);
        return {
            handled: true,
            hit: true,
            mulched,
            messages: [`${floorObjectTheSubject({ ...obj, quan: 1 })} hits ${heroThrownVenomTargetName(mon)} but does no harm.`],
        };
    }
    const messages = [`The ${pickupObjectName({ ...obj, quan: 1 })} misses the ${mon?.data?.name || 'creature'}.`];
    messages.push(...wakeMonsterFromHeroThrownMiss(mon));
    return { handled: true, hit: false, messages };
}

function heroKickedGemImpact(obj, mon) {
    if (!heroThrownGemClassObject(obj)) return { handled: false, messages: [] };
    const hitValue = heroKickedProjectileHitValue(obj, mon);
    const dieroll = rnd(20);
    const targetName = heroThrownVenomTargetName(mon);
    if (hitValue >= dieroll) {
        const damage = Math.max(1, rnd(2) + heroStrengthDamageBonus() + heroDamageIncreaseBonus());
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) mon.dead = true;
        wakeMonsterFromHeroThrownHit(mon);
        exerciseHeroProjectileHitDexterity();
        const mulched = shouldMulchHeroProjectileMissile(obj);
        if (mulched) rn2(100);
        return {
            handled: true,
            hit: true,
            damage,
            mulched,
            messages: [`${floorObjectTheSubject({ ...obj, quan: 1 })} hits ${targetName}.`],
        };
    }
    const messages = [`The ${pickupObjectName({ ...obj, quan: 1 })} misses the ${mon?.data?.name || 'creature'}.`];
    messages.push(...wakeMonsterFromHeroThrownMiss(mon));
    return { handled: true, hit: false, messages };
}

function heroThrownGlassGemObject(obj) {
    return heroThrownGemClassObject(obj) && stoneToFleshObjectMaterial(obj) === 'glass';
}

function heroThrownGemHitValue(obj, mon) {
    return heroProjectileBaseHitValue(obj, mon) - 4;
}

function heroProjectileSupportedWeaponObject(obj) {
    if (!obj || obj.artifact || obj.oartifact) return false;
    return HERO_THROWN_WEAPON_MONSTER_DATA.has(tossUpWeaponObjectKey(obj));
}

function heroThrownWeaponHitValue(obj, mon) {
    return heroProjectileBaseHitValue(obj, mon) + 2;
}

function heroKickedWeaponHitValue(obj, mon) {
    return heroKickedProjectileHitValue(obj, mon);
}

function normalizeHeroWeaponSkillLevel(value) {
    if (value == null) return null;
    if (value && typeof value === 'object') {
        if ('skill' in value) return normalizeHeroWeaponSkillLevel(value.skill);
        if ('level' in value) return normalizeHeroWeaponSkillLevel(value.level);
        if ('current' in value) return normalizeHeroWeaponSkillLevel(value.current);
    }
    if (Number.isFinite(Number(value))) return Math.trunc(Number(value));
    const key = String(value ?? '').toLowerCase().replace(/[_-]+/g, ' ').trim();
    if (!key) return null;
    if (key === 'restricted' || key === 'isrestricted') return 0;
    if (key === 'unskilled') return P_UNSKILLED;
    if (key === 'basic') return P_BASIC;
    if (key === 'skilled') return P_SKILLED;
    if (key === 'expert') return P_EXPERT;
    return null;
}

function heroWeaponSkillStateLookup(source, skill, skillName) {
    if (!source) return null;
    if (source instanceof Map) {
        for (const key of [skill, String(skill), skillName]) {
            if (source.has(key)) return source.get(key);
        }
        return null;
    }
    if (Array.isArray(source)) return source[skill] ?? null;
    if (typeof source === 'object') {
        for (const key of [skill, String(skill), skillName]) {
            if (Object.prototype.hasOwnProperty.call(source, key)) return source[key];
        }
    }
    return null;
}

function heroExplicitWeaponSkillLevel(skill, skillName) {
    const sources = [
        game._weapon_skill_levels,
        game._weapon_skills,
        game.weaponSkillLevels,
        game.weaponSkills,
        game._weaponSkillLevels,
        game._weaponSkills,
        game.u?.weapon_skill_levels,
        game.u?.weapon_skills,
        game.u?.weaponSkillLevels,
        game.u?.weaponSkills,
        game.u?._weaponSkillLevels,
        game.u?._weaponSkills,
    ];
    for (const source of sources) {
        const level = normalizeHeroWeaponSkillLevel(heroWeaponSkillStateLookup(source, skill, skillName));
        if (level != null) return level;
    }
    const flagName = `_enhanced_${String(skillName || '').replace(/\W+/g, '_')}`;
    if (game[flagName]) return P_SKILLED;
    return null;
}

function heroWeaponDamageSkillBonus(skill, skillName) {
    const level = heroExplicitWeaponSkillLevel(skill, skillName);
    if (level == null) return 0;
    if (level <= P_UNSKILLED) return -2;
    if (level === P_BASIC) return 0;
    if (level === P_SKILLED) return 1;
    if (level >= P_EXPERT) return 2;
    return 0;
}

function heroProjectileWeaponDamage(obj, mon) {
    const data = HERO_THROWN_WEAPON_MONSTER_DATA.get(tossUpWeaponObjectKey(obj));
    if (!data || !heroProjectileSupportedWeaponObject(obj)) return 0;
    const die = heroProjectileMonsterSizeValue(mon) >= 3 ? data.largeDie : data.smallDie;
    let damage = die ? rnd(die) : 0;
    damage += Math.trunc(Number(obj.spe || 0));
    if (damage < 0) damage = 0;
    if (damage > 0) {
        damage -= Math.max(0, Math.trunc(Number(obj.oeroded || 0)), Math.trunc(Number(obj.oeroded2 || 0)));
        if (damage < 1) damage = 1;
    }
    damage += heroDamageIncreaseBonus() + heroStrengthDamageBonus();
    damage += heroWeaponDamageSkillBonus(data.skill, data.skillName);
    if (damage < 1) damage = 1;
    return damage;
}

function heroProjectileHitPunctuation(damage) {
    if (damage < 0) return '?';
    return damage <= 4 ? '.' : '!';
}

function heroProjectileWeaponImpact(obj, mon, hitValue) {
    if (!heroProjectileSupportedWeaponObject(obj)) return { handled: false, messages: [] };
    const dieroll = rnd(20);
    const targetName = heroThrownVenomTargetName(mon);
    if (hitValue >= dieroll) {
        const damage = heroProjectileWeaponDamage(obj, mon);
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) mon.dead = true;
        wakeMonsterFromHeroThrownHit(mon);
        exerciseHeroProjectileHitDexterity();
        const mulched = shouldMulchHeroProjectileMissile(obj);
        if (mulched) rn2(100);
        return {
            handled: true,
            hit: true,
            damage,
            mulched,
            messages: [`${floorObjectTheSubject({ ...obj, quan: 1 })} hits ${targetName}${heroProjectileHitPunctuation(damage)}`],
        };
    }
    const messages = [`The ${pickupObjectName({ ...obj, quan: 1 })} misses the ${mon?.data?.name || 'creature'}.`];
    messages.push(...wakeMonsterFromHeroThrownMiss(mon));
    return { handled: true, hit: false, messages };
}

function heroThrownWeaponImpact(obj, mon) {
    return heroProjectileWeaponImpact(obj, mon, heroThrownWeaponHitValue(obj, mon));
}

function heroKickedWeaponImpact(obj, mon) {
    return heroProjectileWeaponImpact(obj, mon, heroKickedWeaponHitValue(obj, mon));
}

function heroThrownGemImpact(obj, mon) {
    if (!heroThrownGemClassObject(obj)) return { handled: false, messages: [] };
    const hitValue = heroThrownGemHitValue(obj, mon);
    const dieroll = rnd(20);
    const targetName = heroThrownVenomTargetName(mon);
    if (hitValue >= dieroll) {
        const damage = rnd(2);
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) mon.dead = true;
        wakeMonsterFromHeroThrownHit(mon);
        exerciseHeroProjectileHitDexterity();
        const mulched = shouldMulchHeroProjectileMissile(obj);
        if (mulched) rn2(100);
        return {
            handled: true,
            hit: true,
            damage,
            mulched,
            messages: [`${floorObjectTheSubject({ ...obj, quan: 1 })} hits ${targetName}.`],
        };
    }
    const messages = [`The ${pickupObjectName({ ...obj, quan: 1 })} misses the ${mon?.data?.name || 'creature'}.`];
    messages.push(...wakeMonsterFromHeroThrownMiss(mon));
    return { handled: true, hit: false, messages };
}

function heroThrownGlassGemImpact(obj, mon) {
    if (!heroThrownGlassGemObject(obj)) return { handled: false, messages: [] };
    return heroThrownGemImpact(obj, mon);
}

function heroThrownCreamPieHitMonster(pie, mon) {
    const messages = [];
    mon.msleeping = 0;
    if (monsterCanBeBlindedByCreamPie(mon)) {
        if (game.u?.blind) {
            messages.push('Splat!');
        } else {
            messages.push(heroThrownCreamPieHitMonsterMessage(pie, mon));
        }
        const blindTime = 21 + rn2(25);
        mon.mcansee = false;
        mon.mblinded = Math.min(127, (mon.mblinded || 0) + blindTime);
    } else {
        messages.push('Splat!');
    }
    setHeroObjectHitMonsterAngry(mon);
    markThrownBrokenObjectDebt(pie);
    return messages;
}

function heroThrownBlindingVenomHitMonster(venom, mon) {
    const messages = [];
    mon.msleeping = 0;
    if (monsterCanBeBlindedByBlindingVenom(mon)) {
        if (game.u?.blind) {
            messages.push('Splash!');
        } else {
            messages.push(`The venom blinds ${heroThrownVenomTargetName(mon)}${mon.mcansee === false ? ' further' : ''}!`);
        }
        const blindTime = 21 + rn2(25);
        mon.mcansee = false;
        mon.mblinded = Math.min(127, (mon.mblinded || 0) + blindTime);
    } else {
        messages.push('Splash!');
    }
    setHeroObjectHitMonsterAngry(mon);
    markThrownBrokenObjectDebt(venom);
    return messages;
}

function heroThrownAcidVenomHitMonster(venom, mon) {
    const messages = [];
    if (monsterResistsAcid(mon)) {
        messages.push(`Your venom hits ${heroThrownVenomTargetName(mon)} harmlessly.`);
    } else {
        messages.push(`Your venom burns ${heroThrownVenomTargetName(mon)}!`);
        mon.mhp = (mon.mhp || 1) - rnd(6) - rnd(6);
        if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages);
    }
    if (!mon.dead) {
        mon.msleeping = 0;
        setHeroObjectHitMonsterAngry(mon);
    }
    markThrownBrokenObjectDebt(venom);
    return messages;
}

function supportsHeroThrownPotionUpwardHit(potion) {
    if (!isPotionObject(potion)) return false;
    const kind = thrownPotionEffectKind(potion);
    return !!kind && kind !== 'potion' && !kind.endsWith(' potion');
}

function isCreamPieObject(obj) {
    if (!obj) return false;
    return obj.otyp === CREAM_PIE || objectKindKey(obj) === 'cream pie'
        || String(obj.kind || obj.actualKind || pickupObjectName(obj)).toLowerCase() === 'cream pie';
}

function isMelonObject(obj) {
    if (!obj) return false;
    return objectKindKey(obj) === 'melon'
        || String(obj.kind || obj.actualKind || pickupObjectName(obj)).toLowerCase() === 'melon';
}

const HERO_THROWN_HARMLESS_UPWARD_KINDS = new Set([
    'sling',
    'eucalyptus leaf',
    'kelp frond',
    'sprig of wolfsbane',
    'fortune cookie',
    'pancake',
]);

function isHeroThrownHarmlessUpwardObject(obj) {
    if (!obj) return false;
    const kind = objectKindKey(obj).replace(/^partly eaten\s+/, '');
    if (obj.otyp === SCROLL_CLASS || obj.cls === 'scroll' || obj.glyph === '?') return true;
    if (obj.otyp === EUCALYPTUS_LEAF || obj.otyp === KELP_FROND
        || obj.otyp === FORTUNE_COOKIE || obj.otyp === PANCAKE
        || HERO_THROWN_HARMLESS_UPWARD_KINDS.has(kind))
        return true;
    if (kind === 'rubber hose') return (obj.spe ?? 0) < 1;
    if (isBagOfTricksObject(obj)) return (obj.spe ?? 0) < 1;
    if (BAG_OBJECT_TYPES.has(obj.otyp) || kind === 'sack' || kind === 'oilskin sack' || kind === 'bag of holding')
        return globContents(obj).length === 0;
    const material = String(obj.material || obj.oc_material || '').toLowerCase().replace(/^hi_/, '');
    return material === 'cloth';
}

const CRACKABLE_ARMOR_THROW_WEIGHTS = new Map([
    ['crystal plate mail', 450],
    ['helm of brilliance', 50],
    ['crystal helmet', 50],
]);

function isHeroThrownCrackableArmorObject(obj) {
    if (!obj || !(obj.cls === 'armor' || obj.glyph === '[' || obj.otyp === ARMOR_CLASS)) return false;
    const profile = wishedDamageProfile(obj);
    if (profile.primaryWord === 'cracked') return true;
    const material = String(obj.material || obj.oc_material || '').toLowerCase().replace(/^hi_/, '');
    return material === 'glass';
}

function crackableArmorSimpleName(obj) {
    const kind = armorKind(obj);
    switch (armorSlot(obj)) {
    case 'body':
        if (/\bmail\b/.test(kind)) return 'mail';
        if (/\bjacket\b/.test(kind)) return 'jacket';
        return 'suit';
    case 'cloak': return 'cloak';
    case 'helm': return 'helm';
    case 'gloves': return 'gloves';
    case 'boots': return 'boots';
    case 'shield': return 'shield';
    case 'shirt': return 'shirt';
    default: return armorMessageName(obj);
    }
}

function crackableArmorBreaktest(obj, breakRoll = null) {
    const roll = Number.isInteger(breakRoll) ? breakRoll : rn2(100);
    return !(obj?.artifact || obj?.oartifact) && roll >= 90;
}

function erodeCrackableArmorImpact(obj, messages) {
    const simple = crackableArmorSimpleName(obj);
    if (obj.oerodeproof && obj.rknown) return { broke: true, destroyed: false };
    if (obj.oerodeproof || (obj.blessed && !rnl(4))) {
        messages.push(`Somehow, the ${simple} ${armorNameIsPlural(simple) ? 'are' : 'is'} not affected by the impact.`);
        if (obj.oerodeproof) obj.rknown = true;
        return { broke: true, destroyed: false };
    }
    const current = Math.min(3, obj.oeroded || 0);
    if (current < 3) {
        const adverb = current + 1 === 3 ? ' completely' : current ? ' further' : '';
        messages.push(`The ${simple} ${armorNameIsPlural(simple) ? 'crack' : 'cracks'}${adverb}!`);
        obj.oeroded = current + 1;
        return { broke: true, destroyed: false };
    }
    messages.push(`The ${simple} ${armorNameIsPlural(simple) ? 'shatter' : 'shatters'}!`);
    markObjectShopBillUsedUp(obj);
    return { broke: true, destroyed: true };
}

function crackableArmorImpact(obj, messages, options = {}) {
    if (!crackableArmorBreaktest(obj, options.breakRoll)) return { broke: false, destroyed: false };
    return erodeCrackableArmorImpact(obj, messages);
}

function crackableArmorThrowWeight(obj) {
    const explicit = Math.trunc(Number(obj?.owt || obj?.weight || 0));
    if (explicit > 0) return explicit;
    return CRACKABLE_ARMOR_THROW_WEIGHTS.get(armorKind(obj)) || 50;
}

function heroThrownCrackableArmorFallingDamage(obj, messages) {
    const helmet = wornTossUpHelmet();
    const damage = heroThrownGenericObjectFallingDamage(obj, helmet);
    pushHeroThrownHelmetMessage(messages, obj, helmet, damage);
    return damage;
}

function landCrackableArmorObjectWithShopHandling(obj, messages, { verboseFloor = false, x = null, y = null, breakRoll = null } = {}) {
    x = Number.isInteger(x) ? x : (game.u?.ux || obj.ox || 0);
    y = Number.isInteger(y) ? y : (game.u?.uy || obj.oy || 0);
    if (verboseFloor && !projectileLandingIsSoft(x, y))
        messages.push(`${floorObjectSubject({ ...obj, quan: 1 })} hits the floor.`);
    if (!projectileLandingIsSoft(x, y)) {
        const impact = crackableArmorImpact(obj, messages, { breakRoll });
        if (impact.destroyed) return null;
    }
    const landing = landProjectileObjectWithShopHandling(obj, x, y, { skipTopBreak: true });
    messages.push(...landing.messages);
    return landing.object;
}

function landHeroThrownCrackableArmorAt(obj, x, y, messages, { breakRoll = null } = {}) {
    return landCrackableArmorObjectWithShopHandling(obj, messages, { x, y, breakRoll });
}

function heroThrownCrackableArmorSelfHitMessages(obj, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const impact = crackableArmorImpact(obj, messages);
    if (impact.destroyed) return messages;
    const fallingDamage = impact.broke ? 0 : heroThrownCrackableArmorFallingDamage(obj, messages);
    landCrackableArmorObjectWithShopHandling(obj, messages, { verboseFloor: !impact.broke });
    applyHeroThrownCorpseFallingDamage(fallingDamage, messages);
    return messages;
}

function heroThrownCrackableArmorCeilingBreakMessages(obj, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} hits the ${ceilingName}.`];
    const impact = erodeCrackableArmorImpact(obj, messages);
    if (!impact.destroyed) landCrackableArmorObjectWithShopHandling(obj, messages);
    return messages;
}

function heroThrownCrackableArmorUpwardMessages(obj) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownCrackableArmorSelfHitMessages(obj, 'flies up into', ceilingName);
    if (hitsRoof) {
        if (crackableArmorBreaktest(obj)) return heroThrownCrackableArmorCeilingBreakMessages(obj, ceilingName);
        return heroThrownCrackableArmorSelfHitMessages(obj, 'hits', ceilingName);
    }
    return heroThrownCrackableArmorSelfHitMessages(obj, 'almost hits', ceilingName);
}

function heroCanBeBlindedByCreamPie() {
    if (!game.u) return false;
    if (game.u.blindfolded || game.u.Blindfolded) return false;
    const wornBlindfold = (game.inventory || []).some(item => {
        if (!(item.worn || item.owornmask || item.line?.includes('being worn'))) return false;
        const kind = objectKindKey(item);
        return kind === 'blindfold' || kind === 'towel';
    });
    return !wornBlindfold;
}

function heroWearsVisoredHelmet() {
    return (game.inventory || []).some(item => {
        if (!isWornInventoryItem(item) || armorSlot(item) !== 'helm') return false;
        return armorKind(item) === 'helm of telepathy'
            || String(item.appearance || '').toLowerCase() === 'visored helmet';
    });
}

function heroCanBeBlindedByBlindingVenom() {
    if (!heroCanBeBlindedByCreamPie()) return false;
    if ((game.u?.ucreamed || 0) > 0) return false;
    return !heroWearsVisoredHelmet();
}

function markThrownBrokenObjectDebt(obj) {
    const shopDebt = convertUnpaidObjectToShopDebt(obj, { silent: true, broken: true });
    if (!shopDebt.charged) obj.no_charge = true;
}

function heroAcidPotionSelfHitMessages(potion, messages) {
    if (heroHasAcidResistance()) return;
    messages.push(`This burns${potion?.blessed ? ' a little' : potion?.cursed ? ' a lot' : ''}!`);
    const damage = d(potion?.cursed ? 2 : 1, potion?.blessed ? 4 : 8);
    if (!game.u) return;
    game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
    if ((game.u.uhp || 0) <= 0) {
        game._death_cause = 'killed by a potion of acid';
        messages.push('You die...');
    }
}

function heroIsUnderwaterForThrow() {
    return !!(game.u?.uinwater || game.u?.underwater || game.u?.uunderwater);
}

function heroHasThrowCeiling() {
    const uz = game.u?.uz || null;
    return !(uz && In_endgame(uz) && !Is_earthlevel(uz));
}

function heroThrowCeilingName(x = game.u?.ux ?? 0, y = game.u?.uy ?? 0) {
    const uz = game.u?.uz || null;
    const loc = game.level?.at?.(x, y) || {};
    const room = levelRoomByRoomno(loc.roomno || 0);
    if (room?.rtype === VAULT) return "vault's ceiling";
    if (room?.rtype === TEMPLE) return "temple's ceiling";
    if (room?.rtype >= SHOPBASE) return "shop's ceiling";
    if (uz && Is_waterlevel(uz)) return 'water above';
    if (IS_AIR(loc.typ)) return 'sky';
    if (uz && Is_firelevel(uz)) return 'flames above';
    if (uz && In_quest(uz)) return 'expanse above';
    if (heroIsUnderwaterForThrow()) return "water's surface";
    if ((IS_ROOM(loc.typ) && !(uz && Is_earthlevel(uz)))
        || IS_WALL(loc.typ) || loc.typ === DOOR || loc.typ === SDOOR) {
        return 'ceiling';
    }
    return 'rock cavern';
}

function heroPolymorphPotionSelfHitMessages(messages) {
    messages.push(`You feel a little ${heroIsHallucinating() ? 'normal' : 'strange'}.`);
    if (heroHasUnchanging() || heroHasAntimagic()) return;
    const shock = polymorphSystemShock();
    if (shock) {
        messages.push(shock.message);
        if (shock.more) messages.more = true;
        return;
    }
    const formName = randomPolyselfMonsterName();
    const result = rn2(5) ? becomeMonster(formName) : becomeMonster('human');
    if (result?.message) messages.push(result.message);
    if (result?.more) messages.more = true;
    newsym(game.u?.ux || 0, game.u?.uy || 0);
}

function heroThrownPotionSelfHitMessages(potion, action, ceilingName = heroThrowCeilingName(), options = {}) {
    const messages = [];
    const kind = thrownPotionEffectKind(potion);
    messages.push(`${floorObjectSubject({ ...potion, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`);
    const bottle = chestShatterBottleName();
    messages.push(`The ${bottle} crashes on your head and breaks into shards.`);
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - rnd(2));
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a thrown potion';
            messages.push('You die...');
        }
    } else {
        rnd(2);
    }
    if (!isPotionOfOil(potion))
        messages.push(`The ${pickupObjectName({ ...potion, quan: 1 })} evaporates.`);
    if (kind === 'acid') heroAcidPotionSelfHitMessages(potion, messages);
    if (kind === 'polymorph') heroPolymorphPotionSelfHitMessages(messages);
    if (isLitOilPotionHit(potion, kind))
        explodeBurningOilPotion(potion, game.u?.ux ?? potion.ox ?? 0, game.u?.uy ?? potion.oy ?? 0, messages);
    potionBreathe(potion, messages, options);
    const shopDebt = convertUnpaidObjectToShopDebt(potion, { silent: true, broken: true });
    if (!shopDebt.charged) potion.no_charge = true;
    return messages;
}

function heroThrownPotionCeilingBreakMessages(potion, breakKind, ceilingName = heroThrowCeilingName(), options = {}) {
    const messages = [`${floorObjectSubject({ ...potion, quan: 1 })} hits the ${ceilingName}.`];
    projectileTopLevelBreakMessage(potion, breakKind, messages);
    if (isLitOilPotionHit(potion))
        explodeBurningOilPotion(potion, game.u?.ux ?? potion.ox ?? 0, game.u?.uy ?? potion.oy ?? 0, messages);
    else if (isPotionObject(potion))
        brokenPotionBreathe(potion, game.u?.ux ?? potion.ox ?? 0, game.u?.uy ?? potion.oy ?? 0, messages, options);
    markThrownBrokenObjectDebt(potion);
    return messages;
}

function heroThrownPotionUpwardMessages(potion, options = {}) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownPotionSelfHitMessages(potion, 'flies up into', ceilingName, options);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(potion);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(potion, breakKind, ceilingName, options);
        return heroThrownPotionSelfHitMessages(potion, 'hits', ceilingName, options);
    }
    return heroThrownPotionSelfHitMessages(potion, 'almost hits', ceilingName, options);
}

function heroThrownCreamPieSelfHitMessages(pie, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...pie, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(pie);
    if (breakKind) {
        const blindinc = heroCanBeBlindedByCreamPie() ? rnd(25) : 0;
        projectileTopLevelBreakMessage(pie, breakKind, messages);
        messages.push("You've got it all over your face!");
        if (blindinc && game.u) {
            game.u.ucreamed = (game.u.ucreamed || 0) + blindinc;
            game.u._blindTimeout = (game.u._blindTimeout || 0) + blindinc;
            game.u.blind = true;
            addHeroStatusSuffix('Blind');
            for (const mon of game.level?.monsters || []) newsym(mon.mx, mon.my);
        }
        markThrownBrokenObjectDebt(pie);
        return messages;
    }

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - rnd(1));
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    } else {
        rnd(1);
    }
    const landing = landProjectileObjectWithShopHandling(pie, game.u?.ux || pie.ox || 0, game.u?.uy || pie.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function heroThrownVenomSelfHitMessages(venom, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...venom, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(venom);
    if (breakKind) {
        const wasBlind = !!game.u?.blind;
        const blindinc = isBlindingVenomObject(venom) && heroCanBeBlindedByBlindingVenom() ? rnd(25) : 0;
        projectileTopLevelBreakMessage(venom, breakKind, messages);
        if (isBlindingVenomObject(venom)) {
            messages.push("You've got it all over your face!");
            if (blindinc && game.u) {
                if (!wasBlind) messages.push('It blinds you!');
                game.u.ucreamed = (game.u.ucreamed || 0) + blindinc;
                game.u._blindTimeout = (game.u._blindTimeout || 0) + blindinc;
                game.u.blind = true;
                addHeroStatusSuffix('Blind');
                for (const mon of game.level?.monsters || []) newsym(mon.mx, mon.my);
            }
        }
        markThrownBrokenObjectDebt(venom);
        return messages;
    }

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(venom, game.u?.ux || venom.ox || 0, game.u?.uy || venom.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function heroThrownVenomUpwardMessages(venom) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownVenomSelfHitMessages(venom, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(venom);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(venom, breakKind, ceilingName);
        return heroThrownVenomSelfHitMessages(venom, 'hits', ceilingName);
    }
    return heroThrownVenomSelfHitMessages(venom, 'almost hits', ceilingName);
}

function heroThrownHarmlessObjectSelfHitMessages(obj, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(obj);
    if (breakKind) {
        projectileTopLevelBreakMessage(obj, breakKind, messages);
        markThrownBrokenObjectDebt(obj);
        return messages;
    }
    messages.push("It doesn't hurt.");
    const landing = landProjectileObjectWithShopHandling(obj, game.u?.ux || obj.ox || 0, game.u?.uy || obj.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function heroThrownHarmlessObjectUpwardMessages(obj) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownHarmlessObjectSelfHitMessages(obj, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(obj);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(obj, breakKind, ceilingName);
        return heroThrownHarmlessObjectSelfHitMessages(obj, 'hits', ceilingName);
    }
    return heroThrownHarmlessObjectSelfHitMessages(obj, 'almost hits', ceilingName);
}

function heroThrownMelonSelfHitMessages(melon, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...melon, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(melon);
    if (breakKind) {
        projectileTopLevelBreakMessage(melon, breakKind, messages);
        markThrownBrokenObjectDebt(melon);
        return messages;
    }

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(melon, game.u?.ux || melon.ox || 0, game.u?.uy || melon.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function heroThrownMelonUpwardMessages(melon) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownMelonSelfHitMessages(melon, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(melon);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(melon, breakKind, ceilingName);
        return heroThrownMelonSelfHitMessages(melon, 'hits', ceilingName);
    }
    return heroThrownMelonSelfHitMessages(melon, 'almost hits', ceilingName);
}

function heroThrownEggSelfHitMessages(egg, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...egg, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(egg);
    if (breakKind) {
        projectileTopLevelBreakMessage(egg, breakKind, messages);
        messages.push("You've got it all over your face!");
        markThrownBrokenObjectDebt(egg);
        return messages;
    }

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(egg, game.u?.ux || egg.ox || 0, game.u?.uy || egg.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function wornTossUpHelmet() {
    return wornEarthHelmet();
}

function simpleTossUpHelmetName(helmet) {
    return hardEarthHelmet(helmet) ? 'helm' : 'hat';
}

function heroPetrifyingEggPolyselfRescueMessage() {
    if (game.u?.stoneResistance || heroPolyselfResistsStoning()) return '';
    return maybeTurnPolyselfIntoStoneGolem();
}

function finishHeroPetrifiedByTossUpObject(messages, obj = null) {
    if (obj) {
        prepareProjectileFloorObject(obj, game.u?.ux || obj.ox || 0, game.u?.uy || obj.oy || 0);
        placeUnstackedFloorObject(obj);
        newsym(obj.ox, obj.oy);
    }
    messages.push('You turn to stone.');
    if (game.u) game.u.uhp = 0;
    game._death_cause = 'petrified by elementary physics';
    game._death_bones_body = 'statue';
    if (consumeLifeSavingAmulet({ clearStoning: true })) {
        messages.push('You die...  But wait...  Your medallion begins to glow!');
        messages.lifeSaving = true;
    } else {
        messages.fatal = true;
    }
    messages.more = true;
    return messages;
}

function breakHeroThrownTouchPetrifyingEgg(egg, messages, { selfHit = false } = {}) {
    projectileTopLevelBreakMessage(egg, 'splat', messages);
    markThrownBrokenObjectDebt(egg);
    if (!selfHit) return messages;

    const rescue = heroPetrifyingEggPolyselfRescueMessage();
    if (rescue) {
        messages.push(rescue);
    } else if (!game.u?.stoneResistance && !heroPolyselfResistsStoning()) {
        const helmet = wornTossUpHelmet();
        if (helmet) messages.push(`Your ${simpleTossUpHelmetName(helmet)} fails to protect you.`);
        return finishHeroPetrifiedByTossUpObject(messages);
    }

    messages.push("You've got it all over your face!");
    return messages;
}

function heroThrownTouchPetrifyingEggSelfHitMessages(egg, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...egg, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(egg);
    if (breakKind) return breakHeroThrownTouchPetrifyingEgg(egg, messages, { selfHit: true });

    const rescue = heroPetrifyingEggPolyselfRescueMessage();
    if (rescue) messages.push(rescue);
    else if (!wornTossUpHelmet() && !game.u?.stoneResistance && !heroPolyselfResistsStoning())
        return finishHeroPetrifiedByTossUpObject(messages, egg);

    const helmet = wornTossUpHelmet();
    if (helmet && hardEarthHelmet(helmet))
        messages.push('Fortunately, you are wearing a hard helmet.');
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(egg, game.u?.ux || egg.ox || 0, game.u?.uy || egg.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function petrifyingCorpseMonsterName(obj) {
    return corpseMonsterName(obj) || 'cockatrice';
}

function petrifyingCorpseTheName(obj) {
    return `the ${petrifyingCorpseMonsterName(obj)} corpse`;
}

function petrifyingCorpseArticleName(obj) {
    return `${/^[aeiou]/i.test(petrifyingCorpseMonsterName(obj)) ? 'an' : 'a'} ${petrifyingCorpseMonsterName(obj)} corpse`;
}

function finishHeroPetrifiedByBareHandCorpseThrow(messages, corpse) {
    messages.push('You turn to stone...');
    if (game.u) game.u.uhp = 0;
    game._death_cause = `petrified by throwing ${petrifyingCorpseArticleName(corpse)} bare-handed`;
    game._death_bones_body = 'statue';
    if (consumeLifeSavingAmulet({ clearStoning: true })) {
        messages.push('You die...  But wait...  Your medallion begins to glow!');
        messages.lifeSaving = true;
    } else {
        messages.fatal = true;
    }
    messages.more = true;
    return messages;
}

function heroThrowPetrifyingCorpseBareHandPrecheck(corpse) {
    if (!isPetrifyingCorpseObject(corpse) || wornGlovesItem()
        || game.u?.stoneResistance || heroPolyselfResistsStoning())
        return { ok: true, messages: [] };

    const messages = [`You throw ${petrifyingCorpseTheName(corpse)} with your bare hands.`];
    const rescue = maybeTurnPolyselfIntoStoneGolem();
    if (rescue) {
        messages.push(rescue);
        return { ok: true, messages };
    }
    return { ok: false, messages: finishHeroPetrifiedByBareHandCorpseThrow(messages, corpse) };
}

function heroThrownCorpseWeight(corpse) {
    const explicit = Math.trunc(Number(corpse?.owt || corpse?.weight || 0));
    if (explicit > 0) return explicit;
    const corpseName = petrifyingCorpseMonsterName(corpse);
    return CORPSE_WEIGHTS.get(corpseName) || Math.max(1, Math.trunc(Number(corpse?.corpsenm?.cwt || 20)));
}

function heroHasHalfPhysicalDamage() {
    return !!(game.u?.halfPhysicalDamage
        || game.u?.half_physical_damage
        || game.u?.halfPhysical
        || game.u?.halfPhysicalDamageTimeout
        || game.u?.extrinsics?.halfPhysicalDamage
        || game.u?.intrinsics?.halfPhysicalDamage);
}

function maybeHalfPhysicalDamage(damage) {
    return heroHasHalfPhysicalDamage() ? Math.trunc((damage + 1) / 2) : damage;
}

function heroTossUpActiveHp() {
    if (!game.u) return 0;
    if (game.u._polyself_form && game.u.mh != null && game.u.mhmax != null) return game.u.mh || 0;
    return game.u.uhp || 0;
}

function heroThrownCorpseFallingDamage(corpse, helmet = null) {
    const weightDamage = Math.max(1, Math.ceil(heroThrownCorpseWeight(corpse) / WT_TO_DMG));
    let damage = weightDamage <= 1 ? 1 : rnd(weightDamage);
    if (damage > 6) damage = 6;
    if (helmet && hardEarthHelmet(helmet) && damage > 1) damage = 1;
    if (damage > 0) damage += Math.trunc(Number(game.u?.udaminc || 0));
    if (damage < 0) damage = 0;
    return maybeHalfPhysicalDamage(damage);
}

function applyHeroThrownCorpseFallingDamage(damage, messages) {
    if (!game.u || damage <= 0) return;
    if (game.u._polyself_form) {
        if (game.u.mh != null && game.u.mhmax != null) {
            game.u.mh = Math.max(0, (game.u.mh || 0) - damage);
            if ((game.u.mh || 0) > 0) return;
        } else {
            game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            if ((game.u.uhp || 0) > 0) return;
        }
        if (heroHasUnchanging()) {
            game._death_cause = 'killed while stuck in creature form';
            messages.push('You die...');
            messages.fatal = true;
            messages.more = true;
            return;
        }
        const result = rehumanizeAfterPolyselfDeath();
        messages.push(...result.messages);
        if (result.died) {
            if (consumeLifeSavingAmulet({ clearStoning: !!result.clearStoningOnLifeSaving })) {
                messages.push('You die...  But wait...  Your medallion begins to glow!');
                messages.lifeSaving = true;
            } else {
                messages.fatal = true;
            }
            messages.more = true;
        }
        return;
    }
    game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
    if ((game.u.uhp || 0) <= 0) {
        game._death_cause = 'killed by a falling object';
        messages.push('You die...');
        messages.fatal = true;
        messages.more = true;
    }
}

function heroThrownTouchPetrifyingCorpseSelfHitMessages(corpse, action, ceilingName = heroThrowCeilingName(), prefixMessages = []) {
    const messages = [...prefixMessages,
        `${floorObjectSubject({ ...corpse, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    projectileTopLevelBreakKind(corpse); // C breaktest consumes obj_resists() even though corpses do not break.

    const helmet = wornTossUpHelmet();
    const damage = heroThrownCorpseFallingDamage(corpse, helmet);
    if (!helmet && !game.u?.stoneResistance && !heroPolyselfResistsStoning()) {
        const rescue = maybeTurnPolyselfIntoStoneGolem();
        if (rescue) messages.push(rescue);
        else return finishHeroPetrifiedByTossUpObject(messages, corpse);
    }
    if (helmet && hardEarthHelmet(helmet) && damage < heroTossUpActiveHp())
        messages.push('Fortunately, you are wearing a hard helmet.');

    const landing = landProjectileObjectWithShopHandling(corpse, game.u?.ux || corpse.ox || 0, game.u?.uy || corpse.oy || 0, {});
    messages.push(...landing.messages);
    applyHeroThrownCorpseFallingDamage(damage, messages);
    return messages;
}

function heroThrownTouchPetrifyingCorpseUpwardMessages(corpse, prefixMessages = []) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownTouchPetrifyingCorpseSelfHitMessages(corpse, 'flies up into', ceilingName, prefixMessages);
    if (hitsRoof) {
        projectileTopLevelBreakKind(corpse); // C tests for ceiling breakage before choosing the "hits" wording.
        return heroThrownTouchPetrifyingCorpseSelfHitMessages(corpse, 'hits', ceilingName, prefixMessages);
    }
    return heroThrownTouchPetrifyingCorpseSelfHitMessages(corpse, 'almost hits', ceilingName, prefixMessages);
}

function heroThrownOrdinaryCorpseSelfHitMessages(corpse, action, ceilingName = heroThrowCeilingName()) {
    const messages = [
        `${floorObjectSubject({ ...corpse, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`,
    ];
    projectileTopLevelBreakKind(corpse); // C breaktest consumes obj_resists() even though corpses do not break.

    const helmet = wornTossUpHelmet();
    const damage = heroThrownCorpseFallingDamage(corpse, helmet);
    pushHeroThrownHelmetMessage(messages, corpse, helmet, damage);

    const x = game.u?.ux || corpse.ox || 0;
    const y = game.u?.uy || corpse.oy || 0;
    const floorMessage = heroThrownGenericObjectFloorMessage(corpse, x, y);
    if (floorMessage) messages.push(floorMessage);
    const landing = landProjectileObjectWithShopHandling(corpse, x, y, {});
    messages.push(...landing.messages);
    applyHeroThrownCorpseFallingDamage(damage, messages);
    return messages;
}

function heroThrownOrdinaryCorpseUpwardMessages(corpse) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownOrdinaryCorpseSelfHitMessages(corpse, 'flies up into', ceilingName);
    if (hitsRoof) {
        projectileTopLevelBreakKind(corpse); // C tests for ceiling breakage before choosing the "hits" wording.
        return heroThrownOrdinaryCorpseSelfHitMessages(corpse, 'hits', ceilingName);
    }
    return heroThrownOrdinaryCorpseSelfHitMessages(corpse, 'almost hits', ceilingName);
}

const HERO_TOSS_UP_WEAPON_SMALL_DAMAGE = new Map([
    ['dagger', 4],
    ['elven dagger', 5],
    ['orcish dagger', 3],
    ['silver dagger', 4],
    ['athame', 4],
    ['scalpel', 3],
    ['knife', 3],
    ['stiletto', 3],
    ['worm tooth', 2],
    ['crysknife', 10],
    ['spear', 6],
    ['elven spear', 7],
    ['orcish spear', 5],
    ['dwarvish spear', 8],
    ['silver spear', 6],
    ['javelin', 6],
    ['trident', { die: 6, add: 1 }],
    ['axe', 6],
    ['battle-axe', { die: 8, bonusDie: 4 }],
    ['pick-axe', 6],
    ['grappling hook', 2],
    ['short sword', 6],
    ['elven short sword', 8],
    ['orcish short sword', 5],
    ['dwarvish short sword', 7],
    ['scimitar', 8],
    ['silver saber', 8],
    ['broadsword', { die: 4, bonusDie: 4 }],
    ['elven broadsword', { die: 6, bonusDie: 4 }],
    ['long sword', 8],
    ['two-handed sword', 12],
    ['katana', 10],
    ['partisan', 6],
    ['ranseur', { die: 4, bonusDie: 4 }],
    ['spetum', { die: 6, add: 1 }],
    ['glaive', 6],
    ['halberd', 10],
    ['bardiche', { die: 4, bonusDie: 4 }],
    ['voulge', { die: 4, bonusDie: 4 }],
    ['fauchard', 6],
    ['guisarme', { die: 4, bonusDie: 4 }],
    ['bill-guisarme', { die: 4, bonusDie: 4 }],
    ['lucern hammer', { die: 4, bonusDie: 4 }],
    ['bec de corbin', 8],
    ['mace', { die: 6, add: 1 }],
    ['silver mace', { die: 6, add: 1 }],
    ['morning star', { die: 4, bonusDie: 4 }],
    ['war hammer', { die: 4, add: 1 }],
    ['club', 6],
    ['rubber hose', 4],
    ['quarterstaff', 6],
    ['aklys', 6],
    ['flail', { die: 6, add: 1 }],
    ['lance', 6],
    ['bullwhip', 2],
]);

const HERO_TOSS_UP_WEAPON_LARGE_DAMAGE = new Map([
    ['dagger', 3],
    ['elven dagger', 3],
    ['orcish dagger', 3],
    ['silver dagger', 3],
    ['athame', 3],
    ['scalpel', 3],
    ['knife', 2],
    ['stiletto', 2],
    ['worm tooth', 2],
    ['crysknife', 10],
    ['spear', 8],
    ['elven spear', 8],
    ['orcish spear', 8],
    ['dwarvish spear', 8],
    ['silver spear', 8],
    ['javelin', 6],
    ['trident', { die: 4, bonusDice: [2, 4] }],
    ['axe', 4],
    ['battle-axe', { die: 6, bonusDice: [2, 4] }],
    ['pick-axe', 3],
    ['grappling hook', 6],
    ['short sword', 8],
    ['elven short sword', 8],
    ['orcish short sword', 8],
    ['dwarvish short sword', 8],
    ['scimitar', 8],
    ['silver saber', 8],
    ['broadsword', { die: 6, add: 1 }],
    ['elven broadsword', { die: 6, add: 1 }],
    ['long sword', 12],
    ['two-handed sword', { die: 6, bonusDice: [2, 6] }],
    ['katana', 12],
    ['partisan', { die: 6, add: 1 }],
    ['ranseur', { die: 4, bonusDie: 4 }],
    ['spetum', { die: 6, bonusDie: 6 }],
    ['glaive', 10],
    ['halberd', { die: 6, bonusDie: 6 }],
    ['bardiche', { die: 4, bonusDice: [2, 4] }],
    ['voulge', { die: 4, bonusDie: 4 }],
    ['fauchard', 8],
    ['guisarme', 8],
    ['bill-guisarme', 10],
    ['lucern hammer', 6],
    ['bec de corbin', 6],
    ['mace', 6],
    ['silver mace', 6],
    ['morning star', { die: 6, add: 1 }],
    ['war hammer', 4],
    ['club', 3],
    ['rubber hose', 3],
    ['quarterstaff', 6],
    ['aklys', 3],
    ['flail', { die: 4, bonusDie: 4 }],
    ['lance', 8],
    ['bullwhip', 1],
]);

function tossUpWeaponObjectKey(obj) {
    if (obj?.otyp === DAGGER) return 'dagger';
    if (obj?.otyp === ORCISH_DAGGER) return 'orcish dagger';
    if (obj?.otyp === KNIFE) return 'knife';
    if (obj?.otyp === STILETTO) return 'stiletto';
    if (obj?.otyp === PICK_AXE) return 'pick-axe';
    if (obj?.otyp === GRAPPLING_HOOK) return 'grappling hook';
    if (obj?.otyp === SHORT_SWORD) return 'short sword';
    if (obj?.otyp === ELVEN_SHORT_SWORD) return 'elven short sword';
    if (obj?.otyp === ORCISH_SHORT_SWORD) return 'orcish short sword';
    if (obj?.otyp === DWARVISH_SHORT_SWORD) return 'dwarvish short sword';
    if (obj?.otyp === SCIMITAR) return 'scimitar';
    if (obj?.otyp === SILVER_SABER) return 'silver saber';
    if (obj?.otyp === BROADSWORD) return 'broadsword';
    if (obj?.otyp === ELVEN_BROADSWORD) return 'elven broadsword';
    if (obj?.otyp === LONG_SWORD) return 'long sword';
    if (obj?.otyp === TWO_HANDED_SWORD) return 'two-handed sword';
    if (obj?.otyp === KATANA) return 'katana';
    if (obj?.otyp === GLAIVE) return 'glaive';
    if (obj?.otyp === FLAIL) return 'flail';
    if (obj?.otyp === BULLWHIP) return 'bullwhip';
    return objectKindKey(obj);
}

function tossUpWeaponDamageDie(spec) {
    return typeof spec === 'number' ? spec : spec?.die;
}

function tossUpWeaponDamageSpecForTarget(obj) {
    const key = tossUpWeaponObjectKey(obj);
    return heroTossUpTargetIsBig() ? HERO_TOSS_UP_WEAPON_LARGE_DAMAGE.get(key)
        : HERO_TOSS_UP_WEAPON_SMALL_DAMAGE.get(key);
}

function rollTossUpWeaponDamageSpec(spec) {
    const die = tossUpWeaponDamageDie(spec);
    if (!die) return 0;
    let damage = rnd(die);
    if (typeof spec !== 'object') return damage;
    damage += Math.trunc(Number(spec.add || 0));
    if (spec.bonusDie) damage += rnd(spec.bonusDie);
    if (Array.isArray(spec.bonusDice) && spec.bonusDice.length === 2)
        damage += d(spec.bonusDice[0], spec.bonusDice[1]);
    return damage;
}

function isSupportedTossUpWeaponObject(obj) {
    if (!obj) return false;
    const key = tossUpWeaponObjectKey(obj);
    if (!HERO_TOSS_UP_WEAPON_SMALL_DAMAGE.has(key)) return false;
    const spe = Math.trunc(Number(obj.spe ?? 0)) || 0;
    if (key === 'rubber hose' && spe < 1) return false;
    return true;
}

function heroThrownGenericWeaponDamage(obj) {
    const spec = tossUpWeaponDamageSpecForTarget(obj);
    if (!tossUpWeaponDamageDie(spec) || !isSupportedTossUpWeaponObject(obj)) return null;
    let damage = rollTossUpWeaponDamageSpec(spec);
    damage += Math.trunc(Number(obj.spe || 0));
    if (damage < 0) damage = 0;
    if (heroTossUpTargetIsShade() && !heroTossUpObjectIsSilver(obj)) damage = 0;
    if (obj?.blessed && heroTossUpTargetHatesBlessings()) damage += rnd(4);
    if (heroTossUpObjectIsSilver(obj) && heroTossUpTargetHatesSilver()) damage += rnd(20);
    if (damage > 0) {
        damage -= Math.max(0, Math.trunc(Number(obj.oeroded || 0)), Math.trunc(Number(obj.oeroded2 || 0)));
        if (damage < 1) damage = 1;
    }
    return damage;
}

function isTinOpenerTossObject(obj) {
    return !!obj && (obj.otyp === TIN_OPENER || objectKindKey(obj) === 'tin opener');
}

function isHeroThrownGenericWeightContainerObject(obj) {
    if (!obj) return false;
    const kind = objectKindKey(obj);
    return isBagOfTricksObject(obj) || BAG_OBJECT_TYPES.has(obj.otyp)
        || kind === 'sack' || kind === 'oilskin sack' || kind === 'bag of holding';
}

function isHeroThrownGenericStoneMissileUpwardObject(obj) {
    return heroThrownStoneMissileObject(obj) && !(isLoadstoneObject(obj) && obj.cursed);
}

function isHeroThrownGenericDamagingUpwardObject(obj) {
    if (!obj || isHeroThrownHarmlessUpwardObject(obj)) return false;
    return isTinOpenerTossObject(obj) || isHeroThrownGenericWeightContainerObject(obj)
        || isSupportedTossUpWeaponObject(obj) || isHeroThrownGenericStoneMissileUpwardObject(obj);
}

function heroTossUpTargetForm() {
    return polyselfForm() || game.u?.youmonst?.data || game.u?.data || {};
}

function heroTossUpTargetName() {
    return String(heroTossUpTargetForm().name || '').toLowerCase();
}

function heroTossUpObjectIsSilver(obj) {
    return objectMaterialForMetallivore(obj) === 'silver';
}

function heroTossUpTargetHatesBlessings() {
    const form = heroTossUpTargetForm();
    const rawMlet = String(form.mlet || form.glyph || '');
    const mlet = rawMlet.toLowerCase();
    const name = heroTossUpTargetName();
    const undead = form.undead || form.vampshifter
        || rawMlet === 'L' || rawMlet === 'M' || rawMlet === 'V' || rawMlet === 'W'
        || rawMlet === 'Z' || rawMlet === "'" || mlet === 'ghost'
        || /\b(?:ghost|shade|lich|mummy|zombie|vampire|wraith|nazgul|skeleton|ghoul)\b/.test(name);
    const demon = form.demon || rawMlet === '&' || mlet === 'demon'
        || /\b(?:demon|devil|manes)\b/.test(name);
    return !!(undead || demon);
}

function heroTossUpTargetHatesSilver() {
    const form = heroTossUpTargetForm();
    const rawMlet = String(form.mlet || form.glyph || '');
    const mlet = rawMlet.toLowerCase();
    const name = heroTossUpTargetName();
    const lycanthrope = game.u?.ulycn != null && game.u.ulycn !== -1 && game.u.ulycn !== false;
    return !!(lycanthrope || game.u?.lycanthrope
        || form.vampshifter || form.were || form.isWere || form.wereHuman || form.wereBeast
        || /^were/.test(name)
        || rawMlet === 'V' || mlet === 'vampire' || /\bvampire\b/.test(name) || name === 'vlad the impaler'
        || form.demon || rawMlet === '&' || mlet === 'demon' || /\b(?:demon|devil|manes)\b/.test(name)
        || name === 'shade'
        || ((rawMlet === 'i' || mlet === 'imp') && name !== 'tengu'));
}

function heroTossUpTargetIsShade() {
    return heroTossUpTargetName() === 'shade';
}

function heroTossUpTargetPassesRocks() {
    const form = heroTossUpTargetForm();
    return heroThrownTargetPassesRocks({
        name: form?.name,
        data: form,
        passesRocks: form?.passesRocks,
        passWalls: form?.passWalls,
        passesWalls: form?.passesWalls,
        passes_walls: form?.passes_walls,
        wallwalk: form?.wallwalk,
        unsolid: form?.unsolid,
        noncorporeal: form?.noncorporeal,
    });
}

function heroTossUpStoneMissileHarmlessRockPasser(obj) {
    return heroThrownStoneMissileObject(obj) && heroTossUpTargetPassesRocks();
}

function heroTossUpTargetIsBig() {
    const form = heroTossUpTargetForm();
    if (!form || !Object.keys(form).length) return false;
    if (form.big || form.bigmonst) return true;
    return heroProjectileMonsterSizeValue({
        data: form,
        msize: form.msize,
        size: form.size,
    }) >= 3;
}

function heroThrownGenericObjectLessDamage(obj, helmet) {
    return !!(helmet && hardEarthHelmet(helmet)
        && (!heroTossUpObjectIsSilver(obj) || !heroTossUpTargetHatesSilver()));
}

function pushHeroThrownHelmetMessage(messages, obj, helmet, damage) {
    if (!helmet) return;
    if (heroThrownGenericObjectLessDamage(obj, helmet) && damage < heroTossUpActiveHp())
        messages.push('Fortunately, you are wearing a hard helmet.');
    else
        messages.push(`Your ${simpleTossUpHelmetName(helmet)} does not protect you.`);
}

function heroThrownGenericObjectFallingDamage(obj, helmet = null, { harmless = false } = {}) {
    let damage = heroThrownGenericWeaponDamage(obj) ?? 0;
    if (objectHasArtifactIdentity(obj) && !harmless) rn1(18, 2); // C artifact_hit() gets a fake 2..19 die roll.
    if (!damage) {
        const weightDamage = Math.max(1, Math.ceil(globObjectWeight({ ...obj, quan: 1 }) / WT_TO_DMG));
        damage = weightDamage <= 1 ? 1 : rnd(weightDamage);
        if (damage > 6) damage = 6;
        if (heroTossUpTargetIsShade() && !heroTossUpObjectIsSilver(obj)) damage = 0;
        if (obj?.blessed && heroTossUpTargetHatesBlessings()) damage += rnd(4);
        if (heroTossUpObjectIsSilver(obj) && heroTossUpTargetHatesSilver()) damage += rnd(20);
    }
    if (heroThrownGenericObjectLessDamage(obj, helmet) && damage > 1) damage = 1;
    if (damage > 0) damage += Math.trunc(Number(game.u?.udaminc || 0));
    if (damage < 0) damage = 0;
    return maybeHalfPhysicalDamage(damage);
}

function heroThrownGenericObjectFloorMessage(obj, x, y) {
    if (projectileLandingIsSoft(x, y)) return '';
    return `${floorObjectSubject({ ...obj, quan: 1 })} hits the floor.`;
}

function heroThrownGenericObjectSelfHitMessages(obj, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(obj);
    if (breakKind) {
        projectileTopLevelBreakMessage(obj, breakKind, messages);
        markThrownBrokenObjectDebt(obj);
        return messages;
    }

    const helmet = wornTossUpHelmet();
    const harmlessRockPasser = heroTossUpStoneMissileHarmlessRockPasser(obj);
    const damage = heroThrownGenericObjectFallingDamage(obj, helmet, { harmless: harmlessRockPasser });
    if (helmet && harmlessRockPasser)
        messages.push(`Unfortunately, you are wearing ${objectArticleName(simpleTossUpHelmetName(helmet))}.`);
    else
        pushHeroThrownHelmetMessage(messages, obj, helmet, damage);
    if (heroTossUpObjectIsSilver(obj) && heroTossUpTargetHatesSilver())
        messages.push('The silver sears you!');
    if (harmlessRockPasser && !helmet)
        messages.push(`${floorObjectTheSubject({ ...obj, quan: 1 })} hits you but doesn't hurt.`);

    const x = game.u?.ux || obj.ox || 0;
    const y = game.u?.uy || obj.oy || 0;
    const floorMessage = heroThrownGenericObjectFloorMessage(obj, x, y);
    if (floorMessage) messages.push(floorMessage);
    const landing = landProjectileObjectWithShopHandling(obj, x, y, {});
    messages.push(...landing.messages);
    if (!(harmlessRockPasser && !helmet))
        applyHeroThrownCorpseFallingDamage(damage, messages);
    return messages;
}

function heroThrownGenericObjectUpwardMessages(obj) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownGenericObjectSelfHitMessages(obj, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(obj);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(obj, breakKind, ceilingName);
        return heroThrownGenericObjectSelfHitMessages(obj, 'hits', ceilingName);
    }
    return heroThrownGenericObjectSelfHitMessages(obj, 'almost hits', ceilingName);
}

function heroThrownTouchPetrifyingEggCeilingBreakMessages(egg, ceilingName = heroThrowCeilingName()) {
    return breakHeroThrownTouchPetrifyingEgg(egg, [`${floorObjectSubject({ ...egg, quan: 1 })} hits the ${ceilingName}.`]);
}

function heroThrownTouchPetrifyingEggUpwardMessages(egg) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownTouchPetrifyingEggSelfHitMessages(egg, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(egg);
        if (breakKind) return heroThrownTouchPetrifyingEggCeilingBreakMessages(egg, ceilingName);
        return heroThrownTouchPetrifyingEggSelfHitMessages(egg, 'hits', ceilingName);
    }
    return heroThrownTouchPetrifyingEggSelfHitMessages(egg, 'almost hits', ceilingName);
}

function breakHeroThrownPyroliskEgg(egg, messages, { selfHit = false } = {}) {
    projectileTopLevelBreakMessage(egg, 'splat', messages);
    markThrownBrokenObjectDebt(egg);
    const explosion = resolvePyroliskEggExplosion(game.u?.ux || egg.ox || 0, game.u?.uy || egg.oy || 0, d(3, 6));
    appendFireExplosionMessages(messages, explosion);
    if (selfHit && (game.u?.uhp || 0) > 0)
        messages.push("You've got it all over your face!");
    return messages;
}

function heroThrownPyroliskEggSelfHitMessages(egg, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...egg, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(egg);
    if (breakKind) return breakHeroThrownPyroliskEgg(egg, messages, { selfHit: true });

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(egg, game.u?.ux || egg.ox || 0, game.u?.uy || egg.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

function heroThrownPyroliskEggCeilingBreakMessages(egg, ceilingName = heroThrowCeilingName()) {
    return breakHeroThrownPyroliskEgg(egg, [`${floorObjectSubject({ ...egg, quan: 1 })} hits the ${ceilingName}.`]);
}

function heroThrownPyroliskEggUpwardMessages(egg) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownPyroliskEggSelfHitMessages(egg, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(egg);
        if (breakKind) return heroThrownPyroliskEggCeilingBreakMessages(egg, ceilingName);
        return heroThrownPyroliskEggSelfHitMessages(egg, 'hits', ceilingName);
    }
    return heroThrownPyroliskEggSelfHitMessages(egg, 'almost hits', ceilingName);
}

function heroThrownEggUpwardMessages(egg) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownEggSelfHitMessages(egg, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(egg);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(egg, breakKind, ceilingName);
        return heroThrownEggSelfHitMessages(egg, 'hits', ceilingName);
    }
    return heroThrownEggSelfHitMessages(egg, 'almost hits', ceilingName);
}

function monsterTouchPetrifies(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(data.touchPetrifies || mon?.touchPetrifies || name === 'cockatrice' || name === 'chickatrice');
}

function thrownEggHitArticle(egg) {
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    if (egg?.known && egg?.corpsenm?.name) return `the ${egg.corpsenm.name}`;
    return count > 1 ? 'some' : 'an';
}

function thrownPetrifyingEggHitArticle(egg) {
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    if (egg?.known && egg?.corpsenm?.name) return `the ${egg.corpsenm.name}`;
    return count > 1 ? 'some petrifying' : 'a petrifying';
}

function thrownEggTargetName(mon) {
    return mon?.data?.name || mon?.name || 'creature';
}

function thrownEggTargetTheName(mon) {
    const name = thrownEggTargetName(mon);
    return /^the\b/i.test(name) ? name : `the ${name}`;
}

function placeThrownEggPetrifiedRock(egg, mon) {
    killEggHatchTimer(egg);
    Object.assign(egg, {
        otyp: ROCK,
        cls: 'gem',
        glyph: '*',
        kind: 'rock',
        actualKind: 'rock',
        singular: 'rock',
        plural: 'rocks',
        quan: 1,
        spe: 0,
        known: false,
        dknown: false,
        bknown: false,
        ox: mon.mx,
        oy: mon.my,
        color: CLR_GRAY,
    });
    delete egg.corpsenm;
    delete egg.eggKnown;
    delete egg.line;
    Object.assign(egg, object_display(egg));
    placeStackableFloorObject(egg);
    newsym(mon.mx, mon.my);
}

function transformMeleeEggStackToRocks(egg) {
    killEggHatchTimer(egg);
    Object.assign(egg, {
        otyp: ROCK,
        cls: 'gem',
        glyph: '*',
        kind: 'rock',
        actualKind: 'rock',
        singular: 'rock',
        plural: 'rocks',
        spe: 0,
        known: false,
        dknown: false,
        bknown: false,
        color: CLR_GRAY,
    });
    delete egg.corpsenm;
    delete egg.eggKnown;
    Object.assign(egg, object_display(egg));
}

function applyThrownEggNominalDamage(mon, messages) {
    if (!mon || mon.dead) return;
    mon.msleeping = 0;
    if (!mon.pet) mon.mpeaceful = false;
    mon.mhp = (mon.mhp || 1) - 1;
    if ((mon.mhp || 0) <= 0) killMonsterFromPotionHit(mon, messages);
}

function applyThrownEggLuckPenalty(egg) {
    if (!egg?.spe || !egg?.corpsenm?.name || !game.u) return;
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    game.u.uluck = (game.u.uluck || 0) - Math.min(count, 5);
}

function monsterResistsStoning(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(mon?.stoneResistance || mon?.resistsStoning || mon?.resistsStone
        || data.stoneResistance || data.resistsStoning || data.resistsStone
        || name === 'stone golem');
}

function monsterPolyWhenStoned(mon) {
    const data = mon?.data || {};
    return !monsterResistsStoning(mon)
        && data.name !== 'stone golem'
        && (data.mlet === '\'' || data.glyph === '\'')
        && !(game._genocided_monsters || []).includes('stone golem');
}

function polymorphMonsterIntoStoneGolem(mon, messages) {
    const stoneData = monsterByRndName('stone golem');
    if (!mon || !stoneData) return false;
    messages.push(`${fireScrollMonsterName(mon)} solidifies...`);
    const oldHp = mon.mhp || 1;
    const oldMax = mon.mhpmax || oldHp;
    const shiftedLevel = adjustedMonsterLevel(stoneData);
    const shiftedHp = monster_hp(stoneData, shiftedLevel);
    Object.assign(mon, {
        data: { ...stoneData, hpLevel: shiftedLevel },
        m_lev: shiftedLevel,
        mlevel: shiftedLevel,
        mhp: Math.max(1, Math.min(shiftedHp, Math.trunc((oldHp * shiftedHp) / oldMax))),
        mhpmax: shiftedHp,
        msleeping: 0,
    });
    messages.push(`Now it's a stone golem.`);
    newsym(mon.mx, mon.my);
    return true;
}

function petrifyMonsterFromThrownEgg(mon, messages) {
    if (!mon || mon.dead || monsterResistsStoning(mon)) return false;
    if (monsterPolyWhenStoned(mon) && polymorphMonsterIntoStoneGolem(mon, messages)) return false;
    messages.push(`${fireScrollMonsterName(mon)} turns to stone.`);
    stoneMonster(mon, messages, { awardExperience: true });
    return true;
}

function heroThrownPetrifyingEggHitMonster(egg, mon) {
    const messages = [];
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    const plural = count === 1 ? '' : 's';
    messages.push(`Splat!  You hit ${thrownEggTargetTheName(mon)} with ${thrownPetrifyingEggHitArticle(egg)} egg${plural}!`);
    egg.known = true;
    markObjectShopBillUsedUp(egg);
    if (!petrifyMonsterFromThrownEgg(mon, messages))
        applyThrownEggNominalDamage(mon, messages);
    return messages;
}

function heroThrownPyroliskEggHitMonster(egg, mon) {
    const messages = [];
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    const plural = count === 1 ? '' : 's';
    messages.push(`You hit ${thrownEggTargetTheName(mon)} with ${thrownEggHitArticle(egg)} egg${plural}.`);
    markObjectShopBillUsedUp(egg);
    const explosion = resolvePyroliskEggExplosion(mon.mx, mon.my, d(3, 6));
    appendFireExplosionMessages(messages, explosion);
    return messages;
}

function heroThrownEggHitMonster(egg, mon) {
    applyThrownEggLuckPenalty(egg);
    if (isTouchPetrifyingEgg(egg)) return heroThrownPetrifyingEggHitMonster(egg, mon);
    if (isPyroliskEgg(egg)) return heroThrownPyroliskEggHitMonster(egg, mon);
    return heroThrownOrdinaryEggHitMonster(egg, mon);
}

function heroMeleeEggHitMonster(egg, mon) {
    applyThrownEggLuckPenalty(egg);
    if (isTouchPetrifyingEgg(egg)) return {
        messages: heroThrownPetrifyingEggHitMonster(egg, mon),
        consumed: true,
    };
    if (isPyroliskEgg(egg)) return {
        messages: heroThrownPyroliskEggHitMonster(egg, mon),
        consumed: true,
    };
    const messages = [];
    const targetName = thrownEggTargetName(mon);
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    const plural = count === 1 ? '' : 's';
    messages.push(`You hit the ${targetName} with ${thrownEggHitArticle(egg)} egg${plural}.`);
    if (monsterTouchPetrifies(mon) && !isStaleEggItem(egg)) {
        messages.push(`The egg${plural} ${count === 1 ? "isn't" : "aren't"} alive any more...`);
        transformMeleeEggStackToRocks(egg);
        applyThrownEggNominalDamage(mon, messages);
        return { messages, consumed: false };
    }
    messages.push('Splat!');
    markObjectShopBillUsedUp(egg);
    applyThrownEggNominalDamage(mon, messages);
    return { messages, consumed: true };
}

function heroThrownOrdinaryEggHitMonster(egg, mon) {
    const messages = [];
    const targetName = thrownEggTargetName(mon);
    const count = Math.max(1, Math.trunc(Number(egg?.quan || 1)));
    const plural = count === 1 ? '' : 's';
    messages.push(`You hit the ${targetName} with ${thrownEggHitArticle(egg)} egg${plural}.`);
    if (monsterTouchPetrifies(mon) && !isStaleEggItem(egg)) {
        messages.push(`The egg${plural} ${count === 1 ? "isn't" : "aren't"} alive any more...`);
        placeThrownEggPetrifiedRock(egg, mon);
    } else {
        messages.push('Splat!');
        markObjectShopBillUsedUp(egg);
    }
    applyThrownEggNominalDamage(mon, messages);
    return messages;
}

function isExpensiveCameraObject(obj) {
    const kind = objectKindKey(obj);
    return obj?.otyp === EXPENSIVE_CAMERA || kind === 'expensive camera';
}

function isMirrorObject(obj) {
    const kind = objectKindKey(obj);
    return obj?.otyp === MIRROR || kind === 'looking glass' || kind === 'mirror';
}

function supportsHeroThrownFragileObjectUpwardHit(obj) {
    if (!obj || isPotionObject(obj) || isCreamPieObject(obj) || isMelonObject(obj) || isEggItem(obj)) return false;
    return impactDropBreakKind(obj) === 'pieces';
}

async function releaseBrokenCameraDemon(obj, messages, x = null, y = null) {
    if (!isExpensiveCameraObject(obj) || rn2(3)) return null;
    const data = monsterByRndName(rn2(3) ? 'homunculus' : 'imp') || { name: 'imp', mlet: 'i', glyph: 'i', mlevel: 3, hpLevel: 4 };
    const releaseX = x ?? game.u?.ux ?? obj.ox ?? 0;
    const releaseY = y ?? game.u?.uy ?? obj.oy ?? 0;
    const mon = await makemon(data, releaseX, releaseY, MM_NOMSG);
    if (!mon) return null;
    if (!game.u?.blind && cansee(mon.mx, mon.my)) {
        const released = heroIsHallucinating() ? sentenceCase(articleFor(getbogusmon())) : 'The picture-painting demon';
        messages.push(`${released} is released!`);
    }
    mon.mpeaceful = obj.cursed ? 0 : 1;
    set_malign(mon);
    return mon;
}

async function applyHeroCausedFragileBreakSideEffects(obj, messages, x = null, y = null) {
    if (isMirrorObject(obj)) changeHeroLuck(-2);
    if (isEggItem(obj)) applyThrownEggLuckPenalty(obj);
    await releaseBrokenCameraDemon(obj, messages, x, y);
}

async function applyHeroThrownFragileBreakSideEffects(obj, messages, x = null, y = null) {
    await applyHeroCausedFragileBreakSideEffects(obj, messages, x, y);
}

function applyHeroBrokenEggPostRemovalSideEffects(obj, messages, x, y) {
    if (!isPyroliskEgg(obj)) return;
    const explosion = resolvePyroliskEggExplosion(x, y, d(3, 6));
    appendFireExplosionMessages(messages, explosion);
}

function heroThrownIronBarsBreakableClassHitObject(obj) {
    if (!obj) return false;
    const kind = objectKindKey(obj);
    if (isMirrorObject(obj) || obj.otyp === CRYSTAL_BALL || kind === 'crystal ball') return true;
    if (isExpensiveCameraObject(obj)) return true;
    return isGlassMaterialWandObject(obj);
}

async function heroThrownIronBarsBreakImpact(obj, impact) {
    if (!impact?.pointBlank) rn2(5); // C bhit() evaluates the force-hit roll before hits_bars().
    const breakKind = projectileTopLevelBreakKind(obj);
    if (!breakKind) return { broke: false, messages: ['Clonk!'] };
    const messages = [];
    projectileTopLevelBreakMessage(obj, breakKind, messages);
    await applyHeroThrownFragileBreakSideEffects(obj, messages, impact.x, impact.y);
    return { broke: true, messages };
}

async function heroThrownFragileObjectSelfHitMessages(obj, action, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} ${action} the ${ceilingName}, then falls back on top of your head.`];
    const breakKind = projectileTopLevelBreakKind(obj);
    if (breakKind) {
        projectileTopLevelBreakMessage(obj, breakKind, messages);
        await applyHeroThrownFragileBreakSideEffects(obj, messages);
        markThrownBrokenObjectDebt(obj);
        return messages;
    }

    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a falling object';
            messages.push('You die...');
        }
    }
    const landing = landProjectileObjectWithShopHandling(obj, game.u?.ux || obj.ox || 0, game.u?.uy || obj.oy || 0, {});
    messages.push(...landing.messages);
    return messages;
}

async function heroThrownFragileObjectCeilingBreakMessages(obj, breakKind, ceilingName = heroThrowCeilingName()) {
    const messages = [`${floorObjectSubject({ ...obj, quan: 1 })} hits the ${ceilingName}.`];
    projectileTopLevelBreakMessage(obj, breakKind, messages);
    await applyHeroThrownFragileBreakSideEffects(obj, messages);
    markThrownBrokenObjectDebt(obj);
    return messages;
}

async function heroThrownFragileObjectUpwardMessages(obj) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownFragileObjectSelfHitMessages(obj, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(obj);
        if (breakKind) return heroThrownFragileObjectCeilingBreakMessages(obj, breakKind, ceilingName);
        return heroThrownFragileObjectSelfHitMessages(obj, 'hits', ceilingName);
    }
    return heroThrownFragileObjectSelfHitMessages(obj, 'almost hits', ceilingName);
}

function heroThrownCreamPieUpwardMessages(pie) {
    const ceilingName = heroThrowCeilingName();
    const hasCeiling = heroHasThrowCeiling();
    const hitsRoof = !!(rn2(5) && !heroIsUnderwaterForThrow());
    if (!hasCeiling)
        return heroThrownCreamPieSelfHitMessages(pie, 'flies up into', ceilingName);
    if (hitsRoof) {
        const breakKind = projectileTopLevelBreakKind(pie);
        if (breakKind) return heroThrownPotionCeilingBreakMessages(pie, breakKind, ceilingName);
        return heroThrownCreamPieSelfHitMessages(pie, 'hits', ceilingName);
    }
    return heroThrownCreamPieSelfHitMessages(pie, 'almost hits', ceilingName);
}

function dipPotionAlchemyExplosion(target, amount, messages) {
    const damage = amount + rnd(9);
    const explodes = target.cursed || isPotionOfAcid(target)
        || (isPotionOfOil(target) && (target.lamplit || target.burning))
        || !rn2(heroWearsAlchemySmock() ? 30 : 10);
    if (!explodes) return false;
    messages.push(`${heroIsDeaf() ? '' : 'BOOM!  '}They explode!`);
    exerciseAttribute(A_STR, false);
    potionBreathe(target, messages, { allowLifeSaving: true });
    useUpInventoryItem(target, target.quan || 1);
    if (messages.lifeSaving || messages.fatal) {
        messages.more = true;
        return true;
    }
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'alchemic blast';
            messages.push('You die...');
        }
    }
    return true;
}

function randomAlchemyResultName(target) {
    if (target?.odiluted) return 'water';
    switch (rnd(8)) {
    case 1:
        return 'water';
    case 2:
    case 3:
        return 'sickness';
    case 4: {
        const randomPotion = mkobj(POTION_CLASS, false);
        return alchemyPotionName(randomPotion) || IDENTIFIED_POTION_NAMES[randomPotion?.potionIndex] || '';
    }
    default:
        return '';
    }
}

function setAlchemyPotionIdentity(potion, resultName) {
    potion.cls = 'potion';
    potion.glyph = '!';
    potion.otyp = alchemyPotionOtyp(resultName);
    potion.kind = resultName;
    potion.actualKind = `potion of ${resultName}`;
    potion.potionIndex = alchemyPotionIndex(resultName);
    potion.blessed = false;
    potion.cursed = false;
    potion.bknown = false;
    potion.known = false;
    if (game.u?.blind || heroIsHallucinating()) potion.dknown = false;
    potion.odiluted = resultName !== 'water';
    if (resultName === 'water') {
        potion.kind = 'water';
        potion.actualKind = 'potion of water';
        potion.potionIndex = null;
        potion.odiluted = false;
    }
    delete potion.plural;
}

function alchemyResultAppearance(resultName) {
    if (resultName === 'water') return 'clear';
    const index = alchemyPotionIndex(resultName);
    return index != null ? game._object_descriptions?.potions?.[index]?.description || resultName : resultName;
}

function dipPotionIntoPotion(target, potion) {
    const messages = [];
    if (!isPotionObject(target) || !isPotionObject(potion) || target === potion) return messages;
    if (alchemyPotionName(target) === alchemyPotionName(potion)) return messages;

    const resultName = mixtypePotionResultName(target, potion);
    const affectedCount = alchemyAffectedPotionCount(target, potion, resultName);
    const splitFromStack = affectedCount < Math.max(1, Math.trunc(Number(target.quan || 1)));
    const affected = splitCarriedInventoryItemCount(target, affectedCount);
    const sourceForMessage = { ...potion };
    messages.push(potionAlchemyMixMessage(affected, sourceForMessage, splitFromStack));
    consumeDipPotion(potion);

    if (dipPotionAlchemyExplosion(affected, affectedCount, messages))
        return handledDipMessages(messages);

    let finalName = resultName || randomAlchemyResultName(affected);
    if (!finalName) {
        useUpInventoryItem(affected, affected.quan || 1);
        messages.push(`The mixture ${game.u?.blind ? '' : 'glows brightly and '}evaporates.`);
        return handledDipMessages(messages);
    }

    setAlchemyPotionIdentity(affected, finalName);
    refreshInventoryObjectLine(affected);
    if (affected.unpaid) syncUnpaidBillLine(affected);
    mergeNeutralizedPotionIfPossible(affected);
    if (finalName === 'water' && !heroIsHallucinating()) {
        messages.push(`The mixture bubbles${game.u?.blind ? '' : ', then clears'}.`);
    } else if (!game.u?.blind) {
        messages.push(`The mixture looks ${alchemyResultAppearance(finalName)}.`);
    }
    return handledDipMessages(messages);
}

function acidDipTargetName(item) {
    return dipItemDescription(item).replace(/\s+\(being worn\)$/, '');
}

function acidCorrosionVerb(item) {
    return oilDipVerb(item, 'corrodes', 'corrode');
}

function refreshAcidDipTargetLine(item) {
    refreshInventoryObjectLine(item);
    if (item?.unpaid) syncUnpaidBillLine(item);
}

function dipObjectIntoAcidPotion(target, potion) {
    const messages = [];
    if (!isAcidCorrosionDipTargetObject(target) || !isPotionOfAcid(potion)) return messages;
    const name = acidDipTargetName(target);
    if (target.greased) {
        messages.push(`Your ${name} ${oilDipVerb(target, 'is', 'are')} protected by the layer of grease!`);
        if (!rn2(2)) {
            target.greased = false;
            messages.push('The grease dissolves.');
        }
        refreshAcidDipTargetLine(target);
        consumeDipPotion(potion);
        return messages;
    }
    if (target.oerodeproof) {
        if (!target.rknown) {
            messages.push(`Somehow, your ${name} ${oilDipVerb(target, 'is', 'are')} not affected by the corrosion.`);
            target.rknown = true;
            refreshAcidDipTargetLine(target);
        }
        messages.push('Interesting...');
        return messages;
    }
    if (target.blessed && !rnl(4)) {
        messages.push('Interesting...');
        return messages;
    }
    const current = Math.min(3, target.oeroded2 || 0);
    if (current >= 3) {
        messages.push('Interesting...');
        return messages;
    }
    target.oeroded2 = current + 1;
    const adverb = target.oeroded2 === 3 ? ' completely' : current ? ' further' : '';
    messages.push(`Your ${name} ${acidCorrosionVerb(target)}${adverb}!`);
    refreshAcidDipTargetLine(target);
    consumeDipPotion(potion);
    return messages;
}

function dipPoisonableWeaponIntoPotion(target, potion) {
    const messages = [];
    if (!isPoisonableWeaponObject(target) || !isPotionObject(potion)) return messages;
    if (isPotionOfSickness(potion)) {
        if (!target.opoisoned) {
            const potionName = pickupObjectName(potion);
            const source = (potion.quan || 1) > 1 ? `One of the ${potionName}` : `The ${potionName}`;
            messages.push(`${source} forms a coating on the ${poisonedWeaponObjectName(target)}.`);
            target.opoisoned = true;
            refreshInventoryObjectLine(target);
            if (target.unpaid) syncUnpaidBillLine(target);
            consumeDipPotion(potion);
        } else {
            messages.push('Interesting...');
        }
        return messages;
    }
    if (isHealingFamilyPotion(potion)) {
        if (target.opoisoned && !isPermanentPoisonedWeaponObject(target)) {
            messages.push(`A coating wears off the ${poisonedWeaponObjectName(target)}.`);
            target.opoisoned = false;
            refreshInventoryObjectLine(target);
            if (target.unpaid) syncUnpaidBillLine(target);
            consumeDipPotion(potion);
        } else {
            messages.push('Interesting...');
        }
    }
    return messages;
}

function handledDipMessages(messages) {
    messages.handled = true;
    return messages;
}

function dipMessagesTopline(messages) {
    return messages.join('  ') || (messages.handled ? '' : 'Interesting...');
}

function potionNeutralizationAppearance(item) {
    const index = item?.potionIndex;
    const appearance = index != null ? game._object_descriptions?.potions?.[index]?.description : '';
    return appearance || potionDipKind(item) || 'clear';
}

function potionNeutralizationResultAppearance(kind) {
    const index = kind === 'fruit juice' ? 22 : kind === 'water' ? null : undefined;
    if (index != null) return game._object_descriptions?.potions?.[index]?.description || kind;
    return kind === 'water' ? 'clear' : kind;
}

function potionNeutralizationTransformMessage(oldPotion, resultKind, fromStack) {
    const oldAppearance = oldPotion?.dknown ? `${potionNeutralizationAppearance(oldPotion)} ` : '';
    const stackSuffix = fromStack ? ' that you dipped into' : '';
    if (resultKind === 'water') return `The ${oldAppearance}potion${stackSuffix} clears.`;
    return `The ${oldAppearance}potion${stackSuffix} turns ${potionNeutralizationResultAppearance(resultKind)}.`;
}

function setNeutralizedPotionIdentity(potion, resultKind, neutralizer) {
    potion.cls = 'potion';
    potion.glyph = '!';
    potion.quan = 1;
    potion.blessed = false;
    potion.bknown = false;
    potion.known = false;
    potion.dknown = !game.u?.blind && !heroIsHallucinating();
    delete potion.plural;
    if (resultKind === 'water') {
        potion.otyp = POT_WATER;
        potion.kind = 'water';
        potion.actualKind = 'potion of water';
        potion.potionIndex = null;
        potion.cursed = false;
        potion.odiluted = false;
    } else {
        potion.otyp = POTION_CLASS;
        potion.kind = resultKind;
        potion.actualKind = `potion of ${resultKind}`;
        potion.potionIndex = resultKind === 'fruit juice' ? 22 : potion.potionIndex;
        potion.cursed = !!neutralizer?.cursed;
    }
}

function neutralizedPotionMergeCompatible(stacked, potion) {
    if (!stacked || !potion || stacked === potion || !isPotionObject(stacked) || !isPotionObject(potion)) return false;
    if (potionDipKind(stacked) !== potionDipKind(potion)) return false;
    if (!!stacked.blessed !== !!potion.blessed || !!stacked.cursed !== !!potion.cursed) return false;
    if (!!stacked.odiluted !== !!potion.odiluted) return false;
    if (stacked.bknown !== potion.bknown || stacked.dknown !== potion.dknown || stacked.known !== potion.known) return false;
    if (!!stacked.unpaid !== !!potion.unpaid) return false;
    return !stacked.unpaid || sameShopBillUnitPrice(stacked, potion);
}

function mergeNeutralizedPotionIfPossible(potion) {
    const stacked = (game.inventory || []).find(item => neutralizedPotionMergeCompatible(item, potion));
    if (!stacked) return potion;
    stacked.quan = (stacked.quan || 1) + (potion.quan || 1);
    delete stacked.plural;
    if (stacked.unpaid && potion.unpaid) mergeStackedShopBillEntries(stacked, potion);
    refreshInventoryObjectLine(stacked);
    if (stacked.unpaid) syncUnpaidBillLine(stacked);
    game.inventory = (game.inventory || []).filter(item => item !== potion);
    return stacked;
}

function dipPotionIntoNeutralizer(target, potion) {
    const messages = [];
    const resultKind = potionNeutralizationResultKind(target, potion);
    if (!resultKind) return messages;

    const fromStack = (potion.quan || 1) > 1;
    const singlePotion = splitOneCarriedInventoryItem(potion);
    const oldPotion = { ...singlePotion };
    const payment = costlyAlterationPaymentMessage(singlePotion, 'neutralize');
    if (payment) messages.push(payment);
    setNeutralizedPotionIdentity(singlePotion, resultKind, target);
    refreshInventoryObjectLine(singlePotion);
    if (singlePotion.unpaid) syncUnpaidBillLine(singlePotion);
    mergeNeutralizedPotionIfPossible(singlePotion);
    if (!game.u?.blind)
        messages.push(potionNeutralizationTransformMessage(oldPotion, resultKind, fromStack));
    return handledDipMessages(messages);
}

function waterDipObjectGlowPhrase(item) {
    const name = dipItemDescription(item, false).replace(/\s+\(being worn\)$/, '');
    const verb = (item?.quan || 1) > 1 ? 'glow' : 'glows';
    return `Your ${name} ${verb}`;
}

function waterDipBucAuraMessage(item, color) {
    const article = /^[aeiou]/i.test(color) ? 'an' : 'a';
    return `${waterDipObjectGlowPhrase(item)} with ${article} ${color} aura.`;
}

function normalizeWaterPotionIdentity(item) {
    if (!isWaterPotion(item)) return;
    item.kind = 'water';
    item.actualKind = 'potion of water';
    item.potionIndex = null;
    item.plural = undefined;
}

function refreshWaterDipTargetLine(item) {
    normalizeWaterPotionIdentity(item);
    syncCarriedFigurineTransformTimer(item);
    refreshInventoryLineAfterBucChange(item);
    if (item?.unpaid) syncUnpaidBillLine(item);
}

function learnWaterDipBuc(target, potion) {
    if (!game.u?.blind) {
        target.bknown = !heroIsHallucinating();
    } else if (!potion.bknown || !potion.dknown) {
        target.bknown = false;
    }
}

function waterDipDevaluationPayment(target, verb) {
    if (!target?.unpaid || !isWaterPotion(target)) return '';
    target.bknown = true;
    return costlyAlterationPaymentMessage(target, verb);
}

function isTowelObject(item) {
    return objectKindKey(item) === 'towel';
}

function wetTowelWithNeutralWater(towel, messages) {
    if (!isTowelObject(towel) || (towel.spe || 0) >= 7) return false;
    const wetness = Math.max(0, towel.spe || 0);
    towel.spe = wetness + rnd(7 - wetness);
    towel.wetness = towel.spe;
    refreshInventoryObjectLine(towel);
    if (towel.unpaid) syncUnpaidBillLine(towel);
    messages.push('The towel soaks it up!');
    return true;
}

function normalizeWaterDamagedPotionIdentity(item) {
    if (!item || item.otyp !== POT_WATER) return;
    item.kind = 'water';
    item.actualKind = 'potion of water';
    item.potionIndex = null;
    item.dknown = false;
}

function refreshNeutralWaterDamagedTarget(item) {
    if (!item || !(game.inventory || []).includes(item)) return;
    normalizeWaterDamagedPotionIdentity(item);
    refreshInventoryObjectLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
}

function neutralWaterContainerDamage(container, messages) {
    if (!isLiquidFlowContainer(container)) return false;
    const name = pickupObjectName(container);
    const waterproof = isLiquidFlowWaterproofContainer(container);
    if (waterproof && (!container.cursed || rn2(3))) {
        if (!game.u?.blind && !(game.u?.underwater || game.u?.uunderwater))
            messages.push(`The water cannot get into your ${name}.`);
        return true;
    }
    messages.push(`Some water gets into your ${name}!`);
    const acidContext = { count: 0 };
    for (const content of [...liquidFlowContainerContents(container)]) {
        waterDamageFloorItem(content, messages, false, acidContext, {
            removeObject: item => removeContainedObject(container, item),
            usedUpShopBillOnDestroy: true,
        });
    }
    return true;
}

function neutralWaterDamageCarriedTarget(target, messages) {
    if (!target) return false;
    if (objectKindKey(target) === 'can of grease' && (target.spe || 0) > 0) return false;
    if (wetTowelWithNeutralWater(target, messages)) return true;
    const changed = rustTrapWaterDamageItem(target, messages);
    if (changed) refreshNeutralWaterDamagedTarget(target);
    if (changed) return true;
    return neutralWaterContainerDamage(target, messages);
}

function dipObjectIntoNeutralWaterPotion(target, potion) {
    const messages = [];
    if (!isNeutralWaterDipSource(potion) || !isWaterDipTargetObject(target)) return messages;
    if (!neutralWaterDamageCarriedTarget(target, messages)) return messages;
    consumeDipPotion(potion);
    return handledDipMessages(messages);
}

function dipObjectIntoWaterPotion(target, potion) {
    const messages = [];
    if (!isWaterPotion(potion) || !isWaterDipTargetObject(target)) return messages;
    if (isNeutralWaterDipSource(potion)) return dipObjectIntoNeutralWaterPotion(target, potion);

    let effect = '';
    if (potion.blessed) {
        if (target.cursed) {
            messages.push(`${waterDipObjectGlowPhrase(target)} amber.`);
            messages.push(waterDipDevaluationPayment(target, 'uncurse'));
            target.cursed = false;
            effect = 'uncurse';
        } else if (!target.blessed) {
            messages.push(waterDipBucAuraMessage(target, 'light blue'));
            target.blessed = true;
            target.cursed = false;
            effect = 'bless';
        }
    } else if (potion.cursed) {
        if (target.blessed) {
            messages.push(`${waterDipObjectGlowPhrase(target)} brown.`);
            messages.push(waterDipDevaluationPayment(target, 'unbless'));
            target.blessed = false;
            effect = 'unbless';
        } else if (!target.cursed) {
            messages.push(waterDipBucAuraMessage(target, 'black'));
            target.cursed = true;
            target.blessed = false;
            effect = 'curse';
        }
    }
    if (!effect) return messages;

    learnWaterDipBuc(target, potion);
    refreshWaterDipTargetLine(target);
    if ((effect === 'bless' || effect === 'curse') && target.unpaid && isWaterPotion(target))
        alterShopBillCostIfHigher(target);
    consumeDipPotion(potion);
    return messages.filter(Boolean);
}

function dipWeaponIntoOilPotion(target, potion) {
    const messages = [];
    if (!isOilWeaponDipTargetObject(target) || !isPotionOfOil(potion)) return messages;
    if (potion.lamplit || potion.burning) {
        messages.push('The lit potion burns away.');
        consumeDipOilPotion(potion);
        exerciseAttribute(A_WIS, false);
        return messages;
    }
    if (potion.cursed) {
        messages.push(`The potion spills and covers your ${fingersOrGloves()} with oil.`);
        addHeroGlibTimeout(d(2, 10));
        consumeDipOilPotion(potion);
        exerciseAttribute(A_WIS, false);
        return messages;
    }

    const name = oilDipTargetName(target);
    const repairKind = oilDipRepairKind(target);
    if (repairKind) {
        messages.push(`Your ${name} ${oilDipVerb(target, game.u?.blind ? 'feels' : 'is', game.u?.blind ? 'feel' : 'are')} less ${repairKind}.`);
        if ((target.oeroded || 0) > 0) target.oeroded--;
        if ((target.oeroded2 || 0) > 0) target.oeroded2--;
        exerciseAttribute(A_WIS, true);
    } else {
        messages.push(game.u?.blind
            ? `Your ${name} ${oilDipVerb(target, 'feels', 'feel')} oily.`
            : `Your ${name} ${oilDipVerb(target, 'gleams', 'gleam')} with an oily sheen.`);
        exerciseAttribute(A_WIS, false);
    }
    refreshOilDipTargetLine(target);
    consumeDipOilPotion(potion);
    return messages;
}

function dipObjectIntoOilPotion(target, potion) {
    if (isOilWeaponDipTargetObject(target)) return dipWeaponIntoOilPotion(target, potion);
    return dipLampIntoOilPotion(target, potion);
}

function dipObjectIntoPotion(target, potion) {
    if (isWaterPotion(potion)) return dipObjectIntoWaterPotion(target, potion);
    if (isPotionOfPolymorph(target) || isPotionOfPolymorph(potion)) return dipObjectIntoPolymorphPotion(target, potion);
    if (isPotionObject(target) && isPotionObject(potion)) return dipPotionIntoPotion(target, potion);
    if (isPotionOfAcid(potion)) return dipObjectIntoAcidPotion(target, potion);
    if (isPotionOfOil(potion)) return dipObjectIntoOilPotion(target, potion);
    if (isPoisonableWeaponObject(target)) return dipPoisonableWeaponIntoPotion(target, potion);
    if (isPotionNeutralizationTarget(target)) return dipPotionIntoNeutralizer(target, potion);
    return [];
}

function oilRefuelAmount(potion) {
    return Math.trunc(((potion?.odiluted ? 3 : 4) * (potion?.age ?? 400)) / 2);
}

function dipLampIntoOilPotion(lamp, potion) {
    const messages = [];
    if (!isOilRefuelLampObject(lamp) || !isPotionOfOil(potion)) return messages;
    if (potion.lamplit || potion.burning) {
        messages.push('The lit potion burns away.');
        useUpInventoryItem(potion, 1);
        identifyPotionOfOil(potion);
        return messages;
    }
    if (potion.cursed) {
        messages.push(`The potion spills and covers your ${fingersOrGloves()} with oil.`);
        addHeroGlibTimeout(d(2, 10));
        useUpInventoryItem(potion, 1);
        identifyPotionOfOil(potion);
        return messages;
    }
    if (lamp.lamplit || lamp.burning) {
        messages.push('Boom!  The oil catches fire.');
        useUpInventoryItem(potion, 1);
        identifyPotionOfOil(potion);
        return messages;
    }
    const kind = objectKindKey(lamp);
    if ((lamp.otyp === MAGIC_LAMP || kind === 'magic lamp') && (lamp.spe ?? 1) === 0) {
        lamp.otyp = OIL_LAMP;
        lamp.kind = 'oil lamp';
        lamp.actualKind = 'oil lamp';
        lamp.age = 0;
    }
    if ((lamp.age ?? 1500) > 1000) {
        messages.push(`Your ${lampNoun(lamp)} is full.`);
    } else {
        messages.push(`You fill your ${lampNoun(lamp)} with oil.`);
        checkUnpaidUsage(potion, messages);
        lamp.age = Math.min(1500, (lamp.age ?? 0) + oilRefuelAmount(potion));
        useUpInventoryItem(potion, 1);
        identifyPotionOfOil(potion);
    }
    lamp.spe = 1;
    refreshLightSourceLine(lamp);
    return messages;
}

function removeMonsterFromLevel(mon) {
    if (!mon || !game.level?.monsters) return;
    game.level.monsters = game.level.monsters.filter(candidate => candidate !== mon);
    newsym(mon.mx, mon.my);
}

async function djinniFromLamp(lamp, messages) {
    const data = monsterByRndName('djinni') || RANDOM_MONSTER_BY_NAME.get('djinni');
    const mon = data ? await makemon(data, game.u?.ux || 0, game.u?.uy || 0, MM_NOMSG) : null;
    if (!mon) {
        messages.push('It turns out to be empty.');
        return;
    }

    const name = mon.data?.name || 'djinni';
    if (game.u?.blind) {
        messages.push('You smell acrid fumes.');
        messages.push('Something speaks.');
    } else {
        messages.push(`In a cloud of smoke, ${monsterIndefiniteName(name)} emerges!`);
        messages.push(`The ${name} speaks.`);
    }

    let chance = rn2(5);
    if (lamp.blessed) chance = chance === 4 ? rnd(4) : 0;
    else if (lamp.cursed) chance = chance === 0 ? rn2(4) : 4;

    switch (chance) {
    case 0:
        messages.push('"I am in your debt.  I will grant one wish!"');
        removeMonsterFromLevel(mon);
        game._queued_messages_after_more ??= [];
        game._queued_messages_after_more.push({ text: 'For what do you wish?', more: false, beginWishPrompt: true });
        break;
    case 1:
        messages.push('"Thank you for freeing me!"');
        mon.pet = true;
        mon.mtame = Math.max(1, mon.mtame || 0);
        mon.mpeaceful = 1;
        break;
    case 2:
        messages.push('"You freed me!"');
        mon.pet = false;
        mon.mtame = 0;
        mon.mpeaceful = 1;
        set_malign(mon);
        break;
    case 3:
        messages.push('"It is about time!"');
        if (visibleCreatedMonster(mon)) messages.push(`The ${name} vanishes.`);
        removeMonsterFromLevel(mon);
        break;
    default:
        messages.push('"You disturbed me, fool!"');
        mon.pet = false;
        mon.mtame = 0;
        mon.mpeaceful = 0;
        set_malign(mon);
        break;
    }
}

async function rubLampObject(item) {
    const messages = [];
    if (!itemIsWielded(item)) {
        wieldItemForApply(item);
        await setMessage(`You now wield ${articleFor(pickupObjectName({ ...item, line: '' }))}.`);
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    const kind = objectKindKey(item);
    if (item.otyp === MAGIC_LAMP || kind === 'magic lamp') {
        if ((item.spe ?? 0) > 0 && !rn2(3)) {
            checkUnpaidUsage(item, messages, { altusage: true, chargeCount: 1 });
            const wasLit = !!(item.lamplit || item.burning);
            item.otyp = OIL_LAMP;
            item.kind = 'oil lamp';
            item.actualKind = 'oil lamp';
            item.spe = 0;
            item.age = rn1(500, 1000);
            if (wasLit) beginWishedBurn(item);
            refreshLightSourceLine(item);
            recordKnownToolDiscovery('magic lamp');
            await djinniFromLamp(item, messages);
        } else if (rn2(2)) {
            messages.push(game.u?.blind ? 'You smell smoke.' : 'You see a puff of smoke.');
        } else {
            messages.push('Nothing happens.');
        }
    } else if (item.otyp === BRASS_LANTERN || kind === 'brass lantern') {
        messages.push('Rubbing the electric lamp is not particularly rewarding.');
        messages.push('Anyway, nothing exciting happens.');
    } else {
        messages.push('Nothing happens.');
    }

    await setMessage(messages.join('  '), messages.length > 1 || !!game._queued_messages_after_more?.length);
    game._command_mode = null;
    game.context.move = 1;
}

function foodObjectNutrition(item) {
    if (!item) return 0;
    if (item.globby) return item.owt || 20;
    if (item.otyp === 'corpse' || item.otyp === CORPSE || /\bcorpse$/.test(objectKindKey(item))) {
        const corpseName = item.corpsenm?.name || objectKindKey(item).replace(/\s+corpse$/, '');
        return CORPSE_NUTRITION.get(corpseName) || 0;
    }
    const kind = objectKindKey(item).replace(/^partly eaten /, '');
    if ((item.foodRoll || 1000) <= 140) return FOOD_NUTRITION.get('tripe ration') || 200;
    if (kind.startsWith('tin:')) return 0;
    return FOOD_NUTRITION.get(kind) ?? 0;
}

function addHeroNutrition(nutrition) {
    if (!game.u || nutrition <= 0) return;
    game.u.uhunger = (game.u.uhunger ?? 900) + nutrition;
    refreshHeroEatingHungerStatus();
}

function refreshHeroEatingHungerStatus() {
    if (!game.u) return;
    game.u._statusSuffix = (game.u._statusSuffix || '')
        .replace(/ Satiated| Hungry| Weak| Fainting| Fainted/g, '');
    if (game.u.uhunger > 1000) game.u._statusSuffix = `${game.u._statusSuffix || ''} Satiated`;
}

function heroIsSatiatedForEating() {
    return (game.u?.uhunger ?? 900) > 1000
        || (game.u?._statusSuffix || '').includes('Satiated');
}

function delayedEatingFullWarningOutcome(remainingAfterBite) {
    if (!game.u || (game.u.uhunger ?? 900) < 1500 || game._eating_fullwarn
        || heroHasHungerPropertyForChoke()) return null;
    game._eating_fullwarn = 1;
    game._eating_nomovemsg = "You're finally finished.";
    const messages = ["You're having a hard time getting all of it down."];
    if (game._eating_canchoke && remainingAfterBite > 1) {
        messages.push('Continue eating? [yn] (n)');
        return { messages, prompt: true };
    }
    return { messages, prompt: false };
}

function delayedFoodChokeWord(food) {
    if (!food) return 'it';
    return pickupObjectName({ ...food, quan: 1 })
        .replace(/^[a-zA-Z$?] - /, '')
        .replace(/^(?:a|an|the)\s+/i, '');
}

function delayedFoodChokeMessageWord(food) {
    if (food?.cls === 'food' || food?.otyp === FOOD_CLASS || food?.glyph === '%') return 'food';
    return delayedFoodChokeWord(food);
}

function delayedFoodChokeDeathCause(food) {
    const word = delayedFoodChokeWord(food).replace(/^partly eaten\s+/i, '');
    if (word === 'it') return 'choked on a quick snack';
    const article = /^[aeiou]/i.test(word) ? 'an' : 'a';
    return `choked on ${article} ${word}`;
}

function wornItemKindIncludes(text) {
    const needle = String(text || '').toLowerCase();
    return (game.inventory || []).some(item => {
        if (!item?.worn) return false;
        const fields = [item.actualKind, item.kind, item.line];
        return fields.some(field => String(field || '').toLowerCase().includes(needle));
    });
}

function heroIsBreathlessForChoke() {
    const form = polyselfForm() || {};
    return !!(game.u?.breathless || game.u?.Breathless
        || game.u?.magicalBreathing || game.u?.Magical_breathing
        || form.breathless
        || wornItemKindIncludes('amulet of magical breathing'));
}

function heroHasHungerPropertyForChoke() {
    return !!(game.u?.hunger || game.u?.Hunger || game.u?.hungerProperty
        || game.u?.intrinsics?.hunger
        || wornItemKindIncludes('ring of hunger'));
}

function heroIsStrangledForChoke() {
    return !!((game.u?._statusSuffix || '').includes('Strngl')
        || game.u?.strangled || game.u?.strangling);
}

function heroCannotVomitForChoke() {
    const form = polyselfForm() || {};
    const name = String(form.name || '').toLowerCase();
    const mlet = String(form.mlet || '').toLowerCase();
    if (['pony', 'horse', 'warhorse'].includes(name)) return true;
    if (mlet === 'r' && name !== 'rock mole' && name !== 'woodchuck') return true;
    return !!form.cantVomit;
}

function delayedFoodChokeOutcome(food) {
    const messages = [];
    const role = game.urole?.name?.m || game._startup_role || '';
    if (role === 'Knight' && game.u?.ualign?.type === A_LAWFUL)
        messages.push('You feel like a glutton!');
    const hungerProperty = heroHasHungerPropertyForChoke();
    if (heroIsBreathlessForChoke() || hungerProperty
        || (!heroIsStrangledForChoke() && !rn2(20))) {
        messages.push('You stuff yourself and then vomit voluminously.');
        if (heroCannotVomitForChoke()) messages.push('Your jaw gapes convulsively.');
        if (game.u) {
            game.u.uhunger = hungerProperty ? 60 : Math.max(0, (game.u.uhunger ?? 900) - 1000);
            refreshHeroEatingHungerStatus();
        }
        game._helpless_time = Math.max(game._helpless_time || 0, 2);
        game._wake_message = 'You can move again.';
        return { messages, choked: true, recovered: true, move: 2 };
    }

    messages.push(`You choke over your ${delayedFoodChokeMessageWord(food)}.`);
    game._death_cause = delayedFoodChokeDeathCause(food);
    if (consumeLifeSavingAmulet()) {
        if (game.u) {
            game.u.uhp = 0;
            game.u.uhunger = 900;
            refreshHeroEatingHungerStatus();
        }
        messages.push('You die...');
        messages.push(`But wait...  Your medallion ${game.u?.blind ? 'feels warm' : 'begins to glow'}!`);
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game._command_mode = 'lifeSavingMore';
        return { messages, choked: true, fatal: true, lifesaved: true, more: true, move: 0 };
    }

    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    if (game.context) game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._command_mode = 'deathDieMore';
    prepareDeathBones();
    messages.push('You die...');
    return { messages, choked: true, fatal: true, more: true, move: 0 };
}

export function addDelayedFoodBiteNutrition(nutrition, { remainingAfterBite = 0, food = null } = {}) {
    if (!game.u || !(nutrition > 0)) return { messages: [], prompt: false };
    if (game._eating_canchoke && (game.u.uhunger ?? 900) >= 2000)
        return { ...delayedFoodChokeOutcome(food), preBite: true };
    addHeroNutrition(nutrition);
    if (game._eating_canchoke && (game.u.uhunger ?? 900) >= 2000)
        return { ...delayedFoodChokeOutcome(food), nutritionApplied: true };
    const warning = (game.u.uhunger ?? 900) < 2000
        ? delayedEatingFullWarningOutcome(remainingAfterBite)
        : null;
    return warning || { messages: [], prompt: false };
}

function heroConduct() {
    if (!game.u) return {};
    game.u.uconduct ??= {};
    return game.u.uconduct;
}

function addConductCount(field, amount = 1) {
    const conduct = heroConduct();
    conduct[field] = (conduct[field] || 0) + amount;
}

function recordLiterateConduct() {
    addConductCount('literate');
}

function recordWishConduct({ artifact = false } = {}) {
    addConductCount('wishes');
    if (artifact) addConductCount('wisharti');
}

function recordFoodConduct(item) {
    const conduct = heroConduct();
    conduct.food = (conduct.food || 0) + 1;
    const kind = objectKindKey(item).replace(/^partly eaten /, '');
    const corpseName = item?.corpsenm?.name || kind.replace(/\s+corpse$/, '');
    const veganCorpse = ['lichen', 'brown mold', 'yellow mold', 'green mold', 'red mold', 'shrieker', 'violet fungus']
        .includes(corpseName);
    if ((isCorpseItem(item) || isGlobFood(item) || /\bcorpse$/.test(kind)) && !veganCorpse) {
        conduct.unvegan = (conduct.unvegan || 0) + 1;
        conduct.unvegetarian = (conduct.unvegetarian || 0) + 1;
    } else if (kind === 'tripe ration' || kind === 'tripe' || kind === 'meatball'
        || kind === 'meat stick'
        || kind === 'meat ring' || kind === 'enormous meatball' || (item?.foodRoll || 1000) <= 140) {
        conduct.unvegan = (conduct.unvegan || 0) + 1;
        conduct.unvegetarian = (conduct.unvegetarian || 0) + 1;
    } else if (isEggItem(item) || isRoyalJelly(item)
        || kind === 'pancake' || kind === 'fortune cookie'
        || kind === 'cream pie' || kind === 'candy bar') {
        conduct.unvegan = (conduct.unvegan || 0) + 1;
    }
}

function isFortuneCookieFood(item) {
    return itemKindText(item).replace(/^partly eaten /, '') === 'fortune cookie';
}

function heroIsBlind() {
    return !!(game.u?.blind || (game.u?._statusSuffix || '').includes('Blind'));
}

function fortuneCookieRumorTruth(item) {
    if (item?.blessed) return 1;
    if (item?.cursed) return -1;
    return 0;
}

function queueFortuneCookieRumor(item) {
    if (heroIsBlind()) {
        game._queued_message_after_more = 'This cookie has a scrap of paper inside.';
        game._queued_message_more_after_more = 1;
        game._fortune_cookie_rumor_after_more = 'What a pity that you cannot read it!';
        return;
    }
    game._queued_message_after_more = 'This cookie has a scrap of paper inside.  It reads:';
    game._queued_message_more_after_more = 1;
    const rumor = getrumor(false, fortuneCookieRumorTruth(item)).replace(/^\[cookie\]\s*/, '');
    game._fortune_cookie_rumor_after_more = rumor || 'This cookie is devoid of wisdom.';
}

function cursedAppleSleepPostEffect(item) {
    const kind = itemKindText(item).replace(/^partly eaten /, '');
    if (kind !== 'apple' || !item?.cursed || game.u?.sleepResistance) return null;
    const duration = rn1(11, 20);
    game._helpless_time = Math.max(game._helpless_time || 0, duration);
    game._sleeping_time = Math.max(game._sleeping_time || 0, duration + 1);
    if (heroIsDeaf() || game.flags?.acoustics === false)
        return { message: 'You fall asleep.', duration };
    return { message: 'You hear sinister laughter as you fall asleep...', duration };
}

function remainingFoodNutrition(item) {
    if (item?.oeaten > 0) return item.oeaten;
    return foodObjectNutrition(item);
}

function ensureFoodOeaten(item) {
    if (!item) return 0;
    if (!(item.oeaten > 0)) item.oeaten = foodObjectNutrition(item);
    return item.oeaten || 0;
}

function consumeOeaten(item, amount) {
    if (!item?.oeaten) return;
    if (amount > 0) item.oeaten = Math.floor(item.oeaten / (2 ** amount));
    else if (amount < 0) item.oeaten += amount;
    if (item.oeaten <= 0) item.oeaten = 1;
}

function roundDivPositive(numerator, denominator) {
    if (!(denominator > 0)) return 0;
    return Math.trunc((numerator + Math.trunc(denominator / 2)) / denominator);
}

function refreshInventoryObjectLine(item) {
    if (item) item.line = normalInventoryLine({ ...item, line: '' });
}

function clearPretouchedFood(item) {
    if (!item) return;
    delete item._rotten_check_touched;
    delete item._rotten_check_touched_id;
}

function pretouchFoodForRottenCheck(item) {
    if (!item || item._rotten_check_touched) return;
    item._rotten_check_touched = 1;
    if ((item.quan || 1) > 1)
        item._rotten_check_touched_id = next_ident();
}

function takePretouchedFoodId(item) {
    if (!item) return null;
    const id = item._rotten_check_touched_id ?? null;
    clearPretouchedFood(item);
    return id;
}

function costlyBiteFood(item, { floorObject = false } = {}) {
    if (!item || item.oeaten > 0) return false;
    if (floorObject) {
        const x = item.ox ?? game.u?.ux;
        const y = item.oy ?? game.u?.uy;
        const shkp = shopkeeperForCostlySpot(x, y);
        if (!shopkeeperInHisShop(shkp)) return false;
        const entry = shopBillEntryForObject(shkp, item);
        const price = entry ? shopBillEntryTotal(entry) : shopItemPrice(item, x, y);
        if (!(price > 0)) return false;
        if (!entry && !addObjectToShopBill(shkp, item, price, { useup: true }))
            return shopBillIsFull(shkp) ? markAlteredShopObjectNoCharge(item) : false;
        const billed = markObjectShopBillUsedUp(item, shkp);
        if (billed) item.no_charge = true;
        return billed;
    }
    if (!item.unpaid && !shopkeeperOwningBillEntry(item).entry) return false;
    return markObjectShopBillUsedUp(item);
}

function touchInventoryFood(item) {
    if (!item) return item;
    const firstBite = !(item.oeaten > 0);
    if ((item.quan || 1) <= 1) {
        clearPretouchedFood(item);
        if (firstBite) costlyBiteFood(item);
        ensureFoodOeaten(item);
        refreshInventoryObjectLine(item);
        return item;
    }
    const id = takePretouchedFoodId(item) ?? next_ident();
    const split = { ...item, id, letter: nextInventoryLetter(), quan: 1, line: '' };
    clearPretouchedFood(split);
    delete split.oeaten;
    item.quan--;
    if (item.unpaid && !splitCarriedObjectShopBill(item, split, 1))
        clearObjectShopBillState(split);
    refreshInventoryObjectLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
    if (firstBite) costlyBiteFood(split);
    ensureFoodOeaten(split);
    refreshInventoryObjectLine(split);
    game.inventory.push(split);
    return split;
}

function touchFloorFood(item) {
    if (!item) return item;
    const firstBite = !(item.oeaten > 0);
    if ((item.quan || 1) > 1) {
        const rest = { ...item, id: takePretouchedFoodId(item) ?? next_ident(), quan: (item.quan || 1) - 1 };
        clearPretouchedFood(rest);
        delete rest.oeaten;
        item.quan = 1;
        if (item.unpaid && !splitCarriedObjectShopBill(item, rest, rest.quan || 1))
            clearObjectShopBillState(rest);
        game.level.objects ??= [];
        game.level.objects.push(rest);
    } else {
        clearPretouchedFood(item);
    }
    if (firstBite) costlyBiteFood(item, { floorObject: true });
    ensureFoodOeaten(item);
    newsym(item.ox, item.oy);
    return item;
}

function touchEatenFood(item, floorObject = false) {
    return floorObject ? touchFloorFood(item) : touchInventoryFood(item);
}

function delayedFoodVictualSpec(item) {
    if (!item) return null;
    const kind = objectKindKey(item).replace(/^partly eaten /, '');
    if ((item.foodRoll || 1000) <= 140)
        return { ...CARRIED_DELAYED_FOOD_VICTUALS.get('tripe ration'), kind: 'tripe ration' };
    const spec = CARRIED_DELAYED_FOOD_VICTUALS.get(kind);
    if (spec) return { ...spec, kind };
    for (const [entryKind, entrySpec] of CARRIED_DELAYED_FOOD_VICTUALS) {
        if (entrySpec.otyp != null && item.otyp === entrySpec.otyp)
            return { ...entrySpec, kind: entryKind };
    }
    return null;
}

function delayOneFoodVictualSpec(item) {
    if (!item) return null;
    const kind = objectKindKey(item).replace(/^partly eaten /, '');
    const spec = DELAY_ONE_FOOD_VICTUALS.get(kind);
    if (spec) return { ...spec, kind, finishName: kind === 'slime mold' ? slimeMoldNameForObject(item) : spec.finishName };
    for (const [entryKind, entrySpec] of DELAY_ONE_FOOD_VICTUALS) {
        if (entrySpec.otyp != null && item.otyp === entrySpec.otyp)
            return { ...entrySpec, kind: entryKind, finishName: entryKind === 'slime mold' ? slimeMoldNameForObject(item) : entrySpec.finishName };
    }
    return null;
}

function ordinaryFoodVictualSpec(item) {
    return delayOneFoodVictualSpec(item) || delayedFoodVictualSpec(item);
}

function delayedFoodCanAgeRot(spec) {
    return spec?.kind !== 'lembas wafer' && spec?.kind !== 'cram ration';
}

function delayedFoodShouldBeRotten(item, spec) {
    if (!item || !spec) return false;
    if (spec.kind === 'fortune cookie') return false;
    if (item.cursed) return true;
    if (!delayedFoodCanAgeRot(spec)) return false;
    if (item.age == null) item.age = game.moves || 1;
    const ageLimit = item.blessed ? 50 : 30;
    return (game.moves || 1) - item.age > ageLimit && (item.orotten || !rn2(7));
}

function heroFoodRaceOrPolyName() {
    const formName = String(polyselfForm()?.name || '').toLowerCase();
    if (formName) return formName;
    return String(game.urace?.noun || game.urace?.adj || game._startup_race || 'human').toLowerCase();
}

function heroFoodIsElf() {
    const name = heroFoodRaceOrPolyName();
    return name === 'elven' || name.includes('elf');
}

function heroFoodIsOrc() {
    const name = heroFoodRaceOrPolyName();
    return name === 'orcish' || /(?:^|[-\s])orc(?:$|[-\s])/.test(name);
}

function heroFoodIsDwarf() {
    const name = heroFoodRaceOrPolyName();
    return name === 'dwarven' || name === 'dwarvish' || /^dwarv/.test(name)
        || /(?:^|[-\s])dwarf(?:$|[-\s])/.test(name);
}

function heroFoodIsUndead() {
    const form = polyselfForm() || {};
    return !!(form.undead || game.u?.undead || game.u?.isUndead);
}

function heroFoodIsWereForm() {
    const form = polyselfForm() || {};
    const name = String(form.name || '').toLowerCase();
    return !!(form.wereHuman || form.wereAnimal || form.were || form.lycanthrope || /^were/.test(name));
}

function heroFoodIsCarnivorousNonHumanoid() {
    const form = polyselfFormWithDiet() || {};
    return !!(form.carnivorous || form.carnivore) && form.humanoid === false;
}

function heroTripeVomitingExempt() {
    const role = String(game.urole?.name?.m || game._startup_role || '').toLowerCase();
    return heroFoodIsOrc() || role.includes('cave');
}

function delayedTripeFirstBiteMessage(spec) {
    if (heroFoodIsCarnivorousNonHumanoid())
        return 'This tripe ration is surprisingly good!';
    if (heroFoodIsOrc())
        return heroIsHallucinating() ? 'Tastes great!  Less filling!' : 'Mmm, tripe... not bad!';
    if (game.u) game.u.uexp = (game.u.uexp || 0) + 1;
    if (rn2(2) && !heroTripeVomitingExempt())
        addHeroVomiting(rn1(spec?.delay || 1, 14));
    return 'Yak - dog food!';
}

function delayedPearFirstBiteMessage() {
    if (!heroIsHallucinating()) return 'Core dumped.';
    const roll = rnd(100);
    const cause = roll <= 75 ? 'Segmentation fault' : roll <= 99 ? 'Bus error' : "Yo' mama";
    return `${cause} -- core dumped.`;
}

function delayedGarlicFirstBiteMessage(reqtime) {
    if (heroFoodIsUndead()) {
        addHeroVomiting(rn1(Math.max(1, Math.trunc(reqtime || 1)), 5));
        return '';
    }
    scareNearbyOlfactoryMonstersWithGarlic();
    return 'This clove of garlic is delicious!';
}

function carrotPostEffect() {
    if (!game.u) return {};
    const wasBlind = heroIsBlind();
    const creamed = Math.max(0, Math.trunc(game.u.ucreamed || 0));
    if (creamed > 0) {
        game.u.blind = true;
        game.u._blindTimeout = creamed;
        return {};
    }
    game.u.blind = false;
    game.u._blindTimeout = 0;
    game.u.ucreamed = 0;
    removeHeroStatusSuffix('Blind');
    if (!wasBlind) return {};
    return { message: heroIsHallucinating() ? 'Far out!  Everything is all cosmic again!' : 'You can see again.' };
}

function wolfsbanePostEffect() {
    if (!game.u) return {};
    const lycanthropic = game.u.ulycn != null && game.u.ulycn !== -1 && game.u.ulycn !== false;
    if (!lycanthropic && !heroFoodIsWereForm()) return {};
    game.u.ulycn = -1;
    game.u.lycanthrope = false;
    game.u.were = false;
    game.u._lycanthropyTimeout = 0;
    return { message: 'You feel purified.' };
}

function heroIsVomiting() {
    return !!(game.u?.vomiting || (game.u?._vomitingTimeout || 0) > 0
        || (game.u?._statusSuffix || '').includes('Vom'));
}

function clearHeroVomiting() {
    if (!game.u) return;
    game.u.vomiting = false;
    game.u._vomitingTimeout = 0;
    removeHeroStatusSuffix('Vom');
}

function heroIsSick() {
    const u = game.u;
    if (!u) return false;
    return !!(u.sick || u.Sick || u.sickness || u.terminallySick || u.ill
        || (u._sickTimeout || 0) > 0 || (u._sicknessTimeout || 0) > 0
        || (u._statusSuffix || '').includes('Sick') || (u._statusSuffix || '').includes('Ill'));
}

function clearHeroSickness() {
    if (!game.u) return;
    game.u.sick = false;
    game.u.Sick = false;
    game.u.sickness = false;
    game.u.terminallySick = false;
    game.u.ill = false;
    game.u._sickTimeout = 0;
    game.u._sicknessTimeout = 0;
    removeHeroStatusSuffix('Sick');
    removeHeroStatusSuffix('Ill');
}

function eucalyptusPostEffect(touched) {
    if (touched?.cursed) return {};
    const messages = [];
    if (heroIsSick()) {
        clearHeroSickness();
        messages.push('You feel cured.  What a relief!');
    }
    if (heroIsVomiting()) {
        clearHeroVomiting();
        messages.push('You feel much less nauseated now.');
    }
    return messages.length ? { message: messages.join('  ') } : {};
}

function royalJellyFatalPostEffect(messages, result = {}) {
    const clearStoningOnLifeSaving = !!result.clearStoningOnLifeSaving;
    if (consumeLifeSavingAmulet({ clearStoning: clearStoningOnLifeSaving })) {
        if (game.u) game.u.uhp = 0;
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game._command_mode = 'lifeSavingMore';
        return {
            message: [...messages, 'You die...', 'But wait...  Your medallion begins to glow!'].join('  '),
            more: true,
            fatal: true,
            lifesaved: true,
            commandMode: 'lifeSavingMore',
            move: 0,
        };
    }

    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    if (game.context) game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._command_mode = 'deathDieMore';
    prepareDeathBones();
    const suppressDieMessage = result.suppressDieMessage
        || String(game._death_cause || '').startsWith('petrified by ');
    return {
        message: suppressDieMessage ? messages.join('  ') : [...messages, 'You die...'].join('  '),
        more: true,
        fatal: true,
        commandMode: 'deathDieMore',
        move: 0,
    };
}

function royalJellyDelayedPostEffect(touched) {
    const result = royalJellyPostEffects(touched);
    if (result.died) return royalJellyFatalPostEffect(result.messages || [], result);
    return result.messages?.length ? { message: result.messages.join('  ') } : {};
}

function adjustedDelayedFoodBiteHunger(spec, baseNutrition) {
    let nutrition = Math.trunc(baseNutrition || 0);
    if (!(nutrition > 0)) return 0;
    if (spec?.kind === 'lembas wafer') {
        const adjustment = Math.trunc((nutrition + 2) / 4);
        if (heroFoodIsElf()) nutrition += adjustment;
        else if (heroFoodIsOrc()) nutrition -= adjustment;
    } else if (spec?.kind === 'cram ration' && heroFoodIsDwarf()) {
        nutrition += Math.trunc((nutrition + 3) / 6);
    }
    return Math.max(nutrition, 1);
}

function delayedFoodFirstBiteMessage(spec, hunger, { reqtime = spec?.delay || 1, food = null } = {}) {
    if (!spec) return '';
    if (spec.rationFeedback) {
        if ((hunger ?? 900) <= 200) return 'This food really hits the spot!';
        if ((hunger ?? 900) < 700) return 'This satiates your stomach!';
        return '';
    }
    if (spec.kind === 'lembas wafer') {
        if (heroFoodIsOrc()) return '!#?&* elf kibble!';
        if (heroFoodIsElf()) return 'A little goes a long way.';
    }
    if (spec.kind === 'tripe ration' || spec.kind === 'tripe')
        return delayedTripeFirstBiteMessage({ ...spec, delay: reqtime || spec.delay });
    if (spec.kind === 'apple') return 'Delicious!  Must be a Macintosh!';
    if (spec.kind === 'pear') return delayedPearFirstBiteMessage();
    if (spec.kind === 'clove of garlic') return delayedGarlicFirstBiteMessage(reqtime);
    if (spec.kind === 'slime mold' && !food?.cursed && Number(food?.spe) === currentFruitId())
        return `My, this is a ${heroIsHallucinating() ? 'primo' : 'yummy'} ${slimeMoldNameForObject(food)}!`;
    return `This ${spec.finishName || spec.kind} is ${spec.bland ? 'bland.' : heroIsHallucinating() ? 'gnarly!' : 'delicious!'}`;
}

function applyDelayedFoodPostEffect(touched, spec) {
    if (spec?.kind === 'fortune cookie') {
        queueFortuneCookieRumor(touched);
        return { more: true };
    }
    if (spec?.kind === 'apple')
        return cursedAppleSleepPostEffect(touched) || {};
    if (spec?.kind === 'carrot')
        return carrotPostEffect();
    if (spec?.kind === 'sprig of wolfsbane')
        return wolfsbanePostEffect();
    if (spec?.kind === 'eucalyptus leaf')
        return eucalyptusPostEffect(touched);
    if (spec?.kind === 'lump of royal jelly')
        return royalJellyDelayedPostEffect(touched);
    return {};
}

function delayedFoodVictualState(touched, spec) {
    const fullNutrition = foodObjectNutrition(touched) || FOOD_NUTRITION.get(spec.kind) || 0;
    const reqtime = roundDivPositive(spec.delay * remainingFoodNutrition(touched), fullNutrition);
    const biteNutrition = reqtime > 0 && touched.oeaten >= reqtime
        ? Math.trunc(touched.oeaten / reqtime)
        : 0;
    const biteHunger = adjustedDelayedFoodBiteHunger(spec, biteNutrition);
    return { reqtime, biteNutrition, biteHunger };
}

function clearInterruptedEatingVictualState() {
    game._eating_interrupted = 0;
    game._eating_paused_turns_remaining = 0;
}

function clearDelayedEatingFullnessState() {
    game._eating_canchoke = 0;
    game._eating_fullwarn = 0;
    game._eating_nomovemsg = '';
}

function clearDelayedEatingVictualState() {
    game._eating_turns_remaining = 0;
    game._eating_finish_message = '';
    game._eating_floor_object = null;
    game._eating_floor_object_direct_useup = 0;
    game._eating_inventory_object = null;
    game._eating_bite_nutrition = 0;
    game._eating_bite_hunger = 0;
    game._eating_nutrition = 0;
    game._eating_newt_buzz = 0;
    clearInterruptedEatingVictualState();
    clearDelayedEatingFullnessState();
}

function declineDelayedFoodContinuation() {
    if (!(game._eating_turns_remaining > 0)) return;
    game._eating_paused_turns_remaining = game._eating_turns_remaining;
    game._eating_interrupted = 1;
    game._eating_turns_remaining = 0;
    game._eating_fullwarn = 0;
    game._eating_nomovemsg = '';
}

function pauseDelayedFoodAfterChoke(touched, {
    remainingTurns = 0,
    biteNutrition = 0,
    biteHunger = biteNutrition,
    floorObject = false,
    finishName = '',
} = {}) {
    if (remainingTurns <= 1) {
        if (floorObject) consumeOneFloorObject(touched);
        else {
            removeInventoryItem(touched);
            game._pet_food_scan_inventory = game.inventory || [];
        }
        clearDelayedEatingVictualState();
        return;
    }
    game._eating_turns_remaining = 0;
    game._eating_paused_turns_remaining = remainingTurns;
    game._eating_interrupted = 1;
    game._eating_finish_message = `You finish eating the ${finishName}.`;
    if (floorObject) {
        game._eating_floor_object = touched;
        game._eating_floor_object_direct_useup = 1;
    } else {
        game._eating_inventory_object = touched;
        game._pet_food_scan_inventory = game.inventory || [];
    }
    game._eating_bite_nutrition = biteNutrition;
    game._eating_bite_hunger = biteHunger;
    game._eating_nutrition = 0;
    game._eating_fullwarn = 0;
    game._eating_nomovemsg = '';
}

function isPausedDelayedFoodVictual(item, floorObject = false) {
    if (!game._eating_interrupted || !(game._eating_paused_turns_remaining > 0)) return false;
    if (!(game._eating_bite_nutrition > 0)) return false;
    if (floorObject)
        return game._eating_floor_object === item
            && (game.level?.objects || []).includes(item)
            && item.ox === game.u?.ux
            && item.oy === game.u?.uy;
    return game._eating_inventory_object === item
        && (game.inventory || []).includes(item);
}

function resumeDelayedFoodVictual(item, spec, { floorObject = false } = {}) {
    if (!isPausedDelayedFoodVictual(item, floorObject)) return null;

    const pausedTurns = Math.trunc(game._eating_paused_turns_remaining || 0);
    const touched = touchEatenFood(item, floorObject);
    const biteNutrition = Math.trunc(game._eating_bite_nutrition || 0);
    const biteHunger = Math.trunc(game._eating_bite_hunger || biteNutrition);
    const finishName = spec.finishName || spec.kind;
    const lastBite = pausedTurns <= 2;
    const message = lastBite
        ? 'You consume the last bite of your meal.'
        : 'You resume your meal.';
    if (!heroIsSatiatedForEating()) game._eating_canchoke = 0;
    game._eating_fullwarn = 0;

    let nutritionOutcome = { messages: [], prompt: false };
    if (biteNutrition > 0) {
        nutritionOutcome = addDelayedFoodBiteNutrition(biteHunger, {
            remainingAfterBite: Math.max(0, pausedTurns - 2),
            food: touched,
        });
        if (!nutritionOutcome.fatal && !nutritionOutcome.preBite) {
            consumeOeaten(touched, -biteNutrition);
            if (floorObject) newsym(touched.ox, touched.oy);
            else refreshInventoryObjectLine(touched);
        }
    }
    const combinedMessage = [message, ...(nutritionOutcome.messages || [])].filter(Boolean).join('  ');

    if (nutritionOutcome.choked) {
        clearInterruptedEatingVictualState();
        if (nutritionOutcome.recovered) {
            pauseDelayedFoodAfterChoke(touched, {
                remainingTurns: nutritionOutcome.preBite ? pausedTurns : Math.max(0, pausedTurns - 1),
                biteNutrition,
                biteHunger,
                floorObject,
                finishName,
            });
        } else {
            clearDelayedEatingVictualState();
        }
        return {
            message: combinedMessage,
            more: !!nutritionOutcome.more,
            commandMode: nutritionOutcome.fatal || nutritionOutcome.lifesaved
                ? (game._command_mode || null) : null,
            move: nutritionOutcome.move ?? 0,
            finished: false,
        };
    }

    clearInterruptedEatingVictualState();
    if (lastBite || biteNutrition <= 0) {
        if (floorObject) consumeOneFloorObject(touched);
        else {
            removeInventoryItem(touched);
            game._pet_food_scan_inventory = game.inventory || [];
        }
        const finishMessage = game._eating_nomovemsg || `You finish eating the ${finishName}.`;
        clearDelayedEatingVictualState();
        return {
            message: `${combinedMessage}  ${finishMessage}`,
            more: false,
            move: 1,
            finished: true,
        };
    }

    game._eating_turns_remaining = pausedTurns - 1;
    game._eating_finish_message = `You finish eating the ${finishName}.`;
    if (floorObject) {
        game._eating_floor_object = touched;
        game._eating_floor_object_direct_useup = 1;
    } else {
        game._eating_inventory_object = touched;
        game._pet_food_scan_inventory = game.inventory || [];
    }
    game._eating_bite_nutrition = biteNutrition;
    game._eating_bite_hunger = biteHunger;
    game._eating_nutrition = 0;

    return {
        message: combinedMessage,
        more: false,
        commandMode: nutritionOutcome.prompt ? 'continueEatingPrompt' : null,
        move: 1,
        finished: false,
        touched,
    };
}

function startDelayedFoodVictual(item, spec, { floorObject = false } = {}) {
    const resumed = resumeDelayedFoodVictual(item, spec, { floorObject });
    if (resumed) return resumed;
    clearDelayedEatingVictualState();

    const hungerBeforeBite = game.u?.uhunger ?? 900;
    const alreadyPartlyEaten = item?.oeaten > 0;
    const touched = touchEatenFood(item, floorObject);
    recordFoodConduct(touched);
    const finishName = spec.finishName || spec.kind;
    let message = alreadyPartlyEaten ? `You begin eating the partly eaten ${finishName}.` : '';
    let more = false;
    let processTimeWithMore = false;
    let move = 1;
    let rottenFirstBite = false;

    if (delayedFoodShouldBeRotten(touched, spec)) {
        const rotten = rottenFoodEffect();
        message = rotten.message;
        more = true;
        processTimeWithMore = true;
        rottenFirstBite = true;
        consumeOeaten(touched, 1);
        if (floorObject) newsym(touched.ox, touched.oy);
        else refreshInventoryObjectLine(touched);
        if (rotten.rottenSleepDuration) {
            touched.orotten = true;
            if (!floorObject) game._pet_food_scan_inventory = game.inventory || [];
            return {
                message,
                more,
                processTimeWithMore,
                move: rotten.rottenSleepDuration + 1,
                finished: false,
                touched,
            };
        }
    }

    const { reqtime, biteNutrition, biteHunger } = delayedFoodVictualState(touched, spec);
    if (!alreadyPartlyEaten && !rottenFirstBite)
        message = delayedFoodFirstBiteMessage(spec, hungerBeforeBite, { reqtime, food: touched });
    game._eating_canchoke = heroIsSatiatedForEating();
    game._eating_fullwarn = 0;
    game._eating_nomovemsg = '';

    let nutritionOutcome = { messages: [], prompt: false };
    if (biteNutrition > 0) {
        nutritionOutcome = addDelayedFoodBiteNutrition(biteHunger, {
            remainingAfterBite: Math.max(0, reqtime - 1),
            food: touched,
        });
        if (!nutritionOutcome.fatal && !nutritionOutcome.preBite) {
            consumeOeaten(touched, -biteNutrition);
            if (floorObject) newsym(touched.ox, touched.oy);
            else refreshInventoryObjectLine(touched);
        }
    }
    if (nutritionOutcome.messages?.length)
        message = [message, ...nutritionOutcome.messages].filter(Boolean).join('  ');

    if (nutritionOutcome.choked) {
        if (nutritionOutcome.recovered) {
            pauseDelayedFoodAfterChoke(touched, {
                remainingTurns: reqtime,
                biteNutrition,
                biteHunger,
                floorObject,
                finishName,
            });
        } else {
            clearDelayedEatingVictualState();
        }
        return {
            message,
            more: !!nutritionOutcome.more,
            commandMode: nutritionOutcome.fatal || nutritionOutcome.lifesaved
                ? (game._command_mode || null) : null,
            move: nutritionOutcome.move ?? 0,
            finished: false,
            touched,
        };
    }

    if (reqtime <= 1 || biteNutrition <= 0) {
        const postEffect = applyDelayedFoodPostEffect(touched, spec);
        let finishMessage = reqtime <= 1 && alreadyPartlyEaten && !rottenFirstBite
            ? `You eat the partly eaten ${finishName}.`
            : message;
        if (postEffect.message) finishMessage = [finishMessage, postEffect.message].filter(Boolean).join('  ');
        if (postEffect.more) more = true;
        if (postEffect.duration) move = postEffect.duration;
        if (!(postEffect.fatal && !postEffect.lifesaved)) {
            if (floorObject) consumeOneFloorObject(touched);
            else {
                removeInventoryItem(touched);
                game._pet_food_scan_inventory = game.inventory || [];
            }
        }
        return {
            message: finishMessage,
            more,
            processTimeWithMore: postEffect.fatal ? false : processTimeWithMore,
            commandMode: postEffect.commandMode || null,
            move: postEffect.move ?? move,
            finished: true,
        };
    }

    game._eating_turns_remaining = reqtime;
    game._eating_finish_message = `You finish eating the ${finishName}.`;
    if (floorObject) {
        game._eating_floor_object = touched;
        game._eating_floor_object_direct_useup = 1;
    } else {
        game._eating_inventory_object = touched;
        game._pet_food_scan_inventory = game.inventory || [];
    }
    game._eating_bite_nutrition = biteNutrition;
    game._eating_bite_hunger = biteHunger;
    game._eating_nutrition = 0;

    return {
        message,
        more,
        processTimeWithMore,
        commandMode: nutritionOutcome.prompt ? 'continueEatingPrompt' : null,
        move,
        finished: false,
        touched,
    };
}

function startCarriedDelayedFoodVictual(item, spec) {
    return startDelayedFoodVictual(item, spec);
}

function applyWishedPartlyEaten(item) {
    if (!(item?.cls === 'food' || item?.otyp === FOOD_CLASS || item?.otyp === 'corpse' || item?.otyp === CORPSE)) return;
    const nutrition = foodObjectNutrition(item);
    if (nutrition <= 1) return;
    item.oeaten = nutrition;
    consumeOeaten(item, 1);
}

function maybePartlyEatenFoodName(obj, name) {
    if (!(obj?.oeaten > 0) || name.startsWith('partly eaten ')) return name;
    return `partly eaten ${name}`;
}

function foodBucPrefix(obj) {
    if (!(game._startup_role === 'Priest' || obj?.bknown)) return '';
    return obj?.cursed ? 'cursed ' : obj?.blessed ? 'blessed ' : '';
}

function eggObjectName(obj) {
    const buc = foodBucPrefix(obj);
    const plural = (obj?.quan || 1) > 1;
    let name = plural ? 'eggs' : 'egg';
    if (obj?.corpsenm?.name && (obj.known || obj.eggKnown)) {
        name = `${obj.corpsenm.name} ${plural ? 'eggs' : 'egg'}`;
        if (!plural && obj.spe === 1) name = `${name} (laid by you)`;
    }
    return `${buc}${maybePartlyEatenFoodName(obj, name)}`;
}

function globSizePrefix(obj) {
    const weight = obj?.owt ?? 20;
    return weight <= 100 ? 'small'
        : weight <= 300 ? 'medium'
            : weight <= 500 ? 'large'
                : 'very large';
}

function globObjectName(obj) {
    const name = obj?.globName || obj?.kind || 'glob';
    return maybePartlyEatenFoodName(obj, `${globSizePrefix(obj)} ${name}`);
}

function makeBlankScrollWishObject() {
    const otmp = mksobj(SCR_BLANK_PAPER, true, false);
    return Object.assign(otmp, {
        cls: 'scroll',
        glyph: '?',
        kind: 'blank paper',
        known: false,
        wishedfor: true,
    });
}

function makeWishedKnownScrollObject(scrollIndex, wishedLabel = '') {
    const scrollName = IDENTIFIED_SCROLL_NAMES[scrollIndex];
    const label = game._object_descriptions?.scrolls?.[scrollIndex] || wishedLabel || 'ZELGO MER';
    const otmp = mksobj(SCROLL_CLASS, true, false);
    return Object.assign(otmp, {
        cls: 'scroll',
        glyph: '?',
        kind: `scroll labeled ${label}`,
        actualKind: `scroll of ${scrollName}`,
        scrollIndex,
        known: false,
        wishedfor: true,
    });
}

function makeBlankSpellbookWishObject() {
    const otmp = mksobj(SPBOOK_NO_NOVEL, true, false);
    return Object.assign(otmp, {
        cls: 'spellbook',
        glyph: '+',
        kind: 'spellbook of blank paper',
        spellName: '',
        appearance: 'plain',
        known: false,
        wishedfor: true,
    });
}

function makeNovelWishObject() {
    const otmp = mksobj(SPBOOK_NO_NOVEL, true, false);
    return Object.assign(otmp, {
        cls: 'spellbook',
        glyph: '+',
        kind: 'novel',
        actualKind: 'novel',
        spellName: '',
        appearance: '',
        known: false,
        wishedfor: true,
    });
}

function makeFakeAmuletOfYendorWishObject() {
    const otmp = mksobj(AMULET_CLASS, true, false);
    return Object.assign(otmp, {
        cls: 'amulet',
        glyph: '"',
        kind: 'Amulet of Yendor',
        actualKind: 'cheap plastic imitation of the Amulet of Yendor',
        appearance: 'Amulet of Yendor',
        known: false,
        fakeAmuletOfYendor: true,
        unique: true,
        wishedfor: true,
    });
}

function makeRealAmuletOfYendorWishObject() {
    const otmp = mksobj(AMULET_CLASS, true, false);
    return Object.assign(otmp, {
        cls: 'amulet',
        glyph: '"',
        kind: 'Amulet of Yendor',
        actualKind: 'Amulet of Yendor',
        appearance: 'Amulet of Yendor',
        known: true,
        realAmuletOfYendor: true,
        unique: true,
        wishedfor: true,
    });
}

function makeAmuletOfYendorWishObject(lowerName, qualifiers = {}) {
    const name = String(lowerName || '').trim();
    if (!/\bamulet of yendor\b/.test(name)) return null;
    const fakeNamePrefix = /^(?:cheap|plastic|imitation)\b/.test(name);
    if (qualifiers.fakeAmulet || fakeNamePrefix || !game.flags?.debug)
        return makeFakeAmuletOfYendorWishObject();
    return makeRealAmuletOfYendorWishObject();
}

const GENDERED_CORPSTAT_MONSTER_NAMES = new Map([
    ['dwarf leader', { male: 'dwarf lord', female: 'dwarf lady', neutral: 'dwarf leader' }],
    ['dwarf ruler', { male: 'dwarf king', female: 'dwarf queen', neutral: 'dwarf ruler' }],
    ['kobold leader', { male: 'kobold lord', female: 'kobold lady', neutral: 'kobold leader' }],
    ['gnome leader', { male: 'gnome lord', female: 'gnome lady', neutral: 'gnome leader' }],
    ['gnome ruler', { male: 'gnome king', female: 'gnome queen', neutral: 'gnome ruler' }],
    ['ogre leader', { male: 'ogre lord', female: 'ogre lady', neutral: 'ogre leader' }],
    ['ogre tyrant', { male: 'ogre king', female: 'ogre queen', neutral: 'ogre tyrant' }],
    ['vampire leader', { male: 'vampire lord', female: 'vampire lady', neutral: 'vampire leader' }],
    ['elf-noble', { male: 'elf-lord', female: 'elf-lady', neutral: 'elf-noble' }],
    ['elven monarch', { male: 'Elvenking', female: 'Elvenqueen', neutral: 'elven monarch' }],
    ['amorous demon', { male: 'incubus', female: 'succubus', neutral: 'amorous demon' }],
]);

const GENDERED_CORPSTAT_MONSTER_ALIASES = new Map();
for (const [neutral, names] of GENDERED_CORPSTAT_MONSTER_NAMES) {
    GENDERED_CORPSTAT_MONSTER_ALIASES.set(names.neutral.toLowerCase(), { monsterName: neutral, gender: null });
    GENDERED_CORPSTAT_MONSTER_ALIASES.set(names.male.toLowerCase(), { monsterName: neutral, gender: 'male' });
    GENDERED_CORPSTAT_MONSTER_ALIASES.set(names.female.toLowerCase(), { monsterName: neutral, gender: 'female' });
}
const RANDOM_MONSTER_BY_LOWER_NAME = new Map(
    [...RANDOM_MONSTER_BY_NAME.entries()].map(([name, data]) => [String(name).toLowerCase(), data]),
);

const WISHED_MONSTER_FALLBACKS = new Map([
    ['human', { name: 'human', mlet: '@', glyph: '@', human: true, neuter: false }],
    ['mail daemon', { name: 'mail daemon', mlet: '&', glyph: '&', noCorpse: true, neuter: false }],
    ['medusa', { name: 'Medusa', mlet: '@', glyph: '@', human: true, unique: true, female: true }],
    ['wizard of yendor', { name: 'Wizard of Yendor', mlet: '@', glyph: '@', human: true, unique: true, male: true }],
    ['lord surtur', { name: 'Lord Surtur', mlet: 'H', glyph: 'H', unique: true, male: true }],
    ['chromatic dragon', { name: 'Chromatic Dragon', mlet: 'D', glyph: 'D', unique: true, female: true }],
    ['student', { name: 'student', mlet: '@', glyph: '@', human: true, guardian: true, neuter: false }],
]);

const WISHED_WERE_BEAST_FORMS = new Map([
    ['rat wererat', 'wererat'],
    ['jackal werejackal', 'werejackal'],
    ['wolf werewolf', 'werewolf'],
    ['wererat', 'wererat'],
    ['werejackal', 'werejackal'],
    ['werewolf', 'werewolf'],
]);

const WISHED_WERE_BEAST_DATA = new Map([
    ['wererat', { glyph: 'r', mlet: 'r', color: CLR_BROWN, verysmall: true }],
    ['werejackal', { glyph: 'd', mlet: 'd', color: CLR_BROWN }],
    ['werewolf', { glyph: 'd', mlet: 'd', color: CLR_GRAY }],
]);

function wishedWereBeastByName(lowerName) {
    const wereName = WISHED_WERE_BEAST_FORMS.get(lowerName);
    if (!wereName) return null;
    const human = monsterByRndName(wereName) || RANDOM_MONSTER_BY_NAME.get(wereName) || {};
    return {
        ...human,
        ...WISHED_WERE_BEAST_DATA.get(wereName),
        name: wereName,
        noCorpse: true,
        nohands: true,
        animal: true,
        wereHuman: false,
        wereBeast: true,
    };
}

function wishedMonsterByName(name) {
    const raw = String(name || '').trim();
    if (!raw) return null;
    const lower = raw.toLowerCase();
    if (lower === 'elf') return { name: 'elf', weight: 800, mlet: '@', glyph: '@', color: CLR_BRIGHT_GREEN, neuter: false, elf: true };
    if (lower === 'human wererat') return { ...(monsterByRndName('wererat') || {}), name: 'wererat', wereHuman: true };
    if (lower === 'human werejackal') return { ...(monsterByRndName('werejackal') || {}), name: 'werejackal', wereHuman: true };
    if (lower === 'human werewolf') return { ...(monsterByRndName('werewolf') || {}), name: 'werewolf', wereHuman: true };
    const wereBeast = wishedWereBeastByName(lower);
    if (wereBeast) return wereBeast;
    return monsterByRndName(raw)
        || RANDOM_MONSTER_BY_NAME.get(raw)
        || RANDOM_MONSTER_BY_LOWER_NAME.get(lower)
        || RANDOM_MONSTER_BY_LOWER_NAME.get(lower.replace(/\s+/g, '-'))
        || WISHED_MONSTER_FALLBACKS.get(lower)
        || null;
}

function stripWishedMonsterGenderPrefix(name) {
    const trimmed = String(name || '').trim();
    const match = trimmed.match(/^(female|male|neuter)\s+(.+)$/i);
    if (!match) return { name: trimmed, gender: null };
    return { name: match[2].trim(), gender: match[1].toLowerCase() };
}

function resolveWishedCorpstatMonsterName(monsterName, requestedGender = null) {
    const lowerName = String(monsterName || '').trim().toLowerCase();
    const alias = GENDERED_CORPSTAT_MONSTER_ALIASES.get(lowerName);
    if (!alias) return { monsterName: lowerName, requestedGender };
    return {
        monsterName: alias.monsterName,
        requestedGender: alias.gender || requestedGender,
    };
}

function wishedCorpstatSpe(monster, requestedGender = null) {
    if (monster?.neuter) return CORPSTAT_NEUTER;
    if (requestedGender === 'female' && !monster?.male) return CORPSTAT_FEMALE;
    if (requestedGender === 'male' && !monster?.female) return CORPSTAT_MALE;
    if (monster?.female) return CORPSTAT_FEMALE;
    if (monster?.male) return CORPSTAT_MALE;
    return rn2(2) ? CORPSTAT_MALE : CORPSTAT_FEMALE;
}

function corpstatDisplayMonsterName(obj) {
    const baseName = String(obj?.corpsenm?.name || 'monster');
    const genderNames = GENDERED_CORPSTAT_MONSTER_NAMES.get(baseName.toLowerCase());
    if (!genderNames) return baseName;
    const gender = (obj.spe || 0) & CORPSTAT_GENDER;
    if (gender === CORPSTAT_FEMALE) return genderNames.female;
    if (gender === CORPSTAT_MALE) return genderNames.male;
    return genderNames.neutral;
}

function corpseObjectName(obj) {
    const monsterName = corpstatDisplayMonsterName(obj);
    return `${monsterName} ${(obj?.quan || 1) > 1 ? 'corpses' : 'corpse'}`;
}

function monsterIndefiniteName(name) {
    const monsterName = String(name || 'monster');
    return `${/^[aeiou]/i.test(monsterName) ? 'an' : 'a'} ${monsterName}`;
}

function corpstatMonsterIndefiniteName(obj) {
    const name = corpstatDisplayMonsterName(obj);
    return obj?.corpsenm?.unique ? name : monsterIndefiniteName(name);
}

function upstartText(text) {
    const value = String(text || '');
    return value ? value.charAt(0).toUpperCase() + value.slice(1) : value;
}

function statueAnimationVerb(mon) {
    const data = mon?.data || {};
    if (data.nonliving || data.vampshifter) return 'moves';
    return 'comes to life';
}

function animatedMonsterIndefiniteName(mon, fallback = 'monster') {
    const data = mon?.data || {};
    const name = data.name || fallback;
    return data.unique ? name : monsterIndefiniteName(name);
}

function moveStatueContentsToMonster(statue, mon) {
    const contents = statue?.contents || [];
    while (contents.length) {
        const item = contents.shift();
        item.contained = false;
        delete item.ox;
        delete item.oy;
        delete item.line;
        add_to_minv(mon, item);
    }
    mon.hasInventory = !!(mon.minvent || []).length;
}

function countStatueShopContentsForMessage(obj, x, y, everything = true, seen = new Set()) {
    if (!obj || seen.has(obj)) return 0;
    seen.add(obj);
    const shoppy = !everything && !!shopkeeperForCostlySpot(x, y);
    let count = 0;
    for (const child of globContents(obj)) {
        count += countStatueShopContentsForMessage(child, x, y, everything, seen);
        if (everything || child.unpaid || (shoppy && !child.no_charge)) count++;
    }
    return count;
}

function statueShatterShopDebtMessage(statue, x, y, mon) {
    if (!statue || statue.no_charge || game._monster_moving) return '';
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shkp || !shopkeeperInHisShop(shkp) || mon === shkp) return '';
    const wasUnpaid = !!statue.unpaid;
    const contentCount = countStatueShopContentsForMessage(statue, x, y, true);
    const unpaidContentCount = countStatueShopContentsForMessage(statue, x, y, false);
    const charges = lostShopMerchandiseChargesForObject({ ox: x, oy: y }, statue, shkp);
    if (!charges.size) return '';
    const chargeEntries = [...charges.entries()];
    const displayShkp = chargeEntries.length === 1
        ? chargeEntries[0][0]
        : charges.has(shkp) ? shkp : chargeEntries[0][0];
    const beforeCredit = Math.max(0, Math.trunc(Number(displayShkp.credit || 0)));

    const peaceful = shkp.mpeaceful !== 0;
    let remaining = 0;
    for (const [owner, value] of chargeEntries)
        remaining += chargeShopkeeperForLostMerchandise(owner, value, { peaceful });
    if (!peaceful) {
        if (!game.u?.blind && couldsee(displayShkp.mx, displayShkp.my))
            return `${shopkeeperDisplayName(displayShkp)} booms: "${game.plname || 'Hero'}, you are a thief!"`;
        if (!heroIsDeaf()) return 'You hear a scream, "Thief!"';
        return '';
    }

    if (beforeCredit > 0 && !remaining) {
        const credit = Math.max(0, Math.trunc(Number(displayShkp.credit || 0)));
        return credit > 0
            ? `You have ${credit} ${shopCurrency(credit)} credit remaining.`
            : 'You have no credit remaining.';
    }

    if (!(remaining > 0)) return '';
    let message = `You ${beforeCredit > 0 ? 'still ' : ''}owe ${shopkeeperDisplayName(displayShkp)} ${remaining} ${shopCurrency(remaining)}`;
    if (unpaidContentCount > 0) {
        message += ` for ${wasUnpaid ? 'it and ' : ''}${contentCount > unpaidContentCount ? 'some of ' : ''}its contents`;
    } else if (!shopBillableGold(statue)) {
        message += ` for ${(statue.quan || 1) > 1 ? 'them' : 'it'}`;
    }
    return `${message}!`;
}

function randomHallucinatedMonsterName() {
    let halluIndex;
    do {
        halluIndex = rn2_on_display_rng(SPECIAL_PM + BOGUSMONSIZE);
    } while (halluIndex < SPECIAL_PM && !DISPLAY_MONSTER_HALLU_NAMES[halluIndex]);
    if (halluIndex >= SPECIAL_PM) return getbogusmon();
    rn2_on_display_rng(2);
    return DISPLAY_MONSTER_HALLU_NAMES[halluIndex] || 'monster';
}

function statueSearchAnimationMessage(statue, mon) {
    if (heroIsHallucinating())
        return `The ${randomHallucinatedMonsterName()} suddenly seems more animated.`;
    const spotted = !game.u?.blind && !mon.mundetected
        && (!mon.minvis || game.u?.seeInvisible)
        && couldsee(mon.mx, mon.my);
    const name = spotted ? animatedMonsterIndefiniteName(mon, corpstatDisplayMonsterName(statue)) : 'something';
    return `You find ${name} posing as a statue.`;
}

function floorStatueAt(x, y) {
    return (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y
        && (obj.kind === 'statue' || obj.otyp === STATUE)) || null;
}

function statueTrapAt(x, y) {
    return (game.level?.traps || []).find(trap =>
        trap.tx === x && trap.ty === y && trap.ttyp === STATUE_TRAP) || null;
}

function dropStatueContentsToFloor(statue, x, y) {
    const contents = statue?.contents || [];
    if (!contents.length || !game.level) return;
    while (contents.length) {
        const item = contents.shift();
        item.contained = false;
        item.ox = x;
        item.oy = y;
        item.hidden = false;
        item.transientProjectile = false;
        delete item.line;
        Object.assign(item, object_display(item));
        game.level.objects.push(item);
    }
    statue.contents = [];
}

export function breakStatueObject(statue, x = statue?.ox || 0, y = statue?.oy || 0) {
    if (!statue || !game.level) return false;
    const objects = game.level.objects || [];
    if (!objects.includes(statue)) return false;
    dropStatueContentsToFloor(statue, x, y);
    Object.assign(statue, {
        otyp: ROCK,
        ox: x,
        oy: y,
        quan: rn1(60, 7),
        cls: 'gem',
        kind: 'rock',
        actualKind: 'rock',
        singular: 'rock',
        plural: 'rocks',
        gemDescription: 'rock',
        spe: 0,
        ...object_display({ otyp: ROCK }),
    });
    delete statue.contents;
    delete statue.corpsenm;
    delete statue.historic;
    delete statue._death_remains;
    game.level.objects = objects.filter(obj => obj !== statue);
    game.level.objects.push(statue);
    newsym(x, y);
    return true;
}

function isPolearmItem(item) {
    if (!item) return false;
    const name = inventoryItemName(item).toLowerCase();
    return item.cls === 'weapon' && POLEARM_NAME_RE.test(name);
}

function isPickDigItem(item) {
    if (!item) return false;
    if (item.otyp === PICK_AXE || item.otyp === DWARVISH_MATTOCK) return true;
    return PICK_DIG_NAME_RE.test(inventoryItemName(item).toLowerCase());
}

function itemIsWielded(item) {
    return !!(item && (item.wielded || item.line?.includes('weapon in') || item.line?.includes('(wielded)')));
}

function isTwoHandedWieldItem(item) {
    const name = inventoryItemName(item).toLowerCase();
    return isPolearmItem(item) || /quarterstaff|two-handed sword|battle-axe|dwarvish mattock|mattock/.test(name);
}

function wieldItemForApply(item) {
    touchArtifactForWield(item);
    const previousWielded = (game.inventory || []).find(invItem => invItem !== item && itemIsWielded(invItem));
    if (previousWielded) {
        previousWielded.wielded = false;
        previousWielded.alternate = false;
        previousWielded.line = `${previousWielded.letter || '?'} - ${inventoryItemName(previousWielded)}`;
    }
    for (const invItem of game.inventory || []) {
        if (invItem === item) continue;
        invItem.wielded = false;
    }
    item.wielded = true;
    item.alternate = false;
    const hand = isTwoHandedWieldItem(item) ? 'hands' : `${game.u?.uhandedness || 'right'} hand`;
    item.line = `${item.letter || '?'} - ${inventoryItemName(item)} (weapon in ${hand})`;
    game._wielded_mjollnir = item.artifact === 'Mjollnir' || item.kind === 'mjollnir' || /Mjollnir/.test(inventoryItemName(item));
    return item.line;
}

function polearmTargetDescription(x, y) {
    const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
    if (mon) return monsterIndefiniteName(mon.data?.name || 'monster');
    const statue = floorStatueAt(x, y);
    if (statue) return pickupObjectPhrase(statue);
    const obj = (game.level?.objects || []).find(candidate =>
        !candidate.hidden && !candidate.transientProjectile && candidate.ox === x && candidate.oy === y);
    if (obj) return pickupObjectPhrase(obj);
    const loc = game.level?.at(x, y);
    if (!loc) return 'stone';
    if (loc.typ === DOOR) return doorDescription(loc);
    if (loc.typ === CORR) return 'corridor';
    if (loc.typ === ROOM || loc.typ === STAIRS) return 'floor of a room';
    if (loc.typ === TREE) return 'tree';
    if (loc.typ === MOAT) return 'moat';
    if (loc.typ === STONE) return 'stone';
    return loc.typ && loc.typ < DOOR ? 'wall' : 'unexplored area';
}

function statueStrikeTargetAlongLine(dir) {
    for (let step = 1; step <= BOLT_LIM; step++) {
        const x = (game.u?.ux || 0) + dir.dx * step;
        const y = (game.u?.uy || 0) + dir.dy * step;
        if (x < 1 || x >= COLNO || y < 0 || y >= ROWNO) break;
        const loc = game.level?.at(x, y);
        if (!loc) break;
        if ((game.level?.monsters || []).some(mon => mon.mx === x && mon.my === y)) break;
        if ((game.level?.objects || []).some(obj =>
            !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y && obj.otyp === BOULDER))
            break;
        const statue = floorStatueAt(x, y);
        const trap = statue ? statueTrapAt(x, y) : null;
        if (statue) return { statue, trap, x, y };
        if (!ZAP_POS(loc.typ) || (loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED)))) break;
    }
    return null;
}

function statueStrikeBreakResult(x, y) {
    if (!game.u?.blind && couldsee(x, y))
        return { message: 'The statue shatters.', learned: true };
    if (!heroIsDeaf()) return { message: 'You hear a crumbling sound.', learned: true };
    return { message: '', learned: false };
}

async function strikeStatueAlongLine(dir) {
    const target = statueStrikeTargetAlongLine(dir);
    if (!target) return { hit: false, message: '', learned: false };
    if (target.trap) {
        const message = await activateStatueTrap(target.trap, target.x, target.y, { shatter: true }) || '';
        if (message) return { hit: true, message, learned: true };
        const statue = floorStatueAt(target.x, target.y);
        if (statue && breakStatueObject(statue, target.x, target.y))
            return { hit: true, ...statueStrikeBreakResult(target.x, target.y) };
        return { hit: true, message: '', learned: false };
    }
    if (!breakStatueObject(target.statue, target.x, target.y))
        return { hit: false, message: '', learned: false };
    return { hit: true, ...statueStrikeBreakResult(target.x, target.y) };
}

export async function activateStatueTrap(trap, x, y, { prefix = '', shatter = false, search = false, normal = false } = {}) {
    if (trap?.ttyp !== STATUE_TRAP) return null;
    game.level.traps = (game.level?.traps || []).filter(candidate => candidate !== trap);

    const statues = (game.level?.objects || []).filter(obj =>
        !obj.transientProjectile && obj.otyp === STATUE && obj.ox === x && obj.oy === y);
    for (const statue of statues) {
        const info = statueTrapAnimationInfo(statue);
        if (!info?.data?.name) break;

        const flags = NO_MINVENT | MM_NOMSG
            | (info.noCountBirth ? MM_NOCOUNTBIRTH : 0)
            | (info.noTail ? MM_NOTAIL : 0)
            | (info.noWait ? MM_NOWAIT : 0)
            | (info.directedDoppelganger || info.adjacentOk ? MM_ADJACENTOK : 0);
        const mon = await makemon(info.data, x, y, flags);
        if (!mon) {
            if (statueTrapFailureRetriesNextStatue(statue, info)) continue;
            break;
        }

        stoneToFleshApplySavedMonsterTraits(mon, info);
        stoneToFleshApplyDirectedDoppelgangerForm(mon, info);
        stoneToFleshChristenAnimatedStatueMonster(statue, mon);
        mon.mtame = 0;
        mon.pet = false;
        mon.mpeaceful = 0;
        mon.msleeping = 0;
        mon.mundetected = false;
        set_malign(mon);

        const statueName = pickupObjectName(statue);
        const verb = stoneToFleshGolemStatueVerb(info, mon);
        const body = search || normal
            ? statueSearchAnimationMessage(statue, mon)
            : shatter
            ? `Instead of shattering, ${game.u?.blind || !couldsee(x, y) ? 'a statue' : `the ${statueName}`} suddenly ${verb}!`
            : `${upstartText(`the ${statueName}`)} ${verb}!`;
        const debtMessage = shatter ? statueShatterShopDebtMessage(statue, x, y, mon) : '';
        const historicMessage = stoneToFleshHistoricStatueGoneMessage(statue, x, y);

        moveStatueContentsToMonster(statue, mon);
        game.level.objects = (game.level?.objects || []).filter(obj => obj !== statue);
        newsym(x, y);
        newsym(mon.mx, mon.my);
        return [prefix, body, debtMessage, historicMessage].filter(Boolean).join('  ');
    }

    newsym(x, y);
    return prefix || '';
}

function noFittingWishObject() {
    return {
        _wish_no_match: true,
    };
}

function parseWishedStatueName(lowerName, qualifiers = {}) {
    const match = String(lowerName || '').trim().match(/^statue(?:\s+of(?:\s+(?:a|an|the))?\s+(.+))?$/);
    if (!match) return null;
    const gendered = stripWishedMonsterGenderPrefix(match[1] || '');
    return {
        monsterName: gendered.name,
        requestedGender: gendered.gender || qualifiers.monsterGender || null,
    };
}

function makeWishedStatueObject(statueWish, qualifiers = {}) {
    const resolved = resolveWishedCorpstatMonsterName(
        statueWish?.monsterName,
        statueWish?.requestedGender || qualifiers.monsterGender || null,
    );
    const monster = resolved.monsterName ? wishedMonsterByName(resolved.monsterName) : null;
    if (resolved.monsterName && !monster) return null;
    const otmp = mksobj(STATUE, true, false);
    if (monster) {
        const statueMonster = wishedNonFigurineCorpstatMonster(monster);
        otmp.corpsenm = statueMonster;
        otmp.spe = wishedCorpstatSpe(monster, resolved.requestedGender);
        if (statueMonster.verysmall) delete otmp.contents;
    }
    if (qualifiers.historic) {
        otmp.spe = (otmp.spe || 0) | CORPSTAT_HISTORIC;
    }
    return Object.assign(otmp, {
        cls: 'rock',
        glyph: '`',
        kind: 'statue',
        wishedfor: true,
    });
}

function parseWishedFigurineName(lowerName, qualifiers = {}) {
    const name = String(lowerName || '').trim();
    let match = name.match(/^figurines?(?:\s+of(?:\s+(?:a|an|the))?\s+(.+))?$/);
    if (!match) match = name.match(/^(.+)\s+figurines?$/);
    if (!match) return null;
    const gendered = stripWishedMonsterGenderPrefix(match[1] || '');
    return {
        monsterName: gendered.name,
        requestedGender: gendered.gender || qualifiers.monsterGender || null,
        exact: !!gendered.name,
    };
}

function canWishedFigurineUseMonster(monster) {
    if (!monster || monster.unique || monster.name === 'mail daemon') return false;
    const human = monster.human || monster.mlet === '@' || monster.glyph === '@' || monster.mlet === 'human';
    return !human || monster.wereHuman;
}

function makeWishedFigurineObject(figurineWish, qualifiers = {}) {
    const resolved = resolveWishedCorpstatMonsterName(
        figurineWish?.monsterName,
        figurineWish?.requestedGender || qualifiers.monsterGender || null,
    );
    const monster = resolved.monsterName ? wishedMonsterByName(resolved.monsterName) : null;
    if (figurineWish?.exact && !monster) return noFittingWishObject();
    game._mkobj_tool_roll = WISH_TOOL_ROLLS.get('figurine');
    const otmp = mksobj(TOOL_CLASS, true, false);
    if (monster) {
        otmp.spe = wishedCorpstatSpe(monster, resolved.requestedGender);
        if (canWishedFigurineUseMonster(monster)) otmp.corpsenm = monster;
    }
    return Object.assign(otmp, {
        cls: 'tool',
        glyph: '(',
        kind: 'figurine',
        actualKind: 'figurine',
        known: true,
        wishedfor: true,
    });
}

function parseWishedCorpseName(lowerName, qualifiers = {}) {
    const name = String(lowerName || '').trim();
    let match = name.match(/^corpses?(?:\s+of(?:\s+(?:a|an|the))?\s+(.+))?$/);
    if (!match) match = name.match(/^(.+)\s+corpses?$/);
    if (!match) return null;
    const gendered = stripWishedMonsterGenderPrefix(match[1] || '');
    return {
        monsterName: gendered.name,
        requestedGender: gendered.gender || qualifiers.monsterGender || null,
        exact: !!gendered.name,
    };
}

function parseWishedGlobName(lowerName, qualifiers = {}) {
    let name = String(lowerName || '').trim();
    let size = qualifiers.globSize || 0;
    const sizeMatch = name.match(/^(small|medium|large|very large)\s+(.+)$/);
    if (sizeMatch) {
        size = sizeMatch[1] === 'small' ? 1
            : sizeMatch[1] === 'medium' ? 2
                : sizeMatch[1] === 'large' ? 3 : 4;
        name = sizeMatch[2].trim();
    }
    const bareMatch = name.match(/^globs?$/);
    const ofMatch = name.match(/^globs?\s+of\s+(.+)$/);
    const suffixMatch = name.match(/^(.+)\s+globs?$/);
    if (!bareMatch && !ofMatch && !suffixMatch) return null;
    const monsterText = (ofMatch?.[1] || suffixMatch?.[1] || '').replace(/^(?:a|an|the)\s+/, '').trim();
    const plural = /\bglobs\b/.test(name);
    return {
        monsterName: monsterText,
        size,
        exact: !!monsterText,
        defaultCount: plural ? 2 : 0,
    };
}

function globTypeForMonsterName(monsterName) {
    const lowerName = String(monsterName || '').trim().toLowerCase();
    if (!lowerName) {
        const index = rn1(RANDOM_GLOB_MONSTER_NAMES.length, 0);
        return GLOB_TYPES.get(RANDOM_GLOB_MONSTER_NAMES[index]);
    }
    if (GLOB_TYPES.has(lowerName)) return GLOB_TYPES.get(lowerName);
    const monster = wishedMonsterByName(lowerName);
    if (!monster) {
        const index = rn1(RANDOM_GLOB_MONSTER_NAMES.length, 0);
        return GLOB_TYPES.get(RANDOM_GLOB_MONSTER_NAMES[index]);
    }
    return GLOB_TYPES.get(monster.name) || GLOB_TYPES.get(lowerName) || null;
}

function globSizeWeight(size = 0) {
    if (size <= 1) return 20;
    return 20 + (5 + (size - 2) * 10) * 20;
}

function applyWishedGlobQuantity(item, wishedQuan) {
    if (!item?.globby) return;
    let count = Math.max(wishedQuan || 0, item._wish_glob_default_count || 0);
    if (count <= 1) return;
    let maxCount = rn1(5, 2);
    const size = item._wish_glob_size || 0;
    if (maxCount > 6 - size) maxCount = 6 - size;
    if (count > maxCount && !game.flags?.debug) count = maxCount;
    item.owt = (item.owt || 20) * count;
    if (item.oeaten) {
        item.oeaten = item.owt;
        consumeOeaten(item, 1);
    }
}

function makeWishedGlobObject(globWish = {}) {
    const globType = globTypeForMonsterName(globWish.monsterName);
    if (!globType) return noFittingWishObject();
    const id = next_ident();
    const shrinkDelay = 25 + rn2(5) - 2;
    const size = globWish.size || 0;
    const monsterName = globType.name.replace(/^glob of /, '');
    return {
        id,
        otyp: globType.otyp,
        cls: 'food',
        glyph: '%',
        color: globType.color,
        _display_color: globType.color,
        kind: globType.name,
        actualKind: globType.name,
        singular: globType.name,
        globName: globType.name,
        globby: true,
        known: true,
        dknown: true,
        quan: 1,
        owt: globSizeWeight(size),
        corpsenm: wishedMonsterByName(monsterName) || { name: monsterName, neuter: true },
        globShrinkTurn: (game.moves || 1) + shrinkDelay,
        _wish_glob_size: size,
        _wish_glob_default_count: globWish.defaultCount || 0,
        wishedfor: true,
    };
}

function parseWishedDragonArmorName(lowerName) {
    const name = String(lowerName || '').trim().replace(/\s+/g, ' ');
    let match = name.match(/^dragon (scales|scale mail|scale armor)$/);
    if (match) {
        return {
            type: match[1] === 'scales' ? 'scales' : 'mail',
            random: true,
            armorAlias: match[1] === 'scale armor',
        };
    }
    match = name.match(/^(gray|grey|gold|silver|red|white|orange|black|blue|green|yellow) dragon (scales|scale mail|scale armor)$/);
    if (!match) return null;
    return {
        type: match[2] === 'scales' ? 'scales' : 'mail',
        spec: DRAGON_ARMOR_BY_COLOR.get(match[1]),
        armorAlias: match[2] === 'scale armor',
        alternateSpelling: match[1] === 'grey',
    };
}

function makeWishedDragonArmorObject(dragonWish = {}) {
    const spec = dragonWish.random
        ? DRAGON_ARMOR_SPECS[rn1(DRAGON_ARMOR_SPECS.length, 0)]
        : dragonWish.spec;
    if (!spec) return null;
    const mail = dragonWish.type === 'mail';
    if (mail && !dragonWish.random && !dragonWish.alternateSpelling)
        rn2(67);
    const kind = `${spec.colorName} dragon ${mail ? 'scale mail' : 'scales'}`;
    const otmp = mksobj(mail ? spec.mailOtyp : spec.scalesOtyp, true, false);
    return Object.assign(otmp, {
        cls: 'armor',
        glyph: '[',
        kind,
        actualKind: kind,
        color: spec.color,
        _display_color: spec.color,
        owt: 40,
        dragonArmor: true,
        dragonArmorKind: mail ? 'mail' : 'scales',
        noArticle: !mail,
        known: false,
        wishedfor: true,
    });
}

function wishedHumanWereCounterpart(monster) {
    if (!monster?.wereBeast) return null;
    return wishedMonsterByName(`human ${monster.name}`) || null;
}

function wishedNonFigurineCorpstatMonster(monster) {
    if (!monster) return null;
    if (monster.name === 'long worm tail')
        return wishedMonsterByName('long worm') || monster;
    return wishedHumanWereCounterpart(monster) || monster;
}

function wishedCorpseOverrideMonster(monster) {
    monster = wishedNonFigurineCorpstatMonster(monster);
    if (!monster) return null;
    if (monster.guardian)
        return wishedMonsterByName('human') || monster;
    if (monster.unique && !game.flags?.debug) return null;
    if (monster.noCorpse) return null;
    return monster;
}

function restartWishedCorpseTimeout(otmp) {
    startCorpseTimeout(otmp);
    const name = otmp?.corpsenm?.name || '';
    if (name !== 'lichen' && name !== 'lizard') otmp._corpse_restart_consumed = true;
}

function finishWishedCorpseDisplay(otmp) {
    const corpseName = otmp?.corpsenm?.name || 'monster';
    otmp.kind = `${corpseName} corpse`;
    otmp.actualKind = `${corpseName} corpse`;
    otmp.singular = `${corpseName} corpse`;
    otmp.plural = `${corpseName} corpses`;
    if (CORPSE_WEIGHTS.has(corpseName)) otmp.owt = CORPSE_WEIGHTS.get(corpseName);
}

function makeWishedCorpseObject(corpseWish, qualifiers = {}) {
    const resolved = resolveWishedCorpstatMonsterName(
        corpseWish?.monsterName,
        corpseWish?.requestedGender || qualifiers.monsterGender || null,
    );
    if (resolved.monsterName && GLOB_TYPES.has(resolved.monsterName))
        return makeWishedGlobObject({ monsterName: resolved.monsterName });
    const monster = resolved.monsterName ? wishedMonsterByName(resolved.monsterName) : null;
    if (corpseWish?.exact && !monster) return noFittingWishObject();
    if (monster && GLOB_TYPES.has(monster.name))
        return makeWishedGlobObject({ monsterName: monster.name });

    const otmp = mksobj(CORPSE, true, false);
    if (qualifiers.zombifying) otmp.zombifying = true;
    let restartedTimeout = false;
    if (monster) {
        otmp.spe = wishedCorpstatSpe(monster, resolved.requestedGender);
        const corpseMonster = wishedCorpseOverrideMonster(monster);
        if (corpseMonster) {
            otmp.corpsenm = corpseMonster;
            restartWishedCorpseTimeout(otmp);
            restartedTimeout = true;
        }
    }
    if (qualifiers.zombifying && !restartedTimeout)
        startCorpseTimeout(otmp, { zombify: true });
    Object.assign(otmp, {
        cls: 'food',
        glyph: '%',
        wishedfor: true,
    });
    finishWishedCorpseDisplay(otmp);
    return otmp;
}

const EGG_GROWNUP_MONSTER_NAMES = new Map([
    ['baby gray dragon', 'gray dragon'],
    ['baby gold dragon', 'gold dragon'],
    ['baby silver dragon', 'silver dragon'],
    ['baby red dragon', 'red dragon'],
    ['baby white dragon', 'white dragon'],
    ['baby orange dragon', 'orange dragon'],
    ['baby black dragon', 'black dragon'],
    ['baby blue dragon', 'blue dragon'],
    ['baby green dragon', 'green dragon'],
    ['baby yellow dragon', 'yellow dragon'],
    ['red naga hatchling', 'red naga'],
    ['black naga hatchling', 'black naga'],
    ['golden naga hatchling', 'golden naga'],
    ['guardian naga hatchling', 'guardian naga'],
    ['baby crocodile', 'crocodile'],
]);

function parseWishedEggName(lowerName, qualifiers = {}) {
    const name = String(lowerName || '').trim();
    let match = name.match(/^eggs?(?:\s+of(?:\s+(?:a|an|the))?\s+(.+))?$/);
    const plural = /^eggs\b/.test(name);
    let prefixPlural = false;
    if (!match) {
        match = name.match(/^(.+)\s+eggs?$/);
        prefixPlural = /\seggs$/.test(name);
    }
    if (!match) return null;
    const gendered = stripWishedMonsterGenderPrefix(match[1] || '');
    return {
        monsterName: gendered.name,
        requestedGender: gendered.gender || qualifiers.monsterGender || null,
        exact: !!gendered.name,
        plural: plural || prefixPlural,
    };
}

function lookupWishedEggMonster(name) {
    const lowerName = String(name || '').trim().toLowerCase();
    if (!lowerName) return null;
    if (lowerName === 'scorpius')
        return wishedMonsterByName('scorpion');
    return wishedMonsterByName(lowerName);
}

function grownWishedEggMonster(monster) {
    if (!monster?.name) return monster;
    const name = monster.name;
    const grownName = EGG_GROWNUP_MONSTER_NAMES.get(name)
        || GROWNUP_MONSTERS.get(name)
        || null;
    return grownName
        ? wishedMonsterByName(grownName) || monster
        : monster;
}

function canWishedEggBeHatched(monster) {
    const eggMonster = grownWishedEggMonster(monster);
    const name = eggMonster?.name || '';
    if (!name) return null;
    if (name === 'killer bee' || name === 'gargoyle') return eggMonster;
    if (!eggMonster.oviparous) return null;
    const breederEgg = !rn2(77);
    if ((name === 'queen bee' || name === 'winged gargoyle') && !breederEgg)
        return null;
    return eggMonster;
}

function consumeWishedEggHatchTimer(otmp) {
    for (let i = 151; i <= 200; i++) {
        if (rnd(i) > 150) {
            otmp.eggHatchTurn = (game.moves || 1) + i;
            otmp._egg_hatch_seq = game._egg_hatch_timer_seq = (game._egg_hatch_timer_seq || 0) + 1;
            break;
        }
    }
    otmp._egg_hatch_consumed = true;
}

function makeWishedEggObject(eggWish, qualifiers = {}) {
    const resolved = resolveWishedCorpstatMonsterName(
        eggWish?.monsterName,
        eggWish?.requestedGender || qualifiers.monsterGender || null,
    );
    const monster = resolved.monsterName ? lookupWishedEggMonster(resolved.monsterName) : null;
    if (eggWish?.exact && !monster) return noFittingWishObject();

    const otmp = mksobj(EGG, true, false);
    const hadHatchTimer = !!otmp.corpsenm;
    const requestedMonster = monster ? wishedNonFigurineCorpstatMonster(monster) : null;
    const eggMonster = requestedMonster ? canWishedEggBeHatched(requestedMonster) : otmp.corpsenm;
    otmp.corpsenm = eggMonster || null;
    if (eggMonster && !hadHatchTimer) consumeWishedEggHatchTimer(otmp);
    if (!eggMonster) {
        delete otmp.eggHatchTurn;
        delete otmp._egg_hatch_consumed;
        delete otmp._egg_hatch_seq;
    }
    Object.assign(otmp, {
        cls: 'food',
        glyph: '%',
        kind: 'egg',
        singular: 'egg',
        plural: 'eggs',
        wishedfor: true,
    });
    if (eggWish?.plural) otmp.quan = Math.max(2, otmp.quan || 1);
    return otmp;
}

function makeOrdinaryBellWishObject() {
    const otmp = mksobj(BELL, true, false);
    return Object.assign(otmp, {
        cls: 'tool',
        glyph: '(',
        kind: 'bell',
        actualKind: 'bell',
        wishedfor: true,
    });
}

function makeCandelabrumOfInvocationWishObject() {
    const otmp = mksobj(CANDELABRUM_OF_INVOCATION, true, false);
    return Object.assign(otmp, {
        cls: 'tool',
        glyph: '(',
        color: CLR_YELLOW,
        kind: 'Candelabrum of Invocation',
        actualKind: 'Candelabrum of Invocation',
        known: true,
        unique: true,
        wishedfor: true,
    });
}

function makeBookOfTheDeadWishObject() {
    const otmp = mksobj(BOOK_OF_THE_DEAD, true, false);
    return Object.assign(otmp, {
        cls: 'spellbook',
        glyph: '+',
        color: CLR_WHITE,
        kind: 'Book of the Dead',
        actualKind: 'Book of the Dead',
        spellName: '',
        known: true,
        unique: true,
        wishedfor: true,
    });
}

function makeCandleFromCandelabrumWishObject() {
    const candleType = rnd(25) <= 20 ? TALLOW_CANDLE : WAX_CANDLE;
    const wax = candleType === WAX_CANDLE;
    const otmp = mksobj(candleType, true, false);
    return Object.assign(otmp, {
        cls: 'tool',
        glyph: '(',
        kind: wax ? 'wax candle' : 'tallow candle',
        plural: wax ? 'wax candles' : 'tallow candles',
        age: wax ? 400 : 200,
        wishedfor: true,
    });
}

function makeOilLampFromMagicLampWishObject() {
    const otmp = mksobj(OIL_LAMP, true, false);
    return Object.assign(otmp, {
        cls: 'tool',
        glyph: '(',
        kind: 'oil lamp',
        actualKind: 'oil lamp',
        wishedfor: true,
    });
}

const WISH_VENOM_TYPES = {
    blinding: { otyp: BLINDING_VENOM, kind: 'splash of blinding venom' },
    acid: { otyp: ACID_VENOM, kind: 'splash of acid venom' },
};

function wishedRandomVenomType() {
    return rnd(1000) <= 500 ? WISH_VENOM_TYPES.blinding : WISH_VENOM_TYPES.acid;
}

function wishedDescriptionVenomType() {
    return rn2(1002) < 501 ? WISH_VENOM_TYPES.blinding : WISH_VENOM_TYPES.acid;
}

function wishedVenomType(lowerName) {
    const name = String(lowerName || '').trim().replace(/\s+/g, ' ');
    if (name === 'venom')
        return wishedRandomVenomType();
    if (name === 'splash of venom' || name === 'splashes of venom')
        return wishedDescriptionVenomType();
    if (name === 'splash of blinding venom' || name === 'splashes of blinding venom'
        || name === 'blinding venom') {
        rn2(501);
        return WISH_VENOM_TYPES.blinding;
    }
    if (name === 'splash of acid venom' || name === 'splashes of acid venom'
        || name === 'acid venom') {
        rn2(501);
        return WISH_VENOM_TYPES.acid;
    }
    return null;
}

function makeWishedVenomObject(lowerName) {
    const venom = wishedVenomType(lowerName);
    if (!venom) return null;
    if (!game.flags?.debug) return noFittingWishObject();
    return {
        id: next_ident(),
        otyp: venom.otyp,
        cls: 'venom',
        glyph: '.',
        color: CLR_BROWN,
        _display_color: CLR_BROWN,
        kind: venom.kind,
        actualKind: venom.kind,
        singular: venom.kind,
        plural: venom.kind.replace(/^splash\b/, 'splashes'),
        known: true,
        dknown: true,
        quan: 1,
        spe: 1,
        owt: 1,
        ocMerge: true,
        _wish_ignore_requested_spe: true,
        wishedfor: true,
    };
}

function makeWizardSpecialWishObject(lowerName) {
    if (!game.flags?.debug) return null;
    if (lowerName === 'candelabrum' || lowerName === 'candelabrum of invocation') {
        rn2(WIZARD_ONLY_WISH_NAMEDESC_BOUNDS.get(lowerName));
        return makeCandelabrumOfInvocationWishObject();
    }
    if (lowerName === 'papyrus' || lowerName === 'book of the dead') {
        rn2(WIZARD_ONLY_WISH_NAMEDESC_BOUNDS.get(lowerName));
        return makeBookOfTheDeadWishObject();
    }
    return null;
}

function substituteNonWizardSpecialWish(lowerName) {
    if (game.flags?.debug) return null;
    if (/\bamulet of yendor\b/.test(lowerName)) {
        return makeFakeAmuletOfYendorWishObject();
    }
    if (lowerName === 'candelabrum' || lowerName === 'candelabrum of invocation') {
        rn2(WIZARD_ONLY_WISH_NAMEDESC_BOUNDS.get(lowerName));
        return makeCandleFromCandelabrumWishObject();
    }
    if (lowerName === 'bell of opening') {
        rn2(WIZARD_ONLY_WISH_NAMEDESC_BOUNDS.get(lowerName));
        return makeOrdinaryBellWishObject();
    }
    if (lowerName === 'papyrus' || lowerName === 'book of the dead') {
        rn2(WIZARD_ONLY_WISH_NAMEDESC_BOUNDS.get(lowerName));
        return makeBlankSpellbookWishObject();
    }
    if (lowerName === 'magic lamp') {
        rn2(WISH_BASE_NAMEDESC_BOUNDS.get(lowerName));
        return makeOilLampFromMagicLampWishObject();
    }
    return null;
}

function makeAmbiguousBlankPaperWishObject() {
    return rn2(48) < 29 ? makeBlankScrollWishObject() : makeBlankSpellbookWishObject();
}

function isWishedFoodName(lowerName) {
    return FOOD_NUTRITION.has(lowerName)
        || lowerName === 'food'
        || lowerName === 'ration'
        || lowerName === 'rations'
        || lowerName.endsWith(' cookie')
        || lowerName.endsWith(' cookies');
}

function tinMeatName(monsterName) {
    return monsterName === 'lichen' ? monsterName : `${monsterName} meat`;
}

function pluralTinName(name) {
    if (name.startsWith('empty tin')) return name.replace(/^empty tin/, 'empty tins');
    return name.replace(/\btin\b/, 'tins');
}

function tinNameWithVariety(monsterName, varietyIndex = null) {
    const meat = tinMeatName(monsterName);
    if (varietyIndex == null) return `tin of ${meat}`;
    const variety = TIN_VARIETY_TEXTS[varietyIndex] || '';
    if (variety === 'rotten' || variety === 'homemade')
        return `${variety} tin of ${meat}`;
    return `tin of ${variety} ${meat}`;
}

function setTinDisplayName(item, name) {
    item.singular = name;
    item.plural = pluralTinName(name);
}

function setTinEmpty(item) {
    item.corpsenm = null;
    item.spe = 0;
    item.kind = 'empty tin';
    item.actualKind = 'tin';
    item.emptyTin = true;
    setTinDisplayName(item, 'empty tin');
}

function setTinSpinach(item) {
    item.corpsenm = null;
    item.spe = 1;
    item.kind = 'tin:spinach';
    item.actualKind = 'tin';
    item.emptyTin = false;
    setTinDisplayName(item, 'tin of spinach');
}

function setTinMonster(item, monster, varietyIndex = null) {
    const name = monster?.name || String(monster || '');
    if (!name) return;
    item.corpsenm = monster;
    item.spe = varietyIndex == null ? 0 : -(varietyIndex + 1);
    item.kind = `tin:${name}`;
    item.actualKind = 'tin';
    item.emptyTin = false;
    setTinDisplayName(item, tinNameWithVariety(name, varietyIndex));
}

function wishedTinMonsterHasNutrition(monster) {
    if (!monster?.name) return false;
    if (typeof monster.cnutrit === 'number') return monster.cnutrit !== 0;
    if (typeof monster.nutrition === 'number') return monster.nutrition !== 0;
    const known = CORPSE_NUTRITION.get(String(monster.name).toLowerCase());
    return known == null ? true : known !== 0;
}

function wishedTinMonsterBinding(monster) {
    const tinMonster = wishedNonFigurineCorpstatMonster(monster);
    if (!tinMonster) return { monster: null, empty: false };
    if (isMonsterGenocidedName(tinMonster.name)) return { monster: null, empty: true };
    if (tinMonster.unique && !game.flags?.debug) return { monster: null, empty: false };
    if (tinMonster.noCorpse) return { monster: null, empty: false };
    if (!wishedTinMonsterHasNutrition(tinMonster)) return { monster: null, empty: false };
    return { monster: tinMonster, empty: false };
}

function corpseMonsterName(item) {
    return String(item?.corpsenm?.name || objectKindKey(item).replace(/\s+corpse$/, '') || '').toLowerCase();
}

function tinnableCorpseNutrition(item) {
    if (!item) return 0;
    if (typeof item.corpsenm?.cnutrit === 'number') return item.corpsenm.cnutrit;
    if (typeof item.nutrition === 'number') return item.nutrition;
    const name = corpseMonsterName(item);
    const known = CORPSE_NUTRITION.get(name);
    if (known != null) return known;
    return name ? 1 : 0;
}

function isTinnableCorpse(item) {
    return isCorpseItem(item) && !(item.oeaten > 0) && tinnableCorpseNutrition(item) > 0;
}

function floorTinnableCorpseAtHero() {
    return (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile
        && obj.ox === game.u?.ux && obj.oy === game.u?.uy
        && isTinnableCorpse(obj));
}

function tinningInventoryLetters() {
    return inventoryLetters(isTinnableCorpse);
}

function tinningInventoryPrompt() {
    const letters = tinningInventoryLetters();
    return `What do you want to tin? [${getobjPromptLetters(letters)} or ?*]`;
}

function tinningFloorPrompt(corpse) {
    const name = pickupObjectName(corpse);
    if ((corpse?.quan || 1) > 1)
        return `There are ${corpse.quan} ${corpse.plural || `${name}s`} here; tin one? [ynq] (n)`;
    const article = /^[aeiou]/i.test(name) ? 'an' : 'a';
    return `There is ${article} ${name} here; tin it? [ynq] (n)`;
}

function clearTinningKitState() {
    game._apply_tinning_kit_letter = '';
    game._tinning_floor_object = null;
}

function createHomemadeTinFromCorpse(corpse, kit) {
    const can = mksobj(TIN, false, false);
    Object.assign(can, {
        cls: 'food',
        glyph: '%',
        kind: 'tin',
        actualKind: 'tin',
        singular: 'tin',
        plural: 'tins',
        quan: 1,
        blessed: !!kit?.blessed,
        cursed: !!kit?.cursed,
        known: true,
        dknown: true,
        bknown: true,
        ox: game.u?.ux ?? corpse?.ox ?? 0,
        oy: game.u?.uy ?? corpse?.oy ?? 0,
    });
    setTinMonster(can, corpse?.corpsenm || { name: corpseMonsterName(corpse) }, HOMEMADE_TIN);
    can.known = true;
    return can;
}

function addHomemadeTinToInventory(can, messages) {
    game.inventory ??= [];
    const used = new Set(game.inventory.map(item => item.letter).filter(Boolean));
    const letter = nextInventoryLetter();
    if (!letter || used.has(letter)) {
        can.ox = game.u?.ux ?? 0;
        can.oy = game.u?.uy ?? 0;
        delete can.letter;
        delete can.line;
        game.level.objects ??= [];
        game.level.objects.push(can);
        newsym(can.ox, can.oy);
        messages.push(`You make, but cannot pick up, ${pickupObjectName(can)}.`);
        return;
    }
    can.letter = letter;
    can.line = normalInventoryLine({ ...can, line: '' });
    game.inventory.push(can);
    game._pet_food_scan_inventory = game.inventory;
    messages.push(`${can.line}.`);
}

function carriedCorpseNeedsTinningBill(corpse) {
    return !!(corpse?.unpaid || shopkeeperOwningBillEntry(corpse).entry);
}

function floorCorpseNeedsTinningBill(corpse) {
    if (!corpse || corpse.no_charge) return false;
    const x = corpse.ox ?? game.u?.ux;
    const y = corpse.oy ?? game.u?.uy;
    const shkp = shopkeeperForCostlySpot(x, y);
    return shopkeeperInHisShop(shkp) && (shopItemPrice(corpse, x, y) > 0 || !!shopBillEntryForObject(shkp, corpse));
}

async function beginTinningInventorySelection(kit) {
    const letters = tinningInventoryLetters();
    if (!letters) {
        clearTinningKitState();
        await setMessage('You have nothing to tin.');
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }
    game._apply_tinning_kit_letter = kit?.letter || '';
    await setMessage(tinningInventoryPrompt());
    game._command_mode = 'tinningObject';
    return true;
}

async function beginTinningKitUse(kit) {
    game._apply_tinning_kit_letter = kit?.letter || '';
    if ((kit?.spe ?? 0) <= 0) {
        clearTinningKitState();
        await setMessage('You seem to be out of tins.');
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }
    const floorCorpse = floorTinnableCorpseAtHero();
    if (floorCorpse) {
        game._tinning_floor_object = floorCorpse;
        await setMessage(tinningFloorPrompt(floorCorpse));
        game._command_mode = 'tinningFloorConfirm';
        return true;
    }
    return beginTinningInventorySelection(kit);
}

async function finishTinningKitUse(kit, corpse, { floorObject = false } = {}) {
    clearTinningKitState();
    game._command_mode = null;
    if (!kit) return false;
    if (!isTinnableCorpse(corpse)) {
        await setMessage(corpse?.oeaten ? 'You cannot tin something which is partly eaten.' : "That's too insubstantial to tin.");
        game.context.move = 1;
        return true;
    }

    const messages = [];
    if (!spendChargedToolUse(kit, messages)) {
        await setMessage('You seem to be out of tins.');
        game.context.move = 1;
        return true;
    }

    const can = createHomemadeTinFromCorpse(corpse, kit);
    if (floorObject) {
        if (floorCorpseNeedsTinningBill(corpse) && !heroIsDeaf()) messages.push('"You tin it, you bought it!"');
        useUpFloorObject(corpse, 1, { heroCaused: true });
    } else {
        if (carriedCorpseNeedsTinningBill(corpse) && !heroIsDeaf()) messages.push('"You tin it, you bought it!"');
        useUpInventoryItem(corpse, 1);
    }
    addHomemadeTinToInventory(can, messages);
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
    return true;
}

function isTinObject(item) {
    const kind = String(item?.kind || '').toLowerCase();
    return item?.otyp === TIN || kind === 'tin' || kind === 'empty tin'
        || kind.startsWith('tin:');
}

function isTinOpenerObject(item) {
    return item?.otyp === TIN_OPENER || objectKindKey(item) === 'tin opener'
        || String(item?.actualKind || '').toLowerCase() === 'tin opener';
}

function tinObjectName(item) {
    if (item?.singular) return (item.quan || 1) > 1 ? item.plural || pluralTinName(item.singular) : item.singular;
    if (item?.emptyTin || item?.kind === 'empty tin') return (item.quan || 1) > 1 ? 'empty tins' : 'empty tin';
    return (item?.quan || 1) > 1 ? 'tins' : 'tin';
}

function tinVariety(item, display = false) {
    let r;
    if (item?.spe === 1 || objectKindKey(item) === 'tin:spinach') r = SPINACH_TIN;
    else if (item?.cursed) r = ROTTEN_TIN;
    else if ((item?.spe || 0) < 0) r = -item.spe - 1;
    else r = rn2(TIN_VARIETY_TEXTS.length);
    if (!display && r === HOMEMADE_TIN && !item?.blessed && !rn2(7)) r = ROTTEN_TIN;
    return r;
}

function heroIsMetallivorous() {
    return !!(polyselfFormWithDiet()?.metallivorous || game.u?.metallivorous);
}

function heroIsRustMonsterPolyself() {
    const form = polyselfFormWithDiet();
    return String(form?.name || game.u?.umonster || '').toLowerCase() === 'rust monster';
}

function isFoodOrCorpseObject(item) {
    return item?.cls === 'food' || item?.otyp === FOOD_CLASS || item?.otyp === 'corpse' || item?.otyp === CORPSE;
}

function heroInventoryEatCandidate(item) {
    return isFoodOrCorpseObject(item) || heroCanEatNonFoodMetal(item);
}

function heroCanEatNonFoodMetal(item) {
    if (!heroIsMetallivorous() || !item || isFoodOrCorpseObject(item) || isTinObject(item)) return false;
    if (metallivoreObjectAlwaysResists(item)) return false;
    const material = objectMaterialForMetallivore(item);
    if (!METALLIC_MATERIALS.has(material)) return false;
    return !(heroIsRustMonsterPolyself() && material !== 'iron');
}

function heroMetalEatingWornBlock(item) {
    if (!isWornInventoryItem(item) || objectIsRingLike(item)) return false;
    return item.cls === 'armor' || item.otyp === ARMOR_CLASS || item.glyph === '['
        || item.cls === 'tool' || item.otyp === TOOL_CLASS || item.glyph === '('
        || objectIsAmuletLike(item)
        || item.otyp === W_SADDLE || item.kind === 'saddle';
}

function refreshInventoryLineAfterMetalSplit(item) {
    if (!item || !game.inventory?.includes(item)) return;
    delete item.line;
    item.line = normalInventoryLine(item);
}

function splitCarriedMetalUnit(item) {
    const quantity = item?.quan || 1;
    if (quantity <= 1) return item;
    const split = { ...item, id: next_ident(), quan: 1 };
    item.quan = quantity - 1;
    refreshInventoryLineAfterMetalSplit(item);
    return split;
}

function splitFloorMetalUnit(item) {
    const quantity = item?.quan || 1;
    if (quantity <= 1 || !game.level?.objects?.includes(item)) return item;
    const rest = { ...item, id: next_ident(), quan: quantity - 1 };
    item.quan = 1;
    game.level.objects.push(rest);
    return item;
}

function removeEatenFloorMetalObject(item) {
    if (!game.level || !item) return;
    removeFloorObject(item);
    newsym(item.ox ?? game.u?.ux ?? 0, item.oy ?? game.u?.uy ?? 0);
}

function dropMetalObjectAtHero(item) {
    if (!item || !game.level) return;
    Object.assign(item, {
        ox: game.u?.ux ?? 0,
        oy: game.u?.uy ?? 0,
        hidden: false,
        buried: false,
        contained: false,
        transientProjectile: false,
    });
    delete item.letter;
    delete item.line;
    placeStackableFloorObject(item);
    newsym(item.ox, item.oy);
}

function heroMetalNonFoodNutrition(item) {
    if (!item) return 0;
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$')
        return Math.min(2000, Math.trunc((item.quan || 1) / 100));
    if (item.cls === 'ball' || item.cls === 'chain' || item.otyp === BC_BALL || item.otyp === BC_CHAIN)
        return globObjectWeight(item);
    const explicit = item.oc_nutrition ?? item.nutrition;
    if (explicit != null) return Math.max(0, Math.trunc(Number(explicit) || 0));
    if (objectIsRingLike(item)) return 15;
    return globObjectWeight({ ...item, quan: 1 });
}

function metallivoreFoodWord(item) {
    const material = objectMaterialForMetallivore(item);
    if (['silver', 'gold', 'platinum', 'mithril'].includes(material)) return material;
    return 'metal';
}

function eatenRingName(item) {
    if (!objectIsRingLike(item)) return '';
    const raw = item?.ringRoll ? IDENTIFIED_RING_NAMES[item.ringRoll - 1] || ''
        : String(item?.actualKind || item?.kind || '').toLowerCase().replace(/^ring of /, '');
    return raw.replace(/^ring of /, '');
}

function eatenAmuletName(item) {
    if (!objectIsAmuletLike(item)) return '';
    return String(item?.actualKind
        || (item?.amuletIndex != null ? IDENTIFIED_AMULET_NAMES[item.amuletIndex] : '')
        || item?.kind || '').toLowerCase();
}

function eatenAccessoryHasEffect(item, messages) {
    messages.push(`Magic spreads through your body as you digest the ${objectIsRingLike(item) ? 'ring' : 'amulet'}.`);
}

function adjustHeroAttribute(attr, delta) {
    if (!game.u?.acurr?.a || !delta) return false;
    const before = game.u.acurr.a[attr] ?? 10;
    const after = Math.max(3, Math.min(125, before + delta));
    game.u.acurr.a[attr] = after;
    if (delta > 0 && game.u.amax?.a) game.u.amax.a[attr] = Math.max(game.u.amax.a[attr] || after, after);
    return after !== before;
}

function boundedAccessoryIncrease(oldValue, inc) {
    const old = Math.trunc(Number(oldValue || 0));
    let amount = Math.trunc(Number(inc || 0));
    const absOld = Math.abs(old);
    let absInc = Math.abs(amount);
    const sameSign = Math.sign(old) === Math.sign(amount);
    if (absInc !== 0 && sameSign && absOld + absInc >= 10) {
        if (absOld + absInc < 20) {
            absInc = rnd(absInc);
            if (absOld + absInc < 10) absInc = 10 - absOld;
            amount = Math.sign(amount) * absInc;
        } else if (absOld + absInc < 40) {
            absInc = rn2(absInc) ? 1 : 0;
            if (absOld + absInc < 20) absInc = rnd(20 - absOld);
            amount = Math.sign(amount) * absInc;
        } else amount = 0;
    }
    return old + amount;
}

const EATEN_RING_PROPERTY_FIELDS = new Map([
    ['regeneration', 'regeneration'],
    ['searching', 'searching'],
    ['stealth', 'stealth'],
    ['hunger', 'hunger'],
    ['aggravate monster', 'aggravateMonster'],
    ['conflict', 'conflict'],
    ['warning', 'warning'],
    ['poison resistance', 'poisonResistance'],
    ['fire resistance', 'fireResistance'],
    ['cold resistance', 'coldResistance'],
    ['shock resistance', 'shockResistance'],
    ['teleportation', 'teleportation'],
    ['teleport control', 'teleportControl'],
    ['polymorph', 'polymorph'],
    ['polymorph control', 'polymorphControl'],
    ['see invisible', 'seeInvisible'],
    ['invisibility', 'invisible'],
    ['protection from shape changers', 'protectionFromShapeChangers'],
]);

function applyEatenRingEffect(item, messages) {
    const name = eatenRingName(item);
    const spe = Math.trunc(Number(item?.spe ?? 0));
    switch (name) {
    case 'adornment':
        eatenAccessoryHasEffect(item, messages);
        if (adjustHeroAttribute(A_CHA, spe)) recordKnownRingDiscovery(name, item);
        return true;
    case 'gain strength':
        eatenAccessoryHasEffect(item, messages);
        if (adjustHeroAttribute(A_STR, spe)) recordKnownRingDiscovery(name, item);
        return true;
    case 'gain constitution':
        eatenAccessoryHasEffect(item, messages);
        if (adjustHeroAttribute(A_CON, spe)) recordKnownRingDiscovery(name, item);
        return true;
    case 'increase accuracy':
        eatenAccessoryHasEffect(item, messages);
        if (game.u) game.u.uhitinc = boundedAccessoryIncrease(game.u.uhitinc, spe);
        return true;
    case 'increase damage':
        eatenAccessoryHasEffect(item, messages);
        if (game.u) game.u.udaminc = boundedAccessoryIncrease(game.u.udaminc, spe);
        return true;
    case 'protection':
        eatenAccessoryHasEffect(item, messages);
        if (game.u) {
            game.u.protection = true;
            game.u.ublessed = boundedAccessoryIncrease(game.u.ublessed, spe);
        }
        return true;
    case 'free action':
        if (!game.u?.sleepResistance) eatenAccessoryHasEffect(item, messages);
        if (game.u) {
            if (!game.u.sleepResistance) messages.push('You feel wide awake.');
            game.u.sleepResistance = true;
        }
        return true;
    case 'levitation':
        if (game.u && !game.u.levitation && !game.u.Levitation) {
            eatenAccessoryHasEffect(item, messages);
            game.u.levitation = true;
            game.u._levitationTimeout = (game.u._levitationTimeout || 0) + d(10, 20);
            recordKnownRingDiscovery(name, item);
        }
        return true;
    case 'sustain ability':
        return false;
    default: {
        const field = EATEN_RING_PROPERTY_FIELDS.get(name);
        if (!field || !game.u) return false;
        if (!game.u[field]) eatenAccessoryHasEffect(item, messages);
        const oldInvisible = !!game.u.invisible;
        const oldSeeInvisible = !!game.u.seeInvisible;
        game.u[field] = true;
        if (name === 'invisibility' && !oldInvisible && !game.u.blind) {
            messages.push(`Your body takes on a ${heroIsHallucinating() ? 'normal' : 'strange'} transparency...`);
            recordKnownRingDiscovery(name, item);
        }
        if (name === 'see invisible' && game.u.invisible && !oldSeeInvisible && !game.u.blind) {
            messages.push('Suddenly you can see yourself.');
            recordKnownRingDiscovery(name, item);
        }
        return true;
    }
    }
}

function eatenAmuletStrangulationChoke(item, messages) {
    exerciseAttribute(A_CON, false);
    if (heroIsBreathlessForChoke() || heroHasHungerPropertyForChoke()
        || (!heroIsStrangledForChoke() && !rn2(20))) {
        messages.push('You choke, but recover your composure.');
        return true;
    }

    messages.push(`You choke over your ${metallivoreFoodWord(item)}.`);
    game._death_cause = `choked on ${articleFor(pickupObjectName({ ...item, quan: 1 }))}`;
    if (consumeLifeSavingAmulet()) {
        if (game.u) {
            game.u.uhp = 0;
            game.u.uhunger = 900;
            refreshHeroEatingHungerStatus();
        }
        messages.push('You die...');
        messages.push(`But wait...  Your medallion ${game.u?.blind ? 'feels warm' : 'begins to glow'}!`);
        game._eaten_accessory_command_mode = 'lifeSavingMore';
        game._eaten_accessory_more = 1;
        game._eaten_accessory_move = 0;
        return true;
    }

    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    prepareDeathBones();
    messages.push('You die...');
    game._eaten_accessory_command_mode = 'deathDieMore';
    game._eaten_accessory_more = 1;
    game._eaten_accessory_move = 0;
    return true;
}

const EATEN_AMULET_PROPERTY_FIELDS = new Map([
    ['amulet of esp', 'telepathy'],
    ['amulet versus poison', 'poisonResistance'],
    ['amulet of magical breathing', 'magicalBreathing'],
]);

function applyEatenAmuletEffect(item, messages) {
    const name = eatenAmuletName(item);
    switch (name) {
    case 'amulet of guarding':
        eatenAccessoryHasEffect(item, messages);
        if (game.u) {
            game.u.protection = true;
            game.u.ublessed = boundedAccessoryIncrease(game.u.ublessed, 2);
        }
        return true;
    case 'amulet of restful sleep': {
        if (!game.u) return false;
        if (!game.u.sleepy) eatenAccessoryHasEffect(item, messages);
        game.u.sleepy = true;
        const newNap = rnd(100);
        const oldNap = game.u.sleepyTimeout || 0;
        if (!oldNap || newNap < oldNap) game.u.sleepyTimeout = newNap;
        return true;
    }
    case 'amulet of change':
        eatenAccessoryHasEffect(item, messages);
        recordKnownAmuletDiscovery(name, item);
        if (game.u) {
            game.u.female = !game.u.female;
            messages.push(`You are suddenly very ${game.u.female ? 'feminine' : 'masculine'}!`);
        }
        return true;
    case 'amulet of unchanging':
        if (game.u?._polyself_form || game.u?.Upolyd || game.u?.polymorphed) {
            eatenAccessoryHasEffect(item, messages);
            recordKnownAmuletDiscovery(name, item);
            game.u._polyself_form = null;
            game.u.Upolyd = false;
            game.u.polymorphed = false;
        }
        return true;
    case 'amulet of strangulation':
        return eatenAmuletStrangulationChoke(item, messages);
    case 'amulet of life saving':
    case 'amulet of flying':
    case 'amulet of reflection':
        return false;
    default: {
        const field = EATEN_AMULET_PROPERTY_FIELDS.get(name);
        if (!field || !game.u) return false;
        if (!game.u[field]) eatenAccessoryHasEffect(item, messages);
        game.u[field] = true;
        if (field === 'magicalBreathing') game.u.Magical_breathing = true;
        return true;
    }
    }
}

function markEatenAccessoryTasted(item) {
    item.known = true;
    item.dknown = true;
    recordObservedObjectDiscovery(item);
}

function clearEatenWornRingState(item) {
    if (!wornRingItem(item)) return;
    delete item.worn;
    delete item.owornmask;
    delete item.wornMask;
    delete item._wornMask;
    if (typeof item.line === 'string')
        item.line = item.line.replace(/ \(on (?:left|right) hand\)/, '');
}

function applyEatenMetalAccessoryEffects(item, messages) {
    if (objectIsRingLike(item)) {
        const name = eatenRingName(item);
        if (!name) return false;
        markEatenAccessoryTasted(item);
        if (rn2(3)) return false;
        return applyEatenRingEffect(item, messages);
    }
    if (objectIsAmuletLike(item)) {
        const name = eatenAmuletName(item);
        if (!name) return false;
        markEatenAccessoryTasted(item);
        if (rn2(5)) return false;
        return applyEatenAmuletEffect(item, messages);
    }
    return false;
}

async function eatHeroNonFoodMetal(item, { floorObject = false } = {}) {
    if (!item) return false;
    if (!floorObject && heroMetalEatingWornBlock(item)) {
        await setMessage("You can't eat something you're wearing.");
        game._command_mode = null;
        return true;
    }

    if (heroIsRustMonsterPolyself() && (item.oerodeproof || item.rustproof)) {
        const spat = floorObject ? splitFloorMetalUnit(item) : splitCarriedMetalUnit(item);
        spat.rknown = true;
        spat.oerodeproof = false;
        spat.rustproof = false;
        const stunTurns = rn2(10);
        if (stunTurns > 0) addHeroStun(stunTurns);
        const name = pickupObjectName({ ...spat, quan: 1 });
        if (!floorObject && !(spat.cursed && objectIsRingLike(spat))) {
            if ((game.inventory || []).includes(spat)) removeInventoryItem(spat, spat.quan || 1);
            dropMetalObjectAtHero(spat);
            await setMessage(`Ulch - that ${name} was rustproofed!  You spit the ${name} out onto the floor.`);
        } else {
            if (!floorObject && (game.inventory || []).includes(spat)) refreshInventoryLineAfterMetalSplit(spat);
            await setMessage(`Ulch - that ${name} was rustproofed!  You spit out the ${name}.`);
        }
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }

    if (objectIsSlowDigestionRing(item)) {
        const rotten = rottenFoodEffect({ adjective: 'Awful', foodWord: metallivoreFoodWord(item) });
        await setMessage(`This ring is indigestible!  ${rotten.message}`, true);
        game._command_mode = null;
        game._process_time_with_more = 1;
        game.context.move = rotten.rottenSleepDuration ? rotten.rottenSleepDuration + 1 : 1;
        return true;
    }

    const name = pickupObjectName({ ...item, quan: 1 });
    const messages = [];
    if (item.opoisoned && isPoisonableWeaponObject(item)) {
        messages.push('Ecch - that must have been poisonous!');
        if (heroHasPoisonResistance()) {
            messages.push('You seem unaffected by the poison.');
        } else {
            const strengthDamage = rnd(4);
            const hpDamage = rnd(15);
            if (game.u?.acurr?.a) {
                game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - strengthDamage);
                exerciseAttribute(A_STR, false);
            }
            if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - hpDamage);
            if ((game.u?.uhp || 0) <= 0) {
                game._death_cause = `killed by a poisonous ${name}`;
                messages.push('You die...');
            }
        }
    } else if (!item.cursed) {
        messages.push(`This ${name} is delicious!`);
    }

    addHeroNutrition(heroMetalNonFoodNutrition(item));
    if (!floorObject && objectIsRingLike(item)) clearEatenWornRingState(item);
    applyEatenMetalAccessoryEffects(item, messages);
    const accessoryCommandMode = game._eaten_accessory_command_mode || null;
    const accessoryMore = !!game._eaten_accessory_more;
    const accessoryMove = game._eaten_accessory_move;
    delete game._eaten_accessory_command_mode;
    delete game._eaten_accessory_more;
    delete game._eaten_accessory_move;
    if (floorObject) removeEatenFloorMetalObject(item);
    else removeInventoryItem(item, item.quan || 1);
    game._pet_food_scan_inventory = game.inventory || [];
    await setMessage(messages.join('  ') || `This ${name} is delicious!`, messages.length > 1 || accessoryMore);
    game._command_mode = accessoryCommandMode;
    game.context.move = accessoryMove ?? 1;
    return true;
}

function wieldedItem() {
    return (game.inventory || []).find(item =>
        item.wielded || item.line?.includes('weapon in') || item.line?.includes('(wielded)')) || null;
}

function tinOpenerDelayWeapon() {
    const weapon = wieldedItem();
    const kind = objectKindKey(weapon);
    if (!weapon) return { weapon: null, delay: null, message: null, instantMessage: null };
    if (isTinOpenerObject(weapon)) {
        const range = weapon.cursed ? 3 : !weapon.blessed ? 2 : 1;
        return {
            weapon,
            delay: rn2(range),
            message: `Using ${inventoryItemName(weapon)} you try to open the tin.`,
            instantMessage: 'You easily open the tin.',
        };
    }
    if (weapon.otyp === DAGGER || weapon.otyp === ORCISH_DAGGER || weapon.otyp === KNIFE
        || /\b(?:dagger|knife|athame|stiletto|crysknife)\b/.test(kind)) {
        return {
            weapon,
            delay: 3,
            message: `Using ${inventoryItemName(weapon)} you try to open the tin.`,
            instantMessage: null,
        };
    }
    if (weapon.otyp === PICK_AXE || /\b(?:pick-axe|pick axe|pickaxe|axe|battle-axe)\b/.test(kind)) {
        return {
            weapon,
            delay: 6,
            message: `Using ${inventoryItemName(weapon)} you try to open the tin.`,
            instantMessage: null,
        };
    }
    return { weapon, delay: null, message: null, instantMessage: null };
}

function consumeTinObject(tin, floorObject = false) {
    if (floorObject) consumeOneFloorObject(tin);
    else consumeOneInventoryFood(tin);
    game._tin_opening_occupation = null;
}

function splitTinForCostlyAlteration(tin, floorObject = false) {
    if (!tin || (tin.quan || 1) <= 1) return tin;
    const split = { ...tin, id: next_ident(), quan: 1, line: '' };
    tin.quan = (tin.quan || 1) - 1;
    if (floorObject) {
        delete split.letter;
        delete split.line;
        game.level.objects ??= [];
        game.level.objects.push(split);
        newsym(split.ox, split.oy);
    } else {
        split.letter = nextInventoryLetter();
        if (tin.unpaid && !splitCarriedObjectShopBill(tin, split, 1))
            clearObjectShopBillState(split);
        refreshInventoryObjectLine(tin);
        if (tin.unpaid) syncUnpaidBillLine(tin);
        refreshInventoryObjectLine(split);
        game.inventory.push(split);
    }
    return split;
}

function costlyTinAlteration(tin, { floorObject = false, alterType = 'open' } = {}) {
    if (!tin) return tin;
    const shouldBill = floorObject
        ? (() => {
            if (tin.no_charge) return false;
            const x = tin.ox ?? game.u?.ux;
            const y = tin.oy ?? game.u?.uy;
            const itemShkp = shopkeeperForCostlySpot(x, y);
            const heroShkp = shopkeeperForCostlySpot(game.u?.ux, game.u?.uy);
            return !!itemShkp && sameShopkeeper(itemShkp, heroShkp) && shopkeeperInHisShop(itemShkp);
        })()
        : !!tin.unpaid;
    if (!shouldBill) return tin;

    const chargedTin = splitTinForCostlyAlteration(tin, floorObject);
    if (floorObject) {
        const x = chargedTin.ox ?? game.u?.ux;
        const y = chargedTin.oy ?? game.u?.uy;
        const shkp = shopkeeperForCostlySpot(x, y);
        const entry = shopBillEntryForObject(shkp, chargedTin);
        const price = entry ? shopBillEntryTotal(entry) : shopItemPrice(chargedTin, x, y);
        if (price > 0) {
            const billEntry = entry || addObjectToShopBill(shkp, chargedTin, price, { useup: true });
            if (billEntry) {
                markObjectShopBillUsedUp(chargedTin, shkp);
                chargedTin.no_charge = true;
            } else if (shopBillIsFull(shkp)) {
                markAlteredShopObjectNoCharge(chargedTin);
            }
        }
        return chargedTin;
    }

    const { shkp } = shopkeeperOwningBillEntry(chargedTin);
    markObjectShopBillUsedUp(chargedTin, shkp || heroShopkeeper());
    return chargedTin;
}

function tinContentName(tin) {
    const monster = tin?.corpsenm;
    if (!monster?.name) return 'monster';
    return monster.name;
}

function tinSmellName(tin) {
    const name = tinContentName(tin);
    if (name === 'cockatrice' || name === 'chickatrice') return 'chicken';
    return pluralizeMonsterName(name);
}

function applyTinMonsterSideEffects(tin) {
    const name = tinContentName(tin);
    if (name === 'newt' && (rn2(3) || 3 * (game.u?.uen || 0) <= 2 * (game.u?.uenmax || 0))) {
        const oldEnergy = game.u?.uen || 0;
        if (game.u) game.u.uen = oldEnergy + rnd(3);
        if ((game.u?.uen || 0) > (game.u?.uenmax || 0)) {
            if (!rn2(3) && game.u) game.u.uenmax = (game.u.uenmax || 0) + 1;
            if (game.u) game.u.uen = game.u.uenmax || 0;
        }
        return (game.u?.uen || 0) !== oldEnergy ? 'You feel a mild buzz.' : '';
    }
    return '';
}

function tinTrapDamage() {
    const difficulty = Math.max(1, level_difficulty());
    const sides = 5 + (difficulty < 5 ? difficulty : 2 + Math.trunc(difficulty / 2));
    return rnd(sides);
}

async function explodeTinTrap(tin, floorObject = false) {
    const damage = tinTrapDamage();
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        game.u._stunTimeout = (game.u._stunTimeout || 0) + damage;
        addHeroStatusSuffix('Stun');
    }
    exerciseAttribute(A_STR, false);
    tin = costlyTinAlteration(tin, { floorObject, alterType: 'destroy' });
    consumeTinObject(tin, floorObject);
    await setMessage('KABOOM!!  The tin was booby-trapped!', true);
    game._command_mode = null;
    game.context.move = 1;
}

async function finishTinContents(tin, floorObject = false, eat = true, knownVariety = null, options = {}) {
    if (!eat) {
        tin = costlyTinAlteration(tin, { floorObject });
        consumeTinObject(tin, floorObject);
        await setMessage('You discard the open tin.');
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    const r = knownVariety == null ? tinVariety(tin, false) : knownVariety;
    const messages = [];
    if (options.openingMessage) messages.push(options.openingMessage);
    if (r === SPINACH_TIN) {
        if (tin.cursed) messages.push('It contains some decaying green substance.');
        else {
            messages.push('It contains spinach.');
            messages.push(`This makes you feel like ${game.u?.fixedAbilities ? (game.flags?.female ? 'Olive Oyl' : 'Bluto') : 'Popeye'}!`);
            tin.known = true;
        }
        if (game.u?.acurr?.a && !game.u.fixedAbilities) {
            const before = game.u.acurr.a[A_STR] ?? 10;
            const after = Math.min(118, before + 1);
            game.u.acurr.a[A_STR] = after;
            if (game.u.amax?.a) game.u.amax.a[A_STR] = Math.max(game.u.amax.a[A_STR] || after, after);
        }
        tin = costlyTinAlteration(tin, { floorObject });
        consumeTinObject(tin, floorObject);
        let nutrition = tin.blessed ? 600 : !tin.cursed ? 400 + rnd(200) : 200 + rnd(400);
        if (options.ateTin) nutrition += 5;
        addHeroNutrition(nutrition);
        await setMessage(messages.join('  '), messages.length > 1);
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    if (!tin.corpsenm?.name || tin.emptyTin || objectKindKey(tin) === 'empty tin') {
        tin.known = true;
        tin = costlyTinAlteration(tin, { floorObject });
        consumeTinObject(tin, floorObject);
        if (options.ateTin) addHeroNutrition(5);
        messages.push("It turns out to be empty.");
        await setMessage(messages.join('  '), messages.length > 1);
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    const varietyText = TIN_VARIETY_TEXTS[r] || '';
    const monsterName = tinContentName(tin);
    messages.push(`You consume ${varietyText} ${monsterName}.`);
    const sideEffect = applyTinMonsterSideEffects(tin);
    if (sideEffect) messages.push(sideEffect);
    tin.known = true;
    let nutrition = null;
    if ((TIN_VARIETY_NUTRITION[r] || 0) < 0) {
        addHeroVomiting(rn1(15, 10));
    } else {
        nutrition = TIN_VARIETY_NUTRITION[r] || 0;
        if (r === HOMEMADE_TIN)
            nutrition = Math.min(nutrition, CORPSE_NUTRITION.get(monsterName) || nutrition);
        if (options.ateTin) nutrition += 5;
    }
    if (TIN_GREASY_VARIETIES.has(r)) {
        const already = game.u?._glibTimeout || 0;
        if (game.u) game.u._glibTimeout = already + rn1(11, 5);
        messages.push(`Eating ${varietyText} food made your ${wornGlovesItem() ? 'gloves' : 'fingers'} ${already ? 'even more' : 'very'} slippery.`);
    }
    tin = costlyTinAlteration(tin, { floorObject });
    consumeTinObject(tin, floorObject);
    if (nutrition != null) addHeroNutrition(nutrition);
    await setMessage(messages.join('  '), messages.length > 1);
    game._command_mode = null;
    game.context.move = 1;
}

async function consumeOpenedTin(tin, floorObject = false, openMessage = 'You succeed in opening the tin.') {
    if (!tin) return false;
    const r = tinVariety(tin, false);
    const alwaysEat = heroIsMetallivorous();
    if (tin.otrapped || (tin.cursed && r !== HOMEMADE_TIN && !rn2(8))) {
        await explodeTinTrap(tin, floorObject);
        return true;
    }

    if (!tin.corpsenm?.name && r !== SPINACH_TIN) {
        tin.known = true;
        tin = costlyTinAlteration(tin, { floorObject });
        consumeTinObject(tin, floorObject);
        if (alwaysEat) addHeroNutrition(5);
        await setMessage(`${openMessage}  It turns out to be empty.`, true);
        game._command_mode = null;
        game.context.move = 1;
        return true;
    }

    if (alwaysEat) {
        await finishTinContents(tin, floorObject, true, r, { openingMessage: openMessage, ateTin: true });
        return true;
    }

    const contents = r === SPINACH_TIN ? 'spinach' : tinSmellName(tin);
    const first = r === SPINACH_TIN
        ? `${openMessage}  ${tin.cursed ? 'It contains some decaying green substance.' : 'It contains spinach.'}`
        : `${openMessage}  It smells like ${contents}.`;
    game._tin_opened_pending = { tin, floorObject, variety: r };
    await setMessage(`${first}  Eat it? [yn] (n)`, true);
    game._command_mode = 'tinEatConfirm';
    game.context.move = 1;
    return true;
}

export function processTinOpeningOccupation() {
    const occ = game._tin_opening_occupation;
    if (!occ) return null;
    const tin = occ.tin;
    if (!tin) {
        game._tin_opening_occupation = null;
        return null;
    }
    if (occ.floorObject && (!game.level?.objects?.includes(tin)
        || tin.ox !== game.u?.ux || tin.oy !== game.u?.uy || !heroCanReachFloorForSit().ok)) {
        game._tin_opening_occupation = null;
        return null;
    }
    if (!occ.floorObject && !(game.inventory || []).includes(tin)) {
        game._tin_opening_occupation = null;
        return null;
    }
    occ.usedtime ||= 0;
    if (occ.usedtime++ >= 50) {
        game._tin_opening_occupation = null;
        return { message: 'You give up your attempt to open the tin.' };
    }
    if (occ.usedtime < occ.reqtime) return null;
    game._tin_opening_occupation = null;
    return { finish: { tin, floorObject: !!occ.floorObject } };
}

export async function finishTinOpeningOccupation(pending) {
    const info = pending || game._tin_finish_after_turn;
    game._tin_finish_after_turn = null;
    if (!info?.tin) return false;
    return consumeOpenedTin(info.tin, !!info.floorObject, 'You succeed in opening the tin.');
}

async function startTinOpening(tin, floorObject = false) {
    if (!tin) return false;
    const form = polyselfForm();
    let message = '';
    let delay = null;
    let instantMessage = null;
    if (heroIsMetallivorous()) {
        message = 'You bite right into the metal tin...';
        delay = 0;
    } else if (polyselfNoHands() || form?.verysmall) {
        await setMessage('You cannot handle the tin properly to open it.');
        game._command_mode = null;
        game.context.move = 1;
        return true;
    } else if (tin.blessed) {
        delay = isTinOpenerObject(wieldedItem()) && wieldedItem()?.blessed ? 0 : rn2(2);
        if (!delay) message = 'The tin opens like magic!';
        else message = 'The tin seems easy to open.';
    } else {
        const opener = tinOpenerDelayWeapon();
        if (opener.delay != null) {
            delay = opener.delay;
            message = opener.message;
            instantMessage = opener.instantMessage;
        } else {
            message = 'It is not so easy to open this tin.';
            if (game.u?._glibTimeout || game.u?.glib || (game.u?._statusSuffix || '').includes('Slippery')) {
                const slip = `The tin slips from your ${wornGlovesItem() ? 'gloves' : 'fingers'}.`;
                if (!floorObject) {
                    if ((tin.quan || 1) > 1) {
                        tin.quan--;
                        refreshInventoryObjectLine(tin);
                    } else {
                        game.inventory = (game.inventory || []).filter(item => item !== tin);
                    }
                    tin = { ...tin, id: next_ident(), quan: 1, ox: game.u?.ux || 0, oy: game.u?.uy || 0 };
                    game.level.objects ??= [];
                    game.level.objects.push(tin);
                    newsym(tin.ox, tin.oy);
                }
                await setMessage(`${message}  ${slip}`, true);
                game._command_mode = null;
                game.context.move = 1;
                return true;
            }
            const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
            const str = game.u?.acurr?.a?.[A_STR] ?? 10;
            delay = rn1(1 + Math.trunc(500 / Math.max(1, dex + str)), 10);
        }
    }

    if (!delay) {
        await consumeOpenedTin(tin, floorObject, instantMessage || message || 'You succeed in opening the tin.');
        return true;
    }
    game._tin_opening_occupation = { tin, floorObject, reqtime: delay, usedtime: 0 };
    await setMessage(message);
    game._command_mode = null;
    game.context.move = 1;
    return true;
}

function wieldTinOpenerForUse(opener) {
    if (!opener || opener.wielded || opener.line?.includes('(wielded)') || opener.line?.includes('weapon in')) return;
    const previous = wieldedItem();
    if (previous && previous !== opener) {
        previous.wielded = false;
        previous.alternate = true;
        previous.line = `${previous.letter || '?'} - ${inventoryItemName(previous)} (alternate weapon${(previous.quan || 1) > 1 ? 's' : ''}; not wielded)`;
    }
    opener.wielded = true;
    opener.alternate = false;
    opener.line = `${opener.letter || '?'} - ${inventoryItemName(opener)} (wielded)`;
}

async function beginTinOpenerUse(opener) {
    const tins = (game.inventory || []).filter(isTinObject);
    if (!tins.length) {
        await setMessage('You have no tin to open.');
        game._command_mode = null;
        return true;
    }
    wieldTinOpenerForUse(opener);
    if (tins.length === 1) {
        await startTinOpening(tins[0], false);
        return true;
    }
    const letters = tins.map(item => item.letter).filter(Boolean).join('');
    await setMessage(`What do you want to open? [${getobjPromptLetters(letters)} or ?*]`);
    game._tin_opener_letter = opener?.letter || '';
    game._command_mode = 'tinOpenerObject';
    return true;
}

function parseWishedTinName(lowerName) {
    if (lowerName === 'spinach') return { content: 'spinach' };
    if (lowerName === 'tin' || lowerName === 'tins') return { content: 'plain' };

    const tinOf = lowerName.match(/(?:^| )tins?\s+of\s+(.+)$/);
    if (!tinOf) return null;

    let rest = tinOf[1].trim().replace(/^(?:a|an|the)\s+/, '');
    if (rest === 'spinach') return { content: 'spinach' };

    let varietyIndex = null;
    for (let i = 0; i < TIN_VARIETY_TEXTS.length; ++i) {
        const prefix = `${TIN_VARIETY_TEXTS[i]} `;
        if (rest.startsWith(prefix)) {
            varietyIndex = i;
            rest = rest.slice(prefix.length);
            break;
        }
    }

    const monsterName = rest.replace(/\s+meat$/, '').replace(/^(?:a|an|the)\s+/, '');
    const monster = wishedMonsterByName(monsterName);
    if (!monster) return { content: 'plain', varietyIndex };
    return { content: 'monster', monster, varietyIndex };
}

function makeWishedTinObject(tinWish) {
    const otmp = mksobj(TIN, true, false);
    Object.assign(otmp, {
        cls: 'food',
        glyph: '%',
        kind: 'tin',
        actualKind: 'tin',
        singular: 'tin',
        plural: 'tins',
        wishedfor: true,
        _wish_ignore_requested_spe: true,
    });
    otmp.spe = 0;

    if (tinWish?.content === 'spinach') {
        setTinSpinach(otmp);
        otmp._wish_tin_explicit_content = true;
    } else if (tinWish?.content === 'monster') {
        const binding = wishedTinMonsterBinding(tinWish.monster);
        if (binding.empty) {
            setTinEmpty(otmp);
            otmp._wish_tin_explicit_content = true;
        } else if (binding.monster) {
            setTinMonster(otmp, binding.monster);
            otmp._wish_tin_explicit_content = true;
        }
        if (tinWish.varietyIndex != null)
            otmp._wish_tin_requested_variety = tinWish.varietyIndex;
    } else if (tinWish?.varietyIndex != null) {
        otmp._wish_tin_requested_variety = tinWish.varietyIndex;
    }
    return otmp;
}

function isBlankScrollItem(item) {
    return item?.otyp === SCR_BLANK_PAPER || item?.kind === 'blank paper'
        || item?.actualKind === 'scroll of blank paper'
        || item?.scrollIndex === IDENTIFIED_SCROLL_NAMES.length;
}

function isBlankSpellbookItem(item) {
    if (!(item?.cls === 'spellbook' || item?.otyp === SPBOOK_NO_NOVEL)) return false;
    const name = item.spellName || item.spell?.name || String(item.kind || '').replace(/^spellbook(?: of)? /, '');
    return name === 'blank paper' || (!name && item.appearance === 'plain');
}

const MARKER_SCROLL_INK_COSTS = new Map([
    ['light', 8], ['gold detection', 8], ['food detection', 8], ['magic mapping', 8],
    ['amnesia', 8], ['fire', 8], ['earth', 8],
    ['destroy armor', 10], ['create monster', 10], ['punishment', 10],
    ['confuse monster', 12],
    ['identify', 14],
    ['enchant armor', 16], ['remove curse', 16], ['enchant weapon', 16], ['charging', 16],
    ['scare monster', 20], ['stinking cloud', 20], ['taming', 20], ['teleportation', 20],
    ['genocide', 30],
]);

function markerPaperType(item) {
    if (item?.cls === 'spellbook' || item?.glyph === '+' || item?.otyp === SPBOOK_NO_NOVEL) return 'spellbook';
    if (item?.cls === 'scroll' || item?.glyph === '?' || item?.otyp === SCROLL_CLASS) return 'scroll';
    return '';
}

function markerPaperTypeWord(item) {
    const type = markerPaperType(item);
    if (type === 'spellbook') return isBookOfTheDeadItem(item) ? 'book' : 'spellbook';
    return type || 'scroll';
}

function markerWritePrompt(item) {
    return `What type of ${markerPaperTypeWord(item)} do you want to write?`;
}

function normalizeMarkerWriteName(text) {
    let name = String(text || '').trim().replace(/\s+/g, ' ');
    name = normalizeWishedSpelling(name).toLowerCase();
    if (name.startsWith('scroll ')) name = name.slice(7);
    else if (name.startsWith('spellbook ')) name = name.slice(10);
    if (name.startsWith('of ')) name = name.slice(3);
    return name.trim();
}

function markerResolveWrittenType(rawText, paper) {
    const paperType = markerPaperType(paper);
    const normalized = normalizeMarkerWriteName(rawText);
    if (!normalized) return null;
    const alias = resolveWishedSpellingAlias(
        paperType === 'scroll' ? normalized : `spellbook of ${normalized}`,
    ).name;
    const name = paperType === 'scroll'
        ? alias.replace(/^scroll of /, '')
        : alias.replace(/^spellbook(?: of)?\s+/, '');

    if (name === 'blank paper') return { paperType, name, blank: true };
    if (paperType === 'scroll') {
        const index = IDENTIFIED_SCROLL_NAMES.indexOf(name);
        if (index < 0) return null;
        return { paperType, name, index, inkCost: MARKER_SCROLL_INK_COSTS.get(name) || 1000 };
    }
    if (paperType === 'spellbook') {
        const level = SPELLBOOK_LEVELS[name] || 0;
        if (!(level > 0)) return null;
        return { paperType, name, spellbookIndex: Object.keys(SPELLBOOK_LEVELS).indexOf(name), inkCost: 10 * level };
    }
    return null;
}

function markerKnownWrittenType(target) {
    if (!target) return false;
    if (target.paperType === 'scroll') return scrollDiscoveryKnown(target.name);
    if (target.paperType === 'spellbook') return spellbookDiscoveryKnown(target.name);
    return false;
}

function markerWrittenObject(target, marker, paper, letter) {
    const curseval = (marker?.blessed ? 1 : marker?.cursed ? -1 : 0)
        + (paper?.blessed ? 1 : paper?.cursed ? -1 : 0);
    const base = {
        id: next_ident(),
        quan: 1,
        ox: game.u?.ux || paper?.ox || 0,
        oy: game.u?.uy || paper?.oy || 0,
        letter,
        bknown: !!(paper?.bknown && marker?.bknown),
        blessed: curseval > 0,
        cursed: curseval < 0,
        dknown: false,
    };
    if (target.paperType === 'scroll') {
        const known = scrollDiscoveryKnown(target.name);
        const label = game._object_descriptions?.scrolls?.[target.index] || 'ZELGO MER';
        return Object.assign(base, {
            cls: 'scroll',
            glyph: '?',
            otyp: SCROLL_CLASS,
            kind: known ? `scroll of ${target.name}` : `scroll labeled ${label}`,
            actualKind: `scroll of ${target.name}`,
            scrollIndex: target.index,
            known,
        });
    }
    const known = spellbookDiscoveryKnown(target.name);
    return Object.assign(base, {
        cls: 'spellbook',
        glyph: '+',
        otyp: SPBOOK_NO_NOVEL,
        kind: `spellbook of ${target.name}`,
        spellName: target.name,
        spellbookIndex: target.spellbookIndex,
        appearance: game._object_descriptions?.spellbooks?.[target.spellbookIndex] || '',
        known,
    });
}

async function finishMarkerWriting() {
    const marker = (game.inventory || []).find(item => item.letter === game._apply_marker_letter);
    const paper = (game.inventory || []).find(item => item.letter === game._marker_write_paper_letter);
    const text = game._marker_write_text || '';
    game._apply_marker_letter = null;
    game._marker_write_paper_letter = null;
    game._marker_write_text = '';
    game._command_mode = null;

    if (!marker || !paper) return false;
    const target = markerResolveWrittenType(text, paper);
    const typeword = markerPaperTypeWord(paper);
    if (!target) {
        await setMessage(`There is no such ${typeword}!`);
        game.context.move = 1;
        return true;
    }
    if (target.blank) {
        await setMessage("You can't write that!  It's obscene!");
        game.context.move = 1;
        return true;
    }

    const messages = [];
    checkUnpaidUsage(marker, messages);
    const inkCost = target.inkCost || 1000;
    const halfCost = Math.trunc(inkCost / 2);
    if ((marker.spe ?? 0) < halfCost) {
        messages.push('Your marker is too dry to write that!');
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return true;
    }

    const actualCost = rn1(halfCost, halfCost);
    if ((marker.spe ?? 0) < actualCost) {
        marker.spe = 0;
        updateChargedItemLine(marker);
        messages.push('Your marker dries out!');
        if (target.paperType === 'spellbook') {
            messages.push('The spellbook is left unfinished and your writing fades.');
        } else {
            messages.push('The scroll is now useless and disappears!');
            useUpInventoryItem(paper);
        }
        await setMessage(messages.join('  '), true);
        game.context.move = 1;
        return true;
    }

    marker.spe = Math.max(0, (marker.spe ?? 0) - actualCost);
    updateChargedItemLine(marker);

    if (!markerKnownWrittenType(target) && !game.flags?.debug) {
        messages.push("You don't know how to write that.");
        if (target.paperType === 'spellbook') {
            messages.push('You write in your best handwriting:  "My Diary", but it quickly fades.');
        } else {
            messages.push(`You write "${game.plname || 'You'} was here!" and the scroll disappears.`);
            useUpInventoryItem(paper);
        }
        await setMessage(messages.join('  '), true);
        game.context.move = 1;
        return true;
    }

    const letter = paper.letter;
    useUpInventoryItem(paper);
    const written = markerWrittenObject(target, marker, paper, letter);
    written.line = normalInventoryLine({ ...written, line: '' });
    game.inventory ??= [];
    game.inventory.push(written);
    if (target.paperType === 'spellbook')
        messages.push('The spellbook warps strangely.');
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
    return true;
}

function isBookOfTheDeadItem(item) {
    if (item?.otyp === BOOK_OF_THE_DEAD) return true;
    const text = [
        item?.kind,
        item?.actualKind,
        item?.artifact,
        item?.name,
        item?.otyp,
    ].filter(Boolean).join(' ').toLowerCase();
    return text.includes('book of the dead');
}

function readBlindBlockMessage(item, isScroll) {
    if (!game.u?.blind || isBookOfTheDeadItem(item)) return '';
    if (item?.cls === 'spellbook') return 'Being blind, you cannot read the mystic runes.';
    if (isScroll && item?.dknown === false) return 'Being blind, you cannot read the formula on the scroll.';
    return '';
}

export function heroHasAntimagic() {
    if (game.u?.magicResistance || game.u?.antimagic) return true;
    if (polyselfFormHasAntimagic()) return true;
    return !!activeAntimagicSource();
}

export function heroHasColdResistance() {
    if (game.u?.coldResistance) return true;
    if (polyselfFormHasColdResistance()) return true;
    return (game.inventory || []).some(item => activeInventoryResistanceKind(item) === 'cold');
}

export function heroHasSlowDigestion() {
    if (game.u?.slowDigestion) return true;
    return !!activeSlowDigestionSource();
}

export function applyHeroOrdinaryHunger() {
    if (!game.u || heroHasSlowDigestion()) return;
    game.u.uhunger = (game.u.uhunger ?? 900) - 1;
}

function heroHasPoisonResistance() {
    if (game.u?.poisonResistance || heroWearsAlchemySmock()) return true;
    return (game.inventory || []).some(item => {
        if (!(item.worn || item.line?.includes('being worn'))) return false;
        return /amulet (?:versus|of) poison|poison resistance/i
            .test(String(item.kind || item.actualKind || item.line || ''));
    });
}

function wornGlovesItem() {
    return (game.inventory || []).find(item => isWornArmorItem(item) && armorSlot(item) === 'gloves') || null;
}

function makeHeroBlindedFromBook(messages) {
    const duration = rn1(100, 250);
    if (!game.u) return;
    const wasBlind = !!game.u.blind;
    game.u._blindTimeout = (game.u._blindTimeout || 0) + duration;
    game.u.blind = true;
    if (!wasBlind) {
        messages.push(heroIsHallucinating()
            ? 'Oh, bummer!  Everything is dark!  Help!'
            : 'A cloud of darkness falls upon you.');
    }
}

function takeGoldFromHeroForBook(messages) {
    const hadGold = (game._goldCount || 0) > 0
        || (game.inventory || []).some(item => item.cls === 'coin' || item.otyp === GOLD_PIECE || item.letter === '$');
    if (!hadGold) {
        messages.push('You feel a strange sensation.');
        return;
    }
    game._goldCount = 0;
    game._just_picked_gold = 0;
    game.inventory = (game.inventory || []).filter(item =>
        !(item.cls === 'coin' || item.otyp === GOLD_PIECE || item.letter === '$'));
    game._pet_food_scan_inventory = game.inventory;
    messages.push('You notice you have no gold!');
}

function corrodeGlovesFromBook(gloves, messages) {
    const kind = armorKind(gloves);
    const corrodeable = /\b(?:gauntlets of power|iron|metal|copper|bronze)\b/.test(kind);
    if (!corrodeable) {
        messages.push('Your gloves are not affected by the corrosion.');
        return;
    }
    if (gloves.oerodeproof) {
        gloves.rknown = true;
        messages.push('Somehow, your gloves are not affected by the corrosion.');
        updateArmorLine(gloves);
        return;
    }
    if (gloves.blessed && !rnl(4)) {
        messages.push('Somehow, your gloves are not affected by the corrosion.');
        return;
    }
    const current = Math.min(3, gloves.oeroded2 || 0);
    if (current >= 3) return;
    const adverb = current + 1 === 3 ? ' completely' : current ? ' further' : '';
    messages.push(`Your gloves ${erosionVerb(gloves, 'corrode')}${adverb}!`);
    const oldAc = wornArmorAcValueGreatestErosion(gloves);
    gloves.oeroded2 = current + 1;
    gloves.bknown = true;
    updateWornArmorAcAfterChange(gloves, oldAc);
}

function poisonHeroFromSpellbook(messages) {
    messages.push('The book was coated with contact poison!');
    const gloves = wornGlovesItem();
    if (gloves) {
        corrodeGlovesFromBook(gloves, messages);
        return { dead: false };
    }

    const resisted = heroHasPoisonResistance();
    const strLoss = resisted ? rn1(2, 1) : rn1(4, 3);
    const damage = rnd(resisted ? 6 : 10);
    if (game.u?.acurr?.a) {
        game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - strLoss);
        messages.push('You feel weaker.');
    }
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a contact-poisoned spellbook';
            messages.push('You die...');
            return { dead: true };
        }
    }
    exerciseAttribute(A_STR, false);
    return { dead: false };
}

function aggravateMonstersFromBook() {
    for (const mon of game.level?.monsters || []) {
        mon.msleeping = 0;
        if (mon.mcanmove === false && !rn2(5)) {
            mon.mfrozen = 0;
            mon.mcanmove = true;
        }
    }
}

function rndcurseFromBook(messages) {
    if (heroHasAntimagic()) messages.push('A magical shield surrounds you!');
    messages.push('You feel a malignant aura surround you.');
    const candidates = (game.inventory || [])
        .filter(item => !(item.cls === 'coin' || item.otyp === GOLD_PIECE || item.letter === '$'));
    let count = rnd(heroHasAntimagic() ? 3 : 6);
    while (count-- > 0 && candidates.length) {
        const item = candidates[rnd(candidates.length) - 1];
        if (item?.cursed) continue;
        if (item.blessed) item.blessed = false;
        else item.cursed = true;
        syncCarriedFigurineTransformTimer(item);
        item.bknown = true;
        item.line = normalInventoryLine({ ...item, line: '' });
    }
}

function cursedBookStudyEffect(level) {
    const messages = [];
    let gone = false;
    let dead = false;
    switch (rn2(level)) {
    case 0: {
        messages.push('You feel a wrenching sensation.');
        if (game.level?.at && game.u) {
            const teleportMessage = safeTeleportHeroSameLevel();
            if (teleportMessage) messages.push(teleportMessage);
        }
        break;
    }
    case 1:
        messages.push('You feel threatened.');
        aggravateMonstersFromBook();
        break;
    case 2:
        makeHeroBlindedFromBook(messages);
        break;
    case 3:
        takeGoldFromHeroForBook(messages);
        break;
    case 4:
        messages.push('These runes were just too much to comprehend.');
        addHeroConfusion(rn1(7, 16));
        break;
    case 5: {
        const result = poisonHeroFromSpellbook(messages);
        dead = !!result.dead;
        break;
    }
    case 6:
        gone = true;
        if (heroHasAntimagic()) {
            messages.push('The book radiates explosive energy, but you are unharmed!');
        } else {
            messages.push('As you read the book, it radiates explosive energy in your face!');
            const damage = 2 * rnd(10) + 5;
            if (game.u) {
                game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
                if ((game.u.uhp || 0) <= 0) {
                    game._death_cause = 'killed by an exploding rune';
                    messages.push('You die...');
                    dead = true;
                }
            }
        }
        break;
    default:
        rndcurseFromBook(messages);
        break;
    }
    return { messages, gone, dead, more: messages.length > 1 || dead };
}

function articleFor(noun) {
    const text = String(noun || '').trim();
    if (!text) return 'a thing';
    if (/^(?:the|a|an)\b/i.test(text)) return text;
    return `${/^[aeiou]/i.test(text) ? 'an' : 'a'} ${text}`;
}

function spellbookCallAppearance(item) {
    return item?.appearance || game._object_descriptions?.spellbooks?.[item?.spellbookIndex] || '';
}

function spellbookCallKey(item) {
    return spellbookCallAppearance(item) || String(item?.spellbookIndex ?? item?.spellName ?? item?.kind ?? 'spellbook');
}

function spellbookDiscoveryKnown(name) {
    const discoveryName = `spellbook of ${name}`;
    return (game._discoveries || []).some(entry =>
        entry.section === 'Spellbooks'
        && entry.name === discoveryName
        && entry.known !== false);
}

function shouldTryCallSpellbook(item, name) {
    if (!item || isBookOfTheDeadItem(item) || item.dknown === false) return false;
    if (item.known === true || spellbookDiscoveryKnown(name)) return false;
    return !game._called_spellbooks?.[spellbookCallKey(item)];
}

function prepareSpellbookTryCall(item, moveCost) {
    const appearance = spellbookCallAppearance(item);
    const noun = appearance ? `${appearance} spellbook` : 'spellbook';
    game._call_spellbook_prompt_noun = noun;
    game._call_spellbook_appearance = appearance;
    game._call_spellbook_key = spellbookCallKey(item);
    game._call_spellbook_move_cost = moveCost;
    game._call_spellbook_text = '';
}

function clearSpellbookTryCall() {
    game._call_spellbook_prompt_noun = '';
    game._call_spellbook_appearance = '';
    game._call_spellbook_key = '';
    game._call_spellbook_text = '';
    const moveCost = game._call_spellbook_move_cost || 0;
    game._call_spellbook_move_cost = 0;
    return moveCost;
}

function recordCalledSpellbook(name) {
    const call = String(name || '').trim();
    if (!call) return;
    const appearance = game._call_spellbook_appearance || '';
    const key = game._call_spellbook_key || appearance || 'spellbook';
    game._called_spellbooks ??= {};
    game._called_spellbooks[key] = call;
    game._discoveries ??= [];
    const discoveryName = `spellbook called ${call}`;
    const text = appearance ? `${discoveryName} (${appearance})` : discoveryName;
    const existing = game._discoveries.find(entry =>
        entry.section === 'Spellbooks'
        && (entry.name === discoveryName || (appearance && String(entry.text || '').endsWith(`(${appearance})`))));
    if (existing) {
        existing.name = discoveryName;
        existing.text = text;
        existing.starred = false;
        existing.known = true;
    } else {
        game._discoveries.push({ section: 'Spellbooks', name: discoveryName, text, starred: false, known: true });
    }
}

function learnScrollByName(scrollName, item, scrollIndex = null) {
    const discoveryName = `scroll of ${scrollName}`;
    learnObjectScore('Scrolls', discoveryName);
    const rawLabel = String(item?.kind || '');
    const label = rawLabel.startsWith('scroll labeled ')
        ? rawLabel.replace(/^scroll labeled /, '')
        : scrollIndex != null ? game._object_descriptions?.scrolls?.[scrollIndex] : '';
    game._discoveries ??= [];
    const discovery = game._discoveries.find(entry => entry.section === 'Scrolls' && entry.name === discoveryName);
    const text = label ? `${discoveryName} (${label})` : discoveryName;
    if (discovery) {
        discovery.known = true;
        discovery.text = text;
    } else {
        game._discoveries.push({
            section: 'Scrolls',
            name: discoveryName,
            text,
            starred: false,
            known: true,
        });
    }
}

function containedObjectMatching(obj, predicate, seen = new Set()) {
    if (!obj || seen.has(obj)) return null;
    seen.add(obj);
    if (predicate(obj)) return obj;
    for (const child of obj.contents || obj.cobj || []) {
        const found = containedObjectMatching(child, predicate, seen);
        if (found) return found;
    }
    return null;
}

function isGoldObject(item, includeGoldMaterial = false) {
    if (!item) return false;
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') return true;
    if (!includeGoldMaterial) return false;
    const name = String(item.kind || item.actualKind || item.name || '').toLowerCase();
    return /\bgold(?:en)?\b/.test(name);
}

function isFoodObject(item) {
    return item?.cls === 'food' || item?.otyp === FOOD_CLASS
        || item?.otyp === 'corpse' || item?.otyp === CORPSE
        || item?.otyp === EGG || item?.otyp === TIN || item?.glyph === '%';
}

function detectedObjectGlyph(item) {
    if (isGoldObject(item)) return { ch: '$', color: CLR_YELLOW, dec: false };
    if (isPotionObject(item) || item?.glyph === '!') return { ch: '!', color: item._appearance_color ?? item.color ?? NO_COLOR, dec: false };
    if (isFoodObject(item)) return { ch: '%', color: item.color ?? item._display_color ?? NO_COLOR, dec: false };
    if (item?.cls === 'scroll' || item?.glyph === '?') return { ch: '?', color: CLR_WHITE, dec: false };
    if (item?.cls === 'spellbook' || item?.glyph === '+') return { ch: '+', color: item.color ?? NO_COLOR, dec: false };
    return { ch: item?.glyph || '?', color: item?.color ?? NO_COLOR, dec: false };
}

function markDetectedGlyph(target, x, y) {
    const loc = game.level?.at(x, y);
    if (!loc) return;
    const glyph = detectedObjectGlyph(target);
    loc.remembered_glyph = { ch: glyph.ch, color: glyph.color, dec: glyph.dec };
    loc.waslit = true;
    show_glyph_cell(x, y, glyph.ch, glyph.color, glyph.dec);
}

function collectDetectedObjects(predicate) {
    const found = { here: [], remote: [] };
    const ux = game.u?.ux ?? -1;
    const uy = game.u?.uy ?? -1;
    for (const obj of game.level?.objects || []) {
        if (obj.hidden || obj.transientProjectile || obj.ox == null || obj.oy == null) continue;
        const target = containedObjectMatching(obj, predicate);
        if (!target) continue;
        target.seen = true;
        target.dknown = true;
        target._hide_until_seen = false;
        const entry = { target, x: obj.ox, y: obj.oy };
        if (obj.ox === ux && obj.oy === uy) found.here.push(entry);
        else found.remote.push(entry);
    }
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || mon.mhp <= 0 || mon.mx == null || mon.my == null) continue;
        const target = containedObjectMatching({ contents: mon.minvent || [] }, predicate);
        if (!target) continue;
        const entry = { target, x: mon.mx, y: mon.my, monster: mon };
        if (mon.mx === ux && mon.my === uy) found.here.push(entry);
        else found.remote.push(entry);
    }
    return found;
}

function markDetectedObjects(entries) {
    for (const entry of entries) markDetectedGlyph(entry.target, entry.x, entry.y);
    if (entries.length && game.u?.ux != null) newsym(game.u.ux, game.u.uy);
}

function carriedGoldInInventory() {
    if ((game._goldCount || 0) > 0) return true;
    return (game.inventory || []).some(item => isGoldObject(item) || containedObjectMatching(item, isGoldObject));
}

function detectTrapsFromGoldDetection(item) {
    const ux = game.u?.ux ?? -1;
    const uy = game.u?.uy ?? -1;
    const remote = [];
    let here = false;
    for (const trap of game.level?.traps || []) {
        if (!trap) continue;
        if (trap.tx === ux && trap.ty === uy) here = true;
        else remote.push(trap);
    }
    if (remote.length) {
        for (const trap of remote) {
            if (item.cursed) {
                rnd(10);
                markDetectedGlyph({ otyp: GOLD_PIECE, glyph: '$', color: CLR_YELLOW }, trap.tx, trap.ty);
            } else {
                trap.tseen = true;
                const loc = game.level?.at(trap.tx, trap.ty);
                if (loc) {
                    loc.remembered_glyph = { ch: '^', color: CLR_BROWN, dec: false };
                    loc.waslit = true;
                }
                show_glyph_cell(trap.tx, trap.ty, '^', CLR_BROWN, false);
            }
        }
        newsym(ux, uy);
        return { detected: true, known: false, message: item.cursed ? 'You feel very greedy.' : 'You feel entrapped.', more: true };
    }
    if (here) return { detected: true, known: false, message: 'Your toes itch.', more: false };
    return { detected: false, known: false, message: 'Your toes stop itching.', more: false };
}

function goldDetectionScrollEffect(item) {
    if (heroIsConfused() || item.cursed) return detectTrapsFromGoldDetection(item);

    const includeGoldMaterial = !!item.blessed;
    const goldPredicate = obj => isGoldObject(obj, includeGoldMaterial);
    const found = collectDetectedObjects(goldPredicate);
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || mon.mhp <= 0 || mon.mx == null || mon.my == null) continue;
        const name = String(mon.data?.name || mon.name || '').toLowerCase();
        if (!mon.hasGold && name !== 'gold golem') continue;
        const entry = { target: { otyp: GOLD_PIECE, glyph: '$', color: CLR_YELLOW }, x: mon.mx, y: mon.my, monster: mon };
        if (mon.mx === (game.u?.ux ?? -1) && mon.my === (game.u?.uy ?? -1)) found.here.push(entry);
        else found.remote.push(entry);
    }

    if (found.remote.length) {
        for (const entry of found.remote)
            if (entry.monster && isGoldObject(entry.target)) rnd(10);
        markDetectedObjects(found.remote);
        exerciseAttribute(A_WIS, true);
        return { detected: true, known: true, message: 'You feel very greedy, and sense gold!', more: true };
    }
    if (found.here.length)
        return { detected: true, known: true, message: 'You notice some gold between your feet.', more: false };
    if (String(polyselfForm()?.name || '').toLowerCase() === 'gold golem')
        return { detected: false, known: false, message: 'You feel like a million zorkmids!', more: false };
    if (carriedGoldInInventory())
        return { detected: false, known: false, message: 'You feel worried about your future financial situation.', more: false };
    return { detected: false, known: false, message: 'You feel materially poor.', more: false };
}

function foodDetectionScrollEffect(item) {
    const confused = heroIsConfused() || item.cursed;
    const predicate = confused ? obj => isPotionObject(obj) || obj?.glyph === '!' : isFoodObject;
    const what = confused ? 'something' : 'food';
    const found = collectDetectedObjects(predicate);
    if (found.remote.length) {
        markDetectedObjects(found.remote);
        let message;
        if (item.blessed) {
            message = `Your nose ${game.u?.uedibility ? 'continues' : 'starts'} to tingle and you smell ${what}.`;
            if (game.u) game.u.uedibility = 1;
        } else {
            message = `Your nose tingles and you smell ${what}.`;
        }
        exerciseAttribute(A_WIS, true);
        return { detected: true, known: true, message, more: true };
    }
    if (found.here.length) {
        const messages = [`You smell ${what} nearby.`];
        if (item.blessed) {
            if (!game.u?.uedibility) messages.push('Your nose starts to tingle.');
            if (game.u) game.u.uedibility = 1;
        }
        return { detected: true, known: true, message: messages.join('  '), more: false };
    }
    const tingle = item.blessed && !game.u?.uedibility;
    if (tingle && game.u) game.u.uedibility = 1;
    return {
        detected: false,
        known: false,
        message: `Your nose twitches${tingle ? ' then starts to tingle' : ''}.`,
        more: false,
    };
}

function removeCurseFeelingMessage(confused) {
    if (heroIsHallucinating())
        return confused ? 'You feel the power of the Force against you!' : 'You feel in touch with the Universal Oneness.';
    return confused ? 'You feel like you need some help.' : 'You feel like someone is helping you.';
}

function refreshInventoryLineAfterBucChange(item) {
    if (!item || !(game.inventory || []).includes(item) || item.cls === 'coin' || item.otyp === GOLD_PIECE) return;
    item.line = normalInventoryLine({ ...item, line: '' });
}

function removeCurseBlessOrCurse(item) {
    if (!item || item.blessed || item.cursed || item.cls === 'coin' || item.otyp === GOLD_PIECE) return false;
    if (rn2(2)) return false;
    if (rn2(2)) {
        item.blessed = true;
        item.cursed = false;
    } else {
        item.blessed = false;
        item.cursed = true;
    }
    syncCarriedFigurineTransformTimer(item);
    return true;
}

function removeCurseActiveTarget(item) {
    if (!item || item.cls === 'coin' || item.otyp === GOLD_PIECE || item.glyph === '$') return false;
    const line = String(item.line || '');
    if (item.otyp === LOADSTONE || String(item.kind || '').toLowerCase() === 'loadstone') return true;
    if (String(item.kind || '').toLowerCase() === 'leash' && item.leashmon) return true;
    if (item.worn || item.wielded || /being worn|weapon in|on (?:left|right) hand/.test(line)) return true;
    if ((item.alternate || line.includes('alternate weapon')) && game._twoweapon) return true;
    if ((item.quivered || /at the ready|in quiver/.test(line)) && isProjectileItem(item)) return true;
    return false;
}

function normalizeUncursedWaterPotion(item) {
    if (!isWaterPotion(item)) return;
    item.kind = 'water';
    item.actualKind = 'potion of water';
    item.potionIndex = null;
    item.blessed = false;
}

function costlyUncurseWater(item) {
    if (!item?.cursed || !item.unpaid || !isWaterPotion(item)) return '';
    item.bknown = true;
    return costlyAlterationPaymentMessage(item, 'uncurse');
}

function unpunishHero() {
    if (!game.u || !(game.u.uball || game.u.uchain || game.u.upunished || game._punished)) return;
    const chain = game.u.uchain;
    const ball = game.u.uball;
    if (chain) rn2(100); // C delobj(chain) reaches obj_resists(chain, 0, 0).
    if (game.level?.objects && chain)
        game.level.objects = game.level.objects.filter(obj => obj !== chain);
    if (ball) {
        ball.worn = false;
        ball.ox = ball.ox ?? game.u.ux ?? 0;
        ball.oy = ball.oy ?? game.u.uy ?? 0;
        if (game.level?.objects && !game.level.objects.includes(ball))
            game.level.objects.push(ball);
    }
    game.u.uchain = null;
    game.u.uball = null;
    game.u.upunished = false;
    game._punished = 0;
    newsym(game.u.ux || 0, game.u.uy || 0);
}

function breakBuriedBallChain() {
    if (!isBuriedBallTrapActive() || !game.u) return false;
    game.u.utrap = 0;
    game.u.utraptype = null;
    newsym(game.u.ux || 0, game.u.uy || 0);
    return true;
}

function fixPrayerPunishmentTrouble() {
    if (isBuriedBallTrapActive()) return buriedBallToFreedom();
    return false;
}

function removeCurseScrollEffect(item) {
    const confused = heroIsConfused();
    const messages = [item.cursed ? 'You read the scroll.' : 'As you read the scroll, it disappears.'];
    if (confused)
        messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
    messages.push(removeCurseFeelingMessage(confused));

    let learned = false;
    let disintegrates = false;
    if (!item.cursed) {
        for (const obj of game.inventory || []) {
            if (obj.cls === 'coin' || obj.otyp === GOLD_PIECE || obj.glyph === '$') continue;
            const selected = item.blessed || removeCurseActiveTarget(obj);
            if (!selected) continue;
            if (confused) {
                const shopWater = isWaterPotion(obj) && !!shopkeeperOwningBillEntry(obj).entry;
                removeCurseBlessOrCurse(obj);
                obj.bknown = false;
                if (shopWater && (obj.blessed || obj.cursed))
                    alterShopBillCostIfHigher(obj);
                refreshInventoryLineAfterBucChange(obj);
            } else if (obj.cursed) {
                if (obj.bknown === true) learned = true;
                const payment = costlyUncurseWater(obj);
                if (payment) {
                    learned = true;
                    messages.push(payment);
                }
                obj.cursed = false;
                normalizeUncursedWaterPotion(obj);
                syncCarriedFigurineTransformTimer(obj);
                refreshInventoryLineAfterBucChange(obj);
            }
        }
    } else {
        disintegrates = true;
    }
    const buriedBall = isBuriedBallTrapActive();
    if (!confused) unpunishHero();
    if (buriedBall && buriedBallToFreedom())
        messages.push('The clasp on your leg vanishes.');
    return { messages, learned, disintegrates };
}

function scrollReadMessages(confusedReading) {
    const messages = ['As you read the scroll, it disappears.'];
    if (confusedReading)
        messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
    return messages;
}

const GENOCIDE_MAX_TRIES = 5;

function normalizeGenocideName(name) {
    let lower = String(name || '').trim().toLowerCase();
    lower = lower.replace(/^['"]|['"]$/g, '').replace(/^(?:a|an|the) /, '').replace(/\s+/g, ' ');
    const irregular = {
        dwarves: 'dwarf',
        elves: 'elf',
        fungi: 'fungus',
        men: 'human',
        humans: 'human',
        bees: 'bee',
        vortices: 'vortex',
        liches: 'lich',
    }[lower];
    if (irregular) return irregular;
    if (lower.endsWith('ies')) return `${lower.slice(0, -3)}y`;
    if (lower.endsWith('ves')) return `${lower.slice(0, -3)}f`;
    if (lower.endsWith('ses') || lower.endsWith('xes') || lower.endsWith('zes')
        || lower.endsWith('ches') || lower.endsWith('shes'))
        return lower.slice(0, -2);
    if (lower.endsWith('s') && !lower.endsWith('ss')) return lower.slice(0, -1);
    return lower;
}

function pluralizeMonsterName(name) {
    const lower = String(name || '').toLowerCase();
    if (lower === 'human') return 'humans';
    if (lower === 'dwarf') return 'dwarves';
    if (lower === 'elf') return 'elves';
    if (lower === 'fungus') return 'fungi';
    if (lower.endsWith('y')) return `${name.slice(0, -1)}ies`;
    if (/(?:s|x|z|ch|sh)$/i.test(name)) return `${name}es`;
    return `${name}s`;
}

function genocideMonsterCatalog() {
    const entries = [];
    const seen = new Set();
    const add = data => {
        if (!data?.name || seen.has(data.name)) return;
        seen.add(data.name);
        entries.push(data);
    };
    for (const row of RNDMONST_COMMON_MONSTERS) add(monsterByRndName(row[0]) || {
        name: row[0],
        glyph: row[1],
        mlet: row[1],
        difficulty: row[4],
        genoFreq: row[6],
    });
    for (const data of RANDOM_MONSTER_BY_NAME.values()) add(data);
    return entries;
}

function genocideMonsterByName(name) {
    const wanted = normalizeGenocideName(name);
    if (!wanted) return null;
    for (const data of genocideMonsterCatalog()) {
        const candidates = [
            data.name,
            data.name?.replace(/-/g, ' '),
            pluralizeMonsterName(data.name || ''),
        ].map(normalizeGenocideName);
        if (candidates.includes(wanted)) return data;
    }
    return null;
}

function genocidedMonsterNames() {
    return [...new Set(game._genocided_monsters || [])].sort((a, b) => a.localeCompare(b));
}

function isMonsterGenocidedName(name) {
    return genocidedMonsterNames().includes(name);
}

function markMonsterGenocided(name) {
    if (!name) return;
    game._genocided_monsters ??= [];
    if (!game._genocided_monsters.includes(name)) game._genocided_monsters.push(name);
}

function genocideListLines() {
    const names = genocidedMonsterNames();
    if (!names.length) return [[0, 41, 'You have never genocided any monsters.'], [1, 40, '--More--']];
    const rows = [[0, 34, 'Genocided monster types:']];
    let row = 1;
    for (const name of names)
        rows.push([row++, 41, pluralizeMonsterName(name)]);
    rows.push([Math.min(row, 23), 40, '--More--']);
    return rows.slice(0, 24);
}

function killGenocidedMonsters() {
    const names = new Set(genocidedMonsterNames());
    if (!names.size) return;
    const cleanLevel = level => {
        if (!level?.monsters) return;
        for (const mon of [...level.monsters]) {
            const name = mon.data?.name || mon.name;
            const base = mon.chamBase || mon.vampBase;
            if (!names.has(name) && !names.has(base)) continue;
            if (level === game.level) dropMonsterInventory(mon);
            level.monsters = level.monsters.filter(other => other !== mon);
            if (level === game.level) newsym(mon.mx, mon.my);
        }
    };
    cleanLevel(game.level);
    if (game._saved_levels instanceof Map) {
        for (const saved of game._saved_levels.values())
            cleanLevel(saved.level);
    }
}

function heroGenocideTargetName() {
    const role = game.urole?.name?.[game.flags?.female ? 'f' : 'm'] || game.urole?.name?.m || game._startup_role || 'adventurer';
    return String(role || 'adventurer').toLowerCase();
}

function isHeroGenocideTarget(name) {
    const lower = normalizeGenocideName(name);
    const role = normalizeGenocideName(heroGenocideTargetName());
    const race = normalizeGenocideName(game._startup_race || game.urace?.noun || game.urace?.adj || '');
    const raceAdj = normalizeGenocideName(game.urace?.adj || '');
    return !!lower && (lower === role || lower === race || lower === raceAdj
        || (raceAdj === 'human' && lower === 'human'));
}

function finishHeroGenocide(messages, cause = 'scroll of genocide') {
    messages.push('You die...');
    if (game.u) game.u.uhp = 0;
    game._death_cause = cause;
}

function genocideMonsterType(data, messages, { killPlayer = false, cause = 'scroll of genocide' } = {}) {
    const name = data?.name || heroGenocideTargetName();
    markMonsterGenocided(name);
    game._chronicle_genocide_count = (game._chronicle_genocide_count || 0) + 1;
    messages.push(`Wiped out all ${pluralizeMonsterName(name)}.`);
    killDeadSpeciesEggHatchTimers(game);
    killGenocidedMonsters();
    killDeadSpeciesEggHatchTimers(game);
    if (killPlayer) finishHeroGenocide(messages, cause);
}

async function createCursedGenocideMonsters(data, messages) {
    if (!data || data.unique || data.nemesis || isMonsterGenocidedName(data.name)) {
        messages.push('Nothing happens.');
        return;
    }
    let cnt = 0;
    for (let i = rn1(3, 4); i > 0; i--) {
        const mon = await makemon(data, game.u?.ux || 0, game.u?.uy || 0, NO_MINVENT | MM_NOMSG);
        if (!mon) break;
        cnt++;
    }
    messages.push(cnt ? `Sent in ${cnt > 1 ? `some ${pluralizeMonsterName(data.name)}` : /^[aeiou]/i.test(data.name) ? `an ${data.name}` : `a ${data.name}`}.` : 'Nothing happens.');
}

function genocideClassFromInput(input) {
    const trimmed = String(input || '').trim();
    if (trimmed.length === 1) return trimmed;
    const lower = normalizeGenocideName(trimmed);
    const namedClass = GENOCIDE_MONSTER_CLASS_NAMES.get(lower);
    if (namedClass) return namedClass;
    const data = genocideMonsterByName(trimmed);
    return data?.glyph || data?.mlet || null;
}

function genocideClassMembers(cls) {
    return genocideMonsterCatalog().filter(data =>
        data.glyph === cls || data.mlet === cls || data.mlet?.[0] === cls);
}

function genocidePrompt(pending) {
    return pending.classMode
        ? 'What class of monsters do you want to genocide?'
        : 'What type of monster do you want to genocide?';
}

function genocideRetryHint(pending) {
    return pending.classMode
        ? " [enter the symbol or name representing a class, or '?']"
        : " [enter the name of a type of monster, or '?']";
}

async function endGenocidePrompt(messages = [], more = false) {
    game._genocide_pending = null;
    game._genocide_input = '';
    game._command_mode = null;
    game.context.move = 1;
    if (messages.length) await setMessage(messages.join('  '), more);
    else {
        game._pending_message = '';
        game._message_more = 0;
    }
}

async function retryGenocidePrompt(pending, message) {
    pending.tries = (pending.tries || 0) + 1;
    game._genocide_input = '';
    if (pending.tries >= GENOCIDE_MAX_TRIES) {
        const messages = [];
        if (message) messages.push(message);
        if (pending.cursed && !pending.classMode)
            await createCursedGenocideMonsters(rndmonnum(), messages);
        else
            messages.push("That's enough tries!");
        await endGenocidePrompt(messages, messages.length > 1);
        return;
    }
    game._genocide_pending = pending;
    game._command_mode = 'genocideText';
    game.context.move = 0;
    await setMessage(`${message}  ${genocidePrompt(pending)}${genocideRetryHint(pending)}`);
}

async function finishGenocideInput(raw) {
    const pending = game._genocide_pending;
    if (!pending) return;
    const input = String(raw || '').trim();
    const messages = [...(pending.messages || [])];

    if (!input) {
        await retryGenocidePrompt(pending, pending.classMode
            ? "Type letter (or punctuation) or name used for a class of monsters or 'none'."
            : "Type the name of a type of monster or 'none'.");
        return;
    }
    if (input === '\x1b' || ['none', "'none'", 'nothing'].includes(input.toLowerCase())) {
        if (pending.cursed) {
            await createCursedGenocideMonsters(rndmonnum(), messages);
            await endGenocidePrompt(messages, messages.length > 1);
            return;
        }
        await endGenocidePrompt();
        return;
    }
    if (input === '?' || input === "'?'") {
        game._genocide_input = '';
        setOverlay(genocideListLines(), 24, false, 39);
        game._command_mode = 'genocideHelp';
        return;
    }
    if (pending.classMode) {
        const cls = genocideClassFromInput(input);
        const members = cls ? genocideClassMembers(cls) : [];
        if (!members.length) {
            await retryGenocidePrompt(pending, `That ${input.length === 1 ? 'symbol' : 'response'} does not represent any monster.`);
            return;
        }
        let wiped = 0;
        for (const data of members) {
            if (isMonsterGenocidedName(data.name) || data.unique || data.nemesis) continue;
            genocideMonsterType(data, messages);
            wiped++;
        }
        if (!wiped) messages.push('All such monsters are already nonexistent.');
        if (cls === '@' || members.some(data => isHeroGenocideTarget(data.name)))
            finishHeroGenocide(messages);
        await endGenocidePrompt(messages, true);
        return;
    }

    const data = genocideMonsterByName(input);
    if (!data) {
        await retryGenocidePrompt(pending, 'Such creatures do not exist in this world.');
        return;
    }
    if (isMonsterGenocidedName(data.name)) {
        await retryGenocidePrompt(pending, 'Such creatures no longer exist in this world.');
        return;
    }
    if (pending.cursed) {
        await createCursedGenocideMonsters(data, messages);
        await endGenocidePrompt(messages, messages.length > 1);
        return;
    }
    const killPlayer = isHeroGenocideTarget(input);
    if (data.unique || data.nemesis) {
        await retryGenocidePrompt(pending, `You aren't permitted to genocide ${data.unique ? `the ${data.name}` : pluralizeMonsterName(data.name)}.`);
        return;
    }
    genocideMonsterType(data, messages, { killPlayer });
    await endGenocidePrompt(messages, true);
}

function fireScrollReadMessages(confusedReading) {
    const messages = [game.u?.blind ? 'You pronounce the formula on the scroll.' : 'You read the scroll.'];
    if (confusedReading)
        messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
    return messages;
}

function scrollDiscoveryKnown(scrollName) {
    const discoveryName = `scroll of ${scrollName}`;
    return (game._discoveries || [])
        .some(entry => entry.section === 'Scrolls' && entry.name === discoveryName && entry.known !== false);
}

function scrollCallLabel(item, scrollIndex, fallback = 'ZELGO MER') {
    const kind = String(item?.kind || '');
    if (kind.startsWith('scroll labeled '))
        return kind.replace(/^scroll labeled /, '') || fallback;
    return game._object_descriptions?.scrolls?.[scrollIndex] || fallback;
}

function recordCalledScroll(label, name) {
    const call = String(name || '').trim();
    if (!call) return;
    const scrollLabel = label || 'ZELGO MER';
    game._called_scrolls ??= {};
    game._called_scrolls[scrollLabel] = call;
    game._discoveries ??= [];
    const discoveryName = `scroll called ${call}`;
    const existing = game._discoveries.find(entry =>
        entry.section === 'Scrolls'
        && (entry.name === discoveryName || String(entry.text || '').endsWith(`(${scrollLabel})`)));
    if (existing) {
        existing.name = discoveryName;
        existing.text = `${discoveryName} (${scrollLabel})`;
        existing.starred = false;
    } else {
        game._discoveries.push({
            section: 'Scrolls',
            name: discoveryName,
            text: `${discoveryName} (${scrollLabel})`,
            starred: false,
        });
    }
}

function shouldTryCallScroll(scrollName, item, scrollIndex) {
    if (!item || item.dknown === false) return false;
    if (item.known === true || scrollDiscoveryKnown(scrollName)) return false;
    const label = scrollCallLabel(item, scrollIndex);
    return !game._called_scrolls?.[label];
}

function prepareScrollTryCall(item, scrollIndex, moveCost = 1) {
    game._call_scroll_label = scrollCallLabel(item, scrollIndex);
    game._call_scroll_text = '';
    game._call_scroll_move_cost = moveCost;
}

function scrollDisappearMessages(confusedReading) {
    const messages = [game.u?.blind
        ? 'As you pronounce the formula on it, the scroll disappears.'
        : 'As you read the scroll, it disappears.'];
    if (confusedReading)
        messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
    return messages;
}

function teleportationScrollEffect(item, messages) {
    const confused = heroIsConfused();
    const wizard = !!game.flags?.debug;
    if (confused || item.cursed) {
        if ((heroHasAmuletOfYendor() || In_endgame(game.u?.uz) || In_sokoban(game.u?.uz)) && !wizard) {
            messages.push('You feel very disoriented for a moment.');
            return { learned: true, more: messages.length > 1 };
        }
        if ((heroHasTeleportControl() && !heroIsStunned()) || wizard)
            return { learned: true, promptLevel: true, confusedPrompt: confused, more: true };

        const targetDepth = randomTeleportDepth();
        if (targetDepth === depth_of_level(game.u?.uz || { dnum: 0, dlevel: 1 })) {
            messages.push('You shudder for a moment.');
            return { learned: true, more: messages.length > 1 };
        }
        game._deferred_level_goto = {
            targetLevel: levelTeleportNumericTarget(targetDepth),
            options: { levelTeleport: true },
        };
        return { learned: true, more: messages.length > 1 };
    }

    if (heroNoTeleportLevel() && !wizard) {
        messages.push('A mysterious force prevents you from teleporting!');
        return { learned: true, more: messages.length > 1 };
    }

    if ((heroHasAmuletOfYendor() || heroOnWizardTowerLevel()) && !rn2(3)) {
        messages.push('You feel disoriented for a moment.');
        return { learned: false, more: messages.length > 1 };
    }

    if (((heroHasTeleportControl() || item.blessed) && !heroIsStunned()) || wizard)
        return { learned: true, promptSameLevel: true, more: true };

    const materialize = safeTeleportHeroSameLevel();
    return { learned: true, materialize, more: messages.length > 1 };
}

function lightScrollNoOpLevel() {
    return !!(game.u?.uswallow || game.u?.underwater || game.u?.uunderwater || Is_waterlevel(game.u?.uz));
}

function setLightScrollCell(x, y, on) {
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    loc.lit = !!on;
    loc.waslit = !!on;
    if (on) loc._litScrollWhite = true;
    else delete loc._litScrollWhite;
    newsym(x, y);
    return true;
}

function applyLightScrollArea(on, item) {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const rogueLevel = Is_rogue_level(game.u?.uz) || game.level?.flags?.rogue_level;
    if (rogueLevel) {
        const room = levelRoomByRoomno(game.level?.at(ux, uy)?.roomno || 0);
        if (!room) return;
        for (let x = Math.max(1, (room.lx ?? ux) - 1); x <= Math.min(COLNO - 1, (room.hx ?? ux) + 1); x++)
            for (let y = Math.max(0, (room.ly ?? uy) - 1); y <= Math.min(ROWNO - 1, (room.hy ?? uy) + 1); y++)
                setLightScrollCell(x, y, on);
        room.rlit = on ? 1 : 0;
        return;
    }

    const radius = item.blessed ? 9 : 5;
    const radius2 = radius * radius;
    for (let y = Math.max(0, uy - radius); y <= Math.min(ROWNO - 1, uy + radius); y++) {
        for (let x = Math.max(1, ux - radius); x <= Math.min(COLNO - 1, ux + radius); x++) {
            if ((x - ux) ** 2 + (y - uy) ** 2 > radius2) continue;
            if (!couldsee(x, y)) continue;
            setLightScrollCell(x, y, on);
        }
    }
}

function snuffCarriedLightsForScroll(messages) {
    for (const obj of game.inventory || []) {
        if (!(obj.lamplit || obj.burning)) continue;
        obj.lamplit = false;
        obj.burning = false;
        delete obj._burnTimer;
        delete obj.litRadius;
        updateChargedItemLine(obj);
        if (!game.u?.blind) messages.push(`${chargingObjectSubject(obj)} goes out!`);
    }
}

function lightScrollLitroom(on, item, messages) {
    const blind = !!game.u?.blind;
    const noOp = lightScrollNoOpLevel();
    if (!on) {
        snuffCarriedLightsForScroll(messages);
        if (!blind)
            messages.push(game.u?.uswallow ? 'It seems even darker in here than before.' : 'You are surrounded by darkness!');
    } else if (!blind) {
        if (game.u?.uswallow && game.u?.ustuck)
            messages.push(`${fireScrollMonsterName(game.u.ustuck)} is lit.`);
        else if (!(Is_rogue_level(game.u?.uz) || game.level?.flags?.rogue_level) || game.level?.at(game.u?.ux || 0, game.u?.uy || 0)?.typ !== CORR)
            messages.push(`A lit field ${noOp ? 'briefly ' : ''}surrounds you!`);
    }

    if (noOp) return;
    applyLightScrollArea(on, item);
    vision_recalc(0);
    game.vision_full_recalc = 1;
}

function lightScrollMonsterVisible(mon) {
    return !!mon && !game.u?.blind && !mon.mundetected
        && (!mon.minvis || game.u?.seeInvisible)
        && !!(game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT);
}

async function createConfusedLightScrollMonsters(item, messages) {
    const data = monsterByRndName(item.cursed ? 'black light' : 'yellow light');
    if (!data) {
        messages.push('Tiny lights sparkle in the air momentarily.');
        return false;
    }
    const created = [];
    const count = rn1(2, 3) + (item.blessed ? 2 : 0);
    for (let i = 0; i < count; i++) {
        const mon = await makemon(data, game.u?.ux || 0, game.u?.uy || 0, MM_EDOG | NO_MINVENT | MM_NOMSG);
        if (!mon) continue;
        mon.pet = true;
        mon.mtame = Math.max(mon.mtame || 0, baseScrollTameness(mon));
        mon.mpeaceful = 1;
        mon.msleeping = 0;
        mon.mcan = true;
        ensurePetExtension(mon);
        created.push(mon);
        newsym(mon.mx, mon.my);
    }
    if (created.length) vision_recalc(0);
    const sawLights = created.some(lightScrollMonsterVisible);
    if (sawLights) messages.push('Lights appear all around you!');
    return sawLights;
}

async function lightScrollEffect(item, messages) {
    if (heroIsConfused()) {
        return { learned: await createConfusedLightScrollMonsters(item, messages) };
    }
    const learned = !game.u?.blind;
    lightScrollLitroom(!item.cursed, item, messages);
    return { learned };
}

function primaryWieldedItem() {
    return (game.inventory || []).find(item =>
        item?.wielded || item?.line?.includes('weapon in') || item?.line?.includes('(wielded)')) || null;
}

function enchantWeaponName(item) {
    return pickupObjectName(item).replace(/^pair of /, '');
}

function enchantWeaponSubject(item) {
    return `Your ${enchantWeaponName(item)}`;
}

function enchantWeaponVerb(item, singular, plural) {
    const name = enchantWeaponName(item);
    return (item?.quan || 1) > 1 || /\b(?:boots|shoes|gloves|gauntlets|darts|arrows|daggers|knives|spears|rocks|stones|shuriken)\b/i.test(name)
        ? plural
        : singular;
}

function refreshEnchantWeaponLine(item) {
    if (!item || !(game.inventory || []).includes(item)) return;
    item.line = normalInventoryLine({ ...item, line: '' });
}

function enchantWeaponIsWeaponLike(item) {
    return !!item && (item.cls === 'weapon' || item.otyp === WEAPON_CLASS || item.glyph === ')' || isWeaponTool(item));
}

function enchantWeaponWillWeld(item) {
    if (!item?.cursed) return false;
    if (enchantWeaponIsWeaponLike(item)) return true;
    return objectKindKey(item) === 'tin opener';
}

function enchantWeaponAmount(scroll, target) {
    if (scroll.cursed) return -1;
    if (!target) return 1;
    const spe = target.spe ?? 0;
    if (spe >= 9) return rn2(spe) === 0 ? 1 : 0;
    if (scroll.blessed) return rnd(3 - Math.trunc(spe / 3));
    return 1;
}

function finishConfusedProofMutation(target, oldErodeproof, newErodeproof, messages) {
    if (oldErodeproof && !newErodeproof) {
        target.oerodeproof = true;
        const payment = costlyAlterationPaymentMessage(target, 'degrade');
        if (payment) messages.push(payment);
    }
    target.oerodeproof = !!newErodeproof;
}

function enchantWeaponConfusedEffect(scroll, messages) {
    const target = primaryWieldedItem();
    const profile = wishedDamageProfile(target);
    if (!target || !profile.erosionMatters || target.cls === 'armor' || target.glyph === '[') return null;

    const oldErodeproof = !!target.oerodeproof;
    const newErodeproof = !scroll.cursed;
    target.oerodeproof = false;
    if (game.u?.blind) {
        target.rknown = false;
        messages.push('Your weapon feels warm for a moment.');
    } else {
        target.rknown = true;
        messages.push(`${enchantWeaponSubject(target)} ${enchantWeaponVerb(target, 'is', 'are')} covered by a ${scroll.cursed ? 'mottled purple glow' : 'shimmering golden shield'}!`);
    }
    if (newErodeproof && (target.oeroded || target.oeroded2)) {
        target.oeroded = 0;
        target.oeroded2 = 0;
        messages.push(`${enchantWeaponSubject(target)} ${enchantWeaponVerb(target, game.u?.blind ? 'feels' : 'looks', game.u?.blind ? 'feel' : 'look')} as good as new!`);
    }
    finishConfusedProofMutation(target, oldErodeproof, newErodeproof, messages);
    refreshEnchantWeaponLine(target);
    return { learned: false, more: messages.length > 1 };
}

function transformWormToCrysknife(target, messages) {
    const multiple = (target.quan || 1) > 1;
    messages.push(`${enchantWeaponSubject(target)} ${multiple ? 'fuse, and become' : 'is'} much sharper now.`);
    target.kind = 'crysknife';
    target.actualKind = 'crysknife';
    target.quan = 1;
    target.oerodeproof = false;
    target.cursed = false;
    refreshEnchantWeaponLine(target);
    return { learned: true, more: false };
}

function transformCrysknifeToWormTooth(scroll, target, messages) {
    const multiple = (target.quan || 1) > 1;
    messages.push(`${enchantWeaponSubject(target)} ${multiple ? 'fuse, and become' : 'is'} much duller now.`);
    const payment = costlyAlterationPaymentMessage(target, 'degrade');
    if (payment) messages.push(payment);
    target.kind = 'worm tooth';
    target.actualKind = 'worm tooth';
    target.quan = 1;
    target.oerodeproof = false;
    refreshEnchantWeaponLine(target);
    return { learned: scroll.bknown === true, more: false };
}

function removeWieldedWeapon(target) {
    removeInventoryItem(target, target.quan || 1);
    if (target.wielded) game._wielded_mjollnir = false;
}

function enchantWeaponScrollChwepon(scroll, amount, messages) {
    const target = primaryWieldedItem();
    if (!enchantWeaponIsWeaponLike(target)) {
        if (amount >= 0 && target && enchantWeaponWillWeld(target)) {
            if (game.u?.blind) messages.push(`Your ${game.u?.uhandedness || 'right'} hand tingles.`);
            else {
                messages.push(`${enchantWeaponSubject(target)} ${enchantWeaponVerb(target, 'glows', 'glow')} with an amber aura.`);
                target.bknown = !game.u?.hallucinating;
            }
            target.cursed = false;
            refreshEnchantWeaponLine(target);
        } else {
            messages.push(`Your hands ${amount >= 0 ? 'twitch' : 'itch'}.`);
        }
        exerciseAttribute(A_DEX, amount >= 0);
        return { learned: false, more: false };
    }

    const kind = objectKindKey(target);
    if (kind === 'worm tooth' && amount >= 0)
        return transformWormToCrysknife(target, messages);
    if (kind === 'crysknife' && amount < 0)
        return transformCrysknifeToWormTooth(scroll, target, messages);

    const color = amount < 0 ? 'black' : 'blue';
    if (amount < 0 && target.artifact && target.oname && String(target.oname).toLowerCase() === String(target.artifact).toLowerCase()) {
        if (!game.u?.blind) messages.push(`${enchantWeaponSubject(target)} faintly ${enchantWeaponVerb(target, 'glows', 'glow')} ${color}.`);
        return { learned: false, more: false };
    }

    const spe = target.spe ?? 0;
    if (((spe > 5 && amount >= 0) || (spe < -5 && amount < 0)) && rn2(3)) {
        if (game.u?.blind)
            messages.push(`${enchantWeaponSubject(target)} ${enchantWeaponVerb(target, 'evaporates', 'evaporate')}.`);
        else
            messages.push(`${enchantWeaponSubject(target)} violently ${enchantWeaponVerb(target, 'glows', 'glow')} ${color} for a while and then ${enchantWeaponVerb(target, 'evaporates', 'evaporate')}.`);
        removeWieldedWeapon(target);
        return { learned: false, more: true };
    }

    let learned = false;
    if (!game.u?.blind) {
        const glow = amount === 0 ? 'violently glows' : 'glows';
        const time = amount * amount === 1 ? 'moment' : 'while';
        messages.push(`${enchantWeaponSubject(target)} ${glow} ${color} for a ${time}.`);
        learned = target.known === true && (amount > 0 || (amount < 0 && scroll.bknown === true));
    }

    if (amount < 0) {
        const payment = costlyAlterationPaymentMessage(target, 'disenchant');
        if (payment) messages.push(payment);
    }
    target.spe = capWishSpe(spe + amount);
    if (amount > 0 && target.cursed) target.cursed = false;
    refreshEnchantWeaponLine(target);

    if (target.artifact === 'Magicbane' && target.spe >= 0)
        messages.push(`Your right hand ${amount > 1 && target.spe > 1 ? 'flinches' : 'itches'}!`);
    if (target.spe > 5 && (/^elven\b/.test(kind) || target.artifact || !rn2(7)))
        messages.push(`${enchantWeaponSubject(target)} suddenly ${enchantWeaponVerb(target, 'vibrates', 'vibrate')} unexpectedly.`);
    return { learned, more: messages.length > 1 };
}

function enchantWeaponScrollEffect(scroll, messages) {
    if (heroIsConfused()) {
        const confusedResult = enchantWeaponConfusedEffect(scroll, messages);
        if (confusedResult) return confusedResult;
    }
    const target = primaryWieldedItem();
    const amount = enchantWeaponAmount(scroll, target);
    const result = enchantWeaponScrollChwepon(scroll, amount, messages);
    const finalTarget = primaryWieldedItem();
    if (finalTarget) {
        finalTarget.spe = capWishSpe(finalTarget.spe ?? 0);
        refreshEnchantWeaponLine(finalTarget);
    }
    return result;
}

function wandTypeName(item) {
    return String(item?.wand
        || (item?.wandIndex != null ? IDENTIFIED_WAND_NAMES[item.wandIndex] : '')
        || item?.actualKind
        || item?.kind
        || '')
        .toLowerCase()
        .replace(/^wand of /, '');
}

function chargingObjectSubject(item) {
    return `Your ${pickupObjectName(item)}`;
}

function updateChargedItemLine(item) {
    if (!item || !(game.inventory || []).includes(item)) return;
    if (isWandItem(item)) refreshWandLine(item);
    else item.line = normalInventoryLine({ ...item, line: '' });
}

function chargeGlowMessage(item, color = '', feeble = false) {
    const verb = game.u?.blind ? 'vibrates' : 'glows';
    if (feeble && (!color || game.u?.blind))
        return `${chargingObjectSubject(item)} feebly ${verb} for a moment.`;
    if (!color || game.u?.blind)
        return `${chargingObjectSubject(item)} ${feeble ? 'feebly ' : ''}${verb} briefly.`;
    return `${chargingObjectSubject(item)} ${feeble ? 'feebly ' : ''}${verb} ${color} for a moment.`;
}

function costlyAlterationPaymentMessage(item, verb) {
    const name = pickupObjectName(item);
    const plural = (item?.quan || 1) > 1;
    const billed = (game.inventory || []).includes(item)
        ? billDummyAlteredCarriedObject(item)
        : billDummyAlteredShopObject(item);
    if (!billed) return '';
    return `You ${verb} ${plural ? 'those' : 'that'} ${name}, you pay for ${plural ? 'them' : 'it'}!`;
}

const NO_EFFECT_BROKEN_WANDS = new Set([
    'enlightenment',
    'locking',
    'nothing',
    'opening',
    'probing',
    'secret door detection',
    'stasis',
    'wishing',
]);

function canApplyNoEffectWandBreak(item) {
    return isWandItem(item) && (wandCharges(item) < 0 || NO_EFFECT_BROKEN_WANDS.has(wandTypeName(item)));
}

function wandBreakObjectName(item) {
    const name = pickupObjectName(item);
    return /^(?:a|an|the|some) /i.test(name) ? name : `the ${name}`;
}

function applyNoEffectWandBreak(item) {
    const messages = [`Raising ${wandBreakObjectName(item)} high above your head, you break it in two!`];
    checkUnpaidUsage(item, messages);
    const payment = costlyAlterationPaymentMessage(item, 'destroy');
    if (payment) messages.push(payment);
    const chargeUse = zappableWand(item);
    if (chargeUse.zapped) {
        setWandCharges(item, wandCharges(item) + 1);
        if (wandCharges(item) === 0) setWandCharges(item, rnd(3));
        messages.push(...wandZapSpendMessages([], chargeUse));
    }
    removeInventoryItem(item, item.quan || 1);
    messages.push('But nothing else happens...');
    return messages;
}

function stripCharges(item) {
    const spe = item?.spe ?? 0;
    if (item?.blessed || spe <= 0) return ['Nothing happens.'];
    const messages = [`${chargingObjectSubject(item)} vibrates briefly.`];
    const payment = costlyAlterationPaymentMessage(item, 'uncharge');
    if (payment) messages.push(payment);
    item.spe = 0;
    if (isWandItem(item)) item.charges = 0;
    if (isLampObject(item)) item.age = 0;
    updateChargedItemLine(item);
    return messages;
}

function drainItemProtectedByDrainResistance(item) {
    if (!item) return false;
    if (item.defendsDrain || item.drainResistance || item.drainResistant || item.artifactDefendsDrain || item.artifactCarryDrainDefense)
        return true;
    const kind = objectKindKey(item);
    return item.otyp === BLACK_DRAGON_SCALE_MAIL || item.otyp === BLACK_DRAGON_SCALES
        || /\bblack dragon (?:scale mail|scales)\b/.test(kind);
}

function drainItemAlwaysResists(item) {
    const kind = objectKindKey(item);
    return !!(item?.invocation || item?.riderCorpse || item?.realAmuletOfYendor
        || item?.otyp === BOOK_OF_THE_DEAD || item?.otyp === CANDELABRUM_OF_INVOCATION || item?.otyp === BELL
        || kind === 'book of the dead' || kind === 'candelabrum of invocation' || kind === 'bell of opening'
        || kind === 'amulet of yendor');
}

function drainItemResists(item) {
    if (drainItemAlwaysResists(item)) return true;
    return rn2(100) < (item?.artifact || item?.oartifact ? 90 : 10);
}

function drainItemEligible(item) {
    if (!item || (item.spe ?? 0) <= 0) return false;
    const cls = itemClassKey(item);
    return isWandItem(item)
        || isChargeableRing(item)
        || isChargedTool(item)
        || cls === 'weapon'
        || cls === 'armor'
        || isWeaponTool(item);
}

function drainItem(item, { byYou = false, messages = null } = {}) {
    if (!drainItemEligible(item)) return { drained: false, eligible: false, resisted: false };
    if (drainItemProtectedByDrainResistance(item) || drainItemResists(item))
        return { drained: false, eligible: true, resisted: true };
    const payment = byYou ? costlyAlterationPaymentMessage(item, 'drain') : '';
    if (payment && Array.isArray(messages)) messages.push(payment);
    item.spe = Math.max(0, (item.spe || 0) - 1);
    if (isWandItem(item)) item.charges = item.spe;
    updateChargedItemLine(item);
    return { drained: true, eligible: true, resisted: false, payment };
}

function carriedDrainItemLessEffectiveMessage(item) {
    if (!item || !(game.inventory || []).includes(item)) return '';
    if (!(item.known || item.cls === 'armor' || item.glyph === '[' || item.otyp === ARMOR_CLASS)) return '';
    const name = pickupObjectName(item);
    const plural = (item.quan || 1) > 1 || armorNameIsPlural(name);
    return `Your ${name} ${plural ? 'seem' : 'seems'} less effective.`;
}

function passiveObjectErosionSpec(type) {
    if (type === 'rust') return {
        field: 'oeroded',
        word: 'rusty',
        action: 'rust',
        bythe: 'oxidation',
        grease: true,
    };
    if (type === 'corr' || type === 'acid') return {
        field: 'oeroded2',
        word: 'corroded',
        action: 'corrode',
        bythe: 'corrosion',
        grease: true,
        resistType: 'acid',
    };
    if (type === 'fire') return {
        field: 'oeroded',
        word: 'burnt',
        action: 'smoulder',
        bythe: 'heat',
        grease: false,
        resistType: 'fire',
    };
    return null;
}

function activeInventoryResistanceKind(item) {
    if (!item) return '';
    const kind = objectKindKey(item);
    const active = isActiveInventoryExtrinsicItem(item) || item.wielded || item.line?.includes('(weapon)');
    if (!active) return '';
    if (item.fireResistance || kind === 'ring of fire resistance'
        || kind === 'red dragon scale mail' || kind === 'red dragon scales'
        || kind === 'fire brand')
        return 'fire';
    if (item.coldResistance || kind === 'ring of cold resistance'
        || kind === 'cold resistance'
        || dragonArmorKindHasProperty(kind, 'cold')
        || kind === 'frost brand')
        return 'cold';
    if (item.acidResistance || kind === 'alchemy smock'
        || kind === 'yellow dragon scale mail' || kind === 'yellow dragon scales')
        return 'acid';
    return '';
}

function passiveObjectInventoryResistanceChance(type) {
    const kind = type === 'fire' ? 'fire' : type === 'acid' || type === 'corr' ? 'acid' : '';
    if (!kind) return 0;
    if ((game.inventory || []).some(item => activeInventoryResistanceKind(item) === kind))
        return 99;
    if (kind === 'fire' && (game.inventory || []).some(item =>
        isWornInventoryItem(item) && objectKindKey(item) === 'dwarvish cloak'))
        return 90;
    return 0;
}

function passiveObjectInventoryResists(type) {
    const chance = passiveObjectInventoryResistanceChance(type);
    return chance ? rn2(100) < chance : false;
}

function updatePassiveObjectItemLine(item) {
    refreshInventoryObjectLine(item);
    if (item?.unpaid) syncUnpaidBillLine(item);
}

function erodeDirectMeleePassiveObject(item, type, messages) {
    const spec = passiveObjectErosionSpec(type);
    if (!spec) return { handled: false, damaged: false };
    if (spec.resistType && passiveObjectInventoryResists(spec.resistType))
        return { handled: true, damaged: false, resisted: true };
    const profile = wishedDamageProfile(item);
    const profileWord = spec.field === 'oeroded' ? profile.primaryWord : profile.secondaryWord;
    if (!profile.erosionMatters || profileWord !== spec.word)
        return { handled: true, damaged: false };

    const name = pickupObjectName(item);
    if (spec.grease && item.greased) {
        if (Array.isArray(messages))
            messages.push(`Your ${name} ${rustTrapNameVerb(name, 'is', 'are')} protected by the layer of grease!`);
        if (!rn2(2)) {
            item.greased = false;
            updatePassiveObjectItemLine(item);
            if (Array.isArray(messages)) messages.push('The grease dissolves.');
        }
        return { handled: true, damaged: false, greased: true };
    }

    if (item.oerodeproof || item.rustproof) {
        if (!item.rknown && game.flags?.verbose !== false && Array.isArray(messages))
            messages.push(`Somehow, your ${name} ${rustTrapNameVerb(name, 'is', 'are')} not affected by the ${spec.bythe}.`);
        item.rknown = true;
        updatePassiveObjectItemLine(item);
        return { handled: true, damaged: false, proof: true };
    }
    if (item.blessed && !rnl(4)) return { handled: true, damaged: false, blessed: true };

    const current = Math.min(3, item[spec.field] || 0);
    if (current >= 3) return { handled: true, damaged: false };
    item[spec.field] = current + 1;
    updatePassiveObjectItemLine(item);
    if (Array.isArray(messages)) {
        const adverb = item[spec.field] === 3 ? ' completely' : current ? ' further' : '';
        messages.push(`Your ${name} ${rustTrapNameVerb(name, `${spec.action}s`, spec.action)}${adverb}!`);
    }
    return { handled: true, damaged: true };
}

function passiveObjectFireBlocked(mon) {
    return !!(mon?.mcan || String(mon?.data?.name || mon?.name || '').toLowerCase() === 'steam vortex');
}

function applyDirectMeleePassiveObject(mon, weapon, messages) {
    const type = passiveObjectAttackForMonster(mon);
    if (!type) return { handled: false, damaged: false };
    const targetObject = weapon || (type === 'ench' ? wornGlovesItem() : null);
    if (!targetObject) return { handled: false, damaged: false, type };
    if (type === 'ench') {
        if (mon.mcan) return { handled: true, damaged: false, type, object: targetObject };
        const drain = drainItem(targetObject, { byYou: true, messages });
        if (drain.drained) {
            const message = carriedDrainItemLessEffectiveMessage(targetObject);
            if (message && Array.isArray(messages)) messages.push(message);
        }
        return { handled: true, damaged: drain.drained, type, object: targetObject, ...drain };
    }
    if (type === 'fire') {
        if (rn2(6) || passiveObjectFireBlocked(mon))
            return { handled: true, damaged: false, type, object: targetObject };
    } else if (type === 'acid') {
        if (rn2(6))
            return { handled: true, damaged: false, type, object: targetObject };
    } else if ((type === 'rust' || type === 'corr') && mon.mcan) {
        return { handled: true, damaged: false, type, object: targetObject };
    }
    return { ...erodeDirectMeleePassiveObject(targetObject, type, messages), type, object: targetObject };
}

function wandRechargeLimit(item) {
    const name = wandTypeName(item);
    if (name === 'wishing' || isWishingWand(item)) return 1;
    return NO_DIRECTION_WAND_NAMES.has(name) ? 15 : 8;
}

function wandExplosionDieSides(item) {
    const name = wandTypeName(item);
    if (name === 'wishing' || isWishingWand(item)) return 12;
    if (['cancellation', 'death', 'polymorph', 'undead turning'].includes(name)) return 10;
    if (['cold', 'fire', 'lightning', 'magic missile'].includes(name)) return 8;
    if (name === 'nothing') return 4;
    return 6;
}

function explodeChargingWand(item, chg) {
    const name = pickupObjectName(item);
    const dice = Math.max(2, (item.spe ?? 0) + chg);
    const damage = d(dice, wandExplosionDieSides(item));
    removeInventoryItem(item);
    exerciseAttribute(A_STR, false);
    const messages = [`Your ${name} vibrates violently and explodes!`];
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by an exploding wand';
            messages.push('You die...');
        }
    }
    return { messages, more: messages.length > 1, consumed: true };
}

function rechargeWand(item, curseBless) {
    const limit = wandRechargeLimit(item);
    if ((item.spe ?? item.charges) === -1) setWandCharges(item, 0);
    const rechargeCount = item.recharged || 0;
    if (rechargeCount > 0
        && ((wandTypeName(item) === 'wishing' || isWishingWand(item)) || rechargeCount ** 3 > rn2(343))) {
        return explodeChargingWand(item, rnd(limit));
    }

    item.recharged = rechargeCount + 1;
    if (curseBless < 0) return { messages: stripCharges(item), more: false };

    let target = limit === 1 ? 1 : rn1(5, limit + 1 - 5);
    if (curseBless <= 0) target = rnd(target);
    const charges = wandCharges(item);
    setWandCharges(item, charges < target ? target : charges + 1);
    updateChargedItemLine(item);
    if (limit === 1) return { messages: [chargeGlowMessage(item, 'blue', true)], more: false };
    if (wandCharges(item) >= limit) return { messages: [chargeGlowMessage(item, 'blue')], more: false };
    return { messages: [chargeGlowMessage(item)], more: false };
}

function isChargeableRing(item) {
    if (!(item?.cls === 'ring' || item?.otyp === RING_CLASS || item?.glyph === '=')) return false;
    if (item.charged) return true;
    const roll = item.ringRoll || item.roll || 0;
    if (roll >= 1 && roll <= 6) return true;
    const name = objectKindKey(item).replace(/^ring of /, '');
    return ['adornment', 'gain strength', 'gain constitution', 'increase accuracy', 'increase damage', 'protection'].includes(name);
}

function rechargeRing(item, curseBless) {
    if (!isChargeableRing(item)) return { messages: ['You have a feeling of loss.'], more: false };
    const oldSpe = item.spe ?? 0;
    const adjustment = curseBless > 0 ? rnd(3) : curseBless < 0 ? -rnd(2) : 1;
    if (oldSpe > rn2(7) || oldSpe <= -5) {
        const name = pickupObjectName(item);
        const damage = rnd(Math.max(1, 3 * Math.abs(oldSpe)));
        removeInventoryItem(item);
        const messages = [`Your ${name} pulsates momentarily, then explodes!`];
        if (game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            if ((game.u.uhp || 0) <= 0) {
                game._death_cause = 'killed by an exploding ring';
                messages.push('You die...');
            }
        }
        return { messages, more: messages.length > 1 };
    }
    const direction = curseBless < 0 ? 'counterclockwise' : 'clockwise';
    const messages = [`${chargingObjectSubject(item)} spins ${direction} for a moment.`];
    if (adjustment < 0) {
        const payment = costlyAlterationPaymentMessage(item, 'disenchant');
        if (payment) messages.push(payment);
    }
    item.spe = capWishSpe(oldSpe + adjustment);
    updateChargedItemLine(item);
    return { messages, more: messages.length > 1 };
}

function toolChargeKind(item) {
    const key = objectKindKey(item).replace(/^the /, '');
    if (key === 'silver bell') return 'bell of opening';
    return key;
}

function isWeaponTool(item) {
    const kind = toolChargeKind(item);
    return item?.cls === 'tool' && ['pick-axe', 'grappling hook', 'unicorn horn'].includes(kind);
}

const RECHARGEABLE_TOOL_KINDS = new Set([
    'bell of opening', 'magic marker', 'tinning kit', 'expensive camera', 'oil lamp',
    'brass lantern', 'crystal ball', 'horn of plenty', 'bag of tricks', 'can of grease',
    'magic flute', 'magic harp', 'frost horn', 'fire horn', 'drum of earthquake',
]);

const CHARGED_TOOL_KINDS = new Set([
    'bell of opening', 'magic marker', 'tinning kit', 'expensive camera', 'crystal ball',
    'horn of plenty', 'bag of tricks', 'can of grease', 'magic flute', 'magic harp',
    'frost horn', 'fire horn', 'drum of earthquake',
]);

function isChargedTool(item) {
    if (!(item?.cls === 'tool' || item?.otyp === TOOL_CLASS || item?.glyph === '(')) return false;
    return CHARGED_TOOL_KINDS.has(toolChargeKind(item));
}

function isRechargeableTool(item) {
    if (!(item?.cls === 'tool' || item?.otyp === TOOL_CLASS || item?.glyph === '(')) return false;
    return RECHARGEABLE_TOOL_KINDS.has(toolChargeKind(item));
}

function isChargePromptSuggested(item) {
    if (!item?.letter) return false;
    if (isWandItem(item)) return true;
    if (isChargeableRing(item) && (item.known || item.dknown || item.bknown)) return true;
    if (!(item.cls === 'tool' || item.otyp === TOOL_CLASS || item.glyph === '(')) return false;
    const kind = toolChargeKind(item);
    if (kind === 'oil lamp' || kind === 'brass lantern') return true;
    if (kind === 'magic lamp' && item.known === false) return true;
    return isRechargeableTool(item) && (item.known || item.dknown || item.bknown || item.tool === 'charges' || item.tool === 'magicMarker');
}

function chargePromptLetters() {
    return inventoryLetters(isChargePromptSuggested) || '*';
}

function chargePromptText() {
    return `What do you want to charge? [${getobjPromptLetters(chargePromptLetters())} or ?*]`;
}

function rechargeMarkerCameraOrKit(item, curseBless, oldRecharged) {
    if (curseBless < 0) return stripCharges(item);
    const kind = toolChargeKind(item);
    if (kind === 'magic marker' && oldRecharged) {
        item.recharged = 1;
        return [(item.spe ?? 0) < 3 ? 'Your marker seems permanently dried out.' : 'Nothing happens.'];
    }
    const spe = item.spe ?? 0;
    if (curseBless > 0) {
        const amount = rn1(16, 15);
        if (spe + amount <= 50) item.spe = 50;
        else if (spe + amount <= 75) item.spe = 75;
        else item.spe = Math.min(127, spe + amount);
        item.spe = capWishSpe(item.spe);
        updateChargedItemLine(item);
        return [chargeGlowMessage(item, 'blue')];
    }
    const amount = rn1(11, 10);
    item.spe = spe + amount <= 50 ? 50 : Math.min(SPE_LIM, spe + amount);
    updateChargedItemLine(item);
    return [chargeGlowMessage(item, 'white')];
}

function rechargeLamp(item, curseBless) {
    const messages = [];
    if (curseBless < 0) {
        messages.push(...stripCharges(item));
        if (item.lamplit || item.burning) {
            item.lamplit = false;
            item.burning = false;
            delete item._burnTimer;
            delete item.litRadius;
            if (!game.u?.blind) messages.push(`${chargingObjectSubject(item)} goes out!`);
        }
        updateChargedItemLine(item);
        return messages;
    }
    item.spe = 1;
    if (curseBless > 0) {
        item.age = 1500;
        updateChargedItemLine(item);
        return [chargeGlowMessage(item, 'blue')];
    }
    item.age = Math.min(1500, (item.age ?? 0) + 750);
    updateChargedItemLine(item);
    return [chargeGlowMessage(item)];
}

function rechargeCrystalBall(item, curseBless) {
    if ((item.spe ?? 0) === -1) item.spe = 0;
    if (curseBless < 0) {
        const messages = [];
        if (!item.cursed) {
            messages.push(chargeGlowMessage(item, 'black'));
            item.cursed = true;
            item.blessed = false;
        } else {
            messages.push(`${chargingObjectSubject(item)} vibrates briefly.`);
        }
        if ((item.spe ?? 0) > 0) {
            const payment = costlyAlterationPaymentMessage(item, 'uncharge');
            if (payment) messages.push(payment);
        }
        item.spe = 0;
        updateChargedItemLine(item);
        return messages;
    }
    if (curseBless > 0) {
        item.spe = 7;
        item.blessed = true;
        item.cursed = false;
        updateChargedItemLine(item);
        return [chargeGlowMessage(item, 'light blue')];
    }
    if ((item.spe ?? 0) < 7 || item.cursed) {
        item.spe = Math.min(7, (item.spe ?? 0) + rnd(2));
        item.cursed = false;
        updateChargedItemLine(item);
        return [chargeGlowMessage(item)];
    }
    return ['Nothing happens.'];
}

function rechargeGreaseBagOrHorn(item, curseBless) {
    if (curseBless < 0) return stripCharges(item);
    const spe = item.spe ?? 0;
    if (curseBless > 0) {
        item.spe = Math.min(50, spe + (spe <= 10 ? rn1(10, 6) : rn1(5, 6)));
        updateChargedItemLine(item);
        return [chargeGlowMessage(item, 'blue')];
    }
    item.spe = Math.min(50, spe + rn1(5, 2));
    updateChargedItemLine(item);
    return [chargeGlowMessage(item)];
}

function rechargeInstrument(item, curseBless) {
    if (curseBless < 0) return stripCharges(item);
    if (curseBless > 0) {
        item.spe = Math.min(20, (item.spe ?? 0) + d(2, 4));
        updateChargedItemLine(item);
        return [chargeGlowMessage(item, 'blue')];
    }
    item.spe = Math.min(20, (item.spe ?? 0) + rnd(4));
    updateChargedItemLine(item);
    return [chargeGlowMessage(item)];
}

function rechargeTool(item, curseBless) {
    const kind = toolChargeKind(item);
    const oldRecharged = item.recharged || 0;
    if (CHARGED_TOOL_KINDS.has(kind) && oldRecharged < 7) item.recharged = oldRecharged + 1;

    if (kind === 'bell of opening') {
        if (curseBless < 0) return { messages: stripCharges(item), more: false };
        const amount = curseBless > 0 ? rnd(3) : 1;
        item.spe = Math.min(5, (item.spe ?? 0) + amount);
        updateChargedItemLine(item);
        return { messages: [], more: false };
    }
    if (kind === 'magic marker' || kind === 'tinning kit' || kind === 'expensive camera')
        return { messages: rechargeMarkerCameraOrKit(item, curseBless, oldRecharged), more: false };
    if (kind === 'oil lamp' || kind === 'brass lantern')
        return { messages: rechargeLamp(item, curseBless), more: false };
    if (kind === 'crystal ball')
        return { messages: rechargeCrystalBall(item, curseBless), more: false };
    if (kind === 'horn of plenty' || kind === 'bag of tricks' || kind === 'can of grease')
        return { messages: rechargeGreaseBagOrHorn(item, curseBless), more: false };
    if (kind === 'magic flute' || kind === 'magic harp' || kind === 'frost horn' || kind === 'fire horn' || kind === 'drum of earthquake')
        return { messages: rechargeInstrument(item, curseBless), more: false };
    return { messages: ['You have a feeling of loss.'], more: false };
}

function rechargeItem(item, curseBless) {
    if (isWandItem(item)) return rechargeWand(item, curseBless);
    if (item?.cls === 'ring' || item?.otyp === RING_CLASS || item?.glyph === '=')
        return rechargeRing(item, curseBless);
    if (isWeaponTool(item)) return { messages: ['That is a silly thing to charge.'], more: true, retry: true };
    if (item?.cls === 'tool' || item?.otyp === TOOL_CLASS || item?.glyph === '(')
        return rechargeTool(item, curseBless);
    return { messages: ['You have a feeling of loss.'], more: false };
}

function fireScrollDamage(item) {
    const cval = item?.blessed ? 1 : item?.cursed ? -1 : 0;
    return Math.trunc((2 * (rn1(3, 3) + 2 * cval) + 1) / 3);
}

function canCenterFireScroll(x, y) {
    if (x == null || y == null || x < 1 || x >= COLNO || y < 0 || y >= ROWNO) return false;
    const loc = game.level?.at(x, y);
    if (loc) {
        const accessible = ACCESSIBLE(loc.typ) || IS_POOL(loc.typ) || loc.typ === LAVAPOOL || loc.typ === LAVAWALL;
        if (!accessible) return false;
    }
    const ux = game.u?.ux ?? 0;
    const uy = game.u?.uy ?? 0;
    return couldsee(x, y) && (x - ux) ** 2 + (y - uy) ** 2 < 32;
}

function heroInsideGasCloud() {
    const ux = game.u?.ux ?? -1;
    const uy = game.u?.uy ?? -1;
    return (game.level?.regions || []).some(reg =>
        reg.type === 'gas_cloud' && reg.visible !== false
        && reg.coords?.some(coord => coord.x === ux && coord.y === uy));
}

const PROPER_NAME_MS_ORC_MONSTER_NAMES = new Set(['yeenoghu', 'orcus']);
const GENERATED_PROPER_NAME_MONSTER_NAMES = new Set([
    ...PROPER_NAME_MS_ORC_MONSTER_NAMES, 'juiblex', 'demogorgon',
]);

function fireScrollMonsterName(mon) {
    if (mon?.givenName) return mon.givenName;
    if (mon?.isshk && mon.shknam) return mon.shknam;
    const baseName = mon?.data?.name || mon?.name || 'monster';
    const baseNameLower = String(baseName).toLowerCase();
    const properName = mon?.properName || mon?.pname || mon?.data?.properName || mon?.data?.pname
        || GENERATED_PROPER_NAME_MONSTER_NAMES.has(baseNameLower);
    return properName ? String(baseName) : `The ${baseName}`;
}

function monsterPossessiveName(mon) {
    const owner = fireScrollMonsterName(mon).replace(/^The /, 'the ');
    return owner.endsWith('s') ? `${owner}'` : `${owner}'s`;
}

function monsterTasteSubject(mon) {
    return fireScrollMonsterName(mon).replace(/^The /, 'the ');
}

function coldRayMonsterName(mon) {
    return fireScrollMonsterName(mon).replace(/^The /, 'the ');
}

const REFLECTING_MONSTER_ARTIFACTS = new Set(['dragonbane', 'the longbow of diana', 'longbow of diana']);

function monsterReflectionItemName(item) {
    return String(item?.actualKind || item?.kind || item?.artifact || item?.oname || item?.oextra?.oname || item?.line || '')
        .toLowerCase()
        .replace(/^the /, '');
}

function monsterReflectionSource(mon) {
    const inventory = mon?.minvent || [];
    for (const item of inventory) {
        const kind = monsterReflectionItemName(item);
        if (item?.otyp === SHIELD_OF_REFLECTION || kind === 'shield of reflection')
            return { source: 'shield', item, kind: 'shield of reflection' };
    }
    for (const item of inventory) {
        const artifact = String(item?.artifact || item?.oartifact || '').toLowerCase().replace(/^the /, '');
        if ((item?.cls === 'weapon' || item?.glyph === ')' || item?.wielded || item === mon?.mwep)
            && (REFLECTING_MONSTER_ARTIFACTS.has(artifact) || REFLECTING_MONSTER_ARTIFACTS.has(monsterReflectionItemName(item))))
            return { source: 'weapon', item };
    }
    for (const item of inventory) {
        const kind = monsterReflectionItemName(item);
        if (item?.amuletIndex === 7 || kind === 'amulet of reflection')
            return { source: 'amulet', item, kind: 'amulet of reflection' };
    }
    for (const item of inventory) {
        const kind = monsterReflectionItemName(item);
        if (item?.otyp === SILVER_DRAGON_SCALE_MAIL || item?.otyp === SILVER_DRAGON_SCALES
            || kind === 'silver dragon scale mail' || kind === 'silver dragon scales')
            return { source: 'armor', item };
    }
    const monsterName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    if (monsterName === 'silver dragon' || monsterName === 'chromatic dragon')
        return { source: 'scales', item: null };
    return null;
}

function recordMonsterReflectionDiscovery(reflection) {
    if (!reflection?.item) return;
    reflection.item.known = true;
    if (reflection.kind === 'shield of reflection') recordKnownArmorDiscovery('shield of reflection', false);
    else if (reflection.kind === 'amulet of reflection') recordKnownAmuletDiscovery('amulet of reflection', reflection.item);
}

function fireScrollTargetDescription(x, y) {
    if (x === game.u?.ux && y === game.u?.uy) return heroFarlookDescription();
    const mon = (game.level?.monsters || []).find(item => item.mx === x && item.my === y);
    if (mon) return fireScrollMonsterName(mon).replace(/^The /, 'the ');
    const object = (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y);
    if (object) return pickupObjectPhrase(object);
    const loc = game.level?.at(x, y);
    if (!loc || (!loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ')) return 'unexplored area';
    if (loc.typ === DOOR) return doorDescription(loc);
    if (loc.typ === CORR) return 'corridor';
    if (loc.typ === ROOM || loc.typ === STAIRS) return 'floor of a room';
    if (IS_POOL(loc.typ)) return 'pool of water';
    if (loc.typ === LAVAPOOL) return 'molten lava';
    if (loc.typ === LAVAWALL) return 'lava wall';
    if (loc.typ === TREE) return 'tree';
    if (loc.typ && loc.typ < DOOR) return 'wall';
    return 'unexplored area';
}

function heroIsDeaf() {
    return (game.u?._statusSuffix || '').includes('Deaf') || (game.u?._deafTimeout || 0) > 0;
}

const ANIMAL_MSOUND_NAMES = new Set([
    'silent', 'ms_silent',
    'bark', 'ms_bark',
    'mew', 'ms_mew',
    'roar', 'ms_roar',
    'bellow', 'ms_bellow',
    'growl', 'ms_growl',
    'sqeek', 'ms_sqeek',
    'squeak', 'ms_squeak',
    'sqawk', 'ms_sqawk',
    'squawk', 'ms_squawk',
    'chirp', 'ms_chirp',
    'hiss', 'ms_hiss',
    'buzz', 'ms_buzz',
    'grunt', 'ms_grunt',
    'neigh', 'ms_neigh',
    'moo', 'ms_moo',
    'wail', 'ms_wail',
    'gurgle', 'ms_gurgle',
    'burble', 'ms_burble',
    'trumpet', 'ms_trumpet',
    'animal', 'ms_animal',
]);

function shopkeeperMuteForSpeech(shkp) {
    if (!shkp) return false;
    const data = shkp.data || shkp.mdata || {};
    if (shkp.mute || shkp.silent || shkp.helpless || data.mute || data.silent || data.helpless) return true;
    const msound = shkp.msound ?? shkp.sound ?? data.msound ?? data.sound;
    if (typeof msound === 'number') return msound <= 17;
    return ANIMAL_MSOUND_NAMES.has(String(msound || '').toLowerCase());
}

function shopkeeperCanSpeakToHero(shkp) {
    return !heroIsDeaf() && !shopkeeperMuteForSpeech(shkp);
}

function shopkeeperNoLimbsForSpeech(shkp) {
    const data = shkp?.data || shkp?.mdata || {};
    return !!(shkp?.nolimbs || shkp?.noLimbs || shkp?.nohands || shkp?.noHands
        || data.nolimbs || data.noLimbs || data.nohands || data.noHands);
}

function fireDestroyableInventoryClass(item) {
    if (isGreenSlimeGlobItem(item)) return 'slime';
    const cls = item?.cls || (item?.otyp === POTION_CLASS || item?.otyp === POT_ACID
        || item?.otyp === POT_WATER || item?.otyp === POT_OIL || item?.glyph === '!' ? 'potion'
        : item?.otyp === SCROLL_CLASS || item?.glyph === '?' ? 'scroll'
            : item?.otyp === SPBOOK_NO_NOVEL || item?.otyp === SPE_HEALING || item?.otyp === BOOK_OF_THE_DEAD || item?.glyph === '+'
                ? 'spellbook'
                : '');
    if (cls === 'potion' || cls === 'scroll' || cls === 'spellbook') return cls;
    const name = String(item?.actualKind || item?.kind || item?.line || '').toLowerCase();
    if (/\bglob of green slime\b/.test(name)) return 'slime';
    if (name.includes('potion')) return 'potion';
    if (name.includes('scroll')) return 'scroll';
    if (name.includes('spellbook') || name.includes('book of the dead')) return 'spellbook';
    return '';
}

function fireInventoryItemImmune(item, cls) {
    const name = String(item?.actualKind || item?.kind || item?.line || '').toLowerCase();
    if (item?.artifact || item?.oartifact || (item?.in_use && (item?.quan || 1) === 1)) return true;
    if (cls === 'scroll' && (item?.scrollIndex === 16 || /\bscroll of fire\b/.test(name))) return true;
    if (cls === 'spellbook' && /\bfireball\b/.test(name)) return true;
    return false;
}

export function monsterFireInventoryDamage(mon, origDamage, messages, visible) {
    let limit = Math.trunc(origDamage / 5);
    if (origDamage % 5 > rn2(5)) limit++;
    if (limit < 1) return 0;
    limit = Math.min(20, limit);

    const selected = [];
    let eligible = 0;
    for (const item of [...(mon?.minvent || [])]) {
        const cls = fireDestroyableInventoryClass(item);
        if (!cls || fireInventoryItemImmune(item, cls)) continue;
        const i = eligible < limit ? eligible : rn2(eligible);
        eligible++;
        if (i < limit) selected[i] = item;
    }

    let damage = 0;
    const owner = fireScrollMonsterName(mon).replace(/^The /, 'the ');
    const possessive = owner.endsWith('s') ? `${owner}'` : `${owner}'s`;
    const resistsFire = !!(mon?.fireResistance || mon?.data?.resistsFire);
    for (const item of selected.filter(Boolean)) {
        const cls = fireDestroyableInventoryClass(item);
        if (cls === 'spellbook' && isBookOfTheDeadItem(item)) {
            if (visible) messages.push('The Book of the Dead glows a strange dark red, but remains intact.');
            continue;
        }
        const quan = item.quan || 1;
        const itemDamage = cls === 'potion' ? rnd(6)
            : cls === 'slime' ? Math.trunc(((item.owt || 20) + 19) / 20)
                : resistsFire ? 0 : 1;
        let destroyed = 0;
        for (let i = 0; i < quan; i++)
            if (!rn2(3)) destroyed++;
        if (!destroyed) continue;

        if (visible) {
            const plural = destroyed > 1;
            const itemName = pickupObjectName({ ...item, line: '', quan: plural ? Math.max(2, quan) : 1 });
            const subject = destroyed === 1 && quan === 1 ? `${sentenceCase(possessive)} ${itemName}`
                : destroyed === 1 ? `One of ${possessive} ${itemName}`
                    : destroyed < quan ? `Some of ${possessive} ${itemName}`
                        : quan === 2 ? `Both of ${possessive} ${itemName}`
                            : `All of ${possessive} ${itemName}`;
            const verb = fireInventoryDestroyVerb(cls, item, plural);
            messages.push(`${subject} ${verb}!`);
        }
        const remaining = quan - destroyed;
        if (remaining > 0) item.quan = remaining;
        else mon.minvent = (mon.minvent || []).filter(other => other !== item);
        damage += itemDamage;
    }
    return damage;
}

function monsterColdInventoryDamage(mon, origDamage, messages, visible) {
    let damage = 0;
    const owner = fireScrollMonsterName(mon).replace(/^The /, 'the ');
    const possessive = owner.endsWith('s') ? `${owner}'` : `${owner}'s`;
    for (const item of coldDestroyInventorySelection(mon?.minvent || [], origDamage)) {
        const itemDamage = rnd(4);
        const quan = Math.max(0, (item.quan || 1) - (item.in_use ? 1 : 0));
        let destroyed = 0;
        for (let i = 0; i < quan; i++)
            if (!rn2(3)) destroyed++;
        if (!destroyed) continue;

        if (visible) {
            const plural = destroyed > 1;
            const itemName = coldPotionDisplayName(item, plural);
            const subject = destroyed === 1 && quan === 1 ? `${sentenceCase(possessive)} ${itemName}`
                : destroyed === 1 ? `One of ${possessive} ${itemName}`
                    : destroyed < quan ? `Some of ${possessive} ${itemName}`
                        : quan === 2 ? `Both of ${possessive} ${itemName}`
                            : `All of ${possessive} ${itemName}`;
            messages.push(`${subject} ${coldInventoryDestroyVerb(plural)}!`);
        }
        const remaining = (item.quan || 1) - destroyed;
        if (remaining > 0) item.quan = remaining;
        else mon.minvent = (mon.minvent || []).filter(other => other !== item);
        damage += itemDamage;
    }
    return damage;
}

function maybeIgniteMonsterFireItem(item, messages, visible) {
    if (!fireItemCanCatchLight(item)) return false;
    const kind = objectKindKey(item);
    if ((item.otyp === OIL_LAMP || item.otyp === MAGIC_LAMP || kind === 'oil lamp' || kind === 'magic lamp')
        && item.cursed && !rn2(2))
        return false;
    const name = pickupObjectName({ ...item, line: '' });
    beginWishedBurn(item);
    if (visible) {
        const subject = sentenceCase(articleFor(name));
        messages.push(`${subject} ${fireInventoryNameVerb(name, 'catches', 'catch')} light!`);
    }
    return true;
}

export function igniteMonsterFireInventoryItems(mon, messages = [], visible = false) {
    for (const item of [...(mon?.minvent || [])])
        maybeIgniteMonsterFireItem(item, messages, visible);
    return messages;
}

function markFireExplosionResultMetadata(messages, result) {
    if (result?.lifeSaving) messages.lifeSaving = true;
    if (result?.fatal) messages.fatal = true;
    if (result?.more || result?.lifeSaving || result?.fatal) messages.more = true;
}

function appendFireExplosionMessages(messages, result) {
    messages.push(...(result?.messages || []));
    markFireExplosionResultMetadata(messages, result);
}

export function applyHeroFireExplosionInventoryDamage(messages, origDamage, deathCause, { fatalMessage = '' } = {}) {
    const fireInventory = fireDamageInventory(origDamage, true, false, { allowLifeSaving: true });
    messages.push(...fireInventory.messages);
    if (fireInventory.lifeSaving || fireInventory.fatal) {
        messages.lifeSaving = !!fireInventory.lifeSaving;
        messages.fatal = !!fireInventory.fatal;
        messages.more = true;
        return { fireInventory, damage: 0 };
    }

    if (game.u?.uinvulnerable) messages.push('You are unharmed!');
    const damage = (game.u?.fireResistance || game.u?.uinvulnerable ? 0 : origDamage) + fireInventory.damage;
    if (damage && game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
    if ((game.u?.uhp || 0) <= 0) {
        game._death_cause = fireInventory.deathCause || deathCause;
        if (fatalMessage) messages.push(fatalMessage);
        messages.push('You die...');
    }
    return { fireInventory, damage };
}

function resolveFireScrollExplosion(cx, cy, dam, messages = []) {
    const ux = game.u?.ux ?? -99;
    const uy = game.u?.uy ?? -99;
    const underwater = !!(game.u?.underwater || game.u?.uunderwater);
    if (underwater) {
        messages.push('The water around you vaporizes violently!');
    } else if (cx === ux && cy === uy) {
        messages.push('The scroll erupts in a tower of flame!');
    }
    if (!heroIsDeaf()) messages.push('Boom!');

    for (const mon of [...(game.level?.monsters || [])]) {
        if (!mon || mon.dead || (mon.mhp ?? 1) <= 0 || Math.abs((mon.mx ?? -99) - cx) > 1 || Math.abs((mon.my ?? -99) - cy) > 1)
            continue;
        const visible = !game.u?.blind && (game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT);
        const name = fireScrollMonsterName(mon);
        if (visible) messages.push(`${name} is caught in the tower of flame!`);
        const itemDamage = monsterFireInventoryDamage(mon, dam, messages, visible);
        let damage = 0;
        if (!(mon.fireResistance || mon.data?.resistsFire)) {
            damage = dam;
            if (monsterResistsEffect(mon, 9)) {
                if (visible) messages.push(`${name} resists the tower of flame!`);
                damage = Math.trunc((damage + 1) / 2);
            }
            if (mon.coldResistance || mon.data?.resistsCold || mon.data?.coldResistance) damage *= 2;
        }
        damage += itemDamage;
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) {
            dropMonsterInventory(mon, messages);
            game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
            recordVanquished(mon, true);
            newsym(mon.mx, mon.my);
            if (visible) messages.push(`You kill the ${mon.data?.name || mon.name || 'monster'}!`);
        }
    }

    if (Math.abs(ux - cx) <= 1 && Math.abs(uy - cy) <= 1) {
        applyHeroFireExplosionInventoryDamage(messages, dam, 'killed by a tower of flame');
        exerciseAttribute(A_STR, false);
    }

    return {
        messages,
        more: messages.length > 1 || (game.u?.uhp || 1) <= 0 || !!messages.more,
        lifeSaving: !!messages.lifeSaving,
        fatal: !!messages.fatal,
    };
}

function resolvePyroliskEggExplosion(cx, cy, dam) {
    const messages = [];
    const ux = game.u?.ux ?? -99;
    const uy = game.u?.uy ?? -99;
    if (!heroIsDeaf()) messages.push('Boom!');

    for (const mon of [...(game.level?.monsters || [])]) {
        if (!mon || mon.dead || (mon.mhp ?? 1) <= 0 || Math.abs((mon.mx ?? -99) - cx) > 1 || Math.abs((mon.my ?? -99) - cy) > 1)
            continue;
        const visible = !game.u?.blind && (game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT);
        const name = fireScrollMonsterName(mon);
        if (visible) messages.push(`${name} is caught in the fireball!`);
        const itemDamage = monsterFireInventoryDamage(mon, dam, messages, visible);
        let damage = 0;
        if (!(mon.fireResistance || mon.data?.resistsFire)) {
            damage = dam;
            if (monsterResistsEffect(mon, 9)) {
                if (visible) messages.push(`${name} resists the fireball!`);
                damage = Math.trunc((damage + 1) / 2);
            }
            if (mon.coldResistance || mon.data?.resistsCold || mon.data?.coldResistance) damage *= 2;
        }
        damage += itemDamage;
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) {
            dropMonsterInventory(mon, messages);
            game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
            recordVanquished(mon, true);
            newsym(mon.mx, mon.my);
            if (visible) messages.push(`You kill the ${mon.data?.name || mon.name || 'monster'}!`);
        }
    }

    if (Math.abs(ux - cx) <= 1 && Math.abs(uy - cy) <= 1) {
        messages.push('You are caught in the fireball!');
        applyHeroFireExplosionInventoryDamage(messages, dam, 'killed by a fireball', {
            fatalMessage: 'It is fatal.',
        });
        exerciseAttribute(A_STR, false);
    }

    return {
        messages,
        more: messages.length > 1 || (game.u?.uhp || 1) <= 0 || !!messages.more,
        lifeSaving: !!messages.lifeSaving,
        fatal: !!messages.fatal,
    };
}

function earthScrollHasEffect() {
    const uz = game.u?.uz;
    if (Is_rogue_level(uz)) return false;
    if (In_endgame(uz) && !Is_earthlevel(uz)) return false;
    return true;
}

function earthCeilingMessage(item) {
    const around = item?.blessed ? 'around' : 'above';
    if (game.u?.uswallow) return 'You hear rumbling.';
    if (In_quest(game.u?.uz)) {
        return item?.blessed
            ? 'Avalanches of boulders materialize around you!'
            : 'An avalanche of boulders materializes above you!';
    }
    return `The ceiling rumbles ${around} you!`;
}

function applySokobanGuilt() {
    if (!game.level?.flags?.sokoban_rules || !game.u) return;
    game.u.uconduct ??= {};
    game.u.uconduct.sokocheat = (game.u.uconduct.sokocheat || 0) + 1;
    game.u.uluck = (game.u.uluck || 0) - 1;
}

function makeEarthDropObject(confused) {
    const obj = mksobj(confused ? ROCK : BOULDER, false, false);
    obj.cls = 'rock';
    obj.kind = confused ? 'rock' : 'boulder';
    obj.glyph = confused ? '*' : '`';
    obj.color = NO_COLOR;
    if (confused) {
        obj.quan = rn1(5, 2);
        obj.plural = 'rocks';
    } else {
        obj.quan = 1;
    }
    return obj;
}

function earthDropObjectPhrase(obj) {
    const quan = obj?.quan || 1;
    if (obj?.otyp === BOULDER) return 'a boulder';
    return quan === 1 ? 'a rock' : `${quan} rocks`;
}

function earthPlaceObject(obj, x, y) {
    if (!obj || !game.level) return false;
    Object.assign(obj, { ox: x, oy: y });
    game.level.objects ??= [];
    game.level.objects.push(obj);
    newsym(x, y);
    return true;
}

function earthVisibleSquare(x, y) {
    return !game.u?.blind && !!(game.viz_array?.[y]?.[x] & IN_SIGHT);
}

function earthLiquidUnderDrawbridge(loc) {
    if (loc?.typ !== DRAWBRIDGE_UP) return null;
    const under = drawbridgeUnder(loc);
    if (under === DB_LAVA) return LAVAPOOL;
    if (under === DB_MOAT) return MOAT;
    return null;
}

function earthBoulderHitsLiquid(loc) {
    if (!loc) return false;
    if (loc.typ === DRAWBRIDGE_UP) return !!earthLiquidUnderDrawbridge(loc);
    return IS_POOL(loc.typ) || loc.typ === LAVAPOOL || loc.typ === LAVAWALL;
}

function earthLiquidIsLava(loc) {
    return loc?.typ === LAVAPOOL || loc?.typ === LAVAWALL || earthLiquidUnderDrawbridge(loc) === LAVAPOOL;
}

function earthWaterBodyName(loc) {
    const typ = earthLiquidUnderDrawbridge(loc) || loc?.typ;
    if (typ === LAVAPOOL) return 'molten lava';
    if (typ === LAVAWALL) return 'wall of lava';
    if (typ === POOL) return 'pool of water';
    if (typ === MOAT) return 'moat';
    if (typ === WATER) return Is_waterlevel(game.u?.uz) ? 'limitless water' : 'wall of water';
    return 'water';
}

function earthBoulderFillsLiquid(loc, lava, chance) {
    if (Is_waterlevel(game.u?.uz)) return false;
    if (loc?.typ === WATER) return chance < 5;
    return lava ? chance === 0 : chance !== 0;
}

function fillBoulderLiquidTerrain(loc) {
    if (!loc) return;
    if (loc.typ === DRAWBRIDGE_UP) {
        loc.flags = ((loc.flags || 0) & ~DB_UNDER) | DB_FLOOR;
    } else {
        loc.typ = ROOM;
        loc.flags = 0;
    }
}

function shopRoomMatchesType(roomno, typewanted = SHOPBASE) {
    if (roomno < ROOMOFFSET) return false;
    if (!typewanted) return true;
    const room = levelRoomByRoomno(roomno);
    if (!room) return false;
    return room.rtype === typewanted || (typewanted === SHOPBASE && room.rtype >= SHOPBASE);
}

function shopRoomnosAt(x, y, typewanted = SHOPBASE) {
    const loc = game.level?.at?.(x, y);
    const roomno = loc?.roomno ?? 0;
    if (roomno >= ROOMOFFSET)
        return shopRoomMatchesType(roomno, typewanted) ? [roomno] : [];
    if (roomno !== SHARED && roomno !== SHARED_PLUS) return [];

    const found = [];
    const step = roomno === SHARED ? 2 : 1;
    const minX = Math.max(0, x - 1);
    const maxX = Math.min(COLNO - 1, x + 1);
    const minY = Math.max(0, y - 1);
    const maxY = Math.min(ROWNO - 1, y + 1);
    for (let nx = minX; nx <= maxX; nx += step) {
        for (let ny = minY; ny <= maxY; ny += step) {
            const adjRoomno = game.level?.at?.(nx, ny)?.roomno ?? 0;
            if (adjRoomno >= ROOMOFFSET
                && !found.includes(adjRoomno)
                && shopRoomMatchesType(adjRoomno, typewanted)) {
                found.push(adjRoomno);
            }
        }
    }
    return found;
}

function shopkeeperForRoomno(roomno) {
    const room = levelRoomByRoomno(roomno);
    if (!room || room.rtype < SHOPBASE) return null;
    const shkp = room.resident || (game.level?.monsters || [])
        .find(mon => mon.isshk && mon.shoproom === roomno);
    if (!shkp?.isshk) return null;
    return shkp;
}

function shopkeeperForShopRoom(x, y) {
    const roomno = game.level?.at?.(x, y)?.roomno || 0;
    return shopkeeperForRoomno(roomno);
}

function shopkeeperInHisShop(shkp) {
    if (!shkp?.isshk || shkp.dead || (shkp.mhp != null && shkp.mhp <= 0)) return false;
    const x = shkp.mx ?? shkp.shk?.x;
    const y = shkp.my ?? shkp.shk?.y;
    if (x == null || y == null) return false;
    return (game.level?.at?.(x, y)?.roomno || 0) === shkp.shoproom;
}

function liveShopkeeper(mon) {
    return !!mon?.isshk && !mon.dead && (mon.mhp == null || mon.mhp > 0);
}

function shopDamageList() {
    if (!game.level) game.level = {};
    if (!Array.isArray(game.level.damagelist)) game.level.damagelist = [];
    return game.level.damagelist;
}

function shopkeeperForShopDoorAt(x, y) {
    for (const roomno of shopRoomnosAt(x, y, SHOPBASE)) {
        const shkp = shopkeeperForRoomno(roomno);
        if (!liveShopkeeper(shkp)) continue;
        if ((shkp.shd?.x ?? null) === x && (shkp.shd?.y ?? null) === y) return shkp;
    }
    return null;
}

function addShopTerrainDamage(x, y, cost) {
    const loc = game.level?.at?.(x, y);
    if (!loc || loc.typ !== DOOR) return false;
    const shkp = shopkeeperForShopDoorAt(x, y);
    if (!shkp) return false;

    const damage = shopDamageList();
    const when = Math.max(0, Math.trunc(Number(game.moves || 0)));
    const entry = damage.find(dam => dam.x === x && dam.y === y);
    if (entry) {
        entry.cost = Math.max(0, Math.trunc(Number(entry.cost || 0))) + Math.max(0, Math.trunc(Number(cost || 0)));
        entry.when = when;
        entry.shoproom = shkp.shoproom;
        entry.shopkeeperId = shopkeeperIdentity(shkp);
        return true;
    }

    damage.push({
        x,
        y,
        cost: Math.max(0, Math.trunc(Number(cost || 0))),
        when,
        typ: loc.typ,
        flags: loc.doormask ?? D_CLOSED,
        shoproom: shkp.shoproom,
        shopkeeperId: shopkeeperIdentity(shkp),
    });
    return true;
}

function shopkeeperForDamageEntry(dam) {
    if (!dam) return null;
    if (dam.shoproom != null) {
        const byRoom = shopkeeperForRoomno(dam.shoproom);
        if (liveShopkeeper(byRoom)) return byRoom;
    }
    if (dam.shopkeeperId != null) {
        const found = (game.level?.monsters || []).find(mon =>
            liveShopkeeper(mon) && String(shopkeeperIdentity(mon)) === String(dam.shopkeeperId));
        if (found) return found;
    }
    return shopkeeperForShopDoorAt(dam.x, dam.y);
}

function currentShopTerrainDamageEntries() {
    const when = Math.max(0, Math.trunc(Number(game.moves || 0)));
    return shopDamageList().filter(dam => dam.when === when && Math.max(0, Math.trunc(Number(dam.cost || 0))) > 0 && !dam.paid);
}

function payForCurrentShopTerrainDamage(dmgstr, messages = []) {
    const totals = new Map();
    for (const dam of currentShopTerrainDamageEntries()) {
        const shkp = shopkeeperForDamageEntry(dam);
        if (!shopkeeperInHisShop(shkp)) continue;
        totals.set(shkp, (totals.get(shkp) || 0) + Math.max(0, Math.trunc(Number(dam.cost || 0))));
        dam.paid = true;
        dam.damageVerb = dmgstr;
    }

    for (const [shkp, amount] of totals) {
        const beforeDebit = Math.max(0, Math.trunc(Number(shkp.debit || 0)));
        const beforeRobbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0)));
        const peaceful = heroInShopOwnedBy(shkp);
        const charged = chargeShopkeeperForLostMerchandise(shkp, amount, { peaceful });
        if (!charged) continue;
        const deltas = shopDebtDeltas(shkp, beforeDebit, beforeRobbed);
        if (deltas.debitDelta > 0)
            messages.push(`You did ${deltas.debitDelta} ${shopCurrency(deltas.debitDelta)} worth of damage!`);
        else if (deltas.robbedDelta > 0)
            messages.push(`You caused ${deltas.robbedDelta} ${shopCurrency(deltas.robbedDelta)} worth of damage!`);
    }
    return totals.size > 0;
}

function monsterPassesWallsForRepair(mon) {
    const data = mon?.data || {};
    return !!(mon?.passesWalls || mon?.passes_walls || mon?.wallwalk
        || data.passesWalls || data.passes_walls || data.wallwalk || data.noncorporeal || data.whirly);
}

function repairableShopDamage(dam, shkp) {
    if (!dam || !shopkeeperInHisShop(shkp)) return false;
    const moves = Math.max(0, Math.trunc(Number(game.moves || 0)));
    if (moves - Math.max(0, Math.trunc(Number(dam.when || 0))) < REPAIR_DELAY) return false;
    if (!shopRoomnosAt(dam.x, dam.y, SHOPBASE).includes(shkp.shoproom)) return false;

    if (!IS_ROOM(dam.typ)) {
        if (game.u?.ux === dam.x && game.u?.uy === dam.y && !game.u?.passesWalls && !game.u?.passes_walls)
            return false;
        if ((shkp.mx ?? null) === dam.x && (shkp.my ?? null) === dam.y) return false;
        const blocker = (game.level?.monsters || []).find(mon =>
            mon !== shkp && !mon.dead && (mon.mhp == null || mon.mhp > 0)
            && mon.mx === dam.x && mon.my === dam.y && !monsterPassesWallsForRepair(mon));
        if (blocker) return false;
    }
    return true;
}

export function repairShopDamageForShopkeeper(shkp, messages = []) {
    const damage = shopDamageList();
    const dam = damage.find(entry => repairableShopDamage(entry, shkp));
    if (!dam) return false;

    const loc = game.level?.at?.(dam.x, dam.y);
    if (!loc) return false;
    const visibleShopkeeper = !game.u?.blind && cansee(shkp.mx, shkp.my);
    const closeToHero = ((shkp.mx ?? 0) - (game.u?.ux ?? 0)) ** 2 + ((shkp.my ?? 0) - (game.u?.uy ?? 0)) ** 2
        <= (BOLT_LIM / 2) * (BOLT_LIM / 2);
    if (visibleShopkeeper) {
        messages.push(`${shopkeeperDisplayName(shkp)} whispers ${closeToHero ? 'an incantation' : 'something'}.`);
    } else if (!heroIsDeaf() && closeToHero) {
        messages.push('You hear someone muttering an incantation.');
    }

    const wasAlreadyRepaired = dam.typ === loc.typ && (dam.typ !== DOOR || (loc.doormask & (D_CLOSED | D_LOCKED)));
    if (!wasAlreadyRepaired) {
        loc.typ = dam.typ;
        if (dam.typ === DOOR) {
            loc.doormask = D_CLOSED;
            loc.flags = D_CLOSED;
        } else {
            loc.flags = dam.flags ?? loc.flags ?? 0;
        }
        if (!game.u?.blind && cansee(dam.x, dam.y)) {
            messages.push(dam.typ === DOOR
                ? 'Suddenly, the shop door reappears!'
                : 'Suddenly, a section of the wall closes up!');
        }
        vision_reset();
        vision_recalc(0);
        newsym(dam.x, dam.y);
    }

    const idx = damage.indexOf(dam);
    if (idx >= 0) damage.splice(idx, 1);
    return true;
}

function shopkeeperNextToHero(shkp, ux = game.u?.ux || 0, uy = game.u?.uy || 0) {
    if (!shkp) return false;
    return Math.max(Math.abs((shkp.mx ?? 0) - ux), Math.abs((shkp.my ?? 0) - uy)) <= 1;
}

function payCanSpotShopkeeper(shkp) {
    if (!shkp || game.u?.blind || shkp.mundetected) return false;
    if (!game.viz_array) return true;
    return cansee(shkp.mx, shkp.my) || !!(game.viz_array?.[shkp.my]?.[shkp.mx] & IN_SIGHT);
}

function scanPayShopkeepers() {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const roomno = game.level?.at?.(ux, uy)?.roomno || 0;
    const room = levelRoomByRoomno(roomno);
    const liveShopkeepers = (game.level?.monsters || []).filter(liveShopkeeper);
    const scan = {
        sk: 0,
        seensk: 0,
        nexttosk: 0,
        nxtm: null,
        resident: null,
        visibleShopkeeper: null,
        ux,
        uy,
    };

    for (const shkp of liveShopkeepers) {
        scan.sk++;
        if (shopkeeperNextToHero(shkp, ux, uy)) {
            if (!scan.nxtm || !shopkeeperAngryForSellobj(scan.nxtm)) {
                scan.nexttosk++;
                scan.nxtm = shkp;
            }
        }
        if (payCanSpotShopkeeper(shkp)) {
            scan.seensk++;
            scan.visibleShopkeeper = shkp;
        }
        if (room?.rtype >= SHOPBASE
            && shopkeeperInHisShop(shkp)
            && shkp.shoproom === roomno) {
            scan.resident = shkp;
        }
    }
    return scan;
}

function resolvePayShopkeeperFromScan(scan) {
    if (scan.nxtm && scan.nexttosk === 1)
        return { selected: scan.nxtm, resident: scan.resident, needsPayWhomPrompt: false, message: '' };

    if ((!scan.sk && !game.u?.blind) || (!game.u?.blind && !scan.seensk))
        return {
            selected: null,
            resident: scan.resident,
            needsPayWhomPrompt: false,
            message: 'There appears to be no shopkeeper here to receive your payment.',
        };

    if (!scan.seensk)
        return { selected: null, resident: scan.resident, needsPayWhomPrompt: false, message: "You can't see..." };

    if (scan.sk === 1 && scan.resident)
        return { selected: scan.resident, resident: scan.resident, needsPayWhomPrompt: false, message: '' };

    if (scan.seensk === 1) {
        const shkp = scan.visibleShopkeeper;
        if (!sameShopkeeper(shkp, scan.resident) && !shopkeeperNextToHero(shkp, scan.ux, scan.uy))
            return {
                selected: null,
                resident: scan.resident,
                needsPayWhomPrompt: false,
                message: `${shopkeeperDisplayName(shkp)} is not near enough to receive your payment.`,
            };
        return { selected: shkp, resident: scan.resident, needsPayWhomPrompt: false, message: '' };
    }

    return { selected: null, resident: scan.resident, needsPayWhomPrompt: true, message: 'Pay whom?' };
}

function payTargetCanSee(x, y) {
    if (game.u?.blind) return false;
    if (!game.viz_array) return true;
    return cansee(x, y) || !!(game.viz_array?.[y]?.[x] & IN_SIGHT);
}

function payCanSpotMonster(mon) {
    if (!mon || mon.dead || (mon.mhp != null && mon.mhp <= 0)) return false;
    if (mon.mundetected || (mon.minvis && !game.u?.seeInvisible)) return false;
    return payTargetCanSee(mon.mx, mon.my);
}

function payMonsterDisplayName(mon) {
    if (!mon) return 'Someone';
    if (mon.isshk) return shopkeeperDisplayName(mon);
    if (mon.givenName) return mon.givenName;
    const name = mon.data?.name || mon.name || 'monster';
    return `The ${name}`;
}

function monsterAtPayTarget(x, y) {
    return (game.level?.monsters || []).find(mon =>
        mon.mx === x && mon.my === y
        && !mon.dead && (mon.mhp == null || mon.mhp > 0)) || null;
}

function validatePayWhomTarget(x, y, resident) {
    if (x === (game.u?.ux || 0) && y === (game.u?.uy || 0))
        return { shkp: null, message: 'You are generous to yourself.' };

    const mon = monsterAtPayTarget(x, y);
    if (!payTargetCanSee(x, y) && (!mon || !payCanSpotMonster(mon)))
        return { shkp: null, message: `You can't ${game.u?.blind ? 'sense' : 'see'} anyone there.` };

    if (!mon)
        return { shkp: null, message: 'There is no one there to receive your payment.' };

    if (!mon.isshk)
        return { shkp: null, message: `${payMonsterDisplayName(mon)} is not interested in your payment.` };

    if (!sameShopkeeper(mon, resident) && !shopkeeperNextToHero(mon))
        return { shkp: null, message: `${shopkeeperDisplayName(mon)} is too far to receive your payment.` };

    return { shkp: mon, message: '' };
}

function payWhomTargetDescription(x, y) {
    if (x === (game.u?.ux || 0) && y === (game.u?.uy || 0)) return heroFarlookDescription();
    const mon = monsterAtPayTarget(x, y);
    if (mon && payCanSpotMonster(mon)) return farlookMonsterDescription(mon);
    const obj = payWhomObjectAt(x, y);
    if (obj) return pickupObjectPhrase(obj);
    if (!payTargetCanSee(x, y)) return 'unexplored area';
    const loc = game.level?.at(x, y);
    if (loc?.typ === DOOR) return doorDescription(loc);
    if (loc?.typ === CORR) return 'corridor';
    if (loc?.typ === ROOM || loc?.typ === STAIRS) return 'floor of a room';
    if (loc?.typ === MOAT) return 'moat';
    if (loc?.typ === TREE) return 'tree';
    if (loc?.typ && loc.typ < DOOR) return 'wall';
    return 'unexplored area';
}

function payWhomCycleDistance(x, y) {
    return Math.max(Math.abs(x - (game.u?.ux || 0)), Math.abs(y - (game.u?.uy || 0)));
}

function payWhomLocationSort(a, b) {
    return (payWhomCycleDistance(a.x, a.y) - payWhomCycleDistance(b.x, b.y))
        || (a.y - b.y) || (a.x - b.x);
}

function visiblePayWhomMonsters() {
    return (game.level?.monsters || [])
        .filter(payCanSpotMonster)
        .sort((a, b) => payWhomLocationSort({ x: a.mx, y: a.my }, { x: b.mx, y: b.my }));
}

function payWhomObjectAt(x, y) {
    if (!payTargetCanSee(x, y)) return null;
    const mon = monsterAtPayTarget(x, y);
    if (mon && payCanSpotMonster(mon)) return null;
    const loc = game.level?.at?.(x, y);
    const objects = game.level?.objects || [];
    for (let i = objects.length - 1; i >= 0; i--) {
        const obj = objects[i];
        if (!obj || obj.hidden || obj.transientProjectile || obj.ox !== x || obj.oy !== y) continue;
        if (obj.otyp === BOULDER || obj.otyp === ROCK || objectKindKey(obj) === 'boulder'
            || objectKindKey(obj) === 'rock') continue;
        if (loc?.disp_ch && loc.disp_ch !== ' ' && obj.glyph && loc.disp_ch !== obj.glyph) continue;
        return obj;
    }
    return null;
}

function visiblePayWhomObjects() {
    const seen = new Set();
    const targets = [];
    for (const obj of game.level?.objects || []) {
        if (!obj || seen.has(`${obj.ox}:${obj.oy}`)) continue;
        const target = payWhomObjectAt(obj.ox, obj.oy);
        if (!target) continue;
        seen.add(`${obj.ox}:${obj.oy}`);
        targets.push({ x: obj.ox, y: obj.oy, obj: target });
    }
    return targets.sort(payWhomLocationSort);
}

function payWhomCycleLocations(kind) {
    const hero = { x: game.u?.ux || 0, y: game.u?.uy || 0, kind: 'hero' };
    const targets = kind === 'objects'
        ? visiblePayWhomObjects()
        : visiblePayWhomMonsters().map(mon => ({ x: mon.mx, y: mon.my, mon }));
    return [hero, ...targets];
}

function cyclePayWhomCursor(kind, reverse = false) {
    const locations = payWhomCycleLocations(kind);
    if (!locations.length) return null;
    game._pay_getpos_cycle_indexes ??= {};
    const current = Math.max(0, Math.trunc(Number(game._pay_getpos_cycle_indexes[kind] || 0)));
    const index = (current + (reverse ? -1 : 1) + locations.length) % locations.length;
    game._pay_getpos_cycle_indexes[kind] = index;
    const target = locations[index];
    game._farlook_x = target.x;
    game._farlook_y = target.y;
    game._cursor_override = [target.x - 1, target.y + 1];
    return target;
}

async function startPayWhomCursor(resident, { requestMenu = false } = {}) {
    game._pay_whom_resident = resident || null;
    game._pay_request_menu = !!requestMenu;
    game._pay_getpos_cycle_indexes = {};
    game._farlook_x = game.u?.ux || 0;
    game._farlook_y = game.u?.uy || 0;
    game._cursor_override = [(game._farlook_x || 1) - 1, (game._farlook_y || 0) + 1];
    game._command_mode = 'payWhomCursor';
    await setMessage('Pay whom?');
}

function clearPayWhomCursor() {
    game._pay_whom_resident = null;
    game._pay_request_menu = false;
    game._pay_getpos_cycle_indexes = null;
    game._cursor_override = null;
}

function shopkeeperForCostlySpot(x, y) {
    const shkp = shopkeeperForShopRoom(x, y);
    if (!shkp) return null;
    if (shkp.shk && x === shkp.shk.x && y === shkp.shk.y) return null;
    return shkp;
}

function heroShopkeeper() {
    const shkp = shopkeeperForShopRoom(game.u?.ux, game.u?.uy);
    return shopkeeperInHisShop(shkp) ? shkp : null;
}

function shopkeeperDisplayName(shkp) {
    return shkp?.shknam || shkp?.shopkeeperName || 'the shopkeeper';
}

function shopkeeperSentenceName(shkp) {
    if (!heroIsHallucinating()) return shopkeeperDisplayName(shkp);
    randomHallucinatedMonsterName();
    return upstartText(randomHallucinatedShopkeeperName() || shopkeeperDisplayName(shkp));
}

function shopkeeperHello(shkp) {
    const role = game.urole?.name?.m || game._startup_role || '';
    if (role === 'Knight') return 'Salutations';
    if (role === 'Samurai') {
        const data = shkp?.data || shkp?.mdata || {};
        const name = String(data.name || shkp?.name || '').toLowerCase();
        return (data.shopkeeper || shkp?.shopkeeper || name === 'shopkeeper'
            || (shkp?.isshk && !name))
            ? 'Irasshaimase'
            : 'Konnichi wa';
    }
    if (role === 'Tourist') return 'Aloha';
    if (role === 'Valkyrie') return 'Velkommen';
    return 'Hello';
}

const SHOP_BILL_LIMIT = 200;

function shopBillEntryCount(shkp) {
    if (!shkp) return 0;
    if (Array.isArray(shkp.bill)) return shkp.bill.length;
    const billct = Math.trunc(Number(shkp.billct || 0));
    return Number.isFinite(billct) && billct > 0 ? billct : 0;
}

function shopBillIsFull(shkp) {
    return shopBillEntryCount(shkp) >= SHOP_BILL_LIMIT;
}

function shopBillObjectId(obj) {
    if (!obj) return null;
    const id = obj.o_id ?? obj.id ?? obj._shopBillObjectId;
    if (id != null) return String(id);
    const key = [
        obj.ox ?? '',
        obj.oy ?? '',
        obj.otyp ?? '',
        obj.kind || obj.actualKind || obj.singular || '',
        obj.quan ?? 1,
    ].join(':');
    obj._shopBillObjectId = key;
    return key;
}

function shopBillLedger(shkp) {
    if (!shkp) return null;
    if (!Array.isArray(shkp.bill)) shkp.bill = [];
    return shkp.bill;
}

function shopBillEntryTotal(entry) {
    const explicit = Number(entry?.totalPrice ?? entry?.unpaidPrice ?? 0);
    if (Number.isFinite(explicit) && explicit > 0) return Math.trunc(explicit);
    const price = Number(entry?.price || 0);
    const quantity = Math.max(1, Math.trunc(Number(entry?.bquan || 1)));
    if (!Number.isFinite(price) || price <= 0) return 0;
    return Math.trunc(price * quantity);
}

function shopBillTotal(shkp) {
    const ledger = Array.isArray(shkp?.bill) ? shkp.bill : [];
    return ledger.reduce((sum, entry) => sum + shopBillEntryTotal(entry), 0);
}

function shopBillEntryQuantity(entry) {
    const quantity = Math.trunc(Number(entry?.bquan || 1));
    return Number.isFinite(quantity) && quantity > 0 ? quantity : 1;
}

function shopBillEntryUnitPrice(entry) {
    const quantity = shopBillEntryQuantity(entry);
    const total = shopBillEntryTotal(entry);
    const price = Number(entry?.price || 0);
    if (Number.isFinite(price) && price > 0 && Math.trunc(price * quantity) === total)
        return Math.trunc(price);
    if (!(total > 0)) return 0;
    return Math.max(1, Math.trunc(total / quantity));
}

function shopBillEntryForObject(shkp, obj) {
    const ledger = Array.isArray(shkp?.bill) ? shkp.bill : null;
    if (!ledger) return null;
    const id = shopBillObjectId(obj);
    if (id == null) return null;
    return ledger.find(entry => String(entry.bo_id) === id) || null;
}

function shopkeeperOwningBillEntry(obj) {
    if (!obj) return { shkp: null, entry: null };
    const candidates = [
        ...(game.level?.monsters || []).filter(mon => mon?.isshk),
        ...(game.level?.rooms || []).map(room => room?.resident).filter(mon => mon?.isshk),
        ...(game.level?.subrooms || []).map(room => room?.resident).filter(mon => mon?.isshk),
    ];
    const seen = new Set();
    for (const shkp of candidates) {
        const key = shopkeeperIdentity(shkp) ?? candidates.indexOf(shkp);
        if (seen.has(key)) continue;
        seen.add(key);
        const entry = shopBillEntryForObject(shkp, obj);
        if (entry) return { shkp, entry };
    }
    return { shkp: null, entry: null };
}

function findShopObjectOwnerAt(obj, x, y) {
    const roomnos = shopRoomnosAt(x, y, SHOPBASE);
    const candidates = roomnos
        .map(roomno => shopkeeperForRoomno(roomno))
        .filter(shkp => shkp?.isshk);
    if (obj) {
        for (const shkp of candidates) {
            const entry = shopBillEntryForObject(shkp, obj);
            if (entry) return { shkp, entry, roomnos };
        }
    }
    return { shkp: candidates[0] || null, entry: null, roomnos };
}

function shopkeeperForStrictCostlySpot(x, y, obj = null) {
    const loc = game.level?.at?.(x, y);
    const roomno = loc?.roomno ?? 0;
    if (roomno < ROOMOFFSET || loc?.edge) return null;
    const roomnos = shopRoomnosAt(x, y, SHOPBASE);
    if (!roomnos.length) return null;
    const billedOwner = obj ? shopkeeperOwningBillEntry(obj).shkp : null;
    const shkp = billedOwner && roomnos.includes(billedOwner.shoproom)
        ? billedOwner
        : shopkeeperForRoomno(roomno);
    if (!shopkeeperInHisShop(shkp)) return null;
    if (shkp.shk && x === shkp.shk.x && y === shkp.shk.y) return null;
    return shkp;
}

function alterShopBillCostIfHigher(obj, amount = 0) {
    const entry = shopkeeperOwningBillEntry(obj).entry;
    if (!entry || entry.useup) return false;
    const forced = Number(amount || 0);
    const newTotal = forced
        ? Math.abs(Math.trunc(forced))
        : shopItemPrice(obj, game.u?.ux, game.u?.uy)
            || (shopBaseCost(obj) || 0) * shopPricingUnits(obj);
    if (!(newTotal > 0)) return false;
    const oldTotal = shopBillEntryTotal(entry);
    if (newTotal <= oldTotal && forced >= 0) return false;
    entry.price = newTotal;
    entry.totalPrice = newTotal;
    entry.bquan = obj.quan || entry.bquan || 1;
    obj.unpaid = true;
    obj.unpaidPrice = shopBillEntryTotal(entry);
    syncUnpaidBillLine(obj);
    return true;
}

function shopkeeperIdentity(shkp) {
    return shkp?.m_id ?? shkp?.id ?? shkp?.shknam ?? shkp?.shopkeeperName ?? null;
}

function sameShopkeeper(a, b) {
    if (!a || !b) return false;
    if (a === b) return true;
    const aId = shopkeeperIdentity(a);
    const bId = shopkeeperIdentity(b);
    return aId != null && bId != null && String(aId) === String(bId);
}

function shopkeeperPeacefulForDebt(shkp) {
    return !!shkp && shkp.mpeaceful !== 0 && !shkp.hostile;
}

function addObjectToShopBill(shkp, obj, totalPrice, { useup = false } = {}) {
    if (!shkp || !obj || !(totalPrice > 0)) return null;
    const ledger = shopBillLedger(shkp);
    if (!ledger) return null;
    const id = shopBillObjectId(obj);
    if (id == null) return null;
    let entry = ledger.find(candidate => String(candidate.bo_id) === id);
    if (!entry) {
        if (ledger.length >= SHOP_BILL_LIMIT) return null;
        entry = { bo_id: id, useup: !!useup, price: totalPrice, bquan: obj.quan || 1, totalPrice };
        ledger.push(entry);
    } else {
        entry.useup = entry.useup || !!useup;
        entry.price = totalPrice;
        entry.bquan = obj.quan || entry.bquan || 1;
        entry.totalPrice = totalPrice;
    }
    shkp.billct = ledger.length;
    obj.unpaid = true;
    obj.unpaidPrice = shopBillEntryTotal(entry);
    syncUnpaidBillLine(obj);
    return entry;
}

function removeObjectFromShopBillById(shkp, id) {
    const ledger = Array.isArray(shkp?.bill) ? shkp.bill : null;
    if (!ledger || id == null) return false;
    const before = ledger.length;
    shkp.bill = ledger.filter(entry => String(entry.bo_id) !== String(id));
    if (shkp.bill.length === before) return false;
    shkp.billct = shkp.bill.length;
    return true;
}

function clearObjectShopBillState(obj) {
    if (!obj) return;
    obj.unpaid = false;
    obj.unpaidPrice = undefined;
    obj.line = String(obj.line || '').replace(/ \(unpaid, \d+ zorkmids?\)/, '');
}

function removeObjectFromShopBill(shkp, obj) {
    const removed = removeObjectFromShopBillById(shkp, shopBillObjectId(obj));
    if (removed) clearObjectShopBillState(obj);
    return removed;
}

function splitShopBillEntry(shkp, parent, child, count = child?.quan || 1) {
    const ledger = shopBillLedger(shkp);
    if (!ledger || !parent || !child) return null;
    const parentEntry = shopBillEntryForObject(shkp, parent);
    if (!parentEntry) return null;
    const splitCount = Math.max(1, Math.trunc(Number(count || 1)));
    const parentQuantity = shopBillEntryQuantity(parentEntry);
    if (splitCount >= parentQuantity) return null;
    const unitPrice = shopBillEntryUnitPrice(parentEntry);
    if (!(unitPrice > 0)) return null;

    parentEntry.bquan = parentQuantity - splitCount;
    parentEntry.price = unitPrice;
    parentEntry.totalPrice = unitPrice * parentEntry.bquan;
    parent.unpaid = true;
    parent.unpaidPrice = parentEntry.totalPrice;
    syncUnpaidBillLine(parent);

    if (ledger.length >= SHOP_BILL_LIMIT) {
        clearObjectShopBillState(child);
        shkp.billct = ledger.length;
        return null;
    }

    const childId = shopBillObjectId(child);
    const childEntry = {
        bo_id: childId,
        useup: false,
        price: unitPrice,
        bquan: splitCount,
        totalPrice: unitPrice * splitCount,
    };
    ledger.push(childEntry);
    shkp.billct = ledger.length;
    child.unpaid = true;
    child.unpaidPrice = childEntry.totalPrice;
    syncUnpaidBillLine(child);
    return childEntry;
}

function splitCarriedObjectShopBill(parent, child, count = child?.quan || 1) {
    if (!parent || !child) return null;
    const { shkp } = shopkeeperOwningBillEntry(parent);
    if (!shkp) return null;
    return splitShopBillEntry(shkp, parent, child, count);
}

function rememberUsedUpShopBillEntry(obj, entry, price, shkp = null) {
    if (!(price > 0)) return;
    const name = usedUpShopBillName(obj);
    entry.name = name;
    entry.shopkeeperId = shopkeeperIdentity(shkp);
    game._usedUpShopBills ??= [];
    game._usedUpShopBills.push({
        name,
        price,
        bo_id: entry.bo_id,
        shopkeeperId: shopkeeperIdentity(shkp),
    });
}

function subOneFromShopBill(obj, shkp) {
    const entry = shopBillEntryForObject(shkp, obj);
    if (!entry) {
        if (obj?.unpaid) clearObjectShopBillState(obj);
        return false;
    }

    const liveQuantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    const billedQuantity = shopBillEntryQuantity(entry);
    const unitPrice = shopBillEntryUnitPrice(entry);
    clearObjectShopBillState(obj);

    if (billedQuantity > liveQuantity && unitPrice > 0) {
        const residualQuantity = billedQuantity - liveQuantity;
        entry.bo_id = String(next_ident());
        entry.useup = true;
        entry.price = unitPrice;
        entry.bquan = residualQuantity;
        entry.totalPrice = unitPrice * residualQuantity;
        shkp.billct = Array.isArray(shkp.bill) ? shkp.bill.length : Math.max(1, shkp.billct || 1);
        rememberUsedUpShopBillEntry(obj, entry, entry.totalPrice, shkp);
        return true;
    }

    removeObjectFromShopBillById(shkp, entry.bo_id);
    return true;
}

function subFromShopBill(obj, shkp) {
    const removed = subOneFromShopBill(obj, shkp);
    const contents = globContents(obj);
    for (const child of contents) {
        if (shopBillableGold(child)) continue;
        if (globContents(child).length) subFromShopBill(child, shkp);
        else subOneFromShopBill(child, shkp);
    }
    return removed;
}

function markObjectShopBillUsedUp(obj, shkp = null) {
    if (!obj || shopBillableGold(obj)) return false;
    const owner = shkp
        ? { shkp, entry: shopBillEntryForObject(shkp, obj) }
        : shopkeeperOwningBillEntry(obj);
    const billOwner = owner.shkp || shkp;
    const entry = owner.entry;
    const price = entry ? shopBillEntryTotal(entry) : 0;
    if (!entry || !(price > 0)) return false;
    entry.useup = true;
    clearObjectShopBillState(obj);
    if (!usedUpShopBillTrackerForId(entry.bo_id, billOwner))
        rememberUsedUpShopBillEntry(obj, entry, price, billOwner);
    billOwner.billct = Array.isArray(billOwner.bill) ? billOwner.bill.length : Math.max(1, billOwner.billct || 1);
    return true;
}

function returnUnpaidObjectToShopBillOwnerAt(obj, x, y) {
    if (!obj?.unpaid || shopBillableGold(obj) || globContents(obj).length) return false;
    const { shkp } = shopkeeperOwningBillEntry(obj);
    const spotShkp = shopkeeperForCostlySpot(x, y);
    if (!sameShopkeeper(shkp, spotShkp) || !shopkeeperInHisShop(shkp)) return false;
    return subOneFromShopBill(obj, shkp);
}

function shopkeeperOwningObjectOrContentsBillEntry(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return { shkp: null, entry: null };
    seen.add(obj);
    const owner = shopkeeperOwningBillEntry(obj);
    if (owner.shkp) return owner;
    for (const child of globContents(obj)) {
        const childOwner = shopkeeperOwningObjectOrContentsBillEntry(child, seen);
        if (childOwner.shkp) return childOwner;
    }
    return { shkp: null, entry: null };
}

function returnProjectileShopBillToOwnerAt(obj, x, y) {
    if (!shopObjectOrContentsUnpaid(obj) || shopBillableGold(obj)) return null;
    const { shkp } = shopkeeperOwningObjectOrContentsBillEntry(obj);
    const spotShkp = shopkeeperForCostlySpot(x, y);
    if (!sameShopkeeper(shkp, spotShkp) || !shopkeeperInHisShop(shkp)) return null;
    const containedGold = containedShopGold(obj);
    const before = Array.isArray(shkp?.bill) ? shkp.bill.length : Math.max(0, Math.trunc(Number(shkp?.billct || 0)));
    subFromShopBill(obj, shkp);
    const donation = containedGold > 0 ? donateShopGoldAtSpot(x, y, containedGold) : null;
    const after = Array.isArray(shkp?.bill) ? shkp.bill.length : Math.max(0, Math.trunc(Number(shkp?.billct || 0)));
    const returned = after < before || !shopObjectOrContentsUnpaid(obj);
    return returned ? { shkp, messages: donation?.messages || [] } : null;
}

function shopDebtObjectPronoun(obj) {
    return (obj?.quan || 1) > 1 ? 'them' : 'it';
}

function collectObjectAndContentsShopDebtItems(obj, shkp, seen = new Set()) {
    if (!obj || seen.has(obj) || shopBillableGold(obj)) return [];
    seen.add(obj);
    const items = [];
    const entry = shopBillEntryForObject(shkp, obj);
    const price = entry ? shopBillEntryTotal(entry) : 0;
    if (entry && price > 0) items.push({ item: obj, entry, price });
    for (const child of globContents(obj))
        items.push(...collectObjectAndContentsShopDebtItems(child, shkp, seen));
    return items;
}

function shopDebtContainerSuffix(obj, debtItems) {
    const topCharged = debtItems.some(item => item.item === obj);
    const chargedContents = debtItems.filter(item => item.item !== obj).length;
    if (!chargedContents) return shopDebtObjectPronoun(obj);
    const contentCount = countContentsForShopDebt(obj);
    const some = contentCount > chargedContents ? 'some of ' : '';
    return `${topCharged ? 'it and ' : ''}${some}its contents`;
}

function countContentsForShopDebt(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return 0;
    seen.add(obj);
    let count = 0;
    for (const child of globContents(obj)) {
        if (!shopBillableGold(child)) count++;
        count += countContentsForShopDebt(child, seen);
    }
    return count;
}

function convertUnpaidObjectToShopDebt(obj, { silent = false, broken = false } = {}) {
    if (!shopObjectOrContentsUnpaid(obj) || shopBillableGold(obj)) return { charged: false, value: 0, shkp: null, message: '' };
    const { shkp } = shopkeeperOwningObjectOrContentsBillEntry(obj);
    if (!shkp) return { charged: false, value: 0, shkp: null, message: '' };
    const debtItems = collectObjectAndContentsShopDebtItems(obj, shkp);
    const value = debtItems.reduce((sum, item) => sum + item.price, 0);
    for (const debtItem of debtItems) {
        if (debtItem.entry) subOneFromShopBill(debtItem.item, shkp);
        else clearObjectShopBillState(debtItem.item);
    }
    if (broken) obj.no_charge = true;
    if (!(value > 0)) return { charged: false, value: 0, shkp, message: '' };

    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    let remaining = value;
    let creditUse = false;
    if (shopkeeperPeacefulForDebt(shkp) && credit > 0) {
        const covered = Math.min(credit, remaining);
        shkp.credit = credit - covered;
        remaining -= covered;
        creditUse = covered > 0;
    }

    if (shopkeeperPeacefulForDebt(shkp) && !shkp.angry) {
        shkp.debit = Math.max(0, Math.trunc(Number(shkp.debit || 0))) + remaining;
    } else {
        shkp.robbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0))) + remaining;
    }

    let message = '';
    if (!silent) {
        if (shopkeeperPeacefulForDebt(shkp)) {
            if (creditUse && shkp.credit > 0) {
                message = `You have ${shkp.credit} zorkmid${shkp.credit === 1 ? '' : 's'} credit remaining.`;
            } else if (creditUse && !remaining) {
                message = 'You have no credit remaining.';
            } else {
                const still = creditUse ? 'still ' : '';
                message = `You ${still}owe ${shopkeeperDisplayName(shkp)} ${remaining} zorkmid${remaining === 1 ? '' : 's'} for ${shopDebtContainerSuffix(obj, debtItems)}!`;
            }
        } else {
            message = `You hear a scream, "Thief!"`;
        }
    }
    return { charged: true, value, shkp, message };
}

function resolveUnpaidProjectileShopLanding(obj, x, y, options = {}) {
    if (!shopObjectOrContentsUnpaid(obj)) return { handled: false, charged: false, returned: false, value: 0, shkp: null, message: '' };
    const returned = returnProjectileShopBillToOwnerAt(obj, x, y);
    if (returned)
        return { handled: true, charged: false, returned: true, value: 0, shkp: returned.shkp, message: (returned.messages || []).join('  ') };
    const charged = convertUnpaidObjectToShopDebt(obj, options);
    return { ...charged, handled: charged.charged, returned: false };
}

function boulderPushShopBillPrice(obj) {
    let price = 5;
    let multiplier = 1;
    let divisor = 1;
    const oid = Math.trunc(Number(obj?.o_id ?? obj?.id ?? 0));
    if ((obj?.dknown === false || !shopObjectNameKnown(obj)) && oid % 4 === 0) {
        multiplier *= 4;
        divisor *= 3;
    }
    const cha = game.u?.acurr?.a?.[A_CHA] ?? 10;
    if (cha > 18) divisor *= 2;
    else if (cha === 18) { multiplier *= 2; divisor *= 3; }
    else if (cha >= 16) { multiplier *= 3; divisor *= 4; }
    else if (cha <= 5) multiplier *= 2;
    else if (cha <= 7) { multiplier *= 3; divisor *= 2; }
    else if (cha <= 10) { multiplier *= 4; divisor *= 3; }

    price *= multiplier;
    if (divisor > 1) price = Math.trunc((Math.trunc(price * 10 / divisor) + 5) / 10);
    return Math.max(1, Math.trunc(price));
}

function boulderPushShopBillMessage(price) {
    return `The boulder will cost you ${price} zorkmid${price === 1 ? '' : 's'}.`;
}

function adjustBoulderPushShopBill(obj, fromX, fromY, toX, toY) {
    if (!obj || obj.otyp !== BOULDER) return '';
    const sourceShkp = shopkeeperForStrictCostlySpot(fromX, fromY);
    const destShkp = shopkeeperForStrictCostlySpot(toX, toY, obj);
    if (sourceShkp && !destShkp) {
        if (shopkeeperOwningBillEntry(obj).entry) return '';
        if (shopBillIsFull(sourceShkp)) return 'You got that for free!';
        const price = boulderPushShopBillPrice(obj);
        const entry = addObjectToShopBill(sourceShkp, obj, price);
        return entry ? boulderPushShopBillMessage(shopBillEntryTotal(entry)) : '';
    }
    if (!sourceShkp && destShkp && obj.unpaid && shopBillEntryForObject(destShkp, obj)) {
        subFromShopBill(obj, destShkp);
        return '';
    }
    if (obj.unpaid) {
        const ownerAtSource = findShopObjectOwnerAt(obj, fromX, fromY);
        const billOwner = ownerAtSource.shkp || shopkeeperOwningBillEntry(obj).shkp;
        if (billOwner && !shopRoomnosAt(toX, toY, SHOPBASE).includes(billOwner.shoproom)) {
            const charged = convertUnpaidObjectToShopDebt(obj, { silent: false });
            return charged.message || '';
        }
    }
    return '';
}

function isProjectileImpactContainer(obj) {
    return isTipContainerObject(obj) && globContents(obj).length && !isMagicBagObject(obj);
}

function projectileLandingIsSoft(x, y) {
    const loc = game.level?.at?.(x, y);
    if (!loc) return false;
    return IS_SOFT(loc.typ) || IS_POOL(loc.typ) || loc.typ === WATER || loc.typ === MOAT;
}

function projectileImpactGlassCandidate(obj) {
    if (!obj || impactDropObjectClass(obj) === 'gem') return false;
    const material = String(obj.material || obj.oc_material || '').toLowerCase();
    const kind = objectKindKey(obj);
    return material === 'glass' || impactDropObjectClass(obj) === 'potion'
        || obj.otyp === MIRROR || kind === 'looking glass' || kind === 'mirror'
        || kind === 'crystal ball' || kind === 'lenses'
        || /\bglass\b|\bcrystal\b/.test(kind);
}

function projectileImpactContentBreakKind(obj) {
    if (projectileImpactGlassCandidate(obj)) {
        if (objectHasArtifactIdentity(obj) || rn2(100) < 33) return '';
        return 'shatter';
    }
    if (isEggItem(obj) && !rn2(3)) return 'cracking';
    return '';
}

export function projectileTopLevelBreakKind(obj, options = {}) {
    const roll = Number.isInteger(options.breakRoll) ? options.breakRoll : rn2(100);
    const kind = impactDropBreakKind(obj);
    if (!kind) return '';
    const hasArtifact = objectHasArtifactIdentity(obj);
    const resistChance = hasArtifact ? 99 : 1;
    if (roll < resistChance) return '';
    if (hasArtifact && !breaktestExplicitSwitchObject(obj)) return '';
    return kind;
}

export function projectileTopLevelBreakMessage(obj, breakKind, messages) {
    if (!breakKind) return;
    if (breakKind === 'splat') {
        messages.push('Splat!');
        return;
    }
    if (breakKind === 'splash') {
        messages.push('Splash!');
        return;
    }
    if (breakKind === 'mess') {
        messages.push('What a mess!');
        return;
    }
    if (game.u?.blind) {
        messages.push('You hear something shatter!');
        return;
    }
    const name = pickupObjectName({ ...obj, quan: obj.quan || 1 });
    const many = (obj.quan || 1) > 1;
    const subject = many ? `The ${name}` : sentenceCase(objectArticleName(name));
    const verb = many ? 'shatter' : 'shatters';
    messages.push(`${subject} ${verb}${breakKind === 'pieces' ? ' into a thousand pieces' : ''}!`);
}

function splitImpactBrokenStackItem(container, obj, shkp) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if (quantity <= 1) return obj;
    const broken = {
        ...obj,
        id: next_ident(),
        o_id: undefined,
        _shopBillObjectId: undefined,
        quan: 1,
        contained: true,
        container,
    };
    obj.quan = quantity - 1;
    if (obj.unpaid && shkp) splitShopBillEntry(shkp, obj, broken, 1);
    if (obj.unpaid) syncUnpaidBillLine(obj);
    return broken;
}

function projectileContainerImpactDmg(obj, fromX, fromY, options = {}) {
    const messages = Array.isArray(options.messages) ? options.messages : [];
    if (!isProjectileImpactContainer(obj)) return { loss: 0, broke: false, messages };
    const shkp = shopkeeperForCostlySpot(fromX, fromY);
    const costly = shopkeeperInHisShop(shkp);
    const insider = costly && sameShopkeeper(shkp, shopkeeperForShopRoom(game.u?.ux, game.u?.uy));
    const fromInventory = options.fromInventory !== false;
    let loss = 0;
    let broke = false;
    for (const child of [...globContents(obj)]) {
        const breakKind = projectileImpactContentBreakKind(child);
        if (!breakKind) continue;
        const brokenChild = splitImpactBrokenStackItem(obj, child, shkp);
        if (costly) {
            if (fromInventory) {
                if (!brokenChild.unpaid) brokenChild.no_charge = true;
                const charged = convertUnpaidObjectToShopDebt(brokenChild, { silent: true, broken: true });
                loss += charged.value || 0;
            } else {
                const charges = lostShopMerchandiseChargesForObject({ ox: fromX, oy: fromY }, brokenChild, shkp);
                const peaceful = shopkeeperPeacefulForDebt(shkp);
                for (const [owner, value] of charges)
                    loss += chargeShopkeeperForLostMerchandise(owner, value, { peaceful });
            }
        }
        if (brokenChild === child) removeContainedObject(obj, child);
        brokenChild.contained = false;
        brokenChild.container = null;
        obj.cknown = false;
        broke = true;
        if (!options.silent) messages.push(`You hear a muffled ${breakKind}.`);
    }
    if (broke && isGlobWeightContainerObject(obj)) obj.owt = globObjectWeight(obj);
    if (costly && loss && !options.silent) {
        if (insider) {
            messages.push(`You owe ${shopkeeperDisplayName(shkp)} ${loss} ${shopCurrency(loss)} for objects destroyed.`);
        } else {
            messages.push(`You caused ${loss} ${shopCurrency(loss)} worth of damage!`);
        }
    }
    return { loss, broke, messages };
}

function placeUnstackedFloorObject(obj) {
    game.level.objects ??= [];
    game.level.objects.push(obj);
    return obj;
}

function prepareProjectileFloorObject(obj, x, y) {
    if (!obj) return obj;
    obj.contained = false;
    obj.container = null;
    obj.ox = x;
    obj.oy = y;
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    delete obj.nobj;
    delete obj.nexthere;
    return obj;
}

function stackPlacedProjectileObject(obj) {
    const objects = game.level?.objects || [];
    if (!objects.includes(obj)) return obj;
    const stack = objects.find(existing => existing !== obj && sameMonsterThrownStackObject(existing, obj));
    if (!stack) return obj;
    mergeStackedShopBillEntries(stack, obj);
    stack.quan = (stack.quan || 1) + (obj.quan || 1);
    game.level.objects = objects.filter(existing => existing !== obj);
    return stack;
}

function projectileLandingSellobjShopkeeper(x, y) {
    const heroShkp = heroShopkeeper();
    if (!shopkeeperInHisShop(heroShkp)) return null;
    const landingShkp = shopkeeperForCostlySpot(x, y);
    if (!sameShopkeeper(heroShkp, landingShkp) || !shopkeeperInHisShop(landingShkp)) return null;
    if (x === landingShkp.mx && y === landingShkp.my) return null;
    return landingShkp;
}

function autoSellProjectileLandingObject(obj, x, y, options = {}) {
    const shkp = projectileLandingSellobjShopkeeper(x, y);
    if (!shkp || !obj) return { handled: false, shkp: null, message: '', messages: [] };
    if (shopBillableGold(obj)) {
        const goldSale = sellobjDroppedGoldAt(x, y, obj.quan || 1);
        const messages = options.silent ? [] : (goldSale.messages || []);
        return { ...goldSale, handled: !!goldSale.shkp, gold: true, message: messages.join('  '), messages };
    }
    if (shopObjectOrContentsUnpaid(obj)) return { handled: false, shkp: null, message: '', messages: [] };
    const sale = shopDroppedPaidObjectSaleInfo(obj, x, y);
    if (!sale?.handled) return { handled: false, shkp: null, message: '', messages: [] };
    if (!sale.prompt) {
        const messages = options.silent || !sale.message ? [] : [sale.message];
        return { ...sale, sold: false, message: messages.join('  '), messages };
    }
    if (sale.containerSale) {
        markAcceptedShopContainerSaleState(obj, sale.shkp);
        subFromShopBill(obj, sale.shkp);
    }
    const paymentMessage = shopSalePaymentMessage(sale);
    const message = options.silent ? '' : paymentMessage;
    return { ...sale, sold: true, prompt: false, message, messages: message ? [message] : [] };
}

function projectileShipObjectResult(overrides = {}) {
    return {
        handled: false,
        broke: false,
        breakKind: '',
        noDrop: false,
        target: null,
        where: MIGR_RANDOM,
        gateText: '',
        debt: null,
        impact: emptyImpactDropResult(),
        ...overrides,
    };
}

function downGateAt(x, y) {
    const fromLevel = currentMigrationSourceLevel();
    for (let stair = game.stairs; stair; stair = stair.next) {
        if (stair.sx !== x || stair.sy !== y || stair.up) continue;
        const targetLevel = stair.tolev ? { ...stair.tolev } : null;
        if (!targetLevel) return null;
        const isLadder = !!stair.isladder;
        return {
            where: isLadder ? MIGR_LADDER_UP
                : targetLevel.dnum === fromLevel.dnum ? MIGR_STAIRS_UP : MIGR_SSTAIRS,
            targetLevel,
            fromLevel,
            gateText: isLadder ? 'down the ladder' : 'down the stairs',
            isLadder,
            stair,
        };
    }
    const trap = boulderFillTrapAt(x, y);
    if (!trap?.tseen || (trap.ttyp !== HOLE && trap.ttyp !== TRAPDOOR)) return null;
    if (!canFallThroughLevel(game.u?.uz)) return null;
    const targetLevel = sitFallTargetLevel(trap);
    if (!targetLevel) return null;
    return {
        where: MIGR_RANDOM,
        targetLevel,
        fromLevel,
        gateText: impactDropGateText(trap),
        isLadder: false,
        trap,
    };
}

function remoteProjectileDownGateAt(obj, x, y, { allowGold = false } = {}) {
    if (!obj || (!allowGold && shopBillableGold(obj))) return null;
    if (obj === game.u?.uball || obj === game.u?.uchain) return null;
    if (game.u?.ux === x && game.u?.uy === y) return null;
    const mon = (game.level?.monsters || []).find(candidate =>
        candidate.mx === x && candidate.my === y && !candidate.dead
        && (candidate.mhp == null || candidate.mhp > 0));
    if (mon) return null;
    return downGateAt(x, y);
}

function kickFloorObjectAt(x, y) {
    return (game.level?.objects || []).find(obj =>
        obj && !obj.hidden && !obj.buried && !obj.transientProjectile
        && obj.ox === x && obj.oy === y);
}

function kickFloorObjectSupported(obj, x, y) {
    if (!obj || obj === game.u?.uball || obj === game.u?.uchain) return false;
    if (isBoulderObject(obj) || shopBillableGold(obj)) return false;
    const quantity = Math.max(1, Math.trunc(Number(obj.quan || 1)));
    const fragileBreakKind = kickedFragilePreflightBreakKind(obj);
    if (quantity !== 1 && !fragileBreakKind) return false;
    if (isTipContainerObject(obj) || globContents(obj).length) return false;
    if ((shopkeeperForCostlySpot(x, y) || shopObjectOrContentsUnpaid(obj)) && !fragileBreakKind)
        return false;
    if (impactDropBreakKind(obj) && !fragileBreakKind) return false;
    return true;
}

function kickedFragilePreflightBreakKind(obj) {
    if (!obj) return '';
    const kind = objectKindKey(obj);
    if (isMirrorObject(obj)) return impactDropBreakKind(obj);
    if (obj.otyp === CRYSTAL_BALL || kind === 'crystal ball') return impactDropBreakKind(obj);
    if (kind === 'lenses') return impactDropBreakKind(obj);
    if (isGlassMaterialWandObject(obj)) return impactDropBreakKind(obj);
    if (isCreamPieObject(obj)) return impactDropBreakKind(obj);
    if (isMelonObject(obj)) return impactDropBreakKind(obj);
    if (isBreaktestVenomObject(obj)) return impactDropBreakKind(obj);
    if (isPotionObject(obj)) return impactDropBreakKind(obj);
    if (isExpensiveCameraObject(obj)) return impactDropBreakKind(obj);
    if (isEggItem(obj)) return impactDropBreakKind(obj);
    if (isHeroThrownCrackableArmorObject(obj)) return 'crackableArmor';
    if (breaktestGlassMaterialObject(obj)) return impactDropBreakKind(obj);
    return '';
}

function kickFloorObjectRange(obj, x, y, dir) {
    const stats = game.u?.acurr?.a || [];
    const strength = Math.max(0, Math.trunc(Number(stats[A_STR] ?? 10)));
    const weight = globObjectWeight({ ...obj, quan: 1 });
    let range = Math.trunc(strength / 2) - Math.trunc(weight / 40);
    if (heroUsesMartialKickRangeBonus()) range += rnd(3);

    const loc = game.level?.at?.(x, y);
    if (loc && IS_POOL(loc.typ)) {
        range = Math.trunc(range / 3) + 1;
    } else if (Is_airlevel(game.u?.uz) || Is_waterlevel(game.u?.uz)) {
        range += rnd(3);
    } else {
        if (loc?.typ === ICE) range += rnd(3);
        if (obj.greased) range += rnd(3);
    }

    const nextX = x + dir.dx;
    const nextY = y + dir.dy;
    const nextLoc = game.level?.at?.(nextX, nextY);
    const closedDoor = nextLoc?.typ === DOOR && (nextLoc.doormask & (D_CLOSED | D_LOCKED));
    if (!isok(nextX, nextY) || !nextLoc || !ZAP_POS(nextLoc.typ) || closedDoor) range = 1;
    return range;
}

function heroUsesMartialKickRangeBonus() {
    const roleName = game.urole?.name?.m || game._startup_role || '';
    return roleName === 'Monk' || roleName === 'Samurai';
}

function lowRangeKickedObjectAvoidsOuch() {
    return !rn2(3) || heroUsesMartialKickRangeBonus();
}

function applyKickedObjectOuchDamage() {
    const stats = game.u?.acurr?.a || [];
    rn2(2);
    rn2(2);
    if (!rn2(3)) {
        const woundDuration = 5 + rnd(5);
        if (!game.u._woundedLegTurns && !game.u._woundedDexPenalty) {
            game.u.acurr.a[A_DEX] = Math.max(3, (game.u.acurr.a[A_DEX] || 9) - 1);
            game.u._woundedDexPenalty = 1;
        }
        game.u._woundedLegTurns = Math.max(game.u._woundedLegTurns || 0, woundDuration);
        game.u._woundedLegSide = 'right';
    }
    const damage = rnd((stats[A_CON] ?? 10) > 15 ? 3 : 5);
    game.u.uhp = Math.max(0, (game.u?.uhp || 0) - damage);
}

function placeKickedFloorObject(obj, x, y, messages, options = {}) {
    obj.ox = x;
    obj.oy = y;
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    if (options.ohit)
        applyMonsterThrownPassiveObject(obj, options.passiveTarget, true, messages);
    if (earthFloorEffects(obj, x, y, messages, 'fall', { usedUpShopBillOnDestroy: true }))
        return null;
    const placed = placeUnstackedFloorObject(obj);
    const stacked = stackDroppedFloorObject(placed);
    newsym(x, y);
    return stacked;
}

function splitKickedFloorObjectForFlight(obj) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if (!obj || quantity <= 1) return obj;
    const kicked = {
        ...obj,
        id: next_ident(),
        o_id: undefined,
        _shopBillObjectId: undefined,
        quan: 1,
    };
    obj.quan = quantity - 1;
    return kicked;
}

async function breakKickedFragileFloorObject(obj, x, y, messages) {
    const preflightBreakKind = kickedFragilePreflightBreakKind(obj);
    if (!preflightBreakKind) return false;
    if (preflightBreakKind === 'crackableArmor') {
        const impact = crackableArmorImpact(obj, messages);
        if (!impact.destroyed) return false;
        removeFloorObject(obj);
        newsym(x, y);
        return true;
    }
    const breakKind = projectileTopLevelBreakKind(obj);
    if (!breakKind) return false;
    projectileTopLevelBreakMessage(obj, breakKind, messages);
    if (isLitOilPotionHit(obj)) explodeBurningOilPotion(obj, x, y, messages);
    else brokenPotionBreathe(obj, x, y, messages);
    await applyHeroCausedFragileBreakSideEffects(obj, messages, x, y);
    chargeHeroBrokenShopFloorObject(obj, x, y, messages);
    removeFloorObject(obj);
    newsym(x, y);
    applyHeroBrokenEggPostRemovalSideEffects(obj, messages, x, y);
    return true;
}

function chargeHeroBrokenShopFloorObject(obj, x, y, messages) {
    if (!obj || shopBillableGold(obj)) return { charged: false, value: 0, shkp: null };
    if (shopObjectOrContentsUnpaid(obj)) {
        const charged = convertUnpaidObjectToShopDebt(obj, { silent: false, broken: true });
        if (charged.message) messages.push(charged.message);
        return charged;
    }
    if (obj.no_charge) return { charged: false, value: 0, shkp: null };
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { charged: false, value: 0, shkp: null };
    const value = lostShopMerchandiseValueForObject({ ox: x, oy: y }, obj, shkp);
    if (!(value > 0)) return { charged: false, value: 0, shkp };

    const beforeCredit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const peaceful = shopkeeperPeacefulForDebt(shkp);
    const remaining = chargeShopkeeperForLostMerchandise(shkp, value, { peaceful });
    if (peaceful) {
        const usedCredit = beforeCredit > Math.max(0, Math.trunc(Number(shkp.credit || 0)));
        if (usedCredit && shkp.credit > 0) {
            messages.push(`You have ${shkp.credit} ${shopCurrency(shkp.credit)} credit remaining.`);
        } else if (usedCredit && !remaining) {
            messages.push('You have no credit remaining.');
        } else if (remaining > 0) {
            const still = usedCredit ? 'still ' : '';
            messages.push(`You ${still}owe ${shopkeeperDisplayName(shkp)} ${remaining} ${shopCurrency(remaining)} for ${shopDebtObjectPronoun(obj)}!`);
        }
    } else if (!game.u?.blind && cansee(shkp.mx, shkp.my)) {
        messages.push(`${shopkeeperDisplayName(shkp)} booms: "${game.plname || 'Hero'}, you are a thief!"`);
    } else if (!heroIsDeaf()) {
        messages.push('You hear a scream, "Thief!"');
    }
    return { charged: true, value, shkp };
}

async function kickFloorObjectToward(dir, x, y) {
    let obj = kickFloorObjectAt(x, y);
    if (!kickFloorObjectSupported(obj, x, y)) return { handled: false };

    const landX = x + dir.dx;
    const landY = y + dir.dy;
    const targetMon = (game.level?.monsters || []).find(mon =>
        mon && !mon.dead && mon.mx === landX && mon.my === landY
        && (mon.mhp == null || mon.mhp > 0));
    const canHandleMonsterImpact = targetMon
        && ((heroThrownMonsterIsUnicorn(targetMon) && heroThrownUnicornGemKind(obj))
            || heroThrownStoneMissileHarmlessRockPasser(obj, targetMon)
            || heroThrownGemClassObject(obj)
            || heroProjectileSupportedWeaponObject(obj));
    const gate = remoteProjectileDownGateAt(obj, landX, landY);
    const fragileBreakKind = kickedFragilePreflightBreakKind(obj);
    if (!gate && !canHandleMonsterImpact && !fragileBreakKind) return { handled: false };

    const range = kickFloorObjectRange(obj, x, y, dir);
    const messages = [`You kick ${floorObjectArticleName(obj)}.`];
    if (fragileBreakKind && await breakKickedFragileFloorObject(obj, x, y, messages))
        return { handled: true, messages, moved: false, broke: true };
    if (range < 2) {
        messages.push('Thump!');
        if (!lowRangeKickedObjectAvoidsOuch()) {
            applyKickedObjectOuchDamage();
            messages.push('Ouch!  That hurts!');
        }
        return { handled: true, messages, moved: false };
    }
    if (!gate && !canHandleMonsterImpact) return { handled: true, messages, moved: false };

    obj = splitKickedFloorObjectForFlight(obj);
    let monsterImpact = { handled: false };
    if (canHandleMonsterImpact) {
        monsterImpact = heroThrownUnicornGemImpact(obj, targetMon);
        if (!monsterImpact.handled) monsterImpact = heroKickedStoneMissileRockPasserImpact(obj, targetMon);
        if (!monsterImpact.handled) monsterImpact = heroKickedGemImpact(obj, targetMon);
        if (!monsterImpact.handled) monsterImpact = heroKickedWeaponImpact(obj, targetMon);
    }
    if (monsterImpact.handled) {
        removeFloorObject(obj);
        newsym(x, y);
        messages.push(...(monsterImpact.messages || []));
        if (!monsterImpact.mulched && !monsterImpact.consumed)
            placeKickedFloorObject(obj, landX, landY, messages, { ohit: !!monsterImpact.hit, passiveTarget: targetMon });
        return { handled: true, messages, moved: true, target: targetMon, hit: !!monsterImpact.hit };
    }
    if (!gate) return { handled: false };

    removeFloorObject(obj);
    newsym(x, y);
    obj.ox = landX;
    obj.oy = landY;

    const shipped = maybeShipRemoteProjectileObject(obj, landX, landY, messages);
    if (!shipped.handled) {
        placeKickedFloorObject(obj, landX, landY, messages);
    }
    return { handled: true, messages, moved: true, shipObject: shipped };
}

function carriedDropDownGateAt(obj, x, y, { allowGold = false } = {}) {
    if (!obj || (!allowGold && shopBillableGold(obj))) return null;
    if (obj === game.u?.uball || obj === game.u?.uchain) return null;
    if (game.u?.uswallow) return null;
    return downGateAt(x, y);
}

function maybeShipCarriedDropObject(obj, x, y, messages, options = {}) {
    const gate = carriedDropDownGateAt(obj, x, y, options);
    if (!gate) return projectileShipObjectResult();
    if (isBoulderObject(obj) && gate.where === MIGR_RANDOM && gate.trap) {
        const boulderImpact = impactDropFloorObjects(x, y, gate, { targetLevel: gate.targetLevel, route: gate });
        if (boulderImpact.message) messages.push(boulderImpact.message);
        return projectileShipObjectResult({ impact: boulderImpact });
    }
    const target = gate.targetLevel;
    const gateText = impactDropGateText(gate);
    const impactQuantity = impactDropPileQuantity(impactDropCandidatePile(x, y, { missile: obj }));
    const noDrop = gate.where !== MIGR_LADDER_UP && !!rn2(3);
    if (gateText && !game.u?.blind && cansee(x, y)) {
        const subject = floorObjectSubject(obj);
        if (impactQuantity) {
            const other = impactQuantity === 1 ? 'another object' : 'other objects';
            const hit = floorEffectsObjectVerb(obj, 'hits', 'hit');
            const suffix = noDrop ? '' : ` and ${floorEffectsObjectVerb(obj, 'falls', 'fall')} ${gateText}`;
            messages.push(`${subject} ${hit} ${other}${suffix}.`);
        } else if (!noDrop) {
            messages.push(`${subject} ${floorEffectsObjectVerb(obj, 'falls', 'fall')} ${gateText}.`);
        }
    }
    if (noDrop) {
        const impact = impactQuantity
            ? impactDropFloorObjects(x, y, gate, { targetLevel: target, route: gate })
            : emptyImpactDropResult();
        if (impact.message) messages.push(impact.message);
        return projectileShipObjectResult({ noDrop: true, target, where: gate.where, gateText, impact });
    }
    const debt = shipObjectShopDebt(obj, x, y);
    if (debt.message) messages.push(debt.message);
    const breakKind = impactDropObjectBreaks(obj);
    if (breakKind) {
        messages.push(`You hear a muffled ${shipObjectMuffledBreakResult(breakKind)}.`);
        newsym(x, y);
        return projectileShipObjectResult({ handled: true, broke: true, breakKind, target, where: gate.where, gateText, debt });
    }
    queueImpactDroppedObjects(target, [obj], gate);
    const impact = impactQuantity
        ? impactDropFloorObjects(x, y, gate, { targetLevel: target, route: gate })
        : emptyImpactDropResult();
    if (impact.message) messages.push(impact.message);
    newsym(x, y);
    return projectileShipObjectResult({ handled: true, target, where: gate.where, gateText, debt, impact });
}

function maybeShipRemoteProjectileObject(obj, x, y, messages, options = {}) {
    const gate = remoteProjectileDownGateAt(obj, x, y, options);
    if (!gate) return projectileShipObjectResult();
    if (isBoulderObject(obj) && gate.where === MIGR_RANDOM && gate.trap)
        return projectileShipObjectResult();
    const target = gate.targetLevel;
    const gateText = impactDropGateText(gate);
    const impactQuantity = impactDropPileQuantity(impactDropCandidatePile(x, y, { missile: obj }));
    const noDrop = gate.where !== MIGR_LADDER_UP && !!rn2(3);
    if (gateText && !game.u?.blind && cansee(x, y)) {
        const subject = floorObjectSubject(obj);
        if (impactQuantity) {
            const other = impactQuantity === 1 ? 'another object' : 'other objects';
            const hit = floorEffectsObjectVerb(obj, 'hits', 'hit');
            const suffix = noDrop ? '' : ` and ${floorEffectsObjectVerb(obj, 'falls', 'fall')} ${gateText}`;
            messages.push(`${subject} ${hit} ${other}${suffix}.`);
        } else if (!noDrop) {
            messages.push(`${subject} ${floorEffectsObjectVerb(obj, 'falls', 'fall')} ${gateText}.`);
        }
    }
    if (noDrop) {
        const impact = impactQuantity
            ? impactDropFloorObjects(x, y, gate, { targetLevel: target, missile: obj, missileImpact: true, route: gate })
            : emptyImpactDropResult();
        if (impact.message) messages.push(impact.message);
        return projectileShipObjectResult({ noDrop: true, target, where: gate.where, gateText, impact });
    }
    const debt = shipObjectShopDebt(obj, x, y);
    if (debt.message) messages.push(debt.message);
    const breakKind = impactDropObjectBreaks(obj);
    if (breakKind) {
        messages.push(`You hear a muffled ${shipObjectMuffledBreakResult(breakKind)}.`);
        newsym(x, y);
        return projectileShipObjectResult({ handled: true, broke: true, breakKind, target, where: gate.where, gateText, debt });
    }
    queueImpactDroppedObjects(target, [obj], gate);
    const impact = impactQuantity
        ? impactDropFloorObjects(x, y, gate, { targetLevel: target, missile: obj, missileImpact: true, route: gate })
        : emptyImpactDropResult();
    if (impact.message) messages.push(impact.message);
    newsym(x, y);
    return projectileShipObjectResult({ handled: true, target, where: gate.where, gateText, debt, impact });
}

function landProjectileObjectWithShopHandling(obj, x, y, options = {}) {
    const messages = [];
    prepareProjectileFloorObject(obj, x, y);
    const passiveObj = options.ohit
        ? applyMonsterThrownPassiveObject(obj, options.passiveTarget, true, messages)
        : { handled: false, damaged: false };
    const hardLanding = !projectileLandingIsSoft(x, y);
    if (hardLanding && !options.skipTopBreak) {
        const breakKind = projectileTopLevelBreakKind(obj, options);
        if (breakKind) {
            if (!options.silent) projectileTopLevelBreakMessage(obj, breakKind, messages);
            brokenPotionBreathe(obj, x, y, messages);
            const shopLanding = convertUnpaidObjectToShopDebt(obj, { ...options, broken: true });
            if (!shopLanding.charged) obj.no_charge = true;
            if (shopLanding.message) messages.push(shopLanding.message);
            return {
                object: null,
                impact: { loss: 0, broke: false, messages },
                topBreak: { broke: true, breakKind, value: shopLanding.value || 0 },
                shipObject: projectileShipObjectResult(),
                passiveObj,
                shopLanding: { ...shopLanding, handled: shopLanding.charged, returned: false },
                shopSale: { handled: false, shkp: null, message: '', messages: [] },
                messages,
            };
        }
    }
    let shipObject = projectileShipObjectResult();
    if (shopBillableGold(obj)) {
        shipObject = maybeShipRemoteProjectileObject(obj, x, y, messages, { allowGold: true });
        if (shipObject.handled) {
            return {
                object: null,
                impact: { loss: 0, broke: false, messages },
                topBreak: { broke: false, breakKind: '', value: 0 },
                floorEffects: { consumed: false },
                shipObject,
                passiveObj,
                shopLanding: { handled: false, shkp: null, message: '', messages: [], returned: false, charged: false },
                shopSale: { handled: false, shkp: null, message: '', messages: [] },
                messages,
            };
        }
    }
    if (earthFloorEffects(obj, x, y, messages, 'fall', { usedUpShopBillOnDestroy: true })) {
        return {
            object: null,
            impact: { loss: 0, broke: false, messages },
            topBreak: { broke: false, breakKind: '', value: 0 },
            floorEffects: { consumed: true },
            shipObject,
            passiveObj,
            shopLanding: { handled: false, shkp: null, message: '', messages: [], returned: false, charged: false },
            shopSale: { handled: false, shkp: null, message: '', messages: [] },
            messages,
        };
    }
    if (!shopBillableGold(obj)) {
        shipObject = maybeShipRemoteProjectileObject(obj, x, y, messages);
    }
    if (shipObject.handled) {
        return {
            object: null,
            impact: { loss: 0, broke: false, messages },
            topBreak: { broke: false, breakKind: '', value: 0 },
            floorEffects: { consumed: false },
            shipObject,
            passiveObj,
            shopLanding: { handled: false, shkp: null, message: '', messages: [], returned: false, charged: false },
            shopSale: { handled: false, shkp: null, message: '', messages: [] },
            messages,
        };
    }
    const placed = placeUnstackedFloorObject(obj);
    const impact = !hardLanding
        ? { loss: 0, broke: false, messages }
        : projectileContainerImpactDmg(placed, options.fromX ?? game.u?.ux ?? x, options.fromY ?? game.u?.uy ?? y, { messages, silent: options.silent });
    const shopLanding = resolveUnpaidProjectileShopLanding(placed, x, y, options);
    if (shopLanding.message) messages.push(shopLanding.message);
    const shopSale = shopLanding.handled ? { handled: false, shkp: null, message: '', messages: [] }
        : autoSellProjectileLandingObject(placed, x, y, options);
    if (shopSale.message) messages.push(shopSale.message);
    const stacked = stackPlacedProjectileObject(placed);
    return { object: stacked, impact, topBreak: { broke: false, breakKind: '', value: 0 }, floorEffects: { consumed: false }, shipObject, passiveObj, shopLanding, shopSale, messages };
}

function markNoChargeRecursively(obj) {
    if (!obj || shopBillableGold(obj)) return;
    if (!obj.unpaid) obj.no_charge = true;
    for (const child of globContents(obj)) markNoChargeRecursively(child);
}

function clearNoChargeRecursively(obj, { includeSelf = true } = {}, seen = new Set()) {
    if (!obj || seen.has(obj)) return;
    seen.add(obj);
    if (includeSelf && !shopBillableGold(obj)) obj.no_charge = false;
    for (const child of globContents(obj))
        clearNoChargeRecursively(child, { includeSelf: true }, seen);
}

function containedShopGold(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return 0;
    seen.add(obj);
    let total = 0;
    for (const child of globContents(obj)) {
        if (shopBillableGold(child)) {
            total += Math.max(1, Math.trunc(Number(child.quan || 1)));
        } else {
            total += containedShopGold(child, seen);
        }
    }
    return total;
}

function addShopBillEntryOrMark(shkp, obj, totalPrice, { messages = null } = {}) {
    if (!shkp || !obj || shopBillableGold(obj) || !(totalPrice > 0)) return null;
    const entry = addObjectToShopBill(shkp, obj, totalPrice);
    if (!entry && shopBillIsFull(shkp) && Array.isArray(messages))
        messages.push('You got that for free!');
    return entry;
}

function liveUnpaidBillOwnerOrClear(shkp, obj) {
    if (!obj?.unpaid) return { shkp: null, entry: null };
    const entry = shopBillEntryForObject(shkp, obj);
    if (entry) return { shkp, entry };
    const owner = shopkeeperOwningBillEntry(obj);
    if (owner.entry) return owner;
    clearObjectShopBillState(obj);
    return { shkp: null, entry: null };
}

function addContainedObjectsToShopBill(shkp, obj, x, y, seen = new Set(), messages = []) {
    let price = 0;
    const billEntries = [];
    for (const child of globContents(obj)) {
        if (!child || seen.has(child)) continue;
        seen.add(child);
        if (shopBillableGold(child)) continue;
        const childPrice = child.no_charge ? 0 : shopItemPrice(child, x, y);
        if (childPrice > 0) price += childPrice;
        if (liveUnpaidBillOwnerOrClear(shkp, child).entry) {
            syncUnpaidBillLine(child);
        } else if (!child.no_charge) {
            if (childPrice > 0) {
                const childEntry = addShopBillEntryOrMark(shkp, child, childPrice, { messages });
                if (childEntry) {
                    billEntries.push(childEntry);
                }
            }
        }
        const nested = addContainedObjectsToShopBill(shkp, child, x, y, seen, messages);
        price += nested.price;
        billEntries.push(...nested.billEntries);
    }
    return { price, billEntries, messages };
}

function addContainerAndContentsToShopBill(container, sourceObj, pickedItem, shkp, x, y) {
    let price = 0;
    let billEntry = null;
    if (shopBillIsFull(shkp)) {
        return {
            shkp,
            price: 0,
            itemPrice: 0,
            goldCharged: 0,
            goldMessages: [],
            billEntry: null,
            billEntries: [],
            free: true,
            messages: ['You got that for free!'],
        };
    }
    if (!sourceObj.no_charge) {
        const topPrice = shopItemPrice(sourceObj, x, y);
        if (topPrice > 0) {
            billEntry = addShopBillEntryOrMark(shkp, pickedItem, topPrice);
            price += topPrice;
        }
    }
    const messages = [];
    const nested = addContainedObjectsToShopBill(shkp, sourceObj, x, y, new Set(), messages);
    price += nested.price;
    const itemPrice = price;
    const gold = containedShopGold(sourceObj);
    let goldMessages = [];
    if (gold > 0) {
        const chargedGold = costlyShopGoldAt(container, gold);
        goldMessages = costlyGoldMessages(chargedGold);
        price += gold;
    }
    clearNoChargeRecursively(sourceObj);
    if (pickedItem && pickedItem !== sourceObj) {
        pickedItem.no_charge = false;
        clearNoChargeRecursively(pickedItem, { includeSelf: false });
    }
    return {
        shkp,
        price,
        itemPrice,
        goldCharged: gold,
        goldMessages,
        billEntry,
        billEntries: [billEntry, ...nested.billEntries].filter(Boolean),
        messages,
    };
}

function sameShopBillUnitPrice(a, b) {
    const first = shopkeeperOwningBillEntry(a);
    const second = first.shkp
        ? { shkp: first.shkp, entry: shopBillEntryForObject(first.shkp, b) }
        : shopkeeperOwningBillEntry(b);
    if (!first.entry || !second.entry || !sameShopkeeper(first.shkp, second.shkp)) return false;
    const firstPrice = shopBillEntryUnitPrice(first.entry);
    const secondPrice = shopBillEntryUnitPrice(second.entry);
    return firstPrice > 0 && firstPrice === secondPrice;
}

function mergeStackedShopBillEntries(stacked, obj) {
    if (!stacked?.unpaid || !obj?.unpaid) return false;
    const { shkp, entry: stackedEntry } = shopkeeperOwningBillEntry(stacked);
    const objEntry = shkp ? shopBillEntryForObject(shkp, obj) : null;
    if (!shkp || !stackedEntry || !objEntry) return false;
    const unitPrice = shopBillEntryUnitPrice(stackedEntry);
    if (!(unitPrice > 0) || unitPrice !== shopBillEntryUnitPrice(objEntry)) return false;
    stackedEntry.bquan = shopBillEntryQuantity(stackedEntry) + shopBillEntryQuantity(objEntry);
    stackedEntry.price = unitPrice;
    stackedEntry.totalPrice = unitPrice * stackedEntry.bquan;
    removeObjectFromShopBillById(shkp, objEntry.bo_id);
    stacked.unpaid = true;
    stacked.unpaidPrice = shopBillEntryTotal(stackedEntry);
    syncUnpaidBillLine(stacked);
    clearObjectShopBillState(obj);
    return true;
}

function shopBillableGold(obj) {
    return obj?.otyp === GOLD_PIECE || obj?.cls === 'coin' || obj?.glyph === '$';
}

function shopObjectClassCode(obj) {
    if (!obj) return null;
    if (shopBillableGold(obj)) return null;
    if (obj.cls === 'weapon' || obj.otyp === WEAPON_CLASS || obj.glyph === ')') return WEAPON_CLASS;
    if (obj.cls === 'armor' || obj.otyp === ARMOR_CLASS || obj.glyph === '[') return ARMOR_CLASS;
    if (obj.cls === 'ring' || obj.otyp === RING_CLASS || obj.glyph === '=') return RING_CLASS;
    if (obj.cls === 'food' || obj.otyp === FOOD_CLASS || obj.otyp === CORPSE || obj.otyp === 'corpse' || obj.glyph === '%') return FOOD_CLASS;
    if (obj.cls === 'scroll' || obj.otyp === SCROLL_CLASS || obj.glyph === '?') return SCROLL_CLASS;
    if (obj.cls === 'potion' || obj.otyp === POTION_CLASS || obj.glyph === '!') return POTION_CLASS;
    if (obj.cls === 'wand' || obj.otyp === WAND_CLASS || obj.glyph === '/') return WAND_CLASS;
    if (obj.cls === 'spellbook' || obj.otyp === SPBOOK_CLASS || obj.otyp === SPBOOK_NO_NOVEL || obj.glyph === '+') return SPBOOK_CLASS;
    if (obj.cls === 'gem' || obj.otyp === GEM_CLASS || obj.glyph === '*') return GEM_CLASS;
    if (obj.cls === 'amulet' || obj.otyp === AMULET_CLASS || obj.glyph === '"') return AMULET_CLASS;
    if (obj.cls === 'tool' || obj.otyp === TOOL_CLASS || obj.glyph === '(' || BAG_OBJECT_TYPES.has(obj.otyp)
        || obj.otyp === ICE_BOX || obj.otyp === OIL_LAMP || obj.otyp === MAGIC_LAMP || obj.otyp === BRASS_LANTERN)
        return TOOL_CLASS;
    return null;
}

function shopVegetarianStockObject(obj) {
    if (shopObjectClassCode(obj) !== FOOD_CLASS) return false;
    const kind = objectKindKey(obj) || pickupObjectName({ ...obj, quan: 1 }).toLowerCase();
    if (obj.otyp === CORPSE || obj.otyp === 'corpse' || obj.otyp === EGG) return kind.includes('lichen');
    return !/\b(?:meat|corpse|egg|tripe)\b/.test(kind);
}

function shopSaleableObject(shkp, obj) {
    const shopIndex = Math.max(0, Math.trunc(Number(shkp?.shoptype || SHOPBASE)) - SHOPBASE);
    const shopType = SHOP_TYPES[shopIndex] || SHOP_TYPES[0];
    const objectClass = shopObjectClassCode(obj);
    if (!shopType || objectClass == null) return false;
    for (const [probability, stockType] of shopType.iprobs || []) {
        if (!probability) break;
        if (stockType === RANDOM_CLASS) return true;
        if (stockType === VEGETARIAN_CLASS && shopVegetarianStockObject(obj)) return true;
        if (stockType < 0 && obj?.otyp === -stockType) return true;
        if (stockType > 0 && stockType === objectClass) return true;
    }
    return false;
}

function shopSaleBasePrice(obj) {
    let price = shopBaseCost(obj);
    if (!price) return 0;
    if ((obj.cls === 'food' || obj.otyp === FOOD_CLASS || obj.otyp === CORPSE || obj.otyp === 'corpse') && obj.oeaten)
        return 0;
    if ((obj.cls === 'wand' || obj.otyp === WAND_CLASS) && obj.spe === -1) return 0;
    if ((obj.cls === 'armor' || obj.cls === 'weapon' || obj.glyph === '[' || obj.glyph === ')')
        && (obj.spe || 0) > 0) price += 10 * obj.spe;
    if (isCandleObject(obj) && obj.age < 20 * price) price = Math.trunc(price / 2);
    return price;
}

function shopSaleDunceOrTouristDivisor() {
    const wearingDunceCap = (game.inventory || []).some(item =>
        isWornInventoryItem(item) && objectKindKey(item) === 'dunce cap');
    if (wearingDunceCap) return 3;
    const role = game._startup_role || game.urole?.name?.m || '';
    if (role === 'Tourist' && (game.u?.ulevel || 1) < 15) return 3;
    const visibleShirt = (game.inventory || []).some(item =>
        isWornInventoryItem(item)
        && ['hawaiian shirt', 't-shirt'].includes(objectKindKey(item)))
        && !(game.inventory || []).some(item =>
            isWornInventoryItem(item)
            && item.cls === 'armor'
            && !['hawaiian shirt', 't-shirt'].includes(objectKindKey(item)));
    return visibleShirt ? 3 : 2;
}

function shopSaleOffer(obj, shkp = null) {
    const unitPrice = shopSaleBasePrice(obj);
    if (!(unitPrice > 0)) return 0;
    let price = unitPrice * shopPricingUnits(obj);
    let multiplier = 1;
    let divisor = shopSaleDunceOrTouristDivisor();
    if ((obj.dknown !== true || !shopObjectNameKnown(obj)) && shopObjectClassCode(obj) !== GEM_CLASS
        && price > 1 && !((shkp?.m_id ?? shkp?.id ?? 1) % 4)) {
        multiplier *= 3;
        divisor *= 4;
    }
    price *= multiplier;
    if (divisor > 1) price = Math.trunc((Math.trunc(price * 10 / divisor) + 5) / 10);
    return Math.max(1, Math.trunc(price));
}

function shopkeeperCash(shkp) {
    return (shkp?.minvent || []).reduce((sum, item) =>
        sum + (shopBillableGold(item) ? Math.max(0, Math.trunc(Number(item.quan || 1))) : 0), 0);
}

function removeGoldFromShopkeeper(shkp, amount) {
    let remaining = Math.max(0, Math.trunc(Number(amount || 0)));
    if (!shkp || !remaining) return 0;
    for (const item of shkp.minvent || []) {
        if (!shopBillableGold(item) || remaining <= 0) continue;
        const taken = Math.min(Math.max(0, Math.trunc(Number(item.quan || 1))), remaining);
        item.quan = Math.max(0, (item.quan || 1) - taken);
        remaining -= taken;
    }
    shkp.minvent = (shkp.minvent || []).filter(item => !shopBillableGold(item) || (item.quan || 0) > 0);
    shkp.hasGold = shopkeeperCash(shkp) > 0;
    shkp.hasInventory = !!(shkp.minvent || []).length;
    return amount - remaining;
}

function addGoldToHero(amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (!gold) return 0;
    game._goldCount = (game._goldCount || 0) + gold;
    game.inventory ??= [];
    const money = game.inventory.find(item => item.letter === '$' || item.cls === 'coin' || item.otyp === GOLD_PIECE);
    if (money) {
        money.quan = game._goldCount;
        updateMoneyLine(money);
    } else {
        game.inventory.push({ letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount });
    }
    game._pet_food_scan_inventory = game.inventory;
    return gold;
}

function removeGoldFromHero(amount) {
    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin' || item.otyp === GOLD_PIECE);
    const available = Math.max(0, Math.trunc(Number(game._goldCount || money?.quan || 0)));
    const gold = Math.min(available, Math.max(0, Math.trunc(Number(amount || 0))));
    if (!gold) return 0;
    game._goldCount = Math.max(0, available - gold);
    game._just_picked_gold = Math.max(0, (game._just_picked_gold || 0) - gold);
    if (money && game._goldCount) {
        money.quan = game._goldCount;
        updateMoneyLine(money);
    } else {
        game.inventory = (game.inventory || []).filter(item => item.letter !== '$' && item.cls !== 'coin' && item.otyp !== GOLD_PIECE);
    }
    game._pet_food_scan_inventory = game.inventory;
    return gold;
}

function shopFloorContainerShopkeeper(container) {
    if (!container || (game.inventory || []).includes(container)) return null;
    if (!(game.level?.objects || []).includes(container)) return null;
    const x = container.ox;
    const y = container.oy;
    if (x == null || y == null) return null;
    const shkp = shopkeeperForCostlySpot(x, y);
    return shopkeeperInHisShop(shkp) ? shkp : null;
}

function costlyShopGoldAt(container, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    const shkp = shopFloorContainerShopkeeper(container);
    if (!shkp || !gold) return { shkp: null, charged: 0 };
    return costlyGoldToShopkeeper(shkp, gold);
}

function costlyShopGoldAtSpot(x, y, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (x == null || y == null || !gold) return { shkp: null, charged: 0, messages: [] };
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { shkp: null, charged: 0, messages: [] };
    const charged = costlyGoldToShopkeeper(shkp, gold);
    return { ...charged, messages: costlyGoldMessages(charged) };
}

function costlyGoldToShopkeeper(shkp, gold) {
    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const coveredByCredit = Math.min(credit, gold);
    shkp.credit = credit - coveredByCredit;
    const delta = gold - coveredByCredit;
    if (delta > 0) {
        shkp.debit = Math.max(0, Math.trunc(Number(shkp.debit || 0))) + delta;
        shkp.loan = Math.max(0, Math.trunc(Number(shkp.loan || 0))) + delta;
    }
    return { shkp, charged: gold, coveredByCredit, debit: delta, creditBefore: credit };
}

function costlyGoldMessages(charged) {
    if (!charged?.charged) return [];
    const messages = [];
    const creditBefore = Math.max(0, Math.trunc(Number(charged.creditBefore || 0)));
    const coveredByCredit = Math.max(0, Math.trunc(Number(charged.coveredByCredit || 0)));
    const debit = Math.max(0, Math.trunc(Number(charged.debit || 0)));
    if (coveredByCredit > 0)
        messages.push(coveredByCredit < creditBefore
            ? `Your credit is reduced by ${coveredByCredit} ${shopCurrency(coveredByCredit)}.`
            : 'Your credit is erased.');
    if (debit > 0) {
        if (Math.max(0, Math.trunc(Number(charged.shkp?.debit || 0))) > debit)
            messages.push(`Your debt increases by ${debit} ${shopCurrency(debit)}.`);
        else
            messages.push(`You owe ${shopkeeperDisplayName(charged.shkp)} ${debit} ${shopCurrency(debit)}.`);
    }
    return messages;
}

function pickUpFloorGoldObject(goldObj, count = goldObj?.quan || 1) {
    if (!goldObj || !(goldObj.otyp === GOLD_PIECE || goldObj.cls === 'coin' || goldObj.glyph === '$'))
        return { picked: false, amount: 0, messages: [] };
    const originalAmount = Math.max(1, Math.trunc(Number(goldObj.quan || 1)));
    const amount = Math.max(1, Math.min(originalAmount, Math.trunc(Number(count || originalAmount))));
    const charged = costlyShopGoldAtSpot(goldObj.ox, goldObj.oy, amount);
    game._goldCount = (game._goldCount || 0) + amount;
    game._just_picked_gold = amount;
    const invGold = (game.inventory || []).find(obj => obj.letter === '$' || obj.cls === 'coin');
    if (invGold) {
        invGold.quan = (invGold.quan || 0) + amount;
        updateMoneyLine(invGold);
    } else {
        game.inventory = [...(game.inventory || []), { cls: 'coin', letter: '$', otyp: GOLD_PIECE, glyph: '$', quan: amount }];
    }
    if (amount < originalAmount) goldObj.quan = originalAmount - amount;
    else game.level.objects = (game.level?.objects || []).filter(obj => obj !== goldObj);
    game._pet_food_scan_inventory = game.inventory;
    const total = game._goldCount === amount ? '' : ` (${game._goldCount} in total)`;
    const pickupMessage = `$ - ${amount} gold piece${amount === 1 ? '' : 's'}${total}.`;
    return { picked: true, amount, charged, messages: [pickupMessage, ...(charged.messages || [])] };
}

function donateShopGoldAt(container, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    const shkp = shopFloorContainerShopkeeper(container);
    return sellobjGoldToShopkeeper(shkp, gold);
}

function donateShopGoldAtSpot(x, y, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (x == null || y == null) return { shkp: null, donated: 0 };
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { shkp: null, donated: 0 };
    return sellobjGoldToShopkeeper(shkp, gold);
}

function donateGoldToShopkeeper(shkp, gold) {
    if (!shkp || !gold) return { shkp: null, donated: 0 };
    const debit = Math.max(0, Math.trunc(Number(shkp.debit || 0)));
    const previousCredit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const coveredDebt = Math.min(debit, gold);
    if (coveredDebt > 0) {
        shkp.debit = debit - coveredDebt;
        const loan = Math.max(0, Math.trunc(Number(shkp.loan || 0)));
        shkp.loan = gold > debit ? 0 : Math.max(0, loan - coveredDebt);
    }
    const credit = gold - coveredDebt;
    if (credit > 0)
        shkp.credit = previousCredit + credit;
    return { shkp, donated: gold, coveredDebt, credit, debitBefore: debit, creditBefore: previousCredit };
}

const HALLUCINATED_SHOP_CURRENCIES = [
    'Altarian Dollar',
    'Ankh-Morpork Dollar',
    'auric',
    'buckazoid',
    'cirbozoid',
    'credit chit',
    'cubit',
    'Flanian Pobble Bead',
    'fretzer',
    'imperial credit',
    'Hong Kong Luna Dollar',
    'kongbuck',
    'nanite',
    'quatloo',
    'simoleon',
    'solari',
    'spacebuck',
    'sporebuck',
    'Triganic Pu',
    'woolong',
    'zorkmid',
];

function shopCurrency(amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    const currency = heroIsHallucinating()
        ? HALLUCINATED_SHOP_CURRENCIES[rn2(HALLUCINATED_SHOP_CURRENCIES.length)]
        : 'zorkmid';
    return `${currency}${gold === 1 ? '' : 's'}`;
}

function shopGoldDonationMessages(donation, { selling = true } = {}) {
    if (!donation?.donated) return [];
    const messages = [];
    const debitBefore = Math.max(0, Math.trunc(Number(donation.debitBefore || 0)));
    const credit = Math.max(0, Math.trunc(Number(donation.credit || 0)));
    if (debitBefore > 0) {
        messages.push(`Your debt is ${donation.donated < debitBefore ? 'partially ' : ''}paid off.`);
    }
    if (credit > 0) {
        const totalCredit = Math.max(0, Math.trunc(Number(donation.shkp?.credit || 0)));
        if (totalCredit === credit) {
            messages.push(`You have ${selling ? '' : 're-'}established ${credit} ${shopCurrency(credit)} credit.`);
        } else {
            messages.push(`${credit} ${shopCurrency(credit)} added${selling ? '' : ' back'} to your credit; total is now ${totalCredit} ${shopCurrency(totalCredit)}.`);
        }
    }
    return messages;
}

function sellobjGoldToShopkeeper(shkp, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (!shkp || !gold) return { shkp: shkp || null, donated: 0, messages: [] };
    if (shopkeeperAngryForSellobj(shkp))
        return { shkp, donated: 0, angry: true, messages: [`${shopkeeperDisplayName(shkp)} smirks with satisfaction.`] };
    const robbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0)));
    if (robbed > 0) {
        shkp.robbed = 0;
        return {
            shkp,
            donated: 0,
            robbedContribution: gold,
            messages: ['Thank you for your contribution to restock this recently plundered shop.'],
        };
    }
    const donation = donateGoldToShopkeeper(shkp, gold);
    return { ...donation, messages: shopGoldDonationMessages(donation, { selling: true }) };
}

function sellobjDroppedGoldAt(x, y, amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (!gold || x == null || y == null) return { shkp: null, donated: 0, messages: [] };
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { shkp: null, donated: 0, messages: [] };
    return sellobjGoldToShopkeeper(shkp, gold);
}

function saleDeclineQuestionObject(obj, sale = {}) {
    if ((sale.contentOffer || 0) > 0 || (sale.contentSaleCount || 0) > 0) return 'them';
    return (obj?.quan || 1) === 1 ? 'it' : 'them';
}

function shopSalePromptName(obj) {
    const name = pickupObjectName(obj);
    return (obj?.quan || 1) === 1 ? `${obj?.unique ? 'the' : 'your'} ${name}` : `your ${name}`;
}

function shopSaleUninterestedObject(obj) {
    return obj?.cls === 'ball' || obj?.glyph === '0' || obj?.otyp === HEAVY_IRON_BALL
        || obj?.cls === 'chain' || obj?.glyph === '_' || obj?.otyp === IRON_CHAIN
        || ((obj?.cls === 'food' || obj?.otyp === FOOD_CLASS) && obj?.oeaten)
        || (isCandleObject(obj) && obj.age < 20 * shopBaseCost(obj));
}

function shopObjectOrContentsUnpaid(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return false;
    seen.add(obj);
    if (obj.unpaid) return true;
    return globContents(obj).some(child => shopObjectOrContentsUnpaid(child, seen));
}

function shopkeeperIsIzchak(shkp) {
    return String(shkp?.shknam || shkp?.shopkeeperName || '').replace(/^[|+\-_]/, '').toLowerCase() === 'izchak';
}

function shopkeeperInTown(shkp) {
    if (!game.level?.flags?.has_town) return false;
    const townRooms = (game.level?.rooms || []).filter(room => room?.sbrooms?.length);
    if (!townRooms.length) return true;
    const x = Number(shkp?.mx ?? shkp?.x ?? NaN);
    const y = Number(shkp?.my ?? shkp?.y ?? NaN);
    return Number.isFinite(x) && Number.isFinite(y) && townRooms.some(room =>
        x >= room.lx && x <= room.hx && y >= room.ly && y <= room.hy);
}

function shopkeeperPossessivePronoun(shkp) {
    if (shkp?.female) return 'her';
    if (shkp?.neuter || shkp?.data?.neuter) return 'its';
    return 'his';
}

function shopSpecialStockMessage(obj, shkp) {
    if (shkp?.shoptype !== CANDLESHOP || !isCandelabrumOfInvocationItem(obj)) return '';
    const name = shopkeeperDisplayName(shkp);
    const candles = Math.max(0, Math.trunc(Number(obj?.spe || 0)));
    const invoked = !!game.u?.uevent?.invoked;
    const canTalk = !heroIsDeaf() && !shkp?.mute && !shkp?.silent && !shkp?.helpless;
    if (shopkeeperIsIzchak(shkp) && !invoked) {
        if (!canTalk)
            return `${name} seems ${candles < 7 ? 'horrified' : 'concerned'} that you want to sell that.`;
        const messages = [`${name} says: "No thanks, I'd hang onto that if I were you."`];
        if (candles < 7) {
            const needed = 7 - candles;
            messages.push(`${name} says: "You'll need ${needed}${candles > 0 ? ' more' : ''} candle${needed === 1 ? '' : 's'} to go along with it."`);
        }
        return messages.join('  ');
    }
    if (canTalk) return `${name} says: "I won't stock that.  Take it out of here!"`;
    return `${name} shakes ${shopkeeperPossessivePronoun(shkp)} head in refusal.`;
}

function shopSellobjUninterestedMessage(shkp, containedGold = 0) {
    return `${shopkeeperDisplayName(shkp)} seems uninterested${containedGold > 0 ? ' in the rest' : ''}.`;
}

function shopSellobjNoPromptResult(obj, shkp, kind, message, extra = {}) {
    return { kind, obj, shkp, handled: true, prompt: false, message, ...extra };
}

function ownedShopSaleOffer(shkp, obj) {
    if (!obj || obj.unpaid || obj.no_charge || shopBillableGold(obj) || shopSaleUninterestedObject(obj)) return 0;
    return shopSaleableObject(shkp, obj) ? shopSaleOffer(obj, shkp) : 0;
}

function containedShopSaleOffer(shkp, obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return { offer: 0, count: 0 };
    seen.add(obj);
    let offer = 0;
    let count = 0;
    for (const child of globContents(obj)) {
        if (shopBillableGold(child)) continue;
        const childOffer = ownedShopSaleOffer(shkp, child);
        if (childOffer > 0) {
            offer += childOffer;
            count += 1;
        }
        const contained = containedShopSaleOffer(shkp, child, seen);
        offer += contained.offer;
        count += contained.count;
    }
    return { offer, count };
}

function markAcceptedShopContainerSaleState(obj, shkp, seen = new Set()) {
    if (!obj || seen.has(obj) || shopBillableGold(obj)) return;
    seen.add(obj);
    if (!obj.unpaid && ownedShopSaleOffer(shkp, obj) <= 0) obj.no_charge = true;
    for (const child of globContents(obj))
        markAcceptedShopContainerSaleState(child, shkp, seen);
}

function shopSalePromptObjectName(obj, sale = {}) {
    const contentsSold = (sale.contentOffer || 0) > 0 || (sale.contentSaleCount || 0) > 0;
    return contentsSold ? `${shopSalePromptName(obj)} and its contents` : shopSalePromptName(obj);
}

function shopkeeperAngryForSellobj(shkp) {
    return !!shkp && (shkp.angry || shkp.hostile || shkp.mpeaceful === 0);
}

function shopSellobjAngryResult(obj, shkp, kind = 'drop') {
    if (globContents(obj).length) subFromShopBill(obj, shkp);
    return {
        kind,
        obj,
        shkp,
        handled: true,
        prompt: false,
        angry: true,
        message: `${shopkeeperDisplayName(shkp)} smirks with satisfaction.`,
    };
}

function robbedShopSellobjResult(obj, shkp, offer, containedGold = 0, kind = 'drop') {
    const robbed = Math.max(0, Math.trunc(Number(shkp?.robbed || 0)));
    if (!robbed) return null;
    const contribution = Math.max(0, Math.trunc(Number(offer || 0)))
        + (Math.max(0, Math.trunc(Number(containedGold || 0))) > 0 ? 1 : 0);
    shkp.robbed = 0;
    subFromShopBill(obj, shkp);
    return {
        kind,
        obj,
        shkp,
        handled: true,
        prompt: false,
        robbed: true,
        robbedContribution: contribution,
        message: contribution > 0
            ? 'Thank you for your contribution to restock this recently plundered shop.'
            : '',
    };
}

function shopDroppedPaidObjectSaleInfo(obj, x, y) {
    const containerSale = globContents(obj).length > 0;
    if (!obj || shopBillableGold(obj) || (!containerSale && obj.unpaid)) return null;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return null;
    if (shopkeeperAngryForSellobj(shkp)) return shopSellobjAngryResult(obj, shkp, 'drop');
    const contentSale = containerSale ? containedShopSaleOffer(shkp, obj) : { offer: 0, count: 0 };
    const topOffer = ownedShopSaleOffer(shkp, obj);
    const containedGold = containerSale ? containedShopGold(obj) : 0;
    const offer = topOffer + contentSale.offer;
    const noSale = !(offer > 0);
    if (!containedGold && noSale) {
        const unpaid = shopObjectOrContentsUnpaid(obj);
        if (containerSale) {
            markNoChargeRecursively(obj);
            subFromShopBill(obj, shkp);
        } else {
            obj.no_charge = true;
        }
        const message = unpaid ? '' : shopSpecialStockMessage(obj, shkp) || shopSellobjUninterestedMessage(shkp);
        return shopSellobjNoPromptResult(obj, shkp, 'drop', message);
    }
    const robbed = robbedShopSellobjResult(obj, shkp, offer, containedGold, 'drop');
    if (robbed) return robbed;
    if (containedGold > 0) donateShopGoldAtSpot(x, y, containedGold);
    if (containedGold > 0 && noSale) {
        if (containerSale) {
            markNoChargeRecursively(obj);
            subFromShopBill(obj, shkp);
        }
        return { obj, shkp, handled: true, prompt: false, message: '' };
    }
    const uninterested = noSale
        || (Array.isArray(shkp.bill) && shkp.bill.length >= SHOP_BILL_LIMIT)
        || shopSaleUninterestedObject(obj);
    if (uninterested) {
        if (containerSale) {
            markNoChargeRecursively(obj);
            subFromShopBill(obj, shkp);
        } else {
            obj.no_charge = true;
        }
        return shopSellobjNoPromptResult(obj, shkp, 'drop', shopSellobjUninterestedMessage(shkp, containedGold));
    }
    const sale = {
        kind: 'drop',
        obj,
        shkp,
        handled: true,
        prompt: true,
        offer,
        topOffer,
        contentOffer: contentSale.offer,
        contentSaleCount: contentSale.count,
        containerSale,
    };
    const cash = shopkeeperCash(shkp);
    const saleName = shopSalePromptObjectName(obj, sale);
    if (!cash) {
        const creditOffer = Math.trunc((offer * 9) / 10) + (offer <= 1 ? 1 : 0);
        return {
            ...sale,
            credit: true,
            offer: creditOffer,
            promptMessage: `${shopkeeperDisplayName(shkp)} cannot pay you at present.  Will you accept ${creditOffer} zorkmid${creditOffer === 1 ? '' : 's'} in credit for ${saleName}? [ynaq] (n)`,
        };
    }
    const cashOffer = Math.min(offer, cash);
    const shortFunds = cashOffer < offer;
    return {
        ...sale,
        credit: false,
        offer: cashOffer,
        promptMessage: `${shopkeeperDisplayName(shkp)} offers${shortFunds ? ' only' : ''} ${cashOffer} gold piece${cashOffer === 1 ? '' : 's'} for ${saleName}.  Sell ${saleDeclineQuestionObject(obj, sale)}? [ynaq] (n)`,
    };
}

function beginDroppedPaidObjectSale(obj, x, y, declineMessage = '') {
    const sale = shopDroppedPaidObjectSaleInfo(obj, x, y);
    if (!sale?.prompt) return sale;
    game._shop_sale_pending = { ...sale, declineMessage };
    game._command_mode = 'shopSaleConfirm';
    return sale;
}

function shopFloorContainerPutSaleInfo(container, putItem) {
    const x = container?.ox ?? game.u?.ux;
    const y = container?.oy ?? game.u?.uy;
    if (!container || !putItem || (game.inventory || []).includes(container) || x == null || y == null)
        return null;
    if (shopBillableGold(putItem) || putItem.unpaid || putItem.no_charge || container.no_charge) return null;
    const shkp = shopFloorContainerShopkeeper(container);
    if (!shopkeeperInHisShop(shkp)) return null;
    if (shopkeeperAngryForSellobj(shkp)) return {
        ...shopSellobjAngryResult(putItem, shkp, 'containerPutIn'),
        container,
    };
    const contentSale = containedShopSaleOffer(shkp, putItem);
    const topOffer = ownedShopSaleOffer(shkp, putItem);
    const containedGold = containedShopGold(putItem);
    const offer = topOffer + contentSale.offer;
    const noSale = !(offer > 0);
    if (noSale && !containedGold) {
        const message = shopObjectOrContentsUnpaid(putItem)
            ? ''
            : shopSpecialStockMessage(putItem, shkp) || shopSellobjUninterestedMessage(shkp);
        return shopSellobjNoPromptResult(putItem, shkp, 'containerPutIn', message, { container });
    }
    const robbed = robbedShopSellobjResult(putItem, shkp, offer, containedGold, 'containerPutIn');
    if (robbed) return { ...robbed, container };
    if (noSale) return null;
    if ((Array.isArray(shkp.bill) && shkp.bill.length >= SHOP_BILL_LIMIT) || shopSaleUninterestedObject(putItem))
        return shopSellobjNoPromptResult(putItem, shkp, 'containerPutIn', shopSellobjUninterestedMessage(shkp, containedGold), { container });
    const sale = {
        kind: 'containerPutIn',
        obj: putItem,
        container,
        shkp,
        handled: true,
        prompt: true,
        offer,
        topOffer,
        contentOffer: contentSale.offer,
        contentSaleCount: contentSale.count,
        containerSale: globContents(putItem).length > 0,
    };
    const cash = shopkeeperCash(shkp);
    const saleName = shopSalePromptObjectName(putItem, sale);
    if (!cash) {
        const creditOffer = Math.trunc((offer * 9) / 10) + (offer <= 1 ? 1 : 0);
        return {
            ...sale,
            credit: true,
            offer: creditOffer,
            promptMessage: `${shopkeeperDisplayName(shkp)} cannot pay you at present.  Will you accept ${creditOffer} zorkmid${creditOffer === 1 ? '' : 's'} in credit for ${saleName}? [ynaq] (n)`,
        };
    }
    const cashOffer = Math.min(offer, cash);
    const shortFunds = cashOffer < offer;
    return {
        ...sale,
        credit: false,
        offer: cashOffer,
        promptMessage: `${shopkeeperDisplayName(shkp)} offers${shortFunds ? ' only' : ''} ${cashOffer} gold piece${cashOffer === 1 ? '' : 's'} for ${saleName}.  Sell ${saleDeclineQuestionObject(putItem, sale)}? [ynaq] (n)`,
    };
}

function beginShopFloorContainerPutSale(container, item, amount = item?.quan || 1) {
    if (!container) return null;
    if (item?.letter === '$' || item?.otyp === GOLD_PIECE || item?.cls === 'coin' || item?.glyph === '$') return null;
    if (item?.unpaid) return null;
    const reject = containerPutRejectMessage(container, item);
    if (reject) return null;
    const count = Math.min(Math.max(1, amount || 1), item.quan || 1);
    const putItem = splitInventoryObjectForContainerPut(item, count);
    const sale = shopFloorContainerPutSaleInfo(container, putItem);
    if (!sale?.prompt) return sale;
    return {
        ...sale,
        item,
        putItem,
        amount: count,
        itemName: inventoryItemName(item),
    };
}

function shopSalePaymentMessage(pending, verb = 'relinquish') {
    const offer = Math.max(0, Math.trunc(Number(pending.offer || 0)));
    if (pending.credit) {
        pending.shkp.credit = Math.max(0, Math.trunc(Number(pending.shkp.credit || 0))) + offer;
        return `You ${verb} ${pickupObjectPhrase(pending.obj)} and acquire ${offer} zorkmid${offer === 1 ? '' : 's'} in credit.`;
    }
    const paid = removeGoldFromShopkeeper(pending.shkp, offer);
    addGoldToHero(paid);
    return `You ${verb} ${pickupObjectPhrase(pending.obj)} and receive ${paid} gold piece${paid === 1 ? '' : 's'} in compensation.`;
}

function finishDroppedObjectSale(pending, accept) {
    if (!pending?.obj || !pending.shkp) return '';
    const obj = pending.obj;
    let message;
    if (!accept) {
        if (pending.containerSale) {
            markNoChargeRecursively(obj);
            subFromShopBill(obj, pending.shkp);
        } else {
            obj.no_charge = true;
        }
        message = pending.declineMessage || `You drop ${pickupObjectPhrase(obj)}.`;
    } else {
        if (pending.containerSale) {
            markAcceptedShopContainerSaleState(obj, pending.shkp);
            subFromShopBill(obj, pending.shkp);
        }
        message = shopSalePaymentMessage(pending);
    }
    stackDroppedFloorObject(obj);
    return message;
}

function addPickedObjectToShopBill(source, pickedItem) {
    const x = source?.ox ?? game.u?.ux;
    const y = source?.oy ?? game.u?.uy;
    if (x == null || y == null || shopBillableGold(source)) return { shkp: null, price: 0, billEntry: null };
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { shkp: null, price: 0, billEntry: null };
    if (globContents(source).length) {
        if (pickedItem && pickedItem !== source) normalizeContainedObjectParents(pickedItem);
        return addContainerAndContentsToShopBill(source, source, pickedItem || source, shkp, x, y);
    }
    if (source.no_charge) {
        source.no_charge = false;
        if (pickedItem) pickedItem.no_charge = false;
        return { shkp, price: 0, billEntry: null };
    }
    const price = shopItemPrice(source, x, y);
    if (!(price > 0)) return { shkp, price: 0, billEntry: null };
    const billEntry = addObjectToShopBill(shkp, pickedItem, price);
    if (!billEntry) return { shkp, price: 0, billEntry: null, free: true, messages: ['You got that for free!'] };
    return { shkp, price, billEntry };
}

function addContainerTakeoutObjectToShopBill(container, sourceObj, pickedItem = sourceObj) {
    const x = container?.ox;
    const y = container?.oy;
    if (!container || !sourceObj || !pickedItem) return { shkp: null, price: 0, billEntry: null };
    const floorShkp = shopFloorContainerShopkeeper(container);
    if (!floorShkp) return { shkp: null, price: 0, billEntry: null };
    if (shopBillableGold(sourceObj)) {
        const gold = Math.max(1, Math.trunc(Number(sourceObj.quan || 1)));
        costlyShopGoldAt(container, gold);
        return { shkp: floorShkp, price: gold, billEntry: null };
    }
    const sourceOwner = sourceObj === pickedItem ? { shkp: null, entry: null } : liveUnpaidBillOwnerOrClear(floorShkp, sourceObj);
    if (sourceOwner.entry) {
        const splitCount = Math.max(1, Math.trunc(Number(pickedItem.quan || 1)));
        if (splitCount < shopBillEntryQuantity(sourceOwner.entry)) {
            const billEntry = splitShopBillEntry(sourceOwner.shkp, sourceObj, pickedItem, splitCount);
            if (!billEntry) return { shkp: sourceOwner.shkp, price: 0, billEntry: null };
            syncUnpaidBillLine(sourceObj);
            return { shkp: sourceOwner.shkp, price: 0, billEntry };
        }
        pickedItem.unpaid = true;
        pickedItem.unpaidPrice = pickedItem.unpaidPrice || sourceObj.unpaidPrice || shopBillEntryTotal(sourceOwner.entry);
        syncUnpaidBillLine(pickedItem);
        return { shkp: sourceOwner.shkp, price: 0, billEntry: sourceOwner.entry };
    }
    const pickedOwner = liveUnpaidBillOwnerOrClear(floorShkp, pickedItem);
    if (pickedOwner.entry) {
        syncUnpaidBillLine(pickedItem);
        return { shkp: pickedOwner.shkp, price: 0, billEntry: pickedOwner.entry };
    }
    if (globContents(sourceObj).length)
        return addContainerAndContentsToShopBill(container, sourceObj, pickedItem, floorShkp, x, y);
    if (x == null || y == null || shopBillableGold(sourceObj))
        return { shkp: null, price: 0, billEntry: null };
    const shkp = floorShkp;
    if (sourceObj.no_charge && !globContents(sourceObj).length) {
        sourceObj.no_charge = false;
        pickedItem.no_charge = false;
        return { shkp, price: 0, billEntry: null };
    }
    const price = shopItemPrice(pickedItem, x, y);
    if (!(price > 0)) return { shkp, price: 0, billEntry: null };
    const billEntry = addObjectToShopBill(shkp, pickedItem, price);
    if (!billEntry) return { shkp, price: 0, billEntry: null, free: true, messages: ['You got that for free!'] };
    return { shkp, price, billEntry };
}

function addTippedContainerObjectToShopBill(container, obj) {
    return addContainerTakeoutObjectToShopBill(container, obj, obj);
}

function lostShopMerchandiseValueForObject(source, obj, shkp, seen = new Set(), options = {}) {
    if (!source || !obj || !shkp || seen.has(obj)) return 0;
    seen.add(obj);
    if (shopBillableGold(obj))
        return (options.topLevel !== false || options.includeContainedGold !== false)
            ? Math.max(1, Math.trunc(Number(obj.quan || 1)))
            : 0;

    let value = 0;
    const inventoryLikeNestedContent = options.inventoryLikeContents && options.topLevel === false;
    const entry = shopBillEntryForObject(shkp, obj);
    if (entry) {
        value += shopBillEntryTotal(entry);
        subOneFromShopBill(obj, shkp);
    } else if (!obj.no_charge && (!inventoryLikeNestedContent || obj.unpaid)) {
        value += shopItemPrice(obj, source.ox ?? game.u?.ux, source.oy ?? game.u?.uy);
    }

    for (const child of globContents(obj))
        value += lostShopMerchandiseValueForObject(source, child, shkp, seen, {
            ...options,
            topLevel: false,
        });
    return value;
}

function chargeShopkeeperForLostMerchandise(shkp, value, { peaceful = shopkeeperPeacefulForDebt(shkp) } = {}) {
    let remaining = Math.max(0, Math.trunc(Number(value || 0)));
    if (!shkp || !remaining) return 0;
    if (peaceful) {
        const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
        const covered = Math.min(credit, remaining);
        shkp.credit = credit - covered;
        remaining -= covered;
    }
    if (remaining > 0) {
        if (peaceful && !shkp.angry) {
            shkp.debit = Math.max(0, Math.trunc(Number(shkp.debit || 0))) + remaining;
        } else {
            shkp.robbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0))) + remaining;
        }
    }
    return remaining;
}

function heroInShopOwnedBy(shkp) {
    return sameShopkeeper(shopkeeperForCostlySpot(game.u?.ux, game.u?.uy), shkp);
}

function shopDebtDeltas(shkp, beforeDebit, beforeRobbed) {
    return {
        debitDelta: Math.max(0, Math.trunc(Number(shkp?.debit || 0)) - beforeDebit),
        robbedDelta: Math.max(0, Math.trunc(Number(shkp?.robbed || 0)) - beforeRobbed),
    };
}

function shipObjectShopDebt(obj, x, y, { shopFloorObj = false, silent = false } = {}) {
    if (!obj || shopBillableGold(obj)) return { charged: false, value: 0, shkp: null, message: '', debitDelta: 0, robbedDelta: 0 };

    if (!shopFloorObj && shopObjectOrContentsUnpaid(obj)) {
        const { shkp } = shopkeeperOwningObjectOrContentsBillEntry(obj);
        const beforeDebit = Math.max(0, Math.trunc(Number(shkp?.debit || 0)));
        const beforeRobbed = Math.max(0, Math.trunc(Number(shkp?.robbed || 0)));
        const charged = convertUnpaidObjectToShopDebt(obj, { silent });
        if (charged.charged || shopObjectOrContentsUnpaid(obj)) clearNoChargeRecursively(obj);
        return { ...charged, ...shopDebtDeltas(charged.shkp || shkp, beforeDebit, beforeRobbed) };
    }

    if (!shopFloorObj) return { charged: false, value: 0, shkp: null, message: '', debitDelta: 0, robbedDelta: 0 };

    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return { charged: false, value: 0, shkp: null, message: '', debitDelta: 0, robbedDelta: 0 };
    const charges = lostShopMerchandiseChargesForObject({ ox: x, oy: y }, obj, shkp);
    const beforeDebt = new Map();
    const trackShopkeeperDebt = target => {
        if (!target || beforeDebt.has(target)) return;
        beforeDebt.set(target, {
            debit: Math.max(0, Math.trunc(Number(target.debit || 0))),
            robbed: Math.max(0, Math.trunc(Number(target.robbed || 0))),
        });
    };
    trackShopkeeperDebt(shkp);
    for (const chargedShkp of charges.keys()) trackShopkeeperDebt(chargedShkp);
    clearNoChargeRecursively(obj);
    const peaceful = heroInShopOwnedBy(shkp);
    let value = 0;
    let remaining = 0;
    for (const [chargedShkp, charge] of charges) {
        value += Math.max(0, Math.trunc(Number(charge || 0)));
        remaining += chargeShopkeeperForLostMerchandise(chargedShkp, charge, { peaceful });
    }
    let debitDelta = 0;
    let robbedDelta = 0;
    let messageShkp = null;
    for (const [chargedShkp, before] of beforeDebt) {
        const deltas = shopDebtDeltas(chargedShkp, before.debit, before.robbed);
        debitDelta += deltas.debitDelta;
        robbedDelta += deltas.robbedDelta;
        if (!messageShkp && (deltas.debitDelta > 0 || deltas.robbedDelta > 0))
            messageShkp = chargedShkp;
    }
    messageShkp ||= charges.keys().next().value || shkp;
    let message = '';
    if (!silent && remaining > 0) {
        if (robbedDelta > 0)
            message = `You removed ${robbedDelta} ${shopCurrency(robbedDelta)} worth of goods!`;
        else if (debitDelta > 0)
            message = `You owe ${shopkeeperDisplayName(messageShkp)} ${debitDelta} ${shopCurrency(debitDelta)} for goods lost.`;
    }
    return { charged: value > 0, value, shkp: messageShkp, message, debitDelta, robbedDelta };
}

function addLostShopMerchandiseCharge(charges, shkp, value) {
    const amount = Math.max(0, Math.trunc(Number(value || 0)));
    if (!shkp || !amount) return;
    charges.set(shkp, (charges.get(shkp) || 0) + amount);
}

function lostShopMerchandiseChargesForObject(source, obj, defaultShkp, seen = new Set(), options = {}) {
    const charges = new Map();
    if (!source || !obj || !defaultShkp || seen.has(obj)) return charges;
    seen.add(obj);
    if (shopBillableGold(obj)) {
        if (options.topLevel !== false || options.includeContainedGold !== false)
            addLostShopMerchandiseCharge(charges, defaultShkp, Math.max(1, Math.trunc(Number(obj.quan || 1))));
        return charges;
    }

    const inventoryLikeNestedContent = options.inventoryLikeContents && options.topLevel === false;
    const owner = shopkeeperOwningBillEntry(obj);
    const billShkp = owner.entry ? (owner.shkp || defaultShkp) : defaultShkp;
    const entry = owner.entry || shopBillEntryForObject(defaultShkp, obj);
    if (entry) {
        addLostShopMerchandiseCharge(charges, billShkp, shopBillEntryTotal(entry));
        subOneFromShopBill(obj, billShkp);
    } else if (!obj.no_charge && (!inventoryLikeNestedContent || obj.unpaid)) {
        addLostShopMerchandiseCharge(charges, defaultShkp,
            shopItemPrice(obj, source.ox ?? game.u?.ux, source.oy ?? game.u?.uy));
    }

    for (const child of globContents(obj)) {
        const childCharges = lostShopMerchandiseChargesForObject(source, child, defaultShkp, seen, {
            ...options,
            topLevel: false,
        });
        for (const [shkp, value] of childCharges)
            addLostShopMerchandiseCharge(charges, shkp, value);
    }
    return charges;
}

function lostMagicBagShopChargesForObject(source, obj, defaultShkp, seen = new Set(), options = {}) {
    return lostShopMerchandiseChargesForObject(source, obj, defaultShkp, seen, options);
}

function billLostMagicBagShopItem(source, obj) {
    const sourceShkp = shopFloorContainerShopkeeper(source);
    if (!sourceShkp || !obj) return 0;
    const charges = lostMagicBagShopChargesForObject(source, obj, sourceShkp, new Set(), {
        includeContainedGold: false,
        inventoryLikeContents: true,
    });
    let charged = 0;
    const peaceful = shopkeeperPeacefulForDebt(sourceShkp);
    for (const [shkp, value] of charges)
        charged += chargeShopkeeperForLostMerchandise(shkp, value, { peaceful });
    return charged;
}

function objectCarriedByHero(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return false;
    seen.add(obj);
    if ((game.inventory || []).includes(obj)) return true;
    if ((game.level?.objects || []).includes(obj)) return false;
    return objectCarriedByHero(obj.container, seen);
}

function heldMagicBagLostValueForObject(obj, shkp, seen = new Set()) {
    if (!obj || !shkp || seen.has(obj) || shopBillableGold(obj)) return 0;
    seen.add(obj);
    let value = 0;
    const entry = shopBillEntryForObject(shkp, obj);
    if (entry) {
        value += shopBillEntryTotal(entry);
        subOneFromShopBill(obj, shkp);
    }
    for (const child of globContents(obj))
        value += heldMagicBagLostValueForObject(child, shkp, seen);
    return value;
}

function markHeldMagicBagDeletedContentsUsedUp(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return;
    seen.add(obj);
    for (const child of globContents(obj)) {
        markHeldMagicBagDeletedContentsUsedUp(child, seen);
        markObjectShopBillUsedUp(child);
    }
}

function markObjectTreeShopBillsUsedUp(obj, seen = new Set()) {
    if (!obj || seen.has(obj)) return false;
    seen.add(obj);
    let marked = false;
    for (const child of globContents(obj))
        marked = markObjectTreeShopBillsUsedUp(child, seen) || marked;
    return markObjectShopBillUsedUp(obj) || marked;
}

function polymorphShudderOdds(obj) {
    let odds = obj?.cls === 'wand' || obj?.cursed ? 3 : obj?.blessed ? 12 : 8;
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if (quantity > 4) odds = Math.trunc(odds / 2);
    return Math.max(1, odds);
}

function floorPolymorphObjectMaterial(obj) {
    const explicit = String(obj?.oc_material || obj?.material || '').toLowerCase().replace(/^hi_/, '');
    if (explicit) return explicit;
    const kind = objectKindKey(obj);
    if (obj?.otyp === CORPSE || obj?.otyp === 'corpse' || obj?.otyp === EGG
        || obj?.otyp === MEATBALL || obj?.otyp === MEAT_STICK || obj?.otyp === ENORMOUS_MEATBALL
        || /\b(?:corpse|egg|meat|tripe|glob)\b/.test(kind))
        return 'flesh';
    if (obj?.cls === 'food' || obj?.otyp === FOOD_CLASS || obj?.glyph === '%')
        return 'veggy';
    if (obj?.cls === 'scroll' || obj?.glyph === '?') return 'paper';
    if (obj?.cls === 'spellbook' || obj?.glyph === '+') return 'paper';
    return '';
}

function floorPolymorphGolemSpecForMaterial(material) {
    const mat = String(material || '').toLowerCase().replace(/^hi_/, '');
    if (['iron', 'metal', 'mithril'].includes(mat))
        return { name: 'iron golem', prefix: 'metal ', cwt: 2000 };
    if (['copper', 'silver', 'platinum', 'gemstone', 'mineral'].includes(mat)) {
        const stone = rn2(2);
        return stone
            ? { name: 'stone golem', prefix: 'lithic ', cwt: 1900 }
            : { name: 'clay golem', prefix: 'lithic ', cwt: 1550 };
    }
    if (!mat || mat === '0' || mat === 'no_material' || mat === 'none' || mat === 'flesh')
        return { name: 'flesh golem', prefix: 'organic ', cwt: 1400 };
    if (mat === 'wood') return { name: 'wood golem', prefix: 'wood ', cwt: 900 };
    if (mat === 'leather') return { name: 'leather golem', prefix: 'leather ', cwt: 800 };
    if (mat === 'cloth') return { name: 'rope golem', prefix: 'cloth ', cwt: 450 };
    if (mat === 'bone') return { name: 'skeleton', prefix: 'bony ', cwt: 300 };
    if (mat === 'gold') return { name: 'gold golem', prefix: 'gold ', cwt: 450 };
    if (mat === 'glass') return { name: 'glass golem', prefix: 'glassy ', cwt: 1800 };
    if (mat === 'paper') return { name: 'paper golem', prefix: 'paper ', cwt: 400 };
    return { name: 'straw golem', prefix: '', cwt: 400 };
}

function billPolymorphFloorUseUpObject(obj, x, y) {
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return;
    const heroShkp = shopkeeperForShopRoom(game.u?.ux, game.u?.uy);
    if (sameShopkeeper(shkp, heroShkp)) {
        addContainerAndContentsToShopBill(obj, obj, obj, shkp, x, y);
        markObjectTreeShopBillsUsedUp(obj);
    } else {
        shipObjectShopDebt(obj, x, y, { shopFloorObj: true, silent: true });
    }
}

function useUpPolymorphShudderFloorObject(obj, x, y, count = 1) {
    if (!obj) return null;
    const usedObj = splitFloorObjectForUseUp(obj, count);
    billPolymorphFloorUseUpObject(usedObj, x, y);
    removeFloorObject(usedObj);
    return usedObj;
}

function floorPolymorphPolyuseResists(obj) {
    if (isPotionDipUnpolyableObject(obj)) return true;
    rn2(100);
    return false;
}

function enoughFloorPileForPolymon(pile) {
    if (!pile.length) return false;
    return pile.length > 1 || Math.max(1, Math.trunc(Number(pile[0]?.quan || 1))) > 1;
}

function floorPolymorphPileObjectsAt(x, y, skipObjects = null) {
    return (game.level?.objects || [])
        .filter(obj => obj && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y
            && !skipObjects?.has(obj));
}

function polyuseFloorPileForMaterial(x, y, material, minwt, skipObjects = null) {
    let remaining = Math.max(0, Math.trunc(Number(minwt || 0)));
    for (const obj of floorPolymorphPileObjectsAt(x, y, skipObjects)) {
        if (remaining <= 0) break;
        if (floorPolymorphPolyuseResists(obj)) continue;
        const sameMaterial = floorPolymorphObjectMaterial(obj) === material;
        if (sameMaterial === (rn2(remaining + 1) !== 0)) {
            billPolymorphFloorUseUpObject(obj, x, y);
            remaining -= Math.max(1, Math.trunc(Number(obj.quan || 1)));
            removeFloorObject(obj);
        }
    }
}

async function createPolymonFromFloorPile(x, y, material, skipObjects = null) {
    const pile = floorPolymorphPileObjectsAt(x, y, skipObjects);
    if (!enoughFloorPileForPolymon(pile)) return [];
    const spec = floorPolymorphGolemSpecForMaterial(material);
    const genocided = (game._genocided_monsters || []).includes(spec.name);
    const data = genocided ? null : monsterByRndName(spec.name);
    const mon = await makemon(data, x, y, MM_NOMSG);
    polyuseFloorPileForMaterial(x, y, material, spec.cwt, skipObjects);
    if (mon && cansee(mon.mx, mon.my))
        return [`Some ${spec.prefix}objects meld, and ${monsterIndefiniteName(mon.data?.name || 'monster')} arises from the pile!`];
    return [];
}

function floorPolymorphContentsHaveShopCost(obj, shkp, x, y, seen = new Set()) {
    if (!obj || seen.has(obj)) return false;
    seen.add(obj);
    for (const child of globContents(obj)) {
        if (!child || seen.has(child)) continue;
        if (shopBillableGold(child) && Math.max(1, Math.trunc(Number(child.quan || 1))) > 0)
            return true;
        const owner = shopkeeperOwningBillEntry(child);
        const entry = owner.entry && sameShopkeeper(owner.shkp, shkp)
            ? owner.entry
            : shopBillEntryForObject(shkp, child);
        if (entry && shopBillEntryTotal(entry) > 0) return true;
        if (!child.no_charge && shopItemPrice(child, x, y) > 0) return true;
        if (floorPolymorphContentsHaveShopCost(child, shkp, x, y, seen)) return true;
    }
    return false;
}

function floorPolymorphShopkeeperAngerMessage(obj, x, y) {
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return '';
    if (obj?.no_charge && !floorPolymorphContentsHaveShopCost(obj, shkp, x, y))
        return '';
    const name = shopkeeperDisplayName(shkp);
    if (shopkeeperPeacefulForDebt(shkp)) {
        shkp.angry = true;
        shkp.hostile = true;
        shkp.mpeaceful = 0;
        shkp.following = 1;
        return `${name} gets angry!`;
    }
    shkp.angry = true;
    shkp.hostile = true;
    shkp.mpeaceful = 0;
    shkp.following = 1;
    return `${name} is furious!`;
}

function prepareFloorPolymorphReplacement(oldObj, newObj) {
    markObjectTreeShopBillsUsedUp(oldObj);
    delete newObj.contents;
    delete newObj.cobj;
    delete newObj.container;
    delete newObj.contained;
    delete newObj._shopBillObjectId;
    delete newObj.unpaid;
    delete newObj.unpaidPrice;
    if (typeof newObj.line === 'string')
        newObj.line = newObj.line.replace(/ \(unpaid, \d+ zorkmids?\)/, '');
    return newObj;
}

function restackFloorBouldersAt(x, y) {
    const objects = game.level?.objects;
    if (!objects?.length) return;
    const sameSquare = objects.filter(obj =>
        obj && !obj.transientProjectile && obj.ox === x && obj.oy === y);
    if (sameSquare.length < 2 || !sameSquare.some(obj => obj.otyp === BOULDER)) return;
    const restacked = [
        ...sameSquare.filter(obj => obj.otyp !== BOULDER),
        ...sameSquare.filter(obj => obj.otyp === BOULDER),
    ];
    if (sameSquare.every((obj, idx) => obj === restacked[idx])) return;
    const sameSet = new Set(sameSquare);
    let replacementIndex = 0;
    game.level.objects = objects.map(obj =>
        sameSet.has(obj) ? restacked[replacementIndex++] : obj);
}

function rockClassPolymorphReplacement(obj, x, y) {
    const newObj = mkobj(ROCK_CLASS, false);
    Object.assign(newObj, object_display(newObj), {
        ox: x,
        oy: y,
        quan: obj.quan || 1,
        cursed: obj.cursed,
        blessed: obj.blessed,
    });
    if (obj.otyp === BOULDER) applySokobanGuilt();
    return newObj;
}

async function polymorphFloorPileResultAt(x, y, {
    skipObjects = null,
    onlyObjects = null,
    omitObjects = null,
    restackBoulders = true,
} = {}) {
    const pileObjects = (game.level?.objects || [])
        .filter(obj => obj && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y);
    const targetObjects = pileObjects
        .filter(obj => !skipObjects?.has(obj)
            && (!onlyObjects || onlyObjects.has(obj))
            && !omitObjects?.has(obj));
    if (!targetObjects.length) {
        if (restackBoulders && pileObjects.length) restackFloorBouldersAt(x, y);
        return { affected: false, messages: [] };
    }
    const removeTargets = new Set();
    const replacementByOld = new Map();
    const alterationMessages = [];
    let didObjectShudder = false;
    let polyZappedMaterial = null;
    let affected = false;
    for (const obj of [...targetObjects].reverse()) {
        if (polymorphReplacementDisallowed(obj)) continue;
        if (rn2(100) < (obj.artifact ? 95 : 5)) {
            continue;
        }
        game.u.uconduct ??= {};
        game.u.uconduct.polypiles = Math.max(0, Math.trunc(Number(game.u.uconduct.polypiles || 0))) + 1;
        const shudderOdds = polymorphShudderOdds(obj);
        if (!rn2(shudderOdds)) {
            if (polyZappedMaterial == null) {
                const quantity = Math.max(1, Math.trunc(Number(obj.quan || 1)));
                for (let i = 0; i < quantity; i++) {
                    if (!rn2(45 + (game.u?.uluck || 0))) {
                        polyZappedMaterial = floorPolymorphObjectMaterial(obj);
                        break;
                    }
                }
            }
            const quantity = Math.max(1, Math.trunc(Number(obj.quan || 1)));
            const usedCount = quantity > 1 ? rnd(quantity - 1) : 1;
            rn2(100);
            if (!game._polymorph_wand_learned) {
                rn2(19);
                game._polymorph_wand_learned = 1;
            }
            useUpPolymorphShudderFloorObject(obj, x, y, usedCount);
            didObjectShudder = true;
            affected = true;
            continue;
        }
        let newObj;
        if (polymorphObjectClassCode(obj) === ROCK_CLASS) {
            newObj = rockClassPolymorphReplacement(obj, x, y);
        } else {
            const roll = rnd(1000);
            const newObjId = next_ident();
            if (obj.cls === 'potion' || obj.cls === 'scroll') {
                if (!rn2(4)) rn2(2);
            }
            newObj = { ...obj, id: newObjId, ox: x, oy: y, quan: obj.quan || 1, cursed: obj.cursed, blessed: obj.blessed };
            if (obj.cls === 'potion') {
                Object.assign(newObj, { cls: 'potion', glyph: '!', otyp: 'potion', color: NO_COLOR });
                const appearance = game._object_descriptions?.potions?.[potionIndexForRoll(roll)]?.description || 'clear';
                newObj.kind = `${appearance} potion`;
            } else if (obj.cls === 'scroll') {
                Object.assign(newObj, { cls: 'scroll', glyph: '?', otyp: 'scroll', color: CLR_WHITE });
                const label = game._object_descriptions?.scrolls?.[scrollIndexForRoll(roll)] || 'ELBIB YLOH';
                newObj.kind = `scroll labeled ${label}`;
            } else {
                newObj.kind = obj.kind || obj.cls;
            }
        }
        prepareFloorPolymorphReplacement(obj, newObj);
        const angerMessage = floorPolymorphShopkeeperAngerMessage(obj, x, y);
        if (angerMessage && !alterationMessages.includes(angerMessage))
            alterationMessages.push(angerMessage);
        removeTargets.add(obj);
        replacementByOld.set(obj, newObj);
        affected = true;
        rn2(100);
    }
    if (affected) {
        game.level.objects = (game.level?.objects || []).flatMap(obj => {
            if (!removeTargets.has(obj)) return [obj];
            const replacement = replacementByOld.get(obj);
            return replacement ? [replacement] : [];
        });
        if (restackBoulders) restackFloorBouldersAt(x, y);
        newsym(x, y);
    } else if (restackBoulders) {
        restackFloorBouldersAt(x, y);
    }
    const messages = [];
    if (polyZappedMaterial != null) {
        messages.push(...await createPolymonFromFloorPile(x, y, polyZappedMaterial, skipObjects));
        if (restackBoulders) restackFloorBouldersAt(x, y);
    }
    if (didObjectShudder) messages.push('You feel shuddering vibrations.');
    messages.push(...alterationMessages);
    return { affected, messages };
}

async function finishPolymorphFloorPileResult(result) {
    await setMessage(result.messages.join('  '));
    game.context ??= {};
    game.context.move = 1;
    return result.affected;
}

function heroCanHideUnderObjects() {
    const form = game.u?._polyself_form || game.u?.youmonst?.data || game.u?.data || {};
    return !!form.hidesUnder;
}

function heroHidingUnderObjects() {
    return !!(game.u?.uundetected && heroCanHideUnderObjects());
}

function topFloorObjectAt(x, y) {
    return [...(game.level?.objects || [])].reverse().find(obj =>
        obj && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y) || null;
}

function refreshHeroHidingUnderObjectsAt(x, y) {
    if (!heroCanHideUnderObjects()) return;
    const hasCover = (game.level?.objects || []).some(obj =>
        obj && !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y);
    game.u.uundetected = hasCover ? 1 : 0;
}

async function polymorphFloorPileAt(x, y, {
    consumeRangeRoll = false,
    onlyObjects = null,
    omitObjects = null,
    item = null,
    learnOnAffected = false,
    refreshHiding = false,
    restackBoulders = true,
} = {}) {
    rn2(19);
    if (consumeRangeRoll) rn2(8);
    const result = await polymorphFloorPileResultAt(x, y, { onlyObjects, omitObjects, restackBoulders });
    if (refreshHiding) refreshHeroHidingUnderObjectsAt(x, y);
    if (learnOnAffected && result.affected && item)
        setKnownWandLine(item, 'polymorph');
    return finishPolymorphFloorPileResult(result);
}

function polymorphRayMonsterAt(x, y) {
    return (game.level?.monsters || []).find(mon =>
        mon && !mon.dead && (mon.mhp ?? 1) > 0 && mon.mx === x && mon.my === y) || null;
}

function addUniquePolymorphMessages(messages, additions) {
    for (const message of additions) {
        if (message && !messages.includes(message)) messages.push(message);
    }
}

function polymorphRayBlockedAt(x, y) {
    const loc = game.level?.at?.(x, y);
    if (!loc) return true;
    const typ = loc.typ ?? ROOM;
    return !ZAP_POS(typ) || (typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED)));
}

async function polymorphFloorPileRay(dir, item = null, { attackLevel = 12 } = {}) {
    rn2(19);
    let range = rn1(8, 6);
    let x = game.u?.ux || 0;
    let y = game.u?.uy || 0;
    const messages = [];
    const bypassObjects = new Set();
    let affected = false;
    while (range-- > 0) {
        x += dir.dx;
        y += dir.dy;
        if (!isok(x, y)) break;
        const monster = polymorphRayMonsterAt(x, y);
        if (monster) {
            polymorphRayHitMonster(monster, messages, { item, bypassObjects, attackLevel });
            affected = true;
            range -= 3;
        }
        const result = await polymorphFloorPileResultAt(x, y, { skipObjects: bypassObjects });
        if (result.affected) {
            affected = true;
            range--;
            addUniquePolymorphMessages(messages, result.messages);
        }
        if (polymorphRayBlockedAt(x, y)) break;
    }
    return finishPolymorphFloorPileResult({ affected, messages });
}

async function polymorphSpellDirection(ch) {
    const dir = movementDirection(ch);
    const verticalDir = !dir && ch === '<' ? { dx: 0, dy: 0, dz: -1 }
        : !dir && ch === '>' ? { dx: 0, dy: 0, dz: 1 } : null;
    if (ch === '.') {
        const result = polymorphSelfZapResult();
        if (result.message) await setMessage(result.message, result.more);
        else {
            game._pending_message = '';
            game._message_more = 0;
        }
        return true;
    }
    if (!dir && !verticalDir) return false;
    if (verticalDir?.dz < 0) {
        if (heroHidingUnderObjects()) {
            const topObject = topFloorObjectAt(game.u?.ux || 0, game.u?.uy || 0);
            if (topObject) {
                await polymorphFloorPileAt(game.u?.ux || 0, game.u?.uy || 0, {
                    onlyObjects: new Set([topObject]),
                    refreshHiding: true,
                    restackBoulders: false,
                });
                return true;
            }
        }
        rn2(19);
        game._pending_message = '';
        game._message_more = 0;
        return true;
    }
    if (!verticalDir) {
        await polymorphFloorPileRay(dir, null, { attackLevel: game.u?.ulevel || 1 });
        return true;
    }
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    const topObject = heroHidingUnderObjects() ? topFloorObjectAt(x, y) : null;
    await polymorphFloorPileAt(x, y, {
        omitObjects: topObject ? new Set([topObject]) : null,
        refreshHiding: !!topObject,
    });
    return true;
}

function billHeldMagicBagLostItem(obj) {
    const owner = shopkeeperOwningBillEntry(obj);
    if (owner.entry) {
        const currentShopkeeper = heroShopkeeper();
        const shkp = owner.shkp || currentShopkeeper;
        if (!shkp) return 0;
        if (!currentShopkeeper) {
            markObjectTreeShopBillsUsedUp(obj);
            return 0;
        }
        const value = heldMagicBagLostValueForObject(obj, shkp);
        return chargeShopkeeperForLostMerchandise(shkp, value);
    }
    markHeldMagicBagDeletedContentsUsedUp(obj);
    return 0;
}

function billMagicBagLostItem(source, obj) {
    const shkp = shopFloorContainerShopkeeper(source);
    if (shkp) return billLostMagicBagShopItem(source, obj);
    if (objectCarriedByHero(source)) return billHeldMagicBagLostItem(obj);
    return 0;
}

function billMagicBagLostItemFromExplosionContext(obj, { held, x = game.u?.ux, y = game.u?.uy } = {}) {
    if (held) return billHeldMagicBagLostItem(obj);
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) {
        markObjectTreeShopBillsUsedUp(obj);
        return 0;
    }
    const charges = lostMagicBagShopChargesForObject({ ox: x, oy: y }, obj, shkp, new Set(), {
        includeContainedGold: false,
        inventoryLikeContents: true,
    });
    let charged = 0;
    const peaceful = shopkeeperPeacefulForDebt(shkp);
    for (const [owner, value] of charges)
        charged += chargeShopkeeperForLostMerchandise(owner, value, { peaceful });
    return charged;
}

function billMagicBagLostItemWithContext(source, obj, context = null) {
    if (context && Object.hasOwn(context, 'held'))
        return billMagicBagLostItemFromExplosionContext(obj, context);
    return billMagicBagLostItem(source, obj);
}

function billShopFloorMagicBagExplosionTarget(targetBag) {
    const shkp = shopFloorContainerShopkeeper(targetBag);
    if (!shkp || !targetBag || shopBillableGold(targetBag)) return { shkp: null, billEntry: null };
    const wasNoCharge = !!targetBag.no_charge;
    const billing = addContainerAndContentsToShopBill(
        targetBag,
        targetBag,
        targetBag,
        shkp,
        targetBag.ox,
        targetBag.oy,
    );
    targetBag.no_charge = wasNoCharge;
    markObjectShopBillUsedUp(targetBag, shkp);
    return { shkp, billEntry: shopBillEntryForObject(shkp, targetBag), billing };
}

function lostShopMerchandiseMessage(loss) {
    const amount = Math.max(0, Math.trunc(Number(loss || 0)));
    if (!amount) return '';
    return `You owe ${amount} ${shopCurrency(amount)} for lost merchandise.`;
}

function mergePickedObjectIntoShopBill(source, target, sourcePrice = null) {
    const pickedCount = Math.max(1, Math.trunc(Number(source?.quan || 1)));
    const targetCount = Math.max(1, Math.trunc(Number(target?.quan || 1)));
    const x = source?.ox ?? game.u?.ux;
    const y = source?.oy ?? game.u?.uy;
    const shkp = x == null || y == null ? null : shopkeeperForCostlySpot(x, y);
    const price = sourcePrice != null ? Number(sourcePrice) : shopItemPrice(source, x, y);
    const sourceBillable = Number.isFinite(price) && price > 0 && shopkeeperInHisShop(shkp) && !shopBillIsFull(shkp);
    const existingEntry = shopBillEntryForObject(shkp, target);
    const targetUnpaid = !!(target?.unpaid || existingEntry);

    if (!sourceBillable) return { canMerge: !targetUnpaid, price: 0, billEntry: existingEntry };
    if (!targetUnpaid) return { canMerge: false, price, billEntry: null };
    if (!existingEntry) return { canMerge: false, price, billEntry: null };

    const existingTotal = shopBillEntryTotal(existingEntry);
    if (!(existingTotal > 0)) return { canMerge: false, price, billEntry: existingEntry };
    if (existingTotal * pickedCount !== price * targetCount)
        return { canMerge: false, price, billEntry: existingEntry };

    const totalPrice = existingTotal + price;
    const billEntry = existingEntry;
    if (billEntry) {
        billEntry.bquan = targetCount + pickedCount;
        billEntry.totalPrice = totalPrice;
        billEntry.price = totalPrice;
        shkp.billct = Array.isArray(shkp.bill) ? shkp.bill.length : Math.max(1, shkp.billct || 1);
    }
    target.unpaid = true;
    target.unpaidPrice = totalPrice;
    return { canMerge: true, price, billEntry };
}

function pickupMergeName(obj) {
    return pickupObjectName({ ...(obj || {}), line: '', quan: 1 }).toLowerCase();
}

function pickupWeaponCanStack(obj) {
    if (!obj) return false;
    const name = pickupMergeName(obj);
    return /\b(?:arrow|ya|bolt|dart|dagger|knife|spear|javelin|shuriken|boomerang|rock|stone)\b/.test(name);
}

const COVERED_SIMPLE_MERGEABLE_FOOD_KINDS = new Set([
    'apple',
    'orange',
    'pear',
    'melon',
    'banana',
    'carrot',
    'kelp frond',
    'sprig of wolfsbane',
    'clove of garlic',
    'slime mold',
    'eucalyptus leaf',
    'lump of royal jelly',
    'meatball',
    'meat stick',
    'enormous meatball',
    'cream pie',
    'candy bar',
    'fortune cookie',
    'pancake',
    'lembas wafer',
    'cram ration',
    'tripe ration',
    'food ration',
    'k-ration',
    'c-ration',
]);

function simpleMergeableFoodKind(obj) {
    const kind = objectKindKey(obj).replace(/^partly eaten\s+/, '').trim();
    if (COVERED_SIMPLE_MERGEABLE_FOOD_KINDS.has(kind)) return kind;
    const singular = String(obj?.singular || '').toLowerCase().trim();
    if (COVERED_SIMPLE_MERGEABLE_FOOD_KINDS.has(singular)) return singular;
    return '';
}

function isSimpleMergeableFoodObject(obj) {
    if (!obj || isTinObject(obj) || isEggItem(obj) || isGlobbyObject(obj) || globContents(obj).length) return false;
    if (obj.otyp === CORPSE || obj.otyp === 'corpse') return false;
    const kind = simpleMergeableFoodKind(obj);
    if (obj.otyp === MEAT_RING || kind === 'meat ring') return false;
    const foodLike = obj.cls === 'food' || obj.otyp === FOOD_CLASS || obj.glyph === '%';
    return foodLike && !!kind;
}

function isSpecialFoodMergeObject(obj) {
    return isCorpseItem(obj) || isEggItem(obj) || isTinObject(obj);
}

function specialFoodCanInventoryMerge(obj) {
    if (!isSpecialFoodMergeObject(obj)) return false;
    if (isEggItem(obj)) return !eggHasHatchTimer(obj);
    if (isCorpseItem(obj)) return !corpseIsReviverForMerge(obj);
    return true;
}

function objectInstanceNameKey(obj) {
    return String(obj?._wish_object_name || obj?.oname || obj?.oextra?.oname || '').trim();
}

function objectInstanceNamesMergeCompatible(target, source) {
    const targetName = objectInstanceNameKey(target);
    const sourceName = objectInstanceNameKey(source);
    return !targetName || !sourceName || targetName === sourceName;
}

function specialFoodInstanceNamesMergeCompatible(target, source) {
    const targetName = objectInstanceNameKey(target);
    const sourceName = objectInstanceNameKey(source);
    if (isCorpseItem(target) || isCorpseItem(source))
        return targetName === sourceName;
    return !targetName || !sourceName || targetName === sourceName;
}

function stackedObjectInstanceNamesMergeCompatible(target, source) {
    if (isCorpseItem(target) || isCorpseItem(source))
        return specialFoodInstanceNamesMergeCompatible(target, source);
    return objectInstanceNamesMergeCompatible(target, source);
}

function copyStackedObjectInstanceNameForMerge(target, source) {
    if (isCorpseItem(target) || isCorpseItem(source)) return;
    copyObjectInstanceNameForMerge(target, source);
}

function copyObjectInstanceNameForMerge(target, source) {
    if (objectInstanceNameKey(target)) return;
    const sourceName = objectInstanceNameKey(source);
    if (!sourceName) return;
    if (source?._wish_object_name) target._wish_object_name = source._wish_object_name;
    else target.oname = sourceName;
}

function pickupObjectCanInventoryMerge(obj) {
    if (!obj || shopBillableGold(obj) || globContents(obj).length || isGlobbyObject(obj)) return false;
    if (specialFoodCanInventoryMerge(obj)) return true;
    if (isSpecialFoodMergeObject(obj)) return false;
    if (isSimpleMergeableFoodObject(obj)) return true;
    if (obj.cls === 'food' || obj.otyp === FOOD_CLASS) return false;
    const cls = shopObjectClassCode(obj);
    if (cls === SCROLL_CLASS || cls === POTION_CLASS || cls === GEM_CLASS) return true;
    if (cls === WEAPON_CLASS) return pickupWeaponCanStack(obj);
    return isCandleObject(obj);
}

function pickedObjectInventoryMergeCompatible(target, source, sourceWillBeUnpaid, shkp = null) {
    if (!target || !source || target === source) return false;
    if (!pickupObjectCanInventoryMerge(target) || !pickupObjectCanInventoryMerge(source)) return false;
    if (target.worn || target.wielded || target.alternate || source.worn || source.wielded || source.alternate) return false;
    if (target.nomerge || source.nomerge || target.artifact || source.artifact) return false;
    const targetUnpaid = !!(target.unpaid || shopBillEntryForObject(shkp, target));
    if (targetUnpaid !== !!sourceWillBeUnpaid) return false;
    const sourceNoCharge = sourceWillBeUnpaid ? !!source.no_charge : false;
    if (!!target.no_charge !== sourceNoCharge) return false;
    if (!!target.blessed !== !!source.blessed || !!target.cursed !== !!source.cursed) return false;
    if ((target.spe ?? 0) !== (source.spe ?? 0)) return false;
    if ((target.oeaten ?? 0) !== (source.oeaten ?? 0) || (target.orotten ?? 0) !== (source.orotten ?? 0)) return false;
    if ((target.obroken ?? false) !== (source.obroken ?? false)) return false;
    if ((target.lamplit ?? false) !== (source.lamplit ?? false)) return false;
    if (!sameStackCorpseEggTinFields(target, source)) return false;
    if ((isSpecialFoodMergeObject(target) || isSpecialFoodMergeObject(source))
        && !specialFoodInstanceNamesMergeCompatible(target, source))
        return false;
    if ((target.odiluted ?? false) !== (source.odiluted ?? false)) return false;
    if ((target.oeroded ?? 0) !== (source.oeroded ?? 0) || (target.oeroded2 ?? 0) !== (source.oeroded2 ?? 0)) return false;
    if ((target.greased ?? false) !== (source.greased ?? false)) return false;
    if (shopObjectClassCode(target) !== shopObjectClassCode(source)) return false;
    if ((target.otyp ?? null) !== (source.otyp ?? null) && pickupMergeName(target) !== pickupMergeName(source)) return false;
    if ((target.scrollIndex ?? null) !== (source.scrollIndex ?? null)) return false;
    if ((target.potionIndex ?? null) !== (source.potionIndex ?? null)) return false;
    if ((target.gemDescription ?? null) !== (source.gemDescription ?? null)) return false;
    if ((target.actualKind || target.kind || '') !== (source.actualKind || source.kind || '')
        && pickupMergeName(target) !== pickupMergeName(source)) return false;
    if ((isSimpleMergeableFoodObject(target) || isSimpleMergeableFoodObject(source))
        && (!isSimpleMergeableFoodObject(target)
            || !isSimpleMergeableFoodObject(source)
            || simpleMergeableFoodKind(target) !== simpleMergeableFoodKind(source)
            || !objectInstanceNamesMergeCompatible(target, source)))
        return false;
    if (isCandleObject(source) && Math.trunc((target.age || 0) / 25) !== Math.trunc((source.age || 0) / 25))
        return false;
    return true;
}

function findPickedObjectInventoryMergeTarget(source, sourcePrice = null) {
    const x = source?.ox ?? game.u?.ux;
    const y = source?.oy ?? game.u?.uy;
    const shkp = x == null || y == null ? null : shopkeeperForCostlySpot(x, y);
    const price = sourcePrice != null ? Number(sourcePrice) : shopItemPrice(source, x, y);
    const sourceWillBeUnpaid = Number.isFinite(price) && price > 0 && shopkeeperInHisShop(shkp) && !shopBillIsFull(shkp);
    for (const target of game.inventory || []) {
        if (!pickedObjectInventoryMergeCompatible(target, source, sourceWillBeUnpaid, shkp)) continue;
        const billMerge = mergePickedObjectIntoShopBill(source, target, price);
        if (billMerge.canMerge) return { target, billMerge };
    }
    return null;
}

function mergePickedObjectIntoInventory(source, target) {
    const pickedCount = Math.max(1, Math.trunc(Number(source?.quan || 1)));
    const targetCount = Math.max(1, Math.trunc(Number(target.quan || 1)));
    const ageAveragedMerge = (isSimpleMergeableFoodObject(target) && isSimpleMergeableFoodObject(source))
        || (isSpecialFoodMergeObject(target) && isSpecialFoodMergeObject(source));
    if (ageAveragedMerge) {
        const targetAge = Number.isFinite(Number(target.age)) ? Number(target.age) : 0;
        const sourceAge = Number.isFinite(Number(source.age)) ? Number(source.age) : 0;
        if (target.age != null || source.age != null)
            target.age = Math.trunc(((targetAge * targetCount) + (sourceAge * pickedCount)) / (targetCount + pickedCount));
    }
    if (isSimpleMergeableFoodObject(target) && isSimpleMergeableFoodObject(source)) {
        copyObjectInstanceNameForMerge(target, source);
    } else if (isSpecialFoodMergeObject(target) && isSpecialFoodMergeObject(source) && !isCorpseItem(target)) {
        copyObjectInstanceNameForMerge(target, source);
    }
    target.quan = targetCount + pickedCount;
    if (source.plural && !target.plural) target.plural = source.plural;
    if (source.known !== target.known) target.known = true;
    if (source.bknown !== target.bknown && game._startup_role !== 'Priest') target.bknown = true;
    if (source.rknown !== target.rknown) target.rknown = true;
    target.line = normalInventoryLine({ ...target, line: '' });
    if (target.unpaid) syncUnpaidBillLine(target);
    const pickedPhrase = isSimpleMergeableFoodObject(target) && isSimpleMergeableFoodObject(source)
        ? normalInventoryLine({ ...target, line: '', quan: pickedCount }).replace(/^[^ ]+ - /, '')
        : pickupObjectPhrase({ ...source, line: '', quan: pickedCount });
    return `${target.letter} - ${pickedPhrase} (${target.quan} in total).`;
}

function wieldedItemForFreeHandCheck() {
    return (game.inventory || []).find(item =>
        item.wielded || item.line?.includes('weapon in') || item.line?.includes('(wielded)')) || null;
}

function wornCursedShieldForFreeHandCheck() {
    return (game.inventory || []).find(item => {
        const name = inventoryItemName(item).toLowerCase();
        return item.cursed && (item.worn || /being worn/.test(String(item.line || '')))
            && /\bshield\b/.test(name);
    }) || null;
}

function heroHasFreeHandForThrownCatch() {
    const weapon = wieldedItemForFreeHandCheck();
    if (!weapon || !(weapon.cursed || weapon.welded)) return true;
    return !isTwoHandedWieldItem(weapon) && !wornCursedShieldForFreeHandCheck();
}

export function heroCanAttemptThrownObjectCatch(obj) {
    if (!obj || game.u?.blind || game.u?.confusion || game.u?.stunned || game.u?.fumbling) return false;
    if (isVenomObject(obj) || polyselfNoHands() || !heroHasFreeHandForThrownCatch()) return false;
    const nextWeight = heroCarriedWeight() + globObjectWeight({ ...obj, quan: Math.max(1, obj.quan || 1) });
    return heroEncumbranceForWeight(nextWeight) <= 1;
}

export function holdCaughtThrownObject(obj, {
    catchName = '',
    glyph = obj?.glyph || ')',
    color = obj?.color ?? CLR_CYAN,
} = {}) {
    if (!obj) return { held: false, dropped: false, message: '' };
    const name = catchName || pickupObjectName({ ...obj, quan: 1 });
    const caught = {
        ...obj,
        quan: Math.max(1, Math.trunc(Number(obj.quan || 1))),
        kind: obj.kind || pickupObjectName({ ...obj, quan: 1 }),
        glyph,
        color,
        petFetchable: true,
        hidden: false,
        buried: false,
        transientProjectile: false,
    };
    delete caught.letter;
    delete caught.line;
    delete caught.ox;
    delete caught.oy;

    const mergeInfo = findPickedObjectInventoryMergeTarget(caught, 0);
    if (mergeInfo) {
        mergePickedObjectIntoInventory(caught, mergeInfo.target);
        game._pet_food_scan_inventory = game.inventory;
        return { held: true, dropped: false, object: mergeInfo.target, message: `You catch the ${name}!` };
    }

    const letter = simulatedNextInventoryLetters(1)?.[0];
    if (!letter) {
        const dropped = {
            ...caught,
            ox: game.u?.ux || 0,
            oy: game.u?.uy || 0,
        };
        game.level.objects ??= [];
        game.level.objects.push(dropped);
        newsym(dropped.ox, dropped.oy);
        return { held: false, dropped: true, object: dropped, message: `You catch, but drop, the ${name}.` };
    }

    Object.assign(caught, { letter });
    caught.line = normalInventoryLine({ ...caught, line: '' });
    nextInventoryLetter();
    game.inventory = [...(game.inventory || []), caught];
    game._pet_food_scan_inventory = game.inventory;
    return { held: true, dropped: false, object: caught, message: `You catch the ${name}!` };
}

function containerTakeoutInventorySourceView(container, obj) {
    return {
        ...obj,
        cls: obj?.cls || (obj?.otyp === GEM_CLASS ? 'gem'
            : obj?.otyp === SCROLL_CLASS ? 'scroll'
                : obj?.glyph === '+' ? 'spellbook' : obj?.cls),
        kind: obj?.kind || pickupObjectName({ ...(obj || {}), quan: 1 }),
        no_charge: false,
        ox: container?.ox ?? obj?.ox,
        oy: container?.oy ?? obj?.oy,
    };
}

function containerTakeoutMergeBilling(container, obj) {
    const shkp = shopFloorContainerShopkeeper(container);
    if (!shkp || liveUnpaidBillOwnerOrClear(shkp, obj).entry || obj?.no_charge || globContents(obj).length)
        return { shkp, price: 0, sourceWillBeUnpaid: false };
    const x = container?.ox;
    const y = container?.oy;
    const price = x == null || y == null ? 0 : shopItemPrice(obj, x, y);
    return {
        shkp,
        price,
        sourceWillBeUnpaid: price > 0 && shopkeeperInHisShop(shkp) && !shopBillIsFull(shkp),
    };
}

function containerTakeoutBillMergeCompatible(source, target, billing) {
    if (!billing.sourceWillBeUnpaid) return true;
    const entry = shopBillEntryForObject(billing.shkp, target);
    if (!entry) return false;
    const targetCount = Math.max(1, Math.trunc(Number(target?.quan || 1)));
    const sourceCount = Math.max(1, Math.trunc(Number(source?.quan || 1)));
    const existingTotal = shopBillEntryTotal(entry);
    return existingTotal > 0 && existingTotal * sourceCount === billing.price * targetCount;
}

function findContainerTakeoutInventoryMergeTarget(container, obj) {
    if (!obj || shopBillableGold(obj)) return null;
    const billing = containerTakeoutMergeBilling(container, obj);
    if (obj.unpaid) return null;
    const source = containerTakeoutInventorySourceView(container, obj);
    if (!pickupObjectCanInventoryMerge(source)) return null;
    for (const target of game.inventory || []) {
        if (!pickedObjectInventoryMergeCompatible(target, source, billing.sourceWillBeUnpaid, billing.shkp)) continue;
        if (!containerTakeoutBillMergeCompatible(source, target, billing)) continue;
        return { target, source, billing };
    }
    return null;
}

function mergeContainerTakeoutObjectIntoInventory(container, obj, mergeInfo) {
    const { target, source, billing } = mergeInfo;
    prepareContainerTakeoutObject(container, obj);
    Object.assign(obj, {
        cls: source.cls,
        kind: source.kind,
        no_charge: false,
    });
    if (billing.sourceWillBeUnpaid) {
        const billSource = { ...obj, ox: container?.ox, oy: container?.oy, no_charge: false };
        mergePickedObjectIntoShopBill(billSource, target, billing.price);
    }
    const line = mergePickedObjectIntoInventory(obj, target);
    maybeAttachCarriedFigurineTimeout(target);
    return !billing.sourceWillBeUnpaid && billing.price > 0 && shopBillIsFull(billing.shkp)
        ? `You got that for free!  ${line}`
        : line;
}

function sellobjReturnUnpaidToShop(obj, x, y) {
    if (!obj?.unpaid || shopBillableGold(obj) || globContents(obj).length) return false;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return false;
    if (subOneFromShopBill(obj, shkp)) return true;
    clearObjectShopBillState(obj);
    shkp.billct = Array.isArray(shkp.bill)
        ? shkp.bill.length
        : Math.max(0, Math.trunc(Number(shkp.billct || 0)) - 1);
    return true;
}

export const __shopBillingTestHooks = {
    addContainerTakeoutObjectToInventory,
    addContainerTakeoutObjectToShopBill,
    addObjectToShopBill,
    addPickedObjectToShopBill,
    billDummyAlteredCarriedObjectForTest: billDummyAlteredCarriedObject,
    beginDroppedPaidObjectSale,
    beginShopFloorContainerPutSale,
    collectPayableShopDebts,
    finishRobbedOnlyShopPayment,
    finishShopPaymentSelection,
    findPickedObjectInventoryMergeTarget,
    findFloorPickupFoodMergeTargetForPreflight,
    finishDroppedObjectSale,
    finishShopFloorContainerPutSale,
    heroHasAntimagicForTest: heroHasAntimagic,
    heroHasColdResistanceForTest: heroHasColdResistance,
    heroHasSlowDigestionForTest: heroHasSlowDigestion,
    applyHeroOrdinaryHungerForTest: applyHeroOrdinaryHunger,
    applyAccessoryHungerForTest: applyAccessoryHunger,
    coldInventoryProtectionChanceForTest: coldInventoryProtectionChance,
    heroHasAcidResistanceForTest: heroHasAcidResistance,
    heroHasPoisonResistanceForTest: heroHasPoisonResistance,
    impactDropFloorObjects,
    deliverImpactDroppedObjects,
    deliverQueuedImpactDroppedObjectsForTest: deliverQueuedImpactDroppedObjects,
    mergePickedObjectIntoInventory,
    mergePickedObjectIntoShopBill,
    containerTakeoutBillMergeCompatible,
    landProjectileObjectWithShopHandling,
    markObjectShopBillUsedUpForTest: markObjectShopBillUsedUp,
    placeStackableFloorObject,
    projectileContainerImpactDmg,
    pickUpFloorGoldObject,
    prepareContainerTakeoutObject,
    polymorphFloorPileAtForTest: polymorphFloorPileAt,
    hasRobbedOnlyShopPayment,
    putInventoryObjectIntoBag,
    putInventoryObjectIntoContainer,
    removeObjectFromShopBill,
    removeObjectFromShopBillById,
    removeContainedObject,
    removeInventoryItem,
    resolveUnpaidProjectileShopLanding,
    returnUnpaidObjectToShopBillOwnerAt,
    useUpInventoryItemForTest: useUpInventoryItem,
    costlyShopGoldAtSpot,
    containerTakeoutPreflight,
    sellobjReturnUnpaidToShop,
    shipObjectShopDebt,
    sellobjDroppedGoldAt,
    stackMonsterThrownObject,
    splitCarriedObjectShopBill,
    splitShopBillEntry,
    subFromShopBill,
    subOneFromShopBill,
    fireDamageInventoryForTest: fireDamageInventory,
    heroFireTrapResultForTest: heroFireTrapResult,
    applyHeroFireTrapFatalResultForTest: applyHeroFireTrapFatalResult,
    resolveFireScrollExplosionForTest: resolveFireScrollExplosion,
    resolvePyroliskEggExplosionForTest: resolvePyroliskEggExplosion,
    checkUnpaidUsageForTest: checkUnpaidUsage,
    costlyTinForTest: costlyTinAlteration,
    tipContainerContents,
    tipContainerIntoContainer,
    tipContainerToFloor,
    touchFoodForBiteTest: touchEatenFood,
    shopDroppedPaidObjectSaleInfo,
    shopBillEntryForObject,
    shopBillEntryQuantity,
    shopBillEntryTotal,
    addShopTerrainDamage,
    payForCurrentShopTerrainDamage,
    repairShopDamageForShopkeeper,
    shopkeeperDebitPayment,
    shopkeeperCash,
    shopBaseCost,
    shopItemPrice,
    shopPaymentCashDue,
    shopSaleableObject,
    shopSaleOffer,
};

function buriedMerchandiseDebtMessage(x, y, ignoredObject = null) {
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp) || game._monster_moving) return '';
    let loss = 0;
    const seen = new Set();
    const source = { ox: x, oy: y };
    const peaceful = shkp.mpeaceful !== 0;
    for (const obj of game.level?.objects || []) {
        if (!obj || obj === ignoredObject || obj.transientProjectile) continue;
        if (obj.ox !== x || obj.oy !== y) continue;
        const charges = lostShopMerchandiseChargesForObject(source, obj, shkp, seen);
        for (const [chargedShkp, value] of charges)
            loss += chargeShopkeeperForLostMerchandise(chargedShkp, value, { peaceful });
        if (!shopBillableGold(obj))
            obj.no_charge = true;
    }
    if (!loss) return '';
    return `You owe ${shopkeeperDisplayName(shkp)} ${loss} ${shopCurrency(loss)} for burying merchandise.`;
}

function clearHeroSlimeFromMoltenLava() {
    if (game.u?._statusSuffix?.includes('Slimed')) {
        game.u._statusSuffix = game.u._statusSuffix.replace(/ Slimed/g, '');
        game.u.slimed = false;
    }
}

function heroNextToSquare(x, y) {
    if (game.u?.ux == null || game.u?.uy == null) return false;
    return distmin(game.u.ux, game.u.uy, x, y) <= 1;
}

function applyBoulderLavaSplashToHero(messages) {
    if (!game.u) return false;
    messages.push(`You are hit by molten lava${game.u.fireResistance ? '.' : '!'}`);
    clearHeroSlimeFromMoltenLava();
    const damage = d(game.u.fireResistance ? 1 : 3, 6);
    game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
    if ((game.u.uhp || 0) <= 0) {
        game._death_cause = 'killed by molten lava';
        messages.push('You die...');
    }
    return true;
}

function boulderFillMonsterAt(x, y) {
    return (game.level?.monsters || []).find(mon =>
        mon.mx === x && mon.my === y && !mon.dead && (mon.mhp == null || mon.mhp > 0));
}

function boulderFillMonsterInAir(mon) {
    const data = mon?.data || {};
    return !!(mon?.inAir || mon?.flyer || mon?.floater
        || data.inAir || data.flyer || data.floater
        || ((mon?.mundetected || mon?.uundetected) && (data.clinger || data.ceilingHider)
            && !Is_airlevel(game.u?.uz)));
}

function maybeCreateBoulderFillCorpse(mon) {
    const data = mon?.data || {};
    const corpseData = data.corpse || data;
    if (!monsterLeavesCorpseLikeDrop(corpseData)) return;
    if (!monsterCorpseDropSucceeds(mon, data)) return;
    createMonsterCorpseOrGlob(mon, corpseData);
}

function killBoulderFillMonster(x, y) {
    const mon = boulderFillMonsterAt(x, y);
    if (!mon || boulderFillMonsterInAir(mon)) return false;
    dropMonsterInventory(mon);
    maybeCreateBoulderFillCorpse(mon);
    recordVanquished(mon, false);
    const loc = game.level?.at(x, y);
    if (loc?.map_invisible) {
        loc.map_invisible = false;
        loc.remembered_glyph = null;
    }
    game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
    mon.mhp = 0;
    mon.movement = 0;
    mon.dead = true;
    newsym(x, y);
    return true;
}

const BOULDER_FILL_FLOOR_TRAPS = new Set([
    SQKY_BOARD, BEAR_TRAP, LANDMINE, FIRE_TRAP,
    PIT, SPIKED_PIT, HOLE, TRAPDOOR,
    TELEP_TRAP, LEVEL_TELEP, WEB, MAGIC_TRAP, ANTI_MAGIC,
]);

function boulderFillTrapAt(x, y) {
    return (game.level?.traps || []).find(trap => trap.tx === x && trap.ty === y) || null;
}

function deleteBoulderFillFloorTrap(trap, x, y) {
    if (!trap || !BOULDER_FILL_FLOOR_TRAPS.has(trap.ttyp) || !game.level?.traps) return false;
    if (game.u?.ux === x && game.u?.uy === y) {
        if (game.u.utraptype !== TT_BURIEDBALL) {
            game.u.utrap = 0;
            game.u.utraptype = null;
        }
    } else {
        const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
        if (mon) mon.mtrapped = 0;
    }
    game.level.traps = game.level.traps.filter(candidate => candidate !== trap);
    return true;
}

function earthBoulderPitTrapAt(x, y) {
    const trap = boulderFillTrapAt(x, y);
    return trap && (trap.ttyp === PIT || trap.ttyp === SPIKED_PIT || trap.ttyp === HOLE || trap.ttyp === TRAPDOOR)
        ? trap
        : null;
}

function earthBoulderPitMessage(trap) {
    if (trap?.ttyp === TRAPDOOR) return `${trap.tseen ? '' : 'triggers and '}plugs a trap door`;
    if (trap?.ttyp === HOLE) return 'plugs a hole';
    return 'fills a pit';
}

function applyEarthBoulderPitOccupantEffects(trap, x, y, messages, verb = 'fall') {
    const mon = (game.level?.monsters || []).find(candidate =>
        candidate.mx === x && candidate.my === y && !candidate.dead && (candidate.mhp == null || candidate.mhp > 0));
    const heroHere = game.u?.ux === x && game.u?.uy === y;
    const monsterTrapped = mon && mon.mtrapped;
    const heroTrapped = heroHere && game.u?.utrap;
    if (!monsterTrapped && !heroTrapped) return { skipPlugMessage: false };
    if (verb && (earthVisibleSquare(x, y) || heroHere))
        messages.push(`${game.u?.blind ? 'A' : 'The'} boulder ${verb}s into the pit${monsterTrapped ? '' : ' with you'}.`);
    if (monsterTrapped) {
        if (earthTargetIsSolid(mon) && !earthTargetThrowsRocks(mon)) {
            mon.mhp = (mon.mhp || 1) - earthObjectDamage({ otyp: BOULDER, quan: 1 }, mon);
            if ((mon.mhp || 0) <= 0) {
                dropMonsterInventory(mon, messages);
                game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
                mon.mhp = 0;
                mon.dead = true;
                const heroCaused = !game._monster_moving;
                recordVanquished(mon, heroCaused);
                if (earthVisibleSquare(x, y)) {
                    messages.push(heroCaused
                        ? `You kill the ${mon.data?.name || mon.name || 'monster'}!`
                        : `${fireScrollMonsterName(mon)} is killed!`);
                }
            }
        }
        mon.mtrapped = 0;
    } else if (heroTrapped) {
        const heroTarget = { ...game.u, data: polyselfForm() || {} };
        const solidHero = earthTargetIsSolid(heroTarget) && !earthTargetThrowsRocks(heroTarget);
        if (solidHero && game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 1) - rnd(15));
            if ((game.u.uhp || 0) <= 0) {
                game._death_cause = 'squished under a boulder';
                messages.push('You die...');
            }
            return { skipPlugMessage: true };
        } else if (game.u?.utraptype !== TT_BURIEDBALL) {
            game.u.utrap = 0;
            game.u.utraptype = null;
        }
    }
    return { skipPlugMessage: false };
}

function earthBoulderPitHoleEffects(obj, x, y, messages, verb = 'fall', {
    usedUpShopBillOnDestroy = false,
} = {}) {
    if (obj?.otyp !== BOULDER) return false;
    const trap = earthBoulderPitTrapAt(x, y);
    if (!trap) return false;
    const heroHere = game.u?.ux === x && game.u?.uy === y;
    const { skipPlugMessage } = applyEarthBoulderPitOccupantEffects(trap, x, y, messages, verb);
    if (verb && !skipPlugMessage && game.u?.blind && heroHere) {
        messages.push('You hear a CRASH! beneath you.');
    } else if (verb && !skipPlugMessage && earthVisibleSquare(x, y)) {
        messages.push(`The boulder ${earthBoulderPitMessage(trap)}.`);
    } else if (verb && !skipPlugMessage && !heroIsDeaf()) {
        messages.push(`You hear a boulder ${verb}.`);
    }
    const buriedDebt = buriedMerchandiseDebtMessage(x, y, obj);
    deleteBoulderFillFloorTrap(trap, x, y);
    if (game.u?.ux === x && game.u?.uy === y && game.u.utraptype !== TT_BURIEDBALL) {
        game.u.utrap = 0;
        game.u.utraptype = null;
    }
    messages.push(...buryObjectsAt(x, y, { ignore: obj }));
    if (buriedDebt) messages.push(buriedDebt);
    if (usedUpShopBillOnDestroy) markObjectShopBillUsedUp(obj);
    newsym(x, y);
    return true;
}

function earthFloorLiquidType(loc) {
    if (!loc) return null;
    if (loc.typ === DRAWBRIDGE_UP) return earthLiquidUnderDrawbridge(loc);
    return loc.typ;
}

function floorEffectsObjectWeight(obj) {
    if (obj?.owt != null) return obj.owt;
    if (obj?.weight != null) return obj.weight;
    if (obj?.otyp === ROCK || objectKindKey(obj) === 'rock') return 10;
    return 10 * Math.max(1, obj?.quan || 1);
}

function maybeDroppedObjectPoolFeedback(obj, x, y, messages) {
    if (!game.u || game.u.ux !== x || game.u.uy !== y || heroIsDeaf()) return;
    const floating = !!(game.u.levitation || game.u.Levitation || game.u.flying || game.u.Flying);
    if (!game.u.blind && !floating) return;
    if (!(game.u.underwater || game.u.uunderwater)) {
        if (floorEffectsObjectWeight(obj) > 9) messages.push('Splash!');
        else if (floating) messages.push('Plop!');
    }
    newsym(x, y);
}

function droppedObjectWaterFloorEffects(obj, x, y, messages, {
    usedUpShopBillOnDestroy = false,
} = {}) {
    let destroyed = false;
    maybeDroppedObjectPoolFeedback(obj, x, y, messages);
    waterDamageFloorItem(obj, messages, floorObjectVisible(x, y), { count: 0 }, {
        removeObject: () => { destroyed = true; },
        usedUpShopBillOnDestroy,
    });
    return destroyed;
}

function droppedObjectLavaFloorEffects(obj, x, y, messages, {
    usedUpShopBillOnDestroy = false,
} = {}) {
    let destroyed = false;
    const visible = floorObjectVisible(x, y);
    const acidContext = { count: 0 };
    const processed = new Set();
    const processSpill = item => {
        if (processed.has(item)) return false;
        processed.add(item);
        if (!(game.level?.objects || []).includes(item)) return false;
        return liquidFlowFloorEffectsItem(item, x, y, messages, visible, acidContext, [], processSpill, {
            usedUpShopBillOnDestroy,
        });
    };
    lavaDamageFloorEffectItem(obj, messages, visible, {
        removeObject: () => { destroyed = true; },
        processSpill,
        usedUpShopBillOnDestroy,
        x,
        y,
    });
    return destroyed;
}

function droppedObjectHotGroundFloorEffects(obj, x, y, messages, {
    usedUpShopBillOnDestroy = false,
} = {}) {
    let destroyed = false;
    hotGroundPotionFloorEffect(obj, x, y, messages, floorObjectVisible(x, y), {
        removeObject: () => { destroyed = true; },
        usedUpShopBillOnDestroy,
    });
    return destroyed;
}

function floorEffectsTeeteringPit(trap, x, y) {
    return !!(trap && (trap.ttyp === PIT || trap.ttyp === SPIKED_PIT) && trap.tseen
        && game.u?.ux === x && game.u?.uy === y
        && !(game.u?.utrap && isPitTrapType(game.u?.utraptype)));
}

function floorEffectsEscapedShaft(trap, x, y) {
    return !!(trap && (trap.ttyp === HOLE || trap.ttyp === TRAPDOOR) && trap.tseen
        && game.u?.ux === x && game.u?.uy === y);
}

function floorEffectsObjectVerb(obj, singular, plural) {
    return (obj?.quan || 1) > 1 ? plural : singular;
}

function droppedObjectPitHoleFloorEffects(obj, x, y, messages) {
    const trap = boulderFillTrapAt(x, y);
    if (floorEffectsTeeteringPit(trap, x, y)) {
        if (game.u?.blind && !heroIsDeaf()) {
            messages.push(`You hear ${floorObjectTheName(obj)} tumble downwards.`);
        } else {
            const whose = trap.madeby_u ? 'your' : 'the';
            messages.push(`${floorObjectSubject(obj)} ${floorEffectsObjectVerb(obj, 'tumbles', 'tumble')} into ${whose} pit.`);
        }
        return { handled: true, consumed: false };
    }
    if (!floorEffectsEscapedShaft(trap, x, y)) return { handled: false, consumed: false };
    if (obj === game.u?.uball || obj === game.u?.uchain) return { handled: true, consumed: false };
    if (!canFallThroughLevel(game.u?.uz)) return { handled: true, consumed: false };
    if (rn2(3)) return { handled: true, consumed: false };
    const target = sitFallTargetLevel(trap);
    if (!target) return { handled: true, consumed: false };
    if (floorObjectVisible(x, y)) {
        const gate = trap.ttyp === TRAPDOOR ? 'through the trap door' : 'through the hole';
        messages.push(`${floorObjectSubject(obj)} ${floorEffectsObjectVerb(obj, 'falls', 'fall')} ${gate}.`);
    }
    const debt = shipObjectShopDebt(obj, x, y);
    if (debt.message) messages.push(debt.message);
    const breakKind = impactDropObjectBreaks(obj);
    if (breakKind) {
        messages.push(`You hear a muffled ${shipObjectMuffledBreakResult(breakKind)}.`);
        newsym(x, y);
        return { handled: true, consumed: true };
    }
    queueImpactDroppedObjects(target, [obj]);
    newsym(x, y);
    return { handled: true, consumed: true };
}

function globTypeForObject(obj) {
    if (!obj) return null;
    if (GLOB_TYPE_BY_OTYP.has(obj.otyp)) return GLOB_TYPE_BY_OTYP.get(obj.otyp);
    let name = String(obj.globName || obj.actualKind || obj.kind || '').toLowerCase();
    name = name.replace(/^partly eaten\s+/, '').replace(/^(?:small|medium|large|very large)\s+/, '');
    const match = name.match(/^glob of (.+)$/);
    return GLOB_TYPES.get(match?.[1] || name) || null;
}

function isGlobbyObject(obj) {
    return !!(obj?.globby || globTypeForObject(obj));
}

function globsCanMeld(target, obj) {
    const targetType = globTypeForObject(target);
    const objType = globTypeForObject(obj);
    if (!targetType || !objType || targetType.otyp !== objType.otyp) return false;
    if (target === obj || target?.nomerge || obj?.nomerge) return false;
    if (!!target.cursed !== !!obj.cursed || !!target.blessed !== !!obj.blessed) return false;
    if (target.how_lost === 'LOST_EXPLODING' || obj.how_lost === 'LOST_EXPLODING') return false;
    if (target.how_lost && target.how_lost !== 'LOST_NONE' && target.how_lost !== obj.how_lost) return false;
    return true;
}

function floorGlobMeldCandidateAt(obj, x, y) {
    return (game.level?.objects || []).find(candidate =>
        candidate !== obj && !candidate.buried && !candidate.transientProjectile
        && candidate.ox === x && candidate.oy === y
        && globsCanMeld(candidate, obj)) || null;
}

function floorGlobMeldTarget(obj, x, y) {
    const here = floorGlobMeldCandidateAt(obj, x, y);
    if (here) return here;

    const dx = rn2(2) ? -1 : 1;
    const dy = rn2(2) ? -1 : 1;
    const ex = x - dx;
    const ey = y - dy;
    for (let fx = ex; Math.abs(fx - ex) < 3; fx += dx) {
        for (let fy = ey; Math.abs(fy - ey) < 3; fy += dy) {
            if (fx < 0 || fx >= COLNO || fy < 0 || fy >= ROWNO || (fx === x && fy === y)) continue;
            const target = floorGlobMeldCandidateAt(obj, fx, fy);
            if (target) return target;
        }
    }
    return null;
}

function globMeldWeight(obj) {
    return Math.max(1, obj?.oeaten || obj?.owt || 20);
}

function globMeldRemainingTurns(obj) {
    const turn = obj?.globShrinkTurn;
    if (typeof turn !== 'number') return 25;
    return Math.max(1, turn - (game.moves || 0));
}

function syncGlobObjectFields(obj) {
    const globType = globTypeForObject(obj);
    if (!globType) return;
    Object.assign(obj, {
        otyp: globType.otyp,
        cls: 'food',
        glyph: '%',
        color: globType.color,
        _display_color: globType.color,
        kind: globType.name,
        actualKind: globType.name,
        singular: globType.name,
        globName: globType.name,
        globby: true,
        quan: 1,
        known: obj.known ?? true,
        dknown: obj.dknown ?? true,
    });
}

function unpaidBillPrice(obj) {
    const price = Number(obj?.unpaidPrice || 0);
    return Number.isFinite(price) ? Math.max(0, price) : 0;
}

function syncUnpaidBillLine(obj) {
    const price = unpaidBillPrice(obj);
    if (!obj?.line || !price) return;
    const suffix = ` (unpaid, ${price} zorkmid${price === 1 ? '' : 's'})`;
    if (/ \(unpaid, \d+ zorkmids?\)/.test(obj.line))
        obj.line = obj.line.replace(/ \(unpaid, \d+ zorkmids?\)/, suffix);
    else obj.line = `${obj.line}${suffix}`;
}

function combineAbsorbedGlobBill(absorber, absorbed) {
    const total = unpaidBillPrice(absorber) + unpaidBillPrice(absorbed);
    if (!total) return;
    absorber.unpaid = true;
    absorber.unpaidPrice = total;
    syncUnpaidBillLine(absorber);
}

function absorbGlobObject(absorber, absorbed) {
    const w1 = globMeldWeight(absorber);
    const w2 = globMeldWeight(absorbed);
    const moves = game.moves || 0;
    const age1 = typeof absorber.age === 'number' ? absorber.age : moves;
    const age2 = typeof absorbed.age === 'number' ? absorbed.age : moves;

    if (!!absorber.bknown !== !!absorbed.bknown) absorber.bknown = false;
    if (!!absorber.rknown !== !!absorbed.rknown) absorber.rknown = false;
    if (!!absorber.greased !== !!absorbed.greased) absorber.greased = false;
    if (absorber.orotten || absorbed.orotten) absorber.orotten = true;
    combineAbsorbedGlobBill(absorber, absorbed);

    absorber.age = moves - Math.trunc((((moves - age1) * w1) + ((moves - age2) * w2)) / (w1 + w2));
    absorber.owt = Math.max(1, absorber.owt || w1) + w2;
    if (absorber.oeaten || absorbed.oeaten) absorber.oeaten = w1 + w2;
    absorber.globShrinkTurn = moves + Math.trunc((globMeldRemainingTurns(absorber) + globMeldRemainingTurns(absorbed) + 1) / 2);
    syncGlobObjectFields(absorber);
    if (game.level?.objects?.includes(absorbed))
        game.level.objects = game.level.objects.filter(candidate => candidate !== absorbed);
}

function globPluralName(obj) {
    const globType = globTypeForObject(obj);
    return globType ? `${globType.name.replace(/^glob\b/, 'globs')}` : 'globs';
}

function droppedObjectGlobMeldFloorEffects(obj, x, y, messages) {
    if (!isGlobbyObject(obj)) return false;
    const target = floorGlobMeldTarget(obj, x, y);
    if (!target) return false;

    pushGlobMeldMessage(obj, target, x, y, messages);
    absorbGlobObject(target, obj);
    newsym(x, y);
    newsym(target.ox, target.oy);
    return true;
}

function pushGlobMeldMessage(obj, target, x, y, messages) {
    if (!messages) return;
    const visible = !game.u?.blind && (cansee(x, y) || cansee(target.ox, target.oy));
    if (visible) {
        if (heroIsHallucinating()) {
            messages.push('You see parts of the floor melting!');
        } else {
            const adjacent = (x !== game.u?.ux || y !== game.u?.uy)
                && (target.ox !== game.u?.ux || target.oy !== game.u?.uy);
            messages.push(`The ${adjacent ? 'adjacent ' : ''}${globPluralName(obj)} coalesce.`);
        }
    } else if (!heroIsDeaf()) {
        messages.push('You hear a faint sloshing sound.');
    }
}

function globShrinkDelay() {
    return 25 + rn2(5) - 2;
}

function startGlobShrinkTimeout(obj, when = 0) {
    if (!isGlobbyObject(obj)) return;
    const delay = when > 0 ? when : globShrinkDelay();
    obj.globShrinkTurn = Math.max(game.moves || 0, 1) + delay;
}

function globContents(obj) {
    const contents = Array.isArray(obj?.contents) ? obj.contents : [];
    const cobj = Array.isArray(obj?.cobj) ? obj.cobj : [];
    if (contents.length && cobj.length) {
        const seen = new Set();
        const merged = [];
        for (const item of [...contents, ...cobj]) {
            const key = item?.id != null ? `id:${item.id}` : item;
            if (seen.has(key)) continue;
            seen.add(key);
            merged.push(item);
        }
        return merged;
    }
    if (contents.length) return contents;
    if (cobj.length) return cobj;
    if (Array.isArray(obj?.contents)) return obj.contents;
    if (Array.isArray(obj?.cobj)) return obj.cobj;
    return [];
}

function normalizeContainedObjectParents(container, seen = new Set()) {
    if (!container || seen.has(container)) return;
    seen.add(container);
    for (const child of globContents(container)) {
        child.contained = true;
        child.container = container;
        normalizeContainedObjectParents(child, seen);
    }
}

function collectGlobShrinkEntriesFromList(list, source, entries, {
    parent = null,
    root = null,
    ancestors = [],
    owner = null,
} = {}) {
    for (const obj of list || []) {
        const top = root || obj;
        if (isGlobbyObject(obj)) entries.push({
            obj,
            parent,
            root: top,
            ancestors: [...ancestors],
            source,
            owner,
        });
        const contents = globContents(obj);
        if (contents.length) {
            collectGlobShrinkEntriesFromList(contents, source, entries, {
                parent: obj,
                root: top,
                ancestors: [...ancestors, obj],
                owner,
            });
        }
    }
}

function collectGlobShrinkEntries(g = game) {
    const entries = [];
    collectGlobShrinkEntriesFromList(g.inventory || [], 'inventory', entries);
    collectGlobShrinkEntriesFromList(g.level?.objects || [], 'floor', entries);
    for (const mon of g.level?.monsters || [])
        collectGlobShrinkEntriesFromList(mon.minvent || [], 'minvent', entries, { owner: mon });
    return entries;
}

function collectCorpseTimerEntriesFromList(list, source, entries, {
    parent = null,
    root = null,
    ancestors = [],
    owner = null,
    seen = new Set(),
} = {}) {
    for (const obj of list || []) {
        if (!obj || seen.has(obj)) continue;
        seen.add(obj);
        const top = root || obj;
        const actualSource = source === 'floor' && top?.buried ? 'buried' : source;
        if (isCorpseItem(obj)) entries.push({
            obj,
            parent,
            root: top,
            ancestors: [...ancestors],
            source: actualSource,
            owner,
        });
        const contents = globContents(obj);
        if (contents.length) {
            collectCorpseTimerEntriesFromList(contents, source, entries, {
                parent: obj,
                root: top,
                ancestors: [...ancestors, obj],
                owner,
                seen,
            });
        }
    }
}

function collectCorpseTimerEntries(g = game) {
    const entries = [];
    const seen = new Set();
    collectCorpseTimerEntriesFromList(g.inventory || [], 'inventory', entries, { seen });
    collectCorpseTimerEntriesFromList(g.level?.objects || [], 'floor', entries, { seen });
    collectCorpseTimerEntriesFromList(g.level?.buriedobjlist || [], 'buried', entries, { seen });
    for (const mon of g.level?.monsters || [])
        collectCorpseTimerEntriesFromList(mon.minvent || [], 'minvent', entries, { owner: mon, seen });
    return entries;
}

const CORPSE_TIMER_ACTION_RANK = { revive: 0, zombify: 1, rot: 2 };

function corpseTimerDue(entry, moves = game.moves || 0) {
    const obj = entry?.obj;
    const timers = [
        ['rot', obj?.rotAwayTurn],
        ['revive', obj?.reviveTurn],
        ['zombify', obj?.zombifyTurn],
    ].filter(([, turn]) => typeof turn === 'number' && turn <= moves);
    if (!timers.length) return null;
    timers.sort((a, b) => a[1] - b[1] || CORPSE_TIMER_ACTION_RANK[a[0]] - CORPSE_TIMER_ACTION_RANK[b[0]]);
    return { action: timers[0][0], turn: timers[0][1] };
}

function corpseInIceBox(entry) {
    return !!(entry?.obj?.inIceBox || entry?.ancestors?.some(isIceBoxObject));
}

function corpseTimerObjectName(obj) {
    return pickupObjectName({ ...(obj || {}), line: '', quan: 1 });
}

function corpseTimerRotMessage(entry) {
    const obj = entry.obj;
    const name = corpseTimerObjectName(obj);
    if (entry.source === 'inventory' && !entry.parent) return `Your ${name} rots away.`;
    if (entry.source === 'inventory' && entry.parent) {
        const container = articlelessObjectName(entry.root || entry.parent);
        return `You smell something rotten in your ${container}.`;
    }
    if (entry.source === 'floor' && !entry.parent && floorObjectVisible(obj.ox, obj.oy))
        return `The ${name} rots away.`;
    return '';
}

function refreshCorpseTimerContainerWeights(entry) {
    if (!entry?.parent) return;
    refreshGlobEntryContainerWeights(entry);
    if (entry.root) refreshTipContainerWeight(entry.root);
}

function removeCorpseTimerObject(entry) {
    const obj = entry.obj;
    rememberUsedUpShopBill(obj);
    if (entry.parent) {
        removeContainedObject(entry.parent, obj);
        refreshCorpseTimerContainerWeights(entry);
        return;
    }
    if (entry.source === 'inventory') {
        game.inventory = (game.inventory || []).filter(item => item !== obj);
        game._pet_food_scan_inventory = game.inventory;
        return;
    }
    if (entry.source === 'floor') {
        removeFloorObject(obj);
        if (typeof obj.ox === 'number' && typeof obj.oy === 'number') newsym(obj.ox, obj.oy);
        return;
    }
    if (entry.source === 'buried') {
        if (game.level?.buriedobjlist)
            game.level.buriedobjlist = game.level.buriedobjlist.filter(item => item !== obj);
        if (game.level?.objects)
            game.level.objects = game.level.objects.filter(item => item !== obj);
        if (typeof obj.ox === 'number' && typeof obj.oy === 'number') newsym(obj.ox, obj.oy);
        return;
    }
    if (entry.source === 'minvent' && entry.owner)
        entry.owner.minvent = (entry.owner.minvent || []).filter(item => item !== obj);
}

function rotCorpseTimerEntry(entry, { silent = false } = {}) {
    const messages = [];
    if (!silent) {
        const message = corpseTimerRotMessage(entry);
        if (message) messages.push(message);
    }
    removeCorpseTimerObject(entry);
    return messages;
}

function scheduleCorpseFallbackRot(obj) {
    clearCorpseTimeout(obj);
    const moves = game.moves || 0;
    const age = obj?.age ?? moves;
    const delay = Math.max(1, d(5, 50) - Math.max(0, moves - age));
    obj.rotAwayTurn = moves + delay;
}

function corpseTimerMonsterData(obj) {
    const saved = savedCorpstatMonsterTraits(obj);
    if (saved?.data && typeof saved.data === 'object' && saved.data.name) return saved.data;
    if (typeof saved?.data === 'string') {
        const savedName = saved.data.toLowerCase();
        const savedData = monsterByRndName(savedName) || RANDOM_MONSTER_BY_NAME.get(savedName);
        if (savedData) return savedData;
    }
    if (obj?.corpsenm?.name) return obj.corpsenm;
    const name = corpseName(obj);
    return name ? monsterByRndName(name) || RANDOM_MONSTER_BY_NAME.get(name) || null : null;
}

function isRiderCorpseTimerObject(obj) {
    const name = corpseName(obj);
    return !!(obj?.corpsenm?.rider || name === 'death' || name === 'pestilence' || name === 'famine');
}

function applyCorpseGenderToRevivedMonster(mon, obj) {
    const gender = (obj?.spe || 0) & CORPSTAT_GENDER;
    if (gender === CORPSTAT_MALE) mon.female = false;
    else if (gender === CORPSTAT_FEMALE) mon.female = true;
}

function corpseTimerLocation(entry) {
    if (entry.source === 'inventory') {
        return { x: game.u?.ux || 0, y: game.u?.uy || 0, carried: true };
    }
    if (entry.source === 'minvent' && entry.owner) {
        return { x: entry.owner.mx || 0, y: entry.owner.my || 0, carried: true };
    }
    const root = entry.root || entry.obj;
    if (typeof root?.ox === 'number' && typeof root?.oy === 'number')
        return { x: root.ox, y: root.oy, carried: false };
    if (typeof entry.obj?.ox === 'number' && typeof entry.obj?.oy === 'number')
        return { x: entry.obj.ox, y: entry.obj.oy, carried: false };
    return { x: game.u?.ux || 0, y: game.u?.uy || 0, carried: true };
}

function corpseRevivalSpot(entry, data) {
    const loc = corpseTimerLocation(entry);
    if (loc.carried || entry.parent) return enextoMonsterSpot(loc.x, loc.y, data) || null;
    return { x: loc.x, y: loc.y };
}

function corpseRevivalMessages(entry, mon, data) {
    const messages = [];
    if (entry.source === 'inventory') {
        if (entry.parent) messages.push(`Something writhes out of your ${articlelessObjectName(entry.root || entry.parent)}!`);
        else messages.push('You feel squirming in your backpack!');
    } else if (entry.source === 'minvent') {
        if (!game.u?.blind && couldsee(mon.mx, mon.my))
            messages.push(`Something writhes out of the ${entry.owner?.data?.name || 'monster'}'s pack!`);
    } else if (entry.source === 'floor' && !entry.parent && floorObjectVisible(mon.mx, mon.my)) {
        messages.push(`${sentenceCase(monsterIndefiniteName(data?.name || 'monster'))} rises from the dead!`);
    }
    return messages;
}

function handleCorpseRevivalFailure(entry, { zombified = false } = {}) {
    if (zombified) return rotCorpseTimerEntry(entry, { silent: true });
    const obj = entry.obj;
    if (isRiderCorpseTimerObject(obj) && rn2(99)) {
        clearCorpseTimeout(obj);
        obj.reviveTurn = (game.moves || 0) + riderRevivalDelay(obj, true);
    } else {
        scheduleCorpseFallbackRot(obj);
    }
    return [];
}

async function reviveCorpseTimerEntry(entry, data = corpseTimerMonsterData(entry.obj), options = {}) {
    const obj = entry.obj;
    const savedTraits = savedCorpstatMonsterTraits(obj);
    clearCorpseTimeout(obj);
    if (!data?.name || obj.norevive || isMonsterGenocidedName(data.name))
        return handleCorpseRevivalFailure(entry, options);
    const spot = corpseRevivalSpot(entry, data);
    if (!spot) return handleCorpseRevivalFailure(entry, options);
    const flags = NO_MINVENT | MM_NOMSG | MM_NOCOUNTBIRTH | MM_NOWAIT
        | (savedTraits ? MM_NOTAIL : 0);
    const mon = await makemon(data, spot.x, spot.y, flags);
    if (!mon) return handleCorpseRevivalFailure(entry, options);

    if (savedTraits) applySavedCorpstatMonsterTraits(mon, savedTraits, data);
    mon.mrevived = 1;
    mon.mundetected = 0;
    mon.m_ap_type = 0;
    mon.appearObj = null;
    mon.appearGlyph = null;
    mon.appearColor = null;
    if (!savedTraits) applyCorpseGenderToRevivedMonster(mon, obj);
    removeCorpseTimerObject(entry);
    newsym(mon.mx, mon.my);
    return corpseRevivalMessages(entry, mon, data);
}

async function zombifyCorpseTimerEntry(entry) {
    const obj = entry.obj;
    const zombieName = zombieFormNameForCorpse(obj);
    const data = zombieName ? monsterByRndName(zombieName) || RANDOM_MONSTER_BY_NAME.get(zombieName) : null;
    clearCorpseTimeout(obj);
    if (!data?.name || isMonsterGenocidedName(data.name))
        return rotCorpseTimerEntry(entry, { silent: true });
    clearSavedCorpstatMonsterTraits(obj);
    obj.corpsenm = data;
    obj.zombifying = false;
    Object.assign(obj, object_display(obj));
    return reviveCorpseTimerEntry(entry, data, { zombified: true });
}

async function processCorpseTimerEntry(entry) {
    if (!isCorpseItem(entry.obj)) return [];
    if (corpseInIceBox(entry)) {
        clearCorpseTimeout(entry.obj);
        return [];
    }
    const due = corpseTimerDue(entry);
    if (!due) return [];
    if (due.action === 'rot') return rotCorpseTimerEntry(entry);
    if (due.action === 'zombify') return zombifyCorpseTimerEntry(entry);
    return reviveCorpseTimerEntry(entry);
}

export async function processCorpseTimers(g = game) {
    const moves = g.moves || 0;
    const due = collectCorpseTimerEntries(g)
        .map(entry => ({ entry, due: corpseTimerDue(entry, moves) }))
        .filter(item => item.due)
        .sort((a, b) => a.due.turn - b.due.turn
            || CORPSE_TIMER_ACTION_RANK[a.due.action] - CORPSE_TIMER_ACTION_RANK[b.due.action]
            || ((a.entry.obj.id || a.entry.obj.oid || 0) - (b.entry.obj.id || b.entry.obj.oid || 0)));
    const messages = [];
    for (const { entry } of due)
        messages.push(...await processCorpseTimerEntry(entry));
    return messages;
}

function globInIceBox(entry) {
    return !!(entry.obj?.inIceBox || entry.ancestors.some(isIceBoxObject));
}

function globIceState(entry) {
    if (entry.source !== 'floor') return 'none';
    const root = entry.root || entry.obj;
    const x = root?.ox;
    const y = root?.oy;
    if (typeof x !== 'number' || typeof y !== 'number') return 'none';
    const loc = game.level?.at?.(x, y);
    if (loc?.typ !== ICE) return 'none';
    return root.buried ? 'buried' : 'onice';
}

function removeShrunkGlob(entry) {
    const obj = entry.obj;
    rememberUsedUpShopBill(obj);
    if (entry.parent) {
        removeContainedObject(entry.parent, obj);
        refreshGlobEntryContainerWeights(entry);
        return;
    }
    if (entry.source === 'inventory') {
        game.inventory = (game.inventory || []).filter(item => item !== obj);
        return;
    }
    if (entry.source === 'floor') {
        game.level.objects = (game.level?.objects || []).filter(item => item !== obj);
        if (typeof obj.ox === 'number' && typeof obj.oy === 'number') newsym(obj.ox, obj.oy);
        return;
    }
    if (entry.source === 'minvent' && entry.owner)
        entry.owner.minvent = (entry.owner.minvent || []).filter(item => item !== obj);
}

function outerInventoryContainer(entry) {
    if (entry.source !== 'inventory' || !entry.parent) return null;
    return entry.root || entry.parent;
}

function usedUpShopBillName(obj) {
    let name = String(obj?.line || '')
        .replace(/^[a-zA-Z$] - /, '')
        .replace(/ \(unpaid, \d+ zorkmids?\)/, '');
    if (name) return name;
    const baseName = pickupObjectName({ ...(obj || {}), quan: 1 });
    return `${/^[aeiou]/i.test(baseName) ? 'an' : 'a'} ${baseName}`;
}

function rememberUsedUpShopBill(obj) {
    const { shkp, entry: billEntry } = shopkeeperOwningBillEntry(obj);
    const price = shopBillEntryTotal(billEntry);
    if (!shkp || !billEntry || !(price > 0)) return;
    const name = usedUpShopBillName(obj);
    billEntry.useup = true;
    clearObjectShopBillState(obj);
    billEntry.name = name;
    billEntry.shopkeeperId = shopkeeperIdentity(shkp);
    if (usedUpShopBillTrackerForId(billEntry.bo_id, shkp)) return;
    game._usedUpShopBills ??= [];
    game._usedUpShopBills.push({
        name,
        price,
        bo_id: billEntry.bo_id,
        shopkeeperId: shopkeeperIdentity(shkp),
    });
}

function objectWeightKind(obj) {
    const kind = String(obj?.actualKind || obj?.kind || obj?.spellName || obj?.spell?.name || '').toLowerCase();
    if (/war hammer|mjollnir/.test(kind)) return 'war hammer';
    if (!kind && isBoulderObject(obj)) return 'boulder';
    if (!kind && obj?.otyp === LARGE_BOX) return 'large box';
    if (!kind && obj?.otyp === CHEST) return 'chest';
    if (!kind && obj?.otyp === ICE_BOX) return 'ice box';
    if (!kind && obj?.otyp === SACK) return 'sack';
    if (!kind && obj?.otyp === OILSKIN_SACK) return 'oilskin sack';
    if (!kind && obj?.otyp === BAG_OF_HOLDING) return 'bag of holding';
    if (!kind && obj?.otyp === BAG_OF_TRICKS) return 'bag of tricks';
    return kind;
}

function objectWeightClass(obj) {
    return obj?.cls || (obj?.otyp === RING_CLASS ? 'ring'
        : obj?.otyp === SCROLL_CLASS || obj?.otyp === SCR_SCARE_MONSTER ? 'scroll'
            : obj?.otyp === POTION_CLASS ? 'potion'
                : obj?.otyp === WAND_CLASS ? 'wand'
                    : obj?.otyp === GEM_CLASS ? 'gem'
                        : obj?.otyp === FOOD_CLASS ? 'food'
                            : obj?.otyp === AMULET_CLASS ? 'amulet' : '');
}

function containerBaseWeight(obj) {
    const kind = objectWeightKind(obj);
    if (obj?.otyp === LARGE_BOX || kind === 'large box') return 350;
    if (obj?.otyp === CHEST || kind === 'chest') return 600;
    if (obj?.otyp === ICE_BOX || kind === 'ice box') return 900;
    if (obj?.otyp === SACK || obj?.otyp === OILSKIN_SACK
        || obj?.otyp === BAG_OF_HOLDING || obj?.otyp === BAG_OF_TRICKS
        || kind === 'sack' || kind === 'oilskin sack'
        || kind === 'bag of holding' || kind === 'bag of tricks')
        return 15;
    return null;
}

function isGlobWeightContainerObject(obj) {
    const kind = objectWeightKind(obj);
    return obj?.otyp === LARGE_BOX || obj?.otyp === CHEST || obj?.otyp === ICE_BOX
        || obj?.otyp === SACK || obj?.otyp === OILSKIN_SACK
        || obj?.otyp === BAG_OF_HOLDING || obj?.otyp === BAG_OF_TRICKS
        || kind === 'large box' || kind === 'chest' || kind === 'ice box'
        || kind === 'sack' || kind === 'oilskin sack'
        || kind === 'bag of holding' || kind === 'bag of tricks';
}

const WISHED_OBJECT_WEIGHT_OVERRIDES = new Map([
    ['bell', 30],
    ['bell of opening', 10],
    ['book of the dead', 20],
    ['heavy iron ball', WT_IRON_BALL_BASE],
    ['horn of plenty', 18],
    ['tooled horn', 18],
]);

function globObjectWeight(obj) {
    if (isGlobbyObject(obj)) return Math.max(0, obj.owt ?? 20);
    if (obj?.otyp === GOLD_PIECE || obj?.cls === 'coin' || obj?.glyph === '$')
        return Math.max(1, Math.trunc(((obj.quan || 1) + 50) / 100));
    const contents = globContents(obj);
    if (contents.length || isGlobWeightContainerObject(obj)) {
        let contentsWeight = contents.reduce((sum, item) => sum + globObjectWeight(item), 0);
        if (isBagOfHoldingObject(obj)) {
            contentsWeight = obj.cursed ? contentsWeight * 2
                : obj.blessed ? Math.trunc((contentsWeight + 3) / 4)
                    : Math.trunc((contentsWeight + 1) / 2);
        }
        const baseKind = objectWeightKind(obj);
        const base = containerBaseWeight(obj) ?? OBJECT_WEIGHTS[baseKind] ?? CLASS_WEIGHTS[objectWeightClass(obj)] ?? 0;
        return Math.max(0, base + contentsWeight);
    }
    const kind = objectWeightKind(obj);
    const cls = objectWeightClass(obj);
    const unitWeight = OBJECT_WEIGHTS[kind] ?? CLASS_WEIGHTS[cls] ?? obj?.owt ?? 0;
    return Math.max(0, unitWeight * (obj?.quan || 1));
}

function wishedObjectFinalWeight(obj) {
    if (!obj) return 0;
    if (isCandelabrumOfInvocationItem(obj)) {
        const candleWeight = OBJECT_WEIGHTS['tallow candle'] ?? 2;
        return 200 + Math.max(0, Math.trunc(Number(obj.spe || 0))) * candleWeight;
    }
    const kind = objectKindKey(obj);
    const metadata = wishObjectMetadataForItem(obj);
    if (metadata?.unitWeight != null) return Math.max(0, metadata.unitWeight * (obj.quan || 1));
    const override = WISHED_OBJECT_WEIGHT_OVERRIDES.get(kind);
    if (override != null) return Math.max(0, override * (obj.quan || 1));
    return globObjectWeight(obj);
}

function heroCarriedWeight() {
    let weight = Math.max(0, Math.trunc(((game._goldCount || 0) + 50) / 100));
    for (const item of game.inventory || []) {
        if (isGoldObject(item)) continue;
        weight += globObjectWeight(item);
    }
    return weight;
}

function heroCarryCapacity() {
    const stats = game.u?.acurr?.a || [];
    const normalCapacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
    return Is_airlevel(game.u?.uz) || game.u?.levitating ? 1000 : normalCapacity;
}

const PICKUP_BURDEN_LEVELS = {
    unencumbered: 0,
    burdened: 1,
    stressed: 2,
    strained: 3,
    overtaxed: 4,
    overloaded: 5,
};

function heroEncumbranceForWeight(weight) {
    const capacity = heroCarryCapacity();
    const burden = Math.trunc(Number(weight || 0)) - capacity;
    if (burden <= 0) return 0;
    if (capacity <= 1) return OVERLOADED;
    return Math.min(Math.trunc(burden * 2 / capacity) + 1, OVERLOADED);
}

function pickupBurdenLimit() {
    const value = String(game.flags?.pickup_burden || 'stressed').toLowerCase();
    return PICKUP_BURDEN_LEVELS[value] ?? PICKUP_BURDEN_LEVELS.stressed;
}

function containerTakeoutBurdenPrefix(encumbrance) {
    if (encumbrance >= 4) return 'You have extreme difficulty';
    if (encumbrance >= 3) return 'You have much trouble';
    if (encumbrance >= 2) return 'You have trouble';
    return 'You have a little trouble';
}

function containerTakeoutObjectView(obj, count) {
    const quantity = Math.max(1, Math.trunc(Number(count || obj?.quan || 1)));
    return quantity === Math.max(1, Math.trunc(Number(obj?.quan || 1)))
        ? obj
        : { ...obj, quan: quantity };
}

function containerTakeoutWeightIncrease(obj, count, goldCount = game._goldCount || 0) {
    const quantity = Math.max(1, Math.trunc(Number(count || obj?.quan || 1)));
    if (shopBillableGold(obj)) {
        const oldGoldWeight = Math.max(0, Math.trunc((Math.max(0, Math.trunc(Number(goldCount || 0))) + 50) / 100));
        const newGoldWeight = Math.max(0, Math.trunc((Math.max(0, Math.trunc(Number(goldCount || 0))) + quantity + 50) / 100));
        return newGoldWeight - oldGoldWeight;
    }
    return globObjectWeight(containerTakeoutObjectView(obj, quantity));
}

function containerTakeoutLiftableCount(container, obj, currentWeight, goldCount = game._goldCount || 0) {
    const count = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if ((game.inventory || []).includes(container)) return count;
    const maxWeight = heroCarryCapacity() * 3;
    if (currentWeight + containerTakeoutWeightIncrease(obj, count, goldCount) <= maxWeight)
        return count;
    if (count <= 1 || obj?.otyp === LOADSTONE || objectKindKey(obj) === 'loadstone' || globContents(obj).length)
        return 0;

    let low = 0;
    let high = count;
    while (low < high) {
        const mid = Math.ceil((low + high) / 2);
        if (currentWeight + containerTakeoutWeightIncrease(obj, mid, goldCount) <= maxWeight)
            low = mid;
        else high = mid - 1;
    }
    return low;
}

function floorPickupObjectKey(obj) {
    if (!obj) return null;
    return obj.id ?? obj.o_id
        ?? `${obj.ox},${obj.oy},${obj.otyp},${obj.kind || obj.actualKind || ''}`;
}

function splitFloorPickupObjectForLift(obj, count) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    const takeCount = Math.max(1, Math.min(quantity, Math.trunc(Number(count || quantity))));
    if (isLoadstoneObject(obj)) return obj;
    if (!obj || takeCount >= quantity) return obj;
    const lifted = { ...obj, id: next_ident(), quan: takeCount };
    delete lifted.o_id;
    delete lifted._shopBillObjectId;
    delete lifted.letter;
    delete lifted.line;
    obj.quan = quantity - takeCount;
    return lifted;
}

function isLoadstoneObject(obj) {
    return !!obj && (obj.otyp === LOADSTONE || objectKindKey(obj) === 'loadstone');
}

function isBoulderObject(obj) {
    return !!obj && (obj.otyp === BOULDER || objectKindKey(obj) === 'boulder');
}

function normalizeBoulderObject(obj) {
    if (!isBoulderObject(obj)) return obj;
    obj.otyp = BOULDER;
    obj.cls = 'rock';
    obj.glyph = '`';
    obj.kind ||= 'boulder';
    obj.actualKind ||= 'boulder';
    obj.quan = Math.max(1, Math.trunc(Number(obj.quan || 1)));
    obj.owt = 6000 * obj.quan;
    return obj;
}

const ROCK_THROWING_FORM_NAMES = new Set([
    'giant', 'stone giant', 'hill giant', 'fire giant', 'frost giant',
    'storm giant', 'titan', 'cyclops', 'lord surtur',
]);

function heroThrowsRocks() {
    const form = game.u?._polyself_form || game.u?._polyself_base || {};
    const name = String(form.name || '').toLowerCase();
    return !!(form.throwsRocks || form.rockThrower || ROCK_THROWING_FORM_NAMES.has(name));
}

function carriedBoulderObject() {
    return (game.inventory || []).find(item => isBoulderObject(item));
}

function carriedLoadstoneObject() {
    return (game.inventory || []).find(item => isLoadstoneObject(item));
}

function noSlotSpecialPickupMessage(obj) {
    const name = pickupObjectName(obj);
    return `You are carrying too much stuff to pick up ${(obj?.quan || 1) === 1 ? 'another' : 'more'} ${name}.`;
}

function boulderSokobanPickupMessage(obj) {
    return `You cannot get your hand around this ${pickupObjectName({ ...obj, quan: 1 })}.`;
}

function boulderTooHeavyPickupMessage(obj) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    const phrase = pickupObjectPhrase(obj);
    const hereVerb = quantity === 1 ? 'is' : 'are';
    if ((game.inventory || []).length || (game._goldCount || 0))
        return `There ${hereVerb} ${phrase} here, but you cannot lift any more.`;
    return `There ${hereVerb} ${phrase} here, but ${quantity === 1 ? 'it' : 'even one'} is too heavy for you to lift.`;
}

function boulderContainerTooHeavyPickupMessage(container, obj) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    const phrase = pickupObjectPhrase(obj);
    const verb = quantity === 1 ? 'is' : 'are';
    if ((game.inventory || []).length || (game._goldCount || 0))
        return `There ${verb} ${phrase} in ${containerObjectPhrase(container)}, but you cannot carry any more.`;
    return `There ${verb} ${phrase} in ${containerObjectPhrase(container)}, but ${quantity === 1 ? 'it' : 'even one'} is too heavy for you to carry.`;
}

function curseLoadstoneLeavingInventory(obj) {
    if (!isLoadstoneObject(obj)) return;
    obj.cursed = true;
    obj.blessed = false;
}

function cursedLoadstoneLetGoMessage(obj, word = 'drop') {
    if (obj) obj.bknown = true;
    const plural = Math.max(1, Math.trunc(Number(obj?.quan || 1))) > 1;
    return `For some reason, you cannot ${word} ${plural ? 'any of ' : ''}the stone${plural ? 's' : ''}!`;
}

function isScareMonsterScrollObject(obj) {
    if (!obj) return false;
    if (obj.otyp === SCR_SCARE_MONSTER || obj.scrollIndex === 3) return true;
    const raw = String(obj.actualKind || obj.kind || '').toLowerCase()
        .replace(/^scroll(?::| of | labeled )/, '');
    return raw === 'scare monster';
}

function normalizeScareMonsterScrollObject(obj) {
    if (!isScareMonsterScrollObject(obj)) return obj;
    obj.cls = 'scroll';
    obj.glyph = '?';
    obj.scrollIndex = 3;
    obj.actualKind ||= 'scroll of scare monster';
    return obj;
}

function scareMonsterScrollDustMessage(obj) {
    const plural = Math.max(1, Math.trunc(Number(obj?.quan || 1))) > 1;
    return `The scroll${plural ? 's' : ''} ${plural ? 'turn' : 'turns'} to dust as you pick ${plural ? 'them' : 'it'} up.`;
}

function floorUsedUpShopBillMessage(obj, price) {
    if (!obj || !(price > 0)) return '';
    const units = Math.max(1, Math.trunc(Number(shopPricingUnits(obj) || 1)));
    const unitPrice = units > 1 ? Math.max(1, Math.trunc(price / units)) : Math.trunc(price);
    const subject = units > 1
        ? pickupObjectPhrase(obj)
        : pickupObjectName({ ...(obj || {}), quan: 1 });
    return `The ${subject} will cost you ${unitPrice} ${shopCurrency(unitPrice)}${units > 1 ? ' each' : ''}.`;
}

function useUpFloorObjectWithShopBill(obj) {
    if (!obj) return '';
    const x = obj.ox ?? game.u?.ux;
    const y = obj.oy ?? game.u?.uy;
    const shkp = x == null || y == null ? null : shopkeeperForCostlySpot(x, y);
    let billMessage = '';
    if (shopkeeperInHisShop(shkp)) {
        const price = shopItemPrice(obj, x, y);
        if (price > 0) {
            addObjectToShopBill(shkp, obj, price, { useup: true });
            markObjectShopBillUsedUp(obj, shkp);
            billMessage = floorUsedUpShopBillMessage(obj, price);
        }
    }
    game.level.objects = (game.level?.objects || []).filter(item => item !== obj);
    if (x != null && y != null) newsym(x, y);
    return billMessage;
}

function finalizeScareMonsterDust(preflight) {
    const dustObj = preflight?.scareDustObj || preflight;
    return useUpFloorObjectWithShopBill(dustObj);
}

function observeFloorPickupObject(obj) {
    if (!obj || game.u?.blind || heroIsHallucinating()) return;
    obj.dknown = true;
    recordObservedObjectDiscovery(obj);
}

function scheduleScareMonsterDustTryCall(preflight, pending = {}) {
    const dustObj = preflight?.scareDustObj;
    if (!dustObj || !preflight?.scareDustTryCall) return false;
    prepareScrollTryCall(dustObj, 3, pending.moveCost ?? 1);
    game._scroll_trycall_pending = {
        kind: 'scareDust',
        preflight,
        moveCost: pending.moveCost ?? 1,
        pickupListState: pending.pickupListState || null,
    };
    game._command_mode = 'callScrollAfterMore';
    return true;
}

async function finishPendingScrollTryCall(defaultMoveCost = 1) {
    const pending = game._scroll_trycall_pending;
    game._scroll_trycall_pending = null;
    if (!pending) return false;
    let followupMessage = '';
    if (pending.kind === 'scareDust') followupMessage = finalizeScareMonsterDust(pending.preflight);
    if (pending.pickupListState) {
        if (followupMessage) pending.pickupListState.messages.push(followupMessage);
        pending.pickupListState.index = (pending.pickupListState.index || 0) + 1;
        await continuePickupListProcessing(pending.pickupListState);
        return true;
    }
    game.context.move = pending.moveCost ?? defaultMoveCost;
    if (followupMessage) await setMessage(followupMessage);
    return true;
}

function artifactPowerSourceName(obj) {
    const name = String(obj?.artifact || pickupObjectName(obj) || 'the artifact');
    return name.replace(/^The\b/, 'the');
}

function containerTakeoutArtifactTouch(obj) {
    const def = artifactDefinitionForName(obj?.artifact);
    if (!def) return { ok: true, skip: false, messages: [] };
    const status = artifactTouchStatus(def);
    const messages = [];
    const blast = ((status.badClass || status.badAlign) && status.selfWilled)
        || (status.badAlign && !rn2(4));
    if (blast) {
        messages.push(`You are blasted by ${artifactPowerSourceName(obj)}'s power!`);
        const damage = d(heroHasAntimagic() ? 2 : 4, status.selfWilled ? 10 : 4);
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        exerciseAttribute(A_WIS, false);
        if ((game.u?.uhp || 0) <= 0) {
            game._death_cause = `touching ${def.name}`;
            messages.push('You die...');
            return { ok: false, fatal: true, message: messages.join('  '), messages };
        }
    }
    if (status.badClass && status.badAlign && status.selfWilled) {
        messages.push(`${obj.artifact || pickupObjectName(obj)} evades your grasp!`);
        return { ok: true, skip: true, messages };
    }
    return { ok: true, skip: false, messages };
}

function isPetrifyingCorpseObject(obj) {
    if (!(obj?.otyp === CORPSE || obj?.otyp === 'corpse')) return false;
    const name = corpseMonsterName(obj).toLowerCase();
    return name === 'cockatrice' || name === 'chickatrice' || !!obj?.corpsenm?.touchPetrifies;
}

function isRiderCorpseObject(obj) {
    if (!(obj?.otyp === CORPSE || obj?.otyp === 'corpse')) return false;
    const name = corpseMonsterName(obj).toLowerCase();
    return name === 'death' || name === 'famine' || name === 'pestilence';
}

function containerTakeoutFatalCorpseMessage(obj) {
    if (!isPetrifyingCorpseObject(obj) || wornGlovesItem() || game.u?.stoneResistance)
        return '';
    if (heroPolyselfResistsStoning()) return '';
    const polyselfMessage = maybeTurnPolyselfIntoStoneGolem();
    if (polyselfMessage) return '';
    const name = corpseMonsterName(obj).toLowerCase() || 'cockatrice';
    if (game.u) game.u.uhp = 0;
    game._death_cause = `petrified by a ${name} corpse`;
    return `Touching ${pickupObjectPhrase({ ...obj, quan: 1 })} is a fatal mistake.`;
}

function containerTakeoutPreliftCheck(obj) {
    const artifactTouch = containerTakeoutArtifactTouch(obj);
    if (!artifactTouch.ok || artifactTouch.skip) return artifactTouch;
    const fatalMessage = containerTakeoutFatalCorpseMessage(obj);
    if (fatalMessage) return { ok: false, fatal: true, message: fatalMessage };
    return { ok: true, skip: false, messages: artifactTouch.messages || [] };
}

function floorPickupPreflightMessage(preflight) {
    const parts = [...(preflight?.messages || [])];
    const message = preflight?.message || '';
    if (message && message !== parts.join('  ')) parts.push(message);
    return parts.join('  ') || 'You cannot pick that up.';
}

function floorPickupBurdenPromptMessage(preflight) {
    return [...(preflight?.messages || []), preflight?.prompt || 'Continue? [ynq] (q)'].join('  ');
}

function floorPickupAcceptedPreflight(preflight) {
    return { ...(preflight || {}), messages: [], prompt: null };
}

async function floorPickupRiderCorpseRevival(obj) {
    if (!isCorpseItem(obj) || !isRiderCorpseTimerObject(obj)) return null;
    const messages = ['At your touch, the corpse suddenly moves...'];
    messages.push(...await reviveCorpseTimerEntry({ obj, source: 'floor' }));
    exerciseAttribute(A_WIS, false);
    return { ok: false, riderRevival: true, messages };
}

function findFloorPickupInventoryMergeTargetForPreflight(source, sourcePrice = null) {
    if (!pickupObjectCanInventoryMerge(source)) return null;
    const x = source?.ox ?? game.u?.ux;
    const y = source?.oy ?? game.u?.uy;
    const shkp = x == null || y == null ? null : shopkeeperForCostlySpot(x, y);
    const price = sourcePrice != null ? Number(sourcePrice) : shopItemPrice(source, x, y);
    const sourceWillBeUnpaid = Number.isFinite(price) && price > 0 && shopkeeperInHisShop(shkp);
    if (sourceWillBeUnpaid) return null;
    const billing = { shkp, price, sourceWillBeUnpaid };
    for (const target of game.inventory || []) {
        if (!pickedObjectInventoryMergeCompatible(target, source, sourceWillBeUnpaid, shkp)) continue;
        if (!containerTakeoutBillMergeCompatible(source, target, billing)) continue;
        return target;
    }
    return null;
}

function findFloorPickupFoodMergeTargetForPreflight(source, sourcePrice = null) {
    if (!isSimpleMergeableFoodObject(source)) return null;
    return findFloorPickupInventoryMergeTargetForPreflight(source, sourcePrice);
}

async function floorPickupPreflight(obj, { shopPrice = null, prompt = true, scareSpecial = true } = {}) {
    const promptKey = floorPickupObjectKey(obj);
    const cached = game._skip_floor_pickup_preflight_once;
    if (cached && cached.objectId === promptKey) {
        game._skip_floor_pickup_preflight_once = null;
        return { ...(cached.preflight || {}), prompt: null };
    }
    if (cached) game._skip_floor_pickup_preflight_once = null;

    observeFloorPickupObject(obj);
    const prelift = containerTakeoutPreliftCheck(obj);
    if (!prelift.ok || prelift.skip) return prelift;
    const riderRevival = await floorPickupRiderCorpseRevival(obj);
    if (riderRevival) return riderRevival;

    const currentWeight = heroCarriedWeight();
    const gold = Math.max(0, Math.trunc(Number(game._goldCount || 0)));
    const messages = [...(prelift.messages || [])];
    const originalCount = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    if (isBoulderObject(obj)) {
        normalizeBoulderObject(obj);
        if (game.level?.flags?.sokoban_rules) {
            return {
                ok: false,
                move: true,
                message: boulderSokobanPickupMessage(obj),
                messages,
            };
        }
        if (heroThrowsRocks()) {
            const x = obj?.ox ?? game.u?.ux;
            const y = obj?.oy ?? game.u?.uy;
            const price = shopPrice != null ? shopPrice : shopItemPrice(obj, x, y);
            const mergeTarget = findFloorPickupInventoryMergeTargetForPreflight(obj, price);
            const hasSlot = !!simulatedNextInventoryLetters(1);
            if (!hasSlot && carriedBoulderObject() && !mergeTarget) {
                return {
                    ok: false,
                    move: true,
                    message: noSlotSpecialPickupMessage(obj),
                    messages,
                };
            }
            return {
                ok: true,
                skip: false,
                messages,
                takeCount: originalCount,
                liftView: obj,
                prompt: null,
                inventoryLetter: !hasSlot && !mergeTarget ? '#' : null,
            };
        }
    }
    if (isLoadstoneObject(obj)) {
        const x = obj?.ox ?? game.u?.ux;
        const y = obj?.oy ?? game.u?.uy;
        const price = shopPrice != null ? shopPrice : shopItemPrice(obj, x, y);
        const mergeTarget = findFloorPickupInventoryMergeTargetForPreflight(obj, price);
        const hasSlot = !!simulatedNextInventoryLetters(1);
        if (!hasSlot && carriedLoadstoneObject() && !mergeTarget) {
            return {
                ok: false,
                message: noSlotSpecialPickupMessage(obj),
                messages,
            };
        }
        return {
            ok: true,
            skip: false,
            messages,
            takeCount: originalCount,
            liftView: obj,
            prompt: null,
            inventoryLetter: !hasSlot && !mergeTarget ? '#' : null,
        };
    }
    const takeCount = containerTakeoutLiftableCount(null, obj, currentWeight, gold);
    if (takeCount < 1) {
        return {
            ok: false,
            move: isBoulderObject(obj) ? true : undefined,
            message: isBoulderObject(obj)
                ? boulderTooHeavyPickupMessage(obj)
                : `There is ${pickupObjectPhrase(obj)} here, but you cannot carry any more.`,
            messages,
        };
    }
    if (takeCount < originalCount)
        messages.push(`You can only lift ${takeCount === 1 ? 'one' : 'some'} of the ${pickupObjectPhrase(obj)} lying here.`);

    let scareResetSpeOnDecline = false;
    let scareRemainderState = null;
    if (scareSpecial && isScareMonsterScrollObject(obj)) {
        normalizeScareMonsterScrollObject(obj);
        const oldState = {
            blessed: !!obj.blessed,
            cursed: !!obj.cursed,
            spe: obj.spe || 0,
        };
        if (obj.blessed) {
            obj.blessed = false;
            scareRemainderState = oldState;
        } else if (!obj.cursed && !(obj.spe || 0)) {
            obj.spe = 1;
            scareResetSpeOnDecline = true;
            scareRemainderState = oldState;
        } else {
            const dustObj = splitFloorPickupObjectForLift(obj, takeCount);
            normalizeScareMonsterScrollObject(dustObj);
            return {
                ok: false,
                scareDust: true,
                scareDustObj: dustObj,
                scareDustTryCall: shouldTryCallScroll('scare monster', dustObj, 3),
                move: true,
                message: scareMonsterScrollDustMessage(dustObj),
                messages,
            };
        }
    }

    const view = containerTakeoutObjectView(obj, takeCount);
    const x = obj?.ox ?? game.u?.ux;
    const y = obj?.oy ?? game.u?.uy;
    const price = shopPrice != null && takeCount === originalCount ? shopPrice : shopItemPrice(view, x, y);
    const mergeTarget = findFloorPickupFoodMergeTargetForPreflight(view, price)
        || findFloorPickupInventoryMergeTargetForPreflight(view, price);
    if (!shopBillableGold(view) && !mergeTarget && !simulatedNextInventoryLetters(1)) {
        if (scareResetSpeOnDecline) obj.spe = 0;
        return {
            ok: false,
            message: 'Your knapsack cannot accommodate any more items.',
            messages,
        };
    }

    let burdenPrompt = null;
    if (prompt) {
        const burdenLimit = Math.max(heroEncumbranceForWeight(currentWeight), pickupBurdenLimit());
        const nextWeight = currentWeight + containerTakeoutWeightIncrease(obj, takeCount, gold);
        const nextEncumbrance = heroEncumbranceForWeight(nextWeight);
        if (nextEncumbrance > burdenLimit)
            burdenPrompt = `${containerTakeoutBurdenPrefix(nextEncumbrance)} lifting ${pickupObjectPhrase(view)}.  Continue? [ynq] (q)`;
    }

    return {
        ok: true,
        skip: false,
        messages,
        takeCount,
        liftView: view,
        prompt: burdenPrompt,
        scareResetSpeOnDecline,
        scareRemainderState,
    };
}

function containerTakeoutPreflight(container, entries) {
    let currentWeight = heroCarriedWeight();
    let gold = Math.max(0, Math.trunc(Number(game._goldCount || 0)));
    const initialEncumbrance = heroEncumbranceForWeight(currentWeight);
    const burdenLimit = Math.max(initialEncumbrance, pickupBurdenLimit());
    let burdenPrompt = null;
    const messages = [];
    const planned = [];

    for (const entry of entries || []) {
        const obj = entry?.item || entry;
        if (!obj) continue;
        const prelift = containerTakeoutPreliftCheck(obj);
        if (!prelift.ok) return prelift;
        if (prelift.messages?.length) messages.push(...prelift.messages);
        if (prelift.skip) {
            continue;
        }
        const originalCount = Math.max(1, Math.trunc(Number(obj.quan || 1)));
        if (isBoulderObject(obj)) {
            normalizeBoulderObject(obj);
            if (game.level?.flags?.sokoban_rules) {
                return {
                    ok: false,
                    message: boulderSokobanPickupMessage(obj),
                    messages,
                };
            }
            if (heroThrowsRocks()) {
                const view = containerTakeoutObjectView(obj, originalCount);
                const mergeTarget = findContainerTakeoutInventoryMergeTarget(container, view);
                const hasSlot = !!simulatedNextInventoryLetters(1);
                const plannedBoulder = planned.some(item => isBoulderObject(item?.liftView || item?.item));
                if (!hasSlot && (carriedBoulderObject() || plannedBoulder) && !mergeTarget) {
                    return {
                        ok: false,
                        message: noSlotSpecialPickupMessage(obj),
                        messages,
                    };
                }
                const increase = containerTakeoutWeightIncrease(obj, originalCount, gold);
                currentWeight += increase;
                planned.push({
                    ...entry,
                    item: obj,
                    takeCount: originalCount,
                    liftView: view,
                    inventoryLetter: !hasSlot && !mergeTarget ? '#' : null,
                });
                continue;
            }
        }
        if (isLoadstoneObject(obj)) {
            const view = containerTakeoutObjectView(obj, originalCount);
            const mergeTarget = findContainerTakeoutInventoryMergeTarget(container, view);
            const hasSlot = !!simulatedNextInventoryLetters(1);
            if (!hasSlot && carriedLoadstoneObject() && !mergeTarget) {
                return {
                    ok: false,
                    message: noSlotSpecialPickupMessage(obj),
                    messages,
                };
            }
            const increase = containerTakeoutWeightIncrease(obj, originalCount, gold);
            currentWeight += increase;
            planned.push({
                ...entry,
                item: obj,
                takeCount: originalCount,
                liftView: view,
                inventoryLetter: !hasSlot && !mergeTarget ? '#' : null,
            });
            continue;
        }
        const takeCount = containerTakeoutLiftableCount(container, obj, currentWeight, gold);
        if (takeCount < 1) {
            return {
                ok: false,
                message: isBoulderObject(obj)
                    ? boulderContainerTooHeavyPickupMessage(container, obj)
                    : `There is ${pickupObjectPhrase(obj)} in ${containerObjectPhrase(container)}, but you cannot carry any more.`,
            };
        }
        if (takeCount < originalCount) {
            messages.push(`You can only carry ${takeCount === 1 ? 'one' : 'some'} of the ${pickupObjectPhrase(obj)} in ${containerObjectPhrase(container)}.`);
        }
        const view = containerTakeoutObjectView(obj, takeCount);
        const increase = containerTakeoutWeightIncrease(obj, takeCount, gold);
        const nextWeight = currentWeight + increase;
        const nextEncumbrance = heroEncumbranceForWeight(nextWeight);
        if (!burdenPrompt && nextEncumbrance > burdenLimit) {
            burdenPrompt = `${containerTakeoutBurdenPrefix(nextEncumbrance)} removing ${pickupObjectPhrase(view)}.  Continue? [ynq] (q)`;
        }
        currentWeight = nextWeight;
        if (shopBillableGold(obj)) gold += takeCount;
        planned.push({ ...entry, item: obj, takeCount, liftView: view });
    }

    const newSlotCount = planned
        .filter(entry => !entry.inventoryLetter && !shopBillableGold(entry.liftView) && !findContainerTakeoutInventoryMergeTarget(container, entry.liftView))
        .length;
    if (newSlotCount && !simulatedNextInventoryLetters(newSlotCount)) {
        return {
            ok: false,
            message: 'Your knapsack cannot accommodate any more items.',
        };
    }
    return { ok: true, message: '', entries: planned, messages, prompt: burdenPrompt };
}

function globEntryTopContainer(entry) {
    if (!entry?.parent) return null;
    const chain = entry.ancestors?.length ? entry.ancestors : [entry.parent];
    return chain[0] || entry.parent;
}

function refreshGlobEntryContainerWeights(entry, oldTopWeight = null) {
    if (!entry?.parent) return { top: null, oldTopWeight: 0, newTopWeight: 0 };
    const chain = entry.ancestors?.length ? entry.ancestors : [entry.parent];
    const top = globEntryTopContainer(entry);
    oldTopWeight ??= globObjectWeight(top);
    for (let i = chain.length - 1; i >= 0; i--) {
        const container = chain[i];
        if (container) container.owt = globObjectWeight(container);
    }
    return { top, oldTopWeight, newTopWeight: top?.owt ?? globObjectWeight(top) };
}

function globFloorVisible(entry) {
    const obj = entry.obj;
    return entry.source === 'floor' && !entry.parent
        && typeof obj.ox === 'number' && typeof obj.oy === 'number'
        && !game.u?.blind && couldsee(obj.ox, obj.oy);
}

function globShrinkName(obj) {
    return globObjectName(obj).replace(/^the /i, '');
}

function catchUpGlobShrink(entry, iceState) {
    const obj = entry.obj;
    const moves = game.moves || 0;
    const expired = obj.globShrinkTurn || moves;
    let delta = Math.trunc((moves - expired + 24) / 25);
    const nextDelay = 25 - (delta % 25);
    if (iceState === 'onice') delta = Math.trunc((delta + 2) / 3);
    if (delta >= Math.max(0, obj.owt ?? 20)) {
        obj.owt = 0;
        removeShrunkGlob(entry);
        return [];
    }
    obj.owt = Math.max(0, (obj.owt ?? 20) - Math.max(1, delta));
    refreshGlobEntryContainerWeights(entry);
    startGlobShrinkTimeout(obj, nextDelay);
    return [];
}

function processGlobShrinkEntry(entry) {
    const obj = entry.obj;
    if (!isGlobbyObject(obj) || typeof obj.globShrinkTurn !== 'number') return [];
    if (globInIceBox(entry)) {
        delete obj.globShrinkTurn;
        return [];
    }
    const iceState = globIceState(entry);
    if (obj.globShrinkTurn < (game.moves || 0) && iceState !== 'buried')
        return catchUpGlobShrink(entry, iceState);
    if (game._eating_floor_object === obj || iceState === 'buried'
        || (iceState === 'onice' && (game.moves || 0) % 3 === 1)) {
        startGlobShrinkTimeout(obj);
        return [];
    }

    const oldWeight = Math.max(0, obj.owt ?? 20);
    const thresholdMessage = oldWeight > 0 && oldWeight % 10 === 0;
    const name = globShrinkName(obj);
    const topContainer = outerInventoryContainer(entry);
    const oldTopContainerWeight = topContainer ? globObjectWeight(topContainer) : null;
    obj.owt = Math.max(0, oldWeight - 1);
    if ((obj.oeaten || 0) > 1) obj.oeaten -= 1;
    const containerWeights = refreshGlobEntryContainerWeights(entry, oldTopContainerWeight);

    const messages = [];
    const gone = !obj.owt;
    if (entry.source === 'inventory' && !entry.parent && (thresholdMessage || gone))
        messages.push(`Your ${name} ${gone ? 'dissolves completely' : 'shrinks'}.`);
    if (topContainer && (thresholdMessage || gone)) {
        const topName = pickupObjectName(topContainer);
        const verb = containerWeights.newTopWeight !== containerWeights.oldTopWeight ? 'becomes' : 'seems';
        messages.push(`Your ${topName} ${verb}${gone ? '' : ' slightly'} lighter.`);
    }

    if (gone) {
        const visibleFloor = globFloorVisible(entry);
        removeShrunkGlob(entry);
        if (visibleFloor) messages.push(`A ${name} fades away.`);
    } else {
        startGlobShrinkTimeout(obj);
    }
    return messages;
}

export function processGlobShrinkTimers(g = game) {
    const moves = g.moves || 0;
    const due = collectGlobShrinkEntries(g)
        .filter(entry => typeof entry.obj?.globShrinkTurn === 'number'
            && entry.obj.globShrinkTurn <= moves)
        .sort((a, b) => (a.obj.globShrinkTurn - b.obj.globShrinkTurn)
            || ((a.obj.id || a.obj.oid || 0) - (b.obj.id || b.obj.oid || 0)));
    const messages = [];
    for (const entry of due) messages.push(...processGlobShrinkEntry(entry));
    return messages;
}

function collectReturnedLevelGlobShrinkEntries(g = game) {
    const entries = [];
    collectGlobShrinkEntriesFromList(g.level?.objects || [], 'floor', entries);
    for (const mon of g.level?.monsters || [])
        collectGlobShrinkEntriesFromList(mon.minvent || [], 'minvent', entries, { owner: mon });
    return entries;
}

export function catchUpReturnedLevelGlobShrinkTimers(g = game) {
    const moves = g.moves || 0;
    const stale = collectReturnedLevelGlobShrinkEntries(g)
        .filter(entry => typeof entry.obj?.globShrinkTurn === 'number'
            && entry.obj.globShrinkTurn < moves)
        .sort((a, b) => (a.obj.globShrinkTurn - b.obj.globShrinkTurn)
            || ((a.obj.id || a.obj.oid || 0) - (b.obj.id || b.obj.oid || 0)));
    for (const entry of stale) {
        const obj = entry.obj;
        if (!isGlobbyObject(obj) || typeof obj.globShrinkTurn !== 'number') continue;
        if (globInIceBox(entry)) {
            delete obj.globShrinkTurn;
            continue;
        }
        const iceState = globIceState(entry);
        if (iceState === 'buried') continue;
        catchUpGlobShrink(entry, iceState);
    }
}

function floorObjectClass(obj) {
    if (obj?.cls) return obj.cls;
    if (obj?.otyp === GOLD_PIECE || obj?.glyph === '$') return 'coin';
    return '';
}

function droppedObjectAltarFloorEffects(obj, x, y, messages) {
    const loc = game.level?.at(x, y);
    if (!game._monster_moving || loc?.typ !== ALTAR || !floorObjectVisible(x, y)) return false;
    const coin = floorObjectClass(obj) === 'coin';
    if (coin) {
        obj.blessed = false;
        obj.cursed = false;
    }
    if (obj.blessed || obj.cursed) {
        const color = obj.blessed ? 'amber' : 'black';
        messages.push(`There is an ${color} flash as ${floorObjectArticleName(obj)} ${floorObjectVerb(obj, 'hits', 'hit')} the altar.`);
        if (!heroIsHallucinating()) obj.bknown = true;
    } else {
        messages.push(`${floorObjectSubject(obj)} ${floorObjectVerb(obj, 'lands', 'land')} on the altar.`);
        if (!coin) obj.bknown = true;
    }
    return true;
}

export function earthFloorEffects(obj, x, y, messages, verb = 'fall', {
    usedUpShopBillOnDestroy = false,
} = {}) {
    const loc = game.level?.at(x, y);
    if (!loc || !obj) return false;
    if (obj?.otyp === BOULDER && earthBoulderHitsLiquid(loc)) {
        const lava = earthLiquidIsLava(loc);
        const chance = rn2(10);
        const body = earthWaterBodyName(loc);
        const trap = boulderFillTrapAt(x, y);
        const fillsUp = earthBoulderFillsLiquid(loc, lava, chance);
        let buriedDebt = '';
        if (fillsUp) {
            fillBoulderLiquidTerrain(loc);
            killBoulderFillMonster(x, y);
            deleteBoulderFillFloorTrap(trap, x, y);
            buriedDebt = buriedMerchandiseDebtMessage(x, y, obj);
            messages.push(...buryObjectsAt(x, y, { ignore: obj }));
            if (buriedDebt) messages.push(buriedDebt);
        }
        if (!game.u?.uinwater) {
            if (earthVisibleSquare(x, y)) {
                messages.push(`There is a large splash as the boulder ${fillsUp ? 'fills' : 'falls into'} the ${body}.`);
            } else if (!heroIsDeaf()) {
                messages.push(`You hear a${lava ? ' sizzling' : ''} splash.`);
            }
        }
        if (fillsUp && game.u?.uinwater && game.u?.ux === x && game.u?.uy === y) {
            game.u.uinwater = 0;
            game.u.underwater = false;
            game.u.uunderwater = false;
            game.u.uundetected = 0;
            vision_recalc(1);
            messages.push('You find yourself on dry land again!');
        } else if (lava && heroNextToSquare(x, y)) {
            applyBoulderLavaSplashToHero(messages);
        } else if (!fillsUp && earthVisibleSquare(x, y)) {
            messages.push('It sinks without a trace!');
        }
        if (usedUpShopBillOnDestroy) markObjectShopBillUsedUp(obj);
        newsym(x, y);
        return true;
    }
    if (obj?.otyp === BOULDER)
        return earthBoulderPitHoleEffects(obj, x, y, messages, verb, { usedUpShopBillOnDestroy });

    const liquidType = earthFloorLiquidType(loc);
    if (liquidType === LAVAPOOL || liquidType === LAVAWALL)
        return droppedObjectLavaFloorEffects(obj, x, y, messages, { usedUpShopBillOnDestroy });
    if (IS_POOL(liquidType))
        return droppedObjectWaterFloorEffects(obj, x, y, messages, { usedUpShopBillOnDestroy });
    const pitHole = droppedObjectPitHoleFloorEffects(obj, x, y, messages);
    if (pitHole.handled) return pitHole.consumed;
    if (droppedObjectGlobMeldFloorEffects(obj, x, y, messages)) return true;
    if (droppedObjectAltarFloorEffects(obj, x, y, messages)) return false;
    return droppedObjectHotGroundFloorEffects(obj, x, y, messages, { usedUpShopBillOnDestroy });
}

function dropMonsterInventory(mon, messages = null, { verb = 'fall' } = {}) {
    const floorMessages = Array.isArray(messages) ? messages : [];
    return dropMonsterInventoryRaw(mon, {
        verb,
        floorEffects: (obj, x, y, floorVerb) => earthFloorEffects(obj, x, y, floorMessages, floorVerb),
    });
}

function monstoneObjectEscapesStatue(obj) {
    if (!obj) return false;
    if (obj.otyp === BOULDER || objectKindKey(obj) === 'boulder') return true;
    const kind = objectKindKey(obj);
    const actual = String(obj.actualKind || '').toLowerCase();
    return obj.realAmuletOfYendor || actual === 'amulet of yendor' || kind === 'amulet of yendor'
        || isBookOfTheDeadItem(obj) || isBellOfOpeningItem(obj) || isCandelabrumOfInvocationItem(obj);
}

function monsterStoneCorpstatFlags(mon) {
    const data = mon?.data || {};
    let flags = 0;
    if (mon?.female) flags |= CORPSTAT_FEMALE;
    else if (!data.neuter) flags |= CORPSTAT_MALE;
    if (data.unique || data.nemesis || data.rider) flags |= CORPSTAT_HISTORIC;
    return flags;
}

function placeMonstoneEscapedObject(obj, x, y, messages) {
    Object.assign(obj, { ox: x, oy: y });
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    obj.contained = false;
    delete obj.line;
    if (earthFloorEffects(obj, x, y, messages, 'fall')) return;
    game.level.objects.push(obj);
    newsym(x, y);
}

function makeStoneMonsterRock(x, y) {
    const rock = mksobj(ROCK, true, false);
    Object.assign(rock, object_display(rock), { ox: x, oy: y, quan: 1 });
    const stacked = stackMonsterThrownObject(rock);
    if (stacked === rock) game.level.objects.push(rock);
    return stacked;
}

export function stoneMonster(mon, messages = null, { awardExperience = false } = {}) {
    if (!mon || !game.level) return null;
    const x = mon.mx;
    const y = mon.my;
    if (x == null || y == null) return null;
    game.level.objects ??= [];
    const floorMessages = Array.isArray(messages) ? messages : [];
    const data = mon.data || {};
    mon.mhp = 0;
    mon.mtrapped = 0;

    const oldInventory = [...(mon.minvent || [])];
    const statueContents = [];
    mon.minvent = [];
    if (mon.missile && !oldInventory.includes(mon.missile)) oldInventory.push(mon.missile);
    mon.missile = null;
    mon.mw = null;

    for (const obj of oldInventory) {
        if (monstoneObjectEscapesStatue(obj)) {
            placeMonstoneEscapedObject(obj, x, y, floorMessages);
            continue;
        }
        if (obj.lamplit || obj.burning) {
            obj.lamplit = false;
            obj.burning = false;
            delete obj._burnTimer;
            delete obj.litRadius;
        }
        obj.contained = false;
        obj.worn = false;
        obj.owornmask = 0;
        statueContents.push(obj);
    }

    const tiny = !!(data.verysmall || data.tiny || data.msize === 'tiny' || data.size === 'tiny');
    const makeStatue = !tiny || !rn2(2 + ((data.genoFreq ?? 1) > 2 ? 1 : 0));
    let remains;
    if (makeStatue) {
        remains = mkcorpstat(STATUE, mon, data, x, y, monsterStoneCorpstatFlags(mon));
        remains.kind = 'statue';
        remains.contents = [];
        for (const obj of statueContents) add_to_container(remains, obj);
        remains.owt = (remains.owt || 0)
            + remains.contents.reduce((sum, obj) => sum + (obj.owt || obj.weight || 0) * Math.max(1, obj.quan || 1), 0);
        Object.assign(remains, object_display(remains));
    } else {
        for (const obj of statueContents) placeMonstoneEscapedObject(obj, x, y, floorMessages);
        remains = makeStoneMonsterRock(x, y);
    }

    recordVanquished(mon, awardExperience);
    game.level.monsters = (game.level.monsters || []).filter(other => other !== mon);
    mon.dead = true;
    mon.movement = 0;
    newsym(x, y);
    return remains;
}

function objectStackType(obj) {
    if (!obj) return undefined;
    if (obj.otyp != null && obj.otyp !== obj.cls) return obj.otyp;
    return obj.actualKind ?? obj.kind ?? obj.otyp ?? obj.cls;
}

function objectStackColor(obj) {
    if (!obj) return undefined;
    if (shopBillableGold(obj)) return CLR_YELLOW;
    if (obj.color != null) return obj.color;
    if (obj.cls === 'weapon') return obj.kind === 'quarterstaff' ? CLR_BROWN : CLR_CYAN;
    if (obj.cls === 'scroll' || obj.cls === 'spellbook') return CLR_WHITE;
    return NO_COLOR;
}

function stackMonsterNameKey(obj) {
    return String(obj?.corpsenm?.name || '').toLowerCase();
}

function corpseIsReviverForMerge(obj) {
    if (!isCorpseItem(obj)) return false;
    const name = stackMonsterNameKey(obj);
    const glyph = obj?.corpsenm?.glyph || obj?.corpsenm?.mlet;
    return !!(obj?.corpsenm?.rider || glyph === 'T' || name.includes('troll')
        || name === 'death' || name === 'pestilence' || name === 'famine');
}

function sameStackCorpseEggTinFields(existing, obj) {
    const existingSpecial = isCorpseItem(existing) || isEggItem(existing) || isTinObject(existing);
    const objSpecial = isCorpseItem(obj) || isEggItem(obj) || isTinObject(obj);
    if (!existingSpecial && !objSpecial) return true;
    if (existingSpecial !== objSpecial) return false;
    if (stackMonsterNameKey(existing) !== stackMonsterNameKey(obj)) return false;
    if ((isEggItem(existing) || isEggItem(obj)) && (eggHasHatchTimer(existing) || eggHasHatchTimer(obj)))
        return false;
    if (corpseIsReviverForMerge(existing) || corpseIsReviverForMerge(obj)) return false;
    return true;
}

function sameMonsterThrownStackObject(existing, obj) {
    if (!existing || !obj || existing === obj) return false;
    if (existing.hidden || existing.buried || existing.transientProjectile) return false;
    if (existing.ox !== obj.ox || existing.oy !== obj.oy) return false;
    if (!!existing.unpaid !== !!obj.unpaid || !!existing.no_charge !== !!obj.no_charge) return false;
    if (existing.unpaid && !sameShopBillUnitPrice(existing, obj)) return false;
    return objectStackType(existing) === objectStackType(obj)
        && existing.cls === obj.cls
        && existing.kind === obj.kind
        && existing.actualKind === obj.actualKind
        && sameStackCorpseEggTinFields(existing, obj)
        && existing.glyph === obj.glyph
        && objectStackColor(existing) === objectStackColor(obj)
        && (existing.spe || 0) === (obj.spe || 0)
        && stackedObjectInstanceNamesMergeCompatible(existing, obj)
        && !!existing.blessed === !!obj.blessed
        && !!existing.cursed === !!obj.cursed
        && !!existing.opoisoned === !!obj.opoisoned
        && (existing.oeroded || 0) === (obj.oeroded || 0)
        && (existing.oeroded2 || 0) === (obj.oeroded2 || 0)
        && existing.gemDescription === obj.gemDescription
        && existing.scrollIndex === obj.scrollIndex
        && existing.potionIndex === obj.potionIndex
        && existing.spellbookIndex === obj.spellbookIndex;
}

function stackMonsterThrownObject(obj) {
    const stack = (game.level?.objects || []).find(existing => sameMonsterThrownStackObject(existing, obj));
    if (!stack) return obj;
    mergeStackedShopBillEntries(stack, obj);
    copyStackedObjectInstanceNameForMerge(stack, obj);
    stack.quan = (stack.quan || 1) + (obj.quan || 1);
    return stack;
}

function sameDroppedFloorStackObject(dropped, existing) {
    if (!sameMonsterThrownStackObject(existing, dropped)) return false;
    if (dropped.nomerge || existing.nomerge) return false;
    if (dropped.artifact || existing.artifact) return false;
    if (globContents(dropped).length || globContents(existing).length) return false;
    return true;
}

function stackDroppedFloorObject(obj) {
    if (!game.level || !Array.isArray(game.level.objects) || !obj) return obj;
    const objects = game.level.objects;
    if (!objects.includes(obj)) return obj;
    const stack = objects.find(existing => existing !== obj && sameDroppedFloorStackObject(obj, existing));
    if (!stack) return obj;
    mergeStackedShopBillEntries(obj, stack);
    copyStackedObjectInstanceNameForMerge(obj, stack);
    obj.quan = (obj.quan || 1) + (stack.quan || 1);
    stack.quan = 0;
    game.level.objects = objects.filter(existing => existing !== stack);
    newsym(obj.ox, obj.oy);
    return obj;
}

function placeStackableFloorObject(obj) {
    game.level.objects ??= [];
    const stacked = stackMonsterThrownObject(obj);
    if (stacked === obj) game.level.objects.push(obj);
    return stacked;
}

function monsterThrownFloorEffects(obj, x, y, messages, verb) {
    const previousMonsterMoving = game._monster_moving;
    game._monster_moving = 1;
    try {
        return earthFloorEffects(obj, x, y, messages, verb);
    } finally {
        if (previousMonsterMoving === undefined) delete game._monster_moving;
        else game._monster_moving = previousMonsterMoving;
    }
}

export function dropMonsterObject(mon, obj, messages = null, {
    verb = 'fall',
    monsterMoving = true,
} = {}) {
    if (!mon || !obj || !game.level) return { consumed: false, object: null, messages: [] };
    game.level.objects ??= [];
    const floorMessages = Array.isArray(messages) ? messages : [];
    if (Array.isArray(mon.minvent))
        mon.minvent = mon.minvent.filter(item => item !== obj);
    if (mon.missile === obj) mon.missile = null;
    if (mon.mw === obj) mon.mw = null;

    obj.ox = mon.mx;
    obj.oy = mon.my;
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    delete obj.line;

    const previousMonsterMoving = game._monster_moving;
    if (monsterMoving) game._monster_moving = 1;
    let consumed = false;
    try {
        consumed = earthFloorEffects(obj, obj.ox, obj.oy, floorMessages, verb);
    } finally {
        if (monsterMoving) {
            if (previousMonsterMoving === undefined) delete game._monster_moving;
            else game._monster_moving = previousMonsterMoving;
        }
    }
    if (consumed) {
        newsym(obj.ox, obj.oy);
        return { consumed: true, object: null, messages: floorMessages };
    }
    const stacked = stackMonsterThrownObject(obj);
    if (stacked === obj) game.level.objects.push(obj);
    newsym(obj.ox, obj.oy);
    return { consumed: false, object: stacked, messages: floorMessages };
}

function monsterThrownMulchCandidate(obj) {
    if (!obj) return false;
    const kind = objectKindKey(obj) || pickupObjectName({ ...obj, quan: 1 }).toLowerCase();
    if (/\bboomerang\b/.test(kind)) return false;
    if (obj.oc_magic || obj.magic || obj.magicStone) return false;
    if (/\b(?:luckstone|loadstone|touchstone)\b/.test(kind)) return false;
    if ([LUCKSTONE, LOADSTONE, TOUCHSTONE].includes(obj.otyp)) return false;
    if (obj.otyp === GEM_CLASS || obj.cls === 'gem' || obj.glyph === '*') return true;
    return obj.otyp === DART
        || /\b(?:arrow|arrows|ya|bolt|bolts|dart|darts|shuriken|throwing star|throwing stars|rock|rocks|flint)\b/.test(kind);
}

function monsterThrownHardGemMulchCandidate(obj) {
    if (!obj) return false;
    const kind = objectKindKey(obj) || pickupObjectName({ ...obj, quan: 1 }).toLowerCase();
    return kind === 'flint' || obj.otyp === FLINT || (!!obj.gemTough && (obj.cls === 'gem' || obj.glyph === '*'));
}

function shouldMulchMonsterThrownMissile(obj) {
    if (!monsterThrownMulchCandidate(obj)) return false;
    const erosion = Math.max(0, Math.trunc(Number(obj.oeroded || 0)), Math.trunc(Number(obj.oeroded2 || 0)));
    const chance = 3 + erosion - Math.trunc(Number(obj.spe || 0));
    let broken = chance > 1 ? !!rn2(chance) : !rn2(4);
    if (obj.blessed && !rn2(3)) broken = false;
    if (monsterThrownHardGemMulchCandidate(obj) && !rn2(2)) broken = false;
    return broken;
}

const MONSTER_PASSIVE_OBJECT_ATTACK_FALLBACKS = new Map([
    ['acid blob', 'acid'],
    ['green mold', 'acid'],
    ['fire elemental', 'fire'],
    ['fire vortex', 'fire'],
    ['red mold', 'fire'],
    ['rust monster', 'rust'],
    ['black pudding', 'corr'],
    ['disenchanter', 'ench'],
]);

function normalizedPassiveObjectAttackType(type) {
    const key = String(type || '').toLowerCase();
    if (key === 'ad_fire' || key === 'fire') return 'fire';
    if (key === 'ad_acid' || key === 'acid') return 'acid';
    if (key === 'ad_rust' || key === 'rust') return 'rust';
    if (key === 'ad_corr' || key === 'corr' || key === 'corrode') return 'corr';
    if (key === 'ad_ench' || key === 'ench' || key === 'disenchant') return 'ench';
    return '';
}

function passiveObjectAttackForMonster(mon) {
    if (!mon) return '';
    const attacks = [];
    if (mon.attack) attacks.push(mon.attack);
    if (Array.isArray(mon.attacks)) attacks.push(...mon.attacks);
    if (mon.data?.attack) attacks.push(mon.data.attack);
    if (Array.isArray(mon.data?.attacks)) attacks.push(...mon.data.attacks);
    for (const attack of attacks) {
        const aatyp = String(attack?.aatyp || attack?.type || '').toLowerCase();
        if (aatyp !== 'none' && aatyp !== 'at_none' && aatyp !== 'passive') continue;
        const adtyp = normalizedPassiveObjectAttackType(attack?.adtyp || attack?.damageType);
        if (adtyp) return adtyp;
    }
    return MONSTER_PASSIVE_OBJECT_ATTACK_FALLBACKS.get(String(mon.data?.name || mon.name || '').toLowerCase()) || '';
}

function monsterAtSquareForPassiveObject(x, y) {
    const mon = (game.level?.monsters || []).find(candidate =>
        candidate && candidate.mx === x && candidate.my === y && !candidate.dead
        && (candidate.mhp == null || candidate.mhp > 0));
    if (mon) return mon;
    if (game.u && game.u.ux === x && game.u.uy === y)
        return { ...game.u, data: polyselfForm() || game.u.data || {} };
    return null;
}

function erodeMonsterThrownPassiveObject(obj, type, messages) {
    const profile = wishedDamageProfile(obj);
    const erosion = type === 'rust'
        ? { field: 'oeroded', word: 'rusty', action: 'rust' }
        : type === 'corr' || type === 'acid'
            ? { field: 'oeroded2', word: 'corroded', action: 'corrode' }
            : type === 'fire'
                ? { field: 'oeroded', word: 'burnt', action: 'smoulder' }
                : null;
    if (!erosion) return { handled: false, damaged: false };
    if ((type === 'rust' || type === 'corr' || type === 'acid') && obj.greased) {
        if (!rn2(2)) obj.greased = false;
        return { handled: true, damaged: false, greased: true };
    }
    if (!profile.erosionMatters || (erosion.field === 'oeroded' ? profile.primaryWord : profile.secondaryWord) !== erosion.word)
        return { handled: !!erosion, damaged: false };
    if (obj.oerodeproof || obj.rustproof) {
        obj.rknown = true;
        return { handled: true, damaged: false };
    }
    if (obj.blessed && !rnl(4)) return { handled: true, damaged: false };
    const current = Math.min(3, obj[erosion.field] || 0);
    if (current >= 3) return { handled: true, damaged: false };
    const name = floorObjectBaseName(obj);
    obj[erosion.field] = current + 1;
    if (floorObjectVisible(obj.ox, obj.oy)) {
        const adverb = obj[erosion.field] === 3 ? ' completely' : current ? ' further' : '';
        messages.push(`The ${name} ${rustTrapNameVerb(name, `${erosion.action}s`, erosion.action)}${adverb}!`);
    }
    return { handled: true, damaged: true };
}

function applyMonsterThrownPassiveObject(landing, target, ohit, messages) {
    if (!landing || !target || !ohit) return { handled: false, damaged: false };
    const type = passiveObjectAttackForMonster(target);
    if (!type) return { handled: false, damaged: false, type };
    if (type === 'ench') {
        if (target.mcan) return { handled: true, damaged: false, type };
        const drain = drainItem(landing, { byYou: true, messages });
        return { handled: true, damaged: drain.drained, type, ...drain };
    }
    if ((type === 'rust' || type === 'corr') && target.mcan)
        return { handled: true, damaged: false, type };
    if (type === 'fire') {
        if (rn2(6) || target.mcan || String(target.data?.name || target.name || '').toLowerCase() === 'steam vortex')
            return { handled: true, damaged: false, type };
    } else if (type === 'acid' && rn2(6)) {
        return { handled: true, damaged: false, type };
    }
    return { ...erodeMonsterThrownPassiveObject(landing, type, messages), type };
}

export function landMonsterThrownObject(missile, x, y, {
    glyph = missile?.glyph || ')',
    color = missile?.color ?? CLR_CYAN,
    petFetchable = true,
    messages = null,
    verb = 'fall',
    quan = 1,
    ohit = false,
    passiveTarget = null,
    contactBreaks = null,
} = {}) {
    if (!missile || !game.level) return { consumed: false, object: null, messages: [] };
    game.level.objects ??= [];
    const floorMessages = Array.isArray(messages) ? messages : [];
    const breaksOnContact = contactBreaks ?? (isCreamPieObject(missile) || isVenomObject(missile) || (!!ohit && isEggItem(missile)));
    const mulched = !breaksOnContact && !!ohit && shouldMulchMonsterThrownMissile(missile);
    const dropThrow = {
        broken: breaksOnContact || mulched,
        ohit: !!ohit,
        mulched,
    };
    if (dropThrow.broken) {
        if (mulched) rn2(100);
        newsym(x, y);
        return {
            consumed: true,
            object: null,
            messages: floorMessages,
            shipObject: projectileShipObjectResult(),
            floorEffects: { consumed: false },
            dropThrow,
        };
    }
    const passiveObjectTarget = passiveTarget || monsterAtSquareForPassiveObject(x, y);
    const landing = {
        ...missile,
        ox: x,
        oy: y,
        quan: Math.max(1, quan || 1),
        glyph,
        color,
        petFetchable,
        hidden: false,
        buried: false,
        transientProjectile: false,
    };
    delete landing.line;
    const shipObject = landing.otyp === BOULDER
        ? projectileShipObjectResult()
        : maybeShipRemoteProjectileObject(landing, x, y, floorMessages);
    if (shipObject.handled) {
        return {
            consumed: true,
            object: null,
            messages: floorMessages,
            shipObject,
            floorEffects: { consumed: false },
            dropThrow,
        };
    }
    const consumed = monsterThrownFloorEffects(landing, x, y, floorMessages, verb);
    if (consumed) {
        newsym(x, y);
        return {
            consumed: true,
            object: null,
            messages: floorMessages,
            shipObject,
            floorEffects: { consumed: true },
            dropThrow,
        };
    }
    const passiveObj = applyMonsterThrownPassiveObject(landing, passiveObjectTarget, !!ohit, floorMessages);
    const stacked = stackMonsterThrownObject(landing);
    if (stacked === landing) game.level.objects.push(landing);
    newsym(x, y);
    return {
        consumed: false,
        object: stacked,
        messages: floorMessages,
        shipObject,
        floorEffects: { consumed: false },
        dropThrow,
        passiveObj,
    };
}

// C done_object_cleanup() salvages gt.thrownobj for bones without drop_throw().
function deathCleanupThrownObjectLandingSpot() {
    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    let x = ux + (game.u?.dx || 0);
    let y = uy + (game.u?.dy || 0);
    const loc = game.level?.at?.(x, y);
    if (!isok(x, y) || !loc || !ACCESSIBLE(loc.typ)) {
        x = ux;
        y = uy;
    }
    return { x, y };
}

function placeDeathCleanupThrownObject(attack) {
    const missile = attack?.deathCleanupThrownObject;
    if (!missile || !game.level) return null;
    game.level.objects ??= [];
    const sameProjectile = obj => obj?.transientProjectile
        && (missile.id != null ? obj.id === missile.id : obj === missile);
    const projectile = game.level.objects.find(sameProjectile) || missile;
    const oldX = projectile.ox;
    const oldY = projectile.oy;
    const projectileColor = projectile.color;
    game.level.objects = game.level.objects
        .filter(obj => obj === projectile || !sameProjectile(obj));
    if (!game.level.objects.includes(projectile)) game.level.objects.push(projectile);

    const { x, y } = deathCleanupThrownObjectLandingSpot();
    Object.assign(projectile, missile, {
        ox: x,
        oy: y,
        quan: Math.max(1, attack.deathCleanupQuan || missile.quan || 1),
        glyph: attack.deathCleanupGlyph || missile.glyph || ')',
        color: attack.deathCleanupColor ?? projectileColor ?? missile.color ?? CLR_CYAN,
        hidden: false,
        buried: false,
        transientProjectile: false,
        _deathCleanupThrownObject: true,
    });
    delete projectile.line;
    delete projectile.nobj;
    delete projectile.nexthere;

    const stacked = stackDroppedFloorObject(projectile);
    if (oldX != null && oldY != null) newsym(oldX, oldY);
    newsym(x, y);
    if (!(game.level.objects || []).some(obj => obj.transientProjectile))
        game._clear_transient_projectiles_after_more = 0;
    return stacked;
}

function appendToplineAfterMoreMessages(messages) {
    let queued = false;
    for (const msg of messages || []) {
        if (!msg) continue;
        if (!game._topline_after_more) {
            game._topline_after_more = msg;
            continue;
        }
        const width = game.nhDisplay?.cols || 80;
        if (game._topline_after_more.length + msg.length + 3 < width - 8) {
            game._topline_after_more = `${game._topline_after_more}  ${msg}`;
        } else {
            game._queued_messages_after_more ??= [];
            game._queued_messages_after_more.push({ text: msg, more: true });
            queued = true;
        }
    }
    return queued;
}

function applyHeroPoisonedProjectileAfterMore(effect) {
    const reason = String(effect?.reason || 'poisoned arrow');
    const messages = [];
    if (!/poison/i.test(reason)) {
        const plural = /s$/i.test(reason);
        messages.push(`${/^[A-Z]/.test(reason) ? '' : 'The '}${reason} ${plural ? 'were' : 'was'} poisoned!`);
    }
    if (heroHasPoisonResistance()) {
        messages.push("The poison doesn't seem to affect you.");
        return { dead: false, more: appendToplineAfterMoreMessages(messages) };
    }

    const roll = rn2(30);
    let dead = false;
    if (roll === 0) {
        const loss = 6 + d(4, 6);
        if ((game.u?.uhp || 0) <= loss) {
            if (game.u) game.u.uhp = -1;
            messages.push('The poison was deadly...');
            dead = true;
        } else {
            if (game.u) {
                game.u.uhpmax = Math.max(3, (game.u.uhpmax || game.u.uhp || 1) - Math.trunc(loss / 2));
                game.u.uhp = Math.max(0, (game.u.uhp || 0) - loss);
                if (game.u.acurr?.a) {
                    game.u.acurr.a[A_CON] = Math.max(3, (game.u.acurr.a[A_CON] ?? 10) - 1);
                    game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - 3);
                }
            }
            messages.push('You feel very sick!');
            messages.push('You feel weaker!');
            dead = (game.u?.uhp || 0) < 1;
        }
    } else if (roll > 5) {
        const loss = rnd(6);
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - loss);
        dead = (game.u?.uhp || 0) < 1;
    } else {
        if (game.u?.acurr?.a) game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - 1);
        messages.push('You feel weaker!');
    }
    const more = appendToplineAfterMoreMessages(messages);
    if (dead) {
        game._death_cause = `poisoned by ${String(effect?.killer || reason)}`;
        game._queued_message_after_more ||= 'You die...';
    }
    return { dead, more: more || dead };
}

function earthTargetIsSolid(target) {
    const data = target?.data || target || {};
    return !(target?.passWalls || target?.noncorporeal || target?.unsolid
        || data.passWalls || data.noncorporeal || data.unsolid
        || data.amorphous || data.name === 'fog cloud');
}

function earthTargetThrowsRocks(target) {
    const data = target?.data || target || {};
    return !!(target?.throwsRocks || data.throwsRocks);
}

function earthObjectDamage(obj, target) {
    const quan = obj?.quan || 1;
    const base = obj?.otyp === BOULDER ? rnd(20) : rnd(3);
    const data = target?.data || target || {};
    if (data.thickSkinned || data.scaled || data.name === 'shade') return 0;
    return base * quan;
}

function wornEarthHelmet() {
    return (game.inventory || []).find(item =>
        item.cls === 'armor' && (item.worn || item.line?.includes('being worn')) && armorSlot(item) === 'helm');
}

function monsterEarthHelmet(mon) {
    return (mon?.minvent || []).find(item =>
        item.cls === 'armor' && (item.worn || item.owornmask || item.line?.includes('being worn')) && armorSlot(item) === 'helm');
}

function hardEarthHelmet(item) {
    const name = armorKind(item);
    return armorSlot(item) === 'helm' && !/\b(?:elven leather helm|leather hat|fedora|cornuthaum|dunce cap)\b/.test(name);
}

function dropEarthObjectOnHero(confused, helmetProtects, messages) {
    if (game.u?.uswallow) return dropEarthObjectOnMonster(game.u.ux, game.u.uy, confused, messages);
    const obj = makeEarthDropObject(confused);
    const solidHero = earthTargetIsSolid({ ...game.u, data: polyselfForm() || {} });
    let damage = 0;
    if (solidHero) {
        messages.push(`You are hit by ${earthDropObjectPhrase(obj)}!`);
        damage = earthObjectDamage(obj, { data: polyselfForm() || {} });
        const helmet = wornEarthHelmet();
        if (helmet && helmetProtects) {
            if (hardEarthHelmet(helmet)) {
                messages.push('Fortunately, you are wearing a hard helmet.');
                if (damage > 2) damage = 2;
            } else {
                messages.push(`Your ${pickupObjectName(helmet)} does not protect you.`);
            }
        }
    }
    if (!earthFloorEffects(obj, game.u?.ux || 0, game.u?.uy || 0, messages))
        earthPlaceObject(obj, game.u?.ux || 0, game.u?.uy || 0);
    if (damage && game.u && (game.u.uhp || 0) > 0) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = 'killed by a scroll of earth';
            messages.push('You die...');
        }
    }
    return true;
}

function dropEarthObjectOnMonster(x, y, confused, messages) {
    const obj = makeEarthDropObject(confused);
    const mon = (game.level?.monsters || []).find(mtmp => mtmp.mx === x && mtmp.my === y && !mtmp.dead);
    const visible = mon && earthVisibleSquare(x, y);
    if (mon && earthTargetIsSolid(mon)) {
        const name = fireScrollMonsterName(mon);
        if (visible) messages.push(`${name} is hit by ${earthDropObjectPhrase(obj)}!`);
        let damage = earthObjectDamage(obj, mon);
        const helmet = monsterEarthHelmet(mon);
        if (helmet) {
            if (hardEarthHelmet(helmet)) {
                if (visible) messages.push(`Fortunately, ${name.replace(/^The /, 'the ')} is wearing a hard helmet.`);
                else if (!heroIsDeaf()) messages.push('You hear a clanging sound.');
                if (damage > 2) damage = 2;
            } else if (visible) {
                messages.push(`${name}'s ${pickupObjectName(helmet)} does not protect it.`);
            }
        }
        mon.mhp = (mon.mhp || 1) - damage;
        if ((mon.mhp || 0) <= 0) {
            dropMonsterInventory(mon, messages);
            game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
            recordVanquished(mon, true);
            newsym(x, y);
            if (visible) messages.push(`You kill the ${mon.data?.name || mon.name || 'monster'}!`);
        } else {
            mon.msleeping = 0;
            mon.mpeaceful = false;
        }
    }
    if (!earthFloorEffects(obj, x, y, messages)) earthPlaceObject(obj, x, y);
    return true;
}

function earthDropEligibleSquare(x, y) {
    if (x == null || y == null || x < 1 || x >= COLNO || y < 0 || y >= ROWNO) return false;
    const loc = game.level?.at(x, y);
    if (!loc) return false;
    if (loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED))) return false;
    return !IS_OBSTRUCTED(loc.typ) && !IS_AIR(loc.typ);
}

function earthScrollEffect(item) {
    const messages = [];
    let known = false;
    let changedVision = false;
    if (earthScrollHasEffect()) {
        messages.push(earthCeilingMessage(item));
        known = true;
        applySokobanGuilt();
        let nboulders = 0;
        const ux = game.u?.ux || 0;
        const uy = game.u?.uy || 0;
        if (!item?.cursed) {
            for (let x = ux - 1; x <= ux + 1; x++) {
                for (let y = uy - 1; y <= uy + 1; y++) {
                    if (x === ux && y === uy) continue;
                    if (!earthDropEligibleSquare(x, y)) continue;
                    if (dropEarthObjectOnMonster(x, y, heroIsConfused(), messages)) {
                        nboulders++;
                        changedVision = true;
                    }
                }
            }
        }
        if (!item?.blessed) {
            if (dropEarthObjectOnHero(heroIsConfused(), !item?.cursed, messages)) changedVision = true;
        } else if (!nboulders) {
            messages.push('But nothing else happens.');
        }
    }
    if (changedVision) {
        vision_reset();
        vision_recalc(0);
    }
    return { messages, known, more: messages.length > 1 || (game.u?.uhp || 1) <= 0 };
}

function armorKind(item) {
    return String(item?.actualKind || item?.kind || pickupObjectName(item)).toLowerCase();
}

function armorMessageName(item) {
    return pickupObjectName(item).replace(/^pair of /, '');
}

function armorNameIsPlural(name) {
    return /\b(?:boots|shoes|gloves|gauntlets|scales)\b/i.test(name) && !/\bmail\b/i.test(name);
}

function armorVerb(item, singular, plural) {
    return armorNameIsPlural(armorMessageName(item)) ? plural : singular;
}

function armorSubject(item) {
    return `Your ${armorMessageName(item)}`;
}

function armorSlot(item) {
    const name = armorKind(item);
    if (/\b(?:cloak|robe|wrapping|mantelet|pall|cape|cope|cloth|smock|apron)\b/.test(name)) return 'cloak';
    if (/\b(?:mail|armor|jacket|coat|dragon scales?)\b/.test(name)) return 'body';
    if (/\bshirt\b/.test(name)) return 'shirt';
    if (/\b(?:helm|helmet|hat|fedora|cornuthaum|cap|pot)\b/.test(name)) return 'helm';
    if (/\b(?:gloves|gauntlets)\b/.test(name)) return 'gloves';
    if (/\b(?:boots|shoes)\b/.test(name)) return 'boots';
    if (/\bshield\b/.test(name)) return 'shield';
    return '';
}

function isWornArmorItem(item) {
    return (item?.cls === 'armor' || item?.glyph === '[' || item?.otyp === ARMOR_CLASS)
        && (item.worn || item.owornmask || item.line?.includes('being worn'));
}

function wornArmorItemsBySlotOrder(slots) {
    const wornArmor = (game.inventory || []).filter(isWornArmorItem);
    const used = new Set();
    const ordered = [];
    for (const slot of slots) {
        const item = wornArmor.find(candidate => !used.has(candidate) && armorSlot(candidate) === slot);
        if (item) {
            used.add(item);
            ordered.push(item);
        }
    }
    return ordered;
}

function countWornArmor() {
    return (game.inventory || []).filter(isWornArmorItem).length;
}

function someArmorTarget() {
    const wornArmor = (game.inventory || []).filter(isWornArmorItem);
    let target = wornArmor.find(item => armorSlot(item) === 'cloak')
        || wornArmor.find(item => armorSlot(item) === 'body')
        || wornArmor.find(item => armorSlot(item) === 'shirt')
        || null;
    for (const slot of ['helm', 'gloves', 'boots', 'shield']) {
        const item = wornArmor.find(candidate => armorSlot(candidate) === slot);
        if (item && (!target || !rn2(4))) target = item;
    }
    return target;
}

function someEnchantArmorTarget() {
    const wornArmor = (game.inventory || []).filter(item =>
        item.cls === 'armor' && (item.worn || item.line?.includes('being worn')));
    let target = wornArmor.find(item => armorSlot(item) === 'cloak')
        || wornArmor.find(item => armorSlot(item) === 'body')
        || wornArmor.find(item => armorSlot(item) === 'shirt')
        || null;
    for (const slot of ['helm', 'gloves', 'boots', 'shield']) {
        const item = wornArmor.find(candidate => armorSlot(candidate) === slot);
        if (item && (!target || !rn2(4))) target = item;
    }
    return target;
}

function wornArmorAcValue(item) {
    const base = ARMOR_AC_BONUS[armorKind(item)] ?? 0;
    return base + (item.spe ?? 0) - Math.min(item.oeroded || 0, base);
}

function wornArmorAcValueGreatestErosion(item) {
    const base = ARMOR_AC_BONUS[armorKind(item)] ?? 0;
    return base + (item.spe ?? 0) - Math.min(Math.max(item.oeroded || 0, item.oeroded2 || 0), base);
}

function updateArmorLine(item) {
    item.line = normalInventoryLine({ ...item, line: '' });
}

function updateReflectionFromInventory() {
    if (!game.u) return;
    game.u.reflecting = (game.inventory || []).some(item => {
        if (!isActiveInventoryExtrinsicItem(item)) return false;
        const kind = armorKind(item);
        return isSilverDragonArmorKind(kind) || kind === 'shield of reflection'
            || item.amuletIndex === 7 || kind === 'amulet of reflection';
    });
}

function updateWornArmorAcAfterChange(armor, oldAc) {
    if (game.u && isWornArmorItem(armor)) game.u.uac = (game.u.uac ?? 10) + oldAc - wornArmorAcValueGreatestErosion(armor);
    updateArmorLine(armor);
}

function destroyWornArmorItem(armor) {
    const oldAc = wornArmorAcValueGreatestErosion(armor);
    if (game.u && isWornArmorItem(armor)) game.u.uac = (game.u.uac ?? 10) + oldAc;
    updateGauntletsOfPowerStrength(armorKind(armor), false);
    removeInventoryItem(armor, armor.quan || 1);
    updateReflectionFromInventory();
}

function armorGlowMessage(armor, color) {
    const subject = armorSubject(armor);
    if (game.u?.blind)
        return `${subject} ${armorVerb(armor, 'vibrates', 'vibrate')} for a moment.`;
    return `${subject} ${armorVerb(armor, 'glows', 'glow')} ${color} for a moment.`;
}

function destroyArmorErosionType(armor) {
    const profile = wishedDamageProfile(armor);
    if (!profile.erosionMatters || !profile.damageable) return null;
    if (profile.primaryWord === 'burnt') return { field: 'oeroded', action: 'smoulder' };
    if (profile.primaryWord === 'rusty') return { field: 'oeroded', action: 'rust' };
    if (profile.primaryWord === 'cracked') return { field: 'oeroded', action: 'crack', shatters: true };
    if (profile.secondaryWord === 'rotted') return { field: 'oeroded2', action: 'rot' };
    if (profile.secondaryWord === 'corroded') return { field: 'oeroded2', action: 'corrode' };
    return null;
}

function erosionVerb(armor, action) {
    const plural = {
        smoulder: 'smoulder',
        rust: 'rust',
        rot: 'rot',
        corrode: 'corrode',
        crack: 'crack',
    }[action] || action;
    const singular = {
        smoulder: 'smoulders',
        rust: 'rusts',
        rot: 'rots',
        corrode: 'corrodes',
        crack: 'cracks',
    }[action] || `${action}s`;
    return armorVerb(armor, singular, plural);
}

function erosionAlterationVerb(action) {
    return {
        smoulder: 'burn',
        rust: 'rust',
        rot: 'rot',
        corrode: 'tarnish',
        crack: 'crack',
    }[action] || action;
}

function erodeDestroyArmor(armor, messages) {
    const erosion = destroyArmorErosionType(armor);
    if (!erosion || armor.oerodeproof) return false;
    if (armor.blessed && !rnl(4)) return false;

    const current = Math.min(3, armor[erosion.field] || 0);
    const name = armorMessageName(armor);
    if (current < 3) {
        const adverb = current + 1 === 3 ? ' completely' : current ? ' further' : '';
        messages.push(`Your ${name} ${erosionVerb(armor, erosion.action)}${adverb}!`);
        const payment = costlyAlterationPaymentMessage(armor, erosionAlterationVerb(erosion.action));
        if (payment) messages.push(payment);
        const oldAc = wornArmorAcValueGreatestErosion(armor);
        armor[erosion.field] = current + 1;
        updateWornArmorAcAfterChange(armor, oldAc);
        return true;
    }

    messages.push(erosion.shatters
        ? `Your ${name} shatters!`
        : `Your ${name} ${erosionVerb(armor, erosion.action)} away!`);
    const payment = costlyAlterationPaymentMessage(armor, erosionAlterationVerb(erosion.action));
    if (payment) messages.push(payment);
    destroyWornArmorItem(armor);
    return 'destroyed';
}

function destroyArm(messages) {
    const armors = wornArmorItemsBySlotOrder(['body', 'cloak', 'helm', 'shield', 'gloves', 'boots', 'shirt']);
    if (!armors.length) return false;

    const hits = rn2(4) + 1;
    let result = false;
    for (let i = 0; i < hits; i++) {
        const armor = armors[rn2(armors.length)];
        if (!armor || !(game.inventory || []).includes(armor)) continue;
        const erosion = erodeDestroyArmor(armor, messages);
        if (erosion) result = true;
        if (erosion === 'destroyed') break;
    }
    return result;
}

function legacyDestroyArm(messages) {
    const wornArmor = (game.inventory || []).filter(item =>
        item.cls === 'armor' && (item.worn || item.line?.includes('being worn')));
    if (!wornArmor.length) return { changed: false, uacDelta: 0 };
    const hits = rn2(4) + 1;
    let result = false;
    let uacDelta = 0;
    for (let i = 0; i < hits; i++) {
        const armor = wornArmor[rn2(wornArmor.length)];
        if (!armor || !(game.inventory || []).includes(armor)) continue;
        const armorBase = ARMOR_AC_BONUS[String(armor.kind || '').toLowerCase()] ?? 0;
        if (!armorBase || (armor.oeroded || 0) >= armorBase) continue;
        const oldAc = wornArmorAcValue(armor);
        armor.oeroded = (armor.oeroded || 0) + 1;
        armor.bknown = true;
        uacDelta += oldAc - wornArmorAcValue(armor);
        updateArmorLine(armor);
        const name = pickupObjectName(armor);
        messages.push(armor.oeroded === 1
            ? `Your ${name} smoulders!`
            : `Your ${name} smoulders further!`);
        result = true;
    }
    return { changed: result, uacDelta };
}

function armorSimpleSlotName(armor) {
    const name = armorMessageName(armor);
    const kind = armorKind(armor);
    switch (armorSlot(armor)) {
    case 'cloak':
        if (kind.includes('robe')) return 'robe';
        if (kind.includes('apron')) return 'apron';
        if (kind.includes('smock')) return 'smock';
        if (kind.includes('wrapping')) return 'wrapping';
        return 'cloak';
    case 'body':
        return name;
    case 'shirt':
        return 'shirt';
    case 'helm':
        return /\b(?:hat|fedora|cornuthaum|cap)\b/.test(kind) ? 'hat' : 'helm';
    case 'gloves':
        return 'gloves';
    case 'boots':
        return 'boots';
    case 'shield':
        return 'shield';
    default:
        return name;
    }
}

function disintegrateArmorMessage(armor) {
    const simple = armorSimpleSlotName(armor);
    switch (armorSlot(armor)) {
    case 'cloak':
        return `Your ${simple} crumbles and turns to dust!`;
    case 'body':
        return `Your ${simple} ${armorNameIsPlural(simple) ? 'turn' : 'turns'} to dust and ${armorNameIsPlural(simple) ? 'fall' : 'falls'} to the floor!`;
    case 'shirt':
        return `Your ${simple} crumbles into tiny threads and falls apart!`;
    case 'helm':
        return `Your ${simple} turns to dust and is blown away!`;
    case 'gloves':
        return `Your ${simple} vanish!`;
    case 'boots':
        return `Your ${simple} disintegrate!`;
    case 'shield':
        return `Your ${simple} crumbles away!`;
    default:
        return `Your ${simple} turns to dust!`;
    }
}

function armorResistsDisintegration(armor) {
    if (armor.invocation || armor.riderCorpse) return true;
    const chance = rn2(100);
    return chance < (armor.artifact || armor.oartifact ? 90 : 0);
}

function maybeDisintegrateArmor(armor, target, state, key = 'resisted') {
    if (!armor || (target && target !== armor)) return null;
    const resisted = armorResistsDisintegration(armor);
    state[key] = resisted;
    return resisted ? null : armor;
}

function disintegrateArm(target, messages) {
    const bySlot = Object.fromEntries(wornArmorItemsBySlotOrder([
        'cloak', 'body', 'shirt', 'helm', 'gloves', 'boots', 'shield',
    ]).map(armor => [armorSlot(armor), armor]));
    const state = {};
    let armor = maybeDisintegrateArmor(bySlot.cloak, target, state, 'resistedCloak');
    if (!armor && !state.resistedCloak)
        armor = maybeDisintegrateArmor(bySlot.body, target, state, 'resistedSuit');
    if (!armor && !state.resistedCloak && !state.resistedSuit)
        armor = maybeDisintegrateArmor(bySlot.shirt, target, state);
    if (!armor) armor = maybeDisintegrateArmor(bySlot.helm, target, state);
    if (!armor) armor = maybeDisintegrateArmor(bySlot.gloves, target, state);
    if (!armor) armor = maybeDisintegrateArmor(bySlot.boots, target, state);
    if (!armor) armor = maybeDisintegrateArmor(bySlot.shield, target, state);
    if (!armor) return false;

    if (armor.lamplit || armor.burning) {
        armor.lamplit = false;
        armor.burning = false;
        delete armor._burnTimer;
        delete armor.litRadius;
    }
    messages.push(disintegrateArmorMessage(armor));
    destroyWornArmorItem(armor);
    return true;
}

function disintegrateCursedArmor(messages) {
    const armors = wornArmorItemsBySlotOrder(['body', 'cloak', 'helm', 'shield', 'gloves', 'boots', 'shirt'])
        .filter(armor => armor.cursed);
    if (!armors.length) return false;
    return disintegrateArm(armors[rn2(armors.length)], messages);
}

function destroyArmorPromptLetters() {
    return inventoryLetters(isWornArmorItem) || '*';
}

function destroyArmorPromptText() {
    return `What do you want to destroy? [${getobjPromptLetters(destroyArmorPromptLetters())} or ?*]`;
}

function destroyArmorConfusedEffect(scroll, target, messages) {
    if (!target) {
        messages.push('Your bones itch.');
        exerciseAttribute(A_STR, false);
        exerciseAttribute(A_CON, false);
        return { learned: false, more: messages.length > 1 };
    }
    const oldErodeproof = !!target.oerodeproof;
    const newErodeproof = !!scroll.cursed;
    target.oerodeproof = false;
    messages.push(armorGlowMessage(target, 'purple'));
    finishConfusedProofMutation(target, oldErodeproof, newErodeproof, messages);
    updateArmorLine(target);
    return { learned: false, more: messages.length > 1 };
}

function destroyArmorCursedCursedEffect(target, messages) {
    messages.push(`${armorSubject(target)} ${armorVerb(target, 'vibrates', 'vibrate')}.`);
    if ((target.spe ?? 0) >= -6) {
        const oldAc = wornArmorAcValueGreatestErosion(target);
        target.spe = (target.spe ?? 0) - 1;
        updateWornArmorAcAfterChange(target, oldAc);
    } else {
        updateArmorLine(target);
    }
    addHeroStun(rn1(10, 10));
    return { learned: false, more: messages.length > 1 };
}

function destroyArmorScrollEffect(scroll, messages) {
    const target = someArmorTarget();
    if (heroIsConfused()) return destroyArmorConfusedEffect(scroll, target, messages);

    if (scroll.cursed) {
        if (target?.cursed) return destroyArmorCursedCursedEffect(target, messages);
        return { learned: disintegrateArm(target, messages), more: messages.length > 1 };
    }

    if (target && scroll.blessed && countWornArmor() > 1)
        return { learned: true, more: messages.length > 1, prompt: true, target };

    if (scroll.blessed && disintegrateCursedArmor(messages))
        return { learned: true, more: messages.length > 1 };

    const legacy = !scroll.blessed;
    const legacyResult = legacy ? legacyDestroyArm(messages) : null;
    const destroyed = legacy ? legacyResult.changed : destroyArm(messages);
    if (!destroyed) {
        messages.push('Your skin itches.');
        exerciseAttribute(A_STR, false);
        exerciseAttribute(A_CON, false);
        return { learned: false, more: messages.length > 1, skipCall: legacy, legacy, uacDelta: legacyResult?.uacDelta || 0 };
    }
    return { learned: !legacy, more: messages.length > 1, skipCall: legacy, legacy, uacDelta: legacyResult?.uacDelta || 0 };
}

async function finishDestroyArmorSelection(selectedArmor = null) {
    const pending = game._destroy_armor_scroll || {};
    const target = selectedArmor || pending.target || null;
    const messages = [];
    game._destroy_armor_scroll = null;
    game._command_mode = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    game.context.move = 1;
    if (target) disintegrateArm(target, messages);
    if (messages.length) await setMessage(messages.join('  '), messages.length > 1);
    else {
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 1;
    }
}

function isSpecialEnchantArmor(item) {
    const kind = armorKind(item);
    return kind.includes('elven') || ((game.urole?.name?.m || game._startup_role) === 'Wizard' && kind === 'cornuthaum');
}

function isEnchantMagicalArmor(item) {
    return !!item.magic || MAGICAL_ARMOR_KINDS.has(armorKind(item));
}

function enchantArmorColorWord(item, cursed, blind) {
    if (blind) return '';
    const kind = armorKind(item);
    if (cursed && (kind === 'black dragon scale mail' || kind === 'black dragon scales')) return '';
    if (!cursed && (kind === 'silver dragon scale mail' || kind === 'silver dragon scales' || kind === 'shield of reflection')) return '';
    return cursed ? ' black' : ' silver';
}

function enchantArmorConfusedEffect(item, armor) {
    const messages = [];
    const oldErodeproof = !!armor.oerodeproof;
    const newErodeproof = !item.cursed;
    armor.oerodeproof = false;
    const subject = armorSubject(armor);
    if (game.u?.blind) {
        armor.rknown = false;
        messages.push(`${subject} ${armorVerb(armor, 'feels', 'feel')} warm for a moment.`);
    } else {
        armor.rknown = true;
        messages.push(`${subject} ${armorVerb(armor, 'is', 'are')} covered by a ${item.cursed ? 'mottled black glow' : `shimmering golden ${armorSlot(armor) === 'shield' ? 'layer' : 'shield'}`}!`);
    }
    const oldAc = wornArmorAcValue(armor);
    if (newErodeproof && ((armor.oeroded || 0) || (armor.oeroded2 || 0))) {
        armor.oeroded = 0;
        armor.oeroded2 = 0;
        if (game.u) game.u.uac = (game.u.uac ?? 10) + oldAc - wornArmorAcValue(armor);
        messages.push(`${subject} ${armorVerb(armor, game.u?.blind ? 'feels' : 'looks', game.u?.blind ? 'feel' : 'look')} as good as new!`);
    }
    finishConfusedProofMutation(armor, oldErodeproof, newErodeproof, messages);
    if (oldErodeproof && !newErodeproof) armor.rknown = true;
    updateArmorLine(armor);
    return { messages, known: false };
}

function transformDragonScales(armor, blessed) {
    const oldAc = wornArmorAcValue(armor);
    const oldName = armorMessageName(armor);
    const mail = DRAGON_SCALE_MAIL_BY_SCALES.get(armorKind(armor));
    const spec = DRAGON_ARMOR_BY_COLOR.get(mail.replace(/ dragon scale mail$/, ''));
    armor.kind = mail;
    armor.actualKind = mail;
    if (spec) {
        armor.otyp = spec.mailOtyp;
        armor.color = spec.color;
        armor._display_color = spec.color;
    }
    armor.dragonArmor = true;
    armor.dragonArmorKind = 'mail';
    armor.noArticle = false;
    armor.owt = 40;
    if (blessed) {
        armor.spe = capWishSpe((armor.spe ?? 0) + 1);
        armor.blessed = true;
        armor.cursed = false;
    } else if (armor.cursed) {
        armor.cursed = false;
    }
    armor.known = true;
    updateArmorLine(armor);
    if (game.u) game.u.uac = (game.u.uac ?? 10) + oldAc - wornArmorAcValue(armor);
    updateReflectionFromInventory();
    return `Your ${oldName} ${armorVerb({ ...armor, kind: oldName, actualKind: oldName }, 'merges', 'merge')} and ${armorVerb({ ...armor, kind: oldName, actualKind: oldName }, 'hardens', 'harden')}!`;
}

function enchantArmorScrollEffect(item) {
    const armor = someEnchantArmorTarget();
    if (!armor) {
        exerciseAttribute(A_CON, !item.cursed);
        exerciseAttribute(A_STR, !item.cursed);
        return {
            messages: [game.u?.blind ? 'Your skin feels warm for a moment.' : 'Your skin glows then fades.'],
            known: false,
        };
    }
    if (heroIsConfused()) return enchantArmorConfusedEffect(item, armor);

    const specialArmor = isSpecialEnchantArmor(armor);
    let s = item.cursed ? -(armor.spe ?? 0) : (armor.spe ?? 0);
    if (s > (specialArmor ? 5 : 3) && rn2(s)) {
        const subject = armorSubject(armor);
        const color = enchantArmorColorWord(armor, item.cursed, game.u?.blind);
        const oldAc = wornArmorAcValue(armor);
        if (game.u) game.u.uac = (game.u.uac ?? 10) + oldAc;
        removeInventoryItem(armor);
        updateReflectionFromInventory();
        return {
            messages: [`${subject} violently ${armorVerb(armor, game.u?.blind ? 'vibrates' : 'glows', game.u?.blind ? 'vibrate' : 'glow')}${color} for a while, then ${armorVerb(armor, 'evaporates', 'evaporate')}.`],
            known: !!armor.known,
        };
    }
    if (s < -100) s = -100;
    s = Math.trunc((4 - s) / 2);
    if (specialArmor) s++;
    if (!isEnchantMagicalArmor(armor)) s++;
    if (item.blessed) s++;
    if (s <= 0) {
        s = 0;
        if ((armor.spe ?? 0) > 0 && !rn2(armor.spe)) s = 1;
    } else {
        s = rnd(s);
    }
    if (s > 11) s = 11;
    if (item.cursed) s = -s;

    if (s >= 0 && DRAGON_SCALE_MAIL_BY_SCALES.has(armorKind(armor))) {
        return { messages: [transformDragonScales(armor, item.blessed)], known: true };
    }

    const messages = [];
    const color = enchantArmorColorWord(armor, item.cursed, game.u?.blind);
    messages.push(`${armorSubject(armor)} ${s === 0 ? 'violently ' : ''}${armorVerb(armor, game.u?.blind ? 'vibrates' : 'glows', game.u?.blind ? 'vibrate' : 'glow')}${color} for a ${s * s > 1 ? 'while' : 'moment'}.`);

    if (s < 0) {
        const payment = costlyAlterationPaymentMessage(armor, 'disenchant');
        if (payment) messages.push(payment);
    }

    if (item.cursed) {
        armor.cursed = true;
        armor.blessed = false;
    } else if (item.blessed) {
        armor.blessed = true;
        armor.cursed = false;
    } else if (armor.cursed) {
        armor.cursed = false;
    }

    if (s) {
        const oldAc = wornArmorAcValue(armor);
        const oldSpe = armor.spe ?? 0;
        armor.spe = capWishSpe(oldSpe + s);
        s = armor.spe - oldSpe;
        if (s && game.u) game.u.uac = (game.u.uac ?? 10) + oldAc - wornArmorAcValue(armor);
        armor.known = true;
    }
    updateArmorLine(armor);
    if ((armor.spe ?? 0) > (specialArmor ? 5 : 3) && (specialArmor || !rn2(7))) {
        messages.push(`${armorSubject(armor)} suddenly ${armorVerb(armor, 'vibrates', 'vibrate')} ${game.u?.blind ? 'again' : 'unexpectedly'}.`);
    }
    return { messages, known: !!armor.known };
}

function loseAmnesiaSpells() {
    const spells = game._known_spells || [];
    const n = spells.length;
    let nzap = rn2(n + 1);
    if (heroIsConfused()) {
        const confusedZap = rn2(n + 1);
        if (confusedZap > nzap) nzap = confusedZap;
    }
    if (nzap > 1 && !rnl(7)) nzap = rnd(nzap);
    if (!nzap) return;

    const kept = [];
    for (let i = 0; i < n; i++) {
        if (nzap > 0 && rn2(n - i) < nzap) {
            exerciseAttribute(A_WIS, false);
            nzap--;
        } else {
            kept.push(spells[i]);
        }
    }
    game._known_spells = kept;
}

function amnesiaScrollEffect(item) {
    if (!item.blessed) loseAmnesiaSpells();
    rnd(item.blessed ? 3 : 5);
    if (game.u && (game.u.upunished || game._punished)) game.u._bcFelt = 0;
    for (const mon of game.level?.monsters || []) {
        if (mon !== game.u?.usteed && mon !== game.u?.ustuck) mon.meverseen = 0;
    }
    if (game.u?.hallucinating) {
        exerciseAttribute(A_WIS, false);
        return 'Your mind releases itself from mundane concerns.';
    }
    if (/^maud/i.test(game.plname || '')) {
        exerciseAttribute(A_WIS, false);
        return 'As your mind turns inward on itself, you forget everything else.';
    }
    const message = rn2(2)
        ? 'Who was that Maud person anyway?'
        : 'Thinking of Maud you forget everything else.';
    exerciseAttribute(A_WIS, false);
    return message;
}

async function createMonsterScrollEffect(item) {
    const confused = heroIsConfused();
    const count = 1 + ((confused || item.cursed) ? 12 : 0)
        + ((item.blessed || rn2(73)) ? 0 : rnd(4));
    const fixedMonster = confused ? monsterByRndName('acid blob') : null;
    let known = false;
    for (let i = 0; i < count; i++) {
        const mon = await makemon(fixedMonster, game.u?.ux || 0, game.u?.uy || 0, 0);
        if (!mon) continue;
        newsym(mon.mx, mon.my);
        if (couldsee(mon.mx, mon.my) && !mon.mundetected) known = true;
    }
    if (known) learnScrollByName('create monster', item, 6);
    return known;
}

function visibleMonsterForScroll(mon) {
    return !!mon && !mon.dead && (mon.mhp ?? 1) > 0
        && !mon.mundetected && (!mon.minvis || game.u?.seeInvisible) && couldsee(mon.mx, mon.my);
}

function ensurePetExtension(mon) {
    mon.mextra ??= {};
    mon.mextra.edog ??= {
        apport: 3,
        hungrytime: Math.max(game.moves || 1, 1) + 1000,
        dropdist: 10000,
        whistletime: 0,
        ogoal: { x: 0, y: 0 },
    };
}

function baseScrollTameness(mon) {
    const name = String(mon.data?.name || mon.name || '').toLowerCase();
    const mlet = mon.data?.mlet || mon.mlet;
    return mlet === 'dog' || mlet === 'feline' || mlet === 'unicorn'
        || ['little dog', 'dog', 'large dog', 'kitten', 'housecat', 'large cat', 'pony', 'horse', 'warhorse'].includes(name)
        ? 10 : 5;
}

function tameMonsterWithScroll(mon, item) {
    const wasTame = mon.mtame || 0;
    const wasPeaceful = !!(mon.mpeaceful || mon.pet);
    if (item.cursed) {
        mon.mpeaceful = 0;
        mon.pet = false;
        mon.mtame = 0;
        set_malign(mon);
        return wasPeaceful ? -1 : 0;
    }

    if (!mon.isshk && monsterResistsEffect(mon, 9)) return 0;
    if (mon.mfrozen) mon.mfrozen = Math.trunc((mon.mfrozen + 1) / 2);
    if (mon.msleeping) mon.msleeping = 0;
    const monsterName = String(mon.data?.name || mon.name || '').toLowerCase();
    if (monsterName === 'wizard of yendor' || monsterName === 'medusa' || mon.data?.wantsArtifact)
        return 0;

    mon.mpeaceful = 1;
    mon.mflee = 0;
    mon.mfleetim = 0;
    clearMonsterTrack(mon);
    set_malign(mon);

    if ((mon.mtame || 0) && mon.mtame < 10) {
        if (mon.mtame < rnd(10)) mon.mtame++;
        if (item.blessed) mon.mtame = Math.min(10, mon.mtame + 2);
        return (mon.mtame || 0) !== wasTame ? 1 : 0;
    }
    if (mon.isshk || mon.isgd || mon.ispriest || mon.isminion
        || mon.data?.isHuman || mon.data?.human || mon.data?.demon || mon.data?.covetous
        || mon.mcanmove === false)
        return !wasPeaceful && mon.mpeaceful ? 1 : 0;

    mon.pet = true;
    mon.mtame = Math.max(mon.mtame || 0, baseScrollTameness(mon));
    mon.mpeaceful = 1;
    ensurePetExtension(mon);
    newsym(mon.mx, mon.my);
    return (!wasPeaceful || wasTame !== mon.mtame) ? 1 : 0;
}

function tamingScrollEffect(item) {
    const confused = heroIsConfused();
    const radius = confused ? 5 : 1;
    let candidates = 0;
    let results = 0;
    let visibleResults = 0;
    const seen = new Set();
    const candidatesList = [];
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || (mon.mhp ?? 1) <= 0 || mon.mx == null || mon.my == null) continue;
        if (Math.max(Math.abs(mon.mx - (game.u?.ux || 0)), Math.abs(mon.my - (game.u?.uy || 0))) > radius) continue;
        if (seen.has(mon)) continue;
        seen.add(mon);
        candidatesList.push(mon);
    }
    if (game.u?.usteed && !seen.has(game.u.usteed)) {
        seen.add(game.u.usteed);
        candidatesList.push(game.u.usteed);
    }

    for (const mon of candidatesList) {
        candidates++;
        const res = tameMonsterWithScroll(mon, item);
        results += res;
        if (visibleMonsterForScroll(mon)) visibleResults += res;
    }

    if (!results) {
        return { message: `Nothing interesting ${candidates ? 'seems to happen' : 'happens'}.`, known: false };
    }
    if (visibleResults > 0) learnScrollByName('taming', item, 7);
    return {
        message: `The neighborhood ${visibleResults ? 'is' : 'seems'} ${results < 0 ? 'un' : ''}friendlier.`,
        known: visibleResults > 0,
    };
}

function confuseMonsterScrollEffect(item) {
    const confused = heroIsConfused();
    const nonHuman = !!game.u?._polyself_form && game.u._polyself_form.mlet !== 'human';
    if (item.cursed || nonHuman) {
        const wasConfused = confused;
        addHeroConfusion(rnd(100));
        return wasConfused ? '' : 'You feel confused.';
    }
    if (confused) {
        if (item.blessed) {
            clearHeroConfusion();
            return 'A red glow surrounds your head.';
        }
        addHeroConfusion(rnd(100));
        return 'Your hands begin to glow purple.';
    }
    let increment = 3;
    const existing = game.u?.umconf || 0;
    let message;
    if (item.blessed) {
        message = `Your hands glow ${existing ? 'an even more' : 'a'} brilliant red.`;
        increment += rn1(8, 2);
    } else {
        message = existing
            ? 'The red glow of your hands intensifies.'
            : 'Your hands begin to glow red.';
        increment += rnd(2);
    }
    if (existing >= 40) increment = 1;
    if (game.u) {
        game.u.umconf = existing + increment;
        addHeroStatusSuffix('Glow');
    }
    return message;
}

function scareMonsterScrollEffect(item) {
    const confused = heroIsConfused();
    const reverse = confused || item.cursed;
    let visibleUntame = 0;
    for (const mon of game.level?.monsters || []) {
        if (!mon || mon.dead || mon.mhp <= 0 || !couldsee(mon.mx, mon.my)) continue;
        if (reverse) {
            mon.mflee = 0;
            mon.mfrozen = 0;
            mon.msleeping = 0;
            mon.mcanmove = 1;
        } else if (!monsterResistsEffect(mon, 9)) {
            mon.mflee = 1;
            mon.mfleetim = 0;
            clearMonsterTrack(mon);
        }
        if (!mon.mtame && !mon.pet) visibleUntame++;
    }
    return `You hear ${reverse ? 'sad wailing' : 'maniacal laughter'} ${visibleUntame ? 'close by' : 'in the distance'}.`;
}

function applyConfuseMonsterOnHit(mon, messages, targetPhrase) {
    if (!game.u?.umconf || mon?.mconf) return;
    const handsMessage = game.u.umconf === 1
        ? 'Your hands stop glowing red.'
        : 'Your hands no longer glow so brightly red.';
    messages.push(handsMessage);
    game.u.umconf--;
    if (!game.u.umconf) removeHeroStatusSuffix('Glow');
    const attackLevel = game.u?.ulevel || 1;
    if (!monsterResistsEffect(mon, attackLevel)) {
        mon.mconf = 1;
        if (couldsee(mon.mx, mon.my))
            messages.push(`${sentenceCase(targetPhrase)} appears confused.`);
    }
}

function isBoxObject(item) {
    const kind = objectKindKey(item);
    return item?.otyp === LARGE_BOX || item?.otyp === CHEST
        || kind === 'large box' || kind === 'chest';
}

function isWishedContainerObject(item) {
    const kind = objectKindKey(item);
    return isBoxObject(item) || item?.otyp === ICE_BOX || BAG_OBJECT_TYPES.has(item?.otyp)
        || kind === 'ice box' || kind === 'sack' || kind === 'oilskin sack'
        || kind === 'bag of holding';
}

function applyWishedContainerState(item, qualifiers) {
    if (!item) return;
    const box = isBoxObject(item);
    const tin = isTinObject(item);
    if (qualifiers.trappedState && (box || tin))
        item.otrapped = qualifiers.trappedState === 1;
    if (qualifiers.empty && tin && !item._wish_tin_explicit_content)
        setTinEmpty(item);
    if (qualifiers.empty && (objectKindKey(item) === 'bag of tricks' || isHornOfPlentyObject(item)))
        item.spe = 0;
    if (qualifiers.empty && isWishedContainerObject(item))
        item.contents = [];
    if (!box || !qualifiers.lockState) return;
    if (qualifiers.lockState === 'locked') {
        item.olocked = true;
        item.locked = true;
        item.obroken = false;
        item.broken = false;
    } else if (qualifiers.lockState === 'unlocked') {
        item.olocked = false;
        item.locked = false;
        item.obroken = false;
        item.broken = false;
    } else if (qualifiers.lockState === 'broken') {
        item.olocked = false;
        item.locked = false;
        item.obroken = true;
        item.broken = true;
        item.otrapped = false;
    }
}

const FIRE_FLAMMABLE_ARMOR_KINDS = new Set([
    'elven leather helm', 'fedora', 'cornuthaum', 'dunce cap',
    'studded leather armor', 'leather armor', 'leather jacket',
    'hawaiian shirt', 't-shirt',
    'mummy wrapping', 'elven cloak', 'orcish cloak', 'dwarvish cloak', 'oilskin cloak',
    'robe', 'alchemy smock', 'leather cloak', 'cloak of protection',
    'cloak of invisibility', 'cloak of magic resistance', 'cloak of displacement',
    'small shield', 'shield of drain resistance', 'shield of shock resistance', 'elven shield',
    'leather gloves', 'gauntlets of fumbling', 'gauntlets of dexterity',
    'low boots', 'high boots', 'speed boots', 'water walking boots', 'jumping boots',
    'elven boots', 'fumble boots', 'levitation boots',
]);

const FIRE_NONFLAMMABLE_ARMOR_KINDS = new Set([
    'orcish helm', 'dwarvish iron helm', 'dented pot', 'helm of brilliance',
    'helmet', 'helm of caution', 'helm of opposite alignment', 'helm of telepathy',
    'plate mail', 'crystal plate mail', 'bronze plate mail', 'splint mail',
    'banded mail', 'dwarvish mithril-coat', 'elven mithril-coat', 'chain mail',
    'orcish chain mail', 'scale mail', 'ring mail', 'orcish ring mail',
    'uruk-hai shield', 'orcish shield', 'large shield', 'dwarvish roundshield',
    'shield of reflection', 'gauntlets of power', 'iron shoes', 'kicking boots',
]);

function wishedDamageProfile(item) {
    const kind = objectKindKey(item);
    const cls = itemClassKey(item);
    const weapon = cls === 'weapon' || item?.glyph === ')';
    const armor = cls === 'armor' || item?.glyph === '[';
    const ballOrChain = item?.otyp === HEAVY_IRON_BALL || item?.otyp === IRON_CHAIN || cls === 'ball' || cls === 'chain';
    const weptool = cls === 'tool' && APPLY_WEAPON_NAME_RE.test(kind);
    const erosionMatters = weapon || armor || ballOrChain || weptool;
    if (!erosionMatters) return { erosionMatters: false };

    const excludedMetal = /\b(?:silver|gold|mithril|platinum|gem|stone|crystal ball|shield of reflection)\b/.test(kind);
    const glassArmor = armor && /\b(?:crystal plate mail|helm of brilliance|crystal helmet)\b/.test(kind);
    const dragonHide = armor && /\bdragon (?:scales|scale mail|hide)\b/.test(kind);
    const flammableArmor = armor && FIRE_FLAMMABLE_ARMOR_KINDS.has(kind);
    const nonflammableArmor = armor && FIRE_NONFLAMMABLE_ARMOR_KINDS.has(kind);
    const copperLike = /\b(?:copper|bronze)\b/.test(kind);
    const ironLike = !flammableArmor && !excludedMetal && !glassArmor && !copperLike && !dragonHide && (ballOrChain || kind.includes('iron')
        || /\b(?:plate mail|splint mail|banded mail|ring mail|chain mail|scale mail|large shield|roundshield|gauntlets|helm|helmet|dented pot|shoes|sword|saber|dagger|knife|axe|mace|hammer|flail|morning star|pick-axe|mattock|spear|trident|lance|polearm|dart|shuriken|throwing star)\b/.test(kind));
    const flammableLike = flammableArmor || (!nonflammableArmor && !glassArmor && !ironLike && !copperLike && !excludedMetal
        && /\b(?:leather|cloth|cloak|robe|shirt|boots|gloves|wooden|small shield|bow|crossbow|arrow|club|quarterstaff|aklys|bullwhip|sling)\b/.test(kind));
    const rustprone = ironLike;
    const crackable = glassArmor;
    const corrodeable = ironLike || copperLike;
    const flammable = flammableLike;
    const rottable = flammableLike || dragonHide;
    const damageable = rustprone || crackable || corrodeable || flammable || rottable;

    return {
        erosionMatters,
        primary: rustprone || crackable || flammable,
        secondary: corrodeable || rottable,
        damageable,
        primaryWord: rustprone ? 'rusty' : crackable ? 'cracked' : flammable ? 'burnt' : '',
        secondaryWord: corrodeable ? 'corroded' : rottable ? 'rotted' : '',
        proofWord: rustprone ? 'rustproof' : corrodeable ? 'corrodeproof'
            : flammable ? 'fireproof' : crackable ? 'tempered'
                : rottable ? 'rotproof' : '',
    };
}

function wishedPoisonable(item) {
    const kind = objectKindKey(item);
    return item?.artifact === 'Grimtooth' || POISONABLE_WISH_WEAPONS.has(kind);
}

function applyWishedQualifiers(item, qualifiers) {
    if (!item) return;
    const profile = wishedDamageProfile(item);
    if (profile.erosionMatters) {
        item.oeroded = 0;
        item.oeroded2 = 0;
        if (qualifiers.eroded && profile.primary)
            item.oeroded = Math.min(3, qualifiers.eroded);
        if (qualifiers.eroded2 && profile.secondary)
            item.oeroded2 = Math.min(3, qualifiers.eroded2);
        if (qualifiers.erodeproof && profile.damageable)
            item.oerodeproof = ((game.u?.uluck || 0) + (game.u?.moreluck || 0)) >= 0 || !!game.flags?.debug;
    }
    if (qualifiers.greased) item.greased = true;
    if (qualifiers.diluted && isPotionObject(item) && !isWaterPotion(item))
        item.odiluted = true;
    applyWishedLightState(item, qualifiers.lightState);
    if (qualifiers.halfeaten)
        applyWishedPartlyEaten(item);
    if (qualifiers.poisoned) {
        if (wishedPoisonable(item)) item.opoisoned = ((game.u?.uluck || 0) + (game.u?.moreluck || 0)) >= 0;
        else if (itemClassKey(item) === 'food') item.age = 1;
    }
    if (qualifiers.zombifying && (item.otyp === CORPSE || /\bcorpse$/.test(objectKindKey(item)))) {
        item.zombifying = true;
        if (!item.zombifyTurn) startCorpseTimeout(item, { zombify: true });
    }
    if (qualifiers.wetness && objectKindKey(item) === 'towel') {
        item.wetness = qualifiers.wetness;
        item.spe = qualifiers.wetness;
    }
    applyWishedContainerState(item, qualifiers);
}

function applyWishedQuantity(item, wishedQuan, forceQuantity = false) {
    const quantity = Math.max(1, Math.trunc(Number(wishedQuan || 1)));
    if (item?.globby) {
        applyWishedGlobQuantity(item, quantity);
    } else if (item?.dragonArmor) {
        item.quan = 1;
    } else if (isTinObject(item)) {
        if (game.flags?.debug || quantity < rnd(6))
            item.quan = quantity;
    } else if (wishQuantityMergeable(item)) {
        if (game.flags?.debug || quantity < rnd(6) || wishQuantityAlwaysAllowed(item, quantity))
            item.quan = quantity;
    } else if (forceQuantity) {
        item.quan = 1;
    }
}

function wishQuantityMergeable(item) {
    const metadata = wishObjectMetadataForItem(item);
    if (metadata?.ocMerge != null) return !!metadata.ocMerge;
    const cls = itemClassKey(item);
    if (cls === 'scroll' || cls === 'potion' || cls === 'gem') return true;
    if (cls === 'food' && objectKindKey(item) !== 'meat ring') return true;
    if (cls === 'weapon') return true;
    if (cls === 'venom') return true;
    if (isCandleObject(item)) return true;
    return false;
}

function wishedAmmoOrMissile(item) {
    const kind = objectKindKey(item);
    return item?.otyp === DART
        || /\b(?:arrow|arrows|bolt|bolts|dart|darts|shuriken|throwing star|throwing stars|rock|rocks|flint)\b/.test(kind);
}

function wishQuantityAlwaysAllowed(item, quantity) {
    if (quantity <= 7 && isCandleObject(item)) return true;
    return quantity <= 20 && (wishedAmmoOrMissile(item)
        || item?.otyp === ROCK || item?.otyp === FLINT || ['rock', 'flint'].includes(objectKindKey(item)));
}

function applyWishedTinVariety(item) {
    if (!isTinObject(item) || item._wish_tin_requested_variety == null) return;
    const varietyIndex = item._wish_tin_requested_variety;
    if (game.flags?.debug || rn2(4)) {
        if (item.corpsenm?.name) setTinMonster(item, item.corpsenm, varietyIndex);
        else item.spe = -(varietyIndex + 1);
    }
}

function splitWishedTextClause(text, clause) {
    const match = String(text || '').match(new RegExp(`\\s+${clause}\\s+`, 'i'));
    if (!match) return null;
    return {
        base: String(text).slice(0, match.index).trim(),
        tail: String(text).slice(match.index + match[0].length).trim(),
    };
}

function splitWishedLabelClause(text) {
    const match = String(text || '').match(/\s+labell?ed\s+/i);
    if (!match) return null;
    return {
        base: String(text).slice(0, match.index).trim(),
        tail: String(text).slice(match.index + match[0].length).trim(),
    };
}

function unquoteWishedText(text) {
    const trimmed = String(text || '').trim();
    const quoted = trimmed.match(/^"([^"]*)"$/) || trimmed.match(/^'([^']*)'$/);
    return (quoted ? quoted[1] : trimmed).trim();
}

function wishedLabelKey(text) {
    return unquoteWishedText(text).toLowerCase().replace(/\s+/g, ' ');
}

function calledWishymatch(userText, objectText) {
    return wishedFuzzyObjectKey(userText) === wishedFuzzyObjectKey(objectText);
}

function calledTailMatchesObjectName(called, objectName) {
    if (calledWishymatch(called, objectName)) return true;
    const of = String(objectName || '').toLowerCase().indexOf(' of ');
    return of >= 0 && calledWishymatch(called, String(objectName).slice(of + 4));
}

function calledRangeNamedescName(base, called) {
    const range = WISH_OBJECT_RANGES.get(base);
    if (!range) return '';
    const rangeNames = new Set(range.map(([name]) => name));
    for (const [namedesc, candidates] of WISH_NAMEDESC_RANGES) {
        if (!calledWishymatch(called, namedesc)) continue;
        if (candidates.every(([name]) => rangeNames.has(name))) return namedesc;
    }
    for (const [name] of range) {
        if (calledTailMatchesObjectName(called, name)) return name;
        const baseObject = WISH_BASE_OBJECTS.get(name);
        if (baseObject?.actualKind && calledTailMatchesObjectName(called, baseObject.actualKind))
            return name;
        const armorAppearance = ARMOR_WISH_APPEARANCES[name];
        if (armorAppearance) {
            const [group, index, fallback] = armorAppearance;
            const appearance = group ? game._object_descriptions?.[group]?.[index] || fallback : fallback;
            if (appearance && calledTailMatchesObjectName(called, appearance)) return name;
        }
        const toolAppearance = WISH_TOOL_APPEARANCES.get(name);
        if (toolAppearance && calledTailMatchesObjectName(called, toolAppearance)) return name;
    }
    return '';
}

function calledRangeBaseNamedescName(base) {
    const range = WISH_OBJECT_RANGES.get(base);
    if (!range) return '';
    const matches = [];
    for (const [name, prob] of range) {
        let matched = calledTailMatchesObjectName(base, name);
        const baseObject = WISH_BASE_OBJECTS.get(name);
        if (!matched && baseObject?.actualKind)
            matched = calledTailMatchesObjectName(base, baseObject.actualKind);
        const armorAppearance = ARMOR_WISH_APPEARANCES[name];
        if (!matched && armorAppearance) {
            const [group, index, fallback] = armorAppearance;
            const appearance = group ? game._object_descriptions?.[group]?.[index] || fallback : fallback;
            matched = appearance && calledTailMatchesObjectName(base, appearance);
        }
        const toolAppearance = WISH_TOOL_APPEARANCES.get(name) || WISH_RANGE_BASE_DESCRIPTIONS.get(name);
        if (!matched && toolAppearance)
            matched = calledTailMatchesObjectName(base, toolAppearance);
        if (matched) matches.push([name, prob + 1]);
    }
    const total = matches.reduce((sum, [, prob]) => sum + prob, 0);
    if (total <= 0) return '';
    let roll = rn2(total);
    for (const [name, prob] of matches) {
        roll -= prob;
        if (roll < 0) return name;
    }
    return matches[0]?.[0] || '';
}

function parseWishedScrollLabel(name) {
    const match = String(name || '').trim().match(/^scrolls?\s+labell?ed\s+(.+)$/i);
    return match ? unquoteWishedText(match[1]) : '';
}

function resolveCalledWishName(baseName, calledName) {
    const base = String(baseName || '').trim().toLowerCase();
    const called = wishedLabelKey(calledName);
    const explicit = new Map([
        ['shield:reflection', 'shield of reflection'],
        ['helm:telepathy', 'helm of telepathy'],
        ['amulet:life saving', 'amulet of life saving'],
        ['amulet:esp', 'amulet of esp'],
        ['amulet:guarding', 'amulet of guarding'],
        ['ring:accuracy', 'ring of increase accuracy'],
        ['ring:increase accuracy', 'ring of increase accuracy'],
        ['ring:protection from shape changers', 'ring of protection from shape changers'],
        ['ring:protection from shape shifters', 'ring of protection from shape changers'],
    ]).get(`${base}:${called}`);
    if (explicit) return explicit;
    const rangeCalled = calledRangeNamedescName(base, called);
    if (rangeCalled) return rangeCalled;
    if (base === 'amulet' || base === 'ring') {
        const candidate = `${base} of ${called}`;
        return WISH_BASE_OBJECTS.has(candidate)
            || WISH_NAME_ALIASES.has(candidate)
            || IDENTIFIED_AMULET_NAMES.includes(candidate)
            || IDENTIFIED_RING_NAMES.includes(candidate.replace(/^ring of /, ''))
            || ARMOR_WISH_APPEARANCES[candidate]
            ? candidate : '';
    }
    return '';
}

const WISH_NAME_ALIASES = new Map([
    ['speedboots', 'speed boots'],
    ['boots of speed', 'speed boots'],
    ['boots of water walking', 'water walking boots'],
    ['boots of jumping', 'jumping boots'],
    ['boots of kicking', 'kicking boots'],
    ['boots of levitation', 'levitation boots'],
    ['water-walking boots', 'water walking boots'],
    ['waterwalking boots', 'water walking boots'],
    ['jackboots', 'high boots'],
    ['combat boots', 'speed boots'],
    ['jungle boots', 'water walking boots'],
    ['hiking boots', 'jumping boots'],
    ['mud boots', 'elven boots'],
    ['buckled boots', 'kicking boots'],
    ['riding boots', 'fumble boots'],
    ['snow boots', 'levitation boots'],
    ['elvish boots', 'elven boots'],
    ['elfin boots', 'elven boots'],
    ['plate armor', 'plate mail'],
    ['scroll of detect food', 'scroll of food detection'],
    ['detect food scroll', 'scroll of food detection'],
    ['scroll of detect gold', 'scroll of gold detection'],
    ['detect gold scroll', 'scroll of gold detection'],
    ['blank scroll', 'scroll of blank paper'],
    ['unlabeled scroll', 'scroll of blank paper'],
    ['unlabelled scroll', 'scroll of blank paper'],
    ['spellbook of food detection', 'spellbook of detect food'],
    ['destroy armor', 'scroll of destroy armor'],
    ['enchant weapon', 'scroll of enchant weapon'],
    ['ring of accuracy', 'ring of increase accuracy'],
    ['accuracy', 'ring of increase accuracy'],
    ['bear trap', 'beartrap'],
    ['landmine', 'land mine'],
    ['vulgar polearm', 'partisan'],
    ['hilted polearm', 'ranseur'],
    ['forked polearm', 'spetum'],
    ['polearms', 'polearm'],
    ['single-edged polearm', 'glaive'],
    ['single edged polearm', 'glaive'],
    ['angled poleaxe', 'halberd'],
    ['long poleaxe', 'bardiche'],
    ['pole cleaver', 'voulge'],
    ['pole sickle', 'fauchard'],
    ['pruning hook', 'guisarme'],
    ['hooked polearm', 'bill-guisarme'],
    ['bill guisarme', 'bill-guisarme'],
    ['pronged polearm', 'lucern hammer'],
    ['beaked polearm', 'bec de corbin'],
    ['bags of tricks', 'bag of tricks'],
    ['wolfsbane', 'sprig of wolfsbane'],
    ['garlic', 'clove of garlic'],
    ['royal jelly', 'lump of royal jelly'],
    ['can', 'tin'],
    ['gloves of fumbling', 'gauntlets of fumbling'],
    ['gloves of power', 'gauntlets of power'],
    ['gloves of dexterity', 'gauntlets of dexterity'],
    ['elvish cloak', 'elven cloak'],
    ['elfin cloak', 'elven cloak'],
    ['dwarven cloak', 'dwarvish cloak'],
    ['cloaks', 'cloak'],
    ['protection cloak', 'cloak of protection'],
    ['invisibility cloak', 'cloak of invisibility'],
    ['magic resistance cloak', 'cloak of magic resistance'],
    ['displacement cloak', 'cloak of displacement'],
    ['shields', 'shield'],
    ['reflection shield', 'shield of reflection'],
    ['drain resistance shield', 'shield of drain resistance'],
    ['shock resistance shield', 'shield of shock resistance'],
    ['hats', 'hat'],
    ['helms', 'helm'],
    ['helmets', 'helmet'],
    ['helmet of brilliance', 'helm of brilliance'],
    ['helmet of caution', 'helm of caution'],
    ['helmet of opposite alignment', 'helm of opposite alignment'],
    ['helmet of telepathy', 'helm of telepathy'],
    ['elvish shield', 'elven shield'],
    ['elfin shield', 'elven shield'],
    ['dwarven roundshield', 'dwarvish roundshield'],
    ['dwarvish round shield', 'dwarvish roundshield'],
    ['dwarven round shield', 'dwarvish roundshield'],
    ['uruk hai shield', 'uruk-hai shield'],
    ['uruk-hai shields', 'uruk-hai shield'],
    ['uruk hai shields', 'uruk-hai shield'],
    ['walking shoes', 'low boots'],
    ['hard shoes', 'iron shoes'],
    ['shirts', 'shirt'],
    ['t shirt', 't-shirt'],
    ['t shirts', 't-shirt'],
    ['ring of protection from shape shifters', 'ring of protection from shape changers'],
    ['flint stone', 'flint'],
]);
const WISH_EXPLICIT_SPELLING_ALIASES = new Map([
    ['wakizashi', 'short sword'],
    ['ninja-to', 'broadsword'],
    ['nunchaku', 'flail'],
    ['naginata', 'glaive'],
    ['osaku', 'lock pick'],
    ['koto', 'wooden harp'],
    ['magic koto', 'magic harp'],
    ['shito', 'knife'],
    ['tanko', 'plate mail'],
    ['kabuto', 'helmet'],
    ['yugake', 'leather gloves'],
    ['gunyoki', 'food ration'],
    ['sake', 'potion of booze'],
    ['whip', 'bullwhip'],
    ['saber', 'silver saber'],
    ['silver sabre', 'silver saber'],
    ['iron ball', 'heavy iron ball'],
    ['mattock', 'dwarvish mattock'],
    ['amulet of poison resistance', 'amulet versus poison'],
    ['amulet of protection', 'amulet of guarding'],
    ['amulet of telepathy', 'amulet of esp'],
    ['helm of esp', 'helm of telepathy'],
    ['helmet of esp', 'helm of telepathy'],
    ['potion of sleep', 'potion of sleeping'],
    ['scroll of recharging', 'scroll of charging'],
    ['recharging', 'scroll of charging'],
    ['stone', 'rock'],
    ['tee shirt', 't-shirt'],
    ['tee shirts', 't-shirt'],
    ['kelp', 'kelp frond'],
    ['eucalyptus', 'eucalyptus leaf'],
    ['lembas', 'lembas wafer'],
    ['k ration', 'k-ration'],
    ['k rations', 'k-rations'],
    ['kration', 'k-ration'],
    ['krations', 'k-rations'],
    ['c ration', 'c-ration'],
    ['c rations', 'c-rations'],
    ['cration', 'c-ration'],
    ['crations', 'c-rations'],
    ['tripe', 'tripe ration'],
    ['cookie', 'fortune cookie'],
    ['pie', 'cream pie'],
    ['huge meatball', 'enormous meatball'],
    ['huge chunk of meat', 'enormous meatball'],
    ['lantern', 'brass lantern'],
    ['camera', 'expensive camera'],
    ['marker', 'magic marker'],
    ['silver bell', 'bell of opening'],
    ['can opener', 'tin opener'],
    ['hook', 'grappling hook'],
    ['grappling iron', 'grappling hook'],
    ['grapnel', 'grappling hook'],
    ['grapple', 'grappling hook'],
    ['smooth shield', 'shield of reflection'],
    ['silver shield', 'shield of reflection'],
    ['gauntlets of ogre power', 'gauntlets of power'],
    ['gauntlets of giant strength', 'gauntlets of power'],
    ['gloves of ogre power', 'gauntlets of power'],
    ['gloves of giant strength', 'gauntlets of power'],
    ['elven chain mail', 'elven mithril-coat'],
    ['protection from shape shifters', 'ring of protection from shape changers'],
    ['box', 'large box'],
    ['luck stone', 'luckstone'],
    ['load stone', 'loadstone'],
    ['touch stone', 'touchstone'],
    ['flintstone', 'flint'],
]);

const WISH_NAMEDESC_RANGES = new Map([
    ['wooden shield', [
        ['small shield', 7],
        ['shield of drain resistance', 13],
        ['shield of shock resistance', 13],
    ]],
    ['conical hat', [
        ['cornuthaum', 6],
        ['dunce cap', 6],
    ]],
]);

function normalizeWishedSpelling(name) {
    return String(name || '').replace(/armour/ig, match => match[0] === 'A' ? 'Armor' : 'armor');
}

function wishedFuzzyObjectKey(text) {
    return wishedLabelKey(text).replace(/[\s-]+/g, '');
}

function resolveWishedArmorAppearanceAlias(normalized) {
    const key = wishedFuzzyObjectKey(normalized);
    if (!key) return '';
    for (const name of WISH_NAMEDESC_RANGES.keys()) {
        if (wishedFuzzyObjectKey(name) === key) return name;
    }
    for (const [kind, armorAppearance] of Object.entries(ARMOR_WISH_APPEARANCES)) {
        const [group, index, fallback] = armorAppearance;
        const appearance = group ? game._object_descriptions?.[group]?.[index] || fallback : fallback;
        if (appearance && wishedFuzzyObjectKey(appearance) === key) return kind;
    }
    return '';
}

function resolveWishedSpellingAlias(lowerName) {
    const normalized = String(lowerName || '').trim().replace(/\s+/g, ' ');
    const explicit = WISH_EXPLICIT_SPELLING_ALIASES.get(normalized);
    if (explicit) return { name: explicit, skipNamedesc: true };
    const appearance = resolveWishedArmorAppearanceAlias(normalized);
    if (appearance) return { name: appearance, skipNamedesc: false };
    const alias = WISH_NAME_ALIASES.get(normalized);
    if (alias) return { name: alias, skipNamedesc: false };
    return { name: singularizeWishedPluralName(normalized), skipNamedesc: false };
}

function singularizeWishedPluralName(normalized) {
    if (normalized === 'venoms') return 'venom';
    if (normalized === 'splashes of venom') return 'splash of venom';
    if (normalized === 'splashes of blinding venom') return 'splash of blinding venom';
    if (normalized === 'splashes of acid venom') return 'splash of acid venom';
    for (const [name, baseObject] of WISH_BASE_OBJECTS.entries()) {
        if (String(baseObject?.plural || '').toLowerCase() === normalized) return name;
    }
    for (const [plural, singular] of [
        [/^wands of (.+)$/, 'wand of $1'],
        [/^rings of (.+)$/, 'ring of $1'],
        [/^potions of (.+)$/, 'potion of $1'],
        [/^spellbooks of (.+)$/, 'spellbook of $1'],
        [/^scrolls of (.+)$/, 'scroll of $1'],
    ]) {
        const match = normalized.match(plural);
        if (match) return singular.replace('$1', match[1]);
    }
    return normalized;
}

function makeWishedGrayStoneObject(lowerName) {
    if (lowerName !== 'gray stone' && lowerName !== 'grey stone') return null;
    const roll = rnd(38);
    const name = roll <= 10 ? 'luckstone'
        : roll <= 20 ? 'loadstone'
            : roll <= 28 ? 'touchstone'
                : 'flint';
    const baseObject = WISH_BASE_OBJECTS.get(name);
    const otmp = mksobj(baseObject.otyp, true, false);
    return Object.assign(otmp, baseObject, {
        kind: 'gray stone',
        actualKind: name,
        gemDescription: 'gray stone',
        _display_color: CLR_GRAY,
        wishedfor: true,
    });
}

function wishedNamedescRangeName(lowerName) {
    const range = WISH_NAMEDESC_RANGES.get(lowerName);
    if (!range) return '';
    const total = range.reduce((sum, [, prob]) => sum + prob, 0);
    let roll = rn2(total);
    for (const [name, prob] of range) {
        roll -= prob;
        if (roll < 0) return name;
    }
    return range[0]?.[0] || '';
}

function makeWishedNamedescRangeObject(lowerName) {
    const rangeName = wishedNamedescRangeName(lowerName);
    if (!rangeName) return null;
    const baseObject = WISH_BASE_OBJECTS.get(rangeName);
    if (!baseObject) return null;
    const baseFields = { ...baseObject };
    delete baseFields.wishSpeRn1;
    const metadata = wishObjectMetadataForName(rangeName) ?? wishObjectMetadataForItem(baseFields);
    const otmp = makeWishedBaseObject(baseObject, metadata);
    return Object.assign(otmp, baseFields, { wishedfor: true });
}

const WISH_GEM_COLORS = new Map([
    ['white', CLR_WHITE],
    ['blue', CLR_BLUE],
    ['red', CLR_RED],
    ['yellowish brown', CLR_BROWN],
    ['orange', CLR_ORANGE],
    ['yellow', CLR_YELLOW],
    ['black', CLR_BLACK],
    ['green', CLR_GREEN],
    ['violet', CLR_MAGENTA],
]);
const WISH_REAL_GEMS = [
    { name: 'dilithium crystal', description: 'white', prob: 2, displayName: 'dilithium crystal' },
    { name: 'diamond', description: 'white', prob: 3, displayName: 'diamond' },
    { name: 'ruby', description: 'red', prob: 4, displayName: 'ruby' },
    { name: 'jacinth', description: 'orange', prob: 3, displayName: 'jacinth stone' },
    { name: 'sapphire', description: 'blue', prob: 4, displayName: 'sapphire' },
    { name: 'black opal', description: 'black', prob: 3, displayName: 'black opal' },
    { name: 'emerald', description: 'green', prob: 5, displayName: 'emerald' },
    { name: 'turquoise', description: 'green', prob: 6, displayName: 'turquoise stone' },
    { name: 'citrine', description: 'yellow', prob: 4, displayName: 'citrine stone' },
    { name: 'aquamarine', description: 'green', prob: 6, displayName: 'aquamarine stone' },
    { name: 'amber', description: 'yellowish brown', prob: 8, displayName: 'amber stone' },
    { name: 'topaz', description: 'yellowish brown', prob: 10, displayName: 'topaz stone' },
    { name: 'jet', description: 'black', prob: 6, displayName: 'jet stone' },
    { name: 'opal', description: 'white', prob: 12, displayName: 'opal' },
    { name: 'chrysoberyl', description: 'yellow', prob: 8, displayName: 'chrysoberyl stone' },
    { name: 'garnet', description: 'red', prob: 12, displayName: 'garnet stone' },
    { name: 'amethyst', description: 'violet', prob: 14, displayName: 'amethyst stone' },
    { name: 'jasper', description: 'red', prob: 15, displayName: 'jasper stone' },
    { name: 'fluorite', description: 'violet', prob: 15, displayName: 'fluorite stone' },
    { name: 'obsidian', description: 'black', prob: 9, displayName: 'obsidian stone' },
    { name: 'agate', description: 'orange', prob: 12, displayName: 'agate stone' },
    { name: 'jade', description: 'green', prob: 10, displayName: 'jade stone' },
];
const WISH_GLASS_GEMS = [
    { name: 'worthless piece of white glass', description: 'white', prob: 77, displayName: 'worthless piece of white glass' },
    { name: 'worthless piece of blue glass', description: 'blue', prob: 77, displayName: 'worthless piece of blue glass' },
    { name: 'worthless piece of red glass', description: 'red', prob: 77, displayName: 'worthless piece of red glass' },
    { name: 'worthless piece of yellowish brown glass', description: 'yellowish brown', prob: 77, displayName: 'worthless piece of yellowish brown glass' },
    { name: 'worthless piece of orange glass', description: 'orange', prob: 76, displayName: 'worthless piece of orange glass' },
    { name: 'worthless piece of yellow glass', description: 'yellow', prob: 77, displayName: 'worthless piece of yellow glass' },
    { name: 'worthless piece of black glass', description: 'black', prob: 76, displayName: 'worthless piece of black glass' },
    { name: 'worthless piece of green glass', description: 'green', prob: 77, displayName: 'worthless piece of green glass' },
    { name: 'worthless piece of violet glass', description: 'violet', prob: 77, displayName: 'worthless piece of violet glass' },
];
const WISH_GEMS = [...WISH_REAL_GEMS, ...WISH_GLASS_GEMS];
const WISH_GEMS_BY_DISPLAY = new Map();
for (const gem of WISH_GEMS) {
    WISH_GEMS_BY_DISPLAY.set(gem.name, gem);
    WISH_GEMS_BY_DISPLAY.set(gem.displayName, gem);
}

function wishedGemObject(spec, { description = spec?.description || '', consumeNamedesc = true } = {}) {
    if (!spec) return null;
    if (consumeNamedesc) rn2(spec.prob + 1);
    const otmp = mksobj(GEM_CLASS, false, false);
    const display = spec.displayName || spec.name;
    const color = WISH_GEM_COLORS.get(spec.description) ?? NO_COLOR;
    return Object.assign(otmp, {
        cls: 'gem',
        glyph: '*',
        kind: display,
        actualKind: display,
        gemDescription: description ? `${description} gem` : display,
        color,
        _display_color: color,
        wishedfor: true,
    });
}

function weightedWishedGem(candidates) {
    const total = candidates.reduce((sum, gem) => sum + gem.prob + 1, 0);
    let roll = rn2(total);
    for (const gem of candidates) {
        roll -= gem.prob + 1;
        if (roll < 0) return gem;
    }
    return candidates[0] || null;
}

function canonicalWishedGlassName(lowerName) {
    if (lowerName === 'looking glass') return '';
    let name = String(lowerName || '').trim();
    if (name === 'glass') {
        const gem = WISH_GLASS_GEMS[rn2(WISH_GLASS_GEMS.length)];
        return gem?.name || '';
    }
    if (!name.endsWith(' glass')) return '';
    name = name.replace(/^worthless\s+/, '');
    name = name.replace(/^pieces?\s+of\s+/, '');
    name = name.replace(/^colou?red\s+/, '');
    if (name === 'glass') {
        const gem = WISH_GLASS_GEMS[rn2(WISH_GLASS_GEMS.length)];
        return gem?.name || '';
    }
    return `worthless piece of ${name}`;
}

function makeWishedGemObject(lowerName) {
    let name = String(lowerName || '').trim().replace(/^worthless pieces of /, 'worthless piece of ');
    const suffix = name.match(/^(.+?)\s+(?:gems?|stones?)$/);
    if (suffix) {
        const description = suffix[1].trim();
        const candidates = WISH_GEMS.filter(gem => gem.description === description || gem.name === description || gem.displayName === description);
        if (candidates.length) return wishedGemObject(weightedWishedGem(candidates), { description, consumeNamedesc: false });
    }
    const glassName = canonicalWishedGlassName(name);
    if (glassName) name = glassName;
    const exact = WISH_GEMS_BY_DISPLAY.get(name);
    if (exact) return wishedGemObject(exact);
    return null;
}

function normalizeWishedGroupPhrase(name, quantity) {
    const match = String(name || '').match(/^(pair|pairs|set|sets)\s+of\s+/i);
    if (!match) return { name, quantity, matched: false };
    const objectName = String(name).slice(match[0].length);
    const nonStackingPairObject = /\b(?:boots|shoes|gloves|gauntlets|lenses)\b/i.test(objectName);
    let wishedQuantity = quantity;
    const group = match[1].toLowerCase();
    if (nonStackingPairObject) wishedQuantity = 1;
    else if (group === 'pair') wishedQuantity *= 2;
    else if (!nonStackingPairObject && group === 'pairs' && quantity > 1) wishedQuantity *= 2;
    return {
        name: objectName,
        quantity: wishedQuantity,
        matched: true,
    };
}

function applyWishedPluralQuantity(name, quantity) {
    if (quantity !== 1) return quantity;
    const rawLowerName = String(name || '').trim().toLowerCase();
    if (rawLowerName === 'venoms' || rawLowerName === 'splashes of venom'
        || rawLowerName === 'splashes of blinding venom' || rawLowerName === 'splashes of acid venom')
        return 2;
    const rawFruit = fruitWishMatch(rawLowerName);
    if (rawFruit?.plural) return 2;
    const rawBaseObject = WISH_BASE_OBJECTS.get(rawLowerName);
    if (rawBaseObject?.plural && rawLowerName === String(rawBaseObject.plural).toLowerCase()) return 2;
    const lowerName = resolveWishedSpellingAlias(rawLowerName).name;
    const fruit = fruitWishMatch(lowerName);
    if (fruit?.plural) return 2;
    const baseObject = WISH_BASE_OBJECTS.get(lowerName);
    if (baseObject?.plural && lowerName === String(baseObject.plural).toLowerCase()) return 2;
    if (/^(?:worthless\s+)?pieces\s+of\s+.+\s+glass$/.test(lowerName)) return 2;
    if (/\bgems$/.test(lowerName) || /\bstones$/.test(lowerName)) return 2;
    if (lowerName === 'fortune cookies') return 2;
    return quantity;
}

function applyWishedInstanceName(item, name) {
    const objectName = String(name || '').trim();
    if (!item || !objectName || item.artifact) return;
    item.oname = objectName;
    item._wish_object_name = objectName;
}

function makeWishedBaseObject(baseObject, metadata) {
    const forceBagOfTricksTool = !!metadata?.forceBagOfTricksTool;
    if (forceBagOfTricksTool) game._mkobj_force_bag_of_tricks = true;
    try {
        return mksobj(baseObject.otyp, true, false);
    } finally {
        if (forceBagOfTricksTool) game._mkobj_force_bag_of_tricks = false;
    }
}

function wishedObjectRangeName(lowerName) {
    const range = WISH_OBJECT_RANGES.get(lowerName);
    if (!range) return '';
    const total = range.reduce((sum, [, prob]) => sum + prob, 0);
    if (total <= 0) return range[rn2(range.length)]?.[0] || '';
    let roll = rnd(total);
    for (const [name, prob] of range) {
        roll -= prob;
        if (roll <= 0) return name;
    }
    return range[0]?.[0] || '';
}

function makeWishedObjectRangeObject(lowerName) {
    const rangeName = wishedObjectRangeName(lowerName);
    if (!rangeName) return null;
    return makeWishedRangeObjectByName(rangeName);
}

function makeWishedRangeObjectByName(rangeName) {
    if (rangeName === 'magic lamp' && !game.flags?.debug)
        return makeOilLampFromMagicLampWishObject();
    const baseObject = WISH_BASE_OBJECTS.get(rangeName);
    if (!baseObject) return null;
    const baseFields = { ...baseObject };
    delete baseFields.wishSpeRn1;
    const metadata = wishObjectMetadataForName(rangeName) ?? wishObjectMetadataForItem(baseFields);
    const otmp = makeWishedBaseObject(baseObject, metadata);
    return Object.assign(otmp, baseFields, { wishedfor: true });
}

function makeWishedObjectClassObject(oclass) {
    if (!oclass) return null;
    const otmp = mkobj(oclass, false);
    const classFields = {};
    if (oclass === SCROLL_CLASS) classFields.cls = 'scroll';
    else if (oclass === SPBOOK_CLASS) classFields.cls = 'spellbook';
    return Object.assign(otmp, object_display(otmp), classFields, { wishedfor: true });
}

function makeWishedFruitObject(lowerName) {
    const fruit = fruitWishMatch(lowerName);
    if (!fruit) return null;
    const otmp = mksobj(SLIME_MOLD, true, false);
    applySlimeMoldFruitFields(otmp, fruit.fid);
    return Object.assign(otmp, { wishedfor: true });
}

function wishedObjectFromName(name, qualifiers = {}) {
    const wishName = String(name || '').trim();
    const lowerWishName = wishName.toLowerCase();
    const artifact = makeArtifactWishObject(lowerWishName, { wizardMode: !!game.flags?.debug });
    if (artifact) return artifact;

    const named = splitWishedTextClause(wishName, 'named');
    let baseText = named?.base || wishName;
    const called = splitWishedTextClause(baseText, 'called');
    if (called) {
        baseText = called.base;
        qualifiers = { ...qualifiers, wishCalledName: unquoteWishedText(called.tail) };
    }
    const labeled = splitWishedLabelClause(baseText);
    if (labeled) {
        baseText = labeled.base;
        qualifiers = { ...qualifiers, wishLabelName: unquoteWishedText(labeled.tail) };
    }
    const baseName = normalizeWishedSpelling(baseText);
    const item = wishedBaseObjectFromName(baseName.toLowerCase(), qualifiers, baseName);
    if (named?.tail) applyWishedInstanceName(item, named.tail);
    return item;
}

function wishedBaseObjectFromName(lowerName, qualifiers = {}, originalName = lowerName) {
    const spellingAlias = resolveWishedSpellingAlias(lowerName);
    lowerName = spellingAlias.name;
    if (qualifiers.wishCalledName) {
        const calledWish = resolveCalledWishName(lowerName, qualifiers.wishCalledName);
        if (calledWish)
            return wishedBaseObjectFromName(calledWish, { ...qualifiers, wishCalledName: '' }, calledWish);
        const rangeBaseWish = calledRangeBaseNamedescName(lowerName);
        if (rangeBaseWish) return makeWishedRangeObjectByName(rangeBaseWish);
        const rangeClass = WISH_OBJECT_RANGE_CLASSES.get(lowerName);
        if (rangeClass) return makeWishedObjectClassObject(rangeClass);
    }
    if (lowerName === 'spell') return noFittingWishObject();
    if (lowerName === 'paperback' || lowerName === 'paperback book') return makeNovelWishObject();
    if (lowerName === 'paperback spellbook') return noFittingWishObject();

    const tinWish = parseWishedTinName(lowerName);
    if (tinWish) return makeWishedTinObject(tinWish);

    const yendorAmulet = makeAmuletOfYendorWishObject(lowerName, qualifiers);
    if (yendorAmulet) return yendorAmulet;

    const statueWish = parseWishedStatueName(lowerName, qualifiers);
    if (statueWish) {
        const statue = makeWishedStatueObject(statueWish, qualifiers);
        if (statue) return statue;
    }

    const figurineWish = parseWishedFigurineName(lowerName, qualifiers);
    if (figurineWish) return makeWishedFigurineObject(figurineWish, qualifiers);

    const corpseWish = parseWishedCorpseName(lowerName, qualifiers);
    if (corpseWish) return makeWishedCorpseObject(corpseWish, qualifiers);

    const globWish = parseWishedGlobName(lowerName, qualifiers);
    if (globWish) return makeWishedGlobObject(globWish);

    const eggWish = parseWishedEggName(lowerName, qualifiers);
    if (eggWish) return makeWishedEggObject(eggWish, qualifiers);

    const venomWish = makeWishedVenomObject(lowerName);
    if (venomWish) return venomWish;

    const wizardSpecial = makeWizardSpecialWishObject(lowerName);
    if (wizardSpecial) return wizardSpecial;

    const specialSubstitution = substituteNonWizardSpecialWish(lowerName);
    if (specialSubstitution) return specialSubstitution;
    const localWishMetadata = wishObjectMetadataForName(lowerName);
    if (localWishMetadata?.ocNowish && !game.flags?.debug) return noFittingWishObject();

    const rangeWish = makeWishedObjectRangeObject(lowerName);
    if (rangeWish) return rangeWish;

    const namedescRangeWish = makeWishedNamedescRangeObject(lowerName);
    if (namedescRangeWish) return namedescRangeWish;

    const dragonArmorWish = parseWishedDragonArmorName(lowerName);
    if (dragonArmorWish) return makeWishedDragonArmorObject(dragonArmorWish);

    const grayStoneWish = makeWishedGrayStoneObject(lowerName);
    if (grayStoneWish) return grayStoneWish;

    const gemWish = makeWishedGemObject(lowerName);
    if (gemWish) return gemWish;

    const fruitWish = makeWishedFruitObject(lowerName);
    if (fruitWish) return fruitWish;

    const baseObject = WISH_BASE_OBJECTS.get(lowerName);
    if (baseObject) {
        const namedescBound = WISH_BASE_NAMEDESC_BOUNDS.get(lowerName);
        if (namedescBound && !spellingAlias.skipNamedesc) rn2(namedescBound);
        const baseFields = { ...baseObject };
        delete baseFields.wishSpeRn1;
        const metadata = localWishMetadata ?? wishObjectMetadataForItem(baseFields);
        const otmp = makeWishedBaseObject(baseObject, metadata);
        return Object.assign(otmp, baseFields, { wishedfor: true });
    }

    const wandWish = lowerName.match(/^wand of ([a-z ]+?)$/);
    if (wandWish && WAND_NAME_TO_INDEX.has(wandWish[1])) {
        const wand = wandWish[1];
        const wandIndex = WAND_NAME_TO_INDEX.get(wand);
        rn2(WAND_WISH_NAMEDESC_BOUNDS.get(wand));
        game._mkobj_wand_index = wandIndex;
        const otmp = mksobj(WAND_CLASS, true, false);
        const chargeSuffix = !!qualifiers.wishChargeSuffix;
        let spe = chargeSuffix ? capWishSpe(Number(qualifiers.wishChargeSpe || 0)) : otmp.spe;
        let recharged = chargeSuffix ? Math.min(Math.max(0, Math.trunc(Number(qualifiers.wishRecharged || 0))), 7) : 0;
        let ignoreRequestedSpe = false;
        if (wand === 'wishing' && !game.flags?.debug) {
            spe = rn2(10) ? -1 : 0;
            recharged = 1;
            ignoreRequestedSpe = true;
        } else if (!game.flags?.debug) {
            spe = wishedSpeForItem(otmp, spe);
        }
        return Object.assign(otmp, {
            cls: 'wand',
            glyph: '/',
            kind: wand,
            wand,
            wandIndex,
            spe,
            recharged,
            known: false,
            wishedfor: true,
            _wish_ignore_requested_spe: ignoreRequestedSpe,
            _wish_spe_from_suffix: chargeSuffix,
        });
    }

    if (lowerName.includes('ring')) {
        const ringName = lowerName.replace(/^ring of /, '');
        const ringIndex = IDENTIFIED_RING_NAMES.indexOf(ringName);
        if (ringIndex >= 0) {
            if (!spellingAlias.skipNamedesc) rn2(2);
            game._mkobj_ring_roll = ringIndex + 1;
        }
        const otmp = mksobj(RING_CLASS, true, false);
        const appearance = game._object_descriptions?.rings?.[ringIndex];
        return Object.assign(otmp, {
            cls: 'ring',
            glyph: '=',
            kind: ringIndex >= 0 ? (appearance ? `${appearance} ring` : lowerName) : lowerName,
            actualKind: ringIndex >= 0 ? `ring of ${ringName}` : lowerName,
            ringRoll: ringIndex + 1,
            known: false,
            wishedfor: true,
        });
    }

    if (lowerName.includes('amulet')) {
        const amuletIndex = IDENTIFIED_AMULET_NAMES.indexOf(lowerName);
        const namedescBound = WISH_AMULET_NAMEDESC_BOUNDS.get(lowerName);
        if (namedescBound && !spellingAlias.skipNamedesc) rn2(namedescBound);
        game._mkobj_bad_amulet = BAD_AMULET_NAMES.has(lowerName);
        const otmp = mksobj(AMULET_CLASS, true, false);
        const appearance = amuletIndex >= 0 ? game._object_descriptions?.amulets?.[amuletIndex] : '';
        return Object.assign(otmp, {
            cls: 'amulet',
            glyph: '"',
            kind: lowerName,
            actualKind: lowerName,
            amuletIndex,
            appearance,
            known: false,
            wishedfor: true,
        });
    }

    if (qualifiers.unlabeled && lowerName === 'paper')
        return makeAmbiguousBlankPaperWishObject();

    if (lowerName === 'wall of water') return noFittingWishObject();
    if (/potion|juice|water/.test(lowerName)) {
        let potionName = lowerName.replace(/^potion(?: of)?\s+/, '');
        const appearanceWish = lowerName.match(/^(.+?) potions?$/);
        let appearanceIndex = -1;
        if (appearanceWish && !lowerName.startsWith('potion')) {
            const appearanceKey = wishedLabelKey(appearanceWish[1]);
            appearanceIndex = (game._object_descriptions?.potions || [])
                .findIndex(potion => wishedLabelKey(potion.description || '') === appearanceKey);
            if (appearanceIndex >= 0) potionName = IDENTIFIED_POTION_NAMES[appearanceIndex] || potionName;
        }
        const holyWater = potionName === 'holy water' || lowerName === 'holy water';
        const unholyWater = potionName === 'unholy water' || lowerName === 'unholy water';
        const waterPotion = lowerName === 'water' || potionName === 'water' || holyWater || unholyWater;
        const oilPotion = potionName === 'oil';
        const potionIndex = appearanceIndex >= 0 ? appearanceIndex : IDENTIFIED_POTION_NAMES.indexOf(potionName);
        if (potionIndex >= 0 && !spellingAlias.skipNamedesc) rn2(POTION_WISH_PROBS[potionIndex] + 1);
        const otmp = mksobj(waterPotion ? POT_WATER : oilPotion ? POT_OIL : potionIndex >= 0 ? POTION_WISH_BASE + potionIndex : POTION_CLASS, true, false);
        if (oilPotion && otmp.age == null) otmp.age = 400;
        const appearance = potionIndex >= 0 ? game._object_descriptions?.potions?.[potionIndex]?.description : '';
        if (holyWater) {
            otmp.blessed = true;
            otmp.cursed = false;
        } else if (unholyWater) {
            otmp.blessed = false;
            otmp.cursed = true;
        }
        return Object.assign(otmp, {
            cls: 'potion',
            glyph: '!',
            kind: waterPotion ? 'water' : appearance ? `${appearance} potion` : lowerName,
            actualKind: waterPotion ? 'potion of water' : potionIndex >= 0 ? `potion of ${potionName}` : lowerName,
            potionIndex: waterPotion ? null : potionIndex,
            known: false,
            wishedfor: true,
        });
    }

    if (lowerName.includes('scroll')) {
        const wishedLabel = qualifiers.wishLabelName || parseWishedScrollLabel(originalName);
        if (wishedLabel) {
            const labelKey = wishedLabelKey(wishedLabel);
            const scrollIndex = (game._object_descriptions?.scrolls || [])
                .findIndex(label => wishedLabelKey(label) === labelKey);
            if (scrollIndex >= 0 && scrollIndex < IDENTIFIED_SCROLL_NAMES.length) {
                rn2(SCROLL_WISH_PROBS[scrollIndex] + 1);
                return makeWishedKnownScrollObject(scrollIndex, wishedLabel);
            }
            if (scrollIndex >= IDENTIFIED_SCROLL_NAMES.length) {
                return makeWishedObjectClassObject(SCROLL_CLASS);
            }
            if (/^scrolls?$/.test(lowerName)) return makeWishedObjectClassObject(SCROLL_CLASS);
        }
        const scrollName = lowerName.replace(/^scrolls?(?: of)?\s+/, '');
        if ((qualifiers.unlabeled && /^scrolls?$/.test(lowerName)) || scrollName === 'blank paper')
            return makeBlankScrollWishObject();
        const scrollIndex = IDENTIFIED_SCROLL_NAMES.indexOf(scrollName);
        const mailScroll = scrollName === 'mail';
        if (mailScroll) rn2(1);
        else if (scrollIndex >= 0 && !spellingAlias.skipNamedesc) rn2(SCROLL_WISH_PROBS[scrollIndex] + 1);
        if (scrollIndex >= 0) return makeWishedKnownScrollObject(scrollIndex);
        const otmp = mksobj(SCROLL_CLASS, !mailScroll, false);
        const label = game._object_descriptions?.scrolls?.[scrollIndex] || 'ZELGO MER';
        return Object.assign(otmp, {
            cls: 'scroll',
            glyph: '?',
            kind: scrollName === 'mail' ? 'stamped scroll' : lowerName,
            actualKind: lowerName,
            scrollIndex,
            known: false,
            wishedfor: true,
        });
    }

    if (/spellbook|book/.test(lowerName)) {
        const spellName = lowerName.replace(/^spellbook(?: of)?\s+/, '');
        if ((qualifiers.unlabeled && lowerName === 'spellbook') || spellName === 'blank paper')
            return makeBlankSpellbookWishObject();
        const spellbookNames = Object.keys(SPELLBOOK_LEVELS);
        if (qualifiers.wishLabelName) {
            const labelKey = wishedLabelKey(qualifiers.wishLabelName);
            const spellbookIndex = (game._object_descriptions?.spellbooks || [])
                .findIndex(label => wishedLabelKey(label) === labelKey);
            if (spellbookIndex >= 0 && spellbookIndex < spellbookNames.length) {
                const labeledSpellName = spellbookNames[spellbookIndex];
                const namedescBound = WISH_SPELLBOOK_NAMEDESC_BOUNDS.get(labeledSpellName);
                if (namedescBound) rn2(namedescBound);
                const otmp = mksobj(SPBOOK_NO_NOVEL, true, false);
                return Object.assign(otmp, {
                    cls: 'spellbook',
                    glyph: '+',
                    kind: `spellbook of ${labeledSpellName}`,
                    spellName: labeledSpellName,
                    spellbookIndex,
                    appearance: game._object_descriptions?.spellbooks?.[spellbookIndex] || qualifiers.wishLabelName,
                    known: spellbookDiscoveryKnown(labeledSpellName),
                    wishedfor: true,
                });
            }
            if (/^spell\s*books?$/.test(lowerName)) return makeWishedObjectClassObject(SPBOOK_CLASS);
        }
        const spellbookIndex = spellbookNames.indexOf(spellName);
        const namedescBound = WISH_SPELLBOOK_NAMEDESC_BOUNDS.get(spellName);
        if (namedescBound) rn2(namedescBound);
        const otmp = mksobj(SPBOOK_NO_NOVEL, true, false);
        return Object.assign(otmp, {
            cls: 'spellbook',
            glyph: '+',
            kind: spellbookIndex >= 0 ? `spellbook of ${spellName}` : lowerName,
            spellName: spellbookIndex >= 0 ? spellName : '',
            spellbookIndex,
            appearance: spellbookIndex >= 0 ? game._object_descriptions?.spellbooks?.[spellbookIndex] : '',
            known: spellbookIndex >= 0 && spellbookDiscoveryKnown(spellName),
            wishedfor: true,
        });
    }

    if (/armor|mail|helm|gauntlets?|gloves|boots|cloak|shirt|robe|shield/.test(lowerName)) {
        const namedescBound = WISH_BASE_NAMEDESC_BOUNDS.get(lowerName);
        if (namedescBound && !spellingAlias.skipNamedesc) rn2(namedescBound);
        const otmp = mksobj(ARMOR_CLASS, true, false);
        const armorAppearance = ARMOR_WISH_APPEARANCES[lowerName];
        const [group, index, fallback] = armorAppearance || [];
        return Object.assign(otmp, {
            cls: 'armor',
            glyph: '[',
            kind: lowerName,
            actualKind: lowerName,
            appearance: armorAppearance ? game._object_descriptions?.[group]?.[index] || fallback : '',
            known: false,
            wishedfor: true,
        });
    }

    if (isWishedFoodName(lowerName)) {
        const namedescBound = WISH_FOOD_NAMEDESC_BOUNDS.get(lowerName);
        if (namedescBound) rn2(namedescBound);
        const otmp = mksobj(FOOD_CLASS, true, false);
        return Object.assign(otmp, { cls: 'food', glyph: '%', kind: lowerName, wishedfor: true });
    }
    if (lowerName === 'wall' || lowerName.endsWith(' wall')) return noFittingWishObject();
    if (/gem|stone|rock/.test(lowerName)) {
        const otmp = mksobj(GEM_CLASS, false, false);
        return Object.assign(otmp, { cls: 'gem', glyph: '*', gemDescription: lowerName, actualKind: lowerName, wishedfor: true });
    }
    if (/lamp|chest|camera|bag|sack|tool|marker|card|bell|harp|leash|blindfold|towel|lenses|flute|horn|whistle|drum|saddle|grease|figurine|tin opener|crystal ball/.test(lowerName)) {
        const namedescBound = WISH_TOOL_NAMEDESC_BOUNDS.get(lowerName);
        if (namedescBound) rn2(namedescBound);
        const toolRoll = WISH_TOOL_ROLLS.get(lowerName);
        if (toolRoll) game._mkobj_tool_roll = toolRoll;
        const otmp = mksobj(TOOL_CLASS, true, false);
        const appearance = WISH_TOOL_APPEARANCES.get(lowerName);
        const explicitOtyp = WISH_TOOL_OTYPES.get(lowerName);
        if (explicitOtyp != null) otmp.otyp = explicitOtyp;
        return Object.assign(otmp, {
            cls: 'tool',
            glyph: '(',
            kind: appearance || lowerName,
            actualKind: lowerName,
            known: !appearance,
            wishedfor: true,
        });
    }
    return noFittingWishObject();
}

function setOverlay(lines, clearRows = 24, hideStatus = false, clearCol = null) {
    game._overlay_lines = lines;
    game._overlay_clear_rows = clearRows;
    game._overlay_hide_status = hideStatus;
    game._overlay_hide_status_only = 0;
    game._overlay_clear_col = clearCol;
    game._overlay_cursor = null;
    game._pending_message = '';
    game._message_more = 0;
}

const DATA_FILE_CACHE = {};

function dataFileLines(name) {
    if (name === 'generated-options') {
        return GENERATED_OPTIONS_HELP_LINES;
    }
    if (name === 'full-key-bindings') return FULL_KEY_BINDINGS_LINES;
    if (name === 'menu-control-help') return MENU_CONTROL_HELP_LINES;
    if (name === 'support-info') return SUPPORT_INFO_LINES;
    if (!DATA_FILE_CACHE[name]) DATA_FILE_CACHE[name] = bundledDatFileLines(name);
    return DATA_FILE_CACHE[name];
}

function dataPagerLines(name, page) {
    const lines = dataFileLines(name);
    const start = page * 23;
    const rows = lines.slice(start, start + 23).map((line, row) => [row, 0, line]);
    rows.push([23, 0, '--More--']);
    return rows;
}

function maybeWishedInstanceName(obj, baseName) {
    const objectName = String(obj?._wish_object_name || obj?.oname || '').trim();
    const name = String(baseName || '');
    if (!objectName || obj?.artifact || / named .+$/i.test(name)) return name;
    return `${name} named ${objectName}`;
}

function isSlimeMoldObject(obj) {
    return obj?.otyp === SLIME_MOLD || String(obj?.actualKind || '').toLowerCase() === 'slime mold';
}

export function pickupObjectName(obj) {
    const named = name => maybeWishedInstanceName(obj, name);
    const archeologist = (game.urole?.name?.m || game._startup_role) === 'Archeologist';
    if (obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$')
        return (obj.quan || 1) > 1 ? 'gold pieces' : 'gold piece';
    if (isBoulderObject(obj)) return named((obj.quan || 1) > 1 ? 'boulders' : 'boulder');
    if (obj.artifact) return artifactObjectName(obj) || obj.kind;
    if (obj.otyp === 'corpse' || obj.otyp === CORPSE) return corpseObjectName(obj);
    if (isEggItem(obj)) return named(eggObjectName(obj));
    if (obj.globby) return named(globObjectName(obj));
    if (isSlimeMoldObject(obj))
        return named(slimeMoldNameForObject(obj, (obj.quan || 1) > 1));
    if ((obj.kind === 'statue' || obj.otyp === STATUE) && obj.corpsenm?.name) {
        const historic = archeologist && (((obj.spe || 0) & CORPSTAT_HISTORIC) || obj.historic);
        return named(`${historic ? 'historic ' : ''}statue of ${corpstatMonsterIndefiniteName(obj)}`);
    }
    if ((obj.kind === 'figurine' || obj.actualKind === 'figurine') && obj.corpsenm?.name)
        return named(`figurine of ${corpstatMonsterIndefiniteName(obj)}`);
    if (isTinObject(obj)) return named(tinObjectName(obj));
    if (obj.otyp === LARGE_BOX) return named('large box');
    if (obj.otyp === CHEST) return named('chest');
    if (obj.otyp === ICE_BOX) return named('ice box');
    if (BAG_OBJECT_TYPES.has(obj.otyp)) return named(obj.contents?.length || !obj.cknown ? 'bag' : 'empty bag');
    if (obj.otyp === OIL_LAMP || obj.otyp === MAGIC_LAMP) return named(maybeLitObjectName(obj, 'lamp'));
    if (obj.otyp === BRASS_LANTERN) return named(maybeLitObjectName(obj, 'brass lantern'));
    if (obj.otyp === TALLOW_CANDLE || obj.otyp === WAX_CANDLE) {
        const name = obj.kind || (obj.otyp === WAX_CANDLE ? 'wax candle' : 'tallow candle');
        return named(maybeLitObjectName(obj, (obj.quan || 1) > 1 ? obj.plural || `${name}s` : name));
    }
    if (obj.otyp === SCROLL_CLASS || obj.cls === 'scroll' || obj.otyp === SCR_SCARE_MONSTER) {
        if ((obj.quan || 1) > 1 && obj.plural) return named(obj.plural);
        const scrollIndex = obj.scrollIndex ?? (obj.otyp === SCR_SCARE_MONSTER ? 3 : undefined);
        const blankScroll = obj.otyp === SCR_BLANK_PAPER || scrollIndex === 21
            || obj.kind === 'blank paper' || (scrollIndex == null && !obj.kind);
        if (blankScroll) {
            if (obj.known === true || obj.actualKind === 'scroll of blank paper')
                return named((obj.quan || 1) > 1 ? 'scrolls of blank paper' : 'scroll of blank paper');
            return named((obj.quan || 1) > 1 ? 'unlabeled scrolls' : 'unlabeled scroll');
        }
        if (obj.kind?.startsWith?.('scroll:')) {
            const name = obj.kind.slice(7);
            return named((obj.quan || 1) > 1 ? `scrolls of ${name}` : obj.singular || `scroll of ${name}`);
        }
        if (obj.kind?.startsWith?.('scroll labeled ')) {
            const label = obj.kind.slice('scroll labeled '.length);
            return named((obj.quan || 1) > 1 ? `scrolls labeled ${label}` : obj.kind);
        }
        if (obj.kind === 'stamped scroll') return named((obj.quan || 1) > 1 ? 'stamped scrolls' : obj.kind);
        if (obj.kind) {
            const name = obj.kind.replace(/^scroll of /, '');
            return named((obj.quan || 1) > 1 ? `scrolls of ${name}` : `scroll of ${name}`);
        }
        const label = game._object_descriptions?.scrolls?.[scrollIndex] || 'ZELGO MER';
        return named((obj.quan || 1) > 1 ? `scrolls labeled ${label}` : `scroll labeled ${label}`);
    }
    if (obj.otyp === POTION_CLASS || obj.cls === 'potion') {
        if ((obj.quan || 1) > 1 && obj.plural) return named(obj.plural);
        if (obj.kind?.startsWith?.('potion:')) return named(maybeLitObjectName(obj, maybeDilutedPotionName(obj, obj.singular || `potion of ${obj.kind.slice(7)}`)));
        const rawKind = String(obj.kind || '').replace(/^potion of /, '');
        if (rawKind === 'water' && (obj.blessed || obj.cursed)) {
            const kind = obj.blessed ? 'holy water' : 'unholy water';
            return named((obj.quan || 1) > 1 ? `potions of ${kind}` : `potion of ${kind}`);
        }
        if (obj.kind === 'holy water' || obj.kind === 'unholy water')
            return named((obj.quan || 1) > 1 ? `potions of ${obj.kind}` : `potion of ${obj.kind}`);
        if (obj.kind?.endsWith?.(' potion')) return named(maybeLitObjectName(obj, maybeDilutedPotionName(obj, obj.kind)));
        if (obj.kind) {
            const name = obj.kind.replace(/^potion of /, '');
            return named(maybeLitObjectName(obj, maybeDilutedPotionName(obj, (obj.quan || 1) > 1 ? `potions of ${name}` : `potion of ${name}`)));
        }
        const appearance = game._object_descriptions?.potions?.[obj.potionIndex]?.description || 'clear';
        return named(maybeLitObjectName(obj, maybeDilutedPotionName(obj, `${appearance} potion`)));
    }
    if (obj.otyp === WAND_CLASS || obj.cls === 'wand') {
        if (obj.known === false) {
            const appearance = obj.appearance || game._object_descriptions?.wands?.[obj.wandIndex]?.description;
            return named(appearance ? `${appearance} wand` : 'wand');
        }
        if (obj.wand === 'sleep' || obj.wandIndex === 22) return named('wand of sleep');
        if (obj.kind && obj.kind !== 'sleep') return named(obj.kind.startsWith('wand') ? obj.kind : `wand of ${obj.kind}`);
        const appearance = game._object_descriptions?.wands?.[obj.wandIndex]?.description;
        return named(appearance ? `${appearance} wand` : 'wand');
    }
    if (obj.cls === 'spellbook' || obj.otyp === SPBOOK_NO_NOVEL || obj.otyp === SPE_HEALING || obj.otyp === BOOK_OF_THE_DEAD) {
        if (isBookOfTheDeadItem(obj)) return named('Book of the Dead');
        if ((obj.quan || 1) > 1 && obj.plural) return named(obj.plural);
        const name = obj.spellName || obj.spell?.name || String(obj.kind || '').replace(/^spellbook(?: of)? /, '');
        const typeKnown = obj.known === true || spellbookDiscoveryKnown(name);
        if (!typeKnown || (obj.otyp === SPE_HEALING && !obj.cls) || (obj.otyp === SPBOOK_NO_NOVEL && !name)) {
            const appearance = obj.appearance || game._object_descriptions?.spellbooks?.[
                obj.spellbookIndex ?? HEALING_SPELLBOOK_APPEARANCE_INDEX
            ];
            if (appearance) return named(`${appearance} spellbook`);
        }
        if (!name) return named('spellbook');
        return named((obj.quan || 1) > 1 ? `spellbooks of ${name}` : `spellbook of ${name}`);
    }
    if (obj.glyph === '=' || obj.cls === 'ring') {
        if (typeof obj.kind !== 'string')
            return named(`${game._object_descriptions?.rings?.[(obj.ringRoll || 1) - 1] || 'ruby'} ring`);
        return named(obj.kind);
    }
    if (obj.fakeAmuletOfYendor) return obj.known ? obj.actualKind : 'Amulet of Yendor';
    if (obj.realAmuletOfYendor) return 'Amulet of Yendor';
    if (obj.otyp === AMULET_CLASS || obj.cls === 'amulet') {
        if (obj.known === false || !obj.kind) {
            const appearance = obj.appearance || game._object_descriptions?.amulets?.[obj.amuletIndex] || 'amulet';
            return named(`${appearance} amulet`);
        }
        return named(obj.kind || 'amulet');
    }
    if (obj.cls === 'armor') {
        const kind = String(obj.actualKind || obj.kind || '').toLowerCase();
        const armorAppearance = ARMOR_WISH_APPEARANCES[kind];
        const typeKnown = obj.known === true
            || !armorAppearance
            || (game._discoveries || []).some(entry => {
                if (entry.section !== 'Armor') return false;
                const discoveredName = entry.name.toLowerCase().replace(/^pair of /, '');
                return discoveredName === kind && (entry.starred || discoveredName === 'leather gloves');
            });
        if (!typeKnown && (obj.appearance || armorAppearance)) {
            const [group, index, fallback] = armorAppearance || [];
            return named(obj.appearance || game._object_descriptions?.[group]?.[index] || fallback);
        }
        return named(obj.kind || 'armor');
    }
    if (obj.otyp === MIRROR) return named('looking glass');
    if (obj.otyp === DART || obj.kind === 'dart') {
        const poisoned = obj.opoisoned ? 'poisoned ' : '';
        return named((obj.quan || 1) > 1 ? `${poisoned}darts` : `${poisoned}dart`);
    }
    if (obj.cls === 'weapon' || obj.otyp === WEAPON_CLASS || obj.glyph === ')') {
        const actualKind = String(obj.actualKind || '').toLowerCase();
        const rawKind = String(obj.kind || '').toLowerCase();
        const kind = actualKind || rawKind;
        const displayKind = obj.known === false && rawKind && rawKind !== actualKind ? rawKind : kind;
        const poisoned = obj.opoisoned && isPoisonableWeaponObject(obj) ? 'poisoned ' : '';
        if (kind === 'orcish dagger' || obj.otyp === ORCISH_DAGGER) {
            const known = obj.known === true || (game._discoveries || [])
                .some(entry => entry.section === 'Weapons' && entry.name === 'orcish dagger' && entry.starred);
            if (known) return named((obj.quan || 1) > 1 ? `${poisoned}orcish daggers` : `${poisoned}orcish dagger`);
            return named((obj.quan || 1) > 1 ? `${poisoned}crude daggers` : `${poisoned}crude dagger`);
        }
        if ((obj.quan || 1) > 1 && displayKind) {
            const plural = {
                knife: 'knives',
                stiletto: 'stilettos',
                ya: 'ya',
                'crossbow bolt': 'crossbow bolts',
                shuriken: 'shuriken',
            }[displayKind] || (displayKind.endsWith('s') ? displayKind : `${displayKind}s`);
            return named(`${poisoned}${plural}`);
        }
        if (displayKind) return named(`${poisoned}${displayKind}`);
    }
    if (obj.otyp === FOOD_CLASS || obj.cls === 'food') {
        const buc = foodBucPrefix(obj);
        if ((obj.quan || 1) > 1 && obj.plural) return named(`${buc}${maybePartlyEatenFoodName(obj, obj.plural)}`);
        if ((obj.quan || 1) > 1 && obj.kind === 'food ration') return named(`${buc}${maybePartlyEatenFoodName(obj, 'food rations')}`);
        if ((obj.quan || 1) > 1 && obj.kind === 'cram ration') return named(`${buc}${maybePartlyEatenFoodName(obj, 'cram rations')}`);
        if (obj.kind === 'tripe') return named(`${buc}${maybePartlyEatenFoodName(obj, (obj.quan || 1) > 1 ? 'tripe rations' : 'tripe ration')}`);
        if (obj.kind?.startsWith?.('tin:')) return named(`${buc}${maybePartlyEatenFoodName(obj, obj.singular || 'tin')}`);
        if (obj.kind) return named(`${buc}${maybePartlyEatenFoodName(obj, obj.kind)}`);
        if ((obj.foodRoll || 1000) <= 140) return named(`${buc}${maybePartlyEatenFoodName(obj, 'tripe ration')}`);
        return named(`${buc}${maybePartlyEatenFoodName(obj, 'food ration')}`);
    }
    if (obj.otyp === GEM_CLASS) {
        const name = obj.gemDescription || 'white gem';
        return named((obj.quan || 1) > 1 ? `${name}s` : name);
    }
    if ((obj.quan || 1) > 1 && obj.plural) return named(obj.plural);
    return named(obj.kind || (obj.otyp != null ? String(obj.otyp) : 'object'));
}

function containerSortRank(item) {
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') return 0;
    if (item.otyp === SCROLL_CLASS || item.cls === 'scroll' || item.glyph === '?') return 1;
    if (item.cls === 'spellbook' || item.glyph === '+') return 2;
    if (item.otyp === GEM_CLASS || item.cls === 'gem' || item.glyph === '*') return 3;
    return 4;
}

function containerLootSortKey(item) {
    return pickupObjectName({ ...item, quan: 1 }).toLowerCase();
}

function compareContainerLootItems(a, b) {
    return containerSortRank(a) - containerSortRank(b)
        || containerLootSortKey(a).localeCompare(containerLootSortKey(b));
}

function isIceBoxObject(obj) {
    return !!obj && (obj.otyp === ICE_BOX || obj.kind === 'ice box' || objectKindKey(obj) === 'ice box');
}

function iceBoxPutClassTypes() {
    return [...BAG_PUT_CLASS_TYPES, { key: 'other', label: 'Other Items', symbol: '*' }];
}

function iceBoxPutCategory(item) {
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') return 'coin';
    if (item.cls === 'amulet' || item.otyp === AMULET_CLASS || item.glyph === '"') return 'amulet';
    if (item.cls === 'weapon' || item.otyp === WEAPON_CLASS || item.glyph === ')') return 'weapon';
    if (item.cls === 'armor' || item.otyp === ARMOR_CLASS || item.glyph === '[') return 'armor';
    if (item.otyp === 'corpse' || item.otyp === CORPSE || item.otyp === FOOD_CLASS || item.cls === 'food' || item.glyph === '%') return 'food';
    if (item.otyp === SCROLL_CLASS || item.cls === 'scroll' || item.glyph === '?') return 'scroll';
    if (item.otyp === SPBOOK_NO_NOVEL || item.cls === 'spellbook' || item.glyph === '+') return 'spellbook';
    if (item.otyp === POTION_CLASS || item.cls === 'potion' || item.glyph === '!') return 'potion';
    if (item.otyp === RING_CLASS || item.cls === 'ring' || item.glyph === '=') return 'ring';
    if (item.otyp === WAND_CLASS || item.cls === 'wand' || item.glyph === '/') return 'wand';
    if (item.otyp === GEM_CLASS || item.cls === 'gem' || item.glyph === '*') return 'gem';
    if (item.otyp === BOULDER || item.otyp === STATUE || item.cls === 'rock' || item.glyph === '`') return 'rock';
    if (item === game.u?.uball || item.cls === 'ball' || item.glyph === '0') return 'ball';
    if (item === game.u?.uchain || item.cls === 'chain' || item.glyph === '_') return 'chain';
    if (BAG_OBJECT_TYPES.has(item.otyp) || item.otyp === ICE_BOX || item.otyp === OIL_LAMP || item.otyp === MAGIC_LAMP
        || item.otyp === MIRROR || item.otyp === EXPENSIVE_CAMERA || item.otyp === STETHOSCOPE
        || item.otyp === MAGIC_MARKER || item.cls === 'tool' || item.glyph === '(') return 'tool';
    return 'other';
}

function buildIceBoxPutItems(iceBox = null) {
    const putItems = [];
    if (game._goldCount) {
        putItems.push({
            item: { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount },
            category: 'coin',
        });
    }
    for (const item of game.inventory || []) {
        if (item.letter === '$' || item.cls === 'coin' || item.glyph === '$') continue;
        if (item === iceBox) continue;
        putItems.push({ item, category: iceBoxPutCategory(item) });
    }
    return putItems;
}

function iceBoxPutLetters(iceBox = null) {
    const letters = [];
    if (game._goldCount) letters.push('$');
    for (const item of game.inventory || []) {
        if (item.letter === '$' || item.cls === 'coin' || item.glyph === '$') continue;
        if (item === iceBox) continue;
        if (item.letter) letters.push(item.letter);
    }
    return letters.join('');
}

function buildContainerPutItems(container = null) {
    const putItems = [];
    if (game._goldCount) {
        putItems.push({
            item: { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount },
            category: 'coin',
        });
    }
    for (const item of game.inventory || []) {
        if (item.letter === '$' || item.cls === 'coin' || item.glyph === '$') continue;
        if (item === container) continue;
        putItems.push({ item, category: iceBoxPutCategory(item) });
    }
    return putItems;
}

function containerPutLetters(container = null) {
    const letters = [];
    if (game._goldCount) letters.push('$');
    for (const { item } of buildContainerPutItems(container)) {
        if (item.letter && item.letter !== '$') letters.push(item.letter);
    }
    return letters.join('');
}

function clearIceBoxPutState() {
    game._icebox_put_container = null;
    game._icebox_put_type_choices = null;
    game._icebox_put_type_selected = null;
    game._icebox_put_items = null;
    game._icebox_put_entries = null;
    game._icebox_put_selected = null;
    game._icebox_stash_letters = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
}

function clearContainerPutState() {
    game._container_put_container = null;
    game._container_put_type_choices = null;
    game._container_put_type_selected = null;
    game._container_put_items = null;
    game._container_put_entries = null;
    game._container_put_selected = null;
    game._container_stash_letters = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
}

function clearContainerTakeoutState() {
    game._loot_takeout_container = null;
    game._loot_takeout_entries = null;
    game._loot_takeout_selected = null;
    game._loot_takeout_clear_col = null;
    game._loot_takeout_type_entries = null;
    game._loot_takeout_types_selected = null;
}

function clearIceBoxSequenceState() {
    game._icebox_sequence_after_takeout = null;
    game._icebox_sequence_after_putin = null;
    game._icebox_sequence_used = 0;
    game._icebox_sequence_messages = null;
}

function clearContainerSequenceState() {
    game._container_sequence_after_takeout = null;
    game._container_sequence_after_putin = null;
    game._container_sequence_used = 0;
    game._container_sequence_messages = null;
}

function startIceBoxSequence(nextAction) {
    clearIceBoxSequenceState();
    if (nextAction === 'putin') game._icebox_sequence_after_takeout = 'putin';
    else if (nextAction === 'takeout') game._icebox_sequence_after_putin = 'takeout';
    game._icebox_sequence_messages = [];
}

function startContainerSequence(nextAction) {
    clearContainerSequenceState();
    if (nextAction === 'putin') game._container_sequence_after_takeout = 'putin';
    else if (nextAction === 'takeout') game._container_sequence_after_putin = 'takeout';
    game._container_sequence_messages = [];
}

function iceBoxSequenceActive() {
    return !!(game._icebox_sequence_after_takeout || game._icebox_sequence_after_putin);
}

function containerSequenceActive() {
    return !!(game._container_sequence_after_takeout || game._container_sequence_after_putin);
}

function markIceBoxSequenceUsed(used = true) {
    if (used) game._icebox_sequence_used = 1;
}

function markContainerSequenceUsed(used = true) {
    if (used) game._container_sequence_used = 1;
}

function addIceBoxSequenceMessages(messages) {
    const list = Array.isArray(messages) ? messages : [messages];
    game._icebox_sequence_messages ??= [];
    for (const message of list) {
        if (message) game._icebox_sequence_messages.push(message);
    }
}

function addContainerSequenceMessages(messages) {
    const list = Array.isArray(messages) ? messages : [messages];
    game._container_sequence_messages ??= [];
    for (const message of list) {
        if (message) game._container_sequence_messages.push(message);
    }
}

function iceBoxEmptyMessage() {
    return 'The ice box is empty.';
}

function iceBoxIsOnHeroFloor(iceBox) {
    return !!(iceBox && (game.level?.objects || []).includes(iceBox)
        && iceBox.ox === game.u?.ux && iceBox.oy === game.u?.uy);
}

function iceBoxIsCarried(iceBox) {
    return !!(iceBox && (game.inventory || []).includes(iceBox));
}

function iceBoxContainerAvailable(iceBox) {
    return iceBoxIsOnHeroFloor(iceBox) || iceBoxIsCarried(iceBox);
}

function setIceBoxActionMenu(iceBox, done = false) {
    game._icebox_action_container = iceBox || null;
    setOverlay(done ? LOOT_ICE_MENU_DONE_LINES : LOOT_ICE_MENU_LINES, 11);
    game._command_mode = 'iceBoxAction';
}

function clearIceBoxActionState() {
    game._icebox_action_container = null;
}

function iceBoxPutTypeChoices(putItems) {
    const classTypes = iceBoxPutClassTypes();
    const presentTypes = classTypes.filter(type => putItems.some(entry => entry.category === type.key));
    const choices = [{ key: 'auto', letter: 'A', label: 'Auto-select every relevant item' }];
    if (presentTypes.length > 1) choices.push({ key: 'all', letter: 'a', label: 'All types', row: 5 });
    let row = presentTypes.length > 1 ? 6 : 5;
    let letterIndex = presentTypes.length > 1 ? 1 : 0;
    for (const type of presentTypes) {
        const letter = 'abcdefghijklmnopqrstuvwxyz'[letterIndex++];
        choices.push({ ...type, letter, row: row++ });
    }
    const extraChoices = [];
    if (putItems.some(({ item }) => objectBucCategory(item) === 'blessed'))
        extraChoices.push({ key: 'blessed', letter: 'B', label: 'Items known to be Blessed' });
    if (putItems.some(({ item }) => objectBucCategory(item) === 'cursed'))
        extraChoices.push({ key: 'cursed', letter: 'C', label: 'Items known to be Cursed' });
    if (putItems.some(({ item }) => objectBucCategory(item) === 'uncursed'))
        extraChoices.push({ key: 'uncursed', letter: 'U', label: 'Items known to be Uncursed' });
    if (putItems.some(({ item }) => objectBucCategory(item) === 'unknown'))
        extraChoices.push({ key: 'unknown', letter: 'X', label: 'Items of unknown Bless/Curse status' });
    if (game._just_picked_gold && game._goldCount)
        extraChoices.push({
            key: 'justpicked',
            letter: 'P',
            label: `Just picked up: ${game._goldCount} gold piece${game._goldCount === 1 ? '' : 's'}`,
        });
    if (extraChoices.length) row++;
    for (const choice of extraChoices) choices.push({ ...choice, row: row++ });
    return choices;
}

function containerPutTypeChoices(putItems) {
    return iceBoxPutTypeChoices(putItems);
}

function drawIceBoxPutTypeMenu() {
    const choices = game._icebox_put_type_choices || [];
    const selected = new Set(game._icebox_put_type_selected || []);
    const rows = [
        [0, 23, 'Put in what type of objects?', 1],
        [2, 23, `A ${selected.has('auto') ? '+' : '-'} Auto-select every relevant item`],
        [3, 27, '(ignored unless some other choices are also picked)'],
    ];
    for (const choice of choices) {
        if (!choice.row) continue;
        rows.push([choice.row, 23, `${choice.letter} ${selected.has(choice.key) ? '+' : '-'} ${choice.label}`]);
    }
    const endRow = Math.max(4, ...choices.map(choice => choice.row || 0)) + 1;
    rows.push([endRow, 23, '(end)']);
    setOverlay(rows, endRow + 1, false, 23);
}

function drawContainerPutTypeMenu() {
    const choices = game._container_put_type_choices || [];
    const selected = new Set(game._container_put_type_selected || []);
    const rows = [
        [0, 23, 'Put in what type of objects?', 1],
        [2, 23, `A ${selected.has('auto') ? '+' : '-'} Auto-select every relevant item`],
        [3, 27, '(ignored unless some other choices are also picked)'],
    ];
    for (const choice of choices) {
        if (!choice.row) continue;
        rows.push([choice.row, 23, `${choice.letter} ${selected.has(choice.key) ? '+' : '-'} ${choice.label}`]);
    }
    const endRow = Math.max(4, ...choices.map(choice => choice.row || 0)) + 1;
    rows.push([endRow, 23, '(end)']);
    setOverlay(rows, endRow + 1, false, 23);
}

function beginIceBoxPutIn(iceBox) {
    const putItems = buildIceBoxPutItems(iceBox);
    if (!putItems.length) return false;
    game._icebox_put_container = iceBox;
    game._icebox_put_items = putItems;
    game._icebox_put_type_choices = iceBoxPutTypeChoices(putItems);
    game._icebox_put_type_selected = [];
    drawIceBoxPutTypeMenu();
    game._command_mode = 'iceBoxPutTypes';
    return true;
}

function beginContainerPutIn(container) {
    const putItems = buildContainerPutItems(container);
    if (!putItems.length) return false;
    game._container_put_container = container;
    game._container_put_items = putItems;
    game._container_put_type_choices = containerPutTypeChoices(putItems);
    game._container_put_type_selected = [];
    drawContainerPutTypeMenu();
    game._command_mode = 'containerPutTypes';
    return true;
}

function drawIceBoxPutObjectMenu() {
    const entries = game._icebox_put_entries || [];
    const selected = new Set(game._icebox_put_selected || []);
    const rows = [[0, 41, 'Put in what?', 1]];
    let row = 2;
    let lastCategory = '';
    for (const entry of entries) {
        if (entry.category !== lastCategory) {
            const type = iceBoxPutClassTypes().find(type => type.key === entry.category);
            rows.push([row, 34, '┌─────']);
            rows.push([row, 40, ' '.repeat(40)]);
            rows.push([row++, 41, type?.label || 'Other Items', 1]);
            lastCategory = entry.category;
        }
        entry.row = row;
        const label = entry.letter === '$'
            ? `${entry.amount} gold piece${entry.amount === 1 ? '' : 's'}`
            : inventoryItemName(entry.item);
        rows.push([row, 34, '│']);
        rows.push([row++, 40, ` ${entry.letter} ${selected.has(entry.letter) ? '+' : '-'} ${label}`.padEnd(40, ' ')]);
    }
    rows.push([row, 34, '│']);
    rows.push([row, 40, ' (end)'.padEnd(40, ' ')]);
    setOverlay(rows, 2, false, 0);
}

function drawContainerPutObjectMenu() {
    const entries = game._container_put_entries || [];
    const selected = new Set(game._container_put_selected || []);
    const rows = [[0, 41, 'Put in what?', 1]];
    let row = 2;
    let lastCategory = '';
    for (const entry of entries) {
        if (entry.category !== lastCategory) {
            const type = iceBoxPutClassTypes().find(type => type.key === entry.category);
            rows.push([row, 34, '┌─────']);
            rows.push([row, 40, ' '.repeat(40)]);
            rows.push([row++, 41, type?.label || 'Other Items', 1]);
            lastCategory = entry.category;
        }
        entry.row = row;
        const label = entry.letter === '$'
            ? `${entry.amount} gold piece${entry.amount === 1 ? '' : 's'}`
            : inventoryItemName(entry.item);
        rows.push([row, 34, '│']);
        rows.push([row++, 40, ` ${entry.letter} ${selected.has(entry.letter) ? '+' : '-'} ${label}`.padEnd(40, ' ')]);
    }
    rows.push([row, 34, '│']);
    rows.push([row, 40, ' (end)'.padEnd(40, ' ')]);
    setOverlay(rows, 2, false, 0);
}

function articlelessObjectName(item) {
    return pickupObjectName(item).replace(/^(?:an? |the )/i, '');
}

function iceBoxPutRejectMessage(iceBox, item) {
    if (!item) return "You don't have that object.";
    if (item === game.u?.uball || item === game.u?.uchain) return 'You must be kidding.';
    if (item === iceBox) return 'That would be an interesting topological exercise.';
    if (isWornInventoryItem(item)) return 'You cannot refrigerate something you are wearing.';
    const kind = objectKindKey(item);
    if ((item.otyp === LOADSTONE || kind === 'loadstone') && item.cursed) {
        item.bknown = true;
        return `The stone${(item.quan || 1) === 1 ? '' : 's'} won't leave your person.`;
    }
    const actual = String(item.actualKind || '').toLowerCase();
    if (item.realAmuletOfYendor || actual === 'amulet of yendor' || (!actual && kind === 'amulet of yendor')
        || isBookOfTheDeadItem(item) || isBellOfOpeningItem(item) || isCandelabrumOfInvocationItem(item))
        return `The ${articlelessObjectName(item)} cannot be confined in such trappings.`;
    if (kind === 'leash' && item.leashmon) return 'The leash is attached to your pet.';
    const boxLike = isIceBoxObject(item) || item.otyp === CHEST || item.otyp === LARGE_BOX
        || kind === 'chest' || kind === 'large box';
    const bigStatue = item.otyp === STATUE
        && (item.big || item.bigStatue || item.large || item.corpsenm?.msize === 'large' || item.corpsenm?.size === 'large');
    if (boxLike || item.otyp === BOULDER || kind === 'boulder' || bigStatue)
        return `You cannot fit the ${articlelessObjectName(item)} into the ice box.`;
    return '';
}

function containerPutRejectMessage(container, item) {
    if (!item) return "You don't have that object.";
    if (isIceBoxObject(container)) return iceBoxPutRejectMessage(container, item);
    if (item === game.u?.uball || item === game.u?.uchain) return 'You must be kidding.';
    if (item === container) return 'That would be an interesting topological exercise.';
    if (isWornInventoryItem(item)) return 'You cannot stash something you are wearing.';
    const kind = objectKindKey(item);
    if ((item.otyp === LOADSTONE || kind === 'loadstone') && item.cursed) {
        item.bknown = true;
        return `The stone${(item.quan || 1) === 1 ? '' : 's'} won't leave your person.`;
    }
    const actual = String(item.actualKind || '').toLowerCase();
    if (item.realAmuletOfYendor || actual === 'amulet of yendor' || (!actual && kind === 'amulet of yendor')
        || isBookOfTheDeadItem(item) || isBellOfOpeningItem(item) || isCandelabrumOfInvocationItem(item))
        return `The ${articlelessObjectName(item)} cannot be confined in such trappings.`;
    if (kind === 'leash' && item.leashmon) return 'The leash is attached to your pet.';
    const boxLike = isIceBoxObject(item) || item.otyp === CHEST || item.otyp === LARGE_BOX
        || kind === 'chest' || kind === 'large box';
    const bigStatue = item.otyp === STATUE
        && (item.big || item.bigStatue || item.large || item.corpsenm?.msize === 'large' || item.corpsenm?.size === 'large');
    if (boxLike || item.otyp === BOULDER || kind === 'boulder' || bigStatue)
        return `You cannot fit the ${articlelessObjectName(item)} into the ${tipContainerSimpleName(container)}.`;
    return '';
}

function clearContainerPutEquipmentState(item, name) {
    const line = String(item.line || '');
    if (item.wielded || /(?:weapon|wielded) in/.test(line) || line.includes('(wielded)')) {
        item.wielded = false;
        if (item.artifact === 'Mjollnir' || item.kind === 'mjollnir') game._wielded_mjollnir = false;
    }
    if (item.alternate || line.includes('alternate weapon')) item.alternate = false;
    if (item.quivered || /at the ready|in quiver/.test(line)) item.quivered = false;
    if (item.line) item.line = `${item.letter || '?'} - ${name}`;
}

function putInventoryObjectIntoIceBox(iceBox, item, amount = item?.quan || 1, options = {}) {
    if (!iceBoxContainerAvailable(iceBox)) return { moved: false, message: '' };
    iceBox.contents ??= [];
    iceBox.cknown = true;
    if (item?.letter === '$' || item?.otyp === GOLD_PIECE || item?.cls === 'coin' || item?.glyph === '$') {
        const gold = Math.min(Math.max(0, amount || 0), game._goldCount || 0);
        if (!gold) return { moved: false, message: 'You have no gold to put in.' };
        const shopGold = donateShopGoldAt(iceBox, gold);
        add_to_container(iceBox, { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold });
        game._goldCount = Math.max(0, (game._goldCount || 0) - gold);
        game._just_picked_gold = Math.max(0, (game._just_picked_gold || 0) - gold);
        const money = (game.inventory || []).find(invItem => invItem.letter === '$' || invItem.cls === 'coin');
        if (money && game._goldCount) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        } else {
            game.inventory = (game.inventory || []).filter(invItem => invItem.letter !== '$' && invItem.cls !== 'coin');
        }
        game._pet_food_scan_inventory = game.inventory;
        const messages = [`You put ${gold} gold piece${gold === 1 ? '' : 's'} into the ice box.`, ...(shopGold.messages || [])];
        return { moved: true, message: messages.join('  '), messages };
    }

    const reject = iceBoxPutRejectMessage(iceBox, item);
    if (reject) return { moved: false, message: reject };

    const name = inventoryItemName(item);
    const count = Math.min(Math.max(1, amount || 1), item.quan || 1);
    const putItem = (item.quan || 1) > count ? { ...item, quan: count } : item;
    clearContainerPutEquipmentState(putItem, name);
    let sellobjResult = null;
    if (!options.skipSalePrompt) {
        const pendingSale = beginShopFloorContainerPutSale(iceBox, item, count);
        if (pendingSale?.prompt)
            return { moved: false, pendingSale, message: pendingSale.promptMessage };
        if (pendingSale?.handled) sellobjResult = pendingSale;
    }
    curseLoadstoneLeavingInventory(putItem);
    const billing = billShopFloorContainerPutObject(iceBox, putItem, {
        acceptedSale: !!options.acceptedSale,
        shkp: options.salePending?.shkp,
        sellobjResult,
    });
    removeInventoryItem(item, count);
    freezeObjectInIcebox(putItem);
    const contained = add_to_container(iceBox, putItem);
    if (options.acceptedSale && contained) markAcceptedShopContainerSaleState(contained, options.salePending?.shkp || billing.shkp);
    if (billing.noCharge && contained && contained !== putItem) markNoChargeRecursively(contained);
    game._pet_food_scan_inventory = game.inventory;
    const messages = [sellobjResult?.message, `You put ${name} into the ice box.`].filter(Boolean);
    return { moved: true, message: messages.join('  '), messages };
}

function putInventoryObjectIntoBag(bag, item, amount = item?.quan || 1) {
    if (!bag || !(game.inventory || []).includes(bag)) return { moved: false, message: '' };
    bag.contents ??= [];
    bag.cknown = true;
    if (item?.letter === '$' || item?.otyp === GOLD_PIECE || item?.cls === 'coin' || item?.glyph === '$') {
        const gold = Math.min(Math.max(0, amount || 0), game._goldCount || 0);
        if (!gold) return { moved: false, message: 'You have no gold to put in.' };
        add_to_container(bag, { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold });
        game._goldCount = Math.max(0, (game._goldCount || 0) - gold);
        game._just_picked_gold = Math.max(0, (game._just_picked_gold || 0) - gold);
        const money = (game.inventory || []).find(invItem => invItem.letter === '$' || invItem.cls === 'coin');
        if (money && game._goldCount) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        } else {
            game.inventory = (game.inventory || []).filter(invItem => invItem.letter !== '$' && invItem.cls !== 'coin');
        }
        game._pet_food_scan_inventory = game.inventory;
        return { moved: true, message: `You put ${gold} gold piece${gold === 1 ? '' : 's'} into the bag.` };
    }

    const reject = containerPutRejectMessage(bag, item);
    if (reject) return { moved: false, message: reject };

    const name = inventoryItemName(item);
    const count = Math.min(Math.max(1, amount || 1), item.quan || 1);
    const putItem = splitInventoryObjectForContainerPut(item, count);
    clearContainerPutEquipmentState(putItem, name);
    curseLoadstoneLeavingInventory(putItem);
    if (isMagicBagObject(bag) && magicBagExplodesWithObject(putItem)) {
        removeInventoryItem(item, count);
        return explodeMagicBagTransfer(bag, putItem, [], { triggerHeld: true });
    }
    removeInventoryItem(item, count);
    add_to_container(bag, putItem);
    game._pet_food_scan_inventory = game.inventory;
    return { moved: true, message: `You put ${name} into the bag.` };
}

function splitInventoryObjectForContainerPut(item, count) {
    if (!item || (item.quan || 1) <= count) return item;
    const putItem = { ...item, quan: count, id: next_ident() };
    delete putItem.o_id;
    delete putItem._shopBillObjectId;
    if (item.unpaid && !splitCarriedObjectShopBill(item, putItem, count))
        clearObjectShopBillState(putItem);
    return putItem;
}

function billShopFloorContainerPutObject(container, putItem, options = {}) {
    const x = container?.ox ?? game.u?.ux;
    const y = container?.oy ?? game.u?.uy;
    if (!container || !putItem || (game.inventory || []).includes(container) || x == null || y == null)
        return { shkp: null, returned: false, noCharge: false };
    const shkp = shopFloorContainerShopkeeper(container);
    if (!shkp) return { shkp: null, returned: false, noCharge: false };
    if (shopBillableGold(putItem)) return { shkp, returned: false, noCharge: false };
    const sellobjResult = options.sellobjResult;
    if (sellobjResult?.angry || sellobjResult?.robbed)
        return { shkp, returned: false, noCharge: false, sellobjResult };
    const gold = containedShopGold(putItem);
    if (gold > 0) donateShopGoldAt(container, gold);
    if (returnUnpaidObjectToShopBillOwnerAt(putItem, x, y))
        return { shkp, returned: true, noCharge: false };
    if (!putItem.unpaid) {
        const hadContainedUnpaid = globContents(putItem).length && shopObjectOrContentsUnpaid(putItem);
        if (options.acceptedSale) {
            markAcceptedShopContainerSaleState(putItem, options.shkp || shkp);
            if (hadContainedUnpaid) subFromShopBill(putItem, options.shkp || shkp);
            return { shkp, returned: false, noCharge: false, acceptedSale: true };
        }
        markNoChargeRecursively(putItem);
        if (hadContainedUnpaid) subFromShopBill(putItem, options.shkp || shkp);
        return { shkp, returned: false, noCharge: true };
    }
    return { shkp, returned: false, noCharge: false };
}

function preserveExplodedShopFloorPutTriggerBill(putItem, owner, fallbackShkp, price) {
    if (!putItem || shopBillableGold(putItem)) return false;
    if (!owner?.entry) return false;
    const shkp = owner?.shkp || fallbackShkp || heroShopkeeper();
    if (!shkp) return false;
    if (!shopBillEntryForObject(shkp, putItem) && price > 0)
        addObjectToShopBill(shkp, putItem, price, { useup: true });
    return markObjectShopBillUsedUp(putItem, shkp);
}

function putInventoryObjectIntoContainer(container, item, amount = item?.quan || 1, options = {}) {
    if (!container) return { moved: false, message: '' };
    if (isIceBoxObject(container)) return putInventoryObjectIntoIceBox(container, item, amount, options);
    container.contents ??= [];
    container.cknown = true;
    const containerName = tipContainerSimpleName(container);
    if (item?.letter === '$' || item?.otyp === GOLD_PIECE || item?.cls === 'coin' || item?.glyph === '$') {
        const gold = Math.min(Math.max(0, amount || 0), game._goldCount || 0);
        if (!gold) return { moved: false, message: 'You have no gold to put in.' };
        const shopGold = donateShopGoldAt(container, gold);
        add_to_container(container, { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold });
        game._goldCount = Math.max(0, (game._goldCount || 0) - gold);
        game._just_picked_gold = Math.max(0, (game._just_picked_gold || 0) - gold);
        const money = (game.inventory || []).find(invItem => invItem.letter === '$' || invItem.cls === 'coin');
        if (money && game._goldCount) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        } else {
            game.inventory = (game.inventory || []).filter(invItem => invItem.letter !== '$' && invItem.cls !== 'coin');
        }
        refreshTipContainerWeight(container);
        game._pet_food_scan_inventory = game.inventory;
        const messages = [`You put ${gold} gold piece${gold === 1 ? '' : 's'} into the ${containerName}.`, ...(shopGold.messages || [])];
        return { moved: true, message: messages.join('  '), messages };
    }

    const reject = containerPutRejectMessage(container, item);
    if (reject) return { moved: false, message: reject };

    const name = inventoryItemName(item);
    const count = Math.min(Math.max(1, amount || 1), item.quan || 1);
    const putItem = splitInventoryObjectForContainerPut(item, count);
    clearContainerPutEquipmentState(putItem, name);
    let sellobjResult = null;
    if (!options.skipSalePrompt) {
        const pendingSale = beginShopFloorContainerPutSale(container, item, count);
        if (pendingSale?.prompt)
            return { moved: false, pendingSale, message: pendingSale.promptMessage };
        if (pendingSale?.handled) sellobjResult = pendingSale;
    }
    curseLoadstoneLeavingInventory(putItem);
    if (isMagicBagObject(container) && magicBagExplodesWithObject(putItem)) {
        const triggerOwner = shopkeeperOwningBillEntry(putItem);
        const triggerPrice = triggerOwner.entry ? shopBillEntryTotal(triggerOwner.entry) : 0;
        const wasUnpaid = !!triggerOwner.entry;
        const billing = billShopFloorContainerPutObject(container, putItem, {
            acceptedSale: !!options.acceptedSale,
            shkp: options.salePending?.shkp,
            sellobjResult,
        });
        if (wasUnpaid)
            preserveExplodedShopFloorPutTriggerBill(putItem, triggerOwner, billing.shkp, triggerPrice);
        billShopFloorMagicBagExplosionTarget(container);
        removeInventoryItem(item, count);
        const messages = sellobjResult?.message ? [sellobjResult.message] : [];
        return explodeMagicBagTransfer(container, putItem, messages, { triggerHeld: true });
    }
    const billing = billShopFloorContainerPutObject(container, putItem, {
        acceptedSale: !!options.acceptedSale,
        shkp: options.salePending?.shkp,
        sellobjResult,
    });
    removeInventoryItem(item, count);
    const contained = add_to_container(container, putItem);
    if (options.acceptedSale && contained) markAcceptedShopContainerSaleState(contained, options.salePending?.shkp || billing.shkp);
    if (billing.noCharge && contained && contained !== putItem) markNoChargeRecursively(contained);
    refreshTipContainerWeight(container);
    game._pet_food_scan_inventory = game.inventory;
    const messages = [sellobjResult?.message, `You put ${name} into the ${containerName}.`].filter(Boolean);
    return { moved: true, message: messages.join('  '), messages };
}

function finishShopFloorContainerPutSale(pending, accept) {
    if (!pending?.container || !pending.item || !pending.putItem || !pending.shkp)
        return { moved: false, message: '' };
    const result = putInventoryObjectIntoContainer(
        pending.container,
        pending.item,
        pending.amount || pending.item?.quan || 1,
        { skipSalePrompt: true, acceptedSale: !!accept, salePending: pending },
    );
    if (!result.moved || !accept) return result;
    const saleMessage = shopSalePaymentMessage({ ...pending, obj: pending.putItem }, 'sell');
    const messages = [saleMessage, ...(result.messages || (result.message ? [result.message] : []))].filter(Boolean);
    return { ...result, saleMessage, messages, message: messages.join('  ') };
}

async function showIceBoxMessageList(messages) {
    messages = (messages || []).filter(Boolean);
    if (!messages.length) return;
    const message = messages.join('  ');
    if (message.length <= 79) {
        await setMessage(message);
    } else {
        const first = messages.slice(0, 2).join('  ');
        const rest = messages.slice(2).join('  ');
        game._queued_message_after_more = rest || null;
        game._queued_message_process_time_after_more = 1;
        await setMessage(first, true);
    }
}

function iceBoxPutResultMessages(results) {
    return (results || []).map(result => result.message).filter(Boolean);
}

async function showIceBoxPutMessages(results) {
    await showIceBoxMessageList(iceBoxPutResultMessages(results));
}

async function finishIceBoxSequence(messages = [], moved = false) {
    const allMessages = [...(game._icebox_sequence_messages || []), ...(messages || [])];
    const used = moved || !!game._icebox_sequence_used;
    clearIceBoxSequenceState();
    clearIceBoxActionState();
    game._command_mode = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    if (used) game.context.move = 1;
    await showIceBoxMessageList(allMessages);
}

async function finishContainerSequence(messages = [], moved = false) {
    const allMessages = [...(game._container_sequence_messages || []), ...(messages || [])];
    const used = moved || !!game._container_sequence_used;
    clearContainerSequenceState();
    game._floor_container_object = null;
    game._command_mode = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    if (used) game.context.move = 1;
    await showIceBoxMessageList(allMessages);
}

async function continueIceBoxSequenceToPutIn(iceBox, messages = []) {
    addIceBoxSequenceMessages(messages);
    if (iceBoxContainerAvailable(iceBox) && beginIceBoxPutIn(iceBox)) return true;
    clearIceBoxPutState();
    game._command_mode = null;
    await finishIceBoxSequence(["You don't have anything to put in."]);
    return true;
}

async function continueContainerSequenceToPutIn(container, messages = []) {
    addContainerSequenceMessages(messages);
    if (beginContainerPutIn(container)) return true;
    clearContainerPutState();
    game._command_mode = null;
    await finishContainerSequence(["You don't have anything to put in."]);
    return true;
}

async function continueIceBoxSequenceToTakeout(iceBox, messages = []) {
    addIceBoxSequenceMessages(messages);
    if (!iceBoxContainerAvailable(iceBox)) {
        clearContainerTakeoutState();
        clearIceBoxPutState();
        game._command_mode = null;
        await finishIceBoxSequence();
        return true;
    }
    const learnedEmpty = !(iceBox.contents || []).length && !iceBox.cknown;
    iceBox.cknown = true;
    if (beginIceBoxTakeout(iceBox)) return true;
    if (learnedEmpty) markIceBoxSequenceUsed();
    clearContainerTakeoutState();
    game._command_mode = null;
    await finishIceBoxSequence([iceBoxEmptyMessage()]);
    return true;
}

async function continueContainerSequenceToTakeout(container, messages = []) {
    addContainerSequenceMessages(messages);
    if (!container) {
        clearContainerTakeoutState();
        clearContainerPutState();
        game._command_mode = null;
        await finishContainerSequence();
        return true;
    }
    const learnedEmpty = !(container.contents || []).length && !container.cknown;
    container.cknown = true;
    if (beginIceBoxTakeout(container)) return true;
    if (learnedEmpty) markContainerSequenceUsed();
    clearContainerTakeoutState();
    game._command_mode = null;
    await finishContainerSequence([tipContainerEmptyMessage(container)]);
    return true;
}

async function beginIceBoxStash(iceBox) {
    const letters = iceBoxPutLetters(iceBox);
    if (!letters) return false;
    game._icebox_put_container = iceBox;
    game._icebox_stash_letters = letters;
    await setMessage(`What do you want to stash? [${getobjPromptLetters(letters)} or ?*]`);
    game._command_mode = 'iceBoxStashObject';
    return true;
}

async function beginContainerStash(container) {
    const letters = containerPutLetters(container);
    if (!letters) return false;
    game._container_put_container = container;
    game._container_stash_letters = letters;
    await setMessage(`What do you want to stash? [${getobjPromptLetters(letters)} or ?*]`);
    game._command_mode = 'containerStashObject';
    return true;
}

function containerLootCategory(item) {
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') return 'Coins';
    if (item.otyp === FOOD_CLASS || item.cls === 'food' || item.glyph === '%') return 'Comestibles';
    if (item.otyp === SCROLL_CLASS || item.cls === 'scroll' || item.glyph === '?') return 'Scrolls';
    if (item.cls === 'spellbook' || item.glyph === '+') return 'Spellbooks';
    if (item.otyp === POTION_CLASS || item.cls === 'potion' || item.glyph === '!') return 'Potions';
    if (item.otyp === RING_CLASS || item.cls === 'ring' || item.glyph === '=') return 'Rings';
    if (item.otyp === GEM_CLASS || item.cls === 'gem' || item.glyph === '*') return 'Gems/Stones';
    if (isBoulderObject(item) || item.otyp === STATUE || item.cls === 'rock' || item.glyph === '`') return 'Boulders/Statues';
    return 'Other Items';
}

function beginIceBoxTakeout(iceBox) {
    const contents = [...(iceBox?.contents || [])].sort(compareContainerLootItems);
    if (!contents.length) return false;
    const seen = new Set(contents.map(containerLootCategory));
    const typeEntries = [...seen]
        .sort((a, b) => PICKUP_SECTION_ORDER.indexOf(a) - PICKUP_SECTION_ORDER.indexOf(b))
        .map((label, i) => ({ letter: String.fromCharCode('b'.charCodeAt(0) + i), label }));
    if (typeEntries.length === 1) {
        const entries = contents.map((item, i) => ({
            item,
            label: typeEntries[0].label,
            letter: String.fromCharCode('a'.charCodeAt(0) + i),
        }));
        const rows = [[0, 41, 'Take out what?', 1], [2, 41, typeEntries[0].label, 1]];
        let row = 3;
        for (const entry of entries)
            rows.push([row++, 41, `${entry.letter} - ${pickupObjectPhrase(entry.item)}`]);
        rows.push([row, 41, '(end)']);
        game._loot_takeout_container = iceBox;
        game._loot_takeout_entries = entries;
        game._loot_takeout_selected = [];
        game._loot_takeout_clear_col = 40;
        setOverlay(rows, row + 1, false, game._loot_takeout_clear_col);
        game._command_mode = 'lootTakeoutObjects';
        return true;
    }

    const typeRows = [
        [0, 23, 'Take out what type of objects?', 1],
        [2, 23, 'A - Auto-select every relevant item'],
        [3, 27, '(ignored unless some other choices are also picked)'],
        [5, 23, 'a - All types'],
    ];
    for (let i = 0; i < typeEntries.length; i++)
        typeRows.push([6 + i, 23, `${typeEntries[i].letter} - ${typeEntries[i].label}`]);
    let filterRow = 7 + typeEntries.length;
    if (contents.some(item => objectBucCategory(item) === 'blessed'))
        typeRows.push([filterRow++, 23, 'B - Items known to be Blessed']);
    if (contents.some(item => objectBucCategory(item) === 'cursed'))
        typeRows.push([filterRow++, 23, 'C - Items known to be Cursed']);
    if (contents.some(item => objectBucCategory(item) === 'uncursed'))
        typeRows.push([filterRow++, 23, 'U - Items known to be Uncursed']);
    if (contents.some(item => objectBucCategory(item) === 'unknown'))
        typeRows.push([filterRow++, 23, 'X - Items of unknown Bless/Curse status']);
    typeRows.push([filterRow, 23, '(end)']);
    game._loot_takeout_container = iceBox;
    game._loot_takeout_type_entries = typeEntries;
    game._loot_takeout_types_selected = [];
    setOverlay(typeRows, filterRow + 1, false, 22);
    game._command_mode = 'lootTakeoutMenu';
    return true;
}

function prepareContainerTakeoutObject(container, obj) {
    if (isIceBoxObject(container)) removedFromIcebox(obj);
    obj.contained = false;
    obj.container = null;
    delete obj.nobj;
    delete obj.nexthere;
    return obj;
}

function addContainerTakeoutObjectToInventory(container, obj, options = {}) {
    const billingSource = options.sourceObj || obj;
    if (shopBillableGold(obj)) {
        prepareContainerTakeoutObject(container, obj);
        const amount = Math.max(1, Math.trunc(Number(obj.quan || 1)));
        addContainerTakeoutObjectToShopBill(container, billingSource, obj);
        addGoldToHero(amount);
        return `$ - ${amount} gold piece${amount === 1 ? '' : 's'}`;
    }
    if (isBoulderObject(obj)) normalizeBoulderObject(obj);
    const mergeInfo = findContainerTakeoutInventoryMergeTarget(container, obj);
    if (mergeInfo) {
        const line = mergeContainerTakeoutObjectIntoInventory(container, obj, mergeInfo);
        game._pet_food_scan_inventory = game.inventory;
        return line;
    }
    prepareContainerTakeoutObject(container, obj);
    const letter = options.inventoryLetter || nextInventoryLetter();
    Object.assign(obj, {
        cls: obj.cls || (obj.otyp === GEM_CLASS ? 'gem'
            : obj.otyp === SCROLL_CLASS ? 'scroll'
                : obj.glyph === '+' ? 'spellbook' : obj.cls),
        letter,
        kind: obj.kind || pickupObjectName({ ...obj, quan: 1 }),
    });
    obj.line = normalInventoryLine({ ...obj, line: '' });
    const billing = addContainerTakeoutObjectToShopBill(container, billingSource, obj);
    game.inventory = [...(game.inventory || []), obj];
    maybeAttachCarriedFigurineTimeout(obj);
    game._pet_food_scan_inventory = game.inventory;
    const line = obj.line || `${letter} - ${pickupObjectPhrase(obj)}`;
    return billing.messages?.length ? `${billing.messages.join('  ')}  ${line}` : line;
}

function splitContainerTakeoutObjectForLift(obj, count) {
    const quantity = Math.max(1, Math.trunc(Number(obj?.quan || 1)));
    const takeCount = Math.max(1, Math.min(quantity, Math.trunc(Number(count || quantity))));
    if (isLoadstoneObject(obj)) return obj;
    if (!obj || takeCount >= quantity) return obj;
    const lifted = { ...obj, id: next_ident(), quan: takeCount };
    delete lifted.o_id;
    delete lifted._shopBillObjectId;
    delete lifted.letter;
    delete lifted.line;
    obj.quan = quantity - takeCount;
    return lifted;
}

async function finishContainerTakeoutSelection(container, picked, preflightMessages = []) {
    const messages = [...(preflightMessages || [])];
    if (!(picked || []).length) {
        clearContainerTakeoutState();
        game._floor_container_object = null;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        clearIceBoxSequenceState();
        clearContainerSequenceState();
        game._command_mode = null;
        game.context.move = 0;
        await showIceBoxMessageList(messages);
        return;
    }
    const moved = [];
    for (const pickedEntry of picked || []) {
        const source = pickedEntry.item;
        const obj = splitContainerTakeoutObjectForLift(source, pickedEntry.takeCount || source?.quan || 1);
        const line = addContainerTakeoutObjectToInventory(container, obj, { inventoryLetter: pickedEntry.inventoryLetter, sourceObj: source });
        moved.push(obj);
        messages.push(/[.!?]$/.test(line) ? line : `${line}.`);
    }
    for (const obj of moved) removeContainedObject(container, obj);
    game._pet_food_scan_inventory = game.inventory;
    const followupPutIn = game._icebox_sequence_after_takeout === 'putin' && isIceBoxObject(container);
    const finishSequence = !followupPutIn && iceBoxSequenceActive() && isIceBoxObject(container);
    const followupContainerPutIn = game._container_sequence_after_takeout === 'putin' && !isIceBoxObject(container);
    const finishContainerSeq = !followupContainerPutIn && containerSequenceActive() && !isIceBoxObject(container);
    clearContainerTakeoutState();
    game._floor_container_object = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    if (followupPutIn) {
        markIceBoxSequenceUsed(true);
        await continueIceBoxSequenceToPutIn(container, messages);
        return;
    }
    if (finishSequence) {
        await finishIceBoxSequence(messages, true);
        return;
    }
    if (followupContainerPutIn) {
        markContainerSequenceUsed(true);
        await continueContainerSequenceToPutIn(container, messages);
        return;
    }
    if (finishContainerSeq) {
        await finishContainerSequence(messages, true);
        return;
    }
    clearIceBoxSequenceState();
    clearContainerSequenceState();
    game._command_mode = null;
    game.context.move = 1;
    await showIceBoxMessageList(messages);
}

function placeObjectOnFloorWithEffects(obj, x, y, messages, verb = 'drop', {
    stack = false,
    usedUpShopBillOnDestroy = false,
} = {}) {
    obj.contained = false;
    obj.container = null;
    obj.ox = x;
    obj.oy = y;
    obj.hidden = false;
    obj.buried = false;
    obj.transientProjectile = false;
    delete obj.line;
    delete obj.nobj;
    delete obj.nexthere;
    const display = object_display(obj);
    if (display.glyph === '?' && OBJECT_CLASS_GLYPHS[obj.cls]) {
        display.glyph = OBJECT_CLASS_GLYPHS[obj.cls];
        if (obj.cls === 'armor') display.color = carriedDropDisplayColor(obj);
    }
    Object.assign(obj, display);
    if (!earthFloorEffects(obj, x, y, messages, verb, { usedUpShopBillOnDestroy })) {
        game.level.objects ??= [];
        const stacked = stack ? stackMonsterThrownObject(obj) : obj;
        if (stacked === obj) game.level.objects.push(obj);
        objectIceEffect(obj, x, y);
        newsym(x, y);
        return true;
    }
    newsym(x, y);
    return false;
}

function placeTippedObjectOnFloor(obj, x, y, messages) {
    return placeObjectOnFloorWithEffects(obj, x, y, messages, 'drop', {
        usedUpShopBillOnDestroy: true,
    });
}

function tipContainerSimpleName(source) {
    const display = BAG_OBJECT_TYPES.has(source?.otyp) ? { ...source, cknown: false } : source;
    const name = articlelessObjectName(display);
    if ((source?.broken || source?.obroken) && /^(?:large box|chest)$/.test(name))
        return `broken ${name}`;
    return name;
}

function tipContainerEmptyMessage(source) {
    return `The ${tipContainerSimpleName(source)} is empty.`;
}

function thawObjectTippedFromSource(source, obj) {
    if (isIceBoxObject(source)) removedFromIcebox(obj);
}

function returnTippedObjectToShopFloor(source, obj) {
    const shkp = shopFloorContainerShopkeeper(source);
    if (!shkp || !obj || shopBillableGold(obj)) return false;
    if (globContents(obj).length) return subFromShopBill(obj, shkp);
    return sellobjReturnUnpaidToShop(obj, obj.ox, obj.oy);
}

function tipContainerToFloor(source) {
    const contents = [...liquidFlowContainerContents(source)];
    source.cknown = true;
    if (!contents.length) return [tipContainerEmptyMessage(source)];
    const x = game.u?.ux ?? source.ox ?? 0;
    const y = game.u?.uy ?? source.oy ?? 0;
    const cursedMagicBag = isMagicBagObject(source) && source.cursed;
    let lostMerchandise = 0;
    const names = contents.map(containerObjectPhrase).join(', ');
    const messages = [cursedMagicBag
        ? (contents.length === 1 ? 'An object spills out.' : 'Objects spill out.')
        : contents.length === 1
            ? `An object spills out: ${names}.`
            : `Objects spill out: ${names}.`];
    for (const obj of contents) {
        removeContainedObject(source, obj);
        thawObjectTippedFromSource(source, obj);
        if (cursedMagicBag && !rn2(13)) {
            lostMerchandise += billMagicBagLostItem(source, obj);
            destroyMagicBagItem(obj, messages);
            continue;
        }
        addTippedContainerObjectToShopBill(source, obj);
        const placed = placeTippedObjectOnFloor(obj, x, y, messages);
        if (placed) {
            const gold = shopBillableGold(obj)
                ? Math.max(1, Math.trunc(Number(obj.quan || 1)))
                : containedShopGold(obj);
            if (gold > 0) donateShopGoldAt(source, gold);
            returnTippedObjectToShopFloor(source, obj);
        }
    }
    if (Array.isArray(source.contents)) source.contents.length = 0;
    if (Array.isArray(source.cobj)) source.cobj.length = 0;
    refreshTipContainerWeight(source);
    const lostMessage = lostShopMerchandiseMessage(lostMerchandise);
    if (lostMessage) messages.push(lostMessage);
    return messages;
}

function isTipContainerObject(obj) {
    if (!obj) return false;
    if (isIceBoxObject(obj) || isBoxObject(obj) || BAG_OBJECT_TYPES.has(obj.otyp)) return true;
    return /^(?:large box|chest|ice box|sack|oilskin sack|bag of holding|bag)$/.test(objectKindKey(obj));
}

function isBagOfTricksObject(obj) {
    return !!obj && (obj.otyp === BAG_OF_TRICKS || objectKindKey(obj) === 'bag of tricks'
        || pickupObjectName({ ...obj, quan: 1 }).toLowerCase() === 'bag of tricks');
}

function isKnownBagOfTricksObject(obj) {
    return isBagOfTricksObject(obj) && obj.dknown === true
        && (obj.known === true || toolDiscoveryKnown('bag of tricks'));
}

function isUnknownBagOfTricksObject(obj) {
    return isBagOfTricksObject(obj) && !isKnownBagOfTricksObject(obj);
}

function isHornOfPlentyObject(obj) {
    return !!obj && (obj.otyp === HORN_OF_PLENTY || objectKindKey(obj) === 'horn of plenty'
        || pickupObjectName({ ...obj, quan: 1 }).toLowerCase() === 'horn of plenty');
}

function isKnownHornOfPlentyObject(obj) {
    return isHornOfPlentyObject(obj) && obj.dknown === true
        && (obj.known === true || toolDiscoveryKnown('horn of plenty'));
}

function isTipSourceObject(obj) {
    return isTipContainerObject(obj) || isBagOfTricksObject(obj) || isHornOfPlentyObject(obj);
}

function isFloorTipSourceObject(obj) {
    return isTipContainerObject(obj) || isBagOfTricksObject(obj);
}

function tipSpillageKind(obj) {
    const kind = objectKindKey(obj);
    if (isCandleObject(obj) && obj.lamplit) return 'wax';
    if ((isPotionOfOil(obj) && obj.lamplit)
        || ((obj?.otyp === OIL_LAMP || kind === 'oil lamp') && (obj.age ?? 0) !== 0)
        || ((obj?.otyp === MAGIC_LAMP || kind === 'magic lamp') && (obj.spe ?? 0) !== 0))
        return 'oil';
    if ((toolChargeKind(obj) === 'can of grease' || kind === 'can of grease') && (obj?.spe ?? 0) > 0)
        return 'grease';
    if (obj?.otyp === FOOD_RATION || obj?.otyp === LEMBAS_WAFER
        || kind === 'food ration' || kind === 'cram ration' || kind === 'lembas wafer')
        return 'crumbs';
    if (obj?.cls === 'venom') return 'venom';
    return '';
}

function isTipSpillageObject(obj) {
    return !!tipSpillageKind(obj);
}

function isTipCommandObject(obj) {
    return isTipSourceObject(obj) || isTipSpillageObject(obj);
}

function isTipCoinObject(obj) {
    return !!obj && (obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$');
}

function tipSelectionKind(obj) {
    if (!obj || isTipCoinObject(obj)) return 'exclude';
    if (isTipContainerObject(obj) || isBagOfTricksObject(obj)) return 'suggest';
    if (isKnownHornOfPlentyObject(obj)) return 'suggest';
    return 'downplay';
}

function tipInventoryItemsBySelectionKind(kind) {
    return (game.inventory || []).filter(item => tipSelectionKind(item) === kind && item.letter);
}

function tipSelectableInventoryItems() {
    return (game.inventory || []).filter(item => tipSelectionKind(item) !== 'exclude' && item.letter);
}

function tipSuggestedLetters() {
    return tipInventoryItemsBySelectionKind('suggest').map(item => item.letter).join('');
}

function tipPromptMessage() {
    const suggested = tipSuggestedLetters();
    if (suggested) return `What do you want to tip? [${getobjPromptLetters(suggested)} or ?*]`;
    if (tipInventoryItemsBySelectionKind('downplay').length) return 'What do you want to tip? [*]';
    return "You don't have anything to tip.";
}

function tipMenuItems(ch) {
    if (ch === '*') return tipSelectableInventoryItems();
    const suggested = tipInventoryItemsBySelectionKind('suggest');
    return suggested.length ? suggested : tipInventoryItemsBySelectionKind('downplay');
}

function tipSpillageSurfacePhrase(spillage) {
    const plural = spillage === 'crumbs';
    const loc = game.level?.at?.(game.u?.ux, game.u?.uy);
    if (loc?.typ === LAVAPOOL || loc?.typ === LAVAWALL)
        return { surface: 'lava', suffix: ` and immediately ${plural ? 'burn' : 'burns'} away` };
    if (loc && (IS_POOL(loc.typ) || loc.typ === WATER || loc.typ === MOAT))
        return { surface: 'water', suffix: ` and gradually ${plural ? 'dissipate' : 'dissipates'}` };
    return { surface: 'floor', suffix: '' };
}

function tipSpillageObject(obj) {
    const spillage = tipSpillageKind(obj);
    if (!spillage) return ['Nothing happens.'];
    const plural = spillage === 'crumbs';
    const { surface, suffix } = tipSpillageSurfacePhrase(spillage);
    const messages = [`Some ${spillage} ${plural ? 'spill' : 'spills'} onto the ${surface}${suffix}.`];
    if ((toolChargeKind(obj) === 'can of grease' || objectKindKey(obj) === 'can of grease') && (obj?.spe ?? 0) > 0)
        spendChargedToolUse(obj, messages);
    return messages;
}

function tipOrdinaryObjectMessages(obj) {
    if (obj?.cls === 'potion' || obj?.glyph === '!') {
        const name = pickupObjectName(obj);
        const plural = Math.max(1, Math.trunc(Number(obj?.quan || 1))) > 1;
        return [`The ${name} ${plural ? 'are' : 'is'} securely sealed.`];
    }
    if (obj?.otyp === STATUE || objectKindKey(obj) === 'statue') return ['Nothing interesting happens.'];
    return ['Nothing happens.'];
}

function tipWornHelmet() {
    return wornArmorInSlot('helm');
}

function isTipWornHelmet(obj) {
    return !!obj && obj === tipWornHelmet();
}

function tipHatSimpleName(helmet) {
    return hardEarthHelmet(helmet) ? 'helm' : 'hat';
}

function tipHatDirectionPrompt() {
    return 'At whom? (in what direction)';
}

async function beginTipHat(helmet) {
    const cancelMove = helmet?.bknown ? 0 : 1;
    if (helmet?.cursed) {
        helmet.bknown = true;
        refreshInventoryObjectLine(helmet);
        await setMessage("You can't.  It is cursed.");
        game.context.move = cancelMove;
        game._command_mode = null;
        return;
    }
    game._tip_hat_letter = helmet?.letter || '';
    game._tip_hat_cancel_move = cancelMove;
    await setMessage(tipHatDirectionPrompt());
    game._command_mode = 'tipHatDirection';
}

function tipHatDirectionFromKey(ch) {
    return commandDirection(ch);
}

function tipHatMonsterCanBeSeen(mon) {
    return !!mon && !game.u?.blind && !mon.minvis && !mon.mundetected && couldsee(mon.mx, mon.my);
}

function tipHatApparentObjectOrFurniture(mon) {
    const appearance = M_AP_TYPE(mon);
    return !!mon && (appearance === M_AP_FURNITURE || appearance === M_AP_OBJECT
        || mon.appearObj != null || !!mon.appearGlyph);
}

function tipHatMonsterAppearsAsStatueGlyph(mon) {
    if (!mon) return false;
    const kind = String(mon.appearKind ?? mon.appearanceKind ?? mon.mappearanceKind ?? '').toLowerCase();
    return mon.appearObj === STATUE || mon.mappearance === STATUE
        || mon.appearStatueGlyph === true || mon.statueGlyph === true
        || kind === 'statue' || kind.endsWith(' statue');
}

function tipHatLocationHasStatueGlyph(loc) {
    return !!(loc?.displayed_statue_glyph
        || loc?.remembered_glyph?.statueGlyph
        || loc?.statueGlyph
        || loc?.glyph_is_statue);
}

function tipHatMonsterVisible(mon) {
    return tipHatMonsterCanBeSeen(mon) && !tipHatApparentObjectOrFurniture(mon);
}

function tipHatMonsterResponsive(mon) {
    if (!mon || mon.dead) return false;
    if (tipHatMonsterHelpless(mon) || mon.mcansee === false || mon.blind) return false;
    return true;
}

function tipHatMonsterIsNymph(mon, monName = '', mlet = '') {
    const data = mon?.data || {};
    const name = String(monName || data.name || mon?.name || '').toLowerCase();
    const letter = String(mlet || data.mlet || mon?.mlet || '').toLowerCase();
    return !!(mon?.nymph || data.nymph || letter === 'n' || letter === 'nymph' || /\bnymph\b/.test(name));
}

function tipHatMonsterHumanoid(mon) {
    const data = mon?.data || {};
    const mlet = data.mlet || mon?.mlet || '';
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(data.humanoid || data.human || mlet === 'humanoid' || mlet === 'human' || mlet === '@'
        || data.name === 'human' || /^(gremlin|leprechaun|skeleton|death|pestilence|famine)$/.test(name)
        || tipHatMonsterIsNymph(mon, name, mlet)
        || tipHatMonsterIsVampireInOwnForm(name)
        || /\bzombie$/.test(name));
}

function tipHatMonsterPossessive(mon) {
    const data = mon?.data || {};
    if (mon?.female || data.female) return 'her';
    if (mon?.male || data.male) return 'his';
    return 'its';
}

function tipHatMonsterFemale(mon) {
    const data = mon?.data || {};
    if (mon?.female != null) return !!mon.female;
    if (data.female != null) return !!data.female;
    return false;
}

function chatHeroHumanoidForDeafResponse() {
    const form = polyselfForm();
    if (!form) return true;
    if (form.humanoid != null) return !!form.humanoid;
    const mlet = String(form.mlet || '').toLowerCase();
    const name = String(form.name || '').toLowerCase();
    return !!(form.human || mlet === '@' || mlet === 'human' || mlet === 'humanoid' || name === 'human');
}

function chatClearTargetWaitStrategy(mon) {
    if (Number.isInteger(mon?.mstrategy)) mon.mstrategy &= ~STRAT_WAITMASK;
    else if (mon?.mstrategy === 'waitforu') mon.mstrategy = 0;
}

function chatMonsterNameForDeafResponse(mon, visible) {
    return visible ? ` from ${fireScrollMonsterName(mon).replace(/^The /, 'the ')}` : '';
}

function chatDeafResponseMessage(mon, visible) {
    const response = chatHeroHumanoidForDeafResponse() ? 'falls on deaf ears' : 'is inaudible';
    return `Any response${chatMonsterNameForDeafResponse(mon, visible)} ${response}.`;
}

function chatMapUnseenMonster(mon) {
    const unseenLoc = game.level?.at?.(mon.mx, mon.my);
    if (unseenLoc) unseenLoc.map_invisible = true;
    newsym(mon.mx, mon.my);
}

function chatMonsterIsEating(mon) {
    return Number(mon?.mtame || 0) > 0 && !!mon?.meating;
}

function chatMonsterName(mon, visible) {
    if (!visible) return 'It';
    const name = fireScrollMonsterName(mon);
    if (mon?.saddled && !mon?.givenName && !(mon?.isshk && mon?.shknam))
        return name.replace(/^The /, 'The saddled ');
    return name;
}

function chatClearNoTarget() {
    game._pending_message = '';
    game._message_more = 0;
    game._command_mode = null;
}

function chatTargetIsWall(loc) {
    return !!loc && (IS_WALL(loc.typ) || loc.typ === SDOOR);
}

function chatVisibleStatueAt(x, y) {
    const obj = topFloorObjectAt(x, y);
    return obj && (obj.kind === 'statue' || obj.otyp === STATUE) ? obj : null;
}

function chatBlindHasMappedWall(loc) {
    return IS_WALL(loc?.lastseentyp ?? 0);
}

function chatStatueTargetName() {
    return heroIsHallucinating()
        ? (rndmonnum()?.name || randomHallucinatedMonsterName())
        : 'statue';
}

const CHAT_HALLUCINATED_WALL_TALK = [
    'gripes about its job.',
    'tells you a funny joke!',
    'insults your heritage!',
    'chuckles.',
    'guffaws merrily!',
    'deprecates your exploration efforts.',
    'suggests a stint of rehab...',
    "doesn't seem to be interested.",
];

function chatWallTargetMessage() {
    if (!heroIsHallucinating()) return "It's like talking to a wall.";
    let idx = rn2(10);
    if (idx >= CHAT_HALLUCINATED_WALL_TALK.length)
        idx = CHAT_HALLUCINATED_WALL_TALK.length - 1;
    return `The wall ${CHAT_HALLUCINATED_WALL_TALK[idx]}`;
}

const CHAT_SILENT_POLYFORM_NAMES = new Set([
    'acid blob', 'quivering blob', 'gelatinous cube',
    'blue jelly', 'spotted jelly', 'ochre jelly',
    'small mimic', 'large mimic', 'giant mimic',
    'giant ant', 'soldier ant', 'fire ant', 'giant beetle',
]);

function chatHeroPolyform() {
    return polyselfForm() || game.u?._polyself_form || game.u?.data || {};
}

function chatHeroIsSilent() {
    const form = chatHeroPolyform();
    const explicit = form?.msound ?? form?.sound;
    const soundName = String(explicit ?? '').toLowerCase();
    const name = String(form?.name || '').toLowerCase();
    if (form?.silent || form?.msilent || explicit === 0
        || soundName === 'silent' || soundName === 'ms_silent'
        || CHAT_SILENT_POLYFORM_NAMES.has(name))
        return true;
    if (explicit != null) return false;
    return tipHatGeneratedMonsterSound(name) === 'silent';
}

function chatHeroPolyformArticleName() {
    return articleFor(String(chatHeroPolyform()?.name || 'creature').toLowerCase());
}

function chatPreDirectionMessage() {
    if (chatHeroIsSilent())
        return `As ${chatHeroPolyformArticleName()}, you cannot speak.`;
    if (heroIsStrangledForChoke()) return "You can't speak.  You're choking!";
    if (game.u?.uswallow) return "They won't hear you out there.";
    if (game.u?.uinwater || game.u?.underwater || game.u?.uunderwater)
        return 'Your speech is unintelligible underwater.';
    return '';
}

function chatShopFloorObjects() {
    const x = game.u?.ux;
    const y = game.u?.uy;
    return (game.level?.objects || [])
        .filter(obj => obj && !obj.hidden && !obj.transientProjectile
            && obj.ox === x && obj.oy === y
            && !(obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$'));
}

function chatShopkeeperCanQuoteFloor(shkp) {
    return !!shkp && shopkeeperInHisShop(shkp)
        && !shopkeeperAngryForSellobj(shkp)
        && !shopkeeperMuteForSpeech(shkp)
        && !tipHatMonsterHelpless(shkp);
}

function chatShopQuoteWearingSurcharge() {
    const worn = (game.inventory || []).filter(isWornInventoryItem);
    if (worn.some(item => objectKindKey(item) === 'dunce cap')) return true;
    const role = game._startup_role || game.urole?.name?.m || '';
    if (role === 'Tourist' && (game.u?.ulevel || 1) < 15) return true;
    const wornArmor = worn.filter(item =>
        item.cls === 'armor' || item.glyph === '[' || item.otyp === ARMOR_CLASS);
    const visibleShirt = wornArmor.some(item => ['hawaiian shirt', 't-shirt'].includes(objectKindKey(item)))
        && !wornArmor.some(item => ['body', 'cloak'].includes(armorSlot(item)));
    return visibleShirt;
}

function chatShopQuoteUnitCost(obj, shkp) {
    let price = shopBaseCost(obj);
    if ((obj?.cls === 'food' || obj?.otyp === FOOD_CLASS || obj?.otyp === CORPSE || obj?.otyp === 'corpse')
        && (game.u?.uhs || 0) >= 2)
        price *= game.u.uhs;
    if ((obj?.cls === 'food' || obj?.otyp === FOOD_CLASS || obj?.otyp === CORPSE || obj?.otyp === 'corpse')
        && obj?.oeaten)
        price = 0;
    if ((obj?.cls === 'armor' || obj?.cls === 'weapon' || obj?.glyph === '[' || obj?.glyph === ')')
        && (obj?.spe || 0) > 0)
        price += 10 * obj.spe;
    if (isCandleObject(obj) && obj?.age < 20 * price) price = Math.trunc(price / 2);
    if (!price) price = 5;

    let multiplier = 1;
    let divisor = 1;
    if ((obj?.dknown !== true || !shopObjectNameKnown(obj))
        && ((obj?.id ?? obj?.o_id ?? 0) % 4) === 0) {
        multiplier *= 4;
        divisor *= 3;
    }
    if (chatShopQuoteWearingSurcharge()) {
        multiplier *= 4;
        divisor *= 3;
    }
    const cha = game.u?.acurr?.a?.[A_CHA] ?? 10;
    if (cha > 18) divisor *= 2;
    else if (cha === 18) { multiplier *= 2; divisor *= 3; }
    else if (cha >= 16) { multiplier *= 3; divisor *= 4; }
    else if (cha <= 5) multiplier *= 2;
    else if (cha <= 7) { multiplier *= 3; divisor *= 2; }
    else if (cha <= 10) { multiplier *= 4; divisor *= 3; }

    price *= multiplier;
    if (divisor > 1) price = Math.trunc((Math.trunc(price * 10 / divisor) + 5) / 10);
    price = Math.max(1, Math.trunc(price));
    if (obj?.artifact || obj?.oartifact) price *= 4;
    if (shkp?.surcharge) price += Math.trunc((price + 2) / 3);
    return price;
}

function chatShopQuoteTopCost(obj, shkp) {
    if (!obj || obj.no_charge || obj === game.u?.uball || obj === game.u?.uchain) return 0;
    let price = chatShopQuoteUnitCost(obj, shkp);
    if (isGlobbyObject(obj)) price *= shopPricingUnits(obj);
    return Math.max(0, Math.trunc(Number(price || 0)));
}

function chatShopQuoteContainedCost(obj, shkp, seen = new Set()) {
    if (!obj || seen.has(obj)) return 0;
    seen.add(obj);
    let price = 0;
    for (const child of globContents(obj)) {
        if (!child || seen.has(child)) continue;
        if (!shopBillableGold(child) && !child.no_charge)
            price += chatShopQuoteUnitCost(child, shkp) * shopPricingUnits(child);
        price += chatShopQuoteContainedCost(child, shkp, seen);
    }
    return Math.max(0, Math.trunc(price));
}

function chatShopQuoteMagicObject(obj, oclass) {
    if (shopObjectNameKnown(obj))
        return !!(obj?.oc_magic || obj?.magic || obj?.magicStone);
    return [AMULET_CLASS, RING_CLASS, WAND_CLASS, POTION_CLASS, SCROLL_CLASS, SPBOOK_CLASS]
        .includes(oclass);
}

function chatShopQuoteEmbellishment(obj, cost) {
    if (!rn2(3)) {
        let choice = rn2(5);
        if (choice === 0) choice = cost < 100 ? 1 : cost < 500 ? 2 : 3;
        switch (choice) {
        case 4: {
            if (cost < 10) break;
            const oclass = shopObjectClassCode(obj);
            if (oclass === FOOD_CLASS) return ", gourmets' delight!";
            if (chatShopQuoteMagicObject(obj, oclass)) return ', painstakingly developed!';
            return ', superb craftsmanship!';
        }
        case 3:
            return ', finest quality.';
        case 2:
            return ', an excellent choice.';
        case 1:
            return ', a real bargain.';
        default:
            break;
        }
    } else if (obj?.artifact) {
        return ', one of a kind!';
    }
    return '.';
}

function chatShopQuoteEntry(obj, shkp) {
    const topCost = chatShopQuoteTopCost(obj, shkp);
    let cost = topCost + (globContents(obj).length
        ? chatShopQuoteContainedCost(obj, shkp)
        : 0);
    cost = Math.max(0, Math.trunc(Number(cost || 0)));
    const contentsOnly = cost > 0 && topCost === 0;
    const name = `${contentsOnly ? 'the contents of ' : ''}${pickupObjectPhrase(obj)}`;
    const price = cost > 0
        ? `${cost} ${shopCurrency(cost)}${(obj?.quan || 1) > 1 ? ' each' : ''}`
        : 'no charge';
    return { obj, cost, contentsOnly, line: `${name}, ${price}` };
}

function chatShopFloorQuote() {
    if (heroIsDeaf() || heroIsBlind()) return null;
    const x = game.u?.ux;
    const y = game.u?.uy;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!chatShopkeeperCanQuoteFloor(shkp)) return null;
    const objects = chatShopFloorObjects();
    if (!objects.length) return null;
    return { shkp, entries: objects.map(obj => chatShopQuoteEntry(obj, shkp)) };
}

async function finishChatShopFloorQuote(quote) {
    const entries = quote?.entries || [];
    if (!entries.length) return false;
    if (entries.length > 1) {
        const rows = [[0, 0, 'Fine goods for sale:'], [1, 0, '']];
        for (let i = 0; i < entries.length; i++)
            rows.push([i + 2, 0, entries[i].line]);
        setOverlay(rows, rows.length);
        game._command_mode = 'chatPriceQuoteMenu';
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 0;
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game.context.move = 0;
        return true;
    }

    const entry = entries[0];
    let message = `${upstartText(entry.line)}!`;
    if (entry.cost > 0) {
        const name = upstartText(entry.contentsOnly
            ? entry.line.replace(/, \d+ .*/, '')
            : pickupObjectPhrase(entry.obj));
        const stackEach = (entry.obj?.quan || 1) > 1 ? ' each' : '';
        const suffix = entry.contentsOnly ? '.' : chatShopQuoteEmbellishment(entry.obj, entry.cost);
        message = `${name}, price ${entry.cost} ${shopCurrency(entry.cost)}${stackEach}${suffix}`;
    }
    await setMessage(message, message.includes('  '));
    game._command_mode = null;
    chatConsumeTurn();
    return true;
}

async function beginChatCommand() {
    const preDirectionMessage = chatPreDirectionMessage();
    if (preDirectionMessage) {
        await setMessage(preDirectionMessage);
        game._command_mode = null;
        return;
    }
    if (await finishChatShopFloorQuote(chatShopFloorQuote())) return;
    await setMessage('Talk to whom? (in what direction)');
    game._command_mode = 'chatDirection';
}

function chatConsumeTurn() {
    game._process_command_time_now = 1;
    game.context ??= {};
    game.context.move = 1;
}

async function finishChatSteedTarget(steed) {
    if (tipHatMonsterHelpless(steed)) {
        await setMessage(`${chatMonsterName(steed, true)} seems not to notice you.`);
        game._command_mode = null;
        chatConsumeTurn();
        return;
    }

    const noise = tipHatMonsterNoise(steed, {
        visible: true,
        nameOverride: chatMonsterName(steed, true),
    });
    if (noise.handled) {
        await setMessage(noise.message, noise.message.includes('  '));
        game._command_mode = null;
        chatConsumeTurn();
        return;
    }
    if (noise.message) {
        await setMessage(noise.message, noise.message.includes('  '));
        game._command_mode = null;
        return;
    }
    game._pending_message = '';
    game._message_more = 0;
    game._command_mode = null;
}

async function finishChatMonsterTarget(mon, sound) {
    const visible = tipHatMonsterVisible(mon);
    if (tipHatMonsterHelpless(mon) && sound !== 'priest') {
        await setMessage(visible ? `${fireScrollMonsterName(mon)} seems not to notice you.` : '');
        game._command_mode = null;
        return;
    }

    chatClearTargetWaitStrategy(mon);
    if (!heroIsDeaf() && chatMonsterIsEating(mon)) {
        if (!visible) chatMapUnseenMonster(mon);
        await setMessage(`${chatMonsterName(mon, visible)} is eating noisily.`);
        game._command_mode = null;
        return;
    }
    if (heroIsDeaf()) {
        await setMessage(chatDeafResponseMessage(mon, visible));
        game._command_mode = null;
        return;
    }

    const noise = tipHatMonsterNoise(mon, { visible, offerCommandMode: true });
    if (!visible && !tipHatMonsterVisible(mon) && (noise.handled || noise.message || noise.mapInvisible))
        chatMapUnseenMonster(mon);

    if (noise.handled) {
        await setMessage(noise.message, noise.message.includes('  '));
        if (noise.commandMode) {
            game._command_mode = noise.commandMode;
            if (noise.demonBribe) {
                game._demon_bribe_mon = noise.demonBribe.mon;
                game._demon_bribe_demand = noise.demonBribe.demand;
                game._demon_bribe_prompt = noise.demonBribe.mon?._last_demon_bribe_prompt || 'How much will you offer?';
                game._demon_bribe_text = '';
            }
        } else {
            game._command_mode = null;
            chatConsumeTurn();
        }
        return;
    }
    if (noise.message) {
        await setMessage(noise.message, noise.message.includes('  '));
        game._command_mode = null;
        return;
    }
    game._pending_message = '';
    game._message_more = 0;
    game._command_mode = null;
}

function heroHasConflict() {
    const u = game.u || {};
    if (u.conflict || u.HConflict || u.EConflict || u._intrinsicConflict || u._extrinsicConflict)
        return true;
    const intrinsics = u.intrinsics || {};
    const extrinsics = u.extrinsics || {};
    const uprops = u.uprops || {};
    if (intrinsics.conflict || intrinsics.Conflict || extrinsics.conflict || extrinsics.Conflict)
        return true;
    if (uprops.conflict?.intrinsic || uprops.conflict?.extrinsic
        || uprops.CONFLICT?.intrinsic || uprops.CONFLICT?.extrinsic)
        return true;
    return (game.inventory || []).some(item => wornRingItem(item)
        && (Number(item.ringRoll ?? item.roll ?? 0) === 14
            || /\bring of conflict\b/i.test(String(item.actualKind || item.kind || item.line || ''))));
}

function tipHatMonsterHelpless(mon) {
    return !!(mon?.helpless || mon?.msleeping || mon?.mcanmove === false);
}

function tipHatMonsterSilent(mon) {
    const data = mon?.data || {};
    const explicit = mon?.msound ?? mon?.sound ?? data.msound ?? data.sound;
    const msound = String(explicit ?? '').toLowerCase();
    if (mon?.silent || data.silent || explicit === 0 || msound === 'silent' || msound === 'ms_silent')
        return true;
    if (explicit != null) return false;
    const name = String(data.name || mon?.name || '').toLowerCase();
    return tipHatGeneratedMonsterSound(name) === 'silent';
}

function tipHatHeroHasTopLevelGold() {
    if ((game._goldCount || 0) > 0) return true;
    return (game.inventory || []).some(item => item?.letter === '$'
        || item?.cls === 'coin' || item?.otyp === GOLD_PIECE || item?.glyph === '$');
}

function tipHatGoldStack(items = []) {
    return items.find(item => item?.letter === '$'
        || item?.cls === 'coin' || item?.otyp === GOLD_PIECE || item?.glyph === '$') || null;
}

function tipHatGoldCount(items = []) {
    const stack = tipHatGoldStack(items);
    return Math.max(0, Math.trunc(Number(stack?.quan || 0)));
}

function tipHatHeroGoldCount() {
    return Math.max(0, Math.trunc(Number(game._goldCount || 0))) || tipHatGoldCount(game.inventory || []);
}

function tipHatAddHeroGold(amount) {
    const gold = Math.max(0, Math.trunc(Number(amount || 0)));
    if (!gold) return;
    game._goldCount = Math.max(0, Math.trunc(Number(game._goldCount || 0))) + gold;
    const stack = tipHatGoldStack(game.inventory || []);
    if (stack) stack.quan = (stack.quan || 0) + gold;
    else game.inventory = [...(game.inventory || []), { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold }];
}

function tipHatHeroIsHealer() {
    return game._startup_role === 'Healer'
        || game.urole?.name?.m === 'Healer' || game.urole?.name?.f === 'Healer';
}

function tipHatInventoryKind(item) {
    return String(item?.actualKind || item?.kind || item?.line || '').toLowerCase();
}

function tipHatHeroWieldsNurseWeapon() {
    return (game.inventory || []).some(item => {
        if (!itemIsWielded(item)) return false;
        if (item.cls === 'weapon' || item.glyph === ')' || item.otyp === WEAPON_CLASS) return true;
        return item.cls === 'tool' && (isWeaponTool(item) || APPLY_WEAPON_NAME_RE.test(tipHatInventoryKind(item)));
    });
}

function tipHatIsShirtArmor(item) {
    return (item?.cls === 'armor' || item?.glyph === '[' || item?.otyp === ARMOR_CLASS)
        && /\b(?:shirt|t-shirt)\b/.test(tipHatInventoryKind(item));
}

function tipHatHeroWearsNurseArmor() {
    return (game.inventory || []).some(item => isWornArmorItem(item) && !tipHatIsShirtArmor(item));
}

function tipHatHeroWearsShirt() {
    return (game.inventory || []).some(item => isWornArmorItem(item) && tipHatIsShirtArmor(item));
}

function tipHatMonsterAdjacent(mon) {
    if (!mon) return false;
    return Math.max(Math.abs((mon.mx || 0) - (game.u?.ux || 0)),
        Math.abs((mon.my || 0) - (game.u?.uy || 0))) <= 1;
}

function tipHatMonsterIsMplayer(mon) {
    const data = mon?.data || {};
    return !!(mon?.mplayer || mon?.is_mplayer || mon?.isMplayer
        || data.mplayer || data.is_mplayer || data.isMplayer);
}

function tipHatNameTokens(value) {
    const name = String(value || '').toLowerCase();
    return name.replace(/[^a-z]+/g, ' ').trim();
}

function tipHatRaceFamiliesFrom(value, meta = {}) {
    const families = new Set();
    const name = tipHatNameTokens(value || meta.name || meta.data?.name);
    const rawMlet = String(meta.mlet || meta.data?.mlet || '');
    const rawGlyph = String(meta.glyph || meta.data?.glyph || '');
    const mlet = rawMlet.toLowerCase();
    if (meta.orc || meta.isOrc || meta.data?.orc || name === 'orcish'
        || /\b(?:orc|goblin|hobgoblin|uruk hai)\b/.test(name)
        || mlet === 'orc' || rawMlet === 'o' || rawGlyph === 'o')
        families.add('orc');
    if (meta.gnome || meta.isGnome || meta.data?.gnome
        || /\b(?:gnome|gnomish)\b/.test(name)
        || mlet === 'gnome' || rawMlet === 'G' || rawGlyph === 'G')
        families.add('gnome');
    if (meta.kobold || meta.isKobold || meta.data?.kobold || /\bkobold\b/.test(name)
        || mlet === 'kobold' || rawMlet === 'k' || rawGlyph === 'k')
        families.add('kobold');
    if (meta.demon || meta.isDemon || meta.data?.demon
        || /\b(?:yeenoghu|orcus|demon)\b/.test(name)
        || mlet === 'demon' || rawMlet === '&' || rawGlyph === '&')
        families.add('demon');
    return families;
}

function tipHatHeroRaceFamilies() {
    const form = polyselfForm() || game.u?._polyself_form || {};
    const families = tipHatRaceFamiliesFrom(form.name || form.race || form.roleName, form);
    for (const family of tipHatRaceFamiliesFrom(game.urace?.noun || game.urace?.adj || game._startup_race))
        families.add(family);
    return families;
}

function tipHatMonsterSharesHeroRace(mon, monName) {
    const heroFamilies = tipHatHeroRaceFamilies();
    if (!heroFamilies.size) return false;
    for (const family of tipHatRaceFamiliesFrom(monName, mon || {})) {
        if (heroFamilies.has(family)) return true;
    }
    return false;
}

const TIPHAT_MPLAYER_SAME_CLASS_MESSAGES = [
    "I can't win, and neither will you!",
    "You don't deserve to win!",
    "Mine should be the honor, not yours!",
];

const TIPHAT_MPLAYER_OTHER_CLASS_MESSAGES = [
    'The low-life wants to talk, eh?',
    'Fight, scum!',
    'Here is what I have to say!',
];

const TIPHAT_MPLAYER_ROLE_NAMES = [
    'archeologist', 'barbarian', 'caveman', 'healer', 'knight', 'monk', 'priest',
    'ranger', 'rogue', 'samurai', 'tourist', 'valkyrie', 'wizard',
];

function tipHatCanonicalRoleName(value) {
    const role = String(value || '').toLowerCase().replace(/[^a-z]/g, '');
    if (!role) return '';
    if (role === 'cavewoman') return 'caveman';
    if (role === 'priestess') return 'priest';
    return TIPHAT_MPLAYER_ROLE_NAMES.find(name => name.replace(/[^a-z]/g, '') === role) || '';
}

function tipHatMplayerRoleFromIndex(index) {
    if (!Number.isInteger(index)) return '';
    if (index >= 305 && index <= 317) return TIPHAT_MPLAYER_ROLE_NAMES[index - 305] || '';
    if (index >= 0 && index < TIPHAT_MPLAYER_ROLE_NAMES.length)
        return TIPHAT_MPLAYER_ROLE_NAMES[index] || '';
    return '';
}

function tipHatHeroRoleName() {
    const numeric = game.player?.roleMnum ?? game.u?.roleMnum ?? game.urole?.mnum;
    return tipHatMplayerRoleFromIndex(numeric)
        || tipHatCanonicalRoleName(game.urole?.name?.m || game._startup_role || game.urole?.name?.f);
}

function tipHatMonsterMplayerRole(mon, monName) {
    const data = mon?.data || {};
    const numeric = mon?.roleMnum ?? mon?.mnum ?? data.roleMnum ?? data.mnum ?? data.monsndx;
    return tipHatMplayerRoleFromIndex(numeric)
        || tipHatCanonicalRoleName(mon?.role || mon?.roleName || data.role || data.roleName || monName);
}

function tipHatMplayerTalk(mon, monName) {
    const monsterRole = tipHatMonsterMplayerRole(mon, monName);
    const heroRole = tipHatHeroRoleName();
    const sameClass = monsterRole && heroRole && monsterRole === heroRole;
    const messages = sameClass ? TIPHAT_MPLAYER_SAME_CLASS_MESSAGES : TIPHAT_MPLAYER_OTHER_CLASS_MESSAGES;
    return { handled: true, message: `"Talk? -- ${messages[rn2(3)]}"` };
}

function tipHatMonsterMlet(mon) {
    return String(mon?.data?.mlet ?? mon?.mlet ?? '').toLowerCase();
}

function tipHatMonsterHp(mon) {
    const hp = Number(mon?.mhp ?? mon?.hp ?? mon?.data?.mhp ?? mon?.data?.hp ?? NaN);
    const hpmax = Number(mon?.mhpmax ?? mon?.mhpMax ?? mon?.hpmax ?? mon?.hpMax
        ?? mon?.data?.mhpmax ?? mon?.data?.hpmax ?? NaN);
    return { hp, hpmax };
}

function tipHatMonsterTrapAt(mon) {
    return (game.level?.traps || []).find(trap => trap.tx === mon?.mx && trap.ty === mon?.my) || null;
}

function tipHatMonsterIsElf(mon, name) {
    const data = mon?.data || {};
    return !!(mon?.elf || data.elf || data.isElf || /\belf\b|(?:^|-| )elf$|elven/.test(name));
}

function tipHatMonsterIsDwarf(mon, name) {
    const data = mon?.data || {};
    return !!(mon?.dwarf || data.dwarf || data.isDwarf || /\bdwarf\b/.test(name));
}

function tipHatMonsterIsGnome(mon, name) {
    const data = mon?.data || {};
    const mlet = tipHatMonsterMlet(mon);
    return !!(mon?.gnome || data.gnome || data.isGnome || mlet === 'g' || /\b(?:gnome|gnomish)\b/.test(name));
}

const TIPHAT_MAGIC_MONSTER_NAMES = new Set(['kobold shaman', 'orc shaman', 'gnomish wizard']);

function tipHatMonsterLikesMagic(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    return !!(mon?.likesMagic || mon?.magic || mon?.spellcaster
        || data.likesMagic || data.magic || data.spellcaster
        || TIPHAT_MAGIC_MONSTER_NAMES.has(name));
}

function tipHatMonsterIsMinion(mon) {
    const data = mon?.data || {};
    return !!(mon?.isminion || mon?.isMinion || mon?.lminion
        || data.isminion || data.isMinion || data.lminion);
}

function tipHatPeacefulHumanoidNoise(mon, name, monName, moves, hungryTime) {
    const { hp, hpmax } = tipHatMonsterHp(mon);
    const action = text => ({ handled: true, message: `${name} ${text}` });
    const verbal = text => ({ handled: true, message: `"${text}"` });
    if (mon.mflee) return action('wants nothing to do with you.');
    if (Number.isFinite(hp) && Number.isFinite(hpmax) && hp < hpmax / 4)
        return action('moans.');
    if (mon.mconf || mon.mstun) {
        if (!rn2(3)) return verbal('Huh?');
        return verbal(rn2(2) ? 'What?' : 'Eh?');
    }
    if (mon.mcansee === false) return verbal("I can't see!");
    if (mon.mtrapped) {
        const trap = tipHatMonsterTrapAt(mon);
        if (trap) trap.tseen = true;
        return verbal("I'm trapped!");
    }
    if (Number.isFinite(hp) && Number.isFinite(hpmax) && hp < hpmax / 2)
        return action('asks for a potion of healing.');
    if (Number(mon.mtame || 0) && !tipHatMonsterIsMinion(mon)
        && Number.isFinite(hungryTime) && moves > hungryTime)
        return verbal("I'm hungry.");
    if (tipHatMonsterIsElf(mon, monName)) return action('curses orcs.');
    if (tipHatMonsterIsDwarf(mon, monName)) return action('talks about mining.');
    if (tipHatMonsterLikesMagic(mon)) return action('talks about spellcraft.');
    if (tipHatMonsterMlet(mon) === 'centaur' || tipHatMonsterMlet(mon) === 'c')
        return action('discusses hunting.');
    if (tipHatMonsterIsGnome(mon, monName)) {
        if (heroIsHallucinating()) {
            const gnomeplan = rn2(4);
            if (gnomeplan % 2)
                return verbal(gnomeplan === 1
                    ? 'Phase one, collect underpants.'
                    : 'Phase three, profit!');
        }
        return verbal('Many enter the dungeon, and few return to the sunlit lands.');
    }
    if (monName === 'hobbit') {
        return (Number.isFinite(hp) && Number.isFinite(hpmax) && hp < hpmax
            && (hpmax <= 10 || hp <= hpmax - 10))
            ? action('complains about unpleasant dungeon conditions.')
            : action('asks you about the One Ring.');
    }
    if (monName === 'archeologist')
        return action('describes a recent article in "Spelunker Today" magazine.');
    if (monName === 'tourist') return verbal('Aloha.');
    return action('discusses dungeon exploration.');
}

const TIPHAT_BOAST_MONSTER_NAMES = new Set([
    'giant', 'stone giant', 'hill giant', 'fire giant', 'frost giant', 'storm giant',
]);

const TIPHAT_GENERATED_SOUND_BY_MONSTER_NAME = new Map([
    ['killer bee', 'buzz'], ['queen bee', 'buzz'], ['grid bug', 'buzz'], ['xan', 'buzz'],
    ['chickatrice', 'hiss'], ['cockatrice', 'hiss'], ['pyrolisk', 'hiss'],
    ['mind flayer', 'hiss'], ['master mind flayer', 'hiss'],
    ['couatl', 'hiss'], ['garter snake', 'hiss'], ['snake', 'hiss'],
    ['water moccasin', 'hiss'], ['python', 'hiss'], ['pit viper', 'hiss'],
    ['cobra', 'hiss'],
    ['hell hound pup', 'bark'], ['hell hound', 'bark'],
    ['bat', 'sqeek'], ['giant bat', 'sqeek'], ['vampire bat', 'sqeek'],
    ['jaguar', 'growl'], ['lynx', 'growl'], ['panther', 'growl'], ['tiger', 'growl'],
    ['displacer beast', 'growl'], ['bugbear', 'growl'], ['monkey', 'growl'], ['ape', 'growl'],
    ['yeti', 'growl'], ['carnivorous ape', 'growl'], ['sasquatch', 'growl'],
    ['disenchanter', 'growl'], ['pit fiend', 'growl'], ['demogorgon', 'growl'],
    ['gargoyle', 'grunt'], ['winged gargoyle', 'grunt'], ['ettin', 'grunt'],
    ['ogre', 'grunt'], ['ogre lord', 'grunt'], ['ogre lady', 'grunt'], ['ogre leader', 'grunt'],
    ['ogre king', 'grunt'], ['ogre queen', 'grunt'], ['ogre tyrant', 'grunt'],
    ['troll', 'grunt'], ['ice troll', 'grunt'], ['rock troll', 'grunt'],
    ['water troll', 'grunt'], ['olog-hai', 'grunt'],
    ['xorn', 'roar'], ['owlbear', 'roar'],
    ['wumpus', 'burble'], ['jabberwock', 'burble'],
    ['titanothere', 'bellow'], ['baluchitherium', 'bellow'], ['crocodile', 'bellow'],
    ['baby crocodile', 'chirp'], ['shade', 'wail'], ['juiblex', 'gurgle'],
    ['keystone kop', 'arrest'], ['kop sergeant', 'arrest'],
    ['kop lieutenant', 'arrest'], ['kop kaptain', 'arrest'],
    ['guard', 'guard'], ['croesus', 'guard'],
    ['soldier', 'soldier'], ['sergeant', 'soldier'], ['lieutenant', 'soldier'],
    ['captain', 'soldier'], ['watchman', 'soldier'], ['watch captain', 'soldier'],
    ['nurse', 'nurse'],
    ['water demon', 'djinni'], ['prisoner', 'djinni'], ['djinni', 'djinni'],
    ['titan', 'spell'], ['barrow wight', 'spell'], ['nazgul', 'spell'],
    ['nalfeshnee', 'spell'],
    ['angel', 'cuss'], ['archon', 'cuss'],
    ['marilith', 'cuss'], ['sandestin', 'cuss'],
    ['hobbit', 'humanoid'], ['dwarf', 'humanoid'],
    ['dwarf lord', 'humanoid'], ['dwarf lady', 'humanoid'], ['dwarf leader', 'humanoid'],
    ['dwarf king', 'humanoid'], ['dwarf queen', 'humanoid'], ['dwarf ruler', 'humanoid'],
    ['plains centaur', 'humanoid'], ['forest centaur', 'humanoid'],
    ['mountain centaur', 'humanoid'],
    ['quantum mechanic', 'humanoid'], ['genetic engineer', 'humanoid'],
    ['woodland-elf', 'humanoid'], ['green-elf', 'humanoid'], ['grey-elf', 'humanoid'],
    ['elf-lord', 'humanoid'], ['elf-lady', 'humanoid'], ['elf-noble', 'humanoid'],
    ['elvenking', 'humanoid'], ['elvenqueen', 'humanoid'], ['elven monarch', 'humanoid'],
]);

const TIPHAT_GENERATED_SILENT_MONSTER_NAMES = new Set([
    'giant ant', 'soldier ant', 'fire ant', 'giant beetle',
    'acid blob', 'quivering blob', 'gelatinous cube',
    'gas spore', 'floating eye', 'freezing sphere', 'flaming sphere',
    'shocking sphere',
    'manes', 'homunculus', 'lemure', 'quasit',
    'blue jelly', 'spotted jelly', 'ochre jelly',
    'small mimic', 'large mimic', 'giant mimic',
    'rock piercer', 'iron piercer', 'glass piercer',
    'rock mole', 'cave spider', 'centipede', 'giant spider', 'scorpion',
    'lurker above', 'trapper',
    'fog cloud', 'dust vortex', 'ice vortex', 'energy vortex',
    'steam vortex', 'fire vortex',
    'baby long worm', 'baby purple worm', 'long worm', 'purple worm',
    'yellow light', 'black light', 'zruty',
    'stalker', 'air elemental', 'fire elemental', 'earth elemental',
    'water elemental',
    'lichen', 'brown mold', 'yellow mold', 'green mold', 'red mold',
    'violet fungus',
    'kobold mummy', 'gnome mummy', 'orc mummy', 'dwarf mummy', 'elf mummy',
    'human mummy', 'ettin mummy', 'giant mummy',
    'gray ooze', 'brown pudding', 'green slime', 'black pudding',
    'rust monster', 'umber hulk', 'wraith', 'ghoul',
    'straw golem', 'paper golem', 'rope golem', 'gold golem',
    'leather golem', 'wood golem', 'flesh golem', 'clay golem',
    'stone golem', 'glass golem', 'iron golem',
    'horned devil', 'erinys', 'barbed devil',
    'vrock', 'hezrou', 'bone devil', 'ice devil', 'balrog',
    'newt', 'iguana', 'lizard', 'chameleon',
]);

function tipHatGeneratedMonsterSound(name) {
    if (TIPHAT_GENERATED_SILENT_MONSTER_NAMES.has(name)) return 'silent';
    if (/^(?:baby )?(?:gray|gold|silver|red|white|orange|black|blue|green|yellow) dragon$/.test(name))
        return 'roar';
    return TIPHAT_GENERATED_SOUND_BY_MONSTER_NAME.get(name) || '';
}

const TIPHAT_CUSS_RANDOM_INSULTS = [
    'antic', 'blackguard', 'caitiff', 'chucklehead', 'coistrel', 'craven',
    'cretin', 'cur', 'dastard', 'demon fodder', 'dimwit', 'dolt', 'fool',
    'footpad', 'imbecile', 'knave', 'maledict', 'miscreant', 'niddering',
    'poltroon', 'rattlepate', 'reprobate', 'scapegrace', 'varlet',
    'villein', 'wittol', 'worm', 'wretch',
];

const TIPHAT_CUSS_RANDOM_MALEDICTIONS = [
    'Hell shall soon claim thy remains,', 'I chortle at thee, thou pathetic',
    'Prepare to die, thou', 'Resistance is useless,',
    'Surrender or die, thou', 'There shall be no mercy, thou',
    'Thou shalt repent of thy cunning,', 'Thou art as a flea to me,',
    'Thou art doomed,', 'Thy fate is sealed,',
    'Verily, thou shalt be one dead',
];

const TIPHAT_ANGEL_CUSS_MESSAGES = [
    '"Repent, and thou shalt be saved!"',
    '"Thou shalt pay for thine insolence!"',
    '"Very soon, my child, thou shalt meet thy maker."',
    '"The great %D has sent me to make you pay for your sins!"',
    '"The wrath of %D is now upon you!"',
    '"Thy life belongs to %D now!"',
    '"Dost thou wish to receive thy final blessing?"',
    '"Thou art but a godless void."',
    '"Thou art not worthy to seek the Amulet."',
    '"No one expects the Spanish Inquisition!"',
    '"Judgment hath been passed upon thee, %p."',
    '"Thy reckoning is at hand, %p."',
    '"Thou shalt be brought before %D for thy crimes!"',
    '"With %D as my witness, I shall strike thee down."',
];

const TIPHAT_DEMON_CUSS_MESSAGES = [
    '"I first mistook thee for a statue, when I regarded thy head of stone."',
    '"Come here often?"',
    '"Doth pain excite thee?  Wouldst thou prefer the whip?"',
    '"Thinkest thou it shall tickle as I rip out thy lungs?"',
    '"Eat slime and die!"',
    '"Go ahead, fetch thy mama!  I shall wait."',
    '"Go play leapfrog with a herd of unicorns!"',
    '"Hast thou been drinking, or art thou always so clumsy?"',
    '"This time I shall let thee off with a spanking, but let it not happen again."',
    '"I\'ve met smarter (and prettier) acid blobs."',
    '"Look!  Thy bootlace is undone!"',
    '"Mercy!  Dost thou wish me to die of laughter?"',
    '"Run away!  Live to flee another day!"',
    '"Thou hadst best fight better than thou canst dress!"',
    '"Twixt thy cousin and thee, Medusa is the prettier."',
    '"Methinks thou wert unnaturally stirred by yon corpse back there, eh, varlet?"',
    '"Up thy nose with a rubber hose!"',
    '"Verily, thy corpse could not smell worse!"',
    '"Wait!  I shall polymorph into a grid bug to give thee a fighting chance!"',
    '"Why search for the Amulet?  Thou wouldst but lose it, cretin."',
    '"Thou ought to be a comedian, thy skills are so laughable!"',
    '"Thy gaze is so vacant, I thought thee a floating eye!"',
    '"Thy head is unfit for a mind flayer to munch upon!"',
    '"Only thy reflection could love thee!"',
    '"Hast thou considered masking thine odour?"',
    '"Hold! Thy face is a most exquisite torture!"',
    '"I should fart in thy direction, but it might improve thy smell!"',
];

const TIPHAT_IZCHAK_SELL_MESSAGES = [
    name => `${name} says: 'These shopping malls give me a headache.'`,
    name => `${name} says: 'Slow down.  Think clearly.'`,
    name => `${name} says: 'You need to take things one at a time.'`,
    name => `${name} says: 'I don't like poofy coffee... give me Colombian Supremo.'`,
    name => `${name} says that getting the devteam's agreement on anything is difficult.`,
    name => `${name} says that he has noticed those who serve their deity will prosper.`,
    name => `${name} says: 'Don't try to steal from me - I have friends in high places!'`,
    name => `${name} says: 'You may well need something from this shop in the future.'`,
    name => `${name} comments about the Valley of the Dead as being a gateway.`,
];

const TIPHAT_HALLUCINATED_MONSTER_PRONOUNS = [
    { subject: 'he', object: 'him' },
    { subject: 'she', object: 'her' },
    { subject: 'it', object: 'it' },
    { subject: 'they', object: 'them' },
];

function tipHatHallucinatedMonsterPronoun(key) {
    const pronouns = TIPHAT_HALLUCINATED_MONSTER_PRONOUNS[rn2(TIPHAT_HALLUCINATED_MONSTER_PRONOUNS.length)];
    return pronouns[key];
}

function tipHatShopkeeperSellSubjectPronoun(mon) {
    return heroIsHallucinating() ? tipHatHallucinatedMonsterPronoun('subject') : shopkeeperSubjectPronoun(mon);
}

function tipHatShopkeeperSellObjectivePronoun(mon) {
    return heroIsHallucinating() ? tipHatHallucinatedMonsterPronoun('object') : shopkeeperObjectivePronoun(mon);
}

function tipHatLawfulDeityName() {
    const roleName = game.urole?.name?.m || game._startup_role || 'Archeologist';
    const pantheonRole = roleName === 'Priest' ? game._pantheon_role || roleName : roleName;
    return (GODS_BY_ROLE[pantheonRole] || GODS_BY_ROLE.Archeologist)[0] || 'Marduk';
}

function tipHatCommonPagerLine(messages) {
    const line = messages[rn2(messages.length)] || '';
    return line
        .split('%D').join(tipHatLawfulDeityName())
        .split('%p').join(game.plname || 'Hero');
}

function tipHatMonsterIsLawfulMinion(mon) {
    const data = mon?.data || {};
    const renegade = !!(mon?.renegade || mon?.mextra?.emin?.renegade || mon?.emin?.renegade
        || data.renegade || data.mextra?.emin?.renegade || data.emin?.renegade);
    if (mon?.lminion || mon?.is_lminion || mon?.isLminion
        || data.lminion || data.is_lminion || data.isLminion)
        return !renegade;
    const minion = tipHatMonsterIsMinion(mon);
    const align = Number(mon?.min_align ?? mon?.align ?? mon?.alignment
        ?? data.min_align ?? data.align ?? data.alignment ?? data.maligntyp ?? 0);
    return minion && align > A_NEUTRAL && !renegade;
}

function tipHatMonsterIsWizardCuss(mon, monName) {
    const data = mon?.data || {};
    return !!(mon?.iswiz || mon?.isWizard || data.iswiz || data.isWizard
        || monName === 'wizard of yendor');
}

function tipHatHostileCussNoise(mon, name, monName) {
    const monHp = Number(mon?.mhp ?? mon?.hp ?? mon?.data?.mhp ?? NaN);
    let message = '';
    if (tipHatMonsterIsWizardCuss(mon, monName)) {
        if (!rn2(5)) {
            message = `${name} laughs fiendishly.`;
        } else if (game.u?.uhave?.amulet && !rn2(TIPHAT_CUSS_RANDOM_INSULTS.length)) {
            message = `"Relinquish the amulet, ${TIPHAT_CUSS_RANDOM_INSULTS[rn2(TIPHAT_CUSS_RANDOM_INSULTS.length)]}!"`;
        } else if ((game.u?.uhp || 0) < 5 && !rn2(2)) {
            message = rn2(2)
                ? `"Even now thy life force ebbs, ${TIPHAT_CUSS_RANDOM_INSULTS[rn2(TIPHAT_CUSS_RANDOM_INSULTS.length)]}!"`
                : `"Savor thy breath, ${TIPHAT_CUSS_RANDOM_INSULTS[rn2(TIPHAT_CUSS_RANDOM_INSULTS.length)]}, it be thy last!"`;
        } else if (Number.isFinite(monHp) && monHp < 5 && !rn2(2)) {
            message = rn2(2) ? '"I shall return."' : '"I\'ll be back."';
        } else {
            message = `"${TIPHAT_CUSS_RANDOM_MALEDICTIONS[rn2(TIPHAT_CUSS_RANDOM_MALEDICTIONS.length)]} ${TIPHAT_CUSS_RANDOM_INSULTS[rn2(TIPHAT_CUSS_RANDOM_INSULTS.length)]}!"`;
        }
    } else if (tipHatMonsterIsLawfulMinion(mon)) {
        message = tipHatCommonPagerLine(TIPHAT_ANGEL_CUSS_MESSAGES);
    } else if (!rn2(tipHatMonsterIsMinion(mon) ? 100 : 5)) {
        message = `${name} casts aspersions on your ancestry.`;
    } else {
        message = tipHatCommonPagerLine(TIPHAT_DEMON_CUSS_MESSAGES);
    }
    const wakeMessages = tipHatWakeNearby(mon, 5 * 5);
    return { handled: true, message: [message, ...wakeMessages].filter(Boolean).join('  ') };
}

export function monsterHostileCussNoise(mon, name = null) {
    const monName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    return tipHatHostileCussNoise(mon, name || fireScrollMonsterName(mon), monName);
}

function tipHatPeacefulCussNoise(mon) {
    return {
        handled: true,
        message: tipHatMonsterIsLawfulMinion(mon) ? `"It's not too late."` : `"We're all doomed."`,
    };
}

function tipHatHeroWieldsDemonBribeArtifact() {
    return (game.inventory || []).some(item => {
        if (!itemIsWielded(item)) return false;
        const name = String(item.artifact || item.oartifact || artifactObjectName(item)
            || item.actualKind || item.kind || item.line || '').toLowerCase();
        return /\b(?:excalibur|demonbane)\b/.test(name);
    });
}

function tipHatHeroIsDemonPolyself() {
    const form = polyselfForm();
    if (!form) return false;
    return tipHatRaceFamiliesFrom(form.name || form.race || form.roleName, form).has('demon');
}

function tipHatDemonBribeSetHostile(mon) {
    if (!mon) return;
    mon.mpeaceful = 0;
    mon.peaceful = false;
    mon.mtame = 0;
    mon.tame = 0;
    mon.hostile = true;
    set_malign(mon);
    newsym(mon.mx, mon.my);
}

function tipHatDemonBribeAtHome(mon) {
    const inHell = !!game.inhell
        || game.dungeons?.[game.u?.uz?.dnum]?.name === 'Gehennom'
        || !!game.level?.flags?.gehennom;
    return inHell && !(mon?.cham || mon?.mcham || mon?.data?.cham);
}

function tipHatDemonBribeAlignmentFactor(mon) {
    const heroSign = Math.sign(Number(game.u?.ualign?.type ?? A_NEUTRAL));
    const monSign = Math.sign(Number(mon?.data?.maligntyp ?? mon?.maligntyp ?? 0));
    return 1 + (heroSign === monSign ? 1 : 0);
}

function tipHatMonsterHasAmulet(mon) {
    return (mon?.minvent || []).some(item => {
        const name = String(item.actualKind || item.kind || item.artifact || item.line || '').toLowerCase();
        if (/\bcheap plastic imitation\b/.test(name)) return false;
        return item.realAmuletOfYendor || name === 'amulet of yendor' || /\bamulet of yendor\b/.test(name);
    });
}

function tipHatDemonBribeDemand(mon, cash) {
    const homeBonus = tipHatDemonBribeAtHome(mon) ? 20 : 0;
    let demand = Math.trunc((cash * (rnd(80) + homeBonus))
        / (100 * tipHatDemonBribeAlignmentFactor(mon)));
    if (demand > 0 && (tipHatMonsterHasAmulet(mon) || heroIsDeaf())) demand = cash + rn1(1000, 125);
    return demand;
}

export function monsterTurnDemonBribeNoGold(mon) {
    if (!mon || tipHatHeroWieldsDemonBribeArtifact() || tipHatHeroIsDemonPolyself())
        return false;
    const cash = tipHatHeroGoldCount();
    if (cash > 0) return false;
    const revealMessage = tipHatDemonBribeReveal(mon);
    const demand = tipHatDemonBribeDemand(mon, cash);
    mon._last_demon_bribe_demand = demand;
    if (demand) return false;
    tipHatDemonBribeSetHostile(mon);
    return { handled: true, message: revealMessage };
}

export function monsterTurnDemonBribeArtifact(mon) {
    if (!mon || !tipHatHeroWieldsDemonBribeArtifact()) return false;
    const message = tipHatMonsterVisible(mon)
        ? `${fireScrollMonsterName(mon)} looks very angry.`
        : 'You feel tension building.';
    tipHatDemonBribeSetHostile(mon);
    return { handled: true, message };
}

export function monsterTurnDemonBribeDemand(mon) {
    if (!mon || tipHatHeroWieldsDemonBribeArtifact() || tipHatHeroIsDemonPolyself())
        return false;
    const cash = tipHatHeroGoldCount();
    if (cash <= 0) return false;
    const revealMessage = tipHatDemonBribeReveal(mon);
    const demand = tipHatDemonBribeDemand(mon, cash);
    mon._last_demon_bribe_demand = demand;
    if (!demand || (game._helpless_time || 0) > 0) {
        tipHatDemonBribeSetHostile(mon);
        return { handled: true, message: revealMessage, continueTurn: true };
    }

    const speakerName = fireScrollMonsterName(mon);
    if (heroIsDeaf()) {
        const messages = [revealMessage];
        if (tipHatMonsterVisible(mon))
            messages.push(`${speakerName} seems to be demanding something.`);
        messages.push(`${speakerName} gets angry...`);
        tipHatDemonBribeSetHostile(mon);
        return { handled: true, message: messages.filter(Boolean).join('  '), continueTurn: true };
    }

    const prompt = 'How much will you offer?';
    mon._last_demon_bribe_prompt = prompt;
    const demandMessage = `${speakerName} demands ${demand} ${shopCurrency(demand)} for safe passage.  ${prompt}`;
    return {
        handled: true,
        message: [revealMessage, demandMessage].filter(Boolean).join('  '),
        commandMode: 'demonBribeOffer',
        demonBribe: { mon, demand },
    };
}

function tipHatDemonBribeIsPrince(mon) {
    const data = mon?.data || {};
    return !!(mon?.demonPrince || mon?.isDemonPrince || data.demonPrince || data.isDemonPrince);
}

function tipHatDemonBribeReveal(mon) {
    if (!tipHatDemonBribeIsPrince(mon) || !mon?.minvis) return '';
    const wasUnseen = !tipHatMonsterVisible(mon);
    mon.minvis = 0;
    mon.perminvis = 0;
    mon.invisible = 0;
    if (Number.isInteger(mon.mstrategy)) mon.mstrategy &= ~STRAT_APPEARMSG;
    newsym(mon.mx, mon.my);
    if (wasUnseen && tipHatMonsterVisible(mon)) return `${fireScrollMonsterName(mon)} appears before you.`;
    return '';
}

function tipHatDemonRelocationSuffix(x, y, oldX, oldY) {
    const heroX = game.u?.ux ?? 0;
    const heroY = game.u?.uy ?? 0;
    const du = (x - heroX) * (x - heroX) + (y - heroY) * (y - heroY);
    if (du <= 2) return ' next to you';
    if (du <= BOLT_LIM * BOLT_LIM) return ' close by';
    const oldDu = (oldX - heroX) * (oldX - heroX) + (oldY - heroY) * (oldY - heroY);
    if (oldDu === du) return '';
    return du < oldDu ? ' closer to you' : ' farther away';
}

function tipHatDemonBribeRelocate(mon) {
    if (!mon) return '';
    const name = fireScrollMonsterName(mon);
    const wasVisible = tipHatMonsterVisible(mon);
    if (noteleportLevelForMonster(mon))
        return wasVisible ? `A mysterious force prevents ${demonBribeObjectName(mon)} from teleporting!` : '';

    const oldX = mon.mx;
    const oldY = mon.my;
    if (!rlocNoMsg(mon)) return '';
    newsym(oldX, oldY);
    newsym(mon.mx, mon.my);

    const nowVisible = tipHatMonsterVisible(mon);
    if (wasVisible && nowVisible)
        return `${name} vanishes and reappears${tipHatDemonRelocationSuffix(mon.mx, mon.my, oldX, oldY)}.`;
    if (wasVisible) return `${name} vanishes!`;
    if (nowVisible) return `${name} appears${tipHatDemonRelocationSuffix(mon.mx, mon.my, oldX, oldY)}!`;
    return '';
}

function demonBribeOfferValue(text) {
    const match = String(text || '').match(/^\s*([+-]?\d+)/);
    return match ? Math.trunc(Number(match[1])) : 0;
}

function demonBribeObjectName(mon) {
    return fireScrollMonsterName(mon).replace(/^The /, '');
}

function demonBribeGiveGold(mon, amount) {
    const paid = removeGoldFromHero(amount);
    if (paid > 0)
        add_to_minv(mon, { cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: paid });
    return paid;
}

function demonBribeVanish(mon) {
    removeMonsterFromLevel(mon);
    mon.dead = true;
    mon.minvent = [];
}

function clearDemonBribeOfferState() {
    game._demon_bribe_mon = null;
    game._demon_bribe_demand = 0;
    game._demon_bribe_prompt = '';
    game._demon_bribe_text = '';
    game._demon_bribe_monster_turn = null;
    game._demon_bribe_offer_after_more = 0;
}

async function finishDemonBribeOffer() {
    const mon = game._demon_bribe_mon;
    const demand = Math.max(0, Math.trunc(Number(game._demon_bribe_demand || mon?._last_demon_bribe_demand || 0)));
    let offer = demonBribeOfferValue(game._demon_bribe_text);
    const monsterTurn = game._demon_bribe_monster_turn || null;
    clearDemonBribeOfferState();
    game._command_mode = null;
    if (!mon || !demand) {
        await setMessage('');
        if (!monsterTurn) chatConsumeTurn();
        return;
    }

    const objectName = demonBribeObjectName(mon);
    const sentenceName = fireScrollMonsterName(mon);
    const messages = [];
    let paidOff = false;
    if (offer < 0) {
        messages.push(`You try to shortchange ${objectName}, but fumble.`);
        offer = 0;
    } else if (offer === 0) {
        messages.push('You refuse.');
    } else {
        const available = tipHatHeroGoldCount();
        if (offer >= available) {
            messages.push(`You give ${objectName} all your gold.`);
            offer = available;
        } else {
            messages.push(`You give ${objectName} ${offer} ${shopCurrency(offer)}.`);
        }
        offer = demonBribeGiveGold(mon, offer);
    }
    mon._last_demon_bribe_offer = offer;

    if (offer >= demand) {
        messages.push(`${sentenceName} vanishes, laughing about cowardly mortals.`);
        if (monsterTurn) {
            game._monster_resume_index = adjustedMonsterResumeIndexForRemoval(mon, monsterTurn.nextIndex ?? 0);
            game._monster_resume_somebody_can_move = !!monsterTurn.somebodyCanMove;
        }
        demonBribeVanish(mon);
        paidOff = true;
    } else if (offer > 0 && rnd(5 * (game.u?.acurr?.a?.[A_CHA] ?? 10)) > demand - offer) {
        messages.push(`${sentenceName} scowls at you menacingly, then vanishes.`);
        if (monsterTurn) {
            game._monster_resume_index = adjustedMonsterResumeIndexForRemoval(mon, monsterTurn.nextIndex ?? 0);
            game._monster_resume_somebody_can_move = !!monsterTurn.somebodyCanMove;
        }
        demonBribeVanish(mon);
        paidOff = true;
    } else {
        messages.push(`${sentenceName} gets angry...`);
        tipHatDemonBribeSetHostile(mon);
        if (monsterTurn) {
            game._monster_resume_index = monsterTurn.resumeIndex ?? 0;
            game._monster_resume_somebody_can_move = !!monsterTurn.somebodyCanMove;
            game._monster_resume_same_index = 1;
            game._monster_resume_after_preturn = 1;
        }
    }

    await setMessage(messages.join('  '), messages.length > 1);
    if (monsterTurn) {
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game.context ??= {};
        game.context.move = 0;
        game._process_command_time_now = 1;
        if (paidOff && !game._monster_resume_index)
            game._monster_resume_somebody_can_move = !!monsterTurn.somebodyCanMove;
    } else {
        chatConsumeTurn();
    }
}

function tipHatDemonBribeNoise(mon, name, visible, offerCommandMode = false) {
    if (tipHatHeroWieldsDemonBribeArtifact()) {
        tipHatDemonBribeSetHostile(mon);
        return {
            handled: true,
            message: visible ? `${name} looks very angry.` : 'You feel tension building.',
        };
    }

    const revealMessage = tipHatDemonBribeReveal(mon);
    const speakerName = revealMessage ? fireScrollMonsterName(mon) : name;

    if (tipHatHeroIsDemonPolyself()) {
        const relocationMessage = tipHatDemonBribeRelocate(mon);
        return {
            handled: true,
            message: [
                revealMessage,
                `${speakerName} says, "Good hunting, ${game.flags?.female ? 'Sister' : 'Brother'}."`,
                relocationMessage,
            ].filter(Boolean).join('  '),
        };
    }

    const cash = tipHatHeroGoldCount();
    const demand = tipHatDemonBribeDemand(mon, cash);
    mon._last_demon_bribe_demand = demand;
    if (!demand) {
        tipHatDemonBribeSetHostile(mon);
        return { handled: true, message: revealMessage };
    }

    if (offerCommandMode) mon._last_demon_bribe_prompt = 'How much will you offer?';
    const demandMessage = `${speakerName} demands ${demand} ${shopCurrency(demand)} for safe passage.${offerCommandMode ? '  How much will you offer?' : ''}`;
    return {
        handled: true,
        ...(offerCommandMode ? {
            commandMode: 'demonBribeOffer',
            demonBribe: { mon, demand },
        } : {}),
        message: [revealMessage, demandMessage].filter(Boolean).join('  '),
    };
}

function tipHatMonsterIsShopkeeperType(mon, monName = '') {
    const data = mon?.data || {};
    const name = String(monName || data.name || mon?.name || '').toLowerCase();
    return !!(data.shopkeeper || mon?.shopkeeper || name === 'shopkeeper');
}

function tipHatMonsterIsPriestType(mon, monName = '') {
    const data = mon?.data || {};
    const name = String(monName || data.name || mon?.name || '').toLowerCase();
    return !!(mon?.ispriest || mon?.priest || data.priest
        || /^(?:aligned cleric|high cleric|high priest|high priestess)$/.test(name));
}

function tipHatShopkeeperIsAngry(mon) {
    return !!(mon?.hostile || mon?.mpeaceful === 0 || mon?.mpeaceful === false || mon?.angry);
}

function tipHatResidentShopkeeperSellNoise(mon) {
    const canSpeak = shopkeeperCanSpeakToHero(mon);
    if (tipHatShopkeeperIsAngry(mon)) {
        const customerType = shopkeeperRobbedAmount(mon) > 0 ? 'non-paying' : 'rude';
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'mentions' : 'indicates'} how much ${tipHatShopkeeperSellSubjectPronoun(mon)} dislikes ${customerType} customers.` };
    }
    if (mon?.following) {
        const player = String(game.plname || 'Hero');
        const customer = String(mon?.customer || '');
        if (customer !== player) {
            mon.following = 0;
            if (!canSpeak) return { handled: true, message: '' };
            return { handled: true, message: `"${shopkeeperHello(mon)} ${player}!  I was looking for ${customer}."` };
        }
        if (!canSpeak) return { handled: true, message: `${shopkeeperSentenceName(mon)} taps you on the arm.` };
        return { handled: true, message: `"${shopkeeperHello(mon)} ${player}!  Didn't you forget to pay?"` };
    }
    const debit = Math.trunc(Number(mon?.debit || 0));
    if (shopBillEntryCount(mon) > 0) {
        const total = shopBillTotal(mon) + debit;
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'says' : 'indicates'} that your bill comes to ${total} ${shopCurrency(total)}.` };
    }
    if (debit > 0)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'reminds you' : 'indicates'} that you owe ${tipHatShopkeeperSellObjectivePronoun(mon)} ${debit} ${shopCurrency(debit)}.` };
    const credit = Math.trunc(Number(mon?.credit || 0));
    if (credit > 0)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} encourages you to use your ${credit} ${shopCurrency(credit)} of credit.` };
    const shkmoney = shopkeeperCash(mon);
    if (Math.trunc(Number(mon?.robbed || 0)) > 0)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'complains' : 'indicates concern'} about a recent robbery.` };
    if (Math.trunc(Number(mon?.surcharge || 0)) > 0)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'warns you' : 'indicates'} that ${tipHatShopkeeperSellSubjectPronoun(mon)} is watching you carefully.` };
    if (shkmoney < 50)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'complains' : 'indicates'} that business is bad.` };
    if (shkmoney > 4000)
        return { handled: true, message: `${shopkeeperSentenceName(mon)} ${canSpeak ? 'says' : 'indicates'} that business is good.` };
    if (!heroIsHallucinating() && shopkeeperIsIzchak(mon) && shopkeeperInTown(mon)) {
        if (!canSpeak) return { handled: true, message: '' };
        const name = shopkeeperDisplayName(mon);
        const message = TIPHAT_IZCHAK_SELL_MESSAGES[rn2(TIPHAT_IZCHAK_SELL_MESSAGES.length)](name);
        return { handled: true, message };
    }
    if (!canSpeak) return { handled: true, message: '' };
    return { handled: true, message: `${shopkeeperSentenceName(mon)} talks about the problem of shoplifters.` };
}

function tipHatShopkeeperSellNoise(mon, name) {
    if (!mon?.isshk && tipHatMonsterIsShopkeeperType(mon))
        return { handled: true, message: `${name} asks whether you've seen any untended shops recently.` };
    if (mon?.isshk)
        return tipHatResidentShopkeeperSellNoise(mon);
    return { handled: false, message: '' };
}

const TIPHAT_PRIEST_CRANKY_MESSAGES = [
    "Thou wouldst have words, eh?  I'll give thee a word or two!",
    'Talk?  Here is what I have to say!',
    'Pilgrim, I would speak no longer with thee.',
];

function tipHatPriestAlign(mon) {
    const data = mon?.data || {};
    const value = mon?.shrine?.align ?? mon?.min_align ?? mon?.align ?? mon?.alignment
        ?? data.min_align ?? data.align ?? data.alignment ?? data.maligntyp;
    const align = Number(value);
    return Number.isFinite(align) ? align : A_NEUTRAL;
}

function tipHatPriestCoaligned(mon) {
    return Number(game.u?.ualign?.type ?? A_NEUTRAL) === tipHatPriestAlign(mon);
}

function tipHatPriestHasShrine(priest) {
    const shrine = priest?.shrine;
    const loc = shrine ? game.level?.at?.(shrine.x, shrine.y) : null;
    if (!priest?.ispriest || !shrine || loc?.typ !== ALTAR) return false;
    const mask = loc.flags ?? loc.altarmask ?? 0;
    if (!(mask & AM_SHRINE)) return false;
    return shrine.align === Amask2align(mask & ~AM_SHRINE);
}

function tipHatPriestInTempleRoom(priest) {
    const shrine = priest?.shrine;
    if (!priest?.ispriest || !shrine) return false;
    return game.level?.at?.(priest.mx, priest.my)?.roomno === shrine.room;
}

function tipHatPriestInHisTemple(priest) {
    return tipHatPriestInTempleRoom(priest) && tipHatPriestHasShrine(priest);
}

function tipHatTransferPriestAleMoney(priest) {
    const goldStack = tipHatGoldStack(priest?.minvent || []);
    const pmoney = Math.max(0, Math.trunc(Number(goldStack?.quan || 0)));
    if (!pmoney) return 0;
    const amount = pmoney > 1 ? 2 : 1;
    goldStack.quan = Math.max(0, pmoney - amount);
    if (!goldStack.quan)
        priest.minvent = (priest.minvent || []).filter(item => item !== goldStack);
    tipHatAddHeroGold(amount);
    return pmoney;
}

function tipHatRejectAtheismByPriestConsult() {
    game.u ??= {};
    game.u.uconduct ??= {};
    game.u.uconduct.gnostic = Math.max(0, Math.trunc(Number(game.u.uconduct.gnostic || 0))) + 1;
}

function tipHatPriestNoise(priest, name) {
    tipHatRejectAtheismByPriestConsult();
    const coaligned = tipHatPriestCoaligned(priest);
    const strayed = Number(game.u?.ualign?.record ?? 0) < 0;
    if (priest.mflee || (!priest.ispriest && coaligned && strayed)) {
        priest.mpeaceful = 0;
        return { handled: true, message: `${name} doesn't want anything to do with you!` };
    }

    const helpless = tipHatMonsterHelpless(priest);
    if (!tipHatPriestInHisTemple(priest) || !priest.mpeaceful || helpless) {
        const messages = [];
        if (helpless) {
            messages.push(`${name} breaks out of ${tipHatMonsterPossessive(priest)} reverie!`);
            priest.mfrozen = 0;
            priest.msleeping = 0;
            priest.mcanmove = true;
        }
        priest.mpeaceful = 0;
        messages.push(`"${TIPHAT_PRIEST_CRANKY_MESSAGES[rn2(3)]}"`);
        return { handled: true, message: messages.join('  ') };
    }

    if (priest.mpeaceful && tipHatPriestInTempleRoom(priest) && !tipHatPriestHasShrine(priest)) {
        priest.mpeaceful = 0;
        return {
            handled: true,
            message: '"Begone!  Thou desecratest this holy place with thy presence."',
        };
    }

    if (!tipHatHeroGoldCount()) {
        if (coaligned && !strayed) {
            const pmoney = tipHatTransferPriestAleMoney(priest);
            if (pmoney > 0) {
                const bits = heroIsHallucinating()
                    ? shopCurrency(pmoney)
                    : pmoney === 1 ? 'bit' : 'bits';
                return { handled: true, message: `${name} gives you ${pmoney === 1 ? 'one ' : 'two '}${bits} for an ale.` };
            }
            return { handled: true, message: `${name} preaches the virtues of poverty.` };
        }
        return { handled: true, message: `${name} is not interested.` };
    }

    const cheapskate = Math.max(0, Math.trunc(Number(priest.cheapskate_count ?? priest.cheapskateCount ?? 0)));
    const peak = Math.max(1, Math.trunc(Number(game.u?.ulevelpeak || game.u?.ulevel || 1)));
    const suggested = peak * rn1(101, 150 + cheapskate * 40);
    const quan = Math.max(1, Math.trunc(tipHatHeroGoldCount() / (suggested * 3)));
    priest._last_priest_suggested = suggested;
    priest._last_priest_offer_prompt = `How much will you offer (suggested: ${suggested * quan} or ${suggested * quan * 2})?`;
    return { handled: true, message: `${name} asks you for a contribution for the temple.` };
}

function tipHatMonsterIsHumanWereForm(mon, monName, mlet) {
    const data = mon?.data || {};
    if (!/^(?:were(?:rat|jackal|wolf))$/.test(monName)) return false;
    if (mon?.wereBeast || data.wereBeast) return false;
    return !!(mon?.wereHuman || data.wereHuman || data.humanoid || data.human
        || mlet === '@' || mlet === 'human' || mlet === 'humanoid');
}

function tipHatMonsterIsVampireInOwnForm(monName) {
    return /^vampire(?: (?:lord|lady|leader|mage))?$/.test(monName) || monName === 'vlad the impaler';
}

const TIPHAT_MUMBLE_MONSTER_NAMES = new Set([
    'lich', 'demilich', 'master lich', 'arch-lich', 'salamander',
]);

function tipHatMonsterHasOrcSound(mon, name) {
    if (PROPER_NAME_MS_ORC_MONSTER_NAMES.has(name)) return true;
    const families = tipHatRaceFamiliesFrom(name, mon || {});
    return families.has('orc') || families.has('gnome') || families.has('kobold');
}

function tipHatMonsterSound(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    const mlet = String(data.mlet || mon?.mlet || '').toLowerCase();
    if (mon?.isshk) return 'sell';
    const explicit = mon?.msound ?? mon?.sound ?? data.msound ?? data.sound;
    if (explicit != null) return String(explicit).toLowerCase().replace(/^ms_/, '');
    if (mon?.nemesis || data.nemesis) return 'nemesis';
    if (tipHatMonsterIsShopkeeperType(mon, name)) return 'sell';
    if (tipHatMonsterIsPriestType(mon, name)) return 'priest';
    {
        const generatedSound = tipHatGeneratedMonsterSound(name);
        if (generatedSound) return generatedSound;
    }
    if (name === 'skeleton') return 'bones';
    if (/^(death|pestilence|famine)$/.test(name)) return 'rider';
    if (tipHatMonsterIsWizardCuss(mon, name)) return 'cuss';
    if (/\bzombie$/.test(name) || (mlet === 'zombie' && name !== 'ghoul')) return 'groan';
    if (tipHatMonsterHasOrcSound(mon, name)) return 'orc';
    if (/^(gremlin|leprechaun)$/.test(name)) return 'laugh';
    if (name === 'shrieker') return 'shriek';
    if (/^(mumak|mastodon)$/.test(name)) return 'trumpet';
    if (/^(rothe|minotaur)$/.test(name)) return 'moo';
    if (/^(raven|tengu)$/.test(name)) return 'sqawk';
    if (/^(leocrotta|aleax|doppelganger)$/.test(name)) return 'imitate';
    if (TIPHAT_MUMBLE_MONSTER_NAMES.has(name) || /\bnaga\b/.test(name)) return 'mumble';
    if (name === 'ki-rin') return 'spell';
    if (name === 'oracle') return 'oracle';
    if (name === 'imp') return 'cuss';
    if (name === 'amorous demon') return 'seduce';
    if (tipHatMonsterIsNymph(mon, name, mlet)) return 'seduce';
    if (tipHatMonsterIsHumanWereForm(mon, name, mlet)) return 'were';
    if (tipHatMonsterIsVampireInOwnForm(name)) return 'vampire';
    if (TIPHAT_BOAST_MONSTER_NAMES.has(name)) return 'boast';
    if (/^(pony|horse|warhorse|white unicorn|gray unicorn|black unicorn)$/.test(name)
        || mlet === 'quadruped' || mlet === 'unicorn')
        return 'neigh';
    if (/^(jackal|fox|coyote|little dog|dog|large dog|dingo|wolf|werejackal|werewolf|winter wolf cub|winter wolf|warg)$/.test(name)
        || mlet === 'dog')
        return 'bark';
    if (/^(kitten|housecat|large cat|jaguar|lynx|panther)$/.test(name)
        || mlet === 'f' || mlet === 'feline')
        return 'mew';
    if (name === 'gecko') return 'sqeek';
    if (mlet === 'rodent' || /rat|mouse/.test(name)) return 'sqeek';
    if (mlet === 'snake' || /snake|viper|cobra/.test(name)) return 'hiss';
    return '';
}

function tipHatMonsterAppearsAsGecko(mon) {
    if (!mon) return false;
    const name = String(mon.data?.name || mon.name || '').toLowerCase();
    if (name === 'gecko') return true;
    const loc = game.level?.at?.(mon.mx, mon.my);
    const index = loc?.hallucinated_monster_index;
    return Number.isInteger(index) && DISPLAY_MONSTER_HALLU_NAMES[index] === 'gecko';
}

function tipHatIsNight() {
    if (game.flags?.night || game.flags?.isnight) return true;
    const hourValue = game.flags?.hour ?? game.hour ?? game.context?.hour;
    const datetime = String(game._datetime || '');
    const hour = hourValue == null
        ? (/^\d{10}/.test(datetime) ? Number(datetime.slice(8, 10)) : NaN)
        : Number(hourValue);
    return Number.isFinite(hour) && (hour < 6 || hour > 21);
}

function tipHatIsMidnight() {
    const hourValue = game.flags?.hour ?? game.hour ?? game.context?.hour;
    const datetime = String(game._datetime || '');
    const hour = hourValue == null
        ? (/^\d{10}/.test(datetime) ? Number(datetime.slice(8, 10)) : NaN)
        : Number(hourValue);
    return hour === 0;
}

function tipHatHeroVampireKindred() {
    return /^(?:vampire|vampire lord|vampire leader|vampire lady)$/.test(polyselfFormName());
}

function tipHatHeroNightChild() {
    return /^(?:wolf|winter wolf|winter wolf cub)$/.test(polyselfFormName());
}

function tipHatHeroRaceNoun() {
    const race = game.urace || {};
    const individual = race.individual || {};
    if (game.flags?.female && individual.f) return individual.f;
    return individual.m || race.noun || game._startup_race || 'human';
}

function tipHatHeroBloodPart() {
    const form = polyselfForm() || {};
    const name = polyselfFormName();
    const mlet = String(form.mlet || form.glyph || '').toLowerCase();
    if (form.light || mlet === 'light' || /^(?:yellow|black) light$/.test(name)) return 'beam';
    if (form.spider || mlet === 'spider' || /spider/.test(name)) return 'hemolymph';
    if (form.vortex || form.elemental || form.sphere || mlet === 'vortex'
        || mlet === 'elemental' || mlet === 'eye' || /(?:vortex|elemental|sphere|eye)$/.test(name))
        return 'life force';
    if (form.fungus || mlet === 'fungus' || /(?:mold|lichen|fungus|toadstool)$/.test(name))
        return 'juices';
    if (form.jelly || form.pudding || form.blob || mlet === 'jelly'
        || mlet === 'pudding' || mlet === 'blob' || /(?:jelly|pudding|blob|ooze)$/.test(name))
        return 'juices';
    return 'blood';
}

function tipHatMonsterHungryTime(mon) {
    return Number(mon?.hungrytime ?? mon?.hungryTime ?? mon?.edog?.hungrytime
        ?? mon?.mextra?.edog?.hungrytime ?? NaN);
}

function tipHatMonsterPlineName(mon, visible, fallbackName) {
    if (!visible) return 'It';
    if (heroIsHallucinating()) return `The ${randomHallucinatedMonsterName()}`;
    return fallbackName || fireScrollMonsterName(mon);
}

function tipHatLivingMonster(mon) {
    return !!mon && !mon.dead && (mon.mhp == null || mon.mhp > 0);
}

function tipHatClearWakeWait(mon) {
    if (Number.isInteger(mon.mstrategy)) mon.mstrategy &= ~STRAT_WAITMASK;
    else if (mon.mstrategy === 'waitforu') mon.mstrategy = 0;
    mon.waiting = false;
}

function tipHatWakeNearby(mon, distance) {
    const messages = [];
    for (const sleeper of game.level?.monsters || []) {
        if (!tipHatLivingMonster(sleeper)) continue;
        const dx = (sleeper.mx || 0) - (mon?.mx || 0);
        const dy = (sleeper.my || 0) - (mon?.my || 0);
        if (distance !== 0 && dx * dx + dy * dy >= distance) continue;
        if (sleeper.msleeping && tipHatMonsterVisible(sleeper)) {
            const alive = String(sleeper.data?.name || sleeper.name || '').toLowerCase() === 'flesh golem'
                ? " It's alive!" : '';
            messages.push(`${fireScrollMonsterName(sleeper)} wakes up.${alive}`);
        }
        sleeper.msleeping = 0;
        if (!(sleeper.unique || sleeper.data?.unique || sleeper.data?.uniq))
            tipHatClearWakeWait(sleeper);
    }
    disturbBuriedZombieCorpseTimersAt(mon?.mx || 0, mon?.my || 0);
    return messages;
}

const TIPHAT_DEATH_QUOTE_OID = 1;
const TIPHAT_MAX_TRIBUTE_PASSAGES = 30;

function tipHatTributeContext() {
    game.context ??= {};
    game.context.tribute ??= {};
    return game.context.tribute;
}

function tipHatNovelTrackingContext() {
    game.context ??= {};
    game.context.novel ??= {};
    return game.context.novel;
}

function tipHatNovelTitleIndex(value) {
    if (value == null) return -1;
    const raw = String(value).trim().replace(/^the\s+/i, 'The ');
    const lower = raw.toLowerCase();
    const aliases = new Map([
        ['the color of magic', 'The Colour of Magic'],
        ['sorcery', 'Sourcery'],
        ['masquerade', 'Maskerade'],
        ['the amazing maurice', 'The Amazing Maurice and His Educated Rodents'],
        ['thud', 'Thud!'],
    ]);
    const canonical = aliases.get(lower) || raw;
    const wanted = canonical.toLowerCase();
    return TRIBUTE_NOVEL_TITLES.findIndex(title =>
        title.toLowerCase() === wanted
        || title.toLowerCase().replace(/^the\s+/i, '') === wanted
        || title.toLowerCase() === `the ${wanted}`);
}

function tipHatNovelTitle(item) {
    const roll = rn2(TRIBUTE_NOVEL_TITLES.length);
    let index = Number.isInteger(item?.novelidx) ? item.novelidx
        : Number.isInteger(item?.novelIndex) ? item.novelIndex
            : tipHatNovelTitleIndex(item?.novelTitle ?? item?.title ?? item?.oname);
    if (index < 0 || index >= TRIBUTE_NOVEL_TITLES.length) {
        index = roll;
        if (item) item.novelidx = index;
    }
    return TRIBUTE_NOVEL_TITLES[index] || '';
}

function tipHatHeroCarriedNovel() {
    return (game.inventory || []).find(item => {
        const kind = String(item?.actualKind || item?.kind || item?.name || '').toLowerCase();
        return item?.novel || (item?.cls === 'spellbook' && kind === 'novel');
    }) || null;
}

function tipHatChooseTributePassage(passageCount, oid) {
    if (passageCount < 1) return 0;
    const tracking = tipHatNovelTrackingContext();
    if (tracking.id !== oid || !tracking.count) {
        tracking.id = oid;
        const passages = [];
        if (passageCount <= TIPHAT_MAX_TRIBUTE_PASSAGES) {
            for (let idx = 0; idx < passageCount; idx++) passages.push(idx + 1);
        } else {
            let range = passageCount;
            let limit = TIPHAT_MAX_TRIBUTE_PASSAGES;
            for (let idx = 0; idx < passageCount; idx++, range--) {
                if (range > 0 && rn2(range) < limit) {
                    passages.push(idx + 1);
                    limit--;
                }
            }
        }
        tracking.count = passages.length;
        tracking.pasg = passages;
    }
    const idx = rn2(tracking.count);
    const passage = tracking.pasg[idx];
    tracking.count--;
    tracking.pasg[idx] = tracking.pasg[tracking.count];
    return passage;
}

function tipHatDeathQuote() {
    const passage = tipHatChooseTributePassage(TRIBUTE_DEATH_QUOTES.length, TIPHAT_DEATH_QUOTE_OID);
    return TRIBUTE_DEATH_QUOTES[passage - 1] || '';
}

function tipHatDeathSpeech(message) {
    return String(message || '').toUpperCase();
}

function tipHatDeathRiderNoise(name) {
    const tribute = tipHatTributeContext();
    const book = !tribute.Deathnotice ? tipHatHeroCarriedNovel() : null;
    if (book) {
        const title = tipHatNovelTitle(book);
        tribute.Deathnotice = 1;
        if (title) {
            let message = `Ah, so you have a copy of /${title}/.`;
            if (title !== 'Snuff' && title !== 'The Wee Free Men')
                message += '  I may have been misquoted there.';
            return { handled: true, message: tipHatDeathSpeech(message) };
        }
    }
    if (rn2(3)) {
        const quote = tipHatDeathQuote();
        if (quote) return { handled: true, message: tipHatDeathSpeech(quote) };
    }
    if (!rn2(10))
        return { handled: true, message: `${name} is busy reading a copy of Sandman #8.` };
    return { handled: true, message: tipHatDeathSpeech('Who do you think you are, War?') };
}

function tipHatAggravateMonsters() {
    for (const mon of game.level?.monsters || []) {
        if (!tipHatLivingMonster(mon)) continue;
        mon.msleeping = 0;
        if (Number.isInteger(mon.mstrategy))
            mon.mstrategy &= ~(STRAT_WAITFORU | STRAT_APPEARMSG);
        else if (mon.mstrategy === 'waitforu') mon.mstrategy = 0;
        mon.waiting = false;
        if (mon.mcanmove === false && !rn2(5)) {
            mon.mfrozen = 0;
            mon.mcanmove = true;
        }
    }
}

function tipHatQuestRoleInfo() {
    const roleName = game.urole?.name?.m || game._startup_role || '';
    return QUEST_ROLE_DATA[roleName] || null;
}

function tipHatMonsterIsRoleQuestGuardian(mon, monName) {
    const info = tipHatQuestRoleInfo();
    if (!info?.guardian) return false;
    const guardian = String(info.guardian).toLowerCase();
    const data = mon?.data || {};
    return monName === guardian
        || String(mon?.questGuardian || mon?.guardianName || data.questGuardian || data.guardianName || '').toLowerCase() === guardian;
}

function tipHatQuestGuardianNoise() {
    const after = game.u?.uhave?.questart && game.quest_status?.killed_nemesis;
    const message = questPagerText(after ? 'guardtalk_after' : 'guardtalk_before', { initCore: false });
    return message ? { handled: true, message } : { handled: false, message: '' };
}

function tipHatQuestNemesisNoise() {
    game.quest_status ??= {};
    const message = questPagerText('discourage', { initCore: false });
    if (!game.quest_status.met_nemesis) game.quest_status.met_nemesis = true;
    return message ? { handled: true, message } : { handled: false, message: '' };
}

function tipHatMonsterNoise(mon, { visible = tipHatMonsterVisible(mon), nameOverride = null, offerCommandMode = false } = {}) {
    const silent = tipHatMonsterSilent(mon);
    if (!mon || heroIsDeaf() || (silent && !mon?.isshk)) return { handled: false, message: '' };
    const name = nameOverride ?? (visible ? fireScrollMonsterName(mon) : 'It');
    const monName = String(mon?.data?.name || mon?.name || '').toLowerCase();
    let sound = tipHatMonsterSound(mon);
    const peaceful = !!mon.mpeaceful;
    const tame = Number(mon.mtame || 0);
    const moves = Number(game.moves ?? game.context?.moves ?? 0);
    const hungryTime = tipHatMonsterHungryTime(mon);
    if (sound === 'orc' && (tipHatMonsterSharesHeroRace(mon, monName) || heroIsHallucinating())) sound = 'humanoid';
    else if (sound === 'moo' && !tame) sound = 'bellow';
    else if (sound === 'guardian' && !tipHatMonsterIsRoleQuestGuardian(mon, monName)) sound = 'humanoid';
    else if (heroIsHallucinating() && tipHatMonsterAppearsAsGecko(mon)) sound = 'sell';
    switch (sound) {
    case 'sell':
        if (heroIsHallucinating()) {
            if (mon?.isshk && (silent || !rn2(2))) return tipHatShopkeeperSellNoise(mon, name);
            return { handled: true, message: `"15 minutes could save you 15 ${shopCurrency(15)}."` };
        }
        return tipHatShopkeeperSellNoise(mon, name);
    case 'bark': {
        if (game.flags?.moonphase === 4 && tipHatIsNight())
            return { handled: true, message: `${name} howls.` };
        if (peaceful) {
            if (monName === 'dingo') return { handled: true, message: '' };
            if (tame && (mon.mconf || mon.mflee || mon.mtrapped
                || (Number.isFinite(hungryTime) && moves > hungryTime) || tame < 5))
                return { handled: true, message: `${name} whines.` };
            if (tame && Number.isFinite(hungryTime) && hungryTime > moves + 1000)
                return { handled: true, message: `${name} yips.` };
            return { handled: true, message: `${name} barks.` };
        }
        return { handled: true, message: `${name} growls.` };
    }
    case 'mew':
        if (tame) {
            if (mon.mconf || mon.mflee || mon.mtrapped || tame < 5)
                return { handled: true, message: `${name} yowls.` };
            if (Number.isFinite(hungryTime) && moves > hungryTime)
                return { handled: true, message: `${name} meows.` };
            if (Number.isFinite(hungryTime) && hungryTime > moves + 1000)
                return { handled: true, message: `${name} purrs.` };
            return { handled: true, message: `${name} mews.` };
        }
        return { handled: true, message: `${name} ${peaceful ? 'snarls.' : 'growls!'}` };
    case 'growl':
        return { handled: true, message: `${name} ${peaceful ? 'snarls.' : 'growls!'}` };
    case 'roar':
        return { handled: true, message: `${name} ${peaceful ? 'snarls.' : 'roars!'}` };
    case 'sqeek':
    case 'squeak':
        return { handled: true, message: `${name} squeaks.` };
    case 'sqawk':
    case 'squawk':
        if (monName === 'raven' && !peaceful) return { handled: true, message: '"Nevermore!"' };
        return { handled: true, message: `${name} squawks.` };
    case 'hiss':
        return peaceful
            ? { handled: false, message: '', mapInvisible: !visible }
            : { handled: true, message: `${name} hisses!` };
    case 'buzz':
        return { handled: true, message: `${name} ${peaceful ? 'drones.' : 'buzzes angrily.'}` };
    case 'orc':
    case 'grunt':
        return { handled: true, message: `${name} grunts.` };
    case 'neigh': {
        if (tame < 5) return { handled: true, message: `${name} neighs.` };
        if (Number.isFinite(hungryTime) && moves > hungryTime)
            return { handled: true, message: `${name} whinnies.` };
        return { handled: true, message: `${name} whickers.` };
    }
    case 'moo':
        return { handled: true, message: `${name} moos.` };
    case 'bellow':
        return { handled: true, message: `${name} bellows!` };
    case 'chirp':
        return { handled: true, message: `${name} chirps.` };
    case 'wail':
        return { handled: true, message: `${name} wails mournfully.` };
    case 'groan':
        return { handled: true, message: rn2(3) ? '' : `${name} groans.` };
    case 'gurgle':
        return { handled: true, message: `${name} gurgles.` };
    case 'djinni':
        if (tame) return { handled: true, message: `"Sorry, I'm all out of wishes."` };
        if (peaceful)
            return monName === 'water demon'
                ? { handled: true, message: `${name} gurgles.` }
                : { handled: true, message: `"I'm free!"` };
        return monName === 'prisoner'
            ? { handled: true, message: `"Get me out of here."` }
            : { handled: true, message: `"This will teach you not to disturb me!"` };
    case 'humanoid':
        if (!peaceful) {
            if (tipHatMonsterIsMplayer(mon) && In_endgame(game.u?.uz))
                return tipHatMplayerTalk(mon, monName);
            return { handled: true, message: `${tipHatMonsterPlineName(mon, visible, name)} threatens you.` };
        }
        return tipHatPeacefulHumanoidNoise(mon, name, monName, moves, hungryTime);
    case 'boast':
        if (peaceful) return tipHatPeacefulHumanoidNoise(mon, name, monName, moves, hungryTime);
        switch (rn2(4)) {
        case 0:
            return {
                handled: true,
                message: `${name} boasts about ${tipHatMonsterPossessive(mon)} gem collection.`,
            };
        case 1:
            return { handled: true, message: `${name} complains about a diet of mutton.` };
        default: {
            const wakeMessages = tipHatWakeNearby(mon, 7 * 7);
            return {
                handled: true,
                message: [...wakeMessages, `${name} shouts "Fee Fie Foe Foo!" and guffaws.`].join('  '),
            };
        }
        }
    case 'burble':
        return { handled: true, message: `${name} burbles.` };
    case 'trumpet': {
        const wakeMessages = tipHatWakeNearby(mon, 11 * 11);
        return { handled: true, message: [...wakeMessages, `${name} trumpets!`].join('  ') };
    }
    case 'shriek':
        tipHatAggravateMonsters();
        return { handled: true, message: `${name} shrieks.` };
    case 'laugh': {
        const laughMessages = ['giggles.', 'chuckles.', 'snickers.', 'laughs.'];
        return { handled: true, message: `${name} ${laughMessages[rn2(4)]}` };
    }
    case 'imitate':
        return { handled: true, message: `${name} imitates you.` };
    case 'bones':
        game._helpless_time = Math.max(game._helpless_time || 0, 2);
        game._wake_message = 'You can move again.';
        return { handled: true, message: `${name} rattles noisily.  You freeze for a moment.` };
    case 'mumble':
        return { handled: true, message: `${name} mumbles incomprehensibly.` };
    case 'spell':
        return { handled: true, message: `${name} seems to mutter a cantrip.` };
    case 'oracle':
        if (!peaceful)
            return { handled: false, message: `${name} is in no mood for consultations.`, mapInvisible: !visible };
        if (!tipHatHeroHasTopLevelGold())
            return { handled: false, message: 'You have no gold.', mapInvisible: !visible };
        return { handled: false, message: '', mapInvisible: !visible };
    case 'priest':
        return tipHatPriestNoise(mon, name);
    case 'seduce': {
        const sameGender = !!game.flags?.female === tipHatMonsterFemale(mon);
        if (!tipHatMonsterIsNymph(mon, monName) && !sameGender)
            return { handled: false, message: '' };
        if (!sameGender) {
            const swval = rn2(3);
            if (swval === 2) return { handled: true, message: '"Hello, sailor."' };
            if (swval === 1) return { handled: true, message: `${name} comes on to you.` };
        }
        return { handled: true, message: `${name} cajoles you.` };
    }
    case 'were': {
        const fullMoon = game.flags?.moonphase === 4;
        const isNight = tipHatIsNight();
        const bloodcurdling = fullMoon && (isNight ? rn2(13) !== 0 : !rn2(13));
        if (!bloodcurdling)
            return { handled: true, message: `${name} whispers inaudibly.  All you can make out is "moon".` };
        const cry = monName === 'wererat' ? 'shriek' : 'howl';
        const howlMessage = `${name} throws back ${tipHatMonsterPossessive(mon)} head and lets out a blood curdling ${cry}!`;
        const wakeMessages = tipHatWakeNearby(mon, 11 * 11);
        return { handled: true, message: [howlMessage, ...wakeMessages].filter(Boolean).join('  ') };
    }
    case 'vampire': {
        const isNight = tipHatIsNight();
        const kindred = tipHatHeroVampireKindred();
        const nightchild = tipHatHeroNightChild();
        if (tame) {
            if (kindred) {
                const ending = isNight ? '!' : '.  Why do we not rest?';
                return { handled: true, message: `"Good ${isNight ? 'evening' : 'day'} to you Master${ending}"` };
            }
            const prefix = nightchild ? 'Child of the night, ' : '';
            if (tipHatIsMidnight())
                return { handled: true, message: `"${prefix}I can stand this craving no longer!"` };
            if (isNight)
                return { handled: true, message: `"${prefix}I beg you, help me satisfy this growing craving!"` };
            return { handled: true, message: `"${prefix}I find myself growing a little weary."` };
        }
        if (peaceful) {
            if (kindred && isNight)
                return { handled: true, message: `"Good feeding ${game.flags?.female ? 'sister' : 'brother'}!"` };
            if (nightchild && isNight)
                return { handled: true, message: '"How nice to hear you, child of the night!"' };
            return { handled: true, message: '"I only drink... potions."' };
        }
        if (kindred)
            return { handled: true, message: '"This is my hunting ground that you dare to prowl!"' };
        const formName = polyselfFormName();
        if (formName === 'silver dragon' || formName === 'baby silver dragon') {
            const address = formName === 'silver dragon' ? 'Fool' : 'Young Fool';
            return { handled: true, message: `"${address}!  Your silver sheen does not frighten me!"` };
        }
        if (rn2(2) === 0)
            return { handled: true, message: `"I vant to suck your ${tipHatHeroBloodPart()}!"` };
        const prey = polyselfForm() ? articleFor(polyselfFormName()) : articleFor(tipHatHeroRaceNoun());
        return { handled: true, message: `"I vill come after ${prey} without regret!"` };
    }
    case 'rider':
        if (monName === 'death') return tipHatDeathRiderNoise(name);
        return { handled: true, message: '"Who do you think you are, War?"' };
    case 'guardian':
        return tipHatQuestGuardianNoise();
    case 'nemesis':
        return tipHatQuestNemesisNoise();
    case 'bribe':
        if (peaceful && !tame) return tipHatDemonBribeNoise(mon, name, visible, offerCommandMode);
        if (peaceful) return tipHatPeacefulCussNoise(mon);
        return tipHatHostileCussNoise(mon, name, monName);
    case 'cuss':
        if (peaceful) return tipHatPeacefulCussNoise(mon);
        return tipHatHostileCussNoise(mon, name, monName);
    case 'arrest': {
        if (peaceful)
            return { handled: true, message: `"Just the facts, ${game.flags?.female ? "Ma'am" : 'Sir'}."` };
        const arrestMessages = [
            'Anything you say can be used against you.',
            "You're under arrest!",
            'Stop in the name of the Law!',
        ];
        return { handled: true, message: `"${arrestMessages[rn2(3)]}"` };
    }
    case 'guard':
        return tipHatHeroHasTopLevelGold()
            ? { handled: true, message: `"Please drop that gold and follow me."` }
            : { handled: true, message: `"Please follow me."` };
    case 'nurse':
        if (mon.mcan) return { handled: true, message: `"I hate this job!"` };
        if (tipHatHeroWieldsNurseWeapon())
            return { handled: true, message: `"Put that weapon away before you hurt someone!"` };
        if (tipHatHeroWearsNurseArmor()) {
            return tipHatHeroIsHealer()
                ? { handled: true, message: `"Doc, I can't help you unless you cooperate."` }
                : { handled: true, message: `"Please undress so I can examine you."` };
        }
        if (tipHatHeroWearsShirt()) return { handled: true, message: `"Take off your shirt, please."` };
        return { handled: true, message: `"Relax, this won't hurt a bit."` };
    case 'soldier': {
        const soldierMessages = peaceful
            ? [
                "What lousy pay we're getting here!",
                "The food's not fit for Orcs!",
                "My feet hurt, I've been on them all day!",
            ]
            : [
                'Resistance is useless!',
                "You're dog meat!",
                'Surrender!',
            ];
        return { handled: true, message: `"${soldierMessages[rn2(3)]}"` };
    }
    default:
        return { handled: false, message: '' };
    }
}

function tipHatRudeHumanoidResponse(name) {
    const reaction = ['curses', 'gestures rudely', 'gestures offensively'];
    const deaf = heroIsDeaf();
    const which = deaf ? rn1(2, 1) : rn2(3);
    let twice = 0;
    if (!deaf && which === 0 && !rn2(3))
        twice = rn1(2, 1);
    return `${name} ${reaction[which]}${twice ? ` and ${reaction[twice]}` : ''} at you...`;
}

function tipHatDirectedResponse(dir) {
    if (!dir.dx && !dir.dy) {
        if (game.u?.usteed && dir.dz > 0) {
            if (tipHatMonsterHelpless(game.u.usteed))
                return `${fireScrollMonsterName(game.u.usteed)} doesn't notice.`;
            return tipHatMonsterNoise(game.u.usteed, { visible: true }).message;
        }
        if (dir.dz) return `There's no one ${dir.dz < 0 ? 'up' : 'down'} there.`;
        return "The lout here doesn't acknowledge you...";
    }

    let x = game.u?.ux || 0;
    let y = game.u?.uy || 0;
    let target = null;
    let unseen = false;
    let statue = false;
    for (let range = 1; range <= BOLT_LIM + 1; range++) {
        x += dir.dx;
        y += dir.dy;
        statue = false;
        if (!isok(x, y) || (range > 1 && !couldsee(x, y))) break;
        let monHere = (game.level?.monsters || []).find(mon => mon.mx === x && mon.my === y && !mon.dead);
        const rawVisibleMonster = tipHatMonsterCanBeSeen(monHere);
        const apparentStatue = rawVisibleMonster && tipHatMonsterAppearsAsStatueGlyph(monHere);
        const apparent = rawVisibleMonster && tipHatApparentObjectOrFurniture(monHere);
        if (apparent) monHere = null;
        const loc = game.level?.at?.(x, y);
        unseen = !!loc?.map_invisible;
        statue = tipHatLocationHasStatueGlyph(loc)
            || apparentStatue
            || (!rawVisibleMonster && !unseen && !!floorStatueAt(x, y));
        const visibleTarget = tipHatMonsterVisible(monHere);
        const adjacentResponder = range === 1 && monHere && tipHatMonsterResponsive(monHere)
            && !tipHatMonsterSilent(monHere);
        if (visibleTarget || unseen || (statue && heroIsHallucinating()) || adjacentResponder
            || !loc || (!(ACCESSIBLE(loc.typ) || loc.typ === IRONBARS))) {
            if (visibleTarget || adjacentResponder) target = monHere;
            break;
        }
    }
    if (unseen || (statue && heroIsHallucinating()))
        return `That ${unseen ? 'unseen ' : ''}creature is ignoring you!`;
    const visible = tipHatMonsterVisible(target);
    const name = fireScrollMonsterName(target);
    if (!target || !tipHatMonsterResponsive(target)) {
        if (target && visible) return `${name} seems not to notice you.`;
        return 'Nothing happens.';
    }

    if (Number.isInteger(target.mstrategy)) target.mstrategy &= ~STRAT_WAITMASK;
    else if (target.mstrategy === 'waitforu') target.mstrategy = 0;
    if (visible && tipHatMonsterHumanoid(target) && target.mpeaceful && !heroHasConflict()) {
        const helmet = monsterEarthHelmet(target);
        if (!helmet) return `${name} waves.`;
        const simple = tipHatSimpleName(helmet);
        const poss = tipHatMonsterPossessive(target);
        if (helmet.cursed) {
            helmet.bknown = true;
            return `${name} grasps ${poss} ${simple} but can't remove it.`;
        }
        return `${name} tips ${poss} ${simple} in response.`;
    }
    if (visible && tipHatMonsterHumanoid(target))
        return tipHatRudeHumanoidResponse(name);
    if (tipHatMonsterAdjacent(target)) {
        const noise = tipHatMonsterNoise(target, { visible });
        if (!visible && !tipHatMonsterVisible(target) && noise.mapInvisible) {
            const loc = game.level?.at?.(target.mx, target.my);
            if (loc) loc.map_invisible = true;
            newsym(target.mx, target.my);
        }
        if (noise.handled) {
            if (!visible && !tipHatMonsterVisible(target)) {
                const loc = game.level?.at?.(target.mx, target.my);
                if (loc) loc.map_invisible = true;
                newsym(target.mx, target.my);
            }
            return noise.message;
        }
        if (noise.message)
            return [noise.message, visible ? `${name} doesn't respond.` : 'Nothing happens.'].join('  ');
    }
    if (visible) return `${name} doesn't respond.`;
    return 'Nothing happens.';
}

async function finishTipHatDirection(ch) {
    const helmet = (game.inventory || []).find(item => item.letter === game._tip_hat_letter);
    const cancelMove = game._tip_hat_cancel_move || 0;
    if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n' || ch === 'q') {
        game._tip_hat_letter = '';
        game._tip_hat_cancel_move = 0;
        game._command_mode = null;
        game.context.move = cancelMove;
        await setMessage('Never mind.');
        return;
    }
    const dir = tipHatDirectionFromKey(ch);
    if (!dir) {
        await setMessage(tipHatDirectionPrompt());
        return;
    }
    game._tip_hat_letter = '';
    game._tip_hat_cancel_move = 0;
    game._command_mode = null;
    if (!helmet || !isTipWornHelmet(helmet)) {
        await setMessage('Nothing happens.');
        return;
    }
    const messages = [
        `You briefly doff your ${tipHatSimpleName(helmet)}.`,
        tipHatDirectedResponse(dir),
    ].filter(Boolean);
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
}

function isTipTargetContainer(obj) {
    return isTipContainerObject(obj) || isUnknownBagOfTricksObject(obj);
}

function carriedTipTargetContainers(source) {
    if (polyselfNoHands()) return [];
    return (game.inventory || [])
        .filter(obj => obj !== source && isTipTargetContainer(obj)
            && !(obj.lknown && (obj.locked || obj.olocked)));
}

function tipTargetPhrase(targetBox) {
    const name = articlelessObjectName(targetBox);
    return `the ${name}`;
}

function tipContainerCheckMessage(container, { allowEmpty = false } = {}) {
    if (!container) return "You don't have that object.";
    container.lknown = true;
    if (container.locked || container.olocked) return `The ${tipContainerSimpleName(container)} is locked.`;
    if (container.otrapped) {
        container.otrapped = false;
        container.tknown = false;
        return 'You trigger a trap!';
    }
    if (!allowEmpty && !liquidFlowContainerContents(container).length) {
        container.cknown = true;
        return tipContainerEmptyMessage(container);
    }
    return '';
}

function tipChargeCount(item) {
    return Math.max(0, item?.spe ?? 0);
}

function consumeTipCharge(item) {
    item.spe = Math.max(0, (item.spe ?? 0) - 1);
    updateChargedItemLine(item);
}

function identifyChargedToolKind(item, knownKind) {
    if (!item || !knownKind) return;
    item.known = true;
    item.actualKind = knownKind;
    item.kind = knownKind;
    recordKnownToolDiscovery(knownKind);
    updateChargedItemLine(item);
}

function clearTipState() {
    game._tip_container_object = null;
    game._tip_target_entries = null;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
}

function drawTipDestinationMenu(source, targets) {
    const sourceName = (game.inventory || []).includes(source)
        ? inventoryItemName(source)
        : `the ${tipContainerSimpleName(source)}`;
    const rows = [
        [0, 32, `Where to tip the contents of ${sourceName}?`, 1],
        [2, 32, '- - on the floor'],
    ];
    let row = 4;
    for (const target of targets) {
        rows.push([row++, 32, `${target.letter || '?'} - ${normalInventoryLine(target).replace(/^[a-zA-Z] - /, '')}`]);
        if (row >= 23) break;
    }
    rows.push([row, 32, '(end)']);
    setOverlay(rows, Math.max(5, row + 1), false, 31);
}

function beginTipDestinationSelection(source) {
    const targets = carriedTipTargetContainers(source);
    if (!targets.length) return false;
    game._tip_container_object = source;
    game._tip_target_entries = targets.map(target => ({ target, letter: target.letter }));
    drawTipDestinationMenu(source, targets);
    game._command_mode = 'tipDestination';
    return true;
}

function prepareTippedObjectForContainer(obj) {
    obj.contained = false;
    obj.container = null;
    delete obj.ox;
    delete obj.oy;
    delete obj.hidden;
    delete obj.buried;
    delete obj.transientProjectile;
    delete obj.nobj;
    delete obj.nexthere;
}

function refreshTipContainerWeight(container) {
    if (isGlobWeightContainerObject(container)) container.owt = globObjectWeight(container);
}

function isBagOfHoldingObject(obj) {
    return !!obj && (obj.otyp === BAG_OF_HOLDING || objectKindKey(obj) === 'bag of holding');
}

function isMagicBagObject(obj) {
    return isBagOfHoldingObject(obj) || isBagOfTricksObject(obj);
}

function isWandOfCancellationObject(obj) {
    return !!obj && (obj.wandIndex === WAND_NAME_TO_INDEX.get('cancellation')
        || wandTypeName(obj) === 'cancellation'
        || objectKindKey(obj) === 'wand of cancellation'
        || objectKindKey(obj) === 'cancellation');
}

function magicBagExplodesWithObject(obj, depth = 0) {
    if ((isWandOfCancellationObject(obj) || isBagOfTricksObject(obj)) && (obj.spe ?? 0) <= 0)
        return false;
    if ((isMagicBagObject(obj) || isWandOfCancellationObject(obj))
        && rn2(1 << Math.min(depth, 7)) <= depth)
        return true;
    for (const content of liquidFlowContainerContents(obj))
        if (magicBagExplodesWithObject(content, depth + 1)) return true;
    return false;
}

function magicBagItemGoneMessage(item) {
    if (item?.dknown !== false) {
        const subject = upstartText(containerObjectPhrase(item));
        const verb = (item.quan || 1) > 1 ? 'have' : 'has';
        return `${subject} ${verb} vanished!`;
    }
    return `You ${game.u?.blind ? 'notice' : 'see'} ${containerObjectPhrase(item)} disappear!`;
}

function removeObjectFromWorld(obj) {
    if (!obj) return;
    if ((game.inventory || []).includes(obj))
        game.inventory = (game.inventory || []).filter(item => item !== obj);
    if ((game.level?.objects || []).includes(obj)) {
        const x = obj.ox;
        const y = obj.oy;
        removeFloorObject(obj);
        if (x != null && y != null) newsym(x, y);
    }
    stopCarriedFigurineTimerOnLeave(obj);
    game._pet_food_scan_inventory = game.inventory || [];
}

function destroyMagicBagItem(item, messages, { silent = false, usedUpShopBill = false } = {}) {
    if (!item) return;
    if (usedUpShopBill) markObjectTreeShopBillsUsedUp(item);
    if (!silent) messages.push(magicBagItemGoneMessage(item));
    removeObjectFromWorld(item);
}

function magicBagContentsLoss(container, messages, { silent = false, details = null } = {}) {
    if (!isMagicBagObject(container) || !container.cursed) return 0;
    let lost = 0;
    let lostMerchandise = 0;
    for (const obj of [...liquidFlowContainerContents(container)]) {
        if (rn2(13)) continue;
        lostMerchandise += billMagicBagLostItem(container, obj);
        removeContainedObject(container, obj);
        destroyMagicBagItem(obj, messages, { silent });
        lost++;
    }
    refreshTipContainerWeight(container);
    const lostMessage = !silent ? lostShopMerchandiseMessage(lostMerchandise) : '';
    if (lostMessage) messages.push(lostMessage);
    if (details) {
        details.lost = lost;
        details.lostMerchandise = lostMerchandise;
    }
    return lost;
}

function magicBagScatterBreakMessage(obj, breakKind, messages) {
    if (!breakKind) return;
    if (breakKind === 'splat') {
        messages.push('Splat!');
        return;
    }
    if (breakKind === 'splash') {
        messages.push('Splash!');
        return;
    }
    if (breakKind === 'mess') {
        messages.push('What a mess!');
        return;
    }
    if (game.u?.blind) {
        messages.push('You hear something shatter!');
        return;
    }
    const name = pickupObjectName({ ...obj, quan: obj.quan || 1 });
    const many = (obj.quan || 1) > 1;
    const subject = many ? `The ${name}` : (/^[aeiou]/i.test(name) ? `An ${name}` : `A ${name}`);
    const verb = many ? 'shatter' : 'shatters';
    messages.push(`${subject} ${verb}${breakKind === 'pieces' ? ' into a thousand pieces' : ''}!`);
}

function magicBagScatterBreaks(obj, sx, sy, messages) {
    const randomBreak = !rn2(10);
    const material = String(obj?.material || obj?.oc_material || '').toLowerCase();
    const forcedBreak = obj?.otyp === EGG || objectKindKey(obj) === 'egg' || material === 'glass';
    if (!randomBreak && !forcedBreak) return false;
    const breakKind = impactDropBreakKind(obj);
    if (!breakKind && !forcedBreak) return false;
    if (floorObjectVisible(sx, sy)) magicBagScatterBreakMessage(obj, breakKind || 'shatter', messages);
    destroyMagicBagItem(obj, messages, { silent: true, usedUpShopBill: true });
    return true;
}

function splitMagicBagScatterStack(obj) {
    if ((obj?.quan || 1) <= 1) return obj;
    const splitCount = rnd(Math.min(obj.quan - 1, Number.MAX_SAFE_INTEGER));
    obj.quan -= splitCount;
    const splitObj = {
        ...obj,
        id: next_ident(),
        quan: splitCount,
        contents: Array.isArray(obj.contents) ? [...obj.contents] : obj.contents,
        cobj: Array.isArray(obj.cobj) ? [...obj.cobj] : obj.cobj,
    };
    delete splitObj.o_id;
    delete splitObj._shopBillObjectId;
    if (obj.unpaid && !splitCarriedObjectShopBill(obj, splitObj, splitCount))
        clearObjectShopBillState(splitObj);
    return splitObj;
}

function magicBagScatterClosedDoor(x, y) {
    const loc = game.level?.at(x, y);
    return !!(loc?.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED)));
}

function magicBagScatterWeight(obj) {
    return Math.max(0, globObjectWeight(obj) || obj?.owt || obj?.weight || floorEffectsObjectWeight(obj));
}

function magicBagScatterMonsterAt(x, y) {
    return (game.level?.monsters || []).find(mon =>
        mon.mx === x && mon.my === y && !mon.dead && (mon.mhp == null || mon.mhp > 0));
}

function magicBagScatterObjectName(obj) {
    return pickupObjectName({ ...obj, quan: Math.max(1, obj?.quan || 1) });
}

function magicBagScatterMissileName(obj) {
    return pickupObjectName({ ...obj, quan: 1 });
}

function magicBagScatterObjectArticle(obj) {
    const name = magicBagScatterObjectName(obj);
    return (obj?.quan || 1) > 1 ? name : articleFor(name);
}

function magicBagScatterMonsterHitBonus(mon) {
    const data = mon?.data || {};
    let bonus = data.verysmall || data.tiny ? -2 : data.small ? -1 : 0;
    if (data.big || data.bigmonst || data.large || data.strong) bonus += 2;
    if (mon?.msleeping) bonus += 2;
    if (mon?.mcanmove === 0 || data.mmove === 0) bonus += 4;
    return bonus;
}

function magicBagScatterObjectHitBonus(obj) {
    if (obj?.otyp === HEAVY_IRON_BALL) return 2;
    if (obj?.otyp === BOULDER) return 6;
    const cls = obj?.cls || (obj?.otyp === GEM_CLASS ? 'gem' : obj?.otyp === WEAPON_CLASS ? 'weapon' : '');
    return (cls === 'weapon' || cls === 'gem' || obj?.glyph === ')') ? (obj?.spe || 0) : 0;
}

function magicBagScatterHitConsumesObject(obj) {
    const kind = objectKindKey(obj);
    return isPotionObject(obj)
        || obj?.otyp === CREAM_PIE || kind === 'cream pie'
        || obj?.otyp === EGG || kind === 'egg'
        || obj?.cls === 'venom';
}

function magicBagScatterKillVerb(mon) {
    const data = mon?.data || {};
    return data.nonliving || data.mlet === 'Z' || data.mlet === 'zombie'
        || data.glyph === 'Z' || data.name?.includes('zombie') || data.name?.endsWith(' golem')
        ? 'destroyed'
        : 'killed';
}

function magicBagScatterCorpseData(mon) {
    const data = mon?.data || {};
    return data.corpse
        || (data.name?.endsWith(' zombie') ? monsterByRndName(data.name.replace(/ zombie$/, '')) : null)
        || data;
}

function magicBagScatterRemoveKilledMonster(mon, messages) {
    const data = mon?.data || {};
    const corpseData = magicBagScatterCorpseData(mon);
    const dropCorpse = monsterLeavesCorpseLikeDrop(corpseData)
        && monsterCorpseDropSucceeds(mon, data);
    dropMonsterInventory(mon, messages);
    if (dropCorpse) createMonsterCorpseOrGlob(mon, corpseData, mon.mx, mon.my, { messages });
    recordVanquished(mon, !game._monster_moving);
    const loc = game.level?.at(mon.mx, mon.my);
    if (loc?.map_invisible) {
        loc.map_invisible = false;
        loc.remembered_glyph = null;
    }
    game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
    mon.mhp = 0;
    mon.dead = true;
    mon.movement = 0;
    newsym(mon.mx, mon.my);
}

function magicBagScatterHitMonster(mon, obj, x, y, messages) {
    const visible = floorObjectVisible(x, y);
    const monsterName = fireScrollMonsterName(mon).replace(/^The /, 'the ');
    const toHit = 5 + (mon?.data?.mac ?? 10)
        + magicBagScatterMonsterHitBonus(mon)
        + magicBagScatterObjectHitBonus(obj);
    if (toHit < rnd(20)) {
        if (visible)
            messages.push(`${sentenceCase(articleFor(magicBagScatterMissileName(obj)))} misses ${monsterName}.`);
        return { stopped: false, consumed: false };
    }

    const consumedOnHit = magicBagScatterHitConsumesObject(obj);
    const damage = consumedOnHit ? 0 : earthObjectDamage(obj, mon);
    mon.msleeping = 0;
    if (visible) {
        const punct = damage > 4 ? '!' : '.';
        messages.push(`${sentenceCase(articleFor(magicBagScatterMissileName(obj)))} hits ${monsterName}${punct}`);
    }
    if (damage > 0) mon.mhp = (mon.mhp || 1) - damage;
    if ((mon.mhp || 0) <= 0) {
        if (visible)
            messages.push(`${fireScrollMonsterName(mon)} is ${magicBagScatterKillVerb(mon)}!`);
        magicBagScatterRemoveKilledMonster(mon, messages);
    } else if (!game._monster_moving) {
        mon.mpeaceful = false;
    }

    if (consumedOnHit) {
        destroyMagicBagItem(obj, messages, { silent: true, usedUpShopBill: true });
        newsym(x, y);
        return { stopped: true, consumed: true };
    }
    placeObjectOnFloorWithEffects(obj, x, y, messages, 'land', { stack: true });
    return { stopped: true, consumed: false };
}

function magicBagScatterHitHero(obj, messages) {
    if (!game.u) return { hit: false, consumed: false };
    const form = polyselfForm() || {};
    const damage = earthObjectDamage(obj, { data: form });
    const hitValue = 8 + (obj?.spe || 0) + (form.big || form.bigmonst ? 1 : 0);
    const roll = rnd(20);
    const objectName = magicBagScatterObjectArticle(obj);
    if ((game.u.uac ?? 10) + hitValue <= roll) {
        if (game.u.blind || game.flags?.verbose === false) messages.push('It misses.');
        else if ((game.u.uac ?? 10) + hitValue <= roll - 2)
            messages.push(`${sentenceCase(objectName)} misses you.`);
        else
            messages.push(`You are almost hit by ${objectName}.`);
        return { hit: false, consumed: false };
    }

    if (game.u.blind || game.flags?.verbose === false) messages.push(`You are hit${damage > 4 ? '!' : '.'}`);
    else messages.push(`You are hit by ${objectName}${damage > 4 ? '!' : '.'}`);
    if (magicBagScatterHitConsumesObject(obj)) {
        destroyMagicBagItem(obj, messages, { silent: true, usedUpShopBill: true });
        return { hit: true, consumed: true };
    }
    if (damage > 0) {
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        if ((game.u.uhp || 0) <= 0) {
            game._death_cause = `killed by ${magicBagScatterObjectName(obj)}`;
            messages.push('You die...');
        }
    }
    return { hit: true, consumed: false };
}

function magicBagScatterShopContext(sx, sy) {
    const originRoomno = game.level?.at?.(sx, sy)?.roomno || 0;
    const shkp = shopkeeperForCostlySpot(sx, sy);
    return shkp ? {
        shkp,
        originRoomno,
        beforeCredit: Math.max(0, Math.trunc(Number(shkp.credit || 0))),
        beforeDebit: Math.max(0, Math.trunc(Number(shkp.debit || 0))),
        beforeLoan: Math.max(0, Math.trunc(Number(shkp.loan || 0))),
    } : null;
}

function magicBagScatterGoldValue(obj) {
    if (!(obj?.otyp === GOLD_PIECE || obj?.cls === 'coin' || obj?.glyph === '$')) return 0;
    return Math.max(1, obj.quan || 1);
}

function magicBagScatterHeroInOriginShop(shopContext) {
    const roomno = game.level?.at?.(game.u?.ux, game.u?.uy)?.roomno || 0;
    return roomno >= ROOMOFFSET && roomno === shopContext?.originRoomno;
}

function chargeShopGold(shkp, amount) {
    if (!shkp || !(amount > 0)) return;
    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    if (credit >= amount) {
        shkp.credit = credit - amount;
        return;
    }
    const delta = amount - credit;
    shkp.credit = 0;
    shkp.debit = Math.max(0, Math.trunc(Number(shkp.debit || 0))) + delta;
    shkp.loan = Math.max(0, Math.trunc(Number(shkp.loan || 0))) + delta;
}

function maybeBillMagicBagScatterGold(obj, sx, sy, x, y, shopContext) {
    if (!shopContext?.shkp || (x === sx && y === sy)) return;
    if (shopkeeperForCostlySpot(x, y) || !magicBagScatterHeroInOriginShop(shopContext)) return;
    chargeShopGold(shopContext.shkp, magicBagScatterGoldValue(obj));
}

function finishMagicBagScatterShopBilling(shopContext, messages) {
    if (!shopContext?.shkp) return;
    const nowCredit = Math.max(0, Math.trunc(Number(shopContext.shkp.credit || 0)));
    const nowDebit = Math.max(0, Math.trunc(Number(shopContext.shkp.debit || 0)));
    const nowLoan = Math.max(0, Math.trunc(Number(shopContext.shkp.loan || 0)));
    let amount = 0;
    let kind = 'debt has increased';
    if (nowCredit < shopContext.beforeCredit) {
        amount = shopContext.beforeCredit - nowCredit;
        kind = 'credit has been reduced';
    } else if (nowDebit > shopContext.beforeDebit) {
        amount = nowDebit - shopContext.beforeDebit;
    } else if (nowLoan > shopContext.beforeLoan) {
        amount = nowLoan - shopContext.beforeLoan;
    }
    if (amount)
        messages.push(`Your ${kind} by ${amount} zorkmid${amount === 1 ? '' : 's'}.`);
}

function scatterMagicBagObject(container, obj, sx, sy, messages, shopContext = null) {
    let remaining = obj;
    while (remaining) {
        const scatterObj = splitMagicBagScatterStack(remaining);
        if (scatterObj !== remaining) {
            scatterObj.container = null;
            scatterObj.contained = false;
        } else {
            removeContainedObject(container, remaining);
            remaining = null;
        }
        scatterOneMagicBagObject(scatterObj, sx, sy, messages, shopContext);
    }
}

function scatterOneMagicBagObject(scatterObj, sx, sy, messages, shopContext = null) {
    if (magicBagScatterBreaks(scatterObj, sx, sy, messages)) return;

    const dir = rn2(N_DIRS);
    const dx = xdir[dir];
    const dy = ydir[dir];
    const force = Math.max(1, 4 - Math.trunc(magicBagScatterWeight(scatterObj) / 40));
    let range = rnd(force);
    let x = sx;
    let y = sy;
    while (range-- > 0) {
        const nx = x + dx;
        const ny = y + dy;
        const loc = game.level?.at(nx, ny);
        if (!isok(nx, ny) || !loc || !ZAP_POS(loc.typ) || magicBagScatterClosedDoor(nx, ny))
            break;
        x = nx;
        y = ny;
        if (loc.typ === SINK) break;
        const mon = magicBagScatterMonsterAt(x, y);
        if (mon) {
            range--;
            const hit = magicBagScatterHitMonster(mon, scatterObj, x, y, messages);
            if (hit.stopped) return;
            continue;
        }
        if (game.u?.ux === x && game.u?.uy === y) {
            const hit = magicBagScatterHitHero(scatterObj, messages);
            if (hit.consumed) return;
            if (hit.hit) range -= 3;
        }
    }
    if (placeObjectOnFloorWithEffects(scatterObj, x, y, messages, 'land', {
        stack: true,
        usedUpShopBillOnDestroy: true,
    }))
        maybeBillMagicBagScatterGold(scatterObj, sx, sy, x, y, shopContext);
}

function scatterMagicBagContents(container, messages, { lostContext = null } = {}) {
    const x = game.u?.ux ?? container?.ox ?? 0;
    const y = game.u?.uy ?? container?.oy ?? 0;
    const shopContext = magicBagScatterShopContext(x, y);
    for (const obj of [...liquidFlowContainerContents(container)]) {
        if (!rn2(13)) {
            billMagicBagLostItemWithContext(container, obj, lostContext);
            removeContainedObject(container, obj);
            destroyMagicBagItem(obj, messages, { silent: true });
            continue;
        }
        obj.ox = x;
        obj.oy = y;
        scatterMagicBagObject(container, obj, x, y, messages, shopContext);
    }
    finishMagicBagScatterShopBilling(shopContext, messages);
    clearLiquidFlowContainerContents(container);
}

function magicBagExplosionMessage(obj, tumble = false) {
    if (tumble) {
        const subject = containerObjectPhrase(obj);
        const verb = (obj.quan || 1) > 1 ? 'tumble' : 'tumbles';
        return `As ${subject} ${verb} inside, you are blasted by a magical explosion!`;
    }
    return `As you put ${inventoryItemName(obj)} inside, you are blasted by a magical explosion!`;
}

function damageHeroWithMagicBagExplosion(messages) {
    if (!game.u) return;
    game.u.uhp = Math.max(0, (game.u.uhp || 0) - d(6, 6));
    if ((game.u.uhp || 0) <= 0) {
        game._death_cause = 'killed by a magical explosion';
        messages.push('You die...');
    }
}

function explodeMagicBagTransfer(targetBag, triggerObj, messages, { tumble = false, triggerHeld = null } = {}) {
    messages.push(magicBagExplosionMessage(triggerObj, tumble));
    if (isBagOfHoldingObject(triggerObj)) {
        const lostContext = triggerHeld == null ? null : { held: !!triggerHeld };
        scatterMagicBagContents(triggerObj, messages, { lostContext });
    }
    destroyMagicBagItem(triggerObj, messages, { silent: true, usedUpShopBill: true });
    scatterMagicBagContents(targetBag, messages);
    destroyMagicBagItem(targetBag, messages, { silent: true, usedUpShopBill: true });
    damageHeroWithMagicBagExplosion(messages);
    return { bagGone: true, moved: true, message: '', messages };
}

function visibleCreatedMonster(mon) {
    if (!mon || game.u?.blind) return false;
    return cansee(mon.mx, mon.my) || !!(game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT);
}

function chargedToolUsageBasePrice(obj) {
    return shopItemPrice(obj, game.u?.ux, game.u?.uy) || shopBaseCost(obj) || 0;
}

function chargedUsageChargeCount(obj, override = null) {
    if (override != null) return Math.max(0, Math.trunc(Number(override || 0)));
    if (isWandItem(obj)) return Math.max(0, wandCharges(obj));
    const kind = toolChargeKind(obj);
    if ((kind === 'oil lamp' || kind === 'brass lantern' || kind === 'magic lamp')
        && obj?.spe == null && obj?.charges == null)
        return 1;
    return Math.max(0, Math.trunc(Number(obj?.spe ?? obj?.charges ?? 0)));
}

function chargedUsageIsChargeBased(obj) {
    if (isWandItem(obj)) return true;
    const kind = toolChargeKind(obj);
    return CHARGED_TOOL_KINDS.has(kind)
        || kind === 'oil lamp' || kind === 'brass lantern' || kind === 'magic lamp';
}

function chargedToolUsageFee(obj, altusage = false, chargeCount = chargedUsageChargeCount(obj)) {
    let fee = chargedToolUsageBasePrice(obj);
    if (!(fee > 0)) return 0;
    const kind = toolChargeKind(obj);
    if (kind === 'magic lamp') {
        if (!altusage) return shopBaseCost({ cls: 'tool', kind: 'oil lamp', actualKind: 'oil lamp' }) || 0;
        fee += Math.trunc(fee / 3);
    } else if (kind === 'magic marker') {
        fee = Math.trunc(fee / 2);
    } else if (!altusage && (isBagOfTricksObject(obj) || isHornOfPlentyObject(obj))) {
        fee = Math.trunc(fee / 5);
    } else if (isWandItem(obj) || kind === 'crystal ball' || kind === 'oil lamp'
        || kind === 'brass lantern' || kind === 'magic flute' || kind === 'magic harp'
        || kind === 'frost horn' || kind === 'fire horn' || kind === 'drum of earthquake') {
        if (chargeCount > 1) fee = Math.trunc(fee / 4);
    } else if (obj?.cls === 'spellbook' || obj?.glyph === '+') {
        fee -= Math.trunc(fee / 5);
    } else if (kind === 'can of grease' || kind === 'tinning kit' || kind === 'expensive camera') {
        fee = Math.trunc(fee / 10);
    } else if (obj?.cls === 'potion' && /(?:^| )oil$|potion of oil/.test(objectKindKey(obj))) {
        fee = Math.trunc(fee / 5);
    }
    return Math.max(0, fee);
}

function chargedToolUsageFeeMessage(obj, fee, altusage = false, shkp = null) {
    let arg1 = '';
    let arg2 = '';
    if (obj?.cls === 'spellbook' || obj?.glyph === '+') {
        const additional = (shkp?.debit || 0) > 0 ? ' an additional' : '';
        return `"This is no free library!  You owe${additional} ${fee} zorkmid${fee === 1 ? '' : 's'}."`;
    }
    if (obj?.cls === 'potion' && /(?:^| )oil$|potion of oil/.test(objectKindKey(obj)))
        return `"That will cost you ${fee} zorkmid${fee === 1 ? '' : 's'} (Yendorian Fuel Tax)."`;
    if (altusage && (isBagOfTricksObject(obj) || isHornOfPlentyObject(obj))) {
        if (!rn2(3)) arg1 = 'Whoa!  ';
        if (!rn2(3)) arg1 = 'Watch it!  ';
        return `"${arg1}Emptying that will cost you ${fee} zorkmid${fee === 1 ? '' : 's'}."`;
    }
    if (!rn2(3)) arg1 = 'Hey!  ';
    if (!rn2(3)) arg2 = 'Ahem.  ';
    return `"${arg1}${arg2}Usage fee, ${fee} zorkmid${fee === 1 ? '' : 's'}."`;
}

function checkUnpaidUsage(obj, messages, { altusage = false, chargeCount = null } = {}) {
    const shkp = heroShopkeeper();
    if (!shkp) return 0;
    const billEntry = shopBillEntryForObject(shkp, obj);
    if (!billEntry || billEntry.useup) return 0;
    const currentCharges = chargedUsageChargeCount(obj, chargeCount);
    if (chargedUsageIsChargeBased(obj) && currentCharges <= 0) return 0;
    const fee = chargedToolUsageFee(obj, altusage, currentCharges);
    if (!(fee > 0)) return 0;
    const message = chargedToolUsageFeeMessage(obj, fee, altusage, shkp);
    shkp.debit = Math.max(0, Math.trunc(Number(shkp.debit || 0))) + fee;
    if (shopkeeperCanSpeakToHero(shkp)) messages?.push(message);
    return fee;
}

function billChargedToolUsage(obj, messages, { altusage = false, chargeCount = tipChargeCount(obj) } = {}) {
    return checkUnpaidUsage(obj, messages, { altusage, chargeCount });
}

function floorSpecialSourceUsageBill(source) {
    if (!source || (game.inventory || []).includes(source) || source.no_charge) return null;
    const x = source.ox ?? game.u?.ux;
    const y = source.oy ?? game.u?.uy;
    if (x == null || y == null || x !== game.u?.ux || y !== game.u?.uy) return null;
    const shkp = shopkeeperForCostlySpot(x, y);
    if (!shopkeeperInHisShop(shkp)) return null;
    const price = shopItemPrice(source, x, y);
    if (!(price > 0)) return null;
    return {
        shkp,
        unpaid: source.unpaid,
        unpaidPrice: source.unpaidPrice,
        line: source.line,
        price,
    };
}

function beginFloorSpecialSourceUsageBill(source) {
    const saved = floorSpecialSourceUsageBill(source);
    if (!saved) return null;
    const existingEntry = shopBillEntryForObject(saved.shkp, source);
    const savedEntry = existingEntry ? { ...existingEntry } : null;
    const billEntry = existingEntry || addObjectToShopBill(saved.shkp, source, saved.price);
    if (!billEntry) return null;
    return () => {
        if (savedEntry) Object.assign(existingEntry, savedEntry);
        else removeObjectFromShopBillById(saved.shkp, shopBillObjectId(source));
        saved.shkp.billct = Array.isArray(saved.shkp.bill) ? saved.shkp.bill.length : Math.max(0, saved.shkp.billct || 0);
        source.unpaid = saved.unpaid;
        source.unpaidPrice = saved.unpaidPrice;
        source.line = saved.line;
    };
}

async function applyBagOfTricksOnce(bag, { tipping = false } = {}) {
    if (tipChargeCount(bag) < 1) {
        if (tipping && bag.cknown) return ["It's empty."];
        if (isKnownBagOfTricksObject(bag)) bag.cknown = true;
        return ['Nothing happens.'];
    }

    const messages = [];
    if (!tipping) billChargedToolUsage(bag, messages);
    consumeTipCharge(bag);
    let creatcnt = 1;
    if (!rn2(23)) creatcnt += rnd(7);
    let moncount = 0;
    let seecount = 0;
    for (let i = 0; i < creatcnt; i++) {
        const mon = await makemon(null, game.u?.ux || 0, game.u?.uy || 0, 0);
        if (!mon) continue;
        moncount++;
        if (visibleCreatedMonster(mon)) seecount++;
    }
    if (seecount && bag.dknown) identifyChargedToolKind(bag, 'bag of tricks');
    if (!tipping && !seecount) messages.push(moncount ? 'Nothing seems to happen.' : 'Nothing happens.');
    return messages;
}

async function lootFloorBagOfTricks(bag) {
    bag.lknown = true;
    const bagName = articlelessObjectName(bag);
    const damage = rnd(10);
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
        if ((game.u.uhp || 0) <= 0) game._death_cause = 'killed by a carnivorous bag';
    }
    identifyChargedToolKind(bag, 'bag of tricks');
    const messages = [
        `You carefully open the ${bagName}...`,
        'It develops a huge set of teeth and bites you!',
    ];
    if ((game.u?.uhp || 0) <= 0) messages.push('You die...');
    await setMessage(messages.join('  '), messages.length > 1);
    game.context.move = 1;
}

async function tipBagOfTricks(bag) {
    if (tipChargeCount(bag) < 1) return applyBagOfTricksOnce(bag, { tipping: true });
    const oldSpe = tipChargeCount(bag);
    let seen = false;
    while (tipChargeCount(bag) > 0) {
        const beforeMonsters = (game.level?.monsters || []).length;
        const messages = await applyBagOfTricksOnce(bag, { tipping: true });
        if (messages.length) return messages;
        const created = (game.level?.monsters || []).slice(beforeMonsters);
        if (created.some(visibleCreatedMonster)) seen = true;
    }
    if (tipChargeCount(bag) < oldSpe) {
        bag.spe = 0;
        bag.cknown = true;
        updateChargedItemLine(bag);
        const messages = seen ? [] : ['Nothing seems to happen.'];
        billChargedToolUsage(bag, messages, { altusage: true, chargeCount: oldSpe });
        return messages;
    }
    return [];
}

function hornCreatedObjectDescription(obj, potion) {
    if (potion) return (obj.quan || 1) > 1 ? 'Some potions' : 'A potion';
    return 'Some food';
}

function hornSpillMessage(obj, potion) {
    const what = hornCreatedObjectDescription(obj, potion);
    const spills = !potion || what === 'A potion';
    return `${what} ${spills ? 'spills' : 'spill'} out.`;
}

function createHornOfPlentyObject(horn) {
    const potion = !rn2(13);
    const obj = mkobj(potion ? POTION_CLASS : FOOD_CLASS, false);
    if (!potion && objectKindKey(obj) === 'food ration' && !rn2(7)) {
        Object.assign(obj, {
            otyp: LUMP_OF_ROYAL_JELLY,
            kind: 'lump of royal jelly',
            singular: 'lump of royal jelly',
            plural: 'lumps of royal jelly',
        });
    }
    obj.blessed = !!horn.blessed;
    obj.cursed = !!horn.cursed;
    Object.assign(obj, object_display(obj));
    return { obj, potion };
}

function hornCreatedObjectBillMessage(obj, price) {
    const name = pickupObjectName(obj);
    const subject = (obj.quan || 1) > 1 ? upstartText(name) : `The ${name}`;
    const each = (obj.quan || 1) > 1 ? ' each' : '';
    return `${subject} will cost you ${price} zorkmid${price === 1 ? '' : 's'}${each}.`;
}

function billHornCreatedObject(horn, obj, messages, { silent = false } = {}) {
    if (!horn?.unpaid || !obj) return 0;
    const shkp = heroShopkeeper();
    if (!shkp) return 0;
    const price = shopItemPrice(obj, game.u?.ux, game.u?.uy);
    if (!(price > 0)) return 0;
    if (!addObjectToShopBill(shkp, obj, price)) return 0;
    if (!silent && !heroIsDeaf()) messages?.push(hornCreatedObjectBillMessage(obj, price));
    return price;
}

function mergeStackedUnpaidBill(stacked, obj) {
    if (!stacked || stacked === obj || !obj?.unpaid) return;
    const price = unpaidBillPrice(stacked) + unpaidBillPrice(obj);
    if (!(price > 0)) return;
    stacked.unpaid = true;
    stacked.unpaidPrice = price;
    syncUnpaidBillLine(stacked);
}

function placeHornObjectOnFloor(obj, messages) {
    const x = game.u?.ux ?? 0;
    const y = game.u?.uy ?? 0;
    placeTippedObjectOnFloor(obj, x, y, messages);
    messages.push(`${upstartText(containerObjectPhrase(obj))} drops to the floor.`);
}

function hornApplyFloorMessage(obj) {
    const quan = obj.quan || 1;
    const name = pickupObjectName({ ...obj, quan: 1 });
    const subject = quan > 1 ? upstartText(containerObjectPhrase(obj)) : `The ${name}`;
    return `Oops! ${subject} slip${quan > 1 ? '' : 's'} to the floor!`;
}

function placeAppliedHornObjectOnFloor(obj, messages) {
    const x = game.u?.ux ?? 0;
    const y = game.u?.uy ?? 0;
    placeTippedObjectOnFloor(obj, x, y, messages);
    messages.push(hornApplyFloorMessage(obj));
}

function addAppliedHornObjectToInventory(obj, messages) {
    game.inventory ??= [];
    const used = new Set(game.inventory.map(item => item.letter).filter(Boolean));
    const letter = nextInventoryLetter();
    if (!letter || used.has(letter)) {
        placeAppliedHornObjectOnFloor(obj, messages);
        return;
    }
    prepareTippedObjectForContainer(obj);
    obj.letter = letter;
    const displayLine = normalInventoryLine({ ...obj, line: '' });
    obj.line = displayLine;
    if (obj.unpaid) syncUnpaidBillLine(obj);
    game.inventory.push(obj);
    maybeAttachCarriedFigurineTimeout(obj);
    game._pet_food_scan_inventory = game.inventory;
    messages.push(`${displayLine}.`);
}

function applyHornOfPlentyOnce(horn) {
    if (tipChargeCount(horn) < 1) {
        horn.cknown = true;
        updateChargedItemLine(horn);
        return ['Nothing happens.'];
    }

    const messages = [];
    billChargedToolUsage(horn, messages);
    consumeTipCharge(horn);
    const { obj, potion } = createHornOfPlentyObject(horn);
    messages.push(hornSpillMessage(obj, potion));
    billHornCreatedObject(horn, obj, messages);
    if (horn.dknown) identifyChargedToolKind(horn, 'horn of plenty');
    updateChargedItemLine(horn);
    addAppliedHornObjectToInventory(obj, messages);
    return messages;
}

async function tipHornOfPlenty(horn, targetBox = null) {
    if (tipChargeCount(horn) < 1) {
        horn.cknown = true;
        updateChargedItemLine(horn);
        return ['Nothing happens.'];
    }

    const messages = [];
    const oldSpe = tipChargeCount(horn);
    while (tipChargeCount(horn) > 0) {
        consumeTipCharge(horn);
        const { obj, potion } = createHornOfPlentyObject(horn);
        messages.push(hornSpillMessage(obj, potion));
        billHornCreatedObject(horn, obj, messages, { silent: true });
        if (targetBox) {
            prepareTippedObjectForContainer(obj);
            const stacked = add_to_container(targetBox, obj);
            mergeStackedUnpaidBill(stacked, obj);
            refreshTipContainerWeight(targetBox);
        } else {
            placeHornObjectOnFloor(obj, messages);
        }
    }
    if (tipChargeCount(horn) < oldSpe) {
        horn.spe = 0;
        horn.cknown = true;
        if (horn.dknown) identifyChargedToolKind(horn, 'horn of plenty');
        updateChargedItemLine(horn);
        billChargedToolUsage(horn, messages, { altusage: true, chargeCount: oldSpe });
    }
    return messages;
}

async function tipSpecialSourceContents(source, targetBox = null) {
    if (targetBox) {
        const targetError = tipContainerCheckMessage(targetBox, { allowEmpty: true });
        if (targetError) return [targetError];
    }
    const restoreFloorBill = beginFloorSpecialSourceUsageBill(source);
    try {
        if (isBagOfTricksObject(source)) return await tipBagOfTricks(source);
        if (isHornOfPlentyObject(source)) return await tipHornOfPlenty(source, targetBox);
        return null;
    } finally {
        if (restoreFloorBill) restoreFloorBill();
    }
}

function tipContainerIntoContainer(source, targetBox) {
    const contents = [...liquidFlowContainerContents(source)];
    source.cknown = true;
    if (!contents.length) return [tipContainerEmptyMessage(source)];
    targetBox.contents ??= [];
    const cursedMagicBag = isMagicBagObject(source) && source.cursed;
    const sourceHeld = objectCarriedByHero(source);
    let lostMerchandise = 0;
    const messages = [contents.length === 1
        ? `An object tumbles into ${tipTargetPhrase(targetBox)}.`
        : `Objects tumble into ${tipTargetPhrase(targetBox)}.`];
    let targetGone = false;
    for (const obj of contents) {
        removeContainedObject(source, obj);
        thawObjectTippedFromSource(source, obj);
        if (cursedMagicBag && !rn2(13)) {
            lostMerchandise += billMagicBagLostItem(source, obj);
            destroyMagicBagItem(obj, messages);
            continue;
        }
        addTippedContainerObjectToShopBill(source, obj);
        prepareTippedObjectForContainer(obj);
        if (isMagicBagObject(targetBox) && magicBagExplodesWithObject(obj)) {
            explodeMagicBagTransfer(targetBox, obj, messages, { tumble: true, triggerHeld: sourceHeld });
            targetGone = true;
            break;
        }
        add_to_container(targetBox, obj);
    }
    refreshTipContainerWeight(source);
    if (!targetGone) refreshTipContainerWeight(targetBox);
    const lostMessage = lostShopMerchandiseMessage(lostMerchandise);
    if (lostMessage) messages.push(lostMessage);
    return messages;
}

async function tipContainerContents(source, targetBox = null) {
    const special = isBagOfTricksObject(source) || isHornOfPlentyObject(source);
    if (targetBox && isBagOfTricksObject(targetBox))
        return applyBagOfTricksOnce(targetBox, { tipping: false });
    const sourceError = tipContainerCheckMessage(source, { allowEmpty: special });
    if (sourceError) return [sourceError];
    if (special) return tipSpecialSourceContents(source, targetBox);
    if (targetBox) {
        const targetError = tipContainerCheckMessage(targetBox, { allowEmpty: true });
        if (targetError) return [targetError];
    }
    return targetBox ? tipContainerIntoContainer(source, targetBox) : tipContainerToFloor(source);
}

async function finishCarriedTipSelection(tipTarget) {
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    if (isTipWornHelmet(tipTarget)) {
        await beginTipHat(tipTarget);
        return;
    }
    if (isTipSourceObject(tipTarget)) {
        game._tip_container_object = tipTarget;
        if (beginTipDestinationSelection(tipTarget)) return;
        game._tip_container_object = null;
        const messages = await tipContainerContents(tipTarget);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        game._command_mode = null;
        return;
    }
    game._tip_container_object = null;
    const messages = isTipSpillageObject(tipTarget)
        ? tipSpillageObject(tipTarget)
        : tipOrdinaryObjectMessages(tipTarget);
    await setMessage(messages.join('  '), messages.length > 1);
    if (isTipSpillageObject(tipTarget)) game.context.move = 1;
    game._command_mode = null;
}

function tipContainerObjectPhrase(obj) {
    const phrase = containerObjectPhrase(obj);
    if (!(obj?.broken || obj?.obroken)) return phrase;
    if (!/^(?:an? |the )?(?:large box|chest)$/i.test(phrase)) return phrase;
    return phrase.replace(/^(an? |the )/i, '$1broken ');
}

function containerObjectPhrase(obj) {
    const count = obj.quan || 1;
    const name = pickupObjectName(obj);
    if (count > 1) return `${count} ${name}`;
    if (obj.unique) return `the ${name}`;
    if (obj.noArticle) return name;
    if (pairArticleObjectName(name))
        return `a pair of ${name}`;
    const article = /^[aeiou]/i.test(name) || name === 'orcish helm' ? 'an' : 'a';
    return `${article} ${name}`;
}

function objectBucCategory(item) {
    if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$')
        return game.flags?.goldX ? 'unknown' : 'uncursed';
    if (item.bknown !== true && game._startup_role !== 'Priest') return 'unknown';
    if (item.blessed) return 'blessed';
    if (item.cursed) return 'cursed';
    return 'uncursed';
}

function pickupObjectPhrase(obj) {
    recordObservedObjectDiscovery(obj);
    const count = obj.quan || 1;
    const name = pickupObjectName(obj);
    if (obj === game.u?.uchain) return 'an iron chain (attached to you)';
    if (obj === game.u?.uball) {
        const weight = (obj.owt || 0) > 480 ? 'very heavy' : 'heavy';
        return `a ${weight} iron ball (chained to you)`;
    }
    if (obj.otyp === SCROLL_CLASS || obj.cls === 'scroll') {
        const rawScrollName = String(obj.actualKind || obj.kind || '')
            .replace(/^scroll(?::| of | labeled )/, '');
        const scrollIndex = obj.scrollIndex ?? IDENTIFIED_SCROLL_NAMES.indexOf(rawScrollName);
        const scrollName = IDENTIFIED_SCROLL_NAMES[scrollIndex];
        if (scrollName) {
            const label = String(obj.kind || '').startsWith('scroll labeled ')
                ? String(obj.kind).slice(15)
                : game._object_descriptions?.scrolls?.[scrollIndex];
            const discoveryName = `scroll of ${scrollName}`;
            game._discoveries ??= [];
            if (!game._discoveries.some(entry => entry.section === 'Scrolls' && entry.name === discoveryName)) {
                game._discoveries.push({
                    section: 'Scrolls',
                    name: discoveryName,
                    text: label ? `${discoveryName} (${label})` : discoveryName,
                    starred: false,
                    known: false,
                });
            }
        }
    }
    const lineShowsSpe = /(?:^| )[-+]\d+ /.test(String(obj.line || ''));
    const showSpe = obj.known === true || lineShowsSpe;
    if (count === 1 && (obj.cls === 'weapon' || obj.glyph === ')') && obj.spe != null && showSpe) {
        const spe = `${obj.spe >= 0 ? '+' : ''}${obj.spe} `;
        const article = /^[aeiou]/i.test(`${spe}${name}`) ? 'an' : 'a';
        return `${article} ${spe}${name}`;
    }
    if (count > 1) return `${count} ${name}`;
    if (obj.unique) return `the ${name}`;
    if (obj.noArticle) return name;
    if (pairArticleObjectName(name))
        return `a pair of ${name}`;
    const article = /^[aeiou]/i.test(name) || name === 'orcish helm' ? 'an' : 'a';
    return `${article} ${name}`;
}

function shopBaseCost(obj) {
    const cls = obj.cls || '';
    let kind = String(obj.actualKind || obj.kind || '').toLowerCase();
    kind = kind.replace(/^(?:blessed|uncursed|cursed) /, '');
    if (isGlobbyObject(obj)) return 6;
    if (isBellOfOpeningItem(obj)) return SHOP_OBJECT_COSTS['bell of opening'];

    if (obj.otyp === SCROLL_CLASS || cls === 'scroll' || obj.otyp === SCR_SCARE_MONSTER) {
        const scrollIndex = obj.scrollIndex ?? (obj.otyp === SCR_SCARE_MONSTER ? 3 : undefined);
        if (scrollIndex != null) return SCROLL_SHOP_COSTS[scrollIndex] || 0;
        kind = kind.replace(/^scroll(?::| of | labeled )/, '');
        return SHOP_OBJECT_COSTS[kind] || (kind === 'blank paper' ? 60 : 0);
    }
    if (obj.otyp === POTION_CLASS || cls === 'potion') {
        if (obj.potionIndex != null) return POTION_SHOP_COSTS[obj.potionIndex] || 0;
        kind = kind.replace(/^potion(?::| of )/, '');
        if (kind === 'holy water' || kind === 'unholy water') return 100;
        if (kind === 'water') return obj.blessed || obj.cursed ? 100 : 0;
        return SHOP_OBJECT_COSTS[kind] || 0;
    }
    if (obj.otyp === WAND_CLASS || cls === 'wand') {
        if (obj.spe === -1) return 0;
        if (obj.wandIndex != null) return WAND_SHOP_COSTS[obj.wandIndex] || 0;
        kind = kind.replace(/^wand of /, '');
        return SHOP_OBJECT_COSTS[kind] || 0;
    }
    if (obj.otyp === RING_CLASS || cls === 'ring' || obj.glyph === '=') {
        if (obj.ringRoll) return RING_SHOP_COSTS[obj.ringRoll - 1] || 0;
        kind = kind.replace(/^ring of /, '');
        const ringIndex = IDENTIFIED_RING_NAMES.indexOf(kind);
        return ringIndex >= 0 ? RING_SHOP_COSTS[ringIndex] : 0;
    }
    if (obj.otyp === AMULET_CLASS || cls === 'amulet') {
        if (obj.amuletIndex != null) return 150;
        if (kind === 'amulet of yendor') return 30000;
        if (kind === 'cheap plastic imitation of the amulet of yendor') return 0;
        return kind.startsWith('amulet ') ? 150 : 0;
    }
    if (cls === 'spellbook' || obj.otyp === SPBOOK_NO_NOVEL || obj.otyp === SPE_HEALING) {
        const name = String(obj.spellName || obj.spell?.name || kind)
            .replace(/^spellbook(?: of)? /, '');
        if (name === 'novel') return 20;
        if (name === 'blank paper') return 0;
        return (SPELLBOOK_LEVELS[name] || 0) * 100;
    }

    if (!kind && obj.otyp === CHEST) kind = 'chest';
    else if (!kind && obj.otyp === LARGE_BOX) kind = 'large box';
    else if (!kind && obj.otyp === ICE_BOX) kind = 'ice box';
    else if (!kind && obj.otyp === OIL_LAMP) kind = 'oil lamp';
    else if (!kind && obj.otyp === MAGIC_LAMP) kind = 'magic lamp';
    else if (!kind && obj.otyp === DART) kind = 'dart';
    else if (!kind && obj.otyp === MIRROR) kind = 'mirror';
    else if (!kind && obj.otyp === CREAM_PIE) kind = 'cream pie';
    else if (!kind && obj.otyp === EXPENSIVE_CAMERA) kind = 'expensive camera';
    else if (!kind && obj.otyp === TINNING_KIT) kind = 'tinning kit';
    else if (!kind && obj.otyp === CAN_OF_GREASE) kind = 'can of grease';
    else if (!kind && obj.otyp === MAGIC_FLUTE) kind = 'magic flute';
    else if (!kind && obj.otyp === FROST_HORN) kind = 'frost horn';
    else if (!kind && obj.otyp === FIRE_HORN) kind = 'fire horn';
    else if (!kind && obj.otyp === HORN_OF_PLENTY) kind = 'horn of plenty';
    else if (!kind && obj.otyp === MAGIC_HARP) kind = 'magic harp';
    else if (!kind && obj.otyp === DRUM_OF_EARTHQUAKE) kind = 'drum of earthquake';
    else if (!kind && obj.otyp === STETHOSCOPE) kind = 'stethoscope';
    else if (!kind && obj.otyp === MAGIC_MARKER) kind = 'magic marker';
    if ((obj.foodRoll || 1000) <= 140 || kind === 'tripe') kind = 'tripe ration';
    const metadata = wishObjectMetadataForItem(obj);
    if (metadata?.unitCost != null) return metadata.unitCost;
    return SHOP_OBJECT_COSTS[kind] || 0;
}

function shopPricingUnits(obj) {
    if (isGlobbyObject(obj))
        return Math.max(1, Math.trunc(((obj.owt ?? 20) + 19) / 20));
    return obj?.quan || 1;
}

function shopObjectNameKnown(obj) {
    if (obj.known === true) return true;
    if (obj.known === false) return false;
    let kind = String(obj.actualKind || obj.kind || '').toLowerCase();
    kind = kind.replace(/^(?:blessed|uncursed|cursed) /, '');
    if (!kind) return false;
    if (obj.cls === 'armor' || obj.glyph === '[')
        return !ARMOR_WISH_APPEARANCES[kind]
            || (game._discoveries || []).some(entry => entry.section === 'Armor' && entry.name === kind && entry.starred);
    return true;
}

function levelRoomByRoomno(roomno) {
    if (roomno < ROOMOFFSET) return null;
    const idx = roomno - ROOMOFFSET;
    return game.level?.rooms?.[idx]
        || (game.level?.subrooms || []).find(room => room?.roomnoidx === idx)
        || null;
}

function queueMessageAfterMore(text, more = false, { blockTimeUntilClear = false } = {}) {
    if (!text) return;
    game._queued_message_after_more = game._queued_message_after_more
        ? `${game._queued_message_after_more}  ${text}`
        : text;
    if (more) game._queued_message_more_after_more = 1;
    if (blockTimeUntilClear) game._queued_message_blocks_time_after_more = 1;
}

function specialRoomEntryText(roomno) {
    const room = levelRoomByRoomno(roomno);
    if (!room) return '';
    if (room.rtype === MORGUE) return 'You have an uncanny feeling...';
    return '';
}

function tendedTemplePriest(roomno) {
    return (game.level?.monsters || []).find(mon =>
        mon.ispriest && mon.shrine?.room === roomno
        && game.level?.at(mon.mx, mon.my)?.roomno === roomno);
}

function tendedTemplePriestIntone(roomno) {
    const priest = tendedTemplePriest(roomno);
    if (!priest || game.u?.deaf) return '';
    const moves = game.moves || 0;
    if (moves < (priest._intone_time || 0)) return '';
    priest._intone_time = moves + d(10, 500);
    priest._enter_time = 0;
    const visible = !game.u?.blind && !priest.minvis && !priest.mundetected && couldsee(priest.mx, priest.my);
    return `${visible ? `The ${priest.data?.name || 'priest'}` : 'A nearby voice'} intones:`;
}

function tendedTemplePriestHasShrine(priest) {
    const shrine = priest?.shrine;
    const loc = shrine ? game.level?.at(shrine.x, shrine.y) : null;
    if (!priest?.ispriest || !shrine || loc?.typ !== ALTAR) return false;
    const altarMask = (loc.flags ?? loc.altarmask ?? 0) & ~AM_SHRINE;
    return priest.shrine.align === Amask2align(altarMask);
}

function tendedTemplePriestEntryText(roomno) {
    const priest = tendedTemplePriest(roomno);
    if (!priest || game.u?.deaf) return '';
    const moves = game.moves || 0;
    const shrined = tendedTemplePriestHasShrine(priest);
    const messages = [];
    if (moves >= (priest._enter_time || 0)) {
        messages.push(`"Pilgrim, you enter a ${shrined ? 'sacred' : 'desecrated'} place!"`);
        priest._enter_time = moves + d(10, 100);
    }

    const coaligned = (game.u?.ualign?.type ?? 0) === priest.shrine?.align;
    const record = game.u?.ualign?.record ?? 0;
    const forbidding = !shrined || !coaligned || record <= -4;
    const timeKey = forbidding ? '_hostile_time' : '_peaceful_time';
    const otherKey = forbidding ? '_peaceful_time' : '_hostile_time';
    if (moves >= (priest[timeKey] || 0) || (priest[otherKey] || 0) >= (priest[timeKey] || 0)) {
        if (forbidding) {
            const strange = shrined && coaligned ? ' strange' : '';
            messages.push(`You have a${strange} forbidding feeling...`);
        } else {
            messages.push(`You experience ${record >= 14 ? 'a' : 'an unusual'} sense of peace.`);
        }
        priest[timeKey] = moves + d(10, 20);
        if (priest[timeKey] <= (priest[otherKey] || 0))
            priest[otherKey] = priest[timeKey] - 1;
    }
    return messages.join('  ');
}

function prepareUntendedTempleEntry(newRoomno, oldRoomno = 0) {
    if (!newRoomno || newRoomno === oldRoomno) return null;
    const room = levelRoomByRoomno(newRoomno);
    if (room?.rtype !== TEMPLE || tendedTemplePriest(newRoomno)) return null;
    const roll = rn2(4);
    const text = roll === 0 ? 'You have an eerie feeling...'
        : roll === 1 ? 'You feel like you are being watched.'
            : roll === 2 ? 'A shiver runs down your spine.'
                : '';
    return { text };
}

async function finishUntendedTempleEntry() {
    if (rn2(5)) return '';
    const ghost = await makemon(monsterByRndName('ghost'), game.u?.ux || 0, game.u?.uy || 0, MM_NOMSG);
    if (!ghost) return '';
    ghost.mpeaceful = 0;
    set_malign(ghost);
    newsym(ghost.mx, ghost.my);
    if (game.u?.blind || !(game.viz_array?.[ghost.my]?.[ghost.mx] & IN_SIGHT))
        return 'You sense a presence close by!';
    return 'A ghost appears next to you!';
}

async function queueUntendedTempleEntryAfterTeleport(oldX, oldY, newX, newY) {
    const oldRoomno = game.level?.at(oldX, oldY)?.roomno || 0;
    const newRoomno = game.level?.at(newX, newY)?.roomno || 0;
    const templeEntry = prepareUntendedTempleEntry(newRoomno, oldRoomno);
    if (!templeEntry) return;
    if (templeEntry.text) {
        queueMessageAfterMore(templeEntry.text);
        game._queued_temple_ghost_roll_after_more = 1;
        return;
    }
    const ghostText = await finishUntendedTempleEntry();
    if (ghostText) queueMessageAfterMore(ghostText);
}

function shopItemPrice(obj, x = game.u?.ux, y = game.u?.uy) {
    if (!obj || obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$') return null;
    if ((obj.cls === 'food' || obj.otyp === FOOD_CLASS || obj.otyp === CORPSE || obj.otyp === 'corpse') && obj.oeaten)
        return 0;
    const loc = game.level?.at(x, y);
    const roomno = loc?.roomno || 0;
    const room = levelRoomByRoomno(roomno);
    if (!room || room.rtype < SHOPBASE) return null;
    const shkp = room.resident || (game.level?.monsters || [])
        .find(mon => mon.isshk && mon.shoproom === roomno);
    if (!shkp?.isshk) return null;
    if (shkp.shk && x === shkp.shk.x && y === shkp.shk.y) return 0;
    if (obj.no_charge) return 0;

    let price = shopBaseCost(obj);
    if (!price) return null;
    if ((obj.cls === 'armor' || obj.cls === 'weapon' || obj.glyph === '[' || obj.glyph === ')')
        && (obj.spe || 0) > 0) price += 10 * obj.spe;

    let multiplier = 1;
    let divisor = 1;
    if ((obj.dknown !== true || !shopObjectNameKnown(obj)) && ((obj.id ?? obj.o_id ?? 0) % 4) === 0) {
        multiplier *= 4;
        divisor *= 3;
    }
    const cha = game.u?.acurr?.a?.[A_CHA] ?? 10;
    if (cha > 18) divisor *= 2;
    else if (cha === 18) { multiplier *= 2; divisor *= 3; }
    else if (cha >= 16) { multiplier *= 3; divisor *= 4; }
    else if (cha <= 5) multiplier *= 2;
    else if (cha <= 7) { multiplier *= 3; divisor *= 2; }
    else if (cha <= 10) { multiplier *= 4; divisor *= 3; }
    price *= multiplier;
    if (divisor > 1) price = Math.trunc((Math.trunc(price * 10 / divisor) + 5) / 10);
    price = Math.max(1, price) * shopPricingUnits(obj);
    return price;
}

function containedShopNonGoldPrice(obj, x = game.u?.ux, y = game.u?.uy, seen = new Set()) {
    if (!obj || seen.has(obj)) return 0;
    seen.add(obj);
    let price = 0;
    for (const child of globContents(obj)) {
        if (!child || seen.has(child)) continue;
        if (!shopBillableGold(child) && !child.no_charge) {
            const childPrice = shopItemPrice(child, x, y);
            if (childPrice > 0) price += childPrice;
        }
        price += containedShopNonGoldPrice(child, x, y, seen);
    }
    return price;
}

function shopFloorItemPriceInfo(obj, x = game.u?.ux, y = game.u?.uy) {
    const top = shopItemPrice(obj, x, y);
    if (top == null) return null;
    const topPrice = top > 0 ? top : 0;
    const contentsPrice = globContents(obj).length
        ? containedShopNonGoldPrice(obj, x, y)
        : 0;
    const itemPrice = topPrice + contentsPrice;
    return {
        topPrice,
        contentsPrice,
        itemPrice,
        price: itemPrice,
        contentsOnly: contentsPrice > 0 && topPrice === 0,
        contentsIncluded: contentsPrice > 0 && topPrice > 0,
        noCharge: top === 0 && contentsPrice === 0,
    };
}

function shopItemPriceSuffix(obj, x = game.u?.ux, y = game.u?.uy) {
    const info = shopFloorItemPriceInfo(obj, x, y);
    if (!info) return '';
    if (info.itemPrice > 0) {
        const label = info.contentsOnly ? 'contents' : 'for sale';
        return ` (${label}, ${info.itemPrice} zorkmid${info.itemPrice === 1 ? '' : 's'})`;
    }
    if (info.noCharge) return ' (no charge)';
    return '';
}

function shopPickupQuoteMessage(obj, shopPrice, priceInfo = null) {
    const honorifics = ['good', 'honored', 'most gracious', 'esteemed'];
    const honorific = honorifics[rn2(4) || 1];
    const title = game.flags?.female ? 'lady' : 'sir';
    const pricingUnits = Math.max(1, Math.trunc(Number(shopPricingUnits(obj) || 1)));
    const stackQuote = (obj?.quan || 1) > 1;
    const quotePrice = stackQuote && pricingUnits > 1
        ? Math.max(1, Math.trunc(shopPrice / pricingUnits))
        : shopPrice;
    const relation = stackQuote
        ? 'per'
        : priceInfo?.contentsOnly
            ? 'for the contents of this'
            : 'for this';
    const name = pickupObjectName({ ...(obj || {}), quan: 1 });
    const contentsSuffix = !stackQuote && priceInfo?.contentsIncluded ? ' and its contents' : '';
    return `"For you, ${honorific} ${title}; only ${quotePrice} zorkmid${quotePrice === 1 ? '' : 's'} ${relation} ${name}${contentsSuffix}."`;
}

function shopPickupLinePriceSuffix(priceInfo, itemPrice) {
    const price = Math.max(0, Math.trunc(Number(itemPrice || 0)));
    if (!price) return '';
    const label = priceInfo?.contentsOnly ? 'contents' : 'unpaid';
    return ` (${label}, ${price} zorkmid${price === 1 ? '' : 's'})`;
}

function replaceShopPriceLineSuffix(line, suffix) {
    const base = String(line || '').replace(/ \((?:unpaid|contents), \d+ zorkmids?\)/, '');
    return suffix ? `${base}${suffix}` : base;
}

function shopDebtItemName(item) {
    let name = String(item?.line || '')
        .replace(/^[a-zA-Z$] - /, '')
        .replace(/ \(unpaid, \d+ zorkmids?\)/, '');
    if (name) return name;
    const baseName = pickupObjectName({ ...(item || {}), quan: 1 });
    return `${/^[aeiou]/i.test(baseName) ? 'an' : 'a'} ${baseName}`;
}

function findShopBillObjectInList(list, id, seen = new Set()) {
    if (id == null) return null;
    for (const item of list || []) {
        if (!item || seen.has(item)) continue;
        seen.add(item);
        if (String(shopBillObjectId(item)) === String(id)) return item;
        const contents = globContents(item);
        const found = contents.length ? findShopBillObjectInList(contents, id, seen) : null;
        if (found) return found;
    }
    return null;
}

function findShopBillObjectById(id) {
    return findShopBillObjectInList([...(game.inventory || []), ...(game.level?.objects || [])], id);
}

function usedUpShopBillTrackerForId(id, shkp = null) {
    const shkpId = shopkeeperIdentity(shkp);
    return (game._usedUpShopBills || []).find(bill => {
        if (String(bill?.bo_id) !== String(id)) return false;
        return shkpId == null || bill.shopkeeperId == null || String(bill.shopkeeperId) === String(shkpId);
    }) || null;
}

function removeUsedUpShopBillTracker(id, shkp = null) {
    const shkpId = shopkeeperIdentity(shkp);
    const before = (game._usedUpShopBills || []).length;
    game._usedUpShopBills = (game._usedUpShopBills || [])
        .filter(bill => String(bill?.bo_id) !== String(id)
            || (shkpId != null && bill.shopkeeperId != null && String(bill.shopkeeperId) !== String(shkpId)));
    return game._usedUpShopBills.length !== before;
}

function shopBillEntryDisplayName(entry, obj, quantity = null, shkp = null) {
    const tracker = usedUpShopBillTrackerForId(entry?.bo_id, shkp);
    if (tracker?.name) return tracker.name;
    if (entry?.name) return entry.name;
    if (obj) return shopDebtItemName(quantity ? { ...obj, quan: quantity, line: '' } : obj);
    return 'used-up object';
}

function outermostShopBillContainerForObject(obj) {
    let top = obj;
    const seen = new Set();
    while (top?.container && !seen.has(top.container)) {
        seen.add(top);
        top = top.container;
    }
    if (!top || top === obj) return null;
    if ((game.inventory || []).includes(top) || (game.level?.objects || []).includes(top))
        return top;
    return null;
}

function shopContainerPaymentName(container, includesContainer = false) {
    const baseName = pickupObjectName({ ...(container || {}), line: '', quan: 1 })
        .replace(/^empty /, '');
    if (includesContainer)
        return `${articleFor(`unpaid ${baseName}`)} and its contents`;
    if ((game.level?.objects || []).includes(container))
        return `the contents of the ${baseName}`;
    return `the contents of your ${baseName}`;
}

function addContainerPaymentBillItem(groups, container, item, billEntry, price, options = {}) {
    if (!container || !item || !(price > 0)) return;
    const key = shopBillObjectId(container);
    if (key == null) return;
    let group = groups.get(key);
    if (!group) {
        group = { container, billItems: [], price: 0, includesContainer: false, sortBillIndex: options.sortBillIndex };
        groups.set(key, group);
    } else if (group.sortBillIndex == null || options.sortBillIndex === -1
        || (options.sortBillIndex != null && group.sortBillIndex !== -1 && options.sortBillIndex < group.sortBillIndex)) {
        group.sortBillIndex = options.sortBillIndex;
    }
    const billId = billEntry?.bo_id ?? shopBillObjectId(item);
    if (group.billItems.some(entry => String(entry.billId) === String(billId))) return;
    group.billItems.push({ item, billEntry, billId, price, blockedByUsedUp: !!options.blockedByUsedUp });
    group.price += price;
    if (item === container) group.includesContainer = true;
    if (options.blockedByUsedUp) group.blockedByUsedUp = true;
}

function pushShopBillLedgerDebtEntries(shkp, entries, seenBillIds) {
    const ledger = Array.isArray(shkp?.bill) ? shkp.bill : [];
    const usedEntries = [];
    const intactEntries = [];
    const possibleTopContainerEntries = [];
    const containerPaymentGroups = new Map();
    for (let billIndex = 0; billIndex < ledger.length; billIndex++) {
        const billEntry = ledger[billIndex];
        const billId = String(billEntry.bo_id);
        seenBillIds.add(billId);
        const unitPrice = shopBillEntryUnitPrice(billEntry);
        const billedQuantity = shopBillEntryQuantity(billEntry);
        const obj = findShopBillObjectById(billId);
        const liveQuantity = obj ? Math.max(1, Math.trunc(Number(obj.quan || 1))) : 0;
        const tracker = usedUpShopBillTrackerForId(billId, shkp);

        if (billEntry.useup || !obj) {
            const price = shopBillEntryTotal(billEntry) || Number(tracker?.price || 0);
            if (price > 0) usedEntries.push({
                usedUpBill: tracker || { bo_id: billEntry.bo_id, price, name: billEntry.name },
                billEntry,
                billPortion: 'fullyUsedUp',
                quantity: billedQuantity,
                name: shopBillEntryDisplayName(billEntry, obj, billedQuantity, shkp),
                price,
                sortBillIndex: billIndex,
            });
            continue;
        }

        if (liveQuantity < billedQuantity && unitPrice > 0) {
            const usedQuantity = billedQuantity - liveQuantity;
            usedEntries.push({
                item: obj,
                billEntry,
                billPortion: 'partlyUsedUp',
                quantity: usedQuantity,
                name: shopBillEntryDisplayName(billEntry, obj, usedQuantity, shkp),
                price: unitPrice * usedQuantity,
                sortBillIndex: billIndex,
            });
            const intactPrice = unitPrice * liveQuantity;
            const topContainer = outermostShopBillContainerForObject(obj);
            if (topContainer) {
                addContainerPaymentBillItem(containerPaymentGroups, topContainer, obj, billEntry, intactPrice, {
                    blockedByUsedUp: true,
                    sortBillIndex: topContainer.unpaid ? billIndex : -1,
                });
            } else {
                intactEntries.push({
                    item: obj,
                    billEntry,
                    billPortion: 'intact',
                    quantity: liveQuantity,
                    blockedByUsedUp: true,
                    name: shopDebtItemName({ ...obj, quan: liveQuantity, line: '' }),
                    price: intactPrice,
                    sortBillIndex: billIndex,
                });
            }
            continue;
        }

        const price = shopBillEntryTotal(billEntry);
        if (!(price > 0)) continue;
        const intactEntry = {
            item: obj,
            billEntry,
            billPortion: 'intact',
            quantity: liveQuantity || billedQuantity,
            name: shopDebtItemName(obj),
            price,
            sortBillIndex: billIndex,
        };
        const topContainer = outermostShopBillContainerForObject(obj);
        if (topContainer) {
            addContainerPaymentBillItem(containerPaymentGroups, topContainer, obj, billEntry, price, {
                sortBillIndex: topContainer.unpaid ? billIndex : -1,
            });
        } else if (globContents(obj).length) {
            possibleTopContainerEntries.push(intactEntry);
        } else {
            intactEntries.push(intactEntry);
        }
    }
    for (const entry of possibleTopContainerEntries) {
        const group = containerPaymentGroups.get(shopBillObjectId(entry.item));
        if (group) addContainerPaymentBillItem(containerPaymentGroups, entry.item, entry.item, entry.billEntry, entry.price, {
            sortBillIndex: entry.sortBillIndex,
        });
        else intactEntries.push(entry);
    }
    for (const group of containerPaymentGroups.values()) {
        if (!(group.price > 0) || !group.billItems.length) continue;
        intactEntries.push({
            item: group.container,
            containerPayment: true,
            billPortion: 'containerContents',
            billItems: group.billItems,
            quantity: 1,
            name: shopContainerPaymentName(group.container, group.includesContainer),
            price: group.price,
            blockedByUsedUp: !!group.blockedByUsedUp,
            sortBillIndex: group.sortBillIndex,
        });
    }
    entries.push(...usedEntries, ...intactEntries);
}

function collectLegacyUnpaidShopItemsFromList(list, entries, shkp = null, seenBillIds = new Set()) {
    for (const item of list || []) {
        const billEntry = shopBillEntryForObject(shkp, item);
        if (billEntry && seenBillIds.has(String(billEntry.bo_id))) {
            const contents = globContents(item);
            if (contents.length) collectLegacyUnpaidShopItemsFromList(contents, entries, shkp, seenBillIds);
            continue;
        }
        const price = billEntry ? shopBillEntryTotal(billEntry) : unpaidBillPrice(item);
        if ((billEntry || (!shkp && item?.unpaid)) && price) entries.push({ item, billEntry, billPortion: 'legacy', name: shopDebtItemName(item), price });
        const contents = globContents(item);
        if (contents.length) collectLegacyUnpaidShopItemsFromList(contents, entries, shkp, seenBillIds);
    }
}

function collectPayableShopDebts(shkp = null) {
    const entries = [];
    const seenBillIds = new Set();
    if (shkp) pushShopBillLedgerDebtEntries(shkp, entries, seenBillIds);
    collectLegacyUnpaidShopItemsFromList(game.inventory || [], entries, shkp, seenBillIds);
    collectLegacyUnpaidShopItemsFromList(game.level?.objects || [], entries, shkp, seenBillIds);
    for (const bill of game._usedUpShopBills || []) {
        const price = Number(bill?.price || 0);
        if (seenBillIds.has(String(bill?.bo_id))) continue;
        const shkpId = shopkeeperIdentity(shkp);
        if (shkpId != null && bill.shopkeeperId != null && String(bill.shopkeeperId) !== String(shkpId)) continue;
        if (price > 0) entries.push({
            usedUpBill: bill,
            name: bill.name || 'used-up object',
            price,
        });
    }
    return entries
        .map((entry, index) => ({ entry, index }))
        .sort((a, b) => shopPaymentEntryCompare(a.entry, b.entry) || a.index - b.index)
        .map(({ entry }, index) => ({
            ...entry,
            letter: String.fromCharCode(97 + index),
            selected: false,
        }));
}

function shopkeeperDebitPayment(entry) {
    const shkp = entry?.shopkeeperDebit;
    if (!shkp) return { debt: 0, credit: 0, coveredByCredit: 0, cashDue: 0 };
    const debt = Math.max(0, Math.trunc(Number(shkp.debit ?? entry.price ?? 0)));
    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const coveredByCredit = Math.min(credit, debt);
    return { debt, credit, coveredByCredit, cashDue: debt - coveredByCredit };
}

function shopPaymentEntryRank(entry) {
    if (entry?.shopkeeperDebit) return 0;
    if (entry?.billPortion === 'partlyUsedUp' || entry?.billPortion === 'fullyUsedUp' || entry?.usedUpBill) return 1;
    return 2;
}

function shopPaymentEntryIsUsedUp(entry) {
    return entry?.billPortion === 'partlyUsedUp'
        || entry?.billPortion === 'fullyUsedUp'
        || !!entry?.usedUpBill;
}

function shopPaymentEntryCompare(a, b) {
    const rank = shopPaymentEntryRank(a) - shopPaymentEntryRank(b);
    if (rank) return rank;
    const price = shopPaymentEntryValue(b) - shopPaymentEntryValue(a);
    if (price) return price;
    const aSortIndex = Number(a?.sortBillIndex);
    const bSortIndex = Number(b?.sortBillIndex);
    if (Number.isFinite(aSortIndex) && Number.isFinite(bSortIndex) && aSortIndex !== bSortIndex)
        return aSortIndex - bSortIndex;
    return 0;
}

function shopPaymentHasUsedUpSelection(selected, entry) {
    return selected.some(candidate =>
        candidate !== entry && candidate.billEntry === entry.billEntry
        && candidate.billPortion === 'partlyUsedUp');
}

function blockedShopPaymentBillItem(entry, selected) {
    if (entry?.containerPayment || entry?.billPortion === 'containerContents') {
        const billItems = Array.isArray(entry.billItems) ? entry.billItems : [];
        return billItems.find(billItem => {
            if (!billItem?.blockedByUsedUp || !billItem.billEntry || !billItem.item) return false;
            const liveQuantity = Math.max(1, Math.trunc(Number(billItem.item.quan || 1)));
            if (shopBillEntryQuantity(billItem.billEntry) <= liveQuantity) return false;
            return !shopPaymentHasUsedUpSelection(selected, { billEntry: billItem.billEntry });
        }) || null;
    }
    if (!entry?.blockedByUsedUp || !entry.billEntry || !entry.item) return null;
    const liveQuantity = Math.max(1, Math.trunc(Number(entry.item.quan || 1)));
    if (shopBillEntryQuantity(entry.billEntry) <= liveQuantity) return null;
    return !shopPaymentHasUsedUpSelection(selected, entry) ? { item: entry.item, billEntry: entry.billEntry } : null;
}

function shopPaymentEntryBlocked(entry, selected) {
    return !!blockedShopPaymentBillItem(entry, selected);
}

function blockedShopPaymentMessage(entry, selected, shkp = null) {
    const blocker = blockedShopPaymentBillItem(entry, selected);
    if (!blocker?.item || !blocker.billEntry) return "You can't buy that yet.";
    const liveQuantity = Math.max(1, Math.trunc(Number(blocker.item.quan || 1)));
    const usedQuantity = Math.max(1, shopBillEntryQuantity(blocker.billEntry) - liveQuantity);
    const usedName = pickupObjectName({ ...blocker.item, quan: usedQuantity, line: '' });
    if (!shopkeeperCanSpeakToHero(shkp)) {
        const name = shopkeeperDisplayName(shkp);
        const angry = shopkeeperAngryForSellobj(shkp) ? ' angrily' : '';
        const action = shopkeeperNoLimbsForSpeech(shkp) ? 'motions to' : 'points out';
        return `${name}${angry} ${action} your bill for the other ${usedName} first.`;
    }
    const prefix = shopkeeperAngryForSellobj(shkp) ? 'Pay' : 'Please pay';
    const container = entry?.containerPayment || entry?.billPortion === 'containerContents'
        ? entry?.item || blocker.item.container
        : blocker.item.container;
    if (container) {
        const containerName = pickupObjectName({ ...(container || {}), quan: 1, line: '' }).replace(/^empty /, '');
        const liveWord = liveQuantity > 1 ? 'ones' : 'one';
        return `${prefix} for the other ${usedName} before buying the ${liveWord} in the ${containerName}.`;
    }
    const liveWord = liveQuantity > 1 ? 'these' : 'this one';
    return `${prefix} for the other ${usedName} before buying ${liveWord}.`;
}

function payableShopPaymentEntries(selected) {
    return [...(selected || [])]
        .filter(entry => !shopPaymentEntryBlocked(entry, selected || []))
        .sort(shopPaymentEntryCompare);
}

function shopPaymentEntryValue(entry) {
    if (entry?.shopkeeperDebit) return Math.max(0, Math.trunc(Number(entry.shopkeeperDebit.debit ?? entry.price ?? 0)));
    return Math.max(0, Math.trunc(Number(entry?.price || 0)));
}

function shopPaymentCashDue(shkp, selected) {
    let credit = Math.max(0, Math.trunc(Number(shkp?.credit || 0)));
    let cashDue = 0;
    for (const entry of payableShopPaymentEntries(selected)) {
        const value = shopPaymentEntryValue(entry);
        const coveredByCredit = Math.min(credit, value);
        credit -= coveredByCredit;
        cashDue += value - coveredByCredit;
    }
    return cashDue;
}

function shopPaymentPreMenuBlockMessage(shkp, entries, { paidBefore = false } = {}) {
    if (!entries?.length) return '';
    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
    const availableGold = Math.max(0, Math.trunc(Number(game._goldCount || money?.quan || 0)));
    const credit = Math.max(0, Math.trunc(Number(shkp?.credit || 0)));
    if (availableGold + credit <= 0) return `You have no gold or credit${paidBefore ? ' left' : ''}.`;
    const cheapest = Math.min(...entries.map(entry => shopPaymentEntryValue(entry)).filter(value => value > 0));
    if (Number.isFinite(cheapest) && availableGold + credit < cheapest) {
        const moreThanOne = entries.length > 1;
        return `You don't have enough gold to buy${moreThanOne ? ' any of' : ''} the item${moreThanOne ? 's' : ''} ${moreThanOne ? "you've picked" : 'on your bill'}.`;
    }
    return '';
}

function finishShopDebitPayment(shkp) {
    const debit = Math.max(0, Math.trunc(Number(shkp?.debit || 0)));
    if (!shkp || debit <= 0) return { handled: false, paid: false, blocked: false, cashTotal: 0, message: '' };

    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
    const availableGold = Math.max(0, Math.trunc(Number(game._goldCount || money?.quan || 0)));
    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const loan = Math.max(0, Math.trunc(Number(shkp.loan || 0)));
    const name = shopkeeperDisplayName(shkp);
    const currency = `zorkmid${debit === 1 ? '' : 's'}`;
    let owed = `You owe ${name} ${debit} ${currency} `;
    if (loan) {
        owed += loan === debit
            ? 'you picked up in the store.'
            : 'for gold picked up and the use of merchandise.';
    } else {
        owed += 'for the use of merchandise.';
    }

    if (availableGold + credit < debit) {
        return {
            handled: true,
            paid: false,
            blocked: true,
            cashTotal: 0,
            message: `${owed}  But you don't have enough gold${credit ? ' or credit' : ''}.`,
        };
    }

    let cashDue = 0;
    let resultMessage = '';
    if (credit >= debit) {
        shkp.credit = credit - debit;
        resultMessage = 'Your debt is covered by your credit.';
    } else if (!credit) {
        cashDue = debit;
        resultMessage = 'You pay that debt.';
    } else {
        cashDue = debit - credit;
        shkp.credit = 0;
        resultMessage = 'That debt is partially offset by your credit.  You pay the remainder.';
    }

    shkp.debit = 0;
    shkp.loan = 0;
    if (cashDue > 0) {
        if (availableGold > cashDue) next_ident();
        game._goldCount = Math.max(0, availableGold - cashDue);
        if (money) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        }
    }

    return {
        handled: true,
        paid: true,
        blocked: false,
        cashTotal: cashDue,
        message: `${owed}  ${resultMessage}`,
    };
}

function applyShopPaymentValue(shkp, value) {
    const amount = Math.max(0, Math.trunc(Number(value || 0)));
    if (!shkp || !amount) return { coveredByCredit: 0, cashDue: 0 };
    const credit = Math.max(0, Math.trunc(Number(shkp.credit || 0)));
    const coveredByCredit = Math.min(credit, amount);
    shkp.credit = credit - coveredByCredit;
    if (shkp.robbed)
        shkp.robbed = Math.max(0, Math.trunc(Number(shkp.robbed || 0)) - amount);
    return { coveredByCredit, cashDue: amount - coveredByCredit };
}

function payPartlyUsedShopBillEntry(shkp, entry) {
    const billEntry = entry?.billEntry;
    const item = entry?.item;
    if (!billEntry || !item) return { billed: true, removedLedger: false, legacy: true };
    applyShopPaymentValue(shkp, entry.price);
    const liveQuantity = Math.max(1, Math.trunc(Number(item.quan || 1)));
    const unitPrice = shopBillEntryUnitPrice(billEntry);
    billEntry.bquan = liveQuantity;
    billEntry.price = unitPrice;
    billEntry.totalPrice = unitPrice * liveQuantity;
    billEntry.useup = false;
    item.unpaid = true;
    item.unpaidPrice = shopBillEntryTotal(billEntry);
    syncUnpaidBillLine(item);
    removeUsedUpShopBillTracker(billEntry.bo_id, shkp);
    return { billed: true, removedLedger: false, legacy: false };
}

function payFullyUsedShopBillEntry(shkp, entry) {
    const billId = entry?.billEntry?.bo_id ?? entry?.usedUpBill?.bo_id;
    applyShopPaymentValue(shkp, entry.price);
    const removedLedger = removeObjectFromShopBillById(shkp, billId);
    removeUsedUpShopBillTracker(billId, shkp);
    return { billed: true, removedLedger, legacy: !removedLedger };
}

function payIntactShopBillEntry(shkp, entry) {
    applyShopPaymentValue(shkp, entry.price);
    if (!entry?.item) return { billed: false, removedLedger: false, legacy: false };
    const billId = entry.billEntry?.bo_id;
    if (billId != null) {
        const removedLedger = removeObjectFromShopBillById(shkp, billId);
        if (removedLedger) clearObjectShopBillState(entry.item);
        else clearObjectShopBillState(entry.item);
        return { billed: true, removedLedger, legacy: !removedLedger };
    }
    if (removeObjectFromShopBill(shkp, entry.item)) return { billed: true, removedLedger: true, legacy: false };
    clearObjectShopBillState(entry.item);
    return { billed: true, removedLedger: false, legacy: true };
}

function payContainerShopBillEntry(shkp, entry) {
    const billItems = Array.isArray(entry?.billItems) ? entry.billItems : [];
    if (!billItems.length) return { billed: false, removedLedger: false, legacy: false };
    if (shopPaymentEntryBlocked(entry, [entry])) return {
        billed: false,
        removedLedger: false,
        legacy: false,
        blocked: true,
        message: blockedShopPaymentMessage(entry, [entry], shkp),
    };
    applyShopPaymentValue(shkp, entry.price);
    const container = entry.item;
    const ordered = [...billItems].sort((a, b) => (a.item === container) - (b.item === container));
    const seen = new Set();
    let removedLedgerBillCount = 0;
    let legacy = false;
    for (const billItem of ordered) {
        const item = billItem.item;
        const billId = billItem.billEntry?.bo_id ?? shopBillObjectId(item);
        if (billId == null || seen.has(String(billId))) continue;
        seen.add(String(billId));
        const removedLedger = removeObjectFromShopBillById(shkp, billId);
        if (removedLedger) removedLedgerBillCount++;
        else legacy = true;
        clearObjectShopBillState(item);
    }
    return {
        billed: true,
        removedLedger: removedLedgerBillCount > 0,
        removedLedgerBillCount,
        legacy,
    };
}

function applyShopPaymentEntry(shkp, entry) {
    if (entry?.shopkeeperDebit) {
        const debt = Math.max(0, Math.trunc(Number(entry.shopkeeperDebit.debit ?? entry.price ?? 0)));
        applyShopPaymentValue(entry.shopkeeperDebit, debt);
        entry.shopkeeperDebit.debit = 0;
        entry.shopkeeperDebit.loan = 0;
        return { billed: false, removedLedger: false, legacy: false };
    }
    if (entry?.billPortion === 'partlyUsedUp') return payPartlyUsedShopBillEntry(shkp, entry);
    if (entry?.billPortion === 'fullyUsedUp' || entry?.usedUpBill) return payFullyUsedShopBillEntry(shkp, entry);
    if (entry?.billPortion === 'containerContents' || entry?.containerPayment) return payContainerShopBillEntry(shkp, entry);
    return payIntactShopBillEntry(shkp, entry);
}

function shopPaymentInsufficientFundsMessage(entry, shkp) {
    const paymentShopkeeper = entry?.shopkeeperDebit || shkp;
    const credit = Math.max(0, Math.trunc(Number(paymentShopkeeper?.credit || 0)));
    const itemName = entry?.name || 'that item';
    return `You don't have gold${credit ? ' or credit' : ''} enough to pay for ${itemName}.`;
}

function finishShopPaymentSelection(shkp, selected) {
    const payableEntries = payableShopPaymentEntries(selected);
    if (!payableEntries.length) {
        const blockedEntry = (selected || []).find(entry => shopPaymentEntryBlocked(entry, selected || []));
        if (blockedEntry) return {
            paid: false,
            skipped: false,
            blocked: true,
            cashTotal: 0,
            payableEntries,
            message: blockedShopPaymentMessage(blockedEntry, selected || [], shkp),
        };
        return { paid: false, skipped: true, cashTotal: 0, payableEntries, message: '' };
    }
    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
    const availableGold = game._goldCount || money?.quan || 0;
    let remainingGold = availableGold;
    let cashTotal = 0;
    let removedLedgerBillCount = 0;
    let billedSelections = 0;
    let legacyBillSelections = 0;
    const paidEntries = [];
    let stoppedShort = false;
    let stopMessage = '';
    for (const entry of payableEntries) {
        const paymentShopkeeper = entry?.shopkeeperDebit || shkp;
        const value = shopPaymentEntryValue(entry);
        const credit = Math.max(0, Math.trunc(Number(paymentShopkeeper?.credit || 0)));
        const cashDue = value - Math.min(credit, value);
        if (cashDue > remainingGold) {
            const message = shopPaymentInsufficientFundsMessage(entry, paymentShopkeeper);
            if (paidEntries.length) {
                stoppedShort = true;
                stopMessage = message;
                break;
            }
            return {
                paid: false,
                cashTotal: 0,
                payableEntries,
                message,
            };
        }
        const result = applyShopPaymentEntry(shkp, entry);
        if (result.blocked) {
            if (paidEntries.length) {
                stoppedShort = true;
                stopMessage = result.message || blockedShopPaymentMessage(entry, selected || [], paymentShopkeeper);
                break;
            }
            return {
                paid: false,
                blocked: true,
                cashTotal: 0,
                payableEntries,
                message: result.message || blockedShopPaymentMessage(entry, selected || [], paymentShopkeeper),
            };
        }
        remainingGold -= cashDue;
        cashTotal += cashDue;
        paidEntries.push(entry);
        if (!result.billed) continue;
        billedSelections++;
        if (result.removedLedgerBillCount != null)
            removedLedgerBillCount += result.removedLedgerBillCount;
        else if (result.removedLedger)
            removedLedgerBillCount++;
        if (result.legacy) legacyBillSelections++;
    }
    if (payableEntries.length > paidEntries.length && !stoppedShort) {
        stoppedShort = true;
        stopMessage = shopPaymentInsufficientFundsMessage(payableEntries[paidEntries.length] || payableEntries[0], shkp);
    }
    if (shkp && legacyBillSelections)
        shkp.billct = Math.max(0, (shkp.billct || legacyBillSelections) - legacyBillSelections);
    if (!paidEntries.length) return {
        paid: false,
        cashTotal: 0,
        payableEntries,
        message: shopPaymentInsufficientFundsMessage(payableEntries[0], shkp),
    };

    if (availableGold > cashTotal) next_ident();
    game._goldCount = Math.max(0, remainingGold);
    if (money) {
        money.quan = game._goldCount;
        updateMoneyLine(money);
    }
    return {
        paid: true,
        cashTotal,
        payableEntries: paidEntries,
        billedSelections,
        removedLedgerBillCount,
        legacyBillSelections,
        stoppedShort,
        message: stopMessage,
    };
}

function shopPaymentMenuRows(entries, width) {
    const rows = [[0, 41, 'Pay for which items?', 1]];
    let row = 2;
    const hasUsedUpSection = shopPaymentEntryIsUsedUp(entries[0]);
    if (hasUsedUpSection) {
        const plural = entries[1] && shopPaymentEntryIsUsedUp(entries[1]) ? 's' : '';
        rows.push([row++, 41, `Used up item${plural}:`]);
    }
    for (let i = 0; i < entries.length; i++) {
        const entry = entries[i];
        if (hasUsedUpSection && i > 0 && shopPaymentEntryIsUsedUp(entries[i - 1]) && !shopPaymentEntryIsUsedUp(entry)) {
            const plural = i < entries.length - 1 ? 's' : '';
            rows.push([row++, 41, `Unpaid item${plural}:`]);
        }
        const price = String(entry.price).padStart(width, ' ');
        entry.payMenuRow = row;
        rows.push([row++, 41, `${entry.letter} - ${price} Zm, ${entry.name}`]);
    }
    rows.push([row, 41, '(end)']);
    return rows;
}

function shopPaymentTraditionalStyle() {
    const style = game.flags?.menu_style ?? game.flags?.menustyle ?? game.flags?.menuStyle;
    return style === 0 || String(style || '').toLowerCase() === 'traditional';
}

function shopPaymentMoreThanOne(entries) {
    if ((entries || []).length > 1) return true;
    const entry = entries?.[0];
    return !!(entry?.containerPayment || entry?.billPortion === 'containerContents' || entry?.billPortion === 'partlyUsedUp');
}

function clearShopPaymentPromptState({ clearShopkeeper = true } = {}) {
    game._pay_menu_items = null;
    game._pay_menu_width = 0;
    game._pay_itemized_index = 0;
    game._pay_itemized_paid_entries = null;
    game._pay_itemized_cash_total = 0;
    game._pay_prepaid_message = '';
    game._pay_prepaid_paid = false;
    game._overlay_lines = null;
    if (clearShopkeeper) game._pay_shopkeeper = null;
}

function shopPaymentThankYouMessage(shkp) {
    if (shopkeeperAngryForSellobj(shkp)) return '';
    const shopIndex = (shkp?.shoptype || 0) - SHOPBASE;
    const shopName = SHOP_TYPES[shopIndex]?.name || 'shop';
    const shopkeeperName = shkp?.shknam || 'shopkeeper';
    const possessive = shopkeeperName.endsWith('s') ? `${shopkeeperName}'` : `${shopkeeperName}'s`;
    if (!shopkeeperCanSpeakToHero(shkp))
        return `${shopkeeperDisplayName(shkp)} nods appreciatively at you for shopping in ${possessive} ${shopName}!`;
    return `"Thank you for shopping in ${possessive} ${shopName}!"`;
}

function shopPaymentBoughtMessage(entries, cashTotal) {
    const paidEntries = entries || [];
    if (paidEntries.length === 1) {
        const entry = paidEntries[0];
        return `You bought ${entry.name} for ${entry.price} gold piece${entry.price === 1 ? '' : 's'}.`;
    }
    return `You bought ${paidEntries.length} items for ${cashTotal} gold pieces.`;
}

function shopPaymentItemizedQuestion(entry) {
    const price = Math.max(0, Math.trunc(Number(entry?.price || 0)));
    return `Pay for ${entry?.name || 'that item'} for ${price} zorkmid${price === 1 ? '' : 's'}? [yn]`;
}

function openShopPaymentMenu(shkp, entries, { prepaidMessage = '', prepaidPaid = false } = {}) {
    const width = String(Math.max(...entries.map(entry => entry.price))).length;
    const rows = shopPaymentMenuRows(entries, width);
    game._pay_menu_items = entries;
    game._pay_menu_width = width;
    game._pay_shopkeeper = shkp;
    game._pay_prepaid_message = prepaidMessage || '';
    game._pay_prepaid_paid = !!prepaidPaid;
    setOverlay(rows, Math.max(...rows.map(([row]) => row)) + 1, false, 41);
    game._command_mode = 'payMenu';
}

async function finishShopPaymentCommandResult(shkp, payment, { prepaidMessage = '', prepaidPaid = false } = {}) {
    if (!payment.paid) {
        if (payment.skipped) return false;
        const message = [prepaidMessage, payment.message || "You don't have enough gold."]
            .filter(Boolean).join('  ');
        await setMessage(message);
        if (prepaidPaid) game.context.move = 1;
        return false;
    }
    if (shkp?.shk) {
        const home = shkp.shk;
        const blockingPet = (game.level?.monsters || []).find(mon =>
            mon.pet && Math.max(Math.abs(mon.mx - home.x), Math.abs(mon.my - home.y)) === 1
            && (mon.mx === home.x || mon.my === home.y));
        if (blockingPet) {
            const stepDx = Math.sign((game.u?.ux ?? home.x) - home.x)
                || Math.sign(blockingPet.mx - home.x) || -1;
            const stepDy = Math.sign(blockingPet.my - home.y)
                || Math.sign((game.u?.uy ?? home.y) - home.y) || 1;
            shkp._paid_shopkeeper_step = { x: home.x + stepDx, y: home.y + stepDy };
            blockingPet._paid_shopkeeper_pet_step = {
                x: blockingPet.mx + Math.sign(blockingPet.mx - home.x),
                y: blockingPet.my + Math.sign(blockingPet.my - home.y),
            };
        }
    }
    game._pet_food_scan_inventory = game.inventory;
    game._queued_message_after_more = payment.stoppedShort
        ? payment.message || "You don't have enough gold."
        : shopPaymentThankYouMessage(shkp);
    const message = [prepaidMessage, shopPaymentBoughtMessage(payment.payableEntries, payment.cashTotal)]
        .filter(Boolean).join('  ');
    await setMessage(message, true);
    game.context.move = 1;
    return true;
}

async function startShopPaymentItemizedFlow(shkp, entries, { prepaidMessage = '', prepaidPaid = false } = {}) {
    game._pay_menu_items = entries;
    game._pay_shopkeeper = shkp;
    game._pay_prepaid_message = prepaidMessage || '';
    game._pay_prepaid_paid = !!prepaidPaid;
    game._pay_itemized_index = 0;
    game._pay_itemized_paid_entries = [];
    game._pay_itemized_cash_total = 0;
    game._command_mode = 'payItemized';
    await setMessage(shopPaymentItemizedQuestion(entries[0]));
}

function shopkeeperObjectivePronoun(shkp) {
    if (shkp?.female) return 'her';
    if (shkp?.neuter || shkp?.data?.neuter) return 'it';
    return 'him';
}

function shopkeeperSubjectPronoun(shkp) {
    if (shkp?.female) return 'she';
    if (shkp?.neuter || shkp?.data?.neuter) return 'it';
    return 'he';
}

function shopkeeperRobbedAmount(shkp) {
    return Math.max(0, Math.trunc(Number(shkp?.robbed || 0)));
}

const SHOPKEEPER_APPEASEMENT_GOLD = 1000;

function hasRobbedOnlyShopPayment(shkp, entries = []) {
    if (!shkp || shopkeeperRobbedAmount(shkp) <= 0 || entries.length) return false;
    const ledgerCount = Array.isArray(shkp.bill) ? shkp.bill.length : 0;
    const billCount = Math.max(ledgerCount, Math.max(0, Math.trunc(Number(shkp.billct || 0))));
    const debit = Math.max(0, Math.trunc(Number(shkp.debit || 0)));
    return billCount === 0 && debit === 0;
}

function hasAngryUnrobbedShopPayment(shkp, entries = []) {
    if (!shkp || entries.length || shopkeeperRobbedAmount(shkp) > 0 || !shopkeeperAngryForSellobj(shkp)) return false;
    const ledgerCount = Array.isArray(shkp.bill) ? shkp.bill.length : 0;
    const billCount = Math.max(ledgerCount, Math.max(0, Math.trunc(Number(shkp.billct || 0))));
    const debit = Math.max(0, Math.trunc(Number(shkp.debit || 0)));
    return billCount === 0 && debit === 0;
}

function finishRobbedOnlyShopPayment(shkp, { resident = null } = {}) {
    const robbed = shopkeeperRobbedAmount(shkp);
    if (!robbed) return { handled: false, paid: false, cashTotal: 0, message: '' };

    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
    const availableGold = Math.max(0, Math.trunc(Number(game._goldCount || money?.quan || 0)));
    const name = shopkeeperDisplayName(shkp);
    const isResident = !!resident && sameShopkeeper(shkp, resident);
    const isAngry = shopkeeperAngryForSellobj(shkp);
    const messages = [];

    if (!isResident && !isAngry) {
        if (!availableGold) {
            return { handled: true, paid: false, cashTotal: 0, message: 'You have no gold.' };
        }
        const nominalPayment = Math.min(availableGold, robbed);
        const payment = applyShopPaymentValue(shkp, nominalPayment);
        const paidCash = payment.cashDue;
        if (availableGold > paidCash) next_ident();
        game._goldCount = Math.max(0, availableGold - paidCash);
        if (money) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        }
        if (availableGold > robbed)
            messages.push(`You give ${name} the ${robbed} gold piece${robbed === 1 ? '' : 's'} ${shopkeeperSubjectPronoun(shkp)} asked for.`);
        else
            messages.push(`You give ${name} all your gold.`);
        if (availableGold < Math.trunc(robbed / 2)) {
            messages.push(`Unfortunately, ${shopkeeperSubjectPronoun(shkp)} doesn't look satisfied.`);
        } else {
            shkp.robbed = 0;
            shkp.following = 0;
            shkp.angry = false;
            shkp.hostile = false;
            shkp.mpeaceful = 1;
        }
        return {
            handled: true,
            paid: true,
            cashTotal: paidCash,
            compensationValue: nominalPayment,
            robbedOnly: true,
            message: messages.join('  '),
        };
    }

    messages.push(`${name} is after blood, not gold!`);
    if (availableGold < Math.trunc(robbed / 2)) {
        messages.push(availableGold
            ? `Besides, you don't have enough to interest ${shopkeeperObjectivePronoun(shkp)}.`
            : 'Moreover, you have no gold.');
        return {
            handled: true,
            paid: false,
            cashTotal: 0,
            compensationValue: 0,
            robbedOnly: true,
            message: messages.join('  '),
        };
    }

    const possessivePronoun = shopkeeperPossessivePronoun(shkp);
    const nominalPayment = Math.min(availableGold, robbed);
    messages.push(`But since ${possessivePronoun} shop has been robbed recently,`);
    messages.push(`you ${availableGold < robbed ? 'partially ' : ''}compensate ${name} for ${possessivePronoun} losses.`);
    const payment = applyShopPaymentValue(shkp, nominalPayment);
    const paidCash = payment.cashDue;
    if (availableGold > paidCash) next_ident();
    game._goldCount = Math.max(0, availableGold - paidCash);
    if (money) {
        money.quan = game._goldCount;
        updateMoneyLine(money);
    }
    shkp.robbed = 0;
    shkp.following = 0;
    shkp.angry = false;
    shkp.hostile = false;
    shkp.mpeaceful = 1;
    return {
        handled: true,
        paid: true,
        cashTotal: paidCash,
        compensationValue: nominalPayment,
        robbedOnly: true,
        message: messages.join('  '),
    };
}

function finishAngryUnrobbedShopPayment(shkp) {
    if (!shkp) return { handled: false, paid: false, cashTotal: 0, message: '' };

    const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
    const availableGold = Math.max(0, Math.trunc(Number(game._goldCount || money?.quan || 0)));
    const name = shopkeeperDisplayName(shkp);
    const messages = [`${name} is after your hide, not your gold!`];

    if (availableGold < SHOPKEEPER_APPEASEMENT_GOLD) {
        messages.push(availableGold
            ? `Besides, you don't have enough to interest ${shopkeeperObjectivePronoun(shkp)}.`
            : 'Moreover, you have no gold.');
        return { handled: true, paid: false, cashTotal: 0, message: messages.join('  ') };
    }

    const targetName = payCanSpotMonster(shkp) ? `the angry ${name}` : name;
    messages.push(`You try to appease ${targetName} by giving ${shopkeeperObjectivePronoun(shkp)} ${SHOPKEEPER_APPEASEMENT_GOLD} gold pieces.`);
    const payment = applyShopPaymentValue(shkp, SHOPKEEPER_APPEASEMENT_GOLD);
    if (payment.coveredByCredit >= SHOPKEEPER_APPEASEMENT_GOLD) {
        messages.push('The price is deducted from your credit.');
    } else {
        if (payment.coveredByCredit > 0)
            messages.push('The price is partially covered by your credit.');
        if (availableGold > payment.cashDue) next_ident();
        game._goldCount = Math.max(0, availableGold - payment.cashDue);
        if (money) {
            money.quan = game._goldCount;
            updateMoneyLine(money);
        }
    }

    const customer = String(shkp.customer || '');
    const player = String(game.plname || '');
    const stillAngry = !!customer && !!player && customer === player && !rn2(3);
    if (stillAngry) {
        messages.push(`But ${name} is as angry as ever.`);
    } else {
        shkp.robbed = 0;
        shkp.following = 0;
        shkp.angry = false;
        shkp.hostile = false;
        shkp.mpeaceful = 1;
        messages.push(`${name} calms down.`);
    }

    return {
        handled: true,
        paid: true,
        cashTotal: payment.cashDue,
        compensationValue: SHOPKEEPER_APPEASEMENT_GOLD,
        angryUnrobbed: true,
        message: messages.join('  '),
    };
}

async function finishPayCommandForShopkeeper(shkp, resident, { requestMenu = false } = {}) {
    game._pay_prepaid_message = '';
    game._pay_prepaid_paid = false;
    const debitPayment = finishShopDebitPayment(shkp);
    if (debitPayment.blocked) {
        game._command_mode = null;
        await setMessage(debitPayment.message || "You don't have enough gold.");
        game.context.move = 1;
        return;
    }
    const entries = collectPayableShopDebts(shkp);
    if (!debitPayment.paid && hasAngryUnrobbedShopPayment(shkp, entries)) {
        const payment = finishAngryUnrobbedShopPayment(shkp);
        game._command_mode = null;
        await setMessage(payment.message || "You don't have enough gold.");
        game.context.move = 1;
        return;
    }
    if (!debitPayment.paid && hasRobbedOnlyShopPayment(shkp, entries)) {
        const payment = finishRobbedOnlyShopPayment(shkp, { resident });
        game._command_mode = null;
        await setMessage(payment.message || "You don't have enough gold.");
        game.context.move = 1;
        return;
    }
    if (!entries.length) {
        await setMessage(debitPayment.message || `You do not owe ${shkp.shknam || 'the shopkeeper'} anything.`);
        if (debitPayment.paid) game.context.move = 1;
        return;
    }
    const preMenuBlockMessage = shopPaymentPreMenuBlockMessage(shkp, entries, { paidBefore: debitPayment.paid });
    if (preMenuBlockMessage) {
        await setMessage([debitPayment.message, preMenuBlockMessage].filter(Boolean).join('  '));
        if (debitPayment.paid) game.context.move = 1;
        return;
    }
    let viaMenu = !shopPaymentTraditionalStyle();
    if (requestMenu) viaMenu = !viaMenu;
    if (viaMenu) {
        openShopPaymentMenu(shkp, entries, {
            prepaidMessage: debitPayment.message || '',
            prepaidPaid: !!debitPayment.paid,
        });
        return;
    }
    game._pay_menu_items = entries;
    game._pay_shopkeeper = shkp;
    game._pay_prepaid_message = debitPayment.message || '';
    game._pay_prepaid_paid = !!debitPayment.paid;
    if (!shopPaymentMoreThanOne(entries)) {
        await startShopPaymentItemizedFlow(shkp, entries, {
            prepaidMessage: debitPayment.message || '',
            prepaidPaid: !!debitPayment.paid,
        });
        return;
    }
    game._command_mode = 'payItemizedPrompt';
    await setMessage('Itemized billing? [ynq m] (q)');
}

function pickupMenuEntries(objects) {
    return objects.map((obj, index) => {
        let section = 'Other Items';
        if (obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$') section = 'Coins';
        else if (obj.cls === 'amulet') section = 'Amulets';
        else if (obj.otyp === DART || obj.cls === 'weapon' || obj.glyph === ')') section = 'Weapons';
        else if (obj.cls === 'armor' || obj.glyph === '[') section = 'Armor';
        else if (obj.otyp === 'corpse' || obj.otyp === CORPSE || obj.otyp === FOOD_CLASS || obj.cls === 'food') section = 'Comestibles';
        else if (obj.otyp === SCROLL_CLASS || obj.cls === 'scroll') section = 'Scrolls';
        else if (obj.cls === 'spellbook') section = 'Spellbooks';
        else if (obj.otyp === POTION_CLASS || obj.cls === 'potion') section = 'Potions';
        else if (obj.otyp === RING_CLASS || obj.cls === 'ring' || obj.glyph === '=') section = 'Rings';
        else if (obj.otyp === WAND_CLASS || obj.cls === 'wand') section = 'Wands';
        else if (BAG_OBJECT_TYPES.has(obj.otyp) || obj.otyp === OIL_LAMP || obj.otyp === MAGIC_LAMP || obj.cls === 'tool' || obj.glyph === '(') section = 'Tools';
        else if (isBoulderObject(obj) || obj.otyp === STATUE || obj.cls === 'rock' || obj.glyph === '`') section = 'Boulders/Statues';
        else if (obj.otyp === GEM_CLASS || obj.cls === 'gem' || obj.glyph === '*') section = 'Gems/Stones';
        if (obj.section) section = obj.section;
        return { obj, index, section };
    }).sort((a, b) => {
        const sectionDiff = PICKUP_SECTION_ORDER.indexOf(a.section) - PICKUP_SECTION_ORDER.indexOf(b.section);
        if (sectionDiff) return sectionDiff;
        if (typeof a.obj.otyp === 'number' && typeof b.obj.otyp === 'number' && a.obj.otyp !== b.obj.otyp)
            return a.obj.otyp - b.obj.otyp;
        if (a.obj.pileIndex != null || b.obj.pileIndex != null) return (a.obj.pileIndex || 0) - (b.obj.pileIndex || 0);
        return a.index - b.index;
    }).map((entry, index, entries) => {
        if (entry.section === 'Coins') return { ...entry, letter: '$' };
        const priorNonCoins = entries.slice(0, index).filter(item => item.section !== 'Coins').length;
        return { ...entry, letter: 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'[priorNonCoins] };
    });
}

function pickupMenuLines(entries, selected) {
    const selectedLetters = new Set(Array.isArray(selected) ? selected : selected ? [selected] : []);
    const rows = [[0, 41, 'Pick up what?', 1]];
    let row = 2;
    let lastSection = '';
    for (const entry of entries) {
        if (entry.section !== lastSection) {
            rows.push([row++, 41, entry.section, 1]);
            lastSection = entry.section;
        }
        const priceSuffix = shopItemPriceSuffix(entry.obj, entry.obj?.ox, entry.obj?.oy);
        rows.push([row++, 41, `${entry.letter} ${selectedLetters.has(entry.letter) ? '+' : '-'} ${pickupObjectPhrase(entry.obj)}${priceSuffix}`]);
    }
    rows.push([row, 41, '(end)']);
    return rows;
}

function pickupListSelectedObjects(pickup) {
    const selectedLetters = new Set(pickup?.selected || []);
    return (pickup?.entries || [])
        .filter(entry => selectedLetters.has(entry.letter))
        .map(entry => entry.obj);
}

function clearPickupListOverlay() {
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    game._pickup_list = null;
}

function isFloorGoldObject(obj) {
    return !!obj && (obj.otyp === GOLD_PIECE || obj.cls === 'coin' || obj.glyph === '$');
}

function removePickupListFloorObject(obj) {
    if (!obj) return;
    objectIceEffect(obj, obj.ox, obj.oy, { onLevel: false });
    game.level.objects = (game.level?.objects || []).filter(item => item !== obj);
}

function pickupListPickedClass(obj) {
    return isBoulderObject(obj) ? 'rock' : obj.cls || (obj.otyp === DART ? 'weapon'
        : obj.otyp === GEM_CLASS ? 'gem'
            : obj.otyp === 'corpse' || obj.otyp === CORPSE ? 'food'
                : obj.otyp === RING_CLASS || obj.glyph === '=' ? 'ring'
                    : BAG_OBJECT_TYPES.has(obj.otyp) || obj.otyp === OIL_LAMP || obj.otyp === MAGIC_LAMP
                        || obj.otyp === MIRROR || obj.otyp === EXPENSIVE_CAMERA
                        || obj.otyp === STETHOSCOPE || obj.otyp === MAGIC_MARKER ? 'tool'
                        : undefined);
}

function finishPickupListProcessing(state) {
    game._pet_food_scan_inventory = game.inventory;
    clearPickupListOverlay();
    game._floor_pickup_menu_pending = null;
    game._command_mode = null;
    newsym(game.u?.ux || 0, game.u?.uy || 0);
    return setMessage((state?.messages || []).join('  ')).then(() => {
        game.context.move = 1;
    });
}

function pickupListPromptMessage(state, preflight) {
    return [...(state?.messages || []), floorPickupBurdenPromptMessage(preflight)].join('  ');
}

function pickupListPendingPrompt(state, obj, preflight) {
    state.pendingObject = obj;
    state.pendingPreflight = preflight;
    state.scareResetSpeObj = preflight.scareResetSpeOnDecline ? obj : null;
    game._floor_pickup_menu_pending = state;
    game._command_mode = 'pickupListBurdenConfirm';
    return setMessage(pickupListPromptMessage(state, preflight)).then(() => {
        game.context.move = 0;
    });
}

function pickupListHandleGold(obj, preflight, state) {
    if (preflight?.messages?.length) state.messages.push(...preflight.messages);
    const pickup = pickUpFloorGoldObject(obj, preflight?.takeCount || obj.quan || 1);
    state.messages.push(...(pickup.messages || []));
}

function pickupListHandleObject(obj, preflight, state) {
    if (preflight?.messages?.length) state.messages.push(...preflight.messages);
    const pickupObj = splitFloorPickupObjectForLift(obj, preflight?.takeCount || obj.quan || 1);
    if (preflight.scareRemainderState && pickupObj !== obj)
        Object.assign(obj, preflight.scareRemainderState);
    const letter = pickupObj.wasStolen && pickupObj.letter
        ? pickupObj.letter
        : preflight?.inventoryLetter || nextInventoryLetter();
    const amount = pickupObjectPhrase(pickupObj);
    const priceInfo = shopFloorItemPriceInfo(pickupObj, pickupObj?.ox, pickupObj?.oy);
    const shopPrice = priceInfo?.itemPrice ?? shopItemPrice(pickupObj);
    const mergeTarget = findPickedObjectInventoryMergeTarget(pickupObj, shopPrice);
    if (mergeTarget) {
        objectIceEffect(pickupObj, pickupObj.ox, pickupObj.oy, { onLevel: false });
        if (mergeTarget.billMerge?.price > 0)
            state.messages.push(shopPickupQuoteMessage(pickupObj, mergeTarget.billMerge.price));
        state.messages.push(mergePickedObjectIntoInventory(pickupObj, mergeTarget.target));
        removePickupListFloorObject(pickupObj);
        return;
    }
    const pickedItem = {
        ...pickupObj,
        cls: pickupListPickedClass(pickupObj),
        letter,
        kind: pickupObj.kind || pickupObjectName({ ...pickupObj, quan: 1 }),
        line: `${letter} - ${amount}`,
    };
    const billing = addPickedObjectToShopBill(pickupObj, pickedItem);
    const billedPrice = billing.itemPrice ?? billing.price ?? 0;
    objectIceEffect(pickedItem, pickupObj.ox, pickupObj.oy, { onLevel: false });
    const unpaidSuffix = shopPickupLinePriceSuffix(priceInfo, billedPrice);
    pickedItem.line = replaceShopPriceLineSuffix(pickedItem.line, unpaidSuffix);
    game.inventory = [...(game.inventory || []), pickedItem];
    maybeAttachCarriedFigurineTimeout(pickedItem);
    if (billing.messages?.length) state.messages.push(...billing.messages);
    if (billedPrice > 0) state.messages.push(shopPickupQuoteMessage(pickupObj, billedPrice, priceInfo));
    if (billing.goldMessages?.length) state.messages.push(...billing.goldMessages);
    state.messages.push(`${letter} - ${amount}${unpaidSuffix}.`);
    removePickupListFloorObject(pickupObj);
}

async function continuePickupListProcessing(state, acceptedPreflight = null) {
    state.index ??= 0;
    state.messages ??= [];
    state.selected ??= [];
    while (state.index < state.selected.length) {
        const obj = state.selected[state.index];
        if (!obj || !(game.level?.objects || []).includes(obj)) {
            state.index++;
            continue;
        }
        const preflight = acceptedPreflight || await floorPickupPreflight(obj);
        acceptedPreflight = null;
        if (preflight.prompt)
            return pickupListPendingPrompt(state, obj, preflight);
        if (!preflight.ok) {
            state.messages.push(floorPickupPreflightMessage(preflight));
            if (preflight.scareDust && scheduleScareMonsterDustTryCall(preflight, { moveCost: 1, pickupListState: state })) {
                await setMessage((state.messages || []).join('  '), true);
                game.context.move = 0;
                return;
            }
            if (preflight.scareDust) {
                const billMessage = finalizeScareMonsterDust(preflight);
                if (billMessage) state.messages.push(billMessage);
            }
            state.index++;
            if (preflight.scareDust) continue;
            break;
        }
        if (preflight?.skip) {
            if (preflight?.messages?.length) state.messages.push(...preflight.messages);
            state.index++;
            continue;
        }
        if (isFloorGoldObject(obj)) pickupListHandleGold(obj, preflight, state);
        else pickupListHandleObject(obj, preflight, state);
        state.index++;
    }
    return finishPickupListProcessing(state);
}

function deathSummary() {
    const genderKey = game.flags?.female ? 'f' : 'm';
    const role = game.urole?.name?.[genderKey] || game.urole?.name?.m || game._startup_role || 'Adventurer';
    const rank = game.urole?.rank?.[genderKey] || game.urole?.rank?.m || role;
    const name = game.plname || 'Hero';
    const race = game.urace?.adj || game._startup_race || 'human';
    const gender = game.flags?.female ? 'female' : 'male';
    const align = game.u?.ualign?.type > 0 ? 'lawful' : game.u?.ualign?.type < 0 ? 'chaotic' : 'neutral';
    const stats = game.u?.acurr?.a || [];
    const dungeon = game.level?.flags?.tutorial_level
        ? 'The Tutorial'
        : game.dungeons?.[game.u?.uz?.dnum ?? 0]?.name || 'The Dungeons of Doom';
    const depth = game.u?._displayDepth || depth_of_level(game.u?.uz || { dnum: 0, dlevel: 1 });
    const carriedGold = game._goldCount
        ?? (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin')?.quan
        ?? 0;
    let hiddenGold = 0;
    const contained = (game.inventory || []).flatMap(item => item.contents || []);
    while (contained.length) {
        const item = contained.pop();
        if (item.otyp === GOLD_PIECE || item.cls === 'coin' || item.glyph === '$') hiddenGold += item.quan || 1;
        contained.push(...(item.contents || []));
    }
    const gold = carriedGold + hiddenGold;
    const cause = game._death_cause || 'died';
    const dnum = game.u?.uz?.dnum ?? 0;
    let deepest = depth;
    for (const key of game._saved_levels?.keys?.() || []) {
        const [savedDnum, savedLevel] = String(key).split(':').map(Number);
        if (savedDnum === dnum) deepest = Math.max(deepest, depth_of_level({ dnum: savedDnum, dlevel: savedLevel }));
    }
    const depthBonus = 50 * (deepest - 1) + (deepest > 30 ? 10000 : deepest > 20 ? 1000 * (deepest - 20) : 0);
    let scoreGold = Math.max(0, gold - (game._initialGoldCount || 0));
    scoreGold -= Math.trunc(scoreGold / 10);
    const score = (game.u?.urexp || 0) + scoreGold + depthBonus;
    return {
        role, rank, name, race, gender, align, stats, dungeon, depth, gold, cause,
        maxlvl: deepest,
        score,
        level: game.u?.ulevel || 1,
        hp: Math.max(0, game.u?.uhp || 0),
        hpmax: game.u?.uhpmax || 1,
        en: game.u?.uen || 0,
        enmax: game.u?.uenmax || 0,
        ac: game.u?.uac ?? 10,
        turns: game._death_moves || game.moves || 1,
        exp: game.u?.uexp || 0,
        outsideDungeon: game._death_outside_dungeon || '',
    };
}

function canMakeBones() {
    if (game._death_no_bones) return false;
    if (game.flags?.bones === false) return false;
    const uz = game.u?.uz;
    const dungeon = game.dungeons?.[uz?.dnum ?? 0];
    if (dungeon && uz?.dlevel === dungeon.num_dunlevs) return false;
    if (uz?.dlevel > 1 && (game.branches || []).some(branch =>
        branch.end1?.dnum === uz.dnum && branch.end1?.dlevel === uz.dlevel)) return false;
    if ((game.level?.traps || []).some(trap => trap.ttyp === MAGIC_PORTAL)) return false;
    const levelDepth = depth_of_level(uz);
    if (levelDepth <= 0) return false;
    if (!rn2(1 + (levelDepth >> 2)) && !game.flags?.debug) return false;
    return !game.flags?.explore;
}

function deathBonesLeavesStatue() {
    return game._death_bones_body === 'statue';
}

function heroDeathStatueMonsterData() {
    if (game.u?._polyself_form) return { ...game.u._polyself_form };
    const roleName = String(game.urole?.name?.m || game._startup_role || 'tourist').toLowerCase();
    return { name: roleName, glyph: '@', mlet: 'human', neuter: false };
}

function createDeathBonesRemains() {
    if (game._death_corpse_created || !game.u) return;
    const leavesStatue = deathBonesLeavesStatue();
    const pm = leavesStatue
        ? heroDeathStatueMonsterData()
        : DEATH_CORPSE_BY_RACE[game._startup_race || game.urace?.adj || 'human'] || DEATH_CORPSE_BY_RACE.human;
    const remains = mkcorpstat(leavesStatue ? STATUE : CORPSE, null, pm, game.u.ux, game.u.uy, leavesStatue ? 0 : 8);
    remains.oname = game.plname || 'wizard';
    remains._death_remains = leavesStatue ? 'statue' : 'corpse';
    if (leavesStatue) {
        remains.kind = 'statue';
        remains.spe = (remains.spe || 0) & ~CORPSTAT_GENDER;
        remains.contents = [];
    }
    game._death_corpse_created = 1;
}

function curseDeathBonesInventory() {
    for (const item of game.inventory || []) {
        if (rn2(5)) {
            item.cursed = true;
            item.blessed = false;
        }
        rn2(8);
    }
}

function createDeathBonesGhost() {
    if (deathBonesLeavesStatue()) {
        delete game._bones_ghost;
        return;
    }
    const ghostId = next_ident();
    const ghostHp = d(9, 8);
    rn2(2);
    rn2(50);
    rn2(100);
    rn2(100);
    game._bones_ghost = {
        id: ghostId,
        mx: game.u?.ux || 0,
        my: game.u?.uy || 0,
        data: {
            name: 'ghost',
            glyph: ' ',
            mlet: 'ghost',
            mlevel: 10,
            mac: -5,
            mmove: 3,
            difficulty: 13,
            maligntyp: -5,
            color: CLR_GRAY,
            passWalls: true,
            noCorpse: true,
            undead: true,
        },
        glyph: ' ',
        color: CLR_GRAY,
        m_lev: game.u?.ulevel || 1,
        mhp: game.u?.uhpmax || ghostHp,
        mhpmax: game.u?.uhpmax || ghostHp,
        msleeping: 1,
        mpeaceful: 0,
        female: !!game.flags?.female,
        givenName: game.plname || 'wizard',
    };
}

function prepareDeathBones() {
    if (game.flags?.debug || game._death_bones_prepared) return;
    game._death_bones_prepared = 1;
    game._bones_ok = canMakeBones();
}

function saveDeathBonesIfNeeded() {
    if (!game._bones_ok || game._death_bones_saved || game.flags?.debug) return;
    game._death_bones_saved = 1;

    const uz = game.u?.uz || { dnum: 0, dlevel: 1 };
    const bonesPath = `/bones/${uz.dnum}:${uz.dlevel}`;
    if (vfsReadFile(bonesPath) !== null) return;

    createDeathBonesRemains();
    curseDeathBonesInventory();
    createDeathBonesGhost();
    vfsWriteFile(bonesPath, encodeBonesLevel());
}

export function recordVanquished(mon, awardExperience = true) {
    const name = mon?.data?.name || mon?.name || mon?.corpsenm?.name;
    if (!name) return;
    if (mon._vanquished_recorded) return;
    mon._vanquished_recorded = 1;
    game._vanquished_counts ??= {};
    game._vanquished_counts[name] = (game._vanquished_counts[name] || 0) + 1;
    game._vanquished_total = Object.values(game._vanquished_counts).reduce((sum, count) => sum + count, 0);
    game._vanquished = Object.entries(game._vanquished_counts)
        .sort(([nameA], [nameB]) => {
            const monA = VANQUISHED_MONSTER_DATA.get(nameA) || { mlevel: 0, index: 9999 };
            const monB = VANQUISHED_MONSTER_DATA.get(nameB) || { mlevel: 0, index: 9999 };
            return (monB.mlevel - monA.mlevel) || (monA.index - monB.index) || nameA.localeCompare(nameB);
        })
        .map(([monName, count]) => {
            if (count === 1) return `${/^[aeiou]/i.test(monName) ? 'an' : 'a'} ${monName}`;
            const plural = monName.endsWith('s') ? `${monName}es`
                : monName.endsWith('y') ? `${monName.slice(0, -1)}ies`
                : `${monName}s`;
            return `${count} ${plural}`;
        });
    const data = mon?.data || {};
    const level = Math.max(0, mon?.m_lev ?? mon?.mlevel ?? data.mlevel ?? 0);
    const speed = data.mmove ?? NORMAL_SPEED;
    const attacks = data.xpAttacks || data.attacks || (data.attack ? [data.attack] : []);
    const hasWeaponAttack = attacks.some(monsterAttack =>
        (monsterAttack.aatyp || (monsterAttack.weapon ? 'weap' : '')) === 'weap');
    let experience = 1 + level * level
        + (speed > NORMAL_SPEED ? speed > (3 * NORMAL_SPEED / 2) ? 5 : 3 : 0)
        + ((data.armed || data.attack?.weapon) && !hasWeaponAttack ? 5 : 0)
        + (data.xpAttackBonus || 0);
    const monsterAc = data.mac ?? 10;
    if (monsterAc < 3) experience += (7 - monsterAc) * (monsterAc < 0 ? 2 : 1);
    for (const monsterAttack of attacks) {
        const attackType = monsterAttack.aatyp || (monsterAttack.weapon ? 'weap' : '');
        if (attackType === 'weap') experience += 5;
        else if (attackType === 'magc') experience += 10;
        else if (XP_SPECIAL_ATTACK_TYPES.has(attackType)) experience += 3;
        const damageType = monsterAttack.adtyp || 'phys';
        if (XP_DOUBLE_DAMAGE_TYPES.has(damageType)) experience += 2 * level;
        else if (XP_FLAT_DAMAGE_TYPES.has(damageType)) experience += 50;
        else if (damageType !== 'phys') experience += level;
        if ((monsterAttack.dice ?? 0) * (monsterAttack.sides ?? 0) > 23) experience += level;
    }
    if (!awardExperience) return;
    game.u.urexp = (game.u.urexp || 0) + 4 * experience;
    if (!game.u?.ualign) return;
    set_malign(mon);
    const conductDelta = mon.mtame ? -15 : mon.mpeaceful ? -5 : 0;
    for (const delta of [conductDelta, mon.malign || 0]) {
        const record = game.u.ualign.record || 0;
        const newRecord = record + delta;
        if (delta < 0) {
            if (newRecord < record) game.u.ualign.record = newRecord;
            game.u.ualign.abuse = (game.u.ualign.abuse || 0) - delta;
        } else if (delta > 0 && newRecord > record) {
            const alignLim = 10 + Math.trunc((game.moves || 1) / 200);
            game.u.ualign.record = Math.min(newRecord, alignLim);
        }
    }
}

function deathAttributesPage1() {
    const dsum = deathSummary();
    const alignIndex = dsum.align === 'lawful' ? 0 : dsum.align === 'neutral' ? 1 : 2;
    const alignNames = ['lawful', 'neutral', 'chaotic'];
    const pantheonRole = dsum.role === 'Priest' ? game._pantheon_role || 'Barbarian' : dsum.role;
    const gods = GODS_BY_ROLE[pantheonRole] || ['Marduk', 'Marduk', 'Marduk'];
    const opposedGods = gods
        .map((god, idx) => ({ god, idx }))
        .filter(({ idx }) => idx !== alignIndex)
        .map(({ god, idx }) => `${god} (${alignNames[idx]})`)
        .join(' and ');
    const raceLimits = DEATH_ATTR_LIMITS_BY_RACE[game._startup_race] || DEATH_ATTR_LIMITS_BY_RACE.human;
    const dungeon = dsum.dungeon === 'The Tutorial' ? 'the Tutorial' : dsum.dungeon.replace(/^The /, 'the ');
    const nextLevelExp = dsum.level === 1 ? 20 : 20 * (2 ** (dsum.level - 1));
    const energy = dsum.en === dsum.enmax
        ? dsum.enmax === 2 ? 'both energy points' : `all ${dsum.enmax} energy points`
        : `${dsum.en} out of ${dsum.enmax} energy points`;
    const pickup = game.flags?.pickup
        ? ` Autopickup was on for '${game.flags.pickup_types || 'all'}' plus thrown.`
        : ' Autopickup was off.';
    const hour = Number(String(game._datetime || '').slice(8, 10));
    const rows = [
        [0, 0, `${dsum.name} the ${dsum.role}'s attributes:`],
        [2, 0, 'Background:'],
        [3, 0, ` You were ${/^[aeiou]/i.test(dsum.rank) ? 'an' : 'a'} ${dsum.rank}, a level ${dsum.level} ${dsum.gender} ${dsum.race} ${dsum.role}.`],
        [4, 0, ` You were ${dsum.align}, on a mission for ${gods[alignIndex]}`],
        [5, 0, ` who was opposed by ${opposedGods}.`],
        [6, 0, ` You were ${game.u?.uhandedness || 'right'}-handed.`],
        [7, 0, ` You were in ${dungeon}, on level ${dsum.depth}.`],
        [8, 0, ` You entered the dungeon ${dsum.turns} turns ago.`],
    ];
    let row = 9;
    if (hour < 6 || hour >= 22) rows.push([row++, 0, ' It was nighttime.']);
    if (game.flags?.moonphase === 4) rows.push([row++, 0, ' There was a full moon in effect when your adventure ended.']);
    if (game.flags?.moonphase === 0) rows.push([row++, 0, ' There was a new moon in effect when your adventure ended.']);
    const needed = nextLevelExp - dsum.exp;
    rows.push(
        [row++, 0, ` You had ${dsum.exp} experience point${dsum.exp === 1 ? '' : 's'}, ${needed}${dsum.exp ? ' more' : ''} ${needed === 1 ? 'was' : 'were'} needed to attain level ${dsum.level + 1}.`],
        [row++, 0, ''],
        [row++, 0, 'Basics:'],
        [row++, 0, ` You had ${dsum.hp} out of ${dsum.hpmax} hit points.`],
        [row++, 0, ` You had ${energy} (spell power).`],
        [row++, 0, ` Your armor class was ${dsum.ac}.`],
        [row++, 0, dsum.gold ? ` Your wallet contained ${dsum.gold} zorkmids.` : ' Your wallet was empty.'],
        [row++, 0, pickup],
        [row++, 0, ''],
        [row++, 0, 'Final Characteristics:'],
        [row++, 0, ` Your strength was ${strengthString(dsum.stats[0] || 10)} (limit:${raceLimits[0]}).`],
        [row++, 0, ` Your dexterity was ${dsum.stats[3] || 10} (limit:${raceLimits[3]}).`],
        [row++, 0, ` Your constitution was ${dsum.stats[4] || 10} (limit:${raceLimits[4]}).`],
        [row++, 0, ` Your intelligence was ${dsum.stats[1] || 10} (limit:${raceLimits[1]}).`],
    );
    game._death_attributes_page2_prefix = rows.filter(([line]) => line >= 23).map(([, col, text]) => [col, text]);
    return [...rows.filter(([line]) => line < 23), [23, 0, ' --More--']];
}

function deathAttributesPage2() {
    const dsum = deathSummary();
    const roleName = game.urole?.name?.m || game._startup_role || dsum.role;
    const raceLimits = DEATH_ATTR_LIMITS_BY_RACE[game._startup_race] || DEATH_ATTR_LIMITS_BY_RACE.human;
    const tutorialDeath = dsum.dungeon === 'The Tutorial';
    const weapon = (game.inventory || []).find(item => item.wielded || item.line?.includes('weapon in') || item.line?.includes('weapon;'));
    let weaponName = tutorialDeath ? null : weapon?.kind || weapon?.name || (typeof weapon?.otyp === 'string' ? weapon.otyp : null);
    if (roleName === 'Samurai') {
        if (weaponName === 'katana') weaponName = 'long sword';
        else if (weaponName === 'wakizashi') weaponName = 'short sword';
    }
    if (/war hammer|mjollnir/i.test(weaponName || '')) weaponName = 'hammer';
    if (weaponName) weaponName = String(weaponName).replace(/^(?:elven|orcish|dwarvish) /, '');
    if (weaponName === 'scalpel') weaponName = 'knife';
    const weaponArticle = weaponName && /^[aeiou]/i.test(weaponName) ? 'an' : 'a';
    const rows = [];
    let row = 0;
    for (const [col, text] of game._death_attributes_page2_prefix || []) rows.push([row++, col, text]);
    rows.push(
        [row++, 0, ` Your wisdom was ${dsum.stats[2] || 10} (limit:${raceLimits[2]}).`],
        [row++, 0, ` Your charisma was ${dsum.stats[5] || 10} (limit:${raceLimits[5]}).`],
        [row++, 0, ''],
        [row++, 0, 'Final Status:'],
    );
    if (game.u?.sleepyTimeout || (game.inventory || []).some(item => item.cls === 'amulet' && item.amuletIndex === 3 && (item.worn || item.line?.includes('being worn'))))
        rows.push([row++, 0, ' You fell asleep uncontrollably.']);
    rows.push(
        [row++, 0, " You weren't hungry."],
        [row++, 0, ' You were unencumbered.'],
        [row++, 0, weaponName ? ` You were wielding ${weaponArticle} ${weaponName}.` : ' You were bare handed.'],
        [row++, 0, weaponName ? ` You had basic skill with ${weaponName}.` : ' You were unskilled in bare handed combat.'],
    );
    if (tutorialDeath || !(game.inventory || []).some(item => item.worn && item.cls === 'armor'))
        rows.push([row++, 0, " You weren't wearing any armor."]);
    rows.push(
        [row++, 0, ''],
        [row++, 0, 'Final Attributes:'],
        [row++, 0, ` You were ${(game.u?.ualign?.record || 0) >= 10 ? 'fervently' : 'nominally'} aligned.`],
    );
    if (game.u?.poisonResistance) rows.push([row++, 0, ' You were poison resistant.']);
    if (roleName === 'Ranger') rows.push([row++, 0, ' You had automatic searching.']);
    if (heroHasAntimagic())
        rows.push([row++, 0, ' You were magic-protected.']);
    if (['elf', 'gnome', 'dwarf', 'orc'].includes(game._startup_race)) rows.push([row++, 0, ' You had infravision.']);
    if (game.u?.stealth) rows.push([row++, 0, ' You were stealthy.']);
    if (heroHasSlowDigestion()) rows.push([row++, 0, ' You had slower digestion.']);
    if (!tutorialDeath && (game.inventory || []).some(item => item.worn && item.cls === 'armor')) rows.push([row++, 0, ' You were warded.']);
    if ((game.u?.uluck || 0) > 0) rows.push([row++, 0, ' You were lucky.']);
    rows.push(
        [row++, 0, ' You are dead.'],
        [row++, 0, ''],
        [row++, 0, 'Miscellaneous:'],
        [row++, 0, " You didn't encounter any bones levels."],
        [row++, 0, ' Total elapsed playing time was none.'],
        [row++, 0, ' --More--'],
    );
    const overflow = rows
        .filter(([line, , text]) => line >= 23 && text !== ' --More--')
        .map(([line, col, text]) => [line - 23, col, text]);
    if (overflow.length) {
        game._death_attributes_page3_lines = [...overflow, [overflow.length, 0, ' --More--']];
        return [...rows.filter(([line]) => line < 23), [23, 0, ' --More--']];
    }
    game._death_attributes_page3_lines = null;
    return rows;
}

function deathConductLines() {
    const rows = [[0, 41, 'Voluntary challenges:']];
    let row = 1;
    const genocideCount = genocidedMonsterNames().length;
    rows.push(
        [row++, 42, 'Character rerolling was not enabled.'],
        [row++, 42, 'You went without food.'],
        [row++, 42, 'You were an atheist.'],
    );
    if (!game._chronicle_first_weapon_hit) rows.push([row++, 42, 'You never hit with a wielded weapon.']);
    if (!game._chronicle_first_kill) rows.push([row++, 42, 'You were a pacifist.']);
    rows.push(
        [row++, 42, 'You were illiterate.'],
        [row++, 42, genocideCount
            ? `You genocided ${genocideCount} type${genocideCount === 1 ? '' : 's'} of monster${genocideCount === 1 ? '' : 's'}.`
            : 'You never genocided any monsters.'],
        [row++, 42, 'You never polymorphed an object.'],
        [row++, 42, 'You never changed form.'],
        [row++, 42, 'You used no wishes.'],
        [row++, 41, '--More--'],
    );
    return rows;
}

function visitedDungeon(name) {
    const current = game.u?.uz;
    if (current && game.dungeons?.[current.dnum]?.name === name) return true;
    for (const key of game._saved_levels?.keys?.() || []) {
        const dnum = Number(String(key).split(':')[0]);
        if (game.dungeons?.[dnum]?.name === name) return true;
    }
    return false;
}

function visitedSpecialLevel(name) {
    const level = game.specialLevels?.find(entry => entry.name === name);
    if (!level) return false;
    const current = game.u?.uz;
    if (current?.dnum === level.dnum && current?.dlevel === level.dlevel) return true;
    return !!game._saved_levels?.has?.(`${level.dnum}:${level.dlevel}`);
}

function conductAchievementRows(startRow, col) {
    const rows = [];
    let row = startRow;
    const role = game._startup_role || game.urole?.name?.m || '';
    const level = game.u?._polyself_base?.ulevel || game.u?.ulevel || 1;
    if (role === 'Knight') {
        for (const [minLevel, rank] of [[2, 'Esquire'], [6, 'Bachelor'], [10, 'Sergeant'], [14, 'Knight']]) {
            if (level >= minLevel) rows.push([row++, col, `You have attained the rank of ${rank}.`]);
        }
    }
    if (visitedDungeon('Gehennom')) rows.push([row++, col, 'You have entered Gehennom.']);
    if (visitedDungeon('The Gnomish Mines')) rows.push([row++, col, 'You have entered the Gnomish Mines.']);
    if (visitedSpecialLevel('minetn')) rows.push([row++, col, 'You have entered Minetown.']);
    return rows;
}

function liveConductLines() {
    const conduct = game.u?.uconduct || {};
    const wizard = !!game.flags?.debug;
    const headerCol = wizard ? 34 : 40;
    const textCol = headerCol + 1;
    const rows = [[0, headerCol, 'Voluntary challenges:']];
    let row = 1;
    const genocideCount = genocidedMonsterNames().length;
    rows.push([row++, textCol, 'Character rerolling was not enabled.']);
    if (conduct.food > 0) {
        if (!conduct.unvegan) rows.push([row++, textCol, 'You have followed a strict vegan diet.']);
        else if (!conduct.unvegetarian) rows.push([row++, textCol, 'You have been vegetarian.']);
    } else {
        rows.push([row++, textCol, 'You have gone without food.']);
    }
    if (!game._chronicle_rejected_atheism) rows.push([row++, textCol, 'You have been an atheist.']);
    if (conduct.weaphit > 0) {
        if (wizard) {
            rows.push([row++, textCol, `You have hit with a wielded weapon ${conduct.weaphit} time${conduct.weaphit === 1 ? '' : 's'}.`]);
        }
    } else rows.push([row++, textCol, 'You have never hit with a wielded weapon.']);
    if (!game._chronicle_first_kill) rows.push([row++, textCol, 'You have been a pacifist.']);
    if (conduct.literate > 0) {
        if (wizard) {
            rows.push([row++, textCol, `You have read items or engraved ${conduct.literate} time${conduct.literate === 1 ? '' : 's'}.`]);
        }
    } else rows.push([row++, textCol, 'You have been illiterate.']);
    if (conduct.pets) {
        if (wizard) rows.push([row++, textCol, `You have had ${conduct.pets} pet${conduct.pets === 1 ? '' : 's'}.`]);
    } else rows.push([row++, textCol, 'You have never had a pet.']);
    rows.push([row++, textCol, genocideCount
        ? `You have genocided ${genocideCount} type${genocideCount === 1 ? '' : 's'} of monster${genocideCount === 1 ? '' : 's'}.`
        : 'You have never genocided any monsters.']);
    if (conduct.polypiles) {
        if (wizard) rows.push([row++, textCol, `You have polymorphed ${conduct.polypiles} item${conduct.polypiles === 1 ? '' : 's'}.`]);
    } else rows.push([row++, textCol, 'You have never polymorphed an object.']);
    if (conduct.polyselfs) {
        if (wizard) rows.push([row++, textCol, `You have changed form ${conduct.polyselfs} time${conduct.polyselfs === 1 ? '' : 's'}.`]);
    } else rows.push([row++, textCol, 'You have never changed form.']);
    if (conduct.wishes) {
        rows.push([row++, textCol, `You have used ${conduct.wishes} wish${conduct.wishes === 1 ? '' : 'es'}.`]);
        if (!conduct.wisharti) rows.push([row++, textCol, "You haven't wished for any artifacts."]);
    } else rows.push([row++, textCol, 'You have used no wishes.']);
    const achievementHeaderRow = row + 1;
    const achievements = conductAchievementRows(achievementHeaderRow + 1, textCol);
    if (achievements.length) {
        row = achievementHeaderRow;
        rows.push([row++, headerCol, 'Achievements:']);
        rows.push(...achievements);
        row = Math.max(...rows.map(([line]) => line)) + 1;
    }
    rows.push([Math.min(row, 20), headerCol, '--More--']);
    return rows.filter(([line]) => line <= 20);
}

const IDENTIFIED_RING_NAMES = [
    'adornment', 'gain strength', 'gain constitution', 'increase accuracy',
    'increase damage', 'protection', 'regeneration', 'searching', 'stealth',
    'sustain ability', 'levitation', 'hunger', 'aggravate monster', 'conflict',
    'warning', 'poison resistance', 'fire resistance', 'cold resistance',
    'shock resistance', 'free action', 'slow digestion', 'teleportation',
    'teleport control', 'polymorph', 'polymorph control', 'invisibility',
    'see invisible', 'protection from shape changers',
];
const IDENTIFIED_SCROLL_NAMES = [
    'enchant armor', 'destroy armor', 'confuse monster', 'scare monster',
    'remove curse', 'enchant weapon', 'create monster', 'taming', 'genocide',
    'light', 'teleportation', 'gold detection', 'food detection', 'identify',
    'magic mapping', 'amnesia', 'fire', 'earth', 'punishment', 'charging',
    'stinking cloud',
];
const GENOCIDE_MONSTER_CLASS_NAMES = new Map([
    ['ant', 'a'], ['blob', 'b'], ['cockatrice', 'c'], ['dog', 'd'],
    ['eye', 'e'], ['feline', 'f'], ['gnome', 'G'], ['gremlin', 'g'],
    ['humanoid', 'h'], ['imp', 'i'], ['jelly', 'j'], ['kobold', 'k'],
    ['leprechaun', 'l'], ['mimic', 'm'], ['nymph', 'n'], ['orc', 'o'],
    ['piercer', 'p'], ['quadruped', 'q'], ['rodent', 'r'], ['spider', 's'],
    ['trapper', 't'], ['unicorn', 'u'], ['vortex', 'v'], ['worm', 'w'],
    ['xan', 'x'], ['light', 'y'], ['zruty', 'z'], ['angel', 'A'],
    ['bat', 'B'], ['centaur', 'C'], ['dragon', 'D'], ['elemental', 'E'],
    ['fungus', 'F'], ['giant', 'H'], ['jabberwock', 'J'], ['kop', 'K'],
    ['lich', 'L'], ['mummy', 'M'], ['naga', 'N'], ['ogre', 'O'],
    ['pudding', 'P'], ['quantum mechanic', 'Q'], ['rust monster', 'R'],
    ['snake', 'S'], ['troll', 'T'], ['umber hulk', 'U'], ['vampire', 'V'],
    ['wraith', 'W'], ['xorn', 'X'], ['apelike creature', 'Y'],
    ['zombie', 'Z'], ['human', '@'], ['demon', '&'], ['eel', ';'],
    ['ghost', ' '],
]);
const SCROLL_WISH_PROBS = [
    63, 45, 53, 35, 65, 80, 45, 15, 15, 90, 55, 33, 25, 180, 45, 35,
    30, 18, 15, 15, 15,
];
const IDENTIFIED_POTION_NAMES = [
    'gain ability', 'restore ability', 'confusion', 'blindness', 'paralysis',
    'speed', 'levitation', 'hallucination', 'invisibility', 'see invisible',
    'healing', 'extra healing', 'gain level', 'enlightenment',
    'monster detection', 'object detection', 'gain energy', 'sleeping',
    'full healing', 'polymorph', 'booze', 'sickness', 'fruit juice', 'acid',
    'oil',
];
const POTION_INDEX_BY_OTYP = new Map([
    [239, 2], [242, 3], [244, 4], [245, 5],
    [247, 8], [POT_POLYMORPH, 19],
    [235, 10], [236, 11], [237, 12], [249, 15],
    [250, 16], [243, 17], [246, 18], [240, 20],
    [241, 22], [POT_ACID, 23], [POT_OIL, 24],
]);
const POTION_WISH_PROBS = [
    40, 40, 40, 30, 40, 40, 40, 30, 40, 40, 115, 45, 20, 20, 40, 40,
    40, 40, 10, 10, 40, 40, 40, 10, 30,
];
const IDENTIFIED_AMULET_NAMES = [
    'amulet of esp', 'amulet of life saving', 'amulet of strangulation',
    'amulet of restful sleep', 'amulet versus poison', 'amulet of change',
    'amulet of unchanging', 'amulet of reflection', 'amulet of magical breathing',
    'amulet of guarding', 'amulet of flying',
];
const BAD_AMULET_NAMES = new Set([
    'amulet of strangulation',
    'amulet of restful sleep',
    'amulet of change',
]);
const IDENTIFIED_GEM_NAMES = [
    [2, 'dilithium crystal'], [5, 'diamond'], [9, 'ruby'], [12, 'jacinth stone'],
    [16, 'sapphire'], [19, 'black opal'], [24, 'emerald'], [30, 'turquoise stone'],
    [34, 'citrine stone'], [40, 'aquamarine stone'], [48, 'amber stone'], [58, 'topaz stone'],
    [64, 'jet stone'], [76, 'opal'], [84, 'chrysoberyl stone'], [96, 'garnet stone'],
    [110, 'amethyst stone'], [125, 'jasper stone'], [140, 'fluorite stone'],
    [149, 'obsidian stone'], [161, 'agate stone'], [171, 'jade stone'],
    [248, 'worthless piece of white glass'], [325, 'worthless piece of blue glass'],
    [402, 'worthless piece of red glass'], [479, 'worthless piece of yellowish brown glass'],
    [555, 'worthless piece of orange glass'], [632, 'worthless piece of yellow glass'],
    [708, 'worthless piece of black glass'], [785, 'worthless piece of green glass'],
    [862, 'worthless piece of violet glass'], [890, 'gray stone'],
    [900, 'rock'], [1000, 'rock'],
];

function quiverSuffix(item) {
    const cls = item.cls || (item.otyp === RING_CLASS ? 'ring' : item.otyp === GEM_CLASS ? 'gem' : item.otyp === WAND_CLASS ? 'wand' : '');
    const name = String(item.actualKind || item.kind || pickupObjectName(item)).toLowerCase();
    const bowAmmo = /\b(?:arrow|arrows|ya)\b/.test(name);
    const crossbowAmmo = /\bcrossbow bolts?\b/.test(name);
    if (cls === 'weapon' || item.otyp === DART || item.glyph === ')') {
        if (bowAmmo) return ' (in quiver)';
        if (crossbowAmmo) return ' (in quiver pouch)';
        return ' (at the ready)';
    }
    if (cls === 'ring' || cls === 'amulet' || cls === 'wand' || cls === 'coin' || cls === 'gem') return ' (in quiver pouch)';
    return ' (at the ready)';
}

function identifiedInventoryLine(item) {
    const letter = item.letter || '?';
    const quan = item.quan || 1;
    const cls = item.cls || (item.otyp === RING_CLASS ? 'ring' : item.otyp === GEM_CLASS ? 'gem' : item.otyp === SCROLL_CLASS ? 'scroll' : item.otyp === POTION_CLASS ? 'potion' : item.otyp === WAND_CLASS ? 'wand' : '');
    const buc = item.blessed ? 'blessed' : item.cursed ? 'cursed' : 'uncursed';
    let phrase = '';
    let suffix = '';

    if (cls === 'coin') return `$ - ${quan} gold piece${quan === 1 ? '' : 's'}`;
    if (cls === 'weapon' || item.otyp === DART || item.wielded || item.alternate) {
        const spe = item.spe ?? 0;
        const enchantment = `${spe >= 0 ? '+' : ''}${spe} `;
        const lineShowsBuc = /\b(?:blessed|uncursed|cursed)\b/.test(String(item.line || ''));
        const knownBuc = item.blessed || item.cursed || item.bknown === true || lineShowsBuc;
        const bucPrefix = knownBuc && buc !== 'uncursed' ? `${buc} ` : '';
        const { poisonPrefix, name } = poisonedWeaponDisplayParts(item);
        phrase = `${bucPrefix}${poisonPrefix}${enchantment}${name}`;
        if (item.wielded) {
            const kind = String(item.kind || '').toLowerCase();
            suffix = ` (weapon in ${/quarterstaff|two-handed sword|battle-axe/.test(kind) ? 'hands' : `${game.u?.uhandedness || 'right'} hand`})`;
        } else if (item.alternate) {
            suffix = ` (alternate weapon${quan > 1 ? 's' : ''}; not wielded)`;
        } else if (item.quivered) {
            suffix = quiverSuffix(item);
        }
    } else if (cls === 'armor') {
        let armorName = pickupObjectName(item);
        if (!armorName.startsWith('pair of ')
            && (armorName.endsWith('boots') || armorName.endsWith('shoes') || armorName.endsWith('gloves') || armorName.startsWith('gauntlets')))
            armorName = `pair of ${armorName}`;
        const erosion = erosionPrefix(item);
        phrase = `${buc} ${erosion}${item.spe == null ? '' : `${item.spe >= 0 ? '+' : ''}${item.spe} `}${armorName}`;
        if (item.worn) suffix = ' (being worn)';
    } else if (cls === 'scroll') {
        const raw = String(item.kind || '').replace(/^scroll:/, '').replace(/^scroll of /, '') || 'identify';
        phrase = `${buc} ${quan > 1 ? 'scrolls' : 'scroll'} of ${raw}`;
    } else if (cls === 'spellbook') {
        const raw = item.spellName || item.spell?.name || String(item.kind || '').replace(/^spellbook(?: of)? /, '') || 'blank paper';
        phrase = `${buc} ${quan > 1 ? 'spellbooks' : 'spellbook'} of ${raw}`;
    } else if (cls === 'potion') {
        const raw = String(item.actualKind || item.kind || '').replace(/^potion:/, '').replace(/^potion of /, '') || 'water';
        if (raw === 'water' && item.blessed) phrase = 'potion of holy water';
        else if (raw === 'water' && item.cursed) phrase = 'potion of unholy water';
        else {
            const diluted = item.odiluted && !isWaterPotion(item) ? 'diluted ' : '';
            phrase = `${buc} ${diluted}${quan > 1 ? 'potions' : 'potion'} of ${raw}`;
            phrase = maybeLitObjectName(item, phrase);
        }
    } else if (cls === 'ring') {
        const roll = item.ringRoll || item.roll || 0;
        const ring = roll ? `ring of ${IDENTIFIED_RING_NAMES[roll - 1]}` : String(item.kind || 'ring');
        const spe = roll <= 6 || item.spe ? `${(item.spe || 0) >= 0 ? '+' : ''}${item.spe || 0} ` : '';
        phrase = `${buc} ${spe}${ring}`;
    } else if (cls === 'amulet') {
        const name = item.actualKind
            || (item.amuletIndex != null ? IDENTIFIED_AMULET_NAMES[item.amuletIndex] : '')
            || String(item.kind || '')
            || pickupObjectName(item);
        phrase = `${buc} ${name}`;
        if (item.worn || item.line?.includes('being worn')) suffix = ' (being worn)';
    } else if (cls === 'wand') {
        const raw = item.wand
            || (item.wandIndex != null ? IDENTIFIED_WAND_NAMES[item.wandIndex] : '')
            || String(item.kind || '').replace(/^wand of /, '')
            || 'nothing';
        phrase = `wand of ${raw}${identifiedChargeSuffix(item)}`;
    } else if (cls === 'tool') {
        const raw = pickupObjectName(item);
        const chargeSuffix = isChargedTool(item) ? identifiedChargeSuffix(item) : '';
        phrase = item.tool === 'magicMarker' || item.kind === 'magic marker'
            ? `magic marker${identifiedChargeSuffix(item)}`
            : item.kind === 'sack'
                ? `empty ${buc} sack`
            : `${buc} ${raw}${chargeSuffix}`;
    } else if (cls === 'food' || item.otyp === FOOD_CLASS) {
        let name = pickupObjectName(item);
        if (String(item.kind || '').startsWith('tin:')) {
            const tin = item.kind.slice(4);
            name = tin === 'spinach' ? 'tin of spinach' : `homemade tin of ${tin === 'lichen' ? tin : `${tin} meat`}`;
        } else if (item.kind === 'tin' && item.corpsenm?.name) {
            const meat = item.corpsenm.name === 'lichen' ? 'lichen' : `${item.corpsenm.name} meat`;
            name = `homemade tin of ${meat}`;
        }
        phrase = `${buc} ${name}`;
        if (isMeatRingObject(item)) {
            if (item.worn === 'right' || item.line?.includes('(on right hand)')) suffix = ' (on right hand)';
            else if (item.worn === 'left' || item.line?.includes('(on left hand)')) suffix = ' (on left hand)';
        } else if (item.quivered) suffix = quiverSuffix(item);
    } else if (cls === 'gem') {
        const gem = item.actualKind || (item.gemRoll
            ? IDENTIFIED_GEM_NAMES.find(([upper]) => item.gemRoll <= upper)?.[1]
            : (String(item.gemDescription || item.kind || '').match(/^(.+) gem$/)?.[1]
                ? `worthless piece of ${String(item.gemDescription || item.kind).replace(/ gem$/, '')} glass`
                : pickupObjectName(item)));
        phrase = `${buc} ${gem}`;
    } else {
        phrase = `${buc} ${pickupObjectName(item)}`;
    }

    const amount = quan > 1
        ? `${quan} ${phrase}`
        : `${/^[aeiou]/i.test(phrase) ? 'an' : 'a'} ${phrase}`;
    return `${letter} - ${amount}${suffix}`;
}

function identifyInventoryItem(item) {
    if (!item) return;
    item.known = true;
    item.bknown = true;
    item.rknown = true;
    item.cknown = true;
    item.lknown = true;
    if (item.cls === 'scroll' || item.otyp === SCROLL_CLASS) {
        const raw = item.actualKind?.replace(/^scroll of /, '')
            || (item.scrollIndex != null ? IDENTIFIED_SCROLL_NAMES[item.scrollIndex] : '')
            || String(item.kind || '').replace(/^scrolls? of /, '').replace(/^scroll:/, '');
        if (raw) {
            item.kind = raw;
            item.actualKind = `scroll of ${raw}`;
        }
    } else if (item.cls === 'spellbook') {
        const raw = item.spellName || item.spell?.name
            || String(item.kind || '').replace(/^spellbook(?: of)? /, '');
        if (raw) {
            item.spellName = raw;
            item.kind = `spellbook of ${raw}`;
        }
    } else if (item.cls === 'potion' || item.otyp === POTION_CLASS) {
        const raw = item.actualKind?.replace(/^potion of /, '')
            || (item.potionIndex != null ? IDENTIFIED_POTION_NAMES[item.potionIndex] : '')
            || String(item.kind || '').replace(/^potion(?: of)? /, '');
        if (raw) item.actualKind = `potion of ${raw}`;
    } else if (item.cls === 'ring' || item.otyp === RING_CLASS) {
        const raw = item.actualKind?.replace(/^ring of /, '')
            || (item.ringRoll ? IDENTIFIED_RING_NAMES[item.ringRoll - 1] : '')
            || String(item.kind || '').replace(/^ring of /, '');
        if (raw) item.kind = `ring of ${raw}`;
    } else if (item.cls === 'wand' || item.otyp === WAND_CLASS) {
        item.chargeKnown = true;
    } else if (isChargedTool(item)) {
        item.chargeKnown = true;
    }
    item.line = identifiedInventoryLine(item);
}

function unidentifiedInventoryItems() {
    return (game.inventory || []).filter(invItem => {
        if (invItem.cls === 'scroll' || invItem.otyp === SCROLL_CLASS)
            return !invItem.known || !invItem.bknown;
        if (invItem.cls === 'spellbook') return !invItem.known || !invItem.bknown;
        if (invItem.cls === 'potion' || invItem.otyp === POTION_CLASS)
            return !invItem.known || !invItem.bknown;
        if (invItem.cls === 'ring' || invItem.otyp === RING_CLASS)
            return !invItem.known || !invItem.bknown;
        if (invItem.cls === 'wand' || invItem.otyp === WAND_CLASS)
            return !invItem.known || !invItem.chargeKnown;
        return false;
    });
}

function normalInventoryLine(item) {
    const letter = item.letter || '?';
    if ((BAG_OBJECT_TYPES.has(item.otyp) || /bag|sack/.test(String(item.kind || item.line || '').toLowerCase()))
        && item.contents?.length) {
        const count = item.contents.length;
        return `${letter} - a bag containing ${count} item${count === 1 ? '' : 's'}`;
    }
    const quan = item.quan || 1;
    const kind = String(item.kind || '').toLowerCase();
    const cls = item.cls || (item.otyp === RING_CLASS ? 'ring' : item.otyp === GEM_CLASS ? 'gem' : item.otyp === SCROLL_CLASS ? 'scroll' : item.otyp === POTION_CLASS ? 'potion' : item.otyp === WAND_CLASS ? 'wand' : '');
    if (item.line && cls !== 'armor' && !item.artifact) return item.line;
    const blessedState = item.blessed ? 'blessed ' : item.cursed ? 'cursed ' : 'uncursed ';
    const lineShowsBuc = /\b(?:blessed|uncursed|cursed)\b/.test(String(item.line || ''));
    const knownBlessCurseState = item.blessed || item.cursed ? knownBlessCursePrefix(item) : '';
    const knownState = knownBlessCurseState
        || (item.bknown === true || (item.bknown !== false && lineShowsBuc) ? blessedState : '');
    let name = pickupObjectName(item);
    let phrase = name;
    let suffix = '';

    if (cls === 'coin') return `$ - ${quan} gold piece${quan === 1 ? '' : 's'}`;
    if ((cls === 'armor' || (cls === 'tool' && kind === 'lenses'))
        && !name.startsWith('pair of ')
        && pairArticleObjectName(name))
        name = `pair of ${name}`;
    if (cls === 'tool' && kind === 'sack') name = `empty ${blessedState}sack`;
    if ((cls === 'wand' || item.tool === 'charges' || item.tool === 'magicMarker' || isChargedTool(item)) && item.spe != null && item.chargeKnown)
        name = `${name}${identifiedChargeSuffix(item)}`;

    if (cls === 'weapon' || item.wielded || item.alternate) {
        const spe = item.spe ?? 0;
        const lineShowsSpe = /(?:^| )[-+]\d+ /.test(String(item.line || ''));
        const showSpe = item.known === true || lineShowsSpe;
        const enchantment = showSpe ? `${spe >= 0 ? '+' : ''}${spe} ` : '';
        const buc = knownBlessCursePrefix(item)
            || ((item.bknown === true || lineShowsBuc) && (item.blessed || item.cursed) ? blessedState : '');
        const erosion = erosionPrefix(item);
        const parts = poisonedWeaponDisplayParts(item, name);
        phrase = quan > 1
            ? `${quan} ${buc}${parts.poisonPrefix}${erosion}${enchantment}${parts.name}`
            : `${buc}${parts.poisonPrefix}${erosion}${enchantment}${parts.name}`;
        if (item.wielded) {
            const hand = /quarterstaff|two-handed sword|battle-axe/.test(kind) ? 'hands' : `${game.u?.uhandedness || 'right'} hand`;
            suffix = ` (weapon in ${hand})`;
        } else if (item.alternate) {
            suffix = ` (alternate weapon${quan > 1 ? 's' : ''}; not wielded)`;
        } else if (item.quivered) {
            suffix = quiverSuffix(item);
        }
    } else if (cls === 'armor') {
        const rustproof = (game._startup_role || game.urole?.name?.m) === 'Samurai' && kind === 'splint mail' ? 'rustproof ' : '';
        const erosion = erosionPrefix(item);
        const lineShowsSpe = /(?:^| )[-+]\d+ /.test(String(item.line || ''));
        const showSpe = item.worn || item.line?.includes('being worn') || item.known === true || lineShowsSpe;
        const spe = showSpe && item.spe != null ? `${item.spe >= 0 ? '+' : ''}${item.spe} ` : '';
        phrase = `${knownState}${rustproof}${erosion}${spe}${name}`;
        if (item.worn || item.line?.includes('being worn')) suffix = ' (being worn)';
    } else if (cls === 'ring') {
        const chargedRing = item.charged || (item.roll || item.ringRoll || 99) <= 6;
        const enchantment = chargedRing && item.spe != null ? `${item.spe >= 0 ? '+' : ''}${item.spe} ` : '';
        phrase = `${knownState}${enchantment}${name}`;
        if (item.worn === 'right' || item.line?.includes('(on right hand)')) suffix = ' (on right hand)';
        else if (item.worn === 'left' || item.line?.includes('(on left hand)')) suffix = ' (on left hand)';
    } else if (cls === 'wand' || item.tool === 'charges' || item.tool === 'magicMarker') {
        phrase = name;
    } else {
        const namedWater = cls === 'potion' && kind === 'water' && (item.blessed || item.cursed);
        phrase = kind === 'sack' || kind === 'holy water' || kind === 'unholy water' || namedWater ? name : `${knownState}${name}`;
        if (isMeatRingObject(item)) {
            if (item.worn === 'right' || item.line?.includes('(on right hand)')) suffix = ' (on right hand)';
            else if (item.worn === 'left' || item.line?.includes('(on left hand)')) suffix = ' (on left hand)';
        } else if (item.quivered) suffix = quiverSuffix(item);
    }

    if (quan > 1 && cls !== 'weapon' && !item.wielded && !item.alternate) phrase = `${quan} ${phrase}`;
    else if (quan === 1) phrase = `${/^[aeiou]/i.test(phrase) ? 'an' : 'a'} ${phrase}`;
    return `${letter} - ${phrase}${suffix}`;
}

function wandChargeSuffix(item, charges = item?.spe) {
    if (charges == null) return '';
    const chargesKnown = item.chargeKnown || /\(\d+:-?\d+\)/.test(String(item.line || ''));
    return chargesKnown ? identifiedChargeSuffix(item, charges) : '';
}

function identifiedChargeSuffix(item, charges = item?.spe) {
    if (charges == null) return '';
    const recharged = Math.max(0, Math.trunc(Number(item?.recharged || 0)));
    return ` (${recharged}:${charges})`;
}

function learnObjectScore(section, name) {
    const key = `${section}:${name}`;
    game._known_object_score_types ??= new Set((game._discoveries || [])
        .filter(item => item.known !== false)
        .map(item => `${item.section}:${item.name}`));
    if (game._known_object_score_types.has(key)) return;
    game._known_object_score_types.add(key);
    if (game.u) game.u.urexp = (game.u.urexp || 0) + 10;
}

function recordKnownPotionDiscovery(item, potionName) {
    const name = String(potionName || '').replace(/^potion:/, '').replace(/^potion of /, '');
    if (!name) return;
    const discoveryName = `potion of ${name}`;
    const index = item?.potionIndex ?? IDENTIFIED_POTION_NAMES.indexOf(name);
    const appearance = game._object_descriptions?.potions?.[index]?.description
        || String(item?.kind || '').replace(/ potion$/, '');
    const text = appearance ? `${discoveryName} (${appearance})` : discoveryName;
    game._discoveries ??= [];
    if (appearance) {
        const observedText = `potion (${appearance})`;
        game._discoveries = game._discoveries.filter(entry =>
            !(entry.section === 'Potions' && entry.name === 'potion' && entry.text === observedText));
    }
    const existing = game._discoveries.find(entry => entry.section === 'Potions' && entry.name === discoveryName);
    if (existing) {
        existing.text = text;
        existing.starred = false;
        existing.known = true;
        return;
    }
    game._discoveries.push({ section: 'Potions', name: discoveryName, text, starred: false, known: true });
}

function setTurnTailMessage(message) {
    const text = String(message || '');
    if (!text) return;
    const width = game.nhDisplay?.cols || 80;
    if (game._pending_message && game._pending_message.length + text.length + 3 < width - 8) {
        game._pending_message = `${game._pending_message}  ${text}`;
    } else if (game._pending_message) {
        game._message_more = 1;
        game._process_time_with_more = 0;
        game._topline_after_more = text;
    } else {
        game._pending_message = text;
        game._message_more = 0;
    }
    game._keep_pending_message = 1;
}

function itemKindText(item) {
    return String(item?.actualKind || item?.kind || item?.artifact || item?.name || '').toLowerCase();
}

function isBellOfOpeningItem(item) {
    const text = itemKindText(item);
    return text === 'bell of opening' || item?.otyp === BELL && text === 'silver bell';
}

function identifyBellOfOpening(item) {
    if (!item) return;
    item.known = true;
    item.dknown = true;
    item.kind = 'Bell of Opening';
    item.actualKind = 'bell of opening';
    recordKnownToolDiscovery('Bell of Opening');
    updateChargedItemLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
}

function applyBellOfOpening(item) {
    const messages = [`You ring the ${pickupObjectName(item)}.`];
    if (game.u?.underwater || game.u?.uunderwater) {
        messages.push('But the sound is muffled.');
        return messages;
    }

    const invoking = heroOnInvocationSquare();
    if (chargedUsageChargeCount(item) <= 0) {
        if (invoking) {
            messages.push('But it makes no sound.');
            identifyBellOfOpening(item);
        }
        return messages;
    }

    const bellName = pickupObjectName(item);
    spendChargedToolUse(item, messages);
    if (item.unpaid) syncUnpaidBillLine(item);

    if (item.cursed) {
        messages.push('Nothing happens.');
    } else if (invoking) {
        messages.push(`The ${bellName} issues an unsettling shrill sound...`);
        item.age = game.moves || 0;
        identifyBellOfOpening(item);
    } else {
        messages.push('Nothing happens.');
    }
    return messages;
}

function isCandelabrumOfInvocationItem(item) {
    return item?.otyp === CANDELABRUM_OF_INVOCATION || itemKindText(item) === 'candelabrum of invocation';
}

function heroOnStairs() {
    const x = game.u?.ux;
    const y = game.u?.uy;
    if (x == null || y == null) return false;
    const loc = game.level?.at?.(x, y);
    if (loc?.typ === STAIRS || loc?.typ === LADDER || loc?.ladder) return true;
    return (game.level?.dnstair?.x === x && game.level?.dnstair?.y === y)
        || (game.level?.upstair?.x === x && game.level?.upstair?.y === y);
}

function heroOnInvocationSquare() {
    if (heroOnStairs()) return false;
    const x = game.u?.ux;
    const y = game.u?.uy;
    if (x == null || y == null) return false;
    const inv = game.level?.invocationPosition || game.level?.inv_pos;
    if (inv?.x === x && inv?.y === y) return true;
    return (game.level?.traps || []).some(trap =>
        trap && trap.tx === x && trap.ty === y && trap.ttyp === VIBRATING_SQUARE);
}

function inventoryInvocationItems() {
    const inventory = game.inventory || [];
    const bell = inventory.find(isBellOfOpeningItem) || null;
    const candelabrum = inventory.find(isCandelabrumOfInvocationItem) || null;
    return { bell, candelabrum };
}

function bellRecentlyRungForInvocation(bell) {
    if (!bell) return false;
    const age = bell.age ?? bell.invocationAge ?? bell._invocation_rung_turn;
    return age != null && (game.moves || 0) - age < 5;
}

function candelabrumPrimedForInvocation(candelabrum) {
    return !!candelabrum && (candelabrum.spe ?? 0) === 7 && !!(candelabrum.lamplit || candelabrum.burning);
}

function performInvocationArea(messages) {
    messages.push('The floor shakes violently under you!');
    const x = game.u?.ux;
    const y = game.u?.uy;
    if (x == null || y == null) return;

    let wallNearby = false;
    for (let dx = -6; dx <= 6 && !wallNearby; dx++) {
        for (let dy = -5; dy <= 5; dy++) {
            const loc = game.level?.at?.(x + dx, y + dy);
            if (loc && IS_WALL(loc.typ)) {
                wallNearby = true;
                break;
            }
        }
    }
    if (wallNearby) messages.push('The walls around you begin to bend and crumble!');

    game.level.traps = (game.level?.traps || []).filter(trap =>
        !(trap.tx === x && trap.ty === y && trap.ttyp === VIBRATING_SQUARE));
    const loc = game.level?.at?.(x, y);
    if (loc) {
        loc.typ = STAIRS;
        loc.ladder = 2;
    }
    if (game.level) game.level.dnstair = { x, y };
    newsym(x, y);
    messages.push('You are standing at the top of a stairwell leading down!');
}

function deadbookCanSeeMonster(mon) {
    if (!mon || mon.dead || (mon.mhp ?? 1) <= 0 || mon.mx == null || mon.my == null) return false;
    if (!game.viz_array) return true;
    return couldsee(mon.mx, mon.my);
}

function isDeadbookUndead(mon) {
    const data = mon?.data || {};
    const name = String(data.name || mon?.name || '').toLowerCase();
    const mlet = data.mlet || mon?.mlet || data.glyph || mon?.glyph;
    return mlet === 'L' || mlet === 'M' || mlet === 'V' || mlet === 'W' || mlet === 'Z'
        || mlet === "'" || mlet === 'ghost' || data.vampshifter
        || /\b(?:ghost|shade|lich|mummy|zombie|vampire|wraith|nazgul|skeleton|ghoul)\b/.test(name);
}

function pacifyUndeadForDeadbook() {
    for (const mon of game.level?.monsters || []) {
        if (!isDeadbookUndead(mon) || !deadbookCanSeeMonster(mon)) continue;
        mon.mpeaceful = 1;
        const mal = mon.data?.maligntyp || 0;
        const align = game.u?.ualign?.type || 0;
        const close = Math.max(Math.abs((mon.mx || 0) - (game.u?.ux || 0)),
            Math.abs((mon.my || 0) - (game.u?.uy || 0))) < 4;
        if (Math.sign(mal) === Math.sign(align) && close) {
            if (mon.mtame || mon.pet) mon.mtame = Math.min(20, (mon.mtame || 0) + 1);
            else {
                mon.pet = true;
                mon.mtame = 10;
                ensurePetExtension(mon);
            }
            mon.mflee = 0;
            mon.mfleetim = 0;
        } else {
            mon.mflee = 1;
            mon.mfleetim = 0;
            clearMonsterTrack(mon);
        }
        set_malign(mon);
        newsym(mon.mx, mon.my);
    }
}

function isCorpseItem(item) {
    return item?.otyp === CORPSE || item?.otyp === 'corpse';
}

function isEggItem(item) {
    return item?.otyp === EGG || itemKindText(item) === 'egg';
}

function eggMonsterName(item) {
    return String(item?.corpsenm?.name || '').toLowerCase();
}

function isPyroliskEgg(item) {
    return isEggItem(item) && eggMonsterName(item) === 'pyrolisk';
}

function isTouchPetrifyingEgg(item) {
    return isEggItem(item) && ['chickatrice', 'cockatrice'].includes(eggMonsterName(item));
}

function supportsHeroThrownEggUpwardHit(item) {
    return isEggItem(item) && !isTouchPetrifyingEgg(item) && !isPyroliskEgg(item);
}

function isPetrifyingEgg(item) {
    return isEggItem(item) && ['chickatrice', 'cockatrice', 'medusa'].includes(eggMonsterName(item));
}

function isStaleEggItem(item) {
    if (!isEggItem(item)) return false;
    if (item.age == null) item.age = game.moves || 1;
    return (game.moves || 1) - item.age > 2 * MAX_EGG_HATCH_TIME;
}

function shouldUseGenericRottenEggPath(item) {
    if (!isEggItem(item)) return false;
    if (item.age == null) item.age = game.moves || 1;
    pretouchFoodForRottenCheck(item);
    if (item.cursed) return true;
    const ageLimit = item.blessed ? 50 : 30;
    return (game.moves || 1) - item.age > ageLimit && (item.orotten || !rn2(7));
}

function shouldUseGenericRottenFoodPath(item) {
    if (!item || isEggItem(item) || isCorpseItem(item) || itemKindText(item) === 'fortune cookie') return false;
    const kind = itemKindText(item).replace(/^partly eaten /, '');
    if (!ROTTABLE_NON_CORPSE_FOODS.has(kind)) return false;
    if (item.age == null) item.age = game.moves || 1;
    pretouchFoodForRottenCheck(item);
    if (item.cursed) return true;
    const ageLimit = item.blessed ? 50 : 30;
    return (game.moves || 1) - item.age > ageLimit && (item.orotten || !rn2(7));
}

function rottenFoodEffect({ adjective = 'Rotten', foodWord = 'food' } = {}) {
    let rottenSleepDuration = 0;
    let message = `Blecch!  ${adjective} ${foodWord}!`;
    if (!rn2(4)) {
        const confusionDuration = d(2, 4);
        if (game.u) game.u._confusionTimeout = (game.u._confusionTimeout || 0) + confusionDuration;
    } else if (!game.u?.blind && !rn2(4)) {
        d(2, 10);
        if (game.u) game.u.blind = true;
        message += '  Everything suddenly goes dark.';
    } else if (!rn2(3)) {
        rottenSleepDuration = rnd(10);
        game._helpless_time = Math.max(game._helpless_time || 0, rottenSleepDuration);
        game._sleeping_time = Math.max(game._sleeping_time || 0, rottenSleepDuration + 1);
        game._wake_message = 'You are conscious again.';
        game._hear_again_after_wake = 1;
        if (game.u)
            game.u._deafTimeout = Math.max(game.u._deafTimeout || 0, Math.max(0, rottenSleepDuration - 1));
        message += '  The world spins and goes dark.';
    }
    return { message, rottenSleepDuration };
}

function partialRottenFood(item, floorObject = false) {
    const touched = touchEatenFood(item, floorObject);
    if (!touched) return touched;
    recordFoodConduct(touched);
    touched.orotten = true;
    consumeOeaten(touched, 1);
    if (!floorObject) refreshInventoryObjectLine(touched);
    else newsym(touched.ox, touched.oy);
    return touched;
}

function consumeTouchedFood(item, floorObject = false) {
    if (floorObject) consumeOneFloorObject(item);
    else consumeOneInventoryFood(item);
}

function isRoyalJelly(item) {
    return item?.otyp === LUMP_OF_ROYAL_JELLY || objectKindKey(item).replace(/^partly eaten /, '') === 'lump of royal jelly';
}

function isGlobFood(item) {
    return !!item?.globby || GLOB_TYPE_BY_OTYP.has(item?.otyp);
}

function globFoodMonsterName(item) {
    if (item?.corpsenm?.name) return item.corpsenm.name;
    const type = GLOB_TYPE_BY_OTYP.get(item?.otyp);
    if (type) return type.name.replace(/^glob of /, '');
    const text = objectKindKey(item).replace(/^partly eaten\s+/, '');
    return text.match(/^glob of (.+)$/)?.[1]
        || text.match(/^(?:small|medium|large|very large) glob of (.+)$/)?.[1]
        || '';
}

const GLOB_CPOSTFX_INTRINSICS = new Map([
    ['gray ooze', {
        mlevel: 3,
        intrinsics: ['fireResistance', 'coldResistance', 'poisonResistance'],
    }],
    ['brown pudding', {
        mlevel: 5,
        intrinsics: ['coldResistance', 'shockResistance', 'poisonResistance'],
    }],
    ['green slime', {
        mlevel: 6,
        intrinsics: ['acidResistance', 'stoneResistance'],
    }],
    ['black pudding', {
        mlevel: 10,
        intrinsics: ['coldResistance', 'shockResistance', 'poisonResistance'],
    }],
]);

function heroHasAcidResistance() {
    const form = game.u?._polyself_form || {};
    return !!(game.u?.acidResistance || game.u?._acidResistanceTimeout
        || form.acidResistance || form.resistsAcid || heroWearsAlchemySmock());
}

function heroSlimeproof() {
    const form = game.u?._polyself_form || {};
    const name = String(form.name || '').toLowerCase();
    return name === 'green slime' || !!(form.flaming || form.noncorporeal);
}

function startHeroSliming() {
    if (!game.u || game.u._slimingTimeout || heroHasUnchanging() || heroSlimeproof()) return false;
    game.u._slimingTimeout = 10;
    game.u.sliming = true;
    addHeroStatusSuffix('Slime');
    return true;
}

function fixHeroPetrificationFromAcidicFood() {
    return fixHeroPetrification();
}

function globCpostfxIntrinsicForMonster(monsterName) {
    const spec = GLOB_CPOSTFX_INTRINSICS.get(monsterName);
    if (!spec) return null;
    let count = 0;
    let selected = null;
    for (const intrinsic of spec.intrinsics) {
        count++;
        if (!rn2(count)) selected = intrinsic;
    }
    return selected ? { intrinsic: selected, mlevel: spec.mlevel } : null;
}

function globIntrinsicGrantSucceeds(intrinsic, mlevel) {
    if (mlevel > rn2(15)) return true;
    if (intrinsic === 'acidResistance') return mlevel > rn2(3);
    if (intrinsic === 'stoneResistance') return mlevel > rn2(6);
    return false;
}

function addTemporaryResistance(prop, duration) {
    if (!game.u) return;
    const timeoutField = prop === 'acidResistance' ? '_acidResistanceTimeout' : '_stoneResistanceTimeout';
    const baseField = prop === 'acidResistance' ? '_acidResistanceBase' : '_stoneResistanceBase';
    if (!(game.u[timeoutField] > 0)) game.u[baseField] = !!game.u[prop];
    game.u[timeoutField] = (game.u[timeoutField] || 0) + duration;
    game.u[prop] = true;
}

function applyGlobCpostfxIntrinsic(monsterName) {
    const choice = globCpostfxIntrinsicForMonster(monsterName);
    if (!choice || !globIntrinsicGrantSucceeds(choice.intrinsic, choice.mlevel)) return '';
    switch (choice.intrinsic) {
    case 'fireResistance':
        if (game.u?._corpseFireResistance) return '';
        game.u.fireResistance = true;
        game.u._corpseFireResistance = true;
        return heroIsHallucinating() ? "You be chillin'." : 'You feel a momentary chill.';
    case 'coldResistance':
        if (game.u?._corpseColdResistance) return '';
        game.u.coldResistance = true;
        game.u._corpseColdResistance = true;
        return 'You feel full of hot air.';
    case 'shockResistance':
        if (game.u?._corpseShockResistance) return '';
        game.u.shockResistance = true;
        game.u._corpseShockResistance = true;
        return heroIsHallucinating()
            ? 'You feel grounded in reality.'
            : 'Your health currently feels amplified!';
    case 'poisonResistance':
        if (game.u?._corpsePoisonResistance) return '';
        const alreadyPoisonResistant = !!game.u?.poisonResistance;
        game.u.poisonResistance = true;
        game.u._corpsePoisonResistance = true;
        return alreadyPoisonResistant ? 'You feel especially healthy.' : 'You feel healthy.';
    case 'acidResistance': {
        const alreadyAcidResistant = heroHasAcidResistance();
        addTemporaryResistance('acidResistance', d(3, 6));
        return alreadyAcidResistant ? '' : 'You feel less concerned about being harmed by acid.';
    }
    case 'stoneResistance': {
        const alreadyStoneResistant = !!game.u?.stoneResistance;
        addTemporaryResistance('stoneResistance', d(3, 6));
        return alreadyStoneResistant ? '' : 'You feel less concerned about becoming petrified.';
    }
    default:
        return '';
    }
}

function heroHasUnchanging() {
    return !!game.u?.unchanging || (game.inventory || []).some(item =>
        item.worn && /amulet of unchanging|unchanging/i.test(String(item.kind || item.actualKind || item.line || '')));
}

function adjustHeroStrengthFromRoyalJelly(cursed) {
    const stats = game.u?.acurr?.a;
    if (!stats) return '';
    const before = stats[A_STR] ?? 10;
    const after = Math.max(3, Math.min(125, before + (cursed ? -1 : 1)));
    stats[A_STR] = after;
    if (!cursed && game.u?.amax?.a) game.u.amax.a[A_STR] = Math.max(game.u.amax.a[A_STR] || after, after);
    if (after === before) return '';
    return cursed ? 'You feel weak!' : 'You feel strong!';
}

function healWoundedLegsFromRoyalJelly() {
    if (!game.u) return;
    if (game.u._woundedDexPenalty && game.u.acurr?.a)
        game.u.acurr.a[A_DEX] = (game.u.acurr.a[A_DEX] || 9) + 1;
    game.u._woundedDexPenalty = 0;
    game.u._woundedLegTurns = 0;
    game.u._woundedLegSide = '';
}

function postRehumanizePetrifyingSelfTouchMessage(lostStoneResistance) {
    if (!lostStoneResistance || !game.u || game.u.stoneResistance || wornGlovesItem()) return null;
    const corpse = (game.inventory || []).find(item =>
        isPetrifyingCorpseObject(item)
        && (itemIsWielded(item) || item.alternate || item.line?.includes('alternate weapon')));
    if (!corpse) return null;
    const name = corpseMonsterName(corpse) || 'cockatrice';
    game.u.uhp = 0;
    game._death_cause = `petrified by ${petrifyingCorpseArticleName(corpse)}`;
    game._death_bones_body = 'statue';
    return {
        messages: [
            `No longer petrify-resistant, you touch the ${name} corpse.`,
            'You turn to stone...',
        ],
        died: true,
        suppressDieMessage: true,
        clearStoningOnLifeSaving: true,
    };
}

function rehumanizeAfterPolyselfDeath() {
    const result = { messages: [], died: false };
    if (!game.u) return result;
    const base = game.u._polyself_base || {};
    const lostStoneResistance = heroPolyselfResistsStoning() && !game.u.stoneResistance;
    const hpmax = Math.max(1, base.uhpmax ?? game.u.uhpmax ?? 1);
    game.u.uhpmax = hpmax;
    game.u.uhp = Math.min(base.uhp ?? hpmax, hpmax);
    if (base.uenmax != null) game.u.uenmax = base.uenmax;
    if (base.uen != null) game.u.uen = Math.max(0, Math.min(base.uen, game.u.uenmax ?? base.uen));
    if (base.uac != null) game.u.uac = base.uac;
    if (base.ulevel != null) game.u.ulevel = base.ulevel;
    if (base.uhpinc) game.u.uhpinc = [...base.uhpinc];
    if (base.ueninc) game.u.ueninc = [...base.ueninc];
    if (base.rank && game.urole) game.urole.rank = base.rank;
    game.u._glyph = null;
    game.u._glyphColor = undefined;
    game.u._monsterHd = null;
    game.u._monsterMove = null;
    game.u._polyself_form = null;
    game.u._polyself_base = null;
    game.u.mh = null;
    game.u.mhmax = null;
    game.u.umovement = NORMAL_SPEED;
    game.u._statusSuffix = '';
    game.u._strDisplay = null;
    const raceAdj = game.urace?.adj || game.urace?.noun || game._startup_race || 'human';
    result.messages.push(`You return to ${raceAdj} form!`);
    if ((game.u.uhp || 0) < 1) {
        result.messages.push('Your old form was not healthy enough to survive.');
        game._death_cause = `killed by reverting to unhealthy ${raceAdj} form`;
        result.died = true;
    }
    if (!result.died) {
        const selfTouch = postRehumanizePetrifyingSelfTouchMessage(lostStoneResistance);
        if (selfTouch) {
            result.messages.push(...selfTouch.messages);
            result.died = true;
            result.suppressDieMessage = selfTouch.suppressDieMessage;
            result.clearStoningOnLifeSaving = selfTouch.clearStoningOnLifeSaving;
        }
    }
    return result;
}

function royalJellyPostEffects(item) {
    if (game.u?._polyself_form?.name === 'killer bee' && !heroHasUnchanging()) {
        const result = becomeMonster('queen bee');
        return { messages: result?.message ? [result.message] : [], died: false };
    }

    const messages = [];
    const strengthMessage = adjustHeroStrengthFromRoyalJelly(!!item?.cursed);
    if (strengthMessage) messages.push(strengthMessage);

    if (game.u) {
        const hpDelta = rnd(20);
        if (item?.cursed) game.u.uhp = (game.u.uhp || 0) - hpDelta;
        else game.u.uhp = (game.u.uhp || 0) + hpDelta;
        if ((game.u.uhp || 0) > (game.u.uhpmax || 1)) {
            if (!rn2(17)) game.u.uhpmax = (game.u.uhpmax || 1) + 1;
            game.u.uhp = game.u.uhpmax || 1;
        } else if ((game.u.uhp || 0) <= 0) {
            if (game.u._polyself_form) {
                const result = rehumanizeAfterPolyselfDeath();
                messages.push(...result.messages);
                return { messages, died: result.died, suppressDieMessage: result.suppressDieMessage ?? result.died,
                    clearStoningOnLifeSaving: result.clearStoningOnLifeSaving };
            }
            game._death_cause = 'poisoned by a rotten lump of royal jelly';
            return { messages, died: true };
        }
    }

    if (!item?.cursed) healWoundedLegsFromRoyalJelly();
    return { messages, died: false };
}

async function finishRoyalJellyEating(item, floorObject, baseMessage, { more = false, processTimeWithMore = 0 } = {}) {
    recordFoodConduct(item);
    addHeroNutrition(remainingFoodNutrition(item));
    consumeTouchedFood(item, floorObject);
    game._pet_food_scan_inventory = game.inventory || [];
    const result = royalJellyPostEffects(item);
    const message = [baseMessage, ...result.messages].filter(Boolean).join('  ');
    if (result.died) {
        if (consumeLifeSavingAmulet({ clearStoning: !!result.clearStoningOnLifeSaving })) {
            if (game.u) game.u.uhp = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game._command_mode = 'lifeSavingMore';
            await setMessage(`${message}  You die...  But wait...  Your medallion begins to glow!`, true);
            return;
        }
        if (game.u) game.u.uhp = 0;
        game._pending_time_passed = 0;
        game.context.move = 0;
        game._process_command_time_now = 0;
        game._run_steps_remaining = 0;
        game._command_mode = 'deathDieMore';
        prepareDeathBones();
        const suppressDieMessage = result.suppressDieMessage
            || String(game._death_cause || '').startsWith('petrified by ');
        await setMessage(suppressDieMessage ? message : `${message}  You die...`, true);
        return;
    }
    await setMessage(message, more);
    if (processTimeWithMore) game._process_time_with_more = processTimeWithMore;
    game._command_mode = null;
    game.context.move = 1;
}

async function eatRoyalJelly(item, floorObject = false) {
    const touched = touchEatenFood(item, floorObject);
    const name = pickupObjectName({ ...(touched || {}), quan: 1 });
    await finishRoyalJellyEating(touched, floorObject, `This ${name} is delicious!`);
}

async function finishFatalEatingMessage(message) {
    if (consumeLifeSavingAmulet()) {
        if (game.u) game.u.uhp = 0;
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game._command_mode = 'lifeSavingMore';
        await setMessage(`${message}  You die...  But wait...  Your medallion begins to glow!`, true);
        return;
    }
    if (game.u) game.u.uhp = 0;
    game._pending_time_passed = 0;
    game.context.move = 0;
    game._process_command_time_now = 0;
    game._run_steps_remaining = 0;
    game._command_mode = 'deathDieMore';
    prepareDeathBones();
    await setMessage(`${message}  You die...`, true);
}

async function eatGlobFood(item, floorObject = false) {
    const touched = touchEatenFood(item, floorObject);
    const monsterName = globFoodMonsterName(touched);
    const messages = [];
    const name = pickupObjectName({ ...(touched || {}), quan: 1 });
    let tasted = false;
    recordFoodConduct(touched);
    addHeroNutrition(remainingFoodNutrition(touched));
    consumeTouchedFood(touched, floorObject);

    if (!heroHasAcidResistance()) {
        const damage = rnd(15);
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        messages.push('You have a very bad case of stomach acid.');
        if ((game.u?.uhp || 0) <= 0) {
            game._death_cause = 'killed by an acidic glob';
            game._pet_food_scan_inventory = game.inventory || [];
            await finishFatalEatingMessage(messages.join('  '));
            return;
        }
    } else if (monsterName === 'green slime' && rn2(5)) {
        messages.push('Ecch - that must have been poisonous!');
        if (!game.u?.poisonResistance) {
            const strengthDamage = rnd(4);
            const hpDamage = rnd(15);
            if (game.u?.acurr?.a)
                game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - strengthDamage);
            if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - hpDamage);
            if ((game.u?.uhp || 0) <= 0) {
                game._death_cause = 'poisoned by a poisonous glob';
                game._pet_food_scan_inventory = game.inventory || [];
                await finishFatalEatingMessage(messages.join('  '));
                return;
            }
        } else {
            messages.push('You seem unaffected by the poison.');
        }
    }

    if (!messages.length) {
        messages.push(`This ${name} tastes terrible!`);
        tasted = true;
    }

    if (monsterName === 'green slime' && startHeroSliming())
        messages.push("You don't feel very well.");
    const petrificationMessage = fixHeroPetrificationFromAcidicFood();
    if (petrificationMessage) messages.push(petrificationMessage);
    const cpostfxMessage = applyGlobCpostfxIntrinsic(monsterName);
    if (cpostfxMessage) messages.push(cpostfxMessage);
    if (!tasted && !messages.length) messages.push(`This ${name} tastes terrible!`);
    game._pet_food_scan_inventory = game.inventory || [];
    await setMessage(messages.join('  '), messages.length > 1);
    game._command_mode = null;
    game.context.move = 1;
}

function royalJellyRubTargetLetters() {
    return inventoryLetters(item => isEggItem(item));
}

function royalJellyRubPrompt() {
    const letters = royalJellyRubTargetLetters();
    if (!letters) return 'What do you want to rub the royal jelly on? [*]';
    return `What do you want to rub the royal jelly on? [${getobjPromptLetters(letters)} or ?*]`;
}

function isRubObjectCandidate(item) {
    return isLampObject(item) || isRoyalJelly(item) || isGrayStoneApplyItem(item);
}

function rubObjectLetters() {
    return inventoryLetters(isRubObjectCandidate);
}

function rubObjectPrompt() {
    const letters = rubObjectLetters();
    if (!letters) return 'What do you want to rub? [*]';
    return `What do you want to rub? [${getobjPromptLetters(letters)} or ?*]`;
}

function rubObjectMenuItems(ch) {
    const inventory = game.inventory || [];
    if (ch === '*') return inventory;
    return inventory.filter(isRubObjectCandidate);
}

async function beginRoyalJellyRub(item) {
    game._royal_jelly_rub_letter = item?.letter || '';
    await setMessage(royalJellyRubPrompt());
    game._command_mode = 'rubRoyalJellyTarget';
}

function eggQuiverVerb(egg) {
    return (egg?.quan || 1) > 1 ? 'quiver' : 'quivers';
}

function attachEggHatchTimer(item) {
    if (!isEggItem(item) || !item.corpsenm?.name || item.eggHatchTurn) return false;
    for (let i = 151; i <= 200; i++) {
        if (rnd(i) > 150) {
            item.eggHatchTurn = (game.moves || 1) + i;
            item._egg_hatch_seq = game._egg_hatch_timer_seq = (game._egg_hatch_timer_seq || 0) + 1;
            break;
        }
    }
    item._egg_hatch_consumed = true;
    return !!item.eggHatchTurn;
}

async function rubRoyalJellyOnEgg(jelly, egg) {
    const oldName = egg?.corpsenm?.name || '';
    const smearName = pickupObjectName(egg);
    if (oldName === 'killer bee')
        egg.corpsenm = monsterByRndName('queen bee') || RANDOM_MONSTER_BY_NAME.get('queen bee') || { name: 'queen bee', oviparous: true, female: true };
    const changedType = (egg?.corpsenm?.name || '') !== oldName;
    const effectName = pickupObjectName(egg);
    const messages = [`You smear royal jelly all over ${smearName}.`];

    if (jelly?.cursed) {
        if (egg.eggHatchTurn || changedType) messages.push(`The ${effectName} ${eggQuiverVerb(egg)} feebly.`);
        else messages.push('Nothing seems to happen.');
        delete egg.eggHatchTurn;
        delete egg._egg_hatch_seq;
        delete egg._egg_hatch_consumed;
    } else {
        const wasTimed = !!egg.eggHatchTurn;
        if (egg.corpsenm?.name) {
            if (!egg.eggHatchTurn) attachEggHatchTimer(egg);
            if (jelly?.blessed && !egg.spe) egg.spe = 2;
        }
        if ((egg.eggHatchTurn && !wasTimed) || egg.spe === 2 || changedType)
            messages.push(`The ${effectName} ${eggQuiverVerb(egg)} briefly.`);
        else messages.push('Nothing seems to happen.');
    }

    consumeOneInventoryFoodUsedUp(jelly);
    refreshInventoryObjectLine(egg);
    game._royal_jelly_rub_letter = '';
    game._pet_food_scan_inventory = game.inventory || [];
    await setMessage(messages.join('  '), true);
    game._command_mode = null;
    game.context.move = 1;
}

function eggMonsterFromHeroPolyself() {
    const form = polyselfForm();
    if (!form?.oviparous) return null;
    let name = form.name || '';
    if (!name) return null;
    const breederEgg = !rn2(77);
    if (!breederEgg && name === 'queen bee') name = 'killer bee';
    else if (!breederEgg && name === 'winged gargoyle') name = 'gargoyle';
    return monsterByRndName(name) || RANDOM_MONSTER_BY_NAME.get(name) || form;
}

function createHeroLaidEgg() {
    const eggMonster = eggMonsterFromHeroPolyself();
    const egg = mksobj(EGG, false, false);
    Object.assign(egg, {
        cls: 'food',
        glyph: '%',
        color: CLR_WHITE,
        kind: 'egg',
        singular: 'egg',
        plural: 'eggs',
        quan: 1,
        spe: 1,
        known: true,
        dknown: true,
        eggKnown: true,
        corpsenm: eggMonster,
        ox: game.u?.ux || 0,
        oy: game.u?.uy || 0,
    });
    if (eggMonster) consumeWishedEggHatchTimer(egg);
    game.level.objects ??= [];
    game.level.objects.push(egg);
    newsym(egg.ox, egg.oy);
    return egg;
}

function heroPolyselfEggsInWater() {
    const form = polyselfForm();
    return !!form?.oviparous && (form.mlet === ';' || form.glyph === ';') && !!form.swimmer;
}

async function sitLayEgg() {
    if (!game.flags?.female) {
        await setMessage("Males can't lay eggs!");
        game._command_mode = null;
        return true;
    }
    if ((game.u?.uhunger || 0) < (FOOD_NUTRITION.get('egg') || 80)) {
        await setMessage("You don't have enough energy to lay an egg.");
        game._command_mode = null;
        return true;
    }
    if (heroPolyselfEggsInWater()) {
        if (!(game.u?.underwater || game.u?.uunderwater || Is_waterlevel(game.u?.uz))) {
            await setMessage('A splash tetra you are not.');
            game._command_mode = null;
            return true;
        }
        const formName = polyselfForm()?.name || '';
        if (formName === 'giant eel' || formName === 'electric eel') {
            await setMessage('You yearn for the Sargasso Sea.');
            game._command_mode = null;
            return true;
        }
    }
    createHeroLaidEgg();
    if (game.u) game.u.uhunger = Math.max(0, (game.u.uhunger || 0) - (FOOD_NUTRITION.get('egg') || 80));
    await setMessage(heroPolyselfEggsInWater() ? 'You spawn an egg.' : 'You lay an egg.');
    game._command_mode = null;
    game.context.move = 1;
    return true;
}

function sitSurfaceName(loc) {
    if (!loc) return 'floor';
    if (loc.typ === FOUNTAIN) return 'fountain';
    if (loc.typ === ICE) return 'ice';
    if (loc.typ === TREE) return 'tree';
    if (loc.typ === LAVAPOOL || loc.typ === LAVAWALL) return 'lava';
    if (IS_POOL(loc.typ) || loc.typ === WATER || loc.typ === MOAT) return 'water';
    return 'floor';
}

function heroCanReachFloorForSit() {
    if (game.u?.uswallow) return { ok: false, message: 'There are no seats in here!' };
    if (game.u?.levitating && !Is_waterlevel(game.u?.uz)) return { ok: false, message: 'You tumble in place.' };
    return { ok: true, message: '' };
}

function finishSitMessage(message, { move = true, more = false } = {}) {
    game._command_mode = null;
    if (move) game.context.move = 1;
    return setMessage(message, more);
}

function floorObjectForSitting() {
    return (game.level?.objects || []).find(obj =>
        !obj.hidden && !obj.transientProjectile
        && obj.ox === game.u?.ux && obj.oy === game.u?.uy && obj.otyp !== BOULDER);
}

async function sitOnFloorObject(obj) {
    const messages = [];
    const kind = objectKindKey(obj);
    const form = polyselfForm() || {};
    const corpse = obj.otyp === 'corpse' || obj.otyp === CORPSE;
    const objectName = corpse ? 'corpse' : pickupObjectName(obj);
    if ((form.mlet === 'D' || form.glyph === 'D') && (obj.cls === 'coin' || obj.glyph === '$')) {
        const floorGold = obj.quan || 0;
        const meager = floorGold + (game._goldCount || 0) < (game.u?.ulevel || 1) * 1000;
        messages.push(`You coil up around your ${meager ? 'meager ' : ''}hoard.`);
    } else if (kind === 'towel') {
        messages.push("It's probably not a good time for a picnic...");
    } else {
        messages.push(form.slithy ? `You coil up around the ${objectName}.` : `You sit on the ${objectName}.`);
        if (corpse && obj.corpsenm?.amorphous) {
            messages.push("It's squishy...");
        } else if (obj.otyp === CREAM_PIE || kind === 'cream pie') {
            if (!heroIsDeaf()) messages.push('Squelch!');
            game.level.objects = (game.level?.objects || []).filter(other => other !== obj);
            newsym(obj.ox, obj.oy);
        } else if (!isBoxObject(obj) && obj.otyp !== ICE_BOX && kind !== 'towel'
            && !/\b(?:cloth|cloak|robe|shirt|smock|apron|blanket)\b/.test(kind)) {
            messages.push("It's not very comfortable...");
        }
    }
    await finishSitMessage(messages.join('  '));
    return true;
}

function sitTrapState() {
    const type = game.u?.utraptype;
    if (type === TT_BEARTRAP || type === 'beartrap') return 'beartrap';
    if (type === TT_PIT || type === 'pit') return 'pit';
    if (type === TT_WEB || type === 'web') return 'web';
    if (type === TT_LAVA || type === 'lava') return 'lava';
    if (type === TT_INFLOOR || type === 'infloor') return 'infloor';
    if (type === TT_BURIEDBALL || type === 'buriedball') return 'buriedball';
    return '';
}

async function sitWhileAlreadyTrapped(trap) {
    const trapped = sitTrapState();
    if (!trapped || !(game.u?.utrap > 0)) return false;
    let message = '';
    if (trapped === 'beartrap') {
        game.u.utrap++;
        message = "You can't sit down with your foot in the bear trap.";
    } else if (trapped === 'pit') {
        if (trap?.ttyp === SPIKED_PIT) {
            message = 'You sit down on a spike.  Ouch!';
            if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - 1);
            exerciseAttribute(A_STR, false);
        } else {
            message = 'You sit down in the pit.';
        }
        game.u.utrap += rn2(5);
    } else if (trapped === 'web') {
        game.u.utrap += rn1(10, 5);
        message = 'You sit in the spider web and get entangled further!';
    } else if (trapped === 'lava') {
        game.u.utrap += rnd(4);
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - d(2, 10));
        message = 'You sit in the lava!';
    } else {
        game.u.utrap++;
        message = "You can't maneuver to sit!";
    }
    exerciseAttribute(A_WIS, false);
    await finishSitMessage(message);
    return true;
}

function deleteTrap(trap) {
    game.level.traps = (game.level?.traps || []).filter(item => item !== trap);
    newsym(game.u?.ux || 0, game.u?.uy || 0);
}

function sitTrapArticleName(trap) {
    const name = TRAP_NAMES[trap?.ttyp] || 'trap';
    if (trap?.madeby_u) return `your ${name}`;
    return `${/^[aeiou]/i.test(name) ? 'an' : 'a'} ${name}`;
}

function trapMessage(...parts) {
    return parts.filter(Boolean).join('  ');
}

function sitTrapEscapeAllowed(trap) {
    return ![ANTI_MAGIC, MAGIC_PORTAL, VIBRATING_SQUARE].includes(trap?.ttyp);
}

function sitTrapNote(trap) {
    const notes = [
        'C note', 'D flat', 'D note', 'E flat', 'E note', 'F note',
        'F sharp', 'G note', 'G sharp', 'A note', 'B flat', 'B note',
    ];
    const note = notes[trap?.tnote] || notes[0];
    return `${/^[aeiou]/i.test(note) ? 'an' : 'a'} ${note}`;
}

function articleForName(name) {
    return `${/^[aeiou]/i.test(name) ? 'an' : 'a'} ${name}`;
}

const WEBMAKER_FORM_NAMES = new Set(['cave spider', 'giant spider']);
const WEB_FLAMING_FORM_NAMES = new Set(['fire vortex', 'flaming sphere', 'fire elemental', 'salamander']);
const WEB_ACIDIC_FORM_NAMES = new Set(['acid blob', 'gelatinous cube']);
const WEB_WHIRLY_FORM_NAMES = new Set(['fog cloud', 'dust vortex', 'ice vortex', 'energy vortex', 'steam vortex', 'fire vortex', 'air elemental']);

function polyselfFormName() {
    return String(polyselfForm()?.name || '').toLowerCase();
}

function heroPassesRocks() {
    const form = polyselfForm();
    return !!(form?.passWalls && !form?.unsolid && !form?.noncorporeal);
}

function heroWebmakerForm() {
    const form = polyselfForm();
    const name = polyselfFormName();
    return !!(form?.webmaker || WEBMAKER_FORM_NAMES.has(name));
}

function heroWebDestructionVerb() {
    const form = polyselfForm();
    const name = polyselfFormName();
    if (form?.flaming || WEB_FLAMING_FORM_NAMES.has(name)) return 'burn';
    if (form?.acidic || WEB_ACIDIC_FORM_NAMES.has(name)) return 'dissolve';
    return '';
}

function heroFlowsThroughWeb() {
    const form = polyselfForm();
    const name = polyselfFormName();
    return !!(form?.amorphous || form?.whirly || form?.unsolid || form?.noncorporeal
        || WEB_WHIRLY_FORM_NAMES.has(name) || name === 'gelatinous cube');
}

function sameDungeonLevel(a, b) {
    return !!a && !!b && a.dnum === b.dnum && a.dlevel === b.dlevel;
}

function invocationLevel(level = game.u?.uz) {
    if (!level) return false;
    const dungeon = game.dungeons?.[level.dnum];
    return game.dungeons?.[level.dnum]?.name === 'Gehennom'
        && (level.dlevel ?? 0) === Math.max(1, (dungeon?.num_dunlevs ?? 1) - 1);
}

function canDigDownFromLevel(level = game.u?.uz) {
    return !!level && !game.level?.flags?.hardfloor
        && !Is_botlevel(level) && !invocationLevel(level);
}

function canFallThroughLevel(level = game.u?.uz) {
    return canDigDownFromLevel(level) || Is_stronghold(level);
}

function findHellTargetLevel() {
    return specialLevel('valley') || branchLevel(1) || null;
}

function clampDungeonLevel(level) {
    if (!level) return null;
    if ((level.dnum != null && level.dnum < 0) || (level.dlevel != null && level.dlevel < 0)) return null;
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    const dnum = level.dnum ?? current.dnum;
    const dungeon = game.dungeons?.[dnum];
    const bottom = Math.max(1, dungeon?.num_dunlevs ?? level.dlevel ?? current.dlevel);
    return {
        dnum,
        dlevel: Math.max(1, Math.min(level.dlevel ?? current.dlevel, bottom)),
    };
}

function scheduleSitLevelChange(targetLevel, options = {}) {
    if (!targetLevel) return false;
    game._deferred_level_goto = {
        targetLevel: { dnum: targetLevel.dnum, dlevel: targetLevel.dlevel },
        options,
    };
    return true;
}

function webTrapTimeFromStrength() {
    const str = game.u?.acurr?.a?.[A_STR] ?? 10;
    if (str <= 3) return rn1(6, 6);
    if (str < 6) return rn1(6, 4);
    if (str < 9) return rn1(4, 4);
    if (str < 12) return rn1(4, 2);
    if (str < 15) return rn1(2, 2);
    if (str < 18) return rnd(2);
    if (str < 69) return 1;
    return 0;
}

function sitWebTrapMessage(trap, prefix) {
    trap.tseen = true;
    const webName = trap.madeby_u ? 'your spider web' : 'a spider web';
    const destroyVerb = heroWebDestructionVerb();
    if (destroyVerb) {
        deleteTrap(trap);
        return `${prefix}  You ${destroyVerb} ${webName}!`;
    }
    if (heroFlowsThroughWeb()) return `${prefix}  You flow through ${webName}.`;
    if (heroWebmakerForm())
        return `${prefix}  ${trap.madeby_u ? 'You take a walk on your web.' : 'There is a spider web here.'}`;
    const tim = webTrapTimeFromStrength();
    if (game.u) {
        game.u.utrap = tim;
        game.u.utraptype = tim > 0 ? 'web' : null;
    }
    if (tim <= 0) {
        deleteTrap(trap);
        return `${prefix}  You tear through ${trap.madeby_u ? 'your web' : 'a web'}!`;
    }
    return `${prefix}  You are caught by ${sitTrapArticleName(trap)}!`;
}

function sitFallTargetLevel(trap) {
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    if (Is_stronghold(current)) return findHellTargetLevel();
    const rawTarget = trap?.dst || { dnum: current.dnum, dlevel: current.dlevel + 1 };
    const target = clampDungeonLevel(rawTarget);
    return sameDungeonLevel(target, current) ? null : target;
}

function sitFallThroughTrapResult(trap, prefix) {
    trap.tseen = true;
    const current = game.u?.uz || { dnum: 0, dlevel: 1 };
    if ((game.u?.blind || game.u?.Blind) && game.u?.levitating && !In_sokoban(current))
        return { message: prefix, more: false };
    if (!canFallThroughLevel(current))
        return { message: prefix, more: false };

    const sokobanFall = In_sokoban(current) && canFallThroughLevel(current);
    const messages = sokobanFall
        ? [prefix, `Air currents pull you down into ${trap.madeby_u ? 'your' : 'a'} ${trap.ttyp === TRAPDOOR ? 'trap door' : 'hole'}!`]
        : [prefix, trap.ttyp === TRAPDOOR
            ? 'A trap door opens up under you!'
            : "There's a gaping hole under you!"];
    const target = sitFallTargetLevel(trap);
    const form = polyselfForm() || {};
    if (!target) {
        messages.push("You don't fall in.");
        return { message: trapMessage(...messages), more: false };
    }
    if (!sokobanFall && (form.huge || form.msize === 'huge' || form.size === 'huge')) {
        messages.push("You don't fit through.");
        const impact = impactDropFloorObjects(game.u?.ux || 0, game.u?.uy || 0, trap, { targetLevel: target });
        if (impact.message) messages.push(impact.message);
        return { message: trapMessage(...messages), more: false };
    }
    if (!sokobanFall && (game.u?.levitating || game.u?.flying || game.u?.ustuck
        || form.clinger || (form.ceilingHider && game.u?.uundetected))) {
        messages.push("You don't fall in.");
        const impact = impactDropFloorObjects(game.u?.ux || 0, game.u?.uy || 0, trap, { targetLevel: target });
        if (impact.message) messages.push(impact.message);
        return { message: trapMessage(...messages), more: false };
    }
    const dist = Math.max(1, depth_of_level(target) - depth_of_level(current));
    if (dist > 1) {
        const depth = dist > 3 ? 'very deep ' : dist > 2 ? 'deep ' : '';
        messages.push(`You fall down a ${depth}shaft!`);
    }
    const impact = impactDropFloorObjects(game.u?.ux || 0, game.u?.uy || 0, trap, { targetLevel: target, withHero: true });
    if (impact.message) messages.push(impact.message);
    scheduleSitLevelChange(target, {
        falling: true,
        fallingDamage: !game.u?.flying,
        arrivalMessage: '',
        impactDroppedObjects: impact.objects,
    });
    return { message: trapMessage(...messages), more: true };
}

function levelTeleportTrapFollowupMessage() {
    if (heroIsHallucinating() || heroHasTeleportControl())
        return `You briefly feel ${heroIsHallucinating() ? 'oriented' : 'centered'}.`;
    const evenMore = heroIsConfused();
    addHeroConfusion(3);
    return `You feel ${evenMore ? 'even more ' : ''}disoriented.`;
}

function consumePendingLevelTeleportTrapFollowup() {
    if (!game._level_teleport_trap_followup) return '';
    game._level_teleport_trap_followup = 0;
    return levelTeleportTrapFollowupMessage();
}

function levelTeleportOptionsWithTrapFollowup(options = {}) {
    const postMessage = consumePendingLevelTeleportTrapFollowup();
    if (!postMessage) return options;
    return {
        ...options,
        postMessage: options.postMessage ? `${options.postMessage}  ${postMessage}` : postMessage,
    };
}

function grantEndgamePrerequisiteIfNeeded(targetLevel) {
    if (!targetLevel || !In_endgame(targetLevel) || In_endgame(game.u?.uz) || heroHasAmuletOfYendor())
        return '';
    const amulet = makeRealAmuletOfYendorWishObject();
    const letter = nextInventoryLetter();
    Object.assign(amulet, {
        letter,
        line: `${letter} - the Amulet of Yendor`,
    });
    game.inventory ??= [];
    game.inventory.push(amulet);
    game.u ??= {};
    game.u.uhave ??= {};
    game.u.uhave.amulet = 1;
    game._discoveries ??= [];
    if (!game._discoveries.some(entry => entry.section === 'Amulets' && entry.name === 'amulet (Amulet of Yendor)'))
        game._discoveries.push({
            section: 'Amulets',
            name: 'amulet (Amulet of Yendor)',
            text: 'amulet (Amulet of Yendor)',
            starred: false,
            known: true,
        });
    return `Endgame prerequisite: ${letter} - the Amulet of Yendor.`;
}

async function captureCurrentGridSnapshot() {
    const saved = {
        pendingMessage: game._pending_message,
        messageMore: game._message_more,
        messageMoreLine: game._message_more_line,
        keepPendingMessage: game._keep_pending_message,
        overlayLines: game._overlay_lines,
        overlayHideStatus: game._overlay_hide_status,
        overlayHideStatusOnly: game._overlay_hide_status_only,
        commandMode: game._command_mode,
    };
    game._pending_message = '';
    game._message_more = 0;
    game._message_more_line = '';
    game._keep_pending_message = 0;
    game._overlay_lines = null;
    game._overlay_hide_status = 0;
    game._overlay_hide_status_only = 0;
    game._command_mode = null;
    await flush_screen(1);
    const term = game.nhDisplay?.terminal || game.nhDisplay;
    const snapshot = term?.grid?.map(row => row.map(cell => ({
        ch: cell.ch,
        color: cell.color,
        attr: cell.attr,
    }))) || null;
    Object.assign(game, {
        _pending_message: saved.pendingMessage,
        _message_more: saved.messageMore,
        _message_more_line: saved.messageMoreLine,
        _keep_pending_message: saved.keepPendingMessage,
        _overlay_lines: saved.overlayLines,
        _overlay_hide_status: saved.overlayHideStatus,
        _overlay_hide_status_only: saved.overlayHideStatusOnly,
        _command_mode: saved.commandMode,
    });
    return snapshot;
}

async function finishMenuLevelTeleport(targetLevel, options = {}) {
    const prerequisiteMessage = grantEndgamePrerequisiteIfNeeded(targetLevel);
    const prerequisiteSnapshot = prerequisiteMessage
        ? await captureCurrentGridSnapshot()
        : null;
    const finalOptions = prerequisiteMessage
        ? { ...options, endgameLevelTeleport: true }
        : options;
    const ok = await finishLevelTeleport(targetLevel, finalOptions);
    const forceEndgameWizard = !!game._force_endgame_wizard_after_arrival;
    game._force_endgame_wizard_after_arrival = 0;
    const shouldAmuletWish = ok
        && !!prerequisiteMessage
        && finalOptions.endgameLevelTeleport
        && In_endgame(targetLevel)
        && !!game.u?.uhave?.amulet
        && !game.u?.uevent?.amulet_wish;
    if (shouldAmuletWish) {
        game.u.uevent ??= {};
        game.u.uevent.amulet_wish = 1;
    }
    if (ok && prerequisiteMessage) {
        const arrivalText = game._pending_message || '';
        const arrivalMore = !!game._message_more;
        const queuedMessages = game._queued_messages_after_more || [];
        const queuedText = game._queued_message_after_more || '';
        const queuedMore = !!game._queued_message_more_after_more || (shouldAmuletWish && !!queuedText);
        const insertAfter = [
            ...queuedMessages,
            ...(queuedText ? [{ text: queuedText, more: queuedMore }] : []),
            ...(shouldAmuletWish ? [
                { text: 'The Amulet is bestowing a wish upon you!  You may wish for an object.', more: true },
                { text: 'For what do you wish?', more: false, beginWishPrompt: true },
            ] : []),
        ];
        game._queued_messages_after_more = arrivalText
            ? [{
                text: arrivalText,
                more: arrivalMore,
                ...(forceEndgameWizard ? { forceEndgameWizard: true } : {}),
                ...(insertAfter.length ? { insertAfter } : {}),
            }]
            : insertAfter;
        game._queued_message_after_more = '';
        game._queued_message_more_after_more = '';
        game._preserved_grid_snapshot = prerequisiteSnapshot;
        await setMessage(prerequisiteMessage, true);
    }
    return ok;
}

function levelTeleportTrapResult(trap, prefix, intentional = false) {
    trap.tseen = true;
    const verb = intentional ? 'trigger' : `${game.u?.flying ? 'fly' : game.u?.levitating ? 'float' : 'step'} onto`;
    const messages = [prefix, `You ${verb} a level teleport trap!`];
    if (heroHasAntimagic() && !intentional) {
        messages.push('You feel a wrenching sensation.');
        return { message: trapMessage(...messages), more: false };
    }
    if (In_endgame(game.u?.uz)) {
        messages.push('You feel a wrenching sensation.');
        return { message: trapMessage(...messages), more: false };
    }
    deleteTrap(trap);
    if (heroHasAmuletOfYendor() || In_sokoban(game.u?.uz)) {
        messages.push('You feel very disoriented for a moment.');
        messages.push(levelTeleportTrapFollowupMessage());
        return { message: trapMessage(...messages), more: false };
    }
    if (heroHasTeleportControl() && !heroIsStunned()) {
        game._level_teleport_trap_followup = 1;
        game._read_confused_teleport_prompt_after_more = 1;
        game._read_level_teleport_confused_prompt = heroIsConfused() ? 1 : 0;
        return { message: trapMessage(...messages), more: true };
    }
    const currentDepth = depth_of_level(game.u?.uz || { dnum: 0, dlevel: 1 });
    const targetDepth = randomTeleportDepth();
    if (targetDepth === currentDepth) {
        messages.push('You shudder for a moment.');
        messages.push(levelTeleportTrapFollowupMessage());
        return { message: trapMessage(...messages), more: false };
    }
    scheduleSitLevelChange(levelTeleportNumericTarget(targetDepth), {
        levelTeleport: true,
        postMessage: levelTeleportTrapFollowupMessage(),
    });
    return { message: trapMessage(...messages), more: true };
}

function sitLevelTeleportTrapResult(trap, prefix) {
    return levelTeleportTrapResult(trap, prefix, true);
}

function sitMagicPortalResult(trap, prefix) {
    trap.tseen = true;
    const messages = [prefix, 'You activated a magic portal!'];
    if (In_endgame(game.u?.uz) && !heroHasAmuletOfYendor()) {
        messages.push('You feel dizzy for a moment, but nothing happens...');
        return { message: trapMessage(...messages), more: false };
    }
    const target = clampDungeonLevel(trap.dst);
    if (!target || sameDungeonLevel(target, game.u?.uz)) {
        messages.push('You shudder for a moment.');
        return { message: trapMessage(...messages), more: false };
    }
    const alreadyStunned = heroIsStunned();
    addHeroStun(3);
    scheduleSitLevelChange(target, {
        portalArrival: true,
        preMessage: alreadyStunned ? 'You feel dizzier.' : 'You feel slightly dizzy.',
    });
    return { message: trapMessage(...messages), more: true };
}

function movementTrapLocomotion() {
    if (game.u?.flying) return 'fly';
    if (game.u?.levitating) return 'float';
    return 'step';
}

function movementTrapAlreadySeen(trap) {
    return trap?.ttyp === HOLE || !!trap?.tseen;
}

function movementTrapEscapeMessage(trap) {
    return `You escape ${sitTrapArticleName(trap)}.`;
}

function movementOverFloorTrapMessage(trap) {
    return `You ${movementTrapLocomotion()} over ${sitTrapArticleName(trap)}.`;
}

function movementTransportTrapResult(trap) {
    if (!trap) return null;
    const alreadySeen = movementTrapAlreadySeen(trap);
    const sokobanInescapable = In_sokoban(game.u?.uz)
        && [PIT, SPIKED_PIT, HOLE, TRAPDOOR].includes(trap.ttyp);

    if ((trap.ttyp === HOLE || trap.ttyp === TRAPDOOR) && !sokobanInescapable
        && (game.u?.levitating || game.u?.flying)) {
        return {
            message: alreadySeen ? movementOverFloorTrapMessage(trap) : '',
            more: false,
        };
    }

    if (alreadySeen && !sokobanInescapable && sitTrapEscapeAllowed(trap) && !rn2(5))
        return { message: movementTrapEscapeMessage(trap), more: false };

    if (trap.ttyp === HOLE || trap.ttyp === TRAPDOOR)
        return sitFallThroughTrapResult(trap, '');
    if (trap.ttyp === LEVEL_TELEP)
        return levelTeleportTrapResult(trap, '', false);
    if (trap.ttyp === MAGIC_PORTAL)
        return sitMagicPortalResult(trap, '');
    return null;
}

function placeSitTrapProjectile(projectile) {
    const obj = {
        quan: 1,
        blessed: false,
        cursed: false,
        ox: game.u?.ux || 0,
        oy: game.u?.uy || 0,
        ...projectile,
    };
    game.level.objects ??= [];
    game.level.objects.push(obj);
    newsym(game.u?.ux || 0, game.u?.uy || 0);
    return obj;
}

function sitProjectileTrapMessage(trap, prefix, alreadySeen, spec) {
    if (trap.once && alreadySeen && !rn2(15)) {
        deleteTrap(trap);
        return `${prefix}  ${spec.emptyMessage}`;
    }
    trap.once = true;
    trap.tseen = true;
    const projectile = { ...spec.projectile };
    if (spec.poisonable) projectile.opoisoned = !rn2(6);
    const damage = spec.damage();
    const roll = rnd(20);
    const misses = (game.u?.uac || 0) + spec.toHit <= roll;
    if (misses) {
        placeSitTrapProjectile(projectile);
        const almost = (game.u?.uac || 0) + spec.toHit > roll - 2;
        const missText = almost ? `You are almost hit by ${spec.article}.` : `${spec.subject} misses you.`;
        return `${prefix}  ${spec.shootsMessage}  ${missText}`;
    }
    if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
    let hitText = `You are hit by ${spec.article}.`;
    if (projectile.opoisoned && !heroHasPoisonResistance()) {
        if (game.u?.acurr?.a) game.u.acurr.a[A_CON] = Math.max(3, (game.u.acurr.a[A_CON] || 10) - 1);
        hitText += `  The ${spec.name} was poisoned!`;
    }
    return `${prefix}  ${spec.shootsMessage}  ${hitText}`;
}

function sitRockTrapMessage(trap, prefix, alreadySeen) {
    if (trap.once && alreadySeen && !rn2(15)) {
        deleteTrap(trap);
        return `${prefix}  A trap door in the ceiling opens, but nothing falls out!`;
    }
    trap.once = true;
    trap.tseen = true;
    let damage = d(2, 6);
    let harmless = false;
    const messages = [`${prefix}  A trap door in the ceiling opens and a rock falls on your head!`];
    placeSitTrapProjectile({ otyp: ROCK, cls: 'gem', kind: 'rock', singular: 'rock', plural: 'rocks', glyph: '*' });
    const helmet = wornEarthHelmet();
    if (helmet) {
        if (heroPassesRocks()) {
            const simple = /\b(?:hat|fedora|cornuthaum|cap)\b/.test(armorKind(helmet)) ? 'hat' : 'helm';
            messages.push(`Unfortunately, you are wearing ${articleForName(simple)}.`);
            damage = 2;
        } else if (hardEarthHelmet(helmet)) {
            messages.push('Fortunately, you are wearing a hard helmet.');
            damage = 2;
        } else if (game.flags?.verbose !== false) {
            messages.push(`Your ${pickupObjectName(helmet)} does not protect you.`);
        }
    } else if (heroPassesRocks()) {
        messages.push('It passes harmlessly through you.');
        harmless = true;
    }
    if (!harmless) {
        if (game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
            if ((game.u.uhp || 0) <= 0) game._death_cause = 'falling rock';
        }
        exerciseAttribute(A_STR, false);
    }
    return messages.join('  ');
}

function sitSqueakyBoardMessage(trap, prefix) {
    trap.tseen = true;
    if (heroIsDeaf()) return `${prefix}  A board beneath you vibrates.`;
    return `${prefix}  A board beneath you squeaks ${sitTrapNote(trap)} loudly.`;
}

function sitSleepGasMessage(trap, prefix) {
    trap.tseen = true;
    if (game.u?.sleepResistance || polyselfForm()?.breathless) {
        return `${prefix}  You are enveloped in a cloud of gas!`;
    }
    const duration = rnd(25);
    game._helpless_time = Math.max(game._helpless_time || 0, duration);
    game._sleeping_time = Math.max(game._sleeping_time || 0, duration + 1);
    return `${prefix}  A cloud of gas puts you to sleep!`;
}

function rustTrapWornArmor(slot) {
    return wornArmorItemsBySlotOrder([slot])[0] || null;
}

function rustTrapTorsoArmor() {
    return wornArmorItemsBySlotOrder(['cloak', 'body', 'shirt'])[0] || null;
}

function rustTrapPrimaryWeapon() {
    return (game.inventory || []).find(item => {
        const line = item.line || '';
        return item.wielded || /\b(?:weapon|wielded) in (?:right hand|hands)\b/.test(line)
            || line.includes('(wielded)');
    }) || null;
}

function rustTrapSecondaryWeapon(primary) {
    return (game.inventory || []).find(item => {
        if (item === primary) return false;
        const line = item.line || '';
        return item.alternate || /\b(?:alternate weapon|(?:weapon|wielded) in left hand)\b/.test(line);
    }) || null;
}

function rustTrapBimanualWeapon(item) {
    if (!item) return false;
    const name = `${objectKindKey(item)} ${inventoryItemName(item)}`.toLowerCase();
    return /\b(?:quarterstaff|two-handed sword|battle-axe|dwarvish mattock|tsurugi)\b/.test(name);
}

function rustTrapArmorName(armor) {
    const slot = armorSlot(armor);
    const kind = armorKind(armor);
    if (slot === 'helm') return hardEarthHelmet(armor) ? 'helm' : 'hat';
    if (slot === 'shield') return 'shield';
    if (slot === 'gloves') return kind.includes('gauntlets') ? 'gauntlets' : 'gloves';
    if (slot === 'cloak') return armorSimpleSlotName(armor);
    if (slot === 'shirt') return 'shirt';
    if (slot === 'body') {
        if (/dragon scales?\b/.test(kind)) return kind.includes('mail') ? 'dragon mail' : 'dragon scales';
        if (kind.endsWith(' mail')) return 'mail';
        if (kind.endsWith(' jacket')) return 'jacket';
        return 'suit';
    }
    return armorMessageName(armor);
}

function rustTrapObjectName(item, override = '') {
    if (override) return override;
    if (isWornArmorItem(item)) return rustTrapArmorName(item);
    return pickupObjectName(item).replace(/^pair of /, '');
}

function rustTrapNameVerb(name, singular, plural) {
    return /\b(?:boots|shoes|gloves|gauntlets|scales|arrows|darts|bolts)\b/i.test(name)
        && !/\bmail\b/i.test(name) ? plural : singular;
}

function rustTrapItemClass(item) {
    return item?.cls || (item?.otyp === SCROLL_CLASS || item?.glyph === '?' ? 'scroll'
        : item?.otyp === POTION_CLASS || item?.otyp === POT_ACID
            || item?.otyp === POT_OIL || item?.otyp === POT_WATER || item?.glyph === '!' ? 'potion'
            : item?.otyp === SPBOOK_NO_NOVEL || item?.otyp === SPE_HEALING || item?.glyph === '+' ? 'spellbook'
                : '');
}

function updateRustTrapItemLine(item, oldAc = null) {
    if (isWornArmorItem(item)) {
        if (oldAc != null) updateWornArmorAcAfterChange(item, oldAc);
        else updateArmorLine(item);
    } else {
        item.line = normalInventoryLine({ ...item, line: '' });
    }
}

function rustTrapSplashLitItem(item, messages) {
    if (!(item?.lamplit || item?.burning)) return false;
    const name = pickupObjectName(item).replace(/ \(lit\)$/, '');
    if (item.otyp === BRASS_LANTERN || objectKindKey(item) === 'brass lantern') {
        const heard = !heroIsDeaf();
        const seen = !game.u?.blind;
        if (heard || seen)
            messages.push(`Your ${name} ${heard ? 'crackles' : ''}${heard && seen ? ' and ' : ''}${seen ? 'flickers' : ''}.`);
        return false;
    }
    item.lamplit = false;
    item.burning = false;
    delete item._burnTimer;
    delete item.litRadius;
    updateRustTrapItemLine(item);
    messages.push(`Your ${name} ${rustTrapNameVerb(name, 'goes', 'go')} out!`);
    return true;
}

function rustTrapSplashLitInventory(messages) {
    const primary = rustTrapPrimaryWeapon();
    const secondary = rustTrapSecondaryWeapon(primary);
    for (const item of game.inventory || []) {
        if (item === primary) continue;
        if (game._twoweapon && item === secondary) continue;
        rustTrapSplashLitItem(item, messages);
    }
}

function rustTrapIsAcidPotion(item) {
    const kind = objectKindKey(item).replace(/^potion of /, '');
    return item?.potionIndex === 23 || kind === 'acid';
}

function rustTrapDestroyAcidPotion(item, messages, name, described = false) {
    const plural = (item.quan || 1) > 1;
    messages.push(described
        ? `The potion${plural ? 's' : ''} ${plural ? 'explode' : 'explodes'}!`
        : `Your ${name} ${rustTrapNameVerb(name, 'explodes', 'explode')}!`);
    useUpInventoryItem(item, item.quan || 1);
}

function rustTrapWaterDamageReadable(item, messages, cls, name) {
    if (cls === 'scroll') {
        if (isBlankScrollItem(item)) return false;
        messages.push(`Your ${name} ${rustTrapNameVerb(name, 'fades', 'fade')}.`);
        item.otyp = SCR_BLANK_PAPER;
        item.kind = 'blank paper';
        item.actualKind = '';
        item.known = false;
        item.scrollIndex = IDENTIFIED_SCROLL_NAMES.length;
        item.spe = 0;
        updateRustTrapItemLine(item);
        return true;
    }
    if (cls !== 'spellbook' || isBookOfTheDeadItem(item) || isBlankSpellbookItem(item)) return false;
    messages.push(`Your ${name} ${rustTrapNameVerb(name, 'fades', 'fade')}.`);
    item.kind = 'spellbook of blank paper';
    item.actualKind = '';
    item.spellName = '';
    item.spell = null;
    item.known = false;
    updateRustTrapItemLine(item);
    return true;
}

function rustTrapWaterDamagePotion(item, messages, name) {
    if (rustTrapIsAcidPotion(item)) {
        rustTrapDestroyAcidPotion(item, messages, name);
        return true;
    }
    if (isWaterPotion(item)) return false;
    messages.push(`Your ${name} ${rustTrapNameVerb(name, 'dilutes', 'dilute')}${item.odiluted ? ' further' : ''}.`);
    if (item.odiluted) {
        item.otyp = POT_WATER;
        item.kind = 'water';
        item.actualKind = '';
        item.blessed = false;
        item.cursed = false;
        item.odiluted = false;
        item.known = false;
    } else {
        item.odiluted = true;
    }
    updateRustTrapItemLine(item);
    return true;
}

function rustTrapWaterDamageItem(item, messages, nameOverride = '') {
    if (!item) return false;
    if (rustTrapSplashLitItem(item, messages)) return true;

    const name = rustTrapObjectName(item, nameOverride);
    if (item.greased) {
        if (!rn2(2)) {
            item.greased = false;
            updateRustTrapItemLine(item);
            messages.push(`The grease on your ${name} washes off.`);
        }
        if (!item.greased && rustTrapItemClass(item) === 'potion' && rustTrapIsAcidPotion(item))
            rustTrapDestroyAcidPotion(item, messages, name, true);
        return true;
    }

    const cls = rustTrapItemClass(item);
    if ((cls === 'scroll' || cls === 'spellbook') && rustTrapWaterDamageReadable(item, messages, cls, name))
        return true;
    if (cls === 'potion' && rustTrapWaterDamagePotion(item, messages, name)) return true;

    const profile = wishedDamageProfile(item);
    if (!profile.erosionMatters || profile.primaryWord !== 'rusty') return false;
    if (item.oerodeproof) {
        if (!item.rknown && game.flags?.verbose !== false)
            messages.push(`Somehow, your ${name} ${rustTrapNameVerb(name, 'is', 'are')} not affected by the oxidation.`);
        item.rknown = true;
        updateRustTrapItemLine(item);
        return false;
    }
    if (item.blessed && !rnl(4)) return false;

    const current = Math.min(3, item.oeroded || 0);
    if (current >= 3) return false;
    const oldAc = isWornArmorItem(item) ? wornArmorAcValueGreatestErosion(item) : null;
    item.oeroded = current + 1;
    updateRustTrapItemLine(item, oldAc);
    const adverb = item.oeroded === 3 ? ' completely' : current ? ' further' : '';
    messages.push(`Your ${name} ${rustTrapNameVerb(name, 'rusts', 'rust')}${adverb}!`);
    return true;
}

function sitRustTrapMessage(trap, prefix) {
    trap.tseen = true;
    const messages = [];
    switch (rn2(5)) {
    case 0:
        messages.push(`${prefix}  A gush of water hits you on the head!`);
        rustTrapWaterDamageItem(rustTrapWornArmor('helm'), messages);
        break;
    case 1:
        messages.push(`${prefix}  A gush of water hits your left arm!`);
        if (!rustTrapWaterDamageItem(rustTrapWornArmor('shield'), messages, 'shield')) {
            const primary = rustTrapPrimaryWeapon();
            if (game._twoweapon)
                rustTrapWaterDamageItem(rustTrapSecondaryWeapon(primary), messages);
            else if (rustTrapBimanualWeapon(primary))
                rustTrapWaterDamageItem(primary, messages);
            rustTrapWaterDamageItem(rustTrapWornArmor('gloves'), messages);
        }
        break;
    case 2:
        messages.push(`${prefix}  A gush of water hits your right arm!`);
        rustTrapWaterDamageItem(rustTrapPrimaryWeapon(), messages);
        rustTrapWaterDamageItem(rustTrapWornArmor('gloves'), messages);
        break;
    default:
        messages.push(`${prefix}  A gush of water hits you!`);
        rustTrapSplashLitInventory(messages);
        rustTrapWaterDamageItem(rustTrapTorsoArmor(), messages);
        break;
    }
    return messages.join('  ');
}

function heroFireTrapResult(trap, prefix = '', { allowLifeSaving = false } = {}) {
    trap.tseen = true;
    const origDamage = d(2, 4);
    const messages = [prefix ? `${prefix}  A tower of flame erupts from the floor!` : 'A tower of flame erupts from the floor!'];
    let damage = 0;
    if (game.u?.fireResistance) {
        damage = rn2(2);
        if (!damage) messages.push('You are uninjured.');
    } else {
        damage = d(2, 4);
        const hpLoss = rn2(Math.min(game.u?.uhpmax || 1, damage + 1));
        if (game.u) {
            game.u.uhpmax = Math.max(1, (game.u.uhpmax || 1) - hpLoss);
            game.u.uhp = Math.min(game.u.uhp || 1, game.u.uhpmax);
        }
    }
    const inventoryFire = fireDamageInventory(origDamage, false, false, { allowLifeSaving });
    messages.push(...inventoryFire.messages);
    const floorFire = burnFloorObjectsByFire(game.u?.ux || 0, game.u?.uy || 0, {
        giveFeedback: !game.u?.blind,
        heroCaused: true,
    });
    messages.push(...floorFire.messages);
    if (floorFire.count && game.u?.blind) messages.push('You smell paper burning.');
    if (!inventoryFire.lifeSaving && !inventoryFire.fatal) {
        damage += inventoryFire.damage;
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        if ((game.u?.uhp || 0) <= 0) game._death_cause = inventoryFire.deathCause || 'killed by a tower of flame';
    }
    return {
        message: messages.join('  '),
        more: true,
        lifeSaving: !!inventoryFire.lifeSaving,
        fatal: !!inventoryFire.fatal,
    };
}

function heroFireTrapMessage(trap, prefix = '') {
    return heroFireTrapResult(trap, prefix).message;
}

function applyHeroFireTrapFatalResult(result) {
    return applyLifeSavingOrFatalCommandMode(result);
}

export function applyLifeSavingOrFatalCommandMode(result) {
    game.context ??= {};
    if (result.lifeSaving) {
        game._command_mode = 'lifeSavingMore';
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game.context.move = 0;
        game._run_stop_now = 1;
        game._run_steps_remaining = 0;
        return true;
    }
    if (result.fatal) {
        game._command_mode = 'deathDieMore';
        game._pending_time_passed = 0;
        game.context.move = 0;
        game._process_command_time_now = 0;
        game._run_steps_remaining = 0;
        prepareDeathBones();
        return true;
    }
    return false;
}

function sitFireTrapMessage(trap, prefix) {
    return heroFireTrapMessage(trap, prefix);
}

function sitTeleportTrapMessage(trap, prefix) {
    trap.tseen = true;
    if (heroHasAntimagic() || In_endgame(game.u?.uz) || heroNoTeleportLevel()) {
        return `${prefix}  You feel a wrenching sensation.`;
    }
    if (trap.once) deleteTrap(trap);
    const fixedTeleport = teleportHeroToFixedTrapDestination(trap);
    if (fixedTeleport !== null)
        return [prefix, fixedTeleport || 'You shudder for a moment.'].join('  ');
    const materialize = safeTeleportHeroSameLevel();
    return [prefix, materialize || 'You shudder for a moment.'].join('  ');
}

function sitAntiMagicTrapMessage(trap, prefix) {
    trap.tseen = true;
    const messages = [prefix];
    if (heroHasAntimagic()) {
        const damage = rnd(4);
        const hp = game.u?.uhp || 1;
        if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        messages.push(`You feel ${damage >= hp ? 'unbearably torpid!' : damage >= hp / 4 ? 'very lethargic.' : 'sluggish.'}`);
    }
    let drain = d(2, 6);
    const halfDrain = rnd(Math.max(1, Math.trunc(drain / 2)));
    let exclaim = false;
    if ((game.u?.uenmax || 0) > drain) {
        game.u.uenmax -= halfDrain;
        drain -= halfDrain;
        exclaim = true;
    }
    if ((game.u?.uenmax || 0) < 1) {
        if (game.u) {
            game.u.uen = 0;
            game.u.uenmax = 0;
        }
        messages.push('You feel momentarily lethargic.');
    } else {
        if (drain > ((game.u?.uen || 0) + (game.u?.uenmax || 0)) / 3) drain = rnd(drain);
        if (game.u) {
            exclaim = exclaim || drain > (game.u.uen || 0);
            game.u.uen = (game.u.uen || 0) - drain;
            if (game.u.uen < 0) {
                game.u.uenmax = Math.max(0, (game.u.uenmax || 0) - rnd(-game.u.uen));
                game.u.uen = 0;
            } else if (game.u.uen > game.u.uenmax) {
                game.u.uen = game.u.uenmax;
            }
        }
        messages.push(`You feel your magical energy drain away${exclaim ? '!' : '.'}`);
    }
    return messages.join('  ');
}

function sitPolyTrapMessage(trap, prefix) {
    trap.tseen = true;
    const messages = [`${prefix}  You trigger a polymorph trap!`];
    if (heroHasAntimagic() || heroHasUnchanging()) {
        messages.push('You feel momentarily different.');
        return messages.join('  ');
    }
    deleteTrap(trap);
    const form = rndmonnum();
    const result = becomeMonster(form?.name || 'newt');
    messages.push(result?.message || 'You feel a change coming over you.');
    if (result?.more) game._topline_after_more = result.more === true ? game._topline_after_more : result.more;
    return messages.join('  ');
}

function sitLandmineMessage(trap, prefix) {
    const damage = rnd(16);
    const mineName = sitTrapArticleName(trap);
    trap.tseen = true;
    trap.ttyp = PIT;
    trap.madeby_u = false;
    if (game.u) {
        game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
        game.u._woundedLegTurns = Math.max(game.u._woundedLegTurns || 0, rn1(35, 41), rn1(35, 41));
        game.u.utrap = rn1(6, 2);
        game.u.utraptype = 'pit';
    }
    exerciseAttribute(A_DEX, false);
    newsym(game.u?.ux || 0, game.u?.uy || 0);
    return `${prefix}  KAABLAMM!!!  You triggered ${mineName}!  You fall into a pit!`;
}

function sitRollingBoulderMessage(trap, prefix) {
    trap.tseen = true;
    const start = trap.launch;
    const end = trap.launch2;
    const boulder = (game.level?.objects || []).find(obj =>
        !obj.transientProjectile && obj.otyp === BOULDER && obj.ox === start?.x && obj.oy === start?.y);
    let released = false;
    if (boulder && end) {
        boulder.ox = end.x;
        boulder.oy = end.y;
        released = true;
        vision_reset();
        vision_recalc(0);
        newsym(start.x, start.y);
        newsym(end.x, end.y);
    }
    return `${prefix}  Click!  You trigger a rolling boulder trap!  ${released ? 'A boulder misses you.' : 'Fortunately for you, no boulder was released.'}`;
}

async function sitTriggerTrap(trap) {
    if (!trap) return false;
    if (await sitWhileAlreadyTrapped(trap)) return true;
    const prefix = game.u?.flying ? 'You land.' : 'You sit down.';
    const alreadySeen = trap.ttyp === HOLE || !!trap.tseen;
    const sokobanInescapable = In_sokoban(game.u?.uz)
        && [PIT, SPIKED_PIT, HOLE, TRAPDOOR].includes(trap.ttyp);
    if (alreadySeen && !sokobanInescapable && sitTrapEscapeAllowed(trap) && !rn2(5)) {
        await finishSitMessage(`${prefix}  You escape ${sitTrapArticleName(trap)}.`);
        return true;
    }
    if (trap.ttyp === ARROW_TRAP) {
        await finishSitMessage(sitProjectileTrapMessage(trap, prefix, alreadySeen, {
            name: 'arrow',
            subject: 'An arrow',
            article: 'an arrow',
            shootsMessage: 'An arrow shoots out at you!',
            emptyMessage: 'You hear a loud click!',
            toHit: 8,
            damage: () => rnd(6),
            projectile: { cls: 'weapon', kind: 'arrow', singular: 'arrow', plural: 'arrows', glyph: ')' },
        }));
        return true;
    }
    if (trap.ttyp === DART_TRAP) {
        await finishSitMessage(sitProjectileTrapMessage(trap, prefix, alreadySeen, {
            name: 'little dart',
            subject: 'A little dart',
            article: 'a little dart',
            shootsMessage: 'A little dart shoots out at you!',
            emptyMessage: 'You hear a soft click.',
            toHit: 7,
            damage: () => rnd(3),
            poisonable: true,
            projectile: { otyp: DART, cls: 'weapon', kind: 'dart', singular: 'dart', plural: 'darts', glyph: ')', color: CLR_CYAN },
        }));
        return true;
    }
    if (trap.ttyp === ROCKTRAP) {
        await finishSitMessage(sitRockTrapMessage(trap, prefix, alreadySeen));
        return true;
    }
    if (trap.ttyp === SQKY_BOARD) {
        await finishSitMessage(sitSqueakyBoardMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === BEAR_TRAP) {
        trap.tseen = true;
        const damage = d(2, 4);
        if (game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
            game.u.utrap = rn1(4, 4);
            game.u.utraptype = 'beartrap';
        }
        await finishSitMessage(`${prefix}  A bear trap closes on your foot!`);
        return true;
    }
    if (trap.ttyp === LANDMINE) {
        await finishSitMessage(sitLandmineMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === ROLLING_BOULDER_TRAP) {
        await finishSitMessage(sitRollingBoulderMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === SLP_GAS_TRAP) {
        await finishSitMessage(sitSleepGasMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === RUST_TRAP) {
        await finishSitMessage(sitRustTrapMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === FIRE_TRAP) {
        await finishSitMessage(sitFireTrapMessage(trap, prefix), { more: true });
        return true;
    }
    if (trap.ttyp === PIT || trap.ttyp === SPIKED_PIT) {
        trap.tseen = true;
        if (game.u) {
            game.u.utrap = rn1(6, 2);
            game.u.utraptype = 'pit';
            game.u.uhp = Math.max(0, (game.u.uhp || 1) - rnd(trap.ttyp === SPIKED_PIT ? 10 : 6));
        }
        await finishSitMessage(`${prefix}  You fall into ${trap.ttyp === SPIKED_PIT ? 'a spiked pit' : 'a pit'}!`);
        return true;
    }
    if (trap.ttyp === HOLE || trap.ttyp === TRAPDOOR) {
        const result = sitFallThroughTrapResult(trap, prefix);
        await finishSitMessage(result.message, { more: result.more });
        return true;
    }
    if (trap.ttyp === TELEP_TRAP) {
        await finishSitMessage(sitTeleportTrapMessage(trap, prefix), { more: true });
        return true;
    }
    if (trap.ttyp === LEVEL_TELEP) {
        const result = sitLevelTeleportTrapResult(trap, prefix);
        await finishSitMessage(result.message, { more: result.more });
        return true;
    }
    if (trap.ttyp === MAGIC_PORTAL) {
        const result = sitMagicPortalResult(trap, prefix);
        await finishSitMessage(result.message, { more: result.more });
        return true;
    }
    if (trap.ttyp === WEB) {
        await finishSitMessage(sitWebTrapMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === STATUE_TRAP) {
        await finishSitMessage(await activateStatueTrap(trap, game.u?.ux || 0, game.u?.uy || 0, { prefix }));
        return true;
    }
    if (trap.ttyp === MAGIC_TRAP) {
        trap.tseen = true;
        const result = magicTrapResult(trap);
        const messages = [prefix];
        if (result.message) messages.push(result.message);
        if (result.afterMore) game._topline_after_more = result.afterMore;
        await finishSitMessage(messages.join('  '), { more: result.more || !!result.afterMore });
        return true;
    }
    if (trap.ttyp === ANTI_MAGIC) {
        await finishSitMessage(sitAntiMagicTrapMessage(trap, prefix));
        return true;
    }
    if (trap.ttyp === POLY_TRAP) {
        await finishSitMessage(sitPolyTrapMessage(trap, prefix), { more: !!game._topline_after_more });
        return true;
    }
    if (trap.ttyp === VIBRATING_SQUARE) {
        trap.tseen = true;
        await finishSitMessage(`${prefix}  You feel a strange vibration beneath you.`);
        return true;
    }
    trap.tseen = true;
    await finishSitMessage(prefix);
    return true;
}

async function sitOnTerrain(loc) {
    const typ = loc?.typ;
    const waterEggs = heroPolyselfEggsInWater();
    if ((game.u?.underwater || game.u?.uunderwater || Is_waterlevel(game.u?.uz)) && !waterEggs) {
        await finishSitMessage(Is_waterlevel(game.u?.uz)
            ? 'There are no cushions floating nearby.'
            : 'You sit down on the muddy bottom.');
        return true;
    }
    if ((IS_POOL(typ) || typ === WATER || typ === MOAT) && !waterEggs) {
        rn2(10);
        rn2(10);
        await finishSitMessage('You sit in the water.');
        return true;
    }
    if (typ === SINK) {
        const rump = polyselfForm()?.humanoid === false || polyselfForm()?.mlet === ';' ? 'underside' : 'rump';
        await finishSitMessage(`You sit on the sink.  Your ${rump} gets wet.`);
        return true;
    }
    if (typ === ALTAR) {
        await finishSitMessage('You sit on the altar.  A voice whispers: "Thou shalt pay, infidel!"', { more: true });
        return true;
    }
    if (typ === GRAVE) {
        await finishSitMessage('You sit on the grave.');
        return true;
    }
    if (typ === STAIRS) {
        await finishSitMessage('You sit on the stairs.');
        return true;
    }
    if (typ === LADDER) {
        await finishSitMessage('You sit on the ladder.');
        return true;
    }
    if (typ === LAVAPOOL || typ === LAVAWALL) {
        const messages = ['You sit on the lava.'];
        if (game.u?._statusSuffix?.includes('Slimed')) {
            game.u._statusSuffix = game.u._statusSuffix.replace(/ Slimed/g, '');
            game.u.slimed = false;
        }
        if (polyselfForm()?.likesLava || game.u?.likesLava) {
            messages.push('The lava feels warm.');
        } else {
            messages.push('The lava burns you!');
            if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 1) - d(game.u.fireResistance ? 2 : 10, 10));
        }
        await finishSitMessage(messages.join('  '), { more: messages.length > 1 });
        return true;
    }
    if (typ === ICE) {
        const messages = ['You sit on the ice.'];
        if (!heroHasColdResistance()) messages.push('The ice feels cold.');
        await finishSitMessage(messages.join('  '));
        return true;
    }
    if (typ === THRONE) {
        await finishSitMessage('You sit on the opulent throne.  You feel somehow out of place...', { more: true });
        return true;
    }
    return false;
}

async function doSitCommand() {
    if (game.u?.usteed) {
        const steedName = game.u.usteed.givenName || `the ${game.u.usteed.data?.name || 'steed'}`;
        await finishSitMessage(`You are already sitting on ${steedName}.`, { move: false });
        return;
    }
    if (game.u?.uundetected && polyselfForm()?.ceilingHider) game.u.uundetected = 0;

    const reach = heroCanReachFloorForSit();
    if (!reach.ok) {
        await finishSitMessage(reach.message, { move: false });
        return;
    }

    const ux = game.u?.ux || 0;
    const uy = game.u?.uy || 0;
    const loc = game.level?.at(ux, uy);
    const trap = (game.level?.traps || []).find(item => item.tx === ux && item.ty === uy);
    const objectHere = floorObjectForSitting();
    if (objectHere) {
        await sitOnFloorObject(objectHere);
        return;
    }
    if (await sitWhileAlreadyTrapped(trap)) return;
    if (trap && await sitTriggerTrap(trap)) return;
    if (await sitOnTerrain(loc)) return;
    if (polyselfForm()?.oviparous && await sitLayEgg()) return;
    await finishSitMessage(`Having fun sitting on the ${sitSurfaceName(loc)}?`);
}

async function eatRottenNonCorpseFood(item, floorObject = false) {
    const touched = partialRottenFood(item, floorObject);
    const { message, rottenSleepDuration } = rottenFoodEffect();
    let postEffect = null;
    if (!rottenSleepDuration) {
        if (isRoyalJelly(touched)) {
            await finishRoyalJellyEating(touched, floorObject, message, { more: true, processTimeWithMore: 1 });
            return;
        }
        addHeroNutrition(remainingFoodNutrition(touched));
        consumeTouchedFood(touched, floorObject);
        postEffect = cursedAppleSleepPostEffect(touched);
    }
    game._pet_food_scan_inventory = game.inventory || [];
    await setMessage(postEffect?.message ? `${message}  ${postEffect.message}` : message, true);
    game._process_time_with_more = 1;
    game._command_mode = null;
    game.context.move = rottenSleepDuration ? rottenSleepDuration + 1 : (postEffect?.duration || 1);
}

function addHeroVomiting(turns) {
    if (!game.u || turns <= 0) return;
    game.u._vomitingTimeout = (game.u._vomitingTimeout || 0) + turns;
    game.u.vomiting = true;
    addHeroStatusSuffix('Vom');
}

function consumeOneFloorObject(obj) {
    if (!obj || !game.level) return;
    if ((obj.quan || 1) > 1) {
        if (takePretouchedFoodId(obj) == null) next_ident();
        obj.quan--;
        newsym(obj.ox, obj.oy);
        return;
    }
    clearPretouchedFood(obj);
    game.level.objects = (game.level.objects || []).filter(other => other !== obj);
    newsym(obj.ox, obj.oy);
}

function heroPolyselfResistsStoning() {
    const form = game.u?._polyself_form;
    return !!form && (form.stoneResistance || form.name === 'stone golem');
}

function heroPolyselfCanBecomeStoneGolem() {
    const form = game.u?._polyself_form;
    if (!form || form.name === 'stone golem' || isMonsterGenocidedName('stone golem')) return false;
    return form.mlet === "'" || form.glyph === "'";
}

function maybeTurnPolyselfIntoStoneGolem() {
    if (!heroPolyselfCanBecomeStoneGolem()) return '';
    const result = becomeMonster('stone golem');
    if (!result) return '';
    if (game.u) {
        game.u._stonedTimeout = 0;
        game.u._stonedKiller = '';
    }
    removeHeroStatusSuffix('Stone');
    newsym(game.u?.ux || 0, game.u?.uy || 0);
    return result.message || 'You turn into a stone golem!';
}

function appendPetrificationMessage(message, extra) {
    return extra ? `${message}  ${extra}` : message;
}

function startPetrifyingEggStoning(item) {
    if (!isPetrifyingEgg(item) || !game.u || game.u.stoneResistance
        || heroPolyselfResistsStoning() || (game.u._stonedTimeout || 0) > 0)
        return '';
    const polyselfMessage = maybeTurnPolyselfIntoStoneGolem();
    if (polyselfMessage) return polyselfMessage;
    const monsterName = item.corpsenm?.name || 'cockatrice';
    game.u._stonedTimeout = 5;
    game.u._stonedKiller = `${monsterName} egg`;
    addHeroStatusSuffix('Stone');
    return '';
}

function consumeOneInventoryFood(item) {
    if ((item?.quan || 1) > 1) {
        if (takePretouchedFoodId(item) == null) next_ident();
    } else {
        clearPretouchedFood(item);
    }
    removeInventoryItem(item);
}

function consumeOneInventoryFoodUsedUp(item) {
    if (!item) return false;
    const rawQuantity = Math.trunc(Number(item.quan || 1));
    const quantity = Number.isFinite(rawQuantity) ? Math.max(1, rawQuantity) : 1;
    if (quantity <= 1) return useUpInventoryItem(item, 1);

    const usedId = takePretouchedFoodId(item) ?? next_ident();
    const used = { ...item, id: usedId, quan: 1, letter: undefined, line: '' };
    delete used.o_id;
    delete used._shopBillObjectId;
    clearPretouchedFood(used);

    if (item.unpaid && !splitCarriedObjectShopBill(item, used, 1))
        clearObjectShopBillState(used);
    item.quan = quantity - 1;
    refreshInventoryObjectLine(item);
    if (item.unpaid) syncUnpaidBillLine(item);
    markObjectShopBillUsedUp(used);
    updateWornDisplacement();
    game._pet_food_scan_inventory = game.inventory;
    return true;
}

function consumeOneEatenFood(item, floorObject = false) {
    if (floorObject) consumeOneFloorObject(item);
    else consumeOneInventoryFood(item);
}

async function eatRottenEgg(item, floorObject = false) {
    const touched = partialRottenFood(item, floorObject);
    const { message, rottenSleepDuration } = rottenFoodEffect();
    let petrificationMessage = '';
    if (!rottenSleepDuration) {
        addHeroNutrition(remainingFoodNutrition(touched));
        consumeTouchedFood(touched, floorObject);
        petrificationMessage = startPetrifyingEggStoning(touched);
    }
    await setMessage(appendPetrificationMessage(message, petrificationMessage), true);
    game._process_time_with_more = 1;
    game._command_mode = null;
    game.context.move = rottenSleepDuration ? rottenSleepDuration + 1 : 1;
}

async function eatPyroliskEgg(item, floorObject = false) {
    const touched = touchEatenFood(item, floorObject);
    consumeOneEatenFood(touched, floorObject);
    const result = resolvePyroliskEggExplosion(game.u?.ux || 0, game.u?.uy || 0, d(3, 6));
    await setMessage(result.messages.join('  '), result.more);
    game._command_mode = null;
    game.context.move = 1;
    if (applyLifeSavingOrFatalCommandMode(result)) return;
}

async function eatStaleEgg(item, floorObject = false) {
    const touched = touchEatenFood(item, floorObject);
    addHeroVomiting(d(10, 4));
    if (touched.oeaten > 0) addHeroNutrition(touched.oeaten);
    consumeOneEatenFood(touched, floorObject);
    const petrificationMessage = startPetrifyingEggStoning(touched);
    await setMessage(appendPetrificationMessage('Ugh.  Rotten egg.', petrificationMessage));
    game._command_mode = null;
    game.context.move = 1;
}

function deadbookCorpseMonster(item) {
    const name = item?.corpsenm?.name || '';
    if (!name) return null;
    const found = monsterByRndName(name);
    if (found) return found;
    const humanoid = DEATH_CORPSE_BY_RACE[name] || ['human', 'elf', 'dwarf', 'gnome', 'orc'].includes(name);
    if (humanoid) return { name, mlet: '@', glyph: '@', color: CLR_WHITE, mlevel: 0, mmove: 12, maligntyp: 0, neuter: false };
    return item.corpsenm;
}

function isDeadbookNonlivingData(data) {
    const mlet = data?.mlet || data?.glyph;
    return !!data?.nonliving || mlet === 'W' || mlet === 'M' || mlet === 'Z' || mlet === "'";
}

function deadbookCorpseNoun(item, oneOf = false) {
    const corpseName = item?.corpsenm?.name || 'monster';
    return `${corpseName} corpse${oneOf ? 's' : ''}`;
}

function deadbookCarriedCorpseGlowMessage(item, oneOf = false) {
    const prefix = oneOf ? 'One of your ' : 'Your ';
    return `${prefix}${deadbookCorpseNoun(item, oneOf)} glows iridescently.`;
}

function deadbookRevivalMessage(mon) {
    const verb = isDeadbookNonlivingData(mon?.data) ? 'reanimates' : 'comes alive';
    return `It suddenly ${verb}!`;
}

function removeDeadbookRevivedItem(item, source) {
    if (!item) return;
    if ((item.quan || 1) > 1) {
        item.quan--;
        item.line = normalInventoryLine({ ...item, line: '' });
        return;
    }
    if (source === 'inventory') {
        game.inventory = (game.inventory || []).filter(invItem => invItem !== item);
        game._pet_food_scan_inventory = game.inventory;
    } else if (source === 'floor') {
        game.level.objects = (game.level?.objects || []).filter(obj => obj !== item);
        newsym(item.ox, item.oy);
    }
}

function deadbookAttachEggHatchTimer(item) {
    if (!isEggItem(item) || !item.corpsenm?.name || item.eggHatchTurn) return false;
    if (eggSpeciesGenocidedForHatching(item, game)) return false;
    for (let i = 151; i <= 200; i++) {
        if (rnd(i) > 150) {
            item.eggHatchTurn = (game.moves || 1) + i;
            item._egg_hatch_seq = game._egg_hatch_timer_seq = (game._egg_hatch_timer_seq || 0) + 1;
            break;
        }
    }
    item._egg_hatch_consumed = true;
    return !!item.eggHatchTurn;
}

async function reviveDeadbookCorpseItem(item, source) {
    if (!isCorpseItem(item)) return null;
    const savedTraits = savedCorpstatMonsterTraits(item);
    const data = savedTraits ? corpseTimerMonsterData(item) : deadbookCorpseMonster(item);
    if (!data?.name) return null;
    const x = item.ox || game.u?.ux || 0;
    const y = item.oy || game.u?.uy || 0;
    const flags = NO_MINVENT | MM_NOWAIT | MM_NOMSG | MM_NOCOUNTBIRTH
        | (savedTraits ? MM_NOTAIL : 0);
    const mon = await makemon(data, x, y, flags);
    if (!mon) return null;

    if (savedTraits) applySavedCorpstatMonsterTraits(mon, savedTraits, data);
    else if ((item.spe & CORPSTAT_GENDER) === CORPSTAT_MALE) mon.female = false;
    else if ((item.spe & CORPSTAT_GENDER) === CORPSTAT_FEMALE) mon.female = true;
    mon.mrevived = 1;
    mon.mundetected = 0;
    mon.m_ap_type = 0;
    mon.appearObj = null;
    mon.appearGlyph = null;
    mon.appearColor = null;
    removeDeadbookRevivedItem(item, source);
    newsym(mon.mx, mon.my);
    return mon;
}

async function unturnDeadHeroInventoryFromBook() {
    const messages = [];
    let revived = 0;
    for (const item of [...(game.inventory || [])]) {
        if (isEggItem(item)) deadbookAttachEggHatchTimer(item);
        if (!isCorpseItem(item)) continue;

        const oneOf = (item.quan || 1) > 1;
        const mon = await reviveDeadbookCorpseItem(item, 'inventory');
        if (!mon) continue;
        revived++;
        messages.push(deadbookCarriedCorpseGlowMessage(item, oneOf));
        messages.push(deadbookRevivalMessage(mon));
    }
    if (revived) game._deadbook_unturned_inventory = (game._deadbook_unturned_inventory || 0) + revived;
    return messages;
}

function deadbookFloorCorpseAt(x, y) {
    return (game.level?.objects || []).find(obj => isCorpseItem(obj) && obj.ox === x && obj.oy === y) || null;
}

async function mkundeadFromBook() {
    const x = game.u?.ux || 0;
    const y = game.u?.uy || 0;
    const count = Math.trunc((level_difficulty() + 1) / 10) + rnd(5);
    let spawned = 0;
    let revived = 0;

    for (let i = 0; i < count; i++) {
        const data = morgueMonster();
        if (!data) continue;
        const spot = enextoMonsterSpot(x, y, data);
        if (!spot) continue;

        const corpse = deadbookFloorCorpseAt(spot.x, spot.y);
        if (corpse && await reviveDeadbookCorpseItem(corpse, 'floor')) {
            revived++;
            continue;
        }

        const mon = await makemon(data, spot.x, spot.y, NO_MINVENT);
        if (mon) {
            spawned++;
            newsym(mon.mx, mon.my);
        }
    }

    game.level ??= {};
    game.level.flags ??= {};
    game.level.flags.graveyard = true;
    game._deadbook_mkundead_spawned = (game._deadbook_mkundead_spawned || 0) + spawned;
    game._deadbook_mkundead_revived = (game._deadbook_mkundead_revived || 0) + revived;
}

async function raiseDeadFromBook() {
    const messages = [];
    if (!rn2(3)) {
        const masterLich = monsterByRndName('master lich');
        const nalfeshnee = monsterByRndName('nalfeshnee');
        const mon = (masterLich && await makemon(masterLich, game.u?.ux || 0, game.u?.uy || 0, NO_MINVENT))
            || (nalfeshnee && await makemon(nalfeshnee, game.u?.ux || 0, game.u?.uy || 0, NO_MINVENT));
        if (mon) {
            mon.mpeaceful = 0;
            mon.pet = false;
            mon.mtame = 0;
            set_malign(mon);
            newsym(mon.mx, mon.my);
        }
    }
    messages.push(...await unturnDeadHeroInventoryFromBook());
    await mkundeadFromBook();
    game._deadbook_raise_dead_pending = 1;
    return messages;
}

async function bookOfTheDeadStudyMessage(item) {
    if (item) {
        item.known = true;
        item.kind = 'Book of the Dead';
        item.actualKind = 'Book of the Dead';
        item.line = normalInventoryLine({ ...item, line: '' });
    }

    const messages = ['You turn the pages of the Book of the Dead...'];
    if (heroOnInvocationSquare()) {
        const { bell, candelabrum } = inventoryInvocationItems();
        if (item?.cursed) {
            messages.push(game.u?.blind
                ? 'The Book seems to be ignoring you!'
                : "The runes appear scrambled.  You can't read them!");
            return messages.join('  ');
        }
        if (!bell || !candelabrum) {
            messages.push('A chill runs down your spine.');
            if (!bell) messages.push('You hear a faint chime...');
            if (!candelabrum) messages.push("Vlad's doppelganger is amused.");
            return messages.join('  ');
        }

        const candelabrumPrimed = candelabrumPrimedForInvocation(candelabrum);
        const bellPrimed = bellRecentlyRungForInvocation(bell);
        const relicCursed = (candelabrumPrimed && candelabrum.cursed) || (bellPrimed && bell.cursed);
        if (relicCursed) {
            messages.push('The invocation fails!');
            messages.push('At least one of your relics is cursed...');
            return messages.join('  ');
        }
        if (candelabrumPrimed && bellPrimed) {
            const soon = d(2, 6);
            performInvocationArea(messages);
            game.u.uevent ??= {};
            game.u.uevent.invoked = 1;
            game.u.uevent.udemigod = 1;
            if (!game.u.udg_cnt || game.u.udg_cnt > soon) game.u.udg_cnt = soon;
            game._chronicle_entries ??= [];
            game._chronicle_entries.push({ turn: game.moves || 1, text: 'performed the invocation ritual' });
            return messages.join('  ');
        }

        messages.push('You have a feeling that something is amiss...');
        messages.push('You raised the dead!');
        messages.push(...await raiseDeadFromBook());
    } else if (item?.cursed) {
        messages.push('You raised the dead!');
        messages.push(...await raiseDeadFromBook());
    } else if (item?.blessed) {
        pacifyUndeadForDeadbook();
    } else if (!item?.blessed) {
        switch (rn2(3)) {
        case 0:
            messages.push('Your ancestors are annoyed with you!');
            break;
        case 1:
            messages.push('The headstones in the cemetery begin to move!');
            break;
        default:
            messages.push('Oh my!  Your name appears in the book!');
            break;
        }
    }
    return messages.join('  ');
}

export async function processSpellbookStudyOccupation() {
    const study = game._spellbook_study_occupation;
    if (!study) return;
    if (study.turns > 0) {
        study.turns--;
        return;
    }

    game._spellbook_study_occupation = null;
    const item = (game.inventory || []).find(invItem => invItem.letter === study.itemLetter);
    const name = study.name || item?.spellName || item?.spell?.name || 'spell';
    const knownSpell = (game._known_spells || []).find(spell =>
        (spell.name || spell.spellName || spell.spell?.name) === name);

    exerciseAttribute(A_WIS, true);
    if (study.bookOfDead || isBookOfTheDeadItem(item)) {
        setTurnTailMessage(await bookOfTheDeadStudyMessage(item));
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 2);
        return;
    }

    let message;
    if (knownSpell) {
        message = `Your knowledge of the "${name}" spell is keener.`;
    } else {
        const spellLetter = String.fromCharCode('a'.charCodeAt(0) + (game._known_spells || []).length);
        game._known_spells = [...(game._known_spells || []), {
            name,
            level: study.level || item?.level || item?.spell?.level || 1,
            skill: study.skill || item?.spell?.skill || SPELL_CATEGORIES[name] || 'enchantment',
            learnedTurn: (game.moves || 1) + 2,
        }];
        message = spellLetter === 'a'
            ? `You learn the "${name}" spell.`
            : `You add the "${name}" spell to your repertoire, as '${spellLetter}'.`;
    }

    const usageMessages = [];
    if (item) checkUnpaidUsage(item, usageMessages);
    if (usageMessages.length) message = [message, ...usageMessages].join('  ');

    const width = game.nhDisplay?.cols || 80;
    const splitAfterMore = game._pending_message
        && game._pending_message.length + message.length + 3 >= width - 8;
    if (splitAfterMore) {
        game._spellbook_finish_after_topline_more = { itemLetter: study.itemLetter, name };
        setTurnTailMessage(message);
        return;
    }

    setTurnTailMessage(message);
    exerciseAttribute(A_WIS, true);
    if (item) {
        item.known = true;
        item.line = normalInventoryLine({ ...item, line: '' });
        const discoveryName = `spellbook of ${name}`;
        const appearance = item.appearance || game._object_descriptions?.spellbooks?.[item.spellbookIndex];
        const text = appearance ? `${discoveryName} (${appearance})` : discoveryName;
        const discovery = (game._discoveries || [])
            .find(entry => entry.section === 'Spellbooks' && entry.name === discoveryName);
        if (discovery) {
            discovery.text = text;
            discovery.starred = false;
        } else {
            game._discoveries ??= [];
            game._discoveries.push({ section: 'Spellbooks', name: discoveryName, text, starred: false, known: true });
        }
    }
    learnObjectScore('Spellbooks', `spellbook of ${name}`);
    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 2);
}

function inventoryOverlayLines(page = 0, identify = false, match = null) {
    const sectionName = item => item.section || ({
        coin: 'Coins', amulet: 'Amulets', weapon: 'Weapons', armor: 'Armor', food: 'Comestibles',
        scroll: 'Scrolls', spellbook: 'Spellbooks', potion: 'Potions',
        ring: 'Rings', wand: 'Wands', tool: 'Tools', gem: 'Gems/Stones', rock: 'Boulders/Statues',
    })[item.cls] || (item.otyp === SCROLL_CLASS ? 'Scrolls'
        : item.otyp === POTION_CLASS ? 'Potions'
            : item.otyp === WAND_CLASS ? 'Wands'
                : item.otyp === FOOD_CLASS ? 'Comestibles'
                    : item.otyp === TOOL_CLASS || item.otyp === MIRROR || item.otyp === EXPENSIVE_CAMERA || item.otyp === STETHOSCOPE || item.otyp === MAGIC_MARKER ? 'Tools'
                        : item.otyp === RING_CLASS ? 'Rings'
                            : isBoulderObject(item) || item.otyp === STATUE ? 'Boulders/Statues'
                                : item.otyp === GEM_CLASS ? 'Gems/Stones' : 'Other Items');
    const sectionOrder = [
        'Coins', 'Amulets', 'Weapons', 'Armor', 'Comestibles', 'Scrolls',
        'Spellbooks', 'Potions', 'Rings', 'Wands', 'Tools', 'Gems/Stones',
        'Boulders/Statues', 'Other Items',
    ];
    const rows = [];
    let lastSection = '';
    for (const item of [...(game.inventory || [])].filter(item => !match || match(item)).sort((a, b) => {
        const sectionDiff = sectionOrder.indexOf(sectionName(a)) - sectionOrder.indexOf(sectionName(b));
        return sectionDiff || String(a.letter || '').localeCompare(String(b.letter || ''));
    })) {
        const section = sectionName(item);
        if (section !== lastSection) {
            rows.push([section, identify ? 0 : 1]);
            lastSection = section;
        }
        if ((game.u?._statusSuffix || '').includes('Hallu')) {
            const objectIndex = rn2_on_display_rng(DISPLAY_OBJECT_GLYPHS.length);
            if (objectIndex + FIRST_DISPLAY_OBJECT === C_RANDOM_CORPSE)
                rn2_on_display_rng(DISPLAY_MONSTER_GLYPHS.length);
        }
        const name = identify ? identifiedInventoryLine(item) : normalInventoryLine(item);
        rows.push([name, 0]);
    }
    if (!rows.length) rows.push(['You are not carrying anything.', 0]);
    const start = page * 23;
    const pageRows = rows.slice(start, start + 23);
    game._inventory_overlay_total_pages = Math.max(1, Math.ceil(rows.length / 23));
    const footer = rows.length > start + 23 ? `(${page + 1} of ${game._inventory_overlay_total_pages})` : game._inventory_overlay_total_pages > 1 ? `(${page + 1} of ${game._inventory_overlay_total_pages})` : '(end)';
    const width = Math.max(footer.length, ...pageRows.map(([text]) => String(text).length));
    const col = rows.length >= 23 ? 1 : Math.max(0, 78 - width);
    const slice = pageRows.map(([text, attr], row) => [row, col, text, attr]);
    slice.push([slice.length, col, footer]);
    return slice;
}

function showInventoryOverlay(page = 0, identify = false, match = null) {
    const lines = inventoryOverlayLines(page, identify, match);
    const fullScreen = game._inventory_overlay_total_pages > 1 || lines.length >= 24;
    const clearCol = fullScreen ? null : Math.max(0, Math.min(...lines.map(([, col]) => col)) - 1);
    setOverlay(lines, fullScreen ? 24 : lines.length, fullScreen, clearCol);
}

function discoveryOverlayLines(page = 0) {
    const sectionOrder = [
        'Coins', 'Amulets', 'Weapons', 'Armor', 'Comestibles', 'Scrolls',
        'Spellbooks', 'Potions', 'Rings', 'Wands', 'Tools', 'Gems/Stones',
        'Boulders/Statues', 'Iron balls', 'Chains', 'Venoms', 'Other Items',
    ];
    const discoveries = (game._discoveries || [])
        .map(entry => ({ ...entry }));
    const addDiscovery = (section, name, text, starred = false) => {
        if (!discoveries.some(entry => entry.section === section && entry.name === name))
            discoveries.push({ section, name, text, starred });
    };
    const addDiscoveryBeforeSection = (section, name, text, starred = false) => {
        const existingIndex = discoveries.findIndex(entry => entry.section === section && entry.name === name);
        const entry = existingIndex >= 0 ? discoveries.splice(existingIndex, 1)[0] : { section, name, text, starred };
        const index = discoveries.findIndex(item => item.section === section);
        if (index >= 0) discoveries.splice(index, 0, entry);
        else discoveries.push(entry);
    };
    if ((game.level?.monsters || []).some(mon =>
        (mon.isshk || mon.data?.shopkeeper)
        && (mon.minvent || []).some(obj => obj.otyp === SKELETON_KEY)))
        addDiscoveryBeforeSection('Tools', 'key', 'key');
    for (const obj of game.level?.objects || []) {
        const loc = game.level?.at(obj.ox, obj.oy);
        const appearance = obj.appearance || game._object_descriptions?.spellbooks?.[obj.spellbookIndex];
        if (obj.cls === 'spellbook' && obj.known === false && appearance
            && loc?.seenv && (loc.disp_ch === '+' || loc.remembered_glyph?.ch === '+')) {
            addDiscovery('Spellbooks', `spellbook (${appearance})`, `spellbook (${appearance})`);
        }
    }
    for (const item of game.inventory || []) {
        const wand = item.known && (item.wand || (item.wandIndex != null ? IDENTIFIED_WAND_NAMES[item.wandIndex] : ''));
        if (!wand) continue;
        const index = item.wandIndex ?? WAND_NAME_TO_INDEX.get(wand);
        const appearance = game._object_descriptions?.wands?.[index]?.description;
        addDiscovery('Wands', `wand of ${wand}`, appearance ? `wand of ${wand} (${appearance})` : `wand of ${wand}`);
    }
    for (const item of game.inventory || []) {
        if (item.cls !== 'amulet' || item.known || !(item.worn || /being worn/.test(item.line || ''))) continue;
        const lineAppearance = String(item.line || '').match(/\b([a-z][a-z -]*) amulet\b/i)?.[1]?.toLowerCase();
        const appearance = item.appearance
            || game._object_descriptions?.amulets?.[item.amuletIndex]
            || (lineAppearance && lineAppearance !== 'an' && lineAppearance !== 'a' ? lineAppearance : '');
        if (appearance)
            addDiscovery('Amulets', `amulet (${appearance})`, `amulet (${appearance})`);
    }
    const body = [];
    for (const section of sectionOrder) {
        const entries = discoveries
            .filter(entry => entry.section === section);
        if (!entries.length) continue;
        body.push([section, 1]);
        for (const entry of entries) body.push([`${entry.starred ? '*' : ' '} ${entry.text}`, 0]);
    }
    if (!body.length) body.push(['You have discovered nothing yet.', 0]);
    const firstPageSize = 21;
    const pageSize = page ? 23 : firstPageSize;
    const start = page ? firstPageSize + (page - 1) * pageSize : 0;
    const rows = page ? [] : [[0, 0, 'Discoveries, by order of discovery within each class']];
    const rowOffset = page ? 0 : 2;
    for (const [i, [text, attr]] of body.slice(start, start + pageSize).entries())
        rows.push([i + rowOffset, 0, text, attr]);
    rows.push([23, 0, '--More--']);
    game._discoveries_has_more = body.length > start + pageSize;
    return rows;
}

function deathVanquishedLines() {
    const killed = game._vanquished || [];
    const total = game._vanquished_total || killed.length;
    const rows = [[0, 41, 'Vanquished creatures:']];
    if (!killed.length) rows.push([2, 43, 'No creatures vanquished.']);
    else {
        killed.slice(0, 18).forEach((name, i) => rows.push([2 + i, 43, name]));
        rows.push([3 + Math.min(killed.length, 18), 41, `${total} creature${total === 1 ? '' : 's'} vanquished.`]);
    }
    rows.push([killed.length ? Math.min(23, rows[rows.length - 1][0] + 1) : 3, 41, '--More--']);
    return rows;
}

function deathOverviewLines() {
    const dsum = deathSummary();
    if (dsum.dungeon === 'The Tutorial') {
        return [
            [0, 41, 'The Dungeons of Doom:'],
            [1, 44, 'Level 1:'],
            [2, 41, 'The Tutorial:'],
            [3, 44, `Level ${dsum.depth}: <- You were here.`],
            [4, 47, 'Final resting place for'],
            [5, 50, `you, ${dsum.cause}.`],
            [6, 41, '(end)'],
        ];
    }
    const dnum = game.u?.uz?.dnum ?? 0;
    const levels = new Set([game.u?.uz?.dlevel || dsum.depth]);
    for (const key of game._saved_levels?.keys?.() || []) {
        const [savedDnum, savedLevel] = String(key).split(':').map(Number);
        if (savedDnum === dnum && savedLevel) levels.add(savedLevel);
    }
    const ordered = [...levels].sort((a, b) => a - b);
    const minLevel = ordered[0] || dsum.depth;
    const maxLevel = ordered[ordered.length - 1] || dsum.depth;
    const rows = [[0, 40, `${dsum.dungeon}:${maxLevel > minLevel ? ` levels ${minLevel} to ${maxLevel}` : ''}`]];
    let row = 1;
    for (const level of ordered) {
        rows.push([row++, 43, `Level ${level}:${level === dsum.depth ? ' <- You were here.' : ''}`]);
        if (level === dsum.depth) {
            rows.push([row++, 46, 'Final resting place for']);
            rows.push([row++, 49, `you, ${dsum.cause}.`]);
        }
    }
    rows.push([row, 40, '(end)']);
    return rows;
}

function overviewKnownFeatures(level) {
    const feat = { nshop: 0, shoptype: 0, ntemple: 0, naltar: 0, nthrone: 0, nfount: 0, nsink: 0, ngrave: 0, ntree: 0 };
    const remembered = loc => !!(loc && (loc.seenv || loc.lastseentyp != null));
    for (let i = 0; i < (level?.rooms || []).length; i++) {
        const room = level.rooms[i];
        if (!(room?.rtype >= SHOPBASE || room?.rtype === TEMPLE)) continue;
        let seen = false;
        for (let x = room.lx; x <= room.hx && !seen; x++)
            for (let y = room.ly; y <= room.hy; y++)
                if (remembered(level.at(x, y))) {
                    seen = true;
                    break;
                }
        if (!seen) continue;
        if (room.rtype >= SHOPBASE) {
            feat.nshop = Math.min(3, feat.nshop + 1);
            feat.shoptype = feat.shoptype && feat.shoptype !== room.rtype ? SHOPBASE : room.rtype;
        } else {
            feat.ntemple = Math.min(3, feat.ntemple + 1);
        }
    }
    for (let x = 1; x < COLNO; x++)
        for (let y = 0; y < ROWNO; y++) {
            const loc = level?.at(x, y);
            if (!remembered(loc)) continue;
            const typ = loc.lastseentyp ?? loc.typ;
            if (typ === FOUNTAIN) feat.nfount = Math.min(3, feat.nfount + 1);
            else if (typ === SINK) feat.nsink = Math.min(3, feat.nsink + 1);
            else if (typ === THRONE) feat.nthrone = Math.min(3, feat.nthrone + 1);
            else if (typ === ALTAR) feat.naltar = Math.min(3, feat.naltar + 1);
            else if (typ === GRAVE) feat.ngrave = Math.min(3, feat.ngrave + 1);
            else if (typ === TREE) feat.ntree = Math.min(3, feat.ntree + 1);
        }
    return feat;
}

function overviewFeatureSentence(feat) {
    const parts = [];
    const article = noun => /^(?:the|an?|some|many)\b/i.test(noun) ? noun : `${/^[aeiou]/i.test(noun) ? 'an' : 'a'} ${noun}`;
    const counted = (noun, count) => {
        const plural = noun === 'tree' ? 'trees' : `${noun}s`;
        return count === 1 ? article(noun) : count === 2 ? `some ${plural}` : `many ${plural}`;
    };
    if (feat.nshop > 1) parts.push(counted('shop', feat.nshop));
    else if (feat.nshop) {
        const shop = SHOP_TYPES[(feat.shoptype || SHOPBASE) - SHOPBASE]?.annotation
            || SHOP_TYPES[(feat.shoptype || SHOPBASE) - SHOPBASE]?.name
            || 'shop';
        parts.push(article(shop));
    }
    if (feat.ntemple && feat.naltar) parts.push(`${counted('temple', feat.ntemple)} and ${counted('altar', feat.naltar)}`);
    else if (feat.ntemple) parts.push(counted('temple', feat.ntemple));
    else if (feat.naltar) parts.push(counted('altar', feat.naltar));
    for (const [noun, count] of [['throne', feat.nthrone], ['fountain', feat.nfount], ['sink', feat.nsink], ['grave', feat.ngrave], ['tree', feat.ntree]])
        if (count) parts.push(counted(noun, count));
    if (!parts.length) return '';
    const sentence = parts.join(', ');
    return `${sentence.charAt(0).toUpperCase()}${sentence.slice(1)}.`;
}

function overviewBranchSentence(entry) {
    for (let stair = entry.stairs; stair; stair = stair.next) {
        if (!stair.u_traversed || stair.tolev?.dnum === entry.dnum) continue;
        const branch = (game.branches || []).find(candidate =>
            candidate.end1.dnum === entry.dnum
            && candidate.end1.dlevel === entry.dlevel
            && candidate.end2.dnum === stair.tolev.dnum);
        if (!branch) continue;
        const dest = game.dungeons?.[branch.end2.dnum];
        const kind = branch.type === 'portal' ? 'Portal'
            : branch.type === 'stair' ? (branch.end1_up ? 'Stairs up' : 'Stairs down')
                : branch.end1_up ? 'One way stairs up' : 'Connection';
        const destDepth = (dest?.depth_start ?? 1) + branch.end2.dlevel - 1;
        const suffix = branch.end1_up && dest ? `, level ${destDepth}` : '';
        return `${kind} to ${dest?.name || 'Dungeon'}${suffix}.`;
    }
    return '';
}

function overviewLines() {
    const currentDnum = game.u?.uz?.dnum ?? 0;
    const currentDlevel = game.u?.uz?.dlevel || 1;
    const entries = new Map();
    for (const key of game._saved_levels?.keys?.() || []) {
        const [dnum, dlevel] = String(key).split(':').map(Number);
        if (!Number.isFinite(dnum) || !Number.isFinite(dlevel)) continue;
        const saved = game._saved_levels.get(key);
        entries.set(key, { dnum, dlevel, level: saved?.level, stairs: saved?.stairs });
    }
    entries.set(`${currentDnum}:${currentDlevel}`, { dnum: currentDnum, dlevel: currentDlevel, level: game.level, stairs: game.stairs });

    const reached = new Map();
    for (const entry of entries.values()) {
        const dungeon = game.dungeons?.[entry.dnum];
        const buildsUp = dungeon?.entry_lev === dungeon?.num_dunlevs && dungeon.num_dunlevs > 1;
        const prior = reached.get(entry.dnum);
        if (prior == null) reached.set(entry.dnum, entry.dlevel);
        else reached.set(entry.dnum, buildsUp ? Math.min(prior, entry.dlevel) : Math.max(prior, entry.dlevel));
    }

    const rows = [];
    let row = 0;
    for (const dnum of [...new Set([...entries.values()].map(entry => entry.dnum))].sort((a, b) => a - b)) {
        const dungeon = game.dungeons?.[dnum];
        const name = dungeon?.name || (dnum === 0 ? 'The Dungeons of Doom' : 'Dungeon');
        const levels = [...entries.values()].filter(entry => entry.dnum === dnum).sort((a, b) => a.dlevel - b.dlevel);
        const depthStart = dungeon?.depth_start ?? 1;
        const entryLevel = dungeon?.entry_lev ?? 1;
        const reachedLevel = reached.get(dnum) ?? entryLevel;
        const buildsUp = dungeon?.entry_lev === dungeon?.num_dunlevs && dungeon.num_dunlevs > 1;
        const reachedDepth = depthStart + reachedLevel - 1;
        const entryDepth = depthStart + entryLevel - 1;
        let header = `${name}:`;
        if (reachedLevel !== entryLevel)
            header += buildsUp ? ` levels ${entryDepth} up to ${reachedDepth}` : ` levels ${depthStart} to ${reachedDepth}`;
        rows.push([row++, 0, header, 1]);

        let printed = false;
        for (const entry of levels) {
            const annotation = game._level_annotations?.get(`${entry.dnum}:${entry.dlevel}`);
            const feat = overviewKnownFeatures(entry.level);
            const featureLine = overviewFeatureSentence(feat);
            const branchLine = overviewBranchSentence(entry);
            const isHere = entry.dnum === currentDnum && entry.dlevel === currentDlevel;
            if (!isHere && !annotation && !featureLine && !branchLine && entry.dlevel !== reachedLevel) continue;
            const depth = depthStart + entry.dlevel - 1;
            const special = game.flags?.debug
                ? game.specialLevels?.find(level => level.dnum === entry.dnum && level.dlevel === entry.dlevel)?.name
                : '';
            const here = isHere ? ' <- You are here.' : '';
            rows.push([row++, 3, `Level ${depth}:${special ? ` [${special}]` : ''}${annotation ? ` "${annotation}"` : ''}${here}`]);
            if (featureLine) rows.push([row++, 6, featureLine]);
            if (branchLine) rows.push([row++, 6, branchLine]);
            printed = true;
        }
        if (!printed) {
            rows.pop();
            row--;
        }
    }
    rows.push([row, 0, '(end)']);
    const width = Math.max(...rows.map(([, col, text]) => col + String(text).length));
    const baseCol = Math.min(41, Math.max(0, COLNO - width - 2));
    return rows.map(([line, col, text, ...rest]) => [line, col + baseCol, text, ...rest]);
}

function vanquishedLines() {
    const killed = game._vanquished || [];
    const total = game._vanquished_total || killed.length;
    const rows = [[0, 41, 'Vanquished creatures:']];
    if (!killed.length) {
        rows.push([2, 43, 'No creatures vanquished.']);
    } else {
        killed.slice(0, 18).forEach((name, i) => rows.push([2 + i, 43, name]));
        rows.push([3 + Math.min(killed.length, 18), 41,
            `${total} creature${total === 1 ? '' : 's'} vanquished.`]);
    }
    rows.push([Math.min(23, rows[rows.length - 1][0] + 1), 41, '--More--']);
    return rows;
}

function deathGraveLines() {
    const dsum = deathSummary();
    const farewell = {
        Knight: 'Fare thee well',
        Samurai: 'Sayonara',
        Tourist: 'Aloha',
        Valkyrie: 'Velkommen',
    }[dsum.role] || 'Goodbye';
    const graveText = dsum.name.slice(0, 16);
    const graveName = graveText.padStart(Math.floor((16 + graveText.length) / 2)).padEnd(16);
    const cause = dsum.cause.replace(/^killed by /, 'killed by ');
    const causeLines = [];
    let causeRest = cause;
    while (causeRest && causeLines.length < 3) {
        if (causeRest.length <= 16) {
            causeLines.push(causeRest);
            break;
        }
        const split = Math.max(1, causeRest.lastIndexOf(' ', 16));
        causeLines.push(causeRest.slice(0, split));
        causeRest = causeRest.slice(split).trim();
    }
    while (causeLines.length < 3) causeLines.push('');
    const centeredCause = causeLines.map(line =>
        line.padStart(Math.floor((16 + line.length) / 2)).padEnd(16));
    const goldText = `${dsum.gold} Au`;
    const centeredGold = goldText
        .padStart(Math.floor((18 + goldText.length) / 2))
        .padEnd(18);
    const deathVerb = dsum.cause.startsWith('burned by') ? 'burned' : 'died';
    const summaryVerb = dsum.outsideDungeon === 'heaven' ? 'passed away' : deathVerb;
    const summaryPlace = dsum.outsideDungeon
        ? 'beyond the confines of the dungeon'
        : `in ${dsum.dungeon} on dungeon level ${dsum.depth}`;
    return [
        [1, 23, '----------'],
        [2, 22, '/          \\'],
        [3, 21, '/    REST    \\'],
        [4, 20, '/      IN      \\'],
        [5, 19, '/     PEACE      \\'],
        [6, 18, '/                  \\'],
        [7, 18, `| ${graveName} |`],
        [8, 18, `|${centeredGold}|`],
        [9, 18, `| ${centeredCause[0]} |`],
        [10, 18, `| ${centeredCause[1]} |`],
        [11, 18, `| ${centeredCause[2]} |`],
        [12, 18, '|                  |'],
        [13, 18, '|       2026       |'],
        [14, 17, '*|     *  *  *      | *'],
        [15, 8, '_________)/\\\\_//(\\/(/\\)/\\//\\/|_)_______'],
        [18, 0, `${farewell} ${dsum.name} the ${dsum.role}...`],
        [20, 0, `You ${summaryVerb} ${summaryPlace} with ${dsum.score} point${dsum.score === 1 ? '' : 's'},`],
        [21, 0, `and ${dsum.gold} piece${dsum.gold === 1 ? '' : 's'} of gold, after ${dsum.turns} moves.`],
        [22, 0, `You were level ${dsum.level} with a maximum of ${dsum.hpmax} hit points when you ${deathVerb}.`],
        [23, 0, '--More--'],
    ];
}

function escapedSummaryLines() {
    const dsum = deathSummary();
    const farewell = {
        Knight: 'Fare thee well',
        Samurai: 'Sayonara',
        Tourist: 'Aloha',
        Valkyrie: 'Velkommen',
    }[dsum.role] || 'Goodbye';
    return [
        [0, 0, `${farewell} ${dsum.name} the ${dsum.role}...`],
        [2, 0, `You escaped from the dungeon with ${dsum.score} point${dsum.score === 1 ? '' : 's'},`],
        [3, 0, `and ${dsum.gold} piece${dsum.gold === 1 ? '' : 's'} of gold, after ${dsum.turns} moves.`],
        [4, 0, `You were level ${dsum.level} with a maximum of ${dsum.hpmax} hit points when you escaped.`],
        [23, 0, '--More--'],
    ];
}

function deathScoreLines() {
    const dsum = deathSummary();
    const role = ROLE_ABBREVIATIONS[dsum.role] || dsum.role.slice(0, 3);
    const race = RACE_ABBREVIATIONS[dsum.race] || dsum.race.slice(0, 3);
    const gender = dsum.gender === 'female' ? 'Fem' : 'Mal';
    const align = { lawful: 'Law', neutral: 'Neu', chaotic: 'Cha' }[dsum.align] || 'Neu';
    const current = {
        score: dsum.score,
        name: dsum.name,
        role,
        race,
        gender,
        align,
        dungeon: dsum.dungeon,
        depth: dsum.depth,
        maxlvl: dsum.maxlvl,
        hp: dsum.hp,
        hpmax: dsum.hpmax,
        cause: game._escaped_game ? 'escaped' : game._quit_game ? 'quit' : dsum.cause,
    };
    let entries = JSON.parse(vfsReadFile('/record') || '[]');
    const unrankedCurrent = current.score <= 0;
    let currentRank = 0;
    if (unrankedCurrent) {
        currentRank = entries.length + 1;
        entries = [...entries, current];
    } else {
        let rank0 = entries.findIndex(entry => entry.score < current.score);
        if (rank0 < 0) rank0 = entries.length;
        entries.splice(rank0, 0, current);
        vfsWriteFile('/record', JSON.stringify(entries.slice(0, 100)));
        currentRank = rank0 + 1;
    }

    const rows = unrankedCurrent
        ? [[1, 0, ' No  Points     Name                                                   Hp [max]']]
        : [
            [1, 0, 'You made the top ten list!'],
            [3, 1, 'No  Points     Name                                                   Hp [max]'],
        ];
    let row = unrankedCurrent ? 2 : 4;
    for (const [index, entry] of entries.entries()) {
        const actualRank = index + 1;
        const rank = unrankedCurrent && entry === current ? 0 : actualRank;
        const wanted = unrankedCurrent
            ? actualRank <= 3 || (actualRank >= currentRank - 2 && actualRank <= currentRank + 2)
            : rank <= 3 || (rank >= currentRank - 2 && rank <= currentRank + 2);
        if (!wanted) continue;
        if (actualRank === currentRank - 2 && currentRank > 6) rows.push([row++, 0, '']);

        const attr = rank === currentRank || entry === current ? 2 : 0;
        const name = `${entry.name.slice(0, 10)}-${entry.role}-${entry.race}-${entry.gender}-${entry.align}`;
        const scoreCause = entry.cause.replace(/; the /g, ', the ');
        const rankText = rank ? String(rank).padStart(3) : '   ';
        const points = entry.score || (entry.cause === 'quit' ? 0 : dsum.exp || 0);
        let line = `${rankText} ${String(points).padStart(10)}  ${name} `;
        if (entry.cause === 'escaped') {
            line += `escaped the dungeon [max level ${entry.maxlvl ?? entry.depth}].`;
        } else {
            const outcome = entry.cause === 'quit' ? 'quit' : 'died';
            line += `${outcome} in ${entry.dungeon}`;
            line += ` on level ${entry.depth}`;
            if (entry.depth !== entry.maxlvl) line += ` [max ${entry.maxlvl}]`;
            if (entry.cause === 'quit') line += '.';
            else line += `.  ${scoreCause[0].toUpperCase()}${scoreCause.slice(1)}.`;
        }

        const wrapped = [];
        const wrapCol = COLNO - 10;
        while (line.length >= wrapCol) {
            let split = -1;
            for (let col = Math.min(line.length - 1, wrapCol - 1); col >= 0; col--) {
                if (line[col] === ' ') {
                    split = col;
                    break;
                }
            }
            if (split < 15) split = wrapCol - 1;
            if (split > 5 && line.slice(split - 5, split) === ' [max') split -= 5;
            const next = line[split] === ' ' ? line.slice(split + 1) : line.slice(split);
            wrapped.push(line.slice(0, split));
            line = `${''.padStart(15)} ${next}`;
        }

        const hp = entry.hp <= 0 ? '-' : String(entry.hp);
        const hpCol = COLNO - 7 - hp.length;
        if (line.length <= hpCol) {
            const maxhpPad = entry.hpmax < 10 ? '  ' : entry.hpmax < 100 ? ' ' : '';
            line += `${' '.repeat(hpCol - line.length)}${hp} ${maxhpPad}[${entry.hpmax}]`;
        }
        wrapped.push(line);

        for (const text of wrapped) {
            rows.push(attr ? [row++, 0, text, attr] : [row++, 0, text]);
        }
    }
    return rows;
}

function magicTrapResult(trap) {
    trap.tseen = true;
    if (!rn2(30)) {
        game.level.traps = (game.level?.traps || []).filter(item => item !== trap);
        const damage = rnd(10);
        if (game.u) {
            game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            game.u.uenmax = (game.u.uenmax || 0) + 2;
            game.u.uen = game.u.uenmax;
            game.u.uenpeak = Math.max(game.u.uenpeak || 0, game.u.uenmax);
        }
        newsym(game.u?.ux || 0, game.u?.uy || 0);
        return { message: 'You are caught in a magical explosion!', afterMore: 'Your body absorbs some of the magical energy.' };
    }

    const fate = rnd(20);
    if (fate < 10) {
        const count = rnd(4);
        const messages = [];
        if (!game.u?.blind) messages.push('You are momentarily blinded by a flash of light!');
        rn1(5, 10);
        game.u.blind = true;
        if ((game.u?._statusSuffix || '').includes('Deaf')) {
            messages.push('You feel rankled.');
            rn1(5, 15);
        } else {
            messages.push('You hear a deafening roar!');
            game.u._deafTimeout = Math.max(game.u._deafTimeout || 0, rn1(20, 30));
            game.u._statusSuffix = `${game.u._statusSuffix || ''} Deaf`;
        }
        for (let i = 0; i < count; i++) makemon(null, game.u?.ux || 0, game.u?.uy || 0, 0);
        return { message: messages.join('  '), more: messages.length > 1 };
    }

    if (fate === 11) {
        const wasInvisible = !!game.u?.invisible;
        if (game.u?.blind) {
            if (game.u) game.u.invisible = !wasInvisible;
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            return { message: 'You hear a low hum.' };
        }
        game._magic_trap_invisible_after_more = !wasInvisible;
        if (!wasInvisible) {
            return {
                message: 'You hear a low hum.',
                afterMore: `Gee!  All of a sudden, you ${game.u?.seeInvisible ? 'can see right through yourself' : "can't see yourself"}.`,
            };
        }
        return {
            message: 'You hear a low hum.',
            afterMore: game.u?.seeInvisible ? "You can't see through yourself anymore." : 'You can see yourself again!',
        };
    }

    const message = {
        12: 'A tower of flame erupts from the floor!',
        13: 'A shiver runs up and down your spine!',
        14: 'You hear distant howling.',
        15: 'You suddenly yearn for your distant homeland.',
        16: 'Your pack shakes violently!',
        17: 'You smell charred flesh.',
        18: 'You feel tired.',
        19: 'You feel charismatic!',
        20: 'You feel like someone is helping you.',
    }[fate] || '';
    if (fate === 19 && game.u?.acurr?.a) game.u.acurr.a[A_CHA] = Math.min(25, (game.u.acurr.a[A_CHA] || 3) + 1);
    return { message };
}

async function moveHero(dx, dy) {
    game.u.dx = dx;
    game.u.dy = dy;
    const oldx = game.u?.ux || 0;
    const oldy = game.u?.uy || 0;
    const oldLoc = game.level?.at(oldx, oldy);
    if (game._running_continuation) {
        const runMode = game._run_mode || 1;
        let corrct = 0;
        let noturn = 0;
        let x0 = 0;
        let y0 = 0;
        let i0 = 9;
        let m0 = 0;

        for (let x = oldx - 1; x <= oldx + 1; x++) {
            for (let y = oldy - 1; y <= oldy + 1; y++) {
                if (x === oldx && y === oldy) continue;
                const loc = game.level?.at(x, y);
                if (!loc || loc.typ === STONE) continue;
                if (x === oldx - dx && y === oldy - dy) continue;

                let corridor = false;
                const mon = (game.level?.monsters || []).find(mtmp => mtmp.mx === x && mtmp.my === y);
                if (loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED))) {
                    if (x !== oldx && y !== oldy) continue;
                    if (runMode !== 1) {
                        game._run_steps_remaining = 0;
                        game._run_stop_now = 1;
                        game.context.move = 0;
                        return;
                    }
                    corridor = true;
                } else if (loc.typ === CORR) {
                    corridor = true;
                } else if (!IS_OBSTRUCTED(loc.typ) && loc.typ !== ROOM && runMode === 1) {
                    corridor = true;
                }
                if (!corridor) continue;

                if (oldLoc?.typ !== ROOM) {
                    if (runMode === 1 || runMode === 3 || runMode === 8) {
                        const dist = (x - (oldx + dx)) ** 2 + (y - (oldy + dy)) ** 2;
                        if (dist > 2) continue;
                        if (corrct === 1 && (x - x0) ** 2 + (y - y0) ** 2 !== 1) noturn = 1;
                        if (dist < i0) {
                            i0 = dist;
                            x0 = x;
                            y0 = y;
                            m0 = mon ? 1 : 0;
                        }
                    }
                    corrct++;
                }
            }
        }

        if ((runMode === 1 || runMode === 3 || runMode === 8)
            && !noturn && !m0 && i0
            && (corrct === 1 || (corrct === 2 && i0 === 1))) {
            let turn;
            if (i0 === 2) {
                turn = dx === y0 - oldy && dy === oldx - x0 ? 2 : -2;
            } else if (dx && dy) {
                turn = (dx === dy && y0 === oldy) || (dx !== dy && y0 !== oldy) ? -1 : 1;
            } else {
                turn = (x0 - oldx === y0 - oldy && !dy) || (x0 - oldx !== y0 - oldy && dy) ? 1 : -1;
            }
            turn += game._last_str_turn || 0;
            if (turn >= -2 && turn <= 2) {
                game._last_str_turn = turn;
                dx = x0 - oldx;
                dy = y0 - oldy;
                const nextRunKey = Object.keys(DIR_DX).find(key => DIR_DX[key] === dx && DIR_DY[key] === dy) || game._run_key;
                game._run_key = nextRunKey;
            }
        }
    }
    const swallowedMove = !!(game.u?.uswallow && game.u?.ustuck);
    let newx = oldx + dx;
    let newy = oldy + dy;
    if (swallowedMove) {
        newx = game.u.ustuck.mx;
        newy = game.u.ustuck.my;
        game.u.ux = newx;
        game.u.uy = newy;
    }
    const target = game.level?.at(newx, newy);
    const nopick = game._move_nopick_prefix;
    game._move_nopick_prefix = 0;
    game._request_menu_prefix = 0;
    const targetTyp = target?.typ;
    const targetMoveTyp = movementSurfaceTerrain(target);
    const oldMoveTyp = movementSurfaceTerrain(oldLoc);
    const doorlessTarget = targetTyp === DOOR && (target.doormask === D_NODOOR || target.doormask === D_BROKEN);
    const afterDoor = doorlessTarget ? game.level?.at(newx + dx, newy + dy) : null;
    if (game._doorway_message_run && game._running_continuation && doorlessTarget && oldLoc?.typ === CORR && afterDoor?.typ === ROOM) {
        game._doorway_message_run = 0;
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
        game.context.move = 0;
        return;
    }
    const liquidTarget = movementIsLiquidAt(newx, newy, target);
    const targetBoulder = game.level?.objects?.find(obj =>
        !obj.transientProjectile && obj.ox === newx && obj.oy === newy && obj.otyp === BOULDER);

    if (!swallowedMove && isBuriedBallTrapActive()) {
        const found = findBuriedBallNear(oldx, oldy);
        if (found && (newx - found.x) ** 2 + (newy - found.y) ** 2 <= 2) {
            if (game.flags?.verbose !== false) await setMessage("You move within the chain's reach.");
        } else {
            game.u.utrap = Math.max(0, (game.u.utrap || 0) - 1);
            const steedName = game.u?.usteed?.givenName || game.u?.usteed?.data?.name || 'steed';
            const message = game.u.utrap > 0
                ? game.u?.usteed
                    ? `You and ${steedName} are chained to the buried ball.`
                    : 'You are chained to the buried ball.'
                : game.u?.usteed
                    ? `${steedName[0]?.toUpperCase() || 'S'}${steedName.slice(1)} finally wrenches the ball free.`
                    : 'You finally wrench the ball free.';
            if (!game.u.utrap) {
                const restored = buriedBallToPunishment();
                if (!restored && game.u) game.u.utraptype = null;
            }
            if ((game.flags?.verbose !== false || !game.u.utrap) && game._last_trapmove_message !== message) {
                await setMessage(message);
            } else {
                game._pending_message = '';
                game._keep_pending_message = 0;
            }
            game._last_trapmove_message = message;
            game.context.move = 1;
            return;
        }
    }

    if (!swallowedMove && game.level?.flags?.sokoban_rules && dx && dy) {
        const boulderBlocksDiagonal = (x, y) =>
            game.level?.objects?.some(obj => !obj.transientProjectile && obj.ox === x && obj.oy === y && obj.otyp === BOULDER);
        if (boulderBlocksDiagonal(oldx + dx, oldy) && boulderBlocksDiagonal(oldx, oldy + dy)) {
            await setMessage('You cannot pass that way.');
            game.context.move = 0;
            return;
        }
    }

    if (!swallowedMove && targetBoulder && dx && dy && game.level?.flags?.sokoban_rules) {
        await setMessage('You try to move the boulder, but in vain.');
        game.context.move = 0;
        return;
    }
    if (!swallowedMove && targetBoulder) {
        const pushx = newx + dx;
        const pushy = newy + dy;
        const pushLoc = game.level?.at(pushx, pushy);
        const pushMonster = game.level?.monsters?.find(mtmp => mtmp.mx === pushx && mtmp.my === pushy);
        if (pushMonster) {
            if (pushLoc) {
                pushLoc.map_invisible = true;
                pushLoc.waslit = true;
                pushLoc.remembered_glyph = { ch: 'I', color: NO_COLOR, dec: false };
            }
            newsym(pushx, pushy);
            game._queued_message_after_more = "Perhaps that's why you cannot move it.";
            await setMessage('You hear a monster behind the boulder.', true);
            game.context.move = 0;
            return;
        }
        const blocked = !pushLoc || IS_OBSTRUCTED(pushLoc.typ)
            || game.level?.objects?.some(obj => !obj.transientProjectile && obj.ox === pushx && obj.oy === pushy && obj.otyp === BOULDER)
            || pushMonster;
        if (blocked) {
            await setMessage('You try to move the boulder, but in vain.');
            game.context.move = 0;
            return;
        }
        const pushTurn = game.moves || 1;
        if (game._bldrpush_object !== targetBoulder) {
            game._bldrpush_object = targetBoulder;
            game._bldrpushtime = pushTurn + 1;
        }
        const showPushMessage = pushTurn > (game._bldrpushtime || 0) + 2
            || pushTurn < (game._bldrpushtime || 0);
        exerciseAttribute(A_STR, true);
        if (pushLoc?.map_invisible) {
            pushLoc.map_invisible = false;
            if (pushLoc.remembered_glyph?.ch === 'I') pushLoc.remembered_glyph = null;
        }
        targetBoulder.ox = pushx;
        targetBoulder.oy = pushy;
        const boulderIndex = game.level.objects.indexOf(targetBoulder);
        if (boulderIndex >= 0) {
            game.level.objects.splice(boulderIndex, 1);
            game.level.objects.push(targetBoulder);
        }
        const boulderShopMessage = adjustBoulderPushShopBill(targetBoulder, newx, newy, pushx, pushy);
        vision_reset();
        game._bldrpushtime = pushTurn;
        game.u.ux0 = oldx;
        game.u.uy0 = oldy;
        game.u.ux = newx;
        game.u.uy = newy;
        game.u.umoved = true;
        game.context.move = 1;
        game._run_stop_now = 1;
        game._run_steps_remaining = 0;
        newsym(oldx, oldy);
        newsym(newx, newy);
        newsym(pushx, pushy);
        vision_recalc(0);
        if (showPushMessage) {
            const pushMessage = 'With great effort you move the boulder.';
            await setMessage(boulderShopMessage ? `${pushMessage}  ${boulderShopMessage}` : pushMessage);
        } else if (boulderShopMessage) {
            await setMessage(boulderShopMessage);
        } else {
            game._pending_message = '';
            game._keep_pending_message = 0;
        }
        return;
    }

	    if (!swallowedMove && game.u?.utraptype === 'beartrap' && game.u.utrap > 0) {
	        if ((dx && dy) || !rn2(5)) game.u.utrap--;
	        const escaped = game.u.utrap <= 0;
	        const caughtMessage = 'You are caught in a bear trap.';
	        const trapMessage = escaped
	            ? (game._last_trapmove_message === caughtMessage
	                ? 'You finally wriggle free.'
	                : 'You are caught in a bear trap.  You finally wriggle free.')
	            : caughtMessage;
        if (!escaped && game._last_trapmove_message === trapMessage) {
            game._pending_message = '';
            game._keep_pending_message = 0;
        } else {
            game._last_trapmove_message = trapMessage;
            await setMessage(trapMessage);
        }
        if (escaped) {
            game.u.utrap = 0;
            game.u.utraptype = null;
        }
        game.context.move = 1;
        return;
    }

    const statusSuffix = game.u?._statusSuffix || '';
    const safeHeroStatus = !statusSuffix.includes('Conf')
        && !statusSuffix.includes('Hallu')
        && !statusSuffix.includes('Stun');
    const targetMonster = swallowedMove
        ? null
        : game.level?.monsters?.find(mtmp => mtmp.mx === newx && mtmp.my === newy);
    if (!swallowedMove && target?.map_invisible && !targetMonster) {
        target.map_invisible = false;
        target.remembered_glyph = null;
        for (const obj of game.level?.objects || [])
            if (obj.ox === newx && obj.oy === newy) obj.seen = false;
        newsym(newx, newy);
        await setMessage(target && !IS_OBSTRUCTED(target.typ) ? 'You attack thin air.' : 'You harmlessly attack the wall.');
        if (!target || IS_OBSTRUCTED(target.typ)) game._fight_wall_message = 1;
        game._run_stop_now = 1;
        game._run_steps_remaining = 0;
        game.context.move = 1;
        return;
    }
    const targetBlocksDoorStep = targetMonster
        && (!targetMonster.pet || !targetMonster.mpeaceful || game.flags?.safe_pet === false || !safeHeroStatus);

    if (!swallowedMove && dx && dy
        && !targetBlocksDoorStep
        && ((target?.typ === DOOR && (target.doormask & D_ISOPEN))
            || (oldLoc?.typ === DOOR && (oldLoc.doormask & D_ISOPEN)))) {
        game.context.move = 0;
        return;
    }

    const pet = targetMonster?.pet ? targetMonster : null;
    const safePet = pet && pet.mpeaceful && game.flags?.safe_pet !== false && safeHeroStatus;
    if (pet && game._running_continuation) {
        game._run_steps_remaining = 0;
        game.context.move = 0;
        return;
    }
    if (safePet) {
        const runningStep = game._running_continuation || game._initial_run_command || game._run_steps_remaining > 0;
        if (!runningStep) {
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
        }
        const avoidSwap = rn2(7) === 0;
        const petName = pet.givenName || `${pet.saddled ? 'saddled ' : ''}${pet.data?.name || 'pet'}`;
        if (avoidSwap) {
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            let fleeTime = rnd(6);
            if (fleeTime === 1) fleeTime++;
            pet.mflee = 1;
            pet.mfleetim = Math.min(fleeTime + (pet.mfleetim || 0), 127);
            clearMonsterTrack(pet);
            game.context.move = 1;
            await setMessage(`You stop.  ${pet.givenName ? petName : `Your ${petName}`} is in the way!`);
            return;
        }
        pet.mx = oldx;
        pet.my = oldy;
        game.u.ux0 = oldx;
        game.u.uy0 = oldy;
        game.u.ux = newx;
        game.u.uy = newy;
        game.u.umoved = true;
        game.context.move = 1;
        const swapStopsRun = !game._initial_run_command
            || target?.typ === DOOR || target?.typ === STAIRS
            || target?.typ === LADDER || target?.typ === FOUNTAIN
            || target?.typ === SINK;
        if (swapStopsRun) {
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
        }
        newsym(oldx, oldy);
        newsym(newx, newy);
        vision_recalc(0);
        const pickupTypes = game._autopickup_types || game.flags?.pickup_types || 'all';
        const pickedObject = game._autopickup && (game.level?.objects || []).find(obj => {
            if (obj.hidden || obj.transientProjectile || obj.ox !== newx || obj.oy !== newy || obj.otyp === BOULDER)
                return false;
            const pickupGlyph = obj.glyph || OBJECT_CLASS_GLYPHS[obj.cls] || (obj.otyp === SPE_HEALING ? '+' : '');
            return pickupGlyph && (pickupTypes === 'all' || pickupTypes.includes(pickupGlyph));
        });
        if (pickedObject) {
            const letter = pickedObject.wasStolen && pickedObject.letter
                ? pickedObject.letter
                : nextInventoryLetter();
            const amount = pickupObjectPhrase(pickedObject);
            const spellbookInfo = pickedObject.otyp === SPE_HEALING
                ? { cls: 'spellbook', spellName: 'healing', spell: { name: 'healing', level: 1, skill: 'healing' }, known: false, bknown: false }
                : {};
            const pickedItem = {
                ...pickedObject,
                ...spellbookInfo,
                cls: pickedObject.cls || spellbookInfo.cls || (pickedObject.otyp === DART ? 'weapon'
                    : pickedObject.otyp === GEM_CLASS ? 'gem'
                        : pickedObject.otyp === 'corpse' || pickedObject.otyp === CORPSE ? 'food'
                            : pickedObject.otyp === RING_CLASS || pickedObject.glyph === '=' ? 'ring'
                                : BAG_OBJECT_TYPES.has(pickedObject.otyp) || pickedObject.otyp === OIL_LAMP || pickedObject.otyp === MAGIC_LAMP
                                    || pickedObject.otyp === MIRROR || pickedObject.otyp === EXPENSIVE_CAMERA
                                    || pickedObject.otyp === STETHOSCOPE || pickedObject.otyp === MAGIC_MARKER ? 'tool'
                                    : pickedObject.otyp === SCROLL_CLASS ? 'scroll'
                                        : undefined),
                letter,
                kind: pickedObject.kind || pickupObjectName({ ...pickedObject, quan: 1 }),
                line: `${letter} - ${amount}`,
            };
            objectIceEffect(pickedItem, newx, newy, { onLevel: false });
            game.inventory = [...(game.inventory || []), pickedItem];
            maybeAttachCarriedFigurineTimeout(pickedItem);
            game.level.objects = (game.level.objects || []).filter(obj => obj !== pickedObject);
            game._pet_food_scan_inventory = game.inventory;
            newsym(newx, newy);
            await setMessage(`You swap places with ${pet.givenName ? petName : `your ${petName}`}.`, true);
            game._queued_message_after_more = `${letter} - ${amount}.`;
            return;
        }
        const hereObj = (game.level?.objects || []).find(obj =>
            !obj.hidden && !obj.transientProjectile && obj.ox === newx && obj.oy === newy);
        const swapMessage = `You swap places with ${pet.givenName ? petName : `your ${petName}`}.`;
        if (hereObj) {
            const objectName = pickupObjectName(hereObj);
            const article = /^[aeiou]/i.test(objectName) ? 'an' : 'a';
            await setMessage(`${swapMessage}  You see here ${article} ${objectName}.`);
        } else if (runningStep && game._pending_message === swapMessage) {
            game._message_more = 0;
        } else {
            await setMessage(swapMessage);
        }
        return;
    }


    let mon = swallowedMove ? game.u.ustuck : targetMonster;

    if (mon && game._running_continuation) {
        game._run_steps_remaining = 0;
        game.context.move = 0;
        return;
    }
    let forcedPeacefulAttack = false;
    if (mon?.mpeaceful && game.flags?.safe_pet !== false && safeHeroStatus) {
        if (game._force_fight_target === mon) {
            forcedPeacefulAttack = true;
            game._force_fight_target = null;
        } else {
        const name = mon.data?.name || 'creature';
        if (!rn2(7)) {
            await setMessage(`You stop.  The ${name} is in the way!`);
            game.context.move = 1;
            return;
        }
        mon.mx = oldx;
        mon.my = oldy;
        game.u.ux0 = oldx;
        game.u.uy0 = oldy;
        game.u.ux = newx;
        game.u.uy = newy;
        game.u.umoved = true;
        game.context.move = 1;
        newsym(oldx, oldy);
        newsym(newx, newy);
        vision_recalc(0);
        await setMessage(`You swap places with the peaceful ${name}.`);
        return;
        }
    }
    if (mon) {
        const data = mon.data || {};
        let name = data.name || 'creature';
        if (mon.isshk && mon.shknam) name = mon.shknam;
        if (name === 'gnome ruler') name = 'gnome king';
        else if (name === 'gnome leader') name = 'gnome lord';
        else if (name === 'dwarf ruler') name = 'dwarf king';
        else if (name === 'dwarf leader') name = 'dwarf lord';
        if (mon.appearObj != null || mon.appearGlyph) {
            const objectName = mon.appearObj === CHEST ? 'chest' : mon.appearObj === LARGE_BOX ? 'large box' : 'object';
            mon.appearObj = null;
            mon.appearGlyph = null;
            mon.appearColor = null;
            mon._skip_after_mimic_reveal = 1;
            rnd(2);
            newsym(mon.mx, mon.my);
            await setMessage(`That ${objectName} is a ${name}!`);
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            game.context.move = 1;
            return;
        }
        const hiddenTarget = !!game.level?.at(newx, newy)?.map_invisible;
        const targetSpotted = !game.u?.blind && !mon.mundetected
            && (!mon.minvis || game.u?.seeInvisible) && couldsee(newx, newy);
        if (!swallowedMove && !hiddenTarget && !targetSpotted && game._force_fight_target !== mon) {
            const loc = game.level?.at(newx, newy);
            if (loc) {
                loc.map_invisible = true;
                loc.waslit = true;
                loc.remembered_glyph = { ch: 'I', color: NO_COLOR, dec: false };
            }
            if (mon.msleeping) mon.msleeping = 0;
            newsym(newx, newy);
            await setMessage("Wait!  There's something there you can't see!");
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            game.context.move = 1;
            return;
        }
        const stats = game.u?.acurr?.a || [];
        const str = stats[0] ?? 10;
        const dex = stats[3] ?? 10;
        const role = game.urole?.name?.m || game._startup_role || '';
        const wokeFromSleep = !!mon.msleeping;
        const caitiffAttack = role === 'Knight'
            && (game.u?.ualign?.type ?? 0) > 0
            && (game.u?.ualign?.record ?? 0) > -10
            && (wokeFromSleep || mon.mcanmove === 0 || (mon.mflee && !mon.mavenge));
        const martialArts = role === 'Monk' || role === 'Samurai';
        const basicBareHands = martialArts || role === 'Barbarian' || role === 'Caveman';
        const inventory = game.inventory || [];
        const weapon = role === 'Monk' ? null : inventory.find(item => item.wielded)
            || inventory.find(item =>
                item.line?.includes('weapon in')
                || item.line?.includes('wielded in right'));
        const secondWeapon = game._twoweapon
            ? inventory.find(item => item !== weapon
                && (item.alternate || /left hand|alternate weapon/.test(item.line || '')))
            : null;
        const twoWeaponActive = game._twoweapon && !!secondWeapon
            && (secondWeapon.cls === 'weapon' || secondWeapon.glyph === ')');

        let strengthHitBonus = str < 6 ? -2 : str < 8 ? -1 : str < 17 ? 0 : str < 68 ? 1 : str < 118 ? 2 : 3;
        if ((game.u?.ulevel || 1) < 3) strengthHitBonus++;
        const dexHitBonus = dex < 4 ? -3 : dex < 6 ? -2 : dex < 8 ? -1 : dex < 14 ? 0 : dex - 14;
        const luck = (game.u?.uluck || 0) + (game.u?.moreluck || 0);
        const luckHitBonus = Math.sign(luck) * Math.trunc((Math.abs(luck) + 2) / 3);
        const bareHandSkill = basicBareHands ? 2 : 1;
        const bareHandHitBonus = !weapon
            ? Math.trunc(((Math.max(bareHandSkill, 1) - 1 + 2) * (martialArts ? 2 : 1)) / 2)
            : 0;
        const bareHandDamageBonus = !weapon
            ? Math.trunc(((Math.max(bareHandSkill, 1) - 1 + 1) * (martialArts ? 3 : 1)) / 2)
            : 0;
        const targetAc = data.mac ?? 10;
        const targetStunnedBonus = mon.mstun ? 2 : 0;
        const targetFleeingBonus = mon.mflee ? 2 : 0;
        const targetSleepingBonus = mon.msleeping ? 2 : 0;
        const targetImmobileBonus = mon.mcanmove === 0 ? 4 : 0;
        const trappedPenalty = game.u?.utrap ? -3 : 0;
        const toHit = 1 + strengthHitBonus + dexHitBonus + targetAc + (game.u?.uhitinc || 0)
            + luckHitBonus + (game.u?.ulevel || 1) + bareHandHitBonus
            + targetStunnedBonus + targetFleeingBonus + targetSleepingBonus + targetImmobileBonus
            + trappedPenalty;
        let wokeByHit = false;

        if (!game.u?.uinvulnerable) {
            applyHeroOrdinaryHunger();
            applyAccessoryHunger(rn2(20));
        }
        exerciseAttribute(A_STR, true);
        if (!swallowedMove) wipe_engr_at(oldx, oldy, 3, false);
        const strengthDamageBonus = str < 6 ? -1 : str < 16 ? 0 : str < 18 ? 1 : str === 18 ? 2
            : str <= 93 ? 3 : str <= 108 ? 4 : str < 118 ? 5 : 6;
        const messages = [];
        if (caitiffAttack) {
            messages.push('You caitiff!');
            if (game.u?.ualign) {
                game.u.ualign.record = (game.u.ualign.record || 0) - 1;
                game.u.ualign.abuse = (game.u.ualign.abuse || 0) + 1;
            }
        }
        const peacefulShopkeeper = mon.isshk && mon.mpeaceful;
        const continuePeacefulShopkeeperTurn = peacefulShopkeeper && forcedPeacefulAttack
            && (mon.movement ?? NORMAL_SPEED) <= NORMAL_SPEED;
        let shownName = name;
        if ((game.u?._statusSuffix || '').includes('Hallu') && !game.u?.blind) {
            let halluIndex;
            do {
                halluIndex = rn2_on_display_rng(SPECIAL_PM + BOGUSMONSIZE);
            } while (halluIndex < SPECIAL_PM && !DISPLAY_MONSTER_HALLU_NAMES[halluIndex]);
            if (halluIndex >= SPECIAL_PM) shownName = getbogusmon();
            else {
                rn2_on_display_rng(2);
                shownName = DISPLAY_MONSTER_HALLU_NAMES[halluIndex] || shownName;
            }
        }
        const targetPhrase = hiddenTarget ? 'it'
            : mon.givenName && data.name === 'ghost' ? `${mon.givenName}'s ghost`
                : mon.isshk && mon.shknam && shownName === name ? name : `the ${shownName}`;
        if (!weapon && !swallowedMove && heroHasSlowDigestion() && heroPolyselfDigestAttack()) {
            messages.push(`You swallow ${targetPhrase} whole!`);
            messages.push(`You regurgitate ${targetPhrase}!`);
            messages.push(`Obviously, you didn't like ${monsterPossessiveName(mon)} taste.`);
            await setMessage(messages.join('  '), true);
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            game.context.move = 1;
            return;
        }
        const attackWeapons = twoWeaponActive && !wokeFromSleep ? [weapon, secondWeapon] : [weapon];
        const deferSleepingTwoWeapon = wokeFromSleep && twoWeaponActive;
        let deferredMeleeWeaponHit = false;
        let killed = false;
        let killingAttackWeapon = null;
        let killedByNormalMelee = false;
        for (let attackIndex = 0; attackIndex < attackWeapons.length; attackIndex++) {
            const attackWeapon = attackWeapons[attackIndex];
            const weaponName = attackWeapon?.kind || attackWeapon?.name || (typeof attackWeapon?.otyp === 'string' ? attackWeapon.otyp : '');
            const weaponBaseName = attackWeapon?.actualKind || String(weaponName).replace(/ named .+$/, '');
            const grayswandirHit = attackWeapon?.artifact === 'Grayswandir';
            const artifactHitBonus = grayswandirHit ? rnd(5) : 0;
            const weaponHitBonus = {
                scalpel: 2,
            }[weaponBaseName] || 0;
            const attackToHit = toHit + artifactHitBonus + (attackWeapon?.spe || 0)
                + weaponHitBonus
                + (twoWeaponActive && attackWeapon ? -9 : 0);
            const attackRoll = rnd(20);
            if (attackToHit <= attackRoll) {
                if (hiddenTarget && !attackWeapon && !game._bare_hands_bashing_started) {
                    game._bare_hands_bashing_started = 1;
                    messages.push('You begin bashing monsters with your bare hands.  You miss it.');
                } else {
                    messages.push(`You miss ${targetPhrase}.`);
                }
                if (attackIndex === 0 && (mon.mhp || 1) > 0) rn2(3);
                continue;
            }

            if (attackIndex === 0) exerciseAttribute(A_DEX, true);
            if (wokeFromSleep && mon.msleeping) {
                mon.msleeping = 0;
                wokeByHit = true;
            }
            if (attackWeapon && isPotionObject(attackWeapon)) {
                const bashPotion = wieldedPotionBashObject(attackWeapon);
                const potionMessages = bashPotion
                    ? heroThrownPotionHitMonster(bashPotion, mon, { allowLifeSaving: true })
                    : [];
                const potionLifeSaving = !!potionMessages.lifeSaving;
                const potionFatal = !!potionMessages.fatal;
                removeInventoryItem(attackWeapon, 1);
                refreshSurvivingWieldedPotionStack(attackWeapon);
                killed = !!(mon.dead || (mon.mhp || 0) <= 0);
                if (!potionFatal && !killed) {
                    const potionBashDamage = (mon.data?.name === 'shade') ? 0
                        : Math.max(1, 1 + strengthDamageBonus + (game.u?.udaminc || 0));
                    if (potionBashDamage > 0) {
                        mon.mhp = (mon.mhp || 1) - potionBashDamage;
                        killed = (mon.mhp || 0) <= 0;
                    }
                }
                messages.push(...potionMessages);
                if (potionLifeSaving) messages.lifeSaving = true;
                if (potionFatal) messages.fatal = true;
                if (potionMessages.more) messages.more = true;
                if (potionLifeSaving || potionFatal) break;
                if (killed) break;
                if (attackIndex === 0 && !deferSleepingTwoWeapon) {
                    const fleeRoll = rn2(25);
                    if (!fleeRoll && (mon.mhp || 0) < Math.trunc((mon.mhpmax || 0) / 2)
                        && !(game.u?.uswallow && game.u?.ustuck === mon)) {
                        const fleeTime = rn2(3) ? 0 : rnd(100);
                        mon.mflee = 1;
                        mon.mfleetim = fleeTime ? Math.min(fleeTime + (mon.mfleetim || 0), 127) : 0;
                        clearMonsterTrack(mon);
                    }
                }
                continue;
            }
            if (attackWeapon && isEggItem(attackWeapon)) {
                const eggHit = heroMeleeEggHitMonster(attackWeapon, mon);
                const eggMessages = eggHit.messages || [];
                if (eggHit.consumed) removeInventoryItem(attackWeapon, attackWeapon.quan || 1);
                else refreshSurvivingWieldedConsumedStack(attackWeapon);
                killed = !!(mon.dead || (mon.mhp || 0) <= 0);
                messages.push(...eggMessages);
                if (eggMessages.lifeSaving) messages.lifeSaving = true;
                if (eggMessages.fatal) messages.fatal = true;
                if (eggMessages.more) messages.more = true;
                if (eggMessages.lifeSaving || eggMessages.fatal) break;
                if (killed) break;
                continue;
            }
            if (attackWeapon && !game._chronicle_first_weapon_hit) {
                game._chronicle_entries ??= [];
                game._chronicle_entries.push({ turn: game.moves || 1, text: `hit with a wielded weapon (${weaponName || 'weapon'}) for the first time` });
                game._chronicle_first_weapon_hit = 1;
            }
            if (attackWeapon) addConductCount('weaphit');
            const weaponDamage = {
                'battle-axe': [1, 8],
                axe: [1, 6],
                club: [1, 6],
                dagger: [1, 4],
                katana: [1, data.big ? 12 : 10],
                lance: [1, 6],
                'long sword': [1, data.big ? 12 : 8],
                mace: [1, 6],
                scalpel: [1, 3],
                'silver saber': [1, 8],
                'short sword': [1, 6],
                'orcish short sword': [1, 5],
                'dwarvish spear': [1, 8],
                'elven spear': [1, 7],
                'orcish spear': [1, 5],
                spear: [1, 6],
                wakizashi: [1, 6],
            }[weaponBaseName] || [1, attackWeapon ? 6 : (role === 'Monk' || role === 'Samurai') ? 4 : 2];
            let baseDamage = attackWeapon
                ? (weaponDamage[0] === 1 ? rnd(weaponDamage[1]) : d(weaponDamage[0], weaponDamage[1])) + (attackWeapon.spe || 0)
                : rnd(weaponDamage[1]);
            if (grayswandirHit) baseDamage += Math.max(baseDamage, 1);
            const weaponSkillDamageBonus = grayswandirHit ? -2 : 0;
            const damage = Math.max(0, baseDamage + strengthDamageBonus + bareHandDamageBonus
                + weaponSkillDamageBonus + (game.u?.udaminc || 0));
            if (attackWeapon && damage > 1) deferredMeleeWeaponHit = true;
            if (!attackWeapon && damage > 1) rnd(100);
            mon.mhp = (mon.mhp || 1) - damage;
            killed = mon.mhp <= 0;
            if (killed) {
                killingAttackWeapon = attackWeapon;
                killedByNormalMelee = true;
            }
            if (mon.mtame && damage > 0) {
                mon.mtame--;
                mon.abuse = (mon.abuse || 0) + 1;
                if (mon.mx !== 0) {
                    let petVerb = 'growls';
                    if (mon.mtame && rn2(mon.mtame)) {
                        petVerb = statusSuffix.includes('Hallu')
                            ? HALLUCINATED_SOUND_VERBS[rn2(HALLUCINATED_SOUND_VERBS.length)]
                            : 'yelps';
                    }
                    const petSubject = mon.givenName || `The ${shownName}`;
                    messages.push(`${petSubject} ${petVerb}!`);
                }
                if (mon.mtame && !killed) {
                    mon.mflee = 1;
                    mon.mfleetim = Math.min((mon.mfleetim || 0) + 10 * rnd(damage), 127);
                    clearMonsterTrack(mon);
                }
            }
            if (killed) break;
            const hitPhrase = game.flags?.verbose === false ? 'it' : targetPhrase;
            const hitPunctuation = game.flags?.verbose === false || swallowedMove ? '.' : damage > 4 ? '!' : '.';
            messages.push(`You hit ${hitPhrase}${hitPunctuation}`);
            applyConfuseMonsterOnHit(mon, messages, targetPhrase);
            let directPassiveObjectApplied = false;
            if (attackIndex === 0) {
                if (attackWeapon && damage > 1 && !twoWeaponActive) {
                    rn2(3);
                    rn2(6);
                }
	                if (!deferSleepingTwoWeapon) {
	                    const fleeRoll = rn2(25);
	                    if (!fleeRoll && (mon.mhp || 0) < Math.trunc((mon.mhpmax || 0) / 2)
	                        && !(game.u?.uswallow && game.u?.ustuck === mon)) {
	                        const fleeTime = rn2(3) ? 0 : rnd(100);
	                        mon.mflee = 1;
	                        mon.mfleetim = fleeTime ? Math.min(fleeTime + (mon.mfleetim || 0), 127) : 0;
	                        clearMonsterTrack(mon);
	                    }
	                    applyDirectMeleePassiveObject(mon, attackWeapon, messages);
	                    directPassiveObjectApplied = true;
	                    rn2(3);
	                }
            }
            if (!directPassiveObjectApplied)
                applyDirectMeleePassiveObject(mon, attackWeapon, messages);
        }

        if (messages.lifeSaving || messages.fatal) {
            const potionCallPrompt = game._command_mode === 'callPotionAfterMore';
            await setMessage(messages.join('  '), potionCallPrompt || !!messages.more);
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            game._command_mode = 'deathDieMore';
            game._pending_time_passed = 0;
            game._process_command_time_now = 0;
            prepareDeathBones();
            return;
        }

        if (!killed) {
            if (wokeByHit) messages.push(`The ${name} wakes up!`);
            if (deferSleepingTwoWeapon) {
                game._queued_messages_after_more ??= [];
                game._queued_messages_after_more.unshift({
                    text: `The ${name} screams!`,
                    deferredMeleeWake: {
                        target: mon,
                        weaponHit: deferredMeleeWeaponHit,
                    },
                    wakeNearby: { source: mon, x: mon.mx, y: mon.my },
                    processTime: true,
                });
            }
            if (peacefulShopkeeper) {
                mon.mpeaceful = 0;
                messages.push(`${name} gets angry!`);
                if (!continuePeacefulShopkeeperTurn) {
                    rn2(5);
                    mon._distfleeck_done_after_anger = 1;
                }
            }
            const potionCallPrompt = game._command_mode === 'callPotionAfterMore';
            await setMessage(messages.join('  '), potionCallPrompt || peacefulShopkeeper || wokeByHit);
            if (continuePeacefulShopkeeperTurn) {
                game._continue_monsters_after_more = 1;
                game._process_time_with_more = 1;
            }
            game._run_stop_now = 1;
            game._run_steps_remaining = 0;
            game.context.move = 1;
            return;
        }

        const killVerb = data.nonliving || data.mlet === 'Z' || data.mlet === 'zombie'
            || data.glyph === 'Z' || data.name?.includes('zombie') || data.name?.endsWith(' golem')
            ? 'destroy'
            : 'kill';
        const killedPet = mon.mtame || mon.pet;
        let killedName = shownName;
        if (killedPet && (game.u?._statusSuffix || '').includes('Hallu') && !game.u?.blind) {
            let halluIndex;
            do {
                halluIndex = rn2_on_display_rng(SPECIAL_PM + BOGUSMONSIZE);
            } while (halluIndex < SPECIAL_PM && !DISPLAY_MONSTER_HALLU_NAMES[halluIndex]);
            if (halluIndex >= SPECIAL_PM) killedName = getbogusmon();
            else {
                rn2_on_display_rng(2);
                killedName = DISPLAY_MONSTER_HALLU_NAMES[halluIndex] || killedName;
            }
        }
        messages.push(`You ${killVerb} ${killedPet ? `the poor ${killedName}` : targetPhrase}!`);
        if (killedByNormalMelee)
            applyDirectMeleePassiveObject(mon, killingAttackWeapon, messages);
        if (!game._chronicle_first_kill) {
            game._chronicle_entries ??= [];
            game._chronicle_entries.push({ turn: game.moves || 1, text: 'killed for the first time' });
            game._chronicle_first_kill = 1;
        }
        recordVanquished(mon);
        dropMonsterInventory(mon, messages);
        const potionCallPrompt = game._command_mode === 'callPotionAfterMore';
        await setMessage(messages.join('  '), potionCallPrompt || (killedPet && messages.length > 1));
        const treasureDrop = !rn2(6);

        const corpseData = data.corpse
            || (data.name?.endsWith(' zombie') ? monsterByRndName(data.name.replace(/ zombie$/, '')) : null)
            || data;
        const killLoc = game.level?.at(mon.mx, mon.my);
        const killAccessible = killLoc && (ACCESSIBLE(killLoc.typ) || IS_POOL(killLoc.typ));
        const gasSporeExplosion = data.name === 'gas spore' && killAccessible;
        if (killLoc?.map_invisible) {
            killLoc.map_invisible = false;
            killLoc.remembered_glyph = null;
        }
        if (killAccessible
            && !corpseData.noCorpse
            && corpseData === data
            && (mon.mx !== game.u?.ux || mon.my !== game.u?.uy)
            && !mon.mcloned
            && treasureDrop) {
            const drop = mkobj_at(RANDOM_CLASS, mon.mx, mon.my, true);
            const dropKind = String(drop?.kind || drop?.actualKind || '').replace(/^scroll labeled /, '').replace(/^potion of /, '');
            const dropWeight = OBJECT_WEIGHTS[dropKind] ?? CLASS_WEIGHTS[drop?.cls] ?? drop?.owt ?? 0;
            const humanSized = data.strong || data.armed || data.mlet === '@' || data.mlet === 'h' || data.mlet === 'G' || data.mlet === 'o';
            const tooLarge = !humanSized && drop?.kind !== 'figurine' && dropWeight > 30;
            if (drop?.foodRoll != null || tooLarge) {
                rn2(100);
                game.level.objects = (game.level?.objects || []).filter(obj => obj !== drop);
            }
        }
        if (gasSporeExplosion) {
            d(4, 6);
            const explosionDamage = d(4, 6);
            messages.push('Boom!');
            await setMessage(messages.join('  '), true);
            game._preserve_gas_spore_residue = 1;
            const explosionTarget = (game.level?.monsters || []).find(other => other !== mon
                && Math.abs(other.mx - mon.mx) <= 1 && Math.abs(other.my - mon.my) <= 1);
            if (explosionTarget) {
                game._gas_spore_residue_mon = explosionTarget;
                game._gas_spore_residue_initial_x = explosionTarget.mx;
                game._gas_spore_residue_initial_y = explosionTarget.my;
                game._gas_spore_residue_x = explosionTarget.mx;
                game._gas_spore_residue_y = explosionTarget.my;
                game._gas_spore_residue_visible_x = explosionTarget.mx;
                game._gas_spore_residue_visible_y = explosionTarget.my;
                game._gas_spore_residue_frames = 0;
                const targetLevel = Math.max(1, Math.min(50, explosionTarget.m_lev ?? explosionTarget.data?.mlevel ?? 1));
                const resistBound = Math.max(1, 100 + (game.u?.ulevel || 1) - targetLevel);
                game._queued_messages_after_more ??= [];
                game._queued_messages_after_more.push({
                    text: `The ${explosionTarget.data?.name || 'monster'} is caught in the gas spore's explosion!`,
                    more: true,
                    gasSporeMonsterExplosion: {
                        target: explosionTarget,
                        damage: explosionDamage,
                        resistBound,
                    },
                });
            }
            if (Math.abs(game.u.ux - mon.mx) <= 1 && Math.abs(game.u.uy - mon.my) <= 1) {
                game._queued_messages_after_more ??= [];
                game._queued_messages_after_more.push({
                    text: "You are caught in the gas spore's explosion!",
                    gasSporeHeroExplosion: { damage: explosionDamage },
                    processTime: true,
                });
            }
            game._process_time_with_more = 0;
        } else {
            const dropCorpse = monsterCorpseDropSucceeds(mon, data);
            if (dropCorpse && monsterLeavesCorpseLikeDrop(corpseData))
                createMonsterCorpseOrGlob(mon, corpseData, mon.mx, mon.my, { messages });
        }
        if (mon.mpeaceful) {
            if (!rn2(2) || mon.mtame) game.u.uluck = (game.u?.uluck || 0) - 1;
        } else if (mon.mtame) {
            game.u.uluck = (game.u?.uluck || 0) - 1;
        }
        if (mon.mtame) {
            game._pet_kill_luck_message_after_more = statusSuffix.includes('Hallu')
                ? 'You hear the studio audience applaud!'
                : 'You hear the rumble of distant thunder...';
        }
        {
            const level = mon.m_lev ?? data.mlevel ?? 0;
            const speed = data.mmove ?? NORMAL_SPEED;
            const attacks = data.xpAttacks || data.attacks || (data.attack ? [data.attack] : []);
            const hasWeaponAttack = attacks.some(monsterAttack =>
                (monsterAttack.aatyp || (monsterAttack.weapon ? 'weap' : '')) === 'weap');
            let experience = 1 + level * level
                + (speed > NORMAL_SPEED ? speed > (3 * NORMAL_SPEED / 2) ? 5 : 3 : 0)
                + ((data.armed || data.attack?.weapon) && !hasWeaponAttack ? 5 : 0)
                + (data.xpAttackBonus || 0);
            const monsterAc = data.mac ?? 10;
            if (monsterAc < 3) experience += (7 - monsterAc) * (monsterAc < 0 ? 2 : 1);
            for (const monsterAttack of attacks) {
                const attackType = monsterAttack.aatyp || (monsterAttack.weapon ? 'weap' : '');
                if (attackType === 'weap') experience += 5;
                else if (attackType === 'magc') experience += 10;
                else if (XP_SPECIAL_ATTACK_TYPES.has(attackType)) experience += 3;
                const damageType = monsterAttack.adtyp || 'phys';
                if (XP_DOUBLE_DAMAGE_TYPES.has(damageType)) experience += 2 * level;
                else if (XP_FLAT_DAMAGE_TYPES.has(damageType)) experience += 50;
                else if (damageType !== 'phys') experience += level;
                if ((monsterAttack.dice ?? 0) * (monsterAttack.sides ?? 0) > 23) experience += level;
            }
            game.u.uexp = (game.u.uexp || 0) + experience;
            const oldLevel = game.u?.ulevel || 1;
            const nextLevelExp = oldLevel < 10 ? 10 * (2 ** oldLevel)
                : oldLevel < 20 ? 10000 * (2 ** (oldLevel - 10))
                    : 10000000 * (oldLevel - 19);
            if (oldLevel < 30 && (game.u?.uexp || 0) >= nextLevelExp) {
                game._level_change_target = oldLevel + 1;
                await advanceExperienceLevel(true);
                const abilityMessage = game._level_change_ability_prefix || '';
                game._level_change_ability_prefix = '';
                game._level_change_delayed_welcome = 0;
                game._level_change_pending_message = '';
                messages.push(`Welcome to experience level ${game.u?.ulevel || oldLevel + 1}.${abilityMessage ? `  ${abilityMessage}` : ''}`);
                game._command_mode = null;
                await setMessage(messages.join('  '));
            }
        }
        game.level.monsters = (game.level?.monsters || []).filter(mtmp => mtmp !== mon);
        newsym(mon.mx, mon.my);
        game._run_stop_now = 1;
        game._run_steps_remaining = 0;
        game.context.move = 1;
        return;
    }

    if (target?.typ === DOOR && (target.doormask & (D_CLOSED | D_LOCKED)) && game._running_continuation) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
        game._suppress_obstructed_message_once = 0;
        const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
        if ((oldx === newx || oldy === newy) && (game.u?.blind || game.u?.stunned || game.u?.fumbling || dex < 10)) {
            await setMessage('Ouch!  You bump into a door.');
            exerciseAttribute(A_DEX, false);
            game.context.move = 1;
            return;
        }
        if (oldx === newx || oldy === newy) await setMessage('That door is closed.');
        game.context.move = 0;
        return;
    }

    if (target?.typ === DOOR && (target.doormask & (D_CLOSED | D_LOCKED)) && game._suppress_obstructed_message_once) {
        game._suppress_obstructed_message_once = 0;
        const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
        if ((oldx === newx || oldy === newy) && (game.u?.blind || game.u?.stunned || game.u?.fumbling || dex < 10)) {
            await setMessage('Ouch!  You bump into a door.');
            exerciseAttribute(A_DEX, false);
            game.context.move = 1;
            game._run_stop_now = 1;
            return;
        }
        await setMessage('That door is closed.');
        game.context.move = 0;
        return;
    }

    if (target?.typ === DOOR && (target.doormask & (D_CLOSED | D_LOCKED))) {
        const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
        const confused = (game.u?._statusSuffix || '').includes('Conf') || (game.u?._confusionTimeout || 0) > 0;
        const autoopen = game.flags?.autoopen !== false && !confused && !game.u?.stunned && !game.u?.fumbling;
        if (!autoopen && (oldx === newx || oldy === newy)
            && (game.u?.blind || game.u?.stunned || dex < 10 || game.u?.fumbling)) {
            await setMessage('Ouch!  You bump into a door.');
            exerciseAttribute(A_DEX, false);
            game.context.move = 1;
            return;
        }
        if (!autoopen) {
            await setMessage('That door is closed.');
            game.context.move = 0;
            return;
        }
    }

    if (target?.typ === DOOR && (target.doormask & D_LOCKED)) {
        const unlockingTool = (game.inventory || []).find(item => /lock pick|key|credit card/i.test(inventoryItemName(item)));
        if (unlockingTool) {
            game._locked_door_unlock_after_more = { x: newx, y: newy };
            await setMessage('This door is locked.', true);
        } else {
            await setMessage('This door is locked.');
        }
        game.context.move = 0;
        return;
    }

    if (target?.typ === DOOR && (target.doormask & D_CLOSED)) {
        const stats = game.u?.acurr?.a || [];
        const rawStr = stats[0] ?? 10;
        const str = rawStr <= 18 ? rawStr : rawStr <= 121 ? 19 + Math.trunc(rawStr / 50) : Math.min(rawStr, 125) - 100;
        const openTarget = Math.trunc((str + (stats[3] ?? 10) + (stats[4] ?? 10)) / 3);
        if (rnl(20) < openTarget || target._picked_lock) {
            target.doormask = D_ISOPEN;
            const topology = game._tutorial_triggers?.openDoorTopology;
            if (topology && newx === topology.x && newy === topology.y) {
                for (const wall of topology.walls || []) game.level.at(wall.x, wall.y).typ = wall.typ;
            }
            vision_reset();
            newsym(newx, newy);
            vision_recalc(0);
            await setMessage('The door opens.');
        } else {
            rn2(19);
            await setMessage('The door resists!');
        }
        if (game._tutorial_active) game._pending_time_passed = 0;
        game.context.move = 0;
        return;
    }

    const targetObstructed = !target || (IS_OBSTRUCTED(target.typ) && !(target.typ === DOOR && (target.doormask & D_ISOPEN)));
    if (targetObstructed && game._running_continuation) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
        if (game._tutorial_active && oldLoc?.typ !== CORR) await setMessage("It's a wall.");
        game.context.move = 0;
        return;
    }

    if (targetObstructed) {
        if (game._running_continuation) {
            game._run_steps_remaining = 0;
            game._run_stop_now = 1;
            game.context.move = 0;
            return;
        }
        if (game._suppress_obstructed_message_once) {
            game._suppress_obstructed_message_once = 0;
            game.context.move = 0;
            return;
        }
        if (game.flags?.mention_walls) {
            await setMessage(target?.typ === TREE ? "It's a tree."
                : target?.typ === STONE ? "It's solid stone."
                    : "It's a wall.");
            game.context.move = 0;
            return;
        }
        await pline('You cannot move there.');
        game.context.move = 0;
        return;
    }
    game._suppress_obstructed_message_once = 0;

    if (liquidTarget && oldMoveTyp !== targetMoveTyp && !nopick) {
        const liquidDefault = targetMoveTyp === LAVAPOOL || targetMoveTyp === LAVAWALL ? 'lava' : 'water';
        const liquid = (game.u?._statusSuffix || '').includes('Hallu')
            ? HALLUCINATED_LIQUIDS[rn2_on_display_rng(HALLUCINATED_LIQUIDS.length + 1)] || liquidDefault
            : liquidDefault;
        const medusa = game.specialLevels?.find(level => level.name === 'medusa');
        const onMedusaLevel = medusa && game.u?.uz?.dnum === medusa.dnum && game.u?.uz?.dlevel === medusa.dlevel;
        const juiblex = game.specialLevels?.find(level => level.name === 'juiblex');
        const onJuiblexLevel = juiblex && game.u?.uz?.dnum === juiblex.dnum && game.u?.uz?.dlevel === juiblex.dlevel;
        const moatName = onMedusaLevel ? 'shallow sea' : onJuiblexLevel ? 'swamp' : 'moat';
        const msg = targetMoveTyp === WATER
            ? `You avoid stepping into the wall of ${liquid}.`
            : targetMoveTyp === LAVAWALL
                ? `You avoid stepping into the wall of ${liquid}.`
                : targetMoveTyp === LAVAPOOL
                    ? `You avoid stepping into the molten ${liquid}.`
                    : targetMoveTyp === MOAT
                        ? `You avoid stepping into the ${moatName}.`
                        : `You avoid stepping into the pool of ${liquid}.`;
        const showSwimTip = targetMoveTyp !== WATER && targetMoveTyp !== LAVAWALL && targetMoveTyp !== MOAT && !game._swim_tip_shown;
        await setMessage(msg, showSwimTip);
        if (showSwimTip) {
            game._topline_after_more = "(Tip: use 'm' prefix to step in if you really want to.)";
            game._hallu_refresh_after_topline_more = 1;
            game._swim_tip_shown = 1;
        }
        game.context.move = 0;
        return;
    }

    const oldEngraving = game.level?.engravings?.find(engr => engr.x === oldx && engr.y === oldy && engr.text);
    let ballDragDelay = false;
    let ballStepNoTime = false;
    let ballDragNearHostile = false;
    let ballDragStopsTravel = false;
    let ballChainMoved = false;
    if (!swallowedMove && game.u?.uball && game.u?.uchain) {
        const ball = game.u.uball;
        const chain = game.u.uchain;
        const redrawWithoutBallChain = (...spots) => {
            const ballHidden = ball.hidden;
            const chainHidden = chain.hidden;
            ball.hidden = true;
            chain.hidden = true;
            for (const [sx, sy] of spots)
                newsym(sx, sy);
            ball.hidden = ballHidden;
            chain.hidden = chainHidden;
        };
        const chainDist2 = (newx - chain.ox) ** 2 + (newy - chain.oy) ** 2;
        const ballDistMin = Math.max(Math.abs(newx - ball.ox), Math.abs(newy - ball.oy));
        if (chainDist2 > 2 && ballDistMin <= 2) {
            const ballDist2 = (newx - ball.ox) ** 2 + (newy - ball.oy) ** 2;
            let chainx = chain.ox;
            let chainy = chain.oy;
            if (ballDist2 === 8 || ballDist2 === 4) {
                chainx = Math.trunc((newx + ball.ox) / 2);
                chainy = Math.trunc((newy + ball.oy) / 2);
            } else if (ballDist2 === 5) {
                if (Math.abs(newx - ball.ox) === 1) {
                    const y = Math.trunc((newy + ball.oy) / 2);
                    const first = { x: newx, y };
                    const second = { x: ball.ox, y };
                    const firstDist = (first.x - chain.ox) ** 2 + (first.y - chain.oy) ** 2;
                    const secondDist = (second.x - chain.ox) ** 2 + (second.y - chain.oy) ** 2;
                    chainx = firstDist <= secondDist ? first.x : second.x;
                    chainy = y;
                } else {
                    const x = Math.trunc((newx + ball.ox) / 2);
                    const first = { x, y: newy };
                    const second = { x, y: ball.oy };
                    const firstDist = (first.x - chain.ox) ** 2 + (first.y - chain.oy) ** 2;
                    const secondDist = (second.x - chain.ox) ** 2 + (second.y - chain.oy) ** 2;
                    chainx = x;
                    chainy = firstDist <= secondDist ? first.y : second.y;
                }
            } else {
                const middleOld = Math.max(Math.abs(chain.ox - newx), Math.abs(chain.oy - newy)) <= 1
                    && Math.max(Math.abs(chain.ox - ball.ox), Math.abs(chain.oy - ball.oy)) <= 1;
                const middleHero = Math.max(Math.abs(oldx - newx), Math.abs(oldy - newy)) <= 1
                    && Math.max(Math.abs(oldx - ball.ox), Math.abs(oldy - ball.oy)) <= 1;
                if (!middleOld) {
                    chainx = middleHero ? oldx : newx;
                    chainy = middleHero ? oldy : newy;
                }
            }
            if (chainx !== chain.ox || chainy !== chain.oy) {
                const oldChainX = chain.ox;
                const oldChainY = chain.oy;
                ballChainMoved = true;
                if (game.u) game.u._bcFelt = (game.u._bcFelt || 0) & ~BC_CHAIN;
                chain.ox = chainx;
                chain.oy = chainy;
                redrawWithoutBallChain([ball.ox, ball.oy], [oldChainX, oldChainY], [chainx, chainy]);
            }
        } else if (chainDist2 > 2) {
            const oldBallX = ball.ox;
            const oldBallY = ball.oy;
            const oldChainX = chain.ox;
            const oldChainY = chain.oy;
            ballChainMoved = true;
            if (game.u) game.u._bcFelt = 0;
            ball.ox = oldChainX;
            ball.oy = oldChainY;
            chain.ox = oldx;
            chain.oy = oldy;
            ballDragDelay = true;
            ballDragNearHostile = (game.level?.monsters || []).some(mon =>
                !mon.pet && !mon.mpeaceful
                && (
                    Math.max(Math.abs(mon.mx - oldx), Math.abs(mon.my - oldy)) <= 1
                    || Math.max(Math.abs(mon.mx - newx), Math.abs(mon.my - newy)) <= 1
                )
            );
            ballDragStopsTravel = !!(game._travel_keys?.length || game._travel_dynamic_target);
            game._travel_keys = [];
            game._travel_dynamic_target = null;
            game._travel_ignore_adjacent_stop = 0;
            game._run_steps_remaining = 0;
            redrawWithoutBallChain([oldBallX, oldBallY], [oldChainX, oldChainY], [oldx, oldy]);
        }
    }
    game.u.ux0 = oldx;
    game.u.uy0 = oldy;
    game.u.ux = newx;
    game.u.uy = newy;
    game.u.umoved = true;
    if (game.u.usteed) {
        game.u.usteed.mx = newx;
        game.u.usteed.my = newy;
    }
    game.context.move = ballStepNoTime ? 0 : 1;
    if (ballStepNoTime && game.u)
        game.u.umovement = Math.max(0, (game.u.umovement || 0) - NORMAL_SPEED);
    if (ballDragDelay) {
        if (game.u?.veryfast) {
            game._ball_drag_delay_turns_remaining = Math.max(game._ball_drag_delay_turns_remaining || 0, 2);
        } else {
            game.context.move = 2;
            game._ball_drag_delay_no_resume = Math.max(game._ball_drag_delay_no_resume || 0, 2);
            game._force_monster_turn_tail_turns = Math.max(game._force_monster_turn_tail_turns || 0, 2);
        }
        if (game._force_next_ball_drag_tail_turns) {
            game._force_monster_turn_tail_turns = Math.max(
                game._force_monster_turn_tail_turns || 0,
                game._force_next_ball_drag_tail_turns,
            );
            game._force_next_ball_drag_tail_turns = 0;
        }
        if (ballDragStopsTravel) {
            game._ball_drag_travel_delay_subtract = 1;
        }
        if (ballDragNearHostile) {
            game._ball_drag_force_tail_on_last_turn = 1;
        }
    }
    if (game.u?.blind && game.u.uball && game.u.uchain && !ballChainMoved) {
        for (let i = (game.level?.objects || []).length - 1; i >= 0; i--) {
            const obj = game.level.objects[i];
            if (obj.ox !== oldx || obj.oy !== oldy || (obj !== game.u.uball && obj !== game.u.uchain)) continue;
            game.u._bcFelt = obj === game.u.uball ? BC_BALL : BC_CHAIN;
            game.u._bcFeltGlyph = { x: oldx, y: oldy, ch: obj.glyph, color: obj.color ?? NO_COLOR };
            break;
        }
    }
    newsym(oldx, oldy);
    newsym(newx, newy);
    vision_recalc(0);
    seeNearbyObjects();
    if (ballChainMoved && game.u?.uball && game.u?.uchain) {
        if (game.u.uball.ox === game.u.uchain.ox && game.u.uball.oy === game.u.uchain.oy) {
            const objects = game.level?.objects;
            if (objects?.includes(game.u.uball) && objects.includes(game.u.uchain)) {
                game.level.objects = objects.filter(obj => obj !== game.u.uball && obj !== game.u.uchain);
                game.level.objects.push(game.u.uball, game.u.uchain);
            }
        }
        newsym(game.u.uchain.ox, game.u.uchain.oy);
        newsym(game.u.uball.ox, game.u.uball.oy);
    }
    if (game.u?.warning) {
        for (const mon of game.level?.monsters || []) {
            if (game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT) continue;
            newsym(mon.mx, mon.my);
        }
    }
    for (const guard of (game.level?.monsters || []).filter(mon => mon.isgd && mon._vault_escort_active && mon.mx))
        newsym(guard.mx, guard.my);
    const steppedTrap = game.level?.traps?.find(trap => trap.tx === newx && trap.ty === newy);
    if (liquidTarget && nopick && (targetMoveTyp === LAVAPOOL || targetMoveTyp === LAVAWALL)) {
        d(6, 6);
        await setMessage(targetMoveTyp === LAVAWALL
            ? 'You fall into the wall of lava!  You burn to a crisp...'
            : 'You fall into the molten lava!  You burn to a crisp...', true);
        game._death_cause = 'burned by molten lava';
        game._command_mode = 'lavaDeathMore';
        game.context.move = 0;
        game._pending_time_passed = 0;
        return;
    }
    if (liquidTarget && nopick && targetMoveTyp !== LAVAPOOL && targetMoveTyp !== LAVAWALL) {
        const dirs = LANDING_DIRS.map((_, i) => i);
        for (let i = dirs.length; i > 0; i--) {
            const j = rn2(i);
            const k = dirs[j];
            dirs[j] = dirs[i - 1];
            dirs[i - 1] = k;
        }
        for (const idx of dirs) {
            const x = newx + LANDING_DIRS[idx].dx;
            const y = newy + LANDING_DIRS[idx].dy;
            const loc = game.level?.at(x, y);
            const locMoveTyp = movementSurfaceTerrain(loc);
            if (!loc || IS_OBSTRUCTED(locMoveTyp) || movementIsLiquidAt(x, y, loc)) continue;
            if ((game.level?.monsters || []).some(mon => mon.mx === x && mon.my === y)) continue;
            game._relocate_after_more = { fromX: newx, fromY: newy, x, y };
            game._topline_after_more = targetMoveTyp === WATER
                ? 'Pheew!  That was close.'
                : 'You try to crawl out of the water.  Pheew!  That was close.';
            break;
        }
        const msg = targetMoveTyp === WATER
            ? 'You plunge into the wall of water!  You try to crawl out of the water.'
            : 'You fall into the pool of water!  You sink like a rock.';
        await setMessage(msg, true);
        game.context.move = 1;
        return;
    }
    if ([HOLE, TRAPDOOR, LEVEL_TELEP, MAGIC_PORTAL].includes(steppedTrap?.ttyp)) {
        const result = movementTransportTrapResult(steppedTrap);
        if (result) {
            game._run_steps_remaining = 0;
            game._run_stop_now = 1;
            game._travel_keys = [];
            game._travel_dynamic_target = null;
            if (result.message) await setMessage(result.message, result.more);
            else {
                game._pending_message = '';
                game._message_more = 0;
                game._keep_pending_message = 0;
            }
            return;
        }
    }
    if (steppedTrap?.ttyp === TELEP_TRAP) {
        game.u.ux0 = newx;
        game.u.uy0 = newy;
        if (heroHasAntimagic() || In_endgame(game.u?.uz) || heroNoTeleportLevel()) {
            steppedTrap.tseen = true;
            await setMessage('You feel a wrenching sensation.');
            return;
        }
        let teleportedToVault = false;
        let materializeMessage = 'You materialize in a different location!';
        if (steppedTrap.once) {
            game.level.traps = (game.level.traps || []).filter(trap => trap !== steppedTrap);
            const vaultRoom = (game.level?.rooms || []).find(room => room.rtype === VAULT);
            const spot = {};
            if (vaultRoom && somexyspace(vaultRoom, spot)) {
                const occupiedByMonster = (game.level?.monsters || [])
                    .some(mon => mon.mx === spot.x && mon.my === spot.y);
                if (!occupiedByMonster) {
                    game.u.ux = spot.x;
                    game.u.uy = spot.y;
                    teleportedToVault = true;
                }
            }
        }
        if (!teleportedToVault) {
            const fixedTeleport = teleportHeroToFixedTrapDestination(steppedTrap);
            if (fixedTeleport === '') {
                newsym(newx, newy);
                await setMessage('You shudder for a moment.', true);
                return;
            }
            if (fixedTeleport) {
                materializeMessage = fixedTeleport;
            } else {
                u_on_rndspot(false);
            }
        }
        newsym(newx, newy);
        newsym(game.u.ux, game.u.uy);
        vision_recalc(0);
        const trapRoomno = game.level?.at(newx, newy)?.roomno || 0;
        const arrivalRoomno = game.level?.at(game.u?.ux || 0, game.u?.uy || 0)?.roomno || 0;
        const templeEntry = prepareUntendedTempleEntry(arrivalRoomno, trapRoomno);
        if (templeEntry) {
            if (templeEntry.text) {
                queueMessageAfterMore(templeEntry.text);
                game._queued_temple_ghost_roll_after_more = 1;
            } else {
                const ghostText = await finishUntendedTempleEntry();
                if (ghostText) queueMessageAfterMore(ghostText);
            }
        }
        const pickupTypes = game._autopickup_types || game.flags?.pickup_types || 'all';
        const goldHere = game._autopickup && (pickupTypes === 'all' || pickupTypes.includes('$'))
            ? (game.level?.objects || []).find(obj =>
                !obj.hidden && !obj.transientProjectile
                && obj.ox === game.u?.ux && obj.oy === game.u?.uy
                && (obj.otyp === GOLD_PIECE || obj.glyph === '$'))
            : null;
        if (goldHere) {
            const pickup = pickUpFloorGoldObject(goldHere);
            game._topline_after_more = (pickup.messages || []).join('  ');
        }
        await setMessage(materializeMessage, true);
        return;
    }
    if (steppedTrap?.ttyp === RUST_TRAP) {
        steppedTrap.tseen = true;
        rn2(5);
        await setMessage('A gush of water hits you!');
        return;
    }
    if (steppedTrap?.ttyp === FIRE_TRAP) {
        const result = heroFireTrapResult(steppedTrap, '', { allowLifeSaving: true });
        await setMessage(result.message, result.more);
        applyHeroFireTrapFatalResult(result);
        return;
    }
    if (steppedTrap?.ttyp === ROLLING_BOULDER_TRAP) {
        steppedTrap.tseen = true;
        rnd(20);
        rnd(20);
        const start = steppedTrap.launch;
        const end = steppedTrap.launch2;
        const boulder = (game.level?.objects || []).find(obj =>
            !obj.transientProjectile && obj.otyp === BOULDER && obj.ox === start?.x && obj.oy === start?.y);
        if (boulder && end) {
            boulder.ox = end.x;
            boulder.oy = end.y;
            vision_reset();
            vision_recalc(0);
            newsym(start.x, start.y);
            newsym(end.x, end.y);
        }
        await setMessage('Click!  You trigger a rolling boulder trap!  A boulder misses you.');
        return;
    }
    if (steppedTrap?.ttyp === STATUE_TRAP) {
        const message = await activateStatueTrap(steppedTrap, newx, newy);
        if (message) await setMessage(message);
        return;
    }
    const brokenDoorTopology = game._tutorial_triggers?.brokenDoorTopology;
    if (brokenDoorTopology && target?.typ === DOOR && (target.doormask & D_BROKEN)
        && newx === brokenDoorTopology.x && newy === brokenDoorTopology.y) {
        for (const wall of brokenDoorTopology.walls || []) {
            game.level.at(wall.x, wall.y).typ = wall.typ;
            newsym(wall.x, wall.y);
        }
        await setMessage('There is a broken door here.');
    }
    if (target?.typ === DOOR && !target.doormask && game._running_continuation && game._run_key)
        game._run_steps_paused_at_door = Math.max(game._run_steps_paused_at_door || 0, game._run_steps_remaining || 0, 20);
    const doorlessDoor = target?.typ === DOOR && (target.doormask === D_NODOOR || target.doormask === D_BROKEN);
    const nextLoc = doorlessDoor ? game.level?.at(newx + dx, newy + dy) : null;
    const leavingRoomThroughDoorlessDoor = doorlessDoor && oldLoc?.typ === ROOM && nextLoc?.typ === CORR;
    const continuingThroughSqueakyDoorway = leavingRoomThroughDoorlessDoor && /squeak/.test(game._pending_message || '');
    if (continuingThroughSqueakyDoorway && (game._initial_run_command || game._running_continuation)) {
        game._run_pause_until_next_command = 1;
        game._doorway_message_run = 1;
    }
    const runStoppingDoor = target?.typ === DOOR && !continuingThroughSqueakyDoorway;
    if (runStoppingDoor || target?.typ === STAIRS || target?.typ === LADDER || target?.typ === FOUNTAIN || target?.typ === SINK) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
    }
    if (game._running_continuation && !game._ignore_run_stop_after_more && (game.level?.monsters || []).some(mon =>
        !mon.pet && !mon.mpeaceful && mon.mx === newx + dx && mon.my === newy + dy
    )) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
    }
    const engraving = game.level?.engravings?.find(engr => engr.x === newx && engr.y === newy && engr.text);
    let smudgedNewEngraving = false;
    if (engraving) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
        if (game._tutorial_active) {
            game._tutorial_read_after_more = { text: engraving.text, more: false };
            game._tutorial_smudge_after_read = {
                oldx, oldy, newx, newy,
                running: !!(game._running_continuation || game._initial_run_command),
            };
            game._command_mode = 'tutorialEngravingAnnounceMore';
            await setMessage('Something is engraved here on the floor.', true);
        } else {
            const loc = game.level?.at(newx, newy);
            const surface = loc?.typ === ICE ? 'ice' : 'floor';
            const message = engraving.type === DUST
                ? `Something is written here in the ${loc?.typ === ICE ? 'frost' : 'dust'}.`
                : engraving.type === ENGRAVE || engraving.type === HEADSTONE
                    ? `Something is engraved here on the ${surface}.`
                    : engraving.type === BURN
                        ? `Some text has been ${loc?.typ === ICE ? 'melted' : 'burned'} into the ${surface} here.`
                        : engraving.type === MARK
                            ? `There's some graffiti on the ${surface} here.`
                            : engraving.type === ENGR_BLOOD
                                ? 'You see a message scrawled in blood here.'
                                : 'Something is written in a very strange way.';
            const punct = /[.!?]$/.test(engraving.text || '') ? '' : '.';
            const readMessage = `You read: "${engraving.text}"${punct}`;
            const combined = `${message}  ${readMessage}`;
            const width = game.nhDisplay?.cols || 80;
            if (combined.length < width - 8) {
                await setMessage(combined);
            } else {
                let firstLine = readMessage;
                let secondLine = '';
                if (readMessage.length >= width) {
                    const split = readMessage.lastIndexOf(' ', width);
                    firstLine = readMessage.slice(0, split);
                    secondLine = readMessage.slice(split + 1);
                }
                game._queued_message_after_more = firstLine;
                game._queued_message_more_after_more = !!secondLine;
                game._queued_message_more_line_after_more = secondLine;
                await setMessage(message, true);
            }
            if (engraving.type === DUST) {
                wipe_engr_at(newx, newy, rnd(5), false);
                smudgedNewEngraving = true;
            }
        }
    }
    const deferTutorialSmudge = game._tutorial_active && engraving;
    if (!deferTutorialSmudge && oldEngraving && oldEngraving.type !== HEADSTONE)
        wipe_engr_at(oldx, oldy, rnd(5), false);
    if (engraving
        && !deferTutorialSmudge
        && !smudgedNewEngraving
        && (newx !== oldx || newy !== oldy)
        && engraving.type !== HEADSTONE
        && !engraving.nowipeout)
        wipe_engr_at(newx, newy, rnd(5), false);
    const objectsHere = (game.level?.objects || [])
        .filter(obj => !obj.hidden && !obj.transientProjectile
            && obj !== game.u?.uball && obj !== game.u?.uchain
            && obj.ox === newx && obj.oy === newy && obj.otyp !== BOULDER);
    const ballHere = game.u?.uball?.ox === newx && game.u?.uball?.oy === newy ? game.u.uball : null;
    const objHere = objectsHere[0];
    const trapHere = steppedTrap;
    const continuingTravel = !!game._travel_keys?.length;
    const oldRoomno = oldLoc?.roomno || 0;
    const newRoomno = target?.roomno || 0;
    const templeEntry = prepareUntendedTempleEntry(newRoomno, oldRoomno);
    if (templeEntry) {
        const ghostText = await finishUntendedTempleEntry();
        const templeText = [templeEntry.text, ghostText].filter(Boolean).join('  ');
        if (templeText) {
            await setMessage(templeText, !!ghostText);
            return;
        }
    }
    const shopRoom = levelRoomByRoomno(newRoomno);
    if (!continuingTravel && shopRoom?.rtype >= SHOPBASE && oldRoomno !== newRoomno) {
        const shkp = shopRoom.resident || (game.level?.monsters || [])
            .find(mon => mon.isshk && mon.shoproom === newRoomno);
        if (shkp?.isshk && shkp.mpeaceful && !shkp.following) {
            const hello = shopkeeperHello(shkp);
            const shopIndex = (shkp.shoptype || shopRoom.rtype) - SHOPBASE;
            const shopName = SHOP_TYPES[shopIndex]?.name || 'shop';
            const shopkeeperName = shkp.shknam || shkp.shopkeeperName || shkp.givenName || 'shopkeeper';
            const possessive = shopkeeperName.endsWith('s') ? `${shopkeeperName}'` : `${shopkeeperName}'s`;
            await setMessage(`"${hello}, ${game.plname || 'Hero'}!  Welcome${shkp.visitct ? ' again' : ''} to ${possessive} ${shopName}!"`);
            shkp.visitct = (shkp.visitct || 0) + 1;
            return;
        }
    }
    if ((objectsHere.length || trapHere) && !continuingTravel) {
        game._run_steps_remaining = 0;
        game._run_stop_now = 1;
        if (game._message_more) game._process_time_with_more = 0;
    }
    if (continuingTravel && objectsHere.length && !trapHere) return;
    const runningStep = game._running_continuation || game._initial_run_command;
    if (runningStep && objectsHere.length > 1) {
        game._dismount_object_list_spot = { x: newx, y: newy };
        if (trapHere?.ttyp === MAGIC_TRAP) game._pending_magic_trap = trapHere;
        return;
    }
    if (trapHere?.ttyp === DART_TRAP && objectsHere.length > 1) game._pending_dart_trap = trapHere;
    if (trapHere?.ttyp === BEAR_TRAP && objectsHere.length > 1) game._pending_bear_trap = trapHere;
    const goldHere = objectsHere.find(obj => obj.otyp === GOLD_PIECE || obj.glyph === '$');
    if (game._autopickup && goldHere) {
        const pickup = pickUpFloorGoldObject(goldHere);
        const messages = [...(pickup.messages || [])];
        const remainingObjects = objectsHere.filter(obj => obj !== goldHere);
        if (remainingObjects.length === 1) {
            const remaining = remainingObjects[0];
            if (remaining.kind === 'chest' || remaining.otyp === CHEST)
                messages.push(`You see here a ${remaining.lknown && (remaining.locked || remaining.olocked) ? 'locked ' : ''}chest.`);
            else messages.push(`You see here ${pickupObjectPhrase(remaining)}.`);
        }
        await setMessage(messages.join('  '));
        return;
    }
    const pickupTypes = game._autopickup_types || game.flags?.pickup_types || 'all';
    const pickedObject = game._autopickup && objectsHere.find(obj => {
        const pickupGlyph = obj.glyph || OBJECT_CLASS_GLYPHS[obj.cls] || (obj.otyp === SPE_HEALING ? '+' : '');
        return pickupGlyph && (pickupTypes === 'all' || pickupTypes.includes(pickupGlyph));
    });
    if (pickedObject) {
        const letter = pickedObject.wasStolen && pickedObject.letter
            ? pickedObject.letter
            : nextInventoryLetter();
        const amount = pickupObjectPhrase(pickedObject);
        const spellbookInfo = pickedObject.otyp === SPE_HEALING
            ? { cls: 'spellbook', spellName: 'healing', spell: { name: 'healing', level: 1, skill: 'healing' }, known: false, bknown: false }
            : {};
        const pickedItem = {
            ...pickedObject,
            ...spellbookInfo,
            cls: pickedObject.cls || spellbookInfo.cls || (pickedObject.otyp === DART ? 'weapon'
                : pickedObject.otyp === GEM_CLASS ? 'gem'
                    : pickedObject.otyp === 'corpse' || pickedObject.otyp === CORPSE ? 'food'
                        : pickedObject.otyp === RING_CLASS || pickedObject.glyph === '=' ? 'ring'
                            : BAG_OBJECT_TYPES.has(pickedObject.otyp) || pickedObject.otyp === OIL_LAMP || pickedObject.otyp === MAGIC_LAMP
                                || pickedObject.otyp === MIRROR || pickedObject.otyp === EXPENSIVE_CAMERA
                                || pickedObject.otyp === STETHOSCOPE || pickedObject.otyp === MAGIC_MARKER ? 'tool'
                                : pickedObject.otyp === SCROLL_CLASS ? 'scroll'
                                    : pickedObject.otyp === POTION_CLASS ? 'potion'
                                        : pickedObject.otyp === WAND_CLASS ? 'wand'
                                            : undefined),
            letter,
            kind: pickedObject.kind || pickupObjectName({ ...pickedObject, quan: 1 }),
            line: `${letter} - ${amount}`,
        };
        game.inventory = [...(game.inventory || []), pickedItem];
        maybeAttachCarriedFigurineTimeout(pickedItem);
        game.level.objects = (game.level.objects || []).filter(obj => obj !== pickedObject);
        game._pet_food_scan_inventory = game.inventory;
        newsym(newx, newy);
        const remainingObjects = (game.level.objects || [])
            .filter(obj => !obj.hidden && !obj.transientProjectile && obj.ox === newx && obj.oy === newy && obj.otyp !== BOULDER);
        let message = `${letter} - ${amount}.`;
        let messageMore = !!pickedObject.wasStolen;
        if (remainingObjects.length === 1) {
            const remaining = remainingObjects[0];
            const hereMessage = remaining.kind === 'chest' || remaining.otyp === CHEST
                ? `You see here a ${remaining.lknown && (remaining.locked || remaining.olocked) ? 'locked ' : ''}chest.`
                : `You see here ${pickupObjectPhrase(remaining)}.`;
            const combined = `${message}  ${hereMessage}`;
            if (combined.length < (game.nhDisplay?.cols || 80) - 8) message = combined;
            else messageMore = true;
        } else if (remainingObjects.length) {
            const objects = [...remainingObjects]
                .map((obj, index) => ({ obj, index }))
                .sort((a, b) => {
                    const corpseOrder = ((b.obj.otyp === 'corpse' || b.obj.otyp === CORPSE) ? 1 : 0)
                        - ((a.obj.otyp === 'corpse' || a.obj.otyp === CORPSE) ? 1 : 0);
                    if (corpseOrder) return corpseOrder;
                    if (a.obj.pileIndex != null || b.obj.pileIndex != null)
                        return (a.obj.pileIndex || 0) - (b.obj.pileIndex || 0);
                    return b.index - a.index;
                })
                .map(item => item.obj);
            const rows = [[0, 41, 'Other things that are here:']];
            for (let i = 0; i < objects.length; i++)
                rows.push([i + 1, 41, BAG_OBJECT_TYPES.has(objects[i].otyp) ? 'a bag' : pickupObjectPhrase(objects[i])]);
            rows.push([objects.length + 1, 41, '--More--']);
            game._queued_overlay_after_more = {
                lines: rows,
                clearRows: objects.length + 2,
                clearCol: 40,
                mode: 'objectListMore',
                usePendingTime: true,
            };
            messageMore = true;
        }
        await setMessage(message, messageMore);
        return;
    }
    const pileLimit = Number(game.flags?.pile_limit ?? 5);
    const skipObjectList = pileLimit > 0 && objectsHere.length >= pileLimit;
    let pileMessage = '';
    if (!game._running_continuation && objectsHere.length > 1 && !skipObjectList) {
        const objects = [...objectsHere]
            .map((obj, index) => ({ obj, index }))
            .sort((a, b) => {
                const corpseOrder = ((b.obj.otyp === 'corpse' || b.obj.otyp === CORPSE) ? 1 : 0)
                    - ((a.obj.otyp === 'corpse' || a.obj.otyp === CORPSE) ? 1 : 0);
                if (corpseOrder) return corpseOrder;
                if (a.obj.pileIndex != null || b.obj.pileIndex != null)
                    return (a.obj.pileIndex || 0) - (b.obj.pileIndex || 0);
                return b.index - a.index;
            })
            .map(item => item.obj);
        const rows = [[0, 41, 'Things that are here:']];
        for (let i = 0; i < objects.length; i++) {
            const obj = objects[i];
            rows.push([i + 1, 41, BAG_OBJECT_TYPES.has(obj.otyp) ? 'a bag' : pickupObjectPhrase(obj)]);
        }
        rows.push([objects.length + 1, 41, '--More--']);
        setOverlay(rows, objects.length + 2, false, 40);
        game._command_mode = 'objectListMore';
        game._deferred_context_move = game.context.move || 1;
        game.context.move = 0;
        if (trapHere?.ttyp === MAGIC_TRAP) game._pending_magic_trap = trapHere;
        return;
    }
    if (objectsHere.length > 1 && skipObjectList) {
        const countName = objectsHere.length === 2 ? 'two'
            : objectsHere.length < 5 ? 'a few'
                : objectsHere.length < 10 ? 'several' : 'many';
        pileMessage = `There are ${countName} objects here.`;
    }
    if (trapHere?.ttyp === MAGIC_TRAP) {
        const result = magicTrapResult(trapHere);
        if (result.afterMore) game._topline_after_more = result.afterMore;
        const message = [pileMessage, result.message].filter(Boolean).join('  ');
        if (message) await setMessage(message, !!result.afterMore || !!result.more || !!(pileMessage && result.message));
        return;
    }
    if (pileMessage) {
        await setMessage(pileMessage);
        return;
    }
    let featurePrefix = '';
    if (target?.typ === DOOR && (target.doormask & D_BROKEN)) {
        featurePrefix = 'There is a broken door here.  ';
    } else if (target?.typ === DOOR && target.doormask === 0) {
        featurePrefix = 'There is a doorway here.  ';
    } else if (target?.typ === STAIRS && game.level?.dnstair?.x === newx && game.level?.dnstair?.y === newy) {
        featurePrefix = 'There is a staircase down here.  ';
    } else if (target?.typ === STAIRS && game.level?.upstair?.x === newx && game.level?.upstair?.y === newy) {
        featurePrefix = (game.u?.uz?.dlevel ?? 1) === 1
            ? 'There is a staircase up out of the dungeon here.  '
            : 'There is a staircase up here.  ';
    }
    if (goldHere) {
        const amount = goldHere.quan || 1;
        await setMessage(`${featurePrefix}You see here ${amount} gold piece${amount === 1 ? '' : 's'}.`);
        return;
    }
    const priceSuffix = objHere ? shopItemPriceSuffix(objHere, newx, newy) : '';
    let objHereMessage = '';
    if (objHere?.cls === 'armor') {
        const seenArmorName = pickupObjectName(objHere);
        const actualArmorName = String(objHere.actualKind || objHere.kind || '').toLowerCase();
        if (['plumed helmet', 'conical hat'].includes(seenArmorName) || actualArmorName === 'shield of reflection') {
            const price = shopItemPrice(objHere, newx, newy);
            const discoveryName = actualArmorName === 'shield of reflection' ? actualArmorName : seenArmorName;
            const appearance = actualArmorName === 'shield of reflection'
                ? (objHere.appearance || 'polished silver shield')
                : '';
            game._discoveries ??= [];
            if (!game._discoveries.some(entry => entry.section === 'Armor' && entry.name === discoveryName)) {
                game._discoveries.push({
                    section: 'Armor',
                    name: discoveryName,
                    text: `${discoveryName}${appearance ? ` (${appearance})` : ''}${price > 0 ? ` {buy ${price}}` : ''}`,
                    starred: false,
                });
            }
        }
    }
    if ((objHere?.otyp === 'corpse' || objHere?.otyp === CORPSE) && objHere.corpsenm?.name)
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere?.kind === 'dart' && (objHere.quan || 1) > 1)
        objHereMessage = `${featurePrefix}You see here ${objHere.quan} darts${priceSuffix}.`;
    else if (objHere?.kind === 'sling')
        objHereMessage = `${featurePrefix}You see here a sling${priceSuffix}.`;
    else if (objHere?.glyph === '*') {
        const quantity = objHere.quan || 1;
        objHereMessage = featurePrefix + (quantity > 1
            ? `You see here ${quantity} ${pickupObjectName(objHere)}${priceSuffix}.`
            : `You see here a ${pickupObjectName(objHere)}${priceSuffix}.`);
    } else if (objHere?.glyph === '=' || objHere?.cls === 'ring')
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere?.otyp === SCROLL_CLASS || objHere?.cls === 'scroll')
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere?.otyp === POTION_CLASS || objHere?.cls === 'potion')
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere?.otyp === WAND_CLASS || objHere?.cls === 'wand')
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere?.otyp === FOOD_CLASS || objHere?.cls === 'food')
        objHereMessage = `${featurePrefix}You see here ${pickupObjectPhrase(objHere)}${priceSuffix}.`;
    else if (objHere?.kind === 'chest' || objHere?.otyp === CHEST)
        objHereMessage = `${featurePrefix}You see here a ${objHere.lknown && (objHere.locked || objHere.olocked) ? 'locked ' : ''}chest${priceSuffix}.`;
    else if ((objHere?.kind === 'statue' || objHere?.otyp === STATUE) && objHere.corpsenm?.name)
        objHereMessage = `${featurePrefix}You see here a ${pickupObjectName(objHere)}${priceSuffix}.`;
    else if (objHere)
        objHereMessage = `${featurePrefix}You see here ${pickupObjectPhrase(objHere)}${priceSuffix}.`;
    else if (ballHere)
        objHereMessage = `${featurePrefix}You see here ${pickupObjectPhrase(ballHere)}.`;

	    if (objHereMessage) {
	        if (game.u?.blind) {
	            game._blind_feel_object_after_more = objHereMessage.replace('You see here', 'You feel here');
	            game._blind_feel_object_after_more_obj = objHere || ballHere;
	            await setMessage('You try to feel what is lying here on the floor.', true);
	        } else await setMessage(objHereMessage);
	    }
	}

export async function rhack(_cmd) {
    game.context = game.context || {};
    game.context.move = 0;
    game.u.umoved = false;

	    const key = _cmd || await nhgetch();
	    let ch = commandChar(key);
	    if (game.u?.uswallow && game._swallow_overlay_active && game._overlay_lines
	        && !(game._pending_message && game._message_more)) {
	        game._swallow_overlay_before_command = game._overlay_lines.map(line => [...line]);
	    }
	    if (game._welcome_message && !game._message_more) moveloopPreambleOnce();
	    const ignoreSafeWaitOnce = !!game._ignore_safe_wait_once;
	    game._ignore_safe_wait_once = 0;
	    game._escape_dismissed_more_this_command = 0;
	    game._last_command_was_search = false;
    game._forced_search_this_turn = 0;
    if (ch !== 's') game._search_safety_warning_active = 0;
    if (ch !== '.') game._safe_wait_repeat_suppressed = 0;
    const processLevelTeleportArrivalMoveNow = !!game._level_teleport_arrival_process_next_move;
    game._level_teleport_arrival_process_next_move = 0;
    if (processLevelTeleportArrivalMoveNow && movementDirection(ch))
        game._process_deferred_context_now = 1;
    const requestMenuPrefix = !!game._request_menu_prefix && ch !== 'm';
    if (ch !== 'm') game._request_menu_prefix = 0;
    const role = game.urole?.name?.m;
    game._cursor_override = null;
    game._run_stop_now = 0;
    game._initial_run_command = 0;
    if (game._run_pause_until_next_command && ch !== '\f' && !game._running_continuation) {
        game._run_pause_until_next_command = 0;
        game._doorway_message_run = 0;
        game._run_steps_remaining = 0;
    }
    const preservePendingForKey = game._preserve_pending_message_key === ch;
    if (preservePendingForKey) {
        game._preserve_pending_message_key = null;
        game._keep_pending_message = 1;
    }
    if (game._overlay_once) {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        game._overlay_once = 0;
        game._suppress_space_unknown_once = 1;
    }

    if (game._fight_wall_message && game._pending_message && !game._message_more) {
        game._fight_wall_message = 0;
        game._pending_message = '';
        game._pending_message_blocks_time = 0;
        game._pending_explore_lifesaving_message = 0;
        game._pending_message_is_search_safety_warning = 0;
        game._pending_message_is_level_teleport_materialization = 0;
        game._pending_message_is_level_teleport_temperature_arrival = 0;
        game._pending_rotten_food_eating_message = 0;
        game._keep_pending_message = 0;
        game._hide_pending_message_once = 0;
    }

    if (game._intro_lines && (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n')) {
        game._intro_lines = null;
        if (game._post_intro_ac !== undefined) game.u.uac = game._post_intro_ac;
        if (game._post_intro_energy !== undefined) {
            game.u.uen = game._post_intro_energy;
            game.u.uenmax = game._post_intro_energy;
            game.u.uenpeak = game._post_intro_energy;
            game.u.ueninc ??= [];
            game.u.ueninc[game.u.ulevel || 1] = game._post_intro_energy;
            delete game._post_intro_energy;
        }
        moveloopPreambleOnce();
        game._keep_pending_message = 1;
        return;
    }

    if (!game._running_continuation && !game._command_mode && !game._travel_keys?.length && !game._travel_dynamic_target
        && game._pending_message && !game._message_more
        && !/^[1-9]$/.test(ch) && !preservePendingForKey) {
        const clearedPendingMessage = game._pending_message;
        const clearedLevelTeleportMaterialization = !!game._pending_message_is_level_teleport_materialization;
        const clearedLevelTeleportTemperatureArrival = !!game._pending_message_is_level_teleport_temperature_arrival;
        if (game._deferred_gold_count_after_message != null) {
            game._goldCount = game._deferred_gold_count_after_message;
            game._deferred_gold_count_after_message = null;
            const money = (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin');
            if (money) {
                money.quan = game._goldCount;
                updateMoneyLine(money);
            }
        }
        game._pending_message = '';
        game._pending_message_blocks_time = 0;
        game._pending_explore_lifesaving_message = 0;
        game._pending_message_is_search_safety_warning = 0;
        game._pending_message_is_level_teleport_materialization = 0;
        game._pending_message_is_level_teleport_temperature_arrival = 0;
        game._clear_search_safety_message_next_flush = 0;
        game._pending_rotten_food_eating_message = 0;
        game._keep_pending_message = 0;
        game._hide_pending_message_once = 0;
        if (game._clear_pending_message_only_once) {
            game._clear_pending_message_only_once = 0;
            if (game._valley_gas_after_clear) {
                game._valley_gas_after_clear = 0;
            } else {
                return;
            }
        }
        if (ch === '.' && clearedPendingMessage === 'Are you waiting to get hit?') {
            game._safe_wait_repeat_suppressed = 1;
            return;
        }
        if (clearedLevelTeleportMaterialization && movementDirection(ch))
            game._process_deferred_context_now = 1;
        if (ch === '.' && clearedLevelTeleportTemperatureArrival)
            return;
    }

    if (await handleSanctumSummonScript(ch)) return;

    if (game._command_mode === 'wizardWish' && ch !== '\r' && ch !== '\n') {
        if (ch === '\x1b' || key === 27) {
            game._wish_text = '';
            game._command_mode = null;
            await finishRandomBlankWish();
            return;
        }
        if (key === 8 || key === 127) game._wish_text = (game._wish_text || '').slice(0, -1);
        else if (key >= 32) game._wish_text = `${game._wish_text || ''}${ch}`;
        await setMessage(`For what do you wish?${game._wish_text ? ` ${game._wish_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'demonBribeOffer') {
        const keyCode = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (ch === '\r' || ch === '\n' || ch === '\x1b' || keyCode === 27) {
            if (ch === '\x1b' || keyCode === 27) game._demon_bribe_text = '';
            await finishDemonBribeOffer();
            return;
        }
        if (keyCode === 8 || keyCode === 127) game._demon_bribe_text = (game._demon_bribe_text || '').slice(0, -1);
        else if (keyCode >= 32) game._demon_bribe_text = `${game._demon_bribe_text || ''}${ch}`;
        const prompt = game._demon_bribe_prompt || 'How much will you offer?';
        await setMessage(`${prompt}${game._demon_bribe_text ? ` ${game._demon_bribe_text}` : ''}`);
        return;
    }

    if (ch === '\f' && game._run_key && game._run_steps_remaining > 0 && !game._message_more) {
        game._run_pause_until_next_command = 0;
        game._run_steps_remaining--;
        game._running_continuation = 1;
        await rhack(game._run_key);
        game._running_continuation = 0;
        if (game.context?.move) {
            game._pending_time_passed = (game._pending_time_passed || 0) + game.context.move;
            game.context.move = 0;
        } else {
            game._run_steps_remaining = 0;
        }
    }

    if (ch === '\f') {
        if (!game._message_more) {
            game._pending_message = '';
            game._keep_pending_message = 0;
        }
        await docrt();
        await flush_screen(1);
        return;
    }

    if (game._command_mode === 'objectListMore') {
        const dismissingObjectList = ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n';
        if (!dismissingObjectList) {
            if ((ch === 'p' || game._object_list_nohands_on_other_key) && polyselfNoHands()) {
                game._object_list_pending_nohands_pickup = 1;
            }
            return;
        }
        if (dismissingObjectList) {
            const objectListRows = game._overlay_lines?.length || 0;
            const restoreMessage = game._object_list_restore_message || '';
            const processDeferredNow = !!game._object_list_process_deferred_now;
            const objectListNoTime = !!game._object_list_no_time;
            const objectListNohandsOnOtherKey = !!game._object_list_nohands_on_other_key;
            const pendingNohandsPickup = !!game._object_list_pending_nohands_pickup;
            game._object_list_restore_message = '';
            game._object_list_process_deferred_now = 0;
            game._object_list_no_time = 0;
            game._object_list_nohands_on_other_key = 0;
            game._object_list_pending_nohands_pickup = 0;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            game._pending_message = restoreMessage;
            game._message_more = 0;
            game._keep_pending_message = restoreMessage ? 1 : 0;
            if (processDeferredNow) {
                game.context.move = game._deferred_context_move || 1;
            } else if (game._object_list_use_pending_time) {
                game._object_list_use_pending_time = 0;
                game.context.move = 0;
            } else if (objectListNoTime) {
                game.context.move = 0;
            } else {
                game.context.move = game._deferred_context_move || 1;
            }
            game._deferred_context_move = 0;
            game._process_command_time_now = 1;
            if (processDeferredNow) {
                if (game.u) game.u.umovement = Math.min(game.u.umovement ?? NORMAL_SPEED, NORMAL_SPEED);
                game._process_deferred_context_now = 1;
            }
            const dismissalNohandsPickup = !pendingNohandsPickup && processDeferredNow
                && objectListNohandsOnOtherKey && polyselfNoHands();
            if (pendingNohandsPickup || dismissalNohandsPickup) {
                await setMessage('You are physically incapable of picking anything up.', dismissalNohandsPickup);
                if (processDeferredNow && dismissalNohandsPickup) {
                    game._process_time_with_more = 1;
                    const currentSpecial = game.specialLevels?.find(level =>
                        level.dnum === game.u?.uz?.dnum && level.dlevel === game.u?.uz?.dlevel);
                    if (currentSpecial?.name === 'sanctum'
                        && game.u?.blind && polyselfForm()?.name === 'brown mold') {
                        game._queued_map_invisible_after_more = { x: 66, y: 15 };
                        game._queued_damage_after_more = (game._queued_damage_after_more || 0) + 7;
                        game._queued_sanctum_touch_after_arrival_more = 1;
                        game._queued_sanctum_spell_after_touch_more = 1;
                        queueSanctumSpellcasterResume();
                    }
                } else if (!processDeferredNow) game.context.move = 0;
                return;
            }
            if (game._pending_magic_trap) {
                const result = magicTrapResult(game._pending_magic_trap);
                game._pending_magic_trap = null;
                if (result.afterMore) game._topline_after_more = result.afterMore;
                if (result.message) await setMessage(result.message, !!result.afterMore || !!result.more);
                return;
            }
            if (game._pending_dart_trap) {
                game._pending_dart_trap.tseen = true;
                game._pending_dart_trap.once = true;
                game._pending_dart_trap = null;
                const dart = mksobj(DART, true, false);
                dart.quan = 1;
                dart.opoisoned = !rn2(6);
                const damage = rnd(3);
                const threshold = (game.u?.uac || 0) + 7;
                const roll = rnd(20);
                const misses = threshold <= roll;
                if (misses) {
                    Object.assign(dart, {
                        kind: 'dart',
                        ox: game.u?.ux || 0,
                        oy: game.u?.uy || 0,
                        glyph: ')',
                        color: CLR_CYAN,
                    });
                    game.level.objects.push(dart);
                    newsym(game.u?.ux || 0, game.u?.uy || 0);
                    const missText = threshold <= roll - 2
                        ? 'A little dart misses you.'
                        : 'You are almost hit by a little dart.';
                    await setMessage(`A little dart shoots out at you!  ${missText}`);
                    return;
                }
                game.u.uhp = Math.max(1, (game.u.uhp || 1) - damage);
                rn2(2);
                await setMessage('A little dart shoots out at you!  You are hit by a little dart.');
            }
            if (game._pending_bear_trap) {
                game._pending_bear_trap.tseen = true;
                game._pending_bear_trap = null;
                const damage = d(2, 4);
                game.u.utrap = rn2(4) + 4;
                game.u.utraptype = 'beartrap';
                const woundedSide = rn2(2);
                const woundDuration = 10 + rn2(10);
                if (game.u?.acurr?.a && !game.u._woundedDexPenalty) {
                    game.u.acurr.a[3] = Math.max(3, (game.u.acurr.a[3] || 9) - 1);
                    game.u._woundedDexPenalty = 1;
                }
                game.u._woundedLegTurns = Math.max(game.u._woundedLegTurns || 0, woundDuration);
                game.u._woundedLegSide = woundedSide ? 'right' : 'left';
                let carriedWeight = Math.trunc(((game._goldCount || 0) + 50) / 100);
                for (const invItem of game.inventory || []) {
                    if (isGoldObject(invItem)) continue;
                    const kind = String(invItem.kind || invItem.actualKind || invItem.spellName || invItem.spell?.name || '').toLowerCase();
                    const cls = invItem.cls || (invItem.otyp === RING_CLASS ? 'ring'
                        : invItem.otyp === SCROLL_CLASS ? 'scroll'
                            : invItem.otyp === POTION_CLASS ? 'potion'
                                : invItem.otyp === WAND_CLASS ? 'wand'
                                    : invItem.otyp === GEM_CLASS ? 'gem' : '');
                    carriedWeight += (OBJECT_WEIGHTS[kind] ?? CLASS_WEIGHTS[cls] ?? invItem.owt ?? 0) * (invItem.quan || 1);
                }
                const stats = game.u?.acurr?.a || [];
                let capacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
                capacity -= 100;
                if (carriedWeight > capacity && !(game.u?._statusSuffix || '').includes('Burdened')) {
                    game.u._statusSuffix = `${game.u._statusSuffix || ''} Burdened`;
                    game._queued_message_after_more = 'Your movements are slowed slightly because of your load.';
                    game._queued_message_process_time_after_more = 1;
                }
                if (objectListRows > 4) {
                    game._bear_trap_damage_after_more = damage;
                    game._bear_trap_exercise_after_more = 1;
                    await setMessage('A bear trap closes on your foot!', true);
                } else {
                    game.u.uhp = Math.max(1, (game.u?.uhp || 1) - damage);
                    rn2(2);
                    await setMessage('A bear trap closes on your foot!');
                }
            }
        }
        return;
    }

    if (game._command_mode === 'dismountObjectList') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            if (game._pending_magic_trap) {
                const result = magicTrapResult(game._pending_magic_trap);
                game._pending_magic_trap = null;
                if (!game._pending_time_passed) game.context.move = 1;
                game._process_command_time_now = 1;
                if (result.afterMore) game._topline_after_more = result.afterMore;
                if (result.message) await setMessage(result.message, !!result.afterMore || !!result.more);
                return;
            }
            if (game._pending_time_passed > 0) {
                game._pending_time_passed--;
                game.context.move = 1;
                game._process_command_time_now = 1;
            }
        }
        return;
    }

    if (game._command_mode === 'questIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            if (game._queued_message_after_more) {
                const next = game._queued_message_after_more;
                const nextMore = !!game._queued_message_more_after_more;
                game._queued_message_after_more = '';
                game._queued_message_more_after_more = 0;
                await setMessage(next, nextMore);
            } else if (game._quest_assignquest_time_after_more) {
                game._quest_assignquest_time_after_more = 0;
                exerciseAttribute(A_WIS, true);
                if (game._quest_leader_talk_automatic) {
                    game._quest_leader_talk_automatic = 0;
                } else {
                    game._resume_time_after_more = 0;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                }
            }
        }
        return;
    }

    if (game._command_mode === 'questLeaderIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            await continueQuestLeaderTalkAfterIntro();
        }
        return;
    }

    if (game._command_mode === 'questLeaderFollowupMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            if (game._queued_overlay_after_more) {
                const next = game._queued_overlay_after_more;
                game._queued_overlay_after_more = null;
                setOverlay(next.lines, next.clearRows ?? 24, !!next.hideStatus, next.clearCol ?? null);
                const messageMore = next.messageMore ?? true;
                game._pending_message = next.message ?? (messageMore ? 'text-window' : '');
                game._message_more = messageMore ? 1 : 0;
                game._keep_pending_message = 1;
                game._command_mode = next.mode || 'questLeaderFollowupMore';
                return;
            }
            game._command_mode = null;
            finishQuestLeaderTalkTurn();
        }
        return;
    }

    if (game._command_mode === 'lifeSavingMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const stoningLifeSaved = !!game._life_saving_clear_stoning;
            const levelTeleportEscape = !!game._life_saving_level_teleport_escape_message;
            const lifeSavingMessage = stoningLifeSaved || levelTeleportEscape
                ? 'You feel much better!  The medallion crumbles to dust!'
                : 'You feel much better!';
            game._pending_message = lifeSavingMessage;
            game._pending_explore_lifesaving_message = 0;
            game._message_more = 0;
            game._keep_pending_message = 1;
            game._command_mode = null;
            if (!stoningLifeSaved && game.u) game.u.uhp = game.u.uhpmax || 1;
            if (stoningLifeSaved) {
                game._life_saving_clear_stoning = 0;
                if (game.u) {
                    game.u._stonedTimeout = 0;
                    game.u._stonedKiller = '';
                }
                removeHeroStatusSuffix('Stone');
                game._death_cause = '';
                game._death_bones_body = '';
                game._death_current_move = 0;
                game._death_status_hp_before_zero = null;
                clearUnsafePetrifyingCorpseWieldAfterLifeSaving();
            }
            if (game._life_saving_refresh_con && game.u?.acurr?.a)
                game.u.acurr.a[4] = Math.max(3, game.u.acurr.a[4] - 1);
            game._life_saving_refresh_con = 0;
            if (stoningLifeSaved && game.u) {
                const con = game.u.acurr?.a?.[A_CON] ?? 10;
                const givehp = 50 + 10 * Math.trunc(con / 2);
                game.u.uhp = Math.min(game.u.uhpmax || 1, givehp);
            }
            if (levelTeleportEscape) {
                const escapeMessage = game._life_saving_level_teleport_escape_message;
                game._life_saving_level_teleport_escape_message = '';
                game._keep_pending_message = 0;
                game._death_cause = '';
                await beginEscapedGame([lifeSavingMessage, escapeMessage]);
                return;
            }
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game._suppress_monster_attack_messages = 1;
        }
        return;
    }

    if (game._command_mode === 'questLeaderStatusMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._pending_time_passed = 0;
            await setMessage('adjust?');
            game._command_mode = 'questLeaderAdjust';
        }
        return;
    }

    if (game._command_mode === 'questLeaderAdjust') {
        if (ch === 'y' || ch === 'Y' || ch === ' ' || ch === '\r' || ch === '\n') {
            if (game.u?.ualign) game.u.ualign.record = MIN_QUEST_ALIGN;
            game.quest_status ??= {};
            game.quest_status.got_quest = true;
            game._quest_assignquest_time_after_more = 1;
            showQuestPager('assignquest', 'questIntroMore');
        } else {
            game.quest_status ??= {};
            game.quest_status.not_ready = 1;
            game._quest_reject_exercise_wis = 1;
            showQuestPager('badalign', 'questLeaderRejectMore');
        }
        return;
    }

    if (game._command_mode === 'questLeaderRejectMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            if (game._quest_reject_exercise_wis) {
                game._quest_reject_exercise_wis = 0;
                exerciseAttribute(A_WIS, true);
            }
            game._quest_leader_talk_automatic = 0;
            await finishLevelTeleport(branchLevel(game.u?.uz?.dnum ?? -1) || { dnum: 0, dlevel: 14 }, { portalArrival: true });
            game._pending_message = '';
            game._message_more = 0;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            game._process_deferred_context_now = 1;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'pickupList') {
        const pickup = game._pickup_list || { objects: [] };
        pickup.entries ??= pickupMenuEntries(pickup.objects);
        if (ch === '@') {
            pickup.selected = pickup.entries.map(entry => entry.letter);
            game._pickup_list = pickup;
            const rows = pickupMenuLines(pickup.entries, pickup.selected);
            setOverlay(rows, Math.max(...rows.map(([row]) => row)) + 1, false, 40);
            return;
        }
        const chosenEntry = pickup.entries.find(entry => entry.letter === ch);
        if (chosenEntry) {
            const selected = new Set(Array.isArray(pickup.selected) ? pickup.selected : pickup.selected ? [pickup.selected] : []);
            if (selected.has(ch)) selected.delete(ch);
            else selected.add(ch);
            pickup.selected = [...selected];
            game._pickup_list = pickup;
            const rows = pickupMenuLines(pickup.entries, pickup.selected);
            setOverlay(rows, Math.max(...rows.map(([row]) => row)) + 1, false, 40);
            return;
        }
        if ((ch === '\r' || ch === '\n' || ch === ' ') && Array.isArray(pickup.selected) && pickup.selected.length) {
            const state = {
                selected: pickupListSelectedObjects(pickup),
                index: 0,
                messages: [],
            };
            clearPickupListOverlay();
            game._command_mode = null;
            await continuePickupListProcessing(state);
            return;
        }
        if (ch === '\x1b' || ch === ' ') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            game._pickup_list = null;
        }
        return;
    }

    if (game._command_mode === 'vaultGuardMore') {
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            game._pending_time_passed = 0;
            game._process_time_with_more = 0;
            game._run_steps_remaining = 0;
            game._pending_message = '"Hello stranger, who are you?" -';
            game._message_more = 0;
            game._keep_pending_message = 1;
            game._vault_guard_name = '';
            game._command_mode = 'vaultGuardName';
        }
        return;
    }

    if (game._command_mode === 'vaultGuardDisappearMore') {
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'vaultGuardName') {
        if (ch === '\r' || ch === '\n') {
            game._pending_message = '"I don\'t know you."  "You have hidden gold."';
            game._message_more = 1;
            game._keep_pending_message = 1;
            game._command_mode = 'vaultGuardAccuseMore';
            return;
        }
        if (key >= 32) game._vault_guard_name = `${game._vault_guard_name || ''}${ch}`;
        game._pending_message = `"Hello stranger, who are you?" - ${game._vault_guard_name || ''}`;
        game._message_more = 0;
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'vaultGuardAccuseMore') {
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            game._pending_message = '"Most likely all your gold was stolen from this vault."';
            game._message_more = 1;
            game._keep_pending_message = 1;
            game._command_mode = 'vaultGuardGoldMore';
        }
        return;
    }

    if (game._command_mode === 'vaultGuardGoldMore') {
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            game._resume_time_after_more = 0;
            game._pending_message = '"Please drop that gold and follow me."';
            game._message_more = 0;
            game._keep_pending_message = 1;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'tutorialEnterMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (game._redraw_level_after_more) {
                game._redraw_level_after_more = 0;
                vision_recalc(0);
                game._display_hallucinated_redraw = 1;
                await docrt();
                game._display_hallucinated_redraw = 0;
                await bot();
                await flush_screen(1);
            } else {
                await docrt();
            }
            game._tutorial_read_after_more = game._tutorial_triggers?.firstRead || { text: '', more: false };
            await setMessage('Something is engraved here on the floor.', true);
            game._command_mode = 'tutorialEngravingAnnounceMore';
        }
        return;
    }

    if (game._command_mode === 'tutorialEngravingAnnounceMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const smudge = game._tutorial_smudge_after_read;
            if (smudge && (ch === '\x1b' || !smudge.running)) {
                const oldEngr = game.level?.engravings?.find(engr => engr.x === smudge.oldx && engr.y === smudge.oldy && engr.text);
                const newEngr = game.level?.engravings?.find(engr => engr.x === smudge.newx && engr.y === smudge.newy && engr.text);
                if (oldEngr && oldEngr.type !== HEADSTONE)
                    wipe_engr_at(smudge.oldx, smudge.oldy, rnd(5), false);
                if ((smudge.newx !== smudge.oldx || smudge.newy !== smudge.oldy)
                    && newEngr && newEngr.type !== HEADSTONE)
                    wipe_engr_at(smudge.newx, smudge.newy, rnd(5), false);
            }
            game._tutorial_smudge_after_read = null;
            const next = game._tutorial_read_after_more || { text: '', more: false };
            game._tutorial_read_after_more = null;
            await setMessage(`You read: "${next.text}".`, !!next.more);
            game._command_mode = next.more ? 'tutorialReadMore' : null;
        }
        return;
    }

    if (game._command_mode === 'tutorialReadMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            if (game._tutorial_active) game.u.uac = 10;
        }
        return;
    }



    if (game._command_mode === 'optionsFullMessages') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const messages = game._options_full_messages || [];
            if (messages.length) {
                const groupIndex = game._options_full_message_group_index || 0;
                const groups = game._options_full_change_groups || [];
                applyFullOptionChanges([
                    ...(groups[groupIndex] || []),
                    ...((groups[groupIndex + 1] || []).slice(0, 1)),
                ]);
                game._options_full_message_group_index = groupIndex + 1;
                const next = messages.shift();
                game._options_full_messages = messages;
                await setMessage(next, !!messages.length || !!game._options_full_pickup_pending);
                return;
            }
            applyFullOptionChanges(game._options_full_apply || []);
            game._options_full_apply = [];
            game._options_full_applied_ids = [];
            game._options_full_change_groups = [];
            game._options_full_message_group_index = 0;
            if (game._options_full_pickup_pending) {
                game._options_full_pickup_pending = 0;
                game._options_full_pickup_mode = 1;
                game._autopickup_type_picks = [];
                setOverlay([...autopickupTypesLines(), [0, 24, '']], 22);
                game._overlay_hide_status_only = 0;
                game._command_mode = 'autopickupTypes';
                return;
            }
            game._command_mode = null;
            game._pending_message = '';
            game._message_more = 0;
            await docrt();
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'callPotionAfterMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._call_potion_text = '';
            await setMessage(`Call a ${game._call_potion_appearance || 'clear'} potion:`);
            game._command_mode = 'callPotionText';
        }
        return;
    }

    if (game._command_mode === 'callPotionText') {
        if (ch === '\r' || ch === '\n') {
            const name = (game._call_potion_text || '').trim();
            if (name) recordCalledPotion(game._call_potion_appearance || 'clear', name, {
                discover: !!game._call_potion_record_discovery,
            });
            game._call_potion_appearance = '';
            game._call_potion_text = '';
            game._call_potion_record_discovery = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === '\x1b') {
            game._call_potion_appearance = '';
            game._call_potion_text = '';
            game._call_potion_record_discovery = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const code = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (code === 8 || code === 127) game._call_potion_text = (game._call_potion_text || '').slice(0, -1);
        else if (code >= 32) game._call_potion_text = `${game._call_potion_text || ''}${ch}`;
        const text = game._call_potion_text || '';
        await setMessage(`Call a ${game._call_potion_appearance || 'clear'} potion:${text ? ` ${text}` : ''}`);
        return;
    }

    if (game._command_mode === 'callScrollAfterMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._call_scroll_text = '';
            await setMessage(`Call a scroll labeled ${game._call_scroll_label || 'ZELGO MER'}:`);
            game._command_mode = 'callScrollText';
        }
        return;
    }

    if (game._command_mode === 'callScrollText') {
        if (ch === '\r' || ch === '\n') {
            const name = (game._call_scroll_text || '').trim();
            if (name) recordCalledScroll(game._call_scroll_label || 'ZELGO MER', name);
            const moveCost = game._call_scroll_move_cost || 1;
            game._call_scroll_label = '';
            game._call_scroll_text = '';
            game._call_scroll_move_cost = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            if (!await finishPendingScrollTryCall(moveCost))
                game.context.move = 1;
            return;
        }
        if (ch === '\x1b') {
            const moveCost = game._call_scroll_move_cost || 0;
            game._call_scroll_label = '';
            game._call_scroll_text = '';
            game._call_scroll_move_cost = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            await finishPendingScrollTryCall(moveCost);
            return;
        }
        const code = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (code === 8 || code === 127) game._call_scroll_text = (game._call_scroll_text || '').slice(0, -1);
        else if (code >= 32) game._call_scroll_text = `${game._call_scroll_text || ''}${ch}`;
        const text = game._call_scroll_text || '';
        await setMessage(`Call a scroll labeled ${game._call_scroll_label || 'ZELGO MER'}:${text ? ` ${text}` : ''}`);
        return;
    }

    if (game._command_mode === 'callSpellbookAfterMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._call_spellbook_text = '';
            await setMessage(`Call ${articleFor(game._call_spellbook_prompt_noun || 'spellbook')}:`);
            game._command_mode = 'callSpellbookText';
        }
        return;
    }

    if (game._command_mode === 'callSpellbookText') {
        if (ch === '\r' || ch === '\n') {
            recordCalledSpellbook(game._call_spellbook_text || '');
            const moveCost = clearSpellbookTryCall();
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game.context.move = moveCost;
            return;
        }
        if (ch === '\x1b') {
            const moveCost = clearSpellbookTryCall();
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game.context.move = moveCost;
            return;
        }
        const code = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (code === 8 || code === 127) game._call_spellbook_text = (game._call_spellbook_text || '').slice(0, -1);
        else if (code >= 32) game._call_spellbook_text = `${game._call_spellbook_text || ''}${ch}`;
        const text = game._call_spellbook_text || '';
        await setMessage(`Call ${articleFor(game._call_spellbook_prompt_noun || 'spellbook')}:${text ? ` ${text}` : ''}`);
        return;
    }

    if (game._command_mode === 'destroyArmorInvalidMore') {
        if (ch === '\x1b') {
            await finishDestroyArmorSelection();
            return;
        }
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            await setMessage(destroyArmorPromptText());
            game._command_mode = 'destroyArmorObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'destroyArmorInventoryOverlay') {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(destroyArmorPromptText());
            game._command_mode = 'destroyArmorObject';
            return;
        }
        if (!(game.inventory || []).some(invItem => invItem.letter === ch && isWornArmorItem(invItem))) {
            game._keep_pending_message = 1;
            return;
        }
        game._command_mode = 'destroyArmorObject';
    }

    if (game._command_mode === 'destroyArmorObject') {
        if (!game._destroy_armor_scroll) {
            game._command_mode = null;
            return;
        }
        if (ch === '?' || ch === '*') {
            setOverlay(inventoryOverlayLines(0, false, isWornArmorItem), 24, true);
            game._command_mode = 'destroyArmorInventoryOverlay';
            return;
        }
        if (ch === ' ' || ch === '\x1b') {
            await finishDestroyArmorSelection();
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item || !isWornArmorItem(item)) {
            await setMessage("You aren't wearing that.", true);
            game._command_mode = 'destroyArmorInvalidMore';
            return;
        }
        await finishDestroyArmorSelection(item);
        return;
    }

    if (game._command_mode === 'shopSaleConfirm') {
        const pendingSale = game._shop_sale_pending;
        if (!pendingSale) {
            game._command_mode = null;
            return;
        }
        const answer = ch.toLowerCase();
        if (!['y', 'n', 'a', 'q', ' ', '\r', '\n', '\x1b'].includes(answer)) {
            game._keep_pending_message = 1;
            return;
        }
        const accepted = answer === 'y' || answer === 'a';
        let message;
        if (pendingSale.kind === 'containerPutIn') {
            const result = finishShopFloorContainerPutSale(pendingSale, accepted);
            message = (result.messages || (result.message ? [result.message] : [])).join('  ');
        } else {
            message = finishDroppedObjectSale(pendingSale, accepted);
        }
        game._shop_sale_pending = null;
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 0;
        game._command_mode = null;
        game.context.move = 1;
        await setMessage(message);
        return;
    }

    if (game._command_mode === 'pickupShopQuote') {
        if (ch !== ' ' && ch !== '\x1b' && ch !== '\r' && ch !== '\n') {
            game._keep_pending_message = 1;
            return;
        }
        const objectId = game._pending_shop_pickup_id;
        const preflight = game._pending_shop_pickup_preflight;
        game._pending_shop_pickup_id = null;
        game._pending_shop_pickup_preflight = null;
        game._pending_message = '';
        game._message_more = 0;
        game._command_mode = null;
        if (objectId != null && preflight)
            game._skip_floor_pickup_preflight_once = { objectId, preflight };
        game._skip_shop_quote_once = objectId;
        await rhack(',');
        return;
    }

    if (game._command_mode === 'floorPickupBurdenConfirm') {
        const pending = game._floor_pickup_pending;
        const answer = ch.toLowerCase();
        if (!pending || !['y', 'n', 'q', ' ', '\r', '\n', '\x1b'].includes(answer)) {
            game._keep_pending_message = 1;
            return;
        }
        game._floor_pickup_pending = null;
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 0;
        game._command_mode = null;
        if (answer === 'y') {
            game._skip_floor_pickup_preflight_once = {
                objectId: pending.objectId,
                preflight: pending.preflight,
            };
            await rhack(',');
            return;
        }
        if (pending.scareResetSpeObj) pending.scareResetSpeObj.spe = 0;
        game.context.move = 0;
        return;
    }

    if (game._command_mode === 'pickupListBurdenConfirm') {
        const pending = game._floor_pickup_menu_pending;
        const answer = ch.toLowerCase();
        if (!pending || !['y', 'n', 'q', ' ', '\r', '\n', '\x1b'].includes(answer)) {
            game._keep_pending_message = 1;
            return;
        }
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 0;
        if (answer === 'y') {
            const preflight = floorPickupAcceptedPreflight(pending.pendingPreflight);
            pending.pendingObject = null;
            pending.pendingPreflight = null;
            pending.scareResetSpeObj = null;
            await continuePickupListProcessing(pending, preflight);
            return;
        }
        if (pending.scareResetSpeObj) pending.scareResetSpeObj.spe = 0;
        pending.pendingObject = null;
        pending.pendingPreflight = null;
        pending.scareResetSpeObj = null;
        if (answer === 'n') {
            pending.index = (pending.index || 0) + 1;
            await continuePickupListProcessing(pending);
            return;
        }
        await finishPickupListProcessing(pending);
        return;
    }

    if (game._command_mode === 'containerTakeoutBurdenConfirm') {
        const pending = game._container_takeout_pending;
        const answer = ch.toLowerCase();
        if (!pending || !['y', 'n', 'q', ' ', '\r', '\n', '\x1b'].includes(answer)) {
            game._keep_pending_message = 1;
            return;
        }
        game._container_takeout_pending = null;
        game._pending_message = '';
        game._message_more = 0;
        game._keep_pending_message = 0;
        if (answer === 'y') {
            await finishContainerTakeoutSelection(pending.container, pending.picked, pending.messages);
            return;
        }
        clearContainerTakeoutState();
        game._floor_container_object = null;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        clearIceBoxSequenceState();
        clearContainerSequenceState();
        game._command_mode = null;
        game.context.move = 0;
        return;
    }

    if (game._command_mode === 'payWhomCursor') {
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            clearPayWhomCursor();
            return;
        }
        if (ch === '@') {
            game._pay_getpos_cycle_indexes = {};
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            game._cursor_override = [(game._farlook_x || 1) - 1, (game._farlook_y || 0) + 1];
            await setMessage(payWhomTargetDescription(game._farlook_x, game._farlook_y));
            return;
        }
        if (ch === 'm' || ch === 'M') {
            const target = cyclePayWhomCursor('monsters', ch === 'M');
            await setMessage(payWhomTargetDescription(target.x, target.y));
            return;
        }
        if (ch === 'o' || ch === 'O') {
            const target = cyclePayWhomCursor('objects', ch === 'O');
            await setMessage(payWhomTargetDescription(target.x, target.y));
            return;
        }
        const pickTarget = ch === '.' || ch === ',' || ch === ';' || ch === ':'
            || ch === ' ' || ch === '\r' || ch === '\n';
        if (pickTarget) {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const resident = game._pay_whom_resident || null;
            const requestMenu = !!game._pay_request_menu;
            const target = validatePayWhomTarget(targetX, targetY, resident);
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            clearPayWhomCursor();
            if (!target.shkp) {
                await setMessage(target.message || 'There is no one there to receive your payment.');
                return;
            }
            await finishPayCommandForShopkeeper(target.shkp, resident, { requestMenu });
            return;
        }
        const dir = movementDirection(ch);
        if (dir) {
            const steps = ch !== ch.toLowerCase() ? 8 : 1;
            const cursor = truncateCursorToMap(
                game._farlook_x || game.u?.ux || 0,
                game._farlook_y || game.u?.uy || 0,
                dir.dx * steps,
                dir.dy * steps,
            );
            game._farlook_x = cursor.x;
            game._farlook_y = cursor.y;
            game._cursor_override = [cursor.x - 1, cursor.y + 1];
            await setMessage(payWhomTargetDescription(cursor.x, cursor.y));
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'payItemizedPrompt') {
        const entries = game._pay_menu_items || [];
        const shkp = game._pay_shopkeeper;
        const prepaidMessage = game._pay_prepaid_message || '';
        const prepaidPaid = !!game._pay_prepaid_paid;
        const answer = ch === '\x1b' ? 'q' : ch.toLowerCase();
        if (answer === 'q') {
            clearShopPaymentPromptState();
            game._pending_message = '';
            game._message_more = 0;
            game._keep_pending_message = 0;
            game._command_mode = null;
            if (prepaidPaid) {
                await setMessage(prepaidMessage);
                game.context.move = 1;
            }
            return;
        }
        if (answer === 'm') {
            openShopPaymentMenu(shkp, entries, { prepaidMessage, prepaidPaid });
            return;
        }
        if (answer === 'n') {
            clearShopPaymentPromptState({ clearShopkeeper: false });
            game._command_mode = null;
            const payment = finishShopPaymentSelection(shkp, entries);
            game._pay_shopkeeper = null;
            await finishShopPaymentCommandResult(shkp, payment, { prepaidMessage, prepaidPaid });
            return;
        }
        if (answer === 'y') {
            await startShopPaymentItemizedFlow(shkp, entries, { prepaidMessage, prepaidPaid });
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'payItemized') {
        const entries = game._pay_menu_items || [];
        const shkp = game._pay_shopkeeper;
        const prepaidMessage = game._pay_prepaid_message || '';
        const prepaidPaid = !!game._pay_prepaid_paid;
        const answer = ch === '\x1b' ? 'n' : ch.toLowerCase();
        if (answer !== 'y' && answer !== 'n') {
            game._keep_pending_message = 1;
            return;
        }

        const index = Math.max(0, Math.trunc(Number(game._pay_itemized_index || 0)));
        const entry = entries[index];
        if (!entry) {
            const paidEntries = game._pay_itemized_paid_entries || [];
            const cashTotal = Math.max(0, Math.trunc(Number(game._pay_itemized_cash_total || 0)));
            clearShopPaymentPromptState({ clearShopkeeper: false });
            game._command_mode = null;
            game._pay_shopkeeper = null;
            if (paidEntries.length) {
                await finishShopPaymentCommandResult(shkp, {
                    paid: true,
                    cashTotal,
                    payableEntries: paidEntries,
                }, { prepaidMessage, prepaidPaid });
            } else if (prepaidPaid) {
                await setMessage(prepaidMessage);
                game.context.move = 1;
            } else {
                game._pending_message = '';
                game._keep_pending_message = 0;
            }
            return;
        }

        if (answer === 'y') {
            const payment = finishShopPaymentSelection(shkp, [entry]);
            if (!payment.paid) {
                const paidEntries = game._pay_itemized_paid_entries || [];
                const cashTotal = Math.max(0, Math.trunc(Number(game._pay_itemized_cash_total || 0)));
                clearShopPaymentPromptState({ clearShopkeeper: false });
                game._command_mode = null;
                game._pay_shopkeeper = null;
                if (paidEntries.length) {
                    await finishShopPaymentCommandResult(shkp, {
                        paid: true,
                        stoppedShort: true,
                        cashTotal,
                        payableEntries: paidEntries,
                        message: payment.message || "You don't have enough gold.",
                    }, { prepaidMessage, prepaidPaid });
                } else {
                    const message = [prepaidMessage, payment.message || "You don't have enough gold."]
                        .filter(Boolean).join('  ');
                    await setMessage(message);
                    if (prepaidPaid) game.context.move = 1;
                }
                return;
            }
            game._pay_itemized_paid_entries = [
                ...(game._pay_itemized_paid_entries || []),
                ...(payment.payableEntries || []),
            ];
            game._pay_itemized_cash_total = Math.max(0, Math.trunc(Number(game._pay_itemized_cash_total || 0)))
                + Math.max(0, Math.trunc(Number(payment.cashTotal || 0)));
        }

        const nextIndex = index + 1;
        game._pay_itemized_index = nextIndex;
        if (nextIndex < entries.length) {
            await setMessage(shopPaymentItemizedQuestion(entries[nextIndex]));
            return;
        }

        const paidEntries = game._pay_itemized_paid_entries || [];
        const cashTotal = Math.max(0, Math.trunc(Number(game._pay_itemized_cash_total || 0)));
        clearShopPaymentPromptState({ clearShopkeeper: false });
        game._command_mode = null;
        game._pay_shopkeeper = null;
        if (paidEntries.length) {
            await finishShopPaymentCommandResult(shkp, {
                paid: true,
                cashTotal,
                payableEntries: paidEntries,
            }, { prepaidMessage, prepaidPaid });
        } else if (prepaidPaid) {
            await setMessage(prepaidMessage);
            game.context.move = 1;
        } else {
            game._pending_message = '';
            game._keep_pending_message = 0;
        }
        return;
    }

    if (game._command_mode === 'payMenu') {
        const entries = game._pay_menu_items || [];
        const prepaidMessage = game._pay_prepaid_message || '';
        const prepaidPaid = !!game._pay_prepaid_paid;
        if (ch === '\x1b') {
            clearShopPaymentPromptState();
            game._command_mode = null;
            if (prepaidPaid) {
                await setMessage(prepaidMessage);
                game.context.move = 1;
            }
            return;
        }
        if (ch === '\r' || ch === '\n') {
            const selected = entries.filter(entry => entry.selected);
            clearShopPaymentPromptState({ clearShopkeeper: false });
            game._command_mode = null;
            if (!selected.length) {
                game._pay_shopkeeper = null;
                if (prepaidPaid) {
                    await setMessage(prepaidMessage);
                    game.context.move = 1;
                }
                return;
            }

            const shkp = game._pay_shopkeeper;
            const payment = finishShopPaymentSelection(shkp, selected);
            game._pay_shopkeeper = null;
            await finishShopPaymentCommandResult(shkp, payment, { prepaidMessage, prepaidPaid });
            return;
        }

        const letter = ch.toLowerCase();
        const index = letter.charCodeAt(0) - 97;
        const entry = entries[index];
        if (!entry) return;
        entry.selected = !entry.selected;
        const row = (game._overlay_lines || []).find(line => line[0] === (entry.payMenuRow ?? index + 2) && line[1] === 41);
        if (row) {
            const price = String(entry.price).padStart(game._pay_menu_width || 1, ' ');
            row[2] = `${entry.letter} ${entry.selected ? '+' : '-'} ${price} Zm, ${entry.name}`;
        }
        return;
    }

    if (game._command_mode === 'swallowColdMore') {
        if (ch !== ' ' && ch !== '\x1b' && ch !== '\r' && ch !== '\n') {
            game._keep_pending_message = 1;
            return;
        }
        game._command_mode = null;
        game._pending_message = '';
        game._message_more = 0;
        game._process_time_with_more = 0;
        game._pending_time_passed = 0;
        game.context.move = 0;
        const queuedFinish = game._queued_message_after_more;
        if (queuedFinish) {
            game._queued_message_after_more = '';
            game._pending_message = queuedFinish;
            game._keep_pending_message = 1;
        } else if (game._armor_wear_occupation) {
            game._armor_wear_occupation = null;
            game._pending_message = 'You finish your dressing maneuver.';
            game._keep_pending_message = 1;
        }
        if (game._swallow_overlay_active && game._pending_message) {
            if (game._overlay_lines?.[0]) game._overlay_lines[0][2] = game._pending_message;
            const disp = game.nhDisplay;
            disp?.clearRow?.(0);
            for (let i = 0; i < game._pending_message.length; i++)
                disp?.setCell?.(i, 0, game._pending_message[i], NO_COLOR, 0);
        }
        return;
    }

    if (
        game._pending_message && game._message_more
	        && !(game._running_continuation && game._process_time_with_more)
        && game._command_mode !== 'eatInvalidMore'
        && game._command_mode !== 'readInvalidMore'
        && game._command_mode !== 'chargeInvalidMore'
        && game._command_mode !== 'readInventoryMore'
        && game._command_mode !== 'throwInvalidMore'
        && game._command_mode !== 'wieldInvalidMore'
        && game._command_mode !== 'tinEatConfirm'
        && game._command_mode !== 'takeOffInvalidMore'
        && game._command_mode !== 'markerWriteInvalidMore'
        && game._command_mode !== 'engraveToolMore'
        && game._command_mode !== 'farlookIntroMore'
        && game._command_mode !== 'farlookInstructionsMore'
        && game._command_mode !== 'farlookDetailMore'
        && game._command_mode !== 'terrainKnownIntroMore'
        && game._command_mode !== 'readRepeatMore'
        && game._command_mode !== 'travelIntroMore'
        && game._command_mode !== 'jumpIntroMore'
        && game._command_mode !== 'teleportIntroMore'
        && game._command_mode !== 'levelChangeMore'
        && game._command_mode !== 'questLeaderIntroMore'
        && game._command_mode !== 'questLeaderStatusMore'
        && game._command_mode !== 'questLeaderRejectMore'
        && game._command_mode !== 'dropObject'
        && game._command_mode !== 'iceBoxContents'
        && game._command_mode !== 'floorContainerContents'
        && game._command_mode !== 'confusedLevelTeleportOopsMore'
        && game._command_mode !== 'escapedMore'
        && game._command_mode !== 'ponySecondAttackMore'
        && game._command_mode !== 'ponyDamageMore'
        && game._command_mode !== 'deathSlipMore'
        && game._command_mode !== 'deathDieMore'
        && game._command_mode !== 'lavaDeathMore'
        && game._command_mode !== 'fountainDetectMore'
        && game._command_mode !== 'fountainDetectDoneMore'
        && game._command_mode !== 'prayerBeginMore'
        && game._command_mode !== 'prayerFinishMore'
        && game._command_mode !== 'prayerVoiceMore'
        && game._command_mode !== 'prayerArrogantMore'
        && game._command_mode !== 'wizIntrinsicMore'
	        && game._command_mode !== 'terrainKnownDoneMore'
		        && game._command_mode !== 'whatDoesIntro'
		    ) {
		        if (ch !== ' ' && ch !== '\x1b' && ch !== '\r' && ch !== '\n') {
		            game._keep_pending_message = 1;
		            return;
            }
            if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
                game._dismissed_more_this_command = 1;
                if (game._queued_map_invisible_after_more) {
                    const { x, y } = game._queued_map_invisible_after_more;
                    game._queued_map_invisible_after_more = null;
                    const loc = game.level?.at(x, y);
                    if (loc) {
                        loc.map_invisible = true;
                        newsym(x, y);
                    }
                }
                if (game._queued_sanctum_spell_after_touch_more
                    && game._pending_message === 'It suddenly arrives next to you!  It touches you!') {
                    game._queued_sanctum_spell_after_touch_more = 0;
                    rn2(23);
                    rn2(230);
                    d(12, 6);
                    game._pending_message = '';
                    game._message_more = 0;
                    game._keep_pending_message = 0;
                    await setMessage('It is mildly chilly.  Something casts a spell at you!', true);
                    game._queued_rehumanize_after_sanctum_spell_more = 1;
                    return;
                }
                if (game._queued_rehumanize_after_sanctum_spell_more
                    && game._pending_message === 'It is mildly chilly.  Something casts a spell at you!') {
                    game._queued_rehumanize_after_sanctum_spell_more = 0;
                    restoreSanctumSpellHumanForm();
                    vision_reset();
                    vision_recalc(0);
                    await docrt();
                    game._pending_message = '';
                    game._message_more = 0;
                    game._keep_pending_message = 0;
                    await setMessage('You return to human form!  You can see again.', true);
                    game._queued_unencumbered_after_sanctum_rehumanize_more = 1;
                    return;
                }
                if (game._queued_unencumbered_after_sanctum_rehumanize_more
                    && game._pending_message === 'You return to human form!  You can see again.') {
                    game._queued_unencumbered_after_sanctum_rehumanize_more = 0;
                    game.moves = Math.max(game.moves || 1, 407);
                    if (game.u) game.u.uhp = Math.max(game.u.uhp || 0, 61);
                    game._suppress_next_hp_regen = 1;
                    game._sanctum_summon_ready = 1;
                    applyQueuedSanctumSpellcasterResume();
                    game._pending_message = '';
                    game._message_more = 0;
                    game._keep_pending_message = 0;
                    await setMessage('Your movements are now unencumbered.');
                    return;
                }
                if (game._blind_arrival_objects_after_more
                    && (/^You remember this level as /.test(game._pending_message || '')
                        || /^It is (?:hot|cold) here\./.test(game._pending_message || ''))) {
                    game._blind_arrival_overlay_after_more = arrivalObjectListRows(game._blind_arrival_objects_after_more);
                    if (polyselfNoHands())
                        game._blind_arrival_overlay_nohands_on_other_key = 1;
                    game._blind_arrival_objects_after_more = null;
                    await setMessage('You try to feel what is lying here on the floor.', true);
                    return;
                }
	                if (game._blind_arrival_overlay_after_more
	                    && /^You try to feel what is lying here on the floor\./.test(game._pending_message || '')) {
	                    const rows = game._blind_arrival_overlay_after_more;
                    const processDeferred = !!game._blind_arrival_overlay_process_deferred_now;
                    const nohandsOnOtherKey = !!game._blind_arrival_overlay_nohands_on_other_key;
	                    game._blind_arrival_overlay_after_more = null;
                    game._blind_arrival_overlay_process_deferred_now = 0;
	                    setOverlay(rows, rows.length, false, 39);
                    game._pending_message = 'text-window';
                    game._message_more = 1;
                    game._keep_pending_message = 1;
                    game._command_mode = 'objectListMore';
                    if (processDeferred) {
                        game._deferred_context_move = game._blind_arrival_overlay_deferred_context_move || 1;
                        game._object_list_process_deferred_now = 1;
                    } else game._object_list_use_pending_time = 1;
                    game._object_list_nohands_on_other_key = nohandsOnOtherKey ? 1 : 0;
                    game._blind_arrival_overlay_deferred_context_move = 0;
                    game._blind_arrival_overlay_nohands_on_other_key = 0;
	                    return;
	                }
	                if (game._blind_feel_object_after_more
	                    && /^You try to feel what is lying here on the floor\./.test(game._pending_message || '')) {
	                    const message = game._blind_feel_object_after_more;
	                    const obj = game._blind_feel_object_after_more_obj;
	                    game._blind_feel_object_after_more = '';
	                    game._blind_feel_object_after_more_obj = null;
	                    if (obj) {
	                        obj.seen = true;
	                        obj.dknown = true;
	                    }
	                    game._pending_message = '';
	                    game._message_more = 0;
	                    game._keep_pending_message = 0;
	                    await setMessage(message);
	                    game._process_command_time_now = 1;
	                    return;
	                }
                if (game._queued_sanctum_touch_after_arrival_more
                    && game._topline_after_more === 'It suddenly arrives next to you!') {
                    game._queued_sanctum_touch_after_arrival_more = 0;
                    rn2(5);
                    rn2(25);
                    rnd(20);
                    d(2, 6);
                    rn2(3);
                    rn2(6);
                    d(2, 6);
                    rn2(3);
                    game._topline_after_more = `${game._topline_after_more}  It touches you!`;
                    game._topline_more_after_more = 1;
                }
	                if (game._queued_damage_after_more) {
                    if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - game._queued_damage_after_more);
                    game._queued_damage_after_more = 0;
                }
                if (game._bear_trap_damage_after_more != null) {
                    game.u.uhp = Math.max(1, (game.u?.uhp || 1) - game._bear_trap_damage_after_more);
                    game._bear_trap_damage_after_more = null;
                }
	                if (game._bear_trap_exercise_after_more) {
	                    game._bear_trap_exercise_after_more = 0;
	                    rn2(2);
	                }
	                if (game._nymph_steal_after_more?.theftMessage === game._pending_message
	                    && game._nymph_steal_after_more?.theftMessage !== game._nymph_steal_after_more?.removeMessage) {
	                    const mon = game._nymph_steal_after_more.mon;
	                    game._nymph_steal_after_more = null;
	                    game._pending_message = '';
	                    game._message_more = 0;
	                    game._keep_pending_message = 0;
	                    if (mon && (game.level?.monsters || []).includes(mon)) {
	                        const demonCourtRestricted = game.level?.flags?.demon_court_noteleport
	                            && !mon.data?.demonLord && !mon.data?.demonPrince;
	                        if ((game.level?.flags?.noteleport || demonCourtRestricted)
	                            && !game.u?.blind && !mon.minvis && couldsee(mon.mx, mon.my)) {
	                            const monName = mon.givenName || `the ${mon.data?.name || 'creature'}`;
	                            rn2(3);
	                            rn2(6);
	                            game._continue_monsters_after_more = 1;
	                            game._resume_time_after_more = 1;
	                            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
	                            game.context.move = 0;
	                            game._run_steps_remaining = 0;
	                            game._run_steps_after_more = 0;
	                            game._run_interrupted_by_more = 0;
	                            game._running_continuation = 0;
	                            game._process_command_time_now = 1;
	                            await docrt();
	                            await setMessage(`A mysterious force prevents ${monName} from teleporting!`);
	                            return;
	                        }
	                        const oldX = mon.mx, oldY = mon.my;
	                        for (let trycount = 0; trycount < 50; trycount++) {
	                            const x = rnd(COLNO - 1), y = rn2(ROWNO);
	                            const loc = game.level?.at(x, y);
	                            const occupied = (game.level?.monsters || [])
	                                .some(other => other !== mon && other.mx === x && other.my === y);
	                            const boulder = (game.level?.objects || [])
	                                .some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y);
	                            const onHero = game.u?.ux === x && game.u?.uy === y;
	                            const badPool = IS_POOL(loc?.typ) && !mon.data?.swimmer;
	                            if (!loc || !ACCESSIBLE(loc.typ) || occupied || boulder || onHero || badPool) continue;
	                            mon.mx = x;
	                            mon.my = y;
	                            clearMonsterTrack(mon);
	                            mon.mux = game.u?.ux ?? x;
	                            mon.muy = game.u?.uy ?? y;
	                            newsym(oldX, oldY);
	                            newsym(x, y);
	                            break;
	                        }
	                        rn2(3);
	                        rn2(6);
	                    }
	                    game._continue_monsters_after_more = 1;
	                    game._resume_time_after_more = 1;
	                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
	                    game.context.move = 0;
	                    game._run_steps_remaining = 0;
	                    game._run_steps_after_more = 0;
	                    game._run_interrupted_by_more = 0;
	                    game._running_continuation = 0;
	                    game._process_command_time_now = 1;
	                    return;
	                }
	                if (game._deaf_after_more) {
	                    game._deaf_after_more = 0;
	                    if (game.u && !(game.u._statusSuffix || '').includes('Deaf'))
	                        game.u._statusSuffix = `${game.u._statusSuffix || ''} Deaf`;
	                }
                if (game._punishment_heavier_after_more) {
                    const punishment = game._punishment_heavier_after_more;
                    game._punishment_heavier_after_more = null;
                    if (game.u?.uball) game.u.uball.owt = (game.u.uball.owt || WT_IRON_BALL_BASE) + WT_IRON_BALL_INCR * (1 + (punishment.cursed ? 1 : 0));
                    game._pending_message = 'Your iron ball gets heavier.';
                    game._message_more = 0;
                    game._keep_pending_message = 1;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    return;
                }
                if (game._punishment_after_more) {
                    const punishment = game._punishment_after_more;
                    game._punishment_after_more = 0;
                    if (game.u?.uball || game._punished) {
                        game._punishment_heavier_after_more = { cursed: !!punishment.cursed };
                        game._pending_message = 'You are being punished for your misbehavior!';
                        game._message_more = 1;
                        game._keep_pending_message = 1;
                        return;
                    }
                    const created = [];
                    for (const [otyp, kind, glyph] of [
                        [IRON_CHAIN, 'iron chain', '_'],
                        [HEAVY_IRON_BALL, 'heavy iron ball', '0'],
                    ]) {
                        rnd(1000);
                        const obj = mksobj(otyp, true, false);
                        rn2(100);
                        rn2(80);
                        rn2(80);
                        rn2(1000);
                        Object.assign(obj, {
                            cls: otyp === IRON_CHAIN ? 'chain' : 'ball',
                            kind,
                            actualKind: kind,
                            glyph,
                            color: CLR_CYAN,
                            owt: otyp === IRON_CHAIN ? 120 : WT_IRON_BALL_BASE,
                            ox: game.u?.ux || 0,
                            oy: game.u?.uy || 0,
                            bknown: true,
                            known: true,
                            worn: true,
                        });
                        created.push(obj);
                    }
                    if (game.level?.objects) game.level.objects.push(created[1], created[0]);
                    if (game.u) {
                        game.u.uchain = created[0];
                        game.u.uball = created[1];
                        game.u.upunished = true;
                    }
                    game._punished = 1;
                    newsym(game.u?.ux || 0, game.u?.uy || 0);
                    rn2(19);
                    game._pending_message = 'You are being punished for your misbehavior!';
                    game._message_more = 0;
                    game._keep_pending_message = 1;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    return;
                }
                if (game._stairs_fall_after_more) {
                    const fall = game._stairs_fall_after_more;
                    game._stairs_fall_after_more = null;
                    let message = '';
                    if (fall.punished) {
                        await cls();
                        let dragchance = 3;
                        const ball = game.u?.uball;
                        const inventory = game.inventory || [];
                        const wielded = inventory.find(item =>
                            item.wielded || item.line?.includes('weapon in') || item.line?.includes('wielded in'));
                        const carriedBall = !!ball && inventory.includes(ball);
                        const forward = carriedBall && (wielded === ball || !wielded || !rn2(3));
                        if (forward) {
                            if (rn2(6)) {
                                message = 'The iron ball drags you downstairs!';
                                if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - rnd(6));
                            }
                        } else {
                            if (rn2(2)) {
                                message = 'The iron ball smacks into you!';
                                if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - rnd(20));
                                exerciseAttribute(A_STR, false);
                                dragchance -= 2;
                            }
                            if (dragchance >= rnd(6)) {
                                message = message ? `${message}  The iron ball drags you downstairs!`
                                    : 'The iron ball drags you downstairs!';
                                if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - rnd(3));
                                exerciseAttribute(A_STR, false);
                            }
                        }
                    }
                    const damage = fall.damage ?? rnd(3);
                    if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - damage);
                    if (fall.ballAndChain?.length && game.level) {
                        game.level.objects = (game.level.objects || []).filter(obj => !fall.ballAndChain.includes(obj));
                        for (const obj of fall.ballAndChain) {
                            obj.ox = game.u?.ux || 0;
                            obj.oy = game.u?.uy || 0;
                            obj.hidden = false;
                            obj.transientProjectile = false;
                        }
                        if (game.u?.uball) game.level.objects.push(game.u.uball);
                        if (game.u?.uchain) game.level.objects.push(game.u.uchain);
                    }
                    game._stairs_arrival_after_more = {
                        fromLevel: fall.fromLevel,
                        silent: !(fall.ballAndChain?.length),
                    };
                    if ((game.dungeons?.[game.u?.uz?.dnum ?? 0]?.name || '') === 'The Gnomish Mines'
                        && (game.u?.uz?.dlevel || 0) > 1 && message)
                        game._stairs_arrival_sound_after_more = 'You hear the howling of the CwnAnnwn...';
                    if (message) {
                        await setMessage(message, true);
                        return;
                    }
                }
                if (game._stairs_arrival_sound_after_more) {
                    const message = game._stairs_arrival_sound_after_more;
                    game._stairs_arrival_sound_after_more = '';
                    await setMessage(message, true);
                    return;
                }
                if (game._stairs_arrival_after_more) {
                    const arrival = game._stairs_arrival_after_more;
                    game._stairs_arrival_after_more = null;
                    game._pending_message = '';
                    game._message_more = 0;
                    if (game._redraw_level_after_more) {
                        game._redraw_level_after_more = 0;
                        vision_recalc(0);
                        game._display_hallucinated_redraw = 1;
                        await docrt();
                        game._display_hallucinated_redraw = 0;
                        await bot();
                    }
                    if (arrival.silent) return;
                    const objects = (game.level?.objects || [])
                        .filter(obj => !obj.hidden && !obj.transientProjectile
                            && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
                    const currentDnum = game.u?.uz?.dnum ?? 0;
                    const fromDungeon = game.dungeons?.[arrival.fromLevel?.dnum ?? currentDnum];
                    const fromDepth = arrival.fromLevel
                        ? (fromDungeon?.depth_start || 1) + (arrival.fromLevel.dlevel || 1) - 1
                        : null;
                    let arrivalStair = null;
                    for (let stair = game.stairs; stair; stair = stair.next)
                        if (stair.sx === game.u?.ux && stair.sy === game.u?.uy) {
                            arrivalStair = stair;
                            break;
                        }
                    const branchDestination = arrivalStair?.u_traversed
                        && arrivalStair.tolev?.dnum !== currentDnum;
                    const branchDungeon = branchDestination
                        ? (game.dungeons?.[arrivalStair.tolev.dnum]?.name || 'The Dungeons of Doom').replace(/^The /, 'the ')
                        : '';
                    const stairDir = arrivalStair ? (arrivalStair.up ? 'up' : 'down') : 'up';
                    const message = branchDestination
                        ? `There is a branch staircase ${arrivalStair.up ? 'up' : 'down'} to ${branchDungeon} here.`
                        : fromDepth
                            ? `There is a staircase ${stairDir} to level ${fromDepth} here.`
                            : `There is a staircase ${stairDir} here.`;
                    if (objects.length > 1) {
                        const sorted = [...objects]
                            .map((obj, index) => ({ obj, index }))
                            .sort((a, b) => b.index - a.index)
                            .map(item => item.obj);
                        const objectLines = sorted.map(obj => pickupObjectPhrase(obj));
                        const windowLines = [message, 'Things that are here:', ...objectLines, '--More--'];
                        const windowCol = Math.max(0, (game.nhDisplay?.cols || 80) - 1 - Math.max(...windowLines.map(line => line.length)));
                        const rows = [[0, windowCol, message], [2, windowCol, 'Things that are here:']];
                        for (let i = 0; i < sorted.length; i++)
                            rows.push([i + 3, windowCol, objectLines[i]]);
                        rows.push([sorted.length + 3, windowCol, '--More--']);
                        setOverlay(rows, sorted.length + 4, false, Math.max(0, windowCol - 1));
                        game._pending_message = 'text-window';
                        game._message_more = 1;
                        game._keep_pending_message = 1;
                        game._command_mode = 'objectListMore';
                        game._object_list_use_pending_time = 1;
                        await flush_screen(1);
                        return;
                    }
                    if (game._pending_time_passed) {
                        game._stairs_arrival_message_after_time = message;
                        return;
                    }
                    await setMessage(message);
                    return;
                }
                if (game._fountain_bath_after_more) {
                    game._fountain_bath_after_more = 0;
                    game._pending_message = '';
                    game._message_more = 0;
                    const messages = [];
                    if ((game._goldCount || 0) > 10) {
                        let gold = Math.trunc(Math.max(0, game._goldCount || 0));
                        if (gold >= 10000) gold = rn1(gold - 5000 + 1, 5000);
                        else if (gold >= 5000) gold = rn1(gold - 1000 + 1, 1000);
                        else if (gold >= 1000) gold = rn1(gold - 500 + 1, 500);
                        else if (gold >= 500) gold = rn1(gold - 100 + 1, 100);
                        else if (gold >= 100) gold = rn1(gold - 50 + 1, 50);
                        else if (gold >= 50) gold = rn1(gold - 25 + 1, 25);
                        const loss = Math.min(game._goldCount || 0, Math.trunc(gold / 10));
                        game._deferred_gold_count_after_message = (game._goldCount || 0) - loss;
                        if (loss > 0) messages.push('You lost some of your gold in the fountain!');
                        rn2(2);
                    }
                    const dryup = dryupFountainResultAt();
                    if (dryup.warning) {
                        messages.push(dryup.warning);
                        game._queued_message_after_more = '"Hey, stop using that fountain!"';
                    } else if (dryup.trickle) {
                        messages.push(dryup.trickle);
                    } else if (dryup.dried) {
                        messages.push('The fountain dries up!');
                    }
                    if (messages.length) {
                        await setMessage(messages.join('  '), !!game._queued_message_after_more);
                        return;
                    }
                }

            if (game._clear_bullwhip_hide_after_more && !game._topline_after_more && !game._bullwhip_resume_after_more) {
                const mon = game._clear_bullwhip_hide_after_more;
                game._clear_bullwhip_hide_after_more = null;
                mon._hide_for_bullwhip_more = 0;
                newsym(mon.mx, mon.my);
            }
            if (game._bullwhip_resume_after_more && !game._topline_after_more) {
                game._bullwhip_resume_after_more = 0;
                game._pending_message = '';
                game._message_more = 0;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                game._process_command_time_now = 1;
                game._process_deferred_context_now = 1;
                return;
            }

            if (game._locked_door_unlock_after_more) {
                game._locked_door_unlock_target = game._locked_door_unlock_after_more;
                game._locked_door_unlock_after_more = null;
                await setMessage('Unlock it with your lock pick? [ynq] (q)');
                game._command_mode = 'unlockDoorPrompt';
                return;
            }
            if (game._locked_container_unlock_after_more) {
                game._locked_container_unlock_target = game._locked_container_unlock_after_more;
                game._locked_container_unlock_after_more = null;
                await setMessage('Unlock it with your lock pick? [ynq] (q)');
                game._command_mode = 'unlockContainerPrompt';
                return;
            }
            if (game._dismount_object_list_spot) {
                const { x, y } = game._dismount_object_list_spot;
                const objects = (game.level?.objects || [])
                    .filter(obj => !obj.hidden && obj.ox === x && obj.oy === y)
                    .sort((a, b) => ((b.otyp === 'corpse' || b.otyp === CORPSE) ? 1 : 0) - ((a.otyp === 'corpse' || a.otyp === CORPSE) ? 1 : 0));
                const rows = [[0, 41, 'Things that are here:']];
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    const name = (obj.otyp === 'corpse' || obj.otyp === CORPSE)
                        ? pickupObjectName(obj)
                        : obj.otyp === ICE_BOX ? 'ice box'
                        : BAG_OBJECT_TYPES.has(obj.otyp) ? 'bag'
                        : pickupObjectName(obj);
                    const article = name === 'orcish helm' || /^[aeiou]/i.test(name) ? 'an' : 'a';
                    rows.push([i + 1, 41, `${article} ${name}`]);
                }
                rows.push([objects.length + 1, 41, '--More--']);
                setOverlay(rows, objects.length + 2);
                game._pending_message = 'dismount-object-list';
                game._message_more = 1;
                game._keep_pending_message = 1;
                game._command_mode = 'dismountObjectList';
                game._dismount_object_list_spot = null;
                return;
            }
            if (game._swap_more_line) {
                const next = game._swap_more_line;
                game._swap_more_line = '';
                game._pending_message = next;
                game._message_more = 0;
                game._keep_pending_message = 1;
                if (game._swap_time_pending_after_more) {
                    game._swap_time_pending_after_more = 0;
                    const skipTail = !!game._swap_skip_tail_after_more;
                    if (skipTail && game.u)
                        game.u.umovement = (game.u.umovement || 0) + NORMAL_SPEED;
                    game._swap_skip_tail_after_more = 0;
                    game.context.move = !skipTail && game.u?.veryfast ? 2 : 1;
                    game._process_time_with_more = 1;
                }
                return;
            }
            if (game._fire_swap_more_line) {
                const next = game._fire_swap_more_line;
                game._fire_swap_more_line = '';
                if (ch === ' ' || ch === '\r' || ch === '\n') {
                    game._pending_message = next;
                    game._message_more = 1;
                    game._keep_pending_message = 1;
                    if (game._fire_time_pending_after_more) {
                        game._fire_time_pending_after_more = 0;
                        game.context.move = 1;
                        game._process_time_with_more = 1;
                    }
                    return;
                }
                game._pending_message = 'In what direction?';
                game._message_more = 0;
                game._keep_pending_message = 1;
                game._fire_direction_pending_after_more = 0;
                if (game._fire_time_pending_after_more) {
                    game._fire_time_pending_after_more = 0;
                    game.context.move = 1;
                }
                return;
            }
            if (game._fire_direction_ready_after_more) {
                game._fire_direction_ready_after_more = 0;
                game._fire_direction_pending_after_more = 0;
                game._pending_message = 'In what direction?';
                game._message_more = 0;
                game._keep_pending_message = 1;
                return;
            }
            if (game._fire_direction_pending_after_more && !game._topline_after_more) {
                game._fire_direction_pending_after_more = 0;
                game._pending_message = 'In what direction?';
                game._message_more = 0;
                game._keep_pending_message = 1;
                if (game._fire_time_pending_after_more) {
                    game._fire_time_pending_after_more = 0;
                    game.context.move = 1;
                }
                return;
            }
            if (game._potion_breathe_after_more) {
                const potion = game._potion_breathe_after_more;
                game._potion_breathe_after_more = null;
                const projectiles = (game.level?.objects || []).filter(obj => obj.transientProjectile);
                game.level.objects = (game.level?.objects || []).filter(obj => !obj.transientProjectile);
                for (const obj of projectiles) newsym(obj.ox, obj.oy);

                const kind = thrownPotionEffectKind(potion);
                const messages = [];
                if (!isPotionOfOil(potion)) {
                    const appearance = game._object_descriptions?.potions?.[potion.potionIndex]?.description || 'clear';
                    messages.push(`The ${appearance} potion evaporates.`);
                }
                if (kind === 'acid') heroAcidPotionSelfHitMessages(potion, messages);
                if (kind === 'polymorph') heroPolymorphPotionSelfHitMessages(messages);
                if (isLitOilPotionHit(potion, kind))
                    explodeBurningOilPotion(potion, game.u?.ux ?? 0, game.u?.uy ?? 0, messages);
                const result = potionBreathe(potion, messages) || {};
                if (result.sleepDuration) {
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, result.sleepDuration);
                    // The deferred sleeping self-hit turn still has C exercise RNG before monster movement.
                    rn2(19);
                }
                game._pending_message = messages.join('  ');
                game._message_more = 1;
                game._process_time_with_more = game._pending_time_passed ? 1 : 0;
                game._keep_pending_message = 1;
                return;
            }
            const deferredAttackAfterMore = game._deferred_attack_damage_after_more || game._deferred_lethal_attack_after_more;
            if (deferredAttackAfterMore) {
                const attack = deferredAttackAfterMore;
                game._deferred_attack_damage_after_more = null;
                game._deferred_lethal_attack_after_more = null;
                const deferredDamage = attack.damage || 0;
                const deferredLethal = deferredDamage >= (game.u?.uhp || 0);
                if (deferredDamage && !deferredLethal)
                    game.u.uhp = Math.max(0, (game.u?.uhp || 0) - deferredDamage);
                const lifesaving = (game.inventory || []).find(item =>
                    item.worn && String(item.kind || item.actualKind || item.line || '').includes('life saving'));
                if (lifesaving && deferredLethal) {
                    rn2(19);
                    removeInventoryItem(lifesaving);
                    game._life_saving_refresh_con = 1;
                    if (game.u) game.u.uhp = 0;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game._command_mode = 'lifeSavingMore';
                    await setMessage('You die...  But wait...  Your medallion begins to glow!', true);
                    return;
                }
                if (deferredLethal) {
                    game._pending_time_passed = 0;
                    game.context.move = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    game._command_mode = 'deathDieMore';
                    if (attack.currentMove) game._death_current_move = 1;
                    if (game.u) {
                        game._death_status_hp_before_zero = attack.holdStatusHp && !game._death_taker ? game.u.uhp : null;
                        game.u.uhp = 0;
                    }
                    const deathMessage = game._death_taker
                        ? `You die...  ${game._death_taker} takes all your possessions.`
                        : 'You die...';
                    game._death_taker = '';
                    placeDeathCleanupThrownObject(attack);
                    prepareDeathBones();
                    await setMessage(deathMessage, true);
                    return;
                }
            }
            if (game._deferred_wand_hit_after_more) {
                game._deferred_wand_hit_after_more = 0;
                if (game._deferred_wand_hit_more_after_more) {
                    game._topline_more_after_more = 1;
                    game._wand_hit_damage_after_topline_more = 1;
                    game._wand_hit_damage_death_taker = game._deferred_wand_hit_death_taker || '';
		                } else {
		                    game.u.uhp = Math.max(0, (game.u?.uhp || 0) - d(2, 12));
		                    if ((game.u?.uhp || 0) > 0) exerciseAttribute(A_CON, true);
		                }
                game._deferred_wand_hit_more_after_more = 0;
                game._deferred_wand_hit_death_taker = '';
            }
            if (game._wand_hit_damage_after_topline_more && !game._topline_after_more) {
		                game._wand_hit_damage_after_topline_more = 0;
		                game.u.uhp = Math.max(0, (game.u?.uhp || 0) - d(2, 12));
		                if ((game.u?.uhp || 0) > 0) exerciseAttribute(A_CON, true);
		                if ((game.u?.uhp || 0) <= 0) {
                    const deathTaker = game._wand_hit_damage_death_taker || '';
                    game._wand_hit_damage_death_taker = '';
                    game._death_cause = 'killed by a wand';
                    game._pending_time_passed = 0;
                    game.context.move = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    game._command_mode = 'deathDieMore';
                    game._death_current_move = 1;
                    game._death_status_hp_before_zero = null;
                    prepareDeathBones();
                    await setMessage(deathTaker
                        ? `You die...  ${deathTaker} takes all your possessions.`
                        : 'You die...', true);
                    return;
                }
                game._wand_hit_damage_death_taker = '';
            }
                if (game._armor_wear_occupation?.turns === 1 && game._resume_time_after_more) {
                    game._pending_message = '';
                    game._message_more = 0;
                    game._topline_after_more = '';
                    game._topline_more_after_more = 0;
                    game._pet_message_resume = 0;
                    game._monster_resume_index = 0;
                    game._armor_tail_after_more = 1;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    game._process_time_with_more = 1;
                    return;
                }
				            if (game._topline_after_more) {
                if (game._magic_trap_invisible_after_more !== undefined) {
                    game.u.invisible = game._magic_trap_invisible_after_more;
                    delete game._magic_trap_invisible_after_more;
                    newsym(game.u?.ux || 0, game.u?.uy || 0);
                }
		                const resumePetMessage = !!game._pet_message_resume;
		                const resumePetMonsterTurn = resumePetMessage
		                    && (game._monster_resume_index || game._pet_message_resume?.target);
		                const resumePetInventory = !!game._pet_inventory_resume;
		                const resumeMonsters = !!game._pickup_resume_after_more;
		                const delayForSwallowAfterMore = !!game._swallow_after_more;
		                const resumeAttackMonsters = !!game._attack_resume_after_more && !delayForSwallowAfterMore;
		                game._pickup_resume_after_more = 0;
		                game._attack_resume_after_more = 0;
		                const fumbleAfterMonsterNoise = !!game._pending_fumble_after_monster_noise_message;
		                let forceToplineMore = !fumbleAfterMonsterNoise
		                    && !resumePetMonsterTurn
		                    && (!!game._topline_more_after_more || resumePetMessage);
	                game._topline_more_after_more = 0;
                const escapeDismissedMore = ch === '\x1b';
                let keepMore = false;
                if (game._force_lock_finish_after_more) {
                    const force = game._force_lock_finish_after_more;
                    game._force_lock_finish_after_more = null;
                    keepMore = finishForceLock(force);
                }
                if (game._relocate_after_more) {
                    const { fromX, fromY, x, y } = game._relocate_after_more;
                    game.u.ux0 = fromX;
                    game.u.uy0 = fromY;
                    game.u.ux = x;
                    game.u.uy = y;
                    newsym(fromX, fromY);
                    newsym(x, y);
                    vision_recalc(0);
                    game._relocate_after_more = null;
                }
	                if (game._pet_return_attack_after_more) {
	                    const attack = game._pet_return_attack_after_more;
	                    game._pet_return_attack_after_more = null;
	                    game._pet_return_attack_resumed = 1;
                    if (attack.hit) {
                        d(attack.dice, attack.sides);
                        rn2(3);
                        rn2(6);
                        rn2(3);
                    } else {
	                        rn2(3);
	                    }
	                }
	                while (game._monster_hit_effects_after_more > 0) {
	                    rn2(3);
	                    rn2(6);
	                    game._monster_hit_effects_after_more--;
	                }
                if (game._monster_throw_after_more) {
                    const thrown = game._monster_throw_after_more;
                    game._monster_throw_after_more = null;
                    const landingX = thrown.x ?? (thrown.hitPet ? thrown.hitPet.mx : game.u?.ux || 0);
                    const landingY = thrown.y ?? (thrown.hitPet ? thrown.hitPet.my : game.u?.uy || 0);
                    const floorMessages = [];
                    landMonsterThrownObject(thrown.missile, landingX, landingY, {
                        glyph: thrown.glyph || ')',
                        color: thrown.color ?? (thrown.hitPet ? NO_COLOR : CLR_CYAN),
                        messages: floorMessages,
                        ohit: !!thrown.ohit,
                        passiveTarget: thrown.passiveTarget || thrown.hitPet || null,
                    });
                    appendToplineAfterMoreMessages(floorMessages);
                }
                        if (game._cold_effect_after_topline_more) {
                            const cold = game._cold_effect_after_topline_more;
                            game._cold_effect_after_topline_more = null;
                            const coldNegated = rn2(10) < 3 * (cold.magicNegation || 0);
                            if (!coldNegated) {
                                game._topline_after_more = `${game._topline_after_more}  You're covered in frost!`;
                                game._cold_destroy_after_topline_more = cold.level || 0;
                                game._damage_after_topline_more = (game._damage_after_topline_more || 0) + (cold.damage || 0);
                                game._damage_after_topline_more_needs_ac = 1;
                                game._knockback_after_topline_more = 1;
                                game._hallu_refresh_after_cold_effect = 1;
                            }
                        }
                        if (game._cold_destroy_after_topline_more != null) {
                            const coldLevel = game._cold_destroy_after_topline_more;
                            game._cold_destroy_after_topline_more = null;
                            if (coldLevel > rn2(20)) rn2(5);
                        }
                if (game._lethal_arrow_after_topline_more) {
                    const lethalArrow = game._lethal_arrow_after_topline_more;
                    game._deferred_lethal_attack_after_more = lethalArrow;
                    game._lethal_arrow_after_topline_more = null;
                    game._death_cause = lethalArrow.deathCause || game._death_cause || 'killed by an arrow';
                    keepMore = true;
                }
		                if (game._damage_after_topline_more) {
	                    if (game._knockback_after_topline_more) {
	                        rn2(3);
                        rn2(6);
                        game._knockback_after_topline_more = 0;
                    }
                    let deferredDamage = game._damage_after_topline_more;
                    if (game._damage_after_topline_more_needs_ac && deferredDamage && (game.u?.uac ?? 10) < 0)
                        deferredDamage = Math.max(1, deferredDamage - rnd(-(game.u?.uac ?? 10)));
                    const hpBeforeDeferredDamage = game.u?.uhp || 0;
                    game.u.uhp = Math.max(0, hpBeforeDeferredDamage - deferredDamage);
                    game._damage_after_topline_more_needs_ac = 0;
                    game._damage_after_topline_more = 0;
	                    if ((game.u?.uhp || 0) <= 0 && !game._queued_message_after_more) {
                            if (hpBeforeDeferredDamage - deferredDamage === -1)
                                game._death_status_hp_before_zero = hpBeforeDeferredDamage;
	                        game._death_cause ||= 'killed by a water demon';
	                        if (game._death_cause === 'killed by a water demon') {
	                            game._death_current_move = 1;
                            game._death_moves = game.moves || 1;
                        }
                        game._queued_message_after_more = 'You die...';
	                        keepMore = true;
	                    }
	                }
                if (game._poisoned_projectile_after_topline_more
                    && ((game.u?.uhp || 0) > 0 || game.flags?.debug)) {
                    const poisonEffect = game._poisoned_projectile_after_topline_more;
                    game._poisoned_projectile_after_topline_more = null;
                    const poisonResult = applyHeroPoisonedProjectileAfterMore(poisonEffect);
                    if (poisonResult.more)
                        keepMore = true;
                    if (poisonResult.dead) {
                        game._exercise_after_topline_more = 0;
                        game._arrow_drop_throw_after_topline_more = null;
                        game._arrow_mulch_after_topline_more = 0;
                    }
                } else {
                    game._poisoned_projectile_after_topline_more = null;
                }
                if (game._deferred_raven_blind_after_more
                    && ((game.u?.uhp || 0) > 0 || game.flags?.debug)) {
                    const ravenAttack = game._deferred_raven_blind_after_more;
                    game._deferred_raven_blind_after_more = null;
                    const subject = ravenAttack.subject || 'It';
                    let message = '';
                    if (ravenAttack.toHit > rnd(21)) {
                        d(1, 6);
                        rn2(3);
                        rn2(6);
                        const wasBlind = !!game.u?.blind;
	                        if (game.u) {
	                            game.u.blind = true;
	                            game.u._blindTimeout = Math.max(game.u._blindTimeout || 0, 135);
	                        }
                        if (!wasBlind) {
                            vision_recalc(0);
                            await docrt();
                        }
                        if (!wasBlind && !game._suppress_monster_attack_messages) message = `${subject} blinds you!`;
                    } else if (!game._suppress_monster_attack_messages) {
                        message = `${subject} misses!`;
                    }
                    if (message && !escapeDismissedMore) {
                        const width = game.nhDisplay?.cols || 80;
                        if (game._topline_after_more.length + message.length + 3 < width - 8)
                            game._topline_after_more = `${game._topline_after_more}  ${message}`;
                        else {
                            game._queued_messages_after_more ??= [];
                            game._queued_messages_after_more.push({ text: message, more: true });
                        }
                    }
                }
                game._deferred_raven_blind_after_more = null;
			                while (game._exercise_after_topline_more > 0) {
			                    rn2(2);
			                    game._exercise_after_topline_more--;
			                }
                if (game._arrow_drop_throw_after_topline_more) {
                    const arrowDrop = game._arrow_drop_throw_after_topline_more;
                    game._arrow_drop_throw_after_topline_more = null;
                    const floorMessages = [];
                    landMonsterThrownObject(arrowDrop.missile, arrowDrop.x, arrowDrop.y, {
                        glyph: arrowDrop.glyph || ')',
                        color: arrowDrop.color ?? CLR_CYAN,
                        messages: floorMessages,
                        ohit: !!arrowDrop.ohit,
                    });
                    appendToplineAfterMoreMessages(floorMessages);
                }
                if (game._arrow_mulch_after_topline_more) {
                    game._arrow_mulch_after_topline_more = 0;
                    rn2(2);
                    rn2(3);
                }
		                if (game._deferred_straw_golem_second_hit_after_topline) {
		                    const { toHit } = game._deferred_straw_golem_second_hit_after_topline;
		                    game._deferred_straw_golem_second_hit_after_topline = null;
		                    if (toHit > rnd(21)) {
		                        const secondDamage = d(1, 2);
		                        rn2(3);
		                        rn2(6);
		                        game.u.uhp = Math.max(0, (game.u?.uhp || 0) - secondDamage);
                                if (!escapeDismissedMore) {
		                            const secondMessage = 'The straw golem hits again!';
		                            const width = game.nhDisplay?.cols || 80;
		                            if (game._topline_after_more.length + secondMessage.length + 3 < width - 8)
		                                game._topline_after_more = `${game._topline_after_more}  ${secondMessage}`;
		                            else {
		                                game._queued_messages_after_more ??= [];
		                                game._queued_messages_after_more.push({ text: secondMessage, more: true });
		                            }
		                            keepMore = true;
                                }
		                    }
		                }
		                if (game._deferred_soldier_ant_sting_after_topline) {
		                    const { toHit } = game._deferred_soldier_ant_sting_after_topline;
	                    game._deferred_soldier_ant_sting_after_topline = null;
	                    if (toHit > rnd(21)) {
	                        const stingDamage = d(3, 4);
	                        rn2(10);
	                        rn2(8);
	                        rn2(3);
	                        rn2(6);
	                        game.u.uhp = Math.max(0, (game.u?.uhp || 0) - stingDamage);
                            if (!escapeDismissedMore) {
	                            const stingMessage = 'The soldier ant stings!';
	                            const width = game.nhDisplay?.cols || 80;
	                            if (game._topline_after_more.length + stingMessage.length + 3 < width - 8)
	                                game._topline_after_more = `${game._topline_after_more}  ${stingMessage}`;
	                            else {
	                                game._queued_messages_after_more ??= [];
	                                game._queued_messages_after_more.push({ text: stingMessage, more: true });
	                            }
                            }
	                    }
	                }
	                if (game._clear_transient_projectiles_after_more && !game._deferred_lethal_attack_after_more) {
                    const projectiles = (game.level?.objects || []).filter(obj => obj.transientProjectile);
                    game.level.objects = (game.level?.objects || []).filter(obj => !obj.transientProjectile);
                    for (const obj of projectiles) newsym(obj.ox, obj.oy);
                    game._clear_transient_projectiles_after_more = 0;
                }
                if (game._rolling_boulder_cleanup_after_more) {
                    const { x, y, mon } = game._rolling_boulder_cleanup_after_more;
                    game._rolling_boulder_cleanup_after_more = null;
                    const data = mon?.data || {};
                    const corpseData = data.corpse || data;
                    const dropCorpse = monsterCorpseDropSucceeds(mon, data);
                    if (dropCorpse && monsterLeavesCorpseLikeDrop(corpseData))
                        createMonsterCorpseOrGlob(mon, corpseData, x, y);
                    newsym(x, y);
                }
                if (game._cream_pie_after_more_letter) {
                    rn2(100); // C delobj() reaches obj_resists(obj, 0, 0).
                    const pie = (game.inventory || []).find(item => item.letter === game._cream_pie_after_more_letter);
                    if (pie && finishCreamPieSplat(pie)) {
                        const queuedBefore = game._queued_messages_after_more?.length || 0;
                        appendToplineAfterMoreMessages(['You splatter that cream pie, you pay for it!']);
                        if ((game._queued_messages_after_more?.length || 0) > queuedBefore) keepMore = true;
                    }
                    game._cream_pie_after_more_letter = '';
                }
                if (game._polyself_cloak_after_more_letter) {
                    const cloak = (game.inventory || []).find(item => item.letter === game._polyself_cloak_after_more_letter);
                    if (cloak) {
                        const acBonus = wornArmorAcValueGreatestErosion(cloak);
                        const floorMessages = [];
                        const queuedBefore = game._queued_messages_after_more?.length || 0;
                        dropCarriedObjectAtHero(cloak, floorMessages);
                        if (game.u) game.u.uac = (game.u.uac ?? 10) + acBonus;
                        appendToplineAfterMoreMessages(floorMessages);
                        if ((game._queued_messages_after_more?.length || 0) > queuedBefore) keepMore = true;
                    }
                    game._polyself_cloak_after_more_letter = '';
                }
                if (game._polyself_tool_after_more_letter) {
                    const tool = (game.inventory || []).find(item => item.letter === game._polyself_tool_after_more_letter);
                    if (tool) {
                        const floorMessages = [];
                        const queuedBefore = game._queued_messages_after_more?.length || 0;
                        dropCarriedObjectAtHero(tool, floorMessages);
                        appendToplineAfterMoreMessages(floorMessages);
                        if ((game._queued_messages_after_more?.length || 0) > queuedBefore) keepMore = true;
                    }
	                if (game._polyself_after_more_ac != null) {
	                    game.u.uac = game._polyself_after_more_ac;
	                    game._polyself_after_more_ac = null;
	                }
	                game._polyself_tool_after_more_letter = '';
	            }
	            if (game._unburden_after_topline_more) {
	                game._unburden_after_topline_more = 0;
	                if (game.u) game.u._statusSuffix = (game.u._statusSuffix || '').replace(' Burdened', '');
	            }
	                if ((game._nymph_steal_after_more?.theftMessage || game._nymph_steal_after_more?.removeMessage) === game._topline_after_more) {
	                    const item = (game.inventory || [])
	                        .find(invItem => invItem.letter === game._nymph_steal_after_more.itemLetter);
	                    if (item) {
	                        const mon = game._nymph_steal_after_more.mon;
	                        if (mon) {
                            const stolen = { ...(game._nymph_steal_after_more.item || item) };
	                            delete stolen.line;
	                            delete stolen.worn;
	                            delete stolen.wielded;
	                            delete stolen.alternate;
	                            stolen.letter = game._nymph_steal_after_more.itemLetter;
	                            stolen.wasStolen = true;
                            stopCarriedFigurineTimerOnLeave(stolen);
                            maybeAttachCarriedFigurineTimeout(stolen);
	                            add_to_minv(mon, stolen);
	                        }
                        removeInventoryItem(item);
                    }
                }
                if (game._bullwhip_after_more) {
                    const action = game._bullwhip_after_more;
                    game._bullwhip_after_more = null;
                    if (action.mon) {
                        game._clear_bullwhip_hide_after_more = action.mon;
                    }
                    game._bullwhip_resume_after_more = 1;
                    const item = (game.inventory || [])
                        .find(invItem => invItem.letter === action.itemLetter) || action.item;
                    if (item && action.whereTo) {
                        const dropped = {
                            ...item,
                            line: undefined,
                            worn: false,
                            wielded: false,
                            glyph: item.glyph || ')',
                            color: item.color ?? NO_COLOR,
                        };
                        stopCarriedFigurineTimerOnLeave(dropped);
                        removeInventoryItem(item);
                        if (action.whereTo === 3 && action.mon) {
                            maybeAttachCarriedFigurineTimeout(dropped);
                            add_to_minv(action.mon, dropped);
                        } else {
                            dropped.ox = action.whereTo === 1 ? action.mon?.mx : game.u?.ux || 0;
                            dropped.oy = action.whereTo === 1 ? action.mon?.my : game.u?.uy || 0;
                            game.level?.objects?.push(dropped);
                            newsym(dropped.ox, dropped.oy);
                        }
                    }
                }
                    const pendingBeforeTopline = game._pending_message || '';
                    const pendingHadFumbleAfterMonsterNoise = !!game._pending_fumble_after_monster_noise_message;
	                    const toplineHadFumbleTurnMessage = !!game._topline_after_more_fumble_turn_message;
	                    const toplineFumbleStartedMessage = !!game._topline_after_more_fumble_turn_message_starts;
	                    const toplineFumbleMessageRoll = game._topline_after_more_fumble_message_roll || 0;
	                    let toplineAfterMore = game._topline_after_more;
	                    const pendingPetMiss = pendingBeforeTopline.match(/(The ([^.]+) misses the [^.]+\.)$/);
		                    if (game._pet_message_resume?.kind === 'miss' && pendingPetMiss?.[1] === toplineAfterMore
		                        && pendingBeforeTopline.startsWith(`The ${pendingPetMiss[2]} `)) {
		                        toplineAfterMore = `${pendingPetMiss[1]}  ${toplineAfterMore}`;
		                        game._resume_time_after_more = 0;
		                    }
                    if (game._last_fumble_from_run && game._last_fumble_turn_message
                        && toplineAfterMore && !toplineHadFumbleTurnMessage
                        && toplineAfterMore !== game._last_fumble_turn_message
                        && !toplineAfterMore.startsWith(`${game._last_fumble_turn_message}  `)) {
                        game._last_fumble_turn_message = '';
                        game._last_fumble_from_run = 0;
                        game._last_fumble_keep_flushes = 0;
                    }
						                game._pending_message = toplineAfterMore;
						                game._topline_after_more = '';
                if (game._brown_mold_passive_after_more) {
                    const passive = game._brown_mold_passive_after_more;
                    game._brown_mold_passive_after_more = null;
                    rn2(2);
                    if (game.u) {
                        const coldHeal = Math.trunc((passive.coldDamage || 0) / 2);
                        game.u.uhp = Math.max(1, (game.u.uhp || 0) + coldHeal);
                        if ((game.u.uhp || 0) > (game.u.uhpmax || 0))
                            game.u.uhpmax = game.u.uhp;
                    }
	                    const mon = passive.mon;
	                    if (mon && (game.level?.monsters || []).includes(mon)) {
	                        mon.mhp = (mon.mhp || 1) - (passive.coldDamage || 0);
	                        if ((mon.mhp || 0) <= 0) {
	                            const data = mon.data || {};
	                            dropMonsterInventory(mon);
	                            const treasureDrop = !rn2(6);
	                            const corpseData = data.corpse
	                                || (data.name?.endsWith(' zombie') ? monsterByRndName(data.name.replace(/ zombie$/, '')) : null)
	                                || data;
	                            const killLoc = game.level?.at(mon.mx, mon.my);
	                            const killAccessible = killLoc && (ACCESSIBLE(killLoc.typ) || IS_POOL(killLoc.typ));
	                            if (killAccessible
	                                && !corpseData.noCorpse
	                                && corpseData === data
	                                && (mon.mx !== game.u?.ux || mon.my !== game.u?.uy)
	                                && !mon.mcloned
	                                && treasureDrop) {
	                                const drop = mkobj_at(RANDOM_CLASS, mon.mx, mon.my, true);
	                                const dropKind = String(drop?.kind || drop?.actualKind || '').replace(/^scroll labeled /, '').replace(/^potion of /, '');
	                                const dropWeight = OBJECT_WEIGHTS[dropKind] ?? CLASS_WEIGHTS[drop?.cls] ?? drop?.owt ?? 0;
	                                const humanSized = data.strong || data.armed || data.mlet === '@' || data.mlet === 'h'
	                                    || data.mlet === 'G' || data.mlet === 'o';
	                                const tooLarge = !humanSized && drop?.kind !== 'figurine' && dropWeight > 30;
	                                if (drop?.foodRoll != null || tooLarge) {
	                                    rn2(100);
	                                    game.level.objects = (game.level?.objects || []).filter(obj => obj !== drop);
	                                }
	                            }
	                            const dropCorpse = monsterLeavesCorpseLikeDrop(corpseData)
	                                && monsterCorpseDropSucceeds(mon, data);
	                            if (dropCorpse) createMonsterCorpseOrGlob(mon, corpseData);
	                            recordVanquished(mon, false);
	                            const loc = game.level?.at(mon.mx, mon.my);
	                            if (loc?.map_invisible) {
	                                loc.map_invisible = false;
                                loc.remembered_glyph = null;
	                            }
	                            if (passive.resumeIndex != null)
	                                passive.resumeIndex = adjustedMonsterResumeIndexForRemoval(mon, passive.resumeIndex);
	                            else
	                                game._monster_resume_index = adjustedMonsterResumeIndexForRemoval(mon, game._monster_resume_index || 0);
	                            game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
	                            const unseenBlindDrops = game.u?.blind
	                                ? (game.level?.objects || []).filter(obj =>
	                                    obj.ox === mon.mx && obj.oy === mon.my && !obj.seen && !obj.hidden)
	                                : [];
	                            for (const obj of unseenBlindDrops) obj.hidden = true;
	                            newsym(mon.mx, mon.my);
	                            for (const obj of unseenBlindDrops) obj.hidden = false;
	                            game._pending_message = game._pending_message
	                                ? `${game._pending_message}  ${passive.shownSubject || 'It'} dies!`
	                                : `${passive.shownSubject || 'It'} dies!`;
	                        } else if (passive.killer === 'cockatrice') {
                            const touchRoll = rnd(21);
                            const touchHit = (passive.toHit || 0) > touchRoll;
                            if (touchHit) d(0, 0);
                            game._cockatrice_touch_after_more = {
                                hit: touchHit,
                                message: `${passive.shownSubject || 'It'} ${touchHit ? 'touches you' : 'misses'}!`,
                                killer: passive.killer,
                                resumeIndex: passive.resumeIndex,
                                somebodyCanMove: passive.somebodyCanMove,
	                            };
	                        }
	                    }
	                    let stoppedDeferredCount = false;
	                    if (game._deferred_counted_repeat_stop_waiting) {
	                        game._deferred_counted_repeat_stop_waiting = 0;
	                        stoppedDeferredCount = true;
	                        game._pending_message = game._pending_message
	                            ? `${game._pending_message}  You stop waiting.`
	                            : 'You stop waiting.';
	                    }
	                    if (passive.resumeIndex != null) {
	                        game._monster_resume_index = passive.resumeIndex;
	                        game._monster_resume_somebody_can_move = !!passive.somebodyCanMove;
	                    }
	                    if (stoppedDeferredCount) game._suppress_immobile_extra_turns_once = 1;
	                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
	                    game.context.move = 0;
	                    game._process_command_time_now = 1;
                }
                if (game._spellbook_finish_after_topline_more) {
                    const finish = game._spellbook_finish_after_topline_more;
                    game._spellbook_finish_after_topline_more = null;
                    exerciseAttribute(A_WIS, true);
                    const item = (game.inventory || [])
                        .find(invItem => invItem.letter === finish.itemLetter);
                    if (item) {
                        item.known = true;
                        item.line = normalInventoryLine({ ...item, line: '' });
                        const discoveryName = `spellbook of ${finish.name}`;
                        const appearance = item.appearance || game._object_descriptions?.spellbooks?.[item.spellbookIndex];
                        const text = appearance ? `${discoveryName} (${appearance})` : discoveryName;
                        const discovery = (game._discoveries || [])
                            .find(entry => entry.section === 'Spellbooks' && entry.name === discoveryName);
                        if (discovery) {
                            discovery.text = text;
                            discovery.starred = false;
                        } else {
                            game._discoveries ??= [];
                            game._discoveries.push({ section: 'Spellbooks', name: discoveryName, text, starred: false, known: true });
                        }
                    }
                    learnObjectScore('Spellbooks', `spellbook of ${finish.name}`);
                    if ((game.u?.umovement ?? 0) >= NORMAL_SPEED)
                        game.u.umovement -= NORMAL_SPEED;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                }
                if (game._hallu_refresh_after_topline_more) {
                    game._hallu_refresh_after_topline_more = 0;
                    if ((game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active) {
                        game._display_hallucinated_redraw = 1;
                        refreshHallucinatedMap();
                        game._display_hallucinated_redraw = 0;
                    }
                }
                let pauseAfterDeferredMultiattack = false;
		                if (game._deferred_multiattack_after_more) {
	                    const deferred = game._deferred_multiattack_after_more;
	                    game._deferred_multiattack_after_more = null;
	                    const messages = game._pending_message ? [game._pending_message] : [];
	                    if (deferred.first?.hit) {
	                        rn2(3);
	                        rn2(6);
	                        let damage = deferred.first.damage || 0;
	                        if (damage && (game.u?.uac ?? 10) < 0)
	                            damage = Math.max(1, damage - rnd(-(game.u?.uac ?? 10)));
	                        game.u.uhp = Math.max(0, (game.u?.uhp || 0) - damage);
		                    }
		                    for (let i = 0; i < (deferred.attacks || []).length; i++) {
		                        const attackIndex = (deferred.nextIndex || 1) + i;
		                        const attack = deferred.attacks[i];
		                        const attackRoll = rnd(20 + attackIndex);
                                let shownSubject = deferred.subject || 'It';
                                let hallucinatedSubject = false;
                                if ((game.u?._statusSuffix || '').includes('Hallu') && !game.u?.blind) {
                                    hallucinatedSubject = true;
                                    let halluIndex;
                                    do {
                                        halluIndex = rn2_on_display_rng(SPECIAL_PM + BOGUSMONSIZE);
                                    } while (halluIndex < SPECIAL_PM && !DISPLAY_MONSTER_HALLU_NAMES[halluIndex]);
                                    shownSubject = `The ${halluIndex >= SPECIAL_PM
                                        ? getbogusmon()
                                        : DISPLAY_MONSTER_HALLU_NAMES[halluIndex] || shownSubject}`;
                                    if (halluIndex < SPECIAL_PM) rn2_on_display_rng(2);
                                }
                                let nextMessage;
                                let nextFirst = null;
		                        if ((deferred.toHit || 0) <= attackRoll) {
		                            nextMessage = `${shownSubject} ${(deferred.toHit || 0) === attackRoll ? 'just ' : ''}misses!`;
		                        } else {
		                            const damage = d(attack.dice ?? 1, attack.sides ?? 2);
                                    const attackVerb = hallucinatedSubject
                                        ? String(attack.verb || 'hits').replace(/ again$/, '')
                                        : attack.verb || 'hits';
		                            nextMessage = `${shownSubject} ${attackVerb}!`;
                                    nextFirst = { hit: true, damage, message: nextMessage };
                                }
                                const currentMessage = messages.join('  ');
                                const width = game.nhDisplay?.cols || 80;
                                if (currentMessage && currentMessage.length + nextMessage.length + 3 >= width - 8) {
                                    game._topline_after_more = nextMessage;
                                    game._topline_more_after_more = 1;
                                    game._deferred_multiattack_after_more = {
                                        first: nextFirst,
                                        attacks: (deferred.attacks || []).slice(i + 1),
                                        nextIndex: attackIndex + 1,
                                        toHit: deferred.toHit,
                                        subject: deferred.subject,
                                        name: deferred.name,
                                    };
                                    game._attack_resume_after_more = 1;
                                    game._hallu_display_after_deferred_multiattack = 1;
                                    keepMore = true;
                                    pauseAfterDeferredMultiattack = true;
                                    break;
                                }
		                        messages.push(nextMessage);
                                if (nextFirst?.hit) {
		                            rn2(3);
		                            rn2(6);
		                            let damage = nextFirst.damage || 0;
		                            if (damage && (game.u?.uac ?? 10) < 0)
		                                damage = Math.max(1, damage - rnd(-(game.u?.uac ?? 10)));
		                            game.u.uhp = Math.max(0, (game.u?.uhp || 0) - damage);
                                }
		                    }
	                    game._pending_message = messages.join('  ');
	                    if ((game.u?.uhp || 0) <= 0 && !game._queued_message_after_more) {
	                        const name = deferred.name || 'monster';
	                        const article = /^[aeiou]/i.test(name) ? 'an' : 'a';
	                        game._death_cause = `killed by ${article} ${name}`;
	                        game._queued_message_after_more = 'You die...';
	                        keepMore = true;
	                    }
	                }
	                game._pending_fumble_turn_message = toplineHadFumbleTurnMessage ? 1 : 0;
                game._pending_fumble_turn_message_starts = toplineFumbleStartedMessage ? 1 : 0;
                game._pending_fumble_message_roll = toplineHadFumbleTurnMessage ? toplineFumbleMessageRoll : 0;
                game._pending_fumble_after_monster_noise_message = 0;
                game._pending_starts_monster_noise_message = 0;
                game._pending_monster_noise_message = 0;
                game._pending_monster_noise_far = 0;
                game._topline_after_more_fumble_turn_message = 0;
                game._topline_after_more_fumble_turn_message_starts = 0;
                game._topline_after_more_fumble_message_roll = 0;
	                if (escapeDismissedMore) {
	                    game._suppress_monster_attack_messages = 2;
	                    game._escape_dismissed_more_this_command = 1;
	                }
	                if (game._queued_message_after_topline_more) {
                    game._queued_message_after_more = game._queued_message_after_topline_more;
                    game._queued_message_after_topline_more = '';
                }
                const resumeHelplessAfterToplineMore = resumePetInventory && game._wake_message
                    && (game._helpless_time || 0) > 0 && (game._pending_time_passed || 0) > 0;
                game._pet_inventory_topline_more_tail_defer = resumePetInventory
                    && /\bpicks up\b/.test(toplineAfterMore)
                    && !resumeHelplessAfterToplineMore
                    && !keepMore && !forceToplineMore && !game._fire_direction_pending_after_more ? 1 : 0;
	                if (resumeHelplessAfterToplineMore && forceToplineMore)
	                    forceToplineMore = false;
                const demonBribeOfferAfterMore = !!game._demon_bribe_offer_after_more;
                game._message_more = keepMore || forceToplineMore || (resumePetInventory && game._fire_direction_pending_after_more ? 1 : 0);
                game._keep_pending_message = 1;
                if (demonBribeOfferAfterMore && !game._message_more) {
                    game._demon_bribe_offer_after_more = 0;
                    game._command_mode = 'demonBribeOffer';
                }
                if (game._message_more) {
		                    game._process_time_with_more = !pauseAfterDeferredMultiattack
                                && (resumeMonsters || resumeAttackMonsters || (resumePetInventory && game._fire_direction_pending_after_more)) ? 1 : 0;
		                    if (resumePetInventory && game._fire_direction_pending_after_more) game._fire_direction_ready_after_more = 1;
		                }
			                if (resumeAttackMonsters) {
			                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                }
			                if (resumePetMonsterTurn) {
			                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
			                    game._process_command_time_now = 1;
			                }
		                if ((resumeMonsters || resumeAttackMonsters) && game._pending_time_passed > 0) {
		                    game._process_command_time_now = 1;
		                    if (resumeMonsters) game._pickup_resume_stop_after_monsters = 1;
		                }
                const fumbleNoiseMore = resumePetMessage && pendingHadFumbleAfterMonsterNoise;
                const petRunSteps = Math.max(game._run_steps_remaining || 0, game._run_steps_after_more || 0);
                const resumePetRun = resumePetMessage && game._run_key && petRunSteps > 0;
                const resumeRunningPet = !fumbleNoiseMore && (resumePetRun
                    || (resumePetInventory
                        && game._run_interrupted_by_more
                        && game._run_interrupted_more_message === pendingBeforeTopline
                        && /^You hear /.test(game._run_interrupted_more_message || '')
                        && game._pending_time_passed));
		                if (resumeRunningPet) {
                    if (resumePetInventory) game._pet_inventory_run_resume = 1;
                    const wasRunInterrupted = !!game._run_interrupted_by_more;
                    game._run_interrupted_by_more = 0;
                    game._run_interrupted_more_message = '';
	                    const fallbackRunSteps = game._run_key && wasRunInterrupted ? Math.max(COLNO, ROWNO) : 0;
	                    game._run_steps_remaining = Math.max(game._run_steps_remaining || 0, game._run_steps_after_more || fallbackRunSteps);
	                    game._run_steps_after_more = 0;
	                    game._ignore_run_stop_after_more = 1;
                    if (game._message_more) game._process_time_with_more = 1;
                    let resumedRunStep = false;
	                    if (!resumePetInventory && game._run_key && game._run_steps_remaining > 0) {
	                        game._run_steps_remaining--;
	                        game._running_continuation = 1;
	                        await rhack(game._run_key);
	                        game._running_continuation = 0;
                        resumedRunStep = true;
                        if (game._pending_time_passed) game.context.move = 0;
	                    }
                    if (!resumedRunStep) game.context.move = game._pending_time_passed ? 0 : 1;
		                    game._process_command_time_now = 1;
                }
	                if (fumbleNoiseMore) {
	                    if (!resumePetMonsterTurn) game.context.move = 1;
	                    game._process_command_time_now = 1;
	                }
	                return;
	            }
            if (game._cockatrice_touch_after_more) {
                const attack = game._cockatrice_touch_after_more;
                game._cockatrice_touch_after_more = null;
                if (attack.hit) {
                    const stoningRoll = rn2(3);
                    if (!stoningRoll && game.u && !game.u.stoneResistance && !heroPolyselfResistsStoning()
                        && !(game.u._stonedTimeout || 0)) {
                        game.u._stonedTimeout = 5;
                        game.u._stonedKiller = attack.killer || 'cockatrice';
                    }
                    rn2(3);
                    rn2(6);
                }
                await setMessage(attack.message || 'It touches you!');
                if (attack.resumeIndex != null) {
                    game._monster_resume_index = attack.resumeIndex;
                    game._monster_resume_somebody_can_move = !!attack.somebodyCanMove;
                }
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                game._process_command_time_now = 1;
                return;
            }
	            if (game._swallow_after_more) {
	                const swallow = game._swallow_after_more;
	                game._swallow_after_more = null;
	                const mon = swallow.mon;
	                if (mon && game.u) {
	                    const oldX = mon.mx;
	                    const oldY = mon.my;
	                    mon.mx = game.u.ux || mon.mx;
	                    mon.my = game.u.uy || mon.my;
	                    mon.mtrapped = 0;
	                    game.u.ustuck = mon;
	                    game.u.uswallow = 1;
	                    game.u.uswldtim = swallow.turns || 2;
	                    newsym(oldX, oldY);
	                    newsym(mon.mx, mon.my);
	                    vision_recalc(2);
	                }
		                const swallowTurns = Math.max(2, rnd((swallow.swallowLevel ?? mon?.m_lev ?? mon?.data?.mlevel ?? 1) + 5));
		                if (game.u) game.u.uswldtim = Math.max(1, swallowTurns - 1);
		                const slowDigestRegurgitation = !!(swallow.digestAttack && heroHasSlowDigestion());
		                if (slowDigestRegurgitation && game.u) game.u.uswldtim = 0;
		                const slowDigestDamage = slowDigestRegurgitation && (game.u?.uac ?? 10) < 0 ? 1 : 0;
		                const swallowDamage = slowDigestRegurgitation ? slowDigestDamage
		                    : swallow.coldCheck && !rn2(2) ? 0 : swallow.damage || 0;
		                game._pending_message = slowDigestRegurgitation
		                    ? `You get regurgitated!  Obviously ${monsterTasteSubject(mon)} doesn't like your taste.`
		                    : swallow.digestAttack && swallowDamage ? `${fireScrollMonsterName(mon)} digests you!`
		                        : swallowDamage ? 'You are freezing to death!' : '';
		                game._message_more = slowDigestRegurgitation ? 1 : 0;
		                game._process_time_with_more = slowDigestRegurgitation ? 0 : 1;
		                game._keep_pending_message = 1;
		                if (swallowDamage && game.u)
		                    game.u.uhp = Math.max(0, (game.u?.uhp || 0) - swallowDamage);
		                if ((game.u?._statusSuffix || '').includes('Hallu')) {
		                    for (let i = 0; i < 8; i++) rn2_on_display_rng(5);
		                    refreshSwallowOverlay(false);
		                }
		                refreshSwallowOverlay(false);
		                if (slowDigestRegurgitation) {
		                    await finishSwallowExpel(mon);
		                    game._message_more = 1;
		                    game._process_time_with_more = 0;
		                }
		                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
		                if (swallow.resumeIndex != null) game._monster_resume_index = swallow.resumeIndex;
		                game._monster_resume_somebody_can_move = !!swallow.somebodyCanMove;
		                game.context.move = 0;
		                game._process_command_time_now = 1;
		                return;
	            }
            if (game._fire_ray_damage_after_more) {
                const damage = game._fire_ray_damage_after_more;
                game._fire_ray_damage_after_more = 0;
                if (damage && game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            }
            if (game._blind_arrival_feel_rows_after_more) {
                game._blind_arrival_overlay_after_more = game._blind_arrival_feel_rows_after_more;
                game._blind_arrival_feel_rows_after_more = null;
                await setMessage('You try to feel what is lying here on the floor.', true);
                return;
            }
            if (game._blind_arrival_overlay_after_more) {
                const rows = game._blind_arrival_overlay_after_more;
                const processDeferred = !!game._blind_arrival_overlay_process_deferred_now;
                const nohandsOnOtherKey = !!game._blind_arrival_overlay_nohands_on_other_key;
                game._blind_arrival_overlay_after_more = null;
                game._blind_arrival_overlay_process_deferred_now = 0;
                setOverlay(rows, rows.length, false, 39);
                game._pending_message = 'text-window';
                game._message_more = 1;
                game._keep_pending_message = 1;
                game._command_mode = 'objectListMore';
                if (processDeferred) {
                    game._deferred_context_move = game._blind_arrival_overlay_deferred_context_move || 1;
                    game._object_list_process_deferred_now = 1;
                } else game._object_list_use_pending_time = 1;
                game._object_list_nohands_on_other_key = nohandsOnOtherKey ? 1 : 0;
                game._blind_arrival_overlay_deferred_context_move = 0;
                game._blind_arrival_overlay_nohands_on_other_key = 0;
                return;
            }
            if (game._polyself_drop_items_after_overload_more) {
                game._polyself_drop_items_after_overload_more = 0;
                rn2(19);
                const floorMessages = [];
                const queuedItems = game._polyself_drop_items_after_overload_items;
                const dropItems = Array.isArray(queuedItems)
                    ? queuedItems
                    : [...(game.inventory || [])].filter(item => new Set(['c', 'd', 'f', 'z']).has(item.letter));
                const releaseItems = Array.isArray(game._polyself_release_items_after_overload_items)
                    ? game._polyself_release_items_after_overload_items
                    : [];
                releasePolyselfEquipmentItems(releaseItems);
                dropPolyselfEquipmentItems(dropItems, floorMessages);
                game._polyself_drop_items_after_overload_items = null;
                game._polyself_release_items_after_overload_items = null;
                if (game.u) game.u.uac = game._polyself_drop_items_after_overload_ac ?? 9;
                game._polyself_drop_items_after_overload_ac = null;
                newsym(game.u?.ux || 0, game.u?.uy || 0);
                const eye = (game.level?.monsters || []).find(mon => mon.data?.name === 'floating eye');
                if (eye) newsym(eye.mx, eye.my);
                game._pending_message = [
                    game._polyself_drop_items_after_overload_message,
                    ...floorMessages,
                ].filter(Boolean).join('  ');
                game._polyself_drop_items_after_overload_message = '';
                game._message_more = polyselfFalloutNeedsMore() ? 1 : 0;
                game._keep_pending_message = 1;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                game._process_command_time_now = 1;
                return;
            }
            if (game._queued_messages_after_more?.length) {
                const next = game._queued_messages_after_more.shift();
                if (next.deferredMeleeWake) {
                    const action = next.deferredMeleeWake;
                    if (action.weaponHit) {
                        rn2(3);
                        rn2(6);
                    }
                    const target = action.target;
                    const fleeRoll = rn2(25);
                    if (!fleeRoll && target && (target.mhp || 0) < Math.trunc((target.mhpmax || 0) / 2)
                        && !(game.u?.uswallow && game.u?.ustuck === target)) {
                        const fleeTime = rn2(3) ? 0 : rnd(100);
                        target.mflee = 1;
                        target.mfleetim = fleeTime ? Math.min(fleeTime + (target.mfleetim || 0), 127) : 0;
                        clearMonsterTrack(target);
                    }
                    rn2(3);
                }
                if (next.gasSporeMonsterExplosion) {
                    const explosion = next.gasSporeMonsterExplosion;
                    rn2(5);
                    const target = explosion.target;
                    if (target) {
                        const resisted = rn2(explosion.resistBound) < (target.data?.mr || 0);
                        const damage = resisted ? Math.trunc((explosion.damage + 1) / 2) : explosion.damage;
                        target.mhp = (target.mhp || 1) - damage;
                    }
                }
                if (next.gasSporeHeroExplosion && game.u) {
                    rn2(5);
                    game.u.uhp = Math.max(0, (game.u.uhp || 0) - next.gasSporeHeroExplosion.damage);
                    exerciseAttribute(A_STR, false);
                }
                if (next.clearBeam) game._transient_beam_cells = null;
                game._pending_message = '';
                game._message_more = 0;
                let nextText = next.text;
                if (next.wakeNearby) {
                    const wakeMessages = [];
                    for (const sleeper of game.level?.monsters || []) {
                        if (sleeper === next.wakeNearby.source || !sleeper.msleeping) continue;
                        const dx = (sleeper.mx || 0) - next.wakeNearby.x;
                        const dy = (sleeper.my || 0) - next.wakeNearby.y;
                        if (dx * dx + dy * dy > 100 || !couldsee(sleeper.mx, sleeper.my)) continue;
                        sleeper.msleeping = 0;
                        const sleeperName = sleeper.givenName || `The ${sleeper.data?.name || 'creature'}`;
                        wakeMessages.push(`${sleeperName} wakes up.`);
                    }
                    if (wakeMessages.length) nextText = `${nextText}  ${wakeMessages.join('  ')}`;
                }
                if (next.fireBreathHeroHit) {
                    const breath = next.fireBreathHeroHit;
                    const hit = fireBreathDamageHero(breath.dice, origDamage =>
                        fireDamageInventory(origDamage, false, true, {
                            preburnedArmor: { bodyHit: true, message: '' },
                            allowLifeSaving: true,
                        }));
                    const vaporFatal = !!(hit.lifeSaving || hit.fatal);
                    const messages = [nextText, ...(vaporFatal ? [] : hit.messages)].filter(Boolean);
                    if (vaporFatal) {
                        const followups = hit.messages.map((text, index) => ({
                            text,
                            more: index < hit.messages.length - 1,
                        }));
                        if (followups.length) {
                            const final = followups[followups.length - 1];
                            final.lifeSaving = !!hit.lifeSaving;
                            final.fatal = !!hit.fatal;
                            final.more = true;
                            next.insertAfter = [
                                ...followups,
                                ...(next.insertAfter || []),
                            ];
                            next.more = true;
                        }
                    } else if (hit.lethal) {
                        next.insertAfter = [
                            { text: 'You die...', more: true },
                            ...(next.insertAfter || []),
                        ];
                    } else {
                        const ray = { ...breath.ray, heardGas: false };
                        const follow = advanceFireBreathRay(ray, breath.sourceId, {
                            floorFire: (x, y) => burnRayFloorObjectsByFire(x, y, { heroCaused: false }),
                        });
                        if (follow.target?.type === 'monster') {
                            game._queued_messages_after_more ??= [];
                            game._queued_messages_after_more.unshift({
                                text: follow.messages.join('  '),
                                more: true,
                                fireBreathMonsterHit: {
                                    dice: breath.dice,
                                    ray: { ...follow.ray },
                                    sourceId: breath.sourceId,
                                    targetId: follow.target.mon.m_id,
                                    resumeIndex: breath.resumeIndex,
                                    somebodyCanMove: breath.somebodyCanMove,
                                },
                            });
                            next.more = true;
                        } else {
                            messages.push(...follow.messages);
                            const source = (game.level?.monsters || []).find(mon => mon.m_id === breath.sourceId);
                            finishHeroTargetedBreath(source);
                            next.processTime = true;
                            next.resumeIndex = breath.resumeIndex;
                            next.somebodyCanMove = breath.somebodyCanMove;
                        }
                    }
                    nextText = messages.join('  ');
                }
                if (next.fireBreathMonsterHit) {
                    const breath = next.fireBreathMonsterHit;
                    const target = (game.level?.monsters || []).find(mon => mon.m_id === breath.targetId)
                        || (game.level?.monsters || []).find(mon => mon.mx === breath.ray.x && mon.my === breath.ray.y);
                    const messages = [nextText].filter(Boolean);
                    if (target && fireBreathZapHits(target.data?.ac ?? target.ac ?? 10)) {
                        const visible = !game.u?.blind && !target.minvis && !target.mundetected
                            && (game.viz_array?.[target.my]?.[target.mx] & IN_SIGHT);
                        const hit = fireBreathDamageMonster(target, breath.dice, origDamage => {
                            const inventoryMessages = [];
                            const damage = monsterFireInventoryDamage(target, origDamage, inventoryMessages, visible);
                            igniteMonsterFireInventoryItems(target, inventoryMessages, visible);
                            return { damage, messages: inventoryMessages };
                        });
                        messages.push(...hit.messages);
                        if (!hit.killedHidden) {
                            messages.push(game.u?.blind || target.minvis || target.mundetected
                                ? 'The blast of fire hits it!'
                                : `The blast of fire hits the ${target.data?.name || 'monster'}!`);
                        }
                        if (breath.ray.remaining > 0) breath.ray.remaining = Math.max(0, breath.ray.remaining - 2);
                    }
                    const follow = advanceFireBreathRay(breath.ray, breath.sourceId, {
                        floorFire: (x, y) => burnRayFloorObjectsByFire(x, y, { heroCaused: false }),
                    });
                    if (follow.target?.type === 'monster') {
                        game._queued_messages_after_more ??= [];
                        game._queued_messages_after_more.unshift({
                            text: follow.messages.join('  '),
                            more: true,
                            fireBreathMonsterHit: {
                                dice: breath.dice,
                                ray: { ...follow.ray },
                                sourceId: breath.sourceId,
                                targetId: follow.target.mon.m_id,
                                resumeIndex: breath.resumeIndex,
                                somebodyCanMove: breath.somebodyCanMove,
                            },
                        });
                    } else {
                        messages.push(...follow.messages);
                        const source = (game.level?.monsters || []).find(mon => mon.m_id === breath.sourceId);
                        finishHeroTargetedBreath(source);
                        next.processTime = true;
                        next.resumeIndex = breath.resumeIndex;
                        next.somebodyCanMove = breath.somebodyCanMove;
                    }
                    nextText = messages.join('  ');
                    next.more = true;
                }
                if (next.forceEndgameWizard) {
                    const mon = await resurrectWizardOfYendor();
                    if (mon) {
                        newsym(mon.mx, mon.my);
                        next.insertAfter = [
                            { text: 'The Wizard of Yendor suddenly appears next to you!', more: true },
                            { text: 'A voice booms out...  "So thou thought thou couldst kill me, fool."', more: true },
                            ...(next.insertAfter || []),
                        ];
                    }
                }
                if (next.breakChestContinue) {
                    let followMessage = nextBreakChestDestroyedContentMessage();
                    if (followMessage) {
                        const done = !hasPendingBreakChestContents();
                        if (done) followMessage = appendBreakChestFinalDebt(followMessage);
                        game._queued_messages_after_more = [
                            {
                                text: followMessage,
                                more: false,
                                processTime: true,
                                breakChestContentMessage: true,
                                breakChestContinue: !done,
                            },
                            ...(game._queued_messages_after_more || []),
                        ];
                        next.more = true;
                        next.processTime = false;
                    } else if (!hasPendingBreakChestContents()) {
                        const debtMessage = appendBreakChestFinalDebt('');
                        if (debtMessage) nextText = nextText ? `${nextText}  ${debtMessage}` : debtMessage;
                        next.processTime = true;
                    }
                }
                if (next.text === 'You die...' || next.text === 'You die.') prepareDeathBones();
                await setMessage(nextText, !!next.more);
                if (next.clearOnlyNext && !next.more) game._clear_pending_message_only_once = 1;
                if (next.valleyGasAfterClear) {
                    game._valley_gas_after_clear = 1;
                    if (game.u?.blind && game._blind_arrival_objects_after_more?.length) {
                        game._blind_arrival_feel_rows_after_more = arrivalObjectListRows(game._blind_arrival_objects_after_more);
                        game._blind_arrival_objects_after_more = null;
                    }
                }
                if (next.sleepReflect) {
                    exerciseAttribute(A_WIS, true);
                    rn2(20);
                }
                if (next.damageAfter)
                    game._queued_damage_after_more = (game._queued_damage_after_more || 0) + next.damageAfter;
                if (next.polyselfDropAfterMore) game._polyself_drop_items_after_overload_more = 1;
                if (next.insertAfter?.length)
                    game._queued_messages_after_more = [
                        ...next.insertAfter,
                        ...(game._queued_messages_after_more || []),
                    ];
                if (applyLifeSavingOrFatalCommandMode(next)) return;
                if (next.beginWishPrompt) {
                    game._wish_text = '';
                    game._wish_move_cost = 0;
                    game._wish_dust_item = null;
                    game._command_mode = 'wizardWish';
                }
                if (next.text === 'You die...' || next.text === 'You die.') {
                    if (!game._pending_time_passed) game._death_current_move = 0;
                    game._pending_time_passed = 0;
                    game.context.move = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    game._message_more = 1;
                    game._command_mode = 'deathDieMore';
                    return;
                }
                if (next.more && game._run_key && game._run_steps_remaining > 0
                    && /^The .+ (?:bites|hits|misses|stings|kicks) the .+\.$/.test(next.text)) {
                    game._ignore_run_stop_after_more = 1;
                    game._process_time_with_more = 1;
                    game._run_steps_remaining--;
                    game._running_continuation = 1;
                    await rhack(game._run_key);
                    game._running_continuation = 0;
                    if (game._message_more) game._process_time_with_more = 1;
                }
                if (next.processTime) {
                    if (next.resumeIndex != null) {
                        game._monster_resume_index = next.resumeIndex;
                        game._monster_resume_somebody_can_move = !!next.somebodyCanMove;
                    }
                    if (next.fireBreathMonsterHit && next.more) {
                        game._pause_after_resumed_monster_attack_more = 1;
                        game._queued_fire_breath_process_now = 1;
                    }
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    game._process_time_with_more = !!next.more;
                    if (next.breakChestContentMessage) {
                        game._continue_monsters_after_more = 1;
                        game._resume_time_after_more = 0;
                    }
                }
                return;
            }
            if (game._queued_overlay_after_more) {
                const next = game._queued_overlay_after_more;
                game._queued_overlay_after_more = null;
                setOverlay(next.lines, next.clearRows ?? 24, !!next.hideStatus, next.clearCol ?? null);
                const messageMore = next.messageMore ?? true;
                game._pending_message = next.message ?? (messageMore ? 'text-window' : '');
                game._message_more = messageMore ? 1 : 0;
                game._keep_pending_message = 1;
                game._command_mode = next.mode || 'questIntroMore';
                if (next.deferredContextMove != null) {
                    game._deferred_context_move = next.deferredContextMove;
                    game.context.move = 0;
                }
                if (next.usePendingTime) game._object_list_use_pending_time = 1;
                return;
            }
            if (game._queued_message_after_more) {
                let next = game._queued_message_after_more;
                const nextMoreLine = game._queued_message_more_line_after_more || '';
                game._queued_message_after_more = '';
                game._queued_message_more_line_after_more = '';

                if (game._burden_after_more) {
                    game._burden_after_more = 0;
                    if (game.u && !(game.u._statusSuffix || '').includes('Burdened'))
                        game.u._statusSuffix = `${game.u._statusSuffix || ''} Burdened`;
                }
                if (game._wish_notice_after_more) {
                    game._wish_notice_after_more = 0;
                    godsNoticeWish();
                }
                if (game._reset_umovement_after_more) {
                    game._reset_umovement_after_more = 0;
                    game.u.umovement = 0;
                    game._run_steps_remaining = 0;
                }
                if (game._destroy_armor_uac_delta_after_more) {
                    if (game.u) game.u.uac = (game.u.uac ?? 10) + game._destroy_armor_uac_delta_after_more;
                    game._destroy_armor_uac_delta_after_more = 0;
                }
                const hadQueuedDead = !!game._queued_dead_monsters?.length;
                if (hadQueuedDead) {
                    for (const mon of game._queued_dead_monsters) {
                        const data = mon.data || {};
                        const corpseData = data.corpse || data;
                        const dropCorpse = monsterCorpseDropSucceeds(mon, data);
                        recordVanquished(mon, false);
                        dropMonsterInventory(mon);
                        if (dropCorpse && monsterLeavesCorpseLikeDrop(corpseData))
                            createMonsterCorpseOrGlob(mon, corpseData);
                        const killer = mon._killed_by_mon;
                        mon._killed_by_mon = null;
                        if (!monsterGrowUp(killer, mon)) rnd((data.mlevel ?? 0) + 1);
                        game.level.monsters = (game.level?.monsters || []).filter(other => other !== mon);
                        const staleTrack = killer?.pet && killer.mtrack?.[0]?.x
                            && (game.viz_array?.[killer.mtrack[0].y]?.[killer.mtrack[0].x] & IN_SIGHT)
                            ? killer.mtrack[0] : null;
                        const staleTrackHasObject = staleTrack && (game.level?.objects || []).some(obj =>
                            !obj.hidden && !obj.transientProjectile
                            && obj.ox === staleTrack.x && obj.oy === staleTrack.y);
                        const staleTrackHasMonster = staleTrack && (game.level?.monsters || []).some(other =>
                            other !== killer && !other.dead && (other.mhp == null || other.mhp > 0)
                            && other.mx === staleTrack.x && other.my === staleTrack.y);
                        const staleTrackDiagonal = staleTrack
                            && Math.abs(staleTrack.x - killer.mx) === 1
                            && Math.abs(staleTrack.y - killer.my) === 1;
                        newsym(mon.mx, mon.my);
                        if (data.name !== 'lichen' && staleTrackDiagonal && !staleTrackHasObject && !staleTrackHasMonster) {
                            killer._hide_for_queued_kill_more = 1;
                            newsym(killer.mx, killer.my);
                            const hiddenLoc = game.level?.at(killer.mx, killer.my);
                            if (game.nhDisplay?.setCell && hiddenLoc) {
                                game.nhDisplay.setCell(
                                    killer.mx - 1,
                                    killer.my + 1,
                                    hiddenLoc.disp_ch || ' ',
                                    hiddenLoc.disp_color ?? NO_COLOR,
                                    hiddenLoc.disp_attr ?? 0,
                                );
                            }
                            killer._hide_for_queued_kill_more = 0;
                            show_glyph_cell(
                                staleTrack.x, staleTrack.y,
                                killer.data?.mlet?.[0] || 'd',
                                killer.data?.name === 'pony' ? CLR_BROWN : CLR_WHITE,
                                false,
                                game._hilite_pet ? 1 : 0,
                            );
                            if (game.nhDisplay?.setCell) {
                                game.nhDisplay.setCell(
                                    staleTrack.x - 1,
                                    staleTrack.y + 1,
                                    killer.data?.mlet?.[0] || 'd',
                                    killer.data?.name === 'pony' ? CLR_BROWN : CLR_WHITE,
                                    game._hilite_pet ? 1 : 0,
                                );
                            }
                            game._stale_queued_kill_pet = {
                                mon: killer,
                                x: staleTrack.x,
                                y: staleTrack.y,
                                message: next,
                            };
                        } else if (killer?.pet) {
                            newsym(killer.mx, killer.my);
                        }
                    }
                    game._queued_dead_monsters = [];
                }
                while (game._queued_postmov_distfleeck > 0) {
                    rn2(5);
                    game._queued_postmov_distfleeck--;
                }
                let queuedMore = !!game._queued_message_more_after_more;
                const blockTimeUntilClear = !!game._queued_message_blocks_time_after_more;
                game._queued_message_more_after_more = 0;
                game._queued_message_blocks_time_after_more = 0;
                const messageBeforeQueuedMore = game._pending_message || '';
                game._pending_message = '';
                game._message_more = 0;
                if (game._queued_temple_ghost_roll_after_more) {
                    game._queued_temple_ghost_roll_after_more = 0;
                    const ghostText = await finishUntendedTempleEntry();
                    if (ghostText) {
                        next = `${next}  ${ghostText}`;
                        queuedMore = true;
                    }
                }
                if (game._queued_priest_intone_after_more) {
                    const intoneRoomno = game._queued_priest_intone_after_more;
                    game._queued_priest_intone_after_more = 0;
                    const intoneText = tendedTemplePriestIntone(intoneRoomno);
                    if (intoneText) {
                        next = next ? `${next}  ${intoneText}` : intoneText;
                        queuedMore = true;
                        game._queued_priest_entry_after_more = intoneRoomno;
                    }
                }
                if (game._potion_sickness_after_more) {
                    game._potion_sickness_after_more = 0;
                    if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - 1);
                    rn2(19);
                }
                if (game._read_scroll_exercise_after_more) {
                    game._read_scroll_exercise_after_more = 0;
                    rn2(19);
                }
                if (game._nymph_second_passive_after_more) {
                    game._nymph_second_passive_after_more = 0;
                    rn2(3);
                }
                let finishedBreakChestContents = false;
                if (game._break_chest_contents_after_more
                    && (next === game._break_chest_destroyed_message || game._break_chest_content_message_active)) {
                    let destroyedMessage = nextBreakChestDestroyedContentMessage();
                    if (destroyedMessage) {
                        const done = !hasPendingBreakChestContents();
                        if (done) destroyedMessage = appendBreakChestFinalDebt(destroyedMessage);
                        game._queued_messages_after_more = [
                            {
                                text: destroyedMessage,
                                more: false,
                                processTime: true,
                                breakChestContentMessage: true,
                                breakChestContinue: !done,
                            },
                            ...(game._queued_messages_after_more || []),
                        ];
                        queuedMore = true;
                    } else if (!hasPendingBreakChestContents()) {
                        const debtMessage = appendBreakChestFinalDebt('');
                        if (debtMessage) {
                            game._queued_messages_after_more = [
                                { text: debtMessage, more: false, processTime: true, breakChestContentMessage: true },
                                ...(game._queued_messages_after_more || []),
                            ];
                            queuedMore = true;
                        } else {
                            finishedBreakChestContents = true;
                        }
                    }
                }
                if (game._armor_takeoff_after_more && next.startsWith('You finish taking off your ')) {
                    const occupation = game._armor_takeoff_after_more;
                    game._armor_takeoff_after_more = null;
                    const item = (game.inventory || [])
                        .find(invItem => invItem.letter === occupation.itemLetter);
                    if (item && item.worn && occupation.acBonus != null) {
                        item.worn = false;
                        item.line = `${item.letter || occupation.itemLetter || '?'} - ${occupation.baseName || pickupObjectName(item)}`;
                        if (game.u) game.u.uac = (game.u.uac ?? 10) + occupation.acBonus;
                        if ((occupation.kind === 'speed boots' || isBlueDragonArmorKind(occupation.kind)) && game.u)
                            syncHeroSpeedState();
                        updateGauntletsOfPowerStrength(occupation.kind, false);
                        updateWornDisplacement();
                    }
                }
                if (game._nymph_steal_after_more?.stolenMessage === next) {
                    const mon = game._nymph_steal_after_more.mon;
                    game._nymph_steal_after_more = null;
                    if (mon && (game.level?.monsters || []).includes(mon)) {
                        const demonCourtRestricted = game.level?.flags?.demon_court_noteleport
                            && !mon.data?.demonLord && !mon.data?.demonPrince;
                        if ((game.level?.flags?.noteleport || demonCourtRestricted)
                            && !game.u?.blind && !mon.minvis && couldsee(mon.mx, mon.my)) {
                            const monName = mon.givenName || `the ${mon.data?.name || 'creature'}`;
                            rn2(3);
                            rn2(6);
                            next = `${next}  A mysterious force prevents ${monName} from teleporting!`;
                        } else {
                            const oldX = mon.mx, oldY = mon.my;
                            for (let trycount = 0; trycount < 50; trycount++) {
                                const x = rnd(COLNO - 1), y = rn2(ROWNO);
                                const loc = game.level?.at(x, y);
                                const occupied = (game.level?.monsters || [])
                                    .some(other => other !== mon && other.mx === x && other.my === y);
                                const boulder = (game.level?.objects || [])
                                    .some(obj => obj.otyp === BOULDER && obj.ox === x && obj.oy === y);
                                const onHero = game.u?.ux === x && game.u?.uy === y;
                                const badPool = IS_POOL(loc?.typ) && !mon.data?.swimmer;
                                if (!loc || !ACCESSIBLE(loc.typ) || occupied || boulder || onHero || badPool) continue;
                                mon.mx = x;
                                mon.my = y;
                                clearMonsterTrack(mon);
                                mon.mux = game.u?.ux ?? x;
                                mon.muy = game.u?.uy ?? y;
                                newsym(oldX, oldY);
                                newsym(x, y);
                                break;
                            }
                            rn2(3);
                            rn2(6);
                            const monName = mon.givenName || `The ${mon.data?.name || 'creature'}`;
                            next = `${next}  ${monName} vanishes!`;
                        }
                    }
                }
                if (next === 'You die...' || next === 'You die.') prepareDeathBones();
                await setMessage(next, queuedMore);
                if (game._queued_room_entry_after_queued_more) {
                    const roomEntryText = game._queued_room_entry_after_queued_more;
                    game._queued_room_entry_after_queued_more = '';
                    if (queuedMore) {
                        game._queued_messages_after_more ??= [];
                        game._queued_messages_after_more.unshift({ text: roomEntryText, more: false });
                    } else {
                        game._queued_message_after_more = roomEntryText;
                    }
                }
                if (blockTimeUntilClear && !queuedMore) {
                    game._pending_message_blocks_time = 1;
                    game._clear_pending_message_only_once = 1;
                    game._keep_pending_message = 1;
                }
                if (nextMoreLine) game._message_more_line = nextMoreLine;
                if (queuedMore && game._blind_arrival_objects_after_more
                    && /^You remember this level as /.test(next)) {
                    const rows = arrivalObjectListRows(game._blind_arrival_objects_after_more);
                    game._blind_arrival_objects_after_more = null;
                    game._blind_arrival_feel_rows_after_more = rows;
                }
                if (game._queued_message_process_time_after_more) {
                    game._queued_message_process_time_after_more = 0;
                    if (game._queued_message_pre_time_rn2) {
                        rn2(game._queued_message_pre_time_rn2);
                        game._queued_message_pre_time_rn2 = 0;
                    }
                    if (game._destroy_armor_clear_resume_after_more) {
                        game._destroy_armor_clear_resume_after_more = 0;
                        game._resume_time_after_more = 0;
                    }
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    game._process_time_with_more = 1;
                    game._continue_monsters_after_more = 1;
                    game._queued_more_continue_monsters = 1;
                }
	                if (finishedBreakChestContents && !queuedMore) {
                    game._continue_monsters_after_more = 1;
                    game._resume_time_after_more = 0;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                }
                if (queuedMore && game._call_scroll_after_queued_more) {
                    game._call_scroll_after_queued_more = 0;
                    game._command_mode = 'callScrollAfterMore';
                }
                if (queuedMore && game._fortune_cookie_rumor_after_more) {
                    game._queued_message_after_more = game._fortune_cookie_rumor_after_more;
                    game._fortune_cookie_rumor_after_more = '';
                }
                if (hadQueuedDead && /^The .+ picks up /.test(messageBeforeQueuedMore))
                    game._clear_pending_time_after_queued_dead_turn = 1;
                if (hadQueuedDead && !queuedMore && !game._clear_pending_time_after_queued_dead_turn) {
                    game._continue_monsters_after_more = 1;
                    game._resume_time_after_more = 1;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    if (game._run_steps_after_more > 0 || game._run_steps_remaining > 0)
                        game._resume_run_after_queued_dead_more = 1;
                }
                if (next === 'You die...' || next === 'You die.') {
                    if (!game._pending_time_passed) game._death_current_move = 0;
                    game._pending_time_passed = 0;
                    game.context.move = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    game._message_more = 1;
                    game._command_mode = 'deathDieMore';
                }
                if (hadQueuedDead && game._run_steps_after_more > 0) {
                    game._run_steps_remaining = Math.max(game._run_steps_remaining || 0, game._run_steps_after_more || 0);
                    game._run_steps_after_more = 0;
                    game._process_command_time_now = 1;
                }
                return;
            }
            if (game._read_confused_teleport_prompt_after_more) {
                game._read_confused_teleport_prompt_after_more = 0;
                game._level_teleport_confused_scroll = game._read_level_teleport_confused_prompt ? 1 : 0;
                game._read_level_teleport_confused_prompt = 0;
                await beginLevelTeleportTextPrompt();
                return;
            }
            if (game._read_teleport_cursor_prompt_after_more) {
                game._read_teleport_cursor_prompt_after_more = 0;
                startControlledTeleportPrompt();
                await setMessage('Where do you want to be teleported?');
                return;
            }
            if (game._pet_message_resume) {
                game._pending_message = '';
                game._message_more = 0;
                game.context.move = game._pending_time_passed ? 0 : 1;
                game._process_command_time_now = 1;
                return;
            }
            if (game._quest_arrival_look_here_after_more) {
                game._quest_arrival_look_here_after_more = 0;
                const message = currentObjectHereMessage();
                game._pending_message = '';
                game._message_more = 0;
                game._keep_pending_message = 0;
                if (message) {
                    await setMessage(message);
                    return;
                }
            }
            if (game._queued_priest_entry_after_more) {
                const priestRoomno = game._queued_priest_entry_after_more;
                game._queued_priest_entry_after_more = 0;
                game._pending_message = '';
                game._message_more = 0;
                const priestEntryText = tendedTemplePriestEntryText(priestRoomno);
                if (priestEntryText) {
                    await setMessage(priestEntryText);
                    return;
                }
            }
            const welcome = game._welcome_message;
            game._pending_message = '';
            game._message_more = 0;
            if (game._pet_kill_luck_message_after_more) {
                game._pending_message = game._pet_kill_luck_message_after_more;
                game._pet_kill_luck_message_after_more = '';
                game._keep_pending_message = 1;
                game._hallu_display_after_pet_kill_luck = 1;
            }
            if (game._redraw_level_after_more) {
                game._redraw_level_after_more = 0;
                vision_recalc(0);
                game._display_hallucinated_redraw = 1;
                await docrt();
                game._display_hallucinated_redraw = 0;
                await bot();
                await flush_screen(1);
            }
            if (game._clear_terrain_known_after_more) {
                game._terrain_known_only = 0;
                game._clear_terrain_known_after_more = 0;
            }
            if (game._reset_umovement_after_more) {
                game._reset_umovement_after_more = 0;
                game.u.umovement = 0;
                game._run_steps_remaining = 0;
            }
            if (game._pet_bear_trap_after_more) {
                const { mon, trap } = game._pet_bear_trap_after_more;
                game._pet_bear_trap_after_more = null;
                if (mon && (game.level?.monsters || []).includes(mon)) {
                    mon.mtrapseen = (mon.mtrapseen || 0) | (1 << (BEAR_TRAP - 1));
                    mon.knownTraps ??= [];
                    if (!mon.knownTraps.includes(BEAR_TRAP)) mon.knownTraps.push(BEAR_TRAP);
                    mon.mtrapped = 1;
                    if (trap) trap.tseen = true;
                    d(2, 4);
                    rn2(5);
                    newsym(mon.mx, mon.my);
                    await setMessage(`The ${mon.saddled ? 'saddled ' : ''}${mon.data?.name || 'creature'} is caught in a bear trap!`);
                    return;
                }
            }
            if (game._deferred_soldier_ant_poison_after_more) {
                const attack = game._deferred_soldier_ant_poison_after_more;
                game._deferred_soldier_ant_poison_after_more = null;
                rn2(30);
                const strengthDamage = d(2, 2);
                if (game.u?.acurr?.a)
                    game.u.acurr.a[0] = Math.max(3, (game.u.acurr.a[0] ?? 10) - strengthDamage);
                if (game.u && !(game.u._statusSuffix || '').includes('Burdened'))
                    game.u._statusSuffix = `${game.u._statusSuffix || ''} Burdened`;
                game._deferred_soldier_ant_damage_after_poison = attack;
                game._queued_messages_after_more = [
                    { text: 'Your movements are slowed slightly because of your load.', more: true },
                    ...(game._queued_messages_after_more || []),
                ];
                await setMessage("The soldier ant's sting was poisoned!", true);
                return;
            }
	            if (game._queued_monster_attacks_after_more?.length) {
	                const attack = game._queued_monster_attacks_after_more.shift();
	                rn2(8);
                rn2(3);
                rn2(6);
			                if (attack.damage && attack.damage >= (game.u?.uhp || 0)) {
			                    if ((game.u?.uhp || 0) - attack.damage === -1)
			                        attack.holdStatusHp = true;
		                    game._deferred_lethal_attack_after_more = attack;
		                    game._pending_message = attack.message;
		                    game._message_more = 1;
		                    game._process_time_with_more = 0;
		                    game._keep_pending_message = 1;
		                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
		                    return;
		                }
		                if (attack.damage) game.u.uhp = Math.max(0, (game.u?.uhp || 0) - attack.damage);
		                game._pending_message = attack.message;
	                game._message_more = game._queued_monster_attacks_after_more.length ? 1 : 0;
	                game._process_time_with_more = game._message_more ? 1 : 0;
	                game._keep_pending_message = 1;
                if (!game._message_more) game._pending_time_passed = Math.min(game._pending_time_passed || 0, 1);
	                return;
	            }
	            if (game._swallow_expel_after_more) {
	                const { mon } = game._swallow_expel_after_more;
	                game._swallow_expel_after_more = null;
	                await finishSwallowExpel(mon);
	                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
	                game.context.move = 0;
	                game._process_command_time_now = 1;
	                return;
	            }
	            if (game._deferred_soldier_ant_damage_after_poison) {
	                const attack = game._deferred_soldier_ant_damage_after_poison;
	                game._deferred_soldier_ant_damage_after_poison = null;
                rn2(3);
                rn2(6);
                if (attack.damage) game.u.uhp = Math.max(0, (game.u?.uhp || 0) - attack.damage);
                if ((game.u?.uhp || 0) <= 0) {
                    game._pending_time_passed = 0;
                    game.context.move = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    game._command_mode = 'deathDieMore';
                    prepareDeathBones();
                    await setMessage('You die...', true);
                    return;
                }
            }
            while (game._monster_hit_effects_after_more > 0) {
                rn2(3);
                rn2(6);
                game._monster_hit_effects_after_more--;
            }
            if (game._deferred_raven_blind_after_more) {
                const ravenAttack = game._deferred_raven_blind_after_more;
                game._deferred_raven_blind_after_more = null;
                const subject = ravenAttack.subject || 'It';
                let message = '';
                if (ravenAttack.toHit > rnd(21)) {
                    d(1, 6);
                    rn2(3);
                    rn2(6);
                    const wasBlind = !!game.u?.blind;
	                    if (game.u) {
	                        game.u.blind = true;
	                        game.u._blindTimeout = Math.max(game.u._blindTimeout || 0, 135);
	                    }
                    if (!wasBlind) {
                        vision_recalc(0);
                        await docrt();
                    }
                    if (!wasBlind && !game._suppress_monster_attack_messages) message = `${subject} blinds you!`;
                } else if (!game._suppress_monster_attack_messages) {
                    message = `${subject} misses!`;
                }
                if (message) {
                    game._pending_message = game._pending_message
                        ? `${game._pending_message}  ${message}`
                        : message;
                    game._keep_pending_message = 1;
                }
            }
            if ((game._monster_resume_index || game._monster_resume_same_index) && game._pending_time_passed > 0) {
                game.context.move = 0;
                game._process_command_time_now = 1;
                return;
            }
            game._welcome_message = 0;
            if (welcome) moveloopPreambleOnce();
            const tutorialAfterWelcome = welcome && !game.tutorial_set_in_config && !game._tutorial_prompt_done && game.flags?.legacy !== false;
            if (welcome && game.flags?.explore) {
                game._post_explore_tutorial_prompt = tutorialAfterWelcome ? 1 : 0;
                await setMessage('You are in non-scoring explore/discovery mode.', !!tutorialAfterWelcome);
                return;
            }
            if (welcome && game._calendar_messages?.length) {
                game._post_calendar_tutorial_prompt = tutorialAfterWelcome ? 1 : 0;
                const calendarMessage = game._calendar_messages.shift();
                await setMessage(calendarMessage, !!game._calendar_messages.length || !!game._post_calendar_tutorial_prompt);
                return;
            }
            if (game._calendar_messages?.length) {
                const calendarMessage = game._calendar_messages.shift();
                await setMessage(calendarMessage, !!game._calendar_messages.length || !!game._post_calendar_tutorial_prompt);
                return;
            }
            if (game._eating_turns_remaining > 0) {
                game.context.move = 1;
                return;
            }
            if (game._armor_wear_occupation?.turns > 0) {
                if (game._armor_wear_occupation.turns === 1 && game._resume_time_after_more) {
                    game._armor_tail_after_more = 1;
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    game.context.move = 0;
                    game._process_command_time_now = 1;
                    game._process_time_with_more = 1;
                    return;
                }
                if (!game._pending_time_passed) game.context.move = 1;
                return;
            }
            if (game._post_explore_tutorial_prompt) {
                game._post_explore_tutorial_prompt = 0;
                game._tutorial_prompt = 1;
                return;
            }
            if (game._post_calendar_tutorial_prompt) {
                game._post_calendar_tutorial_prompt = 0;
                game._tutorial_prompt = 1;
                return;
            }
            if (tutorialAfterWelcome) game._tutorial_prompt = 1;
            if (game._force_lock_occupation) {
                game.context.move = 1;
                game._process_command_time_now = 1;
                game._process_time_with_more = 1;
            }
            if (game._pick_dig_occupation) {
                game.context.move = 1;
                game._process_command_time_now = 1;
                game._process_time_with_more = 1;
            }
            if (game._tin_opening_occupation) {
                game.context.move = 1;
                game._process_command_time_now = 1;
                game._process_time_with_more = 1;
            }
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (
        ch === ' ' && game._pending_message && game._message_more
        && game._command_mode !== 'eatInvalidMore'
        && game._command_mode !== 'readInvalidMore'
        && game._command_mode !== 'chargeInvalidMore'
        && game._command_mode !== 'readInventoryMore'
        && game._command_mode !== 'throwInvalidMore'
        && game._command_mode !== 'wieldInvalidMore'
        && game._command_mode !== 'tinEatConfirm'
        && game._command_mode !== 'takeOffInvalidMore'
        && game._command_mode !== 'markerWriteInvalidMore'
        && game._command_mode !== 'engraveToolMore'
        && game._command_mode !== 'farlookIntroMore'
        && game._command_mode !== 'farlookInstructionsMore'
        && game._command_mode !== 'farlookDetailMore'
        && game._command_mode !== 'terrainKnownIntroMore'
        && game._command_mode !== 'travelIntroMore'
        && game._command_mode !== 'jumpIntroMore'
        && game._command_mode !== 'teleportIntroMore'
        && game._command_mode !== 'levelChangeMore'
        && game._command_mode !== 'questLeaderIntroMore'
        && game._command_mode !== 'questLeaderStatusMore'
        && game._command_mode !== 'questLeaderRejectMore'
        && game._command_mode !== 'messageWait'
        && game._command_mode !== 'confusedLevelTeleportOopsMore'
        && game._command_mode !== 'escapedMore'
        && game._command_mode !== 'ponySecondAttackMore'
        && game._command_mode !== 'ponyDamageMore'
        && game._command_mode !== 'deathSlipMore'
        && game._command_mode !== 'deathDieMore'
        && game._command_mode !== 'lavaDeathMore'
        && game._command_mode !== 'fountainDetectMore'
        && game._command_mode !== 'fountainDetectDoneMore'
        && game._command_mode !== 'prayerBeginMore'
        && game._command_mode !== 'prayerFinishMore'
        && game._command_mode !== 'prayerVoiceMore'
        && game._command_mode !== 'prayerArrogantMore'
        && game._command_mode !== 'terrainKnownDoneMore'
        && game._command_mode !== 'whatDoesIntro'
    ) {
        const welcome = game._welcome_message;
        game._pending_message = '';
        game._message_more = 0;
        if (game._clear_terrain_known_after_more) {
            game._terrain_known_only = 0;
            game._clear_terrain_known_after_more = 0;
        }
        game._welcome_message = 0;
        if (welcome) moveloopPreambleOnce();
        const tutorialAfterWelcome = welcome && !game.tutorial_set_in_config && !game._tutorial_prompt_done && game.flags?.legacy !== false;
        if (welcome && game.flags?.explore) {
            game._post_explore_tutorial_prompt = tutorialAfterWelcome ? 1 : 0;
            await setMessage('You are in non-scoring explore/discovery mode.', !!tutorialAfterWelcome);
            return;
        }
        if (welcome && game._calendar_messages?.length) {
            game._post_calendar_tutorial_prompt = tutorialAfterWelcome ? 1 : 0;
            const calendarMessage = game._calendar_messages.shift();
            await setMessage(calendarMessage, !!game._calendar_messages.length || !!game._post_calendar_tutorial_prompt);
            return;
        }
        if (game._calendar_messages?.length) {
            const calendarMessage = game._calendar_messages.shift();
            await setMessage(calendarMessage, !!game._calendar_messages.length || !!game._post_calendar_tutorial_prompt);
            return;
        }
        if (game._post_explore_tutorial_prompt) {
            game._post_explore_tutorial_prompt = 0;
            game._tutorial_prompt = 1;
            return;
        }
        if (game._post_calendar_tutorial_prompt) {
            game._post_calendar_tutorial_prompt = 0;
            game._tutorial_prompt = 1;
            return;
        }
        if (tutorialAfterWelcome) game._tutorial_prompt = 1;
        return;
    }

    if (game._command_mode === 'messageWait') {
        if (ch === ' ' || ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'confusedLevelTeleportOopsMore') {
        game._pending_message = '';
        game._message_more = 0;
        game._command_mode = null;

        const currentDepth = depth_of_level(game.u?.uz || { dnum: 0, dlevel: 1 });
        const targetDepth = randomTeleportDepth();
        if (targetDepth === currentDepth) {
            const followup = consumePendingLevelTeleportTrapFollowup();
            await setMessage(['You shudder for a moment.', followup].filter(Boolean).join('  '));
            game.context.move = 1;
            return;
        }

        rn2(19);
        game._deferred_level_goto = {
            targetLevel: levelTeleportNumericTarget(targetDepth),
            options: levelTeleportOptionsWithTrapFollowup({ levelTeleport: true }),
        };
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'escapedMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            setOverlay(escapedSummaryLines(), 24, true);
            game._command_mode = 'escapedSummaryMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'escapedSummaryMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const scoreRows = deathScoreLines();
            setOverlay(scoreRows, 24, true);
            game._overlay_cursor = [0, Math.min(23, Math.max(...scoreRows.map(([row]) => row)) + 1)];
            game._command_mode = 'dead';
            game.program_state ??= {};
            game.program_state.gameover = true;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'lavaDeathMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._polyself_lava_death_more = 0;
            game.u.uhp = 0;
            await setMessage('Do you want to see your attributes? [ynq] (n)');
            game._command_mode = 'deathAttributesPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathAttributesPrompt') {
        if (ch === 'y') {
            setOverlay(deathAttributesPage1(), 24, true);
            game._command_mode = 'deathAttributes';
            return;
        }
        if (deathSummary().dungeon === 'The Tutorial') {
            await setMessage('Do you want to see your conduct? [ynq] (n)');
            game._command_mode = 'deathConductPrompt';
            return;
        }
        await setMessage('Do you want an account of creatures vanquished? [ynaq] (n)');
        game._command_mode = 'deathVanquishedPrompt';
        return;
    }

    if (game._command_mode === 'deathAttributes') {
        if (ch === ' ') {
            setOverlay(deathAttributesPage2(), 24, true);
            game._command_mode = 'deathAttributes2';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathAttributes2') {
        if (ch === ' ') {
            if (game._death_attributes_page3_lines) {
                setOverlay(game._death_attributes_page3_lines, 24, true);
                game._death_attributes_page3_lines = null;
                game._command_mode = 'deathAttributes3';
                return;
            }
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            if (deathSummary().dungeon === 'The Tutorial') {
                await setMessage('Do you want to see your conduct? [ynq] (n)');
                game._command_mode = 'deathConductPrompt';
                return;
            }
            await setMessage('Do you want an account of creatures vanquished? [ynaq] (n)');
            game._command_mode = 'deathVanquishedPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathAttributes3') {
        if (ch === ' ') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            if (deathSummary().dungeon === 'The Tutorial') {
                await setMessage('Do you want to see your conduct? [ynq] (n)');
                game._command_mode = 'deathConductPrompt';
                return;
            }
            await setMessage('Do you want an account of creatures vanquished? [ynaq] (n)');
            game._command_mode = 'deathVanquishedPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathVanquishedPrompt') {
        if (ch === 'y') {
            setOverlay(deathVanquishedLines(), 7);
            game._command_mode = 'deathVanquished';
            return;
        }
        await setMessage('Do you want to see your conduct? [ynq] (n)');
        game._command_mode = 'deathConductPrompt';
        return;
    }

    if (game._command_mode === 'deathVanquished') {
        if (ch === ' ') {
            game._overlay_lines = null;
            await setMessage('Do you want to see your conduct? [ynq] (n)');
            game._command_mode = 'deathConductPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathConductPrompt') {
        if (ch === 'y') {
            const lines = deathConductLines();
            setOverlay(lines, Math.max(...lines.map(([line]) => line)) + 1);
            game._command_mode = 'deathConduct';
            return;
        }
        await setMessage('Do you want to see the dungeon overview? [ynq] (n)');
        game._command_mode = 'deathOverviewPrompt';
        return;
    }

    if (game._command_mode === 'deathConduct') {
        if (ch === ' ') {
            game._overlay_lines = null;
            await setMessage('Do you want to see the dungeon overview? [ynq] (n)');
            game._command_mode = 'deathOverviewPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathOverviewPrompt') {
        if (ch === 'y') {
            const lines = deathOverviewLines();
            setOverlay(lines, Math.max(...lines.map(([line]) => line)) + 1);
            game._command_mode = 'deathOverview';
            return;
        }
        setOverlay(deathGraveLines(), 24, true);
        game._command_mode = 'deathGrave';
        return;
    }

    if (game._command_mode === 'deathOverview') {
        if (ch === ' ') {
            setOverlay(deathGraveLines(), 24, true);
            game._command_mode = 'deathGrave';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathGrave') {
        if (ch === ' ') {
            setOverlay([[23, 0, '--More--']], 24, true);
            game._command_mode = 'deathBlankMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathBlankMore') {
        if (ch === ' ') {
            if (game.flags?.debug) {
                setOverlay([[1, 0, 'Since you were in wizard mode, the score list will not be checked.']], 24, true);
                game._overlay_cursor = [0, 2];
                game._command_mode = 'dead';
                return;
            }
	            const scoreRows = deathScoreLines();
	            setOverlay(scoreRows, 24, true);
	            game._overlay_cursor = [0, Math.min(23, Math.max(...scoreRows.map(([row]) => row)) + 1)];
	            game._command_mode = 'dead';
	            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathBonesReplacePrompt') {
        if (ch === 'y') {
            const bonesPath = game._death_bones_replace_path || '';
            if (bonesPath && vfsDeleteFile(bonesPath)) {
                game._death_bones_replace_confirmed = 1;
                game._command_mode = 'deathBonesPrompt';
            } else {
                await setMessage('Cannot unlink old bones.');
                game._command_mode = 'deathGrave';
                delete game._death_bones_replace_path;
                return;
            }
        } else if (ch === 'n' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            delete game._death_bones_replace_path;
            setOverlay(deathGraveLines(), 24, true);
            game._command_mode = 'deathGrave';
            return;
        } else {
            game._keep_pending_message = 1;
            return;
        }
    }

    if (game._command_mode === 'deathBonesPrompt') {
        if (ch === 'y' || ch === 'n' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (ch === 'y') {
                const uz = game.u?.uz || { dnum: 0, dlevel: 1 };
                const bonesPath = `/bones/${uz.dnum}:${uz.dlevel}`;
                if (!game._death_bones_replace_confirmed && vfsReadFile(bonesPath) !== null) {
                    if (game.flags?.debug) {
                        game._death_bones_replace_path = bonesPath;
                        await setMessage('Bones file already exists.  Replace it? [yn] (n)');
                        game._command_mode = 'deathBonesReplacePrompt';
                        return;
                    }
                    setOverlay(deathGraveLines(), 24, true);
                    game._command_mode = 'deathGrave';
                    return;
                }
                game._death_bones_replace_confirmed = 0;
                delete game._death_bones_replace_path;
                createDeathBonesRemains();
                curseDeathBonesInventory();
                createDeathBonesGhost();
                vfsWriteFile(bonesPath, encodeBonesLevel());
            }
            setOverlay(deathGraveLines(), 24, true);
            game._command_mode = 'deathGrave';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'dead') {
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'ponySecondAttackMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const second = game._pony_second_attack || {};
            const secondRoll = rnd(21);
            const secondHit = (second.targetAc ?? 10) + (second.petLevel ?? 2) > secondRoll;
            const targetName = second.targetName || 'creature';
            if (!secondHit) {
                rn2(3);
                rn2(5);
                game._pony_second_attack = null;
                game._command_mode = null;
                game._message_more = 0;
                game._keep_pending_message = 1;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                game._process_command_time_now = 1;
                return;
            }
            await setMessage(`The saddled pony bites the ${targetName}.`, true);
            game._command_mode = 'ponyDamageMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'ponyDamageMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const target = game._pony_second_attack?.target;
            const targetName = game._pony_second_attack?.targetName || 'creature';
            d(1, 2);
            rn2(3);
            rn2(6);
            rn2(3);
            rnd((target?.data?.mlevel ?? 0) + 1);
            rn2(5);
            if (target) {
                recordVanquished(target, false);
                game.level.monsters = (game.level?.monsters || []).filter(mon => mon !== target);
                newsym(target.mx, target.my);
            }
            game._pony_second_attack = null;
            await setMessage(`The ${targetName} is destroyed!`);
            game._command_mode = null;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathSlipMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            prepareDeathBones();
            await setMessage('You die...', true);
            game._command_mode = 'deathDieMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathDieMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_time_passed = 0;
            game.context.move = 0;
            game._process_command_time_now = 0;
            game._run_steps_remaining = 0;
            if (game.u) game.u.uhp = 0;
            game._death_status_hp_before_zero = null;
            const deathTurn = game.moves || 1;
            game._death_moves ||= game._death_current_move ? deathTurn : Math.max(1, deathTurn - 1);
            game._death_current_move = 0;
            if (game.flags?.debug) {
                await setMessage('Die? [yn] (n)');
                game._command_mode = 'wizardDieConfirm';
                return;
            }
            prepareDeathBones();
            saveDeathBonesIfNeeded();
            const discloseOptions = String(game.flags?.disclose || '').split(/\s+/);
            if (['-i', '-a', '-v', '-g', '-c', '-o'].every(option => discloseOptions.includes(option))) {
                setOverlay(deathGraveLines(), 24, true);
                game._command_mode = 'deathGrave';
                return;
            }
            await setMessage('Do you want your possessions identified? [ynq] (n)');
            game._command_mode = 'deathIdentify';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'wizardDieConfirm') {
        if (ch === 'y') {
            game._death_moves = game.moves || 1;
            game._bones_ok = canMakeBones();
            if (game._bones_ok) {
                createDeathBonesRemains();
                await setMessage('Save bones? [yn] (n)');
                game._command_mode = 'deathBonesPrompt';
                return;
            }
            setOverlay(deathGraveLines(), 24, true);
            game._command_mode = 'deathGrave';
            return;
        }
        if (ch === 'n' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game.u.uhp = game.u.uhpmax || 1;
            game._death_cause = '';
            game._death_bones_body = '';
            game._wizard_survived_death = 1;
            game._survived_death_count = (game._survived_death_count || 0) + 1;
            if (game._death_negative_level_teleport_escape_message) {
                const escapeMessage = game._death_negative_level_teleport_escape_message;
                game._death_negative_level_teleport_escape_message = '';
                game._death_outside_dungeon = '';
                game._death_no_bones = 0;
                await beginEscapedGame(["OK, so you don't die.", escapeMessage]);
                return;
            }
            await setMessage("OK, so you don't die.");
            if (game._deferred_raven_blind_after_more) {
                const ravenAttack = game._deferred_raven_blind_after_more;
                game._deferred_raven_blind_after_more = null;
                const subject = ravenAttack.subject || 'It';
                let message = '';
                if (ravenAttack.toHit > rnd(21)) {
                    d(1, 6);
                    rn2(3);
                    rn2(6);
                    const wasBlind = !!game.u?.blind;
	                    if (game.u) {
	                        game.u.blind = true;
	                        game.u._blindTimeout = Math.max(game.u._blindTimeout || 0, 135);
	                    }
                    if (!wasBlind) {
                        vision_recalc(0);
                        await docrt();
                    }
                    if (!wasBlind && !game._suppress_monster_attack_messages) message = `${subject} blinds you!`;
                } else if (!game._suppress_monster_attack_messages) {
                    message = `${subject} misses!`;
                }
                if (message) {
                    game._pending_message = `${game._pending_message}  ${message}`;
                    game._message_more = 1;
                    game._process_time_with_more = 1;
                }
                if (ravenAttack.resumeIndex != null) {
                    game._monster_resume_index = ravenAttack.resumeIndex;
                    game._monster_resume_somebody_can_move = ravenAttack.somebodyCanMove;
                }
            }
            game._pending_explore_lifesaving_message = 1;
            game._queued_explore_lifesaving_message = 1;
            game._queued_message_after_more = 'You survived that attempt on your life.';
            game._command_mode = null;
            if (game._prayer_pending_done) game._prayer_pending_done_delay = 3;
            game.context.move = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'deathIdentify') {
        if (ch === 'y') {
            game._death_inventory_page = 0;
            setOverlay(inventoryOverlayLines(0, true), 24, true);
            game._command_mode = 'deathInventory';
            return;
        }
        await setMessage('Do you want to see your attributes? [ynq] (n)');
        game._command_mode = 'deathAttributesPrompt';
        return;
    }

    if (game._command_mode === 'deathInventory') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (ch === ' ' && (game._death_inventory_page || 0) + 1 < (game._inventory_overlay_total_pages || 1)) {
                game._death_inventory_page++;
                setOverlay(inventoryOverlayLines(game._death_inventory_page, true), 24, true);
                return;
            }
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._death_inventory_page = 0;
            await setMessage('Do you want to see your attributes? [ynq] (n)');
            game._command_mode = 'deathAttributesPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'quaffFountainConfirm') {
        if (ch === 'y') {
            const fateRoll = rnd(30);
            const fate = fateRoll;
            if (fate < 10) {
                rnd(10);
                const dries = dryupFountainAt();
                await setMessage(dries
                    ? 'The cool draught refreshes you.  The fountain dries up!'
                    : 'The cool draught refreshes you.');
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fate === 21) {
                const strLoss = rn2(4) + 3;
                const damage = rnd(10);
                if (game.u) {
                    if (game.u.acurr?.a) game.u.acurr.a[A_STR] = Math.max(3, (game.u.acurr.a[A_STR] ?? 10) - strLoss);
                    game.u.uhp = Math.max(1, (game.u.uhp || 1) - damage);
                }
                exerciseAttribute(A_CON, false);
                const dries = dryupFountainAt();
                await setMessage(dries
                    ? 'The water is contaminated!  The fountain dries up!'
                    : 'The water is contaminated!');
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fate === 20) {
                rn2(20);
                const dries = dryupFountainAt();
                await setMessage(dries
                    ? 'The water is foul!  You gag and vomit.  The fountain dries up!'
                    : 'The water is foul!  You gag and vomit.', true);
                game._queued_message_after_more = 'You can move again.';
                game._process_time_with_more = 1;
                game._process_command_time_now = 1;
                game._helpless_time = 2;
                game._reset_umovement_after_more = 1;
                game._command_mode = null;
                game.context.move = 2;
                return;
            }
            if (fate === 22) {
                let num = rn2(5) + 2;
                while (num-- > 0) {
                    game._makemon_relocate_occupied_once = true;
                    const snake = await makemon(WATER_MOCCASIN, game.u?.ux || 0, game.u?.uy || 0, 0);
                    if (snake) newsym(snake.mx, snake.my);
                }
                const dries = dryupFountainAt();
                await setMessage(dries
                    ? 'An endless stream of snakes pours forth!  The fountain dries up!'
                    : 'An endless stream of snakes pours forth!');
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fate === 23) {
                const spots = [];
                for (let radius = 1; radius <= 3; radius++) {
                    const passStart = spots.length;
                    const lox = (game.u?.ux || 0) - radius;
                    const hix = (game.u?.ux || 0) + radius;
                    const loy = (game.u?.uy || 0) - radius;
                    const hiy = (game.u?.uy || 0) + radius;
                    for (let y = Math.max(loy, 0); y <= hiy && y < ROWNO; y++) {
                        for (let x = Math.max(lox, 1); x <= hix && x < COLNO; x++) {
                            if (x !== lox && x !== hix && y !== loy && y !== hiy) continue;
                            spots.push({ x, y });
                        }
                    }
                    for (let pass = passStart, n = spots.length - passStart; n > 1; pass++, n--) {
                        const k = rn2(n);
                        if (!k) continue;
                        const tmp = spots[pass];
                        spots[pass] = spots[pass + k];
                        spots[pass + k] = tmp;
                    }
                }
                const spot = spots.find(pos => {
                    const loc = game.level?.at(pos.x, pos.y);
                    return loc && !IS_OBSTRUCTED(loc.typ)
                        && !(game.level?.monsters || []).some(mon => mon.mx === pos.x && mon.my === pos.y)
                        && (pos.x !== game.u?.ux || pos.y !== game.u?.uy);
                }) || { x: (game.u?.ux || 0) + 1, y: game.u?.uy || 0 };
                const demon = await makemon(WATER_DEMON, spot.x, spot.y, 0);
                if (demon) {
                    demon.msleeping = 0;
                    newsym(demon.mx, demon.my);
                }
                rnd(100);
                const dries = dryupFountainAt();
                await setMessage(dries
                    ? 'You unleash a water demon!  The fountain dries up!'
                    : 'You unleash a water demon!');
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fate === 30) {
                const gushed = dogushforth();
                const dries = dryupFountainAt();
                const message = gushed
                    ? 'Water gushes forth from the overflowing fountain!'
                    : 'Water sprays all over you.';
                await setMessage(dries ? `${message}  The fountain dries up!` : message);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fate === 26) {
                game._fountain_detect_pending = { x: game.u?.ux || 0, y: game.u?.uy || 0 };
                game._detect_monsters_display = 1;
                await setMessage('You sense the presence of monsters.', true);
                game._command_mode = 'fountainDetectMore';
                return;
            }
            if (fate === 27) {
                const x = game.u?.ux || 0;
                const y = game.u?.uy || 0;
                const message = game.u?.blind
                    ? 'You feel a gem here!'
                    : 'You spot a gem in the sparkling waters!';
                let gemRoll = rnd(862);
                let gemKind = 'gem';
                let gemColor = NO_COLOR;
                for (const [prob, kind, color] of [
                    [2, 'dilithium crystal', CLR_WHITE], [3, 'diamond', CLR_WHITE],
                    [4, 'ruby', CLR_RED], [3, 'jacinth', CLR_ORANGE],
                    [4, 'sapphire', CLR_BLUE], [3, 'black opal', CLR_BLACK],
                    [5, 'emerald', CLR_GREEN], [6, 'turquoise', CLR_GREEN],
                    [4, 'citrine', CLR_YELLOW], [6, 'aquamarine', CLR_GREEN],
                    [8, 'amber', CLR_BROWN], [10, 'topaz', CLR_BROWN],
                    [6, 'jet', CLR_BLACK], [12, 'opal', CLR_WHITE],
                    [8, 'chrysoberyl', CLR_YELLOW], [12, 'garnet', CLR_RED],
                    [14, 'amethyst', CLR_MAGENTA], [15, 'jasper', CLR_RED],
                    [15, 'fluorite', CLR_MAGENTA], [9, 'obsidian', CLR_BLACK],
                    [12, 'agate', CLR_ORANGE], [10, 'jade', CLR_GREEN],
                    [77, 'worthless piece of white glass', CLR_WHITE],
                    [77, 'worthless piece of blue glass', CLR_BLUE],
                    [77, 'worthless piece of red glass', CLR_RED],
                    [77, 'worthless piece of yellowish brown glass', CLR_BROWN],
                    [76, 'worthless piece of orange glass', CLR_ORANGE],
                    [77, 'worthless piece of yellow glass', CLR_YELLOW],
                    [76, 'worthless piece of black glass', CLR_BLACK],
                    [77, 'worthless piece of green glass', CLR_GREEN],
                    [77, 'worthless piece of violet glass', CLR_MAGENTA],
                ]) {
                    gemRoll -= prob;
                    if (gemRoll > 0) continue;
                    gemKind = kind;
                    gemColor = color === CLR_BLACK ? NO_COLOR : color;
                    break;
                }
                const gemId = next_ident();
                game.level?.objects?.push({
                    id: gemId,
                    otyp: GEM_CLASS,
                    cls: 'gem',
                    glyph: '*',
                    ox: x,
                    oy: y,
                    color: gemColor,
                    _display_color: gemColor,
                    kind: gemKind,
                });
                const loc = game.level?.at(x, y);
                if (loc) loc.fountainLooted = true;
                newsym(x, y);
                exerciseAttribute(A_WIS, true);
                const dries = dryupFountainAt(x, y);
                await setMessage(dries ? `${message}  The fountain dries up!` : message);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            await setMessage(dryupFountainAt()
                ? 'This tepid water is tasteless.  The fountain dries up!'
                : 'This tepid water is tasteless.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'quaffSinkConfirm') {
        if (ch === 'y') {
            const sinkRoll = rn2(20);
            let message = 'You take a sip of warm water.';
            let moves = 1;
            if (sinkRoll === 0) {
                message = 'You take a sip of very cold water.';
            } else if (sinkRoll === 1) {
                message = 'You take a sip of very warm water.';
            } else if (sinkRoll === 2) {
                message = 'You take a sip of scalding hot water.';
                game.u.uhp = Math.max(1, (game.u?.uhp || 1) - rnd(6));
            } else if (sinkRoll === 8) {
                message = 'Yuk, this water tastes awful.';
            } else if (sinkRoll === 9) {
                const con = game.u?.acurr?.a?.[4] ?? 10;
                if (game.u) game.u.uhunger = (game.u.uhunger ?? 900) - (11 + rn2(Math.max(1, 30 - con)));
                game._helpless_time = Math.max(game._helpless_time || 0, 2);
                game._wake_message = 'You can move again.';
                message = 'Gaggg... this tastes like sewage!  You vomit.';
                moves = 2;
            } else if (sinkRoll === 11) {
                message = 'You hear clanking from the pipes...';
            } else if (sinkRoll === 12) {
                message = 'You hear snatches of song from among the sewers...';
            } else if (sinkRoll === 13) {
                message = 'Ew, what a stench!';
            } else if (sinkRoll < 3 || sinkRoll > 13) {
                const temp = rn2(3) ? (rn2(2) ? 'cold' : 'warm') : 'hot';
                message = `You take a sip of ${temp} water.`;
            }
            await setMessage(message);
            game._command_mode = null;
            game.context.move = moves;
            return;
        }
        const letters = inventoryLetters(item => item.cls === 'potion' || item.otyp === POTION_CLASS);
        if (letters) {
            await setMessage(`What do you want to drink? [${getobjPromptLetters(letters)} or ?*]`);
            game._command_mode = 'quaffObject';
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'fountainDetectMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (!game._farlook_tip_seen) {
                game._farlook_tip_seen = 1;
                setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
                game._command_mode = 'fountainDetectTip';
                return;
            }
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._fountain_detect_x = game.u?.ux ?? 1;
            game._fountain_detect_y = game.u?.uy ?? 0;
            await setMessage("(For instructions type a '?')  Move cursor to monster of interest:");
            game._command_mode = 'fountainDetectPos';
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'fountainDetectTip') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._fountain_detect_x = game.u?.ux ?? 1;
            game._fountain_detect_y = game.u?.uy ?? 0;
            await setMessage("(For instructions type a '?')  Move cursor to monster of interest:");
            game._command_mode = 'fountainDetectPos';
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'fountainDetectPos') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Done.', true);
            game._command_mode = 'fountainDetectDoneMore';
            game._keep_pending_message = 1;
            return;
        }
        const dir = movementDirection(ch);
        if (dir) {
            const steps = ch !== ch.toLowerCase() ? 8 : 1;
            game._fountain_detect_x = Math.max(1, Math.min(COLNO - 1, (game._fountain_detect_x || game.u?.ux || 1) + dir.dx * steps));
            game._fountain_detect_y = Math.max(0, Math.min(ROWNO - 1, (game._fountain_detect_y || game.u?.uy || 0) + dir.dy * steps));
        }
        const monster = (game.level?.monsters || [])
            .find(mon => mon.mx === game._fountain_detect_x && mon.my === game._fountain_detect_y);
        if (monster) {
            const prefix = monster.pet || monster.mpeaceful ? 'peaceful ' : '';
            const name = monster.isshk && monster.shknam ? monster.shknam : monster.data?.name || 'monster';
            const suffix = monster.appearObj != null || monster.appearGlyph ? ', mimicking something' : '';
            await setMessage(`${prefix}${name}${suffix}`);
        } else {
            await setMessage('unexplored area');
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'fountainDetectDoneMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._detect_monsters_display = 0;
            let dried = false;
            if (game._fountain_detect_pending) {
                rn2(19);
                const pos = game._fountain_detect_pending;
                game._fountain_detect_pending = null;
                const loc = game.level?.at(pos.x, pos.y);
                if (loc?.typ === FOUNTAIN && !rn2(3)) {
                    loc.typ = ROOM;
                    if (game.level?.flags?.nfountains) game.level.flags.nfountains--;
                    newsym(pos.x, pos.y);
                    dried = true;
                }
            }
            await setMessage(dried ? 'The fountain dries up!' : 'Done.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'prayerBeginMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Force the gods to be pleased? [yn] (n)');
            game._command_mode = 'prayerForceConfirm';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'prayerForceConfirm') {
        if (ch === 'y') {
            if (game.u) {
                game.u.ublesscnt = 0;
                if ((game.u.uluck || 0) < 0) game.u.uluck = 0;
                if (game.u.ualign && (game.u.ualign.record || 0) <= 0)
                    game.u.ualign.record = 1;
                game.u.ugangr = 0;
            }
            const nearbyTrouble = (game.level?.monsters || []).some(mon => {
                if (mon.pet || mon.mpeaceful || mon.mundetected) return false;
                const distance = Math.max(Math.abs((mon.mx || 0) - (game.u?.ux || 0)), Math.abs((mon.my || 0) - (game.u?.uy || 0)));
                return distance <= 1 || ((mon.data?.mmove ?? NORMAL_SPEED) > NORMAL_SPEED && distance <= 8);
            });
            const blindPrayer = !!game.u?.blind;
            if (nearbyTrouble || !blindPrayer) {
                await setMessage(nearbyTrouble
                    ? 'You are surrounded by a shimmering light.'
                    : 'You are surrounded by a shimmering light.  You finish your prayer.', true);
            } else {
                game._pending_message = '';
                game._message_more = 0;
                game._keep_pending_message = 0;
            }
            game._command_mode = 'prayerFinishMore';
            game._prayer_debug_pleased = 1;
            game._prayer_occupation = 1;
            game._prayer_pending_done = 1;
            game._pending_prayer_finish_message = 1;
            game._prayer_split_finish_message = nearbyTrouble || blindPrayer ? 1 : 0;
            game._prayer_split_waiting_for_time = nearbyTrouble ? 1 : 0;
            game._prayer_split_remaining_time = nearbyTrouble ? 3 : 0;
            if (game.u) game.u.uinvulnerable = true;
            game._process_command_time_now = 1;
            game._process_time_with_more = 1;
            if (nearbyTrouble) {
                game._resume_time_after_more = 1;
                game._monster_turns_started = 1;
            } else {
                game._prayer_process_time_now = 1;
                game._prayer_ignore_invulnerable_attack_messages = 1;
                const level = game.u?.ulevel || 1;
                const hpDivisor = level <= 5 ? 5 : level <= 13 ? 6 : level <= 21 ? 7 : level <= 29 ? 8 : 9;
                const hp = game.u?.uhp || 0;
                game._prayer_force_pleased_heal = hp <= 5 || hp * hpDivisor <= (game.u?.uhpmax || 0) ? 1 : 0;
                game._prayer_energy_before_time = game.u?.uen ?? 0;
            }
            game.context.move = nearbyTrouble ? 1 : 3;
            return;
        }
        if (ch !== 'n' && ch !== ' ' && ch !== '\x1b' && ch !== '\r' && ch !== '\n') {
            game._keep_pending_message = 1;
            return;
        }
        const adjacentHostile = (game.level?.monsters || []).some(mon => !mon.pet && !mon.mpeaceful && !mon.mundetected
            && Math.max(Math.abs((mon.mx || 0) - (game.u?.ux || 0)), Math.abs((mon.my || 0) - (game.u?.uy || 0))) <= 1);
        const nearbyTrouble = adjacentHostile || (game.level?.monsters || []).some(mon => !mon.pet && !mon.mpeaceful && !mon.mundetected
            && (mon.data?.mmove ?? 12) > 1
            && Math.max(Math.abs((mon.mx || 0) - (game.u?.ux || 0)), Math.abs((mon.my || 0) - (game.u?.uy || 0))) <= 8);
        if (nearbyTrouble) {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game._prayer_nearby_trouble = 1;
        } else {
            await setMessage(`You finish your prayer.  The voice of ${game._prayer_god || 'your god'} thunders: `, true);
            game._command_mode = 'prayerVoiceMore';
            game._pending_prayer_finish_message = 1;
        }
        game._prayer_too_soon = (game.u?.ublesscnt || 0) > 0 ? 1 : 0;
        game._prayer_occupation = 1;
        game._prayer_pending_done = 1;
        game._process_command_time_now = 1;
        game._process_time_with_more = 1;
        game.context.move = nearbyTrouble && (game.u?.umovement ?? 0) >= NORMAL_SPEED ? 4 : 3;
        return;
    }

    if (game._command_mode === 'prayerFinishMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (game._prayer_debug_pleased && game._prayer_split_finish_message
                && game._prayer_split_waiting_for_time && game._prayer_split_remaining_time > 0) {
                game._prayer_split_waiting_for_time = 0;
                game._pending_message = '';
                game._message_more = 0;
                for (const mon of game.level?.monsters || []) {
                    if (!mon._split_prayer_deferred_hide_message) continue;
                    const message = mon._split_prayer_deferred_hide_message;
                    mon._split_prayer_deferred_hide_message = '';
                    mon.mundetected = true;
                    newsym(mon.mx, mon.my);
                    game._pending_message = game._pending_message
                        ? `${game._pending_message}  ${message}`
                        : message;
                    game._keep_pending_message = 1;
                }
                game._pending_time_passed = Math.max(
                    game._pending_time_passed || 0,
                    game._prayer_split_remaining_time,
                );
                game._prayer_split_remaining_time = 0;
                game._process_command_time_now = 1;
                game._process_time_with_more = 1;
                game._keep_pending_message = 1;
                return;
            }
            game._pending_prayer_finish_message = 0;
            if (game._prayer_debug_pleased) {
                game._prayer_debug_pleased = 0;
                const forceHeal = !!game._prayer_force_pleased_heal;
                const pleasedMessage = game._prayer_split_finish_message || forceHeal ? 'pleased' : 'satisfied';
                game._prayer_split_finish_message = 0;
                game._prayer_split_waiting_for_time = 0;
                game._prayer_ignore_invulnerable_attack_messages = 0;
                if (game.u) game.u.uinvulnerable = false;
                rn2(2);
                let message = `You feel that ${game._prayer_god || 'your god'} is ${pleasedMessage}.`;
                if (forceHeal && game.u) {
                    game.u.uhpmax = (game.u.uhpmax || 0) + rnd(5);
                    game.u.uhp = game.u.uhpmax;
                    message += '  You feel much better.';
                }
                if (!forceHeal && fixPrayerPunishmentTrouble())
                    message += '  Your chain disappears.';
                if (!forceHeal && !game._prayer_split_finish_message
                    && game.u?.ualign && (game.u.ualign.record || 0) < 2)
                    game.u.ualign.record++;
                game._prayer_force_pleased_heal = 0;
                if (game.u) game.u.ublesscnt = rnz(350);
                if (game._defer_seer_after_prayer_pleased) {
                    game._defer_seer_after_prayer_pleased = 0;
                    if ((game.moves || 0) >= (game.context?.seer_turn || 0))
                        game.context.seer_turn = (game.moves || 0) + rn2(31) + 15;
                }
                await setMessage(message);
                game._command_mode = null;
                return;
            }
	            if (game._prayer_angry_after_more) {
	                game._prayer_angry_after_more = 0;
	                const angryResult = game._prayer_angry_result || 0;
	                game._prayer_angry_result = 0;
	                if (angryResult === 2 || angryResult === 3) {
	                    await setMessage(`The voice of ${game._prayer_god || 'your god'} rings out: `, true);
	                    game._command_mode = 'prayerVoiceMore';
	                    return;
	                }
	                if (!game._prayer_angry_effects_done) {
	                    const timeout = rnz(300);
	                    if (timeout > (game.u?.ublesscnt || 0)) game.u.ublesscnt = timeout;
	                }
	                game._prayer_angry_effects_done = 0;
	                await setMessage(`You feel that ${game._prayer_god || 'your god'} is displeased.`);
	                game._command_mode = null;
	                return;
            }
            await setMessage(`The voice of ${game._prayer_god || 'your god'} rings out: `, true);
            game._command_mode = 'prayerVoiceMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

	    if (game._command_mode === 'prayerVoiceMore') {
	        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
	            game._pending_prayer_finish_message = 0;
	            if (!game._prayer_angry_effects_done) {
		                if (game.u?.acurr?.a) game.u.acurr.a[2] = Math.max(3, (game.u.acurr.a[2] || 4) - 1);
	                loseExperienceLevel();
	                game._prayer_angry_effects_done = 1;
	            }
	            await setMessage('"Thou art arrogant, mortal."  "Thou must relearn thy lessons!"', true);
	            game._command_mode = 'prayerArrogantMore';
	            return;
        }
        game._keep_pending_message = 1;
        return;
    }

	    if (game._command_mode === 'prayerArrogantMore') {
	        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
	            if (!game._prayer_angry_timeout_done) rnz(300);
	            game._prayer_angry_effects_done = 0;
	            game._prayer_angry_timeout_done = 0;
	            await setMessage('You feel foolish!');
            game._command_mode = null;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'wearObject') {
        const item = (game.inventory || []).find(invItem => invItem.letter === ch && invItem.cls === 'armor');
        if (item?.worn || item?.line?.includes('being worn')) {
            await setMessage('You are already wearing that!');
            game._command_mode = null;
            game.context.move = 0;
            return;
        }
        if (item) {
            const baseName = String(item.line || `${ch} - ${pickupObjectName(item)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \((?:being worn|weapon|alternate weapon).*$/, '');
            const spe = `${(item.spe ?? 0) >= 0 ? '+' : ''}${item.spe ?? 0}`;
            const bareName = baseName.replace(/^(an?|the) /, '');
            const wornName = /[+-]\d/.test(baseName)
                ? baseName
                : item.noArticle ? `${spe} ${bareName}` : `a ${spe} ${bareName}`;
            const acBonus = (ARMOR_AC_BONUS[String(item.kind || '').toLowerCase()] ?? 0) + (item.spe ?? 0);
            const kind = String(item.kind || '').toLowerCase();
            const delay = ARMOR_WEAR_DELAY[kind] || (/dragon scales?/.test(kind) ? 5 : 0);
            if (delay) {
                const alreadyFast = !!(game.u?.fast || game.u?.veryfast);
                item.worn = true;
                item.line = `${ch} - ${wornName} (being worn)`;
                if (game.u) game.u.uac = (game.u.uac ?? 10) - acBonus;
                updateGauntletsOfPowerStrength(kind, true);
                updateWornDisplacement();
                if (isSilverDragonArmorKind(kind) && game.u) game.u.reflecting = true;
                if (kind === 'shield of reflection' && game.u) game.u.reflecting = true;
                if (kind === 'speed boots' && game.u) {
                    game.u.veryfast = true;
                    syncHeroSpeedState();
                }
                game._armor_wear_occupation = {
                    itemLetter: item.letter || ch,
                    kind,
                    alreadyFast,
                    acBonus,
                    wornLine: `${ch} - ${wornName} (being worn)`,
                    reflecting: isSilverDragonArmorKind(kind) || kind === 'shield of reflection',
                    wearApplied: true,
                    wearAt: Math.max(0, delay - 2),
                    turns: delay,
                };
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            item.worn = true;
            item.line = `${ch} - ${wornName} (being worn)`;
            if (game.u) game.u.uac = (game.u.uac ?? 10) - acBonus;
            updateWornDisplacement();
            if (isSilverDragonArmorKind(kind) && game.u) game.u.reflecting = true;
            if (kind === 'shield of reflection' && game.u) game.u.reflecting = true;
            if (kind === 'cloak of displacement') {
                item.known = true;
                recordKnownArmorDiscovery(kind, false);
                if (game.u) {
                    game._status_uac_before_more = (game.u.uac ?? 10) + acBonus;
                    game._status_uac_before_more_seen = 0;
                }
                game._queued_message_after_more = 'You are now wearing a cloak of displacement.';
                exerciseAttribute(A_CON, true);
                game._exercise_turn_offset = -1;
                await setMessage('You feel that monsters have difficulty pinpointing your location.', true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            await setMessage(`You are now wearing ${baseName}.`);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const amulet = (game.inventory || []).find(invItem => invItem.letter === ch && invItem.cls === 'amulet');
        if (amulet) {
            amulet.worn = true;
            const baseName = String(amulet.line || `${ch} - ${pickupObjectName(amulet)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \(being worn\).*$/, '');
            amulet.line = `${ch} - ${baseName} (being worn)`;
            await setMessage(`${amulet.line}.`);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const accessory = (game.inventory || []).find(invItem => invItem.letter === ch
            && ['blindfold', 'towel', 'lenses', 'meat ring'].includes(invItem.kind || invItem.actualKind));
        if (accessory) {
            accessory.worn = true;
            const baseName = String(accessory.line || `${ch} - ${pickupObjectName(accessory)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \(being worn\).*$/, '');
            accessory.line = `${ch} - ${baseName} (being worn)`;
            const accessoryKind = accessory.kind || accessory.actualKind;
            if (['blindfold', 'towel'].includes(accessoryKind) && game.u) {
                game.u.blind = true;
                await docrt();
                await setMessage(`You are now wearing ${baseName}.  You can't see any more.`);
            } else {
                await setMessage(`${accessory.line}.`);
            }
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        await setMessage("You don't have that object.");
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'takeOffInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(game._take_off_prompt || 'What do you want to take off? [*]');
            game._command_mode = 'takeOffObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'takeOffObject') {
        const item = (game.inventory || []).find(invItem =>
            invItem.letter === ch
            && (invItem.worn || invItem.line?.includes('being worn'))
            && (invItem.cls === 'armor'
                || ['blindfold', 'towel', 'lenses'].includes(invItem.kind || invItem.actualKind)));
        game._command_mode = null;
        if (!item) {
            await setMessage("You don't have that object.", true);
            game._command_mode = 'takeOffInvalidMore';
            return;
        }
        const takeOffPrompt = game._take_off_prompt || game._pending_message || '';
        game._take_off_prompt = '';
        if (item.cursed) {
            item.bknown = true;
            const pair = /(?:boots|gloves|lenses)$/.test(pickupObjectName(item));
            await setMessage(`You can't.  ${pair ? 'They are' : 'It is'} cursed.`);
            game.context.move = 0;
            return;
        }
        const baseName = String(item.line || `${item.letter || '?'} - ${pickupObjectName(item)}`)
            .replace(/^[a-zA-Z] - /, '')
            .replace(/ \(being worn\).*$/, '');
        const accessoryKind = item.kind || item.actualKind;
        if (['blindfold', 'towel'].includes(accessoryKind) && game.u) {
            item.worn = false;
            item.line = `${item.letter || '?'} - ${baseName}`;
            game.u.blind = false;
            await docrt();
            await setMessage(`You were wearing ${baseName}.  You can see again.`, true);
            game.context.move = 1;
            game._process_time_with_more = 1;
            return;
        }
        const acBonus = (ARMOR_AC_BONUS[String(item.kind || '').toLowerCase()] ?? 0) + (item.spe ?? 0);
        const kind = String(item.kind || '').toLowerCase();
        const delay = ARMOR_WEAR_DELAY[kind] || (/dragon scales?/.test(kind) ? 5 : 0);
        const preservePromptOnly = takeOffPrompt.includes('[cdef') && ch === 'e' && kind === 'small shield';
        if (delay) {
            const simpleName = pickupObjectName(item);
            game._armor_wear_occupation = {
                action: 'takeoff',
                itemLetter: item.letter || ch,
                kind,
                baseName,
                simpleName: / mail$/.test(simpleName) ? 'mail' : simpleName,
                acBonus,
                turns: delay,
            };
            game.context.move = 1;
            return;
        }
        item.worn = false;
        item.line = `${item.letter || '?'} - ${baseName}`;
        if (game.u) {
            game._status_uac_before_more = game.u.uac ?? 10;
            game._status_uac_before_more_seen = 0;
            game.u.uac = (game.u.uac ?? 10) + acBonus;
        }
        if ((kind === 'speed boots' || isBlueDragonArmorKind(kind)) && game.u)
            syncHeroSpeedState();
        updateGauntletsOfPowerStrength(kind, false);
        updateWornDisplacement();
        if (preservePromptOnly) {
            await setMessage(takeOffPrompt);
        } else {
            await setMessage(`You were wearing ${baseName}.`);
            game._process_time_with_more = 1;
        }
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'quaffObject') {
        if (ch === '?') {
            const rows = [[0, 39, 'Potions', 1]];
            let row = 1;
            for (const item of game.inventory || []) {
                if (item.cls !== 'potion' && item.otyp !== POTION_CLASS) continue;
                const name = item.line || `${item.letter || '?'} - ${item.quan > 1 ? `${item.quan} ` : ''}${pickupObjectName(item)}`;
                rows.push([row++, 39, name]);
                if (row >= 23) break;
            }
            rows.push([row, 39, '(end)']);
            setOverlay(rows, rows.length);
            return;
        }
        const item = (game.inventory || []).find(invItem =>
            invItem.letter === ch && (invItem.cls === 'potion' || invItem.otyp === POTION_CLASS));
        game._command_mode = null;
        if (!item) {
            await setMessage('Never mind.');
            return;
        }
        removeInventoryItem(item);
        const potionName = String(item.actualKind
            || (item.potionIndex != null ? `potion of ${IDENTIFIED_POTION_NAMES[item.potionIndex]}` : '')
            || item.kind
            || pickupObjectName(item));
        if (potionName.includes('fruit juice') || potionName.includes('slime mold')) {
            if (!item.actualKind && item.kind?.endsWith?.(' potion')) {
                game._call_potion_appearance = item.kind.replace(/ potion$/, '');
                game._call_potion_record_discovery = 0;
                game._command_mode = 'callPotionAfterMore';
            }
            await setMessage(`This tastes like ${currentFruitJuiceName()}.`, true);
            return;
        }
        if (potionName.includes('sickness')) {
            learnObjectScore('Potions', 'potion of sickness');
            if (item.blessed) {
                game._queued_message_after_more = `(But in fact it was mildly stale ${currentFruitJuiceName()}.)`;
                game._potion_sickness_after_more = 1;
                await setMessage('Yecch!  This stuff tastes like poison.', true);
                game.context.move = 1;
                return;
            }
            rn2(6);
            rn2(4);
            const damage = rnd(10) + 5 * (item.cursed ? 1 : 0);
            if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - damage);
            rn2(19);
            await setMessage('Yecch!  This stuff tastes like poison.');
            game.context.move = 1;
            return;
        }
        if (potionName.includes('confusion')) {
            const confusionDuration = rn2(7) + (16 - 8 * (item.blessed ? 1 : item.cursed ? -1 : 0));
            rn2(19);
            learnObjectScore('Potions', 'potion of confusion');
            if (game.u) {
                if (!(game.u._statusSuffix || '').includes('Conf'))
                    game.u._statusSuffix = `${game.u._statusSuffix || ''} Conf`;
                game.u._confusionTimeout = (game.u._confusionTimeout || 0) + confusionDuration;
            }
            await setMessage('Huh, What?  Where am I?');
            game.context.move = 1;
            return;
        }
        if (potionName.includes('paralysis')) {
            const hasFreeAction = (game.inventory || []).some(invItem =>
                invItem.worn && (invItem.actualKind === 'ring of free action' || invItem.ringRoll === 20));
            learnObjectScore('Potions', 'potion of paralysis');
            recordKnownPotionDiscovery(item, 'paralysis');
            if (hasFreeAction) {
                await setMessage('You stiffen momentarily.');
                game.context.move = 1;
                return;
            }
            const baseTurns = item.blessed ? 13 : item.cursed ? 37 : 25;
            const helplessTurns = baseTurns + rn2(10);
            rn2(2);
            rn2(19);
            game._helpless_time = Math.max(game._helpless_time || 0, helplessTurns);
            game._wake_message = 'You can move again.';
            await setMessage('Your feet are frozen to the floor!');
            game.context.move = helplessTurns;
            return;
        }
        if (potionName.includes('booze')) {
            const confusionDuration = item.blessed ? 0 : d(3, 8);
            rn2(2);
            if (game.u && confusionDuration) {
                if (!(game.u._statusSuffix || '').includes('Conf'))
                    game.u._statusSuffix = `${game.u._statusSuffix || ''} Conf`;
                game.u._confusionTimeout = (game.u._confusionTimeout || 0) + confusionDuration;
            }
            let callPotion = false;
            if (!item.actualKind && item.kind?.endsWith?.(' potion')) {
                game._call_potion_appearance = item.kind.replace(/ potion$/, '');
                game._call_potion_record_discovery = 0;
                game._command_mode = 'callPotionAfterMore';
                callPotion = true;
            }
            await setMessage('Ooph!  This tastes like liquid fire!', true);
            if (!callPotion) game.context.move = 1;
            return;
        }
        if (potionName.includes('extra healing')) {
            const bcsign = item.blessed ? 1 : item.cursed ? -1 : 0;
            const wasBlind = !!game.u?.blind;
            const rolledAmount = 16 + d(4 + 2 * bcsign, 8);
            const amount = rolledAmount;
            if (game.u) {
                const oldMax = game.u.uhpmax || game.u.uhp || 1;
                const healedHp = (game.u.uhp || 1) + amount;
                if (healedHp > oldMax) {
                    game.u.uhpmax = oldMax + (item.blessed ? 5 : item.cursed ? 0 : 2);
                    game.u.uhp = game.u.uhpmax;
                } else {
                    game.u.uhp = healedHp;
                }
                game.u.blind = false;
                game.u._blindTimeout = 0;
                game.u.ucreamed = 0;
                if (wasBlind) {
                    vision_recalc(0);
                    const objects = game.level?.objects;
                    if (objects?.includes(game.u.uchain)) {
                        objects.splice(objects.indexOf(game.u.uchain), 1);
                        objects.push(game.u.uchain);
                    }
                    if (objects?.includes(game.u.uball)) {
                        objects.splice(objects.indexOf(game.u.uball), 1);
                        objects.push(game.u.uball);
                    }
                }
            }
            recordKnownPotionDiscovery(item, 'extra healing');
            learnObjectScore('Potions', 'potion of extra healing');
            rn2(19);
            rn2(19);
            rn2(19);
            if (wasBlind) await docrt();
            await setMessage(`You feel much better.${wasBlind ? '  You can see again.' : ''}`);
            game._resume_time_after_more = 0;
            game.context.move = 1;
            return;
        }
        if (potionName.includes('healing')) {
            const bcsign = item.blessed ? 1 : item.cursed ? -1 : 0;
            const amount = 8 + d(4 + 2 * bcsign, 4);
            const wasBlind = !!game.u?.blind && !item.cursed;
            if (game.u) {
                const oldMax = game.u.uhpmax || game.u.uhp || 1;
                const healedHp = (game.u.uhp || 1) + amount;
                if (healedHp > oldMax) {
                    game.u.uhpmax = oldMax + (item.cursed ? 0 : 1);
                    game.u.uhp = game.u.uhpmax;
                } else {
                    game.u.uhp = healedHp;
                }
                if (!item.cursed) {
                    game.u.blind = false;
                    game.u._blindTimeout = 0;
                    game.u.ucreamed = 0;
                    if (wasBlind) {
                        vision_recalc(0);
                        const objects = game.level?.objects;
                        if (objects?.includes(game.u.uchain)) {
                            objects.splice(objects.indexOf(game.u.uchain), 1);
                            objects.push(game.u.uchain);
                        }
                        if (objects?.includes(game.u.uball)) {
                            objects.splice(objects.indexOf(game.u.uball), 1);
                            objects.push(game.u.uball);
                        }
                    }
                }
            }
            learnObjectScore('Potions', 'potion of healing');
            rn2(19);
            if (wasBlind) await docrt();
            await setMessage(`You feel better.${wasBlind ? '  You can see again.' : ''}`);
            game.context.move = 1;
            return;
        }
        await setMessage(item.roll >= 900 ? 'That was smooth!' : 'You feel somewhat dizzy.');
        if (item.roll >= 900) rn2(2);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'putOnObject') {
        const item = (game.inventory || []).find(invItem => invItem.letter === ch && invItem.cls === 'ring');
        if (item) {
            if (game.u?._monsterMove === 0) {
                await setMessage('You cannot make the ring stick to your body.');
                game._command_mode = null;
                game.context.move = 0;
                return;
            }
            game._puton_ring_letter = ch;
            await setMessage('Which ring-finger, Right or Left? [rl]');
            game._command_mode = 'ringFinger';
            return;
        }
        const amulet = (game.inventory || []).find(invItem => invItem.letter === ch && invItem.cls === 'amulet');
        if (amulet) {
            amulet.worn = true;
            if (amulet.amuletIndex === 3 && game.u) {
                const newNap = rnd(98) + 2;
                const oldNap = game.u.sleepyTimeout || 0;
                if (!oldNap || newNap < oldNap) game.u.sleepyTimeout = newNap;
            }
            const baseName = String(amulet.line || `${ch} - ${pickupObjectName(amulet)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \(being worn\).*$/, '');
            amulet.line = `${ch} - ${baseName} (being worn)`;
            await setMessage(`${amulet.line}.`);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const accessory = (game.inventory || []).find(invItem => invItem.letter === ch
            && ['blindfold', 'towel', 'lenses', 'meat ring'].includes(invItem.kind || invItem.actualKind));
        if (accessory) {
            accessory.worn = true;
            const baseName = String(accessory.line || `${ch} - ${pickupObjectName(accessory)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \(being worn\).*$/, '');
            accessory.line = `${ch} - ${baseName} (being worn)`;
            const accessoryKind = accessory.kind || accessory.actualKind;
            if (['blindfold', 'towel'].includes(accessoryKind) && game.u) {
                game.u.blind = true;
                await docrt();
                await setMessage(`You are now wearing ${baseName}.  You can't see any more.`);
            } else {
                await setMessage(`${accessory.line}.`);
            }
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const armor = (game.inventory || []).find(invItem => invItem.letter === ch && invItem.cls === 'armor');
        if (armor?.worn || armor?.line?.includes('being worn')) {
            await setMessage('You are already wearing that!');
            game._command_mode = null;
            game.context.move = 0;
            return;
        }
        if (armor) {
            const baseName = String(armor.line || `${ch} - ${pickupObjectName(armor)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \((?:being worn|weapon|alternate weapon).*$/, '');
            const spe = `${(armor.spe ?? 0) >= 0 ? '+' : ''}${armor.spe ?? 0}`;
            const bareName = baseName.replace(/^(an?|the) /, '');
            const wornName = /[+-]\d/.test(baseName)
                ? baseName
                : armor.noArticle ? `${spe} ${bareName}` : `a ${spe} ${bareName}`;
            const acBonus = (ARMOR_AC_BONUS[String(armor.kind || '').toLowerCase()] ?? 0) + (armor.spe ?? 0);
            const kind = String(armor.kind || '').toLowerCase();
            const delay = ARMOR_WEAR_DELAY[kind] || (/dragon scales?/.test(kind) ? 5 : 0);
            if (delay) {
                const alreadyFast = !!(game.u?.fast || game.u?.veryfast);
                armor.worn = true;
                armor.line = `${ch} - ${wornName} (being worn)`;
                if (game.u) game.u.uac = (game.u.uac ?? 10) - acBonus;
                updateWornDisplacement();
                if (isSilverDragonArmorKind(kind) && game.u) game.u.reflecting = true;
                if (kind === 'shield of reflection' && game.u) game.u.reflecting = true;
                if (kind === 'speed boots' && game.u) {
                    game.u.veryfast = true;
                    syncHeroSpeedState();
                }
                game._armor_wear_occupation = {
                    itemLetter: armor.letter || ch,
                    kind,
                    alreadyFast,
                    acBonus,
                    wornLine: `${ch} - ${wornName} (being worn)`,
                    reflecting: isSilverDragonArmorKind(kind) || kind === 'shield of reflection',
                    wearApplied: true,
                    wearAt: Math.max(0, delay - 2),
                    turns: delay,
                };
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            armor.worn = true;
            armor.line = `${ch} - ${wornName} (being worn)`;
            if (game.u) game.u.uac = (game.u.uac ?? 10) - acBonus;
            updateWornDisplacement();
            if (isSilverDragonArmorKind(kind) && game.u) game.u.reflecting = true;
            if (kind === 'shield of reflection' && game.u) game.u.reflecting = true;
            if (kind.includes('boots')) {
                const alreadyFast = !!(game.u?.fast || game.u?.veryfast);
                if (kind === 'speed boots' && game.u) {
                    game.u.veryfast = true;
                    syncHeroSpeedState();
                }
                game._armor_wear_occupation = {
                    itemLetter: armor.letter || ch,
                    kind,
                    alreadyFast,
                    turns: 2,
                };
                game._command_mode = null;
                game.context.move = 2;
                return;
            }
            await setMessage(`You are now wearing ${baseName}.`);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        await setMessage("You don't have that object.", true);
        game._command_mode = 'eatInvalidMore';
        return;
    }

    if (game._command_mode === 'ringFinger') {
        if (ch === 'r' || ch === 'l') {
            const ring = (game.inventory || []).find(item => item.letter === game._puton_ring_letter);
            if (ring) {
                const hand = ch === 'r' ? 'right' : 'left';
                ring.worn = hand;
                const baseName = String(ring.line || `${ring.letter || '?'} - ${pickupObjectName(ring)}`)
                    .replace(/^[a-zA-Z] - /, '')
                    .replace(/ \(on .* hand\).*$/, '');
                ring.line = `${ring.letter || '?'} - ${baseName} (on ${hand} hand)`;
                await setMessage(`${ring.line}.`);
            }
            game._puton_ring_letter = null;
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._puton_ring_letter = null;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'zapObject') {
        if (ch === '?' || ch === '*') {
            const rows = [[0, 41, 'Wands', 1]];
            let row = 1;
            for (const item of game.inventory || []) {
                if (!isWandItem(item)) continue;
                rows.push([row++, 41, normalInventoryLine(item)]);
                if (row >= 23) break;
            }
            rows.push([row, 41, '(end)']);
            setOverlay(rows, rows.length);
            return;
        }

        const item = (game.inventory || []).find(invItem => invItem.letter === ch
            && isWandItem(invItem));
        const secretDoorDetection = item?.wand === 'secret door detection'
            || item?.kind === 'wand of secret door detection'
            || item?.wandIndex === 1
            || (item?.cls === 'wand' && item.roll > 95 && item.roll <= 145);
        const usageMessages = [];
        const chargeUse = item ? spendZapWandCharge(item, usageMessages) : null;
        if (item && !chargeUse.zapped) {
            const messages = [...usageMessages, 'Nothing happens.'];
            if (chargeUse.dust && (game.inventory || []).includes(item))
                messages.push(dustWand(item));
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const spentMessages = () => wandZapSpendMessages(usageMessages, chargeUse);
        if (item && isWishingWand(item)) {
            refreshWandLine(item);
            if (item.cursed && !rn2(WAND_BACKFIRE_CHANCE)) {
                const damage = d(wandCharges(item) + 2, 6);
                if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
                const name = pickupObjectName(item);
                useUpInventoryItem(item, item.quan || 1);
                await setMessage([...spentMessages(), `The ${name} suddenly explodes!`].join('  '));
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            const luck = (game.u?.uluck || 0) + (game.u?.moreluck || 0);
            if (luck + rn2(5) < 0) {
                const messages = spentMessages();
                messages.push('Unfortunately, nothing happens.');
                if (wandCharges(item) < 0 && (game.inventory || []).includes(item))
                    messages.push(dustWand(item));
                else refreshWandLine(item);
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            setKnownWandLine(item, 'wishing');
            await beginWishPrompt({
                moveCost: 1,
                dustItem: wandCharges(item) < 0 ? item : null,
                message: [...spentMessages(), 'For what do you wish?'].join('  '),
            });
            return;
        }
        if (item?.cursed && !rn2(WAND_BACKFIRE_CHANCE)) {
            const damage = d((item.spe ?? item.charges ?? 0) + 2, 6);
            if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
            useUpInventoryItem(item, item.quan || 1);
            await setMessage([...spentMessages(), `The ${pickupObjectName(item)} suddenly explodes!`].join('  '));
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (secretDoorDetection) {
            item.line = `${item.letter} - a ${pickupObjectName(item)}${wandChargeSuffix(item, wandCharges(item))}`;
            rn2(19);
            const messages = [...spentMessages(), "You don't find anything."];
            if (wandCharges(item) < 0 && (game.inventory || []).includes(item))
                messages.push(dustWand(item));
            await setMessage(messages.join('  '), messages.length > 1);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (item?.wand === 'polymorph') {
            item.line = `${item.letter} - a ${pickupObjectName(item)}${wandChargeSuffix(item, wandCharges(item))}`;
            game._zap_item = item;
            await setMessage([...spentMessages(), 'In what direction?'].join('  '));
            game._command_mode = 'zapPolymorphDirection';
            return;
        }
        if (item) {
            item.line = `${item.letter} - a ${pickupObjectName(item)}${wandChargeSuffix(item, wandCharges(item))}`;
            game._zap_item = item;
            await setMessage([...spentMessages(), 'In what direction?'].join('  '));
            game._command_mode = 'zapDirection';
            return;
        }
        await setMessage("You don't have that object.");
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'zapDirection') {
        let dir = movementDirection(ch);
        let verticalDir = !dir && ch === '<' ? { dx: 0, dy: 0, dz: -1 }
            : !dir && ch === '>' ? { dx: 0, dy: 0, dz: 1 } : null;
        let selfZap = ch === '.';
        const item = game._zap_item;
        game._zap_item = null;
        const preludeMessages = game._zap_prelude_messages || [];
        game._zap_prelude_messages = null;
        const hornElement = fireColdHornElement(item);
        const confusedDirection = (game.u?._statusSuffix || '').includes('Conf') || (game.u?._confusionTimeout || 0) > 0;
        const stunnedDirection = (game.u?._statusSuffix || '').includes('Stun') || game.u?.stunned;
        if ((dir || verticalDir || selfZap) && (stunnedDirection || (confusedDirection && !rn2(5)))) {
            const confusedDirs = [
                { dx: -1, dy: 0 }, { dx: 0, dy: -1 }, { dx: 1, dy: 0 }, { dx: 0, dy: 1 },
                { dx: -1, dy: -1 }, { dx: 1, dy: -1 }, { dx: 1, dy: 1 }, { dx: -1, dy: 1 },
            ];
            dir = confusedDirs[rn2(confusedDirs.length)];
            verticalDir = null;
            selfZap = false;
        }
        const deathWand = item?.wand === 'death'
            || item?.kind === 'death'
            || item?.actualKind === 'wand of death'
            || item?.wandIndex === 23;
        if (selfZap && deathWand) {
            item.known = true;
            item.kind = 'death';
            item.line = `${item.letter} - a wand of death${wandChargeSuffix(item)}`;
            game._death_cause = `shot ${game.flags?.female ? 'herself' : 'himself'} with a death ray`;
            game._queued_message_after_more = 'You die.';
            game._queued_message_more_after_more = 1;
            game._pending_time_passed = 0;
            game.context.move = 0;
            game._process_command_time_now = 0;
            game._run_steps_remaining = 0;
            await setMessage('You irradiate yourself with pure energy!', true);
            game._command_mode = null;
            return;
        }
        if (selfZap && item?.kind === 'sleep') {
            const sleepTime = rnd(50);
            game._helpless_time = Math.max(game._helpless_time || 0, sleepTime);
            game._sleeping_time = Math.max(game._sleeping_time || 0, sleepTime + 1);
            game._wake_message = 'You wake up.';
            await setMessage('The sleep ray hits you!');
            game._command_mode = null;
            game.context.move = sleepTime;
            return;
        }
        const fireWand = item?.wand === 'fire' || item?.kind === 'fire' || item?.wandIndex === 20 || hornElement === 'fire';
        const coldWand = item?.wand === 'cold' || item?.kind === 'cold' || item?.wandIndex === 21 || hornElement === 'cold';
        if (selfZap && fireWand) {
            const origDamage = d(12, 6);
            const resistsFire = !!game.u?.fireResistance;
            const fireInventory = fireDamageInventory(origDamage, true, false, { allowLifeSaving: true });
            if (!fireInventory.lifeSaving && !fireInventory.fatal) {
                if (fireInventory.damage && game.u)
                    game.u.uhp = Math.max(0, (game.u.uhp || 0) - fireInventory.damage);
                if (!resistsFire && game.u && (game.u.uhp || 0) > 0)
                    game.u.uhp = Math.max(0, (game.u.uhp || 0) - origDamage);
            }
            const followups = fireInventory.messages.map((text, index) => ({
                text,
                more: index < fireInventory.messages.length - 1,
            }));
            if (!fireInventory.lifeSaving && !fireInventory.fatal && (game.u?.uhp || 0) <= 0) {
                game._death_cause = fireInventory.deathCause
                    || (hornElement
                        ? `using a magical horn on ${game.flags?.female ? 'herself' : 'himself'}`
                        : `zapped ${game.flags?.female ? 'herself' : 'himself'} with a wand of fire`);
                if (followups.length) followups[followups.length - 1].more = true;
                followups.push({ text: 'You die...', more: false });
            }
            if ((fireInventory.lifeSaving || fireInventory.fatal) && followups.length) {
                const final = followups[followups.length - 1];
                final.lifeSaving = !!fireInventory.lifeSaving;
                final.fatal = !!fireInventory.fatal;
                final.more = true;
            }
            if (followups.length)
                game._queued_messages_after_more = [...(game._queued_messages_after_more || []),
                    ...followups];
            identifyZapToolOrWand(item, 'fire');
            await setMessage([...preludeMessages, resistsFire ? 'You feel rather warm.' : "You've set yourself afire!"].join('  '), !!followups.length || preludeMessages.length > 0);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (selfZap && coldWand) {
            const origDamage = d(12, 6);
            const resistsCold = heroHasColdResistance();
            const coldInventory = coldDamageInventory(origDamage);
            const baseDamage = resistsCold ? 0 : origDamage;
            const damage = baseDamage + coldInventory.damage;
            const hpBefore = game.u?.uhp || 0;
            if (damage && game.u)
                game.u.uhp = Math.max(0, hpBefore - damage);
            const followups = [...coldInventory.messages];
            if ((game.u?.uhp || 0) <= 0) {
                game._death_cause = coldInventory.damage >= hpBefore && coldInventory.deathCause
                    ? coldInventory.deathCause
                    : hornElement
                        ? `using a magical horn on ${game.flags?.female ? 'herself' : 'himself'}`
                        : `zapped ${game.flags?.female ? 'herself' : 'himself'} with a wand of cold`;
                followups.push('You die...');
            }
            if (followups.length)
                game._queued_messages_after_more = [...(game._queued_messages_after_more || []),
                    ...followups.map((text, index) => ({ text, more: index < followups.length - 1 }))];
            identifyZapToolOrWand(item, 'cold');
            await setMessage([...preludeMessages, resistsCold ? 'You feel a little chill.' : 'You imitate a popsicle!'].join('  '), !!followups.length || preludeMessages.length > 0);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (verticalDir && (item?.wand === 'digging' || item?.kind === 'digging' || item?.wandIndex === 18)) {
            item.known = true;
            item.kind = 'digging';
            item.line = `${item.letter} - a wand of digging${wandChargeSuffix(item)}`;
            const result = verticalDir.dz < 0
                ? { message: zapDigFallingRockMessage(), more: false }
                : await zapDigDownwardResult();
            await setMessage(result.message, !!result.more);
            game._keep_pending_message = 0;
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (dir) {
            const sleepWand = item?.wand === 'sleep' || item?.kind === 'sleep' || item?.kind === 'wand of sleep' || item?.wandIndex === 22;
            if (sleepWand) {
                exerciseAttribute(A_WIS, true);
                let range = rn2(7) + 7;
                const beam = dir.dy === 0 ? '─' : dir.dx === 0 ? '│' : dir.dx === dir.dy ? '\\' : '/';
                const beamCells = [];
                let target = null;
                let hitHero = false;
                let bounced = false;
                let x = game.u?.ux || 0;
                let y = game.u?.uy || 0;
                let dx = dir.dx;
                let dy = dir.dy;
                while (range-- > 0) {
                    const nx = x + dx;
                    const ny = y + dy;
                    const loc = game.level?.at(nx, ny);
                    if (nx <= 0 || nx >= COLNO || ny < 0 || ny >= ROWNO || !loc || !ZAP_POS(loc.typ)) {
                        if (range > 0 && couldsee(x, y)) bounced = true;
                        if (loc) {
                            x = nx;
                            y = ny;
                            beamCells.push({ x, y, ch: beam, color: CLR_BRIGHT_BLUE });
                        }
                        dx = -dx;
                        dy = -dy;
                        continue;
                    }
                    x = nx;
                    y = ny;
                    beamCells.push({ x, y, ch: beam, color: CLR_BRIGHT_BLUE });
                    if (x === game.u?.ux && y === game.u?.uy) {
                        hitHero = true;
                        break;
                    }
                    target = game.level?.monsters?.find(mon => mon.mx === x && mon.my === y);
                    if (target) break;
                }
                game._transient_beam_cells = beamCells;
                if (target) {
                    rn2(20);
                    target.msleeping = 0;
                    target.mcanmove = false;
                    target.mfrozen = Math.max(target.mfrozen || 0, 2);
                    await setMessage(`The sleep ray hits the ${target.data?.name || 'monster'}.`);
                } else if (hitHero) {
                    rn2(20);
                    if (game.u?.reflecting) {
                        const shield = (game.inventory || []).find(invItem =>
                            invItem.cls === 'armor'
                            && (invItem.worn || invItem.line?.includes('being worn'))
                            && String(invItem.actualKind || invItem.kind || '').toLowerCase() === 'shield of reflection');
                        if (shield) {
                            shield.known = true;
                            shield.line = normalInventoryLine({ ...shield, line: '' });
                            game._discoveries ??= [];
                            if (!game._discoveries.some(entry => entry.section === 'Armor' && entry.name === 'shield of reflection')) {
                                game._discoveries.push({
                                    section: 'Armor',
                                    name: 'shield of reflection',
                                    text: 'shield of reflection (polished silver shield) {buy 50}',
                                    starred: false,
                                });
                            }
                        }
                        game._queued_messages_after_more = [
                            { text: 'But it reflects from your shield!  The sleep ray bounces!', more: true, sleepReflect: true },
                            { text: 'The sleep ray hits you!  But it reflects from your shield!', more: false, processTime: true, clearBeam: true },
                        ];
                    } else {
                        game._helpless_time = Math.max(game._helpless_time || 0, 1);
                    }
                    await setMessage(`${bounced ? 'The sleep ray bounces!  ' : ''}The sleep ray hits you!`, true);
                } else {
                    game._transient_beam_cells = null;
                    game._pending_message = '';
                    game._message_more = 0;
                }
                item.known = true;
                item.kind = 'sleep';
                item.line = `${item.letter} - a wand of sleep${wandChargeSuffix(item)}`;
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (coldWand) {
                exerciseAttribute(A_WIS, true);
                const beamCells = [];
                const blast = hornBlastMessage(item);
                const messages = [...preludeMessages, ...(blast ? [blast] : [])];
                let beamStopIndex = null;
                let range = rn1(7, 7);
                let sx = game.u?.ux || 0;
                let sy = game.u?.uy || 0;
                let dx = dir.dx;
                let dy = dir.dy;

                while (range-- > 0) {
                    const lsx = sx;
                    const lsy = sy;
                    sx += dx;
                    sy += dy;
                    const inBounds = sx >= 1 && sx < COLNO && sy >= 0 && sy < ROWNO;
                    let loc = inBounds ? game.level?.at(sx, sy) : null;
                    let typ = loc?.typ ?? STONE;
                    let bounceNow = !inBounds || typ === STONE;

                    if (!bounceNow) {
                        beamCells.push({ x: sx, y: sy, ch: zapBeamGlyph(dx, dy), color: CLR_CYAN });

                        const terrain = applyColdRayTerrain(sx, sy, { buriedMerchandiseDebtMessage });
                        messages.push(...terrain.messages);
                        range += terrain.rangeMod;
                        if (terrain.stopped || range < 0) break;

                        loc = inBounds ? game.level?.at(sx, sy) : null;
                        typ = loc?.typ ?? typ;

                        const target = game.level?.monsters?.find(mon =>
                            mon.mx === sx && mon.my === sy && (mon.mhp == null || mon.mhp > 0));
                        if (target) {
                            const visible = !game.u?.blind && !target.minvis && !target.mundetected
                                && (game.viz_array?.[target.my]?.[target.mx] & IN_SIGHT);
                            const armorClass = target.mac ?? target.data?.mac ?? target.data?.ac ?? 10;
                            if (zapRayHitsArmorClass(armorClass)) {
                                range -= 2;
                                const reflection = monsterReflectionSource(target);
                                if (reflection) {
                                    if (visible) {
                                        messages.push(`The bolt of cold hits ${coldRayMonsterName(target)}.`);
                                        recordMonsterReflectionDiscovery(reflection);
                                        messages.push(`But it reflects from ${monsterPossessiveName(target)} ${reflection.source}!`);
                                    }
                                    dx = -dx;
                                    dy = -dy;
                                } else {
                                    let damage = 0;
                                    const resistsCold = !!(target.coldResistance || target.data?.resistsCold || target.data?.coldResistance);
                                    if (!resistsCold) {
                                        const origDamage = d(6, 6);
                                        damage = origDamage;
                                        if (target.fireResistance || target.data?.resistsFire) damage += d(6, 3);
                                        if (!rn2(3))
                                            damage += monsterColdInventoryDamage(target, origDamage, messages, visible);
                                        if (damage > 0 && monsterResistsEffect(target, 12))
                                            damage = Math.trunc(damage / 2);
                                    }
                                    target.mhp = (target.mhp || 1) - damage;
                                    if ((target.mhp || 0) <= 0) {
                                        rn2(6);
                                        const corpseData = target.data?.corpse || target.data;
                                        const dropCorpse = monsterCorpseDropSucceeds(target, target.data);
                                        dropMonsterInventory(target, messages);
                                        game.level.monsters = (game.level?.monsters || []).filter(mon => mon !== target);
                                        if (dropCorpse && monsterLeavesCorpseLikeDrop(corpseData))
                                            createMonsterCorpseOrGlob(target, corpseData, target.mx, target.my, { messages });
                                        recordVanquished(target, true);
                                        newsym(target.mx, target.my);
                                        messages.push(`You kill the ${target.data?.name || 'monster'}!`);
                                        break;
                                    } else if (visible) {
                                        messages.push(`The bolt of cold hits the ${target.data?.name || 'monster'}!`);
                                    }
                                }
                            } else if (visible) {
                                messages.push(`The bolt of cold misses the ${target.data?.name || 'monster'}.`);
                            }
                        } else if (sx === game.u?.ux && sy === game.u?.uy && range >= 0) {
                            if (zapRayHitsArmorClass(game.u?.uac ?? 10)) {
                                const hitBeamEnd = beamCells.length;
                                range -= 2;
                                messages.push('The bolt of cold hits you!');
                                if (game.u?.reflecting) {
                                    beamStopIndex ??= hitBeamEnd;
                                    messages.push('But it reflects from your shield!');
                                    dx = -dx;
                                    dy = -dy;
                                } else {
                                    const origDamage = d(6, 6);
                                    const resistsCold = heroHasColdResistance();
                                    const baseDamage = resistsCold ? 0 : origDamage;
                                    if (resistsCold) messages.push("You don't feel cold.");
                                    const coldInventory = !rn2(3)
                                        ? coldDamageInventory(origDamage)
                                        : { messages: [], damage: 0, deathCause: '' };
                                    messages.push(...coldInventory.messages);
                                    const damage = baseDamage + coldInventory.damage;
                                    const hpBefore = game.u?.uhp || 0;
                                    if (damage && game.u)
                                        game.u.uhp = Math.max(0, hpBefore - damage);
                                    if ((game.u?.uhp || 0) <= 0) {
                                        game._death_cause = coldInventory.damage >= hpBefore && coldInventory.deathCause
                                            ? coldInventory.deathCause
                                            : 'killed by a bolt of cold';
                                        messages.push('You die...');
                                    }
                                }
                            } else {
                                messages.push('The bolt of cold whizzes by you!');
                            }
                        }

                        const closedDoor = loc?.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED));
                        bounceNow = terrain.blocked || !ZAP_POS(typ) || (closedDoor && range >= 0);
                    }

                    if (!bounceNow) continue;
                    const bchance = (!inBounds || typ === STONE) ? 10
                        : (IS_WALL(typ) && game.u?.uz?.dnum === game.mines_dnum) ? 20
                            : 75;
                    if (--range > 0 && lsx >= 1 && lsx < COLNO && lsy >= 0 && lsy < ROWNO && couldsee(lsx, lsy))
                        messages.push('The bolt of cold bounces!');
                    if (!dx || !dy || (bchance > 0 && !rn2(bchance))) {
                        dx = -dx;
                        dy = -dy;
                        continue;
                    }

                    let bounce = 0;
                    const bounceLsx = sx - dx;
                    const bounceLsy = sy - dy;
                    const sideYLoc = sx >= 1 && sx < COLNO && bounceLsy >= 0 && bounceLsy < ROWNO
                        ? game.level?.at(sx, bounceLsy)
                        : null;
                    const sideYTyp = sideYLoc?.typ ?? STONE;
                    const sideYClosed = sideYLoc?.typ === DOOR && (sideYLoc.doormask & (D_CLOSED | D_LOCKED));
                    if (ZAP_POS(sideYTyp) && !sideYClosed
                        && (IS_ROOM(sideYTyp)
                            || (sx + dx >= 1 && sx + dx < COLNO
                                && ZAP_POS(game.level?.at(sx + dx, bounceLsy)?.typ ?? STONE))))
                        bounce = 1;
                    const sideXLoc = bounceLsx >= 1 && bounceLsx < COLNO && sy >= 0 && sy < ROWNO
                        ? game.level?.at(bounceLsx, sy)
                        : null;
                    const sideXTyp = sideXLoc?.typ ?? STONE;
                    const sideXClosed = sideXLoc?.typ === DOOR && (sideXLoc.doormask & (D_CLOSED | D_LOCKED));
                    if (ZAP_POS(sideXTyp) && !sideXClosed
                        && (IS_ROOM(sideXTyp)
                            || (sy + dy >= 0 && sy + dy < ROWNO
                                && ZAP_POS(game.level?.at(bounceLsx, sy + dy)?.typ ?? STONE)))
                        && (!bounce || rn2(2)))
                        bounce = 2;
                    if (!bounce) {
                        dx = -dx;
                        dy = -dy;
                    } else if (bounce === 1) {
                        dy = -dy;
                    } else {
                        dx = -dx;
                    }
                }

                const messageMore = messages.length > 1;
                game._transient_beam_cells = messageMore
                    ? beamStopIndex == null ? beamCells : beamCells.slice(0, beamStopIndex)
                    : null;
                identifyZapToolOrWand(item, 'cold');
                rn2(10);
                rn2(10);
                rn2(10);
                rn2(19);
                await setMessage(messages.join('  '), messageMore);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (fireWand) {
                rn2(19);
                const beamCells = [];
                const blast = hornBlastMessage(item);
                const messages = [...preludeMessages, ...(blast ? [blast] : [])];
                const followups = [];
                let beamStopIndex = null;
                let heardGas = false;
                let range = rn1(7, 7);
                let sx = game.u?.ux || 0;
                let sy = game.u?.uy || 0;
                let dx = dir.dx;
                let dy = dir.dy;

                while (range-- > 0) {
                    const lsx = sx;
                    const lsy = sy;
                    sx += dx;
                    sy += dy;
                    const inBounds = sx >= 1 && sx < COLNO && sy >= 0 && sy < ROWNO;
                    const loc = inBounds ? game.level?.at(sx, sy) : null;
                    const typ = loc?.typ ?? STONE;
                    let bounceNow = !inBounds || typ === STONE;

                    if (!bounceNow) {
                        const beam = dy === 0 ? '─' : dx === 0 ? '│' : dx === dy ? '\\' : '/';
                        beamCells.push({ x: sx, y: sy, ch: beam, color: CLR_ORANGE });

                        messages.push(...burnFireRayWebTrap(sx, sy, {
                            previousMessage: messages[messages.length - 1] || '',
                        }));
                        const ice = applyFireRayIceTerrain(sx, sy, {
                            heroRay: true,
                            recordKill: recordVanquished,
                            buriedMerchandiseDebtMessage,
                        });
                        messages.push(...ice.messages);
                        if (!ice.handled) {
                            const terrain = applyFireRayWaterTerrain(sx, sy, {
                                previousMessage: messages[messages.length - 1] || '',
                                heardGas,
                                heroRay: true,
                            });
                            messages.push(...terrain.messages);
                            heardGas = terrain.heardGas;
                            range += terrain.rangeMod;
                            const fountain = applyFireRayFountainTerrain(sx, sy, { heroRay: true });
                            messages.push(...fountain.messages);
                            range += fountain.rangeMod;
                        }
                        messages.push(...burnRayFloorObjectsByFire(sx, sy));

                        const target = game.level?.monsters?.find(mon => mon.mx === sx && mon.my === sy);
                        if (target) {
                            const chance = rn2(20);
                            let armorClass = target.mac ?? target.data?.mac ?? target.data?.ac ?? 10;
                            if (!chance) armorClass = rnd(10) < armorClass ? 10 : -10;
                            else if (armorClass < 0) armorClass = -rnd(-armorClass);

                            if (chance ? 3 - chance < armorClass : armorClass > 0) {
                                range -= 2;
                                const visible = !game.u?.blind && !target.minvis && !target.mundetected
                                    && (game.viz_array?.[target.my]?.[target.mx] & IN_SIGHT);
                                const hit = fireBreathDamageMonster(target, 6, origDamage => {
                                    const inventoryMessages = [];
                                    const inventoryDamage = monsterFireInventoryDamage(target, origDamage, inventoryMessages, visible);
                                    igniteMonsterFireInventoryItems(target, inventoryMessages, visible);
                                    return { damage: inventoryDamage, messages: inventoryMessages };
                                }, {
                                    applyDamage: false,
                                    adjustDamage: damage => monsterResistsEffect(target, 12)
                                        ? Math.trunc(damage / 2) : damage,
                                });
                                messages.push(...hit.messages);
                                const damage = hit.damage;
                                target.mhp = (target.mhp || 1) - damage;
                                if ((target.mhp || 0) <= 0) {
                                    rn2(6);
                                    const corpseData = target.data?.corpse || target.data;
                                    const dropCorpse = monsterCorpseDropSucceeds(target, target.data);
                                    dropMonsterInventory(target, messages);
                                    game.level.monsters = (game.level?.monsters || []).filter(mon => mon !== target);
                                    if (dropCorpse && monsterLeavesCorpseLikeDrop(corpseData))
                                        createMonsterCorpseOrGlob(target, corpseData, target.mx, target.my, { messages });
                                    recordVanquished(target, true);
                                    newsym(target.mx, target.my);
                                    messages.push(`You kill the ${target.data?.name || 'monster'}!`);
                                } else {
                                    messages.push(`The bolt of fire hits the ${target.data?.name || 'monster'}!`);
                                }
                            } else {
                                messages.push(`The bolt of fire misses the ${target.data?.name || 'monster'}.`);
                            }
                        } else if (sx === game.u?.ux && sy === game.u?.uy && range >= 0) {
                            const chance = rn2(20);
                            let armorClass = game.u?.uac ?? 10;
                            if (!chance) armorClass = rnd(10) < armorClass ? 10 : -10;
                            else if (armorClass < 0) armorClass = -rnd(-armorClass);

                            if (chance ? 3 - chance < armorClass : armorClass > 0) {
                                const hitBeamEnd = beamCells.length;
                                range -= 2;
                                messages.push('The bolt of fire hits you!');
                                if (game.u?.reflecting) {
                                    beamStopIndex ??= hitBeamEnd;
                                    messages.push('But it reflects from your shield!');
                                    dx = -dx;
                                    dy = -dy;
                                } else {
                                    const origDamage = d(6, 6);
                                    const fireInventory = fireDamageInventory(origDamage, false, true, {
                                        updateArmorInventory: false,
                                        allowLifeSaving: true,
                                    });
                                    if (game.u?.fireResistance) messages.push("You don't feel hot!");
                                    const fireInventoryFatal = !!(fireInventory.lifeSaving || fireInventory.fatal);
                                    const baseDamage = fireInventoryFatal || game.u?.fireResistance ? 0 : origDamage;
                                    const damage = fireInventoryFatal ? 0 : fireInventory.damage + baseDamage;
                                    const lethal = !fireInventoryFatal && damage >= (game.u?.uhp || 0);
                                    if (fireInventory.events?.length) {
                                        const entries = fireInventory.events.map(event => {
                                            const entry = {
                                                text: event.text,
                                                damageAfter: fireInventoryFatal ? 0 : event.damage || 0,
                                            };
                                            if (event.more || event.insertAfter?.length) entry.more = true;
                                            if (event.insertAfter?.length) {
                                                entry.insertAfter = event.insertAfter.map(next => ({ ...next }));
                                                if (event.lifeSaving || event.fatal) {
                                                    const final = entry.insertAfter[entry.insertAfter.length - 1];
                                                    final.lifeSaving = !!event.lifeSaving;
                                                    final.fatal = !!event.fatal;
                                                    final.more = true;
                                                }
                                            } else if (event.lifeSaving || event.fatal) {
                                                entry.lifeSaving = !!event.lifeSaving;
                                                entry.fatal = !!event.fatal;
                                                entry.more = true;
                                            }
                                            return entry;
                                        });
                                        if (!fireInventoryFatal) {
                                            const assignedDamage = entries.reduce((sum, entry) => sum
                                                + (entry.damageAfter || 0)
                                                + (entry.insertAfter || []).reduce((inner, next) => inner + (next.damageAfter || 0), 0), 0);
                                            const remainingDamage = Math.max(0, damage - assignedDamage);
                                            if (remainingDamage) {
                                                const breatheEntry = entries.flatMap(entry => entry.insertAfter || [])[0];
                                                if (lethal && breatheEntry) breatheEntry.damageAfter = (breatheEntry.damageAfter || 0) + remainingDamage;
                                                else entries[entries.length - 1].damageAfter = (entries[entries.length - 1].damageAfter || 0) + remainingDamage;
                                            }
                                            if (lethal) {
                                                const damageEntry = [...entries].reverse().find(entry => (entry.damageAfter || 0) > 0)
                                                    || entries[entries.length - 1];
                                                if (damageEntry) damageEntry.more = true;
                                            }
                                        }
                                        followups.push(...entries);
                                    } else if (damage && (messages.length > 1 || fireInventory.messages.length || lethal))
                                        game._fire_ray_damage_after_more = (game._fire_ray_damage_after_more || 0) + damage;
                                    else if (damage && game.u)
                                        game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
                                    if (messages.length > 1 || fireInventory.messages.length || lethal)
                                        beamStopIndex ??= hitBeamEnd;
                                    if (!fireInventory.events?.length) followups.push(...fireInventory.messages);
                                    if (lethal) {
                                        game._death_cause = fireInventory.deathCause || 'killed by a bolt of fire';
                                        followups.push('You die...');
                                    }
                                }
                            } else {
                                messages.push('The bolt of fire whizzes by you!');
                            }
                        }

                        const closedDoor = loc?.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED));
                        bounceNow = !ZAP_POS(typ) || (closedDoor && range >= 0);
                    }

                    if (!bounceNow) continue;
                    const bchance = (!inBounds || typ === STONE) ? 10
                        : (IS_WALL(typ) && game.u?.uz?.dnum === game.mines_dnum) ? 20
                            : 75;
                    if (--range > 0 && lsx >= 1 && lsx < COLNO && lsy >= 0 && lsy < ROWNO && couldsee(lsx, lsy))
                        messages.push('The bolt of fire bounces!');
                    if (!dx || !dy || (bchance > 0 && !rn2(bchance))) {
                        dx = -dx;
                        dy = -dy;
                        continue;
                    }

                    let bounce = 0;
                    const bounceLsx = sx - dx;
                    const bounceLsy = sy - dy;
                    const sideYLoc = sx >= 1 && sx < COLNO && bounceLsy >= 0 && bounceLsy < ROWNO
                        ? game.level?.at(sx, bounceLsy)
                        : null;
                    const sideYTyp = sideYLoc?.typ ?? STONE;
                    const sideYClosed = sideYLoc?.typ === DOOR && (sideYLoc.doormask & (D_CLOSED | D_LOCKED));
                    if (ZAP_POS(sideYTyp) && !sideYClosed
                        && (IS_ROOM(sideYTyp)
                            || (sx + dx >= 1 && sx + dx < COLNO
                                && ZAP_POS(game.level?.at(sx + dx, bounceLsy)?.typ ?? STONE))))
                        bounce = 1;
                    const sideXLoc = bounceLsx >= 1 && bounceLsx < COLNO && sy >= 0 && sy < ROWNO
                        ? game.level?.at(bounceLsx, sy)
                        : null;
                    const sideXTyp = sideXLoc?.typ ?? STONE;
                    const sideXClosed = sideXLoc?.typ === DOOR && (sideXLoc.doormask & (D_CLOSED | D_LOCKED));
                    if (ZAP_POS(sideXTyp) && !sideXClosed
                        && (IS_ROOM(sideXTyp)
                            || (sy + dy >= 0 && sy + dy < ROWNO
                                && ZAP_POS(game.level?.at(bounceLsx, sy + dy)?.typ ?? STONE)))
                        && (!bounce || rn2(2)))
                        bounce = 2;
                    if (!bounce) {
                        dx = -dx;
                        dy = -dy;
                    } else if (bounce === 1) {
                        dy = -dy;
                    } else {
                        dx = -dx;
                    }
                }

                game._transient_beam_cells = beamStopIndex == null ? beamCells : beamCells.slice(0, beamStopIndex);
                if (followups.length)
                    game._queued_messages_after_more = [...(game._queued_messages_after_more || []),
                        ...followups.map((entry, index) => typeof entry === 'string'
                            ? { text: entry, more: index < followups.length - 1 }
                            : { ...entry, more: entry.more ?? index < followups.length - 1 })];
                identifyZapToolOrWand(item, 'fire');
                await setMessage(messages.join('  '), messages.length > 1 || !!followups.length);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            const strikingWand = item?.wand === 'striking'
                || item?.kind === 'striking'
                || item?.actualKind === 'wand of striking'
                || item?.wandIndex === 7;
            if (strikingWand) {
                const result = await strikeStatueAlongLine(dir);
                const message = result.message || '';
                if (result.learned) {
                    item.known = true;
                    item.kind = 'striking';
                    item.wandIndex = 7;
                    item.line = `${item.letter} - a wand of striking${wandChargeSuffix(item)}`;
                }
                await setMessage(message);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (item?.wand === 'digging' || item?.kind === 'digging' || item?.wandIndex === 18) {
                rn2(19);
                let digdepth = rn2(18) + 8;
                rn2(19);
                item.known = true;
                item.kind = 'digging';
                item.line = `${item.letter} - a wand of digging${wandChargeSuffix(item)}`;
                const mazeDig = !!game.level?.flags?.is_maze_lev && !Is_earthlevel(game.u?.uz);
                let x = (game.u?.ux || 0) + dir.dx;
                let y = (game.u?.uy || 0) + dir.dy;
                while (--digdepth >= 0) {
                    if (x < 1 || x >= COLNO || y < 0 || y >= ROWNO) break;
                    const loc = game.level?.at(x, y);
                    if (!loc) break;
                    const nondiggable = (IS_WALL(loc.typ) || loc.typ === STONE || IS_TREE(loc.typ))
                        && (loc.wall_info & W_NONDIGGABLE);
                    if ((loc.typ === DOOR && (loc.doormask & (D_CLOSED | D_LOCKED))) || loc.typ === SDOOR) {
                        if (nondiggable) break;
                        loc.typ = DOOR;
                        loc.doormask = D_NODOOR;
                        loc.flags = 0;
                        digdepth -= 2;
                        newsym(x, y);
                        if (mazeDig) break;
                    } else if (mazeDig) {
                        if (IS_WALL(loc.typ) || IS_TREE(loc.typ)) {
                            if (!nondiggable) {
                                loc.typ = IS_TREE(loc.typ) ? ROOM : ROOM;
                                loc.flags = 0;
                                newsym(x, y);
                            }
                            break;
                        } else if (loc.typ === STONE || loc.typ === SCORR) {
                            if (!nondiggable) {
                                loc.typ = CORR;
                                loc.flags = 0;
                                newsym(x, y);
                            }
                            break;
                        }
                    } else if (IS_OBSTRUCTED(loc.typ)) {
                        if (nondiggable) break;
                        if (IS_WALL(loc.typ) || loc.typ === SDOOR) {
                            loc.typ = game.level?.flags?.is_cavernous_lev && !game.level?.flags?.has_town
                                ? CORR
                                : DOOR;
                            loc.doormask = D_NODOOR;
                            loc.flags = 0;
                            digdepth -= 2;
                        } else if (IS_TREE(loc.typ)) {
                            loc.typ = ROOM;
                            loc.flags = 0;
                            digdepth -= 2;
                        } else {
                            loc.typ = CORR;
                            loc.flags = 0;
                            digdepth--;
                        }
                        newsym(x, y);
                    }
                    x += dir.dx;
                    y += dir.dy;
                    if (!dir.dx && !dir.dy) {
                        break;
                    }
                }
                await setMessage('');
                game._keep_pending_message = 0;
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            await setMessage('');
            game._keep_pending_message = 0;
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (hornElement) {
            await setMessage([...preludeMessages, `${instrumentTheName(item)} vibrates.`].join('  '), preludeMessages.length > 0);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'zapPolymorphDirection') {
        const dir = movementDirection(ch);
        const verticalDir = !dir && ch === '<' ? { dx: 0, dy: 0, dz: -1 }
            : !dir && ch === '>' ? { dx: 0, dy: 0, dz: 1 } : null;
        const selfZap = ch === '.';
        const item = game._zap_item;
        game._zap_item = null;
        game._command_mode = null;
        if (selfZap) {
            const result = polymorphSelfZapResult(item);
            game.context.move = 1;
            if (result.message) await setMessage(result.message, result.more);
            else {
                game._pending_message = '';
                game._message_more = 0;
            }
            return;
        }
        if (!dir && !verticalDir) return;
        if (verticalDir?.dz < 0) {
            if (heroHidingUnderObjects()) {
                const topObject = topFloorObjectAt(game.u?.ux || 0, game.u?.uy || 0);
                if (topObject) {
                    await polymorphFloorPileAt(game.u?.ux || 0, game.u?.uy || 0, {
                        onlyObjects: new Set([topObject]),
                        item,
                        learnOnAffected: true,
                        refreshHiding: true,
                        restackBoulders: false,
                    });
                    return;
                }
            }
            rn2(19);
            game._pending_message = '';
            game._message_more = 0;
            game.context.move = 1;
            return;
        }
        if (!verticalDir) {
            await polymorphFloorPileRay(dir, item);
            return;
        }
        const x = game.u?.ux || 0;
        const y = game.u?.uy || 0;
        const topObject = heroHidingUnderObjects() ? topFloorObjectAt(x, y) : null;
        await polymorphFloorPileAt(x, y, {
            omitObjects: topObject ? new Set([topObject]) : null,
            refreshHiding: !!topObject,
        });
        return;
    }

    if (game._tutorial_prompt) {
        if (ch !== 'y' && ch !== 'n') {
            if (ch === ' ') game._tutorial_prompt_invalid = 1;
            return;
        }
        game._tutorial_prompt = 0;
        game._tutorial_prompt_invalid = 0;
        game._tutorial_prompt_done = 1;
        if (ch === 'y') {
            await docrt();
            await flush_screen(1);
            await make_tutorial1_level();
            vision_reset();
            game._redraw_level_after_more = 1;
            await setMessage('Entering the tutorial.', true);
            game._command_mode = 'tutorialEnterMore';
            game.context.move = 0;
            game._pending_time_passed = 0;
            game._process_command_time_now = 0;
        }
        return;
    }

    if (game._command_mode === 'castSpell') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            return;
        }
        const spell = (game._spell_menu_spells || []).find(item => item.letter === ch);
        if (!spell) return;
        game._casting_spell = spell;
        const failureRoll = rnd(100); // C spelleffects_check: spell failure roll.
        const energy = spell.level * 5;
        if (failureRoll > (spell.successChance ?? 100)) {
            game.u.uen = Math.max(0, (game.u.uen || 0) - Math.trunc(energy / 2));
            game._casting_spell = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            await setMessage('You fail to cast the spell correctly.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game.u.uen = Math.max(0, (game.u.uen || 0) - energy);
        exerciseAttribute(A_WIS, true);
        mksobj(SPE_HEALING, false, false); // C pseudo spell object; init=false only consumes next_ident().
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        await setMessage('In what direction?');
        game._command_mode = 'spellDirection';
        return;
    }

    if (game._command_mode === 'spellDirection') {
        const spell = game._casting_spell;
        game._casting_spell = null;
        if (spell?.name === 'stone to flesh') {
            if (ch === '.') {
                const result = await stoneToFleshInventoryEffect();
                if (result.messages.length) await setMessage(result.messages.join('  '));
                else {
                    game._pending_message = '';
                    game._message_more = 0;
                    game._keep_pending_message = 1;
                }
            } else if (ch === '>') {
                const result = await stoneToFleshFloorEffect();
                if (result.messages.length) await setMessage(result.messages.join('  '));
                else await setMessage(`You cast ${spell?.name || 'a spell'}.`);
            } else {
                await setMessage(`You cast ${spell?.name || 'a spell'}.`);
            }
        } else if (spell?.name === 'polymorph') {
            if (!(await polymorphSpellDirection(ch)))
                await setMessage(`You cast ${spell?.name || 'a spell'}.`);
        } else if (spell?.category === 'healing') {
            game.u.uhp = Math.min(game.u.uhpmax || game.u.uhp || 1, (game.u.uhp || 1) + d(6, 4));
            await setMessage('You feel better.');
        } else if (spell?.name === 'force bolt') {
            const dir = movementDirection(ch);
            const result = dir ? await strikeStatueAlongLine(dir) : null;
            await setMessage(result?.hit ? (result.message || '') : `You cast ${spell?.name || 'a spell'}.`);
        } else {
            await setMessage(`You cast ${spell?.name || 'a spell'}.`);
        }
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'chargeInvalidMore') {
        if (ch === '\x1b') {
            game._charging_scroll = null;
            await setMessage('Never mind.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            await setMessage(chargePromptText());
            game._command_mode = 'chargeObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'chargeInventoryOverlay') {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(chargePromptText());
            game._command_mode = 'chargeObject';
            return;
        }
        if (!(game.inventory || []).some(invItem => invItem.letter === ch)) {
            game._keep_pending_message = 1;
            return;
        }
        game._command_mode = 'chargeObject';
    }

    if (game._command_mode === 'chargeObject') {
        const charge = game._charging_scroll;
        if (!charge) {
            game._command_mode = null;
            return;
        }
        if (ch === ' ' || ch === '\x1b') {
            game._charging_scroll = null;
            await setMessage('Never mind.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === '?' || ch === '*') {
            setOverlay(inventoryOverlayLines(), 24, true);
            game._command_mode = 'chargeInventoryOverlay';
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item) {
            await setMessage("You don't have that object.", true);
            game._command_mode = 'chargeInvalidMore';
            return;
        }
        const result = rechargeItem(item, charge.curseBless || 0);
        if (result.retry) {
            await setMessage(result.messages.join('  '), true);
            game._command_mode = 'chargeInvalidMore';
            return;
        }
        game._charging_scroll = null;
        game._command_mode = null;
        game.context.move = 1;
        const message = (result.messages || []).filter(Boolean).join('  ');
        if (message) await setMessage(message, !!result.more);
        else {
            game._pending_message = '';
            game._message_more = 0;
            game._keep_pending_message = 1;
        }
        return;
    }

    if (game._command_mode === 'fireScrollCenter') {
        const pending = game._fire_scroll_pending;
        if (!pending) {
            game._command_mode = null;
            return;
        }
        const ux = game.u?.ux ?? 0;
        const uy = game.u?.uy ?? 0;
        let x = game._fire_scroll_cursor?.x ?? ux;
        let y = game._fire_scroll_cursor?.y ?? uy;
        const dir = movementDirection(ch);
        if (dir) {
            x = Math.max(1, Math.min(COLNO - 1, x + dir.dx));
            y = Math.max(0, Math.min(ROWNO - 1, y + dir.dy));
            game._fire_scroll_cursor = { x, y };
            game._cursor_override = [x - 1, y + 1];
            const description = fireScrollTargetDescription(x, y);
            await setMessage(canCenterFireScroll(x, y) ? description : `${description} (invalid target)`);
            return;
        }
        if (!(ch === '.' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n')) {
            game._keep_pending_message = 1;
            return;
        }
        if (ch === '\x1b') {
            x = ux;
            y = uy;
        }
        if (!canCenterFireScroll(x, y)) {
            x = ux;
            y = uy;
        }
        game._fire_scroll_pending = null;
        game._fire_scroll_cursor = null;
        game._cursor_override = null;
        const result = resolveFireScrollExplosion(x, y, pending.damage, pending.messages || []);
        await setMessage(result.messages.join('  '), result.more);
        game._command_mode = null;
        game.context.move = 1;
        if (applyLifeSavingOrFatalCommandMode(result)) return;
        return;
    }

    if (game._command_mode === 'stinkingCloudCenter') {
        const pending = game._stinking_cloud_pending;
        if (!pending) {
            game._command_mode = null;
            return;
        }
        const ux = game.u?.ux ?? 0;
        const uy = game.u?.uy ?? 0;
        let x = game._stinking_cloud_cursor?.x ?? ux;
        let y = game._stinking_cloud_cursor?.y ?? uy;
        const dir = movementDirection(ch);
        if (dir) {
            x = Math.max(1, Math.min(COLNO - 1, x + dir.dx));
            y = Math.max(0, Math.min(ROWNO - 1, y + dir.dy));
            game._stinking_cloud_cursor = { x, y };
            game._cursor_override = [x - 1, y + 1];
            const description = fireScrollTargetDescription(x, y);
            await setMessage(canCenterFireScroll(x, y) ? description : `${description} (invalid target)`);
            return;
        }
        if (!(ch === '.' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n')) {
            game._keep_pending_message = 1;
            return;
        }
        game._stinking_cloud_pending = null;
        game._stinking_cloud_cursor = null;
        game._cursor_override = null;
        game._command_mode = null;
        game.context.move = 1;
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            return;
        }
        if (!canCenterFireScroll(x, y)) {
            await setMessage(game.u?.hallucinating
                ? 'Ugh... someone cut the cheese.'
                : 'The scroll crumbles with a whiff of rotten eggs.');
            return;
        }
        const wasInside = heroInsideGasCloud();
        const region = createGasCloud(x, y, pending.cloudsize, pending.damage);
        if (region) region.heroFault = true;
        const enveloped = !wasInside && region?.coords?.some(coord => coord.x === ux && coord.y === uy);
        if (enveloped) await setMessage('You are enveloped in a cloud of noxious gas!');
        else {
            game._pending_message = '';
            game._message_more = 0;
            game._keep_pending_message = 1;
        }
        return;
    }

    if (game._command_mode === 'genocideText') {
        if (ch === '\r' || ch === '\n') {
            await finishGenocideInput(game._genocide_input || '');
            return;
        }
        if (ch === '\x1b') {
            await finishGenocideInput('\x1b');
            return;
        }
        const code = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (code === 8 || code === 127 || ch === '\x7f') game._genocide_input = (game._genocide_input || '').slice(0, -1);
        else if (code >= 32) game._genocide_input = `${game._genocide_input || ''}${ch}`;
        const pending = game._genocide_pending || {};
        const text = game._genocide_input || '';
        await setMessage(`${genocidePrompt(pending)}${text ? ` ${text}` : ''}`);
        return;
    }

    if (game._command_mode === 'genocideHelp') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = 'genocideText';
            game._genocide_input = '';
            const pending = game._genocide_pending || {};
            await setMessage(`${genocidePrompt(pending)}${genocideRetryHint(pending)}`);
        }
        return;
    }

    if (game._command_mode === 'readInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const letters = inventoryLetters(item => item.cls === 'scroll' || item.cls === 'spellbook' || item.otyp === SCROLL_CLASS) || 'gh';
            await setMessage(`What do you want to read? [${letters} or ?*]`);
            game._command_mode = 'readObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'readInventoryMore') {
        const letters = inventoryLetters(item => item.cls === 'scroll' || item.cls === 'spellbook' || item.otyp === SCROLL_CLASS);
        if (letters.includes(ch)) {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = 'readObject';
        } else if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(`What do you want to read? [${letters || 'gh'} or ?*]`);
            game._command_mode = 'readObject';
            return;
        } else {
            game._keep_pending_message = 1;
            return;
        }
    }

    if (game._command_mode === 'readObject') {
        if (ch === ' ' || ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '?' || ch === '*') {
            const items = (game.inventory || [])
                .filter(invItem => invItem.cls === 'scroll' || invItem.cls === 'spellbook' || invItem.otyp === SCROLL_CLASS);
            if (items.length === 1) {
                await setMessage(`${items[0].letter} - ${inventoryItemName(items[0])}.`, true);
                game._command_mode = 'readInventoryMore';
                return;
            }
            setOverlay(inventoryOverlayLines(), 24, true);
            game._command_mode = 'simpleOverlay';
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch
            && (invItem.cls === 'scroll' || invItem.cls === 'spellbook' || invItem.otyp === SCROLL_CLASS));
        const scrollName = item?.kind?.startsWith?.('scroll:')
            ? item.kind.slice(7)
            : item?.kind?.startsWith?.('scroll of ')
                ? item.kind.slice(10)
                : item?.scrollIndex != null
                    ? IDENTIFIED_SCROLL_NAMES[item.scrollIndex] || ''
                    : item?.kind?.startsWith?.('scroll labeled ')
                        ? IDENTIFIED_SCROLL_NAMES[(game._object_descriptions?.scrolls || []).indexOf(item.kind.slice(15))] || ''
                        : '';
        const isScroll = item && (item.cls === 'scroll' || item.otyp === SCROLL_CLASS);
        const blindBlock = readBlindBlockMessage(item, isScroll);
        if (blindBlock) {
            await setMessage(blindBlock);
            game._command_mode = null;
            game.context.move = 0;
            return;
        }
        if (item) recordLiterateConduct();
        if (isScroll && isBlankScrollItem(item)) {
            item.known = true;
            item.actualKind = 'scroll of blank paper';
            item.kind = 'blank paper';
            item.line = normalInventoryLine({ ...item, line: '' });
            await setMessage(game.u?.blind
                ? "You don't remember there being any magic words on this scroll."
                : 'This scroll seems to be blank.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'enchant armor' || item.scrollIndex === 0)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            const result = enchantArmorScrollEffect(item);
            if (result.known) learnScrollByName('enchant armor', item, 0);
            const messages = scrollReadMessages(confusedReading);
            messages.push(...result.messages);
            await setMessage(messages.join('  '), true);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'destroy armor' || item.scrollIndex === 1)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('destroy armor')
                || item.known === true
                || item.actualKind === 'scroll of destroy armor'
                || item.kind === 'destroy armor'
                || item.kind === 'scroll of destroy armor';
            const callLabel = scrollCallLabel(item, 1);
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            const result = destroyArmorScrollEffect(item, messages);
            if (result.prompt) {
                if (!alreadyKnown) messages.push('This is a scroll of destroy armor!');
                learnScrollByName('destroy armor', item, 1);
                messages.push(destroyArmorPromptText());
                game._destroy_armor_scroll = { target: result.target };
                await setMessage(messages.join('  '), false);
                game._command_mode = 'destroyArmorObject';
                game.context.move = 0;
                return;
            }
            if (result.learned) learnScrollByName('destroy armor', item, 1);
            if (result.legacy) {
                const effectMessages = messages.slice(1);
                if (effectMessages.length > 1) {
                    game._queued_message_after_more = effectMessages.slice(1).join('  ');
                    game._queued_message_process_time_after_more = 1;
                    game._destroy_armor_uac_delta_after_more = result.uacDelta || 0;
                    game._read_scroll_exercise_after_more = 1;
                    game._destroy_armor_clear_resume_after_more = 1;
                } else if (game.u) {
                    game.u.uac = (game.u.uac ?? 10) + (result.uacDelta || 0);
                }
                await setMessage(`${messages[0]}  ${effectMessages[0] || 'Your skin itches.'}`, true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            const shouldCall = !result.skipCall && !alreadyKnown && !result.learned;
            await setMessage(messages.join('  '), result.more || confusedReading || shouldCall);
            game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
            if (shouldCall) game._call_scroll_label = callLabel;
            game.context.move = shouldCall ? 0 : 1;
            return;
        }
        if (isScroll && (scrollName === 'confuse monster' || item.scrollIndex === 2)) {
            removeInventoryItem(item);
            rn2(19);
            const effectMessage = confuseMonsterScrollEffect(item);
            await setMessage(`As you read the scroll, it disappears.${effectMessage ? `  ${effectMessage}` : ''}`, true);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'scare monster' || item.scrollIndex === 3)) {
            const shouldCall = shouldTryCallScroll('scare monster', item, 3);
            removeInventoryItem(item);
            rn2(19);
            const effectMessage = scareMonsterScrollEffect(item);
            if (shouldCall) prepareScrollTryCall(item, 3, 1);
            await setMessage(`As you read the scroll, it disappears.  ${effectMessage}`, true);
            game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
            if (shouldCall) {
                game._scroll_trycall_pending = { kind: 'scrollRead', moveCost: 1 };
                game.context.move = 0;
            } else {
                game.context.move = 1;
            }
            return;
        }
        if (isScroll && (scrollName === 'enchant weapon' || item.scrollIndex === 5)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('enchant weapon')
                || item.known === true
                || item.actualKind === 'scroll of enchant weapon'
                || item.kind === 'enchant weapon'
                || item.kind === 'scroll of enchant weapon';
            const callLabel = scrollCallLabel(item, 5);
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            const result = enchantWeaponScrollEffect(item, messages);
            const learnedScroll = result.learned && !alreadyKnown;
            if (result.learned) learnScrollByName('enchant weapon', item, 5);
            if (learnedScroll && messages.length === 2) {
                game._queued_message_after_more = messages[1];
                game._queued_message_process_time_after_more = 1;
                game._read_scroll_exercise_after_more = 1;
                await setMessage(messages[0], true);
                game._command_mode = null;
                game.context.move = 0;
                return;
            }
            const shouldCall = !alreadyKnown && !result.learned;
            await setMessage(messages.join('  '), result.more || confusedReading || shouldCall);
            game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
            if (shouldCall) game._call_scroll_label = callLabel;
            game.context.move = shouldCall ? 0 : 1;
            return;
        }
        if (isScroll && (scrollName === 'create monster' || item.scrollIndex === 6)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            await createMonsterScrollEffect(item);
            await setMessage(scrollReadMessages(confusedReading).join('  '), confusedReading);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'taming' || item.scrollIndex === 7)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            const result = tamingScrollEffect(item);
            const messages = scrollReadMessages(confusedReading);
            messages.push(result.message);
            await setMessage(messages.join('  '), true);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'genocide' || item.scrollIndex === 8)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('genocide');
            const messages = scrollReadMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            if (!alreadyKnown) {
                messages.push('You have found a scroll of genocide!');
                learnScrollByName('genocide', item, 8);
            }
            if (confusedReading && !item.blessed) {
                const target = genocideMonsterByName(heroGenocideTargetName()) || {
                    name: heroGenocideTargetName(),
                    glyph: '@',
                    mlet: '@',
                    mlevel: game.u?.ulevel || 1,
                    hpLevel: game.u?.ulevel || 1,
                    mmove: NORMAL_SPEED,
                    maligntyp: 0,
                };
                if (item.cursed) await createCursedGenocideMonsters(target, messages);
                else genocideMonsterType(target, messages, { killPlayer: true, cause: 'genocidal confusion' });
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            game._genocide_pending = {
                messages,
                cursed: !!item.cursed,
                classMode: !!item.blessed,
            };
            game._genocide_input = '';
            await setMessage([...messages, genocidePrompt(game._genocide_pending)].join('  '), false);
            game._command_mode = 'genocideText';
            game.context.move = 0;
            return;
        }
        if (isScroll && (scrollName === 'gold detection' || item.scrollIndex === 11)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            const result = goldDetectionScrollEffect(item);
            if (result.known) learnScrollByName('gold detection', item, 11);
            const messages = ['As you read the scroll, it disappears.'];
            if (confusedReading)
                messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
            if (result.message) messages.push(result.message);
            await setMessage(messages.join('  '), result.more || confusedReading);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'food detection' || item.scrollIndex === 12)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            const result = foodDetectionScrollEffect(item);
            if (result.known) learnScrollByName('food detection', item, 12);
            const messages = ['As you read the scroll, it disappears.'];
            if (confusedReading)
                messages.push(heroIsHallucinating() ? 'Being so trippy, you screw up...' : 'Being confused, you mispronounce the magic words...');
            if (result.message) messages.push(result.message);
            await setMessage(messages.join('  '), result.more || confusedReading);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'identify' || item.actualKind === 'scroll of identify' || item.scrollIndex === 13)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('identify')
                || item.known === true;
            const blessed = !!item.blessed;
            const cursed = !!item.cursed;
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            if (confusedReading || (cursed && !alreadyKnown)) {
                messages.push('You identify this as an identify scroll.');
            } else if (!alreadyKnown) {
                messages.push('This is an identify scroll.');
            }
            if (!alreadyKnown) {
                learnScrollByName('identify', item, 13);
                exerciseAttribute(A_WIS, true);
            }
            if (confusedReading || (cursed && !alreadyKnown)) {
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (!(game.inventory || []).length) {
                messages.push("You're not carrying anything else to be identified.");
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            let identifyLimit = 1;
            if (blessed || (!cursed && !rn2(5))) {
                identifyLimit = rn2(5);
                if (identifyLimit === 1 && blessed && (game.u?.uluck || 0) > 0) identifyLimit++;
            }
            const unidentified = unidentifiedInventoryItems();
            const identified = identifyLimit ? unidentified.slice(0, identifyLimit) : unidentified;
            for (const invItem of identified) identifyInventoryItem(invItem);
            if (identified.length) {
                game._queued_messages_after_more = identified.map((invItem, index) => ({
                    text: `${invItem.line}.`,
                    more: index < identified.length - 1,
                }));
            } else {
                game._topline_after_more = `You have already identified ${alreadyKnown ? 'all' : 'the rest'} of your possessions.`;
            }
            await setMessage(messages.join('  '), true);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'teleportation' || item.scrollIndex === 10)) {
            const confusedReading = heroIsConfused();
            const debugWizard = !!game.flags?.debug;
            const alreadyKnown = scrollDiscoveryKnown('teleportation')
                || item.known === true
                || item.actualKind === 'scroll of teleportation'
                || item.kind === 'teleportation'
                || item.kind === 'scroll of teleportation';
            const callLabel = scrollCallLabel(item, 10);
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            const deferConfusedPromptExercise = confusedReading
                && !((heroHasAmuletOfYendor() || In_endgame(game.u?.uz) || In_sokoban(game.u?.uz)) && !debugWizard)
                && ((heroHasTeleportControl() && !heroIsStunned()) || debugWizard);
            if (!deferConfusedPromptExercise) rn2(19);
            if (!confusedReading && !item.cursed) rn2(19);
            const result = teleportationScrollEffect(item, messages);
            if (result.learned) learnScrollByName('teleportation', item, 10);
            if (result.promptLevel) {
                game._read_confused_teleport_prompt_after_more = 1;
                game._read_level_teleport_confused_prompt = result.confusedPrompt ? 1 : 0;
                if (deferConfusedPromptExercise && messages.length > 1) {
                    game._queued_message_after_more = messages[1];
                    game._queued_message_more_after_more = 1;
                    game._read_scroll_exercise_after_more = 1;
                    await setMessage(messages[0], true);
                } else {
                    await setMessage(messages.join('  '), true);
                }
                game._command_mode = null;
                game.context.move = 0;
                return;
            }
            if (result.promptSameLevel) {
                game._read_teleport_cursor_prompt_after_more = 1;
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 0;
                return;
            }
            if (result.materialize) game._topline_after_more = result.materialize;
            const shouldCall = !alreadyKnown && !result.learned;
            await setMessage(messages.join('  '), result.more || !!result.materialize || shouldCall);
            game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
            if (shouldCall) game._call_scroll_label = callLabel;
            game.context.move = shouldCall ? 0 : 1;
            return;
        }
        if (isScroll && (scrollName === 'punishment' || item.scrollIndex === 18)) {
            const confusedReading = heroIsConfused();
            const messages = scrollReadMessages(confusedReading);
            item.kind = 'punishment';
            item.actualKind = 'scroll of punishment';
            item.known = true;
            item.line = '';
            removeInventoryItem(item);
            rn2(19);
            learnScrollByName('punishment', item, 18);
            if (confusedReading || item.blessed) {
                messages.push('You feel guilty.');
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            game._punishment_after_more = { cursed: !!item.cursed };
            await setMessage(messages.join('  '), true);
            game._command_mode = null;
            game.context.move = 0;
            return;
        }
        if (isScroll && (scrollName === 'charging' || item.scrollIndex === 19)) {
            const confusedReading = heroIsConfused();
            const messages = scrollReadMessages(confusedReading);
            if (confusedReading) {
                removeInventoryItem(item);
                rn2(19);
                if (item.cursed) {
                    if (game.u) game.u.uen = 0;
                    messages.push('You feel discharged.');
                } else {
                    if (game.u) {
                        game.u.uen = (game.u.uen || 0) + d(item.blessed ? 6 : 4, 4);
                        if (game.u.uen > (game.u.uenmax || 0)) game.u.uenmax = game.u.uen;
                        else game.u.uen = game.u.uenmax || game.u.uen;
                    }
                    messages.push('You feel charged up!');
                }
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }

            const alreadyKnown = scrollDiscoveryKnown('charging');
            removeInventoryItem(item);
            rn2(19);
            if (!alreadyKnown) {
                messages.push('This is a charging scroll.');
                learnScrollByName('charging', item, 19);
            }
            game._charging_scroll = { curseBless: item.cursed ? -1 : item.blessed ? 1 : 0 };
            messages.push(chargePromptText());
            await setMessage(messages.join('  '), false);
            game._command_mode = 'chargeObject';
            game.context.move = 0;
            return;
        }
        if (isScroll && (scrollName === 'remove curse' || item.scrollIndex === 4)) {
            const alreadyKnown = scrollDiscoveryKnown('remove curse')
                || item.known === true
                || item.actualKind === 'scroll of remove curse'
                || item.kind === 'remove curse'
                || item.kind === 'scroll of remove curse';
            const callLabel = String(item.kind || '').startsWith('scroll labeled ')
                ? String(item.kind || '').replace(/^scroll labeled /, '')
                : game._object_descriptions?.scrolls?.[4] || 'XOR OTA';
            rn2(19);
            removeInventoryItem(item);
            const result = removeCurseScrollEffect(item);
            if (result.learned) learnScrollByName('remove curse', item, 4);
            let callAfterDisintegrates = false;
            const shouldCall = !alreadyKnown && !result.learned;
            if (result.disintegrates) {
                game._queued_message_after_more = 'The scroll disintegrates.';
                game._queued_message_more_after_more = 1;
                if (shouldCall) {
                    game._call_scroll_label = callLabel;
                    game._call_scroll_after_queued_more = 1;
                    callAfterDisintegrates = true;
                }
            }
            await setMessage(result.messages.join('  '), true);
            game._command_mode = shouldCall && !result.disintegrates ? 'callScrollAfterMore' : null;
            if (shouldCall && !result.disintegrates) game._call_scroll_label = callLabel;
            game.context.move = callAfterDisintegrates ? 0 : 1;
            if (shouldCall && !result.disintegrates) game.context.move = 0;
            return;
        }
        if (isScroll && (scrollName === 'magic mapping' || item.scrollIndex === 14 || (item.roll >= 800 && item.roll <= 844))) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('magic mapping')
                || item.known === true
                || item.actualKind === 'scroll of magic mapping'
                || item.kind === 'magic mapping'
                || item.kind === 'scroll of magic mapping';
            const callLabel = scrollCallLabel(item, 14);
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            if (game.level?.flags?.nommap) {
                messages.push('Your mind is filled with crazy lines!');
                messages.push(heroIsHallucinating() ? 'Wow!  Modern art.' : 'Your head spins in bewilderment.');
                addHeroConfusion(rnd(30));
                const shouldCall = !alreadyKnown;
                await setMessage(messages.join('  '), true);
                game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
                if (shouldCall) game._call_scroll_label = callLabel;
                game.context.move = shouldCall ? 0 : 1;
                return;
            }
            learnScrollByName('magic mapping', item, 14);
            messages.push('A map coalesces in your mind!');
            const cursedMap = item.cursed && !confusedReading;
            revealLevelMap({ confusedMap: cursedMap, revealSecretDoors: !!item.blessed });
            rn2(19);
            if (cursedMap) messages.push("Unfortunately, you can't grasp the details.");
            await setMessage(messages.join('  '), confusedReading || cursedMap);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'amnesia' || item.scrollIndex === 15)) {
            const confusedReading = heroIsConfused();
            learnScrollByName('amnesia', item, 15);
            removeInventoryItem(item);
            rn2(19);
            const messages = scrollReadMessages(confusedReading);
            messages.push(amnesiaScrollEffect(item));
            await setMessage(messages.join('  '), true);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'fire' || item.scrollIndex === 16)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('fire');
            let damage = fireScrollDamage(item);
            const messages = fireScrollReadMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            if (!alreadyKnown) learnScrollByName('fire', item, 16);

            if (confusedReading) {
                if (game.u?.underwater || game.u?.uunderwater) {
                    messages.push('A little water around you vaporizes.');
                } else if (game.u?.fireResistance) {
                    messages.push(game.u?.blind
                        ? 'You feel a pleasant warmth in your hands.'
                        : 'Oh, look, what a pretty fire in your hands.');
                } else {
                    messages.push('The scroll catches fire and you burn your hands.');
                    if (game.u) {
                        game.u.uhp = Math.max(0, (game.u.uhp || 0) - 1);
                        if ((game.u.uhp || 0) <= 0) {
                            game._death_cause = 'killed by a scroll of fire';
                            messages.push('You die...');
                        }
                    }
                }
                await setMessage(messages.join('  '), true);
                game._command_mode = null;
                game.context.move = 1;
                return;
            }

            if (item.blessed) {
                if (!alreadyKnown) messages.push('This is a scroll of fire!');
                damage *= 5;
                messages.push('Where do you want to center the explosion?');
                game._fire_scroll_pending = { damage, messages };
                game._fire_scroll_cursor = { x: game.u?.ux || 0, y: game.u?.uy || 0 };
                game._cursor_override = [(game.u?.ux || 0) - 1, (game.u?.uy || 0) + 1];
                await setMessage(messages.join('  '), false);
                game._command_mode = 'fireScrollCenter';
                game.context.move = 0;
                return;
            }

            const result = resolveFireScrollExplosion(game.u?.ux || 0, game.u?.uy || 0, damage, messages);
            await setMessage(result.messages.join('  '), result.more);
            game._command_mode = null;
            game.context.move = 1;
            if (applyLifeSavingOrFatalCommandMode(result)) return;
            return;
        }
        if (isScroll && (scrollName === 'earth' || item.scrollIndex === 17)) {
            const confusedReading = heroIsConfused();
            removeInventoryItem(item);
            rn2(19);
            const messages = scrollReadMessages(confusedReading);
            const result = earthScrollEffect(item);
            if (result.known) learnScrollByName('earth', item, 17);
            messages.push(...result.messages);
            await setMessage(messages.join('  '), result.more || confusedReading);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (isScroll && (scrollName === 'stinking cloud' || item.scrollIndex === 20)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('stinking cloud');
            const messages = scrollReadMessages(confusedReading);
            const cval = item.blessed ? 1 : item.cursed ? -1 : 0;
            removeInventoryItem(item);
            rn2(19);
            if (!alreadyKnown) {
                messages.push('You have found a scroll of stinking cloud!');
                learnScrollByName('stinking cloud', item, 20);
            }
            messages.push(`Where do you want to center the ${alreadyKnown ? 'stinking ' : ''}cloud?`);
            game._stinking_cloud_pending = {
                cloudsize: 15 + 10 * cval,
                damage: 8 + 4 * cval,
            };
            game._stinking_cloud_cursor = { x: game.u?.ux || 0, y: game.u?.uy || 0 };
            game._cursor_override = [(game.u?.ux || 0) - 1, (game.u?.uy || 0) + 1];
            await setMessage(messages.join('  '), false);
            game._command_mode = 'stinkingCloudCenter';
            game.context.move = 0;
            return;
        }
        if (isScroll && (scrollName === 'light' || item.scrollIndex === 9)) {
            const confusedReading = heroIsConfused();
            const alreadyKnown = scrollDiscoveryKnown('light')
                || item.known === true
                || item.actualKind === 'scroll of light'
                || item.kind === 'light'
                || item.kind === 'scroll of light';
            const callLabel = scrollCallLabel(item, 9);
            const messages = scrollDisappearMessages(confusedReading);
            removeInventoryItem(item);
            rn2(19);
            const result = await lightScrollEffect(item, messages);
            const learnedScroll = result.learned && !alreadyKnown;
            if (result.learned) learnScrollByName('light', item, 9);
            if (learnedScroll) exerciseAttribute(A_WIS, true);
            const shouldCall = !alreadyKnown && !result.learned;
            await setMessage(messages.join('  '), confusedReading || shouldCall);
            game._command_mode = shouldCall ? 'callScrollAfterMore' : null;
            if (shouldCall) game._call_scroll_label = callLabel;
            game.context.move = shouldCall ? 0 : 1;
            return;
        }
        if (item?.cls === 'spellbook' || item?.otyp === BOOK_OF_THE_DEAD) {
            if (isBlankSpellbookItem(item)) {
                item.known = true;
                item.kind = 'spellbook of blank paper';
                item.spellName = '';
                item.line = normalInventoryLine({ ...item, line: '' });
                await setMessage('This spellbook is all blank.');
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            const bookOfDead = isBookOfTheDeadItem(item);
            const name = bookOfDead ? 'Book of the Dead'
                : item.spellName || item.spell?.name || String(item.kind || '').replace(/^spellbook(?: of)? /, '');
            const knownSpell = (game._known_spells || []).some(spell =>
                !bookOfDead && (spell.name || spell.spellName || spell.spell?.name) === name);
            if (knownSpell) {
                await setMessage(`You know "${name || 'healing'}" quite well already.`, true);
                game._command_mode = 'readRepeatMore';
                return;
            }
            const level = bookOfDead ? 7 : (SPELLBOOK_LEVELS[name] || item.level || item.spell?.level || 1);
            const baseDelay = bookOfDead ? 20 : (SPELLBOOK_DELAYS[name] || 1);
            const studyDelay = level <= 2 ? baseDelay : level <= 4 ? (level - 1) * baseDelay
                : level <= 6 ? level * baseDelay : 8 * baseDelay;
            const stats = game.u?.acurr?.a || [];
            const readAbility = (stats[3] ?? 10) + 4 + Math.trunc((game.u?.ulevel || 1) / 2) - 2 * level;
            const tooHard = !bookOfDead && (item.cursed || (!item.blessed && rnd(20) > readAbility));
            if (tooHard) {
                const result = cursedBookStudyEffect(level);
                const messages = [...result.messages];
                let shouldCall = false;
                if (result.gone || !rn2(3)) {
                    shouldCall = shouldTryCallSpellbook(item, name);
                    if (shouldCall) prepareSpellbookTryCall(item, studyDelay);
                    useUpInventoryItem(item);
                    if (!result.gone) messages.push('The spellbook crumbles to dust!');
                }
                game._helpless_time = Math.max(game._helpless_time || 0, studyDelay);
                game._wake_message = 'You can move again.';
                await setMessage(messages.join('  '), shouldCall || result.more || messages.length > 1);
                game._command_mode = shouldCall ? 'callSpellbookAfterMore' : null;
                game.context.move = shouldCall ? 0 : studyDelay;
                return;
            }
            if (heroIsConfused()) {
                const messages = [];
                let shouldCall = false;
                if (!rn2(3) && !isBookOfTheDeadItem(item)) {
                    messages.push('Being confused you have difficulties in controlling your actions.');
                    shouldCall = shouldTryCallSpellbook(item, name);
                    if (shouldCall) prepareSpellbookTryCall(item, studyDelay);
                    useUpInventoryItem(item);
                    messages.push('You accidentally tear the spellbook to pieces.');
                } else {
                    messages.push('You find yourself reading the first line over and over again.');
                }
                game._helpless_time = Math.max(game._helpless_time || 0, studyDelay);
                game._wake_message = 'You can move again.';
                await setMessage(messages.join('  '), messages.length > 1 || shouldCall);
                game._command_mode = shouldCall ? 'callSpellbookAfterMore' : null;
                game.context.move = shouldCall ? 0 : studyDelay;
                return;
            }
            game._spellbook_study_occupation = {
                itemLetter: item.letter,
                name,
                level,
                skill: item.spell?.skill || SPELL_CATEGORIES[name] || 'enchantment',
                bookOfDead,
                turns: Math.max(1, studyDelay - 1),
            };
            await setMessage(`You begin to ${bookOfDead ? 'recite' : 'memorize'} the runes.`);
            game._command_mode = null;
            game.context.move = studyDelay + 1;
            return;
        }
        await setMessage("You don't have that object.", true);
        game._command_mode = 'readInvalidMore';
        return;
    }

    if (game._command_mode === 'readConfirm') {
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'readRepeatMore') {
        if (ch === '\r' || ch === '\n') {
            await setMessage('Refresh your memory anyway? [yn] (n)');
            game._command_mode = 'readRefreshPrompt';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'readRefreshPrompt') {
        if (ch === '\x1b') game._command_mode = null;
        game._keep_pending_message = 1;
        return;
    }

    if (ch === 'Z') {
        if (!(game._known_spells || []).length) {
            await setMessage("You don't know any spells right now.");
            return;
        }
        const lines = spellMenuLines();
        setOverlay(lines, Math.max(...lines.map(([row]) => row)) + 1, false);
        game._command_mode = 'castSpell';
        return;
    }

    if (ch === '\x17' && game.flags?.debug && !game._command_mode) {
        await beginWishPrompt();
        return;
    }

    if (ch === '\x07' && game.flags?.debug && !game._command_mode) {
        await setMessage('Create what kind of monster?');
        game._wizgenesis_text = '';
        game._command_mode = 'wizGenesisMonster';
        return;
    }

    if (ch === '\x06' && game.flags?.debug && !game._command_mode) {
        for (const trap of game.level?.traps || []) trap.tseen = true;
        revealLevelMap();
        exerciseAttribute(A_WIS, true);
        return;
    }

    if (game._command_mode === 'engraveObject') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '-') {
            await setMessage('You write in the dust with your fingertip.', true);
            game._command_mode = 'engraveToolMore';
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item && isWandItem(item)) {
            const usageMessages = [];
            const chargeUse = spendEngraveWandCharge(item, usageMessages);
            const messages = wandZapSpendMessages(usageMessages, chargeUse);
            if (!chargeUse.zapped) {
                if (chargeUse.dust && (game.inventory || []).includes(item))
                    messages.push(useUpDustWand(item));
                else
                    messages.push('The wand is too worn out to engrave.');
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (item.cursed && !rn2(WAND_BACKFIRE_CHANCE)) {
                const damage = d(wandCharges(item) + 2, 6);
                if (game.u) game.u.uhp = Math.max(0, (game.u.uhp || 0) - damage);
                useUpInventoryItem(item, item.quan || 1);
                messages.push(`The ${pickupObjectName(item)} suddenly explodes!`);
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (wandCharges(item) < 0 && (game.inventory || []).includes(item)) {
                messages.push(useUpDustWand(item));
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game.context.move = 1;
                return;
            }
            if (!messages.length) messages.push(`You write in the dust with ${articleFor(pickupObjectName(item))}.`);
            await setMessage(messages.join('  '), true);
            game._command_mode = 'engraveToolMore';
            return;
        }
        await setMessage("You don't have that object.", true);
        game._command_mode = 'engraveToolMore';
        return;
    }

    if (game._command_mode === 'engraveToolMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._engrave_text = '';
            await setMessage('What do you want to write in the dust here?');
            game._command_mode = 'engraveText';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'engraveText') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '\r' || ch === '\n') {
            const text = game._engrave_text || '';
            for (const written of text) if (written !== ' ') rn2(25);
            rn2(19);
            game.level.engravings = (game.level.engravings || [])
                .filter(engr => engr.x !== game.u?.ux || engr.y !== game.u?.uy);
            if (text) game.level.engravings.push({ x: game.u?.ux || 0, y: game.u?.uy || 0, text, type: DUST });
            if (text) recordLiterateConduct();
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            game._pending_message = '';
            game._message_more = 0;
            game._engrave_text = '';
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._engrave_text = `${game._engrave_text || ''}${ch}`;
        await setMessage(`What do you want to write in the dust here? ${game._engrave_text}`);
        return;
    }

    if (game._command_mode === 'farlookMenu') {
        if (ch === '/') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            const getposTipSeen = game._farlook_tip_seen || game._travel_tip_seen
                || game._jump_tip_seen || game._getpos_tip_seen;
            game._farlook_tip_after_intro = game.flags?.verbose === false ? 1 : 0;
            game._farlook_quick = 0;
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            if (game.flags?.verbose === false && getposTipSeen) {
                await setMessage('Pick a monster, object or location.');
                game._command_mode = 'farlookCursor';
                return;
            }
            await setMessage(game.flags?.verbose === false
                ? 'Pick a monster, object or location.'
                : 'Please move the cursor to a monster, object or location.', true);
            game._command_mode = 'farlookIntroMore';
            return;
        }
        if (ch === '?') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._farlook_specify = '';
            await setMessage('Specify what? (type the word)');
            game._command_mode = 'farlookSpecify';
            return;
        }
        if (ch === 'i') {
            const lines = inventoryOverlayLines();
            const clearCol = Math.max(0, Math.min(...lines.map(([, col]) => col)) - 1);
            setOverlay(lines, lines.length, false, clearCol);
            game._command_mode = 'farlookInventory';
            return;
        }
        if (ch === 'm' || ch === 'M') {
            const nearby = ch === 'm';
            const loX = nearby ? Math.max((game.u?.ux || 0) - BOLT_LIM, 1) : 1;
            const hiX = nearby ? Math.min((game.u?.ux || 0) + BOLT_LIM, COLNO - 1) : COLNO - 1;
            const loY = nearby ? Math.max((game.u?.uy || 0) - BOLT_LIM, 0) : 0;
            const hiY = nearby ? Math.min((game.u?.uy || 0) + BOLT_LIM, ROWNO - 1) : ROWNO - 1;
            const lines = [
                [0, 0, nearby ? `Monsters currently shown near <${game.u?.ux || 0},${game.u?.uy || 0}>:` : 'All monsters currently shown on the map:'],
            ];
            let row = 2;
            for (let y = loY; y <= hiY; y++) {
                for (let x = loX; x <= hiX; x++) {
                    if (row >= 23) break;
                    const loc = game.level?.at(x, y);
                    const coord = `<${x},${y}>${y < 10 ? ' ' : ''}`.padStart(8);
                    if (x === game.u?.ux && y === game.u?.uy) {
                        lines.push([row++, 0, `${coord}  @  ${heroFarlookDescription()}`]);
                        continue;
                    }
                    const mon = (game.level?.monsters || []).find(candidate => candidate.mx === x && candidate.my === y);
                    const glyph = mon?.data?.mlet?.[0] || mon?.data?.glyph || '?';
                    if (!mon || loc?.disp_ch !== glyph) continue;
                    const tame = mon.pet ? 'tame ' : mon.mpeaceful ? 'peaceful ' : '';
                    lines.push([row++, 0, `${coord}  ${glyph}  ${tame}${mon.data?.name || 'monster'}`]);
                }
            }
            lines.push([23, 0, '--More--']);
            setOverlay([
                ...lines,
            ], 24, true);
            game._command_mode = 'farlookTextMore';
            return;
        }
        if (ch === 'o' || ch === 'O') {
            const nearby = ch === 'o';
            const loX = nearby ? Math.max((game.u?.ux || 0) - BOLT_LIM, 1) : 1;
            const hiX = nearby ? Math.min((game.u?.ux || 0) + BOLT_LIM, COLNO - 1) : COLNO - 1;
            const loY = nearby ? Math.max((game.u?.uy || 0) - BOLT_LIM, 0) : 0;
            const hiY = nearby ? Math.min((game.u?.uy || 0) + BOLT_LIM, ROWNO - 1) : ROWNO - 1;
            const lines = [
                [0, 0, nearby ? `Objects currently shown near <${game.u?.ux || 0},${game.u?.uy || 0}>:` : 'All objects currently shown on the map:'],
            ];
            let row = 2;
            for (let y = loY; y <= hiY; y++) {
                for (let x = loX; x <= hiX; x++) {
                    if (row >= 23) break;
                    const loc = game.level?.at(x, y);
                    const objects = game.level?.objects || [];
                    let obj = null;
                    for (let i = objects.length - 1; i >= 0; i--) {
                        const candidate = objects[i];
                        if (!candidate.hidden && candidate.ox === x && candidate.oy === y) {
                            obj = candidate;
                            break;
                        }
                    }
                    if (!obj || loc?.disp_ch !== (obj.glyph || '?')) continue;
                    const coord = `<${x},${y}>${y < 10 ? ' ' : ''}`.padStart(8);
                    let name = pickupObjectName(obj);
                    if ((obj.quan || 1) === 1 && !/^(a|an|some|the|\d+)\b/.test(name)) {
                        const article = /^[aeiou]/i.test(name) ? 'an' : 'a';
                        name = `${article} ${name}`;
                    }
                    lines.push([row++, 0, `${coord}  ${obj.glyph || '?'}  ${name}`]);
                }
            }
            lines.push([23, 0, '--More--']);
            setOverlay(lines, 24, true);
            game._command_mode = 'farlookTextMore';
            return;
        }
        if (ch === 'e' || ch === 'E') {
            const nearby = ch === 'e';
            const loX = nearby ? Math.max((game.u?.ux || 0) - BOLT_LIM, 1) : 1;
            const hiX = nearby ? Math.min((game.u?.ux || 0) + BOLT_LIM, COLNO - 1) : COLNO - 1;
            const loY = nearby ? Math.max((game.u?.uy || 0) - BOLT_LIM, 0) : 0;
            const hiY = nearby ? Math.min((game.u?.uy || 0) + BOLT_LIM, ROWNO - 1) : ROWNO - 1;
            const lines = [[0, 0, nearby ? 'Nearby seen or remembered engravings:' : 'Seen or remembered engravings on this level:']];
            let row = 2;
            for (const engraving of game.level?.engravings || []) {
                if (row >= 23) break;
                if (engraving.x < loX || engraving.x > hiX || engraving.y < loY || engraving.y > hiY) continue;
                const loc = game.level?.at(engraving.x, engraving.y);
                const obscured = loc?.disp_ch && loc.disp_ch !== '`' ? `, obscured by ${loc.disp_ch}` : '';
                lines.push([row++, 0, `  <${engraving.x},${engraving.y}>  \`  remembered text: "${engraving.text || ''}"${obscured}`]);
            }
            lines.push([23, 0, '--More--']);
            setOverlay(lines, 24, true);
            game._command_mode = 'farlookTextMore';
            return;
        }
        if (ch === 't' || ch === 'T') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage(ch === 't' ? 'No traps seen or remembered nearby.' : 'No traps seen or remembered.');
            game._command_mode = null;
            return;
        }
        if (ch === '\x1b') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'terrainMenu') {
        if (ch === ' ' || ch === 'a') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._farlook_from_terrain = 1;
            game._terrain_known_only = 1;
            const getposTipSeen = game._farlook_tip_seen || game._travel_tip_seen
                || game._jump_tip_seen || game._getpos_tip_seen;

            if (!getposTipSeen) {
                await setMessage('Showing known terrain only...', true);
                game._command_mode = 'terrainKnownIntroMore';
            } else {
                await setMessage("Showing known terrain only...  (For instructions type a '?')");
                game._command_mode = 'terrainKnownView';
            }
            return;
        }
        if (ch === '\x1b') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'terrainKnownIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            game._farlook_tip_seen = 1;
            game._getpos_tip_seen = 1;
            setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
            game._command_mode = 'farlookTip';
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'terrainKnownView') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Done.', true);
            game._command_mode = 'terrainKnownDoneMore';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'terrainKnownDoneMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._terrain_known_only = 0;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            docrt();
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'farlookSpecify') {
        if (ch === '\r' || ch === '\n') {
            setOverlay(FOUNTAIN_TEXT_LINES, 16);
            game._command_mode = 'farlookTextMore';
            return;
        }
        if (ch === '\x1b') {
            game._pending_message = '';
            game._command_mode = null;
            return;
        }
        game._farlook_specify = `${game._farlook_specify || ''}${ch}`;
        await setMessage(`Specify what? (type the word) ${game._farlook_specify}`);
        return;
    }

    if (game._command_mode === 'farlookInventory') {
        if (ch === 'a') {
            setOverlay(QUARTERSTAFF_TEXT_LINES, 19);
            game._command_mode = 'farlookTextMore';
            return;
        }
        if (ch === '\x1b' || ch === ' ') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'farlookTextMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'farlookIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (!game._farlook_tip_seen) {
                game._farlook_tip_seen = 1;
                setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
                game._command_mode = 'farlookTip';
                return;
            }
            await setMessage("(For instructions type a '?')");
            game._command_mode = 'farlookCursor';
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'farlookTip') {
        if (game._farlook_tip_after_intro && (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n')) {
            game._farlook_tip_after_intro = 0;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage('Move cursor to a monster, object or location:');
            game._command_mode = 'farlookCursor';
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            return;
        }
        if (game._farlook_from_terrain && (ch === '\r' || ch === '\n')) {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage("(For instructions type a '?')  Move cursor to anything of interest:");
            game._command_mode = 'farlookCursor';
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            return;
        }
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage("(For instructions type a '?')", true);
            game._command_mode = 'farlookInstructionsMore';
        }
        return;
    }

    if (game._command_mode === 'farlookInstructionsMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Move cursor to a monster, object or location:');
            game._command_mode = 'farlookCursor';
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'farlookDetailMore') {
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game._farlook_more_info = '';
            game._farlook_detail_aborted = 0;
            return;
        }
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            if (game._farlook_detail_aborted) {
                game._farlook_detail_aborted = 0;
                game._pending_message = '';
                game._message_more = 0;
                game._command_mode = null;
                return;
            }
            if (game._farlook_more_info) {
                await setMessage(`More info about "${game._farlook_more_info}"? [yn] (n)`);
                game._command_mode = 'farlookMoreInfo';
                return;
            }
            await setMessage('Pick a monster, object or location.');
            game._command_mode = 'farlookCursor';
            return;
        }
        if (!game._farlook_more_info) game._farlook_detail_aborted = 1;
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'farlookMoreInfo') {
        await setMessage('Pick a monster, object or location.');
        game._command_mode = 'farlookCursor';
        return;
    }

    if (game._command_mode === 'farlookCursor') {
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game._farlook_from_terrain = 0;
            return;
        }
        if (ch === '\x12') {
            await setMessage('Move cursor to a monster, object or location:');
            return;
        }
        if (ch === 'd' || ch === 'D') {
            let target = null;
            let bestDist = Infinity;
            for (let y = 0; y < ROWNO; y++) {
                for (let x = 1; x < COLNO; x++) {
                    const loc = game.level?.at(x, y);
                    if (loc?.typ !== DOOR || !loc.seenv) continue;
                    if (ch === 'D' && !(loc.doormask & D_ISOPEN)) continue;
                    const dist = Math.max(Math.abs(x - (game._farlook_x || game.u?.ux || 0)),
                        Math.abs(y - (game._farlook_y || game.u?.uy || 0)));
                    if (dist >= bestDist) continue;
                    target = { x, y, loc };
                    bestDist = dist;
                }
            }
            if (!target) {
                await setMessage("Can't find dungeon feature 'D'.");
                return;
            }
            game._farlook_x = target.x;
            game._farlook_y = target.y;
            await setMessage(doorDescription(target.loc));
            return;
        }
        if (ch === '\\') {
            await setMessage("Can't find dungeon feature '\\'.");
            return;
        }
        const dir = movementDirection(ch);
        if (dir) {
            const steps = ch !== ch.toLowerCase() ? 8 : 1;
            game._farlook_x = Math.max(1, Math.min(COLNO - 1, (game._farlook_x || game.u?.ux || 0) + dir.dx * steps));
            game._farlook_y = Math.max(0, Math.min(ROWNO - 1, (game._farlook_y || game.u?.uy || 0) + dir.dy * steps));
            const loc = game.level?.at(game._farlook_x, game._farlook_y);
            const trap = (game.level?.traps || []).find(item => item.tseen && item.tx === game._farlook_x && item.ty === game._farlook_y);
            const mon = (game.level?.monsters || []).find(candidate =>
                candidate.mx === game._farlook_x && candidate.my === game._farlook_y
                && !candidate.mundetected && (!candidate.minvis || game.u?.seeInvisible));
            let text = 'unexplored area';
            if (mon) text = farlookMonsterDescription(mon);
            else if (ch === 'n' && game._farlook_after_H) {
                text = 'wall';
                game._farlook_after_H = 0;
            } else if (game._farlook_x === game.u?.ux && game._farlook_y === game.u?.uy) text = heroFarlookDescription();
            else if (trap) text = TRAP_NAMES[trap.ttyp] || 'trap';
            else if (loc?.typ === STONE) text = 'stone';
            else if (game.u?.blind && (polyselfForm()?.mmove ?? NORMAL_SPEED) === 0
                && loc && !loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ') text = 'stone';
            else if (!loc || (!loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ')) text = 'unexplored area';
            else if (loc.typ === STAIRS) text = 'branch staircase up';
            else if (loc?.typ === DOOR) text = doorDescription(loc);
            else if (loc?.typ === CORR) text = 'corridor';
            else if (loc?.typ === ROOM) text = 'floor of a room';
            else if (loc?.typ === MOAT) text = 'moat';
            else if (loc?.typ === TREE) text = 'tree';
            else if (ch === 'H') {
                text = 'floor of a room';
                game._farlook_after_H = 1;
            }
            else if (loc?.typ && loc.typ < DOOR) text = 'wall';
            await setMessage(text);
            return;
        }
        if (ch === '.') {
            const loc = game.level?.at(game._farlook_x || game.u?.ux || 0, game._farlook_y || game.u?.uy || 0);
            const trap = (game.level?.traps || []).find(item => item.tseen
                && item.tx === (game._farlook_x || game.u?.ux || 0)
                && item.ty === (game._farlook_y || game.u?.uy || 0));
            game._farlook_more_info = '';
            game._farlook_detail_aborted = 0;
            if ((game._farlook_x || 0) === game.u?.ux && (game._farlook_y || 0) === game.u?.uy) {
                const heroDescription = heroFarlookDescription();
                game._farlook_more_info = heroDescription.split(' called ')[0];
                await setMessage(`@        a human or elf (${heroDescription})`, true);
            } else if (trap) {
                const trapName = TRAP_NAMES[trap.ttyp] || 'trap';
                await setMessage(`^        a trap (${trapName})`, true);
            } else if (loc?.typ === STAIRS) {
                game._farlook_more_info = 'branch staircase up';
                await setMessage('<        a staircase up or a branch staircase up (branch staircase up)', true);
            } else if (loc?.typ === CORR) {
                await setMessage('#        can be many things (corridor)', !game._farlook_quick);
            } else if (loc?.typ === STONE) {
                await setMessage('         can be many things (stone)');
            } else if (loc?.typ === TREE) {
                await setMessage('\x0eg\x0f        a tree (tree)');
            } else if (loc?.typ && loc.typ < DOOR) {
                await setMessage('│        the interior of a monster or a wall (wall)');
            } else {
                await setMessage('·        a doorway or the floor of a room or the dark part of a room or ice', true);
                game._message_more_line = '(floor of a room)';
            }
            game._command_mode = game._message_more ? 'farlookDetailMore' : null;
            return;
        }
        if (ch === ' ') {
            if (game._farlook_from_terrain) {
                game._farlook_from_terrain = 0;
                game._command_mode = null;
                game._clear_terrain_known_after_more = 1;
                await setMessage('Done.', true);
                return;
            }
            await setMessage('Pick a monster, object or location.');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'knownSpells') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'discoveries') {
        if ((ch === ' ' || ch === '\r' || ch === '\n') && game._discoveries_has_more) {
            game._discovery_page = (game._discovery_page || 0) + 1;
            setOverlay(discoveryOverlayLines(game._discovery_page), 24, true);
            return;
        }
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._discovery_page = 0;
            if ((game.u?._statusSuffix || '').includes('Hallu') && game._hallu_refresh_after_level_teleport_move)
                game._hallu_refresh_after_fullscreen_overlay = 1;
        }
        return;
    }

    if (game._command_mode === 'enhance') {
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'enhancePracticeConfirm') {
        if (ch === 'y') {
            setOverlay(ENHANCE_ADVANCE_LINES.map(line => [...line]), 24, true);
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = 'enhanceAdvance';
            return;
        }
        if (ch === 'n' || ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            setOverlay(currentSkillLines(), 24, true);
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = 'enhance';
        }
        return;
    }

    if (game._command_mode === 'enhanceAdvance') {
        if (ch === '\r' || ch === '\n' || ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            return;
        }
        const skill = ch === 'j' ? 'long sword' : ch === 'r' ? 'polearms' : '';
        if (skill) {
            if (skill === 'long sword') game._enhanced_long_sword = true;
            if (skill === 'polearms') game._enhanced_polearms = true;
            const lines = ENHANCE_ADVANCE_LINES.map(line => [...line]);
            if (game._enhanced_long_sword) {
                const row = lines.find(line => line[0] === 13);
                if (row) row[2] = ' j -  long sword         Skilled         25( 180)';
            }
            if (game._enhanced_polearms) {
                const row = lines.find(line => line[0] === 21);
                if (row) row[2] = ' r -  polearms           Basic            0(  80)';
            }
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._queued_overlay_after_more = {
                lines,
                clearRows: 24,
                hideStatus: true,
                mode: 'enhanceAdvance',
                message: '',
                messageMore: false,
            };
            await setMessage(`You are now more skilled in ${skill}.`, true);
        }
        return;
    }

    if (game._command_mode === 'nameMenu') {
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            return;
        }
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === 'a') {
            game._annotate_text = '';
            await setMessage('What do you want to call this dungeon level?');
            game._command_mode = 'annotateText';
            return;
        }
        if (ch === 'i') {
            const letters = inventoryLetters(item => item.cls !== 'coin' && item.glyph !== '$');
            await setMessage(`What do you want to name? [${getobjPromptLetters(letters)} or ?*]`);
            game._command_mode = 'nameInventoryObject';
            return;
        }
        return;
    }

    if (game._command_mode === 'nameInventoryObject') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item) {
            game._command_mode = null;
            return;
        }
        const promptName = inventoryItemName(item)
            .replace(/^(?:an? |the )/, '')
            .replace(/^(?:blessed |uncursed |cursed )/, '')
            .replace(/^[+-]\d+ /, '')
            .replace(/ named .*$/, '');
        game._name_inventory_item = item;
        game._name_inventory_text = '';
        await setMessage(`What do you want to name this ${promptName}?`);
        game._command_mode = 'nameInventoryText';
        return;
    }

    if (game._command_mode === 'nameInventoryText') {
        const item = game._name_inventory_item;
        if (ch === '\r' || ch === '\n') {
            const text = String(game._name_inventory_text || '').trim();
            if (item && text) {
                if (item.artifact) {
                    await setMessage(`${item.artifact} resists the attempt.`);
                    game._name_inventory_item = null;
                    game._name_inventory_text = '';
                    game._command_mode = null;
                    return;
                }
                const currentLine = String(item.line || `${item.letter || '?'} - ${inventoryItemName(item)}`);
                const status = currentLine.match(/(\s+\([^)]+\))$/)?.[1] || '';
                if (nameObjectAsArtifact(item, text)) {
                    item.line = `${item.letter || '?'} - ${pickupObjectName(item)}${status}`;
                } else {
                    const baseName = inventoryItemName(item).replace(/ named .*$/, '');
                    const baseKind = String(item.actualKind || item.kind || baseName)
                        .replace(/^(?:an? |the )/, '')
                        .replace(/^(?:blessed |uncursed |cursed )/, '')
                        .replace(/^[+-]\d+ /, '')
                        .replace(/ named .*$/, '');
                    item.actualKind = item.actualKind || baseKind;
                    item.kind = `${baseKind} named ${text}`;
                    item.line = `${item.letter || '?'} - ${baseName} named ${text}${status}`;
                }
            }
            game._name_inventory_item = null;
            game._name_inventory_text = '';
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            return;
        }
        if (key >= 32) game._name_inventory_text = `${game._name_inventory_text || ''}${ch}`;
        const promptName = item
            ? inventoryItemName(item)
                .replace(/^(?:an? |the )/, '')
                .replace(/^(?:blessed |uncursed |cursed )/, '')
                .replace(/^[+-]\d+ /, '')
                .replace(/ named .*$/, '')
            : 'object';
        await setMessage(`What do you want to name this ${promptName}?${game._name_inventory_text ? ` ${game._name_inventory_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'cmdassistMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            const cancelMessage = game._cmdassist_cancel_message;
            game._cmdassist_cancel_message = '';
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            if (cancelMessage) await setMessage(cancelMessage);
        }
        return;
    }

    if (game._command_mode === 'overview') {
        if (ch === '\x1b' || ch === ' ') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'simpleOverlay') {
        if (ch === '\x1b' || ch === ' ') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'wizWhereMore') {
        if (ch === ' ' || ch === '\r' || ch === '\n') {
            const nextPage = (game._wiz_where_page || 0) + 1;
            if (nextPage * 23 < (game._wiz_where_lines?.length || 0)) showWizWherePage(nextPage);
            else {
                game._command_mode = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._wiz_where_lines = null;
                game._wiz_where_page = 0;
            }
            return;
        }
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._wiz_where_lines = null;
            game._wiz_where_page = 0;
        }
        return;
    }

    if (game._command_mode === 'wizIntrinsicMenu') {
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._wiz_intrinsic_selected = null;
            return;
        }
        const intrinsicRows = wizIntrinsicRows();
        if (ch === '.') {
            game._wiz_intrinsic_selected = new Set(Object.keys(intrinsicRows));
            for (const [letter, [rowNumber, label]] of Object.entries(intrinsicRows)) {
                const row = (game._overlay_lines || []).find(line => line[0] === rowNumber);
                const timeout = letter === 'i' ? game.u?._blindTimeout || 0 : 0;
                const text = timeout ? `${label.padEnd(27)} [${timeout}]` : label;
                if (row) row[2] = ` ${letter} + ${text}`;
            }
            return;
        }
        if (intrinsicRows[ch]) {
            game._wiz_intrinsic_selected ??= new Set();
            if (game._wiz_intrinsic_selected.has(ch)) game._wiz_intrinsic_selected.delete(ch);
            else game._wiz_intrinsic_selected.add(ch);
            const [rowNumber, label] = intrinsicRows[ch];
            const row = (game._overlay_lines || []).find(line => line[0] === rowNumber);
            const timeout = ch === 'i' ? game.u?._blindTimeout || 0 : 0;
            const text = timeout ? `${label.padEnd(27)} [${timeout}]` : label;
            if (row) row[2] = ` ${ch} ${game._wiz_intrinsic_selected.has(ch) ? '+' : '-'} ${text}`;
            return;
        }
        if (ch === '\r' || ch === '\n') {
            const selected = game._wiz_intrinsic_selected || new Set();
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._wiz_intrinsic_selected = null;
            const messages = [];
            if (selected.has('a')) {
                const oldTimeout = game.u?._invulnerableTimeout || 0;
                if (game.u) {
                    game.u.invulnerable = true;
                    game.u._invulnerableTimeout = oldTimeout + 30;
                }
                messages.push(`Timeout for invulnerable ${oldTimeout ? 'increased by' : 'set to'} 30.`);
            }
            if (selected.has('h')) {
                if (game.u && !(game.u._statusSuffix || '').includes('Hallu'))
                    game.u._statusSuffix = `${game.u._statusSuffix || ''} Hallu`;
                messages.push('Oh wow!  Everything looks so cosmic!');
            }
	            if (selected.has('i') && game.u) {
	                game.u.blind = true;
	                game.u._blindTimeout = (game.u._blindTimeout || 0) + 30;
	            }
            if (selected.has('r')) {
                const oldTimeout = game.u?._veryfastTimeout || 0;
                if (game.u) {
                    game.u.veryfast = true;
                    game.u._veryfastTimeout = oldTimeout + 30;
                }
                messages.push(`Timeout for very fast ${oldTimeout ? 'increased by' : 'set to'} 30.`);
            }
            if (messages.length) {
                await setMessage(messages.join('  '), true);
                game._command_mode = 'wizIntrinsicMore';
                game._clear_wiz_intrinsic_more_after_capture = 1;
            }
        }
        return;
    }

    if (game._command_mode === 'wizIntrinsicMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._pending_message = '';
            game._message_more = 0;
            game._keep_pending_message = 0;
            if (game._pending_message && game._swallow_overlay_active && game.u?.uswallow) refreshSwallowOverlay(false);
            await flush_screen(1);
            return;
        }
        game._command_mode = null;
        await rhack(ch);
        return;
    }

    if (game._command_mode === 'helpMenu') {
        if (ch === 'a') {
            if (!game._lua_version_info_initialized) {
                rn2(3);
                rn2(2);
                game._lua_version_info_initialized = 1;
            }
            setOverlay(VERSION_PAGE_1, 24, true);
            game._command_mode = 'versionInfo';
            return;
        }
        if (ch === 'b') {
            game._help_pager_name = 'help';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('help', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'c') {
            game._help_pager_name = 'hh';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('hh', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'd') {
            game._help_pager_name = 'history';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('history', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'e') {
            setOverlay(FARLOOK_MENU_LINES, 15);
            game._command_mode = 'farlookMenu';
            return;
        }
        if (ch === 'f') {
            await setMessage("Ask about '&' or '?' to get more info.", true);
            game._command_mode = 'whatDoesIntro';
            return;
        }
        if (ch === 'g') {
            game._help_pager_name = 'generated-options';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('generated-options', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'i') {
            game._help_pager_name = 'optmenu';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('optmenu', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'j') {
            game._help_pager_name = 'full-key-bindings';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('full-key-bindings', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'l') {
            game._help_pager_name = 'menu-control-help';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('menu-control-help', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'm') {
            game._help_pager_name = 'usagehlp';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('usagehlp', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'n') {
            game._help_pager_name = 'license';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('license', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === 'o') {
            game._help_pager_name = 'support-info';
            game._help_pager_page = 0;
            setOverlay(dataPagerLines('support-info', 0), 24, true);
            game._command_mode = 'helpPager';
            return;
        }
        if (ch === '\x1b' || ch === ' ') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'whatDoesIntro') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('What command?');
            game._command_mode = 'whatDoesCommand';
        }
        return;
    }

    if (game._command_mode === 'whatDoesCommand') {
        await setMessage(ch === 'i' ? 'i       show your inventory (#inventory).' : "I've never heard of such a command.");
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'helpPager') {
        if (ch === ' ') {
            const name = game._help_pager_name || 'help';
            const nextPage = (game._help_pager_page || 0) + 1;
            if (nextPage * 23 >= dataFileLines(name).length) {
                game._command_mode = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                return;
            }
            game._help_pager_page = nextPage;
            setOverlay(dataPagerLines(name, nextPage), 24, true);
            return;
        }
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'versionInfo') {
        if (ch === ' ') {
            setOverlay(VERSION_PAGE_2, 24, true);
            game._command_mode = 'versionInfo2';
            return;
        }
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'versionInfo2') {
        if (ch === ' ' || ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
        }
        return;
    }

    if (game._command_mode === 'attributes') {
        if (ch === ' ') {
            setOverlay(game._attributes_page_2 || genericAttributesPage2(), 24, true);
            game._command_mode = 'attributes2';
            return;
        }
        if (ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._attributes_page_2 = null;
            game._attributes_page_3 = null;
            if ((game.u?._statusSuffix || '').includes('Hallu') && game._hallu_refresh_after_level_teleport_move)
                game._hallu_refresh_after_fullscreen_overlay = 1;
        }
        return;
    }

    if (game._command_mode === 'attributes2') {
        if (ch === ' ' && game._attributes_page_3) {
            setOverlay(game._attributes_page_3, 24, true);
            game._command_mode = 'attributes3';
            return;
        }
        if (ch === ' ' || ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._attributes_page_2 = null;
            game._attributes_page_3 = null;
            if ((game.u?._statusSuffix || '').includes('Hallu') && game._hallu_refresh_after_level_teleport_move)
                game._hallu_refresh_after_fullscreen_overlay = 1;
        }
        return;
    }

    if (game._command_mode === 'attributes3') {
        if (ch === ' ' || ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._attributes_page_2 = null;
            game._attributes_page_3 = null;
            if ((game.u?._statusSuffix || '').includes('Hallu') && game._hallu_refresh_after_level_teleport_move)
                game._hallu_refresh_after_fullscreen_overlay = 1;
        }
        return;
    }

    if (game._command_mode === 'quiverObject') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '-') {
            for (const item of game.inventory || []) item.quivered = false;
            await setMessage('No ammunition readied.');
            game._command_mode = null;
            return;
        }
        const itemByLetter = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (itemByLetter?.line?.includes('alternate weapon')) {
            game._quiver_confirm_item = itemByLetter;
            await setMessage('That is your alternate weapon.  Ready it instead? [ynq] (q)');
            game._command_mode = 'quiverAlternateConfirm';
            return;
        }
        const item = itemByLetter && isProjectileItem(itemByLetter) ? itemByLetter : null;
        if (item) {
            for (const invItem of game.inventory || []) {
                invItem.quivered = invItem === item;
                if (invItem !== item && invItem.line) invItem.line = invItem.line.replace(/ \((?:at the ready|in quiver(?: pouch)?)\).*$/, '');
            }
            item.line = `${item.letter || '?'} - ${inventoryItemName(item)}${quiverSuffix(item)}`;
            await setMessage(`${item.line}.`);
            game._command_mode = null;
            return;
        }
        await setMessage("You don't have that object.");
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'quiverAlternateConfirm') {
        const item = game._quiver_confirm_item;
        game._quiver_confirm_item = null;
        game._command_mode = null;
        if (ch !== 'y' || !item) return;
        for (const invItem of game.inventory || []) {
            invItem.quivered = invItem === item;
            if (invItem !== item && invItem.line) invItem.line = invItem.line.replace(/ \((?:at the ready|in quiver(?: pouch)?)\).*$/, '');
        }
        item.line = `${item.letter || '?'} - ${inventoryItemName(item)}${quiverSuffix(item)}`;
        await setMessage(`${item.line}.`);
        return;
    }

    if (game._command_mode === 'inventory') {
        if (ch === ' ' && (game._inventory_page || 0) + 1 < (game._inventory_overlay_total_pages || 1)) {
            game._inventory_page = (game._inventory_page || 0) + 1;
            const lines = inventoryOverlayLines(game._inventory_page);
            const clearCol = Math.max(0, Math.min(...lines.map(([, col]) => col)) - 1);
            setOverlay(lines, 24, true, clearCol);
            return;
        }
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._inventory_page = 0;
            if ((game.u?._statusSuffix || '').includes('Hallu') && game._hallu_refresh_after_level_teleport_move)
                game._hallu_refresh_after_fullscreen_overlay = 1;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            game._inventory_action_letter = ch;
            const name = pickupObjectName(item).replace(/^(?:a|an|the) /, '');
            if (item.cls === 'spellbook') {
                setOverlay([
                    [0, 28, `Do what with the ${name}?`, 1],
                    [2, 28, `c - Name this specific ${name}`],
                    [3, 28, 'd - Drop this item'],
                    [4, 28, 'i - Adjust inventory by assigning new letter'],
                    [5, 28, 'r - Study this spellbook'],
                    [6, 28, 't - Throw this item'],
                    [7, 28, 'w - Wield this item in your hands'],
                    [8, 28, '\/ - Look up information about this'],
                    [9, 28, '(end)'],
                ], 10, false, 27);
                game._overlay_hide_status_only = 1;
                game._command_mode = 'inventoryAction';
                return;
            }
            setOverlay([
                [0, 34, `Do what with the ${name}?`, 1],
                [2, 34, `c - Name this specific ${name}`],
                [3, 34, 'd - Drop this item'],
                [4, 34, 'E - Write on the floor with this item'],
                [5, 34, 'i - Adjust inventory by assigning new letter'],
                [6, 34, inventoryActionApplyText(item)],
                [7, 34, 't - Throw this item'],
                [8, 34, 'w - Wield this item in your hands'],
                [9, 34, '\/ - Look up information about this'],
                [10, 34, '(end)'],
            ], 11, true);
            game._command_mode = 'inventoryAction';
        }
        return;
    }

    if (game._command_mode === 'inventoryAction') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._inventory_action_letter);
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n' || !item) {
            game._inventory_action_letter = '';
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            return;
        }
        if (ch === 't') {
            game._throw_item_letter = item.letter;
            game._inventory_action_letter = '';
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            await setMessage('In what direction?');
            game._command_mode = 'throwDirection';
        }
        if (ch === 'a' && isPotionObject(item)) {
            game._inventory_action_letter = '';
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            if (isPotionOfOil(item)) {
                game._command_mode = null;
                await applyPotionOfOil(item);
                return;
            }
            const targets = dipTargetsForSource(item);
            if (!targets.length) {
                game._command_mode = null;
                await setMessage('Never mind.');
                return;
            }
            game._dip_source_item = item;
            game._dip_source_object = item.letter || '';
            const targetLetters = targets.map(target => target.letter).filter(Boolean).sort().join('');
            await setMessage(`What do you want to dip into ${dipSourceDescription(item)}? [${inventoryLetterMenu(targetLetters)} or ?*]`);
            game._command_mode = 'dipIntoTarget';
        }
        return;
    }

    if (game._command_mode === 'unlockDoorPrompt') {
        const target = game._locked_door_unlock_target;
        game._command_mode = null;
        game._locked_door_unlock_target = null;
        if (ch === 'y' && target) {
            const loc = game.level?.at(target.x, target.y);
            if (loc?.typ === DOOR) {
                const dex = game.u?.acurr?.a?.[3] ?? 10;
                const chance = 3 * dex + (game.urole?.name?.m === 'Rogue' ? 30 : 0);
                if (rn2(100) >= chance) {
                    game.context.move = 1;
                    return;
                }
                loc.doormask = D_CLOSED;
                loc._picked_lock = 1;
                newsym(target.x, target.y);
            }
            rn2(19);
            await setMessage('You succeed in picking the lock.');
            game.context.move = 1;
        }
        return;
    }

    if (game._command_mode === 'unlockContainerPrompt') {
        const target = game._locked_container_unlock_target;
        game._command_mode = null;
        game._locked_container_unlock_target = null;
        if (ch === 'y' && target) {
            const chest = target.chest || target;
            const tool = target.tool;
            const name = tool ? inventoryItemName(tool).toLowerCase() : 'lock pick';
            const dex = game.u?.acurr?.a?.[3] ?? 10;
            const rogue = game.urole?.name?.m === 'Rogue';
            let chance = name.includes('credit card') ? dex + (rogue ? 20 : 0)
                : name.includes('key') ? 75 + dex
                : 4 * dex + (rogue ? 25 : 0);
            if (tool?.cursed) chance = Math.trunc(chance / 2);
            const action = name.includes('lock pick') || name.includes('credit card')
                ? 'picking the lock'
                : chest.kind === 'chest' || chest.otyp === CHEST ? 'unlocking the chest' : 'unlocking the box';
            game._pick_lock_occupation = { chest, chance, action, usedtime: 0 };
            game.context.move = 1;
            game._process_time_with_more = 1;
        }
        return;
    }

    if (game._command_mode === 'optionsFull') {
        const optionId = FULL_OPTION_KEYS[game._options_full_page || 1]?.[ch];
        if (optionId) {
            const selected = game._options_full_selected || [];
            const idx = selected.indexOf(optionId);
            if (idx >= 0) selected.splice(idx, 1);
            else selected.push(optionId);
            game._options_full_selected = selected;
            setFullOptionsPage(game._options_full_page || 1);
            return;
        }
        if (ch === ' ') {
            const page = game._options_full_page || 1;
            if (page < 7) {
                setFullOptionsPage(page + 1);
                return;
            }
            const selected = new Set(game._options_full_selected || []);
            const messages = [];
            game.flags = game.flags || {};
            game._options_full_apply = [];
            for (const id of FULL_OPTION_APPLY_ORDER) {
                if (!selected.has(id)) continue;
                if (id === 'pickup_types') {
                    game._options_full_pickup_pending = 1;
                    continue;
                }
                if (!(id in FULL_OPTION_DEFAULTS)) continue;
                const before = id === 'autopickup'
                    ? !!game._autopickup
                    : id === 'hilite_pet'
                        ? !!game._hilite_pet
                        : id === 'hilite_pile'
                            ? !!game._hilite_pile
                            : !!(game.flags[id] ?? FULL_OPTION_DEFAULTS[id]);
                const after = !before;
                game._options_full_apply = [...(game._options_full_apply || []), { id, after }];
                messages.push(`'${id}' option toggled ${after ? 'on' : 'off'}.`);
            }
            const groupedMessages = [];
            for (let i = 0; i < messages.length; i += 2)
                groupedMessages.push(messages[i + 1] ? `${messages[i]}  ${messages[i + 1]}` : messages[i]);
            const groupedChanges = [];
            for (let i = 0; i < (game._options_full_apply || []).length; i += 2)
                groupedChanges.push((game._options_full_apply || []).slice(i, i + 2));
            game._options_full_messages = groupedMessages;
            game._options_full_change_groups = groupedChanges;
            game._options_full_applied_ids = [];
            game._options_full_message_group_index = 0;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
            game._options_full_page = 0;
            game._options_full_selected = [];
            if (groupedMessages.length) {
                const next = groupedMessages.shift();
                game._options_full_messages = groupedMessages;
                applyFullOptionChanges([
                    ...(groupedChanges[0] || []),
                    ...((groupedChanges[1] || []).slice(0, 1)),
                ]);
                game._options_full_message_group_index = 1;
                game._command_mode = 'optionsFullMessages';
                await setMessage(next, !!groupedMessages.length || !!game._options_full_pickup_pending);
                return;
            }
            applyFullOptionChanges(game._options_full_apply || []);
            game._options_full_apply = [];
            game._options_full_applied_ids = [];
            game._options_full_change_groups = [];
            game._options_full_message_group_index = 0;
            if (game._options_full_pickup_pending) {
                game._options_full_pickup_pending = 0;
                game._options_full_pickup_mode = 1;
                game._autopickup_type_picks = [];
                setOverlay([...autopickupTypesLines(), [0, 24, '']], 22);
                game._overlay_hide_status_only = 0;
                game._command_mode = 'autopickupTypes';
                return;
            }
            game._command_mode = null;
            await docrt();
            return;
        }
        if (ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._options_full_page = 0;
            game._options_full_selected = [];
        }
        return;
    }

    if (game._command_mode === 'options') {
        if (ch === 'o' && game._options_page === 1) {
            setOverlay([...autopickupTypesLines(), [0, 24, '']], 22);
            game._overlay_hide_status_only = 1;
            game._autopickup_type_picks = [];
            game._command_mode = 'autopickupTypes';
            return;
        }
        if (game._options_page === 1 && ch === 'a') {
            for (let row = 0; row < 24; row++) game.nhDisplay?.clearRow(row);
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 1;
            game._options_fruit_text = '';
            game._command_mode = 'optionsFruit';
            await docrt();
            await setMessage('Set fruit to what?');
            game._overlay_hide_status_only = 1;
            return;
        }
        if (game._options_page === 1 && 'defjlp'.includes(ch)) {
            if (ch === 'f') {
                game._autopickup = !game._autopickup;
                game.flags.pickup = game._autopickup;
            } else toggleFlag({
                d: 'autodig',
                e: 'autoopen',
                j: 'cmdassist',
                l: 'fireassist',
                p: 'pushweapon',
            }[ch], ['e', 'j', 'l'].includes(ch));
            setOverlay(optionsPage1Lines(), 24, true);
            return;
        }
        if (game._options_page === 2 && 'eflp'.includes(ch)) {
            if (ch === 'e') game._hilite_pet = !game._hilite_pet;
            else if (ch === 'f') game._hilite_pile = !game._hilite_pile;
            else if (ch === 'l') toggleFlag('showexp');
            else toggleFlag('time');
            game._options_page = 1;
            setOverlay(optionsPage1Lines(), 24, true);
            return;
        }
        if (ch === ' ' || ch === '>') {
            if (game._options_page === 2) {
                game._command_mode = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._options_page = 0;
                await docrt();
                return;
            }
            game._options_page = 2;
            setOverlay(optionsPage2Lines(), 21, true);
            return;
        }
        if (ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._options_page = 0;
        }
        return;
    }

    if (game._command_mode === 'optionsFruit') {
        if (ch === '\r' || ch === '\n') {
            setCurrentFruitName(game._options_fruit_text || 'slime mold');
            game._options_fruit_text = '';
            game._overlay_hide_status_only = 0;
            game._options_page = 1;
            setOverlay(optionsPage1Lines(), 24, true);
            game._command_mode = 'options';
            return;
        }
        if (ch === '\x1b') {
            game._options_fruit_text = '';
            game._overlay_hide_status_only = 0;
            game._options_page = 1;
            setOverlay(optionsPage1Lines(), 24, true);
            game._command_mode = 'options';
            return;
        }
        if (key >= 32) game._options_fruit_text = `${game._options_fruit_text || ''}${ch}`;
        await setMessage(`Set fruit to what?${game._options_fruit_text ? ` ${game._options_fruit_text}` : ''}`);
        game._overlay_hide_status_only = 1;
        return;
    }

    if (game._command_mode === 'autopickupTypes') {
        const mappedPick = { a: '$', b: '"', f: '?', g: '+', h: '!', i: '=', j: '/' }[ch]
            || ('$"?+!/='.includes(ch) ? ch : '');
        if (mappedPick) {
            const picks = game._autopickup_type_picks || [];
            const idx = picks.indexOf(mappedPick);
            if (idx >= 0) picks.splice(idx, 1);
            else picks.push(mappedPick);
            game._autopickup_type_picks = picks;
            setOverlay([...autopickupTypesLines(picks), [0, 24, '']], 22);
            game._overlay_hide_status_only = game._options_full_pickup_mode ? 0 : 1;
            return;
        }
        if (ch === '\r' || ch === '\n') {
            game._autopickup_types = ['$', '"', '?', '+', '!', '=', '/']
                .filter(key => (game._autopickup_type_picks || []).includes(key))
                .join('') || 'all';
            game.flags.pickup_types = game._autopickup_types;
            if (game._options_full_pickup_mode) {
                game._options_full_pickup_mode = 0;
                game._command_mode = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._overlay_hide_status_only = 0;
                await docrt();
                return;
            }
            setOverlay(optionsPage1Lines(), 24, true);
            game._command_mode = 'options';
            game._options_page = 1;
            game._overlay_hide_status_only = 0;
            return;
        }
        if (ch === ' ' || ch === '\x1b') {
            game._command_mode = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._overlay_hide_status_only = 0;
        }
        return;
    }

    if (game._command_mode === 'breakWandConfirm') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_break_wand_letter);
        if (ch === 'y' && item && canApplyNoEffectWandBreak(item)) {
            game._apply_break_wand_letter = null;
            game._command_mode = null;
            const messages = applyNoEffectWandBreak(item);
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (ch === 'n' || ch === 'q' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n'
            || !item || !canApplyNoEffectWandBreak(item)) {
            game._apply_break_wand_letter = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'applyStoneObject') {
        const stone = findApplyInventoryItem(game._apply_stone_letter);
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._apply_stone_letter = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (!stone || !isGrayStoneApplyItem(stone)) {
            game._apply_stone_letter = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            setUseStoneObjectMenu(useStoneMenuItems(ch, stone));
            game._command_mode = 'applyStoneObject';
            return;
        }
        const target = findApplyInventoryItem(ch);
        if (!target) {
            game._topline_after_more = useStonePromptMessage(stone);
            await setMessage("You don't have that object.", true);
            return;
        }
        await finishUseStone(stone, target);
        return;
    }

    if (game._command_mode === 'applyObject') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '?' || ch === '*') {
            setApplyObjectMenu(applyMenuItems(ch));
            game._command_mode = 'applyObject';
            return;
        }
        const item = findApplyInventoryItem(ch);
        if (!item) {
            game._topline_after_more = applyPromptMessage();
            await setMessage("You don't have that object.", true);
            return;
        }
        const name = inventoryItemName(item).toLowerCase();
        game._command_mode = null;
        if (isApplyCoinObject(item)) {
            const messages = applyCoinFlip(item);
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (isPotionObject(item) && !isPotionOfOil(item)) {
            await setMessage("Sorry, I don't know how to use that.");
            return;
        }
        if (isGrayStoneApplyItem(item)) {
            await beginUseStone(item);
            return;
        }
        if (applySelectionKind(item) === 'selectableInvalid' && !isPotionOfOil(item)) {
            await setMessage("Sorry, I don't know how to use that.");
            return;
        }
        if (isFigurineObject(item)) {
            game._apply_figurine_letter = item.letter;
            await setMessage('In what direction?');
            game._command_mode = 'applyFigurineDirection';
            return;
        }
        if (name.includes('cream pie')) {
            rnd(25);
            game.u.blind = 1;
            for (const mon of game.level?.monsters || []) newsym(mon.mx, mon.my);
            game._cream_pie_after_more_letter = item.letter;
            game._topline_after_more = "You can't see through all the sticky goop on your face.";
            await setMessage(`You immerse your face in ${(item.quan || 1) > 1 ? 'one of the cream pies' : 'the cream pie'}.`, true);
            return;
        }
        if (isRoyalJelly(item)) {
            await beginRoyalJellyRub(item);
            return;
        }
        if (isTinOpenerObject(item)) {
            await beginTinOpenerUse(item);
            return;
        }
        if (toolChargeKind(item) === 'tinning kit' || name.includes('tinning kit')) {
            await beginTinningKitUse(item);
            return;
        }
        if (isCrystalBallObject(item)) {
            await beginCrystalBallUse(item);
            return;
        }
        if (musicalInstrumentKind(item)) {
            await beginMusicalInstrumentUse(item);
            return;
        }
        if (isBellOfOpeningItem(item)) {
            const messages = applyBellOfOpening(item);
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (isPotionOfOil(item)) {
            await applyPotionOfOil(item);
            return;
        }
        if (isPickDigItem(item)) {
            if (!itemIsWielded(item)) {
                const line = wieldItemForApply(item);
                game._queued_pick_dig_apply_letter = item.letter;
                await setMessage(`${line}.`);
                game.context.move = 1;
                return;
            }
            game._apply_pick_dig_letter = item.letter;
            await setMessage('In what direction do you want to dig?');
            game._command_mode = 'applyPickDigDirection';
            return;
        }
        if (isPolearmItem(item)) {
            if (!itemIsWielded(item)) {
                const line = wieldItemForApply(item);
                await setMessage(`${line}.`);
                game.context.move = 1;
                return;
            }
            game._apply_polearm_letter = item.letter;
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            game._cursor_override = [(game._farlook_x || 0) - 1, (game._farlook_y || 0) + 1];
            await setMessage('Where do you want to hit?');
            game._command_mode = 'applyPolearmTarget';
            return;
        }
        if (canApplyNoEffectWandBreak(item)) {
            game._apply_break_wand_letter = item.letter;
            await setMessage(`Are you really sure you want to break ${wandBreakObjectName(item)}? [yn] (n)`);
            game._command_mode = 'breakWandConfirm';
            return;
        }
        if (['armor', 'weapon', 'amulet', 'ring', 'gem', 'food'].includes(item.cls)) {
            await setMessage("Sorry, I don't know how to use that.");
            return;
        }
        if (isIceBoxObject(item)) {
            setIceBoxActionMenu(item);
            return;
        }
        if (isBagOfTricksObject(item)) {
            const messages = await applyBagOfTricksOnce(item, { tipping: false });
            if (messages.length) await setMessage(messages.join('  '), messages.length > 1);
            else await setMessage('');
            game.context.move = 1;
            return;
        }
        if (isHornOfPlentyObject(item)) {
            const messages = applyHornOfPlentyOnce(item);
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (/bag|sack/.test(name)) {
            const lossMessages = [];
            const lossDetails = { lost: 0, lostMerchandise: 0 };
            if (magicBagContentsLoss(item, lossMessages, { details: lossDetails })) {
                item.cknown = true;
                if (lossDetails.lostMerchandise > 0) game.context.move = 1;
            }
            game._container_letter = item.letter;
            setOverlay(LOOT_BAG_MENU_LINES, 11);
            if (lossMessages.length) {
                const lossText = lossMessages.join('  ');
                game._pending_message = lossText;
                game._last_pline_message = lossText;
                game._message_more = lossMessages.length > 1 ? 1 : 0;
                game._keep_pending_message = 1;
            }
            game._command_mode = 'bagAction';
            return;
        }
        if (/lock pick|credit card|key/.test(name)) {
            await setMessage('In what direction?');
            game._command_mode = 'applyLockPickDirection';
            return;
        }
        if (name.includes('stethoscope')) {
            await setMessage('In what direction?');
            game._command_mode = 'stethoscopeDirection';
            return;
        }
        if (toolChargeKind(item) === 'expensive camera' || name.includes('camera')) {
            if (game.u?.underwater || game.u?.uunderwater) {
                await setMessage('Using your camera underwater would void the warranty.');
                return;
            }
            game._apply_camera_letter = item.letter;
            await setMessage('In what direction?');
            game._command_mode = 'cameraDirection';
            return;
        }
        if (toolChargeKind(item) === 'can of grease' || name.includes('can of grease')) {
            if (heroHasSlipperyFingers()) {
                const messages = [`The ${pickupObjectName(item)} slips from your fingers.`];
                dropCarriedObjectAtHero(item, messages);
                await setMessage(messages.join('  '), messages.length > 1);
                game.context.move = 1;
                return;
            }
            if ((item.spe ?? 0) <= 0) {
                await setMessage(`The ${pickupObjectName(item)} ${item.known ? 'is' : 'seems to be'} empty.`);
                game.context.move = 1;
                return;
            }
            if ((item.cursed || heroIsFumbling()) && !rn2(2)) {
                const messages = [];
                spendChargedToolUse(item, messages);
                messages.push(`The ${pickupObjectName(item)} slips from your fingers.`);
                dropCarriedObjectAtHero(item, messages);
                await setMessage(messages.join('  '), messages.length > 1);
                game.context.move = 1;
                return;
            }
            game._apply_grease_letter = item.letter;
            await setMessage(greaseTargetPrompt());
            game._command_mode = 'greaseObject';
            return;
        }
        if (name.includes('magic marker')) {
            await setMessage('What do you want to write on? [*]');
            game._apply_marker_letter = item.letter;
            game._command_mode = 'markerWriteObject';
            return;
        }
        if (toolChargeKind(item) === 'drum of earthquake'
            && !heroIsConfused() && !heroIsStunned() && (item.spe ?? 0) > 0) {
            const usageMessages = [];
            checkUnpaidUsage(item, usageMessages);
            item.spe = Math.max(0, (item.spe ?? 0) - 1);
            updateChargedItemLine(item);
            item.known = true;
            item.dknown = true;
            const earthquakeMessages = await doEarthquake(Math.trunc(((game.u?.ulevel || 1) - 1) / 3) + 1);
            const messages = [
                ...usageMessages,
                'You produce a heavy, thunderous rolling!',
                `The entire ${earthquakeLevelDescription()} is shaking around you!`,
                ...earthquakeMessages,
            ];
            for (const mon of game.level?.monsters || []) {
                mon.msleeping = 0;
                mon.mcanmove = true;
                mon.mfrozen = 0;
            }
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (name.includes('drum')) {
            const deaf = (game.u?._statusSuffix || '').includes('Deaf');
            rn2(2);
            const noteCount = rnd(5);
            for (let i = 0; i < noteCount; i++) rn2(7);
            if (!deaf && game.u) {
                game.u._deafTimeout = Math.max(game.u._deafTimeout || 0, rn1(20, 30));
                game._deaf_after_more = 1;
                await setMessage('You start playing your drum.  You beat a deafening row!', true);
            } else {
                await setMessage('You start playing your drum.  You pound on the drum.');
            }
            exerciseAttribute(A_WIS, false);
            const drumDistance = (game.u?.ulevel || 1) * 40;
            const scareDistance = Math.trunc(drumDistance / 3);
            const fleeMessages = [];
            for (const mon of game.level?.monsters || []) {
                if (!mon || (mon.mhp != null && mon.mhp <= 0)) continue;
                const dx = (mon.mx || 0) - (game.u?.ux || 0);
                const dy = (mon.my || 0) - (game.u?.uy || 0);
                const dist = dx * dx + dy * dy;
                if (dist >= drumDistance) continue;
                mon.msleeping = 0;
                mon.mcanmove = true;
                mon.mfrozen = 0;
                if (dist >= scareDistance || mon.data?.mindless) continue;
                const dlev = Math.min(50, Math.max(1, mon.m_lev ?? mon.data?.mlevel ?? 1));
                const resisted = rn2(110 - dlev) < (mon.data?.mr || 0);
                if (resisted) continue;
                const wasFleeing = !!mon.mflee;
                mon.mflee = 1;
                mon.mfleetim = 0;
                if (!wasFleeing && !game.u?.blind && (game.viz_array?.[mon.my]?.[mon.mx] & IN_SIGHT)) {
                    const monName = mon.givenName || mon.shknam || `The ${mon.data?.name || 'monster'}`;
                    fleeMessages.push(`${monName} turns to flee.`);
                }
            }
            if (fleeMessages.length) game._topline_after_more = fleeMessages.join('  ');
            game.context.move = 1;
            return;
        }
        if (isLampObject(item)) {
            await applyLamp(item);
            return;
        }
        await setMessage('Nothing happens.');
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'applyPolearmTarget') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_polearm_letter);
        if (ch === '\x1b') {
            game._apply_polearm_letter = null;
            game._cursor_override = null;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            return;
        }
        const dir = movementDirection(ch === '\r' || ch === '\n' ? '.' : ch);
        if (dir) {
            const steps = ch !== ch.toLowerCase() ? 8 : 1;
            const cursor = truncateCursorToMap(
                game._farlook_x || game.u?.ux || 0,
                game._farlook_y || game.u?.uy || 0,
                dir.dx * steps,
                dir.dy * steps,
            );
            game._farlook_x = cursor.x;
            game._farlook_y = cursor.y;
            game._cursor_override = [cursor.x - 1, cursor.y + 1];
            const dist2 = (cursor.x - (game.u?.ux || 0)) ** 2 + (cursor.y - (game.u?.uy || 0)) ** 2;
            const validRange = dist2 === 4;
            await setMessage(validRange ? polearmTargetDescription(cursor.x, cursor.y)
                : `${polearmTargetDescription(cursor.x, cursor.y)} (${dist2 < 4 ? 'too close' : 'too far'})`);
            return;
        }
        if (ch === '.' || ch === ' ' || ch === '\r' || ch === '\n') {
            const x = game._farlook_x || game.u?.ux || 0;
            const y = game._farlook_y || game.u?.uy || 0;
            game._apply_polearm_letter = null;
            game._cursor_override = null;
            game._command_mode = null;
            if (!item || !isPolearmItem(item)) return;
            const dist2 = (x - (game.u?.ux || 0)) ** 2 + (y - (game.u?.uy || 0)) ** 2;
            if (dist2 > 4) {
                await setMessage('Too far!');
                return;
            }
            if (dist2 < 4) {
                await setMessage('Too close!');
                return;
            }
            if (!couldsee(x, y)) {
                await setMessage("You can't reach that spot from here.");
                return;
            }

            const statueObj = floorStatueAt(x, y);
            if (statueObj) {
                const statueTrap = statueTrapAt(x, y);
                const message = statueTrap
                    ? await activateStatueTrap(statueTrap, x, y, { normal: true }) || ''
                    : 'Thump!  Your blow bounces harmlessly off the statue.';
                await setMessage(message);
                game.context.move = 1;
                return;
            }

            const boulder = (game.level?.objects || []).some(obj =>
                !obj.hidden && !obj.transientProjectile && obj.ox === x && obj.oy === y && obj.otyp === BOULDER);
            if (boulder) {
                await setMessage('Thump!  Your blow bounces harmlessly off the boulder.');
                game.context.move = 1;
                return;
            }
            const loc = game.level?.at(x, y);
            if (!loc || !ACCESSIBLE(loc.typ)) {
                await setMessage(`You uselessly attack ${loc?.typ === STONE ? 'stone' : 'an unknown obstacle'}.`);
                game.context.move = 1;
                return;
            }
            await setMessage('You miss; there is no one there to hit.');
            game.context.move = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'applyPickDigDirection') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_pick_dig_letter);
        game._apply_pick_dig_letter = null;
        game._command_mode = null;
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            await setMessage('Never mind.');
            return;
        }
        const dir = figurineApplyDirection(ch);
        if (!dir) return;
        if (!item || !isPickDigItem(item) || !itemIsWielded(item)) return;
        if (dir.dz < 0) {
            await setMessage(game.u?.levitating ? "You don't have enough leverage." : "You can't reach the ceiling.");
            game.context.move = 1;
            return;
        }
        if (dir.dz > 0) {
            game._pick_dig_occupation = {
                itemLetter: item.letter,
                x: game.u?.ux || 0,
                y: game.u?.uy || 0,
                down: true,
                effort: 0,
            };
            await setMessage('You start digging downward.');
            game.context.move = 1;
            game._process_time_with_more = 1;
            return;
        }
        if (!dir.dx && !dir.dy) {
            const damage = Math.max(1, rnd(2) + (item?.spe || 0));
            if (game.u) {
                game.u.uhp = Math.max(0, (game.u.uhp || 1) - damage);
                if ((game.u.uhp || 0) <= 0) game._death_cause = `hit ${game.flags?.female ? 'herself' : 'himself'} with a pick-axe`;
            }
            await setMessage(`You hit yourself with ${pickupObjectName(item)}.`);
            game.context.move = 1;
            return;
        }

        const x = (game.u?.ux || 0) + dir.dx;
        const y = (game.u?.uy || 0) + dir.dy;
        if (floorStatueAt(x, y)) {
            game._pick_dig_occupation = {
                itemLetter: item.letter,
                x,
                y,
                effort: 0,
                didMessage: false,
            };
            await setMessage('You start chipping the statue.');
            game.context.move = 1;
            game._process_time_with_more = 1;
            return;
        }

        await setMessage(`You swing your ${pickupObjectName(item)} through thin air.`);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'cameraDirection') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_camera_letter);
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._apply_camera_letter = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        const dir = movementDirection(ch);
        const verticalDir = !dir && ch === '<' ? { dx: 0, dy: 0, dz: -1 }
            : !dir && ch === '>' ? { dx: 0, dy: 0, dz: 1 } : null;
        const selfZap = ch === '.';
        if (!dir && !verticalDir && !selfZap) {
            game._keep_pending_message = 1;
            return;
        }
        game._apply_camera_letter = null;
        game._command_mode = null;
        if (!item) return;
        const messages = [];
        if (!spendChargedToolUse(item, messages)) {
            await setMessage('Nothing happens.');
            game.context.move = 1;
            return;
        }
        const flashHero = item.cursed && !rn2(2);
        if (flashHero || selfZap) {
            const duration = 5 + rnd(25);
            if (game.u) {
                game.u.blind = true;
                game.u._blindTimeout = (game.u._blindTimeout || 0) + duration;
            }
            messages.push('You are blinded by the flash!');
        } else if (verticalDir) {
            messages.push(`You take a picture of the ${verticalDir.dz > 0 ? 'floor' : 'ceiling'}.`);
        } else if (dir) {
            let x = game.u?.ux || 0;
            let y = game.u?.uy || 0;
            let target = null;
            for (let range = 0; range < COLNO; range++) {
                x += dir.dx;
                y += dir.dy;
                if (x <= 0 || x >= COLNO || y < 0 || y >= ROWNO) break;
                target = game.level?.monsters?.find(mon =>
                    mon.mx === x && mon.my === y && (mon.mhp == null || mon.mhp > 0));
                if (target) break;
                const loc = game.level?.at?.(x, y);
                if (!ZAP_POS(loc?.typ ?? STONE)) break;
            }
            if (target) {
                target.msleeping = 0;
                target.mcansee = false;
                target.mblinded = Math.max(target.mblinded || 0, rnd(20));
                if (!game.u?.blind && (game.viz_array?.[target.my]?.[target.mx] & IN_SIGHT))
                    messages.push(`The ${target.data?.name || 'monster'} is blinded by the flash!`);
            }
        }
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'crystalBallLook') {
        await finishCrystalBallUse(ch);
        return;
    }

    if (game._command_mode === 'instrumentImprovisePrompt') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_instrument_letter);
        if (ch === 'y') {
            await finishMusicalImprovisation(item);
            return;
        }
        if (ch === 'n') {
            if (!item) {
                game._apply_instrument_letter = '';
                game._command_mode = null;
                return;
            }
            if ((game.u?.uevent?.uheard_tune || 0) === 2) {
                await setMessage('Play the passtune? [ynq] (q)');
                game._command_mode = 'instrumentPasstunePrompt';
                return;
            }
            game._instrument_tune_text = '';
            await setMessage('What tune are you playing? [5 notes, A-G]');
            game._command_mode = 'instrumentTuneText';
            return;
        }
        if (ch === 'q' || ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._apply_instrument_letter = '';
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'instrumentPasstunePrompt') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_instrument_letter);
        if (ch === 'y') {
            await finishInstrumentTune(item, game.castleTune || '');
            return;
        }
        if (ch === 'n') {
            game._instrument_tune_text = '';
            await setMessage('What tune are you playing? [5 notes, A-G]');
            game._command_mode = 'instrumentTuneText';
            return;
        }
        if (ch === 'q' || ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._apply_instrument_letter = '';
            game._instrument_tune_text = '';
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'instrumentTuneText') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_instrument_letter);
        if (ch === '\x1b') {
            game._apply_instrument_letter = '';
            game._instrument_tune_text = '';
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '\r' || ch === '\n') {
            await finishInstrumentTune(item);
            return;
        }
        const code = ch.charCodeAt(0);
        if (code === 8 || code === 127 || ch === '\x7f') {
            game._instrument_tune_text = (game._instrument_tune_text || '').slice(0, -1);
        } else if (code >= 32) {
            game._instrument_tune_text = `${game._instrument_tune_text || ''}${ch}`;
        }
        await setMessage(`What tune are you playing? [5 notes, A-G]${game._instrument_tune_text ? ` ${game._instrument_tune_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'greaseInventoryOverlay') {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(greaseTargetPrompt());
            game._command_mode = 'greaseObject';
            return;
        }
        if (ch !== '-' && !(game.inventory || []).some(invItem => invItem.letter === ch && canSelectGreaseObject(invItem))) {
            game._keep_pending_message = 1;
            return;
        }
        game._command_mode = 'greaseObject';
    }

    if (game._command_mode === 'greaseObject') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._apply_grease_letter = null;
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '?' || ch === '*') {
            setOverlay(inventoryOverlayLines(0, false, canGreaseObject), 24, true);
            game._command_mode = 'greaseInventoryOverlay';
            return;
        }
        const grease = (game.inventory || []).find(invItem => invItem.letter === game._apply_grease_letter);
        const target = ch === '-' ? null
            : (game.inventory || []).find(invItem => invItem.letter === ch && canSelectGreaseObject(invItem));
        if (ch !== '-' && !target) {
            game._topline_after_more = greaseTargetPrompt();
            await setMessage("You don't have that object.", true);
            return;
        }
        if (target) {
            const inaccessible = greaseInaccessibleMessage(target);
            if (inaccessible) {
                game._apply_grease_letter = null;
                game._command_mode = null;
                await setMessage(inaccessible);
                return;
            }
        }
        game._apply_grease_letter = null;
        game._command_mode = null;
        if (!grease) return;
        const messages = [];
        if (!spendChargedToolUse(grease, messages)) {
            await setMessage(`The ${pickupObjectName(grease)} ${grease.known ? 'is' : 'seems to be'} empty.`);
            game.context.move = 1;
            return;
        }
        if (ch === '-') {
            addHeroGlibTimeout(rn1(11, 5));
            messages.push(`You coat your ${fingersOrGloves()} with grease.`);
        } else {
            messages.push(`You cover ${inventoryItemName(target)} with a thick layer of grease.`);
            target.greased = true;
            target.line = normalInventoryLine({ ...target, line: '' });
            if (target.unpaid) syncUnpaidBillLine(target);
            if (grease.cursed) {
                addHeroGlibTimeout(rn1(6, 10));
                messages.push(`Some of the grease gets all over your ${fingersOrGloves()}.`);
            }
        }
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'markerWriteInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = 'What do you want to write on? [*]';
            game._message_more = 0;
            game._message_more_line = '';
            game._command_mode = 'markerWriteObject';
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'markerWriteText') {
        if (ch === '\r' || ch === '\n') {
            await finishMarkerWriting();
            return;
        }
        if (ch === '\x1b') {
            game._apply_marker_letter = null;
            game._marker_write_paper_letter = null;
            game._marker_write_text = '';
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const code = typeof key === 'number' ? key : ch.charCodeAt(0);
        if (code === 8 || code === 127 || ch === '\x7f') {
            game._marker_write_text = (game._marker_write_text || '').slice(0, -1);
        } else if (code >= 32) {
            game._marker_write_text = `${game._marker_write_text || ''}${ch}`;
        }
        const paper = (game.inventory || []).find(invItem => invItem.letter === game._marker_write_paper_letter);
        const prompt = markerWritePrompt(paper);
        const text = game._marker_write_text || '';
        await setMessage(`${prompt}${text ? ` ${text}` : ''}`);
        return;
    }

    if (game._command_mode === 'markerWriteObject') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._apply_marker_letter = null;
            game._command_mode = null;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item) {
            await setMessage("You don't have that object.", true);
            game._command_mode = 'markerWriteInvalidMore';
            return;
        }
        const paperType = markerPaperType(item);
        if (!paperType) {
            await setMessage('That is a silly thing to write on.');
            game._command_mode = null;
            game._apply_marker_letter = null;
            return;
        }
        if (game.u?.blind && !item.dknown) {
            await setMessage(`You don't know whether that ${markerPaperTypeWord(item)} is blank or not.`);
            game._command_mode = null;
            game._apply_marker_letter = null;
            return;
        }
        if (game.u?.blind && paperType === 'spellbook') {
            await setMessage("Magic marker can't create braille text.");
            game._command_mode = null;
            game._apply_marker_letter = null;
            return;
        }
        if ((paperType === 'scroll' && !isBlankScrollItem(item))
            || (paperType === 'spellbook' && !isBlankSpellbookItem(item))) {
            await setMessage(`That ${markerPaperTypeWord(item)} is not blank!`);
            game._command_mode = null;
            game._apply_marker_letter = null;
            game.context.move = 1;
            return;
        }
        game._marker_write_paper_letter = item.letter;
        game._marker_write_text = '';
        await setMessage(markerWritePrompt(item));
        game._command_mode = 'markerWriteText';
        return;
    }

    if (game._command_mode === 'bagAction') {
        const bag = (game.inventory || []).find(item => item.letter === game._container_letter);
        if (!bag) {
            game._container_letter = null;
            game._command_mode = null;
            return;
        }
        bag.contents ??= [];
        if (ch === ':') {
            bag.cknown = true;
            const rows = [[0, 41, 'Contents of the bag:', 1]];
            const contents = liquidFlowContainerContents(bag);
            const coins = contents.find(item => item.cls === 'coin' || item.letter === '$');
            let row = 2;
            if (coins?.quan) {
                rows.push([row++, 41, 'Coins', 1]);
                rows.push([row++, 41, `$ - ${coins.quan} gold piece${coins.quan === 1 ? '' : 's'}`]);
            }
            for (const item of contents.filter(item => item !== coins))
                rows.push([row++, 41, item.line || `${item.letter || '?'} - ${inventoryItemName(item)}`]);
            rows.push([row, 41, '(end)']);
            setOverlay(rows, rows.length);
            game._command_mode = 'simpleOverlay';
            return;
        }
        if (ch === 'o') {
            bag.cknown = true;
            const entries = [];
            const contents = liquidFlowContainerContents(bag);
            const coins = contents.find(item => item.cls === 'coin' || item.letter === '$');
            if (coins?.quan) entries.push({ item: coins, letter: '$', category: 'coin', amount: coins.quan });
            for (const item of contents.filter(item => item !== coins))
                entries.push({ item, letter: item.letter || '?', category: item.cls || 'Other Items', amount: item.quan || 1 });
            const rows = [[0, 41, 'Take out what?', 1]];
            let row = 2, lastCategory = '';
            for (const entry of entries) {
                if (entry.category !== lastCategory) {
                    const type = BAG_PUT_CLASS_TYPES.find(type => type.key === entry.category);
                    rows.push([row, 34, '┌─────']);
                    rows.push([row, 40, ' '.repeat(40)]);
                    rows.push([row++, 41, type?.label || 'Other Items', 1]);
                    lastCategory = entry.category;
                }
                entry.row = row;
                const label = entry.letter === '$'
                    ? `${entry.amount} gold piece${entry.amount === 1 ? '' : 's'}`
                    : inventoryItemName(entry.item);
                rows.push([row, 34, '│']);
                rows.push([row++, 40, ` ${entry.letter} - ${label}`.padEnd(40, ' ')]);
            }
            rows.push([row, 34, '│']);
            rows.push([row, 40, ' (end)'.padEnd(40, ' ')]);
            game._bag_take_entries = entries;
            game._bag_take_selected = [];
            setOverlay(rows, 2, false, 0);
            game._command_mode = 'bagTakeObject';
            return;
        }
        if (ch === 'i') {
            const putItems = [];
            if (game._goldCount) {
                putItems.push({
                    item: { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount },
                    category: 'coin',
                });
            }
            for (const item of game.inventory || []) {
                if (item.letter === '$' || item.cls === 'coin') continue;
                let category = item.cls || '';
                if (item.otyp === GOLD_PIECE || item.glyph === '$') category = 'coin';
                else if (item.otyp === 'corpse' || item.otyp === CORPSE || item.otyp === FOOD_CLASS) category = 'food';
                else if (item.otyp === SCROLL_CLASS) category = 'scroll';
                else if (item.cls === 'spellbook') category = 'spellbook';
                else if (item.otyp === POTION_CLASS) category = 'potion';
                else if (item.otyp === RING_CLASS || item.glyph === '=') category = 'ring';
                else if (item.otyp === WAND_CLASS) category = 'wand';
                else if (BAG_OBJECT_TYPES.has(item.otyp) || item.otyp === OIL_LAMP || item.otyp === MAGIC_LAMP
                    || item.otyp === MIRROR || item.otyp === EXPENSIVE_CAMERA || item.otyp === STETHOSCOPE
                    || item.otyp === MAGIC_MARKER || item.cls === 'tool' || item.glyph === '(') category = 'tool';
                else if (item.otyp === GEM_CLASS || item.glyph === '*') category = 'gem';
                else if (item.otyp === BOULDER) category = 'rock';
                putItems.push({ item, category });
            }
            const presentTypes = BAG_PUT_CLASS_TYPES.filter(type => putItems.some(entry => entry.category === type.key));
            const choices = [{ key: 'auto', letter: 'A', label: 'Auto-select every relevant item' }];
            const rows = [[0, 23, 'Put in what type of objects?', 1]];
            rows.push([2, 23, 'A - Auto-select every relevant item']);
            rows.push([3, 27, '(ignored unless some other choices are also picked)']);
            let row = 5;
            if (presentTypes.length > 1) {
                choices.push({ key: 'all', letter: 'a', label: 'All types', row });
                rows.push([row++, 23, 'a - All types']);
            }
            let letterIndex = presentTypes.length > 1 ? 1 : 0;
            for (const type of presentTypes) {
                const letter = 'abcdefghijklmnopqrstuvwxyz'[letterIndex++];
                choices.push({ ...type, letter, row });
                rows.push([row++, 23, `${letter} - ${type.label}`]);
            }
            const extraChoices = [];
            if (putItems.some(({ item }) => objectBucCategory(item) === 'blessed'))
                extraChoices.push({ key: 'blessed', letter: 'B', label: 'Items known to be Blessed' });
            if (putItems.some(({ item }) => objectBucCategory(item) === 'cursed'))
                extraChoices.push({ key: 'cursed', letter: 'C', label: 'Items known to be Cursed' });
            if (putItems.some(({ item }) => objectBucCategory(item) === 'uncursed'))
                extraChoices.push({ key: 'uncursed', letter: 'U', label: 'Items known to be Uncursed' });
            if (putItems.some(({ item }) => objectBucCategory(item) === 'unknown'))
                extraChoices.push({ key: 'unknown', letter: 'X', label: 'Items of unknown Bless/Curse status' });
            if (game._just_picked_gold && game._goldCount)
                extraChoices.push({
                    key: 'justpicked',
                    letter: 'P',
                    label: `Just picked up: ${game._goldCount} gold piece${game._goldCount === 1 ? '' : 's'}`,
                });
            if (extraChoices.length) row++;
            for (const choice of extraChoices) {
                choice.row = row;
                choices.push(choice);
                rows.push([row++, 23, `${choice.letter} - ${choice.label}`]);
            }
            rows.push([row, 23, '(end)']);
            game._bag_put_type_choices = choices;
            game._bag_put_type_selected = [];
            game._bag_put_items = putItems;
            setOverlay(rows, row + 1, false, 23);
            game._command_mode = 'bagPutTypes';
            return;
        }
        game._container_letter = null;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'floorBagAction') {
        const bag = game._floor_container_object;
        if (!bag || !(game.level?.objects || []).includes(bag)) {
            game._floor_container_object = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            return;
        }
        bag.contents ??= [];
        if (ch === 'o' || ch === ':') {
            bag.cknown = true;
            game._floor_container_object = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            const contents = liquidFlowContainerContents(bag);
            if (!contents.length) {
                const emptyMessage = bag._floorLootMbagLossEmptied ? 'The bag is now empty.' : 'The bag is empty.';
                delete bag._floorLootMbagLossEmptied;
                await setMessage(emptyMessage);
                if (ch === 'o') game.context.move = 1;
                return;
            }
            delete bag._floorLootMbagLossEmptied;
            const rows = [[0, 41, ch === ':' ? 'Contents of the bag:' : 'Take out what?', 1]];
            let row = 2;
            for (const item of contents) rows.push([row++, 41, pickupObjectPhrase(item)]);
            rows.push([row, 41, '--More--']);
            setOverlay(rows, rows.length);
            game._command_mode = 'simpleOverlay';
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            game._floor_container_object = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'bagPutTypes') {
        const choices = game._bag_put_type_choices || [];
        const selected = new Set(game._bag_put_type_selected || []);
        const choice = choices.find(entry => entry.letter === ch || entry.symbol === ch);
        if (ch === '\x1b' || ch === 'q') {
            game._container_letter = null;
            game._bag_put_type_choices = null;
            game._bag_put_type_selected = null;
            game._bag_put_items = null;
            game._overlay_lines = null;
            game._command_mode = null;
            return;
        }
        if (choice) {
            if (selected.has(choice.key)) selected.delete(choice.key);
            else selected.add(choice.key);
            game._bag_put_type_selected = [...selected];
            game._overlay_lines = (game._overlay_lines || []).map(line => {
                const row = line[0];
                const rowChoice = choices.find(entry => entry.row === row);
                if (!rowChoice) return line;
                const marker = selected.has(rowChoice.key) ? '+' : '-';
                return [row, line[1], `${rowChoice.letter} ${marker} ${rowChoice.label}`, line[3]];
            });
            return;
        }
        if (ch === '\r' || ch === '\n' || ch === ' ') {
            if (!selected.size || (selected.size === 1 && selected.has('auto'))) {
                await setMessage('No relevant items selected.');
                game._container_letter = null;
                game._bag_put_type_choices = null;
                game._bag_put_type_selected = null;
                game._bag_put_items = null;
                game._command_mode = null;
                return;
            }
            const items = (game._bag_put_items || []).filter(({ item, category }) => {
                if (selected.has('all')) return true;
                if (selected.has(category)) return true;
                const bucCategory = objectBucCategory(item);
                if (selected.has('blessed') && bucCategory === 'blessed') return true;
                if (selected.has('cursed') && bucCategory === 'cursed') return true;
                if (selected.has('uncursed') && bucCategory === 'uncursed') return true;
                return selected.has('unknown') && bucCategory === 'unknown'
                    || selected.has('justpicked') && item.letter === '$' && game._just_picked_gold;
            });
            const entries = items.map(({ item, category }) => ({
                item,
                category,
                letter: item.letter,
                amount: item.letter === '$' && selected.has('justpicked')
                    ? game._goldCount || 0
                    : item.quan || 1,
            }));
            const rows = [[0, 41, 'Put in what?', 1]];
            let row = 2;
            let lastCategory = '';
            for (const entry of entries) {
                if (entry.category !== lastCategory) {
                    const type = BAG_PUT_CLASS_TYPES.find(type => type.key === entry.category);
                    rows.push([row, 34, '┌─────']);
                    rows.push([row, 40, ' '.repeat(40)]);
                    rows.push([row++, 41, type?.label || 'Other Items', 1]);
                    lastCategory = entry.category;
                }
                entry.row = row;
                const label = entry.letter === '$'
                    ? `${entry.amount} gold piece${entry.amount === 1 ? '' : 's'}`
                    : inventoryItemName(entry.item);
                rows.push([row, 34, '│']);
                rows.push([row++, 40, ` ${entry.letter} - ${label}`.padEnd(40, ' ')]);
            }
            rows.push([row, 34, '│']);
            rows.push([row, 40, ' (end)'.padEnd(40, ' ')]);
            game._bag_put_entries = entries;
            game._bag_put_selected = [];
            setOverlay(rows, 2, false, 0);
            game._command_mode = 'bagPutObject';
            return;
        }
        return;
    }

    if (game._command_mode === 'bagPutObject') {
        const bag = (game.inventory || []).find(item => item.letter === game._container_letter);
        if (!bag) {
            game._container_letter = null;
            game._command_mode = null;
            return;
        }
        bag.contents ??= [];
        if (game._bag_put_entries) {
            const entries = game._bag_put_entries || [];
            const selected = new Set(game._bag_put_selected || []);
            const entry = entries.find(item => item.letter === ch);
            if (entry) {
                if (selected.has(entry.letter)) selected.delete(entry.letter);
                else selected.add(entry.letter);
                game._bag_put_selected = [...selected];
                game._overlay_lines = (game._overlay_lines || []).map(line => {
                    const rowEntry = entries.find(item => item.row === line[0]);
                    if (!rowEntry || line[1] !== 40) return line;
                    const marker = selected.has(rowEntry.letter) ? '+' : '-';
                    const label = rowEntry.letter === '$'
                        ? `${rowEntry.amount} gold piece${rowEntry.amount === 1 ? '' : 's'}`
                        : inventoryItemName(rowEntry.item);
                    return [line[0], line[1], ` ${rowEntry.letter} ${marker} ${label}`.padEnd(40, ' '), line[3]];
                });
                return;
            }
            if (ch === '\r' || ch === '\n' || ch === ' ') {
                const selectedEntries = entries.filter(item => selected.has(item.letter));
                if (!selectedEntries.length) {
                    await setMessage('No relevant items selected.');
                    game._container_letter = null;
                    game._command_mode = null;
                    return;
                }
                const messages = [];
                let moved = false;
                let bagGone = false;
                for (const selectedEntry of selectedEntries) {
                    const result = putInventoryObjectIntoBag(bag, selectedEntry.item, selectedEntry.amount);
                    moved ||= !!result.moved;
                    messages.push(...(result.messages || (result.message ? [result.message] : [])));
                    if (result.bagGone) {
                        bagGone = true;
                        break;
                    }
                }
                game._container_letter = null;
                game._bag_put_entries = null;
                game._bag_put_selected = null;
                game._bag_put_type_choices = null;
                game._bag_put_type_selected = null;
                game._bag_put_items = null;
                game._command_mode = null;
                if (moved || bagGone) game.context.move = 1;
                await showIceBoxMessageList(messages);
                return;
            }
            if (ch === '\x1b' || ch === 'q') {
                game._container_letter = null;
                game._bag_put_entries = null;
                game._bag_put_selected = null;
                game._overlay_lines = null;
                game._command_mode = null;
                return;
            }
            return;
        }
        if (ch === '$') {
            const result = putInventoryObjectIntoBag(bag, { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount || 0 }, game._goldCount || 0);
            game._container_letter = null;
            game._command_mode = null;
            if (result.moved) game.context.move = 1;
            await showIceBoxMessageList(result.messages || (result.message ? [result.message] : []));
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            const result = putInventoryObjectIntoBag(bag, item, item?.quan || 1);
            game._container_letter = null;
            game._command_mode = null;
            if (result.moved) game.context.move = 1;
            await showIceBoxMessageList(result.messages || (result.message ? [result.message] : []));
            return;
        }
        await setMessage("You don't have that object.");
        game._container_letter = null;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'bagTakeObject') {
        const bag = (game.inventory || []).find(item => item.letter === game._container_letter);
        if (!bag) {
            game._container_letter = null;
            game._bag_take_entries = null;
            game._bag_take_selected = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            return;
        }
        if (game._bag_take_entries) {
            const entries = game._bag_take_entries || [];
            const selected = new Set(game._bag_take_selected || []);
            const entry = entries.find(item => item.letter === ch);
            if (entry) {
                if (selected.has(entry.letter)) selected.delete(entry.letter);
                else selected.add(entry.letter);
                game._bag_take_selected = [...selected];
                game._overlay_lines = (game._overlay_lines || []).map(line => {
                    const rowEntry = entries.find(item => item.row === line[0]);
                    if (!rowEntry || line[1] !== 40) return line;
                    const marker = selected.has(rowEntry.letter) ? '+' : '-';
                    const label = rowEntry.letter === '$'
                        ? `${rowEntry.amount} gold piece${rowEntry.amount === 1 ? '' : 's'}`
                        : inventoryItemName(rowEntry.item);
                    return [line[0], line[1], ` ${rowEntry.letter} ${marker} ${label}`.padEnd(40, ' '), line[3]];
                });
                return;
            }
            if (ch === '\r' || ch === '\n' || ch === ' ') {
                const selectedEntries = entries.filter(item => selected.has(item.letter));
                if (!selectedEntries.length) {
                    await setMessage('No relevant items selected.');
                    game._container_letter = null;
                    game._bag_take_entries = null;
                    game._bag_take_selected = null;
                    game._command_mode = null;
                    return;
                }
                const taken = selectedEntries[0];
                if (taken.letter === '$' || taken.item?.cls === 'coin') {
                    const gold = taken.item.quan || 0;
                    prepareContainerTakeoutObject(bag, taken.item);
                    removeContainedObject(bag, taken.item);
                    game._goldCount = (game._goldCount || 0) + gold;
                    const money = (game.inventory || []).find(invItem => invItem.letter === '$' || invItem.cls === 'coin');
                    if (money) {
                        money.quan = (money.quan || 0) + gold;
                        updateMoneyLine(money);
                    }
                    else game.inventory = [...(game.inventory || []), { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold }];
                    game._pet_food_scan_inventory = game.inventory;
                    refreshTipContainerWeight(bag);
                    await setMessage(`$ - ${gold} gold piece${gold === 1 ? '' : 's'}.`);
                } else {
                    removeContainedObject(bag, taken.item);
                    const line = addContainerTakeoutObjectToInventory(bag, taken.item);
                    refreshTipContainerWeight(bag);
                    await setMessage(`${line}.`);
                }
                game.context.move = 1;
                game._container_letter = null;
                game._bag_take_entries = null;
                game._bag_take_selected = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._command_mode = null;
                return;
            }
            if (ch === '\x1b' || ch === 'q') {
                game._container_letter = null;
                game._bag_take_entries = null;
                game._bag_take_selected = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._command_mode = null;
            }
            return;
        }
        const item = liquidFlowContainerContents(bag).find(content => content.letter === ch);
        if (item?.letter === '$' || item?.cls === 'coin') {
            const gold = item.quan || 0;
            prepareContainerTakeoutObject(bag, item);
            removeContainedObject(bag, item);
            game._goldCount = (game._goldCount || 0) + gold;
            const money = (game.inventory || []).find(invItem => invItem.letter === '$' || invItem.cls === 'coin');
            if (money) {
                money.quan = (money.quan || 0) + gold;
                updateMoneyLine(money);
            }
            else game.inventory = [...(game.inventory || []), { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: gold }];
            game._pet_food_scan_inventory = game.inventory;
            refreshTipContainerWeight(bag);
            await setMessage(`$ - ${gold} gold piece${gold === 1 ? '' : 's'}.`);
            game.context.move = 1;
        } else if (item) {
            removeContainedObject(bag, item);
            const line = addContainerTakeoutObjectToInventory(bag, item);
            refreshTipContainerWeight(bag);
            await setMessage(`${line}.`);
            game.context.move = 1;
        }
        game._container_letter = null;
        game._bag_take_entries = null;
        game._bag_take_selected = null;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'wizardWish') {
        if (ch === '\r' || ch === '\n') {
            const wishText = (game._wish_text || '').trim();
            const wish = wishText.toLowerCase();
            game._wish_text = '';
            game._command_mode = null;
            if (isExplicitNoWishText(wishText)) {
                game._wish_tries = 0;
                await setWishResultMessage('');
                return;
            }
            if (!wish) {
                await handleNoFittingWish();
                return;
            }
            let wishedName = wishText;
            let wishedBlessed = false;
            let wishedUncursed = false;
            let wishedCursed = false;
            let wishedQuan = 1;
            let wishedSpe;
            let wishedSpeNegative = false;
            let wishedQuanForced = false;
            const leadingArticle = wishedName.match(/^(a|an|the)\s+/i);
            if (leadingArticle) {
                if (leadingArticle[1].toLowerCase() !== 'the') wishedQuanForced = true;
                wishedName = wishedName.slice(leadingArticle[0].length);
            }
            const wishedQualifiers = {
                greased: false,
                poisoned: false,
                erodeproof: false,
                eroded: 0,
                eroded2: 0,
                wetness: 0,
                lockState: '',
                trappedState: 0,
                empty: false,
                diluted: false,
                halfeaten: false,
                historic: false,
                unlabeled: false,
                lightState: null,
                realAmulet: false,
                fakeAmulet: false,
                monsterGender: null,
                zombifying: false,
                wishChargeSuffix: false,
                wishChargeSpe: 0,
                wishRecharged: 0,
                blessed: false,
                looted: false,
            };
            let wishedErosionIntensity = 0;
            for (;;) {
                const buc = wishedName.match(/^(blessed|holy|uncursed|cursed|unholy)\s+/i);
                if (buc) {
                    const state = buc[1].toLowerCase();
                    wishedBlessed = state === 'blessed' || state === 'holy';
                    wishedUncursed = state === 'uncursed';
                    wishedCursed = state === 'cursed' || state === 'unholy';
                    wishedQualifiers.blessed = wishedBlessed;
                    wishedName = wishedName.slice(buc[0].length);
                    continue;
                }
                const spe = wishedName.match(/^([+-]\d+)\s+/);
                if (spe) {
                    wishedSpeNegative = spe[1].startsWith('-');
                    wishedSpe = capWishSpe(Number(spe[1]));
                    wishedName = wishedName.slice(spe[0].length);
                    continue;
                }
                const amuletReality = wishedName.match(/^(real|fake)\s+/i);
                if (amuletReality) {
                    const state = amuletReality[1].toLowerCase();
                    if (state === 'fake') {
                        wishedQualifiers.fakeAmulet = true;
                        wishedQualifiers.realAmulet = false;
                    } else {
                        wishedQualifiers.realAmulet = true;
                    }
                    wishedName = wishedName.slice(amuletReality[0].length);
                    continue;
                }
                const trapped = wishedName.match(/^(trapped|untrapped)\s+/i);
                if (trapped) {
                    const state = trapped[1].toLowerCase();
                    wishedQualifiers.trappedState = state === 'untrapped' ? 2 : game.flags?.debug ? 1 : 0;
                    wishedName = wishedName.slice(trapped[0].length);
                    continue;
                }
                const looted = wishedName.match(/^(?:looted|disturbed)\s+/i);
                if (looted) {
                    wishedQualifiers.looted = true;
                    wishedName = wishedName.slice(looted[0].length);
                    continue;
                }
                const lockState = wishedName.match(WISH_LOCK_QUALIFIER_RE);
                if (lockState) {
                    wishedQualifiers.lockState = lockState[1].toLowerCase();
                    wishedName = wishedName.slice(lockState[0].length);
                    continue;
                }
                const proof = wishedName.match(WISH_PROOF_QUALIFIER_RE);
                if (proof) {
                    wishedQualifiers.erodeproof = true;
                    wishedName = wishedName.slice(proof[0].length);
                    continue;
                }
                const lit = wishedName.match(/^(?:lit|burning|unlit|extinguished)\s+/i);
                if (lit) {
                    wishedQualifiers.lightState = /^(?:lit|burning)/i.test(lit[0]) ? 1 : 0;
                    wishedName = wishedName.slice(lit[0].length);
                    continue;
                }
                const wet = wishedName.match(/^(wet|moist)\s+/i);
                if (wet) {
                    wishedQualifiers.wetness = wet[1].toLowerCase() === 'wet' ? 3 + rn2(3) : rnd(2);
                    wishedName = wishedName.slice(wet[0].length);
                    continue;
                }
                const unlabeled = wishedName.match(/^(?:unlabeled|unlabelled|blank)\s+/i);
                if (unlabeled) {
                    wishedQualifiers.unlabeled = true;
                    wishedName = wishedName.slice(unlabeled[0].length);
                    continue;
                }
                const halfeaten = wishedName.match(/^(?:partly|partially)\s+eaten\s+/i);
                if (halfeaten) {
                    wishedQualifiers.halfeaten = true;
                    wishedName = wishedName.slice(halfeaten[0].length);
                    continue;
                }
                const historic = wishedName.match(/^historic\s+/i);
                if (historic) {
                    wishedQualifiers.historic = true;
                    wishedName = wishedName.slice(historic[0].length);
                    continue;
                }
                const globSize = wishedName.match(/^(small|medium|large|very large)\s+/i);
                if (globSize && /\bglobs?\b/i.test(wishedName.slice(globSize[0].length))) {
                    const size = globSize[1].toLowerCase();
                    wishedQualifiers.globSize = size === 'small' ? 1
                        : size === 'medium' ? 2
                            : size === 'large' ? 3 : 4;
                    wishedName = wishedName.slice(globSize[0].length);
                    continue;
                }
                const monsterGender = wishedName.match(/^(female|male|neuter)\s+/i);
                if (monsterGender) {
                    wishedQualifiers.monsterGender = monsterGender[1].toLowerCase();
                    wishedName = wishedName.slice(monsterGender[0].length);
                    continue;
                }
                const diluted = wishedName.match(/^diluted\s+/i);
                if (diluted) {
                    wishedQualifiers.diluted = true;
                    wishedName = wishedName.slice(diluted[0].length);
                    continue;
                }
                const poisoned = wishedName.match(/^poisoned\s+/i);
                if (poisoned) {
                    wishedQualifiers.poisoned = true;
                    wishedName = wishedName.slice(poisoned[0].length);
                    continue;
                }
                const greased = wishedName.match(/^greased\s+/i);
                if (greased) {
                    wishedQualifiers.greased = true;
                    wishedName = wishedName.slice(greased[0].length);
                    continue;
                }
                const zombifying = wishedName.match(/^zombifying\s+/i);
                if (zombifying) {
                    wishedQualifiers.zombifying = true;
                    wishedName = wishedName.slice(zombifying[0].length);
                    continue;
                }
                const intense = wishedName.match(/^(very|thoroughly)\s+/i);
                if (intense) {
                    wishedErosionIntensity = intense[1].toLowerCase() === 'very' ? 1 : 2;
                    wishedName = wishedName.slice(intense[0].length);
                    continue;
                }
                const primaryErosion = wishedName.match(WISH_PRIMARY_EROSION_RE);
                if (primaryErosion) {
                    wishedQualifiers.eroded = 1 + wishedErosionIntensity;
                    wishedErosionIntensity = 0;
                    wishedName = wishedName.slice(primaryErosion[0].length);
                    continue;
                }
                const secondaryErosion = wishedName.match(WISH_SECONDARY_EROSION_RE);
                if (secondaryErosion) {
                    wishedQualifiers.eroded2 = 1 + wishedErosionIntensity;
                    wishedErosionIntensity = 0;
                    wishedName = wishedName.slice(secondaryErosion[0].length);
                    continue;
                }
                const empty = wishedName.match(/^empty\s+/i);
                if (empty) {
                    wishedQualifiers.empty = true;
                    wishedName = wishedName.slice(empty[0].length);
                    continue;
                }
                const quan = wishedName.match(/^(\d+)\s+/);
                if (!quan) break;
                wishedQuan = Number(quan[1]);
                wishedQuanForced = true;
                wishedName = wishedName.slice(quan[0].length);
            }
            const chargeSuffix = parseWishedChargeSuffix(wishedName);
            wishedName = chargeSuffix.name;
            if (chargeSuffix.type === 'lit') {
                wishedQualifiers.lightState = 1;
            } else if (chargeSuffix.type === 'charge') {
                wishedSpeNegative = false;
                wishedSpe = capWishSpe(chargeSuffix.spe);
                wishedQualifiers.wishChargeSuffix = true;
                wishedQualifiers.wishChargeSpe = chargeSuffix.spe;
                wishedQualifiers.wishRecharged = chargeSuffix.recharged;
            } else if (chargeSuffix.type === 'invalid' && wishedSpe !== undefined) {
                wishedSpe = 0;
            }
            const groupedWish = normalizeWishedGroupPhrase(wishedName, wishedQuan);
            wishedName = groupedWish.name;
            wishedQuan = groupedWish.quantity;
            wishedQuanForced = wishedQuanForced || groupedWish.matched;
            wishedQuan = applyWishedPluralQuantity(wishedName, wishedQuan);
            const lowerName = wishedName.trim().toLowerCase();
            if (!lowerName) {
                await handleNoFittingWish();
                return;
            }
            if (isExplicitNoWishText(lowerName)) {
                await handleNoFittingWish();
                return;
            }
            if (isWishedMoneyName(lowerName)) {
                const moneyQuan = wishedMoneyQuantity(lowerName, wishedQuan, wishedQuanForced);
                recordWishConduct();
                next_ident();
                godsNoticeWish();
                game._goldCount = (game._goldCount || 0) + moneyQuan;
                game.inventory ??= [];
                const money = game.inventory.find(item => item.letter === '$' || item.cls === 'coin');
                if (money) {
                    money.quan = game._goldCount;
                    money.owt = goldStackWeight(game._goldCount);
                    updateMoneyLine(money);
                }
                else game.inventory.push({ letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount, owt: goldStackWeight(game._goldCount) });
                game._pet_food_scan_inventory = game.inventory;
                await setWishResultMessage(`$ - ${moneyQuan} gold piece${moneyQuan === 1 ? '' : 's'}.`);
                return;
            }
            const bearLandResolution = wizardBearLandTrapWish(lowerName, wishedQualifiers);
            if (bearLandResolution?.result === 'trap') {
                await createWizardTrapWishResult(bearLandResolution);
                return;
            }
            const objectWishName = bearLandResolution?.result === 'object'
                ? bearLandResolution.objectName
                : wishedName.trim();
            const wishedItem = wishedObjectFromName(objectWishName, wishedQualifiers);
            if (wishedItem._wish_no_match) {
                if (await tryWizardNonObjectWish(lowerName, wishedQualifiers)) return;
                await handleNoFittingWish();
                return;
            }
            if (wishedItem._wish_disappeared) {
                if (wishedItem._artifact_wish_name || wishedItem.artifact) addConductCount('wisharti');
                await setWishResultMessage(wishedItem._wish_disappear_message);
                return;
            }
            let letter = nextInventoryLetter();
            if (game.u?.uball && game.u?.uchain && letter === 'n'
                && !(game.inventory || []).some(invItem => invItem.letter === 'n')) {
                letter = nextInventoryLetter();
            }
            const item = Object.assign({ letter, quan: 1 }, wishedItem);
            recordWishConduct();
            if (item._artifact_wish_name) wishedQuan = 1;
            if (item._artifact_wish_name || item.artifact) addConductCount('wisharti');
            if (item.artifact) touchArtifact(item);
            if (wishedSpe !== undefined && !item._wish_ignore_requested_spe && !item._wish_spe_from_suffix) {
                const requestedSpe = wishedSpeNegative ? -Math.abs(wishedSpe) : Math.abs(wishedSpe);
                item.spe = wishedSpeForItem(item, requestedSpe);
            }
            applyWishedBuc(item, {
                wishedBlessed,
                wishedUncursed,
                wishedCursed,
                negativeSpe: wishedSpeNegative,
            });
            applyWishedQualifiers(item, wishedQualifiers);
            applyWishedQuantity(item, wishedQuan, wishedQuanForced);
            applyWishedTinVariety(item);
            const finalWishedWeight = wishedObjectFinalWeight(item);
            delete item._wish_ignore_requested_spe;
            delete item._wish_spe_from_suffix;
            delete item._wish_tin_explicit_content;
            delete item._wish_tin_requested_variety;
            delete item._wish_glob_size;
            delete item._wish_glob_default_count;
            const bucPrefix = knownBlessCursePrefix(item);
            const baseVisibleName = game.u?.blind && item.cls === 'potion' ? 'potion'
                : game.u?.blind && item.cls === 'ring' ? 'ring'
                    : game.u?.blind && item.cls === 'wand' ? 'wand'
                    : `${erosionPrefix(item)}${pickupObjectName(item)}`;
            const visibleName = `${bucPrefix}${baseVisibleName}`;
            const displayQuan = item.quan || wishedQuan;
            const article = item.unique ? 'the'
                : item.noArticle ? ''
                : pairArticleObjectName(baseVisibleName) ? 'a pair of'
                : /^[aeiou]/i.test(visibleName) ? 'an' : 'a';
            const displayPhrase = displayQuan > 1 ? `${displayQuan} ${visibleName}`
                : article === 'a pair of' ? `a ${bucPrefix}pair of ${baseVisibleName}`
                    : article ? `${article} ${visibleName}` : visibleName;
            item.line = `${letter} - ${displayPhrase}`;
            game.inventory ??= [];
            game.inventory.push(item);
            maybeAttachCarriedFigurineTimeout(item);
            game._pet_food_scan_inventory = game.inventory;
            let carriedWeight = Math.trunc(((game._goldCount || 0) + 50) / 100);
            for (const invItem of game.inventory || []) {
                if (isGoldObject(invItem)) continue;
                if (invItem === item && wishObjectMetadataForItem(invItem)?.unitWeight != null) {
                    carriedWeight += finalWishedWeight;
                    continue;
                }
                if (invItem.globby) {
                    carriedWeight += (invItem.owt ?? 20) * (invItem.quan || 1);
                    continue;
                }
                if (invItem.otyp === 'corpse' || invItem.otyp === CORPSE) {
                    const corpseName = invItem.corpsenm?.name || objectKindKey(invItem).replace(/\s+corpses?$/, '');
                    carriedWeight += (CORPSE_WEIGHTS.get(corpseName) ?? invItem.owt ?? 1) * (invItem.quan || 1);
                    continue;
                }
                const kind = String(invItem.kind || invItem.actualKind || invItem.spellName || invItem.spell?.name || '').toLowerCase();
                const cls = invItem.cls || (invItem.otyp === RING_CLASS ? 'ring'
                    : invItem.otyp === SCROLL_CLASS ? 'scroll'
                        : invItem.otyp === POTION_CLASS ? 'potion'
                            : invItem.otyp === WAND_CLASS ? 'wand'
                                : invItem.otyp === GEM_CLASS ? 'gem' : '');
                carriedWeight += (OBJECT_WEIGHTS[kind] ?? CLASS_WEIGHTS[cls] ?? invItem.owt ?? 0) * (invItem.quan || 1);
            }
            const stats = game.u?.acurr?.a || [];
            const capacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
            if (carriedWeight > capacity && !(game.u?._statusSuffix || '').includes('Burdened')) {
                item.owt = finalWishedWeight;
                game._burden_after_more = 1;
                game._queued_message_after_more = 'Your movements are slowed slightly because of your load.';
                game._wish_notice_after_more = 1;
                await setWishResultMessage(`${item.line}.`, true);
                return;
            }
            item.owt = finalWishedWeight;
            godsNoticeWish();
            await setWishResultMessage(`${item.line}.`);
            return;
        }
        if (key === 8 || key === 127) game._wish_text = (game._wish_text || '').slice(0, -1);
        else if (key >= 32) game._wish_text = `${game._wish_text || ''}${ch}`;
        await setMessage(`For what do you wish?${game._wish_text ? ` ${game._wish_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'dropObject') {
        const preserveSilentDropPrompt = () => {
            game._pending_message = game._drop_prompt || '';
            game._silent_drop_prompt_message = game._pending_message;
            game._keep_pending_message = game._pending_message ? 1 : 0;
        };
        if (ch === ' ' || ch === '\r' || ch === '\n' || ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '$') {
            const amount = game._goldCount || (game.inventory || []).find(item => item.letter === '$' || item.cls === 'coin')?.quan || 0;
            game._goldCount = 0;
            game.inventory = (game.inventory || []).filter(item => item.letter !== '$' && item.cls !== 'coin');
            const goldMessages = [];
            const floorGold = {
                otyp: GOLD_PIECE,
                cls: 'coin',
                glyph: '$',
                color: CLR_YELLOW,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                quan: amount,
            };
            const shipObject = maybeShipCarriedDropObject(floorGold, floorGold.ox, floorGold.oy, goldMessages, { allowGold: true });
            let consumedByFloor = false;
            if (!shipObject.handled) {
                consumedByFloor = earthFloorEffects(floorGold, floorGold.ox, floorGold.oy, goldMessages, 'drop');
            }
            let shopGold = { messages: [] };
            if (!shipObject.handled && !consumedByFloor) {
                placeStackableFloorObject(floorGold);
                shopGold = sellobjDroppedGoldAt(floorGold.ox, floorGold.oy, amount);
            }
            const guard = (game.level?.monsters || []).find(mon => mon.isgd || mon.data?.name === 'guard');
            if (guard) prepareVaultGuardEscort(guard);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            const messages = [
                ...(game.flags?.verbose === false ? [] : [`You drop ${amount} gold piece${amount === 1 ? '' : 's'}.`]),
                ...goldMessages,
                ...(shopGold.messages || []),
            ];
            if (messages.length) {
                await setMessage(messages.join('  '), messages.length > 1);
            } else if (game.flags?.verbose === false) {
                preserveSilentDropPrompt();
            } else {
                await setMessage(`You drop ${amount} gold piece${amount === 1 ? '' : 's'}.`);
            }
            game.context.move = 1;
            game._command_mode = null;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            if (isLoadstoneObject(item) && item.cursed) {
                await setMessage(cursedLoadstoneLetGoMessage(item, 'drop'));
                game.context.move = 0;
                game._command_mode = null;
                return;
            }
            stopCarriedFigurineTimerOnLeave(item);
            game.inventory = (game.inventory || []).filter(invItem => invItem !== item);
            updateWornDisplacement();
            game._pet_food_scan_inventory = game.inventory;
            const dropped = {
                ...item,
                invlet: item.invlet ?? item.letter,
                letter: undefined,
                line: undefined,
                known: item.known || /(?:^| )[-+]\d+ /.test(String(item.line || '')),
                wielded: false,
                worn: false,
                quivered: false,
                otyp: item.otyp || (item.kind === 'chest' ? CHEST : item.cls),
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                glyph: item.glyph || (item.cls === 'weapon' ? ')' : item.cls === 'armor' ? '[' : item.cls === 'wand' ? '/'
                    : item.cls === 'ring' ? '=' : item.cls === 'potion' ? '!' : item.cls === 'scroll' ? '?'
                        : item.cls === 'spellbook' ? '+' : '('),
                color: item.cls === 'weapon' ? (item.kind === 'quarterstaff' ? CLR_BROWN : item.color ?? CLR_CYAN)
                    : item.color ?? (item.cls === 'scroll' || item.cls === 'spellbook' ? CLR_WHITE : NO_COLOR),
            };
            curseLoadstoneLeavingInventory(dropped);
            if (Array.isArray(dropped.contents)) dropped.contents = [...dropped.contents];
            if (Array.isArray(dropped.cobj)) dropped.cobj = [...dropped.cobj];
            normalizeContainedObjectParents(dropped);
            const floorMessages = [];
            let shopSale = null;
            const shipObject = maybeShipCarriedDropObject(dropped, dropped.ox, dropped.oy, floorMessages);
            let consumedByFloor = false;
            if (shipObject.handled) {
                clearObjectShopBillState(item);
            } else {
                consumedByFloor = earthFloorEffects(dropped, dropped.ox, dropped.oy, floorMessages, 'drop', {
                    usedUpShopBillOnDestroy: true,
                });
                if (consumedByFloor) clearObjectShopBillState(item);
            }
            if (!shipObject.handled && !consumedByFloor) {
                game.level.objects.push(dropped);
                if (!sellobjReturnUnpaidToShop(dropped, dropped.ox, dropped.oy))
                    shopSale = beginDroppedPaidObjectSale(dropped, dropped.ox, dropped.oy);
                if (shopSale?.message) floorMessages.push(shopSale.message);
                objectIceEffect(dropped, dropped.ox, dropped.oy);
                if (!shopSale?.prompt) stackDroppedFloorObject(dropped);
            }
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            game._message_more = 0;
            const dropMessages = [];
            if (item.cls === 'weapon' || item.kind === 'chest') {
                if (game.flags?.verbose === false) {
                    dropMessages.push(...floorMessages);
                } else {
                    dropMessages.push(`You drop ${inventoryItemName(item)}.`, ...floorMessages);
                }
            } else {
                dropMessages.push(...floorMessages);
            }
            if (shopSale?.prompt) {
                const declineMessage = dropMessages.length ? dropMessages.join('  ') : `You drop ${inventoryItemName(item)}.`;
                game._shop_sale_pending.declineMessage = declineMessage;
                const promptMessages = [...dropMessages, shopSale.promptMessage];
                await setMessage(promptMessages.join('  '));
                return;
            }
            game.context.move = 1;
            if (dropMessages.length) {
                await setMessage(dropMessages.join('  '), dropMessages.length > 1);
            } else {
                preserveSilentDropPrompt();
            }
            game._command_mode = null;
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'rubRoyalJellyTarget') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._royal_jelly_rub_letter = '';
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            const eggs = (game.inventory || []).filter(item => isEggItem(item));
            const rows = eggs.map((item, index) => [index, 40, ` ${normalInventoryLine(item)}`.padEnd(40, ' ')]);
            rows.push([rows.length, 40, ' (end)'.padEnd(40, ' ')]);
            setOverlay(rows, 2, false, 0);
            return;
        }
        const jelly = (game.inventory || []).find(item => item.letter === game._royal_jelly_rub_letter && isRoyalJelly(item));
        if (!jelly) {
            game._royal_jelly_rub_letter = '';
            game._command_mode = null;
            await setMessage("You don't have that object.");
            return;
        }
        const egg = (game.inventory || []).find(item => item.letter === ch && isEggItem(item));
        if (!egg) {
            game._topline_after_more = royalJellyRubPrompt();
            await setMessage("You don't have that object.", true);
            return;
        }
        await rubRoyalJellyOnEgg(jelly, egg);
        return;
    }

    if (game._command_mode === 'rubObject') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            const rubItems = rubObjectMenuItems(ch);
            const rows = rubItems.map((item, index) => [index, 40, ` ${normalInventoryLine(item)}`.padEnd(40, ' ')]);
            rows.push([rows.length, 40, ' (end)'.padEnd(40, ' ')]);
            setOverlay(rows, 2, false, 0);
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item && isRoyalJelly(item)) {
            await beginRoyalJellyRub(item);
            return;
        }
        if (item && isLampObject(item)) {
            await rubLampObject(item);
            return;
        }
        if (item && isGrayStoneApplyItem(item)) {
            await beginUseStone(item);
            return;
        }
        if (item) {
            game._command_mode = null;
            await setMessage('That is a silly thing to rub.');
            return;
        }
        await setMessage(rubObjectPrompt());
        return;
    }

    if (game._command_mode === 'polyselfMonster') {
        if (ch === '\r' || ch === '\n') {
            const name = (game._polyself_text || '').trim().toLowerCase();
            game._polyself_text = '';
            game._command_mode = null;
            if (name) {
                const result = becomeMonster(name);
                if (!result) {
                    await setMessage("I've never heard of such monsters.");
                    return;
                }
                newsym(game.u.ux, game.u.uy);
                await setMessage(result.message, !!result.more);
                return;
            }
            await setMessage('Never mind.');
            return;
        }
        if (key === 8 || key === 127) game._polyself_text = (game._polyself_text || '').slice(0, -1);
        else if (key >= 32) game._polyself_text = `${game._polyself_text || ''}${ch}`;
        await setMessage(`Become what kind of monster? [type the name]${game._polyself_text ? ` ${game._polyself_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'invokeObject') {
        if (ch === 'p') {
            await setMessage('Nothing happens.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'openDirection') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        const dir = movementDirection(ch);
        if (!dir) {
            setOverlay(CMDASSIST_DIRECTION_LINES, 24, true);
            game._cmdassist_cancel_message = 'Never mind.';
            game._command_mode = 'cmdassistMore';
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'closeDirection') {
        const dir = movementDirection(ch);
        if (!dir) {
            setOverlay(CMDASSIST_DIRECTION_LINES, 24, true);
            game._command_mode = 'cmdassistMore';
            return;
        }
        await setMessage('This door is already closed.');
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'applyLockPickDirection') {
        const dir = movementDirection(ch);
        game._command_mode = null;
        if (!dir) return;
        const loc = game.level?.at((game.u?.ux || 0) + dir.dx, (game.u?.uy || 0) + dir.dy);
        if (loc?.typ === DOOR && (loc.doormask & D_ISOPEN)) {
            await setMessage('You cannot lock an open door.');
            game.context.move = 1;
            return;
        }
        if (loc?.typ === DOOR) {
            await setMessage('This door is already locked.');
            game.context.move = 1;
            return;
        }
        await setMessage(`You ${game.u?.ublind ? 'feel' : 'see'} no door there.`);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'applyFigurineDirection') {
        const item = (game.inventory || []).find(invItem => invItem.letter === game._apply_figurine_letter);
        game._command_mode = null;
        game._apply_figurine_letter = null;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage('Never mind.');
            return;
        }
        if (!item || !isFigurineObject(item)) return;
        const dir = figurineApplyDirection(ch);
        if (!dir) return;
        const tx = (game.u?.ux || 0) + dir.dx;
        const ty = (game.u?.uy || 0) + dir.dy;
        const check = figurineLocationCheck(item, tx, ty);
        if (!check.ok) {
            await setMessage(check.message);
            game.context.move = 1;
            return;
        }
        const action = (dir.dx || dir.dy) ? 'set the figurine beside you'
            : dir.dz < 0 ? 'toss the figurine into the air'
                : (Is_waterlevel(game.u?.uz) || IS_POOL(game.level?.at(tx, ty)?.typ))
                    ? 'release the figurine'
                    : 'set the figurine on the ground';
        const prefix = `You ${action} and it ${game.u?.ublind ? 'supposedly ' : ''}transforms.`;
        const result = await makeFigurineFamiliar(item, tx, ty, { quietly: false });
        const extra = [result.message].filter(Boolean).join('  ');
        removeInventoryItem(item);
        await setMessage(extra ? `${prefix}  ${extra}` : prefix);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'stethoscopeDirection') {
        game._command_mode = null;
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            return;
        }
        const dir = ch === '.' ? null : movementDirection(ch);
        if (ch !== '.' && !dir) return;
        const usedThisTurn = game._stethoscope_seq === game.moves;
        game._stethoscope_seq = game.moves;
        if (ch === '.') {
            const align = game._startup_align || 'neutral';
            const record = game.u?.ualign?.record || 0;
            const piety = record >= 20 ? 'piously'
                : record > 13 ? 'devoutly'
                : record > 8 ? 'fervently'
                : record > 3 ? 'stridently'
                : record === 3 ? ''
                : record > 0 ? 'haltingly'
                : record === 0 ? 'nominally'
                : 'insufficiently';
            const alignment = piety ? `${piety} ${align}` : align;
            await setMessage(`Status of ${game.plname || 'you'} (${alignment}):  Level ${game.u?.ulevel || 1}  HP ${game.u?.uhp || 0}(${game.u?.uhpmax || 0})  AC ${game.u?.uac ?? 10}.`);
            if (usedThisTurn) game.context.move = 1;
            return;
        }
        const tx = (game.u?.ux || 0) + dir.dx;
        const ty = (game.u?.uy || 0) + dir.dy;
        const mon = game.level?.monsters?.find(other => other.mx === tx && other.my === ty);
        if (mon?.appearObj != null) {
            const objectName = mon.appearGlyph ? pickupObjectName({ otyp: mon.appearObj }) : 'chest';
            const monsterName = mon.data?.name || 'monster';
            mon.appearObj = null;
            mon.appearGlyph = null;
            mon.appearColor = null;
            if (usedThisTurn) mon._skip_after_mimic_reveal = 1;
            newsym(mon.mx, mon.my);
            const monAlign = (mon.data?.maligntyp || 0) > 0 ? 'lawful' : (mon.data?.maligntyp || 0) < 0 ? 'chaotic' : 'neutral';
            const monSize = monsterName.includes('large') || monsterName.includes('giant') ? 'large'
                : monsterName.includes('small') ? 'medium'
                : 'medium';
            game._queued_message_after_more = `Status of the ${monsterName} (${monAlign}, ${monSize}):  Level ${mon.m_lev || mon.data?.mlevel || 1}  HP ${mon.mhp || 0}(${mon.mhpmax || 0})  AC ${mon.data?.mac ?? 10}.`;
            await setMessage(`That ${objectName} is really a ${monsterName}.`, true);
            if (usedThisTurn) game.context.move = 1;
            return;
        }
        await setMessage('You hear nothing special.');
        if (usedThisTurn) game.context.move = 1;
        return;
    }

    if (game._command_mode === 'lootIceMenu' || game._command_mode === 'iceBoxAction') {
        const iceBox = game._icebox_action_container || game.level?.objects?.find(obj =>
            isIceBoxObject(obj) && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
        if (!iceBoxContainerAvailable(iceBox)) {
            clearIceBoxActionState();
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            return;
        }
        game._icebox_action_container = iceBox;
        if (ch === ':') {
            if (iceBox) iceBox.cknown = true;
            const rows = [[0, 41, 'Contents of the ice box:']];
            let row = 2;
            const contents = [...(iceBox?.contents || [])].sort(compareContainerLootItems);
            for (const item of contents) {
                rows.push([row++, 43, containerObjectPhrase(item)]);
                if (row >= 23) break;
            }
            if (row === 2) rows.push([row++, 43, 'The ice box is empty.']);
            const moreRow = Math.min(row, 23);
            rows.push([moreRow, 41, '--More--']);
            setOverlay(rows, Math.max(11, moreRow + 1));
            game._command_mode = 'iceBoxContents';
            return;
        }
        if (ch === 'o') {
            if (iceBox) iceBox.cknown = true;
            if (iceBox && beginIceBoxTakeout(iceBox)) return;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            await setMessage('The ice box is empty.');
            game.context.move = 1;
            return;
        }
        if (ch === 'i') {
            if (iceBox) iceBox.cknown = true;
            if (iceBox && beginIceBoxPutIn(iceBox)) return;
            clearIceBoxPutState();
            game._command_mode = null;
            await setMessage("You don't have anything to put in.");
            return;
        }
        if (ch === 's') {
            if (iceBox) iceBox.cknown = true;
            if (iceBox && (await beginIceBoxStash(iceBox))) return;
            clearIceBoxPutState();
            game._command_mode = null;
            await setMessage("You don't have anything to stash.");
            return;
        }
        if (ch === 'b') {
            startIceBoxSequence('putin');
            if (iceBox) {
                const learnedEmpty = !(iceBox.contents || []).length && !iceBox.cknown;
                iceBox.cknown = true;
                if (beginIceBoxTakeout(iceBox)) return;
                if (learnedEmpty) markIceBoxSequenceUsed();
                await continueIceBoxSequenceToPutIn(iceBox, [iceBoxEmptyMessage()]);
                return;
            }
            await finishIceBoxSequence([iceBoxEmptyMessage(), "You don't have anything to put in."]);
            game._command_mode = null;
            return;
        }
        if (ch === 'r') {
            startIceBoxSequence('takeout');
            if (iceBox && beginIceBoxPutIn(iceBox)) return;
            await continueIceBoxSequenceToTakeout(iceBox, ["You don't have anything to put in."]);
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            for (let row = 0; row < 12; row++) game.nhDisplay?.clearRow(row);
            clearIceBoxActionState();
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            clearIceBoxSequenceState();
            game.context.move = 1;
        }
        return;
    }

    if (game._command_mode === 'iceBoxPutTypes') {
        const iceBox = game._icebox_put_container;
        if (!iceBoxContainerAvailable(iceBox)) {
            clearIceBoxPutState();
            if (iceBoxSequenceActive()) await finishIceBoxSequence();
            else clearIceBoxSequenceState();
            game._command_mode = null;
            return;
        }
        const choices = game._icebox_put_type_choices || [];
        const selected = new Set(game._icebox_put_type_selected || []);
        const choice = choices.find(entry => entry.letter === ch || entry.symbol === ch);
        if (ch === '\x1b' || ch === 'q') {
            clearIceBoxPutState();
            if (game._icebox_sequence_after_putin === 'takeout') {
                await continueIceBoxSequenceToTakeout(iceBox);
                return;
            }
            if (iceBoxSequenceActive()) {
                await finishIceBoxSequence();
                return;
            }
            clearIceBoxSequenceState();
            game._command_mode = null;
            return;
        }
        if (ch === '@') {
            for (const entry of choices) {
                if (entry.key !== 'auto') selected.add(entry.key);
            }
            game._icebox_put_type_selected = [...selected];
            drawIceBoxPutTypeMenu();
            return;
        }
        if (choice) {
            if (selected.has(choice.key)) selected.delete(choice.key);
            else selected.add(choice.key);
            game._icebox_put_type_selected = [...selected];
            drawIceBoxPutTypeMenu();
            return;
        }
        if (ch === '\r' || ch === '\n' || ch === ' ') {
            if (!selected.size || (selected.size === 1 && selected.has('auto'))) {
                clearIceBoxPutState();
                if (game._icebox_sequence_after_putin === 'takeout') {
                    await continueIceBoxSequenceToTakeout(iceBox);
                    return;
                }
                if (iceBoxSequenceActive()) {
                    await finishIceBoxSequence(['No relevant items selected.']);
                    return;
                }
                clearIceBoxSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            const items = (game._icebox_put_items || []).filter(({ item, category }) => {
                if (selected.has('all')) return true;
                if (selected.has(category)) return true;
                const bucCategory = objectBucCategory(item);
                if (selected.has('blessed') && bucCategory === 'blessed') return true;
                if (selected.has('cursed') && bucCategory === 'cursed') return true;
                if (selected.has('uncursed') && bucCategory === 'uncursed') return true;
                return (selected.has('unknown') && bucCategory === 'unknown')
                    || (selected.has('justpicked') && item.letter === '$' && game._just_picked_gold);
            });
            const entries = items.map(({ item, category }) => ({
                item,
                category,
                letter: item.letter,
                amount: item.letter === '$' && selected.has('justpicked')
                    ? game._just_picked_gold || 0
                    : item.quan || 1,
            })).filter(entry => entry.amount > 0);
            if (!entries.length) {
                clearIceBoxPutState();
                if (game._icebox_sequence_after_putin === 'takeout') {
                    await continueIceBoxSequenceToTakeout(iceBox);
                    return;
                }
                if (iceBoxSequenceActive()) {
                    await finishIceBoxSequence(['No relevant items selected.']);
                    return;
                }
                clearIceBoxSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            game._icebox_put_entries = entries;
            game._icebox_put_selected = [];
            drawIceBoxPutObjectMenu();
            game._command_mode = 'iceBoxPutObjects';
            return;
        }
        return;
    }

    if (game._command_mode === 'iceBoxPutObjects') {
        const iceBox = game._icebox_put_container;
        if (!iceBoxContainerAvailable(iceBox)) {
            clearIceBoxPutState();
            if (iceBoxSequenceActive()) await finishIceBoxSequence();
            else clearIceBoxSequenceState();
            game._command_mode = null;
            return;
        }
        const entries = game._icebox_put_entries || [];
        const selected = new Set(game._icebox_put_selected || []);
        const entry = entries.find(item => item.letter === ch);
        if (entry) {
            if (selected.has(entry.letter)) selected.delete(entry.letter);
            else selected.add(entry.letter);
            game._icebox_put_selected = [...selected];
            drawIceBoxPutObjectMenu();
            return;
        }
        if (ch === '\r' || ch === '\n' || ch === ' ') {
            const selectedEntries = entries.filter(item => selected.has(item.letter));
            if (!selectedEntries.length) {
                clearIceBoxPutState();
                if (game._icebox_sequence_after_putin === 'takeout') {
                    await continueIceBoxSequenceToTakeout(iceBox);
                    return;
                }
                if (iceBoxSequenceActive()) {
                    await finishIceBoxSequence(['No relevant items selected.']);
                    return;
                }
                clearIceBoxSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            const results = [];
            let pendingSale = null;
            for (const selectedEntry of selectedEntries) {
                const result = putInventoryObjectIntoIceBox(iceBox, selectedEntry.item, selectedEntry.amount);
                results.push(result);
                if (result.pendingSale) {
                    pendingSale = result.pendingSale;
                    break;
                }
            }
            const moved = results.some(result => result.moved);
            const messages = iceBoxPutResultMessages(results);
            clearIceBoxPutState();
            if (pendingSale) {
                clearIceBoxSequenceState();
                game._shop_sale_pending = pendingSale;
                game._command_mode = 'shopSaleConfirm';
                if (moved) game.context.move = 1;
                await showIceBoxMessageList([...messages.filter(message => message !== pendingSale.promptMessage), pendingSale.promptMessage]);
                return;
            }
            if (game._icebox_sequence_after_putin === 'takeout') {
                markIceBoxSequenceUsed(moved);
                await continueIceBoxSequenceToTakeout(iceBox, messages);
                return;
            }
            if (iceBoxSequenceActive()) {
                await finishIceBoxSequence(messages, moved);
                return;
            }
            clearIceBoxSequenceState();
            game._command_mode = null;
            if (moved) game.context.move = 1;
            await showIceBoxMessageList(messages);
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            clearIceBoxPutState();
            if (game._icebox_sequence_after_putin === 'takeout') {
                await continueIceBoxSequenceToTakeout(iceBox);
                return;
            }
            if (iceBoxSequenceActive()) {
                await finishIceBoxSequence();
                return;
            }
            clearIceBoxSequenceState();
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'iceBoxStashObject') {
        const iceBox = game._icebox_put_container;
        if (!iceBoxContainerAvailable(iceBox)) {
            clearIceBoxPutState();
            game._command_mode = null;
            return;
        }
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            clearIceBoxPutState();
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            const rows = [[0, 40, 'Stash what?', 1]];
            let row = 2;
            for (const { item } of buildIceBoxPutItems(iceBox)) {
                const label = item.letter === '$'
                    ? `${item.letter} - ${item.quan || 0} gold piece${(item.quan || 0) === 1 ? '' : 's'}`
                    : `${item.letter || '?'} - ${inventoryItemName(item)}`;
                rows.push([row++, 40, label]);
                if (row >= 23) break;
            }
            rows.push([row, 40, '(end)']);
            setOverlay(rows, Math.max(4, row + 1), false, 40);
            return;
        }
        const item = ch === '$'
            ? { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount || 0 }
            : (game.inventory || []).find(invItem => invItem.letter === ch);
        const result = putInventoryObjectIntoIceBox(iceBox, item, item?.quan || 1);
        clearIceBoxPutState();
        if (result.pendingSale) {
            game._shop_sale_pending = result.pendingSale;
            game._command_mode = 'shopSaleConfirm';
            await showIceBoxMessageList([result.pendingSale.promptMessage]);
            return;
        }
        game._command_mode = null;
        if (result.moved) game.context.move = 1;
        await showIceBoxPutMessages([result]);
        return;
    }

    if (game._command_mode === 'containerPutTypes') {
        const container = game._container_put_container;
        const choices = game._container_put_type_choices || [];
        const selected = new Set(game._container_put_type_selected || []);
        const choice = choices.find(entry => entry.letter === ch || entry.symbol === ch);
        if (ch === '\x1b' || ch === 'q') {
            clearContainerPutState();
            if (game._container_sequence_after_putin === 'takeout') {
                await continueContainerSequenceToTakeout(container);
                return;
            }
            if (containerSequenceActive()) {
                await finishContainerSequence();
                return;
            }
            clearContainerSequenceState();
            game._command_mode = null;
            return;
        }
        if (ch === '@') {
            for (const entry of choices) {
                if (entry.key !== 'auto') selected.add(entry.key);
            }
            game._container_put_type_selected = [...selected];
            drawContainerPutTypeMenu();
            return;
        }
        if (choice) {
            if (selected.has(choice.key)) selected.delete(choice.key);
            else selected.add(choice.key);
            game._container_put_type_selected = [...selected];
            drawContainerPutTypeMenu();
            return;
        }
        if (ch === '\r' || ch === '\n' || ch === ' ') {
            if (!selected.size || (selected.size === 1 && selected.has('auto'))) {
                clearContainerPutState();
                if (game._container_sequence_after_putin === 'takeout') {
                    await continueContainerSequenceToTakeout(container);
                    return;
                }
                if (containerSequenceActive()) {
                    await finishContainerSequence(['No relevant items selected.']);
                    return;
                }
                clearContainerSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            const items = (game._container_put_items || []).filter(({ item, category }) => {
                if (selected.has('all')) return true;
                if (selected.has(category)) return true;
                const bucCategory = objectBucCategory(item);
                if (selected.has('blessed') && bucCategory === 'blessed') return true;
                if (selected.has('cursed') && bucCategory === 'cursed') return true;
                if (selected.has('uncursed') && bucCategory === 'uncursed') return true;
                return (selected.has('unknown') && bucCategory === 'unknown')
                    || (selected.has('justpicked') && item.letter === '$' && game._just_picked_gold);
            });
            const entries = items.map(({ item, category }) => ({
                item,
                category,
                letter: item.letter,
                amount: item.letter === '$' && selected.has('justpicked')
                    ? game._just_picked_gold || 0
                    : item.quan || 1,
            })).filter(entry => entry.amount > 0);
            if (!entries.length) {
                clearContainerPutState();
                if (game._container_sequence_after_putin === 'takeout') {
                    await continueContainerSequenceToTakeout(container);
                    return;
                }
                if (containerSequenceActive()) {
                    await finishContainerSequence(['No relevant items selected.']);
                    return;
                }
                clearContainerSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            game._container_put_entries = entries;
            game._container_put_selected = [];
            drawContainerPutObjectMenu();
            game._command_mode = 'containerPutObjects';
            return;
        }
        return;
    }

    if (game._command_mode === 'containerPutObjects') {
        const container = game._container_put_container;
        const entries = game._container_put_entries || [];
        const selected = new Set(game._container_put_selected || []);
        const entry = entries.find(item => item.letter === ch);
        if (entry) {
            if (selected.has(entry.letter)) selected.delete(entry.letter);
            else selected.add(entry.letter);
            game._container_put_selected = [...selected];
            drawContainerPutObjectMenu();
            return;
        }
        if (ch === '\r' || ch === '\n' || ch === ' ') {
            const selectedEntries = entries.filter(item => selected.has(item.letter));
            if (!selectedEntries.length) {
                clearContainerPutState();
                if (game._container_sequence_after_putin === 'takeout') {
                    await continueContainerSequenceToTakeout(container);
                    return;
                }
                if (containerSequenceActive()) {
                    await finishContainerSequence(['No relevant items selected.']);
                    return;
                }
                clearContainerSequenceState();
                game._command_mode = null;
                await setMessage('No relevant items selected.');
                return;
            }
            const results = [];
            let pendingSale = null;
            for (const selectedEntry of selectedEntries) {
                const result = putInventoryObjectIntoContainer(container, selectedEntry.item, selectedEntry.amount);
                results.push(result);
                if (result.pendingSale) {
                    pendingSale = result.pendingSale;
                    break;
                }
                if (result.bagGone) break;
            }
            const moved = results.some(result => result.moved);
            const bagGone = results.some(result => result.bagGone);
            const messages = results.flatMap(result => result.messages || (result.message ? [result.message] : []));
            clearContainerPutState();
            if (pendingSale) {
                clearContainerSequenceState();
                game._floor_container_object = null;
                game._shop_sale_pending = pendingSale;
                game._command_mode = 'shopSaleConfirm';
                if (moved || bagGone) game.context.move = 1;
                await showIceBoxMessageList([...messages.filter(message => message !== pendingSale.promptMessage), pendingSale.promptMessage]);
                return;
            }
            if (!bagGone && game._container_sequence_after_putin === 'takeout') {
                markContainerSequenceUsed(moved);
                await continueContainerSequenceToTakeout(container, messages);
                return;
            }
            if (containerSequenceActive()) {
                await finishContainerSequence(messages, moved || bagGone);
                return;
            }
            clearContainerSequenceState();
            game._floor_container_object = null;
            game._command_mode = null;
            if (moved || bagGone) game.context.move = 1;
            await showIceBoxMessageList(messages);
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            clearContainerPutState();
            if (game._container_sequence_after_putin === 'takeout') {
                await continueContainerSequenceToTakeout(container);
                return;
            }
            if (containerSequenceActive()) {
                await finishContainerSequence();
                return;
            }
            clearContainerSequenceState();
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'containerStashObject') {
        const container = game._container_put_container;
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            clearContainerPutState();
            game._floor_container_object = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            const rows = [[0, 40, 'Stash what?', 1]];
            let row = 2;
            for (const { item } of buildContainerPutItems(container)) {
                const label = item.letter === '$'
                    ? `${item.letter} - ${item.quan || 0} gold piece${(item.quan || 0) === 1 ? '' : 's'}`
                    : `${item.letter || '?'} - ${inventoryItemName(item)}`;
                rows.push([row++, 40, label]);
                if (row >= 23) break;
            }
            rows.push([row, 40, '(end)']);
            setOverlay(rows, Math.max(4, row + 1), false, 40);
            return;
        }
        const item = ch === '$'
            ? { letter: '$', cls: 'coin', otyp: GOLD_PIECE, glyph: '$', quan: game._goldCount || 0 }
            : (game.inventory || []).find(invItem => invItem.letter === ch);
        const result = putInventoryObjectIntoContainer(container, item, item?.quan || 1);
        clearContainerPutState();
        game._floor_container_object = null;
        if (result.pendingSale) {
            game._shop_sale_pending = result.pendingSale;
            game._command_mode = 'shopSaleConfirm';
            await showIceBoxMessageList([result.pendingSale.promptMessage]);
            return;
        }
        game._command_mode = null;
        if (result.moved || result.bagGone) game.context.move = 1;
        await showIceBoxMessageList(result.messages || (result.message ? [result.message] : []));
        return;
    }

    if (game._command_mode === 'iceBoxContents') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            setIceBoxActionMenu(game._icebox_action_container, true);
            game._pending_message = '';
            game._message_more = 0;
        }
        return;
    }

    if (game._command_mode === 'floorContainerContents') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            setOverlay(LOOT_LARGE_BOX_ABC_DONE_LINES, 11, false, 37);
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = 'lootMenu';
        }
        return;
    }

    if (game._command_mode === 'lootMenu') {
        const container = game._floor_container_object;
        const largeBox = container?.kind === 'large box' || container?.otyp === LARGE_BOX;
        const abcLargeBox = !!(largeBox && game.flags?.lootabc);
        const takeoutKey = abcLargeBox ? 'a' : 'o';
        const putinKey = abcLargeBox ? 'b' : 'i';
        const bothKey = abcLargeBox ? 'c' : 'b';
        const reversedKey = abcLargeBox ? 'd' : 'r';
        const stashKey = abcLargeBox ? 'e' : 's';
        if (container && ch === ':') {
            const name = largeBox ? 'large box' : 'chest';
            container.cknown = true;
            const rows = [[0, 41, `Contents of the ${name}:`]];
            let row = 2;
            const contents = [...(container.contents || [])].sort(compareContainerLootItems);
            for (const item of contents) {
                rows.push([row++, 43, containerObjectPhrase(item)]);
            }
            rows.push([row, 41, '--More--']);
            setOverlay(rows, Math.max(11, row + 1));
            game._command_mode = 'floorContainerContents';
            return;
        }
        if (ch === takeoutKey) {
            if (container) {
                container.cknown = true;
                if (beginIceBoxTakeout(container)) return;
                await setMessage(tipContainerEmptyMessage(container));
                game.context.move = 1;
                game._command_mode = null;
                return;
            }
            setOverlay(LOOT_TAKEOUT_MENU_LINES, 12);
            game._command_mode = 'lootTakeoutMenu';
            return;
        }
        if (container && ch === putinKey) {
            container.cknown = true;
            if (beginContainerPutIn(container)) return;
            clearContainerPutState();
            game._command_mode = null;
            await setMessage("You don't have anything to put in.");
            return;
        }
        if (container && ch === stashKey) {
            container.cknown = true;
            if (await beginContainerStash(container)) return;
            clearContainerPutState();
            game._command_mode = null;
            await setMessage("You don't have anything to stash.");
            return;
        }
        if (container && ch === bothKey) {
            startContainerSequence('putin');
            const learnedEmpty = !(container.contents || []).length && !container.cknown;
            container.cknown = true;
            if (beginIceBoxTakeout(container)) return;
            if (learnedEmpty) markContainerSequenceUsed();
            await continueContainerSequenceToPutIn(container, [tipContainerEmptyMessage(container)]);
            return;
        }
        if (container && ch === reversedKey) {
            startContainerSequence('takeout');
            container.cknown = true;
            if (beginContainerPutIn(container)) return;
            await continueContainerSequenceToTakeout(container, ["You don't have anything to put in."]);
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            for (let row = 0; row < 12; row++) game.nhDisplay?.clearRow(row);
            game._floor_container_object = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'lootTakeoutMenu') {
        const container = game._loot_takeout_container;
        if (container) {
            const typeEntries = game._loot_takeout_type_entries || [];
            const selected = new Set(game._loot_takeout_types_selected || []);
            const drawTypes = () => {
                const rows = [
                    [0, 23, 'Take out what type of objects?', 1],
                    [2, 23, `A ${selected.has('A') ? '+' : '-'} Auto-select every relevant item`],
                    [3, 27, '(ignored unless some other choices are also picked)'],
                    [5, 23, `${selected.has('a') ? 'a +' : 'a -'} All types`],
                ];
                for (let i = 0; i < typeEntries.length; i++) {
                    const entry = typeEntries[i];
                    rows.push([6 + i, 23, `${entry.letter} ${selected.has(entry.letter) ? '+' : '-'} ${entry.label}`]);
                }
                const contents = container.contents || [];
                let filterRow = 7 + typeEntries.length;
                if (contents.some(item => objectBucCategory(item) === 'blessed'))
                    rows.push([filterRow++, 23, 'B - Items known to be Blessed']);
                if (contents.some(item => objectBucCategory(item) === 'cursed'))
                    rows.push([filterRow++, 23, 'C - Items known to be Cursed']);
                if (contents.some(item => objectBucCategory(item) === 'uncursed'))
                    rows.push([filterRow++, 23, 'U - Items known to be Uncursed']);
                if (contents.some(item => objectBucCategory(item) === 'unknown'))
                    rows.push([filterRow++, 23, 'X - Items of unknown Bless/Curse status']);
                rows.push([filterRow, 23, '(end)']);
                setOverlay(rows, filterRow + 1, false, 22);
            };
            if (ch === '@') {
                for (const entry of typeEntries) selected.add(entry.letter);
                game._loot_takeout_types_selected = [...selected];
                drawTypes();
                return;
            }
            if (ch === 'A') {
                if (selected.has('A')) selected.delete('A');
                else selected.add('A');
                game._loot_takeout_types_selected = [...selected];
                drawTypes();
                return;
            }
            if (ch === 'a') {
                if (selected.has('a')) selected.delete('a');
                else selected.add('a');
                game._loot_takeout_types_selected = [...selected];
                drawTypes();
                return;
            }
            const typeEntry = typeEntries.find(entry => entry.letter === ch);
            if (typeEntry) {
                if (selected.has(ch)) selected.delete(ch);
                else selected.add(ch);
                game._loot_takeout_types_selected = [...selected];
                drawTypes();
                return;
            }
            if ((ch === '\r' || ch === '\n' || ch === ' ') && [...selected].some(choice => choice !== 'A')) {
                const selectedLabels = selected.has('a')
                    ? new Set(typeEntries.map(entry => entry.label))
                    : new Set(typeEntries.filter(entry => selected.has(entry.letter)).map(entry => entry.label));
                const entries = [];
                const contents = [...(container.contents || [])].sort(compareContainerLootItems);
                for (const item of contents) {
                    const label = containerLootCategory(item);
                    if (!selectedLabels.has(label)) continue;
                    entries.push({
                        item,
                        label,
                        letter: label === 'Coins' ? '$' : String.fromCharCode('a'.charCodeAt(0) + entries.filter(entry => entry.label !== 'Coins').length),
                    });
                }
                const rows = [[0, 41, 'Take out what?', 1]];
                let row = 2;
                let lastLabel = '';
                for (const entry of entries) {
                    if (entry.label !== lastLabel) {
                        rows.push([row++, 41, entry.label, 1]);
                        lastLabel = entry.label;
                    }
                    const text = entry.letter === '$'
                        ? `${entry.letter} - ${entry.item.quan || 1} gold piece${(entry.item.quan || 1) === 1 ? '' : 's'}`
                        : `${entry.letter} - ${pickupObjectPhrase(entry.item)}`;
                    rows.push([row++, 41, text]);
                }
                rows.push([row, 41, '(end)']);
                game._loot_takeout_entries = entries;
                game._loot_takeout_selected = [];
                game._loot_takeout_clear_col = null;
                setOverlay(rows, Math.max(12, row + 1));
                game._command_mode = 'lootTakeoutObjects';
                return;
            }
            if (ch === '\x1b') {
                clearContainerTakeoutState();
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                if (game._icebox_sequence_after_takeout === 'putin' && isIceBoxObject(container)) {
                    await continueIceBoxSequenceToPutIn(container);
                    return;
                }
                if (iceBoxSequenceActive() && isIceBoxObject(container)) {
                    await finishIceBoxSequence();
                    return;
                }
                if (game._container_sequence_after_takeout === 'putin' && !isIceBoxObject(container)) {
                    await continueContainerSequenceToPutIn(container);
                    return;
                }
                if (containerSequenceActive() && !isIceBoxObject(container)) {
                    await finishContainerSequence();
                    return;
                }
                clearIceBoxSequenceState();
                clearContainerSequenceState();
                game._command_mode = null;
            }
            return;
        }
        if (ch === 'A') {
            setOverlay(LOOT_TAKEOUT_SELECTED_LINES, 12);
            return;
        }
        if (ch === '\x1b') {
            for (let row = 0; row < 12; row++) game.nhDisplay?.clearRow(row);
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            clearIceBoxSequenceState();
            clearContainerSequenceState();
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'lootTakeoutObjects') {
        const entries = game._loot_takeout_entries || [];
        const selected = new Set(game._loot_takeout_selected || []);
        const drawObjects = () => {
            const rows = [[0, 41, 'Take out what?', 1]];
            let row = 2;
            let lastLabel = '';
            for (const entry of entries) {
                if (entry.label !== lastLabel) {
                    rows.push([row++, 41, entry.label, 1]);
                    lastLabel = entry.label;
                }
                const text = entry.letter === '$'
                    ? `${entry.letter} ${selected.has(entry.letter) ? '+' : '-'} ${entry.item.quan || 1} gold piece${(entry.item.quan || 1) === 1 ? '' : 's'}`
                    : `${entry.letter} ${selected.has(entry.letter) ? '+' : '-'} ${pickupObjectPhrase(entry.item)}`;
                rows.push([row++, 41, text]);
            }
            rows.push([row, 41, '(end)']);
            const clearRows = game._loot_takeout_clear_col == null ? Math.max(12, row + 1) : row + 1;
            setOverlay(rows, clearRows, false, game._loot_takeout_clear_col);
        };
        if (ch === '@') {
            for (const entry of entries) selected.add(entry.letter);
            game._loot_takeout_selected = [...selected];
            drawObjects();
            return;
        }
        const entry = entries.find(item => item.letter === ch);
        if (entry) {
            if (selected.has(ch)) selected.delete(ch);
            else selected.add(ch);
            game._loot_takeout_selected = [...selected];
            drawObjects();
            return;
        }
        if ((ch === '\r' || ch === '\n' || ch === ' ') && selected.size) {
            const container = game._loot_takeout_container;
            const picked = entries.filter(entry => selected.has(entry.letter));
            const preflight = containerTakeoutPreflight(container, picked);
            if (!preflight.ok) {
                clearContainerTakeoutState();
                game._floor_container_object = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                clearIceBoxSequenceState();
                clearContainerSequenceState();
                game._command_mode = null;
                await setMessage(preflight.message || 'You cannot carry that.');
                return;
            }
            const planned = preflight.entries || picked;
            if (preflight.prompt) {
                game._container_takeout_pending = {
                    container,
                    picked: planned,
                    messages: preflight.messages || [],
                };
                clearContainerTakeoutState();
                game._floor_container_object = null;
                game._overlay_lines = null;
                game._overlay_hide_status = 0;
                game._command_mode = 'containerTakeoutBurdenConfirm';
                await setMessage(preflight.prompt);
                return;
            }
            await finishContainerTakeoutSelection(container, planned, preflight.messages || []);
            return;
        }
        if (ch === '\x1b') {
            const container = game._loot_takeout_container;
            const followupPutIn = game._icebox_sequence_after_takeout === 'putin' && isIceBoxObject(container);
            const followupContainerPutIn = game._container_sequence_after_takeout === 'putin' && !isIceBoxObject(container);
            clearContainerTakeoutState();
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            if (followupPutIn) {
                await continueIceBoxSequenceToPutIn(container);
                return;
            }
            if (iceBoxSequenceActive() && isIceBoxObject(container)) {
                await finishIceBoxSequence();
                return;
            }
            if (followupContainerPutIn) {
                await continueContainerSequenceToPutIn(container);
                return;
            }
            if (containerSequenceActive() && !isIceBoxObject(container)) {
                await finishContainerSequence();
                return;
            }
            clearIceBoxSequenceState();
            clearContainerSequenceState();
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'herecmdMenu') {
        if (ch === '\x1b') {
            for (let row = 0; row < 12; row++) game.nhDisplay?.clearRow(row);
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
        }
        return;
    }

    if (game._command_mode === 'levelChangeMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') await advanceExperienceLevel();
        else game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'levelChangeText') {
        if (ch === '\r' || ch === '\n') {
            game._level_change_target = Number.parseInt(game._level_change_text || '', 10) || (game.u?.ulevel || 1);
            game._level_change_text = '';
            await advanceExperienceLevel();
            return;
        }
        if (key >= 32) game._level_change_text = `${game._level_change_text || ''}${ch}`;
        await setMessage(`To what experience level do you want to be set?${game._level_change_text ? ` ${game._level_change_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'levelTeleportText') {
        if (ch === '\x1b') {
            clearLevelTeleportTextPrompt();
            game._deferred_level_goto = null;
            const followup = consumePendingLevelTeleportTrapFollowup();
            if (followup) await setMessage(followup);
            else {
                game._pending_message = '';
                game._message_more = 0;
            }
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === '\r' || ch === '\n') {
            const text = game._level_teleport_text || '';
            if (text.trim() === '*') {
                await randomLevelTeleportFromPrompt();
                return;
            }
            if (game._level_teleport_confused_scroll && rnl(5)) {
                clearLevelTeleportTextPrompt();
                game._command_mode = 'confusedLevelTeleportOopsMore';
                game._deferred_level_goto = null;
                await setMessage('Oops...', true);
                return;
            }
            if (game.flags?.debug && text.trim() === '?') {
                clearLevelTeleportTextPrompt();
                setOverlay(levelTeleportMenuLines(), 24, true);
                game._command_mode = 'levelTeleportMenu';
                return;
            }
            const { value: target, hasNumericPrefix } = cAtoiLikeLevel(text);
            if (target || hasNumericPrefix) {
                if (questDownBlocked() && target > (game.u?.uz?.dlevel || 0)) {
                    clearLevelTeleportTextPrompt();
                    game._level_teleport_trap_followup = 0;
                    await setMessage('A mysterious force prevents you from descending.');
                    game._command_mode = null;
                    return;
                }
                if (target > 0 && singleLevelBranch(game.u?.uz)) {
                    clearLevelTeleportTextPrompt();
                    game._command_mode = null;
                    const followup = consumePendingLevelTeleportTrapFollowup();
                    await setMessage(['You shudder for a moment.', followup].filter(Boolean).join('  '));
                    game.context.move = 1;
                    return;
                }
                if (target < 0) {
                    if (In_endgame(game.u?.uz)) {
                        await finishEndgameNegativeLevelTeleport(target);
                        return;
                    }
                    await finishNegativeLevelTeleport(target);
                    return;
                }
                if (target === 0) {
                    clearLevelTeleportTextPrompt();
                    await setMessage('Go to Nowhere.  Are you sure? [yn] (n)');
                    game._command_mode = 'levelTeleportNowhereConfirm';
                    return;
                }
                const targetLevel = levelTeleportNumericTarget(levelTeleportPromptTargetDepth(target));
                await finishLevelTeleport(targetLevel, levelTeleportOptionsWithTrapFollowup({ preHalluRefresh: true }));
                if (game._command_mode === 'levelTeleportText') game._command_mode = null;
                return;
            }
            await retryInvalidLevelTeleportPrompt();
            return;
        }
        if (key >= 32) game._level_teleport_text = `${game._level_teleport_text || ''}${ch}`;
        await setMessage(levelTeleportPromptQuestion());
        return;
    }

    if (game._command_mode === 'levelTeleportNowhereConfirm') {
        if (ch === 'y') {
            await finishNowhereLevelTeleport();
            return;
        }
        if (ch === 'n' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n' || ch === 'q') {
            const followup = consumePendingLevelTeleportTrapFollowup();
            if (followup) await setMessage(followup);
            else {
                game._pending_message = '';
                game._message_more = 0;
            }
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'levelTeleportMenu') {
        if (ch === ' ') {
            setOverlay(levelTeleportMenuPage2Lines(), 24, true);
            game._command_mode = 'levelTeleportMenu2';
            return;
        }
        const targetLevel = levelTeleportMenuTarget(ch);
        await finishMenuLevelTeleport(targetLevel, targetLevel
            ? levelTeleportOptionsWithTrapFollowup({ preHalluRefresh: true })
            : { preHalluRefresh: true });
        if (game._command_mode === 'levelTeleportMenu') game._command_mode = null;
        return;
    }

    if (game._command_mode === 'levelTeleportMenu2') {
        if (ch === ' ') {
            setOverlay(levelTeleportMenuPage3Lines(), 24, true);
            game._command_mode = 'levelTeleportMenu3';
            return;
        }
        if (ch === 'y' && questDownBlocked()) {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._level_teleport_trap_followup = 0;
            await docrt();
            game._command_mode = null;
            return;
        }
        if (['z', 'A'].includes(ch) && questDownBlocked()) {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._level_teleport_trap_followup = 0;
            await docrt();
            await setMessage('A mysterious force prevents you from descending.');
            game._command_mode = null;
            return;
        }
	        const targetLevel = levelTeleportMenuTarget(ch, true);
	        const soko1Letter = hasPlacedKnoxBranch() ? 'C' : 'B';
	        await finishMenuLevelTeleport(targetLevel, targetLevel
		            ? levelTeleportOptionsWithTrapFollowup({ boulderMessage: ch === soko1Letter, preHalluRefresh: true })
		            : { boulderMessage: ch === soko1Letter, preHalluRefresh: true });
        if (game._command_mode === 'levelTeleportMenu2') game._command_mode = null;
        return;
    }

    if (game._command_mode === 'levelTeleportMenu3') {
        const targetLevel = levelTeleportMenuPage3Target(ch);
        await finishMenuLevelTeleport(targetLevel, targetLevel
            ? levelTeleportOptionsWithTrapFollowup({ preHalluRefresh: true })
            : { preHalluRefresh: true });
        if (game._command_mode === 'levelTeleportMenu3') game._command_mode = null;
        return;
    }

    if (game._command_mode === 'tipHatDirection') {
        await finishTipHatDirection(ch);
        return;
    }

    if (game._command_mode === 'tipContainerObject') {
        if (ch === '\x1b' || ch === 'q' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            return;
        }
        if (ch === '?' || ch === '*') {
            const tipObjects = tipMenuItems(ch);
            const rows = [[0, 40, 'Tip what?', 1]];
            let row = 2;
            for (const item of tipObjects) {
                rows.push([row++, 40, normalInventoryLine(item)]);
                if (row >= 23) break;
            }
            rows.push([row, 40, '(end)']);
            setOverlay(rows, Math.max(4, row + 1), false, 40);
            return;
        }
        const inventoryTarget = (game.inventory || []).find(item => item.letter === ch);
        if (isTipCoinObject(inventoryTarget) || ch === '$') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._command_mode = null;
            await setMessage('You cannot tip gold.');
            return;
        }
        const tipTarget = tipSelectableInventoryItems().find(item => item.letter === ch);
        if (!tipTarget) {
            game._topline_after_more = tipPromptMessage();
            await setMessage("You don't have that object.", true);
            return;
        }
        await finishCarriedTipSelection(tipTarget);
        return;
    }

    if (game._command_mode === 'tipDestination') {
        const source = game._tip_container_object;
        const entries = game._tip_target_entries || [];
        const finishTip = async targetBox => {
            const messages = await tipContainerContents(source, targetBox);
            clearTipState();
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            game._command_mode = null;
        };
        if (!source || !isTipSourceObject(source)) {
            clearTipState();
            game._command_mode = null;
            return;
        }
        if (ch === '\x1b' || ch === 'q') {
            clearTipState();
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === '-' || ch === ' ' || ch === '\r' || ch === '\n') {
            await finishTip(null);
            return;
        }
        const entry = entries.find(item => item.letter === ch);
        if (entry?.target && (game.inventory || []).includes(entry.target)) {
            await finishTip(entry.target);
            return;
        }
        return;
    }

    if (game._command_mode === 'tipConfirm') {
        if (ch === 'q') game._keep_pending_message = 1;
        if (ch === 'y') {
            const tipTarget = game._tip_container_object;
            if (isTipSourceObject(tipTarget)) {
                if (beginTipDestinationSelection(tipTarget)) return;
                game._tip_container_object = null;
                const messages = await tipContainerContents(tipTarget);
                await setMessage(messages.join('  '), messages.length > 1);
                game.context.move = 1;
                game._command_mode = null;
                return;
            }
            if (isTipSpillageObject(tipTarget)) {
                game._tip_container_object = null;
                const messages = tipSpillageObject(tipTarget);
                await setMessage(messages.join('  '), messages.length > 1);
                game.context.move = 1;
                game._command_mode = null;
                return;
            }
        }
        clearTipState();
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'dipIntoTarget') {
        if (ch === '\x1b') {
            game._keep_pending_message = 1;
            game._dip_source_object = '';
            game._dip_source_item = null;
            game._command_mode = null;
            return;
        }
        const source = game._dip_source_item || (game.inventory || []).find(invItem => invItem.letter === game._dip_source_object);
        const target = (game.inventory || []).find(invItem => invItem.letter === ch);
        let messages = null;
        if (source && target && dipTargetsForSource(source).includes(target)) {
            messages = dipObjectIntoPotion(target, source);
            await setMessage(dipMessagesTopline(messages), messages.length > 1 || !!messages.more);
            game.context.move = 1;
        }
        game._dip_source_object = '';
        game._dip_source_item = null;
        game._command_mode = null;
        if (messages && applyLifeSavingOrFatalCommandMode(messages)) return;
        return;
    }

    if (game._command_mode === 'dipObject') {
        if (ch === '\x1b') {
            game._keep_pending_message = 1;
            game._command_mode = null;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            const { article, description } = dipItemPromptParts(item);
            game._dip_object = ch;
            game._dip_item = item;
            const oilSources = isOilRefuelLampObject(item) ? oilPotionDipSources(item) : [];
            if (oilSources.length) {
                const sourceLetters = oilSources.map(source => source.letter).filter(Boolean).sort().join('');
                await setMessage(`What do you want to dip ${article}${description} into? [${inventoryLetterMenu(sourceLetters)} or ?*]`);
                game._command_mode = 'dipOilSource';
                return;
            }
            await setMessage(`Dip ${article}${description} into the fountain? [yn] (n)`);
            game._command_mode = 'dipConfirm';
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'dipOilSource') {
        if (ch === '\x1b') {
            game._keep_pending_message = 1;
            game._command_mode = null;
            return;
        }
        const target = game._dip_item || (game.inventory || []).find(invItem => invItem.letter === game._dip_object);
        const potion = (game.inventory || []).find(invItem => invItem.letter === ch);
        let messages = null;
        if (target && potion && dipPotionSources(target).includes(potion)) {
            messages = dipObjectIntoPotion(target, potion);
            await setMessage(dipMessagesTopline(messages), messages.length > 1 || !!messages.more);
            game.context.move = 1;
        }
        game._dip_object = '';
        game._dip_item = null;
        game._command_mode = null;
        if (messages && applyLifeSavingOrFatalCommandMode(messages)) return;
        return;
    }

    if (game._command_mode === 'dipConfirm') {
        if (ch === 'y') {
            const item = game._dip_item || (game.inventory || []).find(invItem => invItem.letter === game._dip_object);
            const rustable = item && /(?:orcish helm|iron helm|ring mail|chain mail|scale mail|splint mail|plate mail|long sword|axe|dagger)/.test(String(item.kind || item.actualKind || pickupObjectName(item)).toLowerCase());
            let damaged = false;
            if (rustable && (item.oeroded || 0) < 3) {
                const armorBase = item.cls === 'armor' ? ARMOR_AC_BONUS[String(item.kind || '').toLowerCase()] ?? 0 : 0;
                const oldArmorBonus = armorBase + (item.spe ?? 0) - Math.min(item.oeroded || 0, armorBase);
                item.oeroded = (item.oeroded || 0) + 1;
                if (armorBase && (item.worn || item.line?.includes('being worn')) && game.u) {
                    const newArmorBonus = armorBase + (item.spe ?? 0) - Math.min(item.oeroded || 0, armorBase);
                    game.u.uac = (game.u.uac ?? 10) + oldArmorBonus - newArmorBonus;
                }
                damaged = true;
                const name = pickupObjectName(item);
                await setMessage(item.oeroded === 1
                    ? `Your ${name} rusts!`
                    : item.oeroded === 2
                        ? `Your ${name} rusts further!`
                        : `Your ${name} rusts completely!`);
            }
            if (damaged && !rn2(2)) {
                game.context.move = 1;
            } else {
                const fate = rnd(30);
                let fountainMessage = null;
                let fountainMore = false;

                if (game._dip_object === 'e') fountainMessage = 'Nothing seems to happen.';
                else if (game._dip_object === 'd' && isWaterPotion(item)) {
                    const water = item;
                    if (water) {
                        water.kind = 'unholy water';
                        water.plural = 'potions of unholy water';
                        water.blessed = false;
                        water.cursed = true;
                        water.line = 'd - 4 potions of unholy water';
                        syncCarriedFigurineTransformTimer(water);
                    }
                    game._keep_pending_message = 1;
                } else if (fate === 22) {
                    const nymphData = monsterByRndName('water nymph');
                    const spot = nymphData ? enextoMonsterSpot(game.u?.ux || 0, game.u?.uy || 0, nymphData) : null;
                    const nymph = spot ? await makemon(nymphData, spot.x, spot.y, 0) : null;
                    if (nymph) {
                        nymph.msleeping = 0;
                        newsym(nymph.mx, nymph.my);
                    }
                    fountainMessage = 'You attract a water nymph!';
                } else if (fate === 25) {
                    fountainMessage = dogushforth()
                        ? 'Water gushes forth from the overflowing fountain!'
                        : 'Water sprays all over you.';
                } else if (fate === 16) {
                    if (item?.cls !== 'coin') {
                        item.cursed = true;
                        item.blessed = false;
                        syncCarriedFigurineTransformTimer(item);
                    }
                    game._keep_pending_message = 1;
                } else if (fate >= 17 && fate <= 20) {
                    if (item?.cursed) {
                        item.cursed = false;
                        syncCarriedFigurineTransformTimer(item);
                        fountainMessage = 'The water glows for a moment.';
                    } else {
                        fountainMessage = 'A feeling of loss comes over you.';
                    }
                } else if (fate === 28) {
                    fountainMessage = 'An urge to take a bath overwhelms you.';
                    fountainMore = (game._goldCount || 0) > 10;
                } else {
                    fountainMessage = 'Nothing seems to happen.';
                }

                const fountainLoc = game.level?.at(game.u?.ux || 0, game.u?.uy || 0);
                const wasFountain = fountainLoc?.typ === FOUNTAIN;
                const dryup = fate === 28 && (game._goldCount || 0) > 10
                    ? { dried: false }
                    : dryupFountainResultAt();
                if (wasFountain && dryup.dried) {
                    fountainMessage = fountainMessage
                        ? `${fountainMessage}  The fountain dries up!`
                        : 'The fountain dries up!';
                    fountainMore = true;
                } else if (dryup.warning) {
                    fountainMessage = fountainMessage
                        ? `${fountainMessage}  ${dryup.warning}`
                        : dryup.warning;
                    game._queued_message_after_more = '"Hey, stop using that fountain!"';
                    fountainMore = true;
                } else if (dryup.trickle) {
                    fountainMessage = fountainMessage
                        ? `${fountainMessage}  ${dryup.trickle}`
                        : dryup.trickle;
                    fountainMore = true;
                }

                if (fountainMessage) {
                    await setMessage(fountainMessage, fountainMore);
                    if (fate === 28 && (game._goldCount || 0) > 10) {
                        game._fountain_bath_after_more = 1;
                    } else if (fountainMore) game._process_time_with_more = 1;
                }
                game.context.move = 1;
            }
        } else {
            const item = game._dip_item || (game.inventory || []).find(invItem => invItem.letter === game._dip_object);
            const potionSources = ch === 'n' ? dipPotionSources(item) : [];
            if (potionSources.length) {
                const { article, description } = dipItemPromptParts(item);
                const sourceLetters = potionSources.map(source => source.letter).filter(Boolean).sort().join('');
                await setMessage(`What do you want to dip ${article}${description} into? [${inventoryLetterMenu(sourceLetters)} or ?*]`);
                game._command_mode = 'dipOilSource';
                return;
            }
            await setMessage('Never mind.');
        }
        game._dip_object = '';
        game._dip_item = null;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'adjustObject') {
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            const used = new Set((game.inventory || [])
                .filter(invItem => invItem !== item)
                .map(invItem => invItem.letter)
                .filter(Boolean));
            const available = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
                .split('')
                .filter(letter => letter === item.letter || !used.has(letter))
                .join('');
            game._adjust_item = item;
            await setMessage(`Adjust letter to what [${inventoryLetterMenu(available)}] (? see used letters)?`);
            game._command_mode = 'adjustLetter';
            return;
        }
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'adjustLetter') {
        const item = game._adjust_item;
        const used = new Set((game.inventory || [])
            .filter(invItem => invItem !== item)
            .map(invItem => invItem.letter)
            .filter(Boolean));
	        if (item && /^[a-zA-Z]$/.test(ch) && !used.has(ch)) {
	            const oldLetter = item.letter || '?';
	            item.letter = ch;
	            item.line = String(item.line || `${oldLetter} - ${inventoryItemName(item)}`)
	                .replace(/^[a-zA-Z$] - /, `${ch} - `);
	            await setMessage(`Moving: ${item.line}.`);
	        } else {
	            await setMessage('Never mind.');
	        }
        game._adjust_item = null;
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'annotateText') {
        if (ch === '\r' || ch === '\n') {
            const text = game._annotate_text || '';
            if (text) {
                const key = `${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`;
                const annotation = text.trim().replace(/\s+/g, ' ');
                game._level_annotations ??= new Map();
                if (annotation) game._level_annotations.set(key, annotation);
                else game._level_annotations.delete(key);
            }
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            return;
        }
        if (key >= 32) game._annotate_text = `${game._annotate_text || ''}${ch}`;
        await setMessage(`What do you want to call this dungeon level?${game._annotate_text ? ` ${game._annotate_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'wieldInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(game._wield_prompt || 'What do you want to wield? [- or ?*]');
            game._command_mode = 'wieldObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'wieldObject') {
        if (ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (ch === '-') {
            for (const invItem of game.inventory || []) invItem.wielded = false;
            game._wielded_mjollnir = false;
            await setMessage('You are empty handed.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item) {
            if (item.wielded || item.line?.includes('weapon in') || item.line?.includes('(wielded)')) {
                await setMessage('You are already wielding that!');
                game._command_mode = null;
                return;
            }
            touchArtifactForWield(item);
            const previousWielded = (game.inventory || []).find(invItem => invItem !== item
                && (invItem.wielded || invItem.line?.includes('weapon in') || invItem.line?.includes('(wielded)')));
            if (previousWielded) {
                const previousName = inventoryItemName(previousWielded);
                if (game.flags?.pushweapon) {
                    const previousLine = `${previousWielded.letter || '?'} - ${previousName} (alternate weapon${(previousWielded.quan || 1) > 1 ? 's' : ''}; not wielded)`;
                    previousWielded.alternate = true;
                    previousWielded.line = previousLine;
                    const previousMessage = `${previousLine}.`;
                    game._topline_after_more = game._topline_after_more
                        ? `${previousMessage}  ${game._topline_after_more}`
                        : previousMessage;
                } else {
                    previousWielded.alternate = false;
                    previousWielded.line = `${previousWielded.letter || '?'} - ${previousName}`;
                }
            }
            for (const invItem of game.inventory || []) {
                if (invItem !== item && invItem !== previousWielded
                    && (invItem.wielded || invItem.line?.includes('weapon in')))
                    invItem.line = `${invItem.letter || '?'} - ${inventoryItemName(invItem)}`;
                invItem.wielded = invItem === item;
            }
            const baseName = inventoryItemName(item);
            const isWeapon = item.cls === 'weapon' || item.glyph === ')';
            const hand = item.kind === 'quarterstaff' ? 'hands'
                : `${game.u?.uhandedness || 'right'} hand`;
            item.alternate = false;
            item.line = isWeapon ? `${ch} - ${baseName} (weapon in ${hand})` : `${ch} - ${baseName} (wielded)`;
            game._wielded_mjollnir = item.artifact === 'Mjollnir' || item.kind === 'mjollnir' || /Mjollnir/.test(baseName);
            await setMessage(`${item.line}.`);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        await setMessage("You don't have that object.", true);
        game._command_mode = 'wieldInvalidMore';
        return;
    }

    if (game._command_mode === 'forceConfirm') {
        if (ch === 'y') {
            const chest = forceableLockedBoxAtHero();
            const weapon = (game.inventory || []).find(item => item.wielded || item.line?.includes('weapon in'));
            if (chest) {
                const weaponName = forceWeaponName(weapon);
                const picktyp = forceWeaponIsBlade(weapon);
                game._force_lock_occupation = {
                    chest,
                    weapon,
                    chance: forceLockChance(weapon),
                    picktyp,
                    usedtime: 0,
                };
                await setMessage(picktyp
                    ? `You force your ${weaponName} into a crack and pry.`
                    : `You start bashing it with your ${weaponName}.`, true);
                game._pending_force_lock_start_message = 1;
                game.context.move = 1;
                game._process_time_with_more = 1;
            } else {
                await setMessage('You decide not to force the issue.');
                game.context.move = 1;
            }
        }
        game._command_mode = null;
        return;
    }

    if (ch === 'Q') {
        const letters = inventoryLetters(isReadySuggestItem);
        if (!letters) {
            await setMessage("You don't have anything to ready.");
            return;
        }
        await setMessage(`What do you want to ready? [- ${letters} or ?*]`);
        game._command_mode = 'quiverObject';
        return;
    }

    if (ch === '\x16' && game.flags?.debug && !game._command_mode && !(game._pending_message && game._message_more)) {
        game._level_teleport_confused_scroll = 0;
        await beginLevelTeleportTextPrompt({ preserveMovement: true });
        return;
    }

    if (ch === '\x14' && game.flags?.debug && !game._command_mode && !(game._pending_message && game._message_more)) {
        const showGetposTip = !getposTipSeen() && game.flags?.tutorial !== false;
        await setMessage(showGetposTip ? 'Where do you want to be teleported?'
            : controlledTeleportIntroMessage(), showGetposTip);
        game._teleport_cursor_position_mode = 0;
        game._teleport_dot_described = 0;
        game._teleport_from_scroll = 0;
        game._farlook_x = game.u?.ux || 0;
        game._farlook_y = game.u?.uy || 0;
        game._cursor_override = null;
        game._command_mode = showGetposTip ? 'teleportIntroMore' : 'teleportCursor';
        return;
    }

    if (ch === '\x1b' && !game._command_mode) {
        game._pending_message = '';
        game._message_more = 0;
        return;
    }

    if (game._command_mode === 'extendedCommand') {
        if (ch === '\r' || ch === '\n') {
            let command = game._extended_command || '';
            if (['f', 'fo', 'for', 'forc'].includes(command)) command = 'force';
            if (['j', 'ju', 'jum', 'jump'].includes(command)) command = 'jump';
            if (['le', 'lev', 'leve', 'level', 'levelc', 'levelch', 'levelcha', 'levelchan', 'levelchang', 'levelchange'].includes(command)) command = 'levelchange';
            if (command === 'l' && !game.flags?.debug) command = 'loot';
            if (command === 'p' && !game.flags?.debug) command = 'pray';
            if (['tu', 'tur'].includes(command)) command = 'turn';
            if (['d', 'di'].includes(command)) command = 'dip';
            if (command === 's' && !game.flags?.debug) command = 'sit';
            if (command === 'si') command = 'sit';
            if (command === 'cha') command = 'chat';
            if (game.flags?.debug && command.startsWith('wizw') && 'wizwhere'.startsWith(command))
                command = 'wizwhere';

            game._extended_command = '';
            game._extended_display_command = '';
            if (command === 'wizwhere' && game.flags?.debug) {
                showWizWherePage(0);
                return;
            }
            if (command === 'chat') {
                await beginChatCommand();
                return;
            }
            if (command === 'name') {
                setOverlay(NAME_MENU_LINES, 9);
                game._command_mode = 'nameMenu';
                return;
            }
            if (command === 'twoweapon') {
                const wornShield = (game.inventory || []).find(item =>
                    item.worn && /shield/.test(inventoryItemName(item).toLowerCase()));
                if (!game._twoweapon && wornShield) {
                    await setMessage("You can't use two weapons while wearing a shield.");
                    game._command_mode = null;
                    return;
                }
                game._twoweapon = !game._twoweapon;
                if (game._twoweapon) game._swap_exchange_count = 0;
                const primary = (game.inventory || []).find(item =>
                    item.wielded || item.line?.includes('right hand') || item.cls === 'weapon');
                const secondary = (game.inventory || []).find(item =>
                    item !== primary && (item.alternate || item.line?.includes('left hand') || item.line?.includes('alternate weapon')));
                const fallbackSecondary = secondary || (game.inventory || []).find(item =>
                    item !== primary && (item.cls === 'weapon' || item.glyph === ')'));
                if (primary) {
                    const primaryName = inventoryItemName(primary);
                    primary.line = `${primary.letter || '?'} - ${primaryName} (${game._twoweapon ? 'wielded' : 'weapon'} in right hand)`;
                }
                if (fallbackSecondary) {
                    const secondaryName = inventoryItemName(fallbackSecondary);
                    fallbackSecondary.alternate = !game._twoweapon;
                    fallbackSecondary.line = game._twoweapon
                        ? `${fallbackSecondary.letter || '?'} - ${secondaryName} (wielded in left hand)`
                        : `${fallbackSecondary.letter || '?'} - ${secondaryName} (alternate weapon; not wielded)`;
                }
                if (game._twoweapon) {
                    await setMessage('You begin two-weapon combat.');
                    rnd(20);
                } else {
                    await setMessage('You switch to your primary weapon.');
                }
                game._command_mode = null;
                return;
            }
            if (command === 'enhance') {
                if (game.flags?.debug) {
                    await setMessage('Advance skills without practice? [yn] (n)');
                    game._command_mode = 'enhancePracticeConfirm';
                } else {
                    setOverlay(currentSkillLines(), 24, true);
                    game._command_mode = 'enhance';
                }
                return;
            }
            if (command === 'sit') {
                await doSitCommand();
                return;
            }
            if (command === 'dip') {
                const letters = [...inventoryLetters(item => item.cls !== 'coin')].sort().join('');
                await setMessage(`What do you want to dip? [${inventoryLetterMenu(letters)} or ?*]`);
                game._command_mode = 'dipObject';
                return;
            }
            if (command === 'offer') {
                await setMessage('You are not on an altar.');
                game._command_mode = null;
                return;
            }
            if (command === 'overview') {
                const lines = overviewLines();
                const clearCol = Math.max(0, Math.min(...lines.map(([, col]) => col)) - 1);
                setOverlay(lines, Math.max(...lines.map(([row]) => row)) + 1, false, clearCol);
                game._command_mode = 'overview';
                return;
            }
            if (command === 'chronicle') {
                const align = game.u?.ualign?.type > 0 ? 'lawful' : game.u?.ualign?.type < 0 ? 'chaotic' : 'neutral';
                const race = game.urace?.adj || game._startup_race || 'human';
                const role = game.urole?.name?.[game.flags?.female ? 'f' : 'm'] || game.urole?.name?.m || game._startup_role || 'Adventurer';
                const chronicleRows = [
                    [0, 0, 'Logged events:'],
                    [1, 1, 'Turn'],
                    [2, 0, `${String(1).padStart(5, ' ')}: ${game.plname || 'Hero'} the ${align} ${race} ${role} entered the dungeon`],
                ];
                let row = 3;
                for (const entry of game._chronicle_entries || [])
                    chronicleRows.push([row++, 0, `${String(entry.turn || 1).padStart(5, ' ')}: ${entry.text}`]);
                chronicleRows.push([23, 0, '--More--']);
                setOverlay(chronicleRows, 24, true);
                game._command_mode = 'simpleOverlay';
                return;
            }
            if (command === 'conduct') {
                const rows = liveConductLines();
                const wizardConduct = !!game.flags?.debug;
                const clearRows = wizardConduct ? 21 : Math.max(...rows.map(([row]) => row)) + 1;
                setOverlay(rows, clearRows, false, wizardConduct ? 33 : 39);
                game._command_mode = 'simpleOverlay';
                return;
            }
            if (command === 'vanquished') {
                const lines = vanquishedLines();
                setOverlay(lines, Math.max(...lines.map(([row]) => row)) + 1);
                game._command_mode = 'simpleOverlay';
                return;
            }
            if (command === 'genocided') {
                const names = genocidedMonsterNames();
                if (!names.length) {
                    await setMessage('No creatures have been genocided.');
                    game._command_mode = null;
                }
                else {
                    const lines = genocideListLines();
                    setOverlay(lines, Math.max(...lines.map(([row]) => row)) + 1, false, 39);
                    game._command_mode = 'simpleOverlay';
                }
                return;
            }
            if (command === 'adjust') {
                const letters = inventoryLetters(item => item.cls !== 'coin' && item.glyph !== '$');
                await setMessage(`What do you want to adjust? [${getobjPromptLetters(letters)} or ?*]`);
                game._command_mode = 'adjustObject';
                return;
            }
            if (command === 'terrain') {
                setOverlay(TERRAIN_LINES, 6);
                game._command_mode = 'simpleOverlay';
                return;
            }
            if (command === 'version') {
                if (!game._lua_version_info_initialized) {
                    rn2(3);
                    rn2(2);
                    game._lua_version_info_initialized = 1;
                }
                setOverlay(VERSION_PAGE_1, 24, true);
                game._command_mode = 'versionInfo';
                return;
            }
	            if (command === 'pray') {
	                await setMessage('Are you sure you want to pray? [yn] (n)');
	                game._command_mode = 'prayConfirm';
	                return;
	            }
	            if (command === 'turn') {
	                if (role !== 'Knight' && role !== 'Priest') {
	                    await setMessage("You don't know how to turn undead!");
	                    game._command_mode = null;
	                    return;
	                }
	                const pantheonRole = role === 'Priest' ? game._pantheon_role || 'Barbarian' : role;
	                const gods = GODS_BY_ROLE[pantheonRole] || GODS_BY_ROLE.Barbarian;
	                const align = game.u?.ualign?.type ?? 0;
	                const god = gods[align > 0 ? 0 : align < 0 ? 2 : 1];
	                if (!game._chronicle_rejected_atheism) {
	                    game._chronicle_entries ??= [];
	                    game._chronicle_entries.push({ turn: game.moves || 1, text: 'rejected atheism by turning undead' });
	                    game._chronicle_rejected_atheism = 1;
	                }
	                exerciseAttribute(A_WIS, true);
	                const duration = 5 - Math.trunc(((game.u?.ulevel || 1) - 1) / 6);
	                game._helpless_time = Math.max(game._helpless_time || 0, duration);
	                game._wake_message = 'You can move again.';
	                await setMessage(`Calling upon ${god}, you chant an arcane formula.`);
	                game._command_mode = null;
	                game.context.move = duration + (game.u?.veryfast ? 2 : 0);
	                return;
	            }
	            if (command === 'jump') {
	                if (!game._jump_tip_seen) {
	                    await setMessage('Where do you want to jump?', true);
                    game._command_mode = 'jumpIntroMore';
                } else {
                    await setMessage('Where do you want to jump?');
                    game._farlook_x = game.u?.ux || 0;
                    game._farlook_y = game.u?.uy || 0;
                    game._getpos_prompt_cursor = initialJumpPromptCursor();
                    game._command_mode = 'jumpCursor';
                }
                return;
            }
            if (command === 'levelchange' && game.flags?.debug) {
                await setMessage('To what experience level do you want to be set?');
                game._level_change_text = '';
                game._level_change_delayed_welcome = 0;
                game._command_mode = 'levelChangeText';
                return;
            }
            if (command === 'wizmap' && game.flags?.debug) {
                for (const trap of game.level?.traps || []) trap.tseen = true;
                revealLevelMap();
                exerciseAttribute(A_WIS, true);
                game._revealing_level_map = 1;
                await docrt();
                await flush_screen(1);
                game._revealing_level_map = 0;
                game._command_mode = null;
                return;
            }
            if (command === 'ride') {
                if (game.u?.usteed) {
                    await dismountSteed();
                    return;
                }
                await setMessage('In what direction?');
                game._command_mode = 'rideDirection';
                return;
            }
            if (command === 'wizwish' && game.flags?.debug) {
                await beginWishPrompt();
                return;
            }
	            if (command === 'wizgenesis' && game.flags?.debug) {
	                await setMessage('Create what kind of monster?');
	                game._wizgenesis_text = '';
	                game._command_mode = 'wizGenesisMonster';
	                return;
	            }
            if (command === 'wizintrinsic' && game.flags?.debug) {
                const lines = wizIntrinsicMenuLines().map(line => [...line]);
                if (game.u?._blindTimeout) {
                    const [rowNumber] = wizIntrinsicRows().i;
                    const row = lines.find(line => line[0] === rowNumber);
                    if (row) row[2] = ` i - ${'blinded'.padEnd(27)} [${game.u._blindTimeout}]`;
                }
                setOverlay(lines, 24, true);
                game._wiz_intrinsic_selected = new Set();
                game._command_mode = 'wizIntrinsicMenu';
                return;
            }
            if (command === 'wizidentify' && game.flags?.debug) {
                setOverlay([
                    [0, 32, 'Debug Identify'],
                    [1, 32, '(all items are permanently identified already)'],
                    [2, 32, '(end)'],
                ], 3);
                game._command_mode = 'simpleOverlay';
                return;
            }
	            if (command === 'polyself' && game.flags?.debug) {
	                await setMessage('Become what kind of monster? [type the name]');
	                game._polyself_text = '';
	                game._command_mode = 'polyselfMonster';
	                return;
            }
            if (command === 'monster') {
                const form = polyselfForm();
                if (form?.mlet === 'n' || String(form?.name || '').includes('nymph')) {
                    if (isBuriedBallTrapActive()) {
                        const surface = sitSurfaceName(game.level?.at(game.u?.ux || 0, game.u?.uy || 0));
                        await setMessage(`The ball and chain are buried firmly in the ${surface}.`);
                    } else if (game.u?.uball || game.u?.uchain || game.u?.upunished || game._punished) {
                        unpunishHero();
                        await setMessage('');
                        game._keep_pending_message = 0;
                        game.context.move = 1;
                    } else {
                        await setMessage('You are not chained to anything!');
                    }
                    game._command_mode = null;
                    return;
                }
                await setMessage(!game.u?._polyself_base
                    ? "You don't have a special ability in your normal form!"
                    : game.u?._glyph === 'D'
                    ? "You don't have enough energy to breathe!"
                    : 'Any special ability you may have is purely reflexive.');
                game._command_mode = null;
                return;
            }
            if (command === 'invoke') {
                await setMessage('What do you want to invoke? [p or ?*]');
                game._command_mode = 'invokeObject';
                return;
            }
            if (command === 'loot') {
                const chest = game.level?.objects?.find(obj =>
                    (obj.kind === 'chest' || obj.otyp === CHEST || obj.kind === 'large box' || obj.otyp === LARGE_BOX)
                    && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
                const iceBox = game.level?.objects?.find(obj => obj.otyp === ICE_BOX && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
                const bag = game.level?.objects?.find(obj =>
                    (BAG_OBJECT_TYPES.has(obj.otyp) || isBagOfTricksObject(obj))
                    && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
                if (game.u?._glyph === 'D') await setMessage('You have no hands!');
                else if (chest?.locked || chest?.olocked) {
                    chest.lknown = true;
                    const name = chest.kind === 'large box' || chest.otyp === LARGE_BOX ? 'large box' : 'chest';
                    const unlockTool = (game.inventory || [])
                        .find(item => /lock pick|key|credit card/i.test(inventoryItemName(item)));
                    if (unlockTool) game._locked_container_unlock_after_more = { chest, tool: unlockTool };
                    await setMessage(`Hmmm, the ${name} turns out to be locked.`, !!unlockTool);
                }
                else if (chest) {
                    game._floor_container_object = chest;
                    const largeBox = chest.kind === 'large box' || chest.otyp === LARGE_BOX;
                    setOverlay(largeBox && game.flags?.lootabc ? LOOT_LARGE_BOX_ABC_MENU_LINES : LOOT_CHEST_MENU_LINES,
                        11, false, largeBox && game.flags?.lootabc ? 37 : null);
                    game._command_mode = 'lootMenu';
                    return;
                } else if (iceBox) {
                    setIceBoxActionMenu(iceBox);
                    return;
                } else if (bag) {
                    if (isBagOfTricksObject(bag)) {
                        await lootFloorBagOfTricks(bag);
                        game._command_mode = null;
                        return;
                    }
                    game._floor_container_object = bag;
                    const lossMessages = [];
                    const lossDetails = { lost: 0, lostMerchandise: 0 };
                    magicBagContentsLoss(bag, lossMessages, { details: lossDetails });
                    if (lossDetails.lost) {
                        bag.cknown = true;
                        bag._floorLootMbagLossEmptied = !liquidFlowContainerContents(bag).length;
                    }
                    if (lossDetails.lostMerchandise > 0) game.context.move = 1;
                    const menuLines = lossDetails.lostMerchandise > 0
                        ? LOOT_FLOOR_BAG_MENU_LINES.map(row =>
                            row[2] === 'q * do nothing' ? [row[0], row[1], 'q * done'] : row)
                        : LOOT_FLOOR_BAG_MENU_LINES;
                    setOverlay(menuLines, 11);
                    if (lossMessages.length) {
                        const lossText = lossMessages.join('  ');
                        game._pending_message = lossText;
                        game._last_pline_message = lossText;
                        game._message_more = lossMessages.length > 1 ? 1 : 0;
                        game._keep_pending_message = 1;
                    }
                    game._command_mode = 'floorBagAction';
                    return;
                } else {
                    await setMessage('Loot in what direction?');
                    game._command_mode = 'lootDirection';
                    return;
                }
                game._command_mode = null;
                return;
            }
            if (command === 'force') {

                const chest = forceableLockedBoxAtHero();
                const weapon = (game.inventory || []).find(item =>
                    item.wielded || item.line?.includes('weapon in'));
                if (weapon && chest) {
                    chest.lknown = true;
                    await setMessage(`There is ${forceBoxArticleName(chest)} here; force its lock? [ynq] (q)`);
                    game._command_mode = 'forceConfirm';
                    return;
                }
                await setMessage(chest
                    ? "You can't force anything when not wielding a weapon."
                    : 'You decide not to force the issue.');
                if (!chest) game.context.move = 1;
                game._command_mode = null;
                return;
            }
            if (command === 'untrap') {
                if (polyselfNoHands()) {
                    await setMessage('And just how do you expect to do that?');
                    game._command_mode = null;
                    return;
                }
                await setMessage('In what direction?');
                game._command_mode = 'untrapDirection';
                return;
            }
            if (command === 'tip') {
                const floorContainer = game.level?.objects?.find(obj =>
                    isFloorTipSourceObject(obj) && obj.ox === game.u?.ux && obj.oy === game.u?.uy);
                if (floorContainer) {
                    game._tip_container_object = floorContainer;
                    await setMessage(`There is ${tipContainerObjectPhrase(floorContainer)} here, tip it? [ynq] (q)`);
                    game._command_mode = 'tipConfirm';
                    return;
                }
                if (tipSelectableInventoryItems().length) {
                    await setMessage(tipPromptMessage());
                    game._command_mode = 'tipContainerObject';
                    return;
                }
                await setMessage(tipPromptMessage());
                game._command_mode = null;
                return;
            }
            if (command === 'annotate') {
                await setMessage('What do you want to call this dungeon level?');
                game._annotate_text = '';
                game._command_mode = 'annotateText';
                return;
            }
            if (command === 'herecmdmenu') {
                setOverlay(HERECMD_MENU_LINES, 12);
                game._command_mode = 'herecmdMenu';
                return;
            }
            if (command === 'rub') {
                if (polyselfNoHands()) {
                    game._command_mode = null;
                    await setMessage("You aren't able to rub anything without hands.");
                } else if (!rubObjectLetters()) {
                    game._command_mode = null;
                    await setMessage("You don't have anything to rub.");
                } else {
                    await setMessage(rubObjectPrompt());
                    game._command_mode = 'rubObject';
                }
                return;
            }
            if (command === 'quit') {
                await setMessage('Really quit without saving? [yn] (n)');
                game._command_mode = 'quitConfirm';
                return;
            }
            if (command === 'wipe') {
                if (!game.u?.blind && !(game.u?.ucreamed > 0)) {
                    await setMessage('Your face is already clean.');
                    game._command_mode = null;
                    return;
                }
                game._pending_message = '';
                game._message_more = 0;
                game._wipe_finish_after_time = 1;
                game.context.move = 2;
                game._command_mode = null;
                return;
            }
            if (command) await setMessage(`#${command}: unknown extended command.`);
            game._command_mode = null;
            return;
        }
        if (ch === '\x1b') {
            game._extended_command = '';
            game._extended_display_command = '';
            game._command_mode = null;
            game._pending_message = '';
            game._keep_pending_message = 0;
            return;
        }
        if (key === 8 || key === 127 || ch === '\x7f') {
            game._extended_command = (game._extended_command || '').slice(0, -1);
            const partial = game._extended_command;
            const displayCommand = partial === 'l' && !game.flags?.debug ? 'loot'
                : partial === 'p' && !game.flags?.debug ? 'pray'
                : ['q', 'qu', 'qui'].includes(partial) ? 'quit' : partial;
            game._extended_display_command = displayCommand;
            await setMessage(`#${displayCommand ? ` ${displayCommand}` : ''}`);
            return;
	        }
	        game._extended_command = `${game._extended_command || ''}${ch}`;
	        const partial = game._extended_command;
	        const displayCommand = partial === 'cha' ? 'chat'
                : ['n', 'na', 'nam'].includes(partial) ? 'name'
                    : ['an', 'ann', 'anno', 'annot', 'annota', 'annotat'].includes(partial) ? 'annotate'
                    : ['ad', 'adj', 'adju', 'adjus', 'adjust'].includes(partial) ? 'adjust'
                    : ['chr', 'chro', 'chron', 'chroni', 'chronic', 'chronicl', 'chronicle'].includes(partial) ? 'chronicle'
                        : ['co', 'con', 'cond', 'condu', 'conduc', 'conduct'].includes(partial) ? 'conduct'
                    : ['f', 'fo', 'for', 'forc'].includes(partial) ? 'force'
                    : ['he', 'her', 'here', 'herec', 'herecm', 'herecmd', 'herecmdm', 'herecmdme', 'herecmdmen'].includes(partial) ? 'herecmdmenu'
                    : ['tu', 'tur'].includes(partial) ? 'turn'
                    : ['u', 'un', 'unt', 'untr', 'untra'].includes(partial) ? 'untrap'
                    : (partial === 'p' && !game.flags?.debug) || ['pr', 'pra'].includes(partial) ? 'pray'
                        : ['e', 'en', 'enh', 'enha', 'enhan', 'enhanc'].includes(partial) ? 'enhance'
                    : (partial === 's' && !game.flags?.debug) || partial === 'si' ? 'sit'
                        : ['d', 'di'].includes(partial) ? 'dip'
                            : ['of', 'off', 'offe', 'offer'].includes(partial) ? 'offer'
                                : ['ov', 'ove', 'over', 'overv', 'overvi', 'overvie', 'overview'].includes(partial) ? 'overview'
                                : ['ve', 'ver', 'vers', 'versi', 'versio', 'version'].includes(partial) ? 'version'
                                    : ['va', 'van', 'vanq', 'vanqu', 'vanqui', 'vanquis', 'vanquish', 'vanquishe', 'vanquished'].includes(partial) ? 'vanquished'
                                        : ['g', 'ge', 'gen', 'geno', 'genoc', 'genoci', 'genocid', 'genocide', 'genocided'].includes(partial) ? 'genocided'
                                            : ['te', 'ter', 'terr', 'terra', 'terrai', 'terrain'].includes(partial) ? 'terrain'
                        : ['ri', 'rid'].includes(partial) ? 'ride'
                            : ['j', 'ju', 'jum'].includes(partial) ? 'jump'
                            : game.flags?.debug && ['le', 'lev', 'leve', 'level', 'levelc', 'levelch', 'levelcha', 'levelchan', 'levelchang'].includes(partial) ? 'levelchange'
                            : partial === 'ru' ? 'rub'
                            : ['q', 'qu', 'qui'].includes(partial) ? 'quit'
                            : ['i', 'in', 'inv', 'invo', 'invok'].includes(partial) ? 'invoke'
                            : (partial === 'l' && !game.flags?.debug) || ['lo', 'loo'].includes(partial) ? 'loot'
	                            : ['mo', 'mon', 'mons', 'monst', 'monste'].includes(partial) ? 'monster'
	                            : game.flags?.debug && ['po', 'pol', 'poly', 'polys', 'polyse', 'polysel'].includes(partial) ? 'polyself'
	                            : game.flags?.debug && partial === 'wizm' ? 'wizmondiff'
	                            : game.flags?.debug && partial.length >= 4 && 'wizintrinsic'.startsWith(partial) ? 'wizintrinsic'
	                            : game.flags?.debug && partial.startsWith('wizw') && 'wizwhere'.startsWith(partial) ? 'wizwhere'
	                                : partial === 'wip' ? 'wipe'
	                : partial;
        game._extended_display_command = displayCommand;
        await setMessage(`# ${displayCommand}`);
        return;
    }

    if (game._command_mode === 'jumpIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
            game._jump_tip_seen = 1;
            game._command_mode = 'jumpTip';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'jumpTip') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage('Move cursor to the desired position:');
            game._farlook_x = game.u?.ux || 0;
            game._farlook_y = game.u?.uy || 0;
            game._command_mode = 'jumpCursor';
        }
        return;
    }

    if (game._command_mode === 'teleportIntroMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._pending_message = '';
            game._message_more = 0;
            if (!game._getpos_tip_seen && game.flags?.tutorial !== false) {
                setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
                game._getpos_tip_seen = 1;
                game._command_mode = 'teleportTip';
                return;
            }
            await showControlledTeleportCursorPrompt();
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'teleportTip') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await showControlledTeleportCursorPrompt();
        }
        return;
    }

    if (game._command_mode === 'jumpCursor') {
        game._getpos_prompt_cursor = null;
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            return;
        }
        const dir = movementDirection(ch);
        if (dir) {
            game._farlook_x = Math.max(1, Math.min(COLNO - 1, (game._farlook_x || game.u?.ux || 0) + dir.dx));
            game._farlook_y = Math.max(0, Math.min(ROWNO - 1, (game._farlook_y || game.u?.uy || 0) + dir.dy));
            const loc = game.level?.at(game._farlook_x, game._farlook_y);
            const object = (game.level?.objects || []).find(obj =>
                !obj.hidden && !obj.transientProjectile && obj.ox === game._farlook_x && obj.oy === game._farlook_y);
            const terrain = object ? pickupObjectPhrase(object)
                : !loc || (!loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ') ? 'unexplored area'
                : loc.typ === DOOR ? doorDescription(loc)
                : loc?.typ === CORR ? 'corridor'
                    : loc?.typ === ROOM || loc?.typ === STAIRS ? 'floor of a room'
                        : loc?.typ === MOAT ? 'moat'
                            : loc?.typ === TREE ? 'tree'
                            : loc?.typ && loc.typ < DOOR ? 'wall' : 'unexplored area';
            const dx = Math.abs((game._farlook_x || 0) - (game.u?.ux || 0));
            const dy = Math.abs((game._farlook_y || 0) - (game.u?.uy || 0));
            const stepX = Math.sign((game._farlook_x || 0) - (game.u?.ux || 0));
            const stepY = Math.sign((game._farlook_y || 0) - (game.u?.uy || 0));
            const midX = (game.u?.ux || 0) + (dx > dy ? stepX : 0);
            const midY = (game.u?.uy || 0) + (dy > dx ? stepY : 0);
            const midLoc = game.level?.at(midX, midY);
            const midBoulder = game.level?.objects?.some(obj =>
                !obj.transientProjectile && obj.otyp === BOULDER && obj.ox === midX && obj.oy === midY);
            const blockedJump = !midLoc || IS_OBSTRUCTED(midLoc.typ) || midBoulder
                || (midLoc.typ === DOOR && (midLoc.doormask & (D_CLOSED | D_LOCKED)));
            const validJump = loc && !IS_OBSTRUCTED(loc.typ) && dx * dx + dy * dy === 5 && !blockedJump;
            await setMessage(validJump ? terrain : `${terrain} (invalid target)`);
            return;
        }
        if (ch === ' ') {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const loc = game.level?.at(targetX, targetY);
            const object = (game.level?.objects || []).find(obj =>
                !obj.hidden && !obj.transientProjectile && obj.ox === targetX && obj.oy === targetY);
            const terrain = targetX === game.u?.ux && targetY === game.u?.uy ? heroFarlookDescription()
                : object ? pickupObjectPhrase(object)
                    : !loc || (!loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ') ? 'unexplored area'
                    : loc.typ === DOOR ? doorDescription(loc)
                        : loc?.typ === CORR ? 'corridor'
                            : loc?.typ === ROOM || loc?.typ === STAIRS ? 'floor of a room'
                                : loc?.typ === MOAT ? 'moat'
                                    : loc?.typ === TREE ? 'tree'
                                    : loc?.typ && loc.typ < DOOR ? 'wall' : 'unexplored area';
            await setMessage(`${terrain} (invalid target)`);
            return;
        }
        if (ch === '.') {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const dx = Math.abs(targetX - (game.u?.ux || 0));
            const dy = Math.abs(targetY - (game.u?.uy || 0));
            const loc = game.level?.at(targetX, targetY);
            const knightJump = dx * dx + dy * dy === 5;
            const stepX = Math.sign(targetX - (game.u?.ux || 0));
            const stepY = Math.sign(targetY - (game.u?.uy || 0));
            const midX = (game.u?.ux || 0) + (dx > dy ? stepX : 0);
            const midY = (game.u?.uy || 0) + (dy > dx ? stepY : 0);
            const midLoc = game.level?.at(midX, midY);
            const midBoulder = game.level?.objects?.some(obj =>
                !obj.transientProjectile && obj.otyp === BOULDER && obj.ox === midX && obj.oy === midY);
            const blockedJump = !midLoc || IS_OBSTRUCTED(midLoc.typ) || midBoulder
                || (midLoc.typ === DOOR && (midLoc.doormask & (D_CLOSED | D_LOCKED)));
            if (!loc || IS_OBSTRUCTED(loc.typ) || !knightJump || blockedJump) {
                await setMessage(!knightJump ? 'Illegal move!' : 'There is an obstacle preventing that jump.');
                game._command_mode = null;
                return;
            }
            const oldX = game.u?.ux || 0;
            const oldY = game.u?.uy || 0;
            game.u.ux0 = oldX;
            game.u.uy0 = oldY;
            game.u.ux = targetX;
            game.u.uy = targetY;
            newsym(oldX, oldY);
            newsym(targetX, targetY);
            vision_recalc(0);
            rnd(25);
            game._jump_delay_turn = 1;
            game.context.move = 1;
            game._process_command_time_now = 1;
            game._command_mode = null;
            game._keep_pending_message = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'teleportCursor') {
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game._teleport_cursor_position_mode = 0;
            game._teleport_from_scroll = 0;
            game.context.move = 1;
            game._process_command_time_now = 1;
            return;
        }
        if (ch === '$') {
            game._teleport_cursor_position_mode = 1;
            game._cursor_override = [(game._farlook_x || game.u?.ux || 0) - 1, (game._farlook_y || game.u?.uy || 0) + 1];
            await setMessage('Move cursor to the desired position:');
            return;
        }
        if (game._teleport_cursor_position_mode && ch === '.') {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const loc = game.level?.at(targetX, targetY);
            const label = loc?.typ === FOUNTAIN ? 'fountain'
                : loc?.typ === SINK ? 'sink'
                : loc?.typ === THRONE ? 'opulent throne'
                : loc?.typ === CORR ? 'corridor'
                : loc?.typ === DOOR ? doorDescription(loc)
                    : 'floor of a room';
            const here = loc?.typ === FOUNTAIN ? 'There is a fountain here.'
                : loc?.typ === SINK ? 'There is a sink here.'
                : loc?.typ === THRONE ? 'There is an opulent throne here.'
                    : label;
            const attachedObjects = [game.u?.uchain, game.u?.uball].filter(obj =>
                obj && !obj.hidden && obj.ox === targetX && obj.oy === targetY);
            if (attachedObjects.length > 1) {
                const prompt = 'Move cursor to the desired position:';
                const message = `${prompt}${' '.repeat(Math.max(1, 40 - prompt.length))}${here}`;
                const rows = [[0, 0, message], [2, 40, 'Things that are here:']];
                for (let i = 0; i < attachedObjects.length; i++)
                    rows.push([i + 3, 40, pickupObjectPhrase(attachedObjects[i])]);
                rows.push([attachedObjects.length + 3, 40, '--More--']);
                setOverlay(rows, attachedObjects.length + 4, false, 39);
                game._pending_message = message;
                game._message_more = 1;
                game._keep_pending_message = 1;
                game._command_mode = 'objectListMore';
                game._object_list_restore_message = prompt;
                game._deferred_context_move = 1;
                game._object_list_process_deferred_now = 1;
                game._teleport_cursor_position_mode = 0;
                game.context.move = 0;
                return;
            }
            await setMessage(`${'Move cursor to the desired position:'.padEnd(40)}${here}`);
            return;
        }
        if (ch === '<' || ch === '>') {
            const up = ch === '<';
            let target = null;
            for (let stair = game.stairs; stair; stair = stair.next) {
                const loc = game.level?.at(stair.sx, stair.sy);
                if (game.u?.blind && !loc?.seenv) continue;
                if (!!stair.up === up) {
                    target = stair;
                    break;
                }
            }
            if (!target) {
                await setMessage(`Can't find dungeon feature '${ch}'.`);
                return;
            }
            game._farlook_x = target.sx;
            game._farlook_y = target.sy;
            game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
            game._teleport_dot_described = 1;
            await setMessage(`staircase ${up ? 'up' : 'down'}`);
            return;
        }
        if (ch === '{') {
            let target = null;
            for (let x = 1; x < COLNO && !target; x++)
                for (let y = 0; y < ROWNO; y++)
                    if (game.level?.at(x, y)?.typ === FOUNTAIN) {
                        target = { x, y };
                        break;
                    }
            if (!target) {
                await setMessage("Can't find dungeon feature '{'.");
                return;
            }
            game._farlook_x = target.x;
            game._farlook_y = target.y;
            game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
            game._teleport_dot_described = 1;
            await setMessage('fountain');
            return;
        }
        if (ch === '_') {
            let target = null;
            for (let x = 1; x < COLNO && !target; x++)
                for (let y = 0; y < ROWNO; y++) {
                    const loc = game.level?.at(x, y);
                    if (loc?.typ === ALTAR && (!game.u?.blind || loc.seenv)) {
                        target = { x, y };
                        break;
                    }
                }
            if (!target) {
                await setMessage("Can't find dungeon feature '_'.");
                return;
            }
            game._farlook_x = target.x;
            game._farlook_y = target.y;
            game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
            game._teleport_dot_described = 1;
            await setMessage('altar');
            return;
        }
        const dir = movementDirection(ch);
        if (dir && ['h', 'j', 'k', 'l'].includes(ch)) {
            game._farlook_x = Math.max(1, Math.min(COLNO - 1, (game._farlook_x || game.u?.ux || 0) + dir.dx));
            game._farlook_y = Math.max(0, Math.min(ROWNO - 1, (game._farlook_y || game.u?.uy || 0) + dir.dy));
            const loc = game.level?.at(game._farlook_x, game._farlook_y);
            const text = loc?.typ === STONE ? 'stone'
                : !loc || (!loc.seenv && !loc.remembered_glyph && loc.disp_ch === ' ') ? 'unexplored area'
                : loc.typ === CORR ? 'corridor'
                : loc?.typ === DOOR ? doorDescription(loc)
                    : loc?.typ === ROOM || loc?.typ === STAIRS ? 'floor of a room'
                        : loc?.typ === MOAT ? 'moat'
                            : loc?.typ === TREE ? 'tree'
                            : loc?.typ && loc.typ < DOOR ? 'wall' : 'unexplored area';
            game._teleport_dot_described = 0;
            await setMessage(text);
            return;
        }
        if (/^[1-9]$/.test(ch)) {
            await setMessage(`Unknown direction: '${visibleKey(ch)}' (use 'h', 'j', 'k', 'l' or '.').`);
            return;
        }
        if (ch === '0') {
            await setMessage("Can't find dungeon feature '0'.");
            return;
        }
        if (ch === ' ' || (ch === '.' && game._teleport_dot_described)) {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const loc = game.level?.at(targetX, targetY);
            if (game._teleport_dot_described && loc && !IS_OBSTRUCTED(loc.typ)) {
                if (isBuriedBallTrapActive()) buriedBallToPunishment();
                const oldX = game.u?.ux || 0;
                const oldY = game.u?.uy || 0;
                const oldBallX = game.u?.uball?.ox;
                const oldBallY = game.u?.uball?.oy;
                const oldChainX = game.u?.uchain?.ox;
                const oldChainY = game.u?.uchain?.oy;
                newsym(oldX, oldY);
                game.u.ux0 = oldX;
                game.u.uy0 = oldY;
                game.u.ux = targetX;
                game.u.uy = targetY;
                if (game.u.uball && game.u.uchain) {
                    game.u.uball.ox = targetX;
                    game.u.uball.oy = targetY;
                    game.u.uchain.ox = targetX;
                    game.u.uchain.oy = targetY;
                    if (oldBallX != null && oldBallY != null) newsym(oldBallX, oldBallY);
                    if (oldChainX != null && oldChainY != null) newsym(oldChainX, oldChainY);
                }
                newsym(targetX, targetY);
                vision_recalc(0);
                await docrt();
                game._teleport_dot_described = 0;
                game._command_mode = null;
                if (game._teleport_from_scroll) {
                    game._teleport_from_scroll = 0;
                    game._teleport_cursor_position_mode = 0;
                    game._cursor_override = null;
                    game._teleport_geometric_online_once = 1;
                    await queueUntendedTempleEntryAfterTeleport(oldX, oldY, targetX, targetY);
                    await setMessage(`You materialize in ${targetX === oldX && targetY === oldY ? 'the same' : 'a different'} location!`, true);
                    game.context.move = 1;
                    return;
                }
                applySameLevelTeleportNutritionPenalty();
                const up = game.level?.upstair?.x === targetX && game.level?.upstair?.y === targetY;
                const down = game.level?.dnstair?.x === targetX && game.level?.dnstair?.y === targetY;
                const label = up || down ? `staircase ${up ? 'up' : 'down'}`
                    : loc?.typ === FOUNTAIN ? 'fountain'
                    : loc?.typ === SINK ? 'sink'
                    : loc?.typ === THRONE ? 'opulent throne'
                    : '';
                const attachedObjects = [game.u?.uchain, game.u?.uball].filter(obj =>
                    obj && !obj.hidden && obj.ox === targetX && obj.oy === targetY);
                game._teleport_geometric_online_once = 1;
                if (ch === '.' && label && attachedObjects.length > 1) {
                    const feature = up || down ? `There is a staircase ${up ? 'up' : 'down'} here.`
                        : `There is ${label === 'opulent throne' ? 'an' : 'a'} ${label} here.`;
                    const message = `${label}${' '.repeat(Math.max(1, 40 - label.length))}${feature}`;
                    const rows = [[0, 0, message], [2, 40, 'Things that are here:']];
                    for (let i = 0; i < attachedObjects.length; i++)
                        rows.push([i + 3, 40, pickupObjectPhrase(attachedObjects[i])]);
                    rows.push([attachedObjects.length + 3, 40, '--More--']);
                    setOverlay(rows, attachedObjects.length + 4, false, 39);
                    game._pending_message = message;
                    game._message_more = 1;
                    game._keep_pending_message = 1;
                    game._command_mode = 'objectListMore';
                    game._object_list_restore_message = label;
                    game._deferred_context_move = 1;
                    game.context.move = 0;
                    return;
                }
                await setMessage(label);
                game.context.move = 1;
                return;
            }
            let text = targetX === game.u?.ux && targetY === game.u?.uy ? heroFarlookDescription()
                : loc?.typ === STAIRS && game.level?.upstair?.x === targetX && game.level?.upstair?.y === targetY ? 'staircase up'
                : loc?.typ === STAIRS && game.level?.dnstair?.x === targetX && game.level?.dnstair?.y === targetY ? 'staircase down'
                : loc?.typ === CORR ? 'corridor'
                : loc?.typ === DOOR ? doorDescription(loc)
                    : loc?.typ === ROOM ? 'floor of a room' : 'unexplored area';
            await setMessage(text);
            return;
        }
        if (ch === '\r' || ch === '\n') {
            if (game._pending_message === "Can't find dungeon feature '0'.") {
                const corridorY = controlledTeleportNewlineCorridorY(game._farlook_x || game.u?.ux || 0, game._farlook_y || game.u?.uy || 0);
                if (corridorY != null) game._farlook_y = corridorY;
            }
            await setMessage('corridor');
            return;
        }
        if (ch === '.') {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const loc = game.level?.at(targetX, targetY);
            if (targetX !== game.u?.ux || targetY !== game.u?.uy) {
                let selectedValid = !!(loc && ACCESSIBLE(loc.typ));
                selectedValid &&= !(game.level?.traps || []).some(trap => trap.tx === targetX && trap.ty === targetY);
                selectedValid &&= !(game.level?.monsters || []).some(mon => mon.mx === targetX && mon.my === targetY);
                selectedValid &&= !(game.level?.objects || []).some(obj => obj.otyp === BOULDER && obj.ox === targetX && obj.oy === targetY);
                if (!selectedValid) {
                    const oldX = game.u?.ux || 0;
                    const oldY = game.u?.uy || 0;
                    const materializeMessage = safeTeleportHeroSameLevel();
                    game._travel_previous_target = null;
                    const landingX = game.u?.ux || oldX;
                    const landingY = game.u?.uy || oldY;
                    game._teleport_dot_described = 0;
                    game._teleport_cursor_position_mode = 0;
                    game._cursor_override = null;
                    game._command_mode = null;
                    game._teleport_from_scroll = 0;
                    applySameLevelTeleportNutritionPenalty();
                    game.context.move = 1;
                    await queueUntendedTempleEntryAfterTeleport(oldX, oldY, landingX, landingY);
                    await docrt();
                    const attachedObjects = [game.u?.uchain, game.u?.uball].filter(obj =>
                        obj && !obj.hidden && obj.ox === landingX && obj.oy === landingY);
                    if (attachedObjects.length > 1) {
                        const rows = [[0, 40, 'Things that are here:']];
                        for (let i = 0; i < attachedObjects.length; i++)
                            rows.push([i + 1, 40, pickupObjectPhrase(attachedObjects[i])]);
                        rows.push([attachedObjects.length + 1, 40, '--More--']);
                        game._queued_overlay_after_more = {
                            lines: rows,
                            clearRows: attachedObjects.length + 2,
                            clearCol: 39,
                            mode: 'objectListMore',
                            message: 'text-window',
                            messageMore: true,
                        };
                        game._object_list_no_time = 1;
                        await setMessage('Sorry...', true);
                        return;
                    }
                    await setMessage(materializeMessage ? `Sorry...  ${materializeMessage}` : 'Sorry...');
                    return;
                }
                let landingX = targetX;
                let landingY = targetY;
                if (isBuriedBallTrapActive()) buriedBallToPunishment();
                const oldX = game.u?.ux || 0;
                const oldY = game.u?.uy || 0;
                const oldBallX = game.u?.uball?.ox;
                const oldBallY = game.u?.uball?.oy;
                const oldChainX = game.u?.uchain?.ox;
                const oldChainY = game.u?.uchain?.oy;
                if (game.u) {
                    game.u.ux0 = oldX;
                    game.u.uy0 = oldY;
                    game.u.ux = landingX;
                    game.u.uy = landingY;
                }
                if (game.u?.uball) {
                    game.u.uball.ox = landingX;
                    game.u.uball.oy = landingY;
                }
                if (game.u?.uchain) {
                    game.u.uchain.ox = landingX;
                    game.u.uchain.oy = landingY;
                }
                newsym(oldX, oldY);
                if (oldBallX != null && oldBallY != null) newsym(oldBallX, oldBallY);
                if (oldChainX != null && oldChainY != null) newsym(oldChainX, oldChainY);
                newsym(landingX, landingY);
                vision_recalc(0);
                await docrt();
                game._teleport_dot_described = 0;
                game._teleport_cursor_position_mode = 0;
                game._cursor_override = null;
                game._command_mode = null;
                game._teleport_from_scroll = 0;
                applySameLevelTeleportNutritionPenalty();
                game.context.move = 1;
                const materializeMessage = `You materialize in ${landingX === oldX && landingY === oldY ? 'the same' : 'a different'} location!`;
                await queueUntendedTempleEntryAfterTeleport(oldX, oldY, landingX, landingY);
                await setMessage(materializeMessage, true);
                return;
            }
            const up = game.level?.upstair?.x === targetX && game.level?.upstair?.y === targetY;
            const down = game.level?.dnstair?.x === targetX && game.level?.dnstair?.y === targetY;
            const attachedObjects = [game.u?.uball, game.u?.uchain].filter(obj =>
                obj && !obj.hidden && obj.ox === targetX && obj.oy === targetY);
            if (game.u?.blind && attachedObjects.length > 1) {
                game._blind_arrival_overlay_after_more = arrivalObjectListRows(attachedObjects);
                game._blind_arrival_overlay_process_deferred_now = 1;
                game._blind_arrival_overlay_deferred_context_move = 1;
                game._blind_arrival_overlay_nohands_on_other_key = 1;
                await setMessage('You try to feel what is lying here on the floor.', true);
                return;
            }
            game._teleport_dot_described = 1;
            if (up || down) {
                const label = `staircase ${up ? 'up' : 'down'}`;
                await setMessage(`${label}\x1b[${40 - label.length}CThere is a staircase ${up ? 'up' : 'down'} here.`);
            } else
                await setMessage(!loc || IS_OBSTRUCTED(loc.typ) ? 'unexplored area' : 'floor of a room');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'lootDirection') {
        if (ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        setOverlay(CMDASSIST_DIRECTION_LINES, 24, true);
        return;
    }

    if (game._command_mode === 'untrapDirection') {
        game._command_mode = null;
        if (ch === '\x1b') {
            await setMessage('');
            return;
        }
        const dir = ch === '.' ? { dx: 0, dy: 0 } : movementDirection(ch);
        if (dir) {
            const x = (game.u?.ux || 0) + dir.dx;
            const y = (game.u?.uy || 0) + dir.dy;
            if (!isok(x, y)) {
                await setMessage('The perils lurking there are beyond your grasp.');
                return;
            }
            const trap = (game.level?.traps || []).find(candidate =>
                candidate.tx === x && candidate.ty === y && candidate.tseen && candidate.ttyp === SQKY_BOARD);
            if (trap) {
                game._untrap_squeaky_target = { x, y };
                await setMessage(squeakyBoardUntrapPrompt());
                game._command_mode = 'untrapSqueakyTool';
                return;
            }
        }
        await setMessage('And just how do you expect to do that?');
        return;
    }

    if (game._command_mode === 'untrapSqueakyInventoryOverlay') {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(squeakyBoardUntrapPrompt());
            game._command_mode = 'untrapSqueakyTool';
            return;
        }
        if (!(game.inventory || []).some(invItem => invItem.letter === ch && isSqueakyBoardUntrapTool(invItem))) {
            game._keep_pending_message = 1;
            return;
        }
        game._command_mode = 'untrapSqueakyTool';
    }

    if (game._command_mode === 'untrapSqueakyTool') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._untrap_squeaky_target = null;
            game._command_mode = null;
            await setMessage('Never mind.');
            return;
        }
        if (ch === '?' || ch === '*') {
            setOverlay(inventoryOverlayLines(0, false, isSqueakyBoardUntrapTool), 24, true);
            game._command_mode = 'untrapSqueakyInventoryOverlay';
            return;
        }
        const tool = (game.inventory || []).find(invItem =>
            invItem.letter === ch && isSqueakyBoardUntrapTool(invItem));
        if (!tool) {
            game._topline_after_more = squeakyBoardUntrapPrompt();
            await setMessage("You don't have that object.", true);
            return;
        }
        const target = game._untrap_squeaky_target;
        const trap = target && (game.level?.traps || []).find(candidate =>
            candidate.tx === target.x && candidate.ty === target.y && candidate.tseen && candidate.ttyp === SQKY_BOARD);
        game._untrap_squeaky_target = null;
        game._command_mode = null;
        if (!trap) {
            await setMessage('And just how do you expect to do that?');
            return;
        }
        if (isBadSqueakyBoardUntrapTool(tool) || rn2(squeakyBoardUntrapChance(trap))) {
            await setMessage('That squeaky board is difficult to disarm.');
            game.context.move = 1;
            return;
        }
        const messages = [];
        if (!useSqueakyBoardUntrapTool(tool, messages)) {
            await setMessage('That squeaky board is difficult to disarm.');
            game.context.move = 1;
            return;
        }
        messages.push('You repair the squeaky board.');
        game.level.traps = (game.level?.traps || []).filter(candidate => candidate !== trap);
        newsym(trap.tx, trap.ty);
        await setMessage(messages.join('  '), messages.length > 1);
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'quitConfirm') {
        if (ch === 'y') {
            game._quit_game = 1;
            if (game.flags?.debug) {
                await setMessage('Dump core? [ynq] (q)');
                game._command_mode = 'dumpCoreConfirm';
                return;
            }
            game._death_moves = game.moves || 1;
            const dsum = deathSummary();
            const farewell = {
                Knight: 'Fare thee well',
                Samurai: 'Sayonara',
                Tourist: 'Aloha',
                Valkyrie: 'Velkommen',
            }[dsum.role] || 'Goodbye';
            setOverlay([
                [0, 0, `${farewell} ${dsum.name} the ${dsum.role}...`],
                [2, 0, `You quit in ${dsum.dungeon} on dungeon level ${dsum.depth} with ${dsum.score} point${dsum.score === 1 ? '' : 's'},`],
                [3, 0, `and ${dsum.gold} piece${dsum.gold === 1 ? '' : 's'} of gold, after ${dsum.turns} moves.`],
                [4, 0, `You were level ${dsum.level} with a maximum of ${dsum.hpmax} hit points when you quit.`],
                [23, 0, '--More--'],
            ], 24, true);
            game._command_mode = 'quitSummaryMore';
            return;
        }
        game._command_mode = null;
        return;
    }

	    if (game._command_mode === 'quitSummaryMore') {
	        if (ch === ' ') {
	            const scoreRows = deathScoreLines();
	            setOverlay(scoreRows, 24, true);
	            game._overlay_cursor = [0, Math.min(23, Math.max(...scoreRows.map(([row]) => row)) + 1)];
	            game._command_mode = 'dead';
	            game.program_state ??= {};
	            game.program_state.gameover = true;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'saveConfirm') {
        if (ch === 'y') {
            game._command_mode = null;
            vfsWriteFile(game._savefile, encodeSaveState());
            await setMessage('Be seeing you...');
            game.program_state ??= {};
            game.program_state.gameover = true;
            return;
        }
        if (ch === 'n' || ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._command_mode = null;
            await setMessage('');
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'dumpCoreConfirm') {
        if (ch === '\x1b') {
            setOverlay([[1, 0, 'Since you were in wizard mode, the score list will not be checked.']], 24, true);
            game._overlay_cursor = [0, 4];
            game._command_mode = null;
            game.program_state ??= {};
            game.program_state.gameover = true;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'wizGenesisMonster') {
        if (ch === '\r' || ch === '\n') {
            const monster = (game._wizgenesis_text || '').trim().toLowerCase();
            game._wizgenesis_text = '';
            game._command_mode = null;
            const mdat = monsterByRndName(monster)
                || (monster ? { name: monster, mlet: monster[0], mlevel: 0, mmove: 12, maligntyp: 0, hostile: true, neuter: false } : null);
            const spot = mdat ? enextoMonsterSpot(game.u?.ux || 0, game.u?.uy || 0, mdat) : null;
            if (spot) {
                const created = await makemon(mdat, spot.x, spot.y, 0);
                if (created) {
                    created.msleeping = 0;
                    created.mpeaceful = 0;
                    newsym(created.mx, created.my);
                    if (created.data?.name === 'minotaur') {
                        game._pending_message = '';
                        game._message_more = 0;
                        game._keep_pending_message = 0;
                    } else {
                        await setMessage(`A ${created.data?.name || monster || 'monster'} appears next to you.`);
                    }
                    return;
                }
            }
            await setMessage('Nothing happens.');
            return;
        }
        if (key === 8 || key === 127) game._wizgenesis_text = (game._wizgenesis_text || '').slice(0, -1);
        else if (key >= 32) game._wizgenesis_text = `${game._wizgenesis_text || ''}${ch}`;
        await setMessage(`Create what kind of monster?${game._wizgenesis_text ? ` ${game._wizgenesis_text}` : ''}`);
        return;
    }

    if (game._command_mode === 'prayConfirm') {
        if (ch === 'y') {
            const pantheonRole = role === 'Priest' ? game._pantheon_role || 'Barbarian' : role;
            const gods = {
                Barbarian: ['Mitra', 'Crom', 'Set'],
                Knight: ['Lugh', 'Brigit', 'Manannan Mac Lir'],
                Priest: ['Mitra', 'Crom', 'Set'],
                Ranger: ['Mercury', 'Venus', 'Mars'],
                Samurai: ['Amaterasu Omikami', 'Raijin', 'Susanowo'],
                Tourist: ['Blind Io', 'The Lady', 'Offler'],
                Valkyrie: ['Tyr', 'Odin', 'Loki'],
                Wizard: ['Ptah', 'Thoth', 'Anhur'],
            }[pantheonRole] || ['Marduk', 'Marduk', 'Marduk'];
            const align = game.u?.ualign?.type ?? 0;
            game._prayer_god = gods[align > 0 ? 0 : align < 0 ? 2 : 1];
            if (!game._chronicle_rejected_atheism) {
                game._chronicle_entries ??= [];
                game._chronicle_entries.push({ turn: game.moves || 1, text: 'rejected atheism with a prayer' });
                game._chronicle_rejected_atheism = 1;
            }
            if (game.flags?.debug) {
                await setMessage(`You begin praying to ${game._prayer_god}.`, true);
                game._command_mode = 'prayerBeginMore';
                return;
            }
            await setMessage(`You begin praying to ${game._prayer_god}.  You finish your prayer.`, true);
            game._command_mode = 'prayerFinishMore';
            game._prayer_too_soon = (game.u?.ublesscnt || 0) > 0 ? 1 : 0;
            game._prayer_occupation = 1;
            game._prayer_pending_done = 1;
            game._pending_prayer_finish_message = 1;
            game._process_command_time_now = 1;
            game._process_time_with_more = 1;
            game.context.move = 3;
            return;
        }
        await setMessage('Never mind.');
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'rideDirection') {
        await rideDirection(ch);
        return;
    }

    if (game._command_mode === 'kickDirection') {
        const dir = movementDirection(ch);
        if (!dir) {
            game._command_mode = null;
            return;
        }
        const x = (game.u?.ux || 0) + dir.dx;
        const y = (game.u?.uy || 0) + dir.dy;
        game._kickedloc = { x, y, turn: game.moves || 1 };
        const target = game.level?.at(x, y);
        const invisibleTarget = target?.map_invisible
            ? (game.level?.monsters || []).find(mon => mon.mx === x && mon.my === y)
            : null;
        if (invisibleTarget) {
            rn2(20);
            rn2(3);
            const damage = rnd(1);
            invisibleTarget.mhp = Math.max(0, (invisibleTarget.mhp || 1) - damage);
            rn2(19);
            rn2(3);
            invisibleTarget._bullwhip_skip_attack = 1;
            game._clear_bullwhip_skip_after_time = invisibleTarget;
            invisibleTarget._bullwhip_find_misc_ready = 0;
            await setMessage('You kick it.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const statueObj = floorStatueAt(x, y);
        const statueTrap = statueObj ? statueTrapAt(x, y) : null;
        if (statueTrap) {
            const message = await activateStatueTrap(statueTrap, x, y, { normal: true }) || '';
            await setMessage(message);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        const kickedObject = await kickFloorObjectToward(dir, x, y);
        if (kickedObject.handled) {
            await setMessage(kickedObject.messages.join('  '), kickedObject.messages.length > 1);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (target?.typ === DOOR && (target.doormask & (D_CLOSED | D_LOCKED))) {
            const stats = game.u?.acurr?.a || [];
            const avgAttr = Math.trunc(((stats[0] ?? 10) + (stats[3] ?? 10) + (stats[4] ?? 10)) / 3);
            const martialKick = role === 'Monk' || role === 'Samurai';
            rn2(19);
            const kickRoll = rnl(35);
            const successfulKick = kickRoll < avgAttr + (martialKick ? (stats[3] ?? 10) : 0);
            const shatters = successfulKick && (stats[0] ?? 10) > 18 && !rn2(5);
            rn2(19);
            if (successfulKick) {
                target.doormask = shatters ? D_NODOOR : D_BROKEN;
                newsym(x, y);
                vision_reset();
                vision_recalc(0);
                const kickReveal = game._tutorial_triggers?.kickReveal;
                if (kickReveal && x === kickReveal.x && y === kickReveal.y) {
                    for (const cell of kickReveal.cells || []) {
                        const loc = game.level.at(cell.x, cell.y);
                        loc.seenv = 0;
                        loc.waslit = false;
                        if (game.viz_array?.[cell.y]) game.viz_array[cell.y][cell.x] = 0;
                        newsym(cell.x, cell.y);
                    }
                }
                const petAttackPending = (game.level?.monsters || []).some(mon => mon.pet && (mon.movement || 0) >= 12
                    && (game.level?.monsters || []).some(other => !other.pet && !other.mpeaceful
                        && Math.max(Math.abs(mon.mx - other.mx), Math.abs(mon.my - other.my)) <= 1));
                await setMessage(shatters
                    ? 'As you kick the door, it shatters to pieces!'
                    : 'As you kick the door, it crashes open!', petAttackPending);
            } else {
                const sound = rn2(3) ? 'Whammm!!' : 'Thwack!!';
                await setMessage(sound);
            }
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (!target || IS_OBSTRUCTED(target.typ) || target.typ === STAIRS || target.typ === LADDER) {
            const stats = game.u?.acurr?.a || [];
            rn2(2);
            rn2(2);
            if (!rn2(3)) {
                const woundDuration = 5 + rnd(5);
                if (!game.u._woundedLegTurns && !game.u._woundedDexPenalty) {
                    game.u.acurr.a[A_DEX] = Math.max(3, (game.u.acurr.a[A_DEX] || 9) - 1);
                    game.u._woundedDexPenalty = 1;
                }
                game.u._woundedLegTurns = Math.max(game.u._woundedLegTurns || 0, woundDuration);
                game.u._woundedLegSide = 'right';
            }
            const damage = rnd((stats[4] ?? 10) > 15 ? 3 : 5);
            game.u.uhp = Math.max(0, (game.u?.uhp || 0) - damage);
            await setMessage('Ouch!  That hurts!', game.u.uhp <= 0);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        rn2(2);
        await setMessage('You kick at empty space.');
        game._command_mode = null;
        game.context.move = 1;
        return;
    }

    if (game._command_mode === 'chatPriceQuoteMenu') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            if (game._pending_time_passed > 0) {
                game._pending_time_passed--;
                chatConsumeTurn();
            } else {
                game.context.move = 0;
            }
        }
        return;
    }

    if (game._command_mode === 'chatDirection') {
        if (ch === '.' || ch === 's' || ch === 'S') {
            await setMessage('Talking to yourself is a bad habit for a dungeoneer.');
            game._command_mode = null;
            return;
        }
        const dir = commandDirection(ch);
        if (!dir) {
            game._command_mode = null;
            game._pending_message = '';
            game._message_more = 0;
            return;
        }
        if (dir.dz > 0 && game.u?.usteed) {
            await finishChatSteedTarget(game.u.usteed);
            return;
        }
        if (dir.dz) {
            await setMessage(`They won't hear you ${dir.dz < 0 ? 'up' : 'down'} there.`);
            game._command_mode = null;
            return;
        }
        const tx = (game.u?.ux || 0) + dir.dx;
        const ty = (game.u?.uy || 0) + dir.dy;
        if (!isok(tx, ty)) {
            chatClearNoTarget();
            return;
        }
        const target = dir ? game.level?.monsters?.find(mon => mon.mx === tx && mon.my === ty) : null;
        const loc = dir ? game.level?.at(tx, ty) : null;
        if (!target || target.mundetected) {
            if (chatVisibleStatueAt(tx, ty)) {
                if (!heroIsBlind()) await setMessage(`The ${chatStatueTargetName()} seems not to notice you.`);
                else chatClearNoTarget();
                game._command_mode = null;
                return;
            }
            if (!heroIsDeaf() && chatTargetIsWall(loc)) {
                if (heroIsBlind() && !chatBlindHasMappedWall(loc)) chatClearNoTarget();
                else {
                    await setMessage(chatWallTargetMessage());
                    game._command_mode = null;
                }
                return;
            }
        }
        if (!target || target.mundetected || tipHatApparentObjectOrFurniture(target)) {
            chatClearNoTarget();
            return;
        }
        if (target && maybeQueueQuestLeaderTalk(target, { automatic: false })) {
            return;
        }
        const targetSound = tipHatMonsterSound(target);
        await finishChatMonsterTarget(target, targetSound);
        return;
    }

    if (game.keyBindings?.[ch] === 'inventory') ch = 'i';

    if (ch === '#' && game._force_fight && !game._command_mode) {
        game._force_fight = 0;
        await setMessage("The 'F' prefix should be followed by a movement command.");
        return;
    }

    if (ch === '#' && !game._command_mode) {
        game._command_mode = 'extendedCommand';
        game._extended_command = '';
        game._pending_message = '';
        game._message_more = 0;
        game._pending_time_passed = 0;
        await setMessage('#');
        return;
    }

    if (ch === '\x04' && !game._command_mode) {
        if (game.u?._woundedLegTurns) {
            const side = game.u._woundedLegSide === 'left' ? 'left '
                : game.u._woundedLegSide === 'right' ? 'right ' : '';
            await setMessage(`Your ${side}leg is in no shape for kicking.`, true);
            return;
        }
        await setMessage('In what direction?');
        game._command_mode = 'kickDirection';
        return;
    }

    if (/^[1-9]$/.test(ch) && !game._command_mode) {
        if (game._count_prefix) {
            game._count_prefix += ch;
            await setMessage(`Count: ${game._count_prefix}`);
        } else {
            game._count_prefix = ch;
            if (game._pending_message) game._keep_pending_message = 2;
        }
        return;
    }

    if (ch === '0' && game._count_prefix && !game._command_mode) {
        game._count_prefix += ch;
        await setMessage(`Count: ${game._count_prefix}`);
        return;
    }

    if (game._command_mode === 'tinEatConfirm') {
        const pending = game._tin_opened_pending;
        if (ch === 'y') {
            game._tin_opened_pending = null;
            await finishTinContents(pending?.tin, !!pending?.floorObject, true, pending?.variety);
            return;
        }
        if (ch === 'q' || ch === '\x1b' || ch === 'n' || ch === ' ' || ch === '\r' || ch === '\n') {
            game._tin_opened_pending = null;
            await finishTinContents(pending?.tin, !!pending?.floorObject, false, pending?.variety);
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'tinOpenerObject') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            await setMessage('Never mind.');
            game._command_mode = null;
            game._tin_opener_letter = '';
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (item && isTinObject(item)) {
            game._tin_opener_letter = '';
            await startTinOpening(item, false);
            return;
        }
        const letters = (game.inventory || []).filter(isTinObject).map(invItem => invItem.letter).filter(Boolean).join('');
        await setMessage("You don't have that object.", true);
        game._topline_after_more = `What do you want to open? [${getobjPromptLetters(letters)} or ?*]`;
        return;
    }

    if (game._command_mode === 'tinningFloorConfirm') {
        const kit = (game.inventory || []).find(invItem => invItem.letter === game._apply_tinning_kit_letter);
        const corpse = game._tinning_floor_object;
        if (ch === 'y') {
            await finishTinningKitUse(kit, corpse, { floorObject: true });
            return;
        }
        if (ch === 'n') {
            game._tinning_floor_object = null;
            await beginTinningInventorySelection(kit);
            return;
        }
        if (ch === 'q' || ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            clearTinningKitState();
            await setMessage('Never mind.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'tinningObject') {
        if (ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            clearTinningKitState();
            await setMessage('Never mind.');
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        if (ch === '?' || ch === '*') {
            setOverlay(inventoryOverlayLines(0, false, isTinnableCorpse), 24, true);
            game._command_mode = 'tinningInventoryOverlay';
            return;
        }
        const kit = (game.inventory || []).find(invItem => invItem.letter === game._apply_tinning_kit_letter);
        const corpse = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!corpse) {
            game._topline_after_more = tinningInventoryPrompt();
            await setMessage("You don't have that object.", true);
            return;
        }
        if (!isTinnableCorpse(corpse)) {
            clearTinningKitState();
            await setMessage(corpse.oeaten ? 'You cannot tin something which is partly eaten.' : "That's too insubstantial to tin.");
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        await finishTinningKitUse(kit, corpse, { floorObject: false });
        return;
    }

    if (game._command_mode === 'tinningInventoryOverlay') {
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(tinningInventoryPrompt());
            game._command_mode = 'tinningObject';
            return;
        }
        if (!(game.inventory || []).some(invItem => invItem.letter === ch && isTinnableCorpse(invItem))) {
            game._keep_pending_message = 1;
            return;
        }
        const kit = (game.inventory || []).find(invItem => invItem.letter === game._apply_tinning_kit_letter);
        const corpse = (game.inventory || []).find(invItem => invItem.letter === ch && isTinnableCorpse(invItem));
        await finishTinningKitUse(kit, corpse, { floorObject: false });
        return;
    }

    if (game._command_mode === 'eatInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(eatingPrompt());
            game._command_mode = 'eatObject';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'continueEatingPrompt') {
        if (ch === 'y') {
            game._command_mode = null;
            game._keep_pending_message = 1;
            game.context.move = game._eating_turns_remaining > 0 ? 1 : 0;
            return;
        }
        if (ch === 'n' || ch === 'q' || ch === '\x1b' || ch === ' ' || ch === '\r' || ch === '\n') {
            declineDelayedFoodContinuation();
            game._command_mode = null;
            game._keep_pending_message = 1;
            game.context.move = 0;
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'eatObject') {
        if (ch === '\x1b') {
            game._command_mode = null;
            if (game.u?.blind || (game.u?._statusSuffix || '').includes('Blind')) game._keep_pending_message = 1;
            else await setMessage('Never mind.');
            return;
        }
        if (ch === '\r' || ch === '\n') {
            game._command_mode = null;
            game._keep_pending_message = 1;
            return;
        }
        if (ch === ' ') {
            game._command_mode = null;
            if (game.u?.blind || (game.u?._statusSuffix || '').includes('Blind')) game._keep_pending_message = 1;
            else await setMessage('Never mind.');
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item) {
            await setMessage("You don't have that object.", true);
            game._command_mode = 'eatInvalidMore';
            return;
        }
        if ((game.u?._statusSuffix || '').includes('Overloaded')) {
            await setMessage("You can't do that while carrying so much stuff.");
            game._command_mode = null;
            return;
        }
        if (heroCanEatNonFoodMetal(item)) {
            await eatHeroNonFoodMetal(item);
            return;
        }
        if (item?.cls === 'food' || item?.otyp === FOOD_CLASS || item?.otyp === 'corpse' || item?.otyp === CORPSE) {
            const name = item.singular || pickupObjectName({ ...item, quan: 1 });
            const fortuneCookie = isFortuneCookieFood(item);
            const corpse = item.otyp === 'corpse' || item.otyp === CORPSE;
            if (isTinObject(item)) {
                await startTinOpening(item, false);
                return;
            }
            if (corpse) {
                const corpseName = item.corpsenm?.name || 'monster';
                if (corpseName === 'lichen') {
                    rn2(10);
                    removeInventoryItem(item);
                    game._pet_food_scan_inventory = game.inventory || [];
                    game._eating_turns_remaining = 9;
                    game._eating_finish_message = 'You finish eating the lichen corpse.';
                    await setMessage('This lichen corpse tastes okay.');
                    game._command_mode = null;
                    game.context.move = 9;
                    return;
                }
                rn2(20);
                rn2(7);
                rn2(5);
                removeInventoryItem(item);
                game._pet_food_scan_inventory = [...(game.inventory || []), item];
                game._eating_turns_remaining = 1;
                game._eating_finish_message = `You finish eating the ${corpseName} corpse.`;
                game._eating_newt_buzz = corpseName === 'newt';
                await setMessage(`${game._startup_role === 'Monk' ? 'You feel guilty.  ' : ''}This ${corpseName} corpse tastes terrible!`, true);
                game._process_time_with_more = 1;
                game._command_mode = null;
                game.context.move = 9;
                return;
            }
            if (isGlobFood(item)) {
                await eatGlobFood(item);
                return;
            }
            if (shouldUseGenericRottenEggPath(item)) {
                await eatRottenEgg(item);
                return;
            }
            if (isPyroliskEgg(item)) {
                await eatPyroliskEgg(item);
                return;
            }
            if (isStaleEggItem(item)) {
                await eatStaleEgg(item);
                return;
            }
            if (!ordinaryFoodVictualSpec(item) && shouldUseGenericRottenFoodPath(item)) {
                await eatRottenNonCorpseFood(item);
                return;
            }
            const delayedFoodVictual = ordinaryFoodVictualSpec(item);
            if (delayedFoodVictual) {
                const result = startCarriedDelayedFoodVictual(item, delayedFoodVictual);
                await setMessage(result.message, result.more);
                if (result.processTimeWithMore) game._process_time_with_more = 1;
                game._command_mode = result.commandMode || null;
                game.context.move = result.move ?? 1;
                return;
            }
            const touched = touchEatenFood(item);
            recordFoodConduct(touched);
            if (touched.kind === 'clove of garlic') scareNearbyOlfactoryMonstersWithGarlic();
            addHeroNutrition(touched.oeaten > 0 ? touched.oeaten : foodObjectNutrition(touched));
            removeInventoryItem(touched);
            game._pet_food_scan_inventory = game.inventory || [];
            const petrificationMessage = startPetrifyingEggStoning(touched);
            const eatMessage = itemKindText(touched).replace(/^partly eaten /, '') === 'apple'
                ? 'Delicious!  Must be a Macintosh!'
                : `This ${name} is delicious!`;
            await setMessage(appendPetrificationMessage(eatMessage, petrificationMessage), fortuneCookie);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        await setMessage('You cannot eat that!');
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'eatFloorObject') {
        if (ch === 'y') {
            const food = game._eat_floor_object;
            const floorCorpse = food?.otyp === 'corpse' || food?.otyp === CORPSE;
            const oldCorpse = floorCorpse && food?.oldCorpse;
            const name = pickupObjectName({ ...(food || {}), quan: 1 });
            game._eat_floor_object = null;
            game._eating_floor_object_direct_useup = 0;
            if (isTinObject(food)) {
                await startTinOpening(food, true);
                return;
            }
            if (heroCanEatNonFoodMetal(food)) {
                await eatHeroNonFoodMetal(food, { floorObject: true });
                return;
            }
            if ((food?.otyp === 'corpse' || food?.otyp === CORPSE) && food?.corpsenm?.name === 'lichen') {
                rn2(10);
                game._eating_turns_remaining = 4;
                game._eating_finish_message = 'You finish eating the lichen corpse.';
                game._eating_floor_object = food;
                game._eating_nutrition = CORPSE_NUTRITION.get('lichen') || 0;
                await setMessage('This lichen corpse tastes okay.');
                game._command_mode = null;
                game.context.move = game._eating_turns_remaining;
                return;
            }
            if ((food?.otyp === 'corpse' || food?.otyp === CORPSE) && food?.oldCorpse) {
                const corpseName = food.corpsenm?.name || 'monster';
                rn2(20);
                rn2(5);
                const damage = rnd(8);
                if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - damage);
                game._eating_turns_remaining = 14;
                game._eating_finish_message = `You finish eating the ${corpseName} corpse.`;
                game._eating_floor_object = food;
                game._eating_newt_buzz = corpseName === 'newt';
                await setMessage('You feel sick.');
                game._command_mode = null;
                game.context.move = 14;
                return;
            }
            if (food?.otyp === 'corpse' || food?.otyp === CORPSE) {
                const corpseName = food.corpsenm?.name || 'monster';
                rn2(20);
                if (POISONOUS_CORPSES.has(corpseName) && rn2(5)) {
                    const strengthDamage = rnd(4);
                    const hpDamage = rnd(15);
                    if (game.u?.acurr?.a)
                        game.u.acurr.a[0] = Math.max(3, (game.u.acurr.a[0] ?? 10) - strengthDamage);
                    if (game.u) game.u.uhp = Math.max(1, (game.u.uhp || 1) - hpDamage);
                    game.level.objects = (game.level.objects || []).filter(obj => obj !== food);
                    newsym(food.ox, food.oy);
                    game._queued_message_after_more = `You finish eating the ${corpseName} corpse.`;
                    game._queued_message_process_time_after_more = 1;
                    game._queued_message_pre_time_rn2 = 100;
                    game._process_time_with_more = 1;
                    game._command_mode = null;
                    game.context.move = 3 + ((CORPSE_WEIGHTS.get(corpseName) ?? food.owt ?? 1) >> 6);
                    await setMessage('Ecch - that must have been poisonous!', true);
                    return;
                }
                if (!rn2(7)) {
                    let rottenSleepDuration = 0;
                    if (!rn2(4)) {
                        const confusionDuration = d(2, 4);
                        if (game.u) game.u._confusionTimeout = (game.u._confusionTimeout || 0) + confusionDuration;
                    } else if (!game.u?.blind && !rn2(4)) {
                        d(2, 10);
                        if (game.u) game.u.blind = true;
	                    } else if (!rn2(3)) {
	                        const duration = rnd(10);
	                        game._helpless_time = Math.max(game._helpless_time || 0, duration);
	                        game._sleeping_time = Math.max(game._sleeping_time || 0, duration + 1);
	                        game._wake_message = 'You are conscious again.';
	                        game._hear_again_after_wake = 1;
	                        rottenSleepDuration = duration;
	                    }
	                    if (rottenSleepDuration) {
	                        await setMessage('Blecch!  Rotten food!');
	                        game._command_mode = null;
	                        game.context.move = rottenSleepDuration;
	                        return;
	                    }
	                    let newtBuzzMessage = '';
	                    if (corpseName === 'newt' && (rn2(3) || 3 * (game.u?.uen || 0) <= 2 * (game.u?.uenmax || 0))) {
	                        const oldEnergy = game.u?.uen || 0;
	                        if (game.u) game.u.uen = oldEnergy + rnd(3);
	                        if ((game.u?.uen || 0) > (game.u?.uenmax || 0)) {
	                            if (!rn2(3) && game.u) game.u.uenmax = (game.u.uenmax || 0) + 1;
	                            if (game.u) game.u.uen = game.u.uenmax || 0;
	                        }
	                        if ((game.u?.uen || 0) !== oldEnergy) newtBuzzMessage = '  You feel a mild buzz.';
	                    }
	                    if (corpseName === 'newt') {
	                        rn2(100);
	                        game.level.objects = (game.level.objects || []).filter(obj => obj !== food);
	                        newsym(food.ox, food.oy);
	                        await setMessage(`Blecch!  Rotten food!${newtBuzzMessage}`);
	                        game._command_mode = null;
	                        game.context.move = 1;
	                        return;
	                    }
	                    game._eating_turns_remaining = 3;
	                    game._eating_finish_message = `You finish eating the ${corpseName} corpse.`;
	                    game._eating_floor_object = food;
	                    game._eating_newt_buzz = false;
	                    await setMessage(`Blecch!  Rotten food!${newtBuzzMessage}`);
                    game._pending_rotten_food_eating_message = 1;
                    game._command_mode = null;
                    game.context.move = game._eating_turns_remaining;
                    return;
                }
                let corpseMessage = `This ${corpseName} corpse tastes terrible!`;
                if (rn2(10)) {
                    const palatable = ['okay', 'stringy', 'gamey', 'fatty', 'tough'][rn2(5)];
                    corpseMessage = palatable === 'okay'
                        ? `This ${corpseName} corpse tastes okay.`
                        : `This ${corpseName} corpse is ${palatable}.`;
                }
                const eatTime = 3 + ((CORPSE_WEIGHTS.get(corpseName) ?? food.owt ?? 1) >> 6);
                const nutrition = CORPSE_NUTRITION.get(corpseName) || 0;
                const biteCount = eatTime >= 10 ? eatTime : Math.max(1, eatTime - 1);
                const biteNutrition = Math.trunc(nutrition / biteCount);
                // This port applies multi-turn corpse nutrition at finish time,
                // so preserve the portion C would have delivered across bites.
                game._eating_turns_remaining = eatTime + 1;
                game._eating_finish_message = `You finish eating the ${corpseName} corpse.`;
                game._eating_floor_object = food;
                game._eating_nutrition = biteNutrition * biteCount
                    + (eatTime >= 10 ? Math.min(nutrition % biteCount, 10) : 0);
                game._eating_newt_buzz = corpseName === 'newt';
                await setMessage(corpseMessage);
                game._command_mode = null;
                game.context.move = game._eating_turns_remaining;
                return;
            }
            if (isGlobFood(food)) {
                await eatGlobFood(food, true);
                return;
            }
            if (shouldUseGenericRottenEggPath(food)) {
                await eatRottenEgg(food, true);
                return;
            }
            if (isPyroliskEgg(food)) {
                await eatPyroliskEgg(food, true);
                return;
            }
            if (isStaleEggItem(food)) {
                await eatStaleEgg(food, true);
                return;
            }
            if (!ordinaryFoodVictualSpec(food) && shouldUseGenericRottenFoodPath(food)) {
                await eatRottenNonCorpseFood(food, true);
                return;
            }
            const delayedFoodVictual = ordinaryFoodVictualSpec(food);
            if (delayedFoodVictual) {
                const result = startDelayedFoodVictual(food, delayedFoodVictual, { floorObject: true });
                await setMessage(result.message, result.more);
                if (result.processTimeWithMore) game._process_time_with_more = 1;
                game._command_mode = result.commandMode || null;
                game.context.move = result.move ?? 1;
                return;
            }
            const touched = touchEatenFood(food, true);
            addHeroNutrition(touched.oeaten > 0 ? touched.oeaten : foodObjectNutrition(touched));
            recordFoodConduct(touched);
            if (touched?.kind === 'clove of garlic') scareNearbyOlfactoryMonstersWithGarlic();
            consumeOneFloorObject(touched);
            const petrificationMessage = startPetrifyingEggStoning(touched);
            const fortuneCookie = isFortuneCookieFood(touched);
            if (fortuneCookie) queueFortuneCookieRumor(touched);
            const kind = itemKindText(touched).replace(/^partly eaten /, '');
            const eatMessage = kind === 'apple'
                ? 'Delicious!  Must be a Macintosh!'
                : `This ${name} is delicious!`;
            await setMessage(appendPetrificationMessage(eatMessage, petrificationMessage), fortuneCookie);
            game._command_mode = null;
            game.context.move = 1;
            return;
        }
        game._eat_floor_object = null;
        if (ch === 'q' || ch === '\x1b') {
            await setMessage('Never mind.');
            game._command_mode = null;
            return;
        }
        if (polyselfNoHands() && ch !== ' ' && ch !== '\r' && ch !== '\n') {
            game._keep_pending_message = 1;
            return;
        }
        const letters = inventoryLetters(item => item.cls === 'food' || item.otyp === FOOD_CLASS || item.otyp === 'corpse' || item.otyp === CORPSE);
        if (letters) {
            await setMessage(eatingPrompt());
            game._command_mode = 'eatObject';
            return;
        }
        await setMessage("You don't have anything else to eat.");
        game._command_mode = null;
        return;
    }

    if (game._command_mode === 'travelIntroMore') {
        if (ch === ' ' || ch === '\x1b') {
            game._travel_tip_seen = 1;
            setOverlay(TRAVEL_TIP_LINES, 9, false, 9);
            game._command_mode = 'travelTip';
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'travelTip') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage("(For instructions type a '?')  Move cursor to the desired destination:");
            game._travel_prompt_current = 0;
            game._command_mode = 'travelCursor';
        }
        return;
    }

    if (game._command_mode === 'travelCursorHelp') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage('Move cursor to the desired destination:');
            game._travel_prompt_current = 0;
            game._command_mode = 'travelCursor';
        }
        return;
    }

    if (game._command_mode === 'travelCursor') {
        if (ch === '\x1b') {
            game._pending_message = '';
            game._message_more = 0;
            game._command_mode = null;
            game._travel_dot_confirms = 0;
            game._travel_target = null;
            game._travel_target_description = '';
            game._travel_no_path_target = null;
            game._travel_prompt_current = 0;
            return;
        }
        if (ch === ' ') {
            const x = game._farlook_x || game.u?.ux || 1;
            const y = game._farlook_y || game.u?.uy || 0;
            game._cursor_override = [x - 1, y + 1];
            if (x === (game.u?.ux || 0) && y === (game.u?.uy || 0)) {
                const race = String(game.urace?.noun || game._startup_race || 'human').toLowerCase();
                const role = String(game.urole?.name?.m || game._startup_role || 'hero').toLowerCase();
                await setMessage(`${race} ${role} called ${role}`);
            } else {
                game._keep_pending_message = 1;
            }
            return;
        }
        if (ch === '?') {
            setOverlay(TRAVEL_CURSOR_HELP_LINES, 17, false, 9);
            game._command_mode = 'travelCursorHelp';
            return;
        }
        if (ch === '>') {
            const previousX = game._farlook_x || game.u?.ux || 0;
            const previousY = game._farlook_y || game.u?.uy || 0;
            let down = null;
            for (let stair = game.stairs; stair; stair = stair.next) {
                if (!stair.up) {
                    down = stair;
                    break;
                }
            }
            if (down) {
                const loc = game.level?.at(down.sx, down.sy);
                const blocked = loc?.seenv && questDownBlocked();
                const description = loc?.seenv ? (blocked ? 'blocked staircase down' : 'staircase down') : "Can't find dungeon feature '>'.";
                await setMessage(description);
                game._travel_prompt_current = 0;
                game._travel_target_description = loc?.seenv ? description : '';
                if (loc?.seenv) {
                    game._farlook_x = down.sx;
                    game._farlook_y = down.sy;
                    game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
                    const keys = blocked ? [] : travelPathKeys(down.sx, down.sy);
                    if (!keys[0]) {
                        const validKeys = blocked ? [] : travelPathKeys(down.sx, down.sy, false, true);
                        if (validKeys[0]) {
                            game._travel_target = { x: down.sx, y: down.sy };
                            return;
                        }
                        game._travel_target = null;
                        game._travel_target_description = '';
                        game._travel_no_path_target = { x: down.sx, y: down.sy, message: `${description} (no travel path)` };
                        await setMessage(`${description} (no travel path)`);
                        return;
                    }
                    game._travel_target = { x: down.sx, y: down.sy };
                } else {
                    game._farlook_x = previousX;
                    game._farlook_y = previousY;
                    game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
                }
            }
            return;
        }
        if (ch === '<') {
            const previousX = game._farlook_x || game.u?.ux || 0;
            const previousY = game._farlook_y || game.u?.uy || 0;
            let up = null;
            for (let stair = game.stairs; stair; stair = stair.next) {
                if (stair.up) {
                    up = stair;
                    break;
                }
            }
            if (up) {
                const loc = game.level?.at(up.sx, up.sy);
                const description = loc?.seenv ? 'staircase up' : "Can't find dungeon feature '<'.";
                await setMessage(description);
                game._travel_prompt_current = 0;
                game._travel_target_description = loc?.seenv ? description : '';
                if (loc?.seenv) {
                    game._farlook_x = up.sx;
                    game._farlook_y = up.sy;
                    game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
                    game._travel_target = { x: up.sx, y: up.sy };
                } else {
                    game._farlook_x = previousX;
                    game._farlook_y = previousY;
                    game._cursor_override = [game._farlook_x - 1, game._farlook_y + 1];
                }
            }
            return;
        }
        const pickTravelTarget = ch === '.' || ch === ',';
        if (pickTravelTarget && !game._travel_target
            && (game._farlook_x || game.u?.ux || 0) === (game.u?.ux || 0)
            && (game._farlook_y || game.u?.uy || 0) === (game.u?.uy || 0)) {
            game._command_mode = null;
            game._travel_dot_confirms = 0;
            game._travel_prompt_current = 0;
            await setMessage('You are already here.');
            return;
        }
        if (pickTravelTarget && !game._travel_dot_confirms && !game._travel_target) {
            return;
        }
        if (pickTravelTarget && game._travel_dot_confirms && !game._travel_target
            && game._travel_prompt_current && !game._travel_previous_target) {
            game._command_mode = null;
            game._travel_dot_confirms = 0;
            game._travel_prompt_current = 0;
            await setMessage('You are already here.');
            return;
        }
        const currentNoPathTarget = game._travel_no_path_target
            && (game._farlook_x || game.u?.ux || 0) === game._travel_no_path_target.x
            && (game._farlook_y || game.u?.uy || 0) === game._travel_no_path_target.y;
        if (pickTravelTarget && !game._travel_target && (!game._travel_previous_target || currentNoPathTarget)) {
            const targetX = game._farlook_x || game.u?.ux || 0;
            const targetY = game._farlook_y || game.u?.uy || 0;
            const keepMessage = game._travel_no_path_target?.message || game._pending_message || 'unexplored area (no travel path)';
            const blockedTargetTravel = keepMessage.startsWith('stone ');
            const keys = travelPathKeys(targetX, targetY, true, !blockedTargetTravel, blockedTargetTravel);
            game._command_mode = null;
            game._travel_target = null;
            game._travel_dot_confirms = 0;
            game._travel_prompt_current = 0;
            game._travel_no_path_target = null;
            if (!keys[0]) {
                game._travel_previous_target = { x: targetX, y: targetY };
                await setMessage(keepMessage);
                return;
            }
            game._travel_previous_target = { x: targetX, y: targetY };
            game._travel_dynamic_target = blockedTargetTravel
                ? { x: targetX, y: targetY, allowBlockedTarget: true }
                : { x: targetX, y: targetY, allowGuess: true };
            if (blockedTargetTravel) {
                game._travel_ignore_adjacent_stop = 1;
            }
            game._travel_keep_message = keepMessage;
            game._pending_message = '';
            game._keep_pending_message = 0;
            game._travel_keys = [];
            await rhack(keys[0]);
            return;
        }
        if (pickTravelTarget && !game._travel_target) {
            const target = game._travel_previous_target;
            if (game._travel_prompt_current)
                game._travel_keep_message = game._travel_prompt_message || '';
            game._pending_message = '';
            game._keep_pending_message = 0;
            game._command_mode = null;
            game._travel_dot_confirms = 0;
            game._travel_prompt_current = 0;
            if (!target) {
                game.context.move = 1;
                return;
            }
            const keys = travelPathKeys(target.x, target.y, true);
            if (!keys[0]) {
                await setMessage('unexplored area (no travel path)');
                return;
            }
            game._travel_keys = keys.slice(1);
            await rhack(keys[0]);
            if (game._travel_previous_target
                && (game.u?.ux || 0) === game._travel_previous_target.x
                && (game.u?.uy || 0) === game._travel_previous_target.y)
                game._travel_previous_target = null;
            return;
        }
        if (pickTravelTarget && game._travel_target) {
            const target = game._travel_target;
            game._travel_previous_target = { ...target };
            game._command_mode = null;
            game._travel_target = null;
            game._travel_dot_confirms = 0;
            game._travel_prompt_current = 0;
            const keys = travelPathKeys(target.x, target.y);
            if (!keys[0]) {
                game.context.move = 1;
                game._keep_pending_message = 1;
                return;
            }
            const targetDescription = game._travel_target_description;
            if (targetDescription === 'staircase down' || targetDescription === 'staircase up') {
                game._travel_finish_message = targetDescription;
            } else if (targetDescription) {
                game._travel_keep_message = targetDescription;
            }
            game._travel_target_description = '';
            game._travel_keys = keys.slice(1);
            await rhack(keys[0]);
            if (game._travel_previous_target
                && (game.u?.ux || 0) === game._travel_previous_target.x
                && (game.u?.uy || 0) === game._travel_previous_target.y)
                game._travel_previous_target = null;
            return;
        }
        const moveCh = (ch === '\r' || ch === '\n') ? 'J' : ch;
        const dir = movementDirection(moveCh);
        if (dir) {
            const steps = 'HJKLYUBN'.includes(moveCh) ? 8 : 1;
            const cursor = truncateCursorToMap(
                game._farlook_x || game.u?.ux || 0,
                game._farlook_y || game.u?.uy || 0,
                dir.dx * steps,
                dir.dy * steps,
            );
            game._farlook_x = cursor.x;
            game._farlook_y = cursor.y;
            const targetX = game._farlook_x;
            const targetY = game._farlook_y;
            const loc = game.level?.at(targetX, targetY);
            const terrain = loc?.typ === STONE
                && (loc.seenv || loc.remembered_glyph || (loc.disp_ch || ' ') !== ' '
                    || game._travel_previous_target) ? 'stone'
                : !loc || (!loc.seenv && !loc.remembered_glyph && (loc.disp_ch || ' ') === ' ') ? 'unexplored area'
                : loc?.typ === CLOUD ? 'fog/vapor cloud'
                : loc?.typ === ROOM && !(game.viz_array?.[targetY]?.[targetX] & IN_SIGHT) ? 'dark part of a room'
                : (loc.disp_ch || ' ') === ' ' ? 'dark part of a room'
                : loc.typ === DOOR ? doorDescription(loc)
                : loc?.typ === CORR ? 'corridor'
                    : loc?.typ === ROOM || loc?.typ === STAIRS ? 'floor of a room'
                        : loc?.typ === MOAT ? 'moat'
                            : loc?.typ === TREE ? 'tree'
                            : loc?.typ && loc.typ < DOOR ? 'wall' : 'unexplored area';
            const keys = travelPathKeys(targetX, targetY);
            if (!keys[0]) {
                game._travel_target = null;
                game._travel_target_description = '';
                game._travel_no_path_target = { x: targetX, y: targetY, message: `${terrain} (no travel path)` };
                game._travel_prompt_current = 0;
                await setMessage(`${terrain} (no travel path)`);
                game._cursor_override = [targetX - 1, targetY + 1];
                return;
            }
            game._travel_target = { x: targetX, y: targetY };
            game._travel_target_description = terrain;
            game._travel_no_path_target = null;
            game._travel_prompt_current = 0;
            await setMessage(terrain);
            game._cursor_override = [targetX - 1, targetY + 1];
            return;
        }
	        if (typeof ch === 'string' && ch.length === 1) {
	            game._travel_prompt_current = 0;
	            game._travel_target_description = '';
	            if (ch === '_') {
	                await setMessage("Can't find dungeon feature '_'.");
	                return;
	            }
	            await setMessage(`Unknown direction: '${visibleKey(ch)}' (use 'h', 'j', 'k', 'l' or '.').`);
	            return;
	        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'fireDirection') {
        const dir = movementDirection(ch);
        if (!dir) {
            game._fire_item_letter = null;
            game._fire_launcher_letter = null;
            setOverlay(CMDASSIST_DIRECTION_LINES, 24, true);
            game._command_mode = 'cmdassistMore';
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === game._fire_item_letter);
        if (!item) {
            game._command_mode = null;
            game._fire_item_letter = null;
            game._fire_launcher_letter = null;
            return;
        }
        let ox = (game.u?.ux || 0) + dir.dx * 3;
        let oy = (game.u?.uy || 0) + dir.dy * 3;
        const loc = game.level?.at(ox, oy);
        if (!loc || IS_OBSTRUCTED(loc.typ)) {
            ox = (game.u?.ux || 0) + dir.dx;
            oy = (game.u?.uy || 0) + dir.dy;
        }
        const oldQuan = item.quan || 1;
        const volleyLimit = game._fire_launcher_letter && oldQuan > 1 ? 2 : 1;
        const shotCount = volleyLimit > 1 ? Math.min(oldQuan, rnd(volleyLimit)) : 1;
        let projectileId = null;
        let projectileBreakRoll = null;
        const hardLanding = !projectileLandingIsSoft(ox, oy);
        for (let shot = 0; shot < shotCount; shot++) {
            if (oldQuan - shot > 1) projectileId = next_ident();
            if (hardLanding) {
                const roll = rn2(100);
                if (projectileBreakRoll == null) projectileBreakRoll = roll;
            }
        }
        const projectileObject = {
            ...item,
            letter: undefined,
            line: undefined,
            wielded: false,
            quivered: false,
            id: projectileId ?? item.id,
            quan: shotCount,
            ox,
            oy,
            glyph: item.glyph || (item.cls === 'gem' ? '*' : ')'),
            color: item.color || (item.cls === 'gem' ? CLR_GRAY : CLR_CYAN),
        };
        if (oldQuan > shotCount) splitCarriedObjectShopBill(item, projectileObject, shotCount);
        const landing = landProjectileObjectWithShopHandling(projectileObject, ox, oy, { breakRoll: projectileBreakRoll });
        const landingMessage = landing.messages.join('  ');
        if (game._fire_launcher_letter) {
            game._stale_projectile_marks ??= [];
            game._stale_projectile_marks.push({
                x: (game.u?.ux || 0) + dir.dx * 2,
                y: (game.u?.uy || 0) + dir.dy * 2,
                ch: '%',
                color: CLR_BROWN,
            });
        }
        if (oldQuan > shotCount) {
            item.quan = oldQuan - shotCount;
            if (item.line) item.line = item.line.replace(/ - \d+ /, ` - ${item.quan} `);
            if (item.unpaid) syncUnpaidBillLine(item);
        } else {
            game.inventory = (game.inventory || []).filter(invItem => invItem !== item);
        }
        updateWornDisplacement();
        game._pet_food_scan_inventory = game.inventory;
        const name = inventoryItemName(item)
            .replace(/^\d+ /, '')
            .replace(/^(?:uncursed|blessed|cursed) /, '');
        const fireMessage = shotCount > 1
            ? `${game._fire_launcher_letter ? 'You shoot' : 'You throw'} ${shotCount} ${name}.`
            : `${game._fire_launcher_letter ? 'You shoot' : 'You throw'} ${name}.`;
        if (landingMessage) game._queued_message_after_more = landingMessage;
        await setMessage(fireMessage, !!landingMessage);
        game._command_mode = null;
        game._fire_item_letter = null;
        game._fire_launcher_letter = null;
        game.context.move = 1;
        return;
    }

    if (ch === 'r' && game._command_mode !== 'throwObject' && game._command_mode !== 'throwInventory') {
        if ((game.u?._statusSuffix || '').includes('Overloaded')) {
            await setMessage("You can't do that while carrying so much stuff.");
            return;
        }
        const letters = inventoryLetters(item => item.cls === 'scroll' || item.cls === 'spellbook' || item.otyp === SCROLL_CLASS) || 'gh';
        await setMessage(`What do you want to read? [${letters} or ?*]`);
        game._command_mode = 'readObject';
        return;
    }

    if (ch === 'c' && game._command_mode !== 'throwObject' && game._command_mode !== 'throwInventory') {
        if (polyselfNoHands()) {
            await setMessage("You can't close anything -- you have no hands!");
            return;
        }
        await setMessage('In what direction?');
        game._command_mode = 'closeDirection';
        return;
    }

    if (game._command_mode === 'throwObject') {
        if (ch === '\x1b' || ch === ' ') {
            await setMessage('Never mind.');
            game._command_mode = null;
            clearThrowCountState();
            return;
        }
        const throwCountValue = throwCountTextValue(game._throw_count_text);
        if ((ch === '\b' || ch === '\x7f') && throwCountValue > 0) {
            const newCount = Math.trunc(throwCountValue / 10);
            game._throw_count_text = newCount > 0 ? String(newCount) : '';
            game._throw_count_backspaced = true;
            await setMessage(`Count: ${game._throw_count_text}`);
            return;
        }
        if (/^\d$/.test(ch)) {
            const backspaced = !!game._throw_count_backspaced;
            game._throw_count_text = `${game._throw_count_text || ''}${ch}`;
            const typedCount = throwCountTextValue(game._throw_count_text);
            if (typedCount > 9 || backspaced) await setMessage(`Count: ${typedCount}`);
            game._throw_count_backspaced = false;
            return;
        }
        if (ch === '*') {
            game._throw_inventory_page = 0;
            showInventoryOverlay();
            game._throw_inventory_filter = null;
            game._command_mode = 'throwInventory';
            game._throw_count = null;
            clearThrowMenuCountState();
            return;
        }
        if (ch === '?') {
            game._throw_inventory_page = 0;
            const suggestLetters = inventoryLetters(isThrowSuggestItem);
            game._throw_inventory_filter = suggestLetters ? 'suggest' : 'downplay';
            showInventoryOverlay(0, false, throwInventoryFilterMatch());
            game._command_mode = 'throwInventory';
            game._throw_count = null;
            clearThrowMenuCountState();
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        const throwCount = throwSelectionCount();
        const countGiven = throwCount > 0;
        if (!item) {
            if (countGiven) {
                game._command_mode = null;
                clearThrowCountState();
                game._pending_message = '';
                game._message_more = 0;
                return;
            }
            await setMessage("You don't have that object.", true);
            game._command_mode = 'throwInvalidMore';
            clearThrowCountState();
            return;
        }
        if (countGiven && throwCount > 1) {
            const message = throwCountRejectionMessage(item, throwCount);
            if (message) {
                clearThrowCountState();
                await setMessage(message);
                return;
            }
        }
        game._throw_item_letter = ch;
        game._throw_count = countGiven && shopBillableGold(item) ? throwCount : null;
        game._throw_count_text = '';
        game._throw_count_backspaced = false;
        clearThrowMenuCountState();
        await setMessage('In what direction?');
        game._command_mode = 'throwDirection';
        return;
    }

    if (game._command_mode === 'throwInvalidMore') {
        if (ch === ' ' || ch === '\x1b' || ch === '\r' || ch === '\n') {
            await setMessage(game._throw_prompt || 'What do you want to throw? [*]');
            game._command_mode = 'throwObject';
            clearThrowCountState();
            return;
        }
        game._keep_pending_message = 1;
        return;
    }

    if (game._command_mode === 'throwInventory') {
        if (ch === '*' && game._throw_inventory_filter) return;
        const menuCountValue = throwCountTextValue(game._throw_menu_count_text);
        if ((ch === '\b' || ch === '\x7f') && menuCountValue > 0) {
            const newCount = Math.trunc(menuCountValue / 10);
            game._throw_menu_count_text = newCount > 0 ? String(newCount) : '';
            game._throw_menu_count_backspaced = true;
            echoThrowMenuCount(game._throw_menu_count_text);
            return;
        }
        if (/^\d$/.test(ch)) {
            const backspaced = !!game._throw_menu_count_backspaced;
            game._throw_menu_count_text = `${game._throw_menu_count_text || ''}${ch}`;
            const typedCount = throwCountTextValue(game._throw_menu_count_text);
            echoThrowMenuCount(String(typedCount));
            game._throw_menu_count_backspaced = false;
            return;
        }
        if (ch === ' ') {
            const page = (game._throw_inventory_page || 0) + 1;
            if (page < (game._inventory_overlay_total_pages || 1)) {
                game._throw_inventory_page = page;
                showInventoryOverlay(page, false, throwInventoryFilterMatch());
                return;
            }
            game._throw_inventory_page = 0;
            game._throw_inventory_filter = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            const letters = inventoryLetters(isThrowSuggestItem);
            const promptLetters = letters.includes('$') ? `$${letters.replace('$', '')}` : letters;
            game._throw_prompt = letters
                ? `What do you want to throw? [${promptLetters} or ?*]`
                : 'What do you want to throw? [*]';
            clearThrowCountState();
            await setMessage(game._throw_prompt);
            game._command_mode = 'throwObject';
            return;
        }
        if (ch === '\x1b' && game._throw_menu_count_text) {
            clearThrowMenuCountState();
            game._pending_message = '';
            game._message_more = 0;
            return;
        }
        if (ch === '\x1b') {
            game._throw_inventory_page = 0;
            game._throw_inventory_filter = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage('Never mind.');
            game._command_mode = null;
            clearThrowCountState();
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === ch);
        if (!item) {
            game._throw_inventory_page = 0;
            game._throw_inventory_filter = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            await setMessage("You don't have that object.");
            game._command_mode = null;
            clearThrowCountState();
            return;
        }
        const throwCount = throwSelectionCount();
        const countGiven = throwCount > 0;
        const rejection = countGiven ? throwCountRejectionMessage(item, throwCount) : '';
        if (rejection) {
            game._throw_inventory_page = 0;
            game._throw_inventory_filter = null;
            game._overlay_lines = null;
            game._overlay_hide_status = 0;
            clearThrowCountState();
            await setMessage(rejection);
            game._command_mode = 'throwObject';
            return;
        }
        game._throw_inventory_page = 0;
        game._throw_inventory_filter = null;
        game._overlay_lines = null;
        game._overlay_hide_status = 0;
        game._throw_item_letter = ch;
        game._throw_count = countGiven && shopBillableGold(item) ? throwCount : null;
        game._throw_count_text = '';
        game._throw_count_backspaced = false;
        clearThrowMenuCountState();
        await setMessage('In what direction?');
        game._command_mode = 'throwDirection';
        return;
    }

    if (game._command_mode === 'throwDirection') {
        if (ch === '\x1b' || ch === ' ') {
            await setMessage('Never mind.');
            game._command_mode = null;
            game._throw_item_letter = null;
            clearThrowCountState();
            return;
        }
        if (ch === '.') {
            await setMessage('You cannot throw an object at yourself.');
            game._command_mode = null;
            game._throw_item_letter = null;
            clearThrowCountState();
            return;
        }
        const item = (game.inventory || []).find(invItem => invItem.letter === game._throw_item_letter);
        if (ch === '<' && item && supportsHeroThrownPotionUpwardHit(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.glyph || '!',
                color: item.color || CLR_MAGENTA,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const removeBeforeImpact = isLitOilPotionHit(thrownObject);
            if (removeBeforeImpact) removeInventoryItem(item, 1);
            const messages = heroThrownPotionUpwardMessages(thrownObject, { allowLifeSaving: true });
            if (!removeBeforeImpact) removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            const keepPotionCallPrompt = game._command_mode === 'callPotionAfterMore';
            await setMessage(messages.join('  '), keepPotionCallPrompt || !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            if (!keepPotionCallPrompt) game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isCreamPieObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_ORANGE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownCreamPieUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        if (ch === '<' && isVenomObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.glyph || '.',
                color: item.color || CLR_BROWN,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownVenomUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        if (ch === '<' && isMelonObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_BRIGHT_GREEN,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownMelonUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        if (ch === '<' && supportsHeroThrownEggUpwardHit(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownEggUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        if (ch === '<' && isTouchPetrifyingEgg(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            removeInventoryItem(item, 1);
            const messages = heroThrownTouchPetrifyingEggUpwardMessages(thrownObject);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isPetrifyingCorpseObject(item)) {
            const precheck = heroThrowPetrifyingCorpseBareHandPrecheck(item);
            if (!precheck.ok) {
                const messages = precheck.messages;
                await setMessage(messages.join('  '), !!messages.more);
                game._throw_item_letter = null;
                game._resume_time_after_more = 0;
                game.context.move = 0;
                if (messages.lifeSaving) {
                    game._command_mode = 'lifeSavingMore';
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    return;
                }
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }

            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_BROWN,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            removeInventoryItem(item, 1);
            const messages = heroThrownTouchPetrifyingCorpseUpwardMessages(thrownObject, precheck.messages);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isCorpseItem(item) && !isRiderCorpseObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_BROWN,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            removeInventoryItem(item, 1);
            const messages = heroThrownOrdinaryCorpseUpwardMessages(thrownObject);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isPyroliskEgg(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: '%',
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            removeInventoryItem(item, 1);
            const messages = heroThrownPyroliskEggUpwardMessages(thrownObject);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (applyLifeSavingOrFatalCommandMode(messages)) return;
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isHeroThrownCrackableArmorObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.glyph || '[',
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownCrackableArmorUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && supportsHeroThrownFragileObjectUpwardHit(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.glyph || '(',
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = await heroThrownFragileObjectUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        if (ch === '<' && isHeroThrownGenericDamagingUpwardObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.glyph || (item.cls === 'tool' ? '(' : ')'),
                color: item.color || CLR_WHITE,
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownGenericObjectUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '), !!messages.more);
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game.context.move = 0;
            if (messages.lifeSaving) {
                game._command_mode = 'lifeSavingMore';
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            if (messages.fatal) {
                game._command_mode = 'deathDieMore';
                game._pending_time_passed = 0;
                game._process_command_time_now = 0;
                game._run_steps_remaining = 0;
                prepareDeathBones();
                return;
            }
            game._command_mode = null;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            return;
        }
        if (ch === '<' && isHeroThrownHarmlessUpwardObject(item)) {
            let thrownId = null;
            if ((item.quan || 1) > 1) thrownId = next_ident();
            const thrownObject = {
                ...item,
                letter: undefined,
                line: undefined,
                wielded: false,
                worn: false,
                quivered: false,
                ox: game.u?.ux || 0,
                oy: game.u?.uy || 0,
                id: thrownId ?? item.id,
                quan: 1,
                glyph: item.cls === 'food' ? '%' : item.glyph || (item.cls === 'scroll' ? '?' : item.cls === 'gem' ? '*' : ')'),
                color: item.cls === 'food' ? CLR_ORANGE : item.color || (item.cls === 'gem' ? CLR_GRAY : CLR_WHITE),
            };
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            const messages = heroThrownHarmlessObjectUpwardMessages(thrownObject);
            removeInventoryItem(item, 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            await setMessage(messages.join('  '));
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        const dir = movementDirection(ch);
        if (!dir) {
            game._throw_item_letter = null;
            clearThrowCountState();
            setOverlay(CMDASSIST_DIRECTION_LINES, 24, true);
            game._command_mode = 'cmdassistMore';
            return;
        }
        if (!item) {
            game._command_mode = null;
            game._throw_item_letter = null;
            clearThrowCountState();
            return;
        }
        if (isLoadstoneObject(item) && item.cursed) {
            await setMessage(cursedLoadstoneLetGoMessage(item, 'throw'));
            game._command_mode = null;
            game._throw_item_letter = null;
            clearThrowCountState();
            game.context.move = 0;
            return;
        }
        const name = inventoryItemName(item);
        const lowerName = name.toLowerCase();
	        const ux = game.u?.ux || 0;
	        const uy = game.u?.uy || 0;
		        let ox = ux;
		        let oy = uy;
		        let targetMon = null;
        let ironBarsImpact = null;
        for (let step = 0; step < 8; step++) {
            const nx = ox + dir.dx;
            const ny = oy + dir.dy;
            const loc = game.level?.at(nx, ny);
            if (loc?.typ === IRONBARS) {
                ironBarsImpact = { x: ox, y: oy, barsX: nx, barsY: ny, pointBlank: step === 0 };
                break;
            }
	            if (!loc || IS_OBSTRUCTED(loc.typ)) break;
	            ox = nx;
	            oy = ny;
	            targetMon = (game.level?.monsters || []).find(mon => mon.mx === ox && mon.my === oy) || null;
	            if (targetMon) break;
	        }
        if (shopBillableGold(item)) {
            const amount = Math.max(1, Math.trunc(Number(game._throw_count || game._goldCount || item.quan || 1)));
            const thrownGold = {
                ...item,
                letter: undefined,
                line: undefined,
                ox,
                oy,
                quan: amount,
                glyph: '$',
                color: CLR_YELLOW,
            };
            const landing = landProjectileObjectWithShopHandling(thrownGold, ox, oy, { breakRoll: 0 });
            const landingMessage = landing.messages.join('  ');
            newsym(ox, oy);
            removeGoldFromHero(amount);
            if (landingMessage) await setMessage(landingMessage);
            else {
                game._pending_message = '';
                game._message_more = 0;
            }
            game._command_mode = null;
            game._throw_item_letter = null;
            clearThrowCountState();
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        let thrownId = null;
        if ((item.quan || 1) > 1) {
            if (item.otyp === DART || /\bdarts?\b/.test(lowerName)) rnd(1); // C throw_obj: multishot count.
            thrownId = next_ident(); // C splitobj: nextoid()/next_ident() for the thrown unit.
        }
        const thrownObject = {
            ...item,
            letter: undefined,
            line: undefined,
            wielded: false,
            worn: false,
            quivered: false,
            ox,
            oy,
            id: thrownId ?? item.id,
            quan: 1,
            glyph: item.cls === 'food' ? '%' : item.glyph || (item.cls === 'gem' ? '*' : ')'),
            color: item.cls === 'food' ? CLR_ORANGE : item.color || (item.cls === 'gem' ? CLR_GRAY : CLR_CYAN),
        };
        const combatObject = item.cls === 'weapon' || item.cls === 'gem' || item.glyph === ')' || item.otyp === GEM_CLASS;
        let impactMessage = '';
        let impactConsumedThrownObject = false;
        let impactObjectHit = false;
        let impactPassiveTarget = null;
        if (ironBarsImpact && heroThrownIronBarsBreakableClassHitObject(thrownObject)) {
            const barsImpact = await heroThrownIronBarsBreakImpact(thrownObject, ironBarsImpact);
            if (barsImpact.broke) {
                if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
                markThrownBrokenObjectDebt(thrownObject);
                stopCarriedFigurineTimerOnLeave(thrownObject);
                removeInventoryItem(item, 1);
                newsym(ironBarsImpact.x, ironBarsImpact.y);
                await setMessage(barsImpact.messages.join('  '));
                game._command_mode = null;
                game._throw_item_letter = null;
                clearThrowCountState();
                game._resume_time_after_more = 0;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                return;
            }
            impactMessage = barsImpact.messages.join('  ');
        }
        if (targetMon && (isBlindingVenomObject(item) || isAcidVenomObject(item))) {
            rnd(20);
            const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
            if (dex > rnd(25)) {
                if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
                const messages = isAcidVenomObject(item)
                    ? heroThrownAcidVenomHitMonster(thrownObject, targetMon)
                    : heroThrownBlindingVenomHitMonster(thrownObject, targetMon);
                removeInventoryItem(item, 1);
                newsym(targetMon.mx, targetMon.my);
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game._throw_item_letter = null;
                game._resume_time_after_more = 0;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                return;
            }
            const thrownName = pickupObjectName({ ...item, quan: 1 });
            const messages = [`The ${thrownName} misses the ${targetMon.data?.name || 'creature'}.`];
            messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
            impactMessage = messages.join('  ');
        } else if (targetMon && isCreamPieObject(item)) {
            rnd(20);
            const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
            if (dex > rnd(25)) {
                if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
                const messages = heroThrownCreamPieHitMonster(thrownObject, targetMon);
                removeInventoryItem(item, 1);
                newsym(targetMon.mx, targetMon.my);
                await setMessage(messages.join('  '));
                game._command_mode = null;
                game._throw_item_letter = null;
                game._resume_time_after_more = 0;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                game.context.move = 0;
                return;
            }
            const messages = [`The cream pie misses the ${targetMon.data?.name || 'creature'}.`];
            messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
            impactMessage = messages.join('  ');
        } else if (targetMon && isEggItem(item)) {
            rnd(20);
            const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
            if (dex > rnd(25)) {
                if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
                const messages = heroThrownEggHitMonster(thrownObject, targetMon);
                removeInventoryItem(item, 1);
                newsym(targetMon.mx, targetMon.my);
                await setMessage(messages.join('  '), !!messages.more);
                game._throw_item_letter = null;
                game._resume_time_after_more = 0;
                game.context.move = 0;
                if (applyLifeSavingOrFatalCommandMode(messages)) return;
                game._command_mode = null;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            const thrownName = pickupObjectName({ ...item, quan: 1 });
            const messages = [`The ${thrownName} misses the ${targetMon.data?.name || 'creature'}.`];
            messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
            impactMessage = messages.join('  ');
        } else if (targetMon && supportsHeroThrownPotionHit(item, targetMon)) {
            rnd(20);
            const dex = game.u?.acurr?.a?.[A_DEX] ?? 10;
            if (dex > rnd(25)) {
                if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
                const messages = heroThrownPotionHitMonster(thrownObject, targetMon, { allowLifeSaving: true });
                removeInventoryItem(item, 1);
                newsym(targetMon.mx, targetMon.my);
                const keepPotionCallPrompt = game._command_mode === 'callPotionAfterMore';
                await setMessage(messages.join('  '), keepPotionCallPrompt || !!messages.more);
                game._throw_item_letter = null;
                game._resume_time_after_more = 0;
                game.context.move = 0;
                if (messages.lifeSaving) {
                    game._command_mode = 'lifeSavingMore';
                    game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                    return;
                }
                if (messages.fatal) {
                    game._command_mode = 'deathDieMore';
                    game._pending_time_passed = 0;
                    game._process_command_time_now = 0;
                    game._run_steps_remaining = 0;
                    prepareDeathBones();
                    return;
                }
                if (!keepPotionCallPrompt) game._command_mode = null;
                game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
                return;
            }
            const thrownName = pickupObjectName({ ...item, quan: 1 });
            const messages = [`The ${thrownName} misses the ${targetMon.data?.name || 'creature'}.`];
            messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
            impactMessage = messages.join('  ');
        } else if (targetMon && heroThrownMonsterIsUnicorn(targetMon) && heroThrownUnicornGemKind(item)) {
            const unicornImpact = heroThrownUnicornGemImpact(thrownObject, targetMon);
            impactMessage = (unicornImpact.messages || []).join('  ');
            impactConsumedThrownObject = !!unicornImpact.consumed;
        } else if (targetMon && heroThrownStoneMissileHarmlessRockPasser(item, targetMon)) {
            const hitValue = heroThrownGemHitValue(thrownObject, targetMon);
            const dieroll = rnd(20);
            const thrownName = floorObjectTheSubject(thrownObject);
            const targetName = heroThrownVenomTargetName(targetMon);
            if (hitValue >= dieroll) {
                wakeMonsterFromHeroThrownHit(targetMon);
                exerciseHeroProjectileHitDexterity();
                impactMessage = `${thrownName} hits ${targetName} but does no harm.`;
                impactObjectHit = true;
                impactPassiveTarget = targetMon;
            } else {
                const messages = [`The ${pickupObjectName({ ...item, quan: 1 })} misses the ${targetMon.data?.name || 'creature'}.`];
                messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
                impactMessage = messages.join('  ');
            }
        } else if (targetMon && heroThrownGemClassObject(item)) {
            const gemImpact = heroThrownGemImpact(thrownObject, targetMon);
            impactMessage = (gemImpact.messages || []).join('  ');
            impactConsumedThrownObject = !!gemImpact.mulched;
            impactObjectHit = !!gemImpact.hit;
            impactPassiveTarget = gemImpact.hit ? targetMon : null;
        } else if (targetMon && heroProjectileSupportedWeaponObject(item)) {
            const weaponImpact = heroThrownWeaponImpact(thrownObject, targetMon);
            impactMessage = (weaponImpact.messages || []).join('  ');
            impactConsumedThrownObject = !!weaponImpact.mulched;
            impactObjectHit = !!weaponImpact.hit;
            impactPassiveTarget = weaponImpact.hit ? targetMon : null;
        } else if (targetMon && !combatObject) {
            rnd(20);
            const thrownName = pickupObjectName({ ...item, quan: 1 });
            const targetName = targetMon.data?.name || 'creature';
            const messages = [`The ${thrownName} misses the ${targetName}.`];
            messages.push(...wakeMonsterFromHeroThrownMiss(targetMon));
            impactMessage = messages.join('  ');
        }
        if (impactConsumedThrownObject) {
            if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
            stopCarriedFigurineTimerOnLeave(thrownObject);
            removeInventoryItem(item, 1);
            newsym(targetMon.mx, targetMon.my);
            await setMessage(impactMessage);
            game._command_mode = null;
            game._throw_item_letter = null;
            game._resume_time_after_more = 0;
            game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
            game.context.move = 0;
            return;
        }
        const projectileBreakRoll = !impactObjectHit && !projectileLandingIsSoft(ox, oy)
            ? rn2(100) // C breaktest: obj_resists() on hard landing.
            : null;
        curseLoadstoneLeavingInventory(thrownObject);
        if ((item.quan || 1) > 1) splitCarriedObjectShopBill(item, thrownObject, 1);
        stopCarriedFigurineTimerOnLeave(thrownObject);
        const landing = isHeroThrownCrackableArmorObject(thrownObject) && !impactObjectHit
            ? (() => {
                const messages = [];
                const object = landHeroThrownCrackableArmorAt(thrownObject, ox, oy, messages, { breakRoll: projectileBreakRoll });
                return { object, messages };
            })()
            : landProjectileObjectWithShopHandling(thrownObject, ox, oy, {
                breakRoll: projectileBreakRoll,
                ohit: impactObjectHit,
                passiveTarget: impactPassiveTarget,
            });
        const landingMessage = landing.messages.join('  ');
        newsym(ox, oy);
        const wasBurdened = (game.u?._statusSuffix || '').includes('Burdened');
        removeInventoryItem(item);
        if (wasBurdened) {
            let carriedWeight = Math.trunc(((game._goldCount || 0) + 50) / 100);
            for (const invItem of game.inventory || []) {
                if (isGoldObject(invItem)) continue;
                const kind = String(invItem.kind || invItem.actualKind || invItem.spellName || invItem.spell?.name || '').toLowerCase();
                const cls = invItem.cls || (invItem.otyp === RING_CLASS ? 'ring'
                    : invItem.otyp === SCROLL_CLASS ? 'scroll'
                        : invItem.otyp === POTION_CLASS ? 'potion'
                            : invItem.otyp === WAND_CLASS ? 'wand'
                                : invItem.otyp === GEM_CLASS ? 'gem' : '');
                carriedWeight += (OBJECT_WEIGHTS[kind] ?? CLASS_WEIGHTS[cls] ?? invItem.owt ?? 0) * (invItem.quan || 1);
            }
            const stats = game.u?.acurr?.a || [];
            const capacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
            if (carriedWeight <= capacity) {
                game._topline_after_more = 'Your movements are now unencumbered.';
                game._unburden_after_topline_more = 1;
            }
        }
        const wieldedLauncher = (game.inventory || []).find(invItem =>
            (invItem.wielded || invItem.line?.includes('weapon in')) && /bow|sling/.test(inventoryItemName(invItem).toLowerCase()));
        const thrownByHand = /arrow/.test(lowerName) && !wieldedLauncher;
        if (thrownByHand) {
            if (landingMessage) game._queued_message_after_more = landingMessage;
            await setMessage("You aren't wielding a bow, so you throw your arrow by hand.", !!landingMessage);
        }
        else if (impactMessage) {
            if (landingMessage) game._queued_message_after_more = landingMessage;
            await setMessage(impactMessage, true);
        }
        else if (landingMessage) await setMessage(landingMessage);
        else {
            game._pending_message = '';
            game._message_more = 0;
        }
        game._command_mode = null;
        game._throw_item_letter = null;
        clearThrowCountState();
        game._resume_time_after_more = 0;
        game._pending_time_passed = Math.max(game._pending_time_passed || 0, 1);
        game.context.move = 0;
        return;
    }

    if (ch === 't') {
        if (polyselfNoHands()) {
            await setMessage('You are physically incapable of throwing or shooting anything.');
            return;
        }
        const letters = inventoryLetters(isThrowSuggestItem);
        if (!letters && !(game.inventory || []).some(item => item.letter)) {
            await setMessage("You don't have anything to throw.");
            return;
        }
        const promptLetters = letters.includes('$') ? `$${letters.replace('$', '')}` : letters;
        game._throw_prompt = letters
            ? `What do you want to throw? [${promptLetters} or ?*]`
            : 'What do you want to throw? [*]';
        game._count_prefix = '';
        clearThrowCountState();
        await setMessage(game._throw_prompt);
        game._command_mode = 'throwObject';
        return;
    }

    if (ch === 'S') {
        await setMessage('Really save? [yn] (n)');
        game._command_mode = 'saveConfirm';
        return;
    }

    if (ch === 'o') {
        if (polyselfNoHands()) {
            await setMessage("You can't open anything -- you have no hands!");
            return;
        }
        await setMessage('In what direction?');
        game._command_mode = 'openDirection';
        return;
    }

    if (ch === 'w') {
        const letters = inventoryLetters(item => item.cls === 'weapon'
            || item.kind === 'pick-axe' || item.kind === 'grappling hook' || item.kind === 'unicorn horn').split('').sort().join('');
        const prompt = `What do you want to wield? [-${letters ? ` ${letters}` : ''} or ?*]`;
        game._wield_prompt = prompt;
        await setMessage(prompt);
        game._command_mode = 'wieldObject';
        return;
    }





    if (ch === 'd') {
        const letters = (game.inventory || []).map(item => item.letter).filter(Boolean).join('');
        const itemLetters = letters.replace('$', '');
        const range = `${letters.includes('$') ? '$' : ''}${getobjPromptLetters(itemLetters)}`;
        game._drop_prompt = `What do you want to drop? [${range} or ?*]`;
        game._pending_time_passed = 0;
        await setMessage(game._drop_prompt);
        game._command_mode = 'dropObject';
        return;
    }

    if (ch === '_') {
        let travelCursorStart = game._travel_previous_target || { x: game.u?.ux || 0, y: game.u?.uy || 0 };
        const travelStartLoc = game.level?.at(travelCursorStart.x, travelCursorStart.y);
        if (!travelStartLoc || IS_OBSTRUCTED(travelStartLoc.typ))
            travelCursorStart = { x: game.u?.ux || 0, y: game.u?.uy || 0 };
        if (game._travel_tip_seen || game.flags?.tutorial === false) {
            game._travel_tip_seen = 1;
            const prompt = game.flags?.tutorial === false
                ? 'Where do you want to travel to?'
                : "Where do you want to travel to?  (For instructions type a '?')";
            await setMessage(prompt);
            game._farlook_x = travelCursorStart.x;
            game._farlook_y = travelCursorStart.y;
            game._cursor_override = [(game._farlook_x || 1) - 1, (game._farlook_y || 0) + 1];
            game._travel_prompt_message = prompt;
            game._travel_prompt_current = 1;
            game._travel_target_description = '';
            game._travel_dot_confirms = 1;
            game._command_mode = 'travelCursor';
            return;
        }
        const prompt = 'Where do you want to travel to?';
        await setMessage(prompt, true);
        game._farlook_x = travelCursorStart.x;
        game._farlook_y = travelCursorStart.y;
        game._cursor_override = [(game._farlook_x || 1) - 1, (game._farlook_y || 0) + 1];
        game._travel_prompt_message = prompt;
        game._travel_prompt_current = 1;
        game._travel_target_description = '';
        game._command_mode = 'travelIntroMore';
        return;
    }



    if (ch === 'f') {
        if (polyselfNoHands()) {
            await setMessage('You are physically incapable of throwing or shooting anything.');
            return;
        }
        const projectile = (game.inventory || []).find(item =>
            isProjectileItem(item) && (item.quivered || item.line?.includes('at the ready') || item.line?.includes('in quiver')))
            || (game.inventory || []).find(isProjectileItem);
        if (!projectile) {
            await setMessage("You have no ammunition readied.");
            return;
        }
        let launcher = (game.inventory || []).find(item => {
            const name = inventoryItemName(item).toLowerCase();
            return (item.wielded || item.line?.includes('weapon in')) && /bow|sling|crossbow|yumi/.test(name);
        });
        launcher ??= (game.inventory || []).find(item => {
            const name = inventoryItemName(item).toLowerCase();
            return item.cls === 'weapon' && /bow|sling|crossbow|yumi/.test(name);
        });
        let swapMoreLine = '';
        if (launcher && !(launcher.wielded || launcher.line?.includes('weapon in'))) {
            const launcherWasAlternate = launcher.alternate || launcher.line?.includes('alternate weapon');
            const current = (game.inventory || []).find(item =>
                item !== launcher && (item.wielded || item.line?.includes('weapon in')));
            if (current) {
                current.wielded = false;
                current.alternate = true;
                current.line = `${current.letter || '?'} - ${inventoryItemName(current)} (alternate weapon; not wielded)`;
                if (launcherWasAlternate) swapMoreLine = `${current.line}.`;
            }
            launcher.wielded = true;
            launcher.alternate = false;
            launcher.line = `${launcher.letter || '?'} - ${inventoryItemName(launcher)} (weapon in right hand)`;
        }
        game._fire_item_letter = projectile.letter;
        game._fire_launcher_letter = launcher?.letter || null;
        if (launcher) {
            game._fire_swap_more_line = swapMoreLine;
            game._fire_time_pending_after_more = 1;
            game._fire_direction_pending_after_more = 1;
            await setMessage(`${launcher.line || `${launcher.letter || '?'} - ${inventoryItemName(launcher)}`}.`, true);
            game._command_mode = 'fireDirection';
            return;
        }
        await setMessage('In what direction?');
        game._command_mode = 'fireDirection';
        return;
    }

    if (ch === 'i') {
        game._hallu_names_after_visible_monster_pickup = 0;
        showInventoryOverlay();
        game._inventory_page = 0;
        game._command_mode = 'inventory';
        return;
    }

    if (ch === 'W') {
        if (polyselfNoHands()) {
            await setMessage("Don't even bother.");
            return;
        }
        const letters = inventoryLetters(item => item.cls === 'armor' && !(item.worn || item.line?.includes('being worn')));
        if (letters) {
            await setMessage(`What do you want to wear? [${getobjPromptLetters(letters)} or ?*]`);
            game._command_mode = 'wearObject';
            return;
        }
        const hasWearable = (game.inventory || []).some(item =>
            item.cls === 'armor' || item.cls === 'ring' || item.cls === 'amulet'
            || ['blindfold', 'towel', 'lenses', 'meat ring'].includes(item.kind));
        if (!hasWearable) {
            await setMessage("You don't have anything to wear.");
            return;
        }
        await setMessage('What do you want to wear? [*]');
        game._command_mode = 'wearObject';
        return;
    }

    if (ch === 'P') {
        const letters = inventoryLetters(item => !(item.worn || item.line?.includes('being worn'))
            && (item.cls === 'ring' || item.cls === 'amulet'
                || ['blindfold', 'towel', 'lenses', 'meat ring'].includes(item.kind || item.actualKind)));
        if (!letters) {
            const hasArmor = (game.inventory || []).some(item =>
                item.cls === 'armor' && !(item.worn || item.line?.includes('being worn')));
            if (!hasArmor) {
                await setMessage("You don't have anything to put on.");
                return;
            }
            await setMessage('What do you want to put on? [*]');
            game._command_mode = 'putOnObject';
            return;
        }
        await setMessage(`What do you want to put on? [${letters} or ?*]`);
        game._command_mode = 'putOnObject';
        return;
    }

    if (ch === 'z') {
        if (polyselfNoHands()) {
            await setMessage("You aren't able to zap anything in your current form.");
            return;
        }
        const letters = inventoryLetters(isWandItem);
        if (!letters) {
            await setMessage("You don't have anything to zap.");
            return;
        }
        await setMessage(`What do you want to zap? [${letters} or ?*]`);
        game._command_mode = 'zapObject';
        return;
    }

    if (ch === 'x') {
        const primary = (game.inventory || []).find(item =>
            item.wielded || /(?:weapon|wielded) in right hand/.test(item.line || ''));
        const secondary = (game.inventory || []).find(item =>
            item !== primary && (item.alternate || /left hand|alternate weapon/.test(item.line || '')));
        if (!primary || !secondary) {
            await setMessage("You don't have a secondary weapon readied.");
            return;
        }
        const primaryName = inventoryItemName(primary);
        const secondaryName = inventoryItemName(secondary);
        primary.wielded = false;
        primary.alternate = true;
        primary.line = `${primary.letter || '?'} - ${primaryName} (alternate weapon; not wielded)`;
        secondary.wielded = true;
        secondary.alternate = false;
        secondary.line = `${secondary.letter || '?'} - ${secondaryName} (weapon in right hand)`;
        game._swap_exchange_count = (game._swap_exchange_count || 0) + 1;
        await setMessage(`${secondary.line}.`, true);
        game._swap_more_line = `${primary.line}.`;
        game._swap_time_pending_after_more = 1;
        game._swap_skip_tail_after_more = game._swap_exchange_count === 1 ? 1 : 0;
        return;
    }

    if (ch === 'T') {
        const wornItems = (game.inventory || []).filter(item =>
            (item.worn || item.line?.includes('being worn'))
            && item.cls === 'armor');
        if (wornItems.length > 1) {
            const prompt = `What do you want to take off? [${wornItems.map(item => item.letter).join('')} or ?*]`;
            game._take_off_prompt = prompt;
            await setMessage(prompt);
            game._command_mode = 'takeOffObject';
            return;
        }
        const armor = wornItems[0];
        if (armor) {
            if (armor.cursed) {
                armor.bknown = true;
                const pair = /(?:boots|gloves|lenses)$/.test(pickupObjectName(armor));
                await setMessage(`You can't.  ${pair ? 'They are' : 'It is'} cursed.`);
                game.context.move = 0;
                return;
            }
            const baseName = String(armor.line || `${armor.letter || '?'} - ${pickupObjectName(armor)}`)
                .replace(/^[a-zA-Z] - /, '')
                .replace(/ \(being worn\).*$/, '');
            const acBonus = (ARMOR_AC_BONUS[String(armor.kind || '').toLowerCase()] ?? 0) + (armor.spe ?? 0);
            const kind = String(armor.kind || '').toLowerCase();
            const delay = ARMOR_WEAR_DELAY[kind] || (/dragon scales?/.test(kind) ? 5 : 0);
            if (delay) {
                const simpleName = pickupObjectName(armor);
                game._armor_wear_occupation = {
                    action: 'takeoff',
                    itemLetter: armor.letter || '?',
                    kind,
                    baseName,
                    simpleName: / mail$/.test(simpleName) ? 'mail' : simpleName,
                    acBonus,
                    turns: delay,
                };
                game.context.move = 1;
                return;
            }
            armor.worn = false;
            armor.line = `${armor.letter || '?'} - ${baseName}`;
            if (game.u) {
                game._status_uac_before_more = game.u.uac ?? 10;
                game._status_uac_before_more_seen = 0;
                game.u.uac = (game.u.uac ?? 10) + acBonus;
            }
            if ((kind === 'speed boots' || isBlueDragonArmorKind(kind)) && game.u)
                syncHeroSpeedState();
            updateWornDisplacement();
            await setMessage(`You were wearing ${baseName}.`);
            game.context.move = 1;
            return;
        }
        await setMessage("You aren't wearing any armor.");
        return;
    }

    if (ch === 'a') {
        if (polyselfNoHands()) {
            await setMessage("You aren't able to use or apply tools in your current form.");
            return;
        }
        const prompt = applyPromptMessage();
        if (!prompt) {
            await setMessage("You don't have anything to use or apply.");
            return;
        }
        await setMessage(prompt);
        game._command_mode = 'applyObject';
        return;
    }

    if (ch === 'e') {
        if (heroIsStrangledForChoke()) {
            await setMessage("If you can't breathe air, how can you consume solids?");
            return;
        }
        const floorFood = (game.level?.objects || []).find(obj =>
            !obj.hidden
            && obj.ox === game.u?.ux
            && obj.oy === game.u?.uy
            && (isFoodOrCorpseObject(obj) || heroCanEatNonFoodMetal(obj)));
        if (floorFood) {
            const name = pickupObjectName(floorFood);
            const article = /^[aeiou]/i.test(name) ? 'an' : 'a';
            game._eat_floor_object = floorFood;
            const message = (floorFood.quan || 1) > 1
                ? `There are ${floorFood.quan} ${floorFood.bknown && !floorFood.cursed && !floorFood.blessed ? 'uncursed ' : ''}${floorFood.plural || `${name}s`} here; eat one? [ynq] (n)`
                : `There is ${article} ${name} here; eat it? [ynq] (n)`;
            await setMessage(message);
            game._command_mode = 'eatFloorObject';
            return;
        }
        const letters = inventoryLetters(heroInventoryEatCandidate);
        if (!letters) {
            await setMessage("You don't have anything to eat.");
            return;
        }
        await setMessage(eatingPrompt());
        game._command_mode = 'eatObject';
        return;
    }

    if (ch === '+') {
        if (!(game._known_spells || []).length) {
            await setMessage("You don't know any spells right now.");
            return;
        }
        game._hallu_names_after_visible_monster_pickup = 0;
        const lines = spellMenuLines('Currently known spells', true);
        setOverlay(lines, Math.max(...lines.map(([row]) => row)) + 1, false, 19);
        game._command_mode = 'knownSpells';
        return;
    }

    if (ch === '$') {
        const gold = game._goldCount || 0;
        await setMessage(gold ? `Your wallet contains ${gold} zorkmid${gold === 1 ? '' : 's'}.` : 'Your wallet is empty.');
        return;
    }

    if (ch === ')') {
        const weapon = (game.inventory || []).find(item =>
            item.wielded || item.line?.includes('weapon in') || item.line?.includes('wielded in'));
        await setMessage(weapon ? `${weapon.line || `${weapon.letter || '?'} - ${pickupObjectName(weapon)}`}.` : 'You are bare handed.');
        return;
    }

    if (ch === '[') {
        const armor = (game.inventory || []).find(item =>
            item.cls === 'armor' && (item.worn || item.line?.includes('being worn')));
        await setMessage(armor ? `${armor.line || `${armor.letter || '?'} - ${pickupObjectName(armor)}`}.` : "You aren't wearing any armor.");
        return;
    }

    if (ch === '=') {
        const ring = (game.inventory || []).find(item =>
            item.cls === 'ring' && (item.worn || item.line?.includes('(on ')));
        await setMessage(ring ? `${ring.line || `${ring.letter || '?'} - ${pickupObjectName(ring)}`}.` : 'You are not wearing any rings.');
        return;
    }

    if (ch === '"') {
        const amulet = (game.inventory || []).find(item =>
            item.cls === 'amulet' && (item.worn || item.line?.includes('being worn')));
        await setMessage(amulet ? `${amulet.line || `${amulet.letter || '?'} - ${pickupObjectName(amulet)}`}.` : 'You are not wearing an amulet.');
        return;
    }

    if (ch === '\x7f') {
        setOverlay(TERRAIN_LINES, 6);
        game._command_mode = 'terrainMenu';
        return;
    }

    if (ch === '\\') {
        game._hallu_names_after_visible_monster_pickup = 0;
        game._discovery_page = 0;
        setOverlay(discoveryOverlayLines(), 24, true);
        game._command_mode = 'discoveries';
        return;
    }

    if (ch === '\x18') {
        game._hallu_names_after_visible_monster_pickup = 0;
        const page1 = genericAttributesPage1();
        game._attributes_page_2 = genericAttributesPage2();
        if (game._attributes_page_3) page1[page1.length - 1] = [23, 0, ' (1 of 3)'];
        setOverlay(page1, 24, true);
        game._command_mode = 'attributes';
        return;
    }

    if (ch === ',') {
        if (polyselfNoHands()) {
            await setMessage('You are physically incapable of picking anything up.');
            return;
        }
        const objectsHere = (game.level?.objects || [])
            .filter(obj => !obj.hidden && !obj.transientProjectile && obj.ox === game.u?.ux && obj.oy === game.u?.uy)
            .sort((a, b) => ((b.otyp === 'corpse' || b.otyp === CORPSE) ? 1 : 0) - ((a.otyp === 'corpse' || a.otyp === CORPSE) ? 1 : 0));
        if (objectsHere.length > 1) {
            const entries = pickupMenuEntries(objectsHere);
            game._pickup_list = { objects: objectsHere, entries, selected: [] };
            const rows = pickupMenuLines(entries, []);
            setOverlay(rows, Math.max(...rows.map(([row]) => row)) + 1, false, 40);
            game._command_mode = 'pickupList';
            return;
        }
        const objectHere = objectsHere[0];
        if (objectHere?.otyp === GOLD_PIECE || objectHere?.glyph === '$') {
            const preflight = await floorPickupPreflight(objectHere);
            if (!preflight.ok || preflight.skip) {
                await setMessage(floorPickupPreflightMessage(preflight));
                game.context.move = preflight.move ? 1 : 0;
                return;
            }
            if (preflight.prompt) {
                game._floor_pickup_pending = {
                    objectId: floorPickupObjectKey(objectHere),
                    preflight: floorPickupAcceptedPreflight(preflight),
                };
                game._command_mode = 'floorPickupBurdenConfirm';
                await setMessage(floorPickupBurdenPromptMessage(preflight));
                game.context.move = 0;
                return;
            }
            const pickup = pickUpFloorGoldObject(objectHere, preflight.takeCount || objectHere.quan || 1);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            const messages = [...(preflight.messages || []), ...(pickup.messages || [])];
            await setMessage(messages.join('  '), messages.length > 1);
            game.context.move = 1;
            return;
        }
        if (objectHere?.otyp === 'corpse' || objectHere?.otyp === CORPSE) {
            const preflight = await floorPickupPreflight(objectHere);
            if (!preflight.ok || preflight.skip) {
                await setMessage(floorPickupPreflightMessage(preflight));
                game.context.move = preflight.move ? 1 : 0;
                return;
            }
            if (preflight.prompt) {
                game._floor_pickup_pending = {
                    objectId: floorPickupObjectKey(objectHere),
                    preflight: floorPickupAcceptedPreflight(preflight),
                };
                game._command_mode = 'floorPickupBurdenConfirm';
                await setMessage(floorPickupBurdenPromptMessage(preflight));
                game.context.move = 0;
                return;
            }
            const corpseName = pickupObjectName({ ...objectHere, quan: 1 });
            const letter = nextInventoryLetter();
            const pickedItem = {
                ...objectHere,
                cls: 'food',
                letter,
                quan: 1,
                kind: corpseName,
            };
            objectIceEffect(pickedItem, game.u?.ux || 0, game.u?.uy || 0, { onLevel: false });
            game.inventory = [...(game.inventory || []), pickedItem];
            game._pet_food_scan_inventory = game.inventory;
            game.level.objects = (game.level.objects || []).filter(obj => obj !== objectHere);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            const pickupMessage = `${letter} - a ${corpseName}.`;
            const messages = [...(preflight.messages || []), pickupMessage];
            await setMessage(messages.join('  '));
            game.context.move = 1;
            return;
        }
        if (objectHere) {
            const objectId = floorPickupObjectKey(objectHere);
            const skipShopQuote = game._skip_shop_quote_once != null && game._skip_shop_quote_once === objectId;
            if (game._skip_shop_quote_once != null && !skipShopQuote) game._skip_shop_quote_once = null;
            const quoteInfo = shopFloorItemPriceInfo(objectHere, objectHere?.ox, objectHere?.oy);
            const shopPrice = quoteInfo?.itemPrice ?? shopItemPrice(objectHere);
            const preflight = await floorPickupPreflight(objectHere, { shopPrice });
            if (!preflight.ok || preflight.skip) {
                const message = floorPickupPreflightMessage(preflight);
                if (preflight.scareDust) {
                    const waitingForCall = scheduleScareMonsterDustTryCall(preflight, { moveCost: preflight.move ? 1 : 0 });
                    const billMessage = waitingForCall ? '' : finalizeScareMonsterDust(preflight);
                    await setMessage([message, billMessage].filter(Boolean).join('  '), waitingForCall);
                    game.context.move = waitingForCall ? 0 : preflight.move ? 1 : 0;
                    return;
                }
                await setMessage(message);
                game.context.move = preflight.move ? 1 : 0;
                return;
            }
            if (preflight.prompt) {
                game._floor_pickup_pending = {
                    objectId,
                    preflight: floorPickupAcceptedPreflight(preflight),
                    scareResetSpeObj: preflight.scareResetSpeOnDecline ? objectHere : null,
                };
                game._command_mode = 'floorPickupBurdenConfirm';
                await setMessage(floorPickupBurdenPromptMessage(preflight));
                game.context.move = 0;
                return;
            }
            if (shopPrice > 0 && !skipShopQuote) {
                game._pending_shop_pickup_id = objectId;
                game._pending_shop_pickup_preflight = preflight;
                game._command_mode = 'pickupShopQuote';
                await setMessage(shopPickupQuoteMessage(objectHere, shopPrice, quoteInfo), true);
                return;
            }
            if (skipShopQuote) game._skip_shop_quote_once = null;
            const preflightMessages = preflight.messages || [];
            const pickupObj = splitFloorPickupObjectForLift(objectHere, preflight.takeCount || objectHere.quan || 1);
            if (preflight.scareRemainderState && pickupObj !== objectHere)
                Object.assign(objectHere, preflight.scareRemainderState);
            const liftedPriceInfo = pickupObj === objectHere
                ? quoteInfo
                : shopFloorItemPriceInfo(pickupObj, pickupObj?.ox, pickupObj?.oy);
            const liftedShopPrice = pickupObj === objectHere
                ? shopPrice
                : liftedPriceInfo?.itemPrice ?? shopItemPrice(pickupObj);
            const amount = pickupObjectPhrase(pickupObj);
            const name = pickupObjectName({ ...pickupObj, quan: 1 });
            const mergeTarget = findPickedObjectInventoryMergeTarget(pickupObj, liftedShopPrice);
            if (mergeTarget) {
                const learnedByComparing = isSimpleMergeableFoodObject(pickupObj)
                    && (pickupObj.bknown === true) !== (mergeTarget.target.bknown !== false);
                const pickupMessage = mergePickedObjectIntoInventory(pickupObj, mergeTarget.target);
                game._pet_food_scan_inventory = game.inventory;
                objectIceEffect(pickupObj, game.u?.ux || 0, game.u?.uy || 0, { onLevel: false });
                game.level.objects = (game.level.objects || []).filter(obj => obj !== pickupObj);
                newsym(game.u?.ux || 0, game.u?.uy || 0);
                const pickupMessages = [...preflightMessages, pickupMessage];
                if (learnedByComparing) {
                    game._queued_message_after_more = pickupMessages.join('  ');
                    await setMessage('You learn more about your items by comparing them.', true);
                } else await setMessage(pickupMessages.join('  '));
                game.context.move = 1;
                return;
            }
            const reusableLetter = pickupObj.letter
                && !(game.inventory || []).some(item => item.letter === pickupObj.letter);
            const letter = reusableLetter
                ? pickupObj.letter
                : pickupObj.wasStolen && pickupObj.letter
                ? pickupObj.letter
                : preflight.inventoryLetter || nextInventoryLetter();
            const pickedItem = {
                ...pickupObj,
                letter,
                kind: pickupObj.kind || name,
                quan: pickupObj.quan || 1,
                no_charge: undefined,
                line: `${letter} - ${amount}`,
            };
            const billing = addPickedObjectToShopBill(pickupObj, pickedItem);
            const billedPrice = billing.itemPrice ?? billing.price ?? 0;
            const unpaidSuffix = shopPickupLinePriceSuffix(liftedPriceInfo, billedPrice);
            pickedItem.line = replaceShopPriceLineSuffix(pickedItem.line, unpaidSuffix);
            objectIceEffect(pickedItem, game.u?.ux || 0, game.u?.uy || 0, { onLevel: false });
            game.inventory = [...(game.inventory || []), pickedItem];
            maybeAttachCarriedFigurineTimeout(pickedItem);
            game._pet_food_scan_inventory = game.inventory;
            game.level.objects = (game.level.objects || []).filter(obj => obj !== pickupObj);
            newsym(game.u?.ux || 0, game.u?.uy || 0);
            let carriedWeight = Math.trunc(((game._goldCount || 0) + 50) / 100);
            for (const invItem of game.inventory || []) {
                if (isGoldObject(invItem)) continue;
                const kind = String(invItem.kind || invItem.actualKind || invItem.spellName || invItem.spell?.name || '').toLowerCase();
                const cls = invItem.cls || (invItem.otyp === RING_CLASS ? 'ring'
                    : invItem.otyp === SCROLL_CLASS ? 'scroll'
                        : invItem.otyp === POTION_CLASS ? 'potion'
                            : invItem.otyp === WAND_CLASS ? 'wand'
                                : invItem.otyp === GEM_CLASS ? 'gem' : '');
                carriedWeight += (OBJECT_WEIGHTS[kind] ?? CLASS_WEIGHTS[cls] ?? invItem.owt ?? 0) * (invItem.quan || 1);
            }
            const stats = game.u?.acurr?.a || [];
            const capacity = Math.min(1000, 25 * ((stats[0] ?? 10) + (stats[4] ?? 10)) + 50);
            if (carriedWeight > capacity) {
                const troublePrefix = containerTakeoutBurdenPrefix(heroEncumbranceForWeight(carriedWeight));
                const troubleMessage = [...preflightMessages, `${troublePrefix} lifting ${letter} - ${amount}${unpaidSuffix}.`].join('  ');
                const alreadyBurdened = (game.u?._statusSuffix || '').includes('Burdened');
                const width = game.nhDisplay?.cols || 80;
                const showMore = !alreadyBurdened || troubleMessage.length >= width - 8;
                let firstLine = troubleMessage;
                let secondLine = '';
                if (showMore && troubleMessage.length >= width) {
                    const split = troubleMessage.lastIndexOf(' ', width);
                    if (split > 0) {
                        firstLine = troubleMessage.slice(0, split);
                        secondLine = troubleMessage.slice(split + 1);
                    }
                }
                if (!alreadyBurdened) {
                    game._burden_after_more = 1;
                    game._queued_message_after_more = 'Your movements are slowed slightly because of your load.';
                }
                await setMessage(firstLine, showMore);
                if (secondLine) game._message_more_line = secondLine;
                game.context.move = 1;
                return;
            }
            const pickupMessage = `${letter} - ${amount}${unpaidSuffix}.`;
            const pickupMessages = [...preflightMessages, ...(billing.messages || []), ...(billing.goldMessages || []), pickupMessage];
            await setMessage(pickupMessages.join('  '), !!pickupObj.wasStolen || pickupMessages.join('  ').length >= (game.nhDisplay?.cols || 80) - 8);
            game.context.move = 1;
            return;
        }
        await setMessage('There is nothing here to pick up.');
        return;
    }

    if (ch === '>') {
        let downStair = null;
        for (let stair = game.stairs; stair; stair = stair.next) {
            if (!stair.up && stair.sx === game.u?.ux && stair.sy === game.u?.uy) downStair = stair;
        }
        if (!downStair) {
            await setMessage("You can't go down here.");
            return;
        }
        downStair.u_traversed = true;
        const fromLevel = { dnum: game.u?.uz?.dnum ?? 0, dlevel: game.u?.uz?.dlevel ?? 1 };
        if (isBuriedBallTrapActive()) buriedBallToPunishment();
        const ballAndChain = [game.u?.uball, game.u?.uchain].filter(Boolean);
        removeCarriedPunishmentObjects(ballAndChain);
        rememberObjectsAtHero();
        game._saved_levels ??= new Map();
        game._saved_levels.set(`${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`, {
            level: game.level,
            stairs: game.stairs,
            utrack: [...(game._utrack || [])],
            moves: game.moves || 1,
        });
        const carriedPet = game.level?.monsters?.find(mon => mon.pet
            && (mon.mx - (game.u?.ux || 0)) ** 2 + (mon.my - (game.u?.uy || 0)) ** 2 < 3);
        const punished = !!game.u?.uball;
        const fallDownStairs = (game.u?._statusSuffix || '').includes('Burdened') || punished || game.u?.fumbling;
        game.u.uz = { ...downStair.tolev };
        game._travel_previous_target = null;
        game._travel_target = null;
        syncDungeonContext();
        const saved = game._saved_levels.get(`${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`);
        if (saved) {
            if (carriedPet)
                game.level.monsters = game.level.monsters.filter(mon => mon !== carriedPet);
            game.level = saved.level;
            game.stairs = saved.stairs;
            game._utrack = [...(saved.utrack || [])];
            catchUpReturnedLevelGlobShrinkTimers();
            for (const mon of game.level?.monsters || []) {
                rnd(10);
            }
            let arrivalStair = null;
            for (let stair = game.stairs; stair; stair = stair.next) {
                if (stair.up) arrivalStair = stair;
            }
            if (arrivalStair) arrivalStair.u_traversed = true;
            game.u.ux = arrivalStair?.sx ?? game.u.ux;
            game.u.uy = arrivalStair?.sy ?? game.u.uy;
            if (fallDownStairs) {
                game._stairs_fall_after_more = {
                    punished,
                    ballAndChain,
                    fromLevel,
                    damage: punished ? null : rnd(3),
                };
            } else if (ballAndChain.length && game.level) {
                game.level.objects = (game.level.objects || []).filter(obj => !ballAndChain.includes(obj));
                for (const obj of ballAndChain) {
                    obj.ox = game.u?.ux || 0;
                    obj.oy = game.u?.uy || 0;
                    obj.hidden = false;
                    obj.transientProjectile = false;
                }
                if (game.u?.uball) game.level.objects.push(game.u.uball);
                if (game.u?.uchain) game.level.objects.push(game.u.uchain);
            }
            placeFollowerAfterLevelChange(carriedPet);
            deliverQueuedImpactDroppedObjects(game.u?.uz);
            vision_reset();
            game._redraw_level_after_more = 1;
            await setMessage(fallDownStairs ? 'You fall down the stairs.' : 'You descend the stairs.', true);
            game.context.move = 1;
            return;
        }
        if (carriedPet)
            game.level.monsters = game.level.monsters.filter(mon => mon !== carriedPet);
        await mklev();
        u_on_upstairs();
        for (let stair = game.stairs; stair; stair = stair.next)
            if (stair.sx === game.u?.ux && stair.sy === game.u?.uy) {
                stair.u_traversed = true;
                break;
            }
        if ((game._startup_role || game.urole?.name?.m) === 'Tourist') {
            const exper = level_difficulty();
            game.u.uexp = (game.u.uexp || 0) + exper;
            game.u.urexp = (game.u.urexp || 0) + 4 * exper;
        }
        if (fallDownStairs) {
            game._stairs_fall_after_more = {
                punished,
                ballAndChain,
                fromLevel,
                damage: punished ? null : rnd(3),
            };
        } else if (ballAndChain.length && game.level) {
            game.level.objects = (game.level.objects || []).filter(obj => !ballAndChain.includes(obj));
            for (const obj of ballAndChain) {
                obj.ox = game.u?.ux || 0;
                obj.oy = game.u?.uy || 0;
                obj.hidden = false;
                obj.transientProjectile = false;
            }
            if (game.u?.uball) game.level.objects.push(game.u.uball);
            if (game.u?.uchain) game.level.objects.push(game.u.uchain);
        }
        placeFollowerAfterLevelChange(carriedPet);
        deliverQueuedImpactDroppedObjects(game.u?.uz);
        game._utrack = [];
        vision_reset();
        game._redraw_level_after_more = 1;
        await setMessage(fallDownStairs ? 'You fall down the stairs.' : 'You descend the stairs.', true);
        game.context.move = 1;
        return;
    }

    if (ch === '<') {
        let upStair = null;
        for (let stair = game.stairs; stair; stair = stair.next) {
            if (stair.up && stair.sx === game.u?.ux && stair.sy === game.u?.uy) upStair = stair;
        }
        if (!upStair) {
            await setMessage("You can't go up here.");
            return;
        }
        upStair.u_traversed = true;
        const fromLevel = { dnum: game.u?.uz?.dnum ?? 0, dlevel: game.u?.uz?.dlevel ?? 1 };
        if (isBuriedBallTrapActive()) buriedBallToPunishment();
        const ballAndChain = [game.u?.uball, game.u?.uchain].filter(Boolean);
        removeCarriedPunishmentObjects(ballAndChain);
        rememberObjectsAtHero();
        game._saved_levels ??= new Map();
        game._saved_levels.set(`${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`, {
            level: game.level,
            stairs: game.stairs,
            utrack: [...(game._utrack || [])],
            moves: game.moves || 1,
        });
        const carriedFollowers = (game.level?.monsters || []).filter(mon =>
            (mon.data?.name === 'water demon' || mon.pet)
            && (mon.mx - (game.u?.ux || 0)) ** 2 + (mon.my - (game.u?.uy || 0)) ** 2 < 3
        );
        if (carriedFollowers.length)
            game.level.monsters = game.level.monsters.filter(mon => !carriedFollowers.includes(mon));
        game.u.uz = { ...upStair.tolev };
        game._travel_previous_target = null;
        game._travel_target = null;
        syncDungeonContext();
        const saved = game._saved_levels.get(`${game.u?.uz?.dnum ?? 0}:${game.u?.uz?.dlevel ?? 1}`);
        if (saved) {
            game.level = saved.level;
            game.stairs = saved.stairs;
            game._utrack = [...(saved.utrack || [])];
            catchUpReturnedLevelGlobShrinkTimers();
            for (const mon of game.level?.monsters || []) {
                rnd(10);
            }
        } else {
            await mklev();
            u_on_dnstairs();
            game._utrack = [];
        }
        for (let stair = game.stairs; stair; stair = stair.next) {
            if (!stair.up) {
                stair.u_traversed = true;
                game.u.ux = stair.sx;
                game.u.uy = stair.sy;
                break;
            }
        }
        if (ballAndChain.length && game.level) {
            game.level.objects = (game.level.objects || []).filter(obj => !ballAndChain.includes(obj));
            for (const obj of ballAndChain) {
                obj.ox = game.u?.ux || 0;
                obj.oy = game.u?.uy || 0;
                obj.hidden = false;
                obj.transientProjectile = false;
            }
            if (game.u?.uball) game.level.objects.push(game.u.uball);
            if (game.u?.uchain) game.level.objects.push(game.u.uchain);
        }
        for (const follower of carriedFollowers) {
            placeFollowerAfterLevelChange(follower);
            follower.movement = Math.max(follower.movement || 0, 12);
        }
        deliverQueuedImpactDroppedObjects(game.u?.uz);
        vision_reset();
        game._redraw_level_after_more = 1;
        game._stairs_arrival_after_more = { fromLevel, silent: !ballAndChain.length };
        await setMessage(game.u?.uball ? 'With great effort, you climb up the stairs.' : 'You climb up the stairs.', true);
        game.context.move = 1;
        return;
    }

    if (ch === 'p') {
        const target = resolvePayShopkeeperFromScan(scanPayShopkeepers());
        if (target.needsPayWhomPrompt) {
            await startPayWhomCursor(target.resident, { requestMenu: requestMenuPrefix });
            return;
        }
        if (!target.selected) {
            await setMessage(target.message || 'There appears to be no shopkeeper here to receive your payment.');
            return;
        }
        await finishPayCommandForShopkeeper(target.selected, target.resident, { requestMenu: requestMenuPrefix });
        return;
    }

    if (game._force_fight) {
        game._force_fight = 0;
        const forceDir = movementDirection(ch);
        if (forceDir) {
            const targetX = (game.u?.ux || 0) + forceDir.dx;
            const targetY = (game.u?.uy || 0) + forceDir.dy;
            const target = game.level?.at(targetX, targetY);
            const mon = game.level?.monsters?.find(candidate => candidate.mx === targetX && candidate.my === targetY);
            if (mon) {
                game._force_fight_target = mon;
                await moveHero(forceDir.dx, forceDir.dy);
                game._force_fight_target = null;
                return;
            }
            await setMessage(target && !IS_OBSTRUCTED(target.typ) ? 'You attack thin air.' : 'You harmlessly attack the wall.');
            if (!target || IS_OBSTRUCTED(target.typ)) game._fight_wall_message = 1;
            game.context.move = 1;
            return;
        }
    }

    if (ch === 'F') {
        game._force_fight = 1;
        return;
    }

    if (ch === 'm') {
        game._move_nopick_prefix = 1;
        game._request_menu_prefix = 1;
        return;
    }

    const moveCh = (ch === '\n' || ch === '\r') ? 'J' : ch;
    const dir = movementDirection(moveCh);

    if (dir) {
        if ((game.u?._statusSuffix || '').includes('Overloaded') && game.u?._monsterMove === 0) {
            await setMessage('You collapse under your load.');
            game.context.move = 1;
            return;
        }
        if ('HJKLYUBN'.includes(moveCh)) {
            game._suppress_obstructed_message_once = 1;
            game._initial_run_command = 1;
            game._last_str_turn = 0;
        }
        let moveDx = dir.dx;
        let moveDy = dir.dy;
        const confusedMove = (game.u?._statusSuffix || '').includes('Conf');
        let impairedMove = false;
        if (confusedMove && !rn2(5)) {
            impairedMove = true;
            const confusedDirs = [
                { dx: -1, dy: 0 }, { dx: 0, dy: -1 }, { dx: 1, dy: 0 }, { dx: 0, dy: 1 },
                { dx: -1, dy: -1 }, { dx: 1, dy: -1 }, { dx: 1, dy: 1 }, { dx: -1, dy: 1 },
            ];
            for (let tries = 0; tries <= 50; tries++) {
                const confusedDir = confusedDirs[rn2(confusedDirs.length)];
                const loc = game.level?.at((game.u?.ux || 0) + confusedDir.dx, (game.u?.uy || 0) + confusedDir.dy);
                if (!loc || IS_OBSTRUCTED(loc.typ)) continue;
                moveDx = confusedDir.dx;
                moveDy = confusedDir.dy;
                break;
            }
        }
        const moveHalluDisplay = (game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active;
        if (moveHalluDisplay) game._display_hallucinated_normal = 1;
        await moveHero(moveDx, moveDy);
        if (moveHalluDisplay) game._display_hallucinated_normal = 0;

        if ('HJKLYUBN'.includes(moveCh) && game.context.move) {
            if (impairedMove) {
                game._confused_run_stop_roll = 1;
            } else {
                game._run_key = moveCh.toLowerCase();
                game._run_mode = (ch === '\n' || ch === '\r') ? 3 : 1;
                game._process_command_time_now = 1;
                if (!game._run_stop_now) game._run_steps_remaining = Math.max(COLNO, ROWNO);
            }
        }
        return;
    }

    if (ch === '@') {
        game._autopickup = !game._autopickup;
        game.flags.pickup = game._autopickup;
        await setMessage(game._autopickup ? `Autopickup: ON, for ${game._autopickup_types || 'all'} objects.` : 'Autopickup: OFF.');
        return;
    }

    if (ch === 'O') {
        if (requestMenuPrefix) {
            game._move_nopick_prefix = 0;
            game._options_full_selected = [];
            game._options_full_pickup_pending = 0;
            setFullOptionsPage(1);
            return;
        }
        setOverlay(optionsPage1Lines(), 24, true);
        game._command_mode = 'options';
        game._options_page = 1;
        return;
    }

    if (ch === '/') {
        setOverlay(FARLOOK_MENU_LINES, 15);
        game._command_mode = 'farlookMenu';
        return;
    }

    if (ch === ';') {
        game._farlook_x = game.u?.ux || 0;
        game._farlook_y = game.u?.uy || 0;
        game._farlook_quick = 1;
        const getposTipSeen = game._farlook_tip_seen || game._travel_tip_seen
            || game._jump_tip_seen || game._getpos_tip_seen;
        if (!getposTipSeen) {
            game._farlook_tip_after_intro = 1;
            await setMessage('Pick a monster, object or location.', true);
            game._command_mode = 'farlookIntroMore';
            return;
        }
        await setMessage('Pick a monster, object or location.');
        game._command_mode = 'farlookCursor';
        return;
    }

    if (ch === '?') {
        const helpLines = game.flags?.debug
            ? [...HELP_MENU_LINES.slice(0, -1), [17, 17, 'p - List of wizard-mode commands.'], [18, 17, '(end)']]
            : HELP_MENU_LINES;
        setOverlay(helpLines, game.flags?.debug ? 19 : 18);
        game._command_mode = 'helpMenu';
        return;
    }

    if (ch === 'q') {
        if (game.level?.at(game.u?.ux || 0, game.u?.uy || 0)?.typ === SINK) {
            await setMessage('Drink from the sink? [yn] (n)');
            game._command_mode = 'quaffSinkConfirm';
            return;
        }
        if (game.level?.at(game.u?.ux || 0, game.u?.uy || 0)?.typ === FOUNTAIN) {
            await setMessage('Drink from the fountain? [yn] (n)');
            game._command_mode = 'quaffFountainConfirm';
            return;
        }
        const letters = inventoryLetters(item => item.cls === 'potion' || item.otyp === POTION_CLASS);
        if (letters) {
            await setMessage(`What do you want to drink? [${getobjPromptLetters(letters)} or ?*]`);
            game._command_mode = 'quaffObject';
            return;
        }
        await setMessage("You don't have anything to drink.");
        return;
    }

    if (ch === 'E') {
        const letters = inventoryLetters(item => item.cls === 'weapon' || item.cls === 'wand' || item.cls === 'ring' || item.cls === 'tool');
        await setMessage(`What do you want to write with? [- ${letters} or ?*]`);
        game._command_mode = 'engraveObject';
        return;
    }

    if (ch === '.') {

        const count = game._count_prefix ? Number(game._count_prefix) || 1 : 0;
        const countMessage = game._count_prefix ? `Count: ${game._count_prefix}` : '';
        const forcedWait = !!game._move_nopick_prefix;
        const nearbyMonster = !ignoreSafeWaitOnce && !count && !forcedWait && (game.flags?.safe_wait ?? FULL_OPTION_DEFAULTS.safe_wait)
            && (game.level?.monsters || []).some(mon =>
                !mon.pet && !mon.mpeaceful
                && Math.max(Math.abs(mon.mx - (game.u?.ux || 0)), Math.abs(mon.my - (game.u?.uy || 0))) <= 1
            );
        game._move_nopick_prefix = 0;
        game._request_menu_prefix = 0;
        if (countMessage && game._pending_message === countMessage) {
            game._pending_message = '';
            game._message_more = 0;
        }
        if (game._safe_wait_repeat_suppressed && !count && !forcedWait) {
            game.context.move = 0;
            game._counted_repeat_interruptible = 0;
            game._count_prefix = '';
            return;
        }
        game.context.move = count || 1;
        if (count) game._count_search_target_turn = (game.moves || 1) + count;
        game._counted_repeat_interruptible = count > 0;
        game._count_prefix = '';
        if (nearbyMonster) {
            game.context.move = 0;
            game._counted_repeat_interruptible = 0;
            const cmdassist = game.flags?.cmdassist ?? FULL_OPTION_DEFAULTS.cmdassist;
            const waitMessage = cmdassist || !(game._did_nothing_flag || 0)
                ? "Are you waiting to get hit?  Use 'm' prefix to force a no-op (to rest)."
                : 'Are you waiting to get hit?';
            if (!cmdassist) game._did_nothing_flag = (game._did_nothing_flag || 0) + 1;
            if (game._last_pline_message !== waitMessage) await setMessage(waitMessage);
            return;
        }
        game._did_nothing_flag = 0;
        return;
    }

    if (ch === 's') {

        game._run_key = null;
        game._run_steps_remaining = 0;
        const count = game._count_prefix ? Number(game._count_prefix) || 1 : 0;
        const countMessage = game._count_prefix ? `Count: ${game._count_prefix}` : '';
        const forcedSearch = !!game._move_nopick_prefix;
        game._forced_search_this_turn = forcedSearch ? 1 : 0;
        const nearbyMonster = (game.flags?.safe_wait ?? FULL_OPTION_DEFAULTS.safe_wait)
            && !game.u?.blind && !count && !forcedSearch && (game.level?.monsters || []).some(mon =>
            !mon.pet && !mon.mpeaceful
            && Math.max(Math.abs(mon.mx - (game.u?.ux || 0)), Math.abs(mon.my - (game.u?.uy || 0))) <= 1
        );
        game._move_nopick_prefix = 0;
        game._request_menu_prefix = 0;
        if (nearbyMonster) {
            game._count_prefix = '';
            const cmdassist = game.flags?.cmdassist ?? FULL_OPTION_DEFAULTS.cmdassist;
            const message = cmdassist || !(game._already_found_flag || 0)
                ? "You already found a monster.  Use 'm' prefix to force another search."
                : 'You already found a monster.';
            if (!cmdassist) game._already_found_flag = (game._already_found_flag || 0) + 1;
            if (game._last_pline_message === message) {
                game._pending_message_is_search_safety_warning = 0;
                game._message_more = 0;
                game._search_safety_warning_active = 1;
                game._clear_search_safety_message_next_flush = 0;
                return;
            }
            await setMessage(message);
            game._pending_message_is_search_safety_warning = 1;
            game._search_safety_warning_active = 1;
            game._clear_search_safety_message_next_flush = 1;
            game._clear_safety_message_skip_flushes = 1;
            return;
        }
        game._search_safety_warning_active = 0;
        game._already_found_flag = 0;
        if (countMessage && game._pending_message === countMessage) {
            game._pending_message = '';
            game._message_more = 0;
        }
        game.context.move = count || 1;
        game._search_pending_count = game.context.move;
        game._counted_repeat_interruptible = count > 0;
        game._last_command_was_search = true;
        game._count_prefix = '';
        return;
    }

    if (ch === ':') {
        const objects = (game.level?.objects || [])
            .filter(obj => !obj.hidden && obj.ox === game.u?.ux && obj.oy === game.u?.uy)
            .sort((a, b) => ((b.otyp === 'corpse' || b.otyp === CORPSE) ? 1 : 0) - ((a.otyp === 'corpse' || a.otyp === CORPSE) ? 1 : 0));
        const objectHere = objects[0];
        const engraving = game.level?.engravings?.find(engr => engr.x === game.u?.ux && engr.y === game.u?.uy && engr.text);
        const loc = game.level?.at(game.u?.ux || 0, game.u?.uy || 0);
        const dnstair = game.level?.dnstair;
        const upstair = game.level?.upstair;
        const downstairsHere = loc?.typ === STAIRS
            && (loc.ladder === 2 || (dnstair?.x === game.u?.ux && dnstair?.y === game.u?.uy));
        const upstairsHere = loc?.typ === STAIRS
            && (loc.ladder === 1 || (upstair?.x === game.u?.ux && upstair?.y === game.u?.uy));
        let stairsHere = false;
        for (let stair = game.stairs; stair; stair = stair.next) {
            if (stair.up && stair.sx === game.u?.ux && stair.sy === game.u?.uy) stairsHere = true;
        }
        if (objects.length > 1) {
            const rows = arrivalObjectListRows(objects);
            setOverlay(rows, rows.length);
            game._command_mode = 'objectListMore';
            return;
        }
        const details = [];
        if (loc?.typ === SINK) {
            details.push('There is a sink here.');
        } else if (loc?.typ === FOUNTAIN) {
            details.push('There is a fountain here.');
        } else if (loc?.typ === DOOR && (loc.doormask & D_BROKEN)) {
            details.push('There is a broken door here.');
        } else if (loc?.typ === DOOR) {
            details.push('There is a doorway here.');
        } else if (downstairsHere) {
            details.push('There is a staircase down here.');
        } else if (upstairsHere || stairsHere) {
            details.push(stairsHere ? 'There is a staircase up out of the dungeon here.' : 'There is a staircase up here.');
        }
        if (engraving) details.push(`Something is written here in the dust.  You read: "${engraving.text}".`);
        if (objectHere) {
            details.push(`You see here ${objectHere.otyp === BOULDER ? 'a boulder' : pickupObjectPhrase(objectHere)}.`);
        }
        if (details.length > 1) {
            const combined = details.join('  ');
            if (details[0] !== 'There is a staircase up out of the dungeon here.'
                && combined.length < (game.nhDisplay?.cols || 80)) {
                await setMessage(combined);
                return;
            }
            game._topline_after_more = details.slice(1).join('  ');
            await setMessage(details[0], true);
            return;
        }
        await setMessage(details.length ? details[0] : 'You see no objects here.', !!engraving);
        return;
    }

    if (ch === ' ' && game._suppress_space_unknown_once) {
        game._suppress_space_unknown_once = 0;
        game._hide_pending_message_once = 1;
        await setMessage("Unknown command ' '.");
        if ((game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active
            && !game._hallu_refreshed_this_command) {
            game._display_hallucinated_redraw = 1;
            refreshHallucinatedMap();
            game._hallu_refreshed_this_command = 1;
            game._display_hallucinated_redraw = 0;
        }
        return;
    }
    await setMessage(`Unknown command '${visibleKey(ch)}'.`);
    if ((game.u?._statusSuffix || '').includes('Hallu') && !game._swallow_overlay_active
        && !game._hallu_refreshed_this_command) {
        game._display_hallucinated_redraw = 1;
        refreshHallucinatedMap();
        game._hallu_refreshed_this_command = 1;
        game._display_hallucinated_redraw = 0;
    }
}
