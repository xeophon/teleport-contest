# 05 Food, Inventory, Containers, Shops C Parity Audit

This audit is based only on upstream C and current JS source inspection. It does not infer private-suite expectations.

## Scope

- `eat.c` food, corpse, glob, and tin behavior.
- Inventory pickup/drop, including lift limits, split/merge, and object loss paths.
- Container loot, put-in, take-out, stash, and tip flows.
- Shop billing, unpaid state, shop credit/debit, and object cost side effects.

## Upstream C Reference Map

### Eating

- `nethack-c/upstream/src/eat.c:244-288`: choking and gluttony death path.
- `nethack-c/upstream/src/eat.c:321-388`: object nutrition and `touchfood()` stack split, `oeaten`, and `costly_alteration(COST_BITE)`.
- `nethack-c/upstream/src/eat.c:519-573`: `eatfood()` occupation tick and `done_eating()` cleanup/effects.
- `nethack-c/upstream/src/eat.c:756-864`: `cprefx()` corpse pre-effects, cannibalism, stoning/sliming, Rider handling.
- `nethack-c/upstream/src/eat.c:1127-1328`: `cpostfx()` corpse post-effects and intrinsics.
- `nethack-c/upstream/src/eat.c:1386-1402`: `costly_tin()` shop charging for tins.
- `nethack-c/upstream/src/eat.c:1427-1512`: tin naming and variety selection.
- `nethack-c/upstream/src/eat.c:1527-1699`: `consume_tin()` trap, empty, smell prompt, corpse effects, conduct, nutrition, and use-up order.
- `nethack-c/upstream/src/eat.c:1701-1796`: `opentin()` and `start_tin()` occupation timing.
- `nethack-c/upstream/src/eat.c:1811-2007`: rotten food and `eatcorpse()` age, poison, sickness, timing, and first-bite behavior.
- `nethack-c/upstream/src/eat.c:2022-2074`: `start_eating()` initial bite and occupation setup.
- `nethack-c/upstream/src/eat.c:2508-2600`: `fpostfx()` non-corpse food effects.
- `nethack-c/upstream/src/eat.c:2627-2722`: edibility prompts.
- `nethack-c/upstream/src/eat.c:2817-3082`: `doeat()` command flow and victual timing/nutrition setup.
- `nethack-c/upstream/src/eat.c:3136-3154`: per-turn bite nutrition, choking check, and `oeaten` consumption.
- `nethack-c/upstream/src/eat.c:3289-3330`: `lesshungry()` saturation, choke, and full-warning handling.

### Pickup, Drop, Containers, Tip

- `nethack-c/upstream/src/pickup.c:672-907`: top-level pickup flow, pickup menus, autopickup, and object selection.
- `nethack-c/upstream/src/pickup.c:1571-1795`: `carry_count()` and `lift_object()` partial lifting, burden prompts, slots, boulders, loadstones, and special failures.
- `nethack-c/upstream/src/pickup.c:1802-1942`: `pickup_object()` and `pick_obj()`, including artifact touch, fatal/Rider corpses, scare monster scrolls, `addtobill()`, `addinv()`, and stolen value.
- `nethack-c/upstream/src/pickup.c:2022-2162`: container lookup, loot reachability, locked containers, bag of tricks bite, and `use_container()`.
- `nethack-c/upstream/src/pickup.c:2164-2345`: `doloot()`/`doloot_core()` direction, capacity, confusion/reverse-loot, multiple containers, saddles, and blind cockatrice touch.
- `nethack-c/upstream/src/pickup.c:2558-2807`: `in_container()` and `out_container()` object rejection, shop `sellobj()`, icebox, magic bag explosion, `addtobill()`, held magic-bag loss billing, and inventory insertion.
- `nethack-c/upstream/src/pickup.c:2972-3224`: `use_container()` prompt, trap handling, BoH content loss, lost-merchandise-gated turn cost, and action sequencing.
- `nethack-c/upstream/src/pickup.c:3228-3394`: traditional/menu loot item selection.
- `nethack-c/upstream/src/pickup.c:3479-3677`: `dotip()` source selection, capacity checks, horn/bag special sources, and floor precedence.
- `nethack-c/upstream/src/pickup.c:3687-4055`: `tipcontainer()` target selection, tip checks, cursed BoH loss, shop billing, and item placement.
- `nethack-c/upstream/src/artifact.c:895-970`: `touch_artifact()` self-willed/restricted checks, blast damage, Wisdom exercise, and evasion/refusal return.
- `nethack-c/upstream/src/do.c:27-43`: deliberate drop shop state.
- `nethack-c/upstream/src/do.c:760-843`: `dropx()`, `dropy()`, `dropz()`, floor effects, `sellobj()`, and `stackobj()`.
- `nethack-c/upstream/src/do.c:288-301`, `nethack-c/upstream/src/do.c:786-790`, `nethack-c/upstream/src/dokick.c:1511-1584`, `nethack-c/upstream/src/dokick.c:1639-1746`: seen-hole/trapdoor `flooreffects()` and `ship_object()` migration, including shop-debt charging before objects move between levels.
- `nethack-c/upstream/src/invent.c:1200-1346`: `hold_another_object()`, `useup()`, and `check_unpaid`.
- `nethack-c/upstream/src/invent.c:4363-4460`: bill-aware stack merging rules.

### Shop Billing

- `nethack-c/upstream/src/shk.c:1135-1266`: `onbill()`, `is_unpaid()`, and `obfree()` bill invariants and used-up/merge handling.
- `nethack-c/upstream/src/shk.c:1542-1660`: itemized bills, including unpaid contents and known/unknown containers.
- `nethack-c/upstream/src/shk.c:1757-1854`, `nethack-c/upstream/src/getpos.c:771-1165`, `nethack-c/upstream/src/cmd.c:3168-3178`: `dopay()` target selection, `Pay whom?` `getpos()` validation, pick keys, and monster/object cursor cycling.
- `nethack-c/upstream/src/shk.c:2111-2410`: paying bills and `buy_container()`.
- `nethack-c/upstream/src/shk.c:2805-3098`: shop item cost, pricing units, contained cost, dropped/picked container `no_charge`.
- `nethack-c/upstream/src/shk.c:3102-3139`: `special_stock()` candelabrum refusal for CANDLESHOP/Izchak before invocation.
- `nethack-c/upstream/src/shk.c:3195-3305`: `gem_learned()`, `alter_cost()`, and `unpaid_cost()`.
- `nethack-c/upstream/src/shk.c:3307-3710`: `addtobill()`, `splitbill()`, and `subfrombill()`.
- `nethack-c/upstream/src/shk.c:3712-3828`: stolen container/value recursion.
- `nethack-c/upstream/src/shk.c:3927-4194`: `sellobj()` returns, sale offers, credits, and no-charge state.
- `nethack-c/upstream/src/shk.c:5350-5462`: `costly_spot()`, `shop_object()`, and `price_quote()`.
- `nethack-c/upstream/src/shk.c:5688-5741`: usage fees for unpaid charged/partly used objects.
- `nethack-c/upstream/src/zap.c:2514-2523`, `nethack-c/upstream/src/zap.c:2604-2614`, `nethack-c/upstream/src/zap.c:2642-2651`, `nethack-c/upstream/src/invent.c:1310-1315`, `nethack-c/upstream/src/shk.c:1187-1227`: cursed wand backfire charges normal usage, then `useupall()`/`obfree()` preserves the exploded unpaid wand as a used-up bill row.
- `nethack-c/upstream/src/apply.c:1577-1620`, `nethack-c/upstream/src/shk.c:5688-5741`, `nethack-c/upstream/src/mkobj.c:698-725`: `catch_lit()` charges carried unpaid ignitable objects in shops through normal usage billing, prints the additional-cost message, then preserves the original bill row as a used-up dummy object before `begin_burn()`.
- `nethack-c/upstream/src/zap.c:4598-4661`, `nethack-c/upstream/src/trap.c:4455-4565`, `nethack-c/upstream/src/apply.c:1577-1620`: beam floor destruction calls `useupf()` only when the hero caused the damage; generic `fire_damage()`/`fire_damage_chain()` can light or destroy floor objects through `catch_lit()` without adding floor-item shop billing.
- `nethack-c/upstream/src/mkobj.c:752-826`: generic `costly_alteration()` for bite/open/destroy/cancel/degrade/disenchant/etc.
- `nethack-c/upstream/src/lock.c:162-170`, `nethack-c/upstream/src/lock.c:247-252`: non-destroying box lock break hides contents, runs `costly_alteration(COST_BRKLCK)`, then marks the box broken/unlocked.
- `nethack-c/upstream/src/apply.c:3568-3602`, `nethack-c/upstream/src/invent.c:1430-1452`, `nethack-c/upstream/src/zap.c:1458-1472`, `nethack-c/upstream/include/hack.h:296`: applying a cream pie to self splits one pie from a stack, runs `costly_alteration(COST_SPLAT)`, then deletes the splatted pie through `delobj()`/`obj_resists()`.
- `nethack-c/upstream/src/apply.c:3909-3993`, `nethack-c/upstream/include/hack.h:295`: applying a wand breaks it; unpaid wands run `check_unpaid()` and then `costly_alteration(COST_DSTROY)` before the wand is removed, even when the break result is only `But nothing else happens...`.
- `nethack-c/upstream/src/read.c:1553-1568`, `nethack-c/upstream/include/hack.h:282`: remove curse on cursed unpaid water runs `costly_alteration(COST_UNCURS)` before uncursing, preserving the original unholy-water bill value.
- `nethack-c/upstream/src/wield.c:972-1021`, `nethack-c/upstream/src/read.c:1672`, `nethack-c/upstream/include/hack.h:289`: cursed enchant weapon calls `costly_alteration(COST_DEGRD/COST_DECHNT)` before turning an unpaid crysknife into a worm tooth or lowering an enchanted weapon's `spe`.
- `nethack-c/upstream/src/read.c:1253-1264`: cursed enchant armor calls `costly_alteration(COST_DECHNT)` only when the computed enchantment delta is negative, after the visual message and before BUC/`spe` mutation.
- `nethack-c/upstream/src/read.c:1136-1156`, `nethack-c/upstream/src/read.c:1327-1344`, `nethack-c/upstream/src/read.c:1639-1659`: confused enchant weapon, destroy armor, and enchant armor clear proofing for the effect message, restore old proof before `costly_alteration(COST_DEGRD)` when proof is stripped, then apply the final proof state.

## Current JS Reference Map

### Eating

- `js/cmd.js:9212-9230`: nutrition and hunger update helpers.
- `js/cmd.js:9252-9265`: food conduct bookkeeping.
- `js/cmd.js:9272-9347`: `oeaten` and touch/split helpers.
- `js/cmd.js:10277-10537`: tin naming, variety, consuming, and open-tin finishing.
- `js/cmd.js:10540-10634`: tin opening occupation timing.
- `js/cmd.js:19134-19446`: partial rotten food, royal jelly, and glob eating helpers.
- `js/cmd.js:20852-20975`: rotten non-corpse food and egg paths.
- `js/cmd.js:36237-36540`: inventory and floor eating command paths.
- `js/allmain.js:10806-10844`: turn-loop processing for tin opening and `_eating_turns_remaining`.

### Pickup, Drop, Containers, Tip

- `js/cmd.js:22485-22820`, `js/cmd.js:28474-28503`, `js/cmd.js:28928-28954`: multi-object pickup menu and sequential selected-object pickup, including C-style priced menu rows, ordinary selected-object shop quotes before inventory lines, partial-success ordering where earlier successful objects remain picked up when a later selected object fails, menu scare-monster scroll state/dust handling, per-item burden prompts, and menu gold lift preflight.
- `js/cmd.js:33688-33774`: drop command, direct gold-drop donation, and unpaid-drop shop hook.
- `js/cmd.js:37612-37683`, `js/cmd.js:37808-37933`: fired and thrown projectile split/landing paths.
- `js/cmd.js:19609-20569`: container put-in menus, rejection checks, transfer helpers, and carried-bag partial unpaid stack splitting through the shared bill-split helper.
- `js/cmd.js:15580-15972`, `js/cmd.js:16983-17390`, `js/cmd.js:20249-20445`, `js/cmd.js:38560-38625`: container take-out shop billing, pre-lift touch/weight checks, loadstone weight/slot exceptions, preparation, and inventory insertion.
- `js/cmd.js:8440-8524`: forced box lock break now uses a dummy altered-object bill row for non-destroying shop lock breaks, leaving contents unbilled and the floor box no-charge.
- `js/cmd.js:13380-13422`, `js/cmd.js:17098-17366`, `js/cmd.js:22485-22820`, `js/cmd.js:28474-28503`, `js/cmd.js:28842-28898`, `js/cmd.js:29020-29036`, `js/cmd.js:34471-34486`, `js/cmd.js:42384-42484`, `js/cmd.js:42756-42789`: floor pickup now handles scare-monster scroll identity, blessed/uncursed/used state changes, dusting, C-ordered type-call prompts before `useupf()`-style used-up shop billing/deletion, floor-style used-up billing quotes, and ordinary inventory insertion; reading an unknown scare-monster scroll also enters the scroll type-call path; single shop-floor pickup runs lift/scare preflight and burden prompts before the JS shop quote; ordinary single/menu stack pickup quotes use C per-unit wording before inventory pickup lines; whole-container pickup quote/menu text now prices non-gold contents, uses contents-only wording for no-charge top containers, uses "and its contents" when the top container is billed too, and keeps contained gold out of item price; menu pickup treats dusting as success-and-continue and preserves partial-stack remainder state.
- `js/cmd.js:17015-17039`, `js/cmd.js:17216-17248`, `js/cmd.js:37793-37885`, `js/mklev.js:4535-4554`: loadstones now have C weight/cost/generation metadata, bypass floor/container lift weight and burden prompts, use the C full-inventory overflow/refusal split, avoid partial pickup splits, and curse/refuse ordinary let-go paths.
- `js/cmd.js:17021-17336`, `js/cmd.js:22449-22599`, `js/cmd.js:42553-42650`, `js/mklev.js:5172-5205`: floor boulder pickup now uses C weight/name/class metadata, rejects Sokoban boulders before rock-throwing overrides, lets rock-throwing polyself forms lift boulders without burden prompts or shop billing, and exposes boulders to single and menu pickup.
- `js/cmd.js:20489-20658`, `js/cmd.js:38873-38899`: tip checks and destination selection.
- `js/cmd.js:3078-3152`, `js/cmd.js:17038-17048`, `js/cmd.js:16849-16872`: queued hole/trapdoor impact migrations now route carried unpaid objects, unpaid contents, and shop-floor piles through a `ship_object()`-style shop-debt helper before moving them to the destination level; carried fragile objects run the C `breaktest()`-style gate after debt and before queueing, while shop-floor pile migration intentionally remains no-break like C `impact_drop()`.
- `js/cmd.js:17542-18220`: magic bag, bag of tricks, horn of plenty, usage-fee helpers, and tip into another container.
- `js/cmd.js:33982-34818`: loot UI modes for ice boxes, boxes, bags, put-in, take-out, and stash.
- `js/cmd.js:35034-35129`: tip command UI modes.
- `js/cmd.js:35823-35841`, `js/cmd.js:36434-36478`: command dispatch for tip and loot direction.
- `js/cmd.js:38249-38420`: single-object pickup path.

### Shop Billing

- `js/cmd.js:12715-12743`: shopkeeper lookup helpers.
- `js/cmd.js:12804-13487`: starter shop bill entries, split/subtract helpers, carried projectile bill split, hard-landing container-content impact, top-level hard-landing projectile breakage, same-shop unpaid projectile return, projectile debt conversion, bill-aware floor stack merging, pickup bill merge helpers, loose-gold credit/donation helpers, loose floor-gold pickup charging, shop-floor container take-out/tip billing, unpaid drop return, and test exports.
- `js/cmd.js:4581-4698`, `js/cmd.js:16786-16911`, `js/cmd.js:18358-18361`, `js/cmd.js:45197-45205`: simple `FOOD(...)` pickup and full-inventory pickup preflight now share the inventory merge predicate for `food ration`, `pancake`, `lembas wafer`, `cram ration`, `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`, with exact kind identity, C object-name compatibility, BUC/state checks, `oeaten`/`orotten`, no-charge normalization, same-shop/unit-price unpaid proof, C unit weights/costs where needed, and age averaging.
- `js/cmd.js:15153-15420`, `js/cmd.js:15710-15777`, `js/cmd.js:44656-44683`: ordinary `sellobj()` sale helpers, CANDLESHOP special-stock refusal, uninterested no-sale handling, paid drop sale info, shop-floor container put-in sale info, same-shop paid projectile sale before stacking, and thrown-gold projectile donation/wallet cleanup.
- `js/cmd.js:7305-7456`, `js/cmd.js:8262-8304`, `js/cmd.js:30471-30479`, `js/cmd.js:32637-32664`, `js/cmd.js:36919-36925`: carried inventory `useup()` helper, cream-pie `COST_SPLAT` dummy billing with stack splitting, carried fire destruction, and failed spellbook read destruction now preserve final-copy unpaid objects as used-up bill rows while leaving partial stacks to the `bquan > quan` debt path.
- `js/cmd.js:7756-7778`: carried fire catch-light now mirrors C `catch_lit()` billing for unpaid carried lamps/light sources in shops: usage debit first, "in addition" shopkeeper message second, used-up bill preservation third, and no billing when the hero is outside the shop.
- `js/cmd.js:7660-7700`, `js/cmd.js:7716-7750`, `js/cmd.js:22893-22896`, `js/cmd.js:28474`, `js/cmd.js:28525`, `js/cmd.js:31410`: floor fire now carries C's hero-caused billing flag; hero traps and hero rays bill, monster red dragon breath does not, generic floor catch-light remains unbilled, and destroyed shop-floor stacks split before `useupf()`-style used-up billing or outside-shop robbed value.
- `js/cmd.js:13442-13458`: buried-merchandise debt helper.
- `js/cmd.js:13819-13826`: unpaid line synchronization.
- `js/cmd.js:14240-14268`: used-up bill naming and memory helper.
- `js/cmd.js:168-176`, `js/cmd.js:19300-19373`, `js/cmd.js:33579-33613`: unpaid charged-object usage fee and charge-spend helpers, including no-effect wand `#apply` break billing and cursed wand backfire `useupall()`-style used-up bill preservation for ordinary and wishing wand branches.
- `js/cmd.js:13268-13342`: remove curse now bills selected cursed unpaid water through a dummy used-up bill row before clearing the curse and normalizing the live potion back to plain water.
- `js/cmd.js:13952-14022`: cursed enchant weapon now bills unpaid crysknife degradation and ordinary weapon disenchantment through dummy used-up bill rows before mutating the wielded weapon.
- `js/cmd.js:19399-19404`: cursed enchant armor now bills only negative enchantment deltas through a dummy used-up bill row before mutating the worn armor, and leaves the live bill intact when final `s == 0`.
- `js/cmd.js:13917-13946`, `js/cmd.js:19222-19233`, `js/cmd.js:19313-19333`: confused enchant weapon, destroy armor, and enchant armor now preserve the C proof-stripping order and create a `COST_DEGRD` dummy used-up bill only when old proof is actually removed.
- `js/cmd.js:9332-9472`, `js/cmd.js:9480-9575`, `js/cmd.js:21796-21803`, `js/cmd.js:34066-34123`, `js/cmd.js:34151-34153`, `js/cmd.js:34339-34341`, `js/cmd.js:35678-35695`, `js/cmd.js:39921-39932`: lamp and potion-of-oil apply paths plus lamp `#rub` paths that light/snuff, split oil-potion stacks, convert magic lamps before djinni handling, and route unpaid usage through C-style debit/used-up billing.
- `js/cmd.js:9698-10108`, `js/cmd.js:35330-35365`: shared musical instrument apply shell for ordinary flute/harp/horn/bugle/drum no-charge behavior, underwater and no-blow gates, prompt/cancel timing, C-shaped improvisation RNG, magic flute/harp billing, frost/fire horn direction zaps, charged mundane fallback, and drum-of-earthquake charges.
- `js/cmd.js:23328-23398`: normal spellbook study completion now charges unpaid library usage before learning/discovery finalization.
- `js/cmd.js:34232-34266`, `js/cmd.js:34490-34616`: expensive-camera and can-of-grease apply paths that spend charges through unpaid usage billing.
- `js/cmd.js:17950-18009`: magic bag shop debit context for special tip sources.
- `js/cmd.js:18254-18276`: horn-created object billing and unpaid stack merge helper.
- `js/cmd.js:22235-22982`: shop base cost, price calculation, ledger-first debt collection, itemized bill-row payment, credit/debit handling.
- `js/cmd.js:15052-15149`, `js/cmd.js:31152-31344`, `js/cmd.js:45428-45436`: `Pay whom?` target descriptions, C-distance monster/object cycling, validation, prompt input, and pay command source selection.
- `js/mklev.js:4681-4701`: container stacking now rejects `unpaid`/`no_charge` mismatches and unpaid container merges.
- `js/mklev.js:4902-4921`: duplicate unpaid-price helpers used by level generation/object merging.

## Findings

### 1. Eating and Food State Are Approximate

C keeps an active `victual` with `piece`, `usedtime`, `reqtime`, `nmod`, `canchoke`, `fullwarn`, and per-bite `oeaten` updates. First bite calls `touchfood()`, which can split a stack, set `oeaten`, and bill shop alterations before eating proceeds. Nutrition is applied during occupation ticks, not only at completion.

Current JS has touch/split helpers and a basic `_eating_turns_remaining` occupation, but most non-corpse food is consumed immediately, and multi-turn corpse nutrition is intentionally deferred until finish (`js/cmd.js:36474-36480`, `js/allmain.js:10821-10828`). This misses C behaviors that depend on bite-by-bite state: choking/full warnings, interruption/resume with a partly eaten object, per-bite shop charging, and `oeaten` weight/nutrition changes during occupation.

Concrete gaps:

- `touchInventoryFood()` and `touchFloorFood()` now split, set `oeaten`, and charge unpaid shop food through a C-equivalent `costly_alteration(COST_BITE)` path; remaining victual gaps are per-turn nutrition/use-up and interruption parity.
- Inventory corpse eating removes the item before the occupation finishes (`js/cmd.js:36274-36299`), while C keeps the food as `svc.context.victual.piece` until `done_eating()` or an effect consumes/revives it.
- Generic non-corpse food applies full nutrition and removes the object in one command (`js/cmd.js:36331-36347`, `js/cmd.js:36511-36518`), bypassing C `oc_delay`, `fprefx()`, `fpostfx()`, `lesshungry()`, and choking logic.
- `consumeOeaten()` clamps `oeaten` to at least 1 (`js/cmd.js:9278-9283`); C consumes `oeaten` toward use-up and can finish/delete the object through `done_eating()`/`useup()` (`eat.c:544-573`, `eat.c:3136-3154`).

### 2. Corpse and Glob Effects Are Partial

C corpse eating has three layers: pre-effects in `cprefx()`, rot/poison/sickness/timing in `eatcorpse()`, and post-effects in `cpostfx()`. It covers cannibalism, stoning/sliming, acidic/petrification interactions, Riders, wraiths, nurses, stalkers, lights/bats, mimics, quantum mechanics, lizards, polymorph sources, displacers, disenchanters, mind flayers, magical energy, and many intrinsic grants.

JS has some source-driven corpse and glob logic: corpse nutrition maps, poisonous/old/rotten floor corpse paths, lichen/newt cases, glob acid/poison/slime/petrification support, and royal jelly. That is not the same effect matrix.

Concrete gaps:

- Inventory corpse path is much thinner than floor corpse path and treats most corpses as a fixed short sequence (`js/cmd.js:36274-36299`).
- C tainted corpse sickness, Rider fatal/revival handling, cannibal luck/aggravation, lizard stoning cure, green slime, acid/petrification interactions, and many `cpostfx()` monster-specific outcomes are missing or only covered for a few hand-coded cases.
- Conduct tracking is broad and local (`js/cmd.js:9252-9265`); it does not mirror C's exact conduct ordering around tins, corpses, glob exceptions, eggs, and special food effects.
- Corpse rot/removal timers remember shop bills only for timeout/shrink paths (`js/cmd.js:13335-13350`, `js/cmd.js:13520-13530`), not through a central `useup()`/`obfree()` equivalent.

### 3. Tin Behavior Has Timing but Not Shop/Effect Parity

C tins are a separate path: variety is selected with blessed/health/random logic, cursed tins can explode, contents can be smelled and discarded, empty tins are handled, corpse pre/post effects can consume the tin before nutrition, metallivores eat the tin itself, and `costly_tin()` charges carried unpaid or shop-floor tins before use-up.

JS implements meaningful timing for tin openers, daggers/knives/picks/axes, blessed tins, no-hands, glib slips, and random delay (`js/cmd.js:10574-10634`). It also handles trapped/cursed explosion, empty tins, smell prompt, spinach, rotten/homemade/default nutrition, and newt energy (`js/cmd.js:10277-10537`).

Concrete gaps:

- JS now has narrow `costly_tin(COST_OPEN/COST_DSTROY)`-style billing for opened or trap-destroyed tins, including one-tin stack splits before used-up billing; remaining tin gaps are exact C split/use-up integration across all effect paths, tin variety corrections, metallivore details, and full corpse pre/post effects.
- Tin contents do not run the full `cprefx()`/`cpostfx()` corpse matrix; JS handles only selected outcomes.
- C variety selection corrects rotten tins for non-rotting corpses and homemade tins with rot checks (`eat.c:1460-1512`); JS variety logic is simpler (`js/cmd.js:10346-10354`).
- Metallivore tin nutrition is only partially represented; C gives the tin itself 5 nutrition in relevant paths (`eat.c:1560`, `eat.c:1256`).

### 4. Pickup Flow Does Not Preserve C Billing and Lift Semantics

C pickup computes what can be carried before transferring the object. `carry_count()` and `lift_object()` can reduce quantity, reject based on slots or special objects, ask burden prompts, and trigger special cases. `pick_obj()` calls `addtobill()` before `addinv()` so unpaid merges and bill identity stay correct.

JS single pickup computes a shop price, runs preflight, then shows a shop quote before transferring the picked object (`js/cmd.js:42200-42508`, `js/cmd.js:42756-42789`). It now has a starter floor-pickup preflight before quote/transfer/billing for artifact blast/evasion/death, fatal barehanded cockatrice/chickatrice corpse touch, floor Rider corpse revival, maximum-carry failure, partial stack lifting, single-object burden prompts, gold lift splitting, inventory-slot failure, single-object scare-monster scroll handling including unknown-scroll type-call before used-up billing/deletion and floor-style used-up billing quotes, loadstone weight/slot exceptions, and floor boulder lift/Sokoban/rock-throwing exceptions (`js/cmd.js:17000-17366`, `js/cmd.js:28684-28708`, `js/cmd.js:42302-42484`). Ordinary single and menu shop pickups now share C-style stack quote wording, using the unit price with `per <singular>` before the inventory line. Multi-pickup now displays C-style shop price suffixes in the selection menu and processes selected floor objects sequentially, running preflight and transfer against current inventory state for each selected object, so earlier successful picked shop objects remain carried and billed if a later selected object fails; boulder failures stop later selections while already-picked earlier selections remain, scare-monster scroll dusting in this path now prompts for an unknown type before billing/deletion, prints the used-up floor quote, and then continues to later selected items, partial live stacks restore the floor remainder's old state, per-item burden prompts can skip or abort the remaining selected list, and selected gold uses the same liftable-count split before shop charging (`js/cmd.js:22485-22820`, `js/cmd.js:28474-28503`, `js/cmd.js:28928-28954`).

Concrete gaps:

- Multi-object pickup now has priced menu rows, ordinary selected-object shop quotes before inventory lines, whole-container contents/contained-gold quote handling, and scare-scroll floor-style used-up quotes; remaining quote/display gaps are the full source `addtobill()` positioning across less ordinary transfer paths.
- Covered simple `FOOD(...)` pickup (`food ration`, `pancake`, `lembas wafer`, `cram ration`, `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`) and ordinary stackable non-food pickup now reject paid/unpaid mismatches and carry compatible same-price unpaid bill totals forward. The covered simple-food path also rejects actual BUC/state mismatches even when unknown, normalizes no-charge floor pickup before merging into paid inventory, copies a source object name onto an unnamed target, age-averages merged stacks, and uses C unit weights/costs where needed; tins, corpses, eggs, globs, meat rings, fortune cookies, royal jelly, meatballs, slime mold/custom fruit naming, and broader food-class merge cases remain outside this narrow slice.
- Bill ledger calls exist for ordinary shop pickup and compatible inventory merges. Ordinary pickup, container take-out, and food pickup merge preflight now require a same-shop target bill row before merging billable sources into unpaid targets, but this is not yet a full C `addtobill()`/`addinv()` merge invariant across every transfer path.
- Lift limits are not full C parity: starter pre-transfer slot/max-carry failure, artifact/fatal-corpse touch gates, floor Rider corpse revival, single-object and menu burden prompts, partial stack pickup, gold lift splitting, scare-monster scroll pickup state/dusting/type-call/used-up quote ordering, loadstone weight/slot exceptions, floor/container boulder lift/Sokoban/rock-throwing exceptions, priced pickup menu rows, ordinary pickup quote wording including whole-container contents pricing, and multi-pickup partial-success ordering exist for ordinary floor pickup and container take-out, but the full artifact bane/silver/life-saving matrix remains incomplete.
- Stolen value when carrying merchandise out of a shop is not tied to recursive bill/container state the way `pick_obj()` and `stolen_value()` are in C.

### 5. Drop Flow Only Partially Implements `sellobj()` Semantics

C drop unwields/unquivers as needed, removes from inventory, applies floor effects, places/stacks the object, and calls `sellobj()` when on a costly spot. `sellobj()` handles unpaid returns, deliberate sale offers, credit, no-charge state for dropped containers, and bill removal.

JS drop clones the inventory object to the floor, clears basic equipment flags, applies earth/ice effects, and prints a message (`js/cmd.js:33688-33774`). It now handles ordinary unpaid non-container returns, including the narrow split-stack residual bill branch, paid non-container sale/credit offers with declined-sale `no_charge`, ordinary dropped-container sale/no-sale state for saleable contents, contained gold, recursive no-charge marking, and contained bill-row cleanup, angry/hostile shopkeeper no-pay takeover, robbed-shop restock contribution handling, CANDLESHOP candelabrum special-stock refusal, generic no-sale/full-bill uninterested handling, C-ordered ordinary drop stacking after unpaid return or sale prompt resolution, ordinary gold `donate_gold()` drops with debt, loan, credit, and gold-specific robbed/angry handling, matching floor-gold `costly_gold()` charges when shop-floor gold is picked back up, same-shop paid projectile sale before stack merging, and thrown-gold projectile donation before stack merging. Seen-hole/trapdoor consumed drops now convert unpaid carried objects and unpaid contents to debt before migration and destroy fragile carried objects with the C muffled crash/splat message before queueing; shop-floor pile impact migration still charges stolen-value-style stock, contents, and gold before queueing without breakage, matching C `impact_drop()`. Ordinary whole-container pickup now runs a starter `addtobill()`/`picked_container()` path that recursively bills chargeable contents/contained gold, clears reclaimed `no_charge` containers and contents, and re-parents cloned carried contents. Remaining `sellobj()` gaps are complete recursive `subfrombill()` integration and less ordinary transfer paths beyond the same-shop paid/gold projectile landing branch.

Concrete gaps:

- Split-stack unpaid returns have starter `splitbill`/`subfrombill` helpers, partial inventory use preserves C's `bquan > quan` representation, and throw/fire projectile splits now create child bill rows, break top-level fragile projectiles before placement, place hard-landed containers before shop handling, break fragile contents through a `container_impact_dmg()`-style pass, return same-shop surviving contents, convert off-shop child rows to debt/robbed value, sell same-shop paid projectiles before stacking, donate thrown gold before stacking, and merge compatible unpaid floor bill rows. Hole/trapdoor migration now covers the narrow carried-unpaid and shop-floor-pile `ship_object()` debt path, including unpaid contents and recursive `no_charge` cleanup, and applies the carried-only fragile break gate before migration. Full recursive `subfrombill()`/`sellobj()` compatibility and less ordinary projectile, kicked, pick, and remaining migration ownership branches remain incomplete.
- Angry/hostile, robbed-shop, CANDLESHOP special-stock, and generic uninterested `sellobj()` branches are now represented for ordinary paid drops and dropped containers, including no-pay takeover/restock contributions, no-sale no-charge state, and bill-row cleanup; exact shared-helper integration across less ordinary paths remains incomplete.
- Ordinary dropped-container `sellobj()` now preserves C `dropped_container()` sale/no-sale state and recursively marks contained non-gold objects `no_charge` when the shopkeeper does not buy them; ordinary pickup clears that state through a starter `picked_container()` path, while non-ordinary container branches remain incomplete.
- Ordinary drop/projectile stacking now runs after `sellobj()` handling and preserves the main bill boundaries: paid and `no_charge` stacks do not merge, same-price unpaid stacks merge their bill rows, prompt-based paid sales stack only after accept/decline resolves, same-shop paid projectile sales finish before stack merging, and thrown gold donates before stack merging. Remaining stack parity gaps are the full C `mergable()` matrix around names/artifacts/eggs/corpses/tins and less ordinary kicked/pick/projectile ownership paths, including callers that deliberately request `dropz()` impact and migrated pile follower timing.

### 6. Container Loot Has Partial Shop Ownership

C container operations are deeply tied to shops. Putting an inventory object into a shop-floor container can sell it. Taking an object out of a shop-floor container bills it. Tipping a shop-floor container can bill, steal, or suppress price depending on source and target. Ice boxes, cursed bags of holding, bag of tricks, horn of plenty, traps, locked state, and weight all route through those same object/bill invariants.

JS has a substantial UI implementation for boxes, bags, ice boxes, take-out, put-in, stash, and tip (`js/cmd.js:19609-20658`, `js/cmd.js:38104-38899`). It also implements some magic bag, bag of tricks, and horn of plenty behavior. Ordinary take-out from a shop-floor container now goes through a starter `addtobill()`-style helper, prices from the source container's coordinates, treats raw `unpaid` as live only when backed by a bill row, clears stale field-only unpaid state before billing current shop price, splits live bill rows for partially lifted billed stacks, requires the source container to be a level-floor object, clears ordinary `no_charge` contents when they are taken back without billing, routes loose gold through C-shaped `costly_gold()` credit/debit/loan transitions, runs `out_container()`/`lift_object()`-ordered preflight so restricted/self-willed artifact blast damage and evasion, fatal barehanded cockatrice/chickatrice corpse touch, slot exhaustion, maximum-carry failures, burden prompts, declined burden prompts, overweight stack splits, loadstone weight/slot exceptions, and contained boulder Sokoban/rock-throwing exceptions happen before extraction, recursive billing, gold debt, or inventory insertion, preserves C's lack of immediate Rider revival for container take-out, and can merge stackable no-charge or same-price unpaid contents into compatible carried stacks without requiring a fresh inventory letter. Ordinary put-in now has starter `sellobj()`-style coverage for unpaid non-container returns, partial unpaid split rows, outside-shop debt preservation, loadstone let-go cursing/refusal, no-sale/full-bill uninterested handling with `no_charge` marking, accepted paid-object/container sale prompts with cash/credit handling, and loose-gold `donate_gold()` credit/debt handling, with container stacking guarded against unpaid/no-charge provenance loss (`js/mklev.js:4681-4701`). The older carried-bag put-in path now also routes partial unpaid stacks through the shared split helper, matching C `menu_loot()`/`splitobj()`/`splitbill()` ordering before insertion. Ordinary tip now bills each top-level object from an actual shop-floor source before floor or carried-target placement with the same ledger-first stale-field cleanup; loose gold tipped to the floor also runs the C `dropy()`/`sellobj()` donation offset, while loose gold tipped into a carried container only applies the `costly_gold()` charge. Floor bag-of-tricks `#tip` now uses a temporary live bill row for C's alternate emptying fee, removes that row afterward, and keeps zero-charge/no-charge cases debt-free while floor horns remain unselectable. It now also recurses into moved containers for contained contents/gold where the source C path uses `contained_cost()`, `contained_gold()`, `addtobill()`, or `sellobj()`, ordinary shop-floor magic-bag tip loss/put-in explosion paths now run C-ordered `sellobj()`/sale handling before put-in explosions, clear contained bill rows when only a paid trigger container's contents were unpaid, preserve used-up bill rows for destroyed bags/top-level triggers, and charge vanished merchandise while consuming shop credit. Tipping shop-floor nested trigger containers into carried magic bags now bills the moved outer container and recursive contents before the target explosion and preserves both as used-up rows. Shop-floor cursed magic-bag `#loot` opening runs a C `boh_loss()`-style vanished-content roll before the action menu and charges lost merchandise, vanished shop-floor magic-bag containers now use the C `OBJ_FREE` valuation split so nested coin and unbilled non-coin contents do not inflate debt while directly vanished gold and nested bill rows still count, carried `#apply` magic-bag loss now uses C's known/unknown sighted/blind disappearance messages and spends the immediate apply turn only when lost merchandise value is nonzero, narrow carried magic-bag held losses convert top-level unpaid vanished contents to debt only while the hero is currently in a shop; when off-shop they preserve affected bill rows as used-up entries, and nested unpaid contents in non-unpaid vanished containers also remain used-up rows, and magic-bag scatter breakage, consumed-on-hit objects, destructive floor-effect landings, trigger/target destruction, and scattered unpaid stack splits now preserve existing bill rows through used-up entries.

Concrete gaps:

- Shop-floor put-in now covers ordinary unpaid non-container returns, recursive moved-container contents/contained gold, accepted paid-object/container sale prompts, broader angry/robbed `sellobj()` shopkeeper-state handling, no-sale/full-bill uninterested handling with `no_charge` marking, loose-gold donation/credit, and C-ordered ordinary shop-floor magic-bag put-in explosion handling where `sellobj()`/sale resolution happens before the already-covered target-bag/trigger use-up preservation, vanished-content debt, scatter/useup, and destructive floor-effect billing; paid outer containers with only unpaid trigger contents now clear those content bill rows before explosion rather than preserving them as used-up. Remaining magic-bag source/target handling and shared-helper integration remain incomplete.
- Shop-floor take-out now covers ordinary objects, moved-container recursive contents/contained gold, stale field-only unpaid cleanup with live bill-row preservation and split-bill handling, loose top-level gold, pre-billing artifact blast/damage/evasion for restricted and self-willed artifacts, fatal barehanded touch-petrifying corpse removal, inventory-slot exhaustion, maximum-carry failure, burden prompts, partial stack lifting, loadstone lift exceptions, contained boulder lift exceptions, and stackable no-charge/same-price unpaid inventory merges, but artifact bane/silver/life-saving polish and less common inventory overflow/merge quirks remain incomplete.
- Tip now covers ordinary per-item shop billing, stale field-only unpaid cleanup with current-shop billing, recursive moved-container contents/contained gold, loose gold, floor bag-of-tricks temporary live-row alternate usage billing, ordinary shop-floor cursed magic-bag item loss including extracted-container contained-gold and unbilled nested-content exclusions, shop-floor nested trigger containers tipped into carried magic bags, narrow carried magic-bag held-loss billing with off-shop used-up preservation, and magic-bag scatter/useup preservation for breakage, consumed-on-hit objects, destructive floor-effect landings, and unpaid trigger/target destruction, but remaining non-ordinary sources/targets, full `obfree()` preservation, and container-aware `stolen_value()` debt naming still do not fully match C `tipcontainer()`/`stolen_value()`.
- Loot reachability is simplified. C checks water/lava access, limbs/free hand, multiple containers, confusion reverse-loot, blind cockatrice touch, saddles, and directional cases (`pickup.c:2022-2345`).
- Trap handling for tip/loot is placeholder-level in JS (`js/cmd.js:17388-17394`) compared with C `chest_trap()` and `use_container()` effects.
- Take-out has a starter C `out_container()`/`lift_object()`-ordered preflight for restricted/self-willed artifact blast damage and evasion, fatal barehanded touch-petrifying corpse removal, slots, maximum-carry failure, burden prompts, partial stack lifting, loadstone lift exceptions, and boulder Sokoban/rock-throwing exceptions; artifact bane/silver/life-saving polish and uncommon inventory overflow/merge paths still need follow-up.

### 7. Shop Billing Is Still Transitional

C uses shopkeeper bill entries with explicit invariants: an object is unpaid iff it is on a bill, except used-up bill entries. The system supports split/merge, contained unpaid objects, hidden containers, used-up items, itemized payment, partial usage fees, price quote learning, `no_charge`, stolen value, and alteration billing.

JS uses starter bill entries plus object fields (`unpaid`, `unpaidPrice`), loose shopkeeper counters/debit, and a transitional `_usedUpShopBills` list (`js/cmd.js:14723-14907`, `js/cmd.js:16539-16554`, `js/cmd.js:22569-22762`). This supports visible unpaid suffixes, ordinary pickup/drop billing, compatible pickup stack merges with same-shop bill-row proof for unpaid targets, narrow split-stack unpaid returns, carried-bag partial unpaid put-in splitting, shop-floor container take-out/tip stale-field cleanup with current-shop billing and live split-bill preservation, partial inventory-use debt display, starter C `check_unpaid_usage()` debit coverage for charged-object partial use including wand zaps with a current-shop bill-row authority check and current-price fee base, wand engraving with post-spend billing, no-effect wand `#apply` break billing with pre-break usage fee plus `COST_DSTROY` dummy used-up preservation, camera flashes, can-of-grease applications and `#tip` spillage with C-shaped billing order, floor bag-of-tricks `#tip` alternate emptying through a temporary live bill row, can-of-grease target prompts/inaccessible worn-gear checks, normal lamp lighting, carried fire ignition of unpaid light sources in shops, magic-lamp `#rub` unusual-use billing before oil-lamp conversion/djinni handling, potion-of-oil lighting with Fuel Tax and full-potion used-up bill preservation, successful spellbook study completion, magic-marker writing with C's flat pre-ink usage fee, tinning-kit corpse applications with homemade tin creation and charge/no-charge billing, crystal-ball gazing with pre-decrement charge billing and no-bill cancel/blind paths, shared musical instrument handling for ordinary no-charge instruments, magic flute/harp improvisation with prompt cancel and confused no-charge fallback, frost/fire horn direction zaps with pre-direction billing and no-identify cancel handling, drum-of-earthquake charges, failed spellbook read destruction, carried fire destruction for final-copy inventory objects, ordinary carried-drop and tipped-shop-good terrain destruction, non-destroying shop box lock-break dummy billing, cream-pie `COST_SPLAT` dummy billing with live residual stack preservation, cursed charging `COST_UNCHRG`/`COST_DECHNT` dummy billing for unpaid carried charged objects, remove-curse `COST_UNCURS` dummy billing for cursed unpaid water, cursed enchant weapon `COST_DEGRD`/`COST_DECHNT` dummy billing for unpaid wielded weapons, cursed enchant armor `COST_DECHNT` dummy billing for negative enchantment deltas, cursed wand backfire `useupall()` billing for ordinary and wishing wand branches, field-only stale unpaid rejection for used-up row synthesis, recursive debt/loss, carried magic-bag apply loss message/turn-cost gating, and selected-shop usage/payment, hero-caused floor fire `useupf()` billing, throw/fire child bill rows with off-shop debt conversion, top-level hard-landing projectile breakage before placement, hard-landing projectile container-content impact before shop return/debt, same-shop paid projectile sale before stacking, thrown-gold projectile donation before stacking, container-aware projectile `stolen_value()` debt conversion for unpaid contents, hole/trapdoor `ship_object()` debt conversion for unpaid carried objects and shop-floor piles before migration plus carried-only fragile breakage before queueing, bill-aware floor stack merging, selected magic-bag `obfree()`-style used-up preservation including off-shop carried held/blast losses and target-explosion source tail preservation, ledger-first payment for split and used-up bill rows, carried-container and floor-container itemized `#pay` aggregation including partly-used contained-item rejection/order and equal-price `bidx` tie-breaks, queued itemized payment ordering/partial-affordability stops, itemized pay-menu used-up/unpaid section headings, debit-before-itemized `#pay` settlement, itemized pre-menu no-funds/cheapest-row affordability refusals, traditional `Itemized billing? [ynq m]` choice including per-item prompts and `m` prefix inversion, C-shaped `#pay` target scanning, `Pay whom?` validation, and getpos-style monster/object cursor cycling for live/resident/visible shopkeepers, C-style robbed-only `#pay` compensation, and angry-not-robbed `#pay` appeasement, but does not yet preserve C's full ownership and bill-entry semantics.

Concrete gaps:

- Ledger coverage is partial: starter `bill` entries and narrow split/subtract/debt helpers exist, but there is no full source-equivalent `onbill()`, recursive `subfrombill()`, general container-aware `stolen_value()` outside the covered projectile/magic-bag/hole paths, or general `obfree()`; generic floor-effect deletion outside the explicit ordinary carried-drop, tipped-shop-good, and scatter opt-in paths can still lose bill state, aside from the source-confirmed generic floor fire catch-light case where C does not add billing.
- `billct` is tied to the starter ledger in newer paths; pickup/container/food unpaid merge preflights, charged usage, selected-shop payment, recursive debt/loss helpers, and used-up-row helpers now reject stale unpaid targets without matching bill rows while shop-floor stock loss still prices from current shop cost. It is still not authoritative everywhere because full `onbill()`/`obfree()`/`stolen_value()` ownership routing is not centralized.
- `collectPayableShopDebts()` now enumerates selected shopkeeper bill rows first, splits `bquan > quan` rows into used-up/intact portions, keeps partly-used live residuals inside carried or shop-floor containers folded into the container row, aggregates other ledger-backed unpaid contents inside carried or shop-floor outer containers, sorts payable rows like C itemized bills by used-up state then descending price, and suppresses field-only unpaid fallbacks for a selected shopkeeper (`js/cmd.js:22592-22739`, `js/cmd.js:22830-22941`, `js/cmd.js:43660-43663`).
- Payment now applies shop credit before cash for selected ledger rows, pays shopkeeper debit before itemized bill rows, shrinks partly used bills before intact rows can clear them, aggregates ledger-backed carried-container and floor-container contents under one payable line, rejects a partly-used contained live residual until the used-up row is selected or already paid, clears every constituent bill row, processes queued selected rows in C itemized order with `bidx` tie-breaks for equal-price rows, stops after the first selected row that has become unaffordable while preserving earlier purchases and reporting C's item-specific, credit-aware shortfall wording, separates used-up and unpaid rows in the itemized menu, refuses to open itemized payment when the hero has no gold/credit or cannot afford the cheapest row, supports C's traditional `Itemized billing? [ynq m]` choice with full-bill, menu, quit, and per-item prompt paths, scans live shopkeepers before payment, skips dead shopkeepers, requires `shopkeeperInHisShop()` for resident payment, refuses visible distant nonresidents before mutating debt, preserves the blind unique-adjacent exception, opens a `Pay whom?` cursor for multiple visible candidates, cycles monster and object targets with C's hero wrap marker plus distance/y/x ordering, validates cancel/self/unseen/empty/non-shopkeeper/too-far targets before mutation, handles robbed-only `#pay` compensation with C's resident/angry branch split, and handles angry-not-robbed `#pay` appeasement with C's open-gold threshold, credit offset, and same-customer RNG failure.
- `get_cost()` parity is incomplete: JS has base tables, unknown-name surcharge, enchantment surcharge, charisma adjustment, and pricing units (`js/cmd.js:18486-18713`), but not full C role/status/shopkeeper anger/tourist/dunce/artifact/contained/no-charge/price-quote side effects.
- `check_unpaid_usage()` is still partial. JS now has a reusable debit-only helper with C-style charged-object fee branches and callers for bag/horn use, wand zaps, wand engraving with post-spend billing, no-effect wand `#apply` break pre-destroy usage, camera use, can-of-grease apply and `#tip` use, normal lamp and potion-of-oil apply lighting, carried fire ignition of unpaid lamps/light sources, magic-lamp `#rub` djinni release, successful spellbook study completion, magic-marker writing, tinning-kit corpse applications, crystal-ball gazing, magic flute/harp improvisation, frost/fire horn direction zaps, alternate emptying, and drum-of-earthquake charges; the ordinary musical-instrument shell shares the same C gates while correctly avoiding usage billing for no-charge mundane instruments. Other less-common charge-consuming callers still need to route through the appropriate C billing paths.
- Generic `costly_alteration()` is still partial. JS now covers unpaid food `COST_BITE`, narrow tin `COST_OPEN/COST_DSTROY`, no-effect wand `COST_DSTROY` break billing, non-destroying box-lock `COST_BRKLCK`, cream-pie `COST_SPLAT`, cursed-charging `COST_UNCHRG`/`COST_DECHNT`, remove-curse water `COST_UNCURS`, cursed enchant weapon `COST_DEGRD`/`COST_DECHNT`, cursed enchant armor `COST_DECHNT`, confused proof-stripping `COST_DEGRD`, and blessed destroy-armor erosion carried-object paths, plus local billing for buried merchandise and horn-created objects, but not the shared open/destroy/cancel/degrade billing hook used across C.
- Used-up unpaid items are not centralized. C `useup()`/`obfree()` preserve bills for consumed unpaid objects; JS only remembers selected corpse rot/glob shrink, projectile, payment, magic-bag destruction, failed spellbook read, carried fire destruction and carried fire ignition of unpaid light sources, ordinary carried-drop and tipped-shop-good terrain destruction, cream-pie splats, cursed wand backfires, hard-landing top-level fragile projectiles and fragile container contents, hero-caused floor fire, and scare-scroll pickup dust paths with floor-style billing quotes.

## Recommended Implementation Slices

1. Build the shop ledger foundation first.
   - Add a C-shaped bill model keyed by shopkeeper, object identity, quantity, price, and used-up status.
   - Continue the started split/subtract helpers into source-equivalent `onbill`, full `addtobill`, recursive `subfrombill`, `unpaid_cost`, `contained_cost`, shared `sellobj` helper integration, remaining add-inventory/no-charge cleanup outside ordinary shop pickup, remaining less-common `check_unpaid_usage` callers, and generic `costly_alteration` coverage beyond the covered `COST_BITE`, `COST_OPEN`/`COST_DSTROY`, no-effect wand `COST_DSTROY`, `COST_BRKLCK`, `COST_SPLAT`, `COST_UNCURS`, cursed-charging `COST_UNCHRG`/`COST_DECHNT`, cursed enchant weapon `COST_DEGRD`/`COST_DECHNT`, cursed enchant armor `COST_DECHNT`, confused proof-stripping `COST_DEGRD`, and blessed destroy-armor erosion carried-object slices; ordinary `dropped_container`, ordinary pickup `picked_container`, angry/robbed shopkeeper-state, and special-stock/uninterested branches are covered for direct ordinary paths but still need shared-helper integration.
   - Keep `unpaid`/`unpaidPrice` as derived display state, not the source of truth.

2. Port `touchfood()` and the victual core.
   - Model `piece`, `usedtime`, `reqtime`, `nmod`, `canchoke`, and `fullwarn`.
   - Keep first-touch `COST_BITE` billing wired through the shop ledger, then consume `oeaten` per tick and route finished/used-up unpaid food through central use-up handling.
   - Preserve interruption/resume behavior before expanding more corpse effects.

3. Port tins on top of the ledger and victual/use-up helpers.
   - Finish tin billing beyond the narrow `COST_OPEN/COST_DSTROY` and basic tinning-kit paths: exact C split/use-up integration across all effects, tin variety corrections, metallivore nutrition, special tinning corpse hazards, and full corpse pre/post effect routing for tin contents.
   - Keep the current tin opener timing as a base, but drive billing and effects from C-shaped helpers.

4. Port pickup/drop around shop and stack invariants.
   - Move pickup through `carry_count()`/`lift_object()` style preflight and `pick_obj()` style bill-before-inventory insertion.
   - Continue moving drop through `dropx()`/`dropy()`/`dropz()` style removal, floor effects, complete `sellobj()`, and bill-aware stack merging.
   - Broaden projectile shop resolution beyond same-shop paid/gold landing into kicking/ship/pick special cases and remaining `check_shop_obj()`/`sellobj()` branches, then continue into container flows.

5. Port container operations after pickup/drop and shop billing.
   - Rework remaining put-in, take-out, and tip paths to call ledger-aware `sellobj`, `addtobill`, `subfrombill`, stolen-value, and no-charge helpers.
   - Add C reachability, trap, confusion reverse-loot, blind cockatrice, weight/slot, and target-container checks.
   - Keep current menu plumbing where it matches C prompts, but make object movement flow through C-equivalent transfer helpers.

6. Fill the corpse/non-corpse effect matrix last.
   - Once victual and shop use-up are stable, port `cprefx()`, `eatcorpse()`, `cpostfx()`, `fprefx()`, and `fpostfx()` in source-sized groups.
   - Prefer small slices by source section, with visible behavior checks for each special corpse/food class rather than broad hidden-suite assumptions.

## Highest-Risk Current Behaviors

- Tin paths outside the narrow open/trap-destroy and basic tinning-kit billing slices plus generic non-bite alterations can still avoid C billing because full `costly_tin()` integration and shared non-bite `costly_alteration()` coverage beyond the covered no-effect wand breaking, box-lock, cream-pie, cursed-charging, remove-curse water, cursed enchant weapon, cursed enchant armor, and confused proof-stripping billing remains incomplete.
- Multi-pickup now has ordinary priced menu rows and stack-aware shop pickup quotes, plus whole-container contents/contained-gold quote handling, selected-object partial-success ordering, burden prompts, gold lift preflight, and scare-scroll state/dust/type-call/used-up quote ordering. Remaining visible pickup risk is less ordinary `addtobill()` quote positioning outside whole-container pickup. Carried-bag partial unpaid put-in now preserves split child bill rows, but split-stack object identity can still lose unpaid/shop state in non-projectile delete, other container, and generic merge paths.
- Container-moving objects in shops and complex drops still do not exercise full C `sellobj()`/`addtobill()`/`subfrombill()` paths outside ordinary dropped-container, angry/robbed and special-stock/uninterested `sellobj()` branches, ordinary gold drop/pickup paths, ordinary shop-floor magic-bag tip/loot loss with extracted-container nested-content valuation, paid-container put-in explosion content-bill cleanup, shop-floor nested-trigger tip explosions, ordinary carried-drop and tipped-shop-good terrain destruction, hard-landing projectile top-level/fragile-content impact, same-shop paid projectile sale and thrown-gold donation before stacking, hole/trapdoor `ship_object()` debt and carried fragile breakage before migration, narrow carried magic-bag held-loss billing with off-shop used-up preservation and carried apply turn-cost gating, and magic-bag scatter/useup plus destructive floor-effect preservation; generic merge/destruction paths remain high-risk.
- Carried/non-ordinary loss sources/targets and destroyed recursive contents can still miss full C `obfree()` preservation and `stolen_value()` debt naming; carried in-shop held/blast debt conversion, off-shop held/blast used-up preservation, container-aware projectile debt conversion plus hard-landing top-level/fragile-content debt, failed spellbook reads, carried fire destruction, carried fire ignition, ordinary carried-drop and tipped-shop-good terrain destruction, cursed wand backfires, hero-caused floor fire, and selected scatter/useup preservation are covered through local helpers, not a general C-shaped subsystem.
- Payment is only partly backed by authoritative bill rows; ledger-backed carried and floor container contents now aggregate for `#pay`, including debit-before-itemized settlement, partly-used contained-item rejection/order, queued selected-row ordering/partial-affordability stops with item-specific shortfall wording, used-up/unpaid menu headings, traditional itemized billing prompts, pre-menu no-funds/cheapest-row affordability refusals, live/resident/visible shopkeeper target scanning, blind unique-adjacent target selection, selected-shop stale-field fallback suppression, `Pay whom?` cursor validation and monster/object cycling before mutation, and angry-not-robbed appeasement; the remaining risk is full ownership rather than this prompt branch.
