# C Parity Audit 09: Sub-Agent Findings 2026-05-26

This note preserves the latest parallel source audits. These are implementation candidates only; they are derived from upstream C and current JS, not from private-test inference.

## Completed From This Round

- Object/wish no-match handling: C `makewish()` retries failed `readobjnam()` descriptions and calls `readobjnam(NULL, NULL)` after `MAXWISHTRY` failures (`nethack-c/upstream/src/zap.c:6313-6366`). JS now removes the arbitrary named-weapon fallback, keeps invalid descriptions out of wish conduct, retries the prompt, and randomizes on the fifth bad description.
- Carried fire-ignition shop billing: C `catch_lit()` charges carried unpaid ignitable objects in shops through `check_unpaid()`, prints the additional-cost message, and preserves the bill row with `bill_dummy_object()` before `begin_burn()` (`nethack-c/upstream/src/apply.c:1577-1620`). JS now charges unpaid carried lamps/light sources that catch fire in a shop, records the used-up bill row, and leaves off-shop ignition on the live bill row.
- Wish quantity and requested `spe`: C `readobjnam()` only applies wished counts to mergeable/multigen objects and constrains requested `spe` by class (`nethack-c/upstream/src/objnam.c:5037-5189`, `nethack-c/upstream/src/objnam.c:5255-5268`). JS now keeps non-mergeable wishes such as boots, wands, magic markers, and spellbooks at quantity one, allows covered mergeable/count-capped objects, and applies class-shaped `spe` limits for weapons, armor, weapon-tools, charged rings, wands, crystal balls, and non-`spe` objects.
- Floor fire ownership: C beam floor destruction calls `useupf()` only for hero-caused damage, while generic `fire_damage()`/`fire_damage_chain()` can call `catch_lit()` without adding floor-item shop billing (`nethack-c/upstream/src/zap.c:4598-4661`, `nethack-c/upstream/src/trap.c:4455-4565`). JS now keeps generic floor catch-light unbilled and routes monster red dragon breath through non-hero floor-fire ownership.
- Wand engraving usage fees: C `doengrave()` spends a wand charge before the text prompt, then calls `check_unpaid()` using the post-spend `spe` (`nethack-c/upstream/src/engrave.c:784-797`, `nethack-c/upstream/src/shk.c:5688-5742`). JS now has an engraving-specific wand spend path, bills unpaid use from the remaining charges, skips last-charge usage fees, and preserves cursed backfire wands as used-up bill rows.
- Wish charge suffix parsing: C `readobjnam_parse_charges()` strips the last parenthetical before object lookup, accepts `(lit)`, `(n)`, and `(r:n)`, preserves valid trailing text, strips invalid suffix tails, caps `spe`/recharge values, and only stores `recharged` on wands (`nethack-c/upstream/src/objnam.c:4177-4237`, `nethack-c/upstream/src/objnam.c:5290`). JS now uses one pre-lookup suffix parser for wands and tools, preserves `named` text after valid suffixes, treats signed suffixes as invalid stripping, ignores tool recharge counts, and displays known charges with the real recharge count.
- Floor-container itemized `#pay`: C `make_itemized_bill()` walks bill rows back to live objects, collapses contained objects under their outermost container, and `buy_container()` clears each contained bill row during payment (`nethack-c/upstream/src/shk.c:1542-1660`, `nethack-c/upstream/src/shk.c:2304-2410`). JS now groups ledger-backed unpaid contents inside shop-floor containers the same way as carried containers, pays them through the existing container payment row, clears every constituent ledger row, and leaves the contents inside the floor container.
- Partly-used contained-item `#pay`: C emits a used-up row for `bquan - quan`, keeps the live residual under the outermost container row, rejects buying the container row while the used-up portion is unpaid, and pays selected itemized rows in sorted bill order (`nethack-c/upstream/src/shk.c:1584-1640`, `nethack-c/upstream/src/shk.c:2119-2192`, `nethack-c/upstream/src/shk.c:2336-2450`). JS now keeps the same split for carried and floor containers, reports a blocked container payment when selected alone, and processes selected used-up rows before the container payment so both-selected and stepwise payments clear the live bill.
- Queued itemized `#pay` order and affordability: C sorts itemized rows with used-up entries first and descending cost within each section, menu selections only mark `queuedpay`, and queued payment walks that sorted list, stopping when a later selected row cannot be bought while earlier purchases stay committed (`nethack-c/upstream/src/shk.c:1495-1513`, `nethack-c/upstream/src/shk.c:1668-1736`, `nethack-c/upstream/src/shk.c:2045-2165`, `nethack-c/upstream/src/shk.c:2277-2286`). JS now uses the same narrow ordering for displayed/payable rows and processes selected payments sequentially instead of rejecting the whole selected set upfront.
- Itemized `#pay` menu sections: C shows a `Used up item(s):` heading when used-up rows start the itemized bill and an `Unpaid item(s):` heading when the sorted list transitions to unpaid rows (`nethack-c/upstream/src/shk.c:1695-1708`). JS now mirrors those section headings in the pay overlay while keeping selection letters stable.
- Itemized `#pay` pre-menu affordability: C refuses itemized billing before opening the menu when the hero has no gold/credit or cannot afford the cheapest bill row (`nethack-c/upstream/src/shk.c:2064-2082`). JS now blocks the pay overlay in those two cases and leaves bill rows untouched.
- Debit-before-itemized `#pay`: C settles `eshkp->debit` before building itemized bills and never displays shop debt as a selectable menu row (`nethack-c/upstream/src/shk.c:1951-2004`). JS now pays or refuses debit before collecting item rows for the pay overlay.
- Selected-row itemized `#pay` shortfall wording: C `dopayobj()` calls `insufficient_funds()` after each selected item, producing `You don't have gold[ or credit] enough to pay for <item>.` and returning without the thank-you when a queued later row cannot be bought (`nethack-c/upstream/src/shk.c:2149`, `nethack-c/upstream/src/shk.c:2283`, `nethack-c/upstream/src/shk.c:2472`). JS now uses that item-specific, credit-aware message for first selected-row failure and stopped-short queued payment.
- Container-aware projectile `stolen_value()`: C `check_shop_obj()` treats containers with unpaid contents as unpaid, returns their bill rows when they land in the owning shop, and converts contained bill rows to debt when they leave (`nethack-c/upstream/src/dothrow.c:1181-1212`, `nethack-c/upstream/src/shk.c:3712-3828`). JS now resolves projectile landing through unpaid contents as well as the top object.
- Nonresident distance `#pay`: C only allows paying at a distance from inside a tended shop; a lone visible shopkeeper elsewhere must be adjacent or payment is refused before debit/item state changes (`nethack-c/upstream/src/shk.c:1792-1804`, `nethack-c/upstream/src/shk.c:1951`). JS now removes the lone-shopkeeper fallback outside the resident/adjacent paths.
- `#pay` target scanner: C `dopay()` scans live shopkeepers with `next_shkp()`, prioritizes a unique adjacent target including the order-dependent angry-adjacent quirk, counts spotted shopkeepers, and only treats a shopkeeper as resident when `inhishop()` matches the hero's shop room (`nethack-c/upstream/src/shk.c:214-221`, `nethack-c/upstream/src/shk.c:1757-1810`). JS now skips dead shopkeepers, gates resident payment on `shopkeeperInHisShop()`, refuses a single visible distant nonresident before mutation, defers multiple visible candidates with `Pay whom?`, and routes peaceful robbed nonresidents through the "asked for" gold branch with C-style subject pronouns.
- `Pay whom?` `getpos()` cycling: C `getpos()` includes the hero as a wrap marker, accepts all pick keys for `dopay()`, and cycles monster/object targets with distance/y/x ordering while final validity remains in `dopay()` (`nethack-c/upstream/src/shk.c:1811-1854`, `nethack-c/upstream/src/getpos.c:311-328`, `nethack-c/upstream/src/getpos.c:771-1165`, `nethack-c/upstream/src/cmd.c:3168-3178`). JS now supports `@`, `m/M`, and `o/O` in `Pay whom?`, including non-shopkeeper monster and object targets that validation rejects without mutation.
- `stolen_value()` residual preservation: C computes lost value from the original bill row but removes it through `sub_one_frombill()`, preserving a used-up residual when `bquan > quan` (`nethack-c/upstream/src/shk.c:3753-3785`, `nethack-c/upstream/src/shk.c:3660-3671`). JS now uses the same subtract path for projectile and magic-bag debt conversion instead of deleting bill rows directly.
- Bag-of-tricks tip target ordering: C applies an unknown bag-of-tricks target before source lock/trap/empty checks, billing normal unpaid use with `tipping == FALSE` (`nethack-c/upstream/src/pickup.c:3908-3961`, `nethack-c/upstream/src/makemon.c:2575`, `nethack-c/upstream/src/shk.c:5656-5739`). JS now bills and spends the target bag charge before locked-source refusal can short-circuit.
- Can-of-grease `#tip` billing: C allows non-container inventory choices for `#tip`, spills grease, then consumes one can charge through `consume_obj_charge(..., TRUE)` so unpaid usage is billed after the spill message (`nethack-c/upstream/src/pickup.c:3481-3654`, `nethack-c/upstream/src/pickup.c:3658-3663`, `nethack-c/upstream/src/invent.c:1337-1344`). JS now includes spillage objects in carried `#tip` selection and bills an unpaid can of grease after reporting the spill.
- Floor special-source commands: C floor `#tip` only scans `Is_container()` objects and reaches horn-of-plenty tipping through inventory `getobj()`, while floor `#loot` on a bag of tricks runs the bite/identify path directly without shop usage or cursed magic-bag loss billing (`nethack-c/upstream/src/pickup.c:3479-3492`, `nethack-c/upstream/src/pickup.c:3585-3631`, `nethack-c/upstream/src/pickup.c:2150-2159`). JS now excludes floor horns from `#tip` source selection and handles floor bag-of-tricks `#loot` as a turn-cost bite that leaves shop debt and contained merchandise untouched.
- Partly eaten food lost-merchandise valuation: C `billable()` excludes partly eaten food unless it is already on a shop bill, and `stolen_value()` then either falls back to that bill row or returns zero (`nethack-c/upstream/src/shk.c:3451-3472`, `nethack-c/upstream/src/shk.c:3712-3792`, `nethack-c/upstream/src/shk.c:4335-4336`). JS now prices unbilled partly eaten food at zero while preserving existing bill-row debt conversion for magic-bag loss and related shop valuation.
- Extracted lost-container magic-bag valuation: C `mbag_item_gone()` extracts a vanished shop-floor magic-bag item before `stolen_value()`, so `stolen_container()` treats the lost container as inventory-like (`OBJ_FREE`) and charges nested bill rows but not unbilled nested non-gold contents; contained gold is also skipped for inventory-like containers while directly vanished gold still counts (`nethack-c/upstream/src/pickup.c:2803-2820`, `nethack-c/upstream/src/pickup.c:3757-3766`, `nethack-c/upstream/src/shk.c:3713-3749`, `nethack-c/upstream/src/shk.c:3803-3810`). JS now applies that inventory-like nested-content valuation to cursed shop-floor magic-bag loss for floor tipping, floor `#loot`/action-menu loss, and tipped-into-container paths, so vanished containers charge their own price and nested bill rows while ignoring unbilled nested objects and contained coins.
- Tip-into-magic-bag target explosion tail preservation: C stops the tipping loop after a target magic-bag explosion by setting `nobj = 0`, leaving later source contents in the source container (`nethack-c/upstream/src/pickup.c:3757-3801`). JS now removes only processed source items instead of clearing the whole source after such an explosion.
- Carried cursed magic-bag apply loss: C `boh_loss()` returns only lost shop value, `mbag_item_gone()` chooses the vanished/disappear message from `item->dknown` and blindness, and carried `use_container()` spends `ECMD_TIME` only when lost merchandise value is nonzero (`nethack-c/upstream/src/pickup.c:2536-2552`, `nethack-c/upstream/src/pickup.c:2808-2818`, `nethack-c/upstream/src/pickup.c:3030-3034`). JS now uses the same known/unknown sighted/blind message split and keeps carried `#apply` bag opening at zero immediate turn cost when cursed-bag contents vanish without shop debt.
- Cursed charging alteration billing: Cursed recharging calls `costly_alteration()` before charge/enchantment mutation for `stripspe()` targets, crystal balls, and negatively adjusted charged rings (`nethack-c/upstream/src/read.c:651-663`, `nethack-c/upstream/src/read.c:770-821`, `nethack-c/upstream/src/read.c:857-930`, `nethack-c/upstream/src/read.c:959-982`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now preserves the pre-altered unpaid carried item as a dummy used-up bill row before clearing charges, fuel, or ring enchantment.
- Equal-price itemized `#pay` tie-breaks: C sorts itemized bill rows by used-up state, descending cost, then `bidx`; container-content rows for hero-owned outer containers use `bidx == -1` and therefore sort before equal-price bill-indexed rows (`nethack-c/upstream/src/shk.c:1495-1517`, `nethack-c/upstream/src/shk.c:1567-1661`, `nethack-c/upstream/src/shk.c:2119-2143`). JS now carries a C-style sort bill index on payable rows and container aggregates so equal-price payment display and queued payment follow the same order.
- Hard-landing projectile container impact: C places thrown containers, runs `container_impact_dmg()` for glass/egg contents on hard landings, then performs `check_shop_obj()` return/debt handling and only stacks afterward (`nethack-c/upstream/src/dothrow.c:1780-1838`, `nethack-c/upstream/src/dokick.c:409-485`, `nethack-c/upstream/src/shk.c:3692-3828`). JS now places fired/thrown containers before shop landing resolution, breaks fragile contents using the origin shop square for billing, clears `cknown`, and converts broken unpaid contents to debt before surviving contents return or leave.
- Top-level hard-landing projectile breakage: C runs `breaktest()`/`breakobj()` for fragile thrown objects before floor placement, container-content impact, `check_shop_obj()` return/debt, and stacking (`nethack-c/upstream/src/dothrow.c:1780-1836`, `nethack-c/upstream/src/dothrow.c:1181-1211`, `nethack-c/upstream/src/dothrow.c:2480-2582`). JS now consumes the hard-landing break RNG in the projectile landing path, destroys breakable top-level projectiles before placement, converts unpaid values to debt with broken-object `no_charge`, and skips same-shop return or floor stacking for destroyed objects.
- Same-shop paid projectile and thrown-gold landing sale: C calls `sellobj()` for same-shop paid non-broken projectiles after placement/impact and before stacking, except on the shopkeeper's current square, and thrown gold landing in the hero's shop donates through the same sale path before stacking (`nethack-c/upstream/src/dothrow.c:1181-1214`, `nethack-c/upstream/src/dothrow.c:1780-1838`, `nethack-c/upstream/src/dothrow.c:2656-2730`, `nethack-c/upstream/src/shk.c:3874-3910`, `nethack-c/upstream/src/shk.c:3927-4194`). JS now auto-accepts same-shop paid projectile sales through the existing paid-drop sale calculation, preserves the shopkeeper-square exclusion, donates thrown gold before floor stacking, and keeps command-level thrown gold synchronized with the hero's wallet state.
- Carried food-ration victual runtime: C `touchfood()` splits/stores a food ration, bills `COST_BITE` once, initializes `oeaten`, gives the first `nmod` bite at command start, continues later bites through `eatfood()`, and removes carried food only in `done_eating()` (`nethack-c/upstream/src/eat.c:360-372`, `nethack-c/upstream/src/eat.c:519-538`, `nethack-c/upstream/src/eat.c:2020-2073`, `nethack-c/upstream/src/eat.c:3056-3074`, `nethack-c/upstream/src/eat.c:3808-3875`). JS now routes exact carried non-rotten food ration through the first-bite helper, grants 160 nutrition immediately, leaves `oeaten=640`, stores carried eating state for later ticks, removes the item at finish, and preserves stack splitting plus used-up bill rows.
- Delayed carried ordinary victual expansion: C `FOOD(...)` rows give pancake delay 2/nutrition 200, lembas wafer delay 2/nutrition 800, cram ration delay 3/nutrition 600, and food ration delay 5/nutrition 800; `doeat()` derives `reqtime=rounddiv(delay * oeaten, nutrition)` and `nmod=-(oeaten / reqtime)`, while `adj_victual_nutrition()` adjusts hunger for elf/orc lembas and dwarf cram without changing the base `oeaten` bite (`nethack-c/upstream/include/objects.h:1033`, `nethack-c/upstream/include/objects.h:1105-1110`, `nethack-c/upstream/src/eat.c:345-357`, `nethack-c/upstream/src/eat.c:3026-3074`). JS now routes carried non-rotten pancake, lembas wafer, and cram ration through the same delayed-victual helper, preserves C first-bite messages, tracks adjusted hunger separately from base `oeaten` consumption, marks pancake as unvegan, and tests unpaid-stack first-bite billing for those rows.
- Cursed/rotten carried delayed victuals: C `nonrotting_food()` exempts only lembas wafer and cram ration from age rot while still treating cursed food as rotten; `doeat()` touches carried food first, then checks cursed/age-rotten state, calls `rottenfood()`, halves `oeaten` with `consume_oeaten(otmp, 1)`, and either starts delayed eating or leaves a halved `orotten` item if rotten sleep occurs (`nethack-c/upstream/src/eat.c:63`, `nethack-c/upstream/src/eat.c:3027-3036`, `nethack-c/upstream/src/eat.c:1811`, `nethack-c/upstream/src/eat.c:3056-3074`, `nethack-c/upstream/src/eat.c:3136-3150`). JS now lets carried delayed food enter that rotten branch, preserves the `Blecch!  Rotten food!` first-bite message and possible follow-up effects, recomputes delayed bites after halving, keeps lembas/cram age-nonrotting unless cursed, and tests rotten stack billing plus sleep-leftover prompts.
- Floor delayed ordinary victuals: C `doeat()` selects floor food through `floorfood("eat", 0)`, then sends floor and carried objects through the same `touchfood()`/victual path; floor stacks keep the original object as the touched unit and split the remainder, floor shop bites use `costly_alteration(..., COST_BITE)`/dummy used-up bill rows, and `done_eating()` removes the floor meal with `useupf()` (`nethack-c/upstream/src/eat.c:2829`, `nethack-c/upstream/src/eat.c:360-372`, `nethack-c/upstream/src/eat.c:3026-3074`, `nethack-c/upstream/src/eat.c:544-567`, `nethack-c/upstream/src/mkobj.c:752-826`, `nethack-c/upstream/src/shk.c:3346-3348`). JS now routes floor `food ration`, `pancake`, `lembas wafer`, and `cram ration` through the same delayed-victual helper after floor tins/corpses/globs/eggs/royal jelly, applies first and later bites to the floor object, removes only the eaten unit at finish, preserves residual floor stacks and used-up shop billing, and covers cursed/age-rotten first bites, rotten sleep leftovers, and lembas/cram race nutrition.
- Delay-one apple and fortune-cookie victuals: C still routes `APPLE` and `FORTUNE_COOKIE` through `touchfood()` and `start_eating()`, but their `oc_delay == 1` means the first bite immediately calls `done_eating(FALSE)` with no multi-turn occupation or finish message; fortune cookies never enter the rotten branch and run `outrumor(bcsign(cookie), BY_COOKIE)`, while consumed cursed apples run the Snow White sleep post-effect in `fpostfx()` (`nethack-c/upstream/include/objects.h:1080`, `nethack-c/upstream/include/objects.h:1103`, `nethack-c/upstream/src/eat.c:2020-2073`, `nethack-c/upstream/src/eat.c:2522-2528`, `nethack-c/upstream/src/eat.c:2582-2595`, `nethack-c/upstream/src/eat.c:3026-3036`, `nethack-c/upstream/src/rumors.c:529-565`). JS now runs carried and floor apple/fortune cookie through the immediate victual finish path, preserving first-bite floor/carried stack billing, one-turn use-up, floor residual stacks, fortune-cookie paper/rumor `--More--` flow including BUC truth and blind unreadable text, fortune-cookie unvegan conduct, and cursed-apple rotten-plus-sleep behavior when rotten sleep did not abort the meal.
- Ordinary victual interruption/resume: C runs `eatfood()` during the occupation, then `stop_occupation()`/`reset_eat()` on a nearby threatening monster; unfinished meals print `You stop eating ...`, preserve `victual.piece`, `usedtime`, `reqtime`, `nmod`, and `canchoke`, and later same-object `doeat()` prints `You resume your meal.` or `You consume the last bite of your meal.` before taking the next saved bite. If the last bite has already been consumed, `maybe_finished_meal(TRUE)` finishes instead of printing a stop message (`nethack-c/upstream/src/allmain.c:485-508`, `nethack-c/upstream/src/allmain.c:684-688`, `nethack-c/upstream/src/eat.c:2923-2950`, `nethack-c/upstream/src/eat.c:3874-3888`). JS now pauses carried/floor delayed ordinary victuals when the existing adjacent-hostile interruption check fires, keeps the same live object and bite parameters, resumes only when the player selects that same object, avoids duplicate conduct or shop bite billing, and finishes immediately for final-bite interruption.
- Ordinary victual fullness and choking: C marks `victual.canchoke` when eating starts while satiated, emits `You're having a hard time getting all of it down.` after a bite reaches the 1500-1999 hunger band only when the `Hunger` property is absent, stores `You're finally finished.` as the finish message, prompts `Continue eating?` only when a satiated-start meal has more than one bite remaining, blocks starting `#eat` while `Strangled`, runs `choke()` before or after a bite crosses 2000 hunger with `Breathless || Hunger || (!Strangled && !rn2(20))` recovery, and resets hunger through `init_uhunger()` when life saving rescues `CHOKING` (`nethack-c/upstream/include/context.h:59-74`, `nethack-c/upstream/include/youprop.h:145-147`, `nethack-c/upstream/include/youprop.h:276-277`, `nethack-c/upstream/src/eat.c:123-130`, `nethack-c/upstream/src/eat.c:244-288`, `nethack-c/upstream/src/eat.c:517-540`, `nethack-c/upstream/src/eat.c:2037`, `nethack-c/upstream/src/eat.c:2825-2826`, `nethack-c/upstream/src/eat.c:2923`, `nethack-c/upstream/src/eat.c:3077`, `nethack-c/upstream/src/eat.c:3287-3332`, `nethack-c/upstream/src/end.c:719-720`). JS now applies that warning/finish/choking state for carried and floor `food ration`, `pancake`, `lembas wafer`, and `cram ration`, pauses a declined prompt without printing the ordinary stop message, preserves `canchoke` for later same-object resume, keeps recovered meals paused on the same live object after the C vomit message and hunger reduction, recovers to 60 hunger for the `Hunger` property, blocks random recovery while Strangled, blocks starting `#eat` while Strangled, and resets hunger to 900 when a life-saving amulet rescues choking.
- Wish finalization for special uniques and weight: C substitutes Bell/Candelabrum/Book wishes only outside wizard mode, but `bell of opening` still follows the namedesc silver-bell parser path in the public C trace. C also zeroes `empty` bag-of-tricks/horn-of-plenty charges and recomputes `owt` after final wish qualifiers (`nethack-c/upstream/src/objnam.c:3500-3505`, `nethack-c/upstream/src/objnam.c:5001-5014`, `nethack-c/upstream/src/objnam.c:5290-5319`, `nethack-c/upstream/src/objnam.c:5383-5398`, `nethack-c/upstream/src/mkobj.c:1875-1977`). JS now preserves the Bell silver-bell path, creates the real wizard-mode Candelabrum of Invocation and Book of the Dead, zeroes wished empty horn charges, and assigns final wished-object weight from the local weight helper after the wish burden check.
- Hole/trapdoor `ship_object()` shop-debt routing: C `flooreffects()`/`dropx()` and `impact_drop()`/`ship_object()` charge unpaid carried objects, shop-floor stock, contents, and contained gold before migration, clearing recursive `no_charge` through the `stolen_value()`/`picked_container()` path (`nethack-c/upstream/src/do.c:288-301`, `nethack-c/upstream/src/do.c:786-790`, `nethack-c/upstream/src/dokick.c:1511-1584`, `nethack-c/upstream/src/dokick.c:1639-1746`, `nethack-c/upstream/src/shk.c:1165-1170`, `nethack-c/upstream/src/shk.c:3084-3099`, `nethack-c/upstream/src/shk.c:3712-3828`). JS now routes carried drops consumed by seen holes/trapdoors and shop-floor pile impact migration through `shipObjectShopDebt()`, uses live bill rows for unpaid objects/contents, uses shop stolen-value pricing for floor stock/contents/gold, clears recursive `no_charge`, and sends angry shopkeeper value to `robbed`.
- Carried `ship_object()` fragile breakage: C charges `stolen_value()`/shop debt first, then runs `breaktest()` before migration and prints a muffled crash for glass/camera-like objects or a muffled splat for other breakage (`nethack-c/upstream/src/dokick.c:1695-1743`). JS now applies the existing hard-landing break predicate to carried seen-hole/trapdoor drops after `shipObjectShopDebt()` and before queueing migration, maps JS break kinds to C crash/splat wording, and leaves floor-pile `impact_drop()` no-break behavior intact.
- Remove-curse water `COST_UNCURS` billing: C detects unpaid `POT_WATER` during non-confused remove curse, calls `costly_alteration(obj, COST_UNCURS)` before `uncurse(obj)`, and `bill_dummy_object()` preserves the pre-uncursed unholy-water value as a used-up dummy bill row (`nethack-c/upstream/src/read.c:1553-1568`, `nethack-c/upstream/include/hack.h:282`, `nethack-c/upstream/src/mkobj.c:697-744`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now bills selected cursed unpaid water before clearing the curse, marks the water BUC-known, normalizes the live object back to plain water, and keeps the original unholy-water price as a used-up bill row instead of debit.
- Cursed enchant weapon alteration billing: C `chwepon()` calls `costly_alteration()` after the duller/black-glow message but before turning an unpaid crysknife into a worm tooth or lowering an enchanted weapon's `spe` (`nethack-c/upstream/src/wield.c:972-1021`, `nethack-c/upstream/src/read.c:1672`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now preserves the pre-degraded or pre-disenchanted unpaid wielded weapon as a dummy used-up bill row, clears the live object's unpaid state, then mutates the carried weapon.
- Cursed enchant armor alteration billing: C `seffect_enchant_armor()` calls `costly_alteration(COST_DECHNT)` only after a negative computed enchantment delta, after the glow/vibrate message and before curse/bless or `spe` mutation (`nethack-c/upstream/src/read.c:1253-1264`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now preserves the pre-disenchanted unpaid worn armor as a dummy used-up bill row when `s < 0`, and keeps the live bill row for cursed scrolls whose final `s == 0` only changes BUC.
- Confused proof-stripping alteration billing: C confused enchant weapon, destroy armor, and enchant armor temporarily clear erosionproofing for the effect message, restore the old proof state before `costly_alteration(COST_DEGRD)` when old proof is stripped, then set the final proof state (`nethack-c/upstream/src/read.c:1136-1156`, `nethack-c/upstream/src/read.c:1327-1344`, `nethack-c/upstream/src/read.c:1639-1659`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now shares that ordering across the three confused scroll paths so unpaid proof stripping becomes a used-up dummy bill row after the glow message, while proof-adding remains on the live bill.
- Blessed destroy-armor erosion billing: C blessed non-confused `scroll of destroy armor` can route into `destroy_arm()`, whose `erode_obj(..., EF_PAY | EF_DESTROY)` prints the erosion/destruction message, calls `costly_alteration()` with the erosion verb, and only then mutates or deletes the worn armor (`nethack-c/upstream/src/read.c:1324-1384`, `nethack-c/upstream/src/do_wear.c:3260-3284`, `nethack-c/upstream/src/trap.c:277-319`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now bills ledger-backed unpaid worn armor as a used-up dummy row after the rust/burn/rot/tarnish/crack message and before erosion or destruction, while stale field-only unpaid armor still cannot synthesize a bill row.
- Same-shop unpaid merge proof: C `same_price()` only merges unpaid objects when both objects are on bills for the same shopkeeper at the same unit price (`nethack-c/upstream/src/shk.c:949-1185`, `nethack-c/upstream/src/invent.c:4416-4457`). JS now requires a current-shop bill row before merging a billable pickup/container take-out/food pickup source into an unpaid target stack, so stale `unpaid`/`unpaidPrice` fields no longer create a new current-shop bill row during these merge preflights.
- Covered simple-food pickup merge: C `FOOD(...)` rows make ordinary foods mergeable, and C `mergable()` gates on actual object state instead of display name, clears/normalizes `no_charge` for picked inventory, copies a source name onto an unnamed target, and `merged()` age-averages ordinary stacks (`nethack-c/upstream/include/objects.h:1033`, `nethack-c/upstream/include/objects.h:1103-1113`, `nethack-c/upstream/src/invent.c:771-862`, `nethack-c/upstream/src/invent.c:4377-4491`, `nethack-c/upstream/src/shk.c:949-1185`). JS now routes single-floor pickup and full-inventory pickup preflight through the shared inventory-merge predicate for `food ration`, `pancake`, `lembas wafer`, `cram ration`, `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`, requiring exact kind identity, BUC/state compatibility, `oeaten`/`orotten` compatibility, C object-name compatibility, no-charge normalization, same-shop/unit-price proof for unpaid stacks, age/name merge behavior, C weights for those rows, and C shop costs for K/C rations while keeping fortune cookies, royal jelly, meatballs, slime mold/custom fruit naming, tins, corpses, eggs, globs, meat rings, and broader food-class merges outside this slice.
- Used-up row synthesis from stale fields: C `bill_dummy_object()`/`subfrombill()` preserve actual bill rows, while a field-only unpaid object without `onbill()` is not billing authority (`nethack-c/upstream/src/shk.c:1135`, `nethack-c/upstream/src/shk.c:3660-3671`, `nethack-c/upstream/src/mkobj.c:697-744`). JS now requires a real bill row before `billDummyAlteredCarriedObject()` or `markObjectShopBillUsedUp()` creates a used-up bill, so stale `unpaid`/`unpaidPrice` fields alone no longer synthesize ledger entries.
- Stale-field usage/payment gating: C `check_unpaid_usage()` starts from current shop price through `get_cost()` but relies on the `obj->unpaid`/`onbill()` invariant for billing authority, and itemized `#pay` is built from `ESHK(shkp)->bill_p` rows via `make_itemized_bill()`/`bp_to_obj()` (`nethack-c/upstream/src/shk.c:1545-1661`, `nethack-c/upstream/src/shk.c:5627-5742`). JS now requires a current-shop bill row before charged unpaid usage adds debit, prices the usage fee from current shop cost rather than stale object fields, and suppresses field-only unpaid fallback rows when `collectPayableShopDebts(shkp)` is enumerating a selected shopkeeper.
- Recursive debt/loss stale-field cleanup: C `subfrombill()`, `stolen_value()`, and `obfree()` start from `onbill()`/bill rows for unpaid authority; stale field-only `obj->unpaid` is treated as impossible rather than a price source, while unbilled shop-floor stock can still be priced through `billable()`/`get_cost()` (`nethack-c/upstream/src/shk.c:3660-3710`, `nethack-c/upstream/src/shk.c:3712-3828`, `nethack-c/upstream/src/shk.c:1187-1234`). JS now makes recursive projectile debt collection, carried magic-bag held loss, shop-floor magic-bag lost merchandise, corpse/glob used-up tracking, and shop-floor magic-bag explosion trigger preservation ledger-first; stale `unpaidPrice` no longer creates debt or used-up rows, but shop-floor stock loss still uses current shop price.
- Shop-floor container stale-field cleanup: C's `obj->unpaid iff onbill(obj)` invariant means `out_container()`/`tipcontainer()` only skip `addtobill()` for objects with real bill rows, and `splitobj()` calls `splitbill()` for partial live unpaid stacks (`nethack-c/upstream/src/shk.c:134-137`, `nethack-c/upstream/src/shk.c:1135-1154`, `nethack-c/upstream/src/pickup.c:2727-2766`, `nethack-c/upstream/src/pickup.c:3710-3772`, `nethack-c/upstream/src/shk.c:3623-3657`). JS now treats raw `unpaid` in shop-floor container take-out/tip billing as live only when a shopkeeper ledger row exists, clears stale field-only state before pricing current shop merchandise, recurses the same cleanup into moved container contents, lets stale take-out sources use normal merge preflight, and splits live unpaid stack bill rows when only part of the stack is lifted.
- No-effect wand break billing: C `#apply` on a wand enters `do_break_wand()`, bills unpaid usage with `check_unpaid()` and then calls `costly_alteration(COST_DSTROY)` before removing the wand, even for no-effect results such as wishing/nothing/locking/probing/enlightenment/secret-door-detection/stasis/opening and for non-zappable worn-out wands (`nethack-c/upstream/src/apply.c:3909-3993`, `nethack-c/upstream/include/hack.h:295`, `nethack-c/upstream/src/shk.c:5688-5742`, `nethack-c/upstream/src/mkobj.c:752-826`). JS now prompts before breaking those no-effect/worn-out wand cases, charges the pre-break usage fee only when charges remain, preserves the purchase value as a `COST_DSTROY` dummy used-up bill row, deletes the live wand, and ignores stale field-only unpaid wands without a real bill row.
- Magic-bag container put-in and nested tip billing: C `in_container()` records only top-level `was_unpaid`, runs `sellobj()`, and lets `subfrombill()` recursively clear contained unpaid rows before `mbag_explodes()`; only a top-level unpaid trigger is re-added for later `obfree()` use-up preservation (`nethack-c/upstream/src/pickup.c:2629-2665`, `nethack-c/upstream/src/shk.c:1165-1170`, `nethack-c/upstream/src/shk.c:3660-3692`, `nethack-c/upstream/src/shk.c:3979`). JS now clears unpaid content bill rows from paid trigger containers during accepted or declined shop-floor magic-bag put-in sale handling before the target explosion can recursively mark the tree used-up. C `tipcontainer()` also `addtobill()`s a shop-floor source object and recursive contents before a carried target magic-bag explosion, then `obfree()` preserves those bill rows as used-up (`nethack-c/upstream/src/pickup.c:3770-3791`, `nethack-c/upstream/src/shk.c:3387-3526`, `nethack-c/upstream/src/shk.c:1199-1234`); JS now has coverage for a shop-floor nested cancellation trigger preserving both outer and nested billed rows without debt/robbed value.
- Ordinary carried-drop and tipped-shop-good terrain destruction: C ordinary drops run `flooreffects()` before placement/sale, and shop-floor `#tip` bills contents before `dropy()`/`flooreffects()`; terrain deletion then reaches `obfree()` and preserves billed objects as used-up rows (`nethack-c/upstream/src/do.c:786-807`, `nethack-c/upstream/src/pickup.c:3722-3821`, `nethack-c/upstream/src/invent.c:1430`, `nethack-c/upstream/src/shk.c:1187`). JS now opts the existing `usedUpShopBillOnDestroy` hook into ordinary carried drops, the carried helper drop, and tipped-to-floor objects while leaving generic floor effects opt-in.
- Wish metadata for meat rings, candles, horns, and wishing wands: C singularizes plural meat-ring wishes before quantity handling but keeps `MEAT_RING` non-mergeable and weight 5, gives candles weight 2 with the special seven-candle wish cap, keeps horn of plenty as concrete `HORN_OF_PLENTY`, and forces non-wizard wished `WAN_WISHING` to `spe -1/0` and `recharged = 1` regardless of requested charges (`nethack-c/upstream/include/objects.h:923-925`, `nethack-c/upstream/include/objects.h:991`, `nethack-c/upstream/include/objects.h:1061-1064`, `nethack-c/upstream/src/objnam.c:4435-4453`, `nethack-c/upstream/src/objnam.c:5071-5083`, `nethack-c/upstream/src/objnam.c:5180-5183`, `nethack-c/upstream/src/objnam.c:5290-5295`). JS now resolves `meat rings` to the food object, assigns meat-ring and candle wished weights, preserves wished horn-of-plenty `otyp`, and tests the wishing-wand abuse path.

## Candidate Slices

1. Remaining floor-effect deletion ownership audit.
   - C refs: `nethack-c/upstream/src/do.c:786-827`, `nethack-c/upstream/src/trap.c:4455-4611`, `nethack-c/upstream/src/zap.c:4598-4661`, `nethack-c/upstream/src/invent.c:1430-1461`, `nethack-c/upstream/src/shk.c:1224`, `nethack-c/upstream/src/pickup.c:3770-3822`.
   - JS refs: `js/cmd.js:7945`, `js/cmd.js:21810`, `js/cmd.js:21859`, `js/cmd.js:22437`, `js/cmd.js:39742`.
   - Audit conclusion: generic floor `fire_damage()` catch-light/destruction is not a place to add blanket floor-item billing. Future slices should audit each destructive floor-effect caller against its C ownership path before choosing `useupf()`-style billing, robbed-value debt, or no billing.
   - Status: ordinary carried drops, carried helper drops, and tipped-to-floor shop goods now pass existing `usedUpShopBillOnDestroy` preservation into lava, water, and hot-ground terrain effects. Keep glob ledger merging, liquid-flow terrain changes, high drops/levitation `hitfloor`, projectiles/kicks, boulders, and other `obfree()` callers separate.

2. Projectile and drop shop landing parity.
   - C refs: `nethack-c/upstream/src/dothrow.c:1180`, `nethack-c/upstream/src/dothrow.c:1780`, `nethack-c/upstream/src/do.c:827`, `nethack-c/upstream/src/dokick.c:409`, `nethack-c/upstream/src/shk.c:3692-3927`.
   - JS refs: `js/cmd.js:14901-14945`, `js/cmd.js:15710-15777`, `js/cmd.js:16039-16055`, `js/cmd.js:42499`, `js/cmd.js:42743`, `js/cmd.js:44656-44683`.
   - Status: hard-landing container-content impact, top-level fragile-object breakage, same-shop paid projectile sale, and thrown-gold donation are now covered for fired/thrown projectiles. Remaining slices should audit less ordinary `check_shop_obj()`/`sellobj()` branches, callers that deliberately pass `with_impact` to `dropz()`, kicking/ship/pick special cases, and shared-helper integration around shop ownership transfer.
   - Latest audit detail: C projectile landing order is top-level hard break, floor placement, hard-landing container impact, `check_shop_obj()`, then `stackobj()`; same-shop paid non-broken projectiles call `sellobj()` unless landing on the shopkeeper's square, and thrown gold landing in the hero's shop also runs `sellobj()` before stacking (`nethack-c/upstream/src/dothrow.c:1181-1214`, `nethack-c/upstream/src/dothrow.c:1780-1838`, `nethack-c/upstream/src/dothrow.c:2656-2730`, `nethack-c/upstream/src/shk.c:3874-3910`, `nethack-c/upstream/src/shk.c:3927-4194`). JS now resolves unpaid projectile return/debt, then runs a same-shop paid sale or thrown-gold donation after impact and before stacking, preserving both the costly-spot home-square exclusion and the current shopkeeper-square exclusion. Remaining risk is the broader `dropz(with_impact)`, kick, ship, and pick transfer matrix rather than this same-shop paid/gold landing branch.

3. Victual runtime after first-bite billing.
   - C refs: `nethack-c/upstream/src/eat.c:360`, `nethack-c/upstream/src/eat.c:519`, `nethack-c/upstream/src/eat.c:2020`, `nethack-c/upstream/src/eat.c:3049-3129`, `nethack-c/upstream/src/eat.c:3806`.
   - JS refs: `js/cmd.js:10824-10856`, `js/cmd.js:11148-11367`, `js/cmd.js:44214-44229`, `js/allmain.js:10775-10842`.
   - Status: carried and floor `food ration`, `pancake`, `lembas wafer`, and `cram ration` now use `touchEatenFood()` for C-shaped first-bite stack splitting and shop billing, compute `reqtime`/base `nmod` from the C object row, grant one bite immediately, store eating state, apply later bites in `processEatingOccupationTick()`, and remove the live object only on finish. The same delayed rows now handle cursed/age-rotten first bites, rotten sleep leftovers, blessed age thresholds, and the C nonrotting exemption for aged lembas/cram. Lembas/cram race nutrition keeps C's split between adjusted hunger and base `oeaten` depletion. Floor completion removes only the eaten floor unit, leaving split residual stacks and used-up shop bill rows intact. Delay-one carried/floor apple and fortune cookie now share the C first-bite stack/billing path, finish immediately without an occupation, and cover cursed apple plus fortune-cookie paper/rumor post-effects. Covered delayed ordinary rows now also preserve C-style interruption/resume messages, full-warning/finally-finished state, fatal choking or vomit recovery after satiated-start meals, Breathless/Hunger recovery exemptions, Strangled start/no-random-recovery behavior, choking life-saving hunger reset, and same-object bite state without duplicate billing or conduct. Public replay inspection found carried apple/cookie canaries and a floor apple prompt canary, but no public accepted floor fortune-cookie or shop-floor apple/cookie eating.
   - Remaining narrow slices: broader non-ordinary floor food branches and UNIX-vs-Mac apple message policy remain separate.

4. Tin follow-up.
   - C refs: `nethack-c/upstream/src/eat.c:1389`, `nethack-c/upstream/src/eat.c:1514-1721`.
   - JS refs: `js/cmd.js:11992-12235`.
   - Narrow slice: handle metallivorous empty/meat/spinach tin nutrition and prompt bypass exactly, after ordinary victual runtime is stable.

5. Wish parser and object finalization.
   - C refs: `nethack-c/upstream/src/objnam.c:3978-3996`, `nethack-c/upstream/src/objnam.c:4177-4237`, `nethack-c/upstream/src/objnam.c:5037-5189`, `nethack-c/upstream/src/objnam.c:5255-5268`, `nethack-c/upstream/include/objclass.h:52`, `nethack-c/upstream/include/objclass.h:60`, `nethack-c/upstream/include/obj.h:49`, `nethack-c/upstream/src/zap.c:6360`.
   - JS refs: `js/cmd.js:9485`, `js/cmd.js:9504`, `js/cmd.js:19322`, `js/cmd.js:38258-38512`.
   - Narrow slice status: C-shaped quantity, requested-`spe`, common charge suffix constraints, wizard-mode Candelabrum/Book creation, the Bell namedesc silver-bell path, `empty horn of plenty` charge zeroing, final wished-object `owt` recomputation, lenses weight/pair naming/namedesc matching, meat-ring plural/weight, candle wished weight, horn-of-plenty concrete `otyp`, concrete pancake/cram/K/C ration wish metadata, and wand-of-wishing abuse-charge coverage are now in place for common object classes without replacing the whole parser. A good next slice is a wish-local metadata overlay near `WISH_BASE_OBJECTS` for already-resolved objects, exposing only `oc_merge`, `oc_charged`, `oc_nowish`/special substitution policy, and `unitWeight`, then consuming it from `wishQuantityMergeable()`, `wishedSpeForItem()`, `substituteNonWizardSpecialWish()`, and `wishedObjectFinalWeight()` (`nethack-c/upstream/include/objclass.h:47`, `nethack-c/upstream/src/objnam.c:5071`, `nethack-c/upstream/src/objnam.c:5099`, `nethack-c/upstream/src/objnam.c:5395`, `nethack-c/upstream/src/mkobj.c:1887`). Remaining candidates are exact fuzzy matching/ranges, explicit non-object results, artifact provenance, less common charged-tool metadata, full generated-weight/object-factory unification, and remaining `oc_nowish` filtering.
   - Latest audit detail: lenses are covered. C treats them as non-mergeable eyewear with probability 5, weight 3, cost 80, and tool-class "pair of" naming (`nethack-c/upstream/include/objects.h:944`, `nethack-c/upstream/src/objnam.c:690-695`, `nethack-c/upstream/src/objnam.c:3455-3524`, `nethack-c/upstream/src/objnam.c:4311-4322`, `nethack-c/upstream/src/mkobj.c:1887`). JS now keeps `kind`/`actualKind` as `lenses`, consumes the `oc_prob + 1` namedesc bound, assigns weight 3, preserves cost 80, and shares the C-style pair article across wish, pickup, container, and inventory display sites.
   - Latest audit detail: meat ring/candle/horn metadata is covered in the parser-local path. Remaining work should avoid adding more one-off maps where possible and instead move toward the metadata overlay/factory split above.
   - Latest status: concrete ordinary comestible wish metadata is now covered for `pancake`, `cram ration`, `K-ration`, and `C-ration`. These C `FOOD(...)` rows use `oc_merge=1`; weights/nutrition/costs are pancake 2/200/15, cram ration 15/600/35, K-ration 10/400/25, and C-ration 10/300/20, and their namedesc lookup consumes `oc_prob + 1` bounds 26, 21, 1, and 1 respectively (`nethack-c/upstream/include/objects.h:1033`, `nethack-c/upstream/include/objects.h:1105`, `nethack-c/upstream/include/objects.h:1108`, `nethack-c/upstream/include/objects.h:1112-1113`). JS now resolves exact singular and plural wishes to concrete base objects, preserves C capitalization for K/C rations, keeps bare plurals at quantity two, tolerates C fuzzy hyphen/spacing aliases for K/C ration names, and tests final `owt`, display, nutrition, and shop costs. Remaining ordinary comestible wish work is broader registry/factory unification, plus special naming/state rows such as custom fruit/slime mold, royal jelly, meatballs, corpses, tins, eggs, and globs when their C fields are modeled.

6. Non-ordinary magic-bag and special source/target billing.
   - C refs: `nethack-c/upstream/src/pickup.c:2629`, `nethack-c/upstream/src/pickup.c:2673`, `nethack-c/upstream/src/pickup.c:2815`, `nethack-c/upstream/src/pickup.c:3724`, `nethack-c/upstream/src/pickup.c:3959`, `nethack-c/upstream/src/pickup.c:4009-4030`, `nethack-c/upstream/src/shk.c:3450-3490`, `nethack-c/upstream/src/shk.c:3712-3828`.
   - JS refs: `js/cmd.js:15986`, `js/cmd.js:16103`, `js/cmd.js:20951`, `js/cmd.js:21027`, `js/cmd.js:22168`, `js/cmd.js:22400`, `js/cmd.js:22445`, `js/cmd.js:22842`.
   - Narrow candidates: continue filtering nested magic-bag/loss billing through remaining C `billable()`/`stolen_value()` rules and continue special source/target ordering around less common shop-floor and inventory cases.
   - Latest audit detail: floor bag-of-tricks `#tip` is covered. C temporarily adds a real bill row for a shop-floor bag of tricks before `check_unpaid_usage(..., TRUE)` and removes it afterward; the alternate emptying fee is one full current shop price, with no persistent bill row or `robbed` mutation, and zero-charge/no-charge cases add no debt (`nethack-c/upstream/src/pickup.c:3993-4030`, `nethack-c/upstream/src/shk.c:5626-5732`). JS now creates/removes a temporary real bill row at the special floor-source wrapper, preserves the strict ledger requirement in `checkUnpaidUsage()`, and keeps floor horn-of-plenty `#tip` excluded. C `in_container()` content-bill cleanup before `mbag_explodes()`, shop-floor nested-trigger `tipcontainer()` billing before carried target explosions, carried `#apply` cursed-magic-bag message/turn-cost gating, and cursed shop-floor magic-bag loss of extracted containers are now covered in JS; the last case follows C `stolen_value()` treating extracted `OBJ_FREE` contents as inventory-like, skipping contained coin and unbilled non-coin contents while still charging direct vanished gold and nested bill rows (`nethack-c/upstream/src/pickup.c:3757-3766`, `nethack-c/upstream/src/pickup.c:2815-2820`, `nethack-c/upstream/src/shk.c:3723-3726`, `nethack-c/upstream/src/shk.c:3803-3809`). Remaining follow-up should stay on less common source/target ordering and valuation rules.

7. Object merge metadata and C `oc_merge`.
   - C refs: `nethack-c/upstream/include/objclass.h:52`, `nethack-c/upstream/include/objects.h:1033`, `nethack-c/upstream/src/invent.c:4377-4491`, `nethack-c/upstream/src/shk.c:949-1185`.
   - JS refs: `js/mklev.js:3905`, `js/mklev.js:4708`, `js/cmd.js:16238`, `js/cmd.js:43968`, `js/cmd.js:44056`.
   - Narrow candidate: same-shop bill-row proof is now covered for ordinary pickup, container take-out, and food pickup merge preflights, and covered simple `FOOD(...)` pickup/full-inventory merges now use the shared inventory predicate with C name compatibility, C unit weights/costs where needed, and age averaging. Next add a shared C-shaped merge predicate for corpses, eggs, tins, and broader food/floor pickup that checks `corpsenm`, timed eggs, reviving corpses, tin `spe`, `oeaten`/`orotten`, `oname`, artifact identity, BUC/spe/no-charge, and same-shop/unit-price compatibility before stacking; share the same metadata merge path for quantity, known flags, and age averaging. C clears/normalizes `no_charge` for inventory merge choice, stops source timers during merge, copies source names onto unnamed targets, age-averages ordinary merges, and requires same owner/unit price for unpaid merges (`nethack-c/upstream/src/invent.c:771-819`, `nethack-c/upstream/src/invent.c:819`, `nethack-c/upstream/src/invent.c:4416-4457`, `nethack-c/upstream/src/shk.c:949`). JS still has blanket corpse/egg/tin exclusions and broad food-class exclusions outside `food ration`, `pancake`, `lembas wafer`, `cram ration`, `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`, can overmerge timed eggs/corpses on floor/drop stacking, and does not age-average normal merges outside the covered simple-food pickup path (`js/cmd.js:16786-16911`, `js/cmd.js:18358-18361`, `js/cmd.js:45197-45205`).
   - Latest audit detail: the known stale field-only fallbacks in recursive debt/loss, tracker-only used-up paths, and shop-floor container take-out/tip billing are now ledger-first. Remaining risk is broader, not this specific fallback: non-ordinary source/target ordering, generic `obfree()` preservation, and container-aware `stolen_value()` naming still need a shared C-shaped subsystem. C can create dummy rows for shop-floor stock without a prior carried bill, but that comes from `costly_spot()`/`billable()`/`get_cost()` through `addtobill()`, not from object-local `unpaidPrice`.
   - Latest audit detail: the highest-value merge tests are food-class stack cases. C `mergable()` rejects actual BUC mismatches even when names are both unknown, requires `unpaid`/`spe`/`no_charge`/broken/trapped/lit compatibility, checks `oeaten` and `orotten`, blocks `dknown` mismatch, requires same `corpsenm` for corpses/eggs/tins, blocks hatching eggs and revivable corpses, requires same unpaid unit price, and preserves corpse name asymmetry; `merged()` age-averages normal merges and copies a source name onto an unnamed target (`nethack-c/upstream/src/invent.c:771-862`, `nethack-c/upstream/src/invent.c:4377-4491`). JS covered simple-food pickup now covers the narrow `food ration`/`pancake`/`lembas wafer`/`cram ration`/`cream pie`/`K-ration`/`C-ration`/fruit-and-vegetable path through the shared pickup predicate, but floor/projectile stacking and the full food/corpse/tin/egg field matrix are still open.
   - Latest status: covered simple-food merge parity is covered for pickup and full-inventory pickup preflight for `food ration`, `pancake`, `lembas wafer`, `cram ration`, `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`; the expansion also added C weights for orange/pear/banana/melon/K/C rations and C shop costs for K/C rations. Remaining work should expand from that predicate toward full `oc_merge`: tins, corpses, eggs, globs, hatching/revive timers, `corpsenm`, tin `spe`, source timers, floor/drop/projectile stacking, fortune cookies, royal jelly, meatballs, slime mold/custom fruit naming, and generalized merge metadata.
   - Latest audit detail: the safe pickup-merge expansion for C `FOOD(...)` rows with simple identity and available metadata is now implemented for `cream pie`, `K-ration`, `C-ration`, `apple`, `orange`, `pear`, `melon`, `banana`, and `carrot`, including missing C weights/costs for `orange`, `pear`, `banana` weight 2, `melon` weight 5, `K-ration`/`C-ration` weight 10, `K-ration` cost 25, and `C-ration` cost 20 (`nethack-c/upstream/include/objects.h:1033`, `nethack-c/upstream/include/objects.h:1105-1113`). Tins, corpses, eggs, globs, slime mold/custom fruit, royal jelly, fortune cookies, and meatballs need dedicated slices for `corpsenm`, timers, special names, prompt/RNG sensitivity, or glob billing.

8. Covered `dopay()` prompt and branch parity.
   - C refs: `nethack-c/upstream/src/shk.c:1542-1736`, `nethack-c/upstream/src/shk.c:1757-2010`, `nethack-c/upstream/src/shk.c:2037-2453`, `nethack-c/upstream/src/getpos.c:771-1165`, `nethack-c/upstream/src/cmd.c:3168-3178`.
   - JS refs: `js/cmd.js:15052-15149`, `js/cmd.js:24034-24498`, `js/cmd.js:31152-31344`, `js/cmd.js:45428-45436`.
   - Narrow candidates covered: angry-only 1000-gold appeasement, traditional `Itemized billing? [ynq m]` UI, C-shaped target scanner, `Pay whom?` validation, and getpos-style monster/object cursor cycling. The `Pay whom?` cursor prompt now covers cancel, self, empty, unseen, non-shopkeeper, distant nonresident, valid-target mutation ordering, hero wraparound, non-shopkeeper monster cycling, and visible object cycling (`nethack-c/upstream/src/shk.c:1811-1854`, `nethack-c/upstream/src/getpos.c:1011-1037`).
   - Latest audit detail: blind unique-adjacent `#pay`, the C-shaped target scanner, and local `getpos()` cycling are covered. C chooses one adjacent shopkeeper before blind rejection, skips dead shopkeepers, only treats `inhishop()` shopkeepers as resident, counts spotted shopkeepers for the `seensk == 1` branch, refuses a visible distant nonresident, has the order-dependent angry-adjacent quirk, and cycles `m/M` plus `o/O` through hero and visible targets by distance/y/x (`nethack-c/upstream/src/shk.c:214`, `nethack-c/upstream/src/shk.c:1761-1804`, `nethack-c/upstream/src/getpos.c:311-328`, `nethack-c/upstream/src/getpos.c:1011-1037`). JS now mirrors those scanner decisions, enters a `Pay whom?` cursor before mutation when multiple visible candidates require target choice, supports monster/object cycling for target description, and sends peaceful robbed nonresidents through the asked-for-gold branch.
   - Latest status: selected-row insufficient funds is covered. C `dopayobj()` calls `insufficient_funds()` after each selected item, producing `You don't have gold[ or credit] enough to pay for <item>.` and returning `PAY_CANT` without a thank-you (`nethack-c/upstream/src/shk.c:2149`, `nethack-c/upstream/src/shk.c:2283`, `nethack-c/upstream/src/shk.c:2472`); JS now reports that item-specific, credit-aware message for first selected-row failure and stopped-short queued payment.
   - Latest status: the target-selection slice now has a `Pay whom?` cursor mode with no-mutation cancel/self/empty/unseen/non-shopkeeper/distant-target exits, valid-target continuation into the existing payment path, and `@`/`m`/`M`/`o`/`O` cursor behavior matching the relevant local `getpos()` target classes. The angry-not-robbed path now follows C's "after your hide" branch: it requires 1000 open gold before credit can offset payment, clears anger unless the same-customer `rn2(3)` failure fires, and spends a move for both failure and payment. The traditional itemized branch now supports `q`, `n`, `m`, and `y` item-by-item prompt paths with `m` prefix inversion.

9. Hole/trapdoor `ship_object()` shop-debt routing.
   - C refs: `nethack-c/upstream/src/do.c:288`, `nethack-c/upstream/src/do.c:786`, `nethack-c/upstream/src/dokick.c:1695`, `nethack-c/upstream/src/shk.c:3781`.
   - JS refs: `js/cmd.js:3078`, `js/cmd.js:3116`, `js/cmd.js:3193`, `js/cmd.js:17038`, `js/cmd.js:17040`.
   - Status: the narrow debt-before-migration path is now covered for unpaid carried/free drops and shop-floor pile objects, and the carried path now runs C-ordered fragile breakage after debt but before queueing migration. JS converts live bill rows and unpaid contents to debt, prices shop-floor stock/contents/gold through stolen-value semantics rather than sale offers, clears recursive `no_charge`, routes angry-shopkeeper loss to `robbed`, and keeps floor-pile `impact_drop()` no-break behavior.
   - Remaining follow-ups: impacted pile/follower handling around multi-object migrations, other callers that reach `ship_object()`/`impact_drop()` outside the covered seen-hole/trapdoor paths, exact message ordering when multiple shopkeepers or simultaneous migration effects are involved, and broader cross-level migration ownership cleanup.

10. Remaining `check_unpaid_usage()` and `costly_alteration()` callers.
   - C refs: `nethack-c/upstream/src/apply.c:1201-1252`, `nethack-c/upstream/src/apply.c:3945-4234`, `nethack-c/upstream/src/zap.c:1295-1396`, `nethack-c/upstream/src/read.c:1156-1344`, `nethack-c/upstream/src/read.c:1553-1566`, `nethack-c/upstream/src/read.c:1627`, `nethack-c/upstream/src/wield.c:977-1020`.
   - JS refs: `js/cmd.js:162`, `js/cmd.js:7439`, `js/cmd.js:13289-13307`, `js/cmd.js:13916-13980`, `js/cmd.js:19076-19267`, `js/cmd.js:22332`, `js/cmd.js:34536-35193`, `js/cmd.js:37867-38150`, `js/cmd.js:39550`.
   - Status: remove-curse on cursed unpaid water is now covered for the normal non-confused `COST_UNCURS` path; cursed enchant weapon now covers unpaid crysknife degradation plus ordinary weapon disenchantment; cursed enchant armor now covers `s < 0` unpaid worn armor disenchantment while preserving live bills for `s == 0`; confused enchant weapon, enchant armor, and destroy armor now cover proof-stripping `COST_DEGRD` dummy billing; blessed destroy armor now covers `destroy_arm()` erosion/destroy `EF_PAY` dummy billing for ledger-backed unpaid worn armor; Bell of Opening apply now has the C 5000-zorkmid price row, ring/muffled/no-sound messages, zero-charge no-bill behavior, pre-decrement charged-use billing via the live bill row, and invocation-square `age` priming only for charged non-cursed Bells (`nethack-c/upstream/src/apply.c:1207-1270`, `nethack-c/upstream/src/invent.c:1335`, `nethack-c/upstream/src/shk.c:5688`, `nethack-c/upstream/src/spell.c:272`); no-effect/worn-out wand `#apply` break now charges pre-break usage and preserves the destroyed wand as a `COST_DSTROY` dummy row (`nethack-c/upstream/src/apply.c:3909-3993`, `nethack-c/upstream/include/hack.h:295`). The confused remove-curse price-increase case for uncursed unpaid water becoming holy/unholy remains a live-row `alter_cost(obj, 0L)` follow-up rather than a dummy-bill case (`nethack-c/upstream/src/read.c:1553-1566`, `nethack-c/upstream/src/shk.c:3237-3265`).
   - Narrow candidates: hero-caused drain-life item alteration once spell/ray object-hit paths exist, broader cancellation transformations, and effectful wand-breaking explosions outside the no-effect/worn-out `#apply` branch.
