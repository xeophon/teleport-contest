# C-parity audit — 964 muse-items / seed9127-wiz-muse-items (2026-08-01, wave-5 continuation)

Scope: monster offensive-item use (muse.c wand zap), monster equipment
(spear wield), salamander attack anatomy (touch/roast/grab), life-saving /
search-occupation pacing around resumed attack chains.

Target: sessions-extra/seed9127-wiz-muse-items.session.json
Base (post-merge): RNG 2830/2862, Screens 72/143.
Final (this wave):   RNG 2862/2862 positionally exact (JS emits 2 more tail
                     calls past C's truncated log: the deferred knockback pair
                     of the attack C's recording cut off mid-flight),
                     Screens 139/143 (cursors 143/143).
Resolved (2026-08-02): the 4 remaining screens were the u.uhp == -1
                     bot() paint gate (botl.c:259); session fully PASSes —
                     see audit 977.

## Functions/areas verified against C

- getmattk mspec substitution (mhitu.c:371-392): released grabber's attacks
  become AT_CLAW/AT_TUCH 1d6 — the JS salamander chain already had this; the
  missing piece was that every hitmu-style hit consumes the unconditional
  rn2(3)/rn2(6) pair in mhitm_knockback (uhitm.c:5258/5269) even when no
  knockback can occur (uhitm.c:5280-5284: stick/hug attacker check happens
  after the rolls).
- mattacku to-hit: rnd(20+i) at mhitu.c:806 (touch/claw family) and
  mhitu.c:912 (weapon, hitval added first) — matched by JS chain per slot.
- hitmu base damage d(damn,damd) (mhitu.c:1187); mhitm_adtyping negate roll
  rn2(10) >= 3*armpro (uhitm.c:75-98); fire inventory-burn levy roll
  m_lev > rn2(20) (uhitm.c:2579); destroy_items/maybe_destroy_item roll
  cascade (zap.c:5998/6045/5897); mhitm_ad_phys grab roll rn2(2)
  (uhitm.c:4024); unstuck mspec cooldown rnd(2) (mon.c:3465, via savelife
  release end.c:753-754).
- muse zap path: wand use at use_offensive (rn1(8,6) at muse.c:1884),
  silver wand = striking (+ rnd_offensive_item gates at muse.c:2043/2045),
  "zaps a silver wand!" + "Boing!" page model, obj_resists rn2(100)
  (zap.c:1469) for the hero's wand of lightning.
- Turn cadence per iteration: monster phase → new-turn block (mcalcmove
  sweep mon.c:1164 ×4, maybe_generate_rnd_mon rn2(70) allmain.c:166,
  svm.moves++ allmain.c:244) → per-turn tail (regen_hp rn2(100)
  allmain.c:659-ish, dosounds rn2(300/400) sounds.c:213/220, gethungry
  rn2(20), u_wipe_engr gate rn2(40+3*DEX) allmain.c:360,
  distfleeck/m_move).  JS now continues this through the --More-- that
  pages the savelife nomovemsg, matching C's vm.moves-visible frames.

## Mechanics wired

1. js/allmain.js salamander chain slot-2 substitute-hit now routes through
   salAftermath (knockback pair + damage + stop_occupation) — the RNG fix.
2. Search occupation first-tick exemption: per-press tick counter
   (`_search_ticks_this_press` set in js/cmd.js 's' handler); the
   monster_nearby-shaped post-tick check only fires from tick 2
   (allmain.c:485-508 occupation branch); the per-monster dochugw-shaped
   check keeps the "hero engaged this tick" requirement at tick 1
   (mhitu.c:1265/99 + monmove.c:222-235).
3. Chain completion emits the queued savelife nomovemsg
   ("You survived that attempt on your life.", end.c:727 via unmul
   hack.c:4185) and lets the tail run before blocking on the resulting
   More.

## Remaining unported / divergent behavior in this subsystem

- Deep muse.c branches not exercised by this session stay unported by this
  wave: MUSE_SCR_EARTH boulder loop, camera flash, defensive/misc item
  use classes (use_defensive/use_misc) — only offensive wand-of-striking
  (and rnd_offensive_item gating) is covered on this path.
- seed9127 residual: the 4th death cycle's frames (steps 132-135) show
  C deferring the HP status-line refresh past the mid-hitmu --More-- block
  (cell text matches; only the botl HP differs).  This is status-line/bot()
  sequencing inside done()/mdamageu; not RNG-bearing.
- Trade note: unifying the search-stop checks to the above C semantics moved
  two non-passing extras (seed9102, seed9104) partially: their werewolf
  batches were tuned to the old every-tick check; seed9103 improved to
  RNG-complete under the same change.  Reconciling those belongs to their
  slices (the old behavior was itself not C-faithful).
