# C Parity Audit 266: Tiphat Werecreature Noise

## Sources

- `nethack-c/upstream/src/sounds.c:823-835`: `MS_WERE` whispers about the moon unless it is full moon and the `night() ^ !rn2(13)` fork selects the blood-curdling branch; the loud branch prints before `wake_nearto(..., 11 * 11)`.
- `nethack-c/upstream/src/sounds.c:837-839`: `MS_BARK` howls at full-moon night without consuming the `MS_WERE` random fork.
- `nethack-c/upstream/src/sounds.c:1506-1528`: directed `tiphat()` reaches `domonnoise()` only for adjacent, non-visible/no-humanoid-fallback responders.
- `nethack-c/upstream/include/monflag.h:37`: `MS_WERE` is the lycanthrope-in-human-form sound.
- `nethack-c/upstream/include/monsters.h:220-224` and `267-271`: beast-form werejackals and werewolves use `MS_BARK`.
- `nethack-c/upstream/include/monsters.h:911-915`: beast-form wererats use `MS_SQEEK`.
- `nethack-c/upstream/include/monsters.h:2613-2635`: human wererat, werejackal, and werewolf entries use `MS_WERE`.
- `nethack-c/upstream/src/calendar.c:215-220` and `nethack-c/upstream/include/flag.h:78-81`: `night()` is hour `< 6 || > 21`, and `FULL_MOON` is moon phase `4`.
- `nethack-c/upstream/src/mon.c:4366-4383`: `wake_nearto_core()` wakes monsters whose squared distance is strictly less than the requested distance and clears non-unique wait masks.

## JS Coverage

- `tipHatMonsterSound()` now infers `were` only for human-form wererat, werejackal, and werewolf metadata.
- Beast-form werejackal and werewolf names continue through the bark path, preserving the full-moon night howl branch rather than using `MS_WERE`.
- The `were` noise branch mirrors the C fork: non-full moon consumes no RNG and whispers; full moon consumes exactly one `rn2(13)` and uses the night/day XOR behavior.
- Loud werecreature noise uses `shriek` for human wererats and `howl` for the other human werecreatures, prints the dramatic line first, then wakes nearby monsters with squared distance `< 121`.
- The existing invisible #tip path keeps the unseen source named as `It`, matching the local `Monnam()` shape for this directed fallback.

## Tests

Focused canaries in `test/shop-billing-helpers.test.mjs` cover:

- inferred human-form werewolf sound whispering off full moon without RNG,
- beast-form werewolf staying on the bark/full-moon howl branch without RNG,
- full-moon night human-form werewolf consuming exactly one `rn2(13)`,
- branch-conditioned assertions for the whisper versus loud howl fork without pinning a seed,
- loud were noise waking a nearby sleeper only when the random fork selects the dramatic branch.

## Remaining Gaps

- This remains local to directed helmet tipping; broad shared `domonnoise()` and `#chat` still do not share a unified implementation.
- The tests include a wererat-specific implementation path but do not add a separate wererat `shriek` canary in this slice.
- Full lycanthrope shapechanging behavior is covered by other water-hit/polyself slices and is not part of this sound-only change.
- Shopkeeper `MS_SELL`, quest leaders/nemeses/guardians, priests, vampires, Riders, Oracle, seduction, bribe, and bones speech are still separate sound gaps.

## Verification

- `node --check js/cmd.js`
- `node --check test/shop-billing-helpers.test.mjs`
- `node --test --test-name-pattern "worn helmet tip (infers invisible human werecreature|keeps invisible werebeast|makes full-moon invisible werecreature|makes invisible explicit orc sound grunt)" test/shop-billing-helpers.test.mjs`
- `git diff --check`
- `node --test test/shop-billing-helpers.test.mjs` (`1243/1243` tests passed)
- `node --test test/*.mjs` (`1340/1340` tests passed)
- `npm run score` (`44/44` replay sessions passed)
