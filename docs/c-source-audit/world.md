# World generation and persistent state audit

Source: `nethack-c/upstream/src` at `16ff59115315917b93185d026aeefea06db9b0f4` and its `dat/*.lua` inputs. This is a review of the function bodies and JS call paths listed below, covering all 29 assigned files. It is not an assertion that every branch of every function has been ported. Native file formats and Lua bindings are distinguished from missing gameplay behavior. Existing tests listed here are evidence for particular behavior, not whole-file parity.

## Implemented quest filler group

`js/mklev.js` now dispatches the upper and lower filler programs for Caveman, Healer, Knight, Monk, Ranger, Rogue, Samurai, Tourist, and Valkyrie. The 18 upstream `*-fila.lua`/`*-filb.lua` files are compiled by `tools/generate-quest-fillers.mjs` into `js/quest_filler_data.js`. The generator rejects syntax outside the supported declarative subset and has an exact regeneration check. Runtime generation uses the existing cave generator, room allocator, corridors, object/monster creation, trap creation, wallification, flips, and finalization.

The programs preserve declaration order, including alignment shuffle, randomized initial lighting, cave generation, stairs, objects, traps, and inhabitants. Placement follows `sp_lev.c:get_location` (1202), `is_ok_location` (1280), `pm_to_humidity` (1884), `create_monster` (1925), `lspo_room` (4028), and `l_create_stairway` (4147). Water-preferring monsters first search wet squares; the fixed Samurai map uses the odd centered coordinates from `lspo_map` (6075). Room contents are skipped when C's room creation cannot find a placement. `mkroom.c:inside_room` (678), including the boundary squares of ordinary rooms, is reused through the existing JS helper.

`test/quest-fillers.test.mjs` adds 26 tests: exact source regeneration, all 18 dispatches with source-required inhabitants/objects/stairs across three live seeds, fixed ASCII geometry, eel placement in pools, initialization RNG order for three layout families, and repeatability/variation. The first run had 24 failing tests and two passing tests. The completed suite and existing `special-levels`/`levelgen-rolls` suites pass 42/42. These checks do not establish complete C RNG equality for all downstream inventory and population branches.

The filler group increased implemented quest program dispatches from 19 to 37 among 65 role/program combinations. Subsequent named-map work below raises the current count to 65: all 26 fillers and all 39 named stages. This is a map-dispatch count, not a percentage of the C game implemented.

## Named quest maps and ordered Lua operations

`tools/generate-quest-levels.mjs` compiles 25 additional named stages from their upstream Lua files: Caveman, Healer, Ranger, and Barbarian goals; Caveman, Healer, Ranger, Rogue, Valkyrie, and Knight locate levels; Rogue, Monk, and Valkyrie goals; Samurai goal/locate plus Wizard goal; Monk locate plus Tourist locate/goal; and Caveman, Healer, Samurai, Monk, Tourist, and Valkyrie starts. The existing 11 named builders remain in place. The compiler emits data with explicit local variables, indexed choices, conditional branches, numeric loops, and allowed NetHack helper calls; runtime code never evaluates Lua source text. `nhlib.lua`'s random/shuffle/dice helpers use the live game RNG. Existing `SplevSelection` and `replaceDesTerrain` helpers supply selection and terrain operations.

The new stages include exact ASCII floor/liquid/door geometry, fixed and random inhabitants, quest artifacts and invocation bells, unaligned altars and staffed shrines, irregular room boundaries, conditional lava drawbridges, source trap types, off-map stairs, and delayed entrance regions. Relevant source bodies include `sp_lev.c:create_monster` (1925; extinct/genocided species gates), `create_object` (2193), `lspo_replace_terrain` (5051), `levregion_add` (5371), `lspo_drawbridge` (5720), `lspo_wallify` (5965), `remove_boundary_syms` (1016), and `flip_dbridge_horizontal`/`vertical` (428/442), plus `dbridge.c:create_drawbridge` (235). The Caveman boundary and Valkyrie bridge tests exposed incorrect explicit wallification and missing direction rotation, respectively; both are fixed.

`quest-named-levels` has 16 tests, `quest-locate-levels` has 13, `quest-branching-goals` has 9, and `quest-lua-runtime` has 3. These run actual `mklev` dispatches at varied seeds, compare source geometry and mandatory populations, and test exact helper RNG ordering, single evaluation of loop bounds, skipped branches, and removal/translation of selected coordinates. The first goal milestone began with eight failing tests, all 13 locate tests initially failed, and the latest three-goal milestone began with six failing tests. Additional geometry and runtime checks were added afterward. The combined quest, special-level, level-generation, and themed-room suite passes 159 tests. These are bounded source-derived checks, not proof of complete C RNG equality for every delegated object/inventory branch.

The Monk locate and Tourist locate/goal programs add selection union/subtraction (`nhlsel.c`), random traps at supplied selected coordinates, quantity/weight for source stackable objects, and `sp_lev.c:lspo_engraving` (3881). Monk's two blessed spinach tins share a burning Elbereth location. Tourist maps fill the source shops, barracks, morgues and other rooms, place toilet paper and the police station, and exclude shop floors when selecting trap sites. `role.c:role_init` (2028–2065) supplies per-role leader/guardian/nemesis overrides from a shared species table without changing canonical rows; Master of Thieves receives the Tourist nemesis flags and Bell. Named male/female aliases use `find_montype` gender semantics (3143). Eight tests in `quest-town-levels` include source geometry (with graves allowed only inside source morgues), required objects/inhabitants, all thirteen role overrides, and each selected-coordinate roll immediately preceding the random trap-type roll. All initial seven tests failed; the all-role regression was added afterward. The combined quest, mplayer, creation-armor, Astral, and special-level suite passes 143 tests.

The Caveman, Healer, and Samurai starts add source-ordered monster inventory callbacks from `sp_lev.c:create_monster` (2176), `lspo_monster` (3345–3395), and `spo_end_moninvent` (3031). Normal monster inventory consumes its complete creation RNG before replacement; `steal.c:mdrop_special_objs` (852) and `zap.c:obj_resists` (1458) rescue invocation objects and the current quest artifact. Discarded items release worn effects, artifact existence, and nested timers. Callback objects still choose floor coordinates before creation, then transfer to the monster and receive creation-time wearing. A failed unique-monster creation still executes the callback and leaves its objects on the floor. Guardian inventory uses all groups in `makemon.c:m_initweap` (275–325); quest leaders carry `STRAT_CLOSE` and `STRAT_APPEARMSG`. Thirteen tests in `quest-start-leaders` check all three actual dispatches across three seeds, exact leader equipment/enchantment/proofing, eight guardians, source geometry and branch portals, callback RNG order, retained/defaultless inventories, and cleanup. The initial ten tests failed before implementation; two cleanup/option regressions were added afterward. An independent review added a failing `cobj`/`contents` alias regression; cleanup now reuses the shared object-ownership traversal and processes each reachable child once. The combined quest, mplayer, creation-armor, Astral, and special-level suite passes 156 tests, with exact generated-data checks passing.

The Monk, Tourist, and Valkyrie starts reuse source leader/guardian inventories and add named food objects, non-shuffled item description lookup, and `sp_lev.c:lspo_feature` (4844) with existing-furniture protection and explicit feature flags. Specific food constructors now use the `objects.h` unit weights before stack quantities are finalized. `lvlfill_solid` (374) preserves the even maze bounds from `decl.c` (827/840) and intentionally leaves ICE icedpool metadata unset; map/terrain ICE assignment is a different source path. Valkyrie tests inspect all sixteen pre-map random coordinate selections and lava/water rings before overlay. Monk portal placement exposed `mkmaze.c:put_lregion_here` (410): a fixed entrance must delete an obstructing destroyable trap, release trapped monsters, and clear reciprocal pit links before rechecking. This cleanup is now shared by fixed and exhaustive region placement. Eleven `quest-start-environments` tests cover all three actual dispatches across three seeds, source geometry/entrances, required inhabitants and gear, food placement and weights, terrain RNG order, feature flags, and destroyable/undestroyable entrance traps. The initial eight tests all failed; Norn body-armor expectations were corrected after inspecting her huge species size and `worn.c` restrictions. The combined quest, mplayer, creation-armor, Astral, special-level and arrival suite passes 179 tests, with exact generator checks passing.

The final three starts, Knight, Ranger, and Rogue, now execute their complete source programs through the shared executor. This brings the compiled named group to 28, alongside eleven existing direct named builders. `sp_lev.c:lspo_map` (6075–6246) supplies aligned map tables and preserves unoverlaid cave terrain; `create_monster` (2000–2032) applies furniture disguises after normal mimic generation and honors shapechanger protection. A generated `defsym.h` table shares all 105 source symbols, descriptions and colors with flash messages. `steed.c:can_saddle/put_saddle_on_mon` (26/142) now serves ordinary monster generation, quest inventory callbacks, applied saddles and polymorph fitting. `makemon.c` (1440–1457) uses the existing one-percent roll and adds only the source object identifier draw when constructing a saddle; `NO_MINVENT` excludes this path. Discarding a default saddle also clears the runtime worn aliases. Generated weapon stacks now store total weight as required by `mkobj.c` (1257), so explicit quantities produce correctly weighted ya and daggers.

Thirteen `quest-final-starts` tests cover all three dispatches across three seeds, geometry and entrances, leaders and guards, saddle ownership, stack weights, disguised exits, table alignment, source RNG ordering, shapechanger protection, and all six saddleable species classes. The initial ten tests failed before implementation; three further shared-helper checks were added afterward. The focused quest, mplayer, creation-armor, Astral, special-level, arrival, steed, flash and anatomy group passes 286 tests, and the final-start/shop group passes 3147. One older dart test was corrected from unit weight one to the source total weight six while preserving its full expected RNG sequence. All three quest/symbol generators reproduce their checked-in output exactly. These assertions establish the tested source semantics, not whole-level RNG equality for every seed. Generic discarded-inventory relocation/floor effects outside the tested quest creation sites remain a broader `mdrop_obj` call-path gap.

The starting-pet path now constructs its pony saddle with the same `putSaddleOnMonster` helper, replacing its former discarded object identifier draw. `dog.c:makedog` (216–290) requires this explicit saddle after `NO_MINVENT` creation, with the `uroleplay.pauper` exception. Starting monsters retain their actual `m_id`; `context.startingpet_mid/startingpet_typ` and the `edog.parentmid` reference are set, including `NON_PM` when no pet is requested. Seven `starting-pet-saddle` tests all failed before this fix and now pass: carried/worn/identified ownership, exact constructor RNG order, pauper exclusion, dog/cat/no-pet branches, save identity, floor drops, and real `newgame` Knight startup. This addresses saddle ownership and pet identifiers; other initialization details in the manual startup constructor remain subject to the broader source audit. A subsequent source review corrected the saddle weight to 200 (`objects.h:953`; 150 is its price) and connected pet fetching to `droppables`, including useful tools and worn equipment (`dogmove.c:502,550–576`). A shared carrying-load calculation now uses canonical species strength, weight and size and counts worn saddles (`mon.c:1913–1955`). Five new runtime tests cover these branches (four fail with the former logic), with 56 focused tests passing; all three affected public RNG traces match their C references after the correction.

`mplayer.c:mk_mplayer` (116–329) now supplies the ordinary player-monster branch used by `sp_lev.c:create_monster` (1986): base `makemon` inventory, 1–16 levels, `d(level,10)+30` hit points, all thirteen role weapon choices, weighted default equipment-choice rolls even when armor is not issued, stack sizes/enchantments, and three groups of usable monster supplies. The item constructors reuse the existing class initialization without an extra random-type roll. `makemon.c:m_initweap` (272–274) supplies ninjas with missiles and a melee weapon. `quest-player-levels` adds nine source-geometry/population tests; `mplayer-generation` adds fifteen tests covering every role, the exact retained `makemon` RNG prefix followed by level/HP and unused armor-choice ranges, invalid species, and supplied item identities. All initial 23 tests failed before implementation; one identity regression was added afterward.

The endgame branch now implements `mplayer.c:dev_name/get_mplname` (44–89), enhanced `mk_mplayer` equipment, and `create_mplayers` (327–350). Shared role ranks live in `roles.js`. Developer names, female aliases/ranks, 15–30 levels, extra hit points, fake Amulets, role-specific heavy armor, artifact weapon enchantment, gems/gold/items, and the final supply order are covered. `worn.js:dressMonster` ports creation-time `worn.c:m_dowear/m_dowear_type` (757–999): slot order, species fitting, curse locks, erosion/enchantment scoring, speed-boot preference, two-handed weapon restrictions, amulet preference and worn resistance/invisibility/light effects. The hard-helmet earth-scroll choice follows `muse.c:rnd_offensive_item` (2035). Twenty-one endgame tests and ten armor tests cover these behaviors; the initial seventeen endgame tests had sixteen failures before implementation. Creation wearing is reused for the incoming guardian and mplayers; live armor changes, wear delays/messages and all other generic-monster creation callers remain separate work.

The first Astral portal/level-teleport arrival now calls `do.c:final_level` (2043), after migration/timers and initial redraw: `priest.c:reset_hostility` (755), 3–6 endgame mplayers, then `minion.c:gain_guardian_angel` (498). Tests cover alignment changes, source count/phase ordering, worthiness, petless conduct, hostile conflict angels, guardian equipment/HP and a real arrival followed by revisit. C `set_malign` now honors individual minion alignment and preserves A_NONE. The initial-arrival helper does not yet cover later loss of an existing guardian under conflict or artificial stair entry into Astral; these are follow-on call paths. The combined endgame, armor, Astral, ordinary-player, quest-player, arrival, Wizard, and special-level suite passes 94 tests.

The class-monster gender assignment was independently checked: `lspo_monster` initializes `female = 0` at 3230; a class-string argument leaves it unchanged, and an unspecified class-table gender falls back to zero at 3355. `create_monster` writes that value over the generated monster's gender at 2125. Named monsters instead receive the species-aware gender from `find_montype` (3143). The new executor preserves this source behavior.

## Source-driven Sanctum entry and spell preflight

The former Sanctum command-history script and its display/status offsets are removed. Entering Moloch's tended temple now uses `priest.c:intemple` (420–483): the high priest becomes hostile on first entry even when the hero is deaf or the priest is helpless, and Sanctum entry omits ordinary temple feelings. The spell path shares `mcastu.c:choose_monster_spell` (90), `spell_would_be_useless` (908), and `castmu` (125–247) rules for distinct wizard/cleric lists, selection before cancellation, idle directed-spell rejection, cooldowns, misplaced targets, fumbles and attack-dependent damage rolls. Idle monster casting follows `monmove.c` (889–917), including multiple eligible magic slots; summoning reuses `wizard.js:nasty` instead of the removed duplicate generator.

Eleven initial `sanctum-source-runtime` tests check no-hands arrival without fabricated damage/monsters, cancelled and idle casting, displaced targets, unseen curse feedback, fumble ordering, and normal/deaf/helpless temple entry. Eight initial core tests and three later temple tests failed before their implementations; the combined spell, Wizard, arrival and new runtime suite passed 57 tests. Two subsequent red-to-green tests preserve an overflowing message across failed-cast feedback and distinguish a visually unseen caster from a sensed one. This fixes the failed-cast display regression while retaining source RNG; arch-lich and Wizard-world-tour replays again match all screens and RNG calls.

`mcastu.c:mcast_open_wounds` (624), `mcast_confuse_you` (771), `mcast_paralyze` (746), `mcast_blind_you` (729), and `mcast_geyser` (523) now apply cleric damage, resistance observations, confusion accumulation, incapacitation, fixed blindness duration and physical geyser damage. They use the shared hit-point/life-saving path. The executable C paralysis path returns its duration through `mcast_spell` (881–898) to `mdamageu`, so it also deals that much damage, including one point when resisted; the port deliberately preserves this source quirk. Eighteen effect tests initially failed and now pass, including the Eyes of the Overworld, existing longer incapacitation and lethal-cast continuation. Two provisional assertions were corrected after checking `unmul`'s default recovery message and message capitalization.

`priest.c:priestini` (255–262) now gives the Sanctum high priest the real Amulet through the shared object constructor, also used by real Amulet wishes. The object has its own type, unique/real identity, mithril material and weight 20, with `context.made_amulet` and the source amulet initialization RNG from `mkobj.c` (1060). Four tests initially failed and now pass: actual Sanctum dispatch and floor-drop identity at three seeds, plus initialized/uninitialized constructor state and RNG ordering. The combined new cleric, Amulet, prior spell/temple and special-level tests pass 63/63.

`js/mhitu.js:advanceMonsterAttackSlots` now preserves the adjacent `mattacku` attack index and `hitmu` phase across More, life saving, wizard death refusal, and saved object references. Source checks cover `mhitu.c` (714–726 AC calculation; 763–946 slot dispatch and sleep awakening; 1144–1281 contact damage/occupation tail), `uhitm.c:mhitm_ad_phys` (4050–4140, including gauntlets and the inner negative-AC roll even for a human), and `mhitm_ad_cold` (2626–2693). Wielding spends only the weapon slot; missed contact attacks still reach the later spell. A synchronous completion no longer leaves a pending monster index that skips the next scheduled action. Shared `monsterMagicNegation` now accepts the hero inventory/protection state, including amulet of guarding, intrinsic protection, canonical object type and worn masks. Item damage retains `losehp` warnings; direct monster damage uses `mdamageu`'s no-warning behavior. Cold potion destruction stores the current item and damage/exercise phase, so a saved death prompt cannot remove the stack twice. A narrow-window regression also keeps the death result queued with its displayed line: life-saving recovery cannot precede an overflowing death message.

`mcastu.c:mcast_lightning` (565–601) now runs reflection and discovery before the fresh 8d6 roll, then electrical item destruction, iron-bar/door terrain effects, flash blindness and direct damage. Reflection protects items/terrain/sight but still consumes the damage roll; shock resistance protects HP while allowing the remaining effects. The electrical helper's worn levitation-ring removal can suspend into the shared landing path, with saved pit-death rescue returning to the same spell before item deletion and remaining effects. `monster-attack-slots` adds 25 tests and `cleric-lightning` adds eight; 24 of those cases were observed failing before their implementations or review fixes. The latest focused combined driver, cleric, ring and monster-status run passes 151/151.

The live scheduler now parks the current monster pass while its attack continuation waits for input, then resumes that pass without charging the hero's movement again (`allmain.c:203–216`). This fixes a reproduced fatal-contact loop in which clearing pending time and then decrementing it produced a negative, permanently truthy counter. Eight initial isolated live-loop tests cover physical/cold contact, amulet/wizard revival, and narrow/wide message windows. `tty/topl.c:260–265` also requires a fresh line for "You die" regardless of available width; death result flags remain attached to that queued line. Shared `end.js:scheduleLifeSavingRecovery` implements the `savelife` negative multi, survivor message and reason (end.c:727–736) for the new monster attack/spell continuation consumers. Six further live tests preserve counted-search occupation until `hitmu` returns from `done` and calls `stop_occupation` (`mhitu.c:1258–1266`), and transfer a preceding monster's still-future survivor message into `unmul` state when a serial attack takes over. A real ape attack followed by an arch-lich verifies that this future wake message cannot stall current cold damage. The focused attack, sleep, cleric and timing suite passes 65 tests; `seed9105-wiz-archlich-spells` again matches every screen and all 2,391 RNG calls. Those replay results cover that recording, not all combat branches.

`timeout.c:fall_asleep` (951–974), `hack.c:nomul/unmul` (4161–4214), and the genuine sleep callers in cursed-apple eating, gas traps, dull books and self-zapped sleep now share negative-duration sleep state. `u.usleep` records the current turn, preventing same-turn combat awakening, and is cleared after the wake message is displayed, including saved More suspension. The disabled C deafness block remains disabled, and a longer existing negative multi preserves invulnerability through `nomul`'s early return. Twelve new sleep tests cover those states, source resistance, occupation interruption and movement rates 6/12/24; the focused attack, sleep, book and timer suite passes 129/129. Potion vapor and rotten-food unconsciousness retain their separate `nomul` semantics. The entirely missing quaffed sleeping-potion effect, periodic sleepiness and broader ranged sleep owners remain subsequent work; this does not certify all `timeout.c` or `unmul` branches.

The canonical species table contains 42 species with AT_MAGC. This milestone routes 26 whose adjacent arrays consist of physical/cold contact and wizard/cleric magic. Polymorphed defenders and poisoned/artifact weapons retain the existing combat path until their passive/weapon effects have serial continuations; this is explicit routing scope, not complete combat parity. The remaining 16 species are Angel, Archon, barrow wight, Wizard of Yendor, Yeenoghu, Orcus, Asmodeus, Demogorgon, Minion of Huhetotl, Thoth Amon, Chromatic Dragon, Ixoth, Master Kaen, Nalzok, Dark One and abbot. Their unintegrated slots require elemental magic, stun gaze, level drain, poison, disease, paralysis/confusion/stun, special-object theft or breath dispatch. The common knockback helper still implements only its selection rolls, not displacement. Cleric fire/insects, wizard weakness/death-touch and ranged magic remain subsequent source work. These gaps must be resolved through the owning spell/combat/creation paths without reinstating input-, coordinate-, turn- or HP-dependent scripts.

## Occupied teleport-region arrivals

`mkmaze.c:place_lregion/put_lregion_here` (356–465) now uses a shared placement path for its probabilistic and exhaustive passes. A fixed `LR_TELE`, `LR_UPTELE`, or `LR_DOWNTELE` arrival immediately tries `rloc(RLOC_NOMSG)` on the occupying monster after the two region-coordinate draws. Wider regions reject occupied candidates during the first 200 attempts; their exhaustive pass can displace an occupant. Relocation failure calls `mon.c:m_into_limbo/migrate_mon` (3834–3863), preserving the monster in the current level's migration queue with `MIGR_APPROX_XY` and the remembered coordinate.

The independent-arrival function moved from `cmd.js` into `mklev.js` so it shares relocation, room sampling, and placement helpers. `dog.c:mon_arrive` (420–623) now handles both existing random arrivals and approximate limbo returns: elapsed-time catchup, room-based or clipped-coordinate wandering, direct placement or source-ordered nearby search, and re-queuing failures with a valid destination. Departure releases hero holding and leashes, clears container `no_charge` flags and shop residency, preserves carried object identity, and rescues invocation/quest objects on the arrival square before migration. Carried light/timer ownership follows the retained object graph. The initial eight tests in `teleport-region-occupants` all failed; four further source/integration checks were added. All twelve pass, including an actual `finishLevelTeleport` transition; the combined arrival, monster catchup and affected quest tests pass 66.

This is a bounded arrival/departure slice, not complete `mon_arrive` or `migrate_to_level` coverage. Other migration destination modes, `restore_cham`, full worm-slot lifecycle, punishment redraw during an engulfed departure, generic special-object floor effects outside the supported arrival terrain, and remaining `rloc_pos_ok` teleport-region restrictions need separate source work. The implemented failure path no longer leaves monsters on an ignored queue or discards their destination metadata.

## Shared lighting and anatomy follow-up

`vision.js` now resolves active floor, hero-carried, and monster-carried object lights on every recalculation. It uses C candle-stack/candelabrum radii, lamp/oil radii, active Sunsword and worn gold-dragon armor blessing radii, the existing circle table, and the shared `clear_path` quadrants. Hero-centered sources use `COULD_SEE`; buried/contained objects do not illuminate the map. Monster, polymorphed hero, and steed sources use their current location. C deliberately gives all luminous monster species radius one; this port keeps that value. Normal blind vision bypasses source lighting as in C, while refresh and Rogue vision still run it.

`test/vision-light-sources.test.mjs` has 31 passing tests, including circle geometry, four-quadrant obstruction, moving owners, extinguishing, candle thresholds, artifact/armor states, hidden sources, polymorph, steed position, and zero RNG consumption. A separate source-derived check preserves the distinction between buried monster light (suppressed by `get_mon_location`) and its carried object light (`get_obj_location` checks only the carrier's map position). The initial 28-test run had 27 failures; two provisional refresh/Rogue expectations were corrected after inspecting the exact source call placement. No historical test or recording was changed. Fuel expiration, automatic artifact activation/deactivation, camera flashes, and generic light-source save/relink behavior remain separate lifecycle gaps.

For the pit-climbing caller, `js/mondata.js` now implements `bodyPart`, `locomotion`, and `heroLocomotion` from `polyself.c:mbodypart` (1972), `mondata.c:locomotion` (1380), and `hack.c:u_locomotion` (1817). Its 53 tests compare all 19 selectors against each of the 12 C anatomy tables and exercise species exceptions and movement-word precedence. The source-wide `trapmove` caller check confirmed that ordinary walking passes a null destination trap (`hack.c` 2831), so it attempts to climb before entering an adjacent pit. This is separate from direct pit-entry effect handling.

The latest combined focused run passed 166/166: quest fillers, lighting, anatomy, command bindings (including one prototype-name regression found during cross-review), gas regions, special levels, and level-generation rolls. This does not replace the root task's full unit/public validation.

## File-by-file findings

| C file and inspected source functions | JS owners and observed behavior | Remaining limits / focused evidence |
| --- | --- | --- |
| `bones.c`: `no_bones_level` 18, `resetobjs` 51, `can_make_bones` 356, `remove_mon_from_bones` 390, `savebones` 403, `getbones` 630 | `mklev.js:getbones`, `save.js:encodeBonesLevel`/`resetBonesObjects`, `cmd.js` death path, `storage.js`. JSON bones are loaded, consumed, relinked, and object knowledge/tameness reset. The loader now applies `no_bones_level` after `rn2(3)` and before file access, using source dungeon/special bone IDs and hellish metadata for bottom, multiway branch, invocation and disabled-level gates. | `bones-eligibility` adds 16 tests (eight failed before implementation); combined bones/save validation passed 37/37. Unique-monster cleanup remains: C `mongone` rescues special inventory via `mdrop_special_objs` and relocates the Oracle, so filtering leaders/nemeses/uniques alone would lose required effects. |
| `dungeon.c`: `init_dungeons` 1205, `deepest_lev_reached` 1339, `ledger_no` 1376, `depth` 1431, `level_difficulty` 2027, `lev_by_name` 2098 | `dungeon.js` constructs the source dungeon graph with chained randomized placement; `mklev.js` owns depth, difficulty, branches, arrivals; `const.js` owns dungeon predicates; `cmd.js` owns level navigation. The difficulty path includes endgame, real Amulet, upward branches, and aggravation. | Initialization uses a translated static `DUNGEON_DATA` rather than executing `dungeon.lua`. Source changes need explicit synchronization. Navigation and map overview are distributed across command code; the mapseen lifecycle was not exhaustively compared. `level-arrival-parity`, `special-levels`, and `levelgen-rolls` are targeted evidence. |
| `extralev.c`: `makeroguerooms` 193, `makerogueghost` 288 and corridor/miniwalk callers | `mklev.js:makeroguerooms`, `roguecorr`, `miniwalk`, `makerogueghost` implement the three-by-three layout and ghost treasure. Room geometry and RNG order were compared with the source body. | Ghost loot directly changes ration/arrow quantities without the corresponding C `weight()` refresh in this path. Whole Rogue generation needs a dedicated source-derived test beyond the existing special-level checks. |
| `light.c`: `new_light_source` 62, `do_light_sources` 169, `save_light_sources` 421, `restore_light_sources` 479, `candle_light_range` 843, `arti_light_radius` 881 | `vision.js` resolves current source positions and object/monster radii with circle/LOS rules; `cmd.js` maintains active-light fields; save graph carries object state. | The missing object illumination path is now implemented with 31 focused tests. Generic registry persistence, camera flashes, automatic artifact activation, and fuel timer lifecycle remain separate concerns. |
| `mklev.c`: `makelevel` 1251, `level_finalize` 1544, `mklev` 1577, `mktrap` 2036, `mkstairs` 2159 | `mklev.js` owns ordinary and special generation, trap and stair helpers, and finalization. The quest dispatcher formerly sent the 18 newly covered fillers through ordinary level generation. | The new group closes those concrete dispatch omissions. Remaining special quest maps, invocation interactions, and all random room themes are not certified by these tests. `quest-fillers`, `special-levels`, `mklev-themerooms`, and `levelgen-rolls` exercise separate paths. |
| `mkmap.c`: `init_fill` 37, cellular passes 68/101/124, `flood_fill_rm` 153, `join_map` 258, `finish_map` 331, `mkmap` 451 | `mklev.js` implements cave initialization, passes, connectivity, lighting, and boundary handling through `splevMinesLevelInit`. The new quest data reuses these routines for 13 cave programs. | Tests check biome presence, connected generator output through live dispatch, and the source `rn2(WIDTH - 1)` initialization prefix. Full cell-by-cell C comparison and every smooth/join/wall combination remain outside this slice. |
| `mkmaze.c`: `create_maze` 950, `populate_maze` 1097, `makemaz` 1127, `movebubbles` 1539, `water_friction` 1689, `save_waterlevel` 1723 | `mklev.js` includes maze generation, arrivals, Air-plane bubble movement, and special builders; `cmd.js`/`allmain.js` call world turn hooks. | Current `movebubbles` explicitly returns unless on the Air plane. C additionally carries heroes, monsters, objects, and traps with Water-plane bubbles; that branch is absent. No JS equivalent of the inspected `water_friction` current-induced direction change was found. These are concrete remaining world behavior, separate from the new fillers. |
| `mkroom.c`: `inside_room` 678, `somexy` 694, callers in room population | `mklev.js` shares ordinary/irregular room placement, subroom avoidance, and room population helpers. New filler room programs reuse `splevBuildRoom` and `splevInsideRoom`. | Tests permit C-defined room placement failure and require complete source population when rooms are successfully placed. Every special room population branch and room serialization was not exhaustively compared. |
| `nhlsel.c`: `l_selection_rndcoord` 407, `l_selection_randline` 591, grow/filter entry points | Selection operations are translated into `mklev.js` sets/coordinate helpers, including existing themed-room selection helpers; new filler data only needs `selection.area` for region lighting. | There is no general Lua selection userdata API. Arbitrary source selection programs cannot be dispatched automatically; their coordinate conversion, grow, and percentage filtering need explicit ports or a broader interpreter. The generator deliberately rejects such constructs. |
| `nhlua.c`: `l_nhcore_init` 140, `l_nhcore_call` 169, `save_luadata` 1328, `restore_luadata` 1345, `load_lua` 2392 | `mklev.js:l_nhcore_init` consumes the alignment shuffle; static JS and generated data implement selected source scripts. | This JS function does not instantiate C's sandbox or run arbitrary `nhcore` callbacks. Generic persistent Lua tables and hook execution remain unavailable. Static direct ports are a valid architecture, but the shuffle alone is not a port of the runtime. |
| `rect.c`: `init_rect` 29, `get_rect` 82, `rnd_rect` 104, `intersect` 116, `split_rects` 182 | `rect.js` implements the allocator, inclusive bounds, random choice, recursive removal, and four residual rectangles. The threshold expressions and split order match the inspected C body. | No concrete discrepancy found in these inspected functions. This is a bounded code comparison, with indirect coverage through ordinary room and quest room generation; there is no dedicated exhaustive allocator test in this slice. |
| `region.c`: `create_region` 79, `run_regions` 414, `in_out_region` 480, `m_in_out_region` 533, `create_msg_region` 955, `create_force_field` 1003 | `region.js` creates gas clouds and selection clouds; `allmain.js:advanceRegions` handles cloud thinning, expiration, damage, and fog-cloud extension; `vision.js` blocks visible regions. | Generic region membership, attached-region movement, can-enter/can-leave callbacks, message regions, and force fields are absent from these owners. `gas-cloud-regions.test.mjs` checks cloud-specific geometry, RNG, thinning and expiration, not the general region system. |
| `restore.c`: `restobj` 183, `restmon` 307, `restgamestate` 525, `getlev` 1046, ID-map routines 1447 onward | `save.js` restores a reference-preserving JSON graph, maps, and level prototypes. Decoding directly into the stable exported game root preserves self-references and Map references to that root; a new regression failed before this fix. `cmd.js` owns returning to saved levels and migration; shared `dog.js` now handles monster catch-up. | Native binary files, PID/UID checks, and C ghostly-ID tables are replaced rather than byte-compatible. Save tests cover object identity, equipment pointers, cyclic graphs, and bones ID allocation. Complete elapsed-time timer/region/light catch-up is not established by serialization alone. |
| `save.c`: `savegamestate` 265, `savelev` 452, `save_stairs` 665, `saveobjchn` 726, `savemonchn` 826 | `save.js:encodeSaveState` traverses game state, preserving repeated references and Maps; `storage.js` persists the JSON. This addresses the pointer-relinking requirement by preserving graph identity. | Functions, display/runtime contexts, and the live RNG object are omitted by design; the graph does not preserve arbitrary JS class prototypes or typed arrays. The native save format and version negotiation are not implemented. See `save-graph-review`, `save-identities`, and `save-bones`. |
| `selvar.c`: `selection_rndcoord` 284, `selection_do_randline` 683 and helper callers | `mklev.js` contains source-specific random-coordinate, flood-fill, and recursive line routines. | This is not a complete selection library. For example, the existing Barbarian random-coordinate helper chooses a dry fallback on an empty selection while C's generic helper returns `(-1,-1)`. Reachable empty-selection callers need separate tests before changing that specialized behavior. |
| `sfbase.c`: `sf_init` 647, `sf_setprocs` 658, pointer-normalization entry points 748 onward | JSON serialization in `save.js` replaces the C historical struct/field procedure tables. | Native ABI normalization and multiple C save-format backends are not relevant to reading JS-owned JSON; cross-format import is unsupported. They should not be counted as unimplemented gameplay routines merely because no matching JS file exists. |
| `sfstruct.c`: buffered I/O helpers 415, `bwrite` 494, `mread` 549 | `storage.js` reads/writes complete virtual files and reports failures; `save.js` handles JSON parsing. | Native partial-read/write, file-descriptor buffering, and panic/recovery behavior have no direct browser equivalent. C save-file recovery and malformed binary handling are not supported by this replacement. |
| `sp_lev.c`: `get_location` 1202, `is_ok_location` 1280, `pm_to_humidity` 1884, `create_monster` 1925, `splev_initlev` 2982, `lspo_level_init` 3837, `lspo_room` 4028, `l_create_stairway` 4147, `lspo_map` 6075, `load_special` 6454 | `mklev.js` supplies direct builders and an ordered operation executor for 18 filler and 28 named programs; generated Lua data is consumed without evaluating source strings. All 65 quest program dispatches have builders. Source geometry and placement rules guided this slice. | The executor supports bounded control flow and selection operations required by the source group, not arbitrary Lua or the full `des` API. Monster inventory callbacks, furniture disguises, saddle placement and aligned map tables are implemented for the source group; remaining cleanup/placement options still require work. Endgame mplayer generation now shares the source-derived constructor. Named-map and filler tests directly check source semantics rather than relying on recordings. |
| `stairs.c`: `stairway_find_from` 64, `stairway_find_dir` 79, `stairway_find_type_dir` 89, `stairway_find_special_dir` 99, hero placement 114 onward | `mklev.js` keeps linked stairways and arrival helpers; `cmd.js` handles traversal and destination-level restoration. | Direction and special-branch lookup match the inspected source logic; complete ladder/from-level equivalence is distributed across command paths and requires broader traversal cases. `level-arrival-parity` is the focused test owner. |
| `teleport.c`: `tele_jump_ok` 386, `teleok` 420, `teleds` 448, relocation callers | `cmd.js` implements hero teleport and level teleport; `mklev.js` owns monster relocation. Root's current slice adds same-level terrain/trap/region-boundary validation and releases holding states. | C `teleok` also invokes generic `in_out_region`; the generic callback system remains missing. `teleds` includes punishment drag rules, vault exit alarms, terrain switching, room entry, and invocation sequencing beyond merely changing coordinates. `teleport-position-parity` and `level-arrival-parity` bound the current checks. |
| `timeout.c`: `nh_timeout` 588, egg/burn entry points, `run_timers` 2222, `start_timer` 2247, `stop_timer` 2299, object move/split routines 2339 onward | `allmain.js:afterMoveTurn`, `egg_timers.js`, `figurine.js`, `ice.js`, and corpse/glob code in `cmd.js` implement separate timer families. | C uses one deadline-ordered queue that permits callbacks to mutate it. JS runs ice, corpses, globs, rot, eggs, and figurines in fixed family order; cross-family equal/different-deadline ordering is not equivalent. Lamp `_burnTimer` fields exist but no general burn timer drain was found. `egg-timers` and timer cases in `shop-billing-helpers` cover individual families. |
| `trap.c`: `maketrap` 456, `dotrap` 2996, `mintrap` 3733, `conjoined_pits` 6552, `clear_conjoined_pits` 6585, `adj_nonconjoined_pit` 6609 | `mklev.js` creates traps; `cmd.js` handles hero effects; `allmain.js` handles monster effects; `dig.js` owns pit links. Root's current slice adds linked-pit movement to the existing reciprocal-link implementation. | Full monster/hero trap effect parity is not inferred from trap constants. C pit checks require both reciprocal bits and actual pit types, plus hero trapped status for entry; the new `conjoined-pits` and `pit-movement-parity` cases target those boundaries. |
| `vision.c`: `does_block` 153, `vision_reset` 211, `vision_recalc` 512, `clear_path` 1613, `view_from` 2002 | `vision.js` ports the shadow-casting/clear-path machinery and blocks terrain, boulders, and visible gas regions. It supports blind and Rogue-level branches and now shares clear-path geometry with active source lighting. | `_blocks` omits C's visible mimic-as-door/boulder blockage and underwater moat blockage. The light follow-up addresses radius/LOS separately. A full source-derived vision suite, including underwater and mimic forms, remains needed. |
| `files.c`: `create_savefile` 1159, `lock_file` 2255, `read_tribute` 3474 | `storage.js` provides a localStorage VFS; `save.js` handles saved state; `dat_files.js` and command reading code supply text assets/tributes. | OS file locks and process recovery are replaced, not translated. Multi-tab updates to one VFS owner are not transactionally locked. Full tribute selection/window behavior is outside this review; existence of embedded text does not establish its callers' parity. |
| `dlb.c`: `lib_dlb_fread` 299, `lib_dlb_fseek` 324, public I/O wrappers | `dat_files.js:datFileText`/`datFileLines` and generated modules replace archive-entry access with static imports. | Archive offsets, seek, and boundary reads are intentionally absent. Asset synchronization and whether each asset has a runtime consumer must be checked separately; the new quest generator checks its own inputs exactly. |
| `nhmd4.c`: `nhmd4_init` 183, `nhmd4_update` 196, `nhmd4_final` 235, caller search | C callers are in `report.c` reporting/hash macros; no JS digest implementation or equivalent report hash was found. | This is a reporting utility gap, not a missing dungeon mechanic. The source digest and report integration would need explicit tests if that reporting feature is ported. |
| `nhlobj.c`: `l_obj_add_to_container` 114, `l_obj_giveobj` 142, timer accessors 496/520/547/579 | `mklev.js` object/container/inventory helpers and timer-family modules perform native JS work. | Generic Lua object userdata, reference ownership, and exposed timer operations are unavailable without the Lua runtime. Direct JS object functions do not by themselves implement that scripting interface. |
| `date.c`: `populate_nomakedefs` 52, `free_nomakedefs` 135 | Build/version strings are supplied by JS application constants/build artifacts rather than parsing C compiler `__DATE__`/`__TIME__`. | This file concerns build metadata, not lunar/night gameplay. Native version sanity fields and dynamic metadata ownership are not translated; a C-file count should not classify that alone as missing game behavior. |
| `calendar.c`: `getnow` 32, timestamp conversion 56/95/120, `phase_of_the_moon` 191, `friday_13th` 206, `night` 215, `midnight` 223 | `allmain.js` startup and `save.js:setRestoreCalendar` implement phase/Friday effects; `were.js:nightNow` and dialogue paths use the supplied game datetime. | The night hour bounds match C and are directly tested in `were.test.mjs`. JS uses the game clock/input rather than calling local wall time on each query. Cross-timezone restore and all midnight consumers need broader tests; source date conversion is not a complete shared JS calendar API. |

## Follow-up priorities

1. Continue main-turn timer phase/prompt continuation and destruction/transfer ownership. Burn lifecycle, artifact-light activation, shared deadline ordering and all nine queue callbacks are implemented; this is inspected callback coverage, not a claim that every timeout.c branch is complete.
2. Extend source-derived quest checks beyond the completed 65 dispatches: generic object/floor interactions during inventory disposal and the remaining migration modes/restrictions listed above remain shared behavior gaps. Occupied fixed/exhaustive teleport-region relocation and approximate limbo returns are now implemented. The `des` executor covers the shipped quest programs; arbitrary Lua and unexercised `des` options remain outside this count.
3. Add Water-plane bubble carrying and water-current movement; test occupants and objects as well as terrain.
4. Add bones unique-monster cleanup with special-object rescue and generic region callback semantics; bones-level eligibility is now implemented.
5. Extend vision tests to visible mimics, underwater obstacles, and moving light sources; extend save tests to elapsed-time world state rather than serialization alone.
